### <a name="wpf-changing-a-primary-key-when-displaying-ado-data-in-a-masterdetail-scenario"></a><span data-ttu-id="e6827-101">Klucz podstawowy przy wyświetlaniu danych ADO w scenariuszu wzorzec/szczegół zmiana WPF</span><span class="sxs-lookup"><span data-stu-id="e6827-101">WPF Changing a primary key when displaying ADO data in a Master/Detail scenario</span></span>

|   |   |
|---|---|
|<span data-ttu-id="e6827-102">Szczegóły</span><span class="sxs-lookup"><span data-stu-id="e6827-102">Details</span></span>|<span data-ttu-id="e6827-103">Załóżmy, że masz kolekcję elementów typu ADO <code>Order</code>, z relacji o nazwie &quot;SzczegółyZamówienia&quot; dotyczą kolekcję elementów typu <code>Detail</code> za pośrednictwem klucza podstawowego &quot;OrderID&quot;.</span><span class="sxs-lookup"><span data-stu-id="e6827-103">Suppose you have an ADO collection of items of type <code>Order</code>, with a relation named &quot;OrderDetails&quot; relating it to a collection of items of type <code>Detail</code> via the primary key &quot;OrderID&quot;.</span></span> <span data-ttu-id="e6827-104">W aplikacji WPF można powiązać formant listy szczegóły dla podanej kolejności:</span><span class="sxs-lookup"><span data-stu-id="e6827-104">In your WPF app, you can bind a list control to the details for a given order:</span></span><pre><code class="lang-xml">&lt;ListBox ItemsSource=&quot;{Binding Path=OrderDetails}&quot; &gt;&#13;&#10;</code></pre><span data-ttu-id="e6827-105">w przypadku DataContext <code>Order</code>.</span><span class="sxs-lookup"><span data-stu-id="e6827-105">where the DataContext is an <code>Order</code>.</span></span> <span data-ttu-id="e6827-106">WPF pobiera wartość <code>OrderDetails</code> właściwość - D Kolekcja wszystkich <code>Detail</code> elementów, których <code>OrderID</code> odpowiada <code>OrderID</code> elementu głównego. Zmiana zachowania powstaje podczas zmiany klucza podstawowego <code>OrderID</code> elementu głównego.</span><span class="sxs-lookup"><span data-stu-id="e6827-106">WPF gets the value of the <code>OrderDetails</code> property - a collection D of all the <code>Detail</code> items whose <code>OrderID</code> matches the <code>OrderID</code> of the master item.The behavior change arises when you change the primary key <code>OrderID</code> of the master item.</span></span> <span data-ttu-id="e6827-107">Automatycznie zmieni ADO <code>OrderID</code> każdego odpowiednie rekordy w kolekcji szczegóły (to znaczy te skopiowane do kolekcji D).</span><span class="sxs-lookup"><span data-stu-id="e6827-107">ADO automatically changes the <code>OrderID</code> of each of the affected records in the Details collection (namely the ones copied into collection D).</span></span>  <span data-ttu-id="e6827-108">Ale co się dzieje z D?</span><span class="sxs-lookup"><span data-stu-id="e6827-108">But what happens to D?</span></span><ul><li><span data-ttu-id="e6827-109">Zachowanie starego: kolekcja D jest wyczyszczone.</span><span class="sxs-lookup"><span data-stu-id="e6827-109">Old behavior:   Collection D is cleared.</span></span>   <span data-ttu-id="e6827-110">Element główny jest <em>nie</em> podnieść powiadomienie o zmianie właściwości <code>OrderDetails</code>.</span><span class="sxs-lookup"><span data-stu-id="e6827-110">The master item does <em>not</em> raise a change notification for property <code>OrderDetails</code>.</span></span>  <span data-ttu-id="e6827-111">Element ListBox w dalszym ciągu używać kolekcji D, który jest obecnie pusta.</span><span class="sxs-lookup"><span data-stu-id="e6827-111">The ListBox continues to use collection D, which is now empty.</span></span></li><li><span data-ttu-id="e6827-112">Nowe zachowanie: kolekcja D jest bez zmian.</span><span class="sxs-lookup"><span data-stu-id="e6827-112">New behavior:  Collection D is unchanged.</span></span>   <span data-ttu-id="e6827-113">Każdy z elementów jego zgłasza powiadomienia o zmianie <code>OrderID</code> właściwości.</span><span class="sxs-lookup"><span data-stu-id="e6827-113">Each of its items raises a change notification for the <code>OrderID</code> property.</span></span>  <span data-ttu-id="e6827-114">Pole listy będzie później nadal używać kolekcji D i wyświetla szczegóły z nowym <code>OrderID</code>.</span><span class="sxs-lookup"><span data-stu-id="e6827-114">The ListBox continues to use collection D, and displays the details with the new <code>OrderID</code>.</span></span></li></ul><span data-ttu-id="e6827-115">WPF implementuje nowe zachowanie przez utworzenie kolekcji D w inny sposób: po wywołaniu metody ADO <xref:System.Data.DataRowView.CreateChildView(System.Data.DataRelation,System.Boolean)?displayProperty=nameWithType> z <code>followParent</code> argument wartość <code>true</code>.</span><span class="sxs-lookup"><span data-stu-id="e6827-115">WPF implements the new behavior by creating collection D in a different way:  by calling the ADO method <xref:System.Data.DataRowView.CreateChildView(System.Data.DataRelation,System.Boolean)?displayProperty=nameWithType> with the <code>followParent</code> argument set to <code>true</code>.</span></span>|
|<span data-ttu-id="e6827-116">Sugestia</span><span class="sxs-lookup"><span data-stu-id="e6827-116">Suggestion</span></span>|<span data-ttu-id="e6827-117">Aplikacja pobiera nowe zachowanie przy użyciu następującego przełącznika AppContext.</span><span class="sxs-lookup"><span data-stu-id="e6827-117">An app gets the new behavior by using the following AppContext switch.</span></span><pre><code class="lang-xml">&lt;configuration&gt;&#13;&#10;&lt;runtime&gt;&#13;&#10;&lt;AppContextSwitchOverrides value=&quot;Switch.System.Windows.Data.DoNotUseFollowParentWhenBindingToADODataRelation=false&quot;/&gt;&#13;&#10;&lt;/runtime&gt;&#13;&#10;&lt;/configuration&gt;&#13;&#10;&#13;&#10;</code></pre><span data-ttu-id="e6827-118">Domyślnie przełącznika <code>true</code> (stare zachowanie) dla aplikacji przeznaczonych dla platformy .NET 4.7.1 lub w dół i <code>false</code> (nowe zachowanie) dla aplikacji przeznaczonych dla platformy .NET 4.7.2 lub nowszej.</span><span class="sxs-lookup"><span data-stu-id="e6827-118">The switch defaults to <code>true</code> (old behavior) for apps that target .NET 4.7.1 or below, and to <code>false</code> (new behavior) for apps that target .NET 4.7.2 or above.</span></span>|
|<span data-ttu-id="e6827-119">Zakres</span><span class="sxs-lookup"><span data-stu-id="e6827-119">Scope</span></span>|<span data-ttu-id="e6827-120">Pomocnicza</span><span class="sxs-lookup"><span data-stu-id="e6827-120">Minor</span></span>|
|<span data-ttu-id="e6827-121">Wersja</span><span class="sxs-lookup"><span data-stu-id="e6827-121">Version</span></span>|<span data-ttu-id="e6827-122">4.7.2</span><span class="sxs-lookup"><span data-stu-id="e6827-122">4.7.2</span></span>|
|<span data-ttu-id="e6827-123">Typ</span><span class="sxs-lookup"><span data-stu-id="e6827-123">Type</span></span>|<span data-ttu-id="e6827-124">Przekierowania</span><span class="sxs-lookup"><span data-stu-id="e6827-124">Retargeting</span></span>|

