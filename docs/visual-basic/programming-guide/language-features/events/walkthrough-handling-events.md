---
title: Obsługa zdarzeń (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: 12267e0a70419298bc581421c4f3a220088d205f
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 08/22/2019
ms.locfileid: "69956311"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="1ed89-102">Przewodnik: Obsługa zdarzeń (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="1ed89-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="1ed89-103">Jest to drugi z dwóch tematów, które pokazują, jak korzystać z zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="1ed89-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="1ed89-104">Pierwszy temat, [Przewodnik: Deklarowanie i wywoływanie zdarzeń](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), pokazuje sposób deklarowania i zgłaszania zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="1ed89-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="1ed89-105">Ta sekcja używa formularza i klasy z tego przewodnika, aby pokazać, jak obsługiwać zdarzenia po ich przejściu.</span><span class="sxs-lookup"><span data-stu-id="1ed89-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="1ed89-106">W `Widget` przykładzie klasy są stosowane tradycyjne instrukcje obsługi zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="1ed89-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="1ed89-107">Visual Basic zapewnia inne techniki pracy ze zdarzeniami.</span><span class="sxs-lookup"><span data-stu-id="1ed89-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="1ed89-108">W ramach ćwiczenia można zmodyfikować ten przykład, aby użyć `AddHandler` instrukcji i. `Handles`</span><span class="sxs-lookup"><span data-stu-id="1ed89-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="1ed89-109">Aby obsłużyć zdarzenie PercentDone klasy widget</span><span class="sxs-lookup"><span data-stu-id="1ed89-109">To handle the PercentDone event of the Widget class</span></span>  
  
1. <span data-ttu-id="1ed89-110">Umieść następujący kod w `Form1`:</span><span class="sxs-lookup"><span data-stu-id="1ed89-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#4)]  
  
     <span data-ttu-id="1ed89-111">Słowo kluczowe Określa, że zmienna `mWidget` jest używana do obsługi zdarzeń obiektu. `WithEvents`</span><span class="sxs-lookup"><span data-stu-id="1ed89-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="1ed89-112">Należy określić rodzaj obiektu, dostarczając nazwę klasy, z której zostanie utworzony obiekt.</span><span class="sxs-lookup"><span data-stu-id="1ed89-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="1ed89-113">Zmienna `mWidget` jest zadeklarowana w `Form1` , `WithEvents` ponieważ zmienne muszą być na poziomie klasy.</span><span class="sxs-lookup"><span data-stu-id="1ed89-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="1ed89-114">Ta wartość jest prawdziwa niezależnie od typu klasy, w której je umieszczasz.</span><span class="sxs-lookup"><span data-stu-id="1ed89-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="1ed89-115">Zmienna `mblnCancel` służy do `LongTask` anulowania metody.</span><span class="sxs-lookup"><span data-stu-id="1ed89-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="1ed89-116">Pisanie kodu do obsługi zdarzenia</span><span class="sxs-lookup"><span data-stu-id="1ed89-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="1ed89-117">Po zadeklarowaniu zmiennej przy użyciu `WithEvents`, nazwa zmiennej jest wyświetlana na liście rozwijanej w **edytorze kodu**klasy.</span><span class="sxs-lookup"><span data-stu-id="1ed89-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="1ed89-118">Po wybraniu `mWidget` `Widget` zdarzenia klasy są wyświetlane na liście rozwijanej po prawej stronie.</span><span class="sxs-lookup"><span data-stu-id="1ed89-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="1ed89-119">Wybranie zdarzenia powoduje wyświetlenie odpowiedniej procedury zdarzenia z prefiksem `mWidget` i podkreśleniem.</span><span class="sxs-lookup"><span data-stu-id="1ed89-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="1ed89-120">Wszystkie procedury zdarzeń skojarzone ze `WithEvents` zmienną otrzymują nazwę zmiennej jako prefiks.</span><span class="sxs-lookup"><span data-stu-id="1ed89-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="1ed89-121">Aby obsłużyć zdarzenie</span><span class="sxs-lookup"><span data-stu-id="1ed89-121">To handle an event</span></span>  
  
1. <span data-ttu-id="1ed89-122">Wybierz `mWidget` z listy rozwijanej po lewej stronie w **edytorze kodu**.</span><span class="sxs-lookup"><span data-stu-id="1ed89-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="1ed89-123">`PercentDone` Wybierz zdarzenie z listy rozwijanej po prawej stronie.</span><span class="sxs-lookup"><span data-stu-id="1ed89-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="1ed89-124">**Edytor kodu** otwiera `mWidget_PercentDone` procedurę zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="1ed89-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="1ed89-125">**Edytor kodu** jest przydatny, ale nie jest wymagany, do wstawiania nowych programów obsługi zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="1ed89-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="1ed89-126">W tym instruktażu bardziej bezpośrednim jest tylko kopiowanie programów obsługi zdarzeń bezpośrednio do kodu.</span><span class="sxs-lookup"><span data-stu-id="1ed89-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3. <span data-ttu-id="1ed89-127">Dodaj następujący kod do `mWidget_PercentDone` programu obsługi zdarzeń:</span><span class="sxs-lookup"><span data-stu-id="1ed89-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#5)]  
  
     <span data-ttu-id="1ed89-128">Za każdym razem, gdy `Label` zdarzeniejestzgłaszane,procedurazdarzeniawyświetlaprocentwykonaniawkontrolce.`PercentDone`</span><span class="sxs-lookup"><span data-stu-id="1ed89-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="1ed89-129">Metoda pozwala na odświeżenie etykiety, a także pozwala użytkownikowi kliknąć przycisk **Anuluj.** `DoEvents`</span><span class="sxs-lookup"><span data-stu-id="1ed89-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4. <span data-ttu-id="1ed89-130">Dodaj następujący kod dla `Button2_Click` programu obsługi zdarzeń:</span><span class="sxs-lookup"><span data-stu-id="1ed89-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#6)]  
  
 <span data-ttu-id="1ed89-131">Jeśli użytkownik kliknie przycisk **Anuluj** w trakcie `LongTask` działania, `Button2_Click` zdarzenie jest wykonywane zaraz po tym `DoEvents` , jak instrukcja zezwala na przetwarzanie zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="1ed89-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="1ed89-132">Zmienna `mblnCancel` na poziomie klasy jest ustawiana na `True`, a `mWidget_PercentDone` następnie zdarzenie sprawdza i ustawia `ByRef Cancel` argument na `True`.</span><span class="sxs-lookup"><span data-stu-id="1ed89-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="1ed89-133">Łączenie zmiennej WithEvents z obiektem</span><span class="sxs-lookup"><span data-stu-id="1ed89-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="1ed89-134">`Form1`jest teraz skonfigurowany do obsługi `Widget` zdarzeń obiektu.</span><span class="sxs-lookup"><span data-stu-id="1ed89-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="1ed89-135">To wszystko, co pozostało `Widget` , aby znaleźć gdzieś.</span><span class="sxs-lookup"><span data-stu-id="1ed89-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="1ed89-136">Po zadeklarowaniu zmiennej `WithEvents` w czasie projektowania, żaden obiekt nie jest skojarzony z nim.</span><span class="sxs-lookup"><span data-stu-id="1ed89-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="1ed89-137">`WithEvents` Zmienna jest tak samo jak jakakolwiek inna zmienna obiektu.</span><span class="sxs-lookup"><span data-stu-id="1ed89-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="1ed89-138">Należy utworzyć obiekt i przypisać do niego odwołanie przy użyciu `WithEvents` zmiennej.</span><span class="sxs-lookup"><span data-stu-id="1ed89-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="1ed89-139">Aby utworzyć obiekt i przypisać do niego odwołanie</span><span class="sxs-lookup"><span data-stu-id="1ed89-139">To create an object and assign a reference to it</span></span>  
  
1. <span data-ttu-id="1ed89-140">Wybierz pozycję **(zdarzenia Form1)** z listy rozwijanej po lewej stronie w **edytorze kodu**.</span><span class="sxs-lookup"><span data-stu-id="1ed89-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="1ed89-141">`Load` Wybierz zdarzenie z listy rozwijanej po prawej stronie.</span><span class="sxs-lookup"><span data-stu-id="1ed89-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="1ed89-142">**Edytor kodu** otwiera `Form1_Load` procedurę zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="1ed89-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3. <span data-ttu-id="1ed89-143">Dodaj następujący kod dla `Form1_Load` procedury zdarzenia, aby `Widget`utworzyć:</span><span class="sxs-lookup"><span data-stu-id="1ed89-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#7)]  
  
 <span data-ttu-id="1ed89-144">Gdy ten kod jest wykonywany, Visual Basic tworzy `Widget` obiekt i łączy jego zdarzenia z procedurami zdarzeń skojarzonymi `mWidget`z.</span><span class="sxs-lookup"><span data-stu-id="1ed89-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="1ed89-145">Od tego momentu, gdy `Widget` `PercentDone` wywołuje zdarzenie, `mWidget_PercentDone` procedura zdarzenia jest wykonywana.</span><span class="sxs-lookup"><span data-stu-id="1ed89-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="1ed89-146">Aby wywołać metodę LongTask</span><span class="sxs-lookup"><span data-stu-id="1ed89-146">To call the LongTask method</span></span>  
  
- <span data-ttu-id="1ed89-147">Dodaj następujący kod do `Button1_Click` programu obsługi zdarzeń:</span><span class="sxs-lookup"><span data-stu-id="1ed89-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#8)]  
  
 <span data-ttu-id="1ed89-148">Przed wywołaniem `Boolean`metody, etykieta, w której jest wyświetlany procent wykonania, musi zostać zainicjowana, a flaga poziomu klasy dla anulowania metody musi być ustawiona na `False`. `LongTask`</span><span class="sxs-lookup"><span data-stu-id="1ed89-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="1ed89-149">`LongTask`jest wywoływana z czasem trwania zadania wynoszącym 12,2 sekund.</span><span class="sxs-lookup"><span data-stu-id="1ed89-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="1ed89-150">`PercentDone` Zdarzenie jest zgłaszane raz w jednej trzeciej części sekundy.</span><span class="sxs-lookup"><span data-stu-id="1ed89-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="1ed89-151">Za `mWidget_PercentDone` każdym razem, gdy zdarzenie jest zgłaszane, wykonywana jest procedura zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="1ed89-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="1ed89-152">Gdy `LongTask` to zrobisz, `mblnCancel` zostanie przetestowany w `LongTask` celu sprawdzenia, czy zakończono normalne działanie `mblnCancel` , czy został `True`zatrzymany, ponieważ został ustawiony na.</span><span class="sxs-lookup"><span data-stu-id="1ed89-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="1ed89-153">Procent ukończenia jest aktualizowany tylko w poprzednim przypadku.</span><span class="sxs-lookup"><span data-stu-id="1ed89-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="1ed89-154">Aby uruchomić program</span><span class="sxs-lookup"><span data-stu-id="1ed89-154">To run the program</span></span>  
  
1. <span data-ttu-id="1ed89-155">Naciśnij klawisz F5, aby umieścić projekt w trybie uruchamiania.</span><span class="sxs-lookup"><span data-stu-id="1ed89-155">Press F5 to put the project in run mode.</span></span>  
  
2. <span data-ttu-id="1ed89-156">Kliknij przycisk **Rozpocznij zadanie** .</span><span class="sxs-lookup"><span data-stu-id="1ed89-156">Click the **Start Task** button.</span></span> <span data-ttu-id="1ed89-157">Za każdym razem `PercentDone` , gdy zdarzenie jest zgłaszane, etykieta zostaje zaktualizowana o wartość procentową wykonanego zadania.</span><span class="sxs-lookup"><span data-stu-id="1ed89-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3. <span data-ttu-id="1ed89-158">Kliknij przycisk **Anuluj** , aby zatrzymać zadanie.</span><span class="sxs-lookup"><span data-stu-id="1ed89-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="1ed89-159">Zauważ, że wygląd przycisku **Anuluj** nie zmienia się natychmiast po jego kliknięciu.</span><span class="sxs-lookup"><span data-stu-id="1ed89-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="1ed89-160">Zdarzenie nie może wystąpić, dopóki `My.Application.DoEvents` instrukcja nie zezwala na przetwarzanie zdarzeń. `Click`</span><span class="sxs-lookup"><span data-stu-id="1ed89-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="1ed89-161">`My.Application.DoEvents` Metoda nie przetwarza zdarzeń w taki sam sposób jak w przypadku formularza.</span><span class="sxs-lookup"><span data-stu-id="1ed89-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="1ed89-162">Na przykład w tym instruktażu należy kliknąć przycisk **Anuluj** dwa razy.</span><span class="sxs-lookup"><span data-stu-id="1ed89-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="1ed89-163">Aby umożliwić bezpośrednie obsługiwanie zdarzeń przez formularz, można użyć wielowątkowości.</span><span class="sxs-lookup"><span data-stu-id="1ed89-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="1ed89-164">Aby uzyskać więcej informacji, zobacz sekcję [zarządzane wątki](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="1ed89-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="1ed89-165">Może się pojawić monit o uruchomienie programu z użyciem klawisza F11 i przekroczenie kodu w wierszu.</span><span class="sxs-lookup"><span data-stu-id="1ed89-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="1ed89-166">Można jasno zobaczyć, jak działa wprowadzanie `LongTask`, a następnie krótko wprowadzić ponownie `Form1` przy `PercentDone` każdym wywołaniu zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="1ed89-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="1ed89-167">Co się stanie, jeśli podczas wykonywania z powrotem w kodzie `Form1` `LongTask` , metoda została wywołana ponownie?</span><span class="sxs-lookup"><span data-stu-id="1ed89-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="1ed89-168">W najgorszym przypadku mogą wystąpić przeciążenia stosu `LongTask` , jeśli zostały wywołane za każdym razem, gdy zdarzenie zostało zgłoszone.</span><span class="sxs-lookup"><span data-stu-id="1ed89-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="1ed89-169">Można spowodować, aby zmienna `mWidget` obsługiwała zdarzenia dla innego `Widget` obiektu przez przypisanie odwołania do nowego `Widget` elementu do `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="1ed89-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="1ed89-170">W rzeczywistości kod można wykonać za `Button1_Click` każdym razem, gdy klikniesz przycisk.</span><span class="sxs-lookup"><span data-stu-id="1ed89-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="1ed89-171">Aby obsłużyć zdarzenia dla innego widżetu</span><span class="sxs-lookup"><span data-stu-id="1ed89-171">To handle events for a different widget</span></span>  
  
- <span data-ttu-id="1ed89-172">Dodaj następujący wiersz kodu do `Button1_Click` procedury bezpośrednio przed wierszem, który odczytuje: `mWidget.LongTask(12.2, 0.33)`</span><span class="sxs-lookup"><span data-stu-id="1ed89-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#9)]  
  
 <span data-ttu-id="1ed89-173">Powyższy kod tworzy nowy `Widget` za każdym razem, gdy przycisk zostanie kliknięty.</span><span class="sxs-lookup"><span data-stu-id="1ed89-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="1ed89-174">Gdy tylko `LongTask` Metoda zostanie zakończona, odwołanie `Widget` do `Widget` zostanie wydane i zostanie zniszczone.</span><span class="sxs-lookup"><span data-stu-id="1ed89-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="1ed89-175">Zmienna może zawierać tylko jedno odwołanie do obiektu, więc Jeśli przypiszesz inny `Widget` obiekt do `mWidget`, zdarzenia poprzedniego `Widget` obiektu nie będą już obsługiwane. `WithEvents`</span><span class="sxs-lookup"><span data-stu-id="1ed89-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="1ed89-176">Jeśli `mWidget` jest jedyną zmienną obiektu zawierającą odwołanie do starego `Widget`, obiekt zostanie zniszczony.</span><span class="sxs-lookup"><span data-stu-id="1ed89-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="1ed89-177">Jeśli chcesz obsługiwać zdarzenia z kilku `Widget` obiektów, `AddHandler` Użyj instrukcji, aby przetwarzać zdarzenia z poszczególnych obiektów osobno.</span><span class="sxs-lookup"><span data-stu-id="1ed89-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="1ed89-178">W razie potrzeby można zadeklarować dowolną liczbę `WithEvents` zmiennych, ale `WithEvents` tablice zmiennych nie są obsługiwane.</span><span class="sxs-lookup"><span data-stu-id="1ed89-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="1ed89-179">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="1ed89-179">See also</span></span>

- [<span data-ttu-id="1ed89-180">Przewodnik: Deklarowanie i wywoływanie zdarzeń</span><span class="sxs-lookup"><span data-stu-id="1ed89-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)
- [<span data-ttu-id="1ed89-181">Zdarzenia</span><span class="sxs-lookup"><span data-stu-id="1ed89-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
