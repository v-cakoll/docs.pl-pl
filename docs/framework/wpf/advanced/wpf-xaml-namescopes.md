---
title: Zakresy nazw WPF XAML
ms.date: 03/30/2017
helpviewer_keywords:
- namescopes [WPF]
- styles [WPF], namescopes in
- templates [WPF], namescopes in
- APIs [WPF], name-related
- name-related APIs
- XAML [WPF], namescopes
- classes [WPF], FrameworkContentElement
ms.assetid: 52bbf4f2-15fc-40d4-837b-bb4c21ead7d4
ms.openlocfilehash: edf5c8a828bea182cd87542276fb7eb2df1908be
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 08/22/2019
ms.locfileid: "69917337"
---
# <a name="wpf-xaml-namescopes"></a><span data-ttu-id="bf738-102">Zakresy nazw WPF XAML</span><span class="sxs-lookup"><span data-stu-id="bf738-102">WPF XAML Namescopes</span></span>
<span data-ttu-id="bf738-103">XAML Zakresy nazw WPF to koncepcja, która identyfikuje obiekty zdefiniowane w języku XAML.</span><span class="sxs-lookup"><span data-stu-id="bf738-103">XAML namescopes are a concept that identifies objects that are defined in XAML.</span></span> <span data-ttu-id="bf738-104">Nazwy w namescope języka XAML mogą służyć do ustanawiania relacji między nazwami obiektów zdefiniowanymi w języku XAML a ich odpowiednikami wystąpień w drzewie obiektów.</span><span class="sxs-lookup"><span data-stu-id="bf738-104">The names in a XAML namescope can be used to establish relationships between the XAML-defined names of objects and their instance equivalents in an object tree.</span></span> <span data-ttu-id="bf738-105">Zazwyczaj Zakresy nazw WPF języka XAML w [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] kodzie zarządzanym są tworzone podczas ładowania poszczególnych katalogów głównych stron XAML dla aplikacji XAML.</span><span class="sxs-lookup"><span data-stu-id="bf738-105">Typically, XAML namescopes in [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] managed code are created when loading the individual XAML page roots for a XAML application.</span></span> <span data-ttu-id="bf738-106">Kod XAML Zakresy nazw WPF jako obiekt programistyczny jest definiowany przez <xref:System.Windows.Markup.INameScope> interfejs i jest również zaimplementowany przez klasę <xref:System.Windows.NameScope>praktyczną.</span><span class="sxs-lookup"><span data-stu-id="bf738-106">XAML namescopes as the programming object are defined by the <xref:System.Windows.Markup.INameScope> interface and are also implemented by the practical class <xref:System.Windows.NameScope>.</span></span>  

<a name="Namescopes_in_Loaded_XAML_Applications"></a>   
## <a name="namescopes-in-loaded-xaml-applications"></a><span data-ttu-id="bf738-107">Zakresy nazw WPF w załadowanych aplikacjach XAML</span><span class="sxs-lookup"><span data-stu-id="bf738-107">Namescopes in Loaded XAML Applications</span></span>  
 <span data-ttu-id="bf738-108">W szerszym kontekście programowania lub nauki komputerowym koncepcje programowania często obejmują zasadę unikatowego identyfikatora lub nazwy, która może być używana do uzyskiwania dostępu do obiektu.</span><span class="sxs-lookup"><span data-stu-id="bf738-108">In a broader programming or computer science context, programming concepts often include the principle of a unique identifier or name that can be used to access an object.</span></span> <span data-ttu-id="bf738-109">W przypadku systemów, które używają identyfikatorów lub nazw, namescope definiuje granice, w których proces lub technika będą przeszukiwane, jeśli zażądano obiektu o tej nazwie lub że są wymuszane ograniczenia unikatowości tożsamości nazw.</span><span class="sxs-lookup"><span data-stu-id="bf738-109">For systems that use identifiers or names, the namescope defines the boundaries within which a process or technique will search if an object of that name is requested, or the boundaries wherein uniqueness of identifying names is enforced.</span></span> <span data-ttu-id="bf738-110">Te ogólne zasady są prawdziwe w przypadku języka XAML Zakresy nazw WPF.</span><span class="sxs-lookup"><span data-stu-id="bf738-110">These general principles are true for XAML namescopes.</span></span> <span data-ttu-id="bf738-111">W programie WPF Zakresy nazw WPF języka XAML są tworzone na elemencie głównym strony XAML podczas ładowania strony.</span><span class="sxs-lookup"><span data-stu-id="bf738-111">In WPF, XAML namescopes are created on the root element for a XAML page when the page is loaded.</span></span> <span data-ttu-id="bf738-112">Każda nazwa określona na stronie XAML rozpoczynająca się od elementu głównego strony jest dodawana do odpowiednich NameScope języka XAML.</span><span class="sxs-lookup"><span data-stu-id="bf738-112">Each name specified within the XAML page starting at the page root is added to a pertinent XAML namescope.</span></span>  
  
 <span data-ttu-id="bf738-113">W języku XAML WPF elementy, które są wspólnymi elementami głównymi <xref:System.Windows.Controls.Page>(takie <xref:System.Windows.Window>jak i), zawsze kontrolują namescope języka XAML.</span><span class="sxs-lookup"><span data-stu-id="bf738-113">In WPF XAML, elements that are common root elements (such as <xref:System.Windows.Controls.Page>, and <xref:System.Windows.Window>) always control a XAML namescope.</span></span> <span data-ttu-id="bf738-114">Jeśli element taki jak <xref:System.Windows.FrameworkElement> lub <xref:System.Windows.FrameworkContentElement> jest elementem głównym [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] strony w znaczniku <xref:System.Windows.Controls.Page> , <xref:System.Windows.Controls.Page> procesor dodaje element główny niejawnie, aby można było udostępnić działające namescope języka XAML.</span><span class="sxs-lookup"><span data-stu-id="bf738-114">If an element such as <xref:System.Windows.FrameworkElement> or <xref:System.Windows.FrameworkContentElement> is the root element of the page in markup, a [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] processor adds a <xref:System.Windows.Controls.Page> root implicitly so that the <xref:System.Windows.Controls.Page> can provide a working XAML namescope.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="bf738-115">Akcje kompilacji WPF tworzą namescope języka XAML dla produkcji XAML, nawet jeśli żadne `Name` `x:Name` atrybuty nie są zdefiniowane [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] we wszystkich elementach znacznika.</span><span class="sxs-lookup"><span data-stu-id="bf738-115">WPF build actions create a XAML namescope for a XAML production even if no `Name` or `x:Name` attributes are defined on any elements in the [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] markup.</span></span>  
  
 <span data-ttu-id="bf738-116">Jeśli spróbujesz użyć tej samej nazwy dwa razy w dowolnym namescope języka XAML, zostanie zgłoszony wyjątek.</span><span class="sxs-lookup"><span data-stu-id="bf738-116">If you try to use the same name twice in any XAML namescope, an exception is raised.</span></span> <span data-ttu-id="bf738-117">W przypadku języka XAML WPF, który zawiera kod i jest częścią skompilowanej aplikacji, wyjątek jest wywoływany w czasie kompilacji przez akcje kompilacji WPF, podczas tworzenia wygenerowanej klasy dla strony podczas początkowej kompilacji znaczników.</span><span class="sxs-lookup"><span data-stu-id="bf738-117">For WPF XAML that has code-behind and is part of a compiled application, the exception is raised at build time by WPF build actions, when creating the generated class for the page during the initial markup compile.</span></span> <span data-ttu-id="bf738-118">W przypadku języka XAML, który nie jest kompilowany do znaczników przez żadną akcję kompilacji, wyjątki związane z problemami z namescope XAML mogą zostać zgłoszone podczas ładowania XAML.</span><span class="sxs-lookup"><span data-stu-id="bf738-118">For XAML that is not markup-compiled by any build action, exceptions related to XAML namescope issues might be raised when the XAML is loaded.</span></span> <span data-ttu-id="bf738-119">Projektanci XAML mogą również przewidzieć problemy namescope języka XAML w czasie projektowania.</span><span class="sxs-lookup"><span data-stu-id="bf738-119">XAML designers might also anticipate XAML namescope issues at design time.</span></span>  
  
### <a name="adding-objects-to-runtime-object-trees"></a><span data-ttu-id="bf738-120">Dodawanie obiektów do drzew obiektów środowiska uruchomieniowego</span><span class="sxs-lookup"><span data-stu-id="bf738-120">Adding Objects to Runtime Object Trees</span></span>  
 <span data-ttu-id="bf738-121">Moment, w którym kod XAML jest analizowany, wskazuje moment, w którym jest tworzony i definiowany namescope języka XAML WPF.</span><span class="sxs-lookup"><span data-stu-id="bf738-121">The moment that XAML is parsed represents the moment in time that a WPF XAML namescope is created and defined.</span></span> <span data-ttu-id="bf738-122">W przypadku dodania obiektu do drzewa obiektów w punkcie w czasie po przeanalizowaniu kodu XAML, który wygenerował to drzewo, `Name` lub `x:Name` wartość w nowym obiekcie nie aktualizuje automatycznie informacji w języku XAML namescope.</span><span class="sxs-lookup"><span data-stu-id="bf738-122">If you add an object to an object tree at a point in time after the XAML that produced that tree was parsed, a `Name` or `x:Name` value on the new object does not automatically update the information in a XAML namescope.</span></span> <span data-ttu-id="bf738-123">Aby dodać nazwę dla obiektu do elementu WPF XAML namescope po załadowaniu XAML, należy wywołać odpowiednią implementację <xref:System.Windows.Markup.INameScope.RegisterName%2A> dla obiektu, który definiuje namescope XAML, który jest zazwyczaj głównym katalogiem strony XAML.</span><span class="sxs-lookup"><span data-stu-id="bf738-123">To add a name for an object into a WPF XAML namescope after XAML is loaded, you must call the appropriate implementation of <xref:System.Windows.Markup.INameScope.RegisterName%2A> on the object that defines the XAML namescope, which is typically the XAML page root.</span></span> <span data-ttu-id="bf738-124">Jeśli nazwa nie jest zarejestrowana, do dodanego obiektu nie można odwoływać się za pomocą <xref:System.Windows.FrameworkElement.FindName%2A>metod, takich jak, i nie można użyć tej nazwy dla celów animacji.</span><span class="sxs-lookup"><span data-stu-id="bf738-124">If the name is not registered, the added object cannot be referenced by name through methods such as <xref:System.Windows.FrameworkElement.FindName%2A>, and you cannot use that name for animation targeting.</span></span>  
  
 <span data-ttu-id="bf738-125">Najbardziej typowym scenariuszem dla deweloperów aplikacji jest użycie <xref:System.Windows.FrameworkElement.RegisterName%2A> do rejestracji nazw w języku XAML namescope w bieżącym katalogu głównym strony.</span><span class="sxs-lookup"><span data-stu-id="bf738-125">The most common scenario for application developers is that you will use <xref:System.Windows.FrameworkElement.RegisterName%2A> to register names into the XAML namescope on the current root of the page.</span></span> <span data-ttu-id="bf738-126"><xref:System.Windows.FrameworkElement.RegisterName%2A>jest częścią ważnego scenariusza dla scenorysów, które są obiektami docelowymi dla animacji.</span><span class="sxs-lookup"><span data-stu-id="bf738-126"><xref:System.Windows.FrameworkElement.RegisterName%2A> is part of an important scenario for storyboards that target objects for animations.</span></span> <span data-ttu-id="bf738-127">Aby uzyskać więcej informacji, zobacz [Omówienie scenorysów](../graphics-multimedia/storyboards-overview.md).</span><span class="sxs-lookup"><span data-stu-id="bf738-127">For more information, see [Storyboards Overview](../graphics-multimedia/storyboards-overview.md).</span></span>  
  
 <span data-ttu-id="bf738-128">Jeśli wywołasz <xref:System.Windows.FrameworkElement.RegisterName%2A> obiekt inny niż obiekt definiujący namescope języka XAML, nazwa jest wciąż zarejestrowana w kodzie XAML namescope, w którym znajduje się obiekt wywołujący w, tak jakby był wywoływany <xref:System.Windows.FrameworkElement.RegisterName%2A> w obiekcie języka XAML namescope.</span><span class="sxs-lookup"><span data-stu-id="bf738-128">If you call <xref:System.Windows.FrameworkElement.RegisterName%2A> on an object other than the object that defines the XAML namescope, the name is still registered to the XAML namescope that the calling object is held within, as if you had called <xref:System.Windows.FrameworkElement.RegisterName%2A> on the XAML namescope defining object.</span></span>  
  
### <a name="xaml-namescopes-in-code"></a><span data-ttu-id="bf738-129">Kod XAML Zakresy nazw WPF w kodzie</span><span class="sxs-lookup"><span data-stu-id="bf738-129">XAML Namescopes in Code</span></span>  
 <span data-ttu-id="bf738-130">Możesz tworzyć i używać Zakresy nazw WPF języka XAML w kodzie.</span><span class="sxs-lookup"><span data-stu-id="bf738-130">You can create and then use XAML namescopes in code.</span></span> <span data-ttu-id="bf738-131">Interfejsy API i koncepcje związane z tworzeniem namescope języka XAML są takie same, nawet w przypadku czystego użycia kodu, ponieważ procesor [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] XAML używa tych interfejsów API i koncepcji, gdy przetwarzają one kod XAML.</span><span class="sxs-lookup"><span data-stu-id="bf738-131">The APIs and the concepts involved in XAML namescope creation are the same even for a pure code usage, because the XAML processor for [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] uses these APIs and concepts when it processes XAML itself.</span></span> <span data-ttu-id="bf738-132">Pojęcia i interfejs API są przeznaczone głównie do znajdowania obiektów według nazwy w drzewie obiektów, które są zwykle zdefiniowane częściowo lub w całości w języku XAML.</span><span class="sxs-lookup"><span data-stu-id="bf738-132">The concepts and API exist mainly for the purpose of being able to find objects by name within an object tree that is typically defined partially or entirely in XAML.</span></span>  
  
 <span data-ttu-id="bf738-133">W przypadku aplikacji, które są tworzone programowo, a nie z załadowanego kodu XAML, obiekt definiujący namescope <xref:System.Windows.Markup.INameScope>XAML musi implementować <xref:System.Windows.FrameworkElement> lub <xref:System.Windows.FrameworkContentElement> być klasą pochodną, aby obsługiwała tworzenie namescope XAML w jego Liczba.</span><span class="sxs-lookup"><span data-stu-id="bf738-133">For applications that are created programmatically, and not from loaded XAML, the object that defines a XAML namescope must implement <xref:System.Windows.Markup.INameScope>, or be a <xref:System.Windows.FrameworkElement> or <xref:System.Windows.FrameworkContentElement> derived class, in order to support creation of a XAML namescope on its instances.</span></span>  
  
 <span data-ttu-id="bf738-134">Ponadto dla każdego elementu, który nie jest ładowany i przetwarzany przez procesor XAML, namescope XAML dla obiektu nie jest tworzony ani inicjowany domyślnie.</span><span class="sxs-lookup"><span data-stu-id="bf738-134">Also, for any element that is not loaded and processed by a XAML processor, the XAML namescope for the object is not created or initialized by default.</span></span> <span data-ttu-id="bf738-135">Musisz jawnie utworzyć nowy kod XAML namescope dla każdego obiektu, który zamierza zarejestrować nazwy w późniejszym czasie.</span><span class="sxs-lookup"><span data-stu-id="bf738-135">You must explicitly create a new XAML namescope for any object that you intend to register names into subsequently.</span></span> <span data-ttu-id="bf738-136">Aby utworzyć namescope XAML, należy wywołać metodę statyczną <xref:System.Windows.NameScope.SetNameScope%2A> .</span><span class="sxs-lookup"><span data-stu-id="bf738-136">To create a XAML namescope, you call the static <xref:System.Windows.NameScope.SetNameScope%2A> method.</span></span> <span data-ttu-id="bf738-137">Określ obiekt, który będzie jego właocicielem jako `dependencyObject` parametr, oraz nowe <xref:System.Windows.NameScope.%23ctor%2A> wywołanie konstruktora jako `value` parametr.</span><span class="sxs-lookup"><span data-stu-id="bf738-137">Specify the object that will own it as the `dependencyObject` parameter, and a new <xref:System.Windows.NameScope.%23ctor%2A> constructor call as the `value` parameter.</span></span>  
  
 <span data-ttu-id="bf738-138">Jeśli obiekt dostarczony jako `dependencyObject` dla <xref:System.Windows.NameScope.SetNameScope%2A> nie jest <xref:System.Windows.Markup.INameScope> implementacją, <xref:System.Windows.FrameworkElement> lub <xref:System.Windows.FrameworkContentElement>wywołanie <xref:System.Windows.FrameworkElement.RegisterName%2A> dowolnego elementu podrzędnego nie będzie miało żadnego efektu.</span><span class="sxs-lookup"><span data-stu-id="bf738-138">If the object provided as `dependencyObject` for <xref:System.Windows.NameScope.SetNameScope%2A> is not a <xref:System.Windows.Markup.INameScope> implementation, <xref:System.Windows.FrameworkElement> or <xref:System.Windows.FrameworkContentElement>, calling <xref:System.Windows.FrameworkElement.RegisterName%2A> on any child elements will have no effect.</span></span> <span data-ttu-id="bf738-139">Jeśli nie można jawnie utworzyć nowego języka XAML namescope, wywołanie <xref:System.Windows.FrameworkElement.RegisterName%2A> zostanie zgłoszone jako wyjątek.</span><span class="sxs-lookup"><span data-stu-id="bf738-139">If you fail to create the new XAML namescope explicitly, then calls to <xref:System.Windows.FrameworkElement.RegisterName%2A> will raise an exception.</span></span>  
  
 <span data-ttu-id="bf738-140">Przykład używania interfejsów API namescope języka XAML w kodzie można znaleźć w temacie [Definiowanie zakresu nazw](../graphics-multimedia/how-to-define-a-name-scope.md).</span><span class="sxs-lookup"><span data-stu-id="bf738-140">For an example of using XAML namescope APIs in code, see [Define a Name Scope](../graphics-multimedia/how-to-define-a-name-scope.md).</span></span>  
  
<a name="Namescopes_in_Styles_and_Templates"></a>   
## <a name="xaml-namescopes-in-styles-and-templates"></a><span data-ttu-id="bf738-141">Zakresy nazw WPF języka XAML w stylach i szablonach</span><span class="sxs-lookup"><span data-stu-id="bf738-141">XAML Namescopes in Styles and Templates</span></span>  
 <span data-ttu-id="bf738-142">Style i szablony w [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] programie zapewniają możliwość wielokrotnego użycia i ponownego zastosowania zawartości w prosty sposób.</span><span class="sxs-lookup"><span data-stu-id="bf738-142">Styles and templates in [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] provide the ability to reuse and reapply content in a straightforward way.</span></span> <span data-ttu-id="bf738-143">Jednak style i szablony mogą również zawierać elementy z nazwami XAML zdefiniowanymi na poziomie szablonu.</span><span class="sxs-lookup"><span data-stu-id="bf738-143">However, styles and templates might also include elements with XAML names defined at the template level.</span></span> <span data-ttu-id="bf738-144">Ten sam szablon może być używany wiele razy na stronie.</span><span class="sxs-lookup"><span data-stu-id="bf738-144">That same template might be used multiple times in a page.</span></span> <span data-ttu-id="bf738-145">Z tego powodu style i szablony definiują własne Zakresy nazw WPF języka XAML niezależnie od lokalizacji w drzewie obiektów, w której zastosowano styl lub szablon.</span><span class="sxs-lookup"><span data-stu-id="bf738-145">For this reason, styles and templates both define their own XAML namescopes, independent of whatever location in an object tree where the style or template is applied.</span></span>  
  
 <span data-ttu-id="bf738-146">Rozważmy następujący przykład:</span><span class="sxs-lookup"><span data-stu-id="bf738-146">Consider the following example:</span></span>  
  
 [!code-xaml[XamlOvwSupport#NameScopeTemplates](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page6.xaml#namescopetemplates)]  
  
 <span data-ttu-id="bf738-147">Ten sam szablon jest stosowany do dwóch różnych przycisków.</span><span class="sxs-lookup"><span data-stu-id="bf738-147">Here, the same template is applied to two different buttons.</span></span> <span data-ttu-id="bf738-148">Jeśli szablony nie zawierały dyskretnych Zakresy nazw wpfów XAML `TheBorder` , Nazwa użyta w szablonie spowodowałaby kolizję nazw w namescope XAML.</span><span class="sxs-lookup"><span data-stu-id="bf738-148">If templates did not have discrete XAML namescopes, the `TheBorder` name used in the template would cause a name collision in the XAML namescope.</span></span> <span data-ttu-id="bf738-149">Każde wystąpienie szablonu ma własne namescope języka XAML, więc w tym przykładzie każdy kod XAML namescope szablonu, który będzie zawierał dokładnie jedną nazwę.</span><span class="sxs-lookup"><span data-stu-id="bf738-149">Each instantiation of the template has its own XAML namescope, so in this example each instantiated template's XAML namescope would contain exactly one name.</span></span>  
  
 <span data-ttu-id="bf738-150">Style również definiują własne namescope kodu XAML, aby części scenorysów mogły mieć przypisane określone nazwy.</span><span class="sxs-lookup"><span data-stu-id="bf738-150">Styles also define their own XAML namescope, mostly so that parts of storyboards can have particular names assigned.</span></span> <span data-ttu-id="bf738-151">Te nazwy umożliwiają kontrolę określonych zachowań, które będą elementami docelowymi tej nazwy, nawet jeśli szablon został ponownie zdefiniowany w ramach dostosowywania kontroli.</span><span class="sxs-lookup"><span data-stu-id="bf738-151">These names enable control specific behaviors that will target elements of that name, even if the template was re-defined as part of control customization.</span></span>  
  
 <span data-ttu-id="bf738-152">Ze względu na oddzielne zakresy nazw WPF języka XAML wyszukiwanie nazwanych elementów w szablonie jest trudniejsze niż znalezienie elementu o nazwie nieopartego na szablonie na stronie.</span><span class="sxs-lookup"><span data-stu-id="bf738-152">Because of the separate XAML namescopes, finding named elements in a template is more challenging than finding a non-templated named element in a page.</span></span> <span data-ttu-id="bf738-153">Najpierw należy określić zastosowany szablon, pobierając wartość <xref:System.Windows.Controls.Control.Template%2A> właściwości kontrolki, w której jest stosowany szablon.</span><span class="sxs-lookup"><span data-stu-id="bf738-153">You first need to determine the applied template, by getting the <xref:System.Windows.Controls.Control.Template%2A> property value of the control where the template is applied.</span></span> <span data-ttu-id="bf738-154">Następnie należy wywołać wersję <xref:System.Windows.FrameworkTemplate.FindName%2A>szablonu, przekazując kontrolkę, w której szablon został zastosowany jako drugi parametr.</span><span class="sxs-lookup"><span data-stu-id="bf738-154">Then, you call the template version of <xref:System.Windows.FrameworkTemplate.FindName%2A>, passing the control where the template was applied as the second parameter.</span></span>  
  
 <span data-ttu-id="bf738-155">Jeśli jesteś autorem kontrolki i tworzysz Konwencję, w której konkretny nazwany element w zastosowanym szablonie jest obiektem docelowym dla zachowania zdefiniowanego przez sam formant, możesz użyć <xref:System.Windows.FrameworkElement.GetTemplateChild%2A> metody z kodu implementacji formantu.</span><span class="sxs-lookup"><span data-stu-id="bf738-155">If you are a control author and you are generating a convention where a particular named element in an applied template is the target for a behavior that is defined by the control itself, you can use the <xref:System.Windows.FrameworkElement.GetTemplateChild%2A> method from your control implementation code.</span></span> <span data-ttu-id="bf738-156"><xref:System.Windows.FrameworkElement.GetTemplateChild%2A> Metoda jest chroniona, dlatego tylko autor formantu ma do niego dostęp.</span><span class="sxs-lookup"><span data-stu-id="bf738-156">The <xref:System.Windows.FrameworkElement.GetTemplateChild%2A> method is protected, so only the control author has access to it.</span></span>  
  
 <span data-ttu-id="bf738-157">Jeśli pracujesz z poziomu szablonu i chcesz uzyskać dostęp do NameScope języka XAML, w którym szablon jest zastosowany, Pobierz wartość <xref:System.Windows.FrameworkElement.TemplatedParent%2A>, a następnie Wywołaj <xref:System.Windows.FrameworkElement.FindName%2A> ją.</span><span class="sxs-lookup"><span data-stu-id="bf738-157">If you are working from within a template, and need to get to the XAML namescope where the template is applied, get the value of <xref:System.Windows.FrameworkElement.TemplatedParent%2A>, and then call <xref:System.Windows.FrameworkElement.FindName%2A> there.</span></span> <span data-ttu-id="bf738-158">Przykładem działania w ramach szablonu jest zapisanie implementacji programu obsługi zdarzeń, w której zdarzenie zostanie podniesione z elementu w zastosowaniu szablonu.</span><span class="sxs-lookup"><span data-stu-id="bf738-158">An example of working within the template would be if you are writing the event handler implementation where the event will be raised from an element in an applied template.</span></span>  
  
<a name="Namescopes_and_Name_related_APIs"></a>   
## <a name="xaml-namescopes-and-name-related-apis"></a><span data-ttu-id="bf738-159">Zakresy nazw WPF języka XAML i interfejsy API związane z nazwami</span><span class="sxs-lookup"><span data-stu-id="bf738-159">XAML Namescopes and Name-related APIs</span></span>  
 <span data-ttu-id="bf738-160"><xref:System.Windows.FrameworkElement>ma <xref:System.Windows.FrameworkElement.FindName%2A>metodyi. <xref:System.Windows.FrameworkElement.RegisterName%2A> <xref:System.Windows.FrameworkElement.UnregisterName%2A></span><span class="sxs-lookup"><span data-stu-id="bf738-160"><xref:System.Windows.FrameworkElement> has <xref:System.Windows.FrameworkElement.FindName%2A>, <xref:System.Windows.FrameworkElement.RegisterName%2A> and <xref:System.Windows.FrameworkElement.UnregisterName%2A> methods.</span></span> <span data-ttu-id="bf738-161">Jeśli obiekt wywołuje te metody na własność namescope języka XAML, metody wywołują metody odpowiednich namescope XAML.</span><span class="sxs-lookup"><span data-stu-id="bf738-161">If the object you call these methods on owns a XAML namescope, the methods call into the methods of the relevant XAML namescope.</span></span> <span data-ttu-id="bf738-162">W przeciwnym razie element nadrzędny jest sprawdzany, aby zobaczyć, czy jest właścicielem namescope języka XAML, i ten proces jest kontynuowany cyklicznie do momentu znalezienia namescope XAML (z powodu zachowania procesora XAML, zagwarantowany jest namescope XAML w katalogu głównym).</span><span class="sxs-lookup"><span data-stu-id="bf738-162">Otherwise, the parent element is checked to see if it owns a XAML namescope, and this process continues recursively until a XAML namescope is found (because of the XAML processor behavior, there is guaranteed to be a XAML namescope at the root).</span></span> <span data-ttu-id="bf738-163"><xref:System.Windows.FrameworkContentElement>ma analogiczne zachowania, z wyjątkiem tego, <xref:System.Windows.FrameworkContentElement> że nie będzie nigdy własnością namescope języka XAML.</span><span class="sxs-lookup"><span data-stu-id="bf738-163"><xref:System.Windows.FrameworkContentElement> has analogous behaviors, with the exception that no <xref:System.Windows.FrameworkContentElement> will ever own a XAML namescope.</span></span> <span data-ttu-id="bf738-164">Metody istnieją <xref:System.Windows.FrameworkContentElement> tak, aby wywołania mogły być przekazywane ostatecznie <xref:System.Windows.FrameworkElement> do elementu nadrzędnego.</span><span class="sxs-lookup"><span data-stu-id="bf738-164">The methods exist on <xref:System.Windows.FrameworkContentElement> so that the calls can be forwarded eventually to a <xref:System.Windows.FrameworkElement> parent element.</span></span>  
  
 <span data-ttu-id="bf738-165"><xref:System.Windows.NameScope.SetNameScope%2A>służy do mapowania nowego namescope języka XAML na istniejący obiekt.</span><span class="sxs-lookup"><span data-stu-id="bf738-165"><xref:System.Windows.NameScope.SetNameScope%2A> is used to map a new XAML namescope to an existing object.</span></span> <span data-ttu-id="bf738-166">Możesz wywołać <xref:System.Windows.NameScope.SetNameScope%2A> więcej niż jeden raz, aby zresetować lub wyczyścić namescope XAML, ale nie jest to typowe użycie.</span><span class="sxs-lookup"><span data-stu-id="bf738-166">You can call <xref:System.Windows.NameScope.SetNameScope%2A> more than once in order to reset or clear the XAML namescope, but that is not a common usage.</span></span> <span data-ttu-id="bf738-167">Ponadto nie <xref:System.Windows.NameScope.GetNameScope%2A> jest zazwyczaj używany w kodzie.</span><span class="sxs-lookup"><span data-stu-id="bf738-167">Also, <xref:System.Windows.NameScope.GetNameScope%2A> is not typically used from code.</span></span>  
  
### <a name="xaml-namescope-implementations"></a><span data-ttu-id="bf738-168">Implementacje języka XAML namescope</span><span class="sxs-lookup"><span data-stu-id="bf738-168">XAML Namescope Implementations</span></span>  
 <span data-ttu-id="bf738-169">Następujące klasy implementują <xref:System.Windows.Markup.INameScope> się bezpośrednio:</span><span class="sxs-lookup"><span data-stu-id="bf738-169">The following classes implement <xref:System.Windows.Markup.INameScope> directly:</span></span>  
  
- <xref:System.Windows.NameScope>  
  
- <xref:System.Windows.Style>  
  
- <xref:System.Windows.ResourceDictionary>  
  
- <xref:System.Windows.FrameworkTemplate>  
  
 <span data-ttu-id="bf738-170"><xref:System.Windows.ResourceDictionary>nie używa nazw XAML ani Zakresy nazw WPF; Zamiast tego używa kluczy, ponieważ jest implementacją słownika.</span><span class="sxs-lookup"><span data-stu-id="bf738-170"><xref:System.Windows.ResourceDictionary> does not use XAML names or namescopes ; it uses keys instead, because it is a dictionary implementation.</span></span> <span data-ttu-id="bf738-171">Jedyną przyczyną, która <xref:System.Windows.ResourceDictionary> implementuje <xref:System.Windows.Markup.INameScope> , jest to, że może ona zgłaszać wyjątki do kodu użytkownika, który ułatwia wyjaśnienie rozróżnienia między prawdziwym <xref:System.Windows.ResourceDictionary> namescope języka XAML a sposobem obsługi kluczy, a także gwarantuje, że zakresy nazw WPF XAML nie są stosowane do <xref:System.Windows.ResourceDictionary> według elementów nadrzędnych.</span><span class="sxs-lookup"><span data-stu-id="bf738-171">The only reason that <xref:System.Windows.ResourceDictionary> implements <xref:System.Windows.Markup.INameScope> is so it can raise exceptions to user code that help clarify the distinction between a true XAML namescope and how a <xref:System.Windows.ResourceDictionary> handles keys, and also to assure that XAML namescopes are not applied to a <xref:System.Windows.ResourceDictionary> by parent elements.</span></span>  
  
 <span data-ttu-id="bf738-172"><xref:System.Windows.FrameworkTemplate>i <xref:System.Windows.Style> implementowanie <xref:System.Windows.Markup.INameScope> za poorednictwem jawnych definicji interfejsu.</span><span class="sxs-lookup"><span data-stu-id="bf738-172"><xref:System.Windows.FrameworkTemplate> and <xref:System.Windows.Style> implement <xref:System.Windows.Markup.INameScope> through explicit interface definitions.</span></span> <span data-ttu-id="bf738-173">Jawne implementacje zezwalają, aby te zakresy nazw WPF języka XAML działały zwyczajowo, gdy <xref:System.Windows.Markup.INameScope> dostęp do nich odbywa się za pomocą interfejsu, co jest [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] sposobem komunikacji Zakresy nazw WPF języka XAML przez procesy wewnętrzne.</span><span class="sxs-lookup"><span data-stu-id="bf738-173">The explicit implementations allow these XAML namescopes to behave conventionally when they are accessed through the <xref:System.Windows.Markup.INameScope> interface, which is how XAML namescopes are communicated by [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] internal processes.</span></span> <span data-ttu-id="bf738-174">Jednak jawne definicje <xref:System.Windows.FrameworkTemplate> interfejsu nie są częścią konwencjonalnej powierzchni interfejsu API i <xref:System.Windows.Style>, ponieważ <xref:System.Windows.Markup.INameScope> rzadko trzeba wywołać metody w <xref:System.Windows.FrameworkTemplate> i <xref:System.Windows.Style> bezpośrednio, a zamiast tego używać innego interfejsu API takie jak <xref:System.Windows.FrameworkElement.GetTemplateChild%2A>.</span><span class="sxs-lookup"><span data-stu-id="bf738-174">But the explicit interface definitions are not part of the conventional API surface of <xref:System.Windows.FrameworkTemplate> and <xref:System.Windows.Style>, because you seldom need to call the <xref:System.Windows.Markup.INameScope> methods on <xref:System.Windows.FrameworkTemplate> and <xref:System.Windows.Style> directly, and instead would use other API such as <xref:System.Windows.FrameworkElement.GetTemplateChild%2A>.</span></span>  
  
 <span data-ttu-id="bf738-175">Poniższe klasy definiują własne namescope języka XAML, używając <xref:System.Windows.NameScope?displayProperty=nameWithType> klasy pomocnika i łącząc się z implementacją namescope języka XAML <xref:System.Windows.NameScope.NameScope%2A?displayProperty=nameWithType> za pomocą dołączonej właściwości:</span><span class="sxs-lookup"><span data-stu-id="bf738-175">The following classes define their own XAML namescope, by using the <xref:System.Windows.NameScope?displayProperty=nameWithType> helper class and connecting to its XAML namescope implementation through the <xref:System.Windows.NameScope.NameScope%2A?displayProperty=nameWithType> attached property:</span></span>  
  
- <xref:System.Windows.FrameworkElement>  
  
- <xref:System.Windows.FrameworkContentElement>  
  
## <a name="see-also"></a><span data-ttu-id="bf738-176">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="bf738-176">See also</span></span>

- [<span data-ttu-id="bf738-177">Przestrzeń nazw XAML i mapowanie przestrzeni nazw dla WPF XAML</span><span class="sxs-lookup"><span data-stu-id="bf738-177">XAML Namespaces and Namespace Mapping for WPF XAML</span></span>](xaml-namespaces-and-namespace-mapping-for-wpf-xaml.md)
- [<span data-ttu-id="bf738-178">x:Name, dyrektywa</span><span class="sxs-lookup"><span data-stu-id="bf738-178">x:Name Directive</span></span>](../../xaml-services/x-name-directive.md)
