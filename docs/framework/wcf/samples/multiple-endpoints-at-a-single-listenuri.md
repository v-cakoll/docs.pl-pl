---
title: Wiele punktów końcowych w pojedynczym identyfikatorze ListenUri
ms.date: 03/30/2017
ms.assetid: 911ffad4-4d47-4430-b7c2-79192ce6bcbd
ms.openlocfilehash: 8e26cc18ed35c446dda120c678dd7e879c756c0f
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/12/2020
ms.locfileid: "79183487"
---
# <a name="multiple-endpoints-at-a-single-listenuri"></a><span data-ttu-id="6c41f-102">Wiele punktów końcowych w pojedynczym identyfikatorze ListenUri</span><span class="sxs-lookup"><span data-stu-id="6c41f-102">Multiple Endpoints at a Single ListenUri</span></span>
<span data-ttu-id="6c41f-103">W tym przykładzie pokazano usługę, która obsługuje `ListenUri`wiele punktów końcowych w jednym .</span><span class="sxs-lookup"><span data-stu-id="6c41f-103">This sample demonstrates a service that hosts multiple endpoints at a single `ListenUri`.</span></span> <span data-ttu-id="6c41f-104">Ten przykład jest oparty na [wprowadzenie,](../../../../docs/framework/wcf/samples/getting-started-sample.md) który implementuje usługę kalkulatora.</span><span class="sxs-lookup"><span data-stu-id="6c41f-104">This sample is based on the [Getting Started](../../../../docs/framework/wcf/samples/getting-started-sample.md) that implements a calculator service.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="6c41f-105">Procedura konfiguracji i instrukcje kompilacji dla tego przykładu znajdują się na końcu tego tematu.</span><span class="sxs-lookup"><span data-stu-id="6c41f-105">The setup procedure and build instructions for this sample are located at the end of this topic.</span></span>  
  
 <span data-ttu-id="6c41f-106">Jak pokazano w próbce [wiele punktów końcowych,](../../../../docs/framework/wcf/samples/multiple-endpoints.md) usługa może obsługiwać wiele punktów końcowych, każdy z różnych adresów i ewentualnie również różne powiązania.</span><span class="sxs-lookup"><span data-stu-id="6c41f-106">As demonstrated in the [Multiple Endpoints](../../../../docs/framework/wcf/samples/multiple-endpoints.md) sample, a service can host multiple endpoints, each with different addresses and possibly also different bindings.</span></span> <span data-ttu-id="6c41f-107">W tym przykładzie pokazano, że jest możliwe hostowanie wielu punktów końcowych pod tym samym adresem.</span><span class="sxs-lookup"><span data-stu-id="6c41f-107">This sample shows that it is possible to host multiple endpoints at the same address.</span></span> <span data-ttu-id="6c41f-108">W tym przykładzie pokazano również różnice między dwoma rodzajami `EndpointAddress` adresów, które ma punkt końcowy usługi: i `ListenUri`.</span><span class="sxs-lookup"><span data-stu-id="6c41f-108">This sample also demonstrates the differences between the two kinds of addresses that a service endpoint has: `EndpointAddress` and `ListenUri`.</span></span>  
  
 <span data-ttu-id="6c41f-109">Jest `EndpointAddress` to logiczny adres usługi.</span><span class="sxs-lookup"><span data-stu-id="6c41f-109">The `EndpointAddress` is the logical address of a service.</span></span> <span data-ttu-id="6c41f-110">Jest to adres, do którego adresowane są komunikaty SOAP.</span><span class="sxs-lookup"><span data-stu-id="6c41f-110">It is the address that SOAP messages are addressed to.</span></span> <span data-ttu-id="6c41f-111">Jest `ListenUri` to adres fizyczny usługi.</span><span class="sxs-lookup"><span data-stu-id="6c41f-111">The `ListenUri` is the physical address of the service.</span></span> <span data-ttu-id="6c41f-112">Ma informacje o porcie i adresie, w którym punkt końcowy usługi faktycznie nasłuchuje wiadomości na bieżącym komputerze.</span><span class="sxs-lookup"><span data-stu-id="6c41f-112">It has the port and address information where the service endpoint actually listens for messages on the current machine.</span></span> <span data-ttu-id="6c41f-113">W większości przypadków nie ma potrzeby, aby te adresy się różniły; gdy `ListenUri` nie jest jawnie określony, domyślnie do `EndpointAddress` identyfikatora URI punktu końcowego.</span><span class="sxs-lookup"><span data-stu-id="6c41f-113">In most cases, there is no need for these addresses to differ; when a `ListenUri` is not explicitly specified, it defaults to the URI of the `EndpointAddress` of the endpoint.</span></span> <span data-ttu-id="6c41f-114">W kilku przypadkach warto je odróżnić, na przykład podczas konfigurowania routera, który może akceptować wiadomości zaadresowane do wielu różnych usług.</span><span class="sxs-lookup"><span data-stu-id="6c41f-114">In a few cases, it is useful to distinguish them, such as when configuring a router, which might accept messages addressed to a number of different services.</span></span>  
  
## <a name="service"></a><span data-ttu-id="6c41f-115">Usługa</span><span class="sxs-lookup"><span data-stu-id="6c41f-115">Service</span></span>  
 <span data-ttu-id="6c41f-116">Usługa w tym przykładzie `ICalculator` ma `IEcho`dwie umowy i .</span><span class="sxs-lookup"><span data-stu-id="6c41f-116">The service in this sample has two contracts, `ICalculator` and `IEcho`.</span></span> <span data-ttu-id="6c41f-117">Oprócz niestandardowego punktu `IMetadataExchange` końcowego istnieją trzy punkty końcowe aplikacji, jak pokazano w poniższym kodzie.</span><span class="sxs-lookup"><span data-stu-id="6c41f-117">In addition to the customary `IMetadataExchange` endpoint, there are three application endpoints, as shown in the following code.</span></span>  
  
```xml  
<endpoint address="urn:Stuff"  
        binding="wsHttpBinding"  
        contract="Microsoft.ServiceModel.Samples.ICalculator"
        listenUri="http://localhost/servicemodelsamples/service.svc" />  
<endpoint address="urn:Stuff"  
        binding="wsHttpBinding"  
        contract="Microsoft.ServiceModel.Samples.IEcho"
        listenUri="http://localhost/servicemodelsamples/service.svc" />  
<endpoint address="urn:OtherEcho"  
        binding="wsHttpBinding"  
        contract="Microsoft.ServiceModel.Samples.IEcho"
        listenUri="http://localhost/servicemodelsamples/service.svc" />  
```  
  
 <span data-ttu-id="6c41f-118">Wszystkie trzy punkty końcowe są `ListenUri` hostowane w `binding` tym samym czasie `ListenUri` i używają tego samego — punkty końcowe w tym samym musi mieć to samo powiązanie, ponieważ są one udostępnianie stosu pojedynczego kanału, który nasłuchuje wiadomości pod tym adresem fizycznym na komputerze.</span><span class="sxs-lookup"><span data-stu-id="6c41f-118">All three endpoints are hosted at the same `ListenUri` and use the same `binding` - endpoints at the same `ListenUri` must have the same binding, because they are sharing a single channel stack that listens for messages at that physical address on the machine.</span></span> <span data-ttu-id="6c41f-119">Każdy `address` punkt końcowy jest URN; chociaż zazwyczaj adresy reprezentują lokalizacje fizyczne, w rzeczywistości adres może być dowolnego rodzaju identyfikatora URI, ponieważ adres jest używany do dopasowywania i filtrowania celów, jak pokazano w tym przykładzie.</span><span class="sxs-lookup"><span data-stu-id="6c41f-119">The `address` of each endpoint is a URN; though typically addresses represent physical locations, in fact the address can be any kind of URI, because the address is used for matching and filtering purposes as is demonstrated in this sample.</span></span>  
  
 <span data-ttu-id="6c41f-120">Ponieważ wszystkie trzy punkty końcowe współużytkują to samo `ListenUri`, gdy pojawi się tam komunikat, Windows Communication Foundation (WCF) musi zdecydować, do którego punktu końcowego jest przeznaczona wiadomość.</span><span class="sxs-lookup"><span data-stu-id="6c41f-120">Because all three endpoints share the same `ListenUri`, when a message arrives there, Windows Communication Foundation (WCF) must decide which endpoint the message is destined for.</span></span> <span data-ttu-id="6c41f-121">Każdy punkt końcowy ma filtr wiadomości, który składa się z dwóch części: filtr adresu i filtr kontraktu.</span><span class="sxs-lookup"><span data-stu-id="6c41f-121">Each endpoint has a message filter that is comprised of two parts: the address filter and the contract filter.</span></span> <span data-ttu-id="6c41f-122">Filtr adresu jest `To` zgodny z komunikatem SOAP na adres punktu końcowego usługi.</span><span class="sxs-lookup"><span data-stu-id="6c41f-122">The address filter matches the `To` of the SOAP message to the address of the service endpoint.</span></span> <span data-ttu-id="6c41f-123">Na przykład tylko `To "Urn:OtherEcho"` komunikaty adresowane są kandydatami do trzeciego punktu końcowego tej usługi.</span><span class="sxs-lookup"><span data-stu-id="6c41f-123">For example, only messages addressed `To "Urn:OtherEcho"` are candidates for the third endpoint of this service.</span></span> <span data-ttu-id="6c41f-124">Filtr kontraktu jest zgodny z akcjami skojarzonymi z operacjami określonego kontraktu.</span><span class="sxs-lookup"><span data-stu-id="6c41f-124">The contract filter matches the Actions associated with the operations of a particular contract.</span></span> <span data-ttu-id="6c41f-125">Na przykład wiadomości z `IEcho`działaniem .</span><span class="sxs-lookup"><span data-stu-id="6c41f-125">For example, messages with the action of `IEcho`.</span></span> <span data-ttu-id="6c41f-126">`Echo`dopasowuje filtry kontraktu zarówno drugiego, jak i trzeciego punktu końcowego tej `IEcho` usługi, ponieważ oba te punkty końcowe obsługują kontrakt.</span><span class="sxs-lookup"><span data-stu-id="6c41f-126">`Echo` matches the contract filters of both the second and third endpoints of this service, because both of those endpoints host the `IEcho` contract.</span></span>  
  
 <span data-ttu-id="6c41f-127">W ten sposób połączenie filtru adresów i filtru kontraktu umożliwia `ListenUri` kierowanie każdej wiadomości, która dociera do tej usługi do prawidłowego punktu końcowego.</span><span class="sxs-lookup"><span data-stu-id="6c41f-127">Thus the combination of address filter and contract filter makes it possible to route each message that arrives at this service's `ListenUri` to the correct endpoint.</span></span> <span data-ttu-id="6c41f-128">Trzeci punkt końcowy różni się od pozostałych dwóch, ponieważ akceptuje wiadomości wysyłane na inny adres niż inne punkty końcowe.</span><span class="sxs-lookup"><span data-stu-id="6c41f-128">The third endpoint is differentiated from the other two because it accepts messages sent to a different address from the other endpoints.</span></span> <span data-ttu-id="6c41f-129">Pierwszy i drugi punkty końcowe różnią się od siebie na podstawie ich umów (Akcja wiadomości przychodzącej).</span><span class="sxs-lookup"><span data-stu-id="6c41f-129">The first and second endpoints are differentiated from each other based on their contracts (the Action of the incoming message).</span></span>  
  
## <a name="client"></a><span data-ttu-id="6c41f-130">Klient</span><span class="sxs-lookup"><span data-stu-id="6c41f-130">Client</span></span>  
 <span data-ttu-id="6c41f-131">Podobnie jak punkty końcowe na serwerze mają dwa różne adresy, punkty końcowe klienta mają również dwa adresy.</span><span class="sxs-lookup"><span data-stu-id="6c41f-131">Just as endpoints on the server have two different addresses, client endpoints also have two addresses.</span></span> <span data-ttu-id="6c41f-132">Zarówno na serwerze, jak i `EndpointAddress`na kliencie adres logiczny jest nazywany .</span><span class="sxs-lookup"><span data-stu-id="6c41f-132">On both server and client, the logical address is called the `EndpointAddress`.</span></span> <span data-ttu-id="6c41f-133">Ale podczas gdy adres fizyczny jest nazywany `ListenUri` na serwerze, na `Via`kliencie adres fizyczny jest nazywany .</span><span class="sxs-lookup"><span data-stu-id="6c41f-133">But whereas the physical address is called the `ListenUri` on the server, on the client, the physical address is called the `Via`.</span></span>  
  
 <span data-ttu-id="6c41f-134">Podobnie jak na serwerze, domyślnie te dwa adresy są takie same.</span><span class="sxs-lookup"><span data-stu-id="6c41f-134">As on the server, by default, these two addresses are the same.</span></span> <span data-ttu-id="6c41f-135">Aby określić `Via` na kliencie, który różni się `ClientViaBehavior` od adresu punktu końcowego, jest używany:</span><span class="sxs-lookup"><span data-stu-id="6c41f-135">To specify a `Via` on the client that is different from the endpoint's address, `ClientViaBehavior` is used:</span></span>  
  
```csharp  
Uri via = new Uri("http://localhost/ServiceModelSamples/service.svc");  
CalculatorClient calcClient = new CalculatorClient();  
calcClient.ChannelFactory.Endpoint.Behaviors.Add(  
        new ClientViaBehavior(via));  
```  
  
 <span data-ttu-id="6c41f-136">Jak zwykle adres pochodzi z pliku konfiguracji klienta, który został wygenerowany przez Svcutil.exe.</span><span class="sxs-lookup"><span data-stu-id="6c41f-136">As usual, the address comes from the client configuration file, which was generated by Svcutil.exe.</span></span> <span data-ttu-id="6c41f-137">(który `Via` odpowiada `ListenUri` usługi) nie pojawia się w metadanych usługi, a więc te informacje muszą być przekazywane do klienta poza pasmem (podobnie jak adres metadanych usługi).</span><span class="sxs-lookup"><span data-stu-id="6c41f-137">The `Via` (which corresponds to the `ListenUri` of the service) does not appear in the service's metadata and so this information must be communicated to the client out-of-band (just like the service's metadata address).</span></span>  
  
 <span data-ttu-id="6c41f-138">Klient w tym przykładzie wysyła komunikaty do każdego z trzech punktów końcowych aplikacji serwera, aby wykazać, że może komunikować się (i odróżniać) wszystkie trzy punkty końcowe, nawet jeśli wszystkie mają takie same `Via`.</span><span class="sxs-lookup"><span data-stu-id="6c41f-138">The client in this sample sends messages to each of the server's three application endpoints, to demonstrate that it can communicate with (and differentiate) all three endpoints, even though they all have the same `Via`.</span></span>  
  
#### <a name="to-set-up-build-and-run-the-sample"></a><span data-ttu-id="6c41f-139">Aby skonfigurować, skompilować i uruchomić próbkę</span><span class="sxs-lookup"><span data-stu-id="6c41f-139">To set up, build, and run the sample</span></span>  
  
1. <span data-ttu-id="6c41f-140">Upewnij się, że wykonano [procedurę jednorazowej instalacji dla przykładów fundacji komunikacji systemu Windows](../../../../docs/framework/wcf/samples/one-time-setup-procedure-for-the-wcf-samples.md).</span><span class="sxs-lookup"><span data-stu-id="6c41f-140">Ensure that you have performed the [One-Time Setup Procedure for the Windows Communication Foundation Samples](../../../../docs/framework/wcf/samples/one-time-setup-procedure-for-the-wcf-samples.md).</span></span>  
  
2. <span data-ttu-id="6c41f-141">Aby utworzyć wersję C# lub Visual Basic .NET rozwiązania, postępuj zgodnie z instrukcjami w [tworzenie przykładów programu Windows Communication Foundation](../../../../docs/framework/wcf/samples/building-the-samples.md).</span><span class="sxs-lookup"><span data-stu-id="6c41f-141">To build the C# or Visual Basic .NET edition of the solution, follow the instructions in [Building the Windows Communication Foundation Samples](../../../../docs/framework/wcf/samples/building-the-samples.md).</span></span>  
  
3. <span data-ttu-id="6c41f-142">Aby uruchomić próbkę w konfiguracji z jednym lub krzyżowym komputerem, postępuj zgodnie z instrukcjami w [programie Uruchamianie przykładów fundacji komunikacji systemu Windows](../../../../docs/framework/wcf/samples/running-the-samples.md).</span><span class="sxs-lookup"><span data-stu-id="6c41f-142">To run the sample in a single- or cross-machine configuration, follow the instructions in [Running the Windows Communication Foundation Samples](../../../../docs/framework/wcf/samples/running-the-samples.md).</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="6c41f-143">W przypadku komputera między komputerem należy zastąpić hosta lokalnego w pliku Client.cs nazwą komputera serwisowego.</span><span class="sxs-lookup"><span data-stu-id="6c41f-143">For cross-machine, you must replace localhost in the Client.cs file with the name of the service machine.</span></span>  
  
> [!IMPORTANT]
> <span data-ttu-id="6c41f-144">Próbki mogą być już zainstalowane na komputerze.</span><span class="sxs-lookup"><span data-stu-id="6c41f-144">The samples may already be installed on your machine.</span></span> <span data-ttu-id="6c41f-145">Przed kontynuowaniem sprawdź następujący (domyślny) katalog.</span><span class="sxs-lookup"><span data-stu-id="6c41f-145">Check for the following (default) directory before continuing.</span></span>  
>
> `<InstallDrive>:\WF_WCF_Samples`  
>
> <span data-ttu-id="6c41f-146">Jeśli ten katalog nie istnieje, przejdź do [Windows Communication Foundation (WCF) i Windows Workflow Foundation (WF) Przykłady dla platformy .NET Framework 4,](https://www.microsoft.com/download/details.aspx?id=21459) aby pobrać wszystkie Windows Communication Foundation (WCF) i [!INCLUDE[wf1](../../../../includes/wf1-md.md)] przykłady.</span><span class="sxs-lookup"><span data-stu-id="6c41f-146">If this directory does not exist, go to [Windows Communication Foundation (WCF) and Windows Workflow Foundation (WF) Samples for .NET Framework 4](https://www.microsoft.com/download/details.aspx?id=21459) to download all Windows Communication Foundation (WCF) and [!INCLUDE[wf1](../../../../includes/wf1-md.md)] samples.</span></span> <span data-ttu-id="6c41f-147">Ten przykład znajduje się w następującym katalogu.</span><span class="sxs-lookup"><span data-stu-id="6c41f-147">This sample is located in the following directory.</span></span>  
>
> `<InstallDrive>:\WF_WCF_Samples\WCF\Basic\Services\MultipleEndpointsSingleUri`  
