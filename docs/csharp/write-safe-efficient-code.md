---
title: Zapisz bezpieczny i wydajny C# kod
description: Najnowsze ulepszenia C# języka umożliwiają pisanie możliwego do zweryfikowania bezpiecznego kodu, który został wcześniej powiązany z niebezpiecznym kodem.
ms.date: 10/23/2018
ms.technology: csharp-advanced-concepts
ms.custom: mvc
ms.openlocfilehash: d4a7916b80e15c7f00fa0a7da213ed0593e0959d
ms.sourcegitcommit: 43d10ef65f0f1fd6c3b515e363bde11a3fcd8d6d
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/03/2020
ms.locfileid: "78239979"
---
# <a name="write-safe-and-efficient-c-code"></a><span data-ttu-id="cf3cb-103">Zapisz bezpieczny i wydajny C# kod</span><span class="sxs-lookup"><span data-stu-id="cf3cb-103">Write safe and efficient C# code</span></span>

<span data-ttu-id="cf3cb-104">Nowe funkcje w C# programie umożliwiają pisanie zweryfikowanego bezpiecznego kodu z lepszą wydajnością.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-104">New features in C# enable you to write verifiable safe code with better performance.</span></span> <span data-ttu-id="cf3cb-105">W przypadku starannej zastosowania tych technik mniejsza liczba scenariuszy wymaga niebezpiecznego kodu.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-105">If you carefully apply these techniques, fewer scenarios require unsafe code.</span></span> <span data-ttu-id="cf3cb-106">Te funkcje ułatwiają Używanie odwołań do typów wartości jako argumentów metod i zwracanych metod.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-106">These features make it easier to use references to value types as method arguments and method returns.</span></span> <span data-ttu-id="cf3cb-107">W przypadku bezpiecznego wykonywania tych technik minimalizuje kopiowanie typów wartości.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-107">When done safely, these techniques minimize copying value types.</span></span> <span data-ttu-id="cf3cb-108">Za pomocą typów wartości można zminimalizować liczbę alokacji i przebiegów elementów bezużytecznych.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-108">By using value types, you can minimize the number of allocations and garbage collection passes.</span></span>

<span data-ttu-id="cf3cb-109">Większość przykładowego kodu w tym artykule używa funkcji dodanych w C# 7,2.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-109">Much of the sample code in this article uses features added in C# 7.2.</span></span> <span data-ttu-id="cf3cb-110">Aby korzystać z tych funkcji, należy skonfigurować projekt do korzystania C# z 7,2 lub nowszego.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-110">To use those features, you must configure your project to use C# 7.2 or later.</span></span> <span data-ttu-id="cf3cb-111">Aby uzyskać więcej informacji na temat ustawiania wersji językowej, zobacz [Konfigurowanie wersji językowej](language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="cf3cb-111">For more information on setting the language version, see [configure the language version](language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="cf3cb-112">Ten artykuł koncentruje się na technikach związanych z wydajnym zarządzaniem zasobami.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-112">This article focuses on techniques for efficient resource management.</span></span> <span data-ttu-id="cf3cb-113">Jedną z korzyści w korzystaniu z typów wartości jest to, że często unikają alokacji sterty.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-113">One advantage to using value types is that they often avoid heap allocations.</span></span> <span data-ttu-id="cf3cb-114">Wadą jest to, że są one kopiowane przez wartość.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-114">The disadvantage is that they're copied by value.</span></span> <span data-ttu-id="cf3cb-115">Ten kompromis utrudnia optymalizację algorytmów, które działają w przypadku dużych ilości danych.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-115">This tradeoff makes it harder to optimize algorithms that operate on large amounts of data.</span></span> <span data-ttu-id="cf3cb-116">Nowe funkcje językowe w C# 7,2 zapewniają mechanizmy, które umożliwiają bezpieczny wydajny kod przy użyciu odwołań do typów wartości.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-116">New language features in C# 7.2 provide mechanisms that enable safe efficient code using references to value types.</span></span> <span data-ttu-id="cf3cb-117">Korzystaj z tych funkcji, aby zminimalizować jednocześnie alokacje i operacje kopiowania.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-117">Use these features wisely to minimize both allocations and copy operations.</span></span> <span data-ttu-id="cf3cb-118">Ten artykuł zawiera informacje o tych nowych funkcjach.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-118">This article explores those new features.</span></span>

<span data-ttu-id="cf3cb-119">Ten artykuł koncentruje się na następujących technikach zarządzania zasobami:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-119">This article focuses on the following resource management techniques:</span></span>

- <span data-ttu-id="cf3cb-120">Zadeklaruj [`readonly struct`](language-reference/keywords/readonly.md#readonly-struct-example) , aby wyrazić, że typ jest **niemodyfikowalny** i umożliwia kompilatorowi zapisywanie kopii przy użyciu parametrów [`in`](language-reference/keywords/in-parameter-modifier.md) .</span><span class="sxs-lookup"><span data-stu-id="cf3cb-120">Declare a [`readonly struct`](language-reference/keywords/readonly.md#readonly-struct-example) to express that a type is **immutable** and enables the compiler to save copies when using [`in`](language-reference/keywords/in-parameter-modifier.md) parameters.</span></span>
- <span data-ttu-id="cf3cb-121">Jeśli typ nie może być niezmienny, zadeklaruj `struct` składowe `readonly`, aby wskazać, że element członkowski nie modyfikuje stanu.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-121">If a type can't be immutable, declare `struct` members `readonly` to indicate that the member doesn't modify state.</span></span>
- <span data-ttu-id="cf3cb-122">Użyj [`ref readonly`](language-reference/keywords/ref.md#reference-return-values) zwracać, gdy wartość zwracana jest `struct` większa niż <xref:System.IntPtr.Size?displayProperty=nameWithType> i okres istnienia magazynu jest większy niż Metoda zwracająca wartość.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-122">Use a [`ref readonly`](language-reference/keywords/ref.md#reference-return-values) return when the return value is a `struct` larger than <xref:System.IntPtr.Size?displayProperty=nameWithType> and the storage lifetime is greater than the method returning the value.</span></span>
- <span data-ttu-id="cf3cb-123">Gdy rozmiar `readonly struct` jest większy niż <xref:System.IntPtr.Size?displayProperty=nameWithType>, należy przekazać go jako parametr `in` ze względu na wydajność.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-123">When the size of a `readonly struct` is bigger than <xref:System.IntPtr.Size?displayProperty=nameWithType>, you should pass it as an `in` parameter for performance reasons.</span></span>
- <span data-ttu-id="cf3cb-124">Nigdy nie przekazuj `struct` jako parametru `in`, chyba że jest zadeklarowany za pomocą modyfikatora `readonly` lub metoda wywołuje tylko `readonly` elementów członkowskich struktury.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-124">Never pass a `struct` as an `in` parameter unless it's declared with the `readonly` modifier or the method calls only `readonly` members of the struct.</span></span> <span data-ttu-id="cf3cb-125">Naruszanie tych wskazówek może negatywnie wpłynąć na wydajność i może prowadzić do przesłaniania.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-125">Violating this guidance may negatively affect performance and could lead to an obscure behavior.</span></span>
- <span data-ttu-id="cf3cb-126">Użyj [`ref struct`](language-reference/keywords/ref.md#ref-struct-types)lub `readonly ref struct`, takich jak <xref:System.Span%601> lub <xref:System.ReadOnlySpan%601> do pracy z pamięcią, jako sekwencji bajtów.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-126">Use a [`ref struct`](language-reference/keywords/ref.md#ref-struct-types), or a `readonly ref struct` such as <xref:System.Span%601> or <xref:System.ReadOnlySpan%601> to work with memory as a sequence of bytes.</span></span>

<span data-ttu-id="cf3cb-127">Te techniki wymuszają zrównoważenie dwóch konkurencyjnych celów w odniesieniu do **odwołań** i **wartości**.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-127">These techniques force you to balance two competing goals with regard to **references** and **values**.</span></span> <span data-ttu-id="cf3cb-128">Zmienne, które są [typami odwołań](programming-guide/types/index.md#reference-types) , przechowują odwołanie do lokalizacji w pamięci.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-128">Variables that are [reference types](programming-guide/types/index.md#reference-types) hold a reference to the location in memory.</span></span> <span data-ttu-id="cf3cb-129">Zmienne [typu wartości](programming-guide/types/index.md#value-types) bezpośrednio zawierają ich wartości.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-129">Variables that are [value types](programming-guide/types/index.md#value-types) directly contain their value.</span></span> <span data-ttu-id="cf3cb-130">Różnice te podkreślają kluczowe różnice, które są ważne w przypadku zarządzania zasobami pamięci.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-130">These differences highlight the key differences that are important for managing memory resources.</span></span> <span data-ttu-id="cf3cb-131">**Typy wartości** są zwykle kopiowane, gdy są przesyłane do metody lub zwracane z metody.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-131">**Value types** are typically copied when passed to a method or returned from a method.</span></span> <span data-ttu-id="cf3cb-132">To zachowanie obejmuje kopiowanie wartości `this` podczas wywoływania elementów członkowskich typu wartości.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-132">This behavior includes copying the value of `this` when calling members of a value type.</span></span> <span data-ttu-id="cf3cb-133">Koszt kopii jest związany z rozmiarem typu.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-133">The cost of the copy is related to the size of the type.</span></span> <span data-ttu-id="cf3cb-134">**Typy odwołań** są przydzielane na zarządzanym stosie.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-134">**Reference types** are allocated on the managed heap.</span></span> <span data-ttu-id="cf3cb-135">Każdy nowy obiekt wymaga nowej alokacji, a następnie musi zostać odczytany.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-135">Each new object requires a new allocation, and subsequently must be reclaimed.</span></span> <span data-ttu-id="cf3cb-136">Obie te operacje są czasochłonne.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-136">Both these operations take time.</span></span> <span data-ttu-id="cf3cb-137">Odwołanie jest kopiowane, gdy typ odwołania zostanie przekazana jako argument do metody lub zwrócony z metody.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-137">The reference is copied when a reference type is passed as an argument to a method or returned from a method.</span></span>

<span data-ttu-id="cf3cb-138">W tym artykule przedstawiono przykładową koncepcję struktury punktu 3W w celu wyjaśnienia następujących zaleceń:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-138">This article uses the following example concept of the 3D-point structure to explain these recommendations:</span></span>

```csharp
public struct Point3D
{
    public double X;
    public double Y;
    public double Z;
}
```

<span data-ttu-id="cf3cb-139">Różne przykłady używają różnych implementacji tego pojęcia.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-139">Different examples use different implementations of this concept.</span></span>

## <a name="declare-readonly-structs-for-immutable-value-types"></a><span data-ttu-id="cf3cb-140">Deklarowanie struktur tylko do odczytu dla niezmiennych typów wartości</span><span class="sxs-lookup"><span data-stu-id="cf3cb-140">Declare readonly structs for immutable value types</span></span>

<span data-ttu-id="cf3cb-141">Deklarowanie `struct` przy użyciu modyfikatora `readonly` informuje kompilator, że celem jest utworzenie niezmiennego typu.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-141">Declaring a `struct` using the `readonly` modifier informs the compiler that your intent is to create an immutable type.</span></span> <span data-ttu-id="cf3cb-142">Kompilator wymusza tę decyzję projektową z następującymi regułami:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-142">The compiler enforces that design decision with the following rules:</span></span>

- <span data-ttu-id="cf3cb-143">Wszystkie elementy członkowskie pola muszą być `readonly`</span><span class="sxs-lookup"><span data-stu-id="cf3cb-143">All field members must be `readonly`</span></span>
- <span data-ttu-id="cf3cb-144">Wszystkie właściwości muszą być tylko do odczytu, z uwzględnieniem właściwości wdrożonych domyślnie.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-144">All properties must be read-only, including auto-implemented properties.</span></span>

<span data-ttu-id="cf3cb-145">Te dwie reguły są wystarczające, aby upewnić się, że żaden członek `readonly struct` nie modyfikuje stanu tej struktury.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-145">These two rules are sufficient to ensure that no member of a `readonly struct` modifies the state of that struct.</span></span> <span data-ttu-id="cf3cb-146">`struct` jest niezmienna.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-146">The `struct` is immutable.</span></span> <span data-ttu-id="cf3cb-147">Strukturę `Point3D` można zdefiniować jako niezmienne struktury, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-147">The `Point3D` structure could be defined as an immutable struct as shown in the following example:</span></span>

```csharp
readonly public struct ReadonlyPoint3D
{
    public ReadonlyPoint3D(double x, double y, double z)
    {
        this.X = x;
        this.Y = y;
        this.Z = z;
    }

    public double X { get; }
    public double Y { get; }
    public double Z { get; }
}
```

<span data-ttu-id="cf3cb-148">Postępuj zgodnie z tym zaleceniem za każdym razem, gdy zamierzasz utworzyć niezmienny typ wartości.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-148">Follow this recommendation whenever your design intent is to create an immutable value type.</span></span> <span data-ttu-id="cf3cb-149">Wszelkie ulepszenia wydajności są dodatkową korzyścią.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-149">Any performance improvements are an added benefit.</span></span> <span data-ttu-id="cf3cb-150">`readonly struct` jasno wyraża zamiar projektowania.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-150">The `readonly struct` clearly expresses your design intent.</span></span>

## <a name="declare-readonly-members-when-a-struct-cant-be-immutable"></a><span data-ttu-id="cf3cb-151">Zadeklaruj składowe tylko do odczytu, gdy struktura nie może być niezmienna</span><span class="sxs-lookup"><span data-stu-id="cf3cb-151">Declare readonly members when a struct can't be immutable</span></span>

<span data-ttu-id="cf3cb-152">W C# 8,0 i nowszych, gdy typ struktury jest modyfikowalny, należy zadeklarować składowe, które nie powodują `readonly`mutacji.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-152">In C# 8.0 and later, when a struct type is mutable, you should declare members that don't cause mutation to be `readonly`.</span></span> <span data-ttu-id="cf3cb-153">Na przykład poniżej przedstawiono modyfikowalną odmianę struktury punktu 3W:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-153">For example, the following is a mutable variation of the 3D point structure:</span></span>

```csharp
public struct Point3D
{
    public Point3D(double x, double y, double z)
    {
        _x = x;
        _y = y;
        _z = z;
    }

    private double _x;
    public double X
    {
        readonly get => _x;
        set => _x = value;
    }

    private double _y;
    public double Y
    {
        readonly get => _y;
        set => _y = value;
    }

    private double _z;
    public double Z
    {
        readonly get => _z;
        set => _z = value;
    }

    public readonly double Distance => Math.Sqrt(X * X + Y * Y + Z * Z);

    public readonly override string ToString() => $"{X}, {Y}, {Z}";
}
```

<span data-ttu-id="cf3cb-154">Powyższy przykład pokazuje wiele lokalizacji, w których można zastosować modyfikator `readonly`: metody, właściwości i metod dostępu do właściwości.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-154">The preceding sample shows many of the locations where you can apply the `readonly` modifier: methods, properties, and property accessors.</span></span> <span data-ttu-id="cf3cb-155">Jeśli używasz automatycznie wdrożonych właściwości, kompilator dodaje modyfikator `readonly` do metody dostępu `get` do odczytu i zapisu właściwości.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-155">If you use auto-implemented properties, the compiler adds the `readonly` modifier to the `get` accessor for read-write properties.</span></span> <span data-ttu-id="cf3cb-156">Kompilator dodaje modyfikator `readonly` do automatycznie implementowanych deklaracji właściwości dla właściwości z tylko akcesorem `get`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-156">The compiler adds the `readonly` modifier to the auto-implemented property declarations for properties with only a `get` accessor.</span></span>

<span data-ttu-id="cf3cb-157">Dodanie modyfikatora `readonly` do elementów członkowskich, które nie są zgodne ze stanem, zapewnia dwie powiązane korzyści.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-157">Adding the `readonly` modifier to members that don't mutate state provides two related benefits.</span></span> <span data-ttu-id="cf3cb-158">Najpierw kompilator wymusza zamiar.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-158">First, the compiler enforces your intent.</span></span> <span data-ttu-id="cf3cb-159">Ten element członkowski nie może zmieniać stanu struktury ani nie może uzyskać dostępu do elementu członkowskiego, który nie jest również oznaczony `readonly`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-159">That member can't mutate the struct's state, nor can it access a member that isn't also marked `readonly`.</span></span> <span data-ttu-id="cf3cb-160">Po drugie kompilator nie będzie tworzyć obronnych kopii parametrów `in` podczas uzyskiwania dostępu do `readonly` elementu członkowskiego.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-160">Second, the compiler won't create defensive copies of `in` parameters when accessing a `readonly` member.</span></span> <span data-ttu-id="cf3cb-161">Kompilator może bezpiecznie wykonać tę optymalizację, ponieważ gwarantuje, że `struct` nie jest modyfikowany przez `readonly` składową.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-161">The compiler can make this optimization safely because it guarantees that the `struct` is not modified by a `readonly` member.</span></span>

## <a name="use-ref-readonly-return-statements-for-large-structures-when-possible"></a><span data-ttu-id="cf3cb-162">Używaj instrukcji `ref readonly return` w przypadku dużych struktur, gdy jest to możliwe</span><span class="sxs-lookup"><span data-stu-id="cf3cb-162">Use `ref readonly return` statements for large structures when possible</span></span>

<span data-ttu-id="cf3cb-163">Można zwrócić wartości przez odwołanie, gdy zwracana wartość nie jest lokalna dla zwracanej metody.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-163">You can return values by reference when the value being returned isn't local to the returning method.</span></span> <span data-ttu-id="cf3cb-164">Zwracanie przez odwołanie oznacza, że kopiowane jest tylko odwołanie, a nie strukturę.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-164">Returning by reference means that only the reference is copied, not the structure.</span></span> <span data-ttu-id="cf3cb-165">W poniższym przykładzie właściwość `Origin` nie może użyć powrotu `ref`, ponieważ zwracana wartość jest zmienną lokalną:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-165">In the following example, the `Origin` property can't use a `ref` return because the value being returned is a local variable:</span></span>

```csharp
public Point3D Origin => new Point3D(0,0,0);
```

<span data-ttu-id="cf3cb-166">Jednak następująca Definicja właściwości może być zwracana przez odwołanie, ponieważ zwrócona wartość jest statyczną składową:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-166">However, the following property definition can be returned by reference because the returned value is a static member:</span></span>

```csharp
public struct Point3D
{
    private static Point3D origin = new Point3D(0,0,0);

    // Dangerous! returning a mutable reference to internal storage
    public ref Point3D Origin => ref origin;

    // other members removed for space
}
```

<span data-ttu-id="cf3cb-167">Nie chcesz, aby wywołujący modyfikowali źródło, więc należy zwrócić wartość `ref readonly`:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-167">You don't want callers modifying the origin, so you should return the value by `ref readonly`:</span></span>

```csharp
public struct Point3D
{
    private static Point3D origin = new Point3D(0,0,0);

    public static ref readonly Point3D Origin => ref origin;

    // other members removed for space
}
```

<span data-ttu-id="cf3cb-168">Zwrócenie `ref readonly` umożliwia zapisanie kopiowania większych struktur i zachowanie niezmienności wewnętrznych elementów członkowskich danych.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-168">Returning `ref readonly` enables you to save copying larger structures and preserve the immutability of your internal data members.</span></span>

<span data-ttu-id="cf3cb-169">W odniesieniu do witryny wywołującej może być używana Właściwość `Origin` jako `ref readonly` lub jako wartość:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-169">At the call site, callers make the choice to use the `Origin` property as a `ref readonly` or as a value:</span></span>

[!code-csharp[AssignRefReadonly](../../samples/snippets/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#AssignRefReadonly "Assigning a ref readonly")]

<span data-ttu-id="cf3cb-170">Pierwsze przypisanie w poprzednim kodzie wykonuje kopię `Origin` stałej i przypisuje tę kopię.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-170">The first assignment in the preceding code makes a copy of the `Origin` constant and assigns that copy.</span></span> <span data-ttu-id="cf3cb-171">Drugi przypisuje odwołanie.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-171">The second assigns a reference.</span></span> <span data-ttu-id="cf3cb-172">Należy zauważyć, że modyfikator `readonly` musi być częścią deklaracji zmiennej.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-172">Notice that the `readonly` modifier must be part of the declaration of the variable.</span></span> <span data-ttu-id="cf3cb-173">Nie można zmodyfikować odwołania, do którego się odwołuje.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-173">The reference to which it refers can't be modified.</span></span> <span data-ttu-id="cf3cb-174">Próby wykonania tej operacji spowodują błąd czasu kompilacji.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-174">Attempts to do so result in a compile-time error.</span></span>

<span data-ttu-id="cf3cb-175">Modyfikator `readonly` jest wymagany w deklaracji `originReference`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-175">The `readonly` modifier is required on the declaration of `originReference`.</span></span>

<span data-ttu-id="cf3cb-176">Kompilator wymusza, aby obiekt wywołujący nie mógł zmodyfikować odwołania.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-176">The compiler enforces that the caller can't modify the reference.</span></span> <span data-ttu-id="cf3cb-177">Próbuje przypisać wartość bezpośrednio Wygeneruj błąd czasu kompilacji.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-177">Attempts to assign the value directly generate a compile-time error.</span></span> <span data-ttu-id="cf3cb-178">Jednak kompilator nie może wiedzieć, czy jakakolwiek metoda członkowska modyfikuje stan struktury.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-178">However, the compiler can't know if any member method modifies the state of the struct.</span></span>
<span data-ttu-id="cf3cb-179">Aby upewnić się, że obiekt nie jest modyfikowany, kompilator tworzy kopie i wywołuje odwołania do elementów członkowskich przy użyciu tej kopii.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-179">To ensure that the object isn't modified, the compiler creates a copy and calls member references using that copy.</span></span> <span data-ttu-id="cf3cb-180">Wszelkie modyfikacje dotyczą tej kopii.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-180">Any modifications are to that defensive copy.</span></span>

## <a name="apply-the-in-modifier-to-readonly-struct-parameters-larger-than-systemintptrsize"></a><span data-ttu-id="cf3cb-181">Zastosuj modyfikator `in`, aby `readonly struct` parametry większe niż `System.IntPtr.Size`</span><span class="sxs-lookup"><span data-stu-id="cf3cb-181">Apply the `in` modifier to `readonly struct` parameters larger than `System.IntPtr.Size`</span></span>

<span data-ttu-id="cf3cb-182">Słowo kluczowe `in` uzupełnia istniejące `ref` i `out` słowa kluczowe, aby przekazywać argumenty przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-182">The `in` keyword complements the existing `ref` and `out` keywords to pass arguments by reference.</span></span> <span data-ttu-id="cf3cb-183">Słowo kluczowe `in` określa przekazywanie argumentu przez odwołanie, ale wywołana metoda nie modyfikuje wartości.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-183">The `in` keyword specifies passing the argument by reference, but the called method doesn't modify the value.</span></span>

<span data-ttu-id="cf3cb-184">To dodanie zapewnia pełen słownictwo do wyrażania zamiaru projektowania.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-184">This addition provides a full vocabulary to express your design intent.</span></span>
<span data-ttu-id="cf3cb-185">Typy wartości są kopiowane, gdy są przesyłane do wywołanej metody, jeśli nie określisz żadnego z następujących modyfikatorów w podpisie metody.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-185">Value types are copied when passed to a called method when you don't specify any of the following modifiers in the method signature.</span></span> <span data-ttu-id="cf3cb-186">Każdy z tych modyfikatorów określa, że zmienna jest przenoszona przez odwołanie, unikając kopiowania.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-186">Each of these modifiers specifies that a variable is passed by reference, avoiding the copy.</span></span> <span data-ttu-id="cf3cb-187">Każdy modyfikator wyraża inny cel:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-187">Each modifier expresses a different intent:</span></span>

- <span data-ttu-id="cf3cb-188">`out`: Ta metoda ustawia wartość argumentu używanego jako ten parametr.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-188">`out`: This method sets the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="cf3cb-189">`ref`: Ta metoda może ustawić wartość argumentu używanego jako ten parametr.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-189">`ref`: This method may set the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="cf3cb-190">`in`: Ta metoda nie modyfikuje wartości argumentu używanego jako ten parametr.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-190">`in`: This method doesn't modify the value of the argument used as this parameter.</span></span>

<span data-ttu-id="cf3cb-191">Dodaj modyfikator `in`, aby przekazać argument przez odwołanie i zadeklarować intencję projektowania do przekazywania argumentów przez odwołanie, aby uniknąć niepotrzebnego kopiowania.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-191">Add the `in` modifier to pass an argument by reference and declare your design intent to pass arguments by reference to avoid unnecessary copying.</span></span> <span data-ttu-id="cf3cb-192">Nie zamierzasz modyfikować obiektu używanego jako ten argument.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-192">You don't intend to modify the object used as that argument.</span></span>

<span data-ttu-id="cf3cb-193">To rozwiązanie często zwiększa wydajność dla typów wartości tylko do odczytu, które są większe niż <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-193">This practice often improves performance for readonly value types that are larger than <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span></span> <span data-ttu-id="cf3cb-194">W przypadku typów prostych (`sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`, `char`, `float`, `double`, `decimal` i `bool`, oraz `enum` typów) wszystkie potencjalne zyski wydajności są minimalne.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-194">For simple types (`sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`, `char`, `float`, `double`, `decimal` and `bool`, and `enum` types), any potential performance gains are minimal.</span></span> <span data-ttu-id="cf3cb-195">W rzeczywistości wydajność może się pogorszyć za pomocą przekazywania informacji dla typów mniejszych niż <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-195">In fact, performance may degrade by using pass-by-reference for types smaller than <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span></span>

<span data-ttu-id="cf3cb-196">Poniższy kod przedstawia przykład metody, która oblicza odległość między dwoma punktami w przestrzeni 3D.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-196">The following code shows an example of a method that calculates the distance between two points in 3D space.</span></span>

[!code-csharp[InArgument](../../samples/snippets/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgument "Specifying an in argument")]

<span data-ttu-id="cf3cb-197">Argumenty to dwie struktury, które każda z nich zawiera trzy podwojone.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-197">The arguments are two structures that each contain three doubles.</span></span> <span data-ttu-id="cf3cb-198">Podwójna wartość to 8 bajtów, więc każdy argument ma 24 bajty.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-198">A double is 8 bytes, so each argument is 24 bytes.</span></span> <span data-ttu-id="cf3cb-199">Określając modyfikator `in`, należy przekazać 4-bajtowy lub 8-bajtowy odwołanie do tych argumentów, w zależności od architektury maszyny.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-199">By specifying the `in` modifier, you pass a 4 byte or 8-byte reference to those arguments, depending on the architecture of the machine.</span></span> <span data-ttu-id="cf3cb-200">Różnica w rozmiarze jest mała, ale jest dodawana, gdy aplikacja wywołuje tę metodę w ścisłej pętli używającej wielu różnych wartości.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-200">The difference in size is small, but it adds up when your application calls this method in a tight loop using many different values.</span></span>

<span data-ttu-id="cf3cb-201">Modyfikator `in` uzupełnia także `out` i `ref` w inny sposób.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-201">The `in` modifier complements `out` and `ref` in other ways as well.</span></span> <span data-ttu-id="cf3cb-202">Nie można tworzyć przeciążeń metody, które różnią się tylko w obecności `in`, `out`lub `ref`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-202">You can't create overloads of a method that differ only in the presence of `in`, `out`, or `ref`.</span></span> <span data-ttu-id="cf3cb-203">Te nowe reguły zwiększają takie samo zachowanie, które było zawsze zdefiniowane dla `out` i `ref` parametrów.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-203">These new rules extend the same behavior that had always been defined for `out` and `ref` parameters.</span></span> <span data-ttu-id="cf3cb-204">Podobnie jak `out` i Modyfikatory `ref`, typy wartości nie są opakowane, ponieważ jest stosowany modyfikator `in`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-204">Like the `out` and `ref` modifiers, value types aren't boxed because the `in` modifier is applied.</span></span>

<span data-ttu-id="cf3cb-205">Modyfikator `in` może zostać zastosowany do każdego elementu członkowskiego, który pobiera parametry: metody, Delegaty, wyrażenia lambda, funkcje lokalne, indeksatory i operatory.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-205">The `in` modifier may be applied to any member that takes parameters: methods, delegates, lambdas, local functions, indexers, operators.</span></span>

<span data-ttu-id="cf3cb-206">Inną funkcją `in` parametrów jest, że można użyć wartości literału lub stałych dla argumentu do `in` parametru.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-206">Another feature of `in` parameters is that you may use literal values or constants for the argument to an `in` parameter.</span></span> <span data-ttu-id="cf3cb-207">Ponadto, w przeciwieństwie do `ref` lub `out` parametru, nie trzeba stosować modyfikatora `in` w witrynie wywołania.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-207">Also, unlike a `ref` or `out` parameter, you don't need to apply the `in` modifier at the call site.</span></span> <span data-ttu-id="cf3cb-208">Poniższy kod przedstawia dwa przykłady wywołania metody `CalculateDistance`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-208">The following code shows you two examples of calling the `CalculateDistance` method.</span></span> <span data-ttu-id="cf3cb-209">Pierwsze używa dwóch zmiennych lokalnych przekazaną przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-209">The first uses two local variables passed by reference.</span></span> <span data-ttu-id="cf3cb-210">Drugi zawiera zmienną tymczasową utworzoną w ramach wywołania metody.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-210">The second includes a temporary variable created as part of the method call.</span></span>

[!code-csharp[UseInArgument](../../samples/snippets/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#UseInArgument "Specifying an In argument")]

<span data-ttu-id="cf3cb-211">Istnieje kilka sposobów, w których kompilator wymusza charakter `in` tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-211">There are several ways in which the compiler enforces the read-only nature of an `in` argument.</span></span>  <span data-ttu-id="cf3cb-212">Po pierwsze, wywołana metoda nie może bezpośrednio przypisywać do parametru `in`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-212">First of all, the called method can't directly assign to an `in` parameter.</span></span> <span data-ttu-id="cf3cb-213">Nie można bezpośrednio przypisać do żadnego pola `in` parametru, gdy ta wartość jest typu `struct`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-213">It can't directly assign to any field of an `in` parameter when that value is a `struct` type.</span></span> <span data-ttu-id="cf3cb-214">Ponadto nie można przekazać `in` parametru do żadnej metody za pomocą modyfikatora `ref` lub `out`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-214">In addition, you can't pass an `in` parameter to any method using the `ref` or `out` modifier.</span></span>
<span data-ttu-id="cf3cb-215">Te reguły mają zastosowanie do każdego pola `in` parametru, pod warunkiem, że pole jest typu `struct`, a parametr jest również typem `struct`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-215">These rules apply to any field of an `in` parameter, provided the field is a `struct` type and the parameter is also a `struct` type.</span></span> <span data-ttu-id="cf3cb-216">W rzeczywistości te reguły mają zastosowanie w przypadku wielu warstw dostępu do elementów członkowskich, pod warunkiem `structs`typy na wszystkich poziomach dostępu do elementów członkowskich.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-216">In fact, these rules apply for multiple layers of member access provided the types at all levels of member access are `structs`.</span></span>
<span data-ttu-id="cf3cb-217">Kompilator wymusza, że typy `struct` przekazane jako argumenty `in` i ich składowe `struct` są zmiennymi tylko do odczytu, gdy są używane jako argumenty innych metod.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-217">The compiler enforces that `struct` types passed as  `in` arguments and their `struct` members are read-only variables when used as arguments to other methods.</span></span>

<span data-ttu-id="cf3cb-218">Użycie parametrów `in` może uniknąć potencjalnych kosztów tworzenia kopii.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-218">The use of `in` parameters can avoid the potential performance costs of making copies.</span></span> <span data-ttu-id="cf3cb-219">Nie zmienia ona semantyki żadnego wywołania metody.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-219">It doesn't change the semantics of any method call.</span></span> <span data-ttu-id="cf3cb-220">W związku z tym nie trzeba określać modyfikatora `in` w witrynie wywołania.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-220">Therefore, you don't need to specify the `in` modifier at the call site.</span></span> <span data-ttu-id="cf3cb-221">Pomijanie modyfikatora `in` w witrynie wywołania informuje kompilator, że może on wykonać kopię argumentu z następujących powodów:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-221">Omitting the `in` modifier at the call site informs the compiler that it's allowed to make a copy of the argument for the following reasons:</span></span>

- <span data-ttu-id="cf3cb-222">Istnieje niejawna konwersja, ale nie konwersja tożsamości z typu argumentu na typ parametru.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-222">There exists an implicit conversion but not an identity conversion from the argument type to the parameter type.</span></span>
- <span data-ttu-id="cf3cb-223">Argument jest wyrażeniem, ale nie ma znanej zmiennej magazynu.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-223">The argument is an expression but doesn't have a known storage variable.</span></span>
- <span data-ttu-id="cf3cb-224">Istnieje Przeciążenie, które różni się od obecności lub braku `in`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-224">An overload exists that differs by the presence or absence of `in`.</span></span> <span data-ttu-id="cf3cb-225">W takim przypadku Przeciążenie przez wartość jest lepszym dopasowaniem.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-225">In that case, the by value overload is a better match.</span></span>

<span data-ttu-id="cf3cb-226">Te reguły są przydatne podczas aktualizowania istniejącego kodu w celu użycia argumentów odwołania tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-226">These rules are useful as you update existing code to use read-only reference arguments.</span></span> <span data-ttu-id="cf3cb-227">Wewnątrz metody wywoływanej można wywołać dowolną metodę wystąpienia używaną przez parametry wartości.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-227">Inside the called method, you can call any instance method that uses by value parameters.</span></span> <span data-ttu-id="cf3cb-228">W tych wystąpieniach zostanie utworzona kopia `in` parametru.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-228">In those instances, a copy of the `in` parameter is created.</span></span> <span data-ttu-id="cf3cb-229">Ponieważ kompilator może utworzyć zmienną tymczasową dla dowolnego parametru `in`, można także określić wartości domyślne dla dowolnego parametru `in`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-229">Because the compiler may create a temporary variable for any `in` parameter, you can also specify default values for any `in` parameter.</span></span> <span data-ttu-id="cf3cb-230">Poniższy kod określa źródło (punkt 0, 0) jako wartość domyślną dla drugiego punktu:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-230">The following code specifies the origin (point 0,0) as the default value for the second point:</span></span>

[!code-csharp[InArgumentDefault](../../samples/snippets/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgumentDefault "Specifying defaults for an in parameter")]

<span data-ttu-id="cf3cb-231">Aby wymusić przekazywanie przez kompilator argumentów tylko do odczytu przez odwołanie, określ modyfikator `in` dla argumentów w miejscu wywołania, jak pokazano w poniższym kodzie:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-231">To force the compiler to pass read-only arguments by reference, specify the `in` modifier on the arguments at the call site, as shown in the following code:</span></span>

[!code-csharp[UseInArgument](../../samples/snippets/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#ExplicitInArgument "Specifying an In argument")]

<span data-ttu-id="cf3cb-232">To zachowanie ułatwia stosowanie parametrów `in` w czasie w dużych bazach kodu, w których możliwy jest wzrost wydajności.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-232">This behavior makes it easier to adopt `in` parameters over time in large codebases where performance gains are possible.</span></span> <span data-ttu-id="cf3cb-233">Aby najpierw dodać modyfikator `in` do podpisów metod.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-233">You add the `in` modifier to method signatures first.</span></span> <span data-ttu-id="cf3cb-234">Następnie można dodać modyfikator `in` w lokacjach wywołań i utworzyć typy `readonly struct`, aby umożliwić kompilatorowi uniknięcie tworzenia w większej liczbie obronnych kopii `in` parametrów.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-234">Then, you can add the `in` modifier at call sites and create `readonly struct` types to enable the compiler to avoid creating defensive copies of `in` parameters in more locations.</span></span>

<span data-ttu-id="cf3cb-235">Wyznaczania parametru `in` można również użyć z typami referencyjnymi lub wartościami liczbowymi.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-235">The `in` parameter designation can also be used with reference types or numeric values.</span></span> <span data-ttu-id="cf3cb-236">Jednak korzyści w obu przypadkach są minimalne, o ile istnieją.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-236">However, the benefits in both cases are minimal, if any.</span></span>

## <a name="never-use-mutable-structs-as-in-in-argument"></a><span data-ttu-id="cf3cb-237">Nigdy nie używaj niemodyfikowalnych struktur jako argumentu `in`</span><span class="sxs-lookup"><span data-stu-id="cf3cb-237">Never use mutable structs as in `in` argument</span></span>

<span data-ttu-id="cf3cb-238">Opisane powyżej techniki wyjaśniają, jak uniknąć kopiowania przez zwracanie odwołań i przekazywanie wartości przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-238">The techniques described above explain how to avoid copies by returning references and passing values by reference.</span></span> <span data-ttu-id="cf3cb-239">Techniki te działają najlepiej, gdy typy argumentów są zadeklarowane jako typy `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-239">These techniques work best when the argument types are declared as `readonly struct` types.</span></span> <span data-ttu-id="cf3cb-240">W przeciwnym razie kompilator musi utworzyć **kopie** w wielu sytuacjach, aby wymusić stałość wszystkich argumentów.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-240">Otherwise, the compiler must create **defensive copies** in many situations to enforce the readonly-ness of any arguments.</span></span> <span data-ttu-id="cf3cb-241">Rozważmy poniższy przykład, który oblicza odległość punktu 3W od źródła:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-241">Consider the following example that calculates the distance of a 3D point from the origin:</span></span>

[!code-csharp[InArgument](../../samples/snippets/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgument "Specifying an in argument")]

<span data-ttu-id="cf3cb-242">Struktura `Point3D` *nie* jest strukturą tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-242">The `Point3D` structure is *not* a readonly struct.</span></span> <span data-ttu-id="cf3cb-243">W treści tej metody istnieje sześć różnych wywołań dostępu do właściwości.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-243">There are six different property access calls in the body of this method.</span></span> <span data-ttu-id="cf3cb-244">Przy pierwszej analizie można uważać, że te dostępy były bezpieczne.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-244">On first examination, you may have thought these accesses were safe.</span></span> <span data-ttu-id="cf3cb-245">Po wykonaniu tej operacji metoda dostępu `get` nie powinna modyfikować stanu obiektu.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-245">After all, a `get` accessor shouldn't modify the state of the object.</span></span> <span data-ttu-id="cf3cb-246">Nie istnieje jednak reguła języka, która wymusza ten.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-246">But there's no language rule that enforces that.</span></span> <span data-ttu-id="cf3cb-247">Jest to tylko Wspólna konwencja.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-247">It's only a common convention.</span></span> <span data-ttu-id="cf3cb-248">Każdy typ może zaimplementować metodę dostępu `get`, która zmodyfikowała stan wewnętrzny.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-248">Any type could implement a `get` accessor that modified the internal state.</span></span> <span data-ttu-id="cf3cb-249">Bez gwarancji języka kompilator musi utworzyć tymczasową kopię argumentu przed wywołaniem dowolnego elementu członkowskiego.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-249">Without some language guarantee, the compiler must create a temporary copy of the argument before calling any member.</span></span> <span data-ttu-id="cf3cb-250">Magazyn tymczasowy jest tworzony na stosie, wartości argumentu są kopiowane do magazynu tymczasowego, a wartość jest kopiowana do stosu dla każdego elementu członkowskiego jako argument `this`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-250">The temporary storage is created on the stack, the values of the argument are copied to the temporary storage, and the value is copied to the stack for each member access as the `this` argument.</span></span> <span data-ttu-id="cf3cb-251">W wielu sytuacjach te kopie mają szkodliwy wpływ na wydajność, ponieważ przekazywanie przez wartość jest szybsze niż odwołanie przekazywane przez tylko do odczytu, gdy typ argumentu nie jest `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-251">In many situations, these copies harm performance enough that pass-by-value is faster than pass-by-readonly-reference when the argument type isn't a `readonly struct`.</span></span>

<span data-ttu-id="cf3cb-252">Zamiast tego, jeśli obliczenie odległości używa niezmiennej struktury, `ReadonlyPoint3D`, obiekty tymczasowe nie są potrzebne:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-252">Instead, if the distance calculation uses the immutable struct, `ReadonlyPoint3D`, temporary objects aren't needed:</span></span>

[!code-csharp[readonlyInArgument](../../samples/snippets/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#ReadOnlyInArgument "Specifying a readonly in argument")]

<span data-ttu-id="cf3cb-253">Kompilator generuje bardziej wydajny kod podczas wywoływania elementów członkowskich `readonly struct`: odwołanie `this`, zamiast kopii odbiornika, jest zawsze `in` parametrem przekazaną przez odwołanie do metody członkowskiej.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-253">The compiler generates more efficient code when you call members of a `readonly struct`: The `this` reference, instead of a copy of the receiver, is always an `in` parameter passed by reference to the member method.</span></span> <span data-ttu-id="cf3cb-254">Ta optymalizacja zapisuje kopiowanie w przypadku używania `readonly struct` jako argumentu `in`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-254">This optimization saves copying when you use a `readonly struct` as an `in` argument.</span></span>

<span data-ttu-id="cf3cb-255">Nie należy przekazywać typu wartości null jako argumentu `in`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-255">You shouldn't pass a nullable value type as an `in` argument.</span></span> <span data-ttu-id="cf3cb-256">Typ <xref:System.Nullable%601> nie jest zadeklarowany jako struktura tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-256">The <xref:System.Nullable%601> type isn't declared as a read-only struct.</span></span> <span data-ttu-id="cf3cb-257">Oznacza to, że kompilator musi generować kopie obronne dla każdego argumentu typu wartości null przekazaną do metody przy użyciu modyfikatora `in` w deklaracji parametru.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-257">That means the compiler must generate defensive copies for any nullable value type argument passed to a method using the `in` modifier on the parameter declaration.</span></span>

<span data-ttu-id="cf3cb-258">Możesz zobaczyć Przykładowy program, który demonstruje różnice w wydajności przy użyciu [BenchmarkDotNet](https://www.nuget.org/packages/BenchmarkDotNet/) w naszym [repozytorium przykładów](https://github.com/dotnet/samples/tree/master/csharp/safe-efficient-code/benchmark) w witrynie GitHub.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-258">You can see an example program that demonstrates the performance differences using [BenchmarkDotNet](https://www.nuget.org/packages/BenchmarkDotNet/) in our [samples repository](https://github.com/dotnet/samples/tree/master/csharp/safe-efficient-code/benchmark) on GitHub.</span></span> <span data-ttu-id="cf3cb-259">Porównuje przekazanie modyfikowalnej struktury przez wartość i przez odwołanie z przekazywaniem niezmiennej struktury przez wartość i przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-259">It compares passing a mutable struct by value and by reference with passing an immutable struct by value and by reference.</span></span> <span data-ttu-id="cf3cb-260">Użycie niezmiennej struktury i przekazywanie przez odwołanie jest najszybsze.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-260">The use of the immutable struct and pass by reference is fastest.</span></span>

## <a name="use-ref-struct-types-to-work-with-blocks-or-memory-on-a-single-stack-frame"></a><span data-ttu-id="cf3cb-261">Użyj typów `ref struct` do pracy z blokami lub pamięcią w pojedynczej klatce stosu</span><span class="sxs-lookup"><span data-stu-id="cf3cb-261">Use `ref struct` types to work with blocks or memory on a single stack frame</span></span>

<span data-ttu-id="cf3cb-262">Pokrewna funkcja języka jest możliwość zadeklarować typ wartości, który musi być ograniczony do pojedynczej ramki stosu.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-262">A related language feature is the ability to declare a value type that must be constrained to a single stack frame.</span></span> <span data-ttu-id="cf3cb-263">To ograniczenie umożliwia kompilatorowi wykonywanie kilku optymalizacji.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-263">This restriction enables the compiler to make several optimizations.</span></span> <span data-ttu-id="cf3cb-264">Podstawowa motywacja tej funkcji była <xref:System.Span%601> i powiązane struktury.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-264">The primary motivation for this feature was <xref:System.Span%601> and related structures.</span></span> <span data-ttu-id="cf3cb-265">Ulepszenia wydajności z tych ulepszeń zostaną osiągnięte przy użyciu nowych i zaktualizowanych interfejsów API platformy .NET, które używają typu <xref:System.Span%601>.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-265">You'll achieve performance improvements from these enhancements by using new and updated .NET APIs that make use of the <xref:System.Span%601> type.</span></span>

<span data-ttu-id="cf3cb-266">Podobne wymagania mogą pracować z pamięcią utworzoną przy użyciu [`stackalloc`](language-reference/operators/stackalloc.md) lub w przypadku korzystania z pamięci z interfejsów API międzyoperacyjności.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-266">You may have similar requirements working with memory created using [`stackalloc`](language-reference/operators/stackalloc.md) or when using memory from interop APIs.</span></span> <span data-ttu-id="cf3cb-267">Dla tych potrzeb można definiować własne typy `ref struct`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-267">You can define your own `ref struct` types for those needs.</span></span>

## <a name="readonly-ref-struct-type"></a><span data-ttu-id="cf3cb-268">Typ `readonly ref struct`</span><span class="sxs-lookup"><span data-stu-id="cf3cb-268">`readonly ref struct` type</span></span>

<span data-ttu-id="cf3cb-269">Deklarowanie struktury jako `readonly ref` łączy zalety i ograniczenia `ref struct` i deklaracji `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-269">Declaring a struct as `readonly ref` combines the benefits and restrictions of `ref struct` and `readonly struct` declarations.</span></span> <span data-ttu-id="cf3cb-270">Pamięć używana przez zakres tylko do odczytu jest ograniczona do pojedynczej ramki stosu i nie można modyfikować pamięci używanej przez zakres tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-270">The memory used by the readonly span is restricted to a single stack frame, and the memory used by the readonly span can't be modified.</span></span>

## <a name="conclusions"></a><span data-ttu-id="cf3cb-271">Wnioski</span><span class="sxs-lookup"><span data-stu-id="cf3cb-271">Conclusions</span></span>

<span data-ttu-id="cf3cb-272">Użycie typów wartości minimalizuje liczbę operacji alokacji:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-272">Using value types minimizes the number of allocation operations:</span></span>

- <span data-ttu-id="cf3cb-273">Magazyn dla typów wartości jest przydzielony na stosy dla zmiennych lokalnych i argumentów metod.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-273">Storage for value types is stack allocated for local variables and method arguments.</span></span>
- <span data-ttu-id="cf3cb-274">Magazyn dla typów wartości, które są elementami członkowskimi innych obiektów, jest przydzielany jako część tego obiektu, a nie jako oddzielna alokacja.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-274">Storage for value types that are members of other objects is allocated as part of that object, not as a separate allocation.</span></span>
- <span data-ttu-id="cf3cb-275">Magazyn dla zwracanych wartości typu wartości jest przydzielony przez stos.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-275">Storage for value type return values is stack allocated.</span></span>

<span data-ttu-id="cf3cb-276">Kontrast, który ma typy referencyjne w tych samych sytuacjach:</span><span class="sxs-lookup"><span data-stu-id="cf3cb-276">Contrast that with reference types in those same situations:</span></span>

- <span data-ttu-id="cf3cb-277">Magazyn dla typów referencyjnych to sterta przypisana do zmiennych lokalnych i argumentów metod.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-277">Storage for reference types are heap allocated for local variables and method arguments.</span></span> <span data-ttu-id="cf3cb-278">Odwołanie jest przechowywane na stosie.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-278">The reference is stored on the stack.</span></span>
- <span data-ttu-id="cf3cb-279">Magazyn dla typów referencyjnych, które są elementami członkowskimi innych obiektów, jest przypisywany osobno na stercie.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-279">Storage for reference types that are members of other objects are separately allocated on the heap.</span></span> <span data-ttu-id="cf3cb-280">Obiekt zawierający zawiera odwołanie.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-280">The containing object stores the reference.</span></span>
- <span data-ttu-id="cf3cb-281">Magazyn dla zwracanych wartości typu referencyjnego to przydzieloną sterty.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-281">Storage for reference type return values is heap allocated.</span></span> <span data-ttu-id="cf3cb-282">Odwołanie do tego magazynu jest przechowywane na stosie.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-282">The reference to that storage is stored on the stack.</span></span>

<span data-ttu-id="cf3cb-283">Minimalizacja alokacji obejmuje kompromisy.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-283">Minimizing allocations comes with tradeoffs.</span></span> <span data-ttu-id="cf3cb-284">Kopiuj więcej pamięci, gdy rozmiar `struct` jest większy niż rozmiar odwołania.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-284">You copy more memory when the size of the `struct` is larger than the size of a reference.</span></span> <span data-ttu-id="cf3cb-285">Odwołanie jest zwykle 64 bitów lub 32 bitów i zależy od procesora maszyny docelowej.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-285">A reference is typically 64 bits or 32 bits, and depends on the target machine CPU.</span></span>

<span data-ttu-id="cf3cb-286">Te kompromisy mają zwykle minimalny wpływ na wydajność.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-286">These tradeoffs generally have minimal performance impact.</span></span> <span data-ttu-id="cf3cb-287">Jednak w przypadku dużych struktur lub większych kolekcji zwiększa się wpływ na wydajność.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-287">However, for large structs or larger collections, the performance impact increases.</span></span> <span data-ttu-id="cf3cb-288">Wpływ może być duży w przypadku ścisłych pętli i ścieżek dla programów.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-288">The impact can be large in tight loops and hot paths for programs.</span></span>

<span data-ttu-id="cf3cb-289">Te ulepszenia C# języka są przeznaczone dla krytycznych algorytmów wydajności, w których Minimalizacja alokacji pamięci jest głównym czynnikiem w celu osiągnięcia wymaganej wydajności.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-289">These enhancements to the C# language are designed for performance critical algorithms where minimizing memory allocations is a major factor in achieving the necessary performance.</span></span> <span data-ttu-id="cf3cb-290">Może się okazać, że nie używasz często tych funkcji w kodzie, który napiszesz.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-290">You may find that you don't often use these features in the code you write.</span></span> <span data-ttu-id="cf3cb-291">Jednak te ulepszenia zostały przyjęte w całym środowisku .NET.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-291">However, these enhancements have been adopted throughout .NET.</span></span> <span data-ttu-id="cf3cb-292">Ponieważ więcej i więcej interfejsów API korzystających z tych funkcji, zobaczysz wydajność aplikacji.</span><span class="sxs-lookup"><span data-stu-id="cf3cb-292">As more and more APIs make use of these features, you'll see the performance of your applications improve.</span></span>

## <a name="see-also"></a><span data-ttu-id="cf3cb-293">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="cf3cb-293">See also</span></span>

- [<span data-ttu-id="cf3cb-294">ref — słowo kluczowe</span><span class="sxs-lookup"><span data-stu-id="cf3cb-294">ref keyword</span></span>](language-reference/keywords/ref.md)
- [<span data-ttu-id="cf3cb-295">Wartości zwracane ref i zmienne lokalne ref</span><span class="sxs-lookup"><span data-stu-id="cf3cb-295">Ref returns and ref locals</span></span>](programming-guide/classes-and-structs/ref-returns.md)
