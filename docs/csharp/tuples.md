---
title: Typy krotek — Przewodnik C#
description: 'Dowiedz się więcej o nazwach i nazwanych typach krotek w C #'
ms.date: 05/15/2018
ms.technology: csharp-fundamentals
ms.assetid: ee8bf7c3-aa3e-4c9e-a5c6-e05cc6138baa
ms.openlocfilehash: 497f95811677c300e1fadad65eb495dced7f2da3
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/04/2020
ms.locfileid: "84374619"
---
# <a name="c-tuple-types"></a><span data-ttu-id="f7e03-103">Typy krotek języka C#</span><span class="sxs-lookup"><span data-stu-id="f7e03-103">C# tuple types</span></span>

<span data-ttu-id="f7e03-104">Krotki języka C# są typami definiowanymi przy użyciu uproszczonej składni.</span><span class="sxs-lookup"><span data-stu-id="f7e03-104">C# tuples are types that you define using a lightweight syntax.</span></span> <span data-ttu-id="f7e03-105">Zalety obejmują prostsze składnię, reguły konwersji na podstawie liczby (nazywanej kardynalnością) i typów elementów oraz spójnych reguł dla kopii, testów równości i przypisań.</span><span class="sxs-lookup"><span data-stu-id="f7e03-105">The advantages include a simpler syntax, rules for conversions based on number (referred to as cardinality) and types of elements, and consistent rules for copies, equality tests, and assignments.</span></span> <span data-ttu-id="f7e03-106">Ze kompromisem, krotki nie obsługują niektórych idiomy zorientowanych obiektowo skojarzonych z dziedziczeniem.</span><span class="sxs-lookup"><span data-stu-id="f7e03-106">As a tradeoff, tuples do not support some of the object-oriented idioms associated with inheritance.</span></span> <span data-ttu-id="f7e03-107">Możesz zapoznać się z omówieniem w sekcji dotyczącej [krotek w artykule Co nowego w języku C# 7,0](whats-new/csharp-7.md#tuples) .</span><span class="sxs-lookup"><span data-stu-id="f7e03-107">You can get an overview in the section on [tuples in the What's new in C# 7.0](whats-new/csharp-7.md#tuples) article.</span></span>

<span data-ttu-id="f7e03-108">W tym artykule poznasz reguły języka dotyczące krotek w języku C# 7,0 i nowszych wersjach, różne sposoby ich używania oraz wstępne wskazówki dotyczące pracy z krotkami.</span><span class="sxs-lookup"><span data-stu-id="f7e03-108">In this article, you'll learn the language rules governing tuples in C# 7.0 and later versions, different ways to use them, and initial guidance on working with tuples.</span></span>

> [!NOTE]
> <span data-ttu-id="f7e03-109">Nowe funkcje krotek wymagają <xref:System.ValueTuple> typów.</span><span class="sxs-lookup"><span data-stu-id="f7e03-109">The new tuples features require the <xref:System.ValueTuple> types.</span></span>
> <span data-ttu-id="f7e03-110">Należy dodać pakiet NuGet, [`System.ValueTuple`](https://www.nuget.org/packages/System.ValueTuple/) Aby można go było używać na platformach, które nie zawierają typów.</span><span class="sxs-lookup"><span data-stu-id="f7e03-110">You must add the NuGet package [`System.ValueTuple`](https://www.nuget.org/packages/System.ValueTuple/) in order to use it on platforms that do not include the types.</span></span>
>
> <span data-ttu-id="f7e03-111">Jest to podobne do innych funkcji języka, które opierają się na typach dostarczonych w środowisku.</span><span class="sxs-lookup"><span data-stu-id="f7e03-111">This is similar to other language features that rely on types delivered in the framework.</span></span> <span data-ttu-id="f7e03-112">Przykłady obejmują `async` i `await` polegają na `INotifyCompletion` interfejsie i korzystaniu z programu LINQ `IEnumerable<T>` .</span><span class="sxs-lookup"><span data-stu-id="f7e03-112">Examples include `async` and `await` relying on the `INotifyCompletion` interface, and LINQ relying on `IEnumerable<T>`.</span></span> <span data-ttu-id="f7e03-113">Jednak mechanizm dostarczania jest zmieniany, ponieważ platforma .NET staje się coraz niezależna od platformy.</span><span class="sxs-lookup"><span data-stu-id="f7e03-113">However, the delivery mechanism is changing as .NET is becoming more platform independent.</span></span> <span data-ttu-id="f7e03-114">.NET Framework może nie zawsze dostarczać na tym samym erze co kompilator języka.</span><span class="sxs-lookup"><span data-stu-id="f7e03-114">The .NET Framework may not always ship on the same cadence as the language compiler.</span></span> <span data-ttu-id="f7e03-115">Gdy nowe funkcje językowe korzystają z nowych typów, te typy będą dostępne jako pakiety NuGet, gdy funkcje języka są dostarczane.</span><span class="sxs-lookup"><span data-stu-id="f7e03-115">When new language features rely on new types, those types will be available as NuGet packages when the language features ship.</span></span> <span data-ttu-id="f7e03-116">Po dodaniu nowych typów do interfejsu API .NET Standard i dostarczeniu ich jako części platformy zostanie usunięte wymaganie pakietu NuGet.</span><span class="sxs-lookup"><span data-stu-id="f7e03-116">As these new types get added to the .NET Standard API and delivered as part of the framework, the NuGet package requirement will be removed.</span></span>

<span data-ttu-id="f7e03-117">Zacznijmy od powodów dodawania nowej obsługi spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="f7e03-117">Let's start with the reasons for adding new tuple support.</span></span> <span data-ttu-id="f7e03-118">Metody zwracają pojedynczy obiekt.</span><span class="sxs-lookup"><span data-stu-id="f7e03-118">Methods return a single object.</span></span> <span data-ttu-id="f7e03-119">Krotki umożliwiają łatwiejsze pakowanie wielu wartości w tym pojedynczym obiekcie.</span><span class="sxs-lookup"><span data-stu-id="f7e03-119">Tuples enable you to package multiple values in that single object more easily.</span></span>

<span data-ttu-id="f7e03-120">.NET Framework ma już klasy ogólne `Tuple` .</span><span class="sxs-lookup"><span data-stu-id="f7e03-120">The .NET Framework already has generic `Tuple` classes.</span></span> <span data-ttu-id="f7e03-121">Jednak te klasy miały dwa zasadnicze ograniczenia.</span><span class="sxs-lookup"><span data-stu-id="f7e03-121">These classes, however, had two major limitations.</span></span> <span data-ttu-id="f7e03-122">Dla jednego, `Tuple` klasy o nazwie ich właściwości `Item1` , itd `Item2` .</span><span class="sxs-lookup"><span data-stu-id="f7e03-122">For one, the `Tuple` classes named their properties `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="f7e03-123">Te nazwy nie zawierają informacji semantycznych.</span><span class="sxs-lookup"><span data-stu-id="f7e03-123">Those names carry no semantic information.</span></span> <span data-ttu-id="f7e03-124">Korzystanie `Tuple` z tych typów nie pozwala na komunikowanie znaczenia każdej z właściwości.</span><span class="sxs-lookup"><span data-stu-id="f7e03-124">Using these `Tuple` types does not enable communicating the meaning of each of the properties.</span></span> <span data-ttu-id="f7e03-125">Nowe funkcje języka umożliwiają deklarowanie i używanie semantycznie zrozumiałych nazw dla elementów w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="f7e03-125">The new language features enable you to declare and use semantically meaningful names for the elements in a tuple.</span></span>

<span data-ttu-id="f7e03-126">`Tuple`Klasy powodują zwiększenie wydajności, ponieważ są to typy odwołań.</span><span class="sxs-lookup"><span data-stu-id="f7e03-126">The `Tuple` classes cause more performance concerns because they are reference types.</span></span> <span data-ttu-id="f7e03-127">Użycie jednego z `Tuple` typów oznacza alokowanie obiektów.</span><span class="sxs-lookup"><span data-stu-id="f7e03-127">Using one of the `Tuple` types means allocating objects.</span></span> <span data-ttu-id="f7e03-128">W przypadku ścieżek aktywnych alokowanie wielu małych obiektów może mieć wymierny wpływ na wydajność aplikacji.</span><span class="sxs-lookup"><span data-stu-id="f7e03-128">On hot paths, allocating many small objects can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="f7e03-129">W związku z tym obsługa wielu krotek wykorzystuje nowe `ValueTuple` struktury.</span><span class="sxs-lookup"><span data-stu-id="f7e03-129">Therefore, the language support for tuples leverages the new `ValueTuple` structs.</span></span>

<span data-ttu-id="f7e03-130">Aby uniknąć tych uchybień, można utworzyć `class` lub, `struct` Aby przenieść wiele elementów.</span><span class="sxs-lookup"><span data-stu-id="f7e03-130">To avoid those deficiencies, you could create a `class` or a `struct` to carry multiple elements.</span></span> <span data-ttu-id="f7e03-131">Niestety, to jeszcze więcej pracy i zasłania zamiar projektowania.</span><span class="sxs-lookup"><span data-stu-id="f7e03-131">Unfortunately, that's more work for you, and it obscures your design intent.</span></span> <span data-ttu-id="f7e03-132">Tworzenie `struct` lub `class` oznacza, że definiujesz typ z danymi i zachowaniem.</span><span class="sxs-lookup"><span data-stu-id="f7e03-132">Making a `struct` or `class` implies that you are defining a type with both data and behavior.</span></span> <span data-ttu-id="f7e03-133">Wiele razy, Wystarczy przechowywać wiele wartości w pojedynczym obiekcie.</span><span class="sxs-lookup"><span data-stu-id="f7e03-133">Many times, you simply want to store multiple values in a single object.</span></span>

<span data-ttu-id="f7e03-134">Funkcje języka i `ValueTuple` struktury ogólne wymuszają zasady, że nie można dodać żadnego zachowania (metod) do tych typów krotek.</span><span class="sxs-lookup"><span data-stu-id="f7e03-134">The language features and the `ValueTuple` generic structs enforce the rule that you cannot add any behavior (methods) to these tuple types.</span></span>
<span data-ttu-id="f7e03-135">Wszystkie `ValueTuple` typy są *niemodyfikowalnymi strukturami*.</span><span class="sxs-lookup"><span data-stu-id="f7e03-135">All the `ValueTuple` types are *mutable structs*.</span></span> <span data-ttu-id="f7e03-136">Każde pole elementu członkowskiego jest polem publicznym.</span><span class="sxs-lookup"><span data-stu-id="f7e03-136">Each member field is a public field.</span></span> <span data-ttu-id="f7e03-137">Dzięki temu są one bardzo lekkie.</span><span class="sxs-lookup"><span data-stu-id="f7e03-137">That makes them very lightweight.</span></span> <span data-ttu-id="f7e03-138">Jednak oznacza to, że nie należy używać krotek, gdzie niezmienności jest ważne.</span><span class="sxs-lookup"><span data-stu-id="f7e03-138">However, that means tuples should not be used where immutability is important.</span></span>

<span data-ttu-id="f7e03-139">Krotki są prostsze i bardziej elastyczne kontenery danych niż `class` i `struct` .</span><span class="sxs-lookup"><span data-stu-id="f7e03-139">Tuples are both simpler and more flexible data containers than `class` and `struct` types.</span></span> <span data-ttu-id="f7e03-140">Zapoznaj się z tymi różnicami.</span><span class="sxs-lookup"><span data-stu-id="f7e03-140">Let's explore those differences.</span></span>

## <a name="named-and-unnamed-tuples"></a><span data-ttu-id="f7e03-141">Krotki nazwane i nienazwane</span><span class="sxs-lookup"><span data-stu-id="f7e03-141">Named and unnamed tuples</span></span>

<span data-ttu-id="f7e03-142">`ValueTuple`Struktura ma pola o nazwach `Item1` , `Item2` , `Item3` i tak dalej, podobnie jak właściwości zdefiniowane w istniejących `Tuple` typach.</span><span class="sxs-lookup"><span data-stu-id="f7e03-142">The `ValueTuple` struct has fields named `Item1`, `Item2`, `Item3`, and so on, similar to the properties defined in the existing `Tuple` types.</span></span>
<span data-ttu-id="f7e03-143">Te nazwy są jedynymi nazwami, których można używać w przypadku *krotek nienazwanych*.</span><span class="sxs-lookup"><span data-stu-id="f7e03-143">These names are the only names you can use for *unnamed tuples*.</span></span> <span data-ttu-id="f7e03-144">Jeśli nie podasz żadnych alternatywnych nazw pól dla krotki, utworzona zostanie nienazwana krotka:</span><span class="sxs-lookup"><span data-stu-id="f7e03-144">When you do not provide any alternative field names to a tuple, you've created an unnamed tuple:</span></span>

[!code-csharp[UnnamedTuple](../../samples/snippets/csharp/tuples/program.cs#01_UnNamedTuple "Unnamed tuple")]

<span data-ttu-id="f7e03-145">Krotka w poprzednim przykładzie została zainicjowana przy użyciu stałych literałów i nie ma nazw elementów utworzonych przy użyciu *projekcji nazw pól krotki* w języku C# 7,1.</span><span class="sxs-lookup"><span data-stu-id="f7e03-145">The tuple in the previous example was initialized using literal constants and won't have element names created using *tuple field name projections* in C# 7.1.</span></span>

<span data-ttu-id="f7e03-146">Jednak po zainicjowaniu krotki można użyć nowych funkcji języka, które zapewniają lepsze nazwy dla każdego pola.</span><span class="sxs-lookup"><span data-stu-id="f7e03-146">However, when you initialize a tuple, you can use new language features that give better names to each field.</span></span> <span data-ttu-id="f7e03-147">Spowoduje to utworzenie *nazwanej krotki*.</span><span class="sxs-lookup"><span data-stu-id="f7e03-147">Doing so creates a *named tuple*.</span></span>
<span data-ttu-id="f7e03-148">Nazwane krotki nadal mają elementy o nazwie `Item1` , `Item2` `Item3` i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="f7e03-148">Named tuples still have elements named `Item1`, `Item2`, `Item3` and so on.</span></span>
<span data-ttu-id="f7e03-149">Ale mają także synonimy dla dowolnego z tych elementów, które mają nazwę.</span><span class="sxs-lookup"><span data-stu-id="f7e03-149">But they also have synonyms for any of those elements that you have named.</span></span>
<span data-ttu-id="f7e03-150">Tworzysz nazwaną krotek przez określenie nazw dla każdego elementu.</span><span class="sxs-lookup"><span data-stu-id="f7e03-150">You create a named tuple by specifying the names for each element.</span></span> <span data-ttu-id="f7e03-151">Jednym ze sposobów jest określenie nazw jako części inicjowania krotki:</span><span class="sxs-lookup"><span data-stu-id="f7e03-151">One way is to specify the names as part of the tuple initialization:</span></span>

[!code-csharp[NamedTuple](../../samples/snippets/csharp/tuples/program.cs#02_NamedTuple "Named tuple")]

<span data-ttu-id="f7e03-152">Te synonimy są obsługiwane przez kompilator i język, dzięki czemu można efektywnie używać nazwanych krotek.</span><span class="sxs-lookup"><span data-stu-id="f7e03-152">These synonyms are handled by the compiler and the language so that you can use named tuples effectively.</span></span> <span data-ttu-id="f7e03-153">Środowisk IDE i redaktorzy mogą odczytywać te nazwy semantyczne przy użyciu interfejsów API Roslyn.</span><span class="sxs-lookup"><span data-stu-id="f7e03-153">IDEs and editors can read these semantic names using the Roslyn APIs.</span></span> <span data-ttu-id="f7e03-154">Można odwoływać się do elementów nazwanych krotek według tych nazw semantycznych w dowolnym miejscu w tym samym zestawie.</span><span class="sxs-lookup"><span data-stu-id="f7e03-154">You can reference the elements of a named tuple by those semantic names anywhere in the same assembly.</span></span> <span data-ttu-id="f7e03-155">Kompilator zastępuje nazwy zdefiniowane przy użyciu `Item*` odpowiedników podczas generowania skompilowanych danych wyjściowych.</span><span class="sxs-lookup"><span data-stu-id="f7e03-155">The compiler replaces the names you've defined with `Item*` equivalents when generating the compiled output.</span></span> <span data-ttu-id="f7e03-156">Skompilowany język pośredni firmy Microsoft (MSIL) nie zawiera nazw, które zostały podane przez Ciebie.</span><span class="sxs-lookup"><span data-stu-id="f7e03-156">The compiled Microsoft Intermediate Language (MSIL) does not include the names you've given these elements.</span></span>

<span data-ttu-id="f7e03-157">Począwszy od języka C# 7,1, nazwy pól dla krotki mogą być dostarczone ze zmiennych używanych do inicjowania krotki.</span><span class="sxs-lookup"><span data-stu-id="f7e03-157">Beginning with C# 7.1, the field names for a tuple may be provided from the variables used to initialize the tuple.</span></span> <span data-ttu-id="f7e03-158">Jest to nazywane **[inicjatorami projekcji krotki](#tuple-projection-initializers)**.</span><span class="sxs-lookup"><span data-stu-id="f7e03-158">This is referred to as **[tuple projection initializers](#tuple-projection-initializers)**.</span></span> <span data-ttu-id="f7e03-159">Poniższy kod tworzy krotkę o nazwie `accumulation` z elementami `count` (liczbą całkowitą) i `sum` (podwójną).</span><span class="sxs-lookup"><span data-stu-id="f7e03-159">The following code creates a tuple named `accumulation` with elements `count` (an integer), and `sum` (a double).</span></span>

[!code-csharp[ProjectedTuple](../../samples/snippets/csharp/tuples/program.cs#ProjectedTupleNames "Named tuple")]

<span data-ttu-id="f7e03-160">Kompilator musi komunikować te nazwy, które zostały utworzone dla krotek, które są zwracane z metod publicznych lub właściwości.</span><span class="sxs-lookup"><span data-stu-id="f7e03-160">The compiler must communicate those names you created for tuples that are returned from public methods or properties.</span></span> <span data-ttu-id="f7e03-161">W takich przypadkach kompilator dodaje <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute> atrybut do metody.</span><span class="sxs-lookup"><span data-stu-id="f7e03-161">In those cases, the compiler adds a <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute> attribute on the method.</span></span> <span data-ttu-id="f7e03-162">Ten atrybut zawiera <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute.TransformNames> Właściwość listy zawierającą nazwy nadawane każdemu z elementów w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="f7e03-162">This attribute contains a <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute.TransformNames> list property that contains the names given to each of the elements in the tuple.</span></span>

> [!NOTE]
> <span data-ttu-id="f7e03-163">Narzędzia programistyczne, takie jak Visual Studio, odczytują również te metadane i udostępniają funkcję IntelliSense i inne funkcje przy użyciu nazw pól metadanych.</span><span class="sxs-lookup"><span data-stu-id="f7e03-163">Development Tools, such as Visual Studio, also read that metadata, and provide IntelliSense and other features using the metadata field names.</span></span>

<span data-ttu-id="f7e03-164">Ważne jest, aby zrozumieć te podstawowe podstawy nowych krotek i `ValueTuple` Typ, aby zrozumieć reguły przypisywania nazwanych spójnych krotek.</span><span class="sxs-lookup"><span data-stu-id="f7e03-164">It is important to understand these underlying fundamentals of the new tuples and the `ValueTuple` type in order to understand the rules for assigning named tuples to each other.</span></span>

## <a name="tuple-projection-initializers"></a><span data-ttu-id="f7e03-165">Inicjatory projekcji krotki</span><span class="sxs-lookup"><span data-stu-id="f7e03-165">Tuple projection initializers</span></span>

<span data-ttu-id="f7e03-166">Ogólnie rzecz biorąc, inicjatory projekcji krotki działają przy użyciu nazw zmiennych lub pól z prawej strony instrukcji inicjowania krotki.</span><span class="sxs-lookup"><span data-stu-id="f7e03-166">In general, tuple projection initializers work by using the variable or field names from the right-hand side of a tuple initialization statement.</span></span>
<span data-ttu-id="f7e03-167">Jeśli podano jawną nazwę, która ma pierwszeństwo przed nazwą przewidywaną.</span><span class="sxs-lookup"><span data-stu-id="f7e03-167">If an explicit name is given, that takes precedence over any projected name.</span></span> <span data-ttu-id="f7e03-168">Na przykład w poniższym inicjatorze elementy to `explicitFieldOne` i `explicitFieldTwo` , nie `localVariableOne` i `localVariableTwo` :</span><span class="sxs-lookup"><span data-stu-id="f7e03-168">For example, in the following initializer, the elements are `explicitFieldOne` and `explicitFieldTwo`, not `localVariableOne` and `localVariableTwo`:</span></span>

[!code-csharp[ExplicitNamedTuple](../../samples/snippets/csharp/tuples/program.cs#ProjectionExample_Explicit "Explicitly named tuple")]

<span data-ttu-id="f7e03-169">Dla każdego pola, w którym nie podano jawnej nazwy, odpowiednia nazwa niejawna jest rzutowana.</span><span class="sxs-lookup"><span data-stu-id="f7e03-169">For any field where an explicit name is not provided, an applicable implicit name is projected.</span></span> <span data-ttu-id="f7e03-170">Nie istnieje wymóg udostępniania nazw semantycznych jawnie lub niejawnie.</span><span class="sxs-lookup"><span data-stu-id="f7e03-170">There is no requirement to provide semantic names, either explicitly or implicitly.</span></span> <span data-ttu-id="f7e03-171">Następujący inicjator ma nazwy pól `Item1` , których wartość jest `42` i `stringContent` , której wartością jest "odpowiedź na wszystko":</span><span class="sxs-lookup"><span data-stu-id="f7e03-171">The following initializer has     field names `Item1`, whose value is `42` and `stringContent`, whose value is "The answer to everything":</span></span>

[!code-csharp[MixedTuple](../../samples/snippets/csharp/tuples/program.cs#MixedTuple "mixed tuple")]

<span data-ttu-id="f7e03-172">Istnieją dwa warunki, w których nazwy pól kandydatów nie są rzutowane na pole krotki:</span><span class="sxs-lookup"><span data-stu-id="f7e03-172">There are two conditions where candidate field names are not projected onto the tuple field:</span></span>

1. <span data-ttu-id="f7e03-173">Nazwa kandydata jest zastrzeżoną nazwą krotki.</span><span class="sxs-lookup"><span data-stu-id="f7e03-173">When the candidate name is a reserved tuple name.</span></span> <span data-ttu-id="f7e03-174">Przykłady obejmują `Item3` , `ToString` , lub `Rest` .</span><span class="sxs-lookup"><span data-stu-id="f7e03-174">Examples include `Item3`, `ToString`, or `Rest`.</span></span>
1. <span data-ttu-id="f7e03-175">Nazwa kandydata jest duplikatem innej nazwy pola krotki, jawnej lub niejawnej.</span><span class="sxs-lookup"><span data-stu-id="f7e03-175">When the candidate name is a duplicate of another tuple field name, either explicit or implicit.</span></span>

<span data-ttu-id="f7e03-176">Te warunki nie zezwalają na niejednoznaczność.</span><span class="sxs-lookup"><span data-stu-id="f7e03-176">These conditions avoid ambiguity.</span></span> <span data-ttu-id="f7e03-177">Te nazwy spowodują niejednoznaczność, jeśli były używane jako nazwy pól dla pola w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="f7e03-177">These names would cause an ambiguity if they were used as the field names for a field in a tuple.</span></span> <span data-ttu-id="f7e03-178">Żaden z tych warunków nie powoduje błędów w czasie kompilacji.</span><span class="sxs-lookup"><span data-stu-id="f7e03-178">Neither of these conditions cause compile-time errors.</span></span> <span data-ttu-id="f7e03-179">Zamiast tego elementy bez przewidywanych nazw nie mają dla nich nazw semantycznych.</span><span class="sxs-lookup"><span data-stu-id="f7e03-179">Instead, the elements without projected names do not have semantic names projected for them.</span></span>  <span data-ttu-id="f7e03-180">Poniższe przykłady przedstawiają następujące warunki:</span><span class="sxs-lookup"><span data-stu-id="f7e03-180">The following examples demonstrate these conditions:</span></span>

[!code-csharp-interactive[Ambiguity](../../samples/snippets/csharp/tuples/program.cs#ProjectionAmbiguities "tuples where projections are not performed")]

<span data-ttu-id="f7e03-181">Te sytuacje nie powodują błędów kompilatora, ponieważ mogą to być istotne zmiany w kodzie zapisanym w języku C# 7,0, gdy projekcje nazw pól krotek nie są dostępne.</span><span class="sxs-lookup"><span data-stu-id="f7e03-181">These situations do not cause compiler errors because that would be a breaking change for code written with C# 7.0, when tuple field name projections were not available.</span></span>

## <a name="equality-and-tuples"></a><span data-ttu-id="f7e03-182">Równość i krotki</span><span class="sxs-lookup"><span data-stu-id="f7e03-182">Equality and tuples</span></span>

<span data-ttu-id="f7e03-183">Począwszy od języka C# 7,3, typy krotek obsługują `==` `!=` Operatory i.</span><span class="sxs-lookup"><span data-stu-id="f7e03-183">Beginning with C# 7.3, tuple types support the `==` and `!=` operators.</span></span> <span data-ttu-id="f7e03-184">Te operatory działają przez porównanie poszczególnych elementów członkowskich argumentu po lewej stronie z każdym elementem członkowskim argumentu w odpowiedniej kolejności.</span><span class="sxs-lookup"><span data-stu-id="f7e03-184">These operators work by comparing each member of the left argument to each member of the right argument in order.</span></span> <span data-ttu-id="f7e03-185">Te porównania są skracane.</span><span class="sxs-lookup"><span data-stu-id="f7e03-185">These comparisons short-circuit.</span></span> <span data-ttu-id="f7e03-186">Przestaną oceniać członków, gdy tylko jedna para nie jest równa.</span><span class="sxs-lookup"><span data-stu-id="f7e03-186">They will stop evaluating members as soon as one pair is not equal.</span></span> <span data-ttu-id="f7e03-187">W poniższym przykładzie kodu użyto `==` , ale wszystkie reguły porównania mają zastosowanie do `!=` .</span><span class="sxs-lookup"><span data-stu-id="f7e03-187">The following code examples use `==`, but the comparison rules all apply to `!=`.</span></span> <span data-ttu-id="f7e03-188">Poniższy przykład kodu pokazuje porównanie równości dla dwóch par liczb całkowitych:</span><span class="sxs-lookup"><span data-stu-id="f7e03-188">The following code example shows an equality comparison for two pairs of integers:</span></span>

[!code-csharp-interactive[TupleEquality](../../samples/snippets/csharp/tuples/program.cs#Equality "Testing tuples for equality")]

<span data-ttu-id="f7e03-189">Istnieje kilka reguł, dzięki którym testy równości spójnej kolekcji są wygodniejsze.</span><span class="sxs-lookup"><span data-stu-id="f7e03-189">There are several rules that make tuple equality tests more convenient.</span></span> <span data-ttu-id="f7e03-190">Równość krotki wykonuje [przekształcenia zniesione](~/_csharplang/spec/conversions.md#lifted-conversion-operators) , jeśli jedna z krotek jest krotką dopuszczającą wartość null, jak pokazano w poniższym kodzie:</span><span class="sxs-lookup"><span data-stu-id="f7e03-190">Tuple equality performs [lifted conversions](~/_csharplang/spec/conversions.md#lifted-conversion-operators) if one of the tuples is a nullable tuple, as shown in the following code:</span></span>

[!code-csharp-interactive[NullableTupleEquality](../../samples/snippets/csharp/tuples/program.cs#NullableEquality "Comparing Tuples and nullable tuples")]

<span data-ttu-id="f7e03-191">Równość krotek również wykonuje konwersje niejawne dla każdego elementu członkowskiego obu krotek.</span><span class="sxs-lookup"><span data-stu-id="f7e03-191">Tuple equality also performs implicit conversions on each member of both tuples.</span></span> <span data-ttu-id="f7e03-192">Obejmują one przenoszone konwersje, konwersje rozszerzające lub inne niejawne konwersje.</span><span class="sxs-lookup"><span data-stu-id="f7e03-192">These include lifted conversions, widening conversions, or other implicit conversions.</span></span> <span data-ttu-id="f7e03-193">Poniższe przykłady pokazują, że liczba całkowita 2-krotka może być porównana z długą 2-krotką ze względu na niejawną konwersję z liczby całkowitej na wartość Long:</span><span class="sxs-lookup"><span data-stu-id="f7e03-193">The following examples show that an integer 2-tuple can be compared to a long 2-tuple because of the implicit conversion from integer to long:</span></span>

[!code-csharp-interactive[SnippetMemberConversions](../../samples/snippets/csharp/tuples/program.cs#SnippetMemberConversions "converting tuples for equality tests")]

<span data-ttu-id="f7e03-194">Nazwy składowych krotek nie uczestniczą w testach pod kątem równości.</span><span class="sxs-lookup"><span data-stu-id="f7e03-194">The names of the tuple members do not participate in tests for equality.</span></span> <span data-ttu-id="f7e03-195">Jeśli jednak jeden z operandów jest literałem krotki z jawnymi nazwami, kompilator generuje ostrzeżenie CS8383, jeśli te nazwy nie pasują do nazw innych operandów.</span><span class="sxs-lookup"><span data-stu-id="f7e03-195">However, if one of the operands is a tuple literal with explicit names, the compiler generates warning CS8383 if those names do not match the names of the other operand.</span></span>
<span data-ttu-id="f7e03-196">W przypadku, gdy oba operandy są literałami krotki, ostrzeżenie znajduje się na prawym operandzie, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="f7e03-196">In the case where both operands are tuple literals, the warning is on the right operand as shown in the following example:</span></span>

[!code-csharp-interactive[MemberNames](../../samples/snippets/csharp/tuples/program.cs#SnippetMemberNames "Tuple member names do not participate in equality tests")]

<span data-ttu-id="f7e03-197">Na koniec krotki mogą zawierać krotki zagnieżdżone.</span><span class="sxs-lookup"><span data-stu-id="f7e03-197">Finally, tuples may contain nested tuples.</span></span> <span data-ttu-id="f7e03-198">Równość krotki porównuje "kształt" każdego operandu za pomocą krotek zagnieżdżonych, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="f7e03-198">Tuple equality compares the "shape" of each operand through nested tuples as shown in the following example:</span></span>

[!code-csharp-interactive[NestedTuples](../../samples/snippets/csharp/tuples/program.cs#SnippetNestedTuples "Tuples may contain nested tuples that participate in tuple equality.")]

<span data-ttu-id="f7e03-199">Jest to błąd czasu kompilacji służący do porównywania dwóch krotek w celu równości (lub nierówności), gdy mają różne kształty.</span><span class="sxs-lookup"><span data-stu-id="f7e03-199">It's a compile time error to compare two tuples for equality (or inequality) when they have different shapes.</span></span> <span data-ttu-id="f7e03-200">Kompilator nie będzie podejmować próby odbudowy krotek zagnieżdżonych, aby je porównać.</span><span class="sxs-lookup"><span data-stu-id="f7e03-200">The compiler won't attempt any deconstruction of nested tuples in order to compare them.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="f7e03-201">Przypisanie i krotki</span><span class="sxs-lookup"><span data-stu-id="f7e03-201">Assignment and tuples</span></span>

<span data-ttu-id="f7e03-202">Język obsługuje przypisanie między typami krotek, które mają taką samą liczbę elementów, gdzie każdy element po prawej stronie może być niejawnie konwertowany do odpowiadającego mu elementu po lewej stronie.</span><span class="sxs-lookup"><span data-stu-id="f7e03-202">The language supports assignment between tuple types that have the same number of elements, where each right-hand side element can be implicitly converted to its corresponding left-hand side element.</span></span> <span data-ttu-id="f7e03-203">Inne konwersje nie są brane pod uwagę w przypisaniach.</span><span class="sxs-lookup"><span data-stu-id="f7e03-203">Other conversions aren't considered for assignments.</span></span> <span data-ttu-id="f7e03-204">Jest to błąd czasu kompilacji, aby przypisać jedną krotkę do innej, gdy mają różne kształty.</span><span class="sxs-lookup"><span data-stu-id="f7e03-204">It's a compile time error to assign one tuple to another when they have different shapes.</span></span> <span data-ttu-id="f7e03-205">Kompilator nie będzie podejmować próby odbudowy krotek zagnieżdżonych, aby je przypisać.</span><span class="sxs-lookup"><span data-stu-id="f7e03-205">The compiler won't attempt any deconstruction of nested tuples in order to assign them.</span></span>
<span data-ttu-id="f7e03-206">Przyjrzyjmy się typom przypisań, które są dozwolone między typami krotek.</span><span class="sxs-lookup"><span data-stu-id="f7e03-206">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="f7e03-207">Należy wziąć pod uwagę te zmienne, które są używane w następujących przykładach:</span><span class="sxs-lookup"><span data-stu-id="f7e03-207">Consider these variables used in the following examples:</span></span>

[!code-csharp[VariableCreation](../../samples/snippets/csharp/tuples/program.cs#03_VariableCreation "Variable creation")]

<span data-ttu-id="f7e03-208">Pierwsze dwie zmienne `unnamed` i `anonymous` nie mają nazw semantycznych dla elementów.</span><span class="sxs-lookup"><span data-stu-id="f7e03-208">The first two variables, `unnamed` and `anonymous` do not have semantic names provided for the elements.</span></span> <span data-ttu-id="f7e03-209">Nazwy pól to `Item1` i `Item2` .</span><span class="sxs-lookup"><span data-stu-id="f7e03-209">The field names are `Item1` and `Item2`.</span></span>
<span data-ttu-id="f7e03-210">Ostatnie dwie zmienne `named` i `differentName` mają nazwy semantyczne podane dla elementów.</span><span class="sxs-lookup"><span data-stu-id="f7e03-210">The last two variables, `named` and `differentName` have semantic names given for the elements.</span></span> <span data-ttu-id="f7e03-211">Te dwie krotki mają różne nazwy dla elementów.</span><span class="sxs-lookup"><span data-stu-id="f7e03-211">These two tuples have different names for the elements.</span></span>

<span data-ttu-id="f7e03-212">Wszystkie cztery z tych krotek mają tę samą liczbę elementów (określaną jako Kardynalność) i typy tych elementów są identyczne.</span><span class="sxs-lookup"><span data-stu-id="f7e03-212">All four of these tuples have the same number of elements (referred to as 'cardinality') and the types of those elements are identical.</span></span> <span data-ttu-id="f7e03-213">W związku z tym wszystkie te przypisania działają:</span><span class="sxs-lookup"><span data-stu-id="f7e03-213">Therefore, all of these assignments work:</span></span>

[!code-csharp[VariableAssignment](../../samples/snippets/csharp/tuples/program.cs#04_VariableAssignment "Variable assignment")]

<span data-ttu-id="f7e03-214">Zauważ, że nazwy krotek nie są przypisane.</span><span class="sxs-lookup"><span data-stu-id="f7e03-214">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="f7e03-215">Wartości elementów są przypisywane po kolejności elementów w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="f7e03-215">The values of the elements are assigned following the order of the elements in the tuple.</span></span>

<span data-ttu-id="f7e03-216">Nie można przypisać krotek o różnych typach lub liczbie elementów:</span><span class="sxs-lookup"><span data-stu-id="f7e03-216">Tuples of different types or numbers of elements are not assignable:</span></span>

```csharp
// Does not compile.
// CS0029: Cannot assign Tuple(int,int,int) to Tuple(int, string)
var differentShape = (1, 2, 3);
named = differentShape;
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="f7e03-217">Krotki jako wartości zwracane przez metodę</span><span class="sxs-lookup"><span data-stu-id="f7e03-217">Tuples as method return values</span></span>

<span data-ttu-id="f7e03-218">Jednym z najpopularniejszych zastosowania krotek jest jako wartość zwracana przez metodę.</span><span class="sxs-lookup"><span data-stu-id="f7e03-218">One of the most common uses for tuples is as a method return value.</span></span> <span data-ttu-id="f7e03-219">Przejdźmy do jednego przykładu.</span><span class="sxs-lookup"><span data-stu-id="f7e03-219">Let's walk through one example.</span></span> <span data-ttu-id="f7e03-220">Należy wziąć pod uwagę tę metodę, która oblicza odchylenie standardowe dla sekwencji liczb:</span><span class="sxs-lookup"><span data-stu-id="f7e03-220">Consider this method that computes the standard deviation for a sequence of numbers:</span></span>

[!code-csharp[StandardDeviation](../../samples/snippets/csharp/tuples/statistics.cs#05_StandardDeviation "Compute Standard Deviation")]

> [!NOTE]
> <span data-ttu-id="f7e03-221">Te przykłady obliczają Niepoprawione odchylenie standardowe próbki.</span><span class="sxs-lookup"><span data-stu-id="f7e03-221">These examples compute the uncorrected sample standard deviation.</span></span>
> <span data-ttu-id="f7e03-222">Skorygowana formuła przykładowego odchylenia standardowego podzieli sumę kwadratowych różnic od średniej przez (N-1), a nie N, jako `Average` metodę rozszerzenia.</span><span class="sxs-lookup"><span data-stu-id="f7e03-222">The corrected sample standard deviation formula would divide the sum of the squared differences from the mean by (N-1) instead of N, as the `Average` extension method does.</span></span> <span data-ttu-id="f7e03-223">Zapoznaj się z tekstem statystyk, aby uzyskać szczegółowe informacje o różnicach między tymi formułami dotyczącymi odchylenia standardowego.</span><span class="sxs-lookup"><span data-stu-id="f7e03-223">Consult a statistics text for more details on the differences between these formulas for standard deviation.</span></span>

<span data-ttu-id="f7e03-224">Poprzedni kod jest zgodny z formułą Textbook dla odchylenia standardowego.</span><span class="sxs-lookup"><span data-stu-id="f7e03-224">The preceding code follows the textbook formula for the standard deviation.</span></span> <span data-ttu-id="f7e03-225">Tworzy poprawną odpowiedź, ale jest nieefektywną implementacją.</span><span class="sxs-lookup"><span data-stu-id="f7e03-225">It produces the correct answer, but it's an inefficient implementation.</span></span> <span data-ttu-id="f7e03-226">Ta metoda wylicza dwa razy sekwencję: raz, aby utworzyć średnią, i raz, aby utworzyć średnią kwadratu różnicy średniej.</span><span class="sxs-lookup"><span data-stu-id="f7e03-226">This method enumerates the sequence twice: Once to produce the average, and once to produce the average of the square of the difference of the average.</span></span>
<span data-ttu-id="f7e03-227">(Należy pamiętać, że zapytania LINQ są oceniane opóźnieniem, więc obliczenie różnic między średnią i średnią z tych różnic powoduje tylko jedno Wyliczenie).</span><span class="sxs-lookup"><span data-stu-id="f7e03-227">(Remember that LINQ queries are evaluated lazily, so the computation of the differences from the mean and the average of those differences makes only one enumeration.)</span></span>

<span data-ttu-id="f7e03-228">Istnieje alternatywna formuła, która oblicza odchylenie standardowe przy użyciu tylko jednego wyliczenia sekwencji.</span><span class="sxs-lookup"><span data-stu-id="f7e03-228">There is an alternative formula that computes standard deviation using only one enumeration of the sequence.</span></span>  <span data-ttu-id="f7e03-229">To obliczenie generuje dwie wartości, ponieważ wylicza sekwencję: sumę wszystkich elementów w sekwencji i sumę każdej wartości kwadratowej:</span><span class="sxs-lookup"><span data-stu-id="f7e03-229">This computation produces two values as it enumerates the sequence: the sum of all items in the sequence, and the sum of the each value squared:</span></span>

[!code-csharp[SumOfSquaresFormula](../../samples/snippets/csharp/tuples/statistics.cs#06_SumOfSquaresFormula "Compute Standard Deviation using the sum of squares")]

<span data-ttu-id="f7e03-230">Ta wersja Wylicza sekwencję dokładnie raz.</span><span class="sxs-lookup"><span data-stu-id="f7e03-230">This version enumerates the sequence exactly once.</span></span> <span data-ttu-id="f7e03-231">Ale nie jest to kod wielokrotnego użytku.</span><span class="sxs-lookup"><span data-stu-id="f7e03-231">But it's not reusable code.</span></span> <span data-ttu-id="f7e03-232">W trakcie pracy można się dowiedzieć, że wiele różnych obliczeń statystycznych używa liczby elementów w sekwencji, sumy sekwencji i sumy kwadratów sekwencji.</span><span class="sxs-lookup"><span data-stu-id="f7e03-232">As you keep working, you'll find that many different statistical computations use the number of items in the sequence, the sum of the sequence, and the sum of the squares of the sequence.</span></span> <span data-ttu-id="f7e03-233">Refaktoryzacjmy metodę i piszą metodę narzędzia, która produkuje wszystkie trzy wartości.</span><span class="sxs-lookup"><span data-stu-id="f7e03-233">Let's refactor this method and write a utility method that produces all three of those values.</span></span> <span data-ttu-id="f7e03-234">Wszystkie trzy wartości mogą być zwracane jako krotki.</span><span class="sxs-lookup"><span data-stu-id="f7e03-234">All three values can be returned as a tuple.</span></span>

<span data-ttu-id="f7e03-235">Zaktualizujmy tę metodę, tak aby trzy wartości obliczane podczas wyliczania były przechowywane w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="f7e03-235">Let's update this method so the three values computed during the enumeration are stored in a tuple.</span></span> <span data-ttu-id="f7e03-236">Tworzy tę wersję:</span><span class="sxs-lookup"><span data-stu-id="f7e03-236">That creates this version:</span></span>

[!code-csharp[TupleVersion](../../samples/snippets/csharp/tuples/statistics.cs#07_TupleVersion "Refactor to use tuples")]

<span data-ttu-id="f7e03-237">Obsługa refaktoryzacji programu Visual Studio ułatwia wyodrębnienie funkcji podstawowych statystyk do metody prywatnej.</span><span class="sxs-lookup"><span data-stu-id="f7e03-237">Visual Studio's Refactoring support makes it easy to extract the functionality for the core statistics into a private method.</span></span> <span data-ttu-id="f7e03-238">Zapewnia `private static` metodę, która zwraca typ krotki z trzema wartościami `Sum` , `SumOfSquares` i `Count` :</span><span class="sxs-lookup"><span data-stu-id="f7e03-238">That gives you a `private static` method that returns the tuple type with the three values of `Sum`, `SumOfSquares`, and `Count`:</span></span>

[!code-csharp[TupleMethodVersion](../../samples/snippets/csharp/tuples/statistics.cs#08_TupleMethodVersion "After extracting utility method")]

<span data-ttu-id="f7e03-239">Język umożliwia korzystanie z kilku opcji, których można użyć, jeśli chcesz zrobić kilka szybkiej edycji ręcznie.</span><span class="sxs-lookup"><span data-stu-id="f7e03-239">The language enables a couple more options that you can use, if you want to make a few quick edits by hand.</span></span> <span data-ttu-id="f7e03-240">Najpierw można użyć `var` deklaracji do zainicjowania wyniku krotki z `ComputeSumAndSumOfSquares` wywołania metody.</span><span class="sxs-lookup"><span data-stu-id="f7e03-240">First, you can use the `var` declaration to initialize the tuple result from the `ComputeSumAndSumOfSquares` method call.</span></span> <span data-ttu-id="f7e03-241">Możesz również utworzyć trzy zmienne dyskretne wewnątrz `ComputeSumAndSumOfSquares` metody.</span><span class="sxs-lookup"><span data-stu-id="f7e03-241">You can also create three discrete variables inside the `ComputeSumAndSumOfSquares` method.</span></span> <span data-ttu-id="f7e03-242">Wersja końcowa jest pokazana w poniższym kodzie:</span><span class="sxs-lookup"><span data-stu-id="f7e03-242">The final version is shown in the following code:</span></span>

[!code-csharp[CleanedTupleVersion](../../samples/snippets/csharp/tuples/statistics.cs#09_CleanedTupleVersion "After final cleanup")]

<span data-ttu-id="f7e03-243">Tej ostatecznej wersji można użyć dla każdej metody, która wymaga tych trzech wartości lub dowolnego podzbioru.</span><span class="sxs-lookup"><span data-stu-id="f7e03-243">This final version can be used for any method that needs those three values, or any subset of them.</span></span>

<span data-ttu-id="f7e03-244">Język obsługuje inne opcje zarządzania nazwami elementów w tych metodach zwracających spójność krotek.</span><span class="sxs-lookup"><span data-stu-id="f7e03-244">The language supports other options in managing the names of the elements in these tuple-returning methods.</span></span>

<span data-ttu-id="f7e03-245">Można usunąć nazwy pól z deklaracji wartości zwracanej i zwrócić spójną krotkę:</span><span class="sxs-lookup"><span data-stu-id="f7e03-245">You can remove the field names from the return value declaration and return an unnamed tuple:</span></span>

```csharp
private static (double, double, int) ComputeSumAndSumOfSquares(IEnumerable<double> sequence)
{
    double sum = 0;
    double sumOfSquares = 0;
    int count = 0;

    foreach (var item in sequence)
    {
        count++;
        sum += item;
        sumOfSquares += item * item;
    }

    return (sum, sumOfSquares, count);
}
```

<span data-ttu-id="f7e03-246">Pola tej krotki mają nazwy `Item1` , `Item2` , i `Item3` .</span><span class="sxs-lookup"><span data-stu-id="f7e03-246">The fields of this tuple are named `Item1`, `Item2`, and `Item3`.</span></span>
<span data-ttu-id="f7e03-247">Zaleca się dostarczanie nazw semantycznych do elementów krotek zwracanych z metod.</span><span class="sxs-lookup"><span data-stu-id="f7e03-247">It's recommended that you provide semantic names to the elements of tuples returned from methods.</span></span>

<span data-ttu-id="f7e03-248">Inny idiom, gdzie krotke mogą być przydatne podczas tworzenia zapytań LINQ.</span><span class="sxs-lookup"><span data-stu-id="f7e03-248">Another idiom where tuples can be useful is when you are authoring LINQ queries.</span></span> <span data-ttu-id="f7e03-249">Ostatecznie przewidywany wynik często zawiera niektóre z właściwości wybranych obiektów, ale nie wszystkie.</span><span class="sxs-lookup"><span data-stu-id="f7e03-249">The final projected result often contains some, but not all, of the properties of the objects being selected.</span></span>

<span data-ttu-id="f7e03-250">Tradycyjnie można projektować wyniki zapytania w sekwencji obiektów, które były typu anonimowego.</span><span class="sxs-lookup"><span data-stu-id="f7e03-250">You would traditionally project the results of the query into a sequence of objects that were an anonymous type.</span></span> <span data-ttu-id="f7e03-251">Przedstawiamy wiele ograniczeń, głównie ponieważ typy anonimowe nie mogą wygodnie nazywać się w zwracanym typie dla metody.</span><span class="sxs-lookup"><span data-stu-id="f7e03-251">That presented many limitations, primarily because anonymous types could not conveniently be named in the return type for a method.</span></span> <span data-ttu-id="f7e03-252">Alternatywa przy użyciu `object` lub `dynamic` jako typ wyniku ma znaczny koszt wydajności.</span><span class="sxs-lookup"><span data-stu-id="f7e03-252">Alternatives using `object` or `dynamic` as the type of the result came with significant performance costs.</span></span>

<span data-ttu-id="f7e03-253">Zwracanie sekwencji typu krotki jest proste, a nazwy i typy elementów są dostępne w czasie kompilacji i za pomocą narzędzi IDE.</span><span class="sxs-lookup"><span data-stu-id="f7e03-253">Returning a sequence of a tuple type is easy, and the names and types of the elements are available at compile time and through IDE tools.</span></span>
<span data-ttu-id="f7e03-254">Rozważmy na przykład aplikację do zrobienia.</span><span class="sxs-lookup"><span data-stu-id="f7e03-254">For example, consider a ToDo application.</span></span> <span data-ttu-id="f7e03-255">Można zdefiniować klasę podobną do następującej, aby reprezentować pojedynczy wpis na liście zadań do wykonania:</span><span class="sxs-lookup"><span data-stu-id="f7e03-255">You might define a class similar to the following to represent a single entry in the ToDo list:</span></span>

[!code-csharp[ToDoItem](../../samples/snippets/csharp/tuples/projectionsample.cs#14_ToDoItem "To Do Item")]

<span data-ttu-id="f7e03-256">Aplikacje mobilne mogą obsługiwać zwartą postać bieżących elementów do wykonania, które wyświetlają tylko tytuł.</span><span class="sxs-lookup"><span data-stu-id="f7e03-256">Your mobile applications may support a compact form of the current ToDo items that only displays the title.</span></span> <span data-ttu-id="f7e03-257">To zapytanie LINQ spowodowałoby projekcję zawierającą tylko identyfikator i tytuł.</span><span class="sxs-lookup"><span data-stu-id="f7e03-257">That LINQ query would make a projection that includes only the ID and the title.</span></span> <span data-ttu-id="f7e03-258">Metoda zwracająca sekwencję krotek powoduje, że projekt jest dobrze:</span><span class="sxs-lookup"><span data-stu-id="f7e03-258">A method that returns a sequence of tuples expresses that design well:</span></span>

[!code-csharp[QueryReturningTuple](../../samples/snippets/csharp/tuples/projectionsample.cs#15_QueryReturningTuple "Query returning a tuple")]

> [!NOTE]
> <span data-ttu-id="f7e03-259">W języku C# 7,1, projekcje krotek umożliwiają tworzenie nazwanych krotek przy użyciu elementów w sposób podobny do nazw właściwości w typach anonimowych.</span><span class="sxs-lookup"><span data-stu-id="f7e03-259">In C# 7.1, tuple projections enable you to create named tuples using elements, in a manner similar to the property naming in anonymous types.</span></span> <span data-ttu-id="f7e03-260">W powyższym kodzie, `select` instrukcja w projekcji zapytania tworzy krotkę z elementami `ID` i `Title` .</span><span class="sxs-lookup"><span data-stu-id="f7e03-260">In the above code, the `select` statement in the query projection creates a tuple that has elements `ID` and `Title`.</span></span>

<span data-ttu-id="f7e03-261">Nazwana krotka może być częścią podpisu.</span><span class="sxs-lookup"><span data-stu-id="f7e03-261">The named tuple can be part of the signature.</span></span> <span data-ttu-id="f7e03-262">Pozwala ona narzędziom kompilatora i IDE zapewnić statyczne sprawdzanie, czy wynik jest prawidłowo używany.</span><span class="sxs-lookup"><span data-stu-id="f7e03-262">It lets the compiler and IDE tools provide static checking that you are using the result correctly.</span></span> <span data-ttu-id="f7e03-263">Nazwana krotka również zawiera informacje o typie statycznym, więc nie ma potrzeby używania kosztownych funkcji czasu wykonywania, takich jak odbicie lub dynamiczne powiązanie, aby pracować z wynikami.</span><span class="sxs-lookup"><span data-stu-id="f7e03-263">The named tuple also carries the static type information so there is no need to use expensive run time features like reflection or dynamic binding to work with the results.</span></span>

## <a name="deconstruction"></a><span data-ttu-id="f7e03-264">Dekonstrukcja</span><span class="sxs-lookup"><span data-stu-id="f7e03-264">Deconstruction</span></span>

<span data-ttu-id="f7e03-265">Wszystkie elementy w spójnej kolekcji można rozpakować przez *odbudowę* krotki zwracanej przez metodę.</span><span class="sxs-lookup"><span data-stu-id="f7e03-265">You can unpackage all the items in a tuple by *deconstructing* the tuple returned by a method.</span></span> <span data-ttu-id="f7e03-266">Istnieją trzy różne podejścia do dekonstrukcji krotek.</span><span class="sxs-lookup"><span data-stu-id="f7e03-266">There are three different approaches to deconstructing tuples.</span></span>  <span data-ttu-id="f7e03-267">Najpierw można jawnie zadeklarować typ każdego pola wewnątrz nawiasów, aby utworzyć zmienne dyskretne dla każdego elementu w spójnej kolekcji:</span><span class="sxs-lookup"><span data-stu-id="f7e03-267">First, you can explicitly declare the type of each field inside parentheses to create discrete variables for each of the elements in the tuple:</span></span>

[!code-csharp[Deconstruct](../../samples/snippets/csharp/tuples/statistics.cs#10_Deconstruct "Deconstruct")]

<span data-ttu-id="f7e03-268">Można także zadeklarować niejawnie wpisane zmienne dla każdego pola w krotki przy użyciu `var` słowa kluczowego poza nawiasami:</span><span class="sxs-lookup"><span data-stu-id="f7e03-268">You can also declare implicitly typed variables for each field in a tuple by using the `var` keyword outside the parentheses:</span></span>

[!code-csharp[DeconstructToVar](../../samples/snippets/csharp/tuples/statistics.cs#11_DeconstructToVar "Deconstruct to Var")]

<span data-ttu-id="f7e03-269">Istnieje również możliwość użycia `var` słowa kluczowego z dowolnymi lub wszystkimi deklaracjami zmiennych w nawiasach.</span><span class="sxs-lookup"><span data-stu-id="f7e03-269">It is also legal to use the `var` keyword with any, or all of the variable declarations inside the parentheses.</span></span>

```csharp
(double sum, var sumOfSquares, var count) = ComputeSumAndSumOfSquares(sequence);
```

<span data-ttu-id="f7e03-270">Nie można użyć określonego typu poza nawiasami, nawet jeśli każde pole w krotek ma ten sam typ.</span><span class="sxs-lookup"><span data-stu-id="f7e03-270">You cannot use a specific type outside the parentheses, even if every field in the tuple has the same type.</span></span>

<span data-ttu-id="f7e03-271">Można także rozbudowy krotek z istniejącymi deklaracjami:</span><span class="sxs-lookup"><span data-stu-id="f7e03-271">You can deconstruct tuples with existing declarations as well:</span></span>

```csharp
public class Point
{
    public int X { get; set; }
    public int Y { get; set; }

    public Point(int x, int y) => (X, Y) = (x, y);
}
```

> [!WARNING]
> <span data-ttu-id="f7e03-272">Nie można mieszać istniejących deklaracji z deklaracjami wewnątrz nawiasów.</span><span class="sxs-lookup"><span data-stu-id="f7e03-272">You cannot mix existing declarations with declarations inside the parentheses.</span></span> <span data-ttu-id="f7e03-273">Na przykład następujące elementy są niedozwolone: `(var x, y) = MyMethod();` .</span><span class="sxs-lookup"><span data-stu-id="f7e03-273">For instance, the following is not allowed: `(var x, y) = MyMethod();`.</span></span> <span data-ttu-id="f7e03-274">Powoduje to CS8184 błędu, ponieważ *x* jest zadeklarowany wewnątrz nawiasów, a *y* jest poprzednio zadeklarowany w innym miejscu.</span><span class="sxs-lookup"><span data-stu-id="f7e03-274">This produces error CS8184 because *x* is declared inside the parentheses and *y* is previously declared elsewhere.</span></span>

### <a name="deconstructing-user-defined-types"></a><span data-ttu-id="f7e03-275">Dekonstrukcja typów zdefiniowanych przez użytkownika</span><span class="sxs-lookup"><span data-stu-id="f7e03-275">Deconstructing user-defined types</span></span>

<span data-ttu-id="f7e03-276">Każdy typ krotki można rozbudować, jak pokazano powyżej.</span><span class="sxs-lookup"><span data-stu-id="f7e03-276">Any tuple type can be deconstructed as shown above.</span></span> <span data-ttu-id="f7e03-277">Można również łatwo włączyć dekonstrukcja na dowolnym typie zdefiniowanym przez użytkownika (klasy, struktury, a nawet interfejsy).</span><span class="sxs-lookup"><span data-stu-id="f7e03-277">It's also easy to enable deconstruction on any user-defined type (classes, structs, or even interfaces).</span></span>

<span data-ttu-id="f7e03-278">Autor typu może zdefiniować jedną lub więcej `Deconstruct` metod, które przypisują wartości do dowolnej liczby [ `out` zmiennych](language-reference/keywords/out-parameter-modifier.md) reprezentujących elementy danych, które składają się na typ.</span><span class="sxs-lookup"><span data-stu-id="f7e03-278">The type author can define one or more `Deconstruct` methods that assign values to any number of [`out` variables](language-reference/keywords/out-parameter-modifier.md) representing the data elements that make up the type.</span></span> <span data-ttu-id="f7e03-279">Na przykład następujący `Person` Typ definiuje `Deconstruct` metodę, która dekonstruuje obiekt osoby do elementów reprezentujących imię i nazwisko:</span><span class="sxs-lookup"><span data-stu-id="f7e03-279">For example, the following `Person` type defines a `Deconstruct` method that deconstructs a person object into the elements representing the first name and last name:</span></span>

[!code-csharp[TypeWithDeconstructMethod](../../samples/snippets/csharp/tuples/person.cs#12_TypeWithDeconstructMethod "Type with a deconstruct method")]

<span data-ttu-id="f7e03-280">Metoda dekonstrukcja umożliwia przypisanie od a `Person` do dwóch ciągów reprezentujących `FirstName` `LastName` właściwości i:</span><span class="sxs-lookup"><span data-stu-id="f7e03-280">The deconstruct method enables assignment from a `Person` to two strings, representing the `FirstName` and `LastName` properties:</span></span>

[!code-csharp[Deconstruct Type](../../samples/snippets/csharp/tuples/program.cs#12A_DeconstructType "Deconstruct a class type")]

<span data-ttu-id="f7e03-281">Można włączyć dekonstrukcja nawet dla typów, które nie zostały utworzone.</span><span class="sxs-lookup"><span data-stu-id="f7e03-281">You can enable deconstruction even for types you did not author.</span></span>
<span data-ttu-id="f7e03-282">`Deconstruct`Metoda może być metodą rozszerzenia, która rozpakuje dostępne elementy członkowskie danych obiektu.</span><span class="sxs-lookup"><span data-stu-id="f7e03-282">The `Deconstruct` method can be an extension method that unpackages the accessible data members of an object.</span></span> <span data-ttu-id="f7e03-283">W poniższym przykładzie przedstawiono `Student` Typ, pochodny od `Person` typu, a metoda rozszerzająca, która dekonstrukcjauje `Student` do trzech zmiennych reprezentujących,, `FirstName` `LastName` i `GPA` :</span><span class="sxs-lookup"><span data-stu-id="f7e03-283">The example below shows a `Student` type, derived from the `Person` type, and an extension method that deconstructs a `Student` into three variables, representing the `FirstName`, the `LastName`, and the `GPA`:</span></span>

[!code-csharp[ExtensionDeconstructMethod](../../samples/snippets/csharp/tuples/person.cs#13_ExtensionDeconstructMethod "Type with a deconstruct extension method")]

<span data-ttu-id="f7e03-284">`Student`Obiekt ma teraz dwie dostępne `Deconstruct` metody: Metoda rozszerzająca zadeklarowana dla `Student` typów i składowej `Person` typu.</span><span class="sxs-lookup"><span data-stu-id="f7e03-284">A `Student` object now has two accessible `Deconstruct` methods: the extension method declared for `Student` types, and the member of the `Person` type.</span></span> <span data-ttu-id="f7e03-285">Oba znajdują się w zakresie, co umożliwia `Student` rozbudowanie na dwie zmienne lub trzy.</span><span class="sxs-lookup"><span data-stu-id="f7e03-285">Both are in scope, and that enables a `Student` to be deconstructed into either two variables or three.</span></span>
<span data-ttu-id="f7e03-286">Jeśli student zostanie przypisany do trzech zmiennych, zwracane są imiona, nazwisko, nazwisko i GPA.</span><span class="sxs-lookup"><span data-stu-id="f7e03-286">If you assign a student to three variables, the first name, last name, and GPA are all returned.</span></span> <span data-ttu-id="f7e03-287">Jeśli student zostanie przypisany do dwóch zmiennych, zwracane są tylko imię i nazwisko.</span><span class="sxs-lookup"><span data-stu-id="f7e03-287">If you assign a student to two variables, only the first name and the last name are returned.</span></span>

[!code-csharp[Deconstruct extension method](../../samples/snippets/csharp/tuples/program.cs#13A_DeconstructExtension "Deconstruct a class type using an extension method")]

<span data-ttu-id="f7e03-288">Należy ostrożnie definiować wiele `Deconstruct` metod w klasie lub hierarchii klas.</span><span class="sxs-lookup"><span data-stu-id="f7e03-288">You should be careful defining multiple `Deconstruct` methods in a class or a class hierarchy.</span></span> <span data-ttu-id="f7e03-289">Wiele `Deconstruct` metod, które mają taką samą liczbę `out` parametrów, może szybko spowodować niejasności.</span><span class="sxs-lookup"><span data-stu-id="f7e03-289">Multiple `Deconstruct` methods that have the same number of `out` parameters can quickly cause ambiguities.</span></span> <span data-ttu-id="f7e03-290">Obiekty wywołujące mogą nie być w stanie łatwo wywołać żądanej `Deconstruct` metody.</span><span class="sxs-lookup"><span data-stu-id="f7e03-290">Callers may not be able to easily call the desired `Deconstruct` method.</span></span>

<span data-ttu-id="f7e03-291">W tym przykładzie istnieje minimalna szansa na niejednoznaczne wywołanie, ponieważ `Deconstruct` Metoda `Person` ma dwa parametry wyjściowe, a `Deconstruct` Metoda dla `Student` ma wartość trzy.</span><span class="sxs-lookup"><span data-stu-id="f7e03-291">In this example, there is minimal chance for an ambiguous call because the `Deconstruct` method for `Person` has two output parameters, and the `Deconstruct` method for `Student` has three.</span></span>

<span data-ttu-id="f7e03-292">Operatory dekonstrukcji nie uczestniczą w testowaniu równości.</span><span class="sxs-lookup"><span data-stu-id="f7e03-292">Deconstruction operators do not participate in testing equality.</span></span> <span data-ttu-id="f7e03-293">Poniższy przykład generuje błąd kompilatora CS0019:</span><span class="sxs-lookup"><span data-stu-id="f7e03-293">The following example generates compiler error CS0019:</span></span>

```csharp
Person p = new Person("Althea", "Goodwin");
if (("Althea", "Goodwin") == p)
    Console.WriteLine(p);
```

<span data-ttu-id="f7e03-294">`Deconstruct`Metoda może przekonwertować `Person` obiekt `p` na krotkę zawierającą dwa ciągi, ale nie ma zastosowania w kontekście testów równości.</span><span class="sxs-lookup"><span data-stu-id="f7e03-294">The `Deconstruct` method could convert the `Person` object `p` to a tuple containing two strings, but it is not applicable in the context of equality tests.</span></span>

## <a name="tuples-as-out-parameters"></a><span data-ttu-id="f7e03-295">Krotki jako parametry out</span><span class="sxs-lookup"><span data-stu-id="f7e03-295">Tuples as out parameters</span></span>

<span data-ttu-id="f7e03-296">Krotki mogą służyć jako *same* [ `out` Parametry](language-reference/keywords/out-parameter-modifier.md) .</span><span class="sxs-lookup"><span data-stu-id="f7e03-296">Tuples can be used as [`out` parameters](language-reference/keywords/out-parameter-modifier.md) *themselves*.</span></span> <span data-ttu-id="f7e03-297">Nie należy mylić z żadną niejednoznaczność wymienioną wcześniej w sekcji [dekonstrukcja](#deconstruction) .</span><span class="sxs-lookup"><span data-stu-id="f7e03-297">Not to be confused with any ambiguity previously mentioned in the [Deconstruction](#deconstruction) section.</span></span> <span data-ttu-id="f7e03-298">W wywołaniu metody jest potrzebne tylko opisywanie kształtu krotki:</span><span class="sxs-lookup"><span data-stu-id="f7e03-298">In a method call, you need only describe the tuple's shape:</span></span>

[!code-csharp[TuplesAsOutParameters](~/samples/snippets/csharp/tuples/program.cs#01_TupleAsOutVariable "Tuples as out parameters")]

<span data-ttu-id="f7e03-299">Alternatywnie możesz użyć [_nienazwanej_](#named-and-unnamed-tuples) krotki i odwołać się do jego pól w `Item1` i `Item2` :</span><span class="sxs-lookup"><span data-stu-id="f7e03-299">Alternatively, you could use an [_unnamed_](#named-and-unnamed-tuples) tuple and refer to its fields as `Item1` and `Item2`:</span></span>

```csharp
dict.TryGetValue(2, out (int, string) pair);
// ...
Console.WriteLine($"{pair.Item1}: {pair.Item2}");
```

## <a name="conclusion"></a><span data-ttu-id="f7e03-300">Podsumowanie</span><span class="sxs-lookup"><span data-stu-id="f7e03-300">Conclusion</span></span>

<span data-ttu-id="f7e03-301">Nowy język i obsługa biblioteki nazwanych krotek znacznie ułatwiają pracę z projektami korzystającymi ze struktur danych, które przechowują wiele elementów, ale nie definiują zachowania, jako klas i struktur.</span><span class="sxs-lookup"><span data-stu-id="f7e03-301">The new language and library support for named tuples makes it much easier to work with designs that use data structures that store multiple elements but do not define behavior, as classes and structs do.</span></span> <span data-ttu-id="f7e03-302">Używanie krotek dla tych typów jest łatwe i zwięzłe.</span><span class="sxs-lookup"><span data-stu-id="f7e03-302">It's easy and concise to use tuples for those types.</span></span> <span data-ttu-id="f7e03-303">Uzyskujesz wszystkie korzyści ze sprawdzenia typu statycznego bez konieczności tworzenia typów przy użyciu bardziej szczegółowych informacji `class` lub `struct` składni.</span><span class="sxs-lookup"><span data-stu-id="f7e03-303">You get all the benefits of static type checking, without needing to author types using the more verbose `class` or `struct` syntax.</span></span> <span data-ttu-id="f7e03-304">Nawet dlatego są one najbardziej przydatne w przypadku metod narzędziowych, które są `private` lub `internal` .</span><span class="sxs-lookup"><span data-stu-id="f7e03-304">Even so, they are most useful for utility methods that are `private`, or `internal`.</span></span> <span data-ttu-id="f7e03-305">Tworzenie typów zdefiniowanych przez użytkownika, albo `class` `struct` typów, gdy metody publiczne zwracają wartość, która ma wiele elementów.</span><span class="sxs-lookup"><span data-stu-id="f7e03-305">Create user-defined types, either `class` or `struct` types when your public methods return a value that has multiple elements.</span></span>
