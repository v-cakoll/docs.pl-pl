---
title: await — C# odwołanie
ms.custom: seodec18
ms.date: 05/22/2017
f1_keywords:
- await_CSharpKeyword
helpviewer_keywords:
- await keyword [C#]
- await [C#]
ms.assetid: 50725c24-ac76-4ca7-bca1-dd57642ffedb
ms.openlocfilehash: 2f6fb9fb8c29013165298c186ec072aa1e750ab9
ms.sourcegitcommit: 986f836f72ef10876878bd6217174e41464c145a
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 08/19/2019
ms.locfileid: "69602299"
---
# <a name="await-c-reference"></a><span data-ttu-id="dd20c-102">await (odwołanie w C#)</span><span class="sxs-lookup"><span data-stu-id="dd20c-102">await (C# Reference)</span></span>
<span data-ttu-id="dd20c-103">`await` Operator jest stosowany do zadania w metodzie asynchronicznej, aby wstawić punkt zawieszenia w trakcie wykonywania metody do momentu zakończenia zadania oczekiwania.</span><span class="sxs-lookup"><span data-stu-id="dd20c-103">The `await` operator is applied to a task in an asynchronous method to insert a suspension point in the execution of the method until the awaited task completes.</span></span> <span data-ttu-id="dd20c-104">Zadanie reprezentuje trwającą prace.</span><span class="sxs-lookup"><span data-stu-id="dd20c-104">The task represents ongoing work.</span></span>  
  
<span data-ttu-id="dd20c-105">`await`może być używany tylko w metodzie asynchronicznej modyfikowane przez słowo kluczowe [Async](./async.md) .</span><span class="sxs-lookup"><span data-stu-id="dd20c-105">`await` can only be used in an asynchronous method modified by the [async](./async.md) keyword.</span></span> <span data-ttu-id="dd20c-106">Takie metody, zdefiniowane za pomocą `async` modyfikatora i zwykle zawierające co najmniej jedno `await` wyrażenie, są określane jako *Metoda async*.</span><span class="sxs-lookup"><span data-stu-id="dd20c-106">Such a method, defined by using the `async` modifier and usually containing one or more `await` expressions, is referred to as an *async method*.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="dd20c-107">Słowa kluczowe `await`izostały wprowadzone w C# 5. `async`</span><span class="sxs-lookup"><span data-stu-id="dd20c-107">The `async` and `await` keywords were introduced in C# 5.</span></span> <span data-ttu-id="dd20c-108">Aby zapoznać się z wprowadzeniem do programowania asynchronicznego, zobacz [programowanie asynchroniczne z Async i await](../../programming-guide/concepts/async/index.md).</span><span class="sxs-lookup"><span data-stu-id="dd20c-108">For an introduction to async programming, see [Asynchronous Programming with async and await](../../programming-guide/concepts/async/index.md).</span></span>  
  
<span data-ttu-id="dd20c-109">Zadanie, do którego `await` jest stosowany operator zazwyczaj jest zwracane przez wywołanie metody implementującej [wzorzec asynchroniczny oparty na zadaniach](../../../standard/asynchronous-programming-patterns/task-based-asynchronous-pattern-tap.md).</span><span class="sxs-lookup"><span data-stu-id="dd20c-109">The task to which the `await` operator is applied typically is returned by a call to a method that implements the [Task-Based Asynchronous Pattern](../../../standard/asynchronous-programming-patterns/task-based-asynchronous-pattern-tap.md).</span></span> <span data-ttu-id="dd20c-110">Obejmują one metody, które <xref:System.Threading.Tasks.Task>zwracają <xref:System.Threading.Tasks.Task%601>obiekty <xref:System.Threading.Tasks.ValueTask>,, <xref:System.Threading.Tasks.ValueTask%601> , i.</span><span class="sxs-lookup"><span data-stu-id="dd20c-110">They include methods that return <xref:System.Threading.Tasks.Task>, <xref:System.Threading.Tasks.Task%601>, <xref:System.Threading.Tasks.ValueTask>, and <xref:System.Threading.Tasks.ValueTask%601> objects.</span></span>  

<span data-ttu-id="dd20c-111">W poniższym przykładzie <xref:System.Net.Http.HttpClient.GetByteArrayAsync%2A?displayProperty=nameWithType> Metoda `Task<byte[]>`zwraca.</span><span class="sxs-lookup"><span data-stu-id="dd20c-111">In the following example, the <xref:System.Net.Http.HttpClient.GetByteArrayAsync%2A?displayProperty=nameWithType> method returns a `Task<byte[]>`.</span></span> <span data-ttu-id="dd20c-112">Zadanie to obietnica do tworzenia rzeczywistej tablicy bajtów po zakończeniu zadania.</span><span class="sxs-lookup"><span data-stu-id="dd20c-112">The task is a promise to produce the actual byte array when the task is complete.</span></span> <span data-ttu-id="dd20c-113">Operator zawiesza wykonywanie do momentu zakończenia pracy <xref:System.Net.Http.HttpClient.GetByteArrayAsync%2A> metody. `await`</span><span class="sxs-lookup"><span data-stu-id="dd20c-113">The `await` operator suspends execution until the work of the <xref:System.Net.Http.HttpClient.GetByteArrayAsync%2A> method is complete.</span></span> <span data-ttu-id="dd20c-114">W międzyczasie formant jest zwracany do obiektu wywołującego `GetPageSizeAsync`.</span><span class="sxs-lookup"><span data-stu-id="dd20c-114">In the meantime, control is returned to the caller of `GetPageSizeAsync`.</span></span> <span data-ttu-id="dd20c-115">Po zakończeniu wykonywania zadania, `await` wyrażenie daje w wyniku tablicę bajtów.</span><span class="sxs-lookup"><span data-stu-id="dd20c-115">When the task finishes execution, the `await` expression evaluates to a byte array.</span></span>  

[!code-csharp[await-example](../../../../samples/snippets/csharp/language-reference/keywords/await/await1.cs)]  

> [!IMPORTANT]
> <span data-ttu-id="dd20c-116">Aby zapoznać się z kompletnym [przykładem, zobacz Przewodnik: Uzyskiwanie dostępu do sieci Web za pomocą](../../programming-guide/concepts/async/walkthrough-accessing-the-web-by-using-async-and-await.md)Async i await.</span><span class="sxs-lookup"><span data-stu-id="dd20c-116">For the complete example, see [Walkthrough: Accessing the Web by Using Async and Await](../../programming-guide/concepts/async/walkthrough-accessing-the-web-by-using-async-and-await.md).</span></span> <span data-ttu-id="dd20c-117">Możesz pobrać przykład z [przykładów kodu dla deweloperów](https://code.msdn.microsoft.com/Async-Sample-Accessing-the-9c10497f) w witrynie sieci Web firmy Microsoft.</span><span class="sxs-lookup"><span data-stu-id="dd20c-117">You can download the sample from [Developer Code Samples](https://code.msdn.microsoft.com/Async-Sample-Accessing-the-9c10497f) on the Microsoft website.</span></span> <span data-ttu-id="dd20c-118">Przykład znajduje się w projekcie AsyncWalkthrough_HttpClient.</span><span class="sxs-lookup"><span data-stu-id="dd20c-118">The example is in the AsyncWalkthrough_HttpClient project.</span></span>  
  
<span data-ttu-id="dd20c-119">Jak pokazano w poprzednim `await` przykładzie, jeśli jest stosowany do wyniku wywołania metody, które `Task<TResult>`zwraca, `await` a następnie typ wyrażenia jest `TResult`.</span><span class="sxs-lookup"><span data-stu-id="dd20c-119">As shown in the previous example, if `await` is applied to the result of a method call that returns a `Task<TResult>`, then the type of the `await` expression is `TResult`.</span></span> <span data-ttu-id="dd20c-120">Jeśli `await` jest stosowany do wyniku wywołania metody, które `Task`zwraca, `await` wówczas typem wyrażenia jest `void`.</span><span class="sxs-lookup"><span data-stu-id="dd20c-120">If `await` is applied to the result of a method call that returns a `Task`, then the type of the `await` expression is `void`.</span></span> <span data-ttu-id="dd20c-121">Poniższy przykład ilustruje różnicę.</span><span class="sxs-lookup"><span data-stu-id="dd20c-121">The following example illustrates the difference.</span></span>  
  
```csharp  
// await keyword used with a method that returns a Task<TResult>.  
TResult result = await AsyncMethodThatReturnsTaskTResult();  
  
// await keyword used with a method that returns a Task.  
await AsyncMethodThatReturnsTask();  

// await keyword used with a method that returns a ValueTask<TResult>.
TResult result = await AsyncMethodThatReturnsValueTaskTResult();
```  
  
<span data-ttu-id="dd20c-122">`await` Wyrażenie nie blokuje wątku, w którym jest wykonywane.</span><span class="sxs-lookup"><span data-stu-id="dd20c-122">An `await` expression does not block the thread on which it is executing.</span></span> <span data-ttu-id="dd20c-123">Zamiast tego powoduje, że kompilator rejestruje resztę metody asynchronicznej jako kontynuację w oczekiwanym zadaniu.</span><span class="sxs-lookup"><span data-stu-id="dd20c-123">Instead, it causes the compiler to sign up the rest of the async method as a continuation on the awaited task.</span></span> <span data-ttu-id="dd20c-124">Następnie formant wraca do obiektu wywołującego metody asynchronicznej.</span><span class="sxs-lookup"><span data-stu-id="dd20c-124">Control then returns to the caller of the async method.</span></span> <span data-ttu-id="dd20c-125">Po zakończeniu zadania wywołuje jego kontynuację i wykonywanie metody asynchronicznej zostaje wznowione w miejscu, w którym została przerwana.</span><span class="sxs-lookup"><span data-stu-id="dd20c-125">When the task completes, it invokes its continuation, and execution of the async method resumes where it left off.</span></span>  
  
<span data-ttu-id="dd20c-126">Wyrażenie może wystąpić tylko w treści otaczającej metody, wyrażenia lambda lub metody anonimowej, która musi być oznaczona `async` modyfikatorem. `await`</span><span class="sxs-lookup"><span data-stu-id="dd20c-126">An `await` expression can occur only in the body of its enclosing method, lambda expression, or anonymous method, which must be marked with an `async` modifier.</span></span> <span data-ttu-id="dd20c-127">Termin *await* służy jako słowo kluczowe tylko w tym kontekście.</span><span class="sxs-lookup"><span data-stu-id="dd20c-127">The term *await* serves as a keyword only in that context.</span></span> <span data-ttu-id="dd20c-128">W innym miejscu jest interpretowana jako identyfikator.</span><span class="sxs-lookup"><span data-stu-id="dd20c-128">Elsewhere, it is interpreted as an identifier.</span></span> <span data-ttu-id="dd20c-129">W ramach metody, wyrażenia lambda lub metody `await` anonimowej wyrażenie nie może wystąpić w treści funkcji synchronicznej, w wyrażeniu zapytania, w bloku [instrukcji lock](./lock-statement.md)lub w niebezpiecznym kontekście. [](./unsafe.md)</span><span class="sxs-lookup"><span data-stu-id="dd20c-129">Within the method, lambda expression, or anonymous method, an `await` expression cannot occur in the body of a synchronous function, in a query expression, in the block of a [lock statement](./lock-statement.md), or in an [unsafe](./unsafe.md) context.</span></span>  
  
## <a name="exceptions"></a><span data-ttu-id="dd20c-130">Wyjątki</span><span class="sxs-lookup"><span data-stu-id="dd20c-130">Exceptions</span></span>  
<span data-ttu-id="dd20c-131">Większość metod asynchronicznych zwraca <xref:System.Threading.Tasks.Task> lub <xref:System.Threading.Tasks.Task%601>.</span><span class="sxs-lookup"><span data-stu-id="dd20c-131">Most async methods return a <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="dd20c-132">Właściwości zwróconego zadania zawierają informacje o jego stanie i historii, na przykład o tym, czy zadanie zostało ukończone, czy Metoda asynchroniczna spowodowała wyjątek, czy została anulowana, oraz jaki jest wynik końcowy.</span><span class="sxs-lookup"><span data-stu-id="dd20c-132">The properties of the returned task carry information about its status and history, such as whether the task is complete, whether the async method caused an exception or was canceled, and what the final result is.</span></span> <span data-ttu-id="dd20c-133">Operator uzyskuje dostęp do tych właściwości poprzez wywoływanie metod dla obiektu zwróconego `GetAwaiter` przez metodę. `await`</span><span class="sxs-lookup"><span data-stu-id="dd20c-133">The `await` operator accesses those properties by calling methods on the object returned by the `GetAwaiter` method.</span></span>  
  
<span data-ttu-id="dd20c-134">Jeśli czekasz na metodę asynchroniczną zwracającą zadanie, która powoduje wyjątek, `await` operator ponownie zgłosi wyjątek.</span><span class="sxs-lookup"><span data-stu-id="dd20c-134">If you await a task-returning async method that causes an exception, the `await` operator rethrows the exception.</span></span>  
  
<span data-ttu-id="dd20c-135">Jeśli oczekujesz, że zachodzi Metoda asynchroniczna zwracająca zadanie, `await` która została anulowana, <xref:System.OperationCanceledException>operator ponownie zgłosi.</span><span class="sxs-lookup"><span data-stu-id="dd20c-135">If you await a task-returning async method that's canceled, the `await` operator rethrows an <xref:System.OperationCanceledException>.</span></span>  
  
<span data-ttu-id="dd20c-136">Pojedyncze zadanie, które jest w stanie awarii może odzwierciedlać wiele wyjątków.</span><span class="sxs-lookup"><span data-stu-id="dd20c-136">A single task that is in a faulted state can reflect multiple exceptions.</span></span> <span data-ttu-id="dd20c-137">Na przykład zadanie może być wynikiem wywołania metody <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="dd20c-137">For example, the task might be the result of a call to <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="dd20c-138">Po oczekiwaniu na takie zadanie Operacja await ponownie zgłosi tylko jeden z wyjątków.</span><span class="sxs-lookup"><span data-stu-id="dd20c-138">When you await such a task, the await operation rethrows only one of the exceptions.</span></span> <span data-ttu-id="dd20c-139">Nie można jednak przewidzieć, które wyjątki są ponownie zgłaszane.</span><span class="sxs-lookup"><span data-stu-id="dd20c-139">However, you can't predict which of the exceptions is rethrown.</span></span>  
  
<span data-ttu-id="dd20c-140">Aby zapoznać się z przykładami obsługi błędów w metodach asynchronicznych, zobacz [try-catch](./try-catch.md).</span><span class="sxs-lookup"><span data-stu-id="dd20c-140">For examples of error handling in async methods, see [try-catch](./try-catch.md).</span></span>  
  
## <a name="example"></a><span data-ttu-id="dd20c-141">Przykład</span><span class="sxs-lookup"><span data-stu-id="dd20c-141">Example</span></span>  
<span data-ttu-id="dd20c-142">Poniższy przykład zwraca łączną liczbę znaków na stronach, których adresy URL są przekazane do niego jako argumenty wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="dd20c-142">The following example returns the total number of characters in the pages whose URLs are passed to it as command line arguments.</span></span> <span data-ttu-id="dd20c-143">Przykład wywołuje `GetPageLengthsAsync` metodę, która jest oznaczona `async` za pomocą słowa kluczowego.</span><span class="sxs-lookup"><span data-stu-id="dd20c-143">The example calls the `GetPageLengthsAsync` method, which is marked with the `async` keyword.</span></span> <span data-ttu-id="dd20c-144">Metoda z kolei `await` używa słowa kluczowego <xref:System.Net.Http.HttpClient.GetStringAsync%2A?displayProperty=nameWithType> do oczekiwania na wywołania metody. `GetPageLengthsAsync`</span><span class="sxs-lookup"><span data-stu-id="dd20c-144">The `GetPageLengthsAsync` method in turn uses the `await` keyword to await calls to the <xref:System.Net.Http.HttpClient.GetStringAsync%2A?displayProperty=nameWithType> method.</span></span>  

[!code-csharp[await-example](../../../../samples/snippets/csharp/language-reference/keywords/await/await2.cs)]  

<span data-ttu-id="dd20c-145">W poprzednim przykładzie używany C# jest [ `async` `Main` ](../../programming-guide/main-and-command-args/index.md)7,1, który obsługuje metodę.</span><span class="sxs-lookup"><span data-stu-id="dd20c-145">The preceding example uses C# 7.1, which supports the [`async` `Main` method](../../programming-guide/main-and-command-args/index.md).</span></span> <span data-ttu-id="dd20c-146">Ze C# względu na to, że wcześniejsze wersje nie <xref:System.Threading.Tasks.Task> obsługują punktów wejścia aplikacji zwracających `async` lub <xref:System.Threading.Tasks.Task%601>, nie `Main` można zastosować modyfikatora `GetPageLengthsAsync` do metody i oczekiwania na wywołanie metody.</span><span class="sxs-lookup"><span data-stu-id="dd20c-146">Because earlier C# versions don't support application entry points that return <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601>, you cannot apply the `async` modifier to the `Main` method and await the `GetPageLengthsAsync` method call.</span></span> <span data-ttu-id="dd20c-147">W takim przypadku można upewnić się, że `Main` Metoda czeka na zakończenie operacji asynchronicznej przez pobranie wartości <xref:System.Threading.Tasks.Task%601.Result?displayProperty=nameWithType> właściwości.</span><span class="sxs-lookup"><span data-stu-id="dd20c-147">In that case, you can ensure that the `Main` method waits for the async operation to complete by retrieving the value of the <xref:System.Threading.Tasks.Task%601.Result?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="dd20c-148">W przypadku zadań, które nie zwracają wartości, można wywołać <xref:System.Threading.Tasks.Task.Wait%2A?displayProperty=nameWithType> metodę.</span><span class="sxs-lookup"><span data-stu-id="dd20c-148">For tasks that do not return a value, you can call the <xref:System.Threading.Tasks.Task.Wait%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="dd20c-149">Aby uzyskać informacje o sposobach wybierania wersji językowej, zobacz [Wybieranie C# wersji językowej](../configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="dd20c-149">For information about how to select the language version, see [Select the C# language version](../configure-language-version.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="dd20c-150">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="dd20c-150">See also</span></span>

- [<span data-ttu-id="dd20c-151">Programowanie asynchroniczne z Async i Await</span><span class="sxs-lookup"><span data-stu-id="dd20c-151">Asynchronous Programming with async and await</span></span>](../../programming-guide/concepts/async/index.md)
- [<span data-ttu-id="dd20c-152">Przewodnik: Uzyskiwanie dostępu do sieci Web za pomocą Async i await</span><span class="sxs-lookup"><span data-stu-id="dd20c-152">Walkthrough: Accessing the Web by Using Async and Await</span></span>](../../programming-guide/concepts/async/walkthrough-accessing-the-web-by-using-async-and-await.md)
- [<span data-ttu-id="dd20c-153">async</span><span class="sxs-lookup"><span data-stu-id="dd20c-153">async</span></span>](./async.md)
