---
title: Najlepsze praktyki dotyczące ładowania zestawu
ms.date: 03/30/2017
helpviewer_keywords:
- assemblies,binding
- LoadFrom method
- default load context
- TypeLoadException class,causes
- assembly loading errors
- load contexts
- assemblies,loading
- LoadWithPartialName method
- load-from context
ms.assetid: 68d1c539-6a47-4614-ab59-4b071c9d4b4c
ms.openlocfilehash: 7575c40edf47e977335bcc34fcd9e49debab0980
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/15/2020
ms.locfileid: "79181699"
---
# <a name="best-practices-for-assembly-loading"></a><span data-ttu-id="e9452-102">Najlepsze praktyki dotyczące ładowania zestawu</span><span class="sxs-lookup"><span data-stu-id="e9452-102">Best Practices for Assembly Loading</span></span>
<span data-ttu-id="e9452-103">W tym artykule omówiono sposoby uniknięcia <xref:System.InvalidCastException> <xref:System.MissingMethodException>problemów z tożsamością typu, które mogą prowadzić do , i innych błędów.</span><span class="sxs-lookup"><span data-stu-id="e9452-103">This article discusses ways to avoid problems of type identity that can lead to <xref:System.InvalidCastException>, <xref:System.MissingMethodException>, and other errors.</span></span> <span data-ttu-id="e9452-104">W artykule omówiono następujące zalecenia:</span><span class="sxs-lookup"><span data-stu-id="e9452-104">The article discusses the following recommendations:</span></span>  
  
- [<span data-ttu-id="e9452-105">Zrozumienie zalet i wad kontekstów obciążenia</span><span class="sxs-lookup"><span data-stu-id="e9452-105">Understand the advantages and disadvantages of load contexts</span></span>](#load_contexts)  
  
- [<span data-ttu-id="e9452-106">Unikaj powiązania z nazwami zespołów częściowych</span><span class="sxs-lookup"><span data-stu-id="e9452-106">Avoid binding on partial assembly names</span></span>](#avoid_partial_names)  
  
- [<span data-ttu-id="e9452-107">Unikaj ładowania złożenia do wielu kontekstów</span><span class="sxs-lookup"><span data-stu-id="e9452-107">Avoid loading an assembly into multiple contexts</span></span>](#avoid_loading_into_multiple_contexts)  
  
- [<span data-ttu-id="e9452-108">Unikaj ładowania wielu wersji zestawu do tego samego kontekstu</span><span class="sxs-lookup"><span data-stu-id="e9452-108">Avoid loading multiple versions of an assembly into the same context</span></span>](#avoid_loading_multiple_versions)  
  
- [<span data-ttu-id="e9452-109">Rozważ przełączenie do domyślnego kontekstu obciążenia</span><span class="sxs-lookup"><span data-stu-id="e9452-109">Consider switching to the default load context</span></span>](#switch_to_default)  
  
 <span data-ttu-id="e9452-110">Pierwsze zalecenie, [zrozumieć zalety i wady kontekstów obciążenia](#load_contexts), zawiera podstawowe informacje dla innych zaleceń, ponieważ wszystkie one zależą od znajomości kontekstów obciążenia.</span><span class="sxs-lookup"><span data-stu-id="e9452-110">The first recommendation, [understand the advantages and disadvantages of load contexts](#load_contexts), provides background information for the other recommendations, because they all depend on a knowledge of load contexts.</span></span>  
  
<a name="load_contexts"></a>
## <a name="understand-the-advantages-and-disadvantages-of-load-contexts"></a><span data-ttu-id="e9452-111">Zrozumienie zalet i wad kontekstów obciążenia</span><span class="sxs-lookup"><span data-stu-id="e9452-111">Understand the Advantages and Disadvantages of Load Contexts</span></span>  
 <span data-ttu-id="e9452-112">W domenie aplikacji zestawy mogą być ładowane do jednego z trzech kontekstów lub mogą być ładowane bez kontekstu:</span><span class="sxs-lookup"><span data-stu-id="e9452-112">Within an application domain, assemblies can be loaded into one of three contexts, or they can be loaded without context:</span></span>  
  
- <span data-ttu-id="e9452-113">Domyślny kontekst ładowania zawiera zestawy znalezione przez sondowanie globalnej pamięci podręcznej zestawów, magazynu zestawu hosta, <xref:System.AppDomainSetup.ApplicationBase%2A> <xref:System.AppDomainSetup.PrivateBinPath%2A> jeśli środowisko uruchomieniowe jest hostowane (na przykład w programie SQL Server) oraz domeny aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e9452-113">The default load context contains assemblies found by probing the global assembly cache, the host assembly store if the runtime is hosted (for example, in SQL Server), and the <xref:System.AppDomainSetup.ApplicationBase%2A> and <xref:System.AppDomainSetup.PrivateBinPath%2A> of the application domain.</span></span> <span data-ttu-id="e9452-114">Większość przeciążeń <xref:System.Reflection.Assembly.Load%2A> zestawów obciążenia metody w tym kontekście.</span><span class="sxs-lookup"><span data-stu-id="e9452-114">Most overloads of the <xref:System.Reflection.Assembly.Load%2A> method load assemblies into this context.</span></span>  
  
- <span data-ttu-id="e9452-115">Kontekst ładowania z zawiera zestawy, które są ładowane z lokalizacji, które nie są przeszukiwane przez moduł ładujący.</span><span class="sxs-lookup"><span data-stu-id="e9452-115">The load-from context contains assemblies that are loaded from locations that are not searched by the loader.</span></span> <span data-ttu-id="e9452-116">Na przykład dodatki mogą być instalowane w katalogu, który nie znajduje się pod ścieżką aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e9452-116">For example, add-ins might be installed in a directory that is not under the application path.</span></span> <span data-ttu-id="e9452-117"><xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>, <xref:System.AppDomain.CreateInstanceFrom%2A?displayProperty=nameWithType>i <xref:System.AppDomain.ExecuteAssembly%2A?displayProperty=nameWithType> są przykładami metod, które ładują się według ścieżki.</span><span class="sxs-lookup"><span data-stu-id="e9452-117"><xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>, <xref:System.AppDomain.CreateInstanceFrom%2A?displayProperty=nameWithType>, and <xref:System.AppDomain.ExecuteAssembly%2A?displayProperty=nameWithType> are examples of methods that load by path.</span></span>  
  
- <span data-ttu-id="e9452-118">Kontekst tylko do odbicia zawiera zestawy <xref:System.Reflection.Assembly.ReflectionOnlyLoad%2A> <xref:System.Reflection.Assembly.ReflectionOnlyLoadFrom%2A> ładowane z i metody.</span><span class="sxs-lookup"><span data-stu-id="e9452-118">The reflection-only context contains assemblies loaded with the <xref:System.Reflection.Assembly.ReflectionOnlyLoad%2A> and <xref:System.Reflection.Assembly.ReflectionOnlyLoadFrom%2A> methods.</span></span> <span data-ttu-id="e9452-119">Nie można wykonać kodu w tym kontekście, więc nie jest on omawiany w tym miejscu.</span><span class="sxs-lookup"><span data-stu-id="e9452-119">Code in this context cannot be executed, so it is not discussed here.</span></span> <span data-ttu-id="e9452-120">Aby uzyskać więcej informacji, zobacz [Jak: Ładowanie zestawów do kontekstu tylko do odbicia](../reflection-and-codedom/how-to-load-assemblies-into-the-reflection-only-context.md).</span><span class="sxs-lookup"><span data-stu-id="e9452-120">For more information, see [How to: Load Assemblies into the Reflection-Only Context](../reflection-and-codedom/how-to-load-assemblies-into-the-reflection-only-context.md).</span></span>  
  
- <span data-ttu-id="e9452-121">Jeśli wygenerowano przejściowy zestaw dynamiczny przy użyciu odbicia emitować, zestaw nie jest w żadnym kontekście.</span><span class="sxs-lookup"><span data-stu-id="e9452-121">If you generated a transient dynamic assembly by using reflection emit, the assembly is not in any context.</span></span> <span data-ttu-id="e9452-122">Ponadto większość zestawów, które są <xref:System.Reflection.Assembly.LoadFile%2A> ładowane przy użyciu metody są ładowane bez kontekstu i zestawy, które są ładowane z tablic bajtowych są ładowane bez kontekstu, chyba że ich tożsamość (po zastosowaniu zasad) ustanawia, że są one w globalnej pamięci podręcznej zestawów.</span><span class="sxs-lookup"><span data-stu-id="e9452-122">In addition, most assemblies that are loaded by using the <xref:System.Reflection.Assembly.LoadFile%2A> method are loaded without context, and assemblies that are loaded from byte arrays are loaded without context unless their identity (after policy is applied) establishes that they are in the global assembly cache.</span></span>  
  
 <span data-ttu-id="e9452-123">Konteksty wykonywania mają zalety i wady, jak omówiono w poniższych sekcjach.</span><span class="sxs-lookup"><span data-stu-id="e9452-123">The execution contexts have advantages and disadvantages, as discussed in the following sections.</span></span>  
  
### <a name="default-load-context"></a><span data-ttu-id="e9452-124">Domyślny kontekst ładowania</span><span class="sxs-lookup"><span data-stu-id="e9452-124">Default Load Context</span></span>  
 <span data-ttu-id="e9452-125">Gdy zestawy są ładowane do domyślnego kontekstu obciążenia, ich zależności są ładowane automatycznie.</span><span class="sxs-lookup"><span data-stu-id="e9452-125">When assemblies are loaded into the default load context, their dependencies are loaded automatically.</span></span> <span data-ttu-id="e9452-126">Zależności, które są ładowane do domyślnego kontekstu obciążenia znajdują się automatycznie dla zestawów w kontekście ładowania domyślnego lub kontekstu ładowania z.</span><span class="sxs-lookup"><span data-stu-id="e9452-126">Dependencies that are loaded into the default load context are found automatically for assemblies in the default load context or the load-from context.</span></span> <span data-ttu-id="e9452-127">Ładowanie przez tożsamość zestawu zwiększa stabilność aplikacji, zapewniając, że nieznane wersje zestawów nie są używane (zobacz [Avoid powiązania na częściowe nazwy zestawu](#avoid_partial_names) sekcji).</span><span class="sxs-lookup"><span data-stu-id="e9452-127">Loading by assembly identity increases the stability of applications by ensuring that unknown versions of assemblies are not used (see the [Avoid Binding on Partial Assembly Names](#avoid_partial_names) section).</span></span>  
  
 <span data-ttu-id="e9452-128">Przy użyciu domyślnego kontekstu obciążenia ma następujące wady:</span><span class="sxs-lookup"><span data-stu-id="e9452-128">Using the default load context has the following disadvantages:</span></span>  
  
- <span data-ttu-id="e9452-129">Zależności, które są ładowane do innych kontekstów nie są dostępne.</span><span class="sxs-lookup"><span data-stu-id="e9452-129">Dependencies that are loaded into other contexts are not available.</span></span>  
  
- <span data-ttu-id="e9452-130">Nie można załadować zestawów z lokalizacji poza ścieżką sondowania do domyślnego kontekstu obciążenia.</span><span class="sxs-lookup"><span data-stu-id="e9452-130">You cannot load assemblies from locations outside the probing path into the default load context.</span></span>  
  
### <a name="load-from-context"></a><span data-ttu-id="e9452-131">Kontekst ładowania z</span><span class="sxs-lookup"><span data-stu-id="e9452-131">Load-From Context</span></span>  
 <span data-ttu-id="e9452-132">Kontekst ładowania z umożliwia załadowanie złożenia ze ścieżki, która nie znajduje się pod ścieżką aplikacji i dlatego nie jest uwzględniona w sondowaniu.</span><span class="sxs-lookup"><span data-stu-id="e9452-132">The load-from context lets you load an assembly from a path that is not under the application path, and therefore is not included in probing.</span></span> <span data-ttu-id="e9452-133">Umożliwia zależności do zlokalizowania i załadowania z tej ścieżki, ponieważ informacje o ścieżce jest obsługiwany przez kontekst.</span><span class="sxs-lookup"><span data-stu-id="e9452-133">It enables dependencies to be located and loaded from that path, because the path information is maintained by the context.</span></span> <span data-ttu-id="e9452-134">Ponadto zestawy w tym kontekście można użyć zależności, które są ładowane do domyślnego kontekstu obciążenia.</span><span class="sxs-lookup"><span data-stu-id="e9452-134">In addition, assemblies in this context can use dependencies that are loaded into the default load context.</span></span>  
  
 <span data-ttu-id="e9452-135">Ładowanie zestawów przy <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> użyciu metody lub jednej z innych metod, które ładują się według ścieżki, ma następujące wady:</span><span class="sxs-lookup"><span data-stu-id="e9452-135">Loading assemblies by using the <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> method, or one of the other methods that load by path, has the following disadvantages:</span></span>  
  
- <span data-ttu-id="e9452-136">Jeśli zestaw o tej samej <xref:System.Reflection.Assembly.LoadFrom%2A> tożsamości jest już załadowany, zwraca załadowany zestaw, nawet jeśli określono inną ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="e9452-136">If an assembly with the same identity is already loaded, <xref:System.Reflection.Assembly.LoadFrom%2A> returns the loaded assembly even if a different path was specified.</span></span>  
  
- <span data-ttu-id="e9452-137">Jeśli zestaw jest <xref:System.Reflection.Assembly.LoadFrom%2A>ładowany z , a później zestaw w kontekście obciążenia domyślnego próbuje załadować ten sam zestaw według nazwy wyświetlanej, próba obciążenia kończy się niepowodzeniem.</span><span class="sxs-lookup"><span data-stu-id="e9452-137">If an assembly is loaded with <xref:System.Reflection.Assembly.LoadFrom%2A>, and later an assembly in the default load context tries to load the same assembly by display name, the load attempt fails.</span></span> <span data-ttu-id="e9452-138">Może to nastąpić, gdy zestaw jest deserialized.</span><span class="sxs-lookup"><span data-stu-id="e9452-138">This can occur when an assembly is deserialized.</span></span>  
  
- <span data-ttu-id="e9452-139">Jeśli zestaw jest <xref:System.Reflection.Assembly.LoadFrom%2A>ładowany z , a ścieżka sondowania zawiera zestaw o <xref:System.InvalidCastException> <xref:System.MissingMethodException>tej samej tożsamości, ale w innej lokalizacji, może wystąpić , lub inne nieoczekiwane zachowanie.</span><span class="sxs-lookup"><span data-stu-id="e9452-139">If an assembly is loaded with <xref:System.Reflection.Assembly.LoadFrom%2A>, and the probing path includes an assembly with the same identity but in a different location, an <xref:System.InvalidCastException>, <xref:System.MissingMethodException>, or other unexpected behavior can occur.</span></span>  
  
- <span data-ttu-id="e9452-140"><xref:System.Reflection.Assembly.LoadFrom%2A><xref:System.Security.Permissions.FileIOPermissionAccess.Read?displayProperty=nameWithType> i <xref:System.Security.Permissions.FileIOPermissionAccess.PathDiscovery?displayProperty=nameWithType>, lub <xref:System.Net.WebPermission>, na określonej ścieżce.</span><span class="sxs-lookup"><span data-stu-id="e9452-140"><xref:System.Reflection.Assembly.LoadFrom%2A> demands <xref:System.Security.Permissions.FileIOPermissionAccess.Read?displayProperty=nameWithType> and <xref:System.Security.Permissions.FileIOPermissionAccess.PathDiscovery?displayProperty=nameWithType>, or <xref:System.Net.WebPermission>, on the specified path.</span></span>  
  
- <span data-ttu-id="e9452-141">Jeśli natywny obraz istnieje dla zestawu, nie jest używany.</span><span class="sxs-lookup"><span data-stu-id="e9452-141">If a native image exists for the assembly, it is not used.</span></span>  
  
- <span data-ttu-id="e9452-142">Zestawu nie można załadować jako neutralne domeny.</span><span class="sxs-lookup"><span data-stu-id="e9452-142">The assembly cannot be loaded as domain-neutral.</span></span>  
  
- <span data-ttu-id="e9452-143">W .NET Framework w wersjach 1.0 i 1.1 zasady nie są stosowane.</span><span class="sxs-lookup"><span data-stu-id="e9452-143">In the .NET Framework versions 1.0 and 1.1, policy is not applied.</span></span>  
  
### <a name="no-context"></a><span data-ttu-id="e9452-144">Brak kontekstu</span><span class="sxs-lookup"><span data-stu-id="e9452-144">No Context</span></span>  
 <span data-ttu-id="e9452-145">Ładowanie bez kontekstu jest jedyną opcją dla zespołów przejściowych, które są generowane za pomocą emitowania odbicia.</span><span class="sxs-lookup"><span data-stu-id="e9452-145">Loading without context is the only option for transient assemblies that are generated with reflection emit.</span></span> <span data-ttu-id="e9452-146">Ładowanie bez kontekstu jest jedynym sposobem, aby załadować wiele zestawów, które mają tę samą tożsamość w jednej domenie aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e9452-146">Loading without context is the only way to load multiple assemblies that have the same identity into one application domain.</span></span> <span data-ttu-id="e9452-147">Unika się kosztów sondowania.</span><span class="sxs-lookup"><span data-stu-id="e9452-147">The cost of probing is avoided.</span></span>  
  
 <span data-ttu-id="e9452-148">Zestawy, które są ładowane z tablic bajtów są ładowane bez kontekstu, chyba że tożsamość zestawu, który jest ustanawiany po zastosowaniu zasad, pasuje do tożsamości zestawu w globalnej pamięci podręcznej zestawów; w takim przypadku zestaw jest ładowany z globalnej pamięci podręcznej zestawów.</span><span class="sxs-lookup"><span data-stu-id="e9452-148">Assemblies that are loaded from byte arrays are loaded without context unless the identity of the assembly, which is established when policy is applied, matches the identity of an assembly in the global assembly cache; in that case, the assembly is loaded from the global assembly cache.</span></span>  
  
 <span data-ttu-id="e9452-149">Ładowanie zestawów bez kontekstu ma następujące wady:</span><span class="sxs-lookup"><span data-stu-id="e9452-149">Loading assemblies without context has the following disadvantages:</span></span>  
  
- <span data-ttu-id="e9452-150">Inne zestawy nie można powiązać z zestawami, które <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> są ładowane bez kontekstu, chyba że obsługujesz zdarzenie.</span><span class="sxs-lookup"><span data-stu-id="e9452-150">Other assemblies cannot bind to assemblies that are loaded without context, unless you handle the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event.</span></span>  
  
- <span data-ttu-id="e9452-151">Zależności nie są ładowane automatycznie.</span><span class="sxs-lookup"><span data-stu-id="e9452-151">Dependencies are not loaded automatically.</span></span> <span data-ttu-id="e9452-152">Można wstępnie załadować je bez kontekstu, wstępnie załadować je do <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> domyślnego kontekstu obciążenia lub załadować je przez obsługę zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="e9452-152">You can preload them without context, preload them into the default load context, or load them by handling the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event.</span></span>  
  
- <span data-ttu-id="e9452-153">Ładowanie wielu zestawów o tej samej tożsamości bez kontekstu może spowodować problemy tożsamości typu podobne do tych spowodowanych przez ładowanie zestawów o tej samej tożsamości w wielu kontekstach.</span><span class="sxs-lookup"><span data-stu-id="e9452-153">Loading multiple assemblies with the same identity without context can cause type identity problems similar to those caused by loading assemblies with the same identity into multiple contexts.</span></span> <span data-ttu-id="e9452-154">Zobacz [Unikanie ładowania zestawu do wielu kontekstów](#avoid_loading_into_multiple_contexts).</span><span class="sxs-lookup"><span data-stu-id="e9452-154">See [Avoid Loading an Assembly into Multiple Contexts](#avoid_loading_into_multiple_contexts).</span></span>  
  
- <span data-ttu-id="e9452-155">Jeśli natywny obraz istnieje dla zestawu, nie jest używany.</span><span class="sxs-lookup"><span data-stu-id="e9452-155">If a native image exists for the assembly, it is not used.</span></span>  
  
- <span data-ttu-id="e9452-156">Zestawu nie można załadować jako neutralne domeny.</span><span class="sxs-lookup"><span data-stu-id="e9452-156">The assembly cannot be loaded as domain-neutral.</span></span>  
  
- <span data-ttu-id="e9452-157">W .NET Framework w wersjach 1.0 i 1.1 zasady nie są stosowane.</span><span class="sxs-lookup"><span data-stu-id="e9452-157">In the .NET Framework versions 1.0 and 1.1, policy is not applied.</span></span>  
  
<a name="avoid_partial_names"></a>
## <a name="avoid-binding-on-partial-assembly-names"></a><span data-ttu-id="e9452-158">Unikanie powiązania nazw zespołów częściowych</span><span class="sxs-lookup"><span data-stu-id="e9452-158">Avoid Binding on Partial Assembly Names</span></span>  
 <span data-ttu-id="e9452-159">Częściowe powiązanie nazwy występuje po określeniu<xref:System.Reflection.Assembly.FullName%2A>tylko części nazwy wyświetlanej złożenia ( ) podczas ładowania złożenia.</span><span class="sxs-lookup"><span data-stu-id="e9452-159">Partial name binding occurs when you specify only part of the assembly display name (<xref:System.Reflection.Assembly.FullName%2A>) when you load an assembly.</span></span> <span data-ttu-id="e9452-160">Na przykład można wywołać <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> metodę tylko z prostą nazwą zestawu, pomijając wersję, kultury i tokenu klucza publicznego.</span><span class="sxs-lookup"><span data-stu-id="e9452-160">For example, you might call the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method with only the simple name of the assembly, omitting the version, culture, and public key token.</span></span> <span data-ttu-id="e9452-161">Lub można wywołać <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> metodę, która <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> najpierw wywołuje metodę i, jeśli nie można zlokalizować zestawu, przeszukuje globalną pamięć podręczną zestawu i ładuje najnowszą dostępną wersję zestawu.</span><span class="sxs-lookup"><span data-stu-id="e9452-161">Or you might call the <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> method, which first calls the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and, if that fails to locate the assembly, searches the global assembly cache and loads the latest available version of the assembly.</span></span>  
  
 <span data-ttu-id="e9452-162">Częściowe powiązanie nazwy może powodować wiele problemów, w tym następujące:</span><span class="sxs-lookup"><span data-stu-id="e9452-162">Partial name binding can cause many problems, including the following:</span></span>  
  
- <span data-ttu-id="e9452-163">Metoda <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> może załadować inny zestaw o tej samej prostej nazwie.</span><span class="sxs-lookup"><span data-stu-id="e9452-163">The <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> method might load a different assembly with the same simple name.</span></span> <span data-ttu-id="e9452-164">Na przykład dwie aplikacje mogą zainstalować dwa zupełnie różne `GraphicsLibrary` zestawy, które obie mają prostą nazwę w globalnej pamięci podręcznej zestawów.</span><span class="sxs-lookup"><span data-stu-id="e9452-164">For example, two applications might install two completely different assemblies that both have the simple name `GraphicsLibrary` into the global assembly cache.</span></span>  
  
- <span data-ttu-id="e9452-165">Zestaw, który jest faktycznie załadowany może nie być wstecznie kompatybilny.</span><span class="sxs-lookup"><span data-stu-id="e9452-165">The assembly that is actually loaded might not be backward-compatible.</span></span> <span data-ttu-id="e9452-166">Na przykład nie określenie wersji może spowodować załadowanie znacznie nowszej wersji niż wersja, której program został pierwotnie napisany do użycia.</span><span class="sxs-lookup"><span data-stu-id="e9452-166">For example, not specifying the version might result in the loading of a much later version than the version your program was originally written to use.</span></span> <span data-ttu-id="e9452-167">Zmiany w nowszej wersji mogą powodować błędy w aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e9452-167">Changes in the later version might cause errors in your application.</span></span>  
  
- <span data-ttu-id="e9452-168">Zestaw, który jest faktycznie załadowany może nie być zgodny z przodu.</span><span class="sxs-lookup"><span data-stu-id="e9452-168">The assembly that is actually loaded might not be forward-compatible.</span></span> <span data-ttu-id="e9452-169">Na przykład być może zostały smątą i przetestowaną aplikacją przy użyciu najnowszej wersji zestawu, ale częściowe powiązanie może załadować znacznie wcześniejszą wersję, która nie ma funkcji używanych przez aplikację.</span><span class="sxs-lookup"><span data-stu-id="e9452-169">For example, you might have built and tested your application with the latest version of an assembly, but partial binding might load a much earlier version that lacks features your application uses.</span></span>  
  
- <span data-ttu-id="e9452-170">Instalowanie nowych aplikacji może przerwać istniejące aplikacje.</span><span class="sxs-lookup"><span data-stu-id="e9452-170">Installing new applications can break existing applications.</span></span> <span data-ttu-id="e9452-171">Aplikacja, która używa <xref:System.Reflection.Assembly.LoadWithPartialName%2A> metody można podzielić, instalując nowszą, niezgodną wersję zestawu udostępnionego.</span><span class="sxs-lookup"><span data-stu-id="e9452-171">An application that uses the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method can be broken by installing a newer, incompatible version of a shared assembly.</span></span>  
  
- <span data-ttu-id="e9452-172">Może wystąpić nieoczekiwane ładowanie zależności.</span><span class="sxs-lookup"><span data-stu-id="e9452-172">Unexpected dependency loading can occur.</span></span> <span data-ttu-id="e9452-173">To załadować dwa zestawy, które współużytkują zależność, ładowanie ich z częściowego powiązania może spowodować jeden zestaw przy użyciu składnika, który nie został zbudowany lub przetestowane z.</span><span class="sxs-lookup"><span data-stu-id="e9452-173">It you load two assemblies that share a dependency, loading them with partial binding might result in one assembly using a component that it was not built or tested with.</span></span>  
  
 <span data-ttu-id="e9452-174">Ze względu na problemy, <xref:System.Reflection.Assembly.LoadWithPartialName%2A> które może spowodować, metoda została oznaczona jako przestarzałe.</span><span class="sxs-lookup"><span data-stu-id="e9452-174">Because of the problems it can cause, the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method has been marked obsolete.</span></span> <span data-ttu-id="e9452-175">Zaleca się użycie <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> metody zamiast i określić pełne nazwy wyświetlane zestawu.</span><span class="sxs-lookup"><span data-stu-id="e9452-175">We recommend that you use the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method instead, and specify full assembly display names.</span></span> <span data-ttu-id="e9452-176">Zobacz [Opis zalet i wad kontekstów obciążenia](#load_contexts) i rozważ [przełączenie do kontekstu obciążenia domyślnego](#switch_to_default).</span><span class="sxs-lookup"><span data-stu-id="e9452-176">See [Understand the Advantages and Disadvantages of Load Contexts](#load_contexts) and [Consider Switching to the Default Load Context](#switch_to_default).</span></span>  
  
 <span data-ttu-id="e9452-177">Jeśli chcesz użyć <xref:System.Reflection.Assembly.LoadWithPartialName%2A> metody, ponieważ ułatwia ładowanie zestawu, należy wziąć pod uwagę, że o aplikacji nie powiedzie się z komunikatem o błędzie, który identyfikuje brakujący zestaw może zapewnić lepsze środowisko użytkownika niż automatycznie przy użyciu nieznanej wersji zestawu, co może spowodować nieprzewidywalne zachowanie i luki w zabezpieczeniach.</span><span class="sxs-lookup"><span data-stu-id="e9452-177">If you want to use the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method because it makes assembly loading easy, consider that having your application fail with an error message that identifies the missing assembly is likely to provide a better user experience than automatically using an unknown version of the assembly, which might cause unpredictable behavior and security holes.</span></span>  
  
<a name="avoid_loading_into_multiple_contexts"></a>
## <a name="avoid-loading-an-assembly-into-multiple-contexts"></a><span data-ttu-id="e9452-178">Unikaj ładowania zestawu do wielu kontekstów</span><span class="sxs-lookup"><span data-stu-id="e9452-178">Avoid Loading an Assembly into Multiple Contexts</span></span>  
 <span data-ttu-id="e9452-179">Ładowanie zestawu do wielu kontekstów może spowodować problemy z tożsamością typu.</span><span class="sxs-lookup"><span data-stu-id="e9452-179">Loading an assembly into multiple contexts can cause type identity problems.</span></span> <span data-ttu-id="e9452-180">Jeśli ten sam typ jest ładowany z tego samego zestawu do dwóch różnych kontekstów, to tak, jakby dwa różne typy o tej samej nazwie zostały załadowane.</span><span class="sxs-lookup"><span data-stu-id="e9452-180">If the same type is loaded from the same assembly into two different contexts, it is as if two different types with the same name had been loaded.</span></span> <span data-ttu-id="e9452-181">Jest <xref:System.InvalidCastException> generowany, jeśli spróbujesz rzutować jeden typ do `MyType` drugiego, z `MyType`mylącą wiadomością, że typ nie może być rzutowania do typu .</span><span class="sxs-lookup"><span data-stu-id="e9452-181">An <xref:System.InvalidCastException> is thrown if you try to cast one type to the other, with the confusing message that type `MyType` cannot be cast to type `MyType`.</span></span>  
  
 <span data-ttu-id="e9452-182">Załóżmy na `ICommunicate` przykład, że interfejs `Utility`jest zadeklarowany w zestawie o nazwie , do którego odwołuje się program, a także inne zestawy, które program ładuje.</span><span class="sxs-lookup"><span data-stu-id="e9452-182">For example, suppose that the `ICommunicate` interface is declared in an assembly named `Utility`, which is referenced by your program and also by other assemblies that your program loads.</span></span> <span data-ttu-id="e9452-183">Te inne zestawy zawierają typy, które implementują `ICommunicate` interfejs, dzięki czemu program z nich korzystać.</span><span class="sxs-lookup"><span data-stu-id="e9452-183">These other assemblies contain types that implement the `ICommunicate` interface, allowing your program to use them.</span></span>  
  
 <span data-ttu-id="e9452-184">Teraz zastanów się, co się stanie, gdy program jest uruchamiany.</span><span class="sxs-lookup"><span data-stu-id="e9452-184">Now consider what happens when your program is run.</span></span> <span data-ttu-id="e9452-185">Zestawy, do których odwołuje się program, są ładowane do domyślnego kontekstu obciążenia.</span><span class="sxs-lookup"><span data-stu-id="e9452-185">Assemblies that are referenced by your program are loaded into the default load context.</span></span> <span data-ttu-id="e9452-186">Jeśli załadować zestaw docelowy przez <xref:System.Reflection.Assembly.Load%2A> jego tożsamości, przy użyciu metody, będzie w kontekście obciążenia domyślnego i tak będzie jego zależności.</span><span class="sxs-lookup"><span data-stu-id="e9452-186">If you load a target assembly by its identity, using the <xref:System.Reflection.Assembly.Load%2A> method, it will be in the default load context, and so will its dependencies.</span></span> <span data-ttu-id="e9452-187">Zarówno program, jak i zestaw `Utility` docelowy będą używać tego samego zestawu.</span><span class="sxs-lookup"><span data-stu-id="e9452-187">Both your program and the target assembly will use the same `Utility` assembly.</span></span>  
  
 <span data-ttu-id="e9452-188">Załóżmy jednak, że zestaw docelowy jest <xref:System.Reflection.Assembly.LoadFile%2A> ładowany przez jego ścieżkę pliku, przy użyciu metody.</span><span class="sxs-lookup"><span data-stu-id="e9452-188">However, suppose you load the target assembly by its file path, using the <xref:System.Reflection.Assembly.LoadFile%2A> method.</span></span> <span data-ttu-id="e9452-189">Zestaw jest ładowany bez kontekstu, więc jego zależności nie są automatycznie ładowane.</span><span class="sxs-lookup"><span data-stu-id="e9452-189">The assembly is loaded without any context, so its dependencies are not automatically loaded.</span></span> <span data-ttu-id="e9452-190">Może mieć program obsługi <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> dla zdarzenia do dostarczania zależności i `Utility` może załadować zestaw <xref:System.Reflection.Assembly.LoadFile%2A> bez kontekstu przy użyciu metody.</span><span class="sxs-lookup"><span data-stu-id="e9452-190">You might have a handler for the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event to supply the dependency, and it might load the `Utility` assembly with no context by using the <xref:System.Reflection.Assembly.LoadFile%2A> method.</span></span> <span data-ttu-id="e9452-191">Teraz podczas tworzenia wystąpienia typu, który jest zawarty w zestawie docelowym i spróbuj `ICommunicate`przypisać <xref:System.InvalidCastException> go do zmiennej typu, jest generowany, ponieważ środowisko uruchomieniowe uważa `ICommunicate` interfejsy w dwóch kopiach `Utility` zestawu za różne typy.</span><span class="sxs-lookup"><span data-stu-id="e9452-191">Now when you create an instance of a type that is contained in the target assembly and try to assign it to a variable of type `ICommunicate`, an <xref:System.InvalidCastException> is thrown because the runtime considers the `ICommunicate` interfaces in the two copies of the `Utility` assembly to be different types.</span></span>  
  
 <span data-ttu-id="e9452-192">Istnieje wiele innych scenariuszy, w których zestaw można załadować do wielu kontekstów.</span><span class="sxs-lookup"><span data-stu-id="e9452-192">There are many other scenarios in which an assembly can be loaded into multiple contexts.</span></span> <span data-ttu-id="e9452-193">Najlepszym rozwiązaniem jest uniknięcie konfliktów przez przeniesienie zestawu docelowego <xref:System.Reflection.Assembly.Load%2A> w ścieżce aplikacji i przy użyciu metody z pełną nazwą wyświetlaną.</span><span class="sxs-lookup"><span data-stu-id="e9452-193">The best approach is to avoid conflicts by relocating the target assembly in your application path and using the <xref:System.Reflection.Assembly.Load%2A> method with the full display name.</span></span> <span data-ttu-id="e9452-194">Zestaw jest następnie ładowany do domyślnego kontekstu `Utility` obciążenia, a oba zestawy używają tego samego zestawu.</span><span class="sxs-lookup"><span data-stu-id="e9452-194">The assembly is then loaded into the default load context, and both assemblies use the same `Utility` assembly.</span></span>  
  
 <span data-ttu-id="e9452-195">Jeśli zestaw docelowy musi pozostać poza ścieżką <xref:System.Reflection.Assembly.LoadFrom%2A> aplikacji, można użyć metody, aby załadować go do kontekstu ładowania z.</span><span class="sxs-lookup"><span data-stu-id="e9452-195">If the target assembly must remain outside your application path, you can use the <xref:System.Reflection.Assembly.LoadFrom%2A> method to load it into the load-from context.</span></span> <span data-ttu-id="e9452-196">Jeśli zestaw docelowy został skompilowany z `Utility` odwołaniem do `Utility` zestawu aplikacji, użyje zestawu, który aplikacja załadowała do domyślnego kontekstu obciążenia.</span><span class="sxs-lookup"><span data-stu-id="e9452-196">If the target assembly was compiled with a reference to your application's `Utility` assembly, it will use the `Utility` assembly that your application has loaded into the default load context.</span></span> <span data-ttu-id="e9452-197">Należy zauważyć, że mogą wystąpić problemy, jeśli zestaw `Utility` docelowy ma zależność od kopii zestawu znajdującego się poza ścieżką aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e9452-197">Note that problems can occur if the target assembly has a dependency on a copy of the `Utility` assembly located outside your application path.</span></span> <span data-ttu-id="e9452-198">Jeśli ten zestaw jest ładowany do kontekstu `Utility` obciążenia z przed aplikacją ładuje zestawu, obciążenie aplikacji zakończy się niepowodzeniem.</span><span class="sxs-lookup"><span data-stu-id="e9452-198">If that assembly is loaded into the load-from context before your application loads the `Utility` assembly, your application's load will fail.</span></span>  
  
 <span data-ttu-id="e9452-199">Sekcja [Rozważ przełączenie do kontekstu ładowania domyślnego](#switch_to_default) omówiono alternatywy dla używania obciążeń ścieżki pliku, takich jak <xref:System.Reflection.Assembly.LoadFile%2A> i <xref:System.Reflection.Assembly.LoadFrom%2A>.</span><span class="sxs-lookup"><span data-stu-id="e9452-199">The [Consider Switching to the Default Load Context](#switch_to_default) section discusses alternatives to using file path loads such as <xref:System.Reflection.Assembly.LoadFile%2A> and <xref:System.Reflection.Assembly.LoadFrom%2A>.</span></span>  
  
<a name="avoid_loading_multiple_versions"></a>
## <a name="avoid-loading-multiple-versions-of-an-assembly-into-the-same-context"></a><span data-ttu-id="e9452-200">Unikaj ładowania wielu wersji zestawu w tym samym kontekście</span><span class="sxs-lookup"><span data-stu-id="e9452-200">Avoid Loading Multiple Versions of an Assembly into the Same Context</span></span>  
 <span data-ttu-id="e9452-201">Ładowanie wielu wersji zestawu w jednym kontekście obciążenia może spowodować problemy z tożsamością typu.</span><span class="sxs-lookup"><span data-stu-id="e9452-201">Loading multiple versions of an assembly into one load context can cause type identity problems.</span></span> <span data-ttu-id="e9452-202">Jeśli ten sam typ jest ładowany z dwóch wersji tego samego zestawu, jest tak, jakby dwa różne typy o tej samej nazwie zostały załadowane.</span><span class="sxs-lookup"><span data-stu-id="e9452-202">If the same type is loaded from two versions of the same assembly, it is as if two different types with the same name had been loaded.</span></span> <span data-ttu-id="e9452-203">Jest <xref:System.InvalidCastException> generowany, jeśli spróbujesz rzutować jeden typ do `MyType` drugiego, z `MyType`mylącą wiadomością, że typ nie może być rzutowania do typu .</span><span class="sxs-lookup"><span data-stu-id="e9452-203">An <xref:System.InvalidCastException> is thrown if you try to cast one type to the other, with the confusing message that type `MyType` cannot be cast to type `MyType`.</span></span>  
  
 <span data-ttu-id="e9452-204">Na przykład program może załadować `Utility` jedną wersję zestawu bezpośrednio, a później może załadować inny `Utility` zestaw, który ładuje inną wersję zestawu.</span><span class="sxs-lookup"><span data-stu-id="e9452-204">For example, your program might load one version of the `Utility` assembly directly, and later it might load another assembly that loads a different version of the `Utility` assembly.</span></span> <span data-ttu-id="e9452-205">Lub błąd kodowania może spowodować dwie różne ścieżki kodu w aplikacji, aby załadować różne wersje zestawu.</span><span class="sxs-lookup"><span data-stu-id="e9452-205">Or a coding error might cause two different code paths in your application to load different versions of an assembly.</span></span>  
  
 <span data-ttu-id="e9452-206">W kontekście ładowania domyślnego ten problem <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> może wystąpić podczas korzystania z metody i określić pełne nazwy wyświetlane zestawu, które zawierają różne numery wersji.</span><span class="sxs-lookup"><span data-stu-id="e9452-206">In the default load context, this problem can occur when you use the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and specify complete assembly display names that include different version numbers.</span></span> <span data-ttu-id="e9452-207">Dla zestawów, które są ładowane bez kontekstu, problem może być spowodowane przy użyciu <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType> metody, aby załadować ten sam zestaw z różnych ścieżek.</span><span class="sxs-lookup"><span data-stu-id="e9452-207">For assemblies that are loaded without context, the problem can be caused by using the <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType> method to load the same assembly from different paths.</span></span> <span data-ttu-id="e9452-208">Środowisko wykonawcze uważa, że dwa zestawy, które są ładowane z różnych ścieżek, są różne zestawy, nawet jeśli ich tożsamości są takie same.</span><span class="sxs-lookup"><span data-stu-id="e9452-208">The runtime considers two assemblies that are loaded from different paths to be different assemblies, even if their identities are the same.</span></span>  
  
 <span data-ttu-id="e9452-209">Oprócz problemów z tożsamością typu wiele wersji <xref:System.MissingMethodException> zestawu może spowodować, jeśli typ, który jest ładowany z jednej wersji zestawu jest przekazywany do kodu, który oczekuje tego typu z innej wersji.</span><span class="sxs-lookup"><span data-stu-id="e9452-209">In addition to type identity problems, multiple versions of an assembly can cause a <xref:System.MissingMethodException> if a type that is loaded from one version of the assembly is passed to code that expects that type from a different version.</span></span> <span data-ttu-id="e9452-210">Na przykład kod może oczekiwać metody, która została dodana do nowszej wersji.</span><span class="sxs-lookup"><span data-stu-id="e9452-210">For example, the code might expect a method that was added to the later version.</span></span>  
  
 <span data-ttu-id="e9452-211">Bardziej subtelne błędy mogą wystąpić, jeśli zachowanie typu zmieniono między wersjami.</span><span class="sxs-lookup"><span data-stu-id="e9452-211">More subtle errors can occur if the behavior of the type changed between versions.</span></span> <span data-ttu-id="e9452-212">Na przykład metoda może zgłosić nieoczekiwany wyjątek lub zwrócić nieoczekiwaną wartość.</span><span class="sxs-lookup"><span data-stu-id="e9452-212">For example, a method might throw an unexpected exception or return an unexpected value.</span></span>  
  
 <span data-ttu-id="e9452-213">Dokładnie przejrzyj kod, aby upewnić się, że tylko jedna wersja zestawu jest ładowany.</span><span class="sxs-lookup"><span data-stu-id="e9452-213">Carefully review your code to ensure that only one version of an assembly is loaded.</span></span> <span data-ttu-id="e9452-214">Za pomocą <xref:System.AppDomain.GetAssemblies%2A?displayProperty=nameWithType> tej metody można określić, które zestawy są ładowane w danym momencie.</span><span class="sxs-lookup"><span data-stu-id="e9452-214">You can use the <xref:System.AppDomain.GetAssemblies%2A?displayProperty=nameWithType> method to determine which assemblies are loaded at any given time.</span></span>  
  
<a name="switch_to_default"></a>
## <a name="consider-switching-to-the-default-load-context"></a><span data-ttu-id="e9452-215">Rozważ przełączenie do kontekstu obciążenia domyślnego</span><span class="sxs-lookup"><span data-stu-id="e9452-215">Consider Switching to the Default Load Context</span></span>  
 <span data-ttu-id="e9452-216">Sprawdź wzorce ładowania i wdrażania zestawu aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e9452-216">Examine your application's assembly loading and deployment patterns.</span></span> <span data-ttu-id="e9452-217">Czy można wyeliminować zestawy, które są ładowane z tablic bajtowych?</span><span class="sxs-lookup"><span data-stu-id="e9452-217">Can you eliminate assemblies that are loaded from byte arrays?</span></span> <span data-ttu-id="e9452-218">Czy można przenieść zestawy do ścieżki sondowania?</span><span class="sxs-lookup"><span data-stu-id="e9452-218">Can you move assemblies into the probing path?</span></span> <span data-ttu-id="e9452-219">Jeśli zestawy znajdują się w globalnej pamięci podręcznej zestawów lub w <xref:System.AppDomainSetup.ApplicationBase%2A> ścieżce sondowania domeny aplikacji (czyli jej i <xref:System.AppDomainSetup.PrivateBinPath%2A>), można załadować zestaw według jego tożsamości.</span><span class="sxs-lookup"><span data-stu-id="e9452-219">If assemblies are located in the global assembly cache or in the application domain's probing path (that is, its <xref:System.AppDomainSetup.ApplicationBase%2A> and <xref:System.AppDomainSetup.PrivateBinPath%2A>), you can load the assembly by its identity.</span></span>  
  
 <span data-ttu-id="e9452-220">Jeśli nie jest możliwe umieszczenie wszystkich zestawów w ścieżce sondowania, należy wziąć pod uwagę alternatywy, takie jak przy użyciu modelu dodatku .NET Framework, umieszczanie zestawów w globalnej pamięci podręcznej zestawów lub tworzenie domen aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e9452-220">If it is not possible to put all your assemblies in the probing path, consider alternatives such as using the .NET Framework add-in model, placing assemblies into the global assembly cache, or creating application domains.</span></span>  
  
### <a name="consider-using-the-net-framework-add-in-model"></a><span data-ttu-id="e9452-221">Rozważ użycie modelu dodatku .NET Framework</span><span class="sxs-lookup"><span data-stu-id="e9452-221">Consider Using the .NET Framework Add-In Model</span></span>  
 <span data-ttu-id="e9452-222">Jeśli używasz kontekstu ładowania z do zaimplementowania dodatków, które zazwyczaj nie są zainstalowane w bazie aplikacji, należy użyć modelu dodatku .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="e9452-222">If you are using the load-from context to implement add-ins, which typically are not installed in the application base, use the .NET Framework add-in model.</span></span> <span data-ttu-id="e9452-223">Ten model zapewnia izolację na poziomie domeny aplikacji lub procesu, bez konieczności samodzielnego zarządzania domenami aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e9452-223">This model provides isolation at the application domain or process level, without requiring you to manage application domains yourself.</span></span> <span data-ttu-id="e9452-224">Aby uzyskać informacje o modelu dodatku, zobacz [Dodatki i rozszerzalność](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/bb384200(v%3dvs.100)).</span><span class="sxs-lookup"><span data-stu-id="e9452-224">For information about the add-in model, see [Add-ins and Extensibility](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/bb384200(v%3dvs.100)).</span></span>  
  
### <a name="consider-using-the-global-assembly-cache"></a><span data-ttu-id="e9452-225">Rozważ użycie globalnej pamięci podręcznej zestawów</span><span class="sxs-lookup"><span data-stu-id="e9452-225">Consider Using the Global Assembly Cache</span></span>  
 <span data-ttu-id="e9452-226">Umieść zestawy w globalnej pamięci podręcznej zestawów, aby uzyskać korzyści ze ścieżki zestawu udostępnionego, która znajduje się poza bazą aplikacji, bez utraty zalet kontekstu obciążenia domyślnego lub biorąc na wady innych kontekstów.</span><span class="sxs-lookup"><span data-stu-id="e9452-226">Place assemblies in the global assembly cache to get the benefit of a shared assembly path that is outside the application base, without losing the advantages of the default load context or taking on the disadvantages of the other contexts.</span></span>  
  
### <a name="consider-using-application-domains"></a><span data-ttu-id="e9452-227">Rozważ użycie domen aplikacji</span><span class="sxs-lookup"><span data-stu-id="e9452-227">Consider Using Application Domains</span></span>  
 <span data-ttu-id="e9452-228">Jeśli okaże się, że niektóre zestawy nie można wdrożyć w ścieżce sondowania aplikacji, należy rozważyć utworzenie nowej domeny aplikacji dla tych zestawów.</span><span class="sxs-lookup"><span data-stu-id="e9452-228">If you determine that some of your assemblies cannot be deployed in the application's probing path, consider creating a new application domain for those assemblies.</span></span> <span data-ttu-id="e9452-229">Użyj, <xref:System.AppDomainSetup> aby utworzyć nową domenę <xref:System.AppDomainSetup.ApplicationBase%2A?displayProperty=nameWithType> aplikacji i użyj właściwości, aby określić ścieżkę zawierającą zestawy, które chcesz załadować.</span><span class="sxs-lookup"><span data-stu-id="e9452-229">Use an <xref:System.AppDomainSetup> to create the new application domain, and use the <xref:System.AppDomainSetup.ApplicationBase%2A?displayProperty=nameWithType> property to specify the path that contains the assemblies you want to load.</span></span> <span data-ttu-id="e9452-230">Jeśli masz wiele katalogów do sondowania, można ustawić <xref:System.AppDomainSetup.ApplicationBase%2A> do <xref:System.AppDomainSetup.PrivateBinPath%2A?displayProperty=nameWithType> katalogu głównego i użyć właściwości do identyfikowania podkatalogów do sondowania.</span><span class="sxs-lookup"><span data-stu-id="e9452-230">If you have multiple directories to probe, you can set the <xref:System.AppDomainSetup.ApplicationBase%2A> to a root directory and use the <xref:System.AppDomainSetup.PrivateBinPath%2A?displayProperty=nameWithType> property to identify the subdirectories to probe.</span></span> <span data-ttu-id="e9452-231">Alternatywnie można utworzyć wiele domen aplikacji <xref:System.AppDomainSetup.ApplicationBase%2A> i ustawić każdej domeny aplikacji do odpowiedniej ścieżki dla jego zestawów.</span><span class="sxs-lookup"><span data-stu-id="e9452-231">Alternatively, you can create multiple application domains and set the <xref:System.AppDomainSetup.ApplicationBase%2A> of each application domain to the appropriate path for its assemblies.</span></span>  
  
 <span data-ttu-id="e9452-232">Należy zauważyć, że <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> można użyć metody, aby załadować te zestawy.</span><span class="sxs-lookup"><span data-stu-id="e9452-232">Note that you can use the <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> method to load these assemblies.</span></span> <span data-ttu-id="e9452-233">Ponieważ są one teraz w ścieżce sondowania, zostaną załadowane do domyślnego kontekstu ładowania zamiast kontekstu ładowania z.</span><span class="sxs-lookup"><span data-stu-id="e9452-233">Because they are now in the probing path, they will be loaded into the default load context instead of the load-from context.</span></span> <span data-ttu-id="e9452-234">Jednak zaleca się, aby <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> przełączyć się do metody i podać pełne nazwy wyświetlania zestawu, aby upewnić się, że poprawne wersje są zawsze używane.</span><span class="sxs-lookup"><span data-stu-id="e9452-234">However, we recommend that you switch to the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and supply full assembly display names to ensure that correct versions are always used.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="e9452-235">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="e9452-235">See also</span></span>

- <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>
- <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>
- <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType>
- <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType>
