---
title: Standardowe wzorce zdarzeń platformy .NET
description: Dowiedz się więcej o wzorcach zdarzeń programu .NET oraz o tym, jak tworzyć standardowe źródła zdarzeń oraz subskrybować i przetwarzać zdarzenia standardowe w kodzie.
ms.date: 06/20/2016
ms.technology: csharp-fundamentals
ms.assetid: 8a3133d6-4ef2-46f9-9c8d-a8ea8898e4c9
ms.openlocfilehash: dec516767e43a6bf4edfa555e34f3adcc21a46e3
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/14/2020
ms.locfileid: "79146144"
---
# <a name="standard-net-event-patterns"></a><span data-ttu-id="c3783-103">Standardowe wzorce zdarzeń platformy .NET</span><span class="sxs-lookup"><span data-stu-id="c3783-103">Standard .NET event patterns</span></span>

[<span data-ttu-id="c3783-104">Wstecz</span><span class="sxs-lookup"><span data-stu-id="c3783-104">Previous</span></span>](events-overview.md)

<span data-ttu-id="c3783-105">Zdarzenia .NET zazwyczaj wykonaj kilka znanych wzorców.</span><span class="sxs-lookup"><span data-stu-id="c3783-105">.NET events generally follow a few known patterns.</span></span> <span data-ttu-id="c3783-106">Standaryzacja tych wzorców oznacza, że deweloperzy mogą wykorzystać wiedzę na temat tych wzorców standardowych, które mogą być stosowane do dowolnego programu zdarzeń .NET.</span><span class="sxs-lookup"><span data-stu-id="c3783-106">Standardizing on these patterns means that developers can leverage knowledge of those standard patterns, which can be applied to any .NET event program.</span></span>

<span data-ttu-id="c3783-107">Przejdźmy przez te wzorce standardowe, dzięki czemu będziesz miał całą wiedzę potrzebną do tworzenia standardowych źródeł zdarzeń i subskrybowania i przetwarzania standardowych zdarzeń w kodzie.</span><span class="sxs-lookup"><span data-stu-id="c3783-107">Let's go through these standard patterns so you will have all the knowledge you need to create standard event sources, and subscribe and process standard events in your code.</span></span>

## <a name="event-delegate-signatures"></a><span data-ttu-id="c3783-108">Podpisy delegatów zdarzeń</span><span class="sxs-lookup"><span data-stu-id="c3783-108">Event Delegate Signatures</span></span>

<span data-ttu-id="c3783-109">Standardowy podpis pełnomocnika zdarzenia .NET to:</span><span class="sxs-lookup"><span data-stu-id="c3783-109">The standard signature for a .NET event delegate is:</span></span>

```csharp
void OnEventRaised(object sender, EventArgs args);
```

<span data-ttu-id="c3783-110">Typ zwracany jest nieważny.</span><span class="sxs-lookup"><span data-stu-id="c3783-110">The return type is void.</span></span> <span data-ttu-id="c3783-111">Zdarzenia są oparte na delegatów i są delegatów multiemisji.</span><span class="sxs-lookup"><span data-stu-id="c3783-111">Events are based on delegates and are multicast delegates.</span></span> <span data-ttu-id="c3783-112">Który obsługuje wielu subskrybentów dla dowolnego źródła zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="c3783-112">That supports multiple subscribers for any event source.</span></span> <span data-ttu-id="c3783-113">Pojedyncza wartość zwracana z metody nie jest skalowana do wielu subskrybentów zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="c3783-113">The single return value from a method doesn't scale to multiple event subscribers.</span></span> <span data-ttu-id="c3783-114">Jaką wartość zwracaną widzi źródło zdarzenia po zgłoszeniu zdarzenia?</span><span class="sxs-lookup"><span data-stu-id="c3783-114">Which return value does the event source see after raising an event?</span></span> <span data-ttu-id="c3783-115">W dalszej części tego artykułu zobaczysz, jak utworzyć protokoły zdarzeń, które obsługują subskrybentów zdarzeń, które zgłaszają informacje do źródła zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="c3783-115">Later in this article you'll see how to create event protocols that support event subscribers that report information to the event source.</span></span>

<span data-ttu-id="c3783-116">Lista argumentów zawiera dwa argumenty: nadawcę i argumenty zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="c3783-116">The argument list contains two arguments: the sender, and the event arguments.</span></span> <span data-ttu-id="c3783-117">Typ czasu kompilacji `sender` `System.Object`jest , nawet jeśli prawdopodobnie znasz typ bardziej pochodny, który zawsze będzie poprawny.</span><span class="sxs-lookup"><span data-stu-id="c3783-117">The compile time type of `sender` is `System.Object`, even though you likely know a more derived type that would always be correct.</span></span> <span data-ttu-id="c3783-118">Zgodnie z `object`konwencją użyj .</span><span class="sxs-lookup"><span data-stu-id="c3783-118">By convention, use `object`.</span></span>

<span data-ttu-id="c3783-119">Drugi argument jest zazwyczaj typem, który `System.EventArgs`pochodzi od .</span><span class="sxs-lookup"><span data-stu-id="c3783-119">The second argument has typically been a type that is derived from `System.EventArgs`.</span></span> <span data-ttu-id="c3783-120">(W [następnej sekcji](modern-events.md) zobaczysz, że ta konwencja nie jest już wymuszana). Jeśli typ zdarzenia nie wymaga żadnych dodatkowych argumentów, nadal będzie podać oba argumenty.</span><span class="sxs-lookup"><span data-stu-id="c3783-120">(You'll see in the [next section](modern-events.md) that this convention is no longer enforced.) If your event type does not need any additional arguments, you will still provide both arguments.</span></span>
<span data-ttu-id="c3783-121">Istnieje specjalna wartość, `EventArgs.Empty` której należy użyć do oznaczenia, że zdarzenie nie zawiera żadnych dodatkowych informacji.</span><span class="sxs-lookup"><span data-stu-id="c3783-121">There is a special value, `EventArgs.Empty` that you should use to denote that your event does not contain any additional information.</span></span>

<span data-ttu-id="c3783-122">Skompilujmy klasę, która wyświetla listę plików w katalogu lub dowolną z jego podkatalogów, które postępują zgodnie z wzorcem.</span><span class="sxs-lookup"><span data-stu-id="c3783-122">Let's build a class that lists files in a directory, or any of its subdirectories that follow a pattern.</span></span> <span data-ttu-id="c3783-123">Ten składnik wywołuje zdarzenie dla każdego znalezionego pliku, które pasuje do wzorca.</span><span class="sxs-lookup"><span data-stu-id="c3783-123">This component raises an event for each file found that matches the pattern.</span></span>

<span data-ttu-id="c3783-124">Korzystanie z modelu zdarzeń zapewnia pewne zalety projektu.</span><span class="sxs-lookup"><span data-stu-id="c3783-124">Using an event model provides some design advantages.</span></span> <span data-ttu-id="c3783-125">Można utworzyć wiele detektorów zdarzeń, które wykonują różne akcje po znalezieniu poszukiwanego pliku.</span><span class="sxs-lookup"><span data-stu-id="c3783-125">You can create multiple event listeners that perform different actions when a sought file is found.</span></span> <span data-ttu-id="c3783-126">Łączenie różnych odbiorników można utworzyć bardziej niezawodne algorytmy.</span><span class="sxs-lookup"><span data-stu-id="c3783-126">Combining the different listeners can create more robust algorithms.</span></span>

<span data-ttu-id="c3783-127">Oto wstępne oświadczenie argument zdarzenia dla znalezienia poszukiwanego pliku:</span><span class="sxs-lookup"><span data-stu-id="c3783-127">Here is the initial event argument declaration for finding a sought file:</span></span>

[!code-csharp[EventArgs](../../samples/snippets/csharp/events/Program.cs#EventArgsV1 "Define event arguments")]

<span data-ttu-id="c3783-128">Mimo że ten typ wygląda jak mały typ tylko do danych, należy`class`postępować zgodnie z konwencją i uczynić go odwołaniem ( ) typu.</span><span class="sxs-lookup"><span data-stu-id="c3783-128">Even though this type looks like a small, data-only type, you should follow the convention and make it a reference (`class`) type.</span></span> <span data-ttu-id="c3783-129">Oznacza to, że obiekt argumentu będą przekazywane przez odwołanie, a wszelkie aktualizacje danych będą wyświetlane przez wszystkich subskrybentów.</span><span class="sxs-lookup"><span data-stu-id="c3783-129">That means the argument object will be passed by reference, and any updates to the data will be viewed by all subscribers.</span></span> <span data-ttu-id="c3783-130">Pierwsza wersja jest obiektem niezmiennym.</span><span class="sxs-lookup"><span data-stu-id="c3783-130">The first version is an immutable object.</span></span> <span data-ttu-id="c3783-131">Wolisz, aby właściwości w typie argumentu zdarzenia były niezmienne.</span><span class="sxs-lookup"><span data-stu-id="c3783-131">You should prefer to make the properties in your event argument type immutable.</span></span> <span data-ttu-id="c3783-132">W ten sposób jeden subskrybent nie może zmienić wartości, zanim inny subskrybent je zobaczy.</span><span class="sxs-lookup"><span data-stu-id="c3783-132">That way, one subscriber cannot change the values before another subscriber sees them.</span></span> <span data-ttu-id="c3783-133">(Istnieją wyjątki od tej reguły, jak widać poniżej.)</span><span class="sxs-lookup"><span data-stu-id="c3783-133">(There are exceptions to this, as you'll see below.)</span></span>  

<span data-ttu-id="c3783-134">Następnie należy utworzyć deklarację zdarzenia w FileSearcher klasy.</span><span class="sxs-lookup"><span data-stu-id="c3783-134">Next, we need to create the event declaration in the FileSearcher class.</span></span> <span data-ttu-id="c3783-135">Wykorzystanie `EventHandler<T>` tego typu oznacza, że nie trzeba tworzyć jeszcze innej definicji typu.</span><span class="sxs-lookup"><span data-stu-id="c3783-135">Leveraging the `EventHandler<T>` type means that you don't need to create yet another type definition.</span></span> <span data-ttu-id="c3783-136">Wystarczy użyć ogólnej specjalizacji.</span><span class="sxs-lookup"><span data-stu-id="c3783-136">You simply use a generic specialization.</span></span>

<span data-ttu-id="c3783-137">Wypełnijmy FileSearcher klasy, aby wyszukać pliki, które pasują do wzorca i podnieść poprawne zdarzenie, gdy dopasowanie jest wykrywany.</span><span class="sxs-lookup"><span data-stu-id="c3783-137">Let's fill out the FileSearcher class to search for files that match a pattern, and raise the correct event when a match is discovered.</span></span>

[!code-csharp[FileSearcher](../../samples/snippets/csharp/events/Program.cs#FileSearcherV1 "Create the initial file searcher")]

## <a name="defining-and-raising-field-like-events"></a><span data-ttu-id="c3783-138">Definiowanie i podnoszenie zdarzeń podobnych do pola</span><span class="sxs-lookup"><span data-stu-id="c3783-138">Defining and Raising Field-Like Events</span></span>

<span data-ttu-id="c3783-139">Najprostszym sposobem dodania zdarzenia do klasy jest zadeklarowanie tego zdarzenia jako pola publicznego, jak w poprzednim przykładzie:</span><span class="sxs-lookup"><span data-stu-id="c3783-139">The simplest way to add an event to your class is to declare that event as a public field, as in the preceding example:</span></span>

[!code-csharp[DeclareEvent](../../samples/snippets/csharp/events/Program.cs#DeclareEvent "Declare the file found event")]

<span data-ttu-id="c3783-140">Wygląda na to, że deklaruje pole publiczne, które wydaje się być złą praktyką obiektową.</span><span class="sxs-lookup"><span data-stu-id="c3783-140">This looks like it's declaring a public field, which would appear to be bad object-oriented practice.</span></span> <span data-ttu-id="c3783-141">Chcesz chronić dostęp do danych za pomocą właściwości lub metod.</span><span class="sxs-lookup"><span data-stu-id="c3783-141">You want to protect data access through properties, or methods.</span></span> <span data-ttu-id="c3783-142">Chociaż może to wyglądać na złe praktyki, kod generowany przez kompilator tworzy otoki, dzięki czemu obiekty zdarzeń są dostępne tylko w bezpieczny sposób.</span><span class="sxs-lookup"><span data-stu-id="c3783-142">While this may look like a bad practice, the code generated by the compiler does create wrappers so that the event objects can only be accessed in safe ways.</span></span> <span data-ttu-id="c3783-143">Jedynymi operacjami dostępnymi w zdarzeniu przypominanym z pola są program obsługi dodawania:</span><span class="sxs-lookup"><span data-stu-id="c3783-143">The only operations available on a field-like event are add handler:</span></span>

[!code-csharp[DeclareEventHandler](../../samples/snippets/csharp/events/Program.cs#DeclareEventHandler "Declare the file found event handler")]

<span data-ttu-id="c3783-144">i usuń program obsługi:</span><span class="sxs-lookup"><span data-stu-id="c3783-144">and remove handler:</span></span>

[!code-csharp[RemoveEventHandler](../../samples/snippets/csharp/events/Program.cs#RemoveHandler "Remove the event handler")]

<span data-ttu-id="c3783-145">Należy zauważyć, że istnieje zmienna lokalna dla programu obsługi.</span><span class="sxs-lookup"><span data-stu-id="c3783-145">Note that there's a local variable for the handler.</span></span> <span data-ttu-id="c3783-146">Jeśli używasz ciała lambda, usunięcie nie będzie działać poprawnie.</span><span class="sxs-lookup"><span data-stu-id="c3783-146">If you used the body of the lambda, the remove would not work correctly.</span></span> <span data-ttu-id="c3783-147">Byłoby to inne wystąpienie delegata i po cichu nic nie robić.</span><span class="sxs-lookup"><span data-stu-id="c3783-147">It would be a different instance of the delegate, and silently do nothing.</span></span>

<span data-ttu-id="c3783-148">Kod poza klasą nie może wywołać zdarzenia, ani nie może wykonywać żadnych innych operacji.</span><span class="sxs-lookup"><span data-stu-id="c3783-148">Code outside the class cannot raise the event, nor can it perform any other operations.</span></span>

## <a name="returning-values-from-event-subscribers"></a><span data-ttu-id="c3783-149">Zwracanie wartości od subskrybentów zdarzeń</span><span class="sxs-lookup"><span data-stu-id="c3783-149">Returning Values from Event Subscribers</span></span>

<span data-ttu-id="c3783-150">Twoja prosta wersja działa dobrze.</span><span class="sxs-lookup"><span data-stu-id="c3783-150">Your simple version is working fine.</span></span> <span data-ttu-id="c3783-151">Dodajmy kolejną funkcję: Anulowanie.</span><span class="sxs-lookup"><span data-stu-id="c3783-151">Let's add another feature: Cancellation.</span></span>

<span data-ttu-id="c3783-152">Po wywołaniu znalezionego zdarzenia, detektory powinny być w stanie zatrzymać dalsze przetwarzanie, jeśli ten plik jest ten ostatni poszukiwany.</span><span class="sxs-lookup"><span data-stu-id="c3783-152">When you raise the found event, listeners should be able to stop further processing, if this file is that last one sought.</span></span>

<span data-ttu-id="c3783-153">Programy obsługi zdarzeń nie zwracają wartość, więc należy komunikować się, że w inny sposób.</span><span class="sxs-lookup"><span data-stu-id="c3783-153">The event handlers do not return a value, so you need to communicate that in another way.</span></span> <span data-ttu-id="c3783-154">Standardowy wzorzec zdarzenia używa EventArgs obiektu do uwzględnienia pól, które subskrybenci zdarzeń mogą używać do komunikowania się anulować.</span><span class="sxs-lookup"><span data-stu-id="c3783-154">The standard event pattern uses the EventArgs object to include fields that event subscribers can use to communicate cancel.</span></span>

<span data-ttu-id="c3783-155">Istnieją dwa różne wzorce, które mogą być używane, na podstawie semantyki anuluj umowy.</span><span class="sxs-lookup"><span data-stu-id="c3783-155">There are two different patterns that could be used, based on the semantics of the Cancel contract.</span></span> <span data-ttu-id="c3783-156">W obu przypadkach do zdarzenia znalezionego pliku zostanie dodane pole logiczne.</span><span class="sxs-lookup"><span data-stu-id="c3783-156">In both cases, you'll add a boolean field to the EventArguments for the found file event.</span></span>

<span data-ttu-id="c3783-157">Jeden wzorzec pozwoli jeden subskrybent anulować operację.</span><span class="sxs-lookup"><span data-stu-id="c3783-157">One pattern would allow any one subscriber to cancel the operation.</span></span>
<span data-ttu-id="c3783-158">Dla tego wzorca nowe pole `false`jest inicjowane do .</span><span class="sxs-lookup"><span data-stu-id="c3783-158">For this pattern, the new field is initialized to `false`.</span></span> <span data-ttu-id="c3783-159">Każdy subskrybent może go `true`zmienić na .</span><span class="sxs-lookup"><span data-stu-id="c3783-159">Any subscriber can change it to `true`.</span></span> <span data-ttu-id="c3783-160">Po wszystkich subskrybentów widzieli zdarzenie wywoływane, FileSearcher składnik sprawdza wartość logiczną i wykonuje akcję.</span><span class="sxs-lookup"><span data-stu-id="c3783-160">After all subscribers have seen the event raised, the FileSearcher component examines the boolean value and takes action.</span></span>

<span data-ttu-id="c3783-161">Drugi wzorzec tylko anulować operację, jeśli wszyscy subskrybenci chcieli operacji anulowane.</span><span class="sxs-lookup"><span data-stu-id="c3783-161">The second pattern would only cancel the operation if all subscribers wanted the operation cancelled.</span></span> <span data-ttu-id="c3783-162">W tym wzorcu nowe pole jest inicjowane, aby wskazać, że operacja powinna zostać anulowana, a każdy subskrybent może ją zmienić, aby wskazać, że operacja powinna być kontynuowana.</span><span class="sxs-lookup"><span data-stu-id="c3783-162">In this pattern, the new field is initialized to indicate the operation should cancel, and any subscriber could change it to indicate the operation should continue.</span></span>
<span data-ttu-id="c3783-163">Po wszystkich subskrybentów widzieli zdarzenie wywoływane, FileSearcher składnik sprawdza wartość logiczną i wykonuje akcję.</span><span class="sxs-lookup"><span data-stu-id="c3783-163">After all subscribers have seen the event raised, the FileSearcher component examines the boolean and takes action.</span></span> <span data-ttu-id="c3783-164">Istnieje jeden dodatkowy krok w tym wzorcu: składnik musi wiedzieć, czy subskrybenci widzieli zdarzenie.</span><span class="sxs-lookup"><span data-stu-id="c3783-164">There is one extra step in this pattern: the component needs to know if any subscribers have seen the event.</span></span> <span data-ttu-id="c3783-165">Jeśli nie ma subskrybentów, pole będzie oznaczać anuluj niepoprawnie.</span><span class="sxs-lookup"><span data-stu-id="c3783-165">If there are no subscribers, the field would indicate a cancel incorrectly.</span></span>

<span data-ttu-id="c3783-166">Zaimplementujmy pierwszą wersję dla tego przykładu.</span><span class="sxs-lookup"><span data-stu-id="c3783-166">Let's implement the first version for this sample.</span></span> <span data-ttu-id="c3783-167">Do `FileFoundArgs` typu należy dodać pole `CancelRequested` logiczne o nazwie:</span><span class="sxs-lookup"><span data-stu-id="c3783-167">You need to add a boolean field named `CancelRequested` to the `FileFoundArgs` type:</span></span>

[!code-csharp[EventArgs](../../samples/snippets/csharp/events/Program.cs#EventArgs "Update event arguments")]

<span data-ttu-id="c3783-168">To nowe pole jest automatycznie `false`inicjowane do wartości domyślnej dla pola logicznego, dzięki czemu nie można anulować przypadkowo.</span><span class="sxs-lookup"><span data-stu-id="c3783-168">This new Field is automatically initialized to `false`, the default value for a Boolean field, so you don't cancel accidentally.</span></span> <span data-ttu-id="c3783-169">Jedyną inną zmianą w składniku jest sprawdzenie flagi po zgłoszeniu zdarzenia, aby sprawdzić, czy którykolwiek z subskrybentów zażądał anulowania:</span><span class="sxs-lookup"><span data-stu-id="c3783-169">The only other change to the component is to check the flag after raising the event to see if any of the subscribers have requested a cancellation:</span></span>

```csharp
public void List(string directory, string searchPattern)
{
    foreach (var file in Directory.EnumerateFiles(directory, searchPattern))
    {
        var args = new FileFoundArgs(file);
        FileFound?.Invoke(this, args);
        if (args.CancelRequested)
            break;
    }
}
```

<span data-ttu-id="c3783-170">Jedną z zalet tego wzorca jest to, że nie jest to przełomowa zmiana.</span><span class="sxs-lookup"><span data-stu-id="c3783-170">One advantage of this pattern is that it isn't a breaking change.</span></span>
<span data-ttu-id="c3783-171">Żaden z subskrybentów zażądał anulowania wcześniej i nadal nie są.</span><span class="sxs-lookup"><span data-stu-id="c3783-171">None of the subscribers requested a cancellation before, and they still are not.</span></span> <span data-ttu-id="c3783-172">Żaden z kodu subskrybenta wymaga aktualizacji, chyba że chce obsługiwać nowy protokół anulowania.</span><span class="sxs-lookup"><span data-stu-id="c3783-172">None of the subscriber code needs updating unless they want to support the new cancel protocol.</span></span> <span data-ttu-id="c3783-173">Jest bardzo luźno sprzęgł.</span><span class="sxs-lookup"><span data-stu-id="c3783-173">It's very loosely coupled.</span></span>

<span data-ttu-id="c3783-174">Zaktualizujmy subskrybenta tak, aby żądał anulowania po znalezieniu pierwszego pliku wykonywalnego:</span><span class="sxs-lookup"><span data-stu-id="c3783-174">Let's update the subscriber so that it requests a cancellation once it finds the first executable:</span></span>

```csharp
EventHandler<FileFoundArgs> onFileFound = (sender, eventArgs) =>
{
    Console.WriteLine(eventArgs.FoundFile);
    eventArgs.CancelRequested = true;
};
```

## <a name="adding-another-event-declaration"></a><span data-ttu-id="c3783-175">Dodawanie innej deklaracji zdarzenia</span><span class="sxs-lookup"><span data-stu-id="c3783-175">Adding Another Event Declaration</span></span>

<span data-ttu-id="c3783-176">Dodajmy jeszcze jedną funkcję i zademonstrujmy inne idiomy języka dla zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="c3783-176">Let's add one more feature, and demonstrate other language idioms for events.</span></span> <span data-ttu-id="c3783-177">Dodajmy przeciążenie `Search` metody, która przechodzi przez wszystkie podkatalogi w poszukiwaniu plików.</span><span class="sxs-lookup"><span data-stu-id="c3783-177">Let's add an overload of the `Search` method that traverses all subdirectories in search of files.</span></span>

<span data-ttu-id="c3783-178">Może to być długa operacja w katalogu z wieloma podkatalogami.</span><span class="sxs-lookup"><span data-stu-id="c3783-178">This could get to be a lengthy operation in a directory with many sub-directories.</span></span> <span data-ttu-id="c3783-179">Dodajmy zdarzenie, które zostanie podniesione po rozpoczęciu każdego nowego wyszukiwania w katalogu.</span><span class="sxs-lookup"><span data-stu-id="c3783-179">Let's add an event that gets raised when each new directory search begins.</span></span> <span data-ttu-id="c3783-180">Dzięki temu subskrybenci śledzić postęp i zaktualizować użytkownika w miarę postępu.</span><span class="sxs-lookup"><span data-stu-id="c3783-180">This enables subscribers to track progress, and update the user as to progress.</span></span> <span data-ttu-id="c3783-181">Wszystkie przykłady, które zostały utworzone do tej pory są publiczne.</span><span class="sxs-lookup"><span data-stu-id="c3783-181">All the samples you've created so far are public.</span></span> <span data-ttu-id="c3783-182">Zróbmy to wydarzenie wewnętrzne.</span><span class="sxs-lookup"><span data-stu-id="c3783-182">Let's make this one an internal event.</span></span> <span data-ttu-id="c3783-183">Oznacza to, że można również dokonać typów używanych dla argumentów wewnętrzny, jak również.</span><span class="sxs-lookup"><span data-stu-id="c3783-183">That means you can also make the types used for the arguments internal as well.</span></span>

<span data-ttu-id="c3783-184">Rozpoczniesz od utworzenia nowej klasy pochodnej EventArgs do raportowania nowego katalogu i postępu.</span><span class="sxs-lookup"><span data-stu-id="c3783-184">You'll start by creating the new EventArgs derived class for reporting the new directory and progress.</span></span>

[!code-csharp[DirEventArgs](../../samples/snippets/csharp/events/Program.cs#SearchDirEventArgs "Define search directory event arguments")]

<span data-ttu-id="c3783-185">Ponownie można wykonać zalecenia, aby niezmienny typ odwołania dla argumentów zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="c3783-185">Again, you can follow the recommendations to make an immutable reference type for the event arguments.</span></span>

<span data-ttu-id="c3783-186">Następnie zdefiniuj zdarzenie.</span><span class="sxs-lookup"><span data-stu-id="c3783-186">Next, define the event.</span></span> <span data-ttu-id="c3783-187">Tym razem użyjesz innej składni.</span><span class="sxs-lookup"><span data-stu-id="c3783-187">This time, you'll use a different syntax.</span></span> <span data-ttu-id="c3783-188">Oprócz używania składni pola, można jawnie utworzyć właściwość, z dodawaniem i usuwaniem programów obsługi.</span><span class="sxs-lookup"><span data-stu-id="c3783-188">In addition to using the field syntax, you can explicitly create the property, with add and remove handlers.</span></span> <span data-ttu-id="c3783-189">W tym przykładzie nie będzie potrzebny dodatkowy kod w tych programach obsługi, ale to pokazuje, jak można je utworzyć.</span><span class="sxs-lookup"><span data-stu-id="c3783-189">In this sample, you won't need extra code in those handlers, but this shows how you would create them.</span></span>

[!code-csharp[Declare event with add and remove handlers](../../samples/snippets/csharp/events/Program.cs#DeclareSearchEvent "Declare the event with add and remove handlers")]

<span data-ttu-id="c3783-190">Na wiele sposobów kod, który piszesz w tym miejscu odzwierciedla kod kompilator generuje dla definicji zdarzeń pola, które widziałeś wcześniej.</span><span class="sxs-lookup"><span data-stu-id="c3783-190">In many ways, the code you write here mirrors the code the compiler generates for the field event definitions you've seen earlier.</span></span> <span data-ttu-id="c3783-191">Zdarzenie można utworzyć przy użyciu składni bardzo podobnej do tej używanej dla [właściwości](properties.md).</span><span class="sxs-lookup"><span data-stu-id="c3783-191">You create the event using syntax very similar to that used for [properties](properties.md).</span></span> <span data-ttu-id="c3783-192">Należy zauważyć, że programy `add` obsługi `remove`mają różne nazwy: i .</span><span class="sxs-lookup"><span data-stu-id="c3783-192">Notice that the handlers have different names: `add` and `remove`.</span></span> <span data-ttu-id="c3783-193">Są one wywoływane, aby zapisać się do zdarzenia lub zrezygnować z wydarzenia.</span><span class="sxs-lookup"><span data-stu-id="c3783-193">These are called to subscribe to the event, or unsubscribe from the event.</span></span> <span data-ttu-id="c3783-194">Należy zauważyć, że należy również zadeklarować prywatne pole zapasowe do przechowywania zmiennej zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="c3783-194">Notice that you also must declare a private backing field to store the event variable.</span></span> <span data-ttu-id="c3783-195">Jest inicjowany do wartości null.</span><span class="sxs-lookup"><span data-stu-id="c3783-195">It is initialized to null.</span></span>

<span data-ttu-id="c3783-196">Następnie dodajmy przeciążenie `Search` metody, która przechodzi przez podkatalogi i wywołuje oba zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="c3783-196">Next, let's add the overload of the `Search` method that traverses subdirectories and raises both events.</span></span> <span data-ttu-id="c3783-197">Najprostszym sposobem osiągnięcia tego celu jest użycie argumentu domyślnego w celu określenia, że chcesz przeszukać wszystkie katalogi:</span><span class="sxs-lookup"><span data-stu-id="c3783-197">The easiest way to accomplish this is to use a default argument to specify that you want to search all directories:</span></span>

[!code-csharp[SearchImplementation](../../samples/snippets/csharp/events/Program.cs#FinalImplementation "Implementation to search directories")]

<span data-ttu-id="c3783-198">W tym momencie można uruchomić aplikację wywołującą przeciążenie do wyszukiwania wszystkich podkatalogów.</span><span class="sxs-lookup"><span data-stu-id="c3783-198">At this point, you can run the application calling the overload for searching all sub-directories.</span></span> <span data-ttu-id="c3783-199">Nie ma subskrybentów `ChangeDirectory` na nowe wydarzenie, ale przy użyciu `?.Invoke()` idiomu zapewnia, że to działa poprawnie.</span><span class="sxs-lookup"><span data-stu-id="c3783-199">There are no subscribers on the new `ChangeDirectory` event, but using the `?.Invoke()` idiom ensures that this works correctly.</span></span>

 <span data-ttu-id="c3783-200">Dodajmy program obsługi, aby napisać wiersz, który pokazuje postęp w oknie konsoli.</span><span class="sxs-lookup"><span data-stu-id="c3783-200">Let's add a handler to write a line that shows the progress in the console window.</span></span>

[!code-csharp[Search](../../samples/snippets/csharp/events/Program.cs#Search "Declare event handler")]

<span data-ttu-id="c3783-201">Widzieliście wzorce, które są przestrzegane w całym ekosystemie .NET.</span><span class="sxs-lookup"><span data-stu-id="c3783-201">You've seen patterns that are followed throughout the .NET ecosystem.</span></span>
<span data-ttu-id="c3783-202">Ucząc się tych wzorców i konwencji, będziesz pisać idiomatyczne C# i .NET szybko.</span><span class="sxs-lookup"><span data-stu-id="c3783-202">By learning these patterns and conventions, you'll be writing idiomatic C# and .NET quickly.</span></span>

<span data-ttu-id="c3783-203">Następnie zobaczysz pewne zmiany w tych wzorców w najnowszej wersji .NET.</span><span class="sxs-lookup"><span data-stu-id="c3783-203">Next, you'll see some changes in these patterns in the most recent release of .NET.</span></span>

[<span data-ttu-id="c3783-204">Dalej</span><span class="sxs-lookup"><span data-stu-id="c3783-204">Next</span></span>](modern-events.md)
