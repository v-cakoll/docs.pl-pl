---
title: 'Zarządzanie zasobami: Słowo kluczowe use'
description: Dowiedz się F# więcej o słowie kluczowym "use" i funkcji "Using", która może kontrolować inicjalizację i wydawanie zasobów.
ms.date: 05/16/2016
ms.openlocfilehash: 5e0838401bee02050343b2f6dcc646a8dc8b4dc0
ms.sourcegitcommit: f20dd18dbcf2275513281f5d9ad7ece6a62644b4
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 07/30/2019
ms.locfileid: "68627257"
---
# <a name="resource-management-the-use-keyword"></a><span data-ttu-id="bfa39-103">Zarządzanie zasobami: Słowo kluczowe use</span><span class="sxs-lookup"><span data-stu-id="bfa39-103">Resource Management: The use Keyword</span></span>

<span data-ttu-id="bfa39-104">W tym temacie opisano słowo `use` kluczowe `using` i funkcję, które mogą kontrolować inicjalizację i wydawanie zasobów.</span><span class="sxs-lookup"><span data-stu-id="bfa39-104">This topic describes the keyword `use` and the `using` function, which can control the initialization and release of resources.</span></span>

## <a name="resources"></a><span data-ttu-id="bfa39-105">Zasoby</span><span class="sxs-lookup"><span data-stu-id="bfa39-105">Resources</span></span>

<span data-ttu-id="bfa39-106">Termin *zasób* jest używany w więcej niż jeden sposób.</span><span class="sxs-lookup"><span data-stu-id="bfa39-106">The term *resource* is used in more than one way.</span></span> <span data-ttu-id="bfa39-107">Tak, zasoby mogą być danymi, które są używane przez aplikację, takich jak ciągi, grafika i podobne, ale w tym kontekście *zasoby* odnoszą się do zasobów oprogramowania lub systemu operacyjnego, takich jak konteksty urządzeń graficznych, dojścia do plików, Sieć i baza danych. połączenia, obiekty współbieżności, takie jak uchwyty oczekiwania i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="bfa39-107">Yes, resources can be data that an application uses, such as strings, graphics, and the like, but in this context, *resources* refers to software or operating system resources, such as graphics device contexts, file handles, network and database connections, concurrency objects such as wait handles, and so on.</span></span> <span data-ttu-id="bfa39-108">Użycie tych zasobów przez aplikacje obejmuje Nabycie zasobu od systemu operacyjnego lub innego dostawcy zasobów, a następnie późniejsze wydanie zasobu do puli, dzięki czemu można je dostarczyć innym aplikacjom.</span><span class="sxs-lookup"><span data-stu-id="bfa39-108">The use of these resources by applications involves the acquisition of the resource from the operating system or other resource provider, followed by the later release of the resource to the pool so that it can be provided to another application.</span></span> <span data-ttu-id="bfa39-109">Problemy występują, gdy aplikacje nie zwalniają zasobów z powrotem do wspólnej puli.</span><span class="sxs-lookup"><span data-stu-id="bfa39-109">Problems occur when applications do not release resources back to the common pool.</span></span>

## <a name="managing-resources"></a><span data-ttu-id="bfa39-110">Zarządzanie zasobami</span><span class="sxs-lookup"><span data-stu-id="bfa39-110">Managing Resources</span></span>

<span data-ttu-id="bfa39-111">Aby efektywnie i odpowiedzialnie zarządzać zasobami w aplikacji, musisz natychmiast zwolnić zasoby i w sposób przewidywalny.</span><span class="sxs-lookup"><span data-stu-id="bfa39-111">To efficiently and responsibly manage resources in an application, you must release resources promptly and in a predictable manner.</span></span> <span data-ttu-id="bfa39-112">.NET Framework pomaga to zrobić, dostarczając `System.IDisposable` interfejs.</span><span class="sxs-lookup"><span data-stu-id="bfa39-112">The .NET Framework helps you do this by providing the `System.IDisposable` interface.</span></span> <span data-ttu-id="bfa39-113">Typ, który implementuje `System.IDisposable` , `System.IDisposable.Dispose` ma metodę, która poprawnie zwalnia zasoby.</span><span class="sxs-lookup"><span data-stu-id="bfa39-113">A type that implements `System.IDisposable` has the `System.IDisposable.Dispose` method, which correctly frees resources.</span></span> <span data-ttu-id="bfa39-114">Dobrze napisano aplikacje gwarantujące, że jest wywoływana natychmiast, `System.IDisposable.Dispose` gdy dowolny obiekt, który przechowuje ograniczony zasób, nie jest już wymagany.</span><span class="sxs-lookup"><span data-stu-id="bfa39-114">Well-written applications guarantee that `System.IDisposable.Dispose` is called promptly when any object that holds a limited resource is no longer needed.</span></span> <span data-ttu-id="bfa39-115">Na szczęście większość języków .NET zapewnia pomoc techniczną i F# nie ma wyjątku.</span><span class="sxs-lookup"><span data-stu-id="bfa39-115">Fortunately, most .NET languages provide support to make this easier, and F# is no exception.</span></span> <span data-ttu-id="bfa39-116">Istnieją dwa przydatne konstrukcje języka, które obsługują wzorzec Dispose: `use` powiązanie `using` i funkcję.</span><span class="sxs-lookup"><span data-stu-id="bfa39-116">There are two useful language constructs that support the dispose pattern: the `use` binding and the `using` function.</span></span>

## <a name="use-binding"></a><span data-ttu-id="bfa39-117">Użyj powiązania</span><span class="sxs-lookup"><span data-stu-id="bfa39-117">use Binding</span></span>

<span data-ttu-id="bfa39-118">Słowo kluczowe ma postać podobną `let` do powiązania: `use`</span><span class="sxs-lookup"><span data-stu-id="bfa39-118">The `use` keyword has a form that resembles that of the `let` binding:</span></span>

<span data-ttu-id="bfa39-119">Użyj*wyrażenia* *wartości* = </span><span class="sxs-lookup"><span data-stu-id="bfa39-119">use *value* = *expression*</span></span>

<span data-ttu-id="bfa39-120">Zapewnia takie same funkcje jak `let` powiązanie, ale dodaje wywołanie do `Dispose` wartości, gdy wartość wykracza poza zakres.</span><span class="sxs-lookup"><span data-stu-id="bfa39-120">It provides the same functionality as a `let` binding but adds a call to `Dispose` on the value when the value goes out of scope.</span></span> <span data-ttu-id="bfa39-121">Należy zauważyć, że kompilator wstawia sprawdzanie wartości null na wartość, więc jeśli wartość jest `null`, `Dispose` wywołanie nie jest podejmowane.</span><span class="sxs-lookup"><span data-stu-id="bfa39-121">Note that the compiler inserts a null check on the value, so that if the value is `null`, the call to `Dispose` is not attempted.</span></span>

<span data-ttu-id="bfa39-122">Poniższy przykład pokazuje, `use` jak zamknąć plik automatycznie przy użyciu słowa kluczowego.</span><span class="sxs-lookup"><span data-stu-id="bfa39-122">The following example shows how to close a file automatically by using the `use` keyword.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-2/snippet6301.fs)]

> [!NOTE]
> <span data-ttu-id="bfa39-123">Można używać `use` w wyrażeniach obliczeniowych, w których przypadku używana jest dostosowana `use` wersja wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="bfa39-123">You can use `use` in computation expressions, in which case a customized version of the `use` expression is used.</span></span> <span data-ttu-id="bfa39-124">Aby uzyskać więcej informacji, [](sequences.md)Zobacz sekwencje, [asynchroniczne przepływy pracy](asynchronous-workflows.md)i [wyrażenia obliczeń](computation-expressions.md).</span><span class="sxs-lookup"><span data-stu-id="bfa39-124">For more information, see [Sequences](sequences.md), [Asynchronous Workflows](asynchronous-workflows.md), and [Computation Expressions](computation-expressions.md).</span></span>

## <a name="using-function"></a><span data-ttu-id="bfa39-125">Funkcja using</span><span class="sxs-lookup"><span data-stu-id="bfa39-125">using Function</span></span>

<span data-ttu-id="bfa39-126">`using` Funkcja ma następującą postać:</span><span class="sxs-lookup"><span data-stu-id="bfa39-126">The `using` function has the following form:</span></span>

<span data-ttu-id="bfa39-127">`using`(*wyrażenie1*) *Function-lub-lambda*</span><span class="sxs-lookup"><span data-stu-id="bfa39-127">`using` (*expression1*) *function-or-lambda*</span></span>

<span data-ttu-id="bfa39-128">W wyrażeniu wyrażenie1 tworzy obiekt, który musi zostać usunięty. `using`</span><span class="sxs-lookup"><span data-stu-id="bfa39-128">In a `using` expression, *expression1* creates the object that must be disposed.</span></span> <span data-ttu-id="bfa39-129">Wynik *wyrażenie1* (obiekt, który musi zostać usunięty), jest argumentem, *wartością*, do *funkcji-lub-lambda*, która jest funkcją, która oczekuje jednego pozostałego argumentu typu, który pasuje do wartości wygenerowanej przez  *wyrażenie1*lub wyrażenie lambda, które oczekuje argumentu tego typu.</span><span class="sxs-lookup"><span data-stu-id="bfa39-129">The result of *expression1* (the object that must be disposed) becomes an argument, *value*, to *function-or-lambda*, which is either a function that expects a single remaining argument of a type that matches the value produced by *expression1*, or a lambda expression that expects an argument of that type.</span></span> <span data-ttu-id="bfa39-130">Po zakończeniu wykonywania funkcji środowisko uruchomieniowe wywołuje `Dispose` i zwalnia zasoby (chyba że jest `null`to wartość, w takim przypadku nie jest podejmowana próba wywołania metody Dispose).</span><span class="sxs-lookup"><span data-stu-id="bfa39-130">At the end of the execution of the function, the runtime calls `Dispose` and frees the resources (unless the value is `null`, in which case the call to Dispose is not attempted).</span></span>

<span data-ttu-id="bfa39-131">Poniższy przykład ilustruje `using` wyrażenie z wyrażeniem lambda.</span><span class="sxs-lookup"><span data-stu-id="bfa39-131">The following example demonstrates the `using` expression with a lambda expression.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-2/snippet6302.fs)]

<span data-ttu-id="bfa39-132">W następnym przykładzie pokazano `using` wyrażenie z funkcją.</span><span class="sxs-lookup"><span data-stu-id="bfa39-132">The next example shows the `using` expression with a function.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-2/snippet6303.fs)]

<span data-ttu-id="bfa39-133">Należy zauważyć, że funkcja może być funkcją, która ma już zastosowane argumenty.</span><span class="sxs-lookup"><span data-stu-id="bfa39-133">Note that the function could be a function that has some arguments applied already.</span></span> <span data-ttu-id="bfa39-134">Poniższy przykład kodu demonstruje ten sposób.</span><span class="sxs-lookup"><span data-stu-id="bfa39-134">The following code example demonstrates this.</span></span> <span data-ttu-id="bfa39-135">Tworzy plik, który zawiera ciąg `XYZ`.</span><span class="sxs-lookup"><span data-stu-id="bfa39-135">It creates a file that contains the string `XYZ`.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-2/snippet6304.fs)]

<span data-ttu-id="bfa39-136">`using` Funkcja`use` i powiązanie są niemal równoważnymi sposobami wykonywania tego samego celu.</span><span class="sxs-lookup"><span data-stu-id="bfa39-136">The `using` function and the `use` binding are nearly equivalent ways to accomplish the same thing.</span></span> <span data-ttu-id="bfa39-137">Słowo `using` kluczowe zapewnia większą kontrolę nad tym `Dispose` , kiedy jest wywoływana.</span><span class="sxs-lookup"><span data-stu-id="bfa39-137">The `using` keyword provides more control over when `Dispose` is called.</span></span> <span data-ttu-id="bfa39-138">Gdy używasz `Dispose` `use` , jest wywoływana na końcu funkcji lub wyrażenia lambda; w przypadku użycia słowa kluczowego `Dispose` jest wywoływana na końcu bloku zawierającego kod. `using`</span><span class="sxs-lookup"><span data-stu-id="bfa39-138">When you use `using`, `Dispose` is called at the end of the function or lambda expression; when you use the `use` keyword, `Dispose` is called at the end of the containing code block.</span></span> <span data-ttu-id="bfa39-139">Ogólnie rzecz biorąc należy używać `use` zamiast `using` funkcji.</span><span class="sxs-lookup"><span data-stu-id="bfa39-139">In general, you should prefer to use `use` instead of the `using` function.</span></span>

## <a name="see-also"></a><span data-ttu-id="bfa39-140">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="bfa39-140">See also</span></span>

- [<span data-ttu-id="bfa39-141">Dokumentacja języka F#</span><span class="sxs-lookup"><span data-stu-id="bfa39-141">F# Language Reference</span></span>](index.md)
