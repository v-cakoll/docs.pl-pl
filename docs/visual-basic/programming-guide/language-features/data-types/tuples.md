---
title: Krotki w Visual Basic
ms.date: 04/23/2017
helpviewer_keywords:
- tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
ms.openlocfilehash: fdca36e47d0b1234a8964d7475354a726a61f085
ms.sourcegitcommit: 4f4a32a5c16a75724920fa9627c59985c41e173c
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 10/17/2019
ms.locfileid: "72524580"
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="19080-102">Krotki (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="19080-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="19080-103">Począwszy od Visual Basic 2017, język Visual Basic oferuje wbudowaną obsługę spójnych krotek, które ułatwiają tworzenie spójnych krotek i uzyskiwanie dostępu do elementów krotek.</span><span class="sxs-lookup"><span data-stu-id="19080-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="19080-104">Krotka jest lekkim strukturą danych, która ma określoną liczbę i sekwencję wartości.</span><span class="sxs-lookup"><span data-stu-id="19080-104">A tuple is a lightweight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="19080-105">Podczas tworzenia wystąpienia krotki należy zdefiniować liczbę i typ danych każdej wartości (lub elementu).</span><span class="sxs-lookup"><span data-stu-id="19080-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="19080-106">Na przykład 2-krotka (lub para) ma dwa elementy.</span><span class="sxs-lookup"><span data-stu-id="19080-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="19080-107">Pierwsza może być `Boolean` wartość, podczas gdy druga jest `String`.</span><span class="sxs-lookup"><span data-stu-id="19080-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="19080-108">Ponieważ krotki ułatwiają przechowywanie wielu wartości w pojedynczym obiekcie, są one często używane jako lekki sposób zwracania wielu wartości z metody.</span><span class="sxs-lookup"><span data-stu-id="19080-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="19080-109">Obsługa krotki wymaga typu <xref:System.ValueTuple>.</span><span class="sxs-lookup"><span data-stu-id="19080-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="19080-110">Jeśli .NET Framework 4,7 nie jest zainstalowana, należy dodać pakiet NuGet `System.ValueTuple`, który jest dostępny w galerii NuGet.</span><span class="sxs-lookup"><span data-stu-id="19080-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="19080-111">Bez tego pakietu może zostać wyświetlony komunikat o błędzie kompilacji podobny do: "wstępnie zdefiniowany typ" ValueTuple (z,,,) "nie został zdefiniowany ani zaimportowany."</span><span class="sxs-lookup"><span data-stu-id="19080-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="19080-112">Tworzenie wystąpień i używanie spójnej kolekcji</span><span class="sxs-lookup"><span data-stu-id="19080-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="19080-113">Tworzysz wystąpienie spójnej kolekcji, umieszczając ich wartości rozdzielane przecinkami.</span><span class="sxs-lookup"><span data-stu-id="19080-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="19080-114">Każda z tych wartości zostaje następnie do pola krotki.</span><span class="sxs-lookup"><span data-stu-id="19080-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="19080-115">Na przykład poniższy kod definiuje potrójną (lub 3-krotkę) z `Date` jako pierwszą wartością, `String` jako sekundę i `Boolean` jako trzecią.</span><span class="sxs-lookup"><span data-stu-id="19080-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="19080-116">Domyślnie nazwa każdego pola w kolekcji składa się z ciągu `Item` wraz z jedną pozycją opartą na tym polu w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="19080-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="19080-117">W przypadku tej 3-spójnej kolekcji pole `Date` jest `Item1`, pole `String` jest `Item2`, a pole `Boolean` jest `Item3`.</span><span class="sxs-lookup"><span data-stu-id="19080-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="19080-118">Poniższy przykład wyświetla wartości pól krotki utworzone w poprzednim wierszu kodu</span><span class="sxs-lookup"><span data-stu-id="19080-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="19080-119">Pola krotki Visual Basic są do odczytu i zapisu; Po utworzeniu wystąpienia spójnej kolekcji można zmodyfikować jej wartości.</span><span class="sxs-lookup"><span data-stu-id="19080-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="19080-120">Poniższy przykład modyfikuje dwa z trzech pól krotki utworzonej w poprzednim przykładzie i wyświetla wynik.</span><span class="sxs-lookup"><span data-stu-id="19080-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="19080-121">Tworzenie wystąpienia i używanie nazwanej krotki</span><span class="sxs-lookup"><span data-stu-id="19080-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="19080-122">Zamiast używać domyślnych nazw dla pól krotki, można utworzyć wystąpienie *nazwanej krotki* przez przypisanie własnych nazw do elementów krotki.</span><span class="sxs-lookup"><span data-stu-id="19080-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="19080-123">Dostęp do pól krotki można uzyskać za pomocą przypisanych im nazw *lub* według ich nazw domyślnych.</span><span class="sxs-lookup"><span data-stu-id="19080-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="19080-124">Poniższy przykład tworzy wystąpienie tej samej 3-spójnej kolekcji jak poprzednio, z tą różnicą, że jawnie nazywa pierwsze pole `EventDate`, drugi `Name` i trzeci `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="19080-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="19080-125">Następnie wyświetla wartości pól, modyfikuje je i ponownie wyświetla wartości pól.</span><span class="sxs-lookup"><span data-stu-id="19080-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="inferred-tuple-element-names"></a><span data-ttu-id="19080-126">Wywnioskowane nazwy elementów krotki</span><span class="sxs-lookup"><span data-stu-id="19080-126">Inferred tuple element names</span></span>

<span data-ttu-id="19080-127">Począwszy od Visual Basic 15,3, Visual Basic może wywnioskować nazwy elementów krotki; nie trzeba ich jawnie przypisywać.</span><span class="sxs-lookup"><span data-stu-id="19080-127">Starting with Visual Basic 15.3, Visual Basic can infer the names of tuple elements; you do not have to assign them explicitly.</span></span> <span data-ttu-id="19080-128">Wywnioskowane nazwy krotek są przydatne w przypadku inicjowania krotki z zestawu zmiennych i chcesz, aby nazwa elementu krotki była taka sama jak nazwa zmiennej.</span><span class="sxs-lookup"><span data-stu-id="19080-128">Inferred tuple names are useful when you initialize a tuple from a set of variables, and you want the tuple element name to be the same as the variable name.</span></span>

<span data-ttu-id="19080-129">Poniższy przykład tworzy krotkę `stateInfo`, która zawiera trzy jawnie nazwane elementy, `state`, `stateName` i `capital`.</span><span class="sxs-lookup"><span data-stu-id="19080-129">The following example creates a `stateInfo` tuple that contains three explicitly named elements, `state`, `stateName`, and `capital`.</span></span> <span data-ttu-id="19080-130">Należy zauważyć, że w przypadku nazywania elementów instrukcja inicjowania krotki po prostu przypisuje nazwane elementy wartości zmiennych o identycznych nazwach.</span><span class="sxs-lookup"><span data-stu-id="19080-130">Note that, in naming the elements, the tuple initialization statement simply assigns the named elements the values of the identically named variables.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#1)]

<span data-ttu-id="19080-131">Ponieważ elementy i zmienne mają taką samą nazwę, kompilator Visual Basic może wywnioskować nazwy pól, jak pokazano w poniższym przykładzie.</span><span class="sxs-lookup"><span data-stu-id="19080-131">Because elements and variables have the same name, the Visual Basic compiler can infer the names of the fields, as the following example shows.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#2)]

<span data-ttu-id="19080-132">Aby włączyć wywnioskowane nazwy elementów krotki, należy zdefiniować wersję kompilatora Visual Basic, która ma być używana w pliku projektu Visual Basic (\*. vbproj):</span><span class="sxs-lookup"><span data-stu-id="19080-132">To enable inferred tuple element names, you must define the version of the Visual Basic compiler to use in your Visual Basic project (\*.vbproj) file:</span></span>

```xml
<PropertyGroup>
  <LangVersion>15.3</LangVersion>
</PropertyGroup>
```

<span data-ttu-id="19080-133">Numer wersji może być dowolną wersją kompilatora Visual Basic rozpoczynającą się od 15,3.</span><span class="sxs-lookup"><span data-stu-id="19080-133">The version number can be any version of the Visual Basic compiler starting with 15.3.</span></span> <span data-ttu-id="19080-134">Zamiast nakodować twardą wersję kompilatora, można także określić "Najnowsza" jako wartość `LangVersion` do skompilowania przy użyciu najnowszej wersji kompilatora Visual Basic zainstalowanej w systemie.</span><span class="sxs-lookup"><span data-stu-id="19080-134">Rather than hard-coding a specific compiler version, you can also specify "Latest" as the value of `LangVersion` to compile with the most recent version of the Visual Basic compiler installed on your system.</span></span>

<span data-ttu-id="19080-135">Aby uzyskać więcej informacji, zobacz [Ustawianie wersji językowej Visual Basic](../../../language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="19080-135">For more information, see [setting the Visual Basic language version](../../../language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="19080-136">W niektórych przypadkach kompilator Visual Basic nie może wywnioskować nazwy elementu krotki z nazwy kandydującej, a do pola krotki można odwoływać się tylko przy użyciu nazwy domyślnej, takiej jak `Item1`, `Item2` itd. Należą do nich:</span><span class="sxs-lookup"><span data-stu-id="19080-136">In some cases, the Visual Basic compiler cannot infer the tuple element name from the candidate name, and the tuple field can only be referenced using its default name, such as `Item1`, `Item2`, etc. These include:</span></span>

- <span data-ttu-id="19080-137">Nazwa kandydata jest taka sama jak nazwa składowej spójnej kolekcji, taka jak `Item3`, `Rest` lub `ToString`.</span><span class="sxs-lookup"><span data-stu-id="19080-137">The candidate name is the same as the name of a tuple member, such as `Item3`, `Rest`, or `ToString`.</span></span>

- <span data-ttu-id="19080-138">Nazwa kandydata jest zduplikowana w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="19080-138">The candidate name is duplicated in the tuple.</span></span>

<span data-ttu-id="19080-139">Gdy wnioskowanie o nazwie pola nie powiedzie się, Visual Basic nie generuje błędu kompilatora ani nie występuje wyjątek w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="19080-139">When field name inference fails, Visual Basic does not generate a compiler error, nor is an exception thrown at runtime.</span></span> <span data-ttu-id="19080-140">Zamiast tego pola krotek muszą być przywoływane przez ich wstępnie zdefiniowane nazwy, takie jak `Item1` i `Item2`.</span><span class="sxs-lookup"><span data-stu-id="19080-140">Instead, tuple fields must be referenced by their predefined names, such as `Item1` and `Item2`.</span></span>

## <a name="tuples-versus-structures"></a><span data-ttu-id="19080-141">Krotki i struktury</span><span class="sxs-lookup"><span data-stu-id="19080-141">Tuples versus structures</span></span>

<span data-ttu-id="19080-142">Krotka Visual Basic jest typem wartości, który jest wystąpieniem jednego z typów ogólnych **System. ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="19080-142">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="19080-143">Na przykład `holiday` krotka zdefiniowana w poprzednim przykładzie jest wystąpieniem struktury <xref:System.ValueTuple%603>.</span><span class="sxs-lookup"><span data-stu-id="19080-143">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="19080-144">Jest on przeznaczony do uproszczonego kontenera dla danych.</span><span class="sxs-lookup"><span data-stu-id="19080-144">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="19080-145">Ponieważ krotka ma ułatwić tworzenie obiektu z wieloma elementami danych, nie ma niektórych funkcji, które mogą mieć niestandardową strukturę.</span><span class="sxs-lookup"><span data-stu-id="19080-145">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="19080-146">Należą do nich następujące elementy:</span><span class="sxs-lookup"><span data-stu-id="19080-146">These include:</span></span>

- <span data-ttu-id="19080-147">Niestandardowe elementy członkowskie.</span><span class="sxs-lookup"><span data-stu-id="19080-147">Custom members.</span></span> <span data-ttu-id="19080-148">Nie można definiować własnych właściwości, metod lub zdarzeń dla krotki.</span><span class="sxs-lookup"><span data-stu-id="19080-148">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="19080-149">Zatwierdzenia.</span><span class="sxs-lookup"><span data-stu-id="19080-149">Validation.</span></span> <span data-ttu-id="19080-150">Nie można sprawdzić poprawności danych przypisanych do pól.</span><span class="sxs-lookup"><span data-stu-id="19080-150">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="19080-151">Niezmienności.</span><span class="sxs-lookup"><span data-stu-id="19080-151">Immutability.</span></span> <span data-ttu-id="19080-152">Kolekcje Visual Basic są modyfikowalne.</span><span class="sxs-lookup"><span data-stu-id="19080-152">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="19080-153">W przeciwieństwie do struktury niestandardowej można kontrolować, czy wystąpienie jest modyfikowalne czy niezmienne.</span><span class="sxs-lookup"><span data-stu-id="19080-153">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="19080-154">Jeśli niestandardowe elementy członkowskie, walidacja właściwości i pól lub niezmienności są ważne, należy użyć instrukcji Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) , aby zdefiniować niestandardowy typ wartości.</span><span class="sxs-lookup"><span data-stu-id="19080-154">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="19080-155">Krotka Visual Basic dziedziczy elementy członkowskie jego typu **ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="19080-155">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="19080-156">Oprócz pól te elementy obejmują następujące metody:</span><span class="sxs-lookup"><span data-stu-id="19080-156">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="19080-157">Element członkowski</span><span class="sxs-lookup"><span data-stu-id="19080-157">Member</span></span> | <span data-ttu-id="19080-158">Opis</span><span class="sxs-lookup"><span data-stu-id="19080-158">Description</span></span> |
| ---|---|
| <span data-ttu-id="19080-159">CompareTo</span><span class="sxs-lookup"><span data-stu-id="19080-159">CompareTo</span></span> | <span data-ttu-id="19080-160">Porównuje bieżącą krotkę z inną krotką o tej samej liczbie elementów.</span><span class="sxs-lookup"><span data-stu-id="19080-160">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="19080-161">Ubiegł</span><span class="sxs-lookup"><span data-stu-id="19080-161">Equals</span></span> | <span data-ttu-id="19080-162">Określa, czy bieżąca Krotka jest równa innej kolekcji lub obiektowi.</span><span class="sxs-lookup"><span data-stu-id="19080-162">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="19080-163">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="19080-163">GetHashCode</span></span> | <span data-ttu-id="19080-164">Oblicza wartość skrótu dla bieżącego wystąpienia.</span><span class="sxs-lookup"><span data-stu-id="19080-164">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="19080-165">ToString</span><span class="sxs-lookup"><span data-stu-id="19080-165">ToString</span></span> | <span data-ttu-id="19080-166">Zwraca ciąg reprezentujący tę krotkę, która przyjmuje formularz `(Item1, Item2...)`, gdzie `Item1` i `Item2` reprezentują wartości pól krotki.</span><span class="sxs-lookup"><span data-stu-id="19080-166">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="19080-167">Ponadto typy **ValueTuple** implementują interfejsy <xref:System.Collections.IStructuralComparable> i <xref:System.Collections.IStructuralEquatable>, które umożliwiają definiowanie porównywania klientów.</span><span class="sxs-lookup"><span data-stu-id="19080-167">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="19080-168">Przypisanie i krotki</span><span class="sxs-lookup"><span data-stu-id="19080-168">Assignment and tuples</span></span>

<span data-ttu-id="19080-169">Visual Basic obsługuje przypisanie między typami krotek, które mają taką samą liczbę pól.</span><span class="sxs-lookup"><span data-stu-id="19080-169">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="19080-170">Typy pól mogą być konwertowane, jeśli jest spełniony jeden z następujących warunków:</span><span class="sxs-lookup"><span data-stu-id="19080-170">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="19080-171">Pole źródłowe i docelowe są tego samego typu.</span><span class="sxs-lookup"><span data-stu-id="19080-171">The source and target field are of the same type.</span></span>

- <span data-ttu-id="19080-172">Zdefiniowano niejawną konwersję typu źródłowego na typ docelowy.</span><span class="sxs-lookup"><span data-stu-id="19080-172">A widening (or implicit) conversion of the source type to the target type is defined.</span></span>

- <span data-ttu-id="19080-173">`Option Strict` jest `On` i zdefiniowano Zawężanie (lub jawne) konwersji typu źródłowego na typ docelowy.</span><span class="sxs-lookup"><span data-stu-id="19080-173">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="19080-174">Ta konwersja może zgłosić wyjątek, jeśli wartość źródłowa znajduje się poza zakresem typu docelowego.</span><span class="sxs-lookup"><span data-stu-id="19080-174">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="19080-175">Inne konwersje nie są brane pod uwagę w przypisaniach.</span><span class="sxs-lookup"><span data-stu-id="19080-175">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="19080-176">Przyjrzyjmy się typom przypisań, które są dozwolone między typami krotek.</span><span class="sxs-lookup"><span data-stu-id="19080-176">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="19080-177">Należy wziąć pod uwagę te zmienne, które są używane w następujących przykładach:</span><span class="sxs-lookup"><span data-stu-id="19080-177">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="19080-178">Pierwsze dwie zmienne, `unnamed` i `anonymous`, nie mają nazw semantycznych dla pól.</span><span class="sxs-lookup"><span data-stu-id="19080-178">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="19080-179">Nazwy pól są domyślnymi `Item1` i `Item2`.</span><span class="sxs-lookup"><span data-stu-id="19080-179">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="19080-180">Ostatnie dwie zmienne `named` i `differentName` mają nazwy pól semantycznych.</span><span class="sxs-lookup"><span data-stu-id="19080-180">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="19080-181">Należy zauważyć, że te dwie krotki mają różne nazwy pól.</span><span class="sxs-lookup"><span data-stu-id="19080-181">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="19080-182">Wszystkie cztery z tych krotek mają tę samą liczbę pól (określaną jako "liczba argumentów"), a typy tych pól są identyczne.</span><span class="sxs-lookup"><span data-stu-id="19080-182">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="19080-183">W związku z tym wszystkie te przypisania działają:</span><span class="sxs-lookup"><span data-stu-id="19080-183">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="19080-184">Zauważ, że nazwy krotek nie są przypisane.</span><span class="sxs-lookup"><span data-stu-id="19080-184">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="19080-185">Wartości pól są przypisywane po kolejności pól w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="19080-185">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="19080-186">Na koniec należy zauważyć, że możemy przypisać krotkę `named` do krotki `conversion`, mimo że pierwsze pole `named` jest `Integer`, a pierwsze pole `conversion` jest `Long`.</span><span class="sxs-lookup"><span data-stu-id="19080-186">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="19080-187">To przypisanie powiedzie się, ponieważ konwersja `Integer` na `Long` jest konwersją rozszerzającą.</span><span class="sxs-lookup"><span data-stu-id="19080-187">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="19080-188">Nie można przypisać krotek z różnymi liczbami pól:</span><span class="sxs-lookup"><span data-stu-id="19080-188">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="19080-189">Krotki jako wartości zwracane przez metodę</span><span class="sxs-lookup"><span data-stu-id="19080-189">Tuples as method return values</span></span>

<span data-ttu-id="19080-190">Metoda może zwracać tylko jedną wartość.</span><span class="sxs-lookup"><span data-stu-id="19080-190">A method can return only a single value.</span></span> <span data-ttu-id="19080-191">Często, chociaż chcemy, aby wywołanie metody zwracało wiele wartości.</span><span class="sxs-lookup"><span data-stu-id="19080-191">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="19080-192">Istnieje kilka sposobów obejścia tego ograniczenia:</span><span class="sxs-lookup"><span data-stu-id="19080-192">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="19080-193">Można utworzyć niestandardową klasę lub strukturę, której właściwości lub pola reprezentują wartości zwracane przez metodę.</span><span class="sxs-lookup"><span data-stu-id="19080-193">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="19080-194">Jest to bardzo ciężki rozwiązanie; wymaga zdefiniowania typu niestandardowego, którego jedynym celem jest pobieranie wartości z wywołania metody.</span><span class="sxs-lookup"><span data-stu-id="19080-194">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="19080-195">Można zwrócić jedną wartość z metody i zwrócić pozostałe wartości przez przekazanie ich przez odwołanie do metody.</span><span class="sxs-lookup"><span data-stu-id="19080-195">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="19080-196">Wiąże się to z narzutem tworzenia wystąpienia zmiennej i ryzyka przypadkowo zastępującej wartość zmiennej przekazanej przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="19080-196">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="19080-197">Możesz użyć krotki, która udostępnia lekkie rozwiązanie do pobierania wielu zwracanych wartości.</span><span class="sxs-lookup"><span data-stu-id="19080-197">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="19080-198">Na przykład metody **TryParse** w programie .net zwracają `Boolean` wartość, która wskazuje, czy operacja analizowania zakończyła się pomyślnie.</span><span class="sxs-lookup"><span data-stu-id="19080-198">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="19080-199">Wynik operacji analizowania jest zwracany w zmiennej przekazaną przez odwołanie do metody.</span><span class="sxs-lookup"><span data-stu-id="19080-199">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="19080-200">Zwykle wywołanie metody analizy, takiej jak <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> wygląda następująco:</span><span class="sxs-lookup"><span data-stu-id="19080-200">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="19080-201">Możemy zwrócić krotkę z operacji analizowania, jeśli zawijamy wywołanie metody <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> w naszej metodzie.</span><span class="sxs-lookup"><span data-stu-id="19080-201">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="19080-202">W poniższym przykładzie `NumericLibrary.ParseInteger` wywołuje metodę <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> i zwraca nazwaną krotkę z dwoma elementami.</span><span class="sxs-lookup"><span data-stu-id="19080-202">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="19080-203">Następnie można wywołać metodę z kodem podobnym do poniższego:</span><span class="sxs-lookup"><span data-stu-id="19080-203">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="19080-204">Visual Basic krotek i krotki w .NET Framework</span><span class="sxs-lookup"><span data-stu-id="19080-204">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="19080-205">Krotka Visual Basic to wystąpienie jednego z typów ogólnych **System. ValueTuple** , które zostały wprowadzone w .NET Framework 4,7.</span><span class="sxs-lookup"><span data-stu-id="19080-205">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="19080-206">.NET Framework obejmuje również zestaw rodzajowych klas **System. krotek** .</span><span class="sxs-lookup"><span data-stu-id="19080-206">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="19080-207">Jednak klasy te różnią się od Visual Basic krotek i typów ogólnych **System. ValueTuple** na wiele sposobów:</span><span class="sxs-lookup"><span data-stu-id="19080-207">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="19080-208">Elementy klas **krotek** są właściwościami o nazwie `Item1`, `Item2` i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="19080-208">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="19080-209">W Visual Basic krotekch i typach **ValueTuple** elementy krotki to pola.</span><span class="sxs-lookup"><span data-stu-id="19080-209">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="19080-210">Nie można przypisać znaczących nazw do elementów wystąpienia **krotki** lub wystąpienia **ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="19080-210">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="19080-211">Visual Basic umożliwia przypisanie nazw, które komunikują znaczenie pól.</span><span class="sxs-lookup"><span data-stu-id="19080-211">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="19080-212">Właściwości wystąpienia **krotki** są tylko do odczytu; krotki są niezmienne.</span><span class="sxs-lookup"><span data-stu-id="19080-212">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="19080-213">W Visual Basic krotekch i typach **ValueTuple** pola krotek są do odczytu i zapisu; krotki są modyfikowalne.</span><span class="sxs-lookup"><span data-stu-id="19080-213">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="19080-214">Typy **kolekcji** generycznej to typy odwołań.</span><span class="sxs-lookup"><span data-stu-id="19080-214">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="19080-215">Korzystanie z tych typów **krotek** oznacza alokowanie obiektów.</span><span class="sxs-lookup"><span data-stu-id="19080-215">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="19080-216">W przypadku ścieżek gorąca może to mieć wymierny wpływ na wydajność aplikacji.</span><span class="sxs-lookup"><span data-stu-id="19080-216">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="19080-217">Kolekcje Visual Basic i typy **ValueTuple** są typami wartości.</span><span class="sxs-lookup"><span data-stu-id="19080-217">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="19080-218">Metody rozszerzające w klasie <xref:System.TupleExtensions> ułatwiają konwersję między Visual Basic krotekmi i obiektami **kolekcji** programu .NET.</span><span class="sxs-lookup"><span data-stu-id="19080-218">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="19080-219">Metoda **ToTuple** konwertuje krotkę Visual Basic do obiektu **krotki** platformy .NET, a Metoda **ToValueTuple** Konwertuje obiekt **krotki** .NET na krotkę Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="19080-219">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="19080-220">Poniższy przykład tworzy krotkę, konwertuje ją na obiekt **krotki** .NET i konwertuje ją z powrotem do krotki Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="19080-220">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="19080-221">Przykład następnie porównuje tę spójną krotkę z oryginalną, aby upewnić się, że są równe.</span><span class="sxs-lookup"><span data-stu-id="19080-221">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="19080-222">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="19080-222">See also</span></span>

- [<span data-ttu-id="19080-223">Dokumentacja języka Visual Basic</span><span class="sxs-lookup"><span data-stu-id="19080-223">Visual Basic Language Reference</span></span>](index.md)
