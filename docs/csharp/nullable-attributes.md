---
title: Uaktualnij interfejsy API dla typów odwołań dopuszczających wartości null z atrybutami, które definiują oczekiwania dla wartości zerowych
description: Dowiedz się, jak używać opisowych atrybutów AllowNull, DisallowNull, MaybeNull, NotNull i innych, aby w pełni opisać stan null interfejsów API.
ms.technology: csharp-null-safety
ms.date: 07/31/2019
ms.openlocfilehash: 7142fe0566b1cc7373f5dc777c36443041114c4f
ms.sourcegitcommit: 81ad1f09b93f3b3e6706a7f2e4ddf50ef229ea3d
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 11/20/2019
ms.locfileid: "74204632"
---
# <a name="update-libraries-to-use-nullable-reference-types-and-communicate-nullable-rules-to-callers"></a><span data-ttu-id="b096b-103">Aktualizowanie bibliotek do używania typów referencyjnych dopuszczających wartości null i przekazywanie reguł dopuszczających wartość null do wywoływania</span><span class="sxs-lookup"><span data-stu-id="b096b-103">Update libraries to use nullable reference types and communicate nullable rules to callers</span></span>

<span data-ttu-id="b096b-104">Dodanie [typów referencyjnych dopuszczających wartość null](nullable-references.md) oznacza, czy można zadeklarować, czy wartość `null` jest dozwolona, czy nieoczekiwana dla każdej zmiennej.</span><span class="sxs-lookup"><span data-stu-id="b096b-104">The addition of [nullable reference types](nullable-references.md) means you can declare whether or not a `null` value is allowed or expected for every variable.</span></span> <span data-ttu-id="b096b-105">Ponadto można zastosować wiele atrybutów: `AllowNull`, `DisallowNull`, `MaybeNull`, `NotNull`, `NotNullWhen`, `MaybeNullWhen`i `NotNullWhenNotNull`, aby całkowicie opisać Stany null argumentu i wartości zwracane.</span><span class="sxs-lookup"><span data-stu-id="b096b-105">In addition, you can apply a number of attributes: `AllowNull`, `DisallowNull`, `MaybeNull`, `NotNull`, `NotNullWhen`, `MaybeNullWhen`, and `NotNullWhenNotNull` to completely describe the null states of argument and return values.</span></span> <span data-ttu-id="b096b-106">Zapewnia to doskonałe środowisko podczas pisania kodu.</span><span class="sxs-lookup"><span data-stu-id="b096b-106">That provides a great experience as you write code.</span></span> <span data-ttu-id="b096b-107">Otrzymasz ostrzeżenia, jeśli zmienna niedopuszczające wartości null może być ustawiona na `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-107">You get warnings if a non-nullable variable might be set to `null`.</span></span> <span data-ttu-id="b096b-108">Są wyświetlane ostrzeżenia, jeśli zmienna dopuszczająca wartość null nie jest sprawdzana przed usunięciem odwołania do niej.</span><span class="sxs-lookup"><span data-stu-id="b096b-108">You get warnings if a nullable variable isn't null-checked before you dereference it.</span></span> <span data-ttu-id="b096b-109">Aktualizowanie bibliotek może zająć trochę czasu, ale Payoffs.</span><span class="sxs-lookup"><span data-stu-id="b096b-109">Updating your libraries can take time, but the payoffs are worth it.</span></span> <span data-ttu-id="b096b-110">Więcej informacji udostępnianych kompilatorowi o tym, *kiedy* `null` wartość jest dozwolona lub zabroniona, będzie można uzyskać lepszych ostrzeżeń użytkowników interfejsu API.</span><span class="sxs-lookup"><span data-stu-id="b096b-110">The more information you provide to the compiler about *when* a `null` value is allowed or prohibited, the better warnings users of your API will get.</span></span> <span data-ttu-id="b096b-111">Zacznijmy od znanego przykładu.</span><span class="sxs-lookup"><span data-stu-id="b096b-111">Let's start with a familiar example.</span></span> <span data-ttu-id="b096b-112">Wyobraź sobie, że biblioteka zawiera następujący interfejs API do pobrania ciągu zasobu:</span><span class="sxs-lookup"><span data-stu-id="b096b-112">Imagine your library has the following API to retrieve a resource string:</span></span>

```csharp
bool TryGetMessage(string key, out string message)
```

<span data-ttu-id="b096b-113">Poprzedni przykład jest zgodny ze znanym wzorcem `Try*` w programie .NET.</span><span class="sxs-lookup"><span data-stu-id="b096b-113">The preceding example follows the familiar `Try*` pattern in .NET.</span></span> <span data-ttu-id="b096b-114">Dla tego interfejsu API istnieją dwa argumenty odwołania: `key` i `message`.</span><span class="sxs-lookup"><span data-stu-id="b096b-114">There are two reference arguments for this API: the `key` and the `message` parameter.</span></span> <span data-ttu-id="b096b-115">Ten interfejs API ma następujące reguły dotyczące wartości null tych argumentów:</span><span class="sxs-lookup"><span data-stu-id="b096b-115">This API has the following rules relating to the nullness of these arguments:</span></span>

- <span data-ttu-id="b096b-116">Obiekty wywołujące nie powinny przekazywać `null` jako argumentu dla `key`.</span><span class="sxs-lookup"><span data-stu-id="b096b-116">Callers shouldn't pass `null` as the argument for `key`.</span></span>
- <span data-ttu-id="b096b-117">Obiekty wywołujące mogą przekazać zmienną, której wartość jest `null` jako argument dla `message`.</span><span class="sxs-lookup"><span data-stu-id="b096b-117">Callers can pass a variable whose value is `null` as the argument for `message`.</span></span>
- <span data-ttu-id="b096b-118">Jeśli metoda `TryGetMessage` zwraca `true`, wartość `message` nie jest równa null.</span><span class="sxs-lookup"><span data-stu-id="b096b-118">If the `TryGetMessage` method returns `true`, the value of `message` isn't null.</span></span> <span data-ttu-id="b096b-119">Jeśli wartość zwracana jest `false,` wartość `message` (a jej stan null) ma wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-119">If the return value is `false,` the value of `message` (and its null state) is null.</span></span>

<span data-ttu-id="b096b-120">Reguła dla `key` może być całkowicie wyrażona przez typ zmiennej: `key` powinna być typem referencyjnym, który nie dopuszcza wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-120">The rule for `key` can be completely expressed by the variable type: `key` should be a non-nullable reference type.</span></span> <span data-ttu-id="b096b-121">`message` parametr jest bardziej skomplikowany.</span><span class="sxs-lookup"><span data-stu-id="b096b-121">The `message` parameter is more complex.</span></span> <span data-ttu-id="b096b-122">Umożliwia `null` jako argument, ale gwarantuje, że po powodzeniu `out` argument nie ma wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-122">It allows `null` as the argument, but guarantees that, on success, that `out` argument isn't null.</span></span> <span data-ttu-id="b096b-123">W tych scenariuszach potrzebujesz bogatszego słownictwa do opisywania oczekiwań.</span><span class="sxs-lookup"><span data-stu-id="b096b-123">For these scenarios, you need a richer vocabulary to describe the expectations.</span></span>

<span data-ttu-id="b096b-124">Aktualizacja biblioteki na potrzeby odwołań do wartości null wymaga więcej niż `?` na niektórych zmiennych i nazwach typów.</span><span class="sxs-lookup"><span data-stu-id="b096b-124">Updating your library for nullable references requires more than sprinkling `?` on some of the variables and type names.</span></span> <span data-ttu-id="b096b-125">Powyższy przykład pokazuje, że należy przejrzeć interfejsy API i uwzględnić oczekiwania dla każdego argumentu wejściowego.</span><span class="sxs-lookup"><span data-stu-id="b096b-125">The preceding example shows that you need to examine your APIs and consider your expectations for each input argument.</span></span> <span data-ttu-id="b096b-126">Należy wziąć pod uwagę gwarancje wartości zwracanej oraz wszystkie `out` lub `ref` argumenty przy zwracaniu metody.</span><span class="sxs-lookup"><span data-stu-id="b096b-126">Consider the guarantees for the return value, and any `out` or `ref` arguments upon the method's return.</span></span> <span data-ttu-id="b096b-127">Następnie Przekaż te reguły do kompilatora, a kompilator dostarczy ostrzeżenia, gdy wywołujący nie przestrzegają tych reguł.</span><span class="sxs-lookup"><span data-stu-id="b096b-127">Then communicate those rules to the compiler, and the compiler will provide warnings when callers don't abide by those rules.</span></span>

<span data-ttu-id="b096b-128">To działanie jest czasochłonne.</span><span class="sxs-lookup"><span data-stu-id="b096b-128">This work takes time.</span></span> <span data-ttu-id="b096b-129">Zacznijmy od strategii, aby dowiedzieć się, jak Twoja biblioteka lub aplikacja doznała wartości null, a także zrównoważyć inne wymagania i elementy dostarczane.</span><span class="sxs-lookup"><span data-stu-id="b096b-129">Let's start with strategies to make your library or application nullable-aware, while balancing other requirements and deliverables.</span></span> <span data-ttu-id="b096b-130">Zobaczysz, jak zrównoważyć bieżące programowanie, włączając typy referencyjne dopuszczające wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-130">You'll see how to balance ongoing development enabling nullable reference types.</span></span> <span data-ttu-id="b096b-131">Nauczysz się wyzwań dla definicji typów ogólnych.</span><span class="sxs-lookup"><span data-stu-id="b096b-131">You'll learn challenges for generic type definitions.</span></span> <span data-ttu-id="b096b-132">Dowiesz się, jak zastosować atrybuty do opisywania warunków wstępnych i postanowień dotyczących poszczególnych interfejsów API.</span><span class="sxs-lookup"><span data-stu-id="b096b-132">You'll learn to apply attributes to describe pre- and post-conditions on individual APIs.</span></span>

## <a name="choose-a-nullable-strategy"></a><span data-ttu-id="b096b-133">Wybierz strategię dopuszczającą wartość null</span><span class="sxs-lookup"><span data-stu-id="b096b-133">Choose a nullable strategy</span></span>

<span data-ttu-id="b096b-134">Pierwszy wybór polega na tym, czy typy odwołań dopuszczające wartość null mają być domyślnie włączone czy wyłączone.</span><span class="sxs-lookup"><span data-stu-id="b096b-134">The first choice is whether nullable reference types should be on or off by default.</span></span> <span data-ttu-id="b096b-135">Istnieją dwie strategie:</span><span class="sxs-lookup"><span data-stu-id="b096b-135">You have two strategies:</span></span>

- <span data-ttu-id="b096b-136">Włącz typy referencyjne dopuszczające wartość null dla całego projektu i wyłącz je w kodzie, który nie jest gotowy.</span><span class="sxs-lookup"><span data-stu-id="b096b-136">Enable nullable reference types for the entire project, and disable it in code that's not ready.</span></span>
- <span data-ttu-id="b096b-137">Włącz tylko typy referencyjne dopuszczające wartość null dla kodu, który jest oznaczony adnotacją dla typów referencyjnych dopuszczających wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-137">Only enable nullable reference types for code that's been annotated for nullable reference types.</span></span>

<span data-ttu-id="b096b-138">Pierwsza strategia działa najlepiej, gdy dodajesz inne funkcje do biblioteki, gdy zaktualizujesz ją do typów referencyjnych dopuszczających wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-138">The first strategy works best when you're adding other features to the library as you update it for nullable reference types.</span></span> <span data-ttu-id="b096b-139">Cały nowy projekt ma świadomość dopuszczający wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-139">All new development is nullable aware.</span></span> <span data-ttu-id="b096b-140">Gdy aktualizujesz istniejący kod, włączasz typy odwołań do wartości null w tych klasach.</span><span class="sxs-lookup"><span data-stu-id="b096b-140">As you update existing code, you enable nullable reference types in those classes.</span></span>

<span data-ttu-id="b096b-141">Postępując zgodnie z pierwszą strategią, wykonaj następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="b096b-141">Following this first strategy, you do the following:</span></span>

1. <span data-ttu-id="b096b-142">Włącz Typy dopuszczające wartość null dla całego projektu, dodając `<Nullable>enable</Nullable>` element do plików *csproj* .</span><span class="sxs-lookup"><span data-stu-id="b096b-142">Enable nullable types for the entire project by adding the `<Nullable>enable</Nullable>` element to your *csproj* files.</span></span> 
1. <span data-ttu-id="b096b-143">Dodaj `#nullable disable` pragma do każdego pliku źródłowego w projekcie.</span><span class="sxs-lookup"><span data-stu-id="b096b-143">Add the `#nullable disable` pragma to every source file in your project.</span></span> 
1. <span data-ttu-id="b096b-144">Podczas pracy nad każdym plikiem Usuń pragmę i rozwiąż wszelkie ostrzeżenia.</span><span class="sxs-lookup"><span data-stu-id="b096b-144">As you work on each file, remove the pragma and address any warnings.</span></span>

<span data-ttu-id="b096b-145">Ta pierwsza strategia ma więcej zadań z góry, aby dodać pragmę do każdego pliku.</span><span class="sxs-lookup"><span data-stu-id="b096b-145">This first strategy has more up-front work to add the pragma to every file.</span></span> <span data-ttu-id="b096b-146">Korzyść polega na tym, że każdy nowy plik kodu dodany do projektu będzie miał włączoną wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-146">The advantage is that every new code file added to the project will be nullable enabled.</span></span> <span data-ttu-id="b096b-147">Każda nowa służbowa będzie mieć świadomość wartości null; należy zaktualizować tylko istniejący kod.</span><span class="sxs-lookup"><span data-stu-id="b096b-147">Any new work will be nullable aware; only existing code must be updated.</span></span>

<span data-ttu-id="b096b-148">Druga strategia działa lepiej, jeśli biblioteka jest ogólnie stabilna, a głównym celem rozwoju jest przyjęcie typów referencyjnych dopuszczających wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-148">The second strategy works better if the library is generally stable, and the main focus of the development is to adopt nullable reference types.</span></span> <span data-ttu-id="b096b-149">W przypadku dodawania adnotacji do interfejsów API należy włączyć typy odwołań do wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-149">You turn on nullable reference types as you annotate APIs.</span></span> <span data-ttu-id="b096b-150">Po zakończeniu włączać dla całego projektu typy referencyjne dopuszczające wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-150">When you've finished, you enable nullable reference types for the entire project.</span></span>

<span data-ttu-id="b096b-151">Postępując zgodnie z tą drugą strategią, należy wykonać następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="b096b-151">Following this second strategy you do the following:</span></span>

1. <span data-ttu-id="b096b-152">Dodaj `#nullable enable` pragma do pliku, który ma być dopuszczający wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-152">Add the `#nullable enable` pragma to the file you want to make nullable aware.</span></span>
1. <span data-ttu-id="b096b-153">Rozwiązywanie wszelkich ostrzeżeń.</span><span class="sxs-lookup"><span data-stu-id="b096b-153">Address any warnings.</span></span>
1. <span data-ttu-id="b096b-154">Wykonaj te dwa pierwsze kroki, dopóki cała biblioteka nie doznała wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-154">Continue these first two steps until you've made the entire library nullable aware.</span></span>
1. <span data-ttu-id="b096b-155">Włącz Typy dopuszczające wartość null dla całego projektu, dodając `<Nullable>enable</Nullable>` element do plików *csproj* .</span><span class="sxs-lookup"><span data-stu-id="b096b-155">Enable nullable types for the entire project by adding the `<Nullable>enable</Nullable>` element to your *csproj* files.</span></span> 
1. <span data-ttu-id="b096b-156">Usuń `#nullable enable` pragmy, ponieważ nie są już potrzebne.</span><span class="sxs-lookup"><span data-stu-id="b096b-156">Remove the `#nullable enable` pragmas, as they're no longer needed.</span></span>

<span data-ttu-id="b096b-157">Ta druga strategia ma mniej pracy z góry.</span><span class="sxs-lookup"><span data-stu-id="b096b-157">This second strategy has less work up-front.</span></span> <span data-ttu-id="b096b-158">Wadą jest to, że pierwsze zadanie podczas tworzenia nowego pliku to dodanie dyrektywy pragma i przekazanie jej do wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-158">The tradeoff is that the first task when you create a new file is to add the pragma and make it nullable aware.</span></span> <span data-ttu-id="b096b-159">Jeśli wszyscy deweloperzy w zespole zapomnili, nowy kod jest teraz w zaległości prac, aby umożliwić sobie dopełnienie kodu do wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-159">If any developers on your team forget, that new code is now in the backlog of work to make all code nullable aware.</span></span>

<span data-ttu-id="b096b-160">Które z tych strategii są wybierane, zależy od tego, jak dużo aktywnego programowania odbywa się w projekcie.</span><span class="sxs-lookup"><span data-stu-id="b096b-160">Which of these strategies you pick depends on how much active development is taking place in your project.</span></span> <span data-ttu-id="b096b-161">Im bardziej dojrzały i stabilny projekt, tym lepiej druga strategia.</span><span class="sxs-lookup"><span data-stu-id="b096b-161">The more mature and stable your project, the better the second strategy.</span></span> <span data-ttu-id="b096b-162">Im więcej funkcji jest opracowywanych, tym lepsza jest pierwsza strategia.</span><span class="sxs-lookup"><span data-stu-id="b096b-162">The more features being developed, the better the first strategy.</span></span>

## <a name="should-nullable-warnings-introduce-breaking-changes"></a><span data-ttu-id="b096b-163">Czy ostrzeżenia o wartościach null wprowadzają istotne zmiany?</span><span class="sxs-lookup"><span data-stu-id="b096b-163">Should nullable warnings introduce breaking changes?</span></span>

<span data-ttu-id="b096b-164">Przed włączeniem typów referencyjnych dopuszczających wartości null, zmienne są uznawane za *Oblivious dopuszczające wartość null*.</span><span class="sxs-lookup"><span data-stu-id="b096b-164">Before you enable nullable reference types, variables are considered *nullable oblivious*.</span></span> <span data-ttu-id="b096b-165">Po włączeniu typów referencyjnych dopuszczających wartości null wszystkie te zmienne *nie dopuszczają wartości null*.</span><span class="sxs-lookup"><span data-stu-id="b096b-165">Once you enable nullable reference types, all those variables are *non-nullable*.</span></span> <span data-ttu-id="b096b-166">Kompilator będzie wystawiał ostrzeżenia, jeśli te zmienne nie są zainicjowane do wartości innych niż null.</span><span class="sxs-lookup"><span data-stu-id="b096b-166">The compiler will issue warnings if those variables aren't initialized to non-null values.</span></span>

<span data-ttu-id="b096b-167">Inne prawdopodobnie Źródło ostrzeżeń ma zwracane wartości, gdy wartość nie została zainicjowana.</span><span class="sxs-lookup"><span data-stu-id="b096b-167">Another likely source of warnings is return values when the value hasn't been initialized.</span></span>

<span data-ttu-id="b096b-168">Pierwszym krokiem w przypadku rozwiązywania ostrzeżeń kompilatora jest użycie adnotacji `?` dla parametrów i zwracanych typów, aby wskazać, kiedy argumenty lub wartości zwracane mogą mieć wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-168">The first step in addressing the compiler warnings is to use `?` annotations on parameter and return types to indicate when arguments or return values may be null.</span></span> <span data-ttu-id="b096b-169">Gdy zmienne odwołania nie mogą mieć wartości null, oryginalna deklaracja jest poprawna.</span><span class="sxs-lookup"><span data-stu-id="b096b-169">When reference variables must not be null, the original declaration is correct.</span></span> <span data-ttu-id="b096b-170">W takim przypadku cel nie tylko naprawi ostrzeżenia.</span><span class="sxs-lookup"><span data-stu-id="b096b-170">As you do this, your goal isn't just to fix warnings.</span></span> <span data-ttu-id="b096b-171">Im bardziej istotny jest sposób, aby kompilator mógł zrozumieć zamiar dla potencjalnych wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-171">The more important goal is to make the compiler understand your intent for potential null values.</span></span> <span data-ttu-id="b096b-172">Podczas badania ostrzeżeń możesz przejść do następnej głównej decyzji dotyczącej biblioteki.</span><span class="sxs-lookup"><span data-stu-id="b096b-172">As you examine the warnings, you reach your next major decision for your library.</span></span> <span data-ttu-id="b096b-173">Czy chcesz rozważyć modyfikowanie sygnatur interfejsów API, aby dokładniej komunikować swój projekt?</span><span class="sxs-lookup"><span data-stu-id="b096b-173">Do you want to consider modifying API signatures to more clearly communicate your design intent?</span></span> <span data-ttu-id="b096b-174">Lepszym podpisem interfejsu API dla metody `TryGetMessage` przeanalizowanej wcześniej może być:</span><span class="sxs-lookup"><span data-stu-id="b096b-174">A better API signature for the `TryGetMessage` method examined earlier could be:</span></span>

```csharp
string? TryGetMessage(string key);
```

<span data-ttu-id="b096b-175">Wartość zwracana wskazuje powodzenie lub niepowodzenie i przenosi wartość w przypadku znalezienia wartości.</span><span class="sxs-lookup"><span data-stu-id="b096b-175">The return value indicates success or failure, and carries the value if the value was found.</span></span> <span data-ttu-id="b096b-176">W wielu przypadkach zmiana sygnatur interfejsów API może poprawić sposób, w jaki komunikują się wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-176">In many cases, changing API signatures can improve how they communicate null values.</span></span>

<span data-ttu-id="b096b-177">Jednak w przypadku bibliotek publicznych lub bibliotek z dużymi bazami użytkowników można preferować nie wprowadzać żadnych zmian sygnatury interfejsu API.</span><span class="sxs-lookup"><span data-stu-id="b096b-177">However, for public libraries, or libraries with large user bases, you may prefer not introducing any API signature changes.</span></span> <span data-ttu-id="b096b-178">W tych przypadkach i innych wspólnych wzorców można zastosować atrybuty, aby dokładniej zdefiniować, kiedy może być `null`argumentu lub wartości zwracanej.</span><span class="sxs-lookup"><span data-stu-id="b096b-178">For those cases, and other common patterns, you can apply attributes to more clearly define when an argument or return value may be `null`.</span></span> <span data-ttu-id="b096b-179">Bez względu na to, czy należy rozważyć zmianę powierzchni interfejsu API, prawdopodobnie okaże się, że tylko adnotacje typu nie są wystarczające do opisywania wartości `null` argumentów lub zwracanych wartości.</span><span class="sxs-lookup"><span data-stu-id="b096b-179">Whether or not you consider changing the surface of your API, you'll likely find that type annotations alone aren't sufficient for describing `null` values for arguments or return values.</span></span> <span data-ttu-id="b096b-180">W tych przypadkach można zastosować atrybuty, aby dokładniej opisać interfejs API.</span><span class="sxs-lookup"><span data-stu-id="b096b-180">In those instances, you can apply attributes to more clearly describe an API.</span></span> 

## <a name="attributes-extend-type-annotations"></a><span data-ttu-id="b096b-181">Atrybuty rozszerzonego typu</span><span class="sxs-lookup"><span data-stu-id="b096b-181">Attributes extend type annotations</span></span>

<span data-ttu-id="b096b-182">Dodano kilka atrybutów w celu wyrażenia dodatkowych informacji o stanie null zmiennych.</span><span class="sxs-lookup"><span data-stu-id="b096b-182">Several attributes have been added to express additional information about the null state of variables.</span></span> <span data-ttu-id="b096b-183">Wszystkie kod, który zapisano C# przed 8 wprowadzonymi typami odwołań dopuszczających wartości null, miał *wartość null Oblivious*.</span><span class="sxs-lookup"><span data-stu-id="b096b-183">All code you wrote before C# 8 introduced nullable reference types was *null oblivious*.</span></span> <span data-ttu-id="b096b-184">Oznacza to, że jakakolwiek zmienna typu referencyjnego może mieć wartość null, ale sprawdzanie wartości null nie jest wymagane.</span><span class="sxs-lookup"><span data-stu-id="b096b-184">That means any reference type variable may be null, but null checks aren't required.</span></span> <span data-ttu-id="b096b-185">Gdy kod *dopuszcza wartość null*, te reguły zostaną zmienione.</span><span class="sxs-lookup"><span data-stu-id="b096b-185">Once your code is *nullable aware*, those rules change.</span></span> <span data-ttu-id="b096b-186">Typ referencyjny nigdy nie powinien być wartością `null`, a typy referencyjne dopuszczające wartość null muszą zostać sprawdzone względem `null` przed usunięciem odwołania.</span><span class="sxs-lookup"><span data-stu-id="b096b-186">Reference types should never be the `null` value, and nullable reference types must be checked against `null` before being dereferenced.</span></span>

<span data-ttu-id="b096b-187">Reguły interfejsów API mogą być bardziej skomplikowane, jak pokazano w scenariuszu interfejsu API `TryGetValue`.</span><span class="sxs-lookup"><span data-stu-id="b096b-187">The rules for your APIs are likely more complicated, as you saw with the `TryGetValue` API scenario.</span></span> <span data-ttu-id="b096b-188">Wiele interfejsów API ma bardziej złożone reguły dla sytuacji, gdy zmienne mogą lub nie mogą być `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-188">Many of your APIs have more complex rules for when variables can or can't be `null`.</span></span> <span data-ttu-id="b096b-189">W takich przypadkach użyjesz jednego z następujących atrybutów, aby przedstawić te reguły:</span><span class="sxs-lookup"><span data-stu-id="b096b-189">In these cases, you'll use one of the following attributes to express those rules:</span></span>

- <span data-ttu-id="b096b-190">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): Argument wejściowy niedopuszczający wartości null może mieć wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-190">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): A non-nullable input argument may be null.</span></span>
- <span data-ttu-id="b096b-191">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): Argument wejściowy dopuszczający wartość null nigdy nie powinien mieć wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-191">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): A nullable input argument should never be null.</span></span>
- <span data-ttu-id="b096b-192">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): wartość zwracana niedopuszczający wartości null może być równa null.</span><span class="sxs-lookup"><span data-stu-id="b096b-192">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): A non-nullable return value may be null.</span></span>
- <span data-ttu-id="b096b-193">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): wartość zwracana do wartości null nigdy nie będzie równa null.</span><span class="sxs-lookup"><span data-stu-id="b096b-193">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): A nullable return value will never be null.</span></span>
- <span data-ttu-id="b096b-194">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): Argument wejściowy, który nie dopuszcza wartości null, może mieć wartość null, gdy metoda zwraca określoną wartość `bool`.</span><span class="sxs-lookup"><span data-stu-id="b096b-194">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): A non-nullable input argument may be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="b096b-195">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): Argument wejściowy do wartości null nie będzie miał wartości null, gdy metoda zwróci określoną wartość `bool`.</span><span class="sxs-lookup"><span data-stu-id="b096b-195">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): A nullable input argument will not be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="b096b-196">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): wartość zwracana nie ma wartości null, jeśli argument dla określonego parametru nie ma wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-196">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): A return value isn't null if the argument for the specified parameter isn't null.</span></span>

<span data-ttu-id="b096b-197">Powyższe opisy stanowią krótkie informacje o tym, co robi każdy atrybut.</span><span class="sxs-lookup"><span data-stu-id="b096b-197">The preceding descriptions are a quick reference to what each attribute does.</span></span> <span data-ttu-id="b096b-198">W poniższych sekcjach opisano zachowanie i ich znaczenie.</span><span class="sxs-lookup"><span data-stu-id="b096b-198">Each following section describes the behavior and meaning more thoroughly.</span></span>

<span data-ttu-id="b096b-199">Dodanie tych atrybutów zapewnia kompilatorowi więcej informacji na temat reguł dla interfejsu API.</span><span class="sxs-lookup"><span data-stu-id="b096b-199">Adding these attributes gives the compiler more information about the rules for your API.</span></span> <span data-ttu-id="b096b-200">Gdy Wywoływanie kodu jest kompilowane w kontekście włączonego wartości null, kompilator ostrzega wywoływania, gdy narusza te reguły.</span><span class="sxs-lookup"><span data-stu-id="b096b-200">When calling code is compiled in a nullable enabled context, the compiler will warn callers when they violate those rules.</span></span> <span data-ttu-id="b096b-201">Te atrybuty nie umożliwiają wykonywania dodatkowych operacji sprawdzania w implementacji.</span><span class="sxs-lookup"><span data-stu-id="b096b-201">These attributes don't enable additional checks on your implementation.</span></span>

## <a name="specify-preconditions-allownull-and-disallownull"></a><span data-ttu-id="b096b-202">Określ warunki wstępne: `AllowNull` i `DisallowNull`</span><span class="sxs-lookup"><span data-stu-id="b096b-202">Specify preconditions: `AllowNull` and `DisallowNull`</span></span>

<span data-ttu-id="b096b-203">Należy rozważyć właściwość odczytu/zapisu, która nigdy nie zwraca `null`, ponieważ ma ona rozsądną wartość domyślną.</span><span class="sxs-lookup"><span data-stu-id="b096b-203">Consider a read/write property that never returns `null` because it has a reasonable default value.</span></span> <span data-ttu-id="b096b-204">Obiekty wywołujące przechodzą `null` do zestawu metod dostępu podczas ustawiania tej wartości domyślnej.</span><span class="sxs-lookup"><span data-stu-id="b096b-204">Callers pass `null` to the set accessor when setting it to that default value.</span></span> <span data-ttu-id="b096b-205">Rozważmy na przykład system obsługi komunikatów, który prosi o podanie nazwy ekranu w pokoju rozmowy.</span><span class="sxs-lookup"><span data-stu-id="b096b-205">For example, consider a messaging system that asks for a screen name in a chat room.</span></span> <span data-ttu-id="b096b-206">Jeśli żaden nie jest podany, system generuje nazwę losową:</span><span class="sxs-lookup"><span data-stu-id="b096b-206">If none is provided, the system generates a random name:</span></span>

```csharp
public string ScreenName
{
   get => screenName;
   set => screenName = value ?? GenerateRandomScreenName();
}
private string screenName;
```

<span data-ttu-id="b096b-207">Gdy kompilujesz poprzedni kod w kontekście dopuszczającym wartość null, wszystko jest bardzo precyzyjne.</span><span class="sxs-lookup"><span data-stu-id="b096b-207">When you compile the preceding code in a nullable oblivious context, everything is fine.</span></span> <span data-ttu-id="b096b-208">Po włączeniu typów referencyjnych dopuszczających wartości null Właściwość `ScreenName` będzie niedostępna do wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-208">Once you enable nullable reference types, the `ScreenName` property becomes a non-nullable reference.</span></span> <span data-ttu-id="b096b-209">Jest to poprawne dla metody dostępu `get`: nigdy nie zwraca `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-209">That's correct for the `get` accessor: it never returns `null`.</span></span> <span data-ttu-id="b096b-210">Obiekty wywołujące nie muszą sprawdzać zwracanej właściwości dla `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-210">Callers don't need to check the returned property for `null`.</span></span> <span data-ttu-id="b096b-211">Ale teraz ustawienie właściwości na `null` generuje ostrzeżenie.</span><span class="sxs-lookup"><span data-stu-id="b096b-211">But now setting the property to `null` generates a warning.</span></span> <span data-ttu-id="b096b-212">Aby kontynuować obsługę tego typu kodu, Dodaj atrybut <xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute?displayProperty=nameWithType> do właściwości, jak pokazano w poniższym kodzie:</span><span class="sxs-lookup"><span data-stu-id="b096b-212">In order to continue to support this type of code, you add the <xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute?displayProperty=nameWithType> attribute to the property, as shown in the following code:</span></span> 

```csharp
[AllowNull]
public string ScreenName
{
   get => screenName;
   set => screenName = value ?? GenerateRandomScreenName();
}
private string screenName = GenerateRandomScreenName();
```

<span data-ttu-id="b096b-213">Może być konieczne dodanie dyrektywy `using` dla <xref:System.Diagnostics.CodeAnalysis>, aby użyć tego i innych atrybutów omówionych w tym artykule.</span><span class="sxs-lookup"><span data-stu-id="b096b-213">You may need to add a `using` directive for <xref:System.Diagnostics.CodeAnalysis> to use this and other attributes discussed in this article.</span></span> <span data-ttu-id="b096b-214">Ten atrybut jest stosowany do właściwości, a nie metody dostępu `set`.</span><span class="sxs-lookup"><span data-stu-id="b096b-214">The attribute is applied to the property, not the `set` accessor.</span></span> <span data-ttu-id="b096b-215">Atrybut `AllowNull` określa *warunki wstępne*i ma zastosowanie tylko do danych wejściowych.</span><span class="sxs-lookup"><span data-stu-id="b096b-215">The `AllowNull` attribute specifies *pre-conditions*, and only applies to inputs.</span></span> <span data-ttu-id="b096b-216">Metoda dostępu `get` ma wartość zwracaną, ale nie ma argumentów wejściowych.</span><span class="sxs-lookup"><span data-stu-id="b096b-216">The `get` accessor has a return value, but no input arguments.</span></span> <span data-ttu-id="b096b-217">W związku z tym, atrybut `AllowNull` ma zastosowanie tylko do `set` metody dostępu.</span><span class="sxs-lookup"><span data-stu-id="b096b-217">Therefore, the `AllowNull` attribute only applies to the `set` accessor.</span></span>

<span data-ttu-id="b096b-218">W poprzednim przykładzie pokazano, co należy szukać podczas dodawania atrybutu `AllowNull` w argumencie:</span><span class="sxs-lookup"><span data-stu-id="b096b-218">The preceding example demonstrates what to look for when adding the `AllowNull` attribute on an argument:</span></span>

1. <span data-ttu-id="b096b-219">Ogólnym kontraktem tej zmiennej jest, że nie należy `null`, więc potrzebujesz typu referencyjnego, który nie dopuszcza wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-219">The general contract for that variable is that it shouldn't be `null`, so you want a non-nullable reference type.</span></span>
1. <span data-ttu-id="b096b-220">Istnieją scenariusze dla zmiennej wejściowej, która ma być `null`, ale nie są one najczęstszym użyciem.</span><span class="sxs-lookup"><span data-stu-id="b096b-220">There are scenarios for the input variable to be `null`, though they aren't the most common usage.</span></span>

<span data-ttu-id="b096b-221">Najczęściej ten atrybut jest wymagany dla właściwości, `in`, `out`i `ref` argumentów.</span><span class="sxs-lookup"><span data-stu-id="b096b-221">Most often you'll need this attribute for properties, or `in`, `out`, and `ref` arguments.</span></span> <span data-ttu-id="b096b-222">Atrybut `AllowNull` jest najlepszym wyborem, gdy zmienna jest zwykle inna niż null, ale musisz zezwolić na `null` jako warunek wstępny.</span><span class="sxs-lookup"><span data-stu-id="b096b-222">The `AllowNull` attribute is the best choice when a variable is typically non-null, but you need to allow `null` as a precondition.</span></span>

<span data-ttu-id="b096b-223">Kontrast zawierający scenariusze używania `DisallowNull`: ten atrybut służy do określenia, że zmienna wejściowa typu dopuszczającego wartość null nie powinna być `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-223">Contrast that with scenarios for using `DisallowNull`: You use this attribute to specify that an input variable of a nullable type shouldn't be `null`.</span></span> <span data-ttu-id="b096b-224">Rozważmy właściwość, w której `null` jest wartością domyślną, ale klienci mogą ustawić tylko wartość różną od null.</span><span class="sxs-lookup"><span data-stu-id="b096b-224">Consider a property where `null` is the default value, but clients can only set it to a non-null value.</span></span> <span data-ttu-id="b096b-225">Rozważmy następujący kod:</span><span class="sxs-lookup"><span data-stu-id="b096b-225">Consider the following code:</span></span>

```csharp
public string ReviewComment
{
    get => _comment;
    set => _comment = value ?? throw new ArgumentNullException(nameof(value), "Cannot set to null");
}
string _comment;
```

<span data-ttu-id="b096b-226">Poprzedni kod jest najlepszym sposobem wyrażania projektu, że `ReviewComment` może być `null`, ale nie można go ustawić jako `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-226">The preceding code is the best way to express your design that the `ReviewComment` could be `null`, but can't be set to `null`.</span></span> <span data-ttu-id="b096b-227">Gdy ten kod dopuszcza wartość null, można jawnie wyrazić ten pomysł do wywoływania przy użyciu <xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute?displayProperty=nameWithType>:</span><span class="sxs-lookup"><span data-stu-id="b096b-227">Once this code is nullable aware, you can express this concept more clearly to callers using the <xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute?displayProperty=nameWithType>:</span></span>

```csharp
[DisallowNull] 
public string? ReviewComment
{
    get => _comment;
    set => _comment = value ?? throw new ArgumentNullException(nameof(value), "Cannot set to null");
}
string? _comment;
```

<span data-ttu-id="b096b-228">W kontekście dopuszczającym wartość null metoda dostępu `ReviewComment` `get` może zwracać wartość domyślną `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-228">In a nullable context, the `ReviewComment` `get` accessor could return the default value of `null`.</span></span> <span data-ttu-id="b096b-229">Kompilator ostrzega o tym, że musi zostać sprawdzony przed dostępem.</span><span class="sxs-lookup"><span data-stu-id="b096b-229">The compiler warns that it must be checked before access.</span></span> <span data-ttu-id="b096b-230">Ponadto ostrzega wywołujących, że mimo że może być `null`, obiekty wywołujące nie powinny jawnie ustawiać go do `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-230">Furthermore, it warns callers that, even though it could be `null`, callers shouldn't explicitly set it to `null`.</span></span> <span data-ttu-id="b096b-231">Atrybut `DisallowNull` określa również *warunek wstępny*, nie ma wpływu na `get` metodę dostępu.</span><span class="sxs-lookup"><span data-stu-id="b096b-231">The `DisallowNull` attribute also specifies a *pre-condition*, it does not affect the `get` accessor.</span></span> <span data-ttu-id="b096b-232">Należy wybrać użycie atrybutu `DisallowNull`, gdy zobaczysz następujące cechy:</span><span class="sxs-lookup"><span data-stu-id="b096b-232">You should choose to use the `DisallowNull` attribute when you observe these characteristics about:</span></span>

1. <span data-ttu-id="b096b-233">Zmienna może być `null` w scenariuszach podstawowych, często podczas pierwszego wystąpienia.</span><span class="sxs-lookup"><span data-stu-id="b096b-233">The variable could be `null` in core scenarios, often when first instantiated.</span></span>
1. <span data-ttu-id="b096b-234">Zmienna nie powinna być jawnie ustawiona na `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-234">The variable shouldn't be explicitly set to `null`.</span></span>

<span data-ttu-id="b096b-235">Te sytuacje często występują w kodzie, który pierwotnie miał *wartość null Oblivious*.</span><span class="sxs-lookup"><span data-stu-id="b096b-235">These situations are common in code that was originally *null oblivious*.</span></span> <span data-ttu-id="b096b-236">Może być to, że właściwości obiektu są ustawiane w dwóch odrębnych operacjach inicjalizacji.</span><span class="sxs-lookup"><span data-stu-id="b096b-236">It may be that object properties are set in two distinct initialization operations.</span></span> <span data-ttu-id="b096b-237">Niektóre właściwości mogą być ustawione tylko po zakończeniu pewnej asynchronicznej pracy.</span><span class="sxs-lookup"><span data-stu-id="b096b-237">It may be that some properties are set only after some asynchronous work has completed.</span></span>

<span data-ttu-id="b096b-238">Atrybuty `AllowNull` i `DisallowNull` umożliwiają określenie, że warunki wstępne dotyczące zmiennych mogą nie pasować do adnotacji dopuszczających wartości null w tych zmiennych.</span><span class="sxs-lookup"><span data-stu-id="b096b-238">The `AllowNull` and `DisallowNull` attributes enable you to specify that preconditions on variables may not match the nullable annotations on those variables.</span></span> <span data-ttu-id="b096b-239">Zapewniają one więcej szczegółowych informacji o cechach interfejsu API.</span><span class="sxs-lookup"><span data-stu-id="b096b-239">These provide more detail about the characteristics of your API.</span></span> <span data-ttu-id="b096b-240">Te dodatkowe informacje ułatwiają wywoływanie interfejsu API.</span><span class="sxs-lookup"><span data-stu-id="b096b-240">This additional information helps callers use your API correctly.</span></span> <span data-ttu-id="b096b-241">Należy pamiętać o określeniu warunków wstępnych przy użyciu następujących atrybutów:</span><span class="sxs-lookup"><span data-stu-id="b096b-241">Remember you specify preconditions using the following attributes:</span></span>

- <span data-ttu-id="b096b-242">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): Argument wejściowy niedopuszczający wartości null może mieć wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-242">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): A non-nullable input argument may be null.</span></span>
- <span data-ttu-id="b096b-243">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): Argument wejściowy dopuszczający wartość null nigdy nie powinien mieć wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-243">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): A nullable input argument should never be null.</span></span>

## <a name="specify-post-conditions-maybenull-and-notnull"></a><span data-ttu-id="b096b-244">Określ warunki końcowe: `MaybeNull` i `NotNull`</span><span class="sxs-lookup"><span data-stu-id="b096b-244">Specify post-conditions: `MaybeNull` and `NotNull`</span></span>

<span data-ttu-id="b096b-245">Załóżmy, że masz metodę o następującej sygnaturze:</span><span class="sxs-lookup"><span data-stu-id="b096b-245">Suppose you have a method with the following signature:</span></span>

```csharp
public Customer FindCustomer(string lastName, string firstName)
```

<span data-ttu-id="b096b-246">Prawdopodobnie Zapisano metodę podobną do tej, aby zwrócić `null`, gdy nie znaleziono poszukiwanej nazwy.</span><span class="sxs-lookup"><span data-stu-id="b096b-246">You've likely written a method like this to return `null` when the name sought wasn't found.</span></span> <span data-ttu-id="b096b-247">`null` jasno wskazuje, że nie znaleziono rekordu.</span><span class="sxs-lookup"><span data-stu-id="b096b-247">The `null` clearly indicates that the record wasn't found.</span></span> <span data-ttu-id="b096b-248">W tym przykładzie istnieje możliwość zmiany typu zwracanego z `Customer` na `Customer?`.</span><span class="sxs-lookup"><span data-stu-id="b096b-248">In this example, you'd likely change the return type from `Customer` to `Customer?`.</span></span> <span data-ttu-id="b096b-249">Deklarowanie wartości zwracanej jako typ referencyjny dopuszczający wartość null Określa zamiar tego interfejsu API jasno.</span><span class="sxs-lookup"><span data-stu-id="b096b-249">Declaring the return value as a nullable reference type specifies the intent of this API clearly.</span></span> 

<span data-ttu-id="b096b-250">Z przyczyn objętych [definicjami ogólnymi i wartością null](#generic-definitions-and-nullability) ta technika nie działa z metodami ogólnymi.</span><span class="sxs-lookup"><span data-stu-id="b096b-250">For reasons covered under [Generic definitions and nullability](#generic-definitions-and-nullability) that technique does not work with generic methods.</span></span> <span data-ttu-id="b096b-251">Może istnieć Metoda ogólna, która następuje po podobnym wzorcu:</span><span class="sxs-lookup"><span data-stu-id="b096b-251">You may have a generic method that follows a similar pattern:</span></span>

```csharp
public T Find<T>(IEnumerable<T> sequence, Func<T, bool> match)
```

<span data-ttu-id="b096b-252">Nie można określić, że wartość zwracana jest `T?`.</span><span class="sxs-lookup"><span data-stu-id="b096b-252">You can't specify that the return value is `T?`.</span></span> <span data-ttu-id="b096b-253">Metoda zwraca `null`, gdy nie znaleziono poszukiwanego elementu.</span><span class="sxs-lookup"><span data-stu-id="b096b-253">The method returns `null` when the sought item isn't found.</span></span> <span data-ttu-id="b096b-254">Ponieważ nie można zadeklarować typu zwracanego `T?`, należy dodać adnotację `MaybeNull` do zwracanej metody:</span><span class="sxs-lookup"><span data-stu-id="b096b-254">Since you can't declare a `T?` return type, you add the `MaybeNull` annotation to the method return:</span></span>

```csharp
[return: MaybeNull]
public T Find<T>(IEnumerable<T> sequence, Func<T, bool> match)
```

<span data-ttu-id="b096b-255">Poprzedni kod informuje wywołujących, że kontrakt implikuje typ niedopuszczający wartości null, ale zwracana wartość *może* być równa null.</span><span class="sxs-lookup"><span data-stu-id="b096b-255">The preceding code informs callers that the contract implies a non-nullable type, but the return value *may* actually be null.</span></span>  <span data-ttu-id="b096b-256">Użyj atrybutu `MaybeNull`, gdy interfejs API powinien być typu niedopuszczający wartości null, zazwyczaj jest parametrem typu ogólnego, ale mogą występować wystąpienia, w których `null` zostałyby zwrócone.</span><span class="sxs-lookup"><span data-stu-id="b096b-256">Use the `MaybeNull` attribute when your API should be a non-nullable type, typically a generic type parameter, but there may be instances where `null` would be returned.</span></span>

<span data-ttu-id="b096b-257">Można również określić, że wartość zwracana lub `out` lub argument `ref` nie ma wartości null, mimo że typ jest typem dopuszczającym wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-257">You can also specify that a return value or an `out` or `ref` argument isn't null even though the type is a nullable type.</span></span> <span data-ttu-id="b096b-258">Rozważmy metodę, która zapewnia, że tablica jest wystarczająco duża, aby pomieścić liczbę elementów.</span><span class="sxs-lookup"><span data-stu-id="b096b-258">Consider a method that ensures an array is large enough to hold a number of elements.</span></span> <span data-ttu-id="b096b-259">Jeśli argument wejściowy nie ma pojemności, procedura przydzieli nową tablicę i skopiuje do niej wszystkie istniejące elementy.</span><span class="sxs-lookup"><span data-stu-id="b096b-259">If the input argument doesn't have capacity, the routine would allocate a new array and copy all the existing elements into it.</span></span> <span data-ttu-id="b096b-260">Jeśli argument wejściowy jest `null`, procedura przydzieli nowy magazyn.</span><span class="sxs-lookup"><span data-stu-id="b096b-260">If the input argument is `null`, the routine would allocate new storage.</span></span> <span data-ttu-id="b096b-261">W przypadku wystarczającej pojemności procedura nie wykonuje żadnych czynności:</span><span class="sxs-lookup"><span data-stu-id="b096b-261">If there's sufficient capacity, the routine does nothing:</span></span>

```csharp
public void EnsureCapacity<T>(ref T[] storage, int size)
```

<span data-ttu-id="b096b-262">Tę procedurę można wywołać w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="b096b-262">You could call this routine as follows:</span></span>

```csharp
// messages has the default value (null) when EnsureCapacity is called:
EnsureCapacity<string>(ref messages, 10);
// messages is not null.
EnsureCapacity<string>(messages, 50);
```

<span data-ttu-id="b096b-263">Po włączeniu typów odwołań o wartości null, należy upewnić się, że poprzedni kod kompiluje bez ostrzeżeń.</span><span class="sxs-lookup"><span data-stu-id="b096b-263">After enabling null reference types, you want to ensure that the preceding code compiles without warnings.</span></span> <span data-ttu-id="b096b-264">Gdy metoda zwraca, gwarantowany argument `storage` nie ma wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-264">When the method returns, the `storage` argument is guaranteed to be not null.</span></span> <span data-ttu-id="b096b-265">Możliwe jest jednak, aby wywoływać `EnsureCapacity` z odwołaniem null.</span><span class="sxs-lookup"><span data-stu-id="b096b-265">However, it's acceptable to call `EnsureCapacity` with a null reference.</span></span> <span data-ttu-id="b096b-266">Można wprowadzić `storage` typu referencyjnego dopuszczającego wartość null i dodać `NotNull` po warunku do deklaracji parametru:</span><span class="sxs-lookup"><span data-stu-id="b096b-266">You can make `storage` a nullable reference type, and add the `NotNull` post-condition to the parameter declaration:</span></span>

```csharp
public void EnsureCapacity<T>([NotNull]ref T[]? storage, int size)
```

<span data-ttu-id="b096b-267">Poprzedni kod wyraża istniejący kontrakt bardzo jasno: obiekty wywołujące mogą przekazać zmienną z wartością `null`, ale wartość zwracana jest gwarantowana, aby nigdy nie była równa null.</span><span class="sxs-lookup"><span data-stu-id="b096b-267">The preceding code expresses the existing contract very clearly: Callers can pass a variable with the `null` value, but the return value is guaranteed to never be null.</span></span> <span data-ttu-id="b096b-268">Atrybut `NotNull` jest najbardziej przydatny dla argumentów `ref` i `out`, w których `null` może być przekazane jako argument, ale ten argument ma gwarancję not null, gdy metoda zwraca.</span><span class="sxs-lookup"><span data-stu-id="b096b-268">The `NotNull` attribute is most useful for `ref` and `out` arguments where `null` may be passed as an argument, but that argument is guaranteed to be not null when the method returns.</span></span>

<span data-ttu-id="b096b-269">Należy określić warunki końcowe bezwarunkowe, korzystając z następujących atrybutów:</span><span class="sxs-lookup"><span data-stu-id="b096b-269">You specify unconditional postconditions using the following attributes:</span></span>

- <span data-ttu-id="b096b-270">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): wartość zwracana niedopuszczający wartości null może być równa null.</span><span class="sxs-lookup"><span data-stu-id="b096b-270">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): A non-nullable return value may be null.</span></span>
- <span data-ttu-id="b096b-271">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): wartość zwracana do wartości null nigdy nie będzie równa null.</span><span class="sxs-lookup"><span data-stu-id="b096b-271">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): A nullable return value will never be null.</span></span>

## <a name="specify-conditional-post-conditions-notnullwhen-maybenullwhen-and-notnullifnotnull"></a><span data-ttu-id="b096b-272">Określ warunkowe warunki końcowe: `NotNullWhen`, `MaybeNullWhen`i `NotNullIfNotNull`</span><span class="sxs-lookup"><span data-stu-id="b096b-272">Specify conditional post-conditions: `NotNullWhen`, `MaybeNullWhen`, and `NotNullIfNotNull`</span></span>

<span data-ttu-id="b096b-273">Najkorzystniej znasz metodę `string` <xref:System.String.IsNullOrEmpty(System.String)?DisplayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="b096b-273">You're likely familiar with the `string` method <xref:System.String.IsNullOrEmpty(System.String)?DisplayProperty=nameWithType>.</span></span> <span data-ttu-id="b096b-274">Ta metoda zwraca `true`, gdy argument ma wartość null lub jest pustym ciągiem.</span><span class="sxs-lookup"><span data-stu-id="b096b-274">This method returns `true` when the argument is null or an empty string.</span></span> <span data-ttu-id="b096b-275">Jest to forma sprawdzania wartości null: obiekty wywołujące nie muszą mieć wartości null — Sprawdź, czy metoda zwraca `false`.</span><span class="sxs-lookup"><span data-stu-id="b096b-275">It's a form of null-check: Callers don't need to null-check the argument if the method returns `false`.</span></span> <span data-ttu-id="b096b-276">Aby określić metodę, taką jak ta, która dopuszcza wartość null, należy ustawić argument na typ dopuszczający wartość null i dodać atrybut `NotNullWhen`:</span><span class="sxs-lookup"><span data-stu-id="b096b-276">To make a method like this nullable aware, you'd set the argument to a nullable type, and add the `NotNullWhen` attribute:</span></span>

```csharp
bool IsNullOrEmpty([NotNullWhen(false)]string? value);
```

<span data-ttu-id="b096b-277">Informuje kompilator, że żaden kod, w którym wartość zwracana jest `false` nie musi być sprawdzona null.</span><span class="sxs-lookup"><span data-stu-id="b096b-277">That informs the compiler that any code where the return value is `false` need not be null-checked.</span></span> <span data-ttu-id="b096b-278">Dodanie atrybutu informuje o analizie statycznej kompilatora, który `IsNullOrEmpty` wykonuje wymagane sprawdzanie wartości null: gdy zwraca `false`, argument wejściowy nie jest `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-278">The addition of the attribute informs the compiler's static analysis that `IsNullOrEmpty` performs the necessary null check: when it returns `false`, the input argument is not `null`.</span></span>

```csharp
string? userInput = GetUserInput();
if (!string.IsNullOrEmpty(userInput))
{
   int messageLength = userInput.Length; // no null check needed.
}
// null check needed on userInput here.
```

<span data-ttu-id="b096b-279">Metoda <xref:System.String.IsNullOrEmpty(System.String)?DisplayProperty=nameWithType> zostanie umieszczona w adnotacji, jak pokazano powyżej dla programu .NET Core 3,0.</span><span class="sxs-lookup"><span data-stu-id="b096b-279">The <xref:System.String.IsNullOrEmpty(System.String)?DisplayProperty=nameWithType> method will be annotated as shown above for .NET Core 3.0.</span></span> <span data-ttu-id="b096b-280">W bazie kodu mogą znajdować się podobne metody, które sprawdzają stan obiektów dla wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-280">You may have similar methods in your codebase that check the state of objects for null values.</span></span> <span data-ttu-id="b096b-281">Kompilator nie rozpoznaje niestandardowych metod sprawdzania wartości null i należy samodzielnie dodać adnotacje.</span><span class="sxs-lookup"><span data-stu-id="b096b-281">The compiler won't recognize custom null check methods, and you'll need to add the annotations yourself.</span></span> <span data-ttu-id="b096b-282">Po dodaniu atrybutu analiza statyczna kompilatora wie, kiedy sprawdzona zmienna ma wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-282">When you add the attribute, the compiler's static analysis knows when the tested variable has been null checked.</span></span>

<span data-ttu-id="b096b-283">Innym zastosowaniem tych atrybutów jest wzorzec `Try*`.</span><span class="sxs-lookup"><span data-stu-id="b096b-283">Another use for these attributes is the `Try*` pattern.</span></span> <span data-ttu-id="b096b-284">Warunki końcowe dla zmiennych `ref` i `out` są przekazywane przez wartość zwracaną.</span><span class="sxs-lookup"><span data-stu-id="b096b-284">The postconditions for `ref` and `out` variables are communicated through the return value.</span></span> <span data-ttu-id="b096b-285">Rozważmy tę metodę pokazaną wcześniej:</span><span class="sxs-lookup"><span data-stu-id="b096b-285">Consider this method shown earlier:</span></span>

```csharp
bool TryGetMessage(string key, out string message)
```

<span data-ttu-id="b096b-286">Poprzednia metoda jest zgodna z typowym środowiskiem .NET idiom: wartość zwracana wskazuje, czy `message` została ustawiona na znalezioną wartość, lub jeśli nie zostanie znaleziony żaden komunikat do wartości domyślnej.</span><span class="sxs-lookup"><span data-stu-id="b096b-286">The preceding method follows a typical .NET idiom: the return value indicates if `message` was set to the found value or, if no message is found, to the default value.</span></span> <span data-ttu-id="b096b-287">Jeśli metoda zwraca `true`, wartość `message` nie jest równa null; w przeciwnym razie metoda ustawia `message` na null.</span><span class="sxs-lookup"><span data-stu-id="b096b-287">If the method returns `true`, the value of `message` isn't null; otherwise, the method sets `message` to null.</span></span>

<span data-ttu-id="b096b-288">Można komunikować się z idiom przy użyciu atrybutu `NotNullWhen`.</span><span class="sxs-lookup"><span data-stu-id="b096b-288">You can communicate that idiom using the `NotNullWhen` attribute.</span></span> <span data-ttu-id="b096b-289">Podczas aktualizowania podpisu dla typów referencyjnych dopuszczających wartości null, należy `message` `string?` i dodać atrybut:</span><span class="sxs-lookup"><span data-stu-id="b096b-289">When you update the signature for nullable reference types, make `message` a `string?` and add an attribute:</span></span>

```csharp
bool TryGetMessage(string key, [NotNullWhen(true)] out string? message)
```

<span data-ttu-id="b096b-290">W powyższym przykładzie wartość `message` jest znana od null, gdy `TryGetMessage` zwraca `true`.</span><span class="sxs-lookup"><span data-stu-id="b096b-290">In the preceding example, the value of `message` is known to be not null when `TryGetMessage` returns `true`.</span></span> <span data-ttu-id="b096b-291">Należy dodać adnotacje do podobnych metod w bazie kodu w taki sam sposób: argumenty mogą być `null`i nie mieć wartości null, gdy metoda zwraca `true`.</span><span class="sxs-lookup"><span data-stu-id="b096b-291">You should annotate similar methods in your codebase in the same way: the arguments could be `null`, and are known to be not null when the method returns `true`.</span></span>

<span data-ttu-id="b096b-292">Istnieje również jeden atrybut końcowy.</span><span class="sxs-lookup"><span data-stu-id="b096b-292">There's one final attribute you may also need.</span></span> <span data-ttu-id="b096b-293">Czasami stan null wartości zwracanej zależy od stanu zerowego co najmniej jednego argumentu wejściowego.</span><span class="sxs-lookup"><span data-stu-id="b096b-293">Sometimes the null state of a return value depends on the null state of one or more input arguments.</span></span> <span data-ttu-id="b096b-294">Metody te zwracają wartość różną od null, gdy pewne argumenty wejściowe nie będą `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-294">These methods will return a non-null value whenever certain input arguments aren't `null`.</span></span> <span data-ttu-id="b096b-295">Aby poprawnie dodać adnotację do tych metod, Użyj atrybutu `NotNullIfNotNull`.</span><span class="sxs-lookup"><span data-stu-id="b096b-295">To correctly annotate these methods, you use the `NotNullIfNotNull` attribute.</span></span> <span data-ttu-id="b096b-296">Weź pod uwagę następującą metodę:</span><span class="sxs-lookup"><span data-stu-id="b096b-296">Consider the following method:</span></span>

```csharp
string GetTopLevelDomainFromFullUrl(string url);
```

<span data-ttu-id="b096b-297">Jeśli argument `url` nie ma wartości null, dane wyjściowe nie `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-297">If the `url` argument isn't null, the output isn't `null`.</span></span> <span data-ttu-id="b096b-298">Po włączeniu odwołań do wartości null ten podpis działa prawidłowo, pod warunkiem, że interfejs API nigdy nie akceptuje danych wejściowych o wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-298">Once nullable references are enabled, that signature works correctly, provided your API never accepts a null input.</span></span> <span data-ttu-id="b096b-299">Jeśli jednak dane wejściowe mogą mieć wartość null, wówczas wartość zwracana może być również wartością null.</span><span class="sxs-lookup"><span data-stu-id="b096b-299">However, if the input could be null, then return value could also be null.</span></span> <span data-ttu-id="b096b-300">W związku z tym można zmienić sygnaturę na następujący kod:</span><span class="sxs-lookup"><span data-stu-id="b096b-300">Therefore, you could change the signature to the following code:</span></span>

```csharp
string? GetTopLevelDomainFromFullUrl(string? url);
```

<span data-ttu-id="b096b-301">To również działa, ale często wymuszają wywoływania dodatkowych `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-301">That also works, but will often force callers to implement extra `null` checks.</span></span> <span data-ttu-id="b096b-302">Kontrakt polega na tym, że wartość zwracana byłaby `null` tylko wtedy, gdy argument wejściowy `url` jest `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-302">The contract is that the return value would be `null` only when the input argument `url` is `null`.</span></span> <span data-ttu-id="b096b-303">Aby można było wyrazić ten kontrakt, należy dodać adnotację do tej metody, jak pokazano w poniższym kodzie:</span><span class="sxs-lookup"><span data-stu-id="b096b-303">To express that contract, you would annotate this method as shown in the following code:</span></span>

```csharp
[return: NotNullIfNotNull("url")]
string? GetTopLevelDomainFromFullUrl(string? url);
```

<span data-ttu-id="b096b-304">Wartość zwracana i argument mają adnotację z `?` wskazującą, że można `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-304">The return value and the argument have both been annotated with the `?` indicating that either could be `null`.</span></span> <span data-ttu-id="b096b-305">Ten atrybut dokładniej objaśnia, że wartość zwracana nie będzie równa null, jeśli argument `url` nie jest `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-305">The attribute further clarifies that the return value won't be null when the `url` argument isn't `null`.</span></span>

<span data-ttu-id="b096b-306">Należy określić warunkowe warunki końcowe przy użyciu następujących atrybutów:</span><span class="sxs-lookup"><span data-stu-id="b096b-306">You specify conditional postconditions using these attributes:</span></span>

- <span data-ttu-id="b096b-307">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): Argument wejściowy, który nie dopuszcza wartości null, może mieć wartość null, gdy metoda zwraca określoną wartość `bool`.</span><span class="sxs-lookup"><span data-stu-id="b096b-307">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): A non-nullable input argument may be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="b096b-308">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): Argument wejściowy do wartości null nie będzie miał wartości null, gdy metoda zwróci określoną wartość `bool`.</span><span class="sxs-lookup"><span data-stu-id="b096b-308">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): A nullable input argument will not be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="b096b-309">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): wartość zwracana nie ma wartości null, jeśli argument wejściowy dla określonego parametru nie ma wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-309">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): A return value isn't null if the input argument for the specified parameter isn't null.</span></span>

## <a name="generic-definitions-and-nullability"></a><span data-ttu-id="b096b-310">Definicje generyczne i wartości null</span><span class="sxs-lookup"><span data-stu-id="b096b-310">Generic definitions and nullability</span></span>

<span data-ttu-id="b096b-311">Poprawne powiadamianie o stanie null typów ogólnych i metod ogólnych wymaga szczególnej uwagi.</span><span class="sxs-lookup"><span data-stu-id="b096b-311">Correctly communicating the null state of generic types and generic methods requires special care.</span></span> <span data-ttu-id="b096b-312">Wynika to z faktu, że typ wartości null i typ referencyjny dopuszczający wartość null są zasadniczo różne.</span><span class="sxs-lookup"><span data-stu-id="b096b-312">This stems from the fact that a nullable value type and a nullable reference type are fundamentally different.</span></span> <span data-ttu-id="b096b-313">`int?` jest synonimem dla `Nullable<int>`, a `string?` jest `string` z atrybutem dodanym przez kompilator.</span><span class="sxs-lookup"><span data-stu-id="b096b-313">An `int?` is a synonym for `Nullable<int>`, whereas `string?` is `string` with an attribute added by the compiler.</span></span> <span data-ttu-id="b096b-314">W efekcie kompilator nie może wygenerować poprawnego kodu dla `T?` bez znajomości, czy `T` jest `class` lub `struct`.</span><span class="sxs-lookup"><span data-stu-id="b096b-314">The result is that the compiler can't generate correct code for `T?` without knowing if `T` is a `class` or a `struct`.</span></span> 

<span data-ttu-id="b096b-315">Nie oznacza to, że nie można użyć typu dopuszczającego wartości null (typu wartości lub typu referencyjnego) jako argumentu typu dla zamkniętego typu ogólnego.</span><span class="sxs-lookup"><span data-stu-id="b096b-315">This doesn't mean you can't use a nullable type (either value type or reference type) as the type argument for a closed generic type.</span></span> <span data-ttu-id="b096b-316">Zarówno `List<string?>`, jak i `List<int?>` są prawidłowymi wystąpieniami `List<T>`.</span><span class="sxs-lookup"><span data-stu-id="b096b-316">Both `List<string?>` and `List<int?>` are valid instantiations of `List<T>`.</span></span> 

<span data-ttu-id="b096b-317">Oznacza to, że nie można użyć `T?` w ogólnej klasie lub deklaracji metody bez ograniczeń.</span><span class="sxs-lookup"><span data-stu-id="b096b-317">What it does mean is that you can't use `T?` in a generic class or method declaration without constraints.</span></span> <span data-ttu-id="b096b-318">Na przykład <xref:System.Linq.Enumerable.FirstOrDefault%60%601(System.Collections.Generic.IEnumerable%7B%60%600%7D)?displayProperty=nameWithType> nie zostanie zmieniony na zwraca `T?`.</span><span class="sxs-lookup"><span data-stu-id="b096b-318">For example, <xref:System.Linq.Enumerable.FirstOrDefault%60%601(System.Collections.Generic.IEnumerable%7B%60%600%7D)?displayProperty=nameWithType> won't be changed to return `T?`.</span></span> <span data-ttu-id="b096b-319">Ograniczenie to można przezwyciężyć, dodając ograniczenie `struct` lub `class`.</span><span class="sxs-lookup"><span data-stu-id="b096b-319">You can overcome this limitation by adding either the `struct` or `class` constraint.</span></span> <span data-ttu-id="b096b-320">Z dowolnego z tych ograniczeń kompilator wie, jak generować kod dla `T` i `T?`.</span><span class="sxs-lookup"><span data-stu-id="b096b-320">With either of those constraints, the compiler knows how to generate code for both `T` and `T?`.</span></span>

<span data-ttu-id="b096b-321">Możesz chcieć ograniczyć typy używane dla argumentu typu ogólnego jako niedopuszczające wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-321">You may want to restrict the types used for a generic type argument to be non-nullable types.</span></span> <span data-ttu-id="b096b-322">Można to zrobić przez dodanie ograniczenia `notnull` tego argumentu typu.</span><span class="sxs-lookup"><span data-stu-id="b096b-322">You can do that by adding the `notnull` constraint on that type argument.</span></span> <span data-ttu-id="b096b-323">Po zastosowaniu tego ograniczenia argument typu nie może być typem dopuszczającym wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-323">When that constraint is applied, the type argument must not be a nullable type.</span></span>

## <a name="conclusions"></a><span data-ttu-id="b096b-324">Wnioski</span><span class="sxs-lookup"><span data-stu-id="b096b-324">Conclusions</span></span>

<span data-ttu-id="b096b-325">Dodanie typów referencyjnych dopuszczających wartość null zapewnia wstępne słownictwo do opisywania oczekiwań interfejsów API dla zmiennych, które mogą być `null`.</span><span class="sxs-lookup"><span data-stu-id="b096b-325">Adding nullable reference types provides an initial vocabulary to describe your APIs expectations for variables that could be `null`.</span></span> <span data-ttu-id="b096b-326">Dodatkowe atrybuty zapewniają bogatszy słownictwo do opisania stanu wartości null zmiennych jako warunków wstępnych i warunki końcowe.</span><span class="sxs-lookup"><span data-stu-id="b096b-326">The additional attributes provide a richer vocabulary to describe the null state of variables as preconditions and postconditions.</span></span> <span data-ttu-id="b096b-327">Te atrybuty bardziej wyraźnie opisują oczekiwania i zapewniają lepszy komfort używania interfejsów API przez deweloperów.</span><span class="sxs-lookup"><span data-stu-id="b096b-327">These attributes more clearly describe your expectations and provide a better experience for the developers using your APIs.</span></span>

<span data-ttu-id="b096b-328">Podczas aktualizowania bibliotek dla kontekstu dopuszczającego wartość null należy dodać te atrybuty, aby ułatwić użytkownikom interfejsów API poprawne użycie.</span><span class="sxs-lookup"><span data-stu-id="b096b-328">As you update libraries for a nullable context, add these attributes to guide users of your APIs to the correct usage.</span></span> <span data-ttu-id="b096b-329">Te atrybuty pomagają w pełni opisać stan null argumentów wejściowych i zwracanych wartości:</span><span class="sxs-lookup"><span data-stu-id="b096b-329">These attributes help you fully describe the null-state of input arguments and return values:</span></span>

- <span data-ttu-id="b096b-330">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): Argument wejściowy niedopuszczający wartości null może mieć wartość null.</span><span class="sxs-lookup"><span data-stu-id="b096b-330">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): A non-nullable input argument may be null.</span></span>
- <span data-ttu-id="b096b-331">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): Argument wejściowy dopuszczający wartość null nigdy nie powinien mieć wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-331">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): A nullable input argument should never be null.</span></span>
- <span data-ttu-id="b096b-332">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): wartość zwracana niedopuszczający wartości null może być równa null.</span><span class="sxs-lookup"><span data-stu-id="b096b-332">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): A non-nullable return value may be null.</span></span>
- <span data-ttu-id="b096b-333">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): wartość zwracana do wartości null nigdy nie będzie równa null.</span><span class="sxs-lookup"><span data-stu-id="b096b-333">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): A nullable return value will never be null.</span></span>
- <span data-ttu-id="b096b-334">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): Argument wejściowy, który nie dopuszcza wartości null, może mieć wartość null, gdy metoda zwraca określoną wartość `bool`.</span><span class="sxs-lookup"><span data-stu-id="b096b-334">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): A non-nullable input argument may be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="b096b-335">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): Argument wejściowy do wartości null nie będzie miał wartości null, gdy metoda zwróci określoną wartość `bool`.</span><span class="sxs-lookup"><span data-stu-id="b096b-335">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): A nullable input argument will not be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="b096b-336">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): wartość zwracana nie ma wartości null, jeśli argument wejściowy dla określonego parametru nie ma wartości null.</span><span class="sxs-lookup"><span data-stu-id="b096b-336">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): A return value isn't null if the input argument for the specified parameter isn't null.</span></span>
