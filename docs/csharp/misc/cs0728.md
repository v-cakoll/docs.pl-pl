---
title: Ostrzeżenie o kompilatorze (poziom 2) CS0728
ms.date: 07/20/2015
f1_keywords:
- CS0728
helpviewer_keywords:
- CS0728
ms.assetid: ad6d860d-bac4-48f3-9eab-1efd2b6de6c0
ms.openlocfilehash: 290c5e8a51da0b9dff6fd555a70079f340d30943
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/12/2020
ms.locfileid: "79171914"
---
# <a name="compiler-warning-level-2-cs0728"></a><span data-ttu-id="e4d56-102">Ostrzeżenie o kompilatorze (poziom 2) CS0728</span><span class="sxs-lookup"><span data-stu-id="e4d56-102">Compiler Warning (level 2) CS0728</span></span>
<span data-ttu-id="e4d56-103">Ewentualnie niepoprawne przypisanie do lokalnej "zmiennej", która jest argumentem instrukcji using lub lock.</span><span class="sxs-lookup"><span data-stu-id="e4d56-103">Possibly incorrect assignment to local 'variable' which is the argument to a using or lock statement.</span></span>  <span data-ttu-id="e4d56-104">Dispose wywołania lub odblokowania nastąpi na oryginalnej wartości lokalnych.</span><span class="sxs-lookup"><span data-stu-id="e4d56-104">The Dispose call or unlocking will happen on the original value of the local.</span></span>  
  
 <span data-ttu-id="e4d56-105">Istnieje kilka scenariuszy, w których `using` lub `lock` bloków spowoduje tymczasowy wyciek zasobów.</span><span class="sxs-lookup"><span data-stu-id="e4d56-105">There are several scenarios where `using` or `lock` blocks will result in a temporary leak of resources.</span></span> <span data-ttu-id="e4d56-106">Oto jeden przykład:</span><span class="sxs-lookup"><span data-stu-id="e4d56-106">Here is one example:</span></span>  
  
 `thisType f = null;`  
  
 `using (f)`  
  
 `{`  
  
 `f = new thisType();`  
  
 `...`  
  
 `}`  
  
 <span data-ttu-id="e4d56-107">W takim przypadku oryginalna wartość, na przykład `thisType` null, zmiennej `using` zostanie usunięte po zakończeniu `thisType` wykonywania bloku, ale obiekt utworzony wewnątrz bloku nie będzie, mimo że ostatecznie będzie odbierać śmieci.</span><span class="sxs-lookup"><span data-stu-id="e4d56-107">In this case, the original value, such as null, of the variable `thisType` will be disposed of when the `using` block finishes executing, but the `thisType` object created inside the block will not be, although it will eventually get garbage collected.</span></span>  
  
 <span data-ttu-id="e4d56-108">Aby rozwiązać ten błąd, użyj następującego formularza:</span><span class="sxs-lookup"><span data-stu-id="e4d56-108">To resolve this error, use the following form:</span></span>  
  
 `using (thisType f = new thisType())`  
  
 `{`  
  
 `...`  
  
 `}`  
  
 <span data-ttu-id="e4d56-109">W takim przypadku nowo `thisType` przydzielony obiekt zostanie usunięty.</span><span class="sxs-lookup"><span data-stu-id="e4d56-109">In this case, the newly allocated `thisType` object will be disposed of.</span></span>  
  
## <a name="example"></a><span data-ttu-id="e4d56-110">Przykład</span><span class="sxs-lookup"><span data-stu-id="e4d56-110">Example</span></span>  
 <span data-ttu-id="e4d56-111">Poniższy kod wygeneruje ostrzeżenie CS0728.</span><span class="sxs-lookup"><span data-stu-id="e4d56-111">The following code will generate warning CS0728.</span></span>  
  
```csharp
// CS0728.cs  
  
using System;  
public class ValidBase : IDisposable  
{  
    public void Dispose() {  }  
}  
  
public class Logger  
{  
    public static void dummy()  
    {  
        ValidBase vb = null;  
        using (vb)
        {  
            vb = null;  // CS0728  
        }  
        vb = null;  
    }  
    public static void Main() { }  
}  
```
