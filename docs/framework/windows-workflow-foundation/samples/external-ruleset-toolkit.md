---
title: Zewnętrzne przybornik zestawu reguł
ms.date: 03/30/2017
ms.assetid: a306d283-a031-475e-aa01-9ae86e7adcb0
ms.openlocfilehash: c453c6137beeae8eee0e356734a1f9cdf8d8568b
ms.sourcegitcommit: bce0586f0cccaae6d6cbd625d5a7b824d1d3de4b
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/02/2019
ms.locfileid: "58840240"
---
# <a name="external-ruleset-toolkit"></a><span data-ttu-id="bca28-102">Zewnętrzne przybornik zestawu reguł</span><span class="sxs-lookup"><span data-stu-id="bca28-102">External Ruleset Toolkit</span></span>

<span data-ttu-id="bca28-103">Zwykle, gdy zasady są używane w aplikacji przepływu pracy, reguły są częścią zestawu.</span><span class="sxs-lookup"><span data-stu-id="bca28-103">Normally when rules are used within a workflow application, the rules are part of the assembly.</span></span> <span data-ttu-id="bca28-104">W niektórych przypadkach warto zachować zestawów reguł, niezależnie od zestawu, dzięki czemu mogą być aktualizowane bez ponownego tworzenia i wdrażania zestawu przepływu pracy.</span><span class="sxs-lookup"><span data-stu-id="bca28-104">In some scenarios, you may want to maintain the RuleSets separately from the assembly so that they can be updated without rebuilding and deploying the workflow assembly.</span></span> <span data-ttu-id="bca28-105">Ten przykład umożliwia zarządzanie i Edytuj zestawów reguł w bazie danych i uzyskać dostęp do tych zestawów reguł z przepływu pracy w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="bca28-105">This sample allows you to manage and edit RuleSets in a database and access those RuleSets from a workflow at runtime.</span></span> <span data-ttu-id="bca28-106">Umożliwia to uruchamianie wystąpienia przepływu pracy automatycznie zastosować zmian zestaw reguł.</span><span class="sxs-lookup"><span data-stu-id="bca28-106">This enables running workflow instances to automatically incorporate RuleSet changes.</span></span>

<span data-ttu-id="bca28-107">Przykład zewnętrzne Przybornik zestawu reguł zawiera narzędzie oparte na formularzach Windows, używanej do zarządzania i Edytuj wersje zestawu reguł w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="bca28-107">The External RuleSet Toolkit sample contains a Windows Forms-based tool that you can use to manage and edit RuleSet versions in a database.</span></span> <span data-ttu-id="bca28-108">Zawiera także działania, a usługa hosta do wykonywania tych zasad.</span><span class="sxs-lookup"><span data-stu-id="bca28-108">It also includes an activity and a host service for executing those rules.</span></span>

> [!NOTE]
> <span data-ttu-id="bca28-109">Ten przykładowy skrypt wymaga [programu Microsoft SQL Server](https://go.microsoft.com/fwlink/?LinkId=96181).</span><span class="sxs-lookup"><span data-stu-id="bca28-109">This sample requires [Microsoft SQL Server](https://go.microsoft.com/fwlink/?LinkId=96181).</span></span>

<span data-ttu-id="bca28-110">Program Visual Studio udostępnia edytorze zestawu reguł w ramach programu Windows Workflow Foundation (WF).</span><span class="sxs-lookup"><span data-stu-id="bca28-110">Visual Studio provides a RuleSet editor as part of the Windows Workflow Foundation (WF).</span></span> <span data-ttu-id="bca28-111">Ten edytor można uruchomić przez dwukrotne kliknięcie `Policy` działania w przepływie pracy; to szereguje zdefiniowany obiekt RuleSet w pliku Rules skojarzone z przepływem pracy ( `Policy` działanie jest uruchamiane wystąpienie zestaw reguł dla przepływu pracy).</span><span class="sxs-lookup"><span data-stu-id="bca28-111">You can start this editor by double-clicking the `Policy` activity in a workflow; it serializes the defined RuleSet object to the .rules file associated with the workflow (a `Policy` activity runs a RuleSet instance against the workflow).</span></span> <span data-ttu-id="bca28-112">Plik Rules jest skompilowany w zestawie jako zasób, podczas kompilowania projektu przepływu pracy.</span><span class="sxs-lookup"><span data-stu-id="bca28-112">The .rules file is compiled into the assembly as a resource when you build the workflow project.</span></span>

<span data-ttu-id="bca28-113">Składnik to między innymi próbki:</span><span class="sxs-lookup"><span data-stu-id="bca28-113">The components of this sample include:</span></span>

- <span data-ttu-id="bca28-114">Narzędzie interfejsu interfejs graficzny użytkownika zestaw reguł, które służy do edytowania i zarządzanie wersjami zestawu reguł w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="bca28-114">A RuleSet graphical user interface tool that you can use to edit and manage RuleSet versions in the database.</span></span>

- <span data-ttu-id="bca28-115">Usługa zestaw reguł, która jest konfigurowana dla aplikacji hosta i uzyskuje dostęp do zestawów reguł z bazy danych.</span><span class="sxs-lookup"><span data-stu-id="bca28-115">A RuleSet service that is configured on the host application and accesses RuleSets from the database.</span></span>

- <span data-ttu-id="bca28-116">`ExternalPolicy` Działania, żądania zestaw reguł z usługi zestaw reguł, która jest uruchamiana zestaw reguł dla przepływu pracy.</span><span class="sxs-lookup"><span data-stu-id="bca28-116">An `ExternalPolicy` activity that requests a RuleSet from the RuleSet service and runs the RuleSet against the workflow.</span></span>

<span data-ttu-id="bca28-117">Interakcja składników jest wyświetlany na poniższej ilustracji.</span><span class="sxs-lookup"><span data-stu-id="bca28-117">The interaction of the components is shown in the following image.</span></span> <span data-ttu-id="bca28-118">W kolejnych sekcjach opisano każdego składnika.</span><span class="sxs-lookup"><span data-stu-id="bca28-118">The sections that follow describe each component.</span></span>

![Diagram przedstawiający omówienie przykładowych zewnętrzne Przybornik zestawu reguł.](./media/external-ruleset-toolkit/ruleset-toolkit-overview.gif)

> [!IMPORTANT]
> <span data-ttu-id="bca28-120">Przykłady może już być zainstalowany na tym komputerze.</span><span class="sxs-lookup"><span data-stu-id="bca28-120">The samples may already be installed on your machine.</span></span> <span data-ttu-id="bca28-121">Przed kontynuowaniem sprawdź, czy są dostępne dla następującego katalogu (ustawienie domyślne).</span><span class="sxs-lookup"><span data-stu-id="bca28-121">Check for the following (default) directory before continuing.</span></span>
>
> `<InstallDrive>:\WF_WCF_Samples`
>
> <span data-ttu-id="bca28-122">Jeśli ten katalog nie istnieje, przejdź do strony [Windows Communication Foundation (WCF) i przykłady Windows Workflow Foundation (WF) dla platformy .NET Framework 4](https://go.microsoft.com/fwlink/?LinkId=150780) do pobierania wszystkich Windows Communication Foundation (WCF) i [!INCLUDE[wf1](../../../../includes/wf1-md.md)] przykładów.</span><span class="sxs-lookup"><span data-stu-id="bca28-122">If this directory does not exist, go to [Windows Communication Foundation (WCF) and Windows Workflow Foundation (WF) Samples for .NET Framework 4](https://go.microsoft.com/fwlink/?LinkId=150780) to download all Windows Communication Foundation (WCF) and [!INCLUDE[wf1](../../../../includes/wf1-md.md)] samples.</span></span> <span data-ttu-id="bca28-123">W tym przykładzie znajduje się w następującym katalogu.</span><span class="sxs-lookup"><span data-stu-id="bca28-123">This sample is located in the following directory.</span></span>
>
> `<InstallDrive>:\WF_WCF_Samples\WF\Scenario\ExternalRuleSetToolKit`

## <a name="ruleset-tool"></a><span data-ttu-id="bca28-124">Narzędzia zestawu reguł</span><span class="sxs-lookup"><span data-stu-id="bca28-124">RuleSet Tool</span></span>

<span data-ttu-id="bca28-125">Poniższej ilustracji przedstawiono zrzut ekranu narzędzia do zestawu reguł.</span><span class="sxs-lookup"><span data-stu-id="bca28-125">The following image is a screenshot of the RuleSet tool.</span></span> <span data-ttu-id="bca28-126">Z **Store reguły** menu, można załadować dostępne zestawy reguł z bazy danych i zapisać zmodyfikowane zestawów reguł do sklepu.</span><span class="sxs-lookup"><span data-stu-id="bca28-126">From the **Rule Store** menu, you can load the available RuleSets from the database and save modified RuleSets back to the store.</span></span> <span data-ttu-id="bca28-127">Plik konfiguracji aplikacji zawiera parametry połączenia bazy danych dla bazy danych zestaw reguł.</span><span class="sxs-lookup"><span data-stu-id="bca28-127">An application configuration file provides a database connection string for the RuleSet database.</span></span> <span data-ttu-id="bca28-128">Możesz uruchomić to narzędzie, automatycznie ładuje zestawy reguł z bazy danych skonfigurowane.</span><span class="sxs-lookup"><span data-stu-id="bca28-128">When you start the tool, it automatically loads the RuleSets from the configured database.</span></span>

![Zrzut ekranu przedstawiający przeglądarkę zestaw reguł.](./media/external-ruleset-toolkit/ruleset-browser-dialog.gif)

<span data-ttu-id="bca28-130">Narzędzie RuleSet dotyczy numery wersji głównych i pomocniczych zestawów reguł, umożliwiając jednocześnie obsługiwać i przechowywać wiele wersji (to narzędzie zawiera bez blokowania lub innych konfiguracji funkcji zarządzania, oprócz możliwości przechowywania wersji).</span><span class="sxs-lookup"><span data-stu-id="bca28-130">The RuleSet tool applies major and minor version numbers to the RuleSets, allowing you to simultaneously maintain and store multiple versions (the tool provides no locking or other configuration management features in addition to the versioning capability).</span></span> <span data-ttu-id="bca28-131">Za pomocą narzędzia, możesz utworzyć nowe wersje zestawu reguł lub usunąć istniejące wersje.</span><span class="sxs-lookup"><span data-stu-id="bca28-131">Using the tool, you can create new RuleSet versions or delete existing versions.</span></span> <span data-ttu-id="bca28-132">Po kliknięciu **New**, narzędzie tworzy nową nazwę zestawu reguł i stosuje w wersji 1.0.</span><span class="sxs-lookup"><span data-stu-id="bca28-132">When you click **New**, the tool creates a new RuleSet name and applies version 1.0.</span></span> <span data-ttu-id="bca28-133">Podczas kopiowania wersję narzędzia tworzy kopię wybranej wersji zestawu reguł, włączając reguły zawarte i przypisuje numery wersji nowych, unikatowych.</span><span class="sxs-lookup"><span data-stu-id="bca28-133">When you copy a version, the tool creates a copy of the selected RuleSet version, including the contained rules, and assigns new, unique version numbers.</span></span> <span data-ttu-id="bca28-134">Te numery wersji są oparte na numery wersji istniejących zestawów reguł.</span><span class="sxs-lookup"><span data-stu-id="bca28-134">These version numbers are based on the version numbers of existing RuleSets.</span></span> <span data-ttu-id="bca28-135">Można zmienić numery nazwą i wersją zestawu reguł przy użyciu skojarzonego pola w formularzu.</span><span class="sxs-lookup"><span data-stu-id="bca28-135">You can change the RuleSet name and version numbers using the associated fields on the form.</span></span>

<span data-ttu-id="bca28-136">Po kliknięciu **Edycja reguł**, uruchomiony Edytor zestawu reguł, jak pokazano na poniższej ilustracji:</span><span class="sxs-lookup"><span data-stu-id="bca28-136">When you click **Edit Rules**, the RuleSet editor starts, as shown in the following image:</span></span>

![Zrzut ekranu przedstawiający z edytora zestawu reguł.](./media/external-ruleset-toolkit/ruleset-editor-dialog.gif)

<span data-ttu-id="bca28-138">Jest to, ponownie hostingu okno dialogowe Edytor, który jest częścią dodatku Windows Workflow Foundation programu Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="bca28-138">This is a re-hosting of the editor dialog that is part of the Windows Workflow Foundation Visual Studio add-in.</span></span> <span data-ttu-id="bca28-139">Zapewnia te same funkcje, w tym obsługę funkcji Intellisense.</span><span class="sxs-lookup"><span data-stu-id="bca28-139">It provides the same functionality, including Intellisense support.</span></span> <span data-ttu-id="bca28-140">Zasady są tworzone na typ docelowy (np. przepływ pracy), który jest skojarzony z zestawu reguł w narzędziu; Po kliknięciu **Przeglądaj** w oknie dialogowym Narzędzia główne **selektor przepływu pracy i typu** zostanie wyświetlone okno dialogowe, jak pokazano na rysunku 4.</span><span class="sxs-lookup"><span data-stu-id="bca28-140">The rules are authored against a target type (such as a workflow) that is associated with the RuleSet in the tool; when you click **Browse** in the main tool dialog, the **Workflow/Type Selector** dialog appears, as shown in Figure 4.</span></span>

<span data-ttu-id="bca28-141">![Przepływ pracy &#47;wpisz wybór](./media/71f08d57-e8f2-499e-8151-ece2cbdcabfd.gif "71f08d57-e8f2-499e-8151-ece2cbdcabfd")</span><span class="sxs-lookup"><span data-stu-id="bca28-141">![Workflow &#47;Type Selection](./media/71f08d57-e8f2-499e-8151-ece2cbdcabfd.gif "71f08d57-e8f2-499e-8151-ece2cbdcabfd")</span></span>

<span data-ttu-id="bca28-142">Rysunek 4: Selektor typu/przepływu pracy</span><span class="sxs-lookup"><span data-stu-id="bca28-142">Figure 4: Workflow/Type Selector</span></span>

<span data-ttu-id="bca28-143">Możesz użyć **selektor przepływu pracy i typu** okna dialogowego, aby określić zestaw i określonego typu, w tym zestawie.</span><span class="sxs-lookup"><span data-stu-id="bca28-143">You can use the **Workflow/Type Selector** dialog to specify an assembly and a specific type within that assembly.</span></span> <span data-ttu-id="bca28-144">Ten typ jest typem docelowym, względem którego zasady są tworzone (i uruchom).</span><span class="sxs-lookup"><span data-stu-id="bca28-144">This type is the target type against which the rules are authored (and run).</span></span> <span data-ttu-id="bca28-145">W wielu przypadkach typ docelowy jest przepływ pracy lub innego typu działania.</span><span class="sxs-lookup"><span data-stu-id="bca28-145">In many cases, the target type is a workflow or some other activity type.</span></span> <span data-ttu-id="bca28-146">Można jednak uruchomić zestaw reguł, dla dowolnego typu .NET.</span><span class="sxs-lookup"><span data-stu-id="bca28-146">However, you can run a RuleSet against any .NET type.</span></span>

<span data-ttu-id="bca28-147">Ścieżka do pliku zestawu i typu `name are stored with the` zestaw reguł w bazie danych, tak, aby przy zestaw reguł są pobierane z bazy danych, narzędzie próbuje automatycznie załadować typ docelowy.</span><span class="sxs-lookup"><span data-stu-id="bca28-147">The path to the assembly file and the type `name are stored with the` RuleSet in the database, so that when the RuleSet is retrieved from the database, the tool attempts to automatically load the target type.</span></span>

<span data-ttu-id="bca28-148">Po kliknięciu **OK** w **selektor przepływu pracy i typu** okno dialogowe, sprawdza poprawność wybranego typu względem zestaw reguł, aby upewnić się, że typ docelowy wszystkie elementy członkowskie przywoływany przez zasady.</span><span class="sxs-lookup"><span data-stu-id="bca28-148">When you click **OK** in the **Workflow/Type Selector** dialog, it validates the selected type against the RuleSet, to ensure that the target type has all the members referenced by the rules.</span></span> <span data-ttu-id="bca28-149">Błędy są wyświetlane w **błędy sprawdzania poprawności** okna dialogowego.</span><span class="sxs-lookup"><span data-stu-id="bca28-149">Errors are shown in a **Validation Errors** dialog.</span></span> <span data-ttu-id="bca28-150">Można kontynuować zmiany, pomimo błędów, lub kliknij przycisk **anulować**.</span><span class="sxs-lookup"><span data-stu-id="bca28-150">You can choose to continue with the change despite the errors, or click **Cancel**.</span></span> <span data-ttu-id="bca28-151">Z **narzędzia** menu w oknie dialogowym Narzędzia główne, możesz kliknąć pozycję **weryfikacji** do ponownego sprawdzania poprawności wersji zestaw reguł względem działanie docelowe.</span><span class="sxs-lookup"><span data-stu-id="bca28-151">From the **Tools** menu in the main tool dialog, you can click **Validate** to re-validate the RuleSet version against the target activity.</span></span>

![Zrzut ekranu przedstawiający okno dialogowe błędy sprawdzania poprawności.](./media/external-ruleset-toolkit/validation-errors-dialog.png)

<span data-ttu-id="bca28-153">Z **danych** menu Narzędzia, można importować i eksportować zestawów reguł.</span><span class="sxs-lookup"><span data-stu-id="bca28-153">From the **Data** menu in the tool, you can import and export RuleSets.</span></span> <span data-ttu-id="bca28-154">Po kliknięciu **importu**, pojawi się okno dialogowe selektora plików, w którym można wybrać plik rules.</span><span class="sxs-lookup"><span data-stu-id="bca28-154">When you click **Import**, a file-chooser dialog appears, from which you can select a .rules file.</span></span> <span data-ttu-id="bca28-155">To może lub nie może być plikiem początkowo utworzona w programie Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="bca28-155">This may or may not be a file initially created in Visual Studio.</span></span> <span data-ttu-id="bca28-156">Ten plik Rules powinien zawierać Zserializowany obiekt `RuleDefinitions` wystąpienia, które zawiera kolekcję warunków i kolekcji zestawów reguł.</span><span class="sxs-lookup"><span data-stu-id="bca28-156">The .rules file should contain a serialized `RuleDefinitions` instance that contains a collection of conditions and a collection of RuleSets.</span></span> <span data-ttu-id="bca28-157">Narzędzie nie używa Kolekcja warunków, ale zajmuje `RuleDefinitions` format Rules umożliwiające interakcje ze środowiskiem programu Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="bca28-157">The tool does not use the conditions collection, but it does use the `RuleDefinitions` .rules format to allow interaction with the Visual Studio environment.</span></span>

<span data-ttu-id="bca28-158">Po wybraniu pliku Rules **selektor RuleSet** zostanie wyświetlone okno dialogowe.</span><span class="sxs-lookup"><span data-stu-id="bca28-158">After selecting a .rules file, a **RuleSet Selector** dialog appears.</span></span> <span data-ttu-id="bca28-159">Okno dialogowe umożliwia wybieranie zestawów reguł z pliku, który chcesz zaimportować (wartość domyślna Określa wszystkie zestawy reguł).</span><span class="sxs-lookup"><span data-stu-id="bca28-159">You can use the dialog to select the RuleSets from the file that you want to import (the default specifies all RuleSets).</span></span> <span data-ttu-id="bca28-160">Zestawy reguł w pliku Rules nie mają numery wersji, ponieważ ich przechowywanie wersji w ramach projektu WF jest taka sama jak wersja zestawu.</span><span class="sxs-lookup"><span data-stu-id="bca28-160">RuleSets in the .rules file do not have version numbers, because their versioning within a WF project is the same as the version of the assembly.</span></span> <span data-ttu-id="bca28-161">Podczas procesu importowania narzędzie automatycznie przypisuje następny dostępny główny numer wersji (która może zostać zmieniona po zaimportowaniu); można wyświetlić numery wersji przypisane w **selektor RuleSet** listy.</span><span class="sxs-lookup"><span data-stu-id="bca28-161">During the importing process, the tool automatically assigns the next available major version number (which you can change after importing); you can see the assigned version numbers in the **RuleSet Selector** list.</span></span>

<span data-ttu-id="bca28-162">Dla każdego zestawu reguł, który importuje narzędzie spróbuje znaleźć skojarzony typ z folderu bin\Debug znajdujące się w lokalizacji pliku Rules (jeśli istnieje), na podstawie elementów członkowskich, używany w zestawie reguł.</span><span class="sxs-lookup"><span data-stu-id="bca28-162">For each RuleSet it imports, the tool attempts to locate the associated type from the bin\Debug folder under the location of the .rules file (if it exists), based on the members used in the RuleSet.</span></span> <span data-ttu-id="bca28-163">Jeśli narzędzie wykryje wiele typów zgodnych, próbuje wybierz typ na podstawie dopasowania między nazwę typu i nazwę pliku Rules (na przykład `Workflow1` typ odpowiada Workflow1.rules).</span><span class="sxs-lookup"><span data-stu-id="bca28-163">If the tool finds multiple matching types, it attempts to choose a type based on a match between the .rules file name and the type name (for example, the `Workflow1` type corresponds to Workflow1.rules).</span></span> <span data-ttu-id="bca28-164">Jeśli istnieje wiele dopasowań, pojawia się monit o wybranie typu.</span><span class="sxs-lookup"><span data-stu-id="bca28-164">If multiple matches exist, you are prompted to select the type.</span></span> <span data-ttu-id="bca28-165">Jeśli ten mechanizm automatycznej identyfikacji nie znajdzie zgodnego zestawu lub typu, a następnie po zaimportowaniu można kliknąć **Przeglądaj** w oknie dialogowym Narzędzia główne, aby przejść do skojarzonego typu.</span><span class="sxs-lookup"><span data-stu-id="bca28-165">If this auto-identification mechanism fails to locate a matching assembly or type, then after importing you can click **Browse** on the main tool dialog to navigate to the associated type.</span></span> <span data-ttu-id="bca28-166">Na poniższej ilustracji pokazano selektor zestaw reguł:</span><span class="sxs-lookup"><span data-stu-id="bca28-166">The following image shows the RuleSet Selector:</span></span>

![Zrzut ekranu przedstawiający okno dialogowe selektora zestaw reguł.](./media/external-ruleset-toolkit/ruleset-selector-dialog.gif)

<span data-ttu-id="bca28-168">Po kliknięciu **Eksport danych** menu głównego narzędzia **selektor RuleSet** zostanie wyświetlone okno dialogowe, w którym można określić zestawy reguł z bazy danych, które powinny być wyeksportowane.</span><span class="sxs-lookup"><span data-stu-id="bca28-168">When you click **Data-Export** from the main tool menu, the **RuleSet Selector** dialog appears again, from which you can determine the RuleSets from the database that should be exported.</span></span> <span data-ttu-id="bca28-169">Po kliknięciu **OK**, **Zapisz plik** zostanie wyświetlone okno dialogowe, w którym można określić nazwę i lokalizację pliku wynikowego rules.</span><span class="sxs-lookup"><span data-stu-id="bca28-169">When you click **OK**, a **Save File** dialog appears, in which you can specify the name and location of the resulting .rules file.</span></span> <span data-ttu-id="bca28-170">Ponieważ plik Rules nie zawiera informacji o wersji, można wybrać tylko jedną wersję zestawu reguł o danej nazwie zestawu reguł.</span><span class="sxs-lookup"><span data-stu-id="bca28-170">Because the .rules file does not contain version information, you can only select one RuleSet version with a given RuleSet name.</span></span>

## <a name="policyfromservice-activity"></a><span data-ttu-id="bca28-171">Działanie PolicyFromService</span><span class="sxs-lookup"><span data-stu-id="bca28-171">PolicyFromService Activity</span></span>

<span data-ttu-id="bca28-172">Kod `PolicyFromService` działania jest proste.</span><span class="sxs-lookup"><span data-stu-id="bca28-172">The code for the `PolicyFromService` activity is straightforward.</span></span> <span data-ttu-id="bca28-173">Jak działa `Policy` działanie jest dostępne przy użyciu programu WF, ale zamiast pobieranie docelowy zestaw reguł z pliku Rules, wywołuje usługę hosta, do uzyskania wystąpienia zestaw reguł.</span><span class="sxs-lookup"><span data-stu-id="bca28-173">It works much like the `Policy` activity provided with WF, but instead of retrieving the target RuleSet from the .rules file, it calls a host service to obtain the RuleSet instance.</span></span> <span data-ttu-id="bca28-174">Następnie uruchamia zestaw reguł względem wystąpienia działania głównego przepływu pracy.</span><span class="sxs-lookup"><span data-stu-id="bca28-174">It then runs the RuleSet against the root workflow activity instance.</span></span>

<span data-ttu-id="bca28-175">Aby użyć działania w przepływie pracy, należy dodać odwołanie do `PolicyActivities` i `RuleSetService` zestawów z projektu przepływu pracy.</span><span class="sxs-lookup"><span data-stu-id="bca28-175">To use the activity in a workflow, add a reference to the `PolicyActivities` and `RuleSetService` assemblies from your workflow project.</span></span> <span data-ttu-id="bca28-176">Zapoznaj się z procedurą na końcu tego tematu, aby uzyskać informacje dotyczące sposobu dodawania działań do przybornika.</span><span class="sxs-lookup"><span data-stu-id="bca28-176">See the procedure at the end of this topic for a discussion of how to add the activity to the toolbox.</span></span>

<span data-ttu-id="bca28-177">Po umieszczeniu działania w przepływie pracy, należy podać nazwę zestawu reguł do uruchomienia.</span><span class="sxs-lookup"><span data-stu-id="bca28-177">After placing the activity in your workflow, you must provide the name of the RuleSet to be run.</span></span> <span data-ttu-id="bca28-178">Wprowadź nazwę jako wartości literału lub powiązać zmiennej przepływu pracy lub właściwość kolejnego działania.</span><span class="sxs-lookup"><span data-stu-id="bca28-178">You can enter the name as a literal value, or bind to a workflow variable or property of another activity.</span></span> <span data-ttu-id="bca28-179">Opcjonalnie można wprowadzić numery wersji określonego zestawu reguł, który powinien zostać uruchomiony.</span><span class="sxs-lookup"><span data-stu-id="bca28-179">Optionally, you can enter version numbers for the specific RuleSet that should be run.</span></span> <span data-ttu-id="bca28-180">Pozostawienie wartości domyślnej 0 dla numerów wersji głównych i pomocniczych numer najnowszej wersji w bazie danych jest dostarczana automatycznie dla działania.</span><span class="sxs-lookup"><span data-stu-id="bca28-180">If you leave the default value of 0 for the major and minor version numbers, the latest version number in the database is automatically provided for the activity.</span></span>

## <a name="ruleset-service"></a><span data-ttu-id="bca28-181">Zestaw reguł usługi</span><span class="sxs-lookup"><span data-stu-id="bca28-181">RuleSet Service</span></span>

<span data-ttu-id="bca28-182">Usługa jest odpowiedzialna za pobieranie określonej wersji zestawu reguł z bazy danych i przywróceniem go do wywoływania działania.</span><span class="sxs-lookup"><span data-stu-id="bca28-182">The service is responsible for retrieving the specified RuleSet version from the database and returning it to the calling activity.</span></span> <span data-ttu-id="bca28-183">Jak wspomniano, jeśli przekazany wartości wersji głównych i pomocniczych `GetRuleSet` wywołania są oba 0, usługa pobierze najnowszą wersję.</span><span class="sxs-lookup"><span data-stu-id="bca28-183">As previously discussed, if the major and minor version values passed in the `GetRuleSet` call are both 0, the service retrieves the latest version.</span></span> <span data-ttu-id="bca28-184">W tym momencie nie występuje buforowanie definicji zestawu reguł lub wystąpień; Podobnie nie istnieją żadne funkcje do oznaczania wersji zestaw reguł jako "wdrożone", aby odróżnić je od zestawów reguł w toku.</span><span class="sxs-lookup"><span data-stu-id="bca28-184">At this point, there is no caching of RuleSet definitions or instances; similarly, there are no features for marking RuleSet versions as "deployed" to differentiate them from in-progress RuleSets.</span></span>

<span data-ttu-id="bca28-185">Bazy danych można uzyskać dostęp przez usługę, należy skonfigurować na hoście przy użyciu pliku konfiguracji aplikacji.</span><span class="sxs-lookup"><span data-stu-id="bca28-185">The database to be accessed by the service should be configured on the host using an application configuration file.</span></span>

#### <a name="to-run-the-tool"></a><span data-ttu-id="bca28-186">Aby uruchomić narzędzie</span><span class="sxs-lookup"><span data-stu-id="bca28-186">To run the tool</span></span>

1. <span data-ttu-id="bca28-187">Folder, który konfiguruje Tabela zestaw reguł, używana przez narzędzia i usługi zawiera plik Setup.sql.</span><span class="sxs-lookup"><span data-stu-id="bca28-187">The folder that sets up the RuleSet table used by the tool and the service contains a Setup.sql file.</span></span> <span data-ttu-id="bca28-188">Możesz uruchomić plik wsadowy plik Setup.cmd do tworzenia bazy danych reguły na program SQL Express i konfigurowanie tabeli zestaw reguł.</span><span class="sxs-lookup"><span data-stu-id="bca28-188">You can run the Setup.cmd batch file to create the Rules database on SQL Express and to set up the RuleSet table.</span></span>

2. <span data-ttu-id="bca28-189">Jeśli poddasz edycji pliku wsadowego lub Setup.sql i określ nie należy używać programu SQL Express lub do umieszczenia w tabeli w bazie danych o nazwie coś innego niż `Rules`, pliki konfiguracji aplikacji w narzędziu zestaw reguł i `UsageSample` projektów powinny być edytowane o takiej samej informacje.</span><span class="sxs-lookup"><span data-stu-id="bca28-189">If you edit the batch file or Setup.sql and specify not to use SQL Express or to place the table in a database named something other than `Rules`, the application configuration files in the RuleSet tool and `UsageSample` projects should be edited with the same information.</span></span>

3. <span data-ttu-id="bca28-190">Po uruchomieniu skryptu Setup.sql, można tworzyć `ExternalRuleSetToolkit` rozwiązania, a następnie uruchom zestaw reguł narzędzia z projektu ExternalRuleSetTool.</span><span class="sxs-lookup"><span data-stu-id="bca28-190">After you run the Setup.sql script, you can build the `ExternalRuleSetToolkit` solution and then launch the RuleSet tool from the ExternalRuleSetTool project.</span></span>

4. <span data-ttu-id="bca28-191">`RuleSetToolkitUsageSample` Rozwiązanie sekwencyjne Aplikacja konsoli przepływu pracy zawiera przykładowy przepływ pracy.</span><span class="sxs-lookup"><span data-stu-id="bca28-191">The `RuleSetToolkitUsageSample` Sequential Workflow Console Application solution includes a sample workflow.</span></span> <span data-ttu-id="bca28-192">Przepływ pracy składa się z `PolicyFromService` działanie i dwie zmienne `orderValue` i `discount`, względem którego działa docelowy zestaw reguł.</span><span class="sxs-lookup"><span data-stu-id="bca28-192">The workflow consists of a `PolicyFromService` activity and two variables, `orderValue` and `discount`, against which the target RuleSet runs.</span></span>

5. <span data-ttu-id="bca28-193">Aby korzystać z przykładu, kompilacji `RuleSetToolkitUsageSample` rozwiązania.</span><span class="sxs-lookup"><span data-stu-id="bca28-193">To use the sample, build the `RuleSetToolkitUsageSample` solution.</span></span> <span data-ttu-id="bca28-194">Następnie z menu głównego narzędzia zestaw reguł, kliknij przycisk **importowania danych** i wskaż plik DiscountRuleSet.rules w folderze RuleSetToolkitUsageSample.</span><span class="sxs-lookup"><span data-stu-id="bca28-194">Then from the RuleSet tool main menu, click **Data-Import** and point to the DiscountRuleSet.rules file in the RuleSetToolkitUsageSample folder.</span></span> <span data-ttu-id="bca28-195">Kliknij przycisk **zapisem Store reguły** opcję menu, aby zapisać zaimportowany zestaw reguł w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="bca28-195">Click the **Rule Store-Save** menu option to save the imported RuleSet to the database.</span></span>

6. <span data-ttu-id="bca28-196">Ponieważ `PolicyActivities` odwołuje się do zestawu z przykładowego projektu przepływu pracy `PolicyFromService` działanie pojawiło się w przepływie pracy.</span><span class="sxs-lookup"><span data-stu-id="bca28-196">Because the `PolicyActivities` assembly is referenced from the sample workflow project, the `PolicyFromService` activity appears in the workflow.</span></span> <span data-ttu-id="bca28-197">Nie, jednak wydaje w przyborniku domyślnie.</span><span class="sxs-lookup"><span data-stu-id="bca28-197">It does not, however, appear in the toolbox by default.</span></span> <span data-ttu-id="bca28-198">Aby dodać go do przybornika, wykonaj następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="bca28-198">To add it to the toolbox, do the following:</span></span>

    - <span data-ttu-id="bca28-199">Kliknij prawym przyciskiem myszy przybornika, a następnie wybierz pozycję **wybierz elementy** (może to potrwać chwilę).</span><span class="sxs-lookup"><span data-stu-id="bca28-199">Right-click the toolbox and select **Choose Items** (this may take a while).</span></span>

    - <span data-ttu-id="bca28-200">Gdy **wybierz elementy przybornika** zostanie wyświetlone okno dialogowe, kliknij przycisk **działania** kartę.</span><span class="sxs-lookup"><span data-stu-id="bca28-200">When the **Choose Toolbox Items** dialog appears, click the **Activities** tab.</span></span>

    - <span data-ttu-id="bca28-201">Przejdź do `PolicyActivities` zestawu w `ExternalRuleSetToolkit` rozwiązań i kliknij przycisk **Otwórz**.</span><span class="sxs-lookup"><span data-stu-id="bca28-201">Browse to the `PolicyActivities` assembly in the `ExternalRuleSetToolkit` solution and click **Open**.</span></span>

    - <span data-ttu-id="bca28-202">Upewnij się, że `PolicyFromService` wybrano działania **wybierz elementy przybornika** okna dialogowego, a następnie kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="bca28-202">Ensure that the `PolicyFromService` activity is selected in the **Choose Toolbox Items** dialog and then click **OK**.</span></span>

    - <span data-ttu-id="bca28-203">Działanie powinien zostać wyświetlony na przybornika w **składniki RuleSetToolkitUsageSample** kategorii.</span><span class="sxs-lookup"><span data-stu-id="bca28-203">The activity should now appear in the toolbox in the **RuleSetToolkitUsageSample Components** category.</span></span>

7. <span data-ttu-id="bca28-204">Usługa zestaw reguł jest już skonfigurowany na hoście aplikacji konsoli przy użyciu następujących instrukcji w pliku Program.cs.</span><span class="sxs-lookup"><span data-stu-id="bca28-204">The RuleSet service is already configured on the console application host using the following statement in Program.cs.</span></span>

    ```csharp
    workflowRuntime.AddService(new RuleSetService());
    ```

8. <span data-ttu-id="bca28-205">Można również skonfigurować usługę na hoście przy użyciu pliku konfiguracji; można znaleźć w dokumentacji zestawu SDK, aby uzyskać szczegółowe informacje.</span><span class="sxs-lookup"><span data-stu-id="bca28-205">You can also configure the service on the host using a configuration file; see the SDK documentation for details.</span></span>

9. <span data-ttu-id="bca28-206">Plik konfiguracji aplikacji zostanie dodany do projektu przepływu pracy, aby określić parametry połączenia dla bazy danych, który będzie używany przez usługę.</span><span class="sxs-lookup"><span data-stu-id="bca28-206">An application configuration file is added to the workflow project to specify the connection string for the database to be used by the service.</span></span> <span data-ttu-id="bca28-207">Powinna to być parametry połączenia używane przez narzędzie zestaw reguł, które wskazują na bazę danych, zawierającą tabelę zestaw reguł.</span><span class="sxs-lookup"><span data-stu-id="bca28-207">This should be the same connection string used by the RuleSet tool, which points to the database that contains the RuleSet table.</span></span>

10. <span data-ttu-id="bca28-208">Możesz teraz uruchomić `RuleSetToolkitUsageSample` projektu, jak w przypadku innych aplikacji konsoli przepływu pracy.</span><span class="sxs-lookup"><span data-stu-id="bca28-208">You can now run the `RuleSetToolkitUsageSample` project as you would any other workflow console application.</span></span> <span data-ttu-id="bca28-209">Naciśnij klawisz F5 lub Ctrl + F5 w programie Visual Studio lub uruchom plik RuleSetToolkitUsageSample.exe bezpośrednio.</span><span class="sxs-lookup"><span data-stu-id="bca28-209">Press F5 or Ctrl+F5 within Visual Studio or run the RuleSetToolkitUsageSample.exe file directly.</span></span>

    > [!NOTE]
    > <span data-ttu-id="bca28-210">Należy zamknąć narzędzie zestaw reguł, aby ponownie skompilować przykład użycia, ponieważ narzędzie ładuje zestaw próbki użycia.</span><span class="sxs-lookup"><span data-stu-id="bca28-210">You must close the RuleSet tool to recompile the usage sample, because the tool loads the usage sample assembly.</span></span>
