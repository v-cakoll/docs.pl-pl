---
title: Ograniczenia dotyczące parametrów typu — Przewodnik programowania języka C#
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 0035f7d8aa862b4bd1b09a6f122a89786a6e295b
ms.sourcegitcommit: 465547886a1224a5435c3ac349c805e39ce77706
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/21/2020
ms.locfileid: "81738250"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="2ee7f-102">Ograniczenia dotyczące parametrów typu (Przewodnik programowania języka C#)</span><span class="sxs-lookup"><span data-stu-id="2ee7f-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="2ee7f-103">Ograniczenia informują kompilator o możliwościach, które musi mieć argument typu.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="2ee7f-104">Bez żadnych ograniczeń argument typu może być dowolnego typu.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="2ee7f-105">Kompilator może przyjąć tylko <xref:System.Object?displayProperty=nameWithType>członków , który jest ostateczną klasą podstawową dla dowolnego typu .NET.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="2ee7f-106">Aby uzyskać więcej informacji, zobacz [Dlaczego należy używać ograniczeń](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="2ee7f-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="2ee7f-107">Jeśli kod klienta używa typu, który nie spełnia ograniczenia, kompilator generuje błąd.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-107">If client code uses a type that doesn't satisfy a constraint, the compiler issues an error.</span></span> <span data-ttu-id="2ee7f-108">Ograniczenia są określone przy `where` użyciu kontekstowego słowa kluczowego.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="2ee7f-109">W poniższej tabeli wymieniono siedem typów ograniczeń:</span><span class="sxs-lookup"><span data-stu-id="2ee7f-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="2ee7f-110">Ograniczenie</span><span class="sxs-lookup"><span data-stu-id="2ee7f-110">Constraint</span></span>|<span data-ttu-id="2ee7f-111">Opis</span><span class="sxs-lookup"><span data-stu-id="2ee7f-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="2ee7f-112">Argument typu musi być typem wartości nienastępulnej.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-112">The type argument must be a non-nullable value type.</span></span> <span data-ttu-id="2ee7f-113">Aby uzyskać informacje o typach wartości, które można do wartości null, zobacz [Typy wartości możliwe do wartości null](../../language-reference/builtin-types/nullable-value-types.md).</span><span class="sxs-lookup"><span data-stu-id="2ee7f-113">For information about nullable value types, see [Nullable value types](../../language-reference/builtin-types/nullable-value-types.md).</span></span> <span data-ttu-id="2ee7f-114">Ponieważ wszystkie typy wartości mają dostępne `struct` bez parametrów `new()` konstruktora, ograniczenie `new()` implikuje ograniczenie i nie można połączyć z ograniczeniem.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-114">Because all value types have an accessible parameterless constructor, the `struct` constraint implies the `new()` constraint and can't be combined with the `new()` constraint.</span></span> <span data-ttu-id="2ee7f-115">Nie można połączyć `struct` ograniczenia `unmanaged` z ograniczeniem.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-115">You can't combine the `struct` constraint with the `unmanaged` constraint.</span></span>|
|`where T : class`|<span data-ttu-id="2ee7f-116">Argument typu musi być typem odwołania.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-116">The type argument must be a reference type.</span></span> <span data-ttu-id="2ee7f-117">To ograniczenie dotyczy również dowolnej klasy, interfejsu, delegata lub typu tablicy.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-117">This constraint applies also to any class, interface, delegate, or array type.</span></span> <span data-ttu-id="2ee7f-118">W kontekście nullable w języku C# 8.0 lub nowszym, `T` musi być typem odwołania niepodjętego do wartości null.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-118">In a nullable context in C# 8.0 or later, `T` must be a non-nullable reference type.</span></span> |
|`where T : class?`|<span data-ttu-id="2ee7f-119">Argument typu musi być typem odwołania, można anulować lub nie można anulować.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-119">The type argument must be a reference type, either nullable or non-nullable.</span></span> <span data-ttu-id="2ee7f-120">To ograniczenie dotyczy również dowolnej klasy, interfejsu, delegata lub typu tablicy.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-120">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="2ee7f-121">Argument typu musi być typem nienastępalnym.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-121">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="2ee7f-122">Argument może być typu odwołania niepodwymiernego w języku C# 8.0 lub nowszym lub typu wartości nienastępu.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-122">The argument can be a non-nullable reference type in C# 8.0 or later, or a non-nullable value type.</span></span> |
|`where T : unmanaged`|<span data-ttu-id="2ee7f-123">Argument typu musi być [typem niezarządzanym](../../language-reference/builtin-types/unmanaged-types.md)bez wartości null.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-123">The type argument must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="2ee7f-124">Ograniczenie `unmanaged` implikuje `struct` ograniczenie i nie można ich `struct` łączyć z ograniczeniami lub ograniczeniami. `new()`</span><span class="sxs-lookup"><span data-stu-id="2ee7f-124">The `unmanaged` constraint implies the `struct` constraint and can't be combined with either the `struct` or `new()` constraints.</span></span>|
|`where T : new()`|<span data-ttu-id="2ee7f-125">Argument typu musi mieć publiczny konstruktor bez parametrów.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-125">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="2ee7f-126">W połączeniu z innymi `new()` ograniczeniami, ograniczenie musi być określony jako ostatni.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-126">When used together with other constraints, the `new()` constraint must be specified last.</span></span> <span data-ttu-id="2ee7f-127">Ograniczenia `new()` nie można łączyć `struct` z `unmanaged` ograniczeniami i ograniczeniami.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-127">The `new()` constraint can't be combined with the `struct` and `unmanaged` constraints.</span></span>|
|<span data-ttu-id="2ee7f-128">`where T :`*nazwa klasy podstawowej>\<*</span><span class="sxs-lookup"><span data-stu-id="2ee7f-128">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="2ee7f-129">Argument typu musi być lub pochodzić z określonej klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-129">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="2ee7f-130">W kontekście nullable w języku C# 8.0 i nowszych, `T` musi być typu odwołania nienastępne null pochodną określonej klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-130">In a nullable context in C# 8.0 and later, `T` must be a non-nullable reference type derived from the specified base class.</span></span> |
|<span data-ttu-id="2ee7f-131">`where T :`*nazwa klasy podstawowej>? \<*</span><span class="sxs-lookup"><span data-stu-id="2ee7f-131">`where T :` *\<base class name>?*</span></span>|<span data-ttu-id="2ee7f-132">Argument typu musi być lub pochodzić z określonej klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-132">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="2ee7f-133">W kontekście nullable w języku C# 8.0 i nowszych, `T` może być typu nullable lub niedopuszczania do null pochodzi od określonej klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-133">In a nullable context in C# 8.0 and later, `T` may be either a nullable or non-nullable type derived from the specified base class.</span></span> |
|<span data-ttu-id="2ee7f-134">`where T :`>nazwa interfejsu \* \<\*</span><span class="sxs-lookup"><span data-stu-id="2ee7f-134">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="2ee7f-135">Argument typu musi być lub implementować określony interfejs.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-135">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="2ee7f-136">Można określić wiele ograniczeń interfejsu.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-136">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="2ee7f-137">Interfejs ograniczający może być również ogólny.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-137">The constraining interface can also be generic.</span></span> <span data-ttu-id="2ee7f-138">W kontekście nullable w języku C# 8.0 i nowszych, musi być typu niedopuszczania do wartości null, `T` który implementuje określony interfejs.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-138">In a nullable context in C# 8.0 and later, `T` must be a non-nullable type that implements the specified interface.</span></span>|
|<span data-ttu-id="2ee7f-139">`where T :`nazwa interfejsu>? \* \<\*</span><span class="sxs-lookup"><span data-stu-id="2ee7f-139">`where T :` *\<interface name>?*</span></span>|<span data-ttu-id="2ee7f-140">Argument typu musi być lub implementować określony interfejs.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-140">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="2ee7f-141">Można określić wiele ograniczeń interfejsu.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-141">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="2ee7f-142">Interfejs ograniczający może być również ogólny.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-142">The constraining interface can also be generic.</span></span> <span data-ttu-id="2ee7f-143">W kontekście nullable w języku C# `T` 8.0, może być typu odwołania nullable, typu odwołania niepodjętego do null lub typu wartości.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-143">In a nullable context in C# 8.0, `T` may be a nullable reference type, a non-nullable reference type, or a value type.</span></span> <span data-ttu-id="2ee7f-144">`T`może nie być typem wartości możliwej do wartości null.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-144">`T` may not be a nullable value type.</span></span>|
|`where T : U`|<span data-ttu-id="2ee7f-145">Podany argument `T` typu musi być lub pochodzić `U`z argumentu dostarczonego dla .</span><span class="sxs-lookup"><span data-stu-id="2ee7f-145">The type argument supplied for `T` must be or derive from the argument supplied for `U`.</span></span> <span data-ttu-id="2ee7f-146">W kontekście nullable, jeśli `U` jest typem `T` odwołania niepodjętego do wartości null, musi być typem odwołania niepodjętego do null.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-146">In a nullable context, if `U` is a non-nullable reference type, `T` must be non-nullable reference type.</span></span> <span data-ttu-id="2ee7f-147">Jeśli `U` jest typem odwołania `T` do nullable, może być nullable lub nie nullable.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-147">If `U` is a nullable reference type, `T` may be either nullable or non-nullable.</span></span> |

## <a name="why-use-constraints"></a><span data-ttu-id="2ee7f-148">Dlaczego warto korzystać z ograniczeń</span><span class="sxs-lookup"><span data-stu-id="2ee7f-148">Why use constraints</span></span>

<span data-ttu-id="2ee7f-149">Ograniczenia określają możliwości i oczekiwania parametru typu.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-149">Constraints specify the capabilities and expectations of a type parameter.</span></span> <span data-ttu-id="2ee7f-150">Deklarowanie tych ograniczeń oznacza, że można użyć operacji i wywołań metody typu ograniczającego.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-150">Declaring those constraints means you can use the operations and method calls of the constraining type.</span></span> <span data-ttu-id="2ee7f-151">Jeśli klasa lub metoda rodzajowa używa dowolnej operacji na ogólne elementy członkowskie <xref:System.Object?displayProperty=nameWithType>poza proste przypisanie lub wywołanie żadnych metod nie jest obsługiwany przez , trzeba będzie zastosować ograniczenia do parametru typu.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-151">If your generic class or method uses any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="2ee7f-152">Na przykład ograniczenie klasy podstawowej informuje kompilator, że tylko obiekty tego typu lub pochodne tego typu będą używane jako argumenty typu.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-152">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="2ee7f-153">Gdy kompilator ma tę gwarancję, można zezwolić na metody tego typu, które mają być wywoływane w klasie rodzajowej.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-153">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="2ee7f-154">Poniższy przykład kodu pokazuje funkcje, które `GenericList<T>` można dodać do klasy (w [wprowadzenie do generics)](../../../standard/generics/index.md)stosując ograniczenie klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-154">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](../../../standard/generics/index.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="2ee7f-155">Ograniczenie umożliwia klasy ogólnej do `Employee.Name` korzystania z właściwości.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-155">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="2ee7f-156">Ograniczenie określa, że wszystkie `T` elementy typu są gwarantowane jako `Employee` obiekt lub `Employee`obiekt, który dziedziczy z .</span><span class="sxs-lookup"><span data-stu-id="2ee7f-156">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="2ee7f-157">Wiele ograniczeń można zastosować do tego samego parametru typu, a same ograniczenia mogą być typami ogólnymi w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="2ee7f-157">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="2ee7f-158">Podczas stosowania `where T : class` ograniczenia, należy `==` `!=` unikać i operatorów na parametr typu, ponieważ te operatory będą testować tylko tożsamości odwołania, a nie dla równości wartości.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-158">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="2ee7f-159">To zachowanie występuje, nawet jeśli te operatory są przeciążone w typie, który jest używany jako argument.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-159">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="2ee7f-160">Poniższy kod ilustruje ten punkt; dane wyjściowe jest <xref:System.String> false, mimo `==` że klasa przeciąża operatora.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-160">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="2ee7f-161">Kompilator wie `T` tylko, że jest typem odwołania w czasie kompilacji i musi używać domyślnych operatorów, które są prawidłowe dla wszystkich typów odwołań.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-161">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="2ee7f-162">Jeśli należy przetestować pod kątem równości wartości, `where T : IEquatable<T>` zalecanym sposobem jest również zastosowanie lub `where T : IComparable<T>` ograniczenie i zaimplementowanie interfejsu w dowolnej klasie, która będzie używana do konstruowania klasy ogólnej.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-162">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="2ee7f-163">Ograniczanie wielu parametrów</span><span class="sxs-lookup"><span data-stu-id="2ee7f-163">Constraining multiple parameters</span></span>

<span data-ttu-id="2ee7f-164">Ograniczenia można zastosować do wielu parametrów i wielu ograniczeń do pojedynczego parametru, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="2ee7f-164">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="2ee7f-165">Parametry typu bez ograniczeń</span><span class="sxs-lookup"><span data-stu-id="2ee7f-165">Unbounded type parameters</span></span>

 <span data-ttu-id="2ee7f-166">Parametry typu, które nie mają żadnych ograniczeń, takie jak T w klasie `SampleClass<T>{}`publicznej, są nazywane parametrami typu niezwiązanych.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-166">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="2ee7f-167">Parametry typu niezwiązany mają następujące reguły:</span><span class="sxs-lookup"><span data-stu-id="2ee7f-167">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="2ee7f-168">I `!=` `==` operatorów nie można używać, ponieważ nie ma gwarancji, że argument typu betonu będzie obsługiwać tych operatorów.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-168">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="2ee7f-169">Mogą one być konwertowane do i z `System.Object` lub jawnie konwertowane na dowolny typ interfejsu.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-169">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="2ee7f-170">Można je porównać z [wartością null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="2ee7f-170">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="2ee7f-171">Jeśli parametr niezwiązany `null`jest porównywany do , porównanie zawsze zwróci false, jeśli argument typu jest typem wartości.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-171">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="2ee7f-172">Parametry typu jako wiązania</span><span class="sxs-lookup"><span data-stu-id="2ee7f-172">Type parameters as constraints</span></span>

<span data-ttu-id="2ee7f-173">Użycie parametru typu ogólnego jako ograniczenia jest przydatne, gdy funkcja elementu członkowskiego z własnym parametrem typu musi ograniczyć ten parametr do parametru typu typu zawierającego, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="2ee7f-173">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="2ee7f-174">W poprzednim przykładzie `T` jest ograniczenie typu w `Add` kontekście metody i parametr typu nieograniczonego `List` w kontekście klasy.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-174">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="2ee7f-175">Parametry typu mogą być również używane jako wiązania w definicjach klas ogólnych.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-175">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="2ee7f-176">Parametr typu musi być zadeklarowany w nawiasach kątowych wraz z innymi parametrami typu:</span><span class="sxs-lookup"><span data-stu-id="2ee7f-176">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="2ee7f-177">Przydatność parametrów typu jako ograniczenia z klasami ogólnymi jest ograniczona, ponieważ kompilator `System.Object`nie może zakładać nic o parametrze typu, z wyjątkiem tego, że pochodzi on od .</span><span class="sxs-lookup"><span data-stu-id="2ee7f-177">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="2ee7f-178">Użyj parametrów typu jako ograniczeń klas ogólnych w scenariuszach, w których chcesz wymusić relację dziedziczenia między dwoma parametrami typu.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-178">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="2ee7f-179">Ograniczenie NotNull</span><span class="sxs-lookup"><span data-stu-id="2ee7f-179">NotNull constraint</span></span>

<span data-ttu-id="2ee7f-180">Począwszy od języka C# 8.0 w kontekście nullable, można użyć `notnull` ograniczenia, aby określić, że argument typu musi być typu wartości nienastępujących do wartości null lub typu odwołania niepodjęwalnego.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-180">Beginning with C# 8.0 in a nullable context, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="2ee7f-181">Ograniczenie `notnull` może być używane `nullable enable` tylko w kontekście.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-181">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="2ee7f-182">Kompilator generuje ostrzeżenie, jeśli `notnull` dodasz ograniczenie w kontekście nullable nieświadomy.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-182">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span>

<span data-ttu-id="2ee7f-183">W przeciwieństwie do innych ograniczeń, gdy `notnull` argument typu narusza ograniczenie, kompilator generuje ostrzeżenie, gdy ten kod jest kompilowany w `nullable enable` kontekście.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-183">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="2ee7f-184">Jeśli kod jest kompilowany w kontekście nullable nieświadomy, kompilator nie generuje żadnych ostrzeżeń lub błędów.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-184">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

<span data-ttu-id="2ee7f-185">Począwszy od języka C# 8.0 `class` w kontekście nullable, ograniczenie określa, że argument typu musi być typu odwołania nie można anulować.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-185">Beginning with C# 8.0 in a nullable context, the `class` constraint specifies that the type argument must be a non-nullable reference type.</span></span> <span data-ttu-id="2ee7f-186">W kontekście nullable, gdy parametr typu jest typem odwołania do wartości null, kompilator generuje ostrzeżenie.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-186">In a nullable context, when a type parameter is a nullable reference type, the compiler generates a warning.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="2ee7f-187">Ograniczenie niezarządzane</span><span class="sxs-lookup"><span data-stu-id="2ee7f-187">Unmanaged constraint</span></span>

<span data-ttu-id="2ee7f-188">Począwszy od języka C# 7.3, można użyć `unmanaged` ograniczenia, aby określić, że parametr typu musi być [typem niezarządzanym bez](../../language-reference/builtin-types/unmanaged-types.md)wartości null.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-188">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="2ee7f-189">Ograniczenie `unmanaged` umożliwia pisanie procedur wielokrotnegoużytnia do pracy z typami, które mogą być manipulowane jako bloki pamięci, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="2ee7f-189">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="2ee7f-190">Poprzednia metoda musi być skompilowana w `unsafe` `sizeof` kontekście, ponieważ używa operatora na typ nie wiadomo, że jest typem wbudowanym.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-190">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="2ee7f-191">Bez `unmanaged` ograniczenia `sizeof` operator jest niedostępny.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-191">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

<span data-ttu-id="2ee7f-192">Ograniczenie `unmanaged` implikuje `struct` ograniczenie i nie można go połączyć.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-192">The `unmanaged` constraint implies the `struct` constraint and can't be combined with it.</span></span> <span data-ttu-id="2ee7f-193">Ponieważ `struct` ograniczenie implikuje `new()` ograniczenie, `unmanaged` ograniczenia nie można `new()` również łączyć z ograniczeniem.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-193">Because the `struct` constraint implies the `new()` constraint, the `unmanaged` constraint can't be combined with the `new()` constraint as well.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="2ee7f-194">Delegować ograniczenia</span><span class="sxs-lookup"><span data-stu-id="2ee7f-194">Delegate constraints</span></span>

<span data-ttu-id="2ee7f-195">Również począwszy od C# 7.3, można użyć <xref:System.Delegate?displayProperty=nameWithType> lub <xref:System.MulticastDelegate?displayProperty=nameWithType> jako ograniczenie klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-195">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="2ee7f-196">Clr zawsze dozwolone tego ograniczenia, ale język C# nie zezwala na to.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-196">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="2ee7f-197">Ograniczenie `System.Delegate` umożliwia pisanie kodu, który współpracuje z delegatów w sposób bezpieczny dla typu.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-197">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="2ee7f-198">Poniższy kod definiuje metodę rozszerzenia, która łączy dwóch delegatów, pod warunkiem, że są tego samego typu:</span><span class="sxs-lookup"><span data-stu-id="2ee7f-198">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="2ee7f-199">Powyższa metoda służy do łączenia delegatów, które są tego samego typu:</span><span class="sxs-lookup"><span data-stu-id="2ee7f-199">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="2ee7f-200">Jeśli uncomment ostatni wiersz, nie będzie kompilować.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-200">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="2ee7f-201">Oba `first` `test` i są typy delegata, ale są one różne typy delegatów.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-201">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="2ee7f-202">Ograniczenia wyliczenia</span><span class="sxs-lookup"><span data-stu-id="2ee7f-202">Enum constraints</span></span>

<span data-ttu-id="2ee7f-203">Począwszy od C# 7.3, <xref:System.Enum?displayProperty=nameWithType> można również określić typ jako ograniczenie klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-203">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="2ee7f-204">Clr zawsze dozwolone tego ograniczenia, ale język C# nie zezwala na to.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-204">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="2ee7f-205">Ogólne przy `System.Enum` użyciu zapewnienia bezpiecznego dla typu programowania do `System.Enum`pamięci podręcznej wyniki przy użyciu metod statycznych w .</span><span class="sxs-lookup"><span data-stu-id="2ee7f-205">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="2ee7f-206">Poniższy przykład znajduje wszystkie prawidłowe wartości dla typu wyliczenia, a następnie tworzy słownik, który mapuje te wartości do jego reprezentacji ciągu.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-206">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the enum constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="2ee7f-207">Metody stosowane do korzystania z odbicia, co ma wpływ na wydajność.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-207">The methods used to make use of reflection, which has performance implications.</span></span> <span data-ttu-id="2ee7f-208">Można wywołać tę metodę, aby utworzyć kolekcję, która jest buforowana i ponownie, a nie powtarzanie wywołań, które wymagają odbicia.</span><span class="sxs-lookup"><span data-stu-id="2ee7f-208">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="2ee7f-209">Można go użyć, jak pokazano w poniższym przykładzie, aby utworzyć wyliczenie i utworzyć słownik jego wartości i nazw:</span><span class="sxs-lookup"><span data-stu-id="2ee7f-209">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[enum definition](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the enum constrained method](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="2ee7f-210">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="2ee7f-210">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="2ee7f-211">C# Przewodnik programowania</span><span class="sxs-lookup"><span data-stu-id="2ee7f-211">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="2ee7f-212">Wprowadzenie do typów ogólnych</span><span class="sxs-lookup"><span data-stu-id="2ee7f-212">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="2ee7f-213">Klasy ogólne</span><span class="sxs-lookup"><span data-stu-id="2ee7f-213">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="2ee7f-214">nowe ograniczenie</span><span class="sxs-lookup"><span data-stu-id="2ee7f-214">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
