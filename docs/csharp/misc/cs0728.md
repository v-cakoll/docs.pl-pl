---
title: CS0728 ostrzeżenie (poziom 2) kompilator
ms.date: 07/20/2015
f1_keywords:
- CS0728
helpviewer_keywords:
- CS0728
ms.assetid: ad6d860d-bac4-48f3-9eab-1efd2b6de6c0
ms.openlocfilehash: 2cd0d1f35074c254d3d061cd58602d16db8281d5
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/23/2019
ms.locfileid: "61654993"
---
# <a name="compiler-warning-level-2-cs0728"></a><span data-ttu-id="351fd-102">CS0728 ostrzeżenie (poziom 2) kompilator</span><span class="sxs-lookup"><span data-stu-id="351fd-102">Compiler Warning (level 2) CS0728</span></span>
<span data-ttu-id="351fd-103">Prawdopodobnie niepoprawne przypisanie do elementu lokalnego "Zmienna", który jest argumentem instrukcji using lub lock.</span><span class="sxs-lookup"><span data-stu-id="351fd-103">Possibly incorrect assignment to local 'variable' which is the argument to a using or lock statement.</span></span>  <span data-ttu-id="351fd-104">Wywołanie metody Dispose lub odblokowanie nastąpi w oryginalnej wartości elementu lokalnego.</span><span class="sxs-lookup"><span data-stu-id="351fd-104">The Dispose call or unlocking will happen on the original value of the local.</span></span>  
  
 <span data-ttu-id="351fd-105">Istnieje kilka scenariuszy gdzie `using` lub `lock` bloków spowoduje przeciek tymczasowe zasobów.</span><span class="sxs-lookup"><span data-stu-id="351fd-105">There are several scenarios where `using` or `lock` blocks will result in a temporary leak of resources.</span></span> <span data-ttu-id="351fd-106">Oto przykład:</span><span class="sxs-lookup"><span data-stu-id="351fd-106">Here is one example:</span></span>  
  
 `thisType f = null;`  
  
 `using (f)`  
  
 `{`  
  
 `f = new thisType();`  
  
 `...`  
  
 `}`  
  
 <span data-ttu-id="351fd-107">W tym przypadku oryginalne wartości, takie jak wartości null zmiennej `thisType` zostaną usunięte podczas `using` bloku zakończy się wykonywanie, ale `thisType` obiekt utworzony wewnątrz bloku nie będzie, mimo że ostatecznie spowoduje to dojście wyrzucania elementów zbierane.</span><span class="sxs-lookup"><span data-stu-id="351fd-107">In this case, the original value, such as null, of the variable `thisType` will be disposed of when the `using` block finishes executing, but the `thisType` object created inside the block will not be, although it will eventually get garbage collected.</span></span>  
  
 <span data-ttu-id="351fd-108">Aby rozwiązać ten problem, należy użyć następującej postaci:</span><span class="sxs-lookup"><span data-stu-id="351fd-108">To resolve this error, use the following form:</span></span>  
  
 `using (thisType f = new thisType())`  
  
 `{`  
  
 `...`  
  
 `}`  
  
 <span data-ttu-id="351fd-109">W tym przypadku nowo przydzielonego `thisType` obiekt zostanie usunięty.</span><span class="sxs-lookup"><span data-stu-id="351fd-109">In this case, the newly allocated `thisType` object will be disposed of.</span></span>  
  
## <a name="example"></a><span data-ttu-id="351fd-110">Przykład</span><span class="sxs-lookup"><span data-stu-id="351fd-110">Example</span></span>  
 <span data-ttu-id="351fd-111">Poniższy kod, spowoduje wygenerowanie ostrzeżenia CS0728.</span><span class="sxs-lookup"><span data-stu-id="351fd-111">The following code will generate warning CS0728.</span></span>  
  
```csharp 
// CS0728.cs  
  
using System;  
public class ValidBase : IDisposable  
{  
    public void Dispose() {  }  
}  
  
public class Logger  
{  
    public static void dummy()  
    {  
        ValidBase vb = null;  
        using (vb)   
        {  
            vb = null;  // CS0728  
        }  
        vb = null;  
    }  
    public static void Main() { }  
}  
```
