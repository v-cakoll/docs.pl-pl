---
title: Ostrzeżenie kompilatora (poziom 1) CS1956
ms.date: 07/20/2015
f1_keywords:
- CS1956
helpviewer_keywords:
- CS1956
ms.assetid: 841f8b2b-692b-49ae-a587-a40797cf9797
ms.openlocfilehash: f848cff8aaedc86cbb2b6abb4a9b856bde85741c
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/14/2020
ms.locfileid: "61682576"
---
# <a name="compiler-warning-level-1-cs1956"></a><span data-ttu-id="82bee-102">Ostrzeżenie kompilatora (poziom 1) CS1956</span><span class="sxs-lookup"><span data-stu-id="82bee-102">Compiler Warning (level 1) CS1956</span></span>
<span data-ttu-id="82bee-103">Element członkowski "name" implementuje element członkowski interfejsu "nazwa" w typie "typ".</span><span class="sxs-lookup"><span data-stu-id="82bee-103">Member 'name' implements interface member 'name' in type 'type'.</span></span> <span data-ttu-id="82bee-104">Istnieje wiele dopasowań dla elementu członkowskiego interfejsu w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="82bee-104">There are multiple matches for the interface member at run-time.</span></span> <span data-ttu-id="82bee-105">Jest to implementacja zależna, która metoda zostanie wywołana.</span><span class="sxs-lookup"><span data-stu-id="82bee-105">It is implementation dependent which method will be called.</span></span>  
  
 <span data-ttu-id="82bee-106">To ostrzeżenie można wygenerować, gdy dwie metody interfejsu są zróżnicowane tylko `ref` przez `out`to, czy określony parametr jest oznaczony lub z .</span><span class="sxs-lookup"><span data-stu-id="82bee-106">This warning can be generated when two interface methods are differentiated only by whether a particular parameter is marked with `ref` or with `out`.</span></span> <span data-ttu-id="82bee-107">Najlepiej jest zmienić kod, aby uniknąć tego ostrzeżenia, ponieważ nie jest oczywiste lub gwarantowane, która metoda jest wywoływana w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="82bee-107">It is best to change your code to avoid this warning because it is not obvious or guaranteed which method is called at runtime.</span></span>  
  
 <span data-ttu-id="82bee-108">Mimo że C# `out` `ref`rozróżnia między i , CLR widzi je jako takie same.</span><span class="sxs-lookup"><span data-stu-id="82bee-108">Although C# distinguishes between `out` and `ref`, the CLR sees them as the same.</span></span> <span data-ttu-id="82bee-109">Przy podejmowaniu decyzji, która metoda implementuje interfejs, CLR po prostu wybiera jeden.</span><span class="sxs-lookup"><span data-stu-id="82bee-109">When deciding which method implements the interface, the CLR just picks one.</span></span>  
  
### <a name="to-avoid-this-warning"></a><span data-ttu-id="82bee-110">Aby uniknąć tego ostrzeżenia</span><span class="sxs-lookup"><span data-stu-id="82bee-110">To avoid this warning</span></span>  
  
1. <span data-ttu-id="82bee-111">Daj kompilatorowi jakiś sposób, aby odróżnić metody.</span><span class="sxs-lookup"><span data-stu-id="82bee-111">Give the compiler some way to differentiate the methods.</span></span> <span data-ttu-id="82bee-112">Na przykład można nadać im różne nazwy lub podać dodatkowy parametr na jednym z nich.</span><span class="sxs-lookup"><span data-stu-id="82bee-112">For example, you can give them different names or provide an additional parameter on one of them.</span></span>  
  
## <a name="example"></a><span data-ttu-id="82bee-113">Przykład</span><span class="sxs-lookup"><span data-stu-id="82bee-113">Example</span></span>  
 <span data-ttu-id="82bee-114">Poniższy kod generuje CS1956, `Test` ponieważ dwie `Base` metody różnią `ref` / `out` się tylko modyfikatorem na pierwszy parametr.</span><span class="sxs-lookup"><span data-stu-id="82bee-114">The following code generates CS1956 because the two `Test` methods in `Base` differ only by the `ref`/`out` modifier on the first parameter.</span></span>  
  
```csharp  
// cs1956.cs  
class Base<T, S>  
{  
    // This is the method that should be called.  
    public virtual int Test(out T x) // CS1956  
    {  
        x = default(T);  
        return 0;  
    }  
  
    // This is the "last" method and is the one that ends up being called  
    public virtual int Test(ref S x)  
    {  
        return 1;  
    }  
}  
  
interface IFace  
{  
    int Test(out int x);  
}  
  
class Derived : Base<int, int>, IFace  
{  
    static int Main()  
    {  
        IFace x = new Derived();  
        int y;  
        return x.Test(out y);  
    }  
}  
```
