---
title: "\"#ElseIf\", \"#Else\" lub \"#End If\" muszą być poprzedzone odpowiadającą jej instrukcją \"#If\""
ms.date: 07/20/2015
f1_keywords:
- vbc30013
- bc30013
helpviewer_keywords:
- BC30013
ms.assetid: 8fe2d23c-8b8f-46d8-90f2-7f8857ea43bb
ms.openlocfilehash: d9356860aae0355d9141f1c36464b84c982046da
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/04/2020
ms.locfileid: "84366843"
---
# <a name="elseif-else-or-end-if-must-be-preceded-by-a-matching-if"></a><span data-ttu-id="9d238-102">"#ElseIf", "#Else" lub "#End If" muszą być poprzedzone odpowiadającą jej instrukcją "#If"</span><span class="sxs-lookup"><span data-stu-id="9d238-102">'#ElseIf', '#Else', or '#End If' must be preceded by a matching '#If'</span></span>
<span data-ttu-id="9d238-103">`#ElseIf`, `#Else` , i `#End If` są dyrektywami kompilacji warunkowej.</span><span class="sxs-lookup"><span data-stu-id="9d238-103">`#ElseIf`, `#Else`, and `#End If` are conditional compilation directives.</span></span> <span data-ttu-id="9d238-104">`#ElseIf`, `#Else` , Lub `#End If` nie jest poprzedzony przez odpowiednią `#If` dyrektywę.</span><span class="sxs-lookup"><span data-stu-id="9d238-104">The `#ElseIf`, `#Else`, or `#End If` is not preceded by a corresponding `#If` directive.</span></span>  
  
 <span data-ttu-id="9d238-105">**Identyfikator błędu:** BC30013</span><span class="sxs-lookup"><span data-stu-id="9d238-105">**Error ID:** BC30013</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="9d238-106">Aby poprawić ten błąd</span><span class="sxs-lookup"><span data-stu-id="9d238-106">To correct this error</span></span>  
  
1. <span data-ttu-id="9d238-107">Upewnij się, że zamierzona `#If` nie jest oddzielona od klauzuli, w której występuje działający blok kompilacji warunkowej lub nieprawidłowo umieszczony `#End If` .</span><span class="sxs-lookup"><span data-stu-id="9d238-107">Check that the intended `#If` is not separated from the clause in question by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="9d238-108">`#Else`W każdym bloku dozwolony jest tylko jeden `#If` , więc dwie kolejne `#Else` dyrektywy powodują wystąpienie tego błędu.</span><span class="sxs-lookup"><span data-stu-id="9d238-108">Only one `#Else` is permitted in each `#If` block, so two successive `#Else` directives cause this error.</span></span>  
  
2. <span data-ttu-id="9d238-109">Sprawdź, czy w `#` poprzedniej dyrektywie nie brakuje wiodącego elementu `#If` .</span><span class="sxs-lookup"><span data-stu-id="9d238-109">Check that the leading `#` is not missing from an earlier `#If` directive.</span></span>  
  
3. <span data-ttu-id="9d238-110">Jeśli wszystko inne jest w porządku, Dodaj `#If` dyrektywę na początku bloku kompilacji warunkowej.</span><span class="sxs-lookup"><span data-stu-id="9d238-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="9d238-111">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="9d238-111">See also</span></span>

- [<span data-ttu-id="9d238-112">#If... Then... #Else — dyrektywy</span><span class="sxs-lookup"><span data-stu-id="9d238-112">#If...Then...#Else Directives</span></span>](../language-reference/directives/if-then-else-directives.md)
