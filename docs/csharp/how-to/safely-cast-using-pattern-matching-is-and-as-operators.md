---
title: Jak bezpiecznie rzutować przy użyciu dopasowania wzorca i operatorów is i AS
description: Dowiedz się, jak używać technik dopasowywania wzorców, aby bezpiecznie rzutować zmienne na inny typ. Możesz użyć dopasowania wzorca oraz operatorów is i AS, aby bezpiecznie skonwertować typy.
ms.date: 09/05/2018
helpviewer_keywords:
- cast operators [C#], as and is operators
- as operator [C#]
- is operator [C#]
ms.openlocfilehash: f10ce837057cc61b84130f237a13af708849dfc5
ms.sourcegitcommit: 7137e12f54c4e83a94ae43ec320f8cf59c1772ea
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/10/2020
ms.locfileid: "84662969"
---
# <a name="how-to-safely-cast-by-using-pattern-matching-and-the-is-and-as-operators"></a><span data-ttu-id="07ceb-104">Jak bezpiecznie rzutować przy użyciu dopasowania wzorca i operatorów is i AS</span><span class="sxs-lookup"><span data-stu-id="07ceb-104">How to safely cast by using pattern matching and the is and as operators</span></span>

<span data-ttu-id="07ceb-105">Ponieważ obiekty są polimorficzne, istnieje możliwość, aby zmienna typu klasy bazowej była przechowywana [typu](../programming-guide/types/index.md)pochodnego.</span><span class="sxs-lookup"><span data-stu-id="07ceb-105">Because objects are polymorphic, it's possible for a variable of a base class type to hold a derived [type](../programming-guide/types/index.md).</span></span> <span data-ttu-id="07ceb-106">Aby uzyskać dostęp do elementów członkowskich wystąpienia typu pochodnego, konieczne jest [rzutowanie](../programming-guide/types/casting-and-type-conversions.md) wartości z powrotem na typ pochodny.</span><span class="sxs-lookup"><span data-stu-id="07ceb-106">To access the derived type's instance members, it's necessary to [cast](../programming-guide/types/casting-and-type-conversions.md) the value back to the derived type.</span></span> <span data-ttu-id="07ceb-107">Jednak rzutowanie tworzy ryzyko wyrzucania <xref:System.InvalidCastException> .</span><span class="sxs-lookup"><span data-stu-id="07ceb-107">However, a cast creates the risk of throwing an <xref:System.InvalidCastException>.</span></span> <span data-ttu-id="07ceb-108">Język C# zawiera instrukcje [dopasowania wzorca](../pattern-matching.md) , które wykonują rzutowanie warunkowo tylko wtedy, gdy powiedzie się.</span><span class="sxs-lookup"><span data-stu-id="07ceb-108">C# provides [pattern matching](../pattern-matching.md) statements that perform a cast conditionally only when it will succeed.</span></span> <span data-ttu-id="07ceb-109">Język C# zawiera również operatory [is](../language-reference/operators/type-testing-and-cast.md#is-operator) i [as](../language-reference/operators/type-testing-and-cast.md#as-operator) do sprawdzenia, czy wartość jest określonego typu.</span><span class="sxs-lookup"><span data-stu-id="07ceb-109">C# also provides the [is](../language-reference/operators/type-testing-and-cast.md#is-operator) and [as](../language-reference/operators/type-testing-and-cast.md#as-operator) operators to test if a value is of a certain type.</span></span>

<span data-ttu-id="07ceb-110">Poniższy przykład pokazuje, jak używać instrukcji dopasowania do wzorca `is` :</span><span class="sxs-lookup"><span data-stu-id="07ceb-110">The following example shows how to use the pattern matching `is` statement:</span></span>

:::code language="csharp" source="../../../samples/snippets/csharp/how-to/safelycast/patternmatching/Program.cs" id="PatternMatchingIs":::

<span data-ttu-id="07ceb-111">Powyższy przykład ilustruje kilka cech składni dopasowania do wzorca.</span><span class="sxs-lookup"><span data-stu-id="07ceb-111">The preceding sample demonstrates a few features of pattern matching syntax.</span></span> <span data-ttu-id="07ceb-112">`if (a is Mammal m)`Instrukcja łączy test z przypisaniem inicjalizacji.</span><span class="sxs-lookup"><span data-stu-id="07ceb-112">The `if (a is Mammal m)` statement combines the test with an initialization assignment.</span></span> <span data-ttu-id="07ceb-113">Przypisanie występuje tylko wtedy, gdy test zakończy się pomyślnie.</span><span class="sxs-lookup"><span data-stu-id="07ceb-113">The assignment occurs only when the test succeeds.</span></span> <span data-ttu-id="07ceb-114">Zmienna `m` jest tylko w zakresie w osadzonej instrukcji, w `if` której został przypisany.</span><span class="sxs-lookup"><span data-stu-id="07ceb-114">The variable `m` is only in scope in the embedded `if` statement where it has been assigned.</span></span> <span data-ttu-id="07ceb-115">Nie można uzyskać dostępu `m` dalej w tej samej metodzie.</span><span class="sxs-lookup"><span data-stu-id="07ceb-115">You cannot access `m` later in the same method.</span></span> <span data-ttu-id="07ceb-116">W poprzednim przykładzie pokazano również, jak użyć [ `as` operatora](../language-reference/operators/type-testing-and-cast.md#as-operator) , aby przekonwertować obiekt na określony typ.</span><span class="sxs-lookup"><span data-stu-id="07ceb-116">The preceding example also shows how to use the [`as` operator](../language-reference/operators/type-testing-and-cast.md#as-operator) to convert an object to a specified type.</span></span>

<span data-ttu-id="07ceb-117">Można także użyć tej samej składni do testowania, jeśli [Typ wartości null](../language-reference/builtin-types/nullable-value-types.md) ma wartość, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="07ceb-117">You can also use the same syntax for testing if a [nullable value type](../language-reference/builtin-types/nullable-value-types.md) has a value, as shown in the following example:</span></span>

:::code language="csharp" source="../../../samples/snippets/csharp/how-to/safelycast/nullablepatternmatching/Program.cs" id="PatternMatchingNullable":::

<span data-ttu-id="07ceb-118">Powyższy przykład ilustruje inne funkcje dopasowania do wzorca do użycia z konwersjemi.</span><span class="sxs-lookup"><span data-stu-id="07ceb-118">The preceding sample demonstrates other features of pattern matching to use with conversions.</span></span> <span data-ttu-id="07ceb-119">Można testować zmienną dla wzorca o wartości null, sprawdzając odpowiednie `null` wartości.</span><span class="sxs-lookup"><span data-stu-id="07ceb-119">You can test a variable for the null pattern by checking specifically for the `null` value.</span></span> <span data-ttu-id="07ceb-120">Gdy wartość środowiska uruchomieniowego zmiennej to `null` , `is` Sprawdzanie instrukcji dla typu zawsze zwraca wartość `false` .</span><span class="sxs-lookup"><span data-stu-id="07ceb-120">When the runtime value of the variable is `null`, an `is` statement checking for a type always returns `false`.</span></span> <span data-ttu-id="07ceb-121">Instrukcja dopasowania wzorca `is` nie zezwala na typ wartości null, np `int?` `Nullable<int>` . lub, ale można testować dla dowolnego innego typu wartości.</span><span class="sxs-lookup"><span data-stu-id="07ceb-121">The pattern matching `is` statement doesn't allow a nullable value type, such as `int?` or `Nullable<int>`, but you can test for any other value type.</span></span> <span data-ttu-id="07ceb-122">`is`Wzorce z poprzedniego przykładu nie są ograniczone do typów wartości dopuszczających wartość null.</span><span class="sxs-lookup"><span data-stu-id="07ceb-122">The `is` patterns from the preceding example are not limited to the nullable value types.</span></span> <span data-ttu-id="07ceb-123">Można również użyć tych wzorców do sprawdzenia, czy zmienna typu referencyjnego ma wartość lub `null` .</span><span class="sxs-lookup"><span data-stu-id="07ceb-123">You can also use those patterns to test if a variable of a reference type has a value or it's `null`.</span></span>

<span data-ttu-id="07ceb-124">Powyższy przykład pokazuje również, jak używać wzorca typu w instrukcji, `switch` gdzie zmienna może być jednym z wielu różnych typów.</span><span class="sxs-lookup"><span data-stu-id="07ceb-124">The preceding sample also shows how you use the type pattern in a `switch` statement where the variable may be one of many different types.</span></span>

<span data-ttu-id="07ceb-125">Jeśli chcesz sprawdzić, czy zmienna jest danym typem, ale nie jest przypisana do nowej zmiennej, można użyć `is` `as` operatorów i dla typów referencyjnych i wartości null.</span><span class="sxs-lookup"><span data-stu-id="07ceb-125">If you want to test if a variable is a given type, but not assign it to a new variable, you can use the `is` and `as` operators for reference types and nullable value types.</span></span> <span data-ttu-id="07ceb-126">Poniższy kod pokazuje, jak używać `is` `as` instrukcji i, które były częścią języka C# przed dopasowaniem do wzorca, aby sprawdzić, czy zmienna ma dany typ:</span><span class="sxs-lookup"><span data-stu-id="07ceb-126">The following code shows how to use the `is` and `as` statements that were part of the C# language before pattern matching was introduced to test if a variable is of a given type:</span></span>

:::code language="csharp" source="../../../samples/snippets/csharp/how-to/safelycast/asandis/Program.cs" id="IsAndAs":::

<span data-ttu-id="07ceb-127">Jak widać, porównując ten kod z kodem dopasowania wzorca, Składnia dopasowania wzorca zapewnia bardziej niezawodne funkcje, łącząc test i przypisanie w pojedynczej instrukcji.</span><span class="sxs-lookup"><span data-stu-id="07ceb-127">As you can see by comparing this code with the pattern matching code, the pattern matching syntax provides more robust features by combining the test and the assignment in a single statement.</span></span> <span data-ttu-id="07ceb-128">Jeśli to możliwe, użyj składni dopasowania wzorca.</span><span class="sxs-lookup"><span data-stu-id="07ceb-128">Use the pattern matching syntax whenever possible.</span></span>
