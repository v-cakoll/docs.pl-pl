---
title: 'Wskazówki: wywoływanie Windows API'
ms.date: 07/20/2015
helpviewer_keywords:
- DLLs, calling
- Windows API, walkthroughs
- platform invoke, walkthroughs
- API calls [Visual Basic], walkthroughs [Visual Basic]
- Windows API, calling
- walkthroughs [Visual Basic], API calls
- DllImport attribute, calling Windows API
- Declare statement [Visual Basic], declaring DLL functions
ms.assetid: 9280ca96-7a93-47a3-8d01-6d01be0657cb
ms.openlocfilehash: ec6b8ddc8769fadde52aaebd6ad3701183fac77a
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 11/22/2019
ms.locfileid: "74338667"
---
# <a name="walkthrough-calling-windows-apis-visual-basic"></a><span data-ttu-id="7790d-102">Wskazówki: wywoływanie Windows API (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="7790d-102">Walkthrough: Calling Windows APIs (Visual Basic)</span></span>
<span data-ttu-id="7790d-103">Interfejsy API systemu Windows to biblioteki dołączane dynamicznie (dll), które są częścią systemu operacyjnego Windows.</span><span class="sxs-lookup"><span data-stu-id="7790d-103">Windows APIs are dynamic-link libraries (DLLs) that are part of the Windows operating system.</span></span> <span data-ttu-id="7790d-104">Są one używane do wykonywania zadań, gdy trudno jest pisać równoważne procedury.</span><span class="sxs-lookup"><span data-stu-id="7790d-104">You use them to perform tasks when it is difficult to write equivalent procedures of your own.</span></span> <span data-ttu-id="7790d-105">Na przykład system Windows udostępnia funkcję o nazwie `FlashWindowEx`, która umożliwia zmianę paska tytułu aplikacji między jasnym i ciemnym odcieniami.</span><span class="sxs-lookup"><span data-stu-id="7790d-105">For example, Windows provides a function named `FlashWindowEx` that lets you make the title bar for an application alternate between light and dark shades.</span></span>  
  
 <span data-ttu-id="7790d-106">Zalety używania interfejsów API systemu Windows w kodzie polega na tym, że mogą one zaoszczędzić czas opracowywania, ponieważ zawierają dziesiątki użytecznych funkcji, które są już zapisane i oczekują na użycie.</span><span class="sxs-lookup"><span data-stu-id="7790d-106">The advantage of using Windows APIs in your code is that they can save development time because they contain dozens of useful functions that are already written and waiting to be used.</span></span> <span data-ttu-id="7790d-107">Wadą jest to, że interfejsy API systemu Windows mogą być trudne do pracy z i Unforgiving, gdy coś się nie stało.</span><span class="sxs-lookup"><span data-stu-id="7790d-107">The disadvantage is that Windows APIs can be difficult to work with and unforgiving when things go wrong.</span></span>  
  
 <span data-ttu-id="7790d-108">Interfejsy API systemu Windows reprezentują specjalną kategorię współdziałania.</span><span class="sxs-lookup"><span data-stu-id="7790d-108">Windows APIs represent a special category of interoperability.</span></span> <span data-ttu-id="7790d-109">Interfejsy API systemu Windows nie korzystają z kodu zarządzanego, nie mają wbudowanych bibliotek typów i używają typów danych, które są inne niż używane w programie Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="7790d-109">Windows APIs do not use managed code, do not have built-in type libraries, and use data types that are different than those used with Visual Studio.</span></span> <span data-ttu-id="7790d-110">Ze względu na te różnice, ponieważ interfejsy API systemu Windows nie są obiektami COM, współdziałanie z interfejsami API systemu Windows i .NET Framework odbywa się przy użyciu funkcji Invoke platformy lub PInvoke.</span><span class="sxs-lookup"><span data-stu-id="7790d-110">Because of these differences, and because Windows APIs are not COM objects, interoperability with Windows APIs and the .NET Framework is performed using platform invoke, or PInvoke.</span></span> <span data-ttu-id="7790d-111">Wywołanie platformy to usługa, która umożliwia kodowi zarządzanemu wywoływanie funkcji niezarządzanych wdrożonych w bibliotekach DLL.</span><span class="sxs-lookup"><span data-stu-id="7790d-111">Platform invoke is a service that enables managed code to call unmanaged functions implemented in DLLs.</span></span> <span data-ttu-id="7790d-112">Aby uzyskać więcej informacji, zobacz wykorzystywanie [niezarządzanych funkcji DLL](../../../framework/interop/consuming-unmanaged-dll-functions.md).</span><span class="sxs-lookup"><span data-stu-id="7790d-112">For more information, see [Consuming Unmanaged DLL Functions](../../../framework/interop/consuming-unmanaged-dll-functions.md).</span></span> <span data-ttu-id="7790d-113">Możesz użyć funkcji PInvoke w Visual Basic, używając instrukcji `Declare` lub stosując atrybut `DllImport` do pustej procedury.</span><span class="sxs-lookup"><span data-stu-id="7790d-113">You can use PInvoke in Visual Basic by using either the `Declare` statement or applying the `DllImport` attribute to an empty procedure.</span></span>  
  
 <span data-ttu-id="7790d-114">Wywołania interfejsu API systemu Windows były istotną częścią programowania Visual Basic w przeszłości, ale rzadko są konieczne w przypadku Visual Basic .NET.</span><span class="sxs-lookup"><span data-stu-id="7790d-114">Windows API calls were an important part of Visual Basic programming in the past, but are seldom necessary with Visual Basic .NET.</span></span> <span data-ttu-id="7790d-115">Jeśli to możliwe, należy używać kodu zarządzanego z .NET Framework do wykonywania zadań, zamiast wywołań interfejsu API systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="7790d-115">Whenever possible, you should use managed code from the .NET Framework to perform tasks, instead of Windows API calls.</span></span> <span data-ttu-id="7790d-116">Ten Instruktaż zawiera informacje dotyczące tych sytuacji, w których jest wymagane korzystanie z interfejsów API systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="7790d-116">This walkthrough provides information for those situations in which using Windows APIs is necessary.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="api-calls-using-declare"></a><span data-ttu-id="7790d-117">Wywołania interfejsu API za pomocą deklaracji DECLARE</span><span class="sxs-lookup"><span data-stu-id="7790d-117">API Calls Using Declare</span></span>  
 <span data-ttu-id="7790d-118">Najbardziej typowym sposobem wywołania interfejsów API systemu Windows jest użycie instrukcji `Declare`.</span><span class="sxs-lookup"><span data-stu-id="7790d-118">The most common way to call Windows APIs is by using the `Declare` statement.</span></span>  
  
### <a name="to-declare-a-dll-procedure"></a><span data-ttu-id="7790d-119">Aby zadeklarować procedurę DLL</span><span class="sxs-lookup"><span data-stu-id="7790d-119">To declare a DLL procedure</span></span>  
  
1. <span data-ttu-id="7790d-120">Określ nazwę funkcji, która ma zostać wywołana, oraz jej argumenty, typy argumentów i wartość zwracaną, a także nazwę i lokalizację biblioteki DLL, która ją zawiera.</span><span class="sxs-lookup"><span data-stu-id="7790d-120">Determine the name of the function you want to call, plus its arguments, argument types, and return value, as well as the name and location of the DLL that contains it.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="7790d-121">Aby uzyskać pełne informacje na temat interfejsów API systemu Windows, zobacz dokumentację zestawu Win32 SDK w interfejsie API systemu Windows zestawu SDK platformy.</span><span class="sxs-lookup"><span data-stu-id="7790d-121">For complete information about the Windows APIs, see the Win32 SDK documentation in the Platform SDK Windows API.</span></span> <span data-ttu-id="7790d-122">Aby uzyskać więcej informacji na temat stałych używanych przez interfejsy API systemu Windows, należy przejrzeć pliki nagłówkowe, takie jak Windows. h, dołączone do zestawu SDK platformy.</span><span class="sxs-lookup"><span data-stu-id="7790d-122">For more information about the constants that Windows APIs use, examine the header files such as Windows.h included with the Platform SDK.</span></span>  
  
2. <span data-ttu-id="7790d-123">Otwórz nowy projekt aplikacji systemu Windows, klikając pozycję **Nowy** w menu **plik** , a następnie klikając pozycję **projekt**.</span><span class="sxs-lookup"><span data-stu-id="7790d-123">Open a new Windows Application project by clicking **New** on the **File** menu, and then clicking **Project**.</span></span> <span data-ttu-id="7790d-124">Pojawi się okno dialogowe **Nowy projekt** .</span><span class="sxs-lookup"><span data-stu-id="7790d-124">The **New Project** dialog box appears.</span></span>  
  
3. <span data-ttu-id="7790d-125">Wybierz pozycję **aplikacja systemu Windows** z listy Visual Basic szablonów projektu.</span><span class="sxs-lookup"><span data-stu-id="7790d-125">Select **Windows Application** from the list of Visual Basic project templates.</span></span> <span data-ttu-id="7790d-126">Zostanie wyświetlony nowy projekt.</span><span class="sxs-lookup"><span data-stu-id="7790d-126">The new project is displayed.</span></span>  
  
4. <span data-ttu-id="7790d-127">Dodaj następującą funkcję `Declare` do klasy lub modułu, w którym ma być używana biblioteka DLL:</span><span class="sxs-lookup"><span data-stu-id="7790d-127">Add the following `Declare` function either to the class or module in which you want to use the DLL:</span></span>  
  
     [!code-vb[VbVbalrInterop#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#9)]  
  
### <a name="parts-of-the-declare-statement"></a><span data-ttu-id="7790d-128">Części instrukcji DECLARE</span><span class="sxs-lookup"><span data-stu-id="7790d-128">Parts of the Declare Statement</span></span>  
 <span data-ttu-id="7790d-129">Instrukcja `Declare` zawiera następujące elementy.</span><span class="sxs-lookup"><span data-stu-id="7790d-129">The `Declare` statement includes the following elements.</span></span>  
  
#### <a name="auto-modifier"></a><span data-ttu-id="7790d-130">Modyfikator Autokorekty</span><span class="sxs-lookup"><span data-stu-id="7790d-130">Auto modifier</span></span>  
 <span data-ttu-id="7790d-131">Modyfikator `Auto` instruuje środowisko uruchomieniowe, aby przekonwertował ciąg na podstawie nazwy metody zgodnie z regułami środowiska uruchomieniowego języka wspólnego (lub nazwą aliasu, jeśli jest określona).</span><span class="sxs-lookup"><span data-stu-id="7790d-131">The `Auto` modifier instructs the runtime to convert the string based on the method name according to common language runtime rules (or alias name if specified).</span></span>  
  
#### <a name="lib-and-alias-keywords"></a><span data-ttu-id="7790d-132">Lib i słowa kluczowe aliasu</span><span class="sxs-lookup"><span data-stu-id="7790d-132">Lib and Alias keywords</span></span>  
 <span data-ttu-id="7790d-133">Nazwa, po słowie kluczowym `Function`, to nazwa używana przez program w celu uzyskania dostępu do zaimportowanej funkcji.</span><span class="sxs-lookup"><span data-stu-id="7790d-133">The name following the `Function` keyword is the name your program uses to access the imported function.</span></span> <span data-ttu-id="7790d-134">Może być taka sama jak rzeczywista nazwa wywoływanej funkcji lub można użyć dowolnej prawidłowej nazwy procedury, a następnie zastosować słowo kluczowe `Alias`, aby określić rzeczywistą nazwę wywoływanej funkcji.</span><span class="sxs-lookup"><span data-stu-id="7790d-134">It can be the same as the real name of the function you are calling, or you can use any valid procedure name and then employ the `Alias` keyword to specify the real name of the function you are calling.</span></span>  
  
 <span data-ttu-id="7790d-135">Określ słowo kluczowe `Lib`, a następnie nazwę i lokalizację biblioteki DLL zawierającej wywoływaną funkcję.</span><span class="sxs-lookup"><span data-stu-id="7790d-135">Specify the `Lib` keyword, followed by the name and location of the DLL that contains the function you are calling.</span></span> <span data-ttu-id="7790d-136">Nie trzeba określać ścieżki dla plików znajdujących się w katalogach systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="7790d-136">You do not need to specify the path for files located in the Windows system directories.</span></span>  
  
 <span data-ttu-id="7790d-137">Użyj słowa kluczowego `Alias`, jeśli nazwa wywoływanej funkcji nie jest prawidłową nazwą procedury Visual Basic lub powoduje konflikt z nazwą innych elementów w aplikacji.</span><span class="sxs-lookup"><span data-stu-id="7790d-137">Use the `Alias` keyword if the name of the function you are calling is not a valid Visual Basic procedure name, or conflicts with the name of other items in your application.</span></span> <span data-ttu-id="7790d-138">`Alias` wskazuje rzeczywistą nazwę wywoływanej funkcji.</span><span class="sxs-lookup"><span data-stu-id="7790d-138">`Alias` indicates the true name of the function being called.</span></span>  
  
#### <a name="argument-and-data-type-declarations"></a><span data-ttu-id="7790d-139">Deklaracje typu argumentu i danych</span><span class="sxs-lookup"><span data-stu-id="7790d-139">Argument and Data Type Declarations</span></span>  
 <span data-ttu-id="7790d-140">Zadeklaruj argumenty i ich typy danych.</span><span class="sxs-lookup"><span data-stu-id="7790d-140">Declare the arguments and their data types.</span></span> <span data-ttu-id="7790d-141">Ta część może być trudne, ponieważ typy danych używane przez system Windows nie odpowiadają typom danych programu Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="7790d-141">This part can be challenging because the data types that Windows uses do not correspond to Visual Studio data types.</span></span> <span data-ttu-id="7790d-142">Visual Basic wykonuje dużą część pracy przez konwersję argumentów na zgodne typy danych, proces o nazwie *Marshaling*.</span><span class="sxs-lookup"><span data-stu-id="7790d-142">Visual Basic does a lot of the work for you by converting arguments to compatible data types, a process called *marshaling*.</span></span> <span data-ttu-id="7790d-143">Można jawnie kontrolować sposób, w jaki argumenty są organizowane przy użyciu atrybutu <xref:System.Runtime.InteropServices.MarshalAsAttribute> zdefiniowanego w przestrzeni nazw <xref:System.Runtime.InteropServices>.</span><span class="sxs-lookup"><span data-stu-id="7790d-143">You can explicitly control how arguments are marshaled by using the <xref:System.Runtime.InteropServices.MarshalAsAttribute> attribute defined in the <xref:System.Runtime.InteropServices> namespace.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="7790d-144">Poprzednie wersje Visual Basic umożliwiają deklarowanie parametrów `As Any`, co oznacza, że można użyć danych dowolnego typu danych.</span><span class="sxs-lookup"><span data-stu-id="7790d-144">Previous versions of Visual Basic allowed you to declare parameters `As Any`, meaning that data of any data type could be used.</span></span> <span data-ttu-id="7790d-145">Visual Basic wymaga użycia określonego typu danych dla wszystkich instrukcji `Declare`.</span><span class="sxs-lookup"><span data-stu-id="7790d-145">Visual Basic requires that you use a specific data type for all `Declare` statements.</span></span>  
  
#### <a name="windows-api-constants"></a><span data-ttu-id="7790d-146">Stałe interfejsu API systemu Windows</span><span class="sxs-lookup"><span data-stu-id="7790d-146">Windows API Constants</span></span>  
 <span data-ttu-id="7790d-147">Niektóre argumenty są kombinacją stałych.</span><span class="sxs-lookup"><span data-stu-id="7790d-147">Some arguments are combinations of constants.</span></span> <span data-ttu-id="7790d-148">Na przykład interfejs API `MessageBox` przedstawiony w tym instruktażu akceptuje argument liczby całkowitej o nazwie `Typ`, który kontroluje sposób wyświetlania okna komunikatu.</span><span class="sxs-lookup"><span data-stu-id="7790d-148">For example, the `MessageBox` API shown in this walkthrough accepts an integer argument called `Typ` that controls how the message box is displayed.</span></span> <span data-ttu-id="7790d-149">Możesz określić wartość liczbową tych stałych, sprawdzając instrukcje `#define` w pliku WinUser. h.</span><span class="sxs-lookup"><span data-stu-id="7790d-149">You can determine the numeric value of these constants by examining the `#define` statements in the file WinUser.h.</span></span> <span data-ttu-id="7790d-150">Wartości liczbowe są zwykle wyświetlane w formacie szesnastkowym, więc możesz chcieć użyć kalkulatora, aby dodać je i przekonwertować na wartość typu Decimal.</span><span class="sxs-lookup"><span data-stu-id="7790d-150">The numeric values are generally shown in hexadecimal, so you may want to use a calculator to add them and convert to decimal.</span></span> <span data-ttu-id="7790d-151">Na przykład, jeśli chcesz połączyć stałe dla stylu wykrzyknika `MB_ICONEXCLAMATION` 0x00000030 i styl yes/no `MB_YESNO` 0x00000004, można dodać liczby i uzyskać wynik 0x00000034 lub 52 dziesiętnych.</span><span class="sxs-lookup"><span data-stu-id="7790d-151">For example, if you want to combine the constants for the exclamation style `MB_ICONEXCLAMATION` 0x00000030 and the Yes/No style `MB_YESNO` 0x00000004, you can add the numbers and get a result of 0x00000034, or 52 decimal.</span></span> <span data-ttu-id="7790d-152">Mimo że można użyć bezpośredniego wyniku dziesiętnego, lepiej jest zadeklarować te wartości jako stałe w aplikacji i połączyć je za pomocą operatora `Or`.</span><span class="sxs-lookup"><span data-stu-id="7790d-152">Although you can use the decimal result directly, it is better to declare these values as constants in your application and combine them using the `Or` operator.</span></span>  
  
##### <a name="to-declare-constants-for-windows-api-calls"></a><span data-ttu-id="7790d-153">Aby zadeklarować stałe dla wywołań interfejsu API systemu Windows</span><span class="sxs-lookup"><span data-stu-id="7790d-153">To declare constants for Windows API calls</span></span>  
  
1. <span data-ttu-id="7790d-154">Zapoznaj się z dokumentacją dla wywoływanej funkcji systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="7790d-154">Consult the documentation for the Windows function you are calling.</span></span> <span data-ttu-id="7790d-155">Określ nazwę stałych, z których korzysta, i nazwę pliku h, który zawiera wartości liczbowe dla tych stałych.</span><span class="sxs-lookup"><span data-stu-id="7790d-155">Determine the name of the constants it uses and the name of the .h file that contains the numeric values for these constants.</span></span>  
  
2. <span data-ttu-id="7790d-156">Użyj edytora tekstu, takiego jak Notatnik, aby wyświetlić zawartość pliku nagłówkowego (. h) i znaleźć wartości skojarzone z używanymi przez Ciebie stałych.</span><span class="sxs-lookup"><span data-stu-id="7790d-156">Use a text editor, such as Notepad, to view the contents of the header (.h) file, and find the values associated with the constants you are using.</span></span> <span data-ttu-id="7790d-157">Na przykład interfejs API `MessageBox` używa stałej `MB_ICONQUESTION` do wyświetlania znaku zapytania w oknie komunikatu.</span><span class="sxs-lookup"><span data-stu-id="7790d-157">For example, the `MessageBox` API uses the constant `MB_ICONQUESTION` to show a question mark in the message box.</span></span> <span data-ttu-id="7790d-158">Definicja `MB_ICONQUESTION` jest w WinUser. h i wygląda następująco:</span><span class="sxs-lookup"><span data-stu-id="7790d-158">The definition for `MB_ICONQUESTION` is in WinUser.h and appears as follows:</span></span>  
  
     `#define MB_ICONQUESTION             0x00000020L`  
  
3. <span data-ttu-id="7790d-159">Dodaj równoważne instrukcje `Const` do klasy lub modułu, aby udostępnić te stałe dla aplikacji.</span><span class="sxs-lookup"><span data-stu-id="7790d-159">Add equivalent `Const` statements to your class or module to make these constants available to your application.</span></span> <span data-ttu-id="7790d-160">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="7790d-160">For example:</span></span>  
  
     [!code-vb[VbVbalrInterop#11](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#11)]  
  
###### <a name="to-call-the-dll-procedure"></a><span data-ttu-id="7790d-161">Aby wywołać procedurę DLL</span><span class="sxs-lookup"><span data-stu-id="7790d-161">To call the DLL procedure</span></span>  
  
1. <span data-ttu-id="7790d-162">Dodaj przycisk o nazwie `Button1` do formularza startowego dla projektu, a następnie kliknij go dwukrotnie, aby wyświetlić jego kod.</span><span class="sxs-lookup"><span data-stu-id="7790d-162">Add a button named `Button1` to the startup form for your project, and then double-click it to view its code.</span></span> <span data-ttu-id="7790d-163">Zostanie wyświetlony program obsługi zdarzeń dla przycisku.</span><span class="sxs-lookup"><span data-stu-id="7790d-163">The event handler for the button is displayed.</span></span>  
  
2. <span data-ttu-id="7790d-164">Dodaj kod do programu obsługi zdarzeń `Click` dla przycisku, który został dodany, aby wywołać procedurę i podać odpowiednie argumenty:</span><span class="sxs-lookup"><span data-stu-id="7790d-164">Add code to the `Click` event handler for the button you added, to call the procedure and provide the appropriate arguments:</span></span>  
  
     [!code-vb[VbVbalrInterop#12](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#12)]  
  
3. <span data-ttu-id="7790d-165">Uruchom projekt, naciskając klawisz F5.</span><span class="sxs-lookup"><span data-stu-id="7790d-165">Run the project by pressing F5.</span></span> <span data-ttu-id="7790d-166">Zostanie wyświetlone okno komunikatu z przyciskami **tak** i **bez** odpowiedzi.</span><span class="sxs-lookup"><span data-stu-id="7790d-166">The message box is displayed with both **Yes** and **No** response buttons.</span></span> <span data-ttu-id="7790d-167">Kliknij jeden z nich.</span><span class="sxs-lookup"><span data-stu-id="7790d-167">Click either one.</span></span>  
  
#### <a name="data-marshaling"></a><span data-ttu-id="7790d-168">Kierowanie danych</span><span class="sxs-lookup"><span data-stu-id="7790d-168">Data Marshaling</span></span>  
 <span data-ttu-id="7790d-169">Visual Basic automatycznie konwertuje typy danych parametrów i wartości zwracane dla wywołań interfejsu API systemu Windows, ale można użyć atrybutu `MarshalAs`, aby jawnie określić niezarządzane typy danych, których oczekuje interfejs API.</span><span class="sxs-lookup"><span data-stu-id="7790d-169">Visual Basic automatically converts the data types of parameters and return values for Windows API calls, but you can use the `MarshalAs` attribute to explicitly specify unmanaged data types that an API expects.</span></span> <span data-ttu-id="7790d-170">Aby uzyskać więcej informacji na temat organizowania międzyoperacyjności, zobacz [organizowanie międzyoperacyjnych](../../../framework/interop/interop-marshaling.md).</span><span class="sxs-lookup"><span data-stu-id="7790d-170">For more information about interop marshaling, see [Interop Marshaling](../../../framework/interop/interop-marshaling.md).</span></span>  
  
##### <a name="to-use-declare-and-marshalas-in-an-api-call"></a><span data-ttu-id="7790d-171">Aby użyć DECLARE i MarshalAs w wywołaniu interfejsu API</span><span class="sxs-lookup"><span data-stu-id="7790d-171">To use Declare and MarshalAs in an API call</span></span>  
  
1. <span data-ttu-id="7790d-172">Określ nazwę funkcji, którą chcesz wywołać, oraz jej argumenty, typy danych i wartość zwracaną.</span><span class="sxs-lookup"><span data-stu-id="7790d-172">Determine the name of the function you want to call, plus its arguments, data types, and return value.</span></span>  
  
2. <span data-ttu-id="7790d-173">Aby uprościć dostęp do atrybutu `MarshalAs`, Dodaj instrukcję `Imports` na początku kodu dla klasy lub modułu, jak w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="7790d-173">To simplify access to the `MarshalAs` attribute, add an `Imports` statement to the top of the code for the class or module, as in the following example:</span></span>  
  
     [!code-vb[VbVbalrInterop#13](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#13)]  
  
3. <span data-ttu-id="7790d-174">Dodaj prototyp funkcji dla zaimportowanej funkcji do używanej klasy lub modułu i zastosuj atrybut `MarshalAs` do parametrów lub wartości zwracanej.</span><span class="sxs-lookup"><span data-stu-id="7790d-174">Add a function prototype for the imported function to the class or module you are using, and apply the `MarshalAs` attribute to the parameters or return value.</span></span> <span data-ttu-id="7790d-175">W poniższym przykładzie wywołanie interfejsu API, które oczekuje, że typ `void*` jest organizowane jako `AsAny`:</span><span class="sxs-lookup"><span data-stu-id="7790d-175">In the following example, an API call that expects the type `void*` is marshaled as `AsAny`:</span></span>  
  
     [!code-vb[VbVbalrInterop#14](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#14)]  
  
## <a name="api-calls-using-dllimport"></a><span data-ttu-id="7790d-176">Wywołania interfejsu API używające DllImport</span><span class="sxs-lookup"><span data-stu-id="7790d-176">API Calls Using DllImport</span></span>  
 <span data-ttu-id="7790d-177">Atrybut `DllImport` zapewnia drugi sposób wywoływania funkcji w bibliotekach DLL bez bibliotek typów.</span><span class="sxs-lookup"><span data-stu-id="7790d-177">The `DllImport` attribute provides a second way to call functions in DLLs without type libraries.</span></span> <span data-ttu-id="7790d-178">`DllImport` jest w przybliżeniu odpowiednikiem przy użyciu instrukcji `Declare`, ale zapewnia większą kontrolę nad sposobem wywoływania funkcji.</span><span class="sxs-lookup"><span data-stu-id="7790d-178">`DllImport` is roughly equivalent to using a `Declare` statement but provides more control over how functions are called.</span></span>  
  
 <span data-ttu-id="7790d-179">Można użyć `DllImport` z większością wywołań interfejsu API systemu Windows, o ile wywołanie odwołuje się do współdzielonej (nazywanej czasami *static*) metody.</span><span class="sxs-lookup"><span data-stu-id="7790d-179">You can use `DllImport` with most Windows API calls as long as the call refers to a shared (sometimes called *static*) method.</span></span> <span data-ttu-id="7790d-180">Nie można użyć metod, które wymagają wystąpienia klasy.</span><span class="sxs-lookup"><span data-stu-id="7790d-180">You cannot use methods that require an instance of a class.</span></span> <span data-ttu-id="7790d-181">W przeciwieństwie do instrukcji `Declare`, wywołania `DllImport` nie mogą używać atrybutu `MarshalAs`.</span><span class="sxs-lookup"><span data-stu-id="7790d-181">Unlike `Declare` statements, `DllImport` calls cannot use the `MarshalAs` attribute.</span></span>  
  
### <a name="to-call-a-windows-api-using-the-dllimport-attribute"></a><span data-ttu-id="7790d-182">Aby wywołać interfejs API systemu Windows przy użyciu atrybutu DllImport</span><span class="sxs-lookup"><span data-stu-id="7790d-182">To call a Windows API using the DllImport attribute</span></span>  
  
1. <span data-ttu-id="7790d-183">Otwórz nowy projekt aplikacji systemu Windows, klikając pozycję **Nowy** w menu **plik** , a następnie klikając pozycję **projekt**.</span><span class="sxs-lookup"><span data-stu-id="7790d-183">Open a new Windows Application project by clicking **New** on the **File** menu, and then clicking **Project**.</span></span> <span data-ttu-id="7790d-184">Pojawi się okno dialogowe **Nowy projekt** .</span><span class="sxs-lookup"><span data-stu-id="7790d-184">The **New Project** dialog box appears.</span></span>  
  
2. <span data-ttu-id="7790d-185">Wybierz pozycję **aplikacja systemu Windows** z listy Visual Basic szablonów projektu.</span><span class="sxs-lookup"><span data-stu-id="7790d-185">Select **Windows Application** from the list of Visual Basic project templates.</span></span> <span data-ttu-id="7790d-186">Zostanie wyświetlony nowy projekt.</span><span class="sxs-lookup"><span data-stu-id="7790d-186">The new project is displayed.</span></span>  
  
3. <span data-ttu-id="7790d-187">Dodaj przycisk o nazwie `Button2` do formularza startowego.</span><span class="sxs-lookup"><span data-stu-id="7790d-187">Add a button named `Button2` to the startup form.</span></span>  
  
4. <span data-ttu-id="7790d-188">Kliknij dwukrotnie `Button2`, aby otworzyć widok kodu dla formularza.</span><span class="sxs-lookup"><span data-stu-id="7790d-188">Double-click `Button2` to open the code view for the form.</span></span>  
  
5. <span data-ttu-id="7790d-189">Aby uprościć dostęp do `DllImport`, Dodaj instrukcję `Imports` na początku kodu dla klasy formularza startowego:</span><span class="sxs-lookup"><span data-stu-id="7790d-189">To simplify access to `DllImport`, add an `Imports` statement to the top of the code for the startup form class:</span></span>  
  
     [!code-vb[VbVbalrInterop#13](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#13)]  
  
6. <span data-ttu-id="7790d-190">Zadeklaruj pustą funkcję przed instrukcją `End Class` dla formularza i nazwij `MoveFile`funkcji.</span><span class="sxs-lookup"><span data-stu-id="7790d-190">Declare an empty function preceding the `End Class` statement for the form, and name the function `MoveFile`.</span></span>  
  
7. <span data-ttu-id="7790d-191">Zastosuj Modyfikatory `Public` i `Shared` do deklaracji funkcji i ustaw parametry `MoveFile` na podstawie argumentów używanych przez funkcję interfejsu API systemu Windows:</span><span class="sxs-lookup"><span data-stu-id="7790d-191">Apply the `Public` and `Shared` modifiers to the function declaration and set parameters for `MoveFile` based on the arguments the Windows API function uses:</span></span>  
  
     [!code-vb[VbVbalrInterop#16](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#16)]  
  
     <span data-ttu-id="7790d-192">Funkcja może mieć dowolną poprawną nazwę procedury; atrybut `DllImport` określa nazwę w bibliotece DLL.</span><span class="sxs-lookup"><span data-stu-id="7790d-192">Your function can have any valid procedure name; the `DllImport` attribute specifies the name in the DLL.</span></span> <span data-ttu-id="7790d-193">Obsługuje ona również kierowanie współdziałania dla parametrów i wartości zwracanych, dzięki czemu można wybrać typy danych programu Visual Studio, które są podobne do typów danych używanych przez interfejs API.</span><span class="sxs-lookup"><span data-stu-id="7790d-193">It also handles interoperability marshaling for the parameters and return values, so you can choose Visual Studio data types that are similar to the data types the API uses.</span></span>  
  
8. <span data-ttu-id="7790d-194">Zastosuj atrybut `DllImport` do pustej funkcji.</span><span class="sxs-lookup"><span data-stu-id="7790d-194">Apply the `DllImport` attribute to the empty function.</span></span> <span data-ttu-id="7790d-195">Pierwszy parametr jest nazwą i lokalizacją biblioteki DLL zawierającej wywoływaną funkcję.</span><span class="sxs-lookup"><span data-stu-id="7790d-195">The first parameter is the name and location of the DLL containing the function you are calling.</span></span> <span data-ttu-id="7790d-196">Nie trzeba określać ścieżki dla plików znajdujących się w katalogach systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="7790d-196">You do not need to specify the path for files located in the Windows system directories.</span></span> <span data-ttu-id="7790d-197">Drugi parametr jest argumentem nazwanym, który określa nazwę funkcji w interfejsie API systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="7790d-197">The second parameter is a named argument that specifies the name of the function in the Windows API.</span></span> <span data-ttu-id="7790d-198">W tym przykładzie atrybut `DllImport` wymusza wywołania `MoveFile`, które mają być przekazywane do `MoveFileW` w KERNEL32. Bibliotece.</span><span class="sxs-lookup"><span data-stu-id="7790d-198">In this example, the `DllImport` attribute forces calls to `MoveFile` to be forwarded to `MoveFileW` in KERNEL32.DLL.</span></span> <span data-ttu-id="7790d-199">Metoda `MoveFileW` kopiuje plik ze ścieżki `src` do `dst`ścieżki.</span><span class="sxs-lookup"><span data-stu-id="7790d-199">The `MoveFileW` method copies a file from the path `src` to the path `dst`.</span></span>  
  
     [!code-vb[VbVbalrInterop#17](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#17)]  
  
9. <span data-ttu-id="7790d-200">Dodaj kod do programu obsługi zdarzeń `Button2_Click`, aby wywołać funkcję:</span><span class="sxs-lookup"><span data-stu-id="7790d-200">Add code to the `Button2_Click` event handler to call the function:</span></span>  
  
     [!code-vb[VbVbalrInterop#18](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#18)]  
  
10. <span data-ttu-id="7790d-201">Utwórz plik o nazwie test. txt i umieść go w katalogu C:\Tmp na dysku twardym.</span><span class="sxs-lookup"><span data-stu-id="7790d-201">Create a file named Test.txt and place it in the C:\Tmp directory on your hard drive.</span></span> <span data-ttu-id="7790d-202">W razie potrzeby Utwórz katalog tmp.</span><span class="sxs-lookup"><span data-stu-id="7790d-202">Create the Tmp directory if necessary.</span></span>  
  
11. <span data-ttu-id="7790d-203">Naciśnij klawisz F5, aby uruchomić aplikację.</span><span class="sxs-lookup"><span data-stu-id="7790d-203">Press F5 to start the application.</span></span> <span data-ttu-id="7790d-204">Zostanie wyświetlony formularz główny.</span><span class="sxs-lookup"><span data-stu-id="7790d-204">The main form appears.</span></span>  
  
12. <span data-ttu-id="7790d-205">Kliknij pozycję **Button2**.</span><span class="sxs-lookup"><span data-stu-id="7790d-205">Click **Button2**.</span></span> <span data-ttu-id="7790d-206">Komunikat "pomyślnie przeniesiono plik" jest wyświetlany, jeśli plik może zostać przeniesiony.</span><span class="sxs-lookup"><span data-stu-id="7790d-206">The message "The file was moved successfully" is displayed if the file can be moved.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="7790d-207">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="7790d-207">See also</span></span>

- <xref:System.Runtime.InteropServices.DllImportAttribute>
- <xref:System.Runtime.InteropServices.MarshalAsAttribute>
- [<span data-ttu-id="7790d-208">Declare, instrukcja</span><span class="sxs-lookup"><span data-stu-id="7790d-208">Declare Statement</span></span>](../../../visual-basic/language-reference/statements/declare-statement.md)
- [<span data-ttu-id="7790d-209">Auto</span><span class="sxs-lookup"><span data-stu-id="7790d-209">Auto</span></span>](../../../visual-basic/language-reference/modifiers/auto.md)
- [<span data-ttu-id="7790d-210">Użyj</span><span class="sxs-lookup"><span data-stu-id="7790d-210">Alias</span></span>](../../../visual-basic/language-reference/statements/alias-clause.md)
- [<span data-ttu-id="7790d-211">Usługa międzyoperacyjna modelu COM</span><span class="sxs-lookup"><span data-stu-id="7790d-211">COM Interop</span></span>](../../../visual-basic/programming-guide/com-interop/index.md)
- [<span data-ttu-id="7790d-212">Tworzenie prototypów w kodzie zarządzanym</span><span class="sxs-lookup"><span data-stu-id="7790d-212">Creating Prototypes in Managed Code</span></span>](../../../framework/interop/creating-prototypes-in-managed-code.md)
- [<span data-ttu-id="7790d-213">Marshaling delegata jako metoda wywołania zwrotnego</span><span class="sxs-lookup"><span data-stu-id="7790d-213">Marshaling a Delegate as a Callback Method</span></span>](../../../framework/interop/marshaling-a-delegate-as-a-callback-method.md)
