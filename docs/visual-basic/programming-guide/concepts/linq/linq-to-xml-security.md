---
title: LINQ do XML zabezpieczeń (Visual Basic)
ms.date: 07/20/2015
ms.assetid: d99b4af2-d447-4a3b-991b-6da0231a8637
ms.openlocfilehash: 3f75377502b30b03090bb2a5fe720faf4e12a028
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 05/04/2018
---
# <a name="linq-to-xml-security-visual-basic"></a><span data-ttu-id="cab0d-102">LINQ do XML zabezpieczeń (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="cab0d-102">LINQ to XML Security (Visual Basic)</span></span>
<span data-ttu-id="cab0d-103">W tym temacie opisano problemy dotyczące zabezpieczeń skojarzone z LINQ do XML.</span><span class="sxs-lookup"><span data-stu-id="cab0d-103">This topic describes security issues associated with LINQ to XML.</span></span> <span data-ttu-id="cab0d-104">Ponadto zawiera on pewne wskazówki dotyczące zmniejszenia zagrożenie bezpieczeństwa.</span><span class="sxs-lookup"><span data-stu-id="cab0d-104">In addition, it provides some guidance for mitigating security exposure.</span></span>  
  
## <a name="linq-to-xml-security-overview"></a><span data-ttu-id="cab0d-105">LINQ do XML-Przegląd zabezpieczeń</span><span class="sxs-lookup"><span data-stu-id="cab0d-105">LINQ to XML Security Overview</span></span>  
 <span data-ttu-id="cab0d-106">LINQ do XML więcej zaprojektowano pod kątem programowania wygody niż dla aplikacji serwerowych z wymaganiami bezpieczeństwa.</span><span class="sxs-lookup"><span data-stu-id="cab0d-106">LINQ to XML is designed more for programming convenience than for server-side applications with stringent security requirements.</span></span> <span data-ttu-id="cab0d-107">Większość scenariuszy XML składają się z przetwarzania zaufanych dokumentów XML, a nie przetwarzania niezaufanych dokumentów XML, które są przekazywane do serwera.</span><span class="sxs-lookup"><span data-stu-id="cab0d-107">Most XML scenarios consist of processing trusted XML documents, rather than processing untrusted XML documents that are uploaded to a server.</span></span> <span data-ttu-id="cab0d-108">LINQ do XML jest zoptymalizowana pod kątem tych scenariuszy.</span><span class="sxs-lookup"><span data-stu-id="cab0d-108">LINQ to XML is optimized for these scenarios.</span></span>  
  
 <span data-ttu-id="cab0d-109">Jeśli niezaufany danych z nieznanych źródeł musi przetworzyć, firma Microsoft zaleca używanie wystąpienia elementu <xref:System.Xml.XmlReader> klasy, który został skonfigurowany, aby odfiltrować znane XML "odmowa usługi" (DoS).</span><span class="sxs-lookup"><span data-stu-id="cab0d-109">If you must process untrusted data from unknown sources, Microsoft recommends that you use an instance of the <xref:System.Xml.XmlReader> class that has been configured to filter out known XML denial of service (DoS) attacks.</span></span>  
  
 <span data-ttu-id="cab0d-110">Jeśli skonfigurowano <xref:System.Xml.XmlReader> złagodzić ataki, umożliwia tego czytnika wypełnić LINQ do XML drzewa i nadal korzystać z udoskonaleń dotyczących produktywności programisty programu LINQ do XML.</span><span class="sxs-lookup"><span data-stu-id="cab0d-110">If you have configured an <xref:System.Xml.XmlReader> to mitigate denial of service attacks, you can use that reader to populate a LINQ to XML tree and still benefit from the programmer productivity enhancements of LINQ to XML.</span></span> <span data-ttu-id="cab0d-111">Wiele metod środki zaradcze obejmują tworzenie tych, które są skonfigurowane w celu złagodzenia problem z zabezpieczeniami i tworzenie wystąpień drzewo XML za pomocą skonfigurowanych czytnika.</span><span class="sxs-lookup"><span data-stu-id="cab0d-111">Many mitigation techniques involve creating readers that are configured to mitigate the security issue, and then instantiating an XML tree through the configured reader.</span></span>  
  
 <span data-ttu-id="cab0d-112">XML jest bardzo narażone na ataki, ponieważ dokumenty są niepowiązane w rozmiarze, głębokość, rozmiar nazwy elementu i.</span><span class="sxs-lookup"><span data-stu-id="cab0d-112">XML is intrinsically vulnerable to denial of service attacks because documents are unbounded in size, depth, element name size, and more.</span></span> <span data-ttu-id="cab0d-113">Niezależnie od tego składnika, który umożliwia proces XML użytkownik powinien zawsze można przygotować do odtworzenia domeny aplikacji, gdy jest używana funkcja nadmiernego zasobów.</span><span class="sxs-lookup"><span data-stu-id="cab0d-113">Regardless of the component that you use to process XML, you should always be prepared to recycle the application domain if it uses excessive resources.</span></span>  
  
## <a name="mitigation-of-xml-xsd-xpath-and-xslt-attacks"></a><span data-ttu-id="cab0d-114">Środki zaradcze, XML, XSD, XPath i ataków XSLT</span><span class="sxs-lookup"><span data-stu-id="cab0d-114">Mitigation of XML, XSD, XPath, and XSLT Attacks</span></span>  
 <span data-ttu-id="cab0d-115">LINQ do XML jest oparty na <xref:System.Xml.XmlReader> i <xref:System.Xml.XmlWriter>.</span><span class="sxs-lookup"><span data-stu-id="cab0d-115">LINQ to XML is built upon <xref:System.Xml.XmlReader> and <xref:System.Xml.XmlWriter>.</span></span> <span data-ttu-id="cab0d-116">LINQ do XML obsługuje XSD i XPath za pośrednictwem metody rozszerzenia w <xref:System.Xml.Schema?displayProperty=nameWithType> i <xref:System.Xml.XPath?displayProperty=nameWithType> przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="cab0d-116">LINQ to XML supports XSD and XPath through extension methods in the <xref:System.Xml.Schema?displayProperty=nameWithType> and <xref:System.Xml.XPath?displayProperty=nameWithType> namespaces.</span></span> <span data-ttu-id="cab0d-117">Przy użyciu <xref:System.Xml.XmlReader>, <xref:System.Xml.XPath.XPathNavigator>, i <xref:System.Xml.XmlWriter> klas w połączeniu z LINQ do XML, można wywołać XSLT do transformacji XML drzewa.</span><span class="sxs-lookup"><span data-stu-id="cab0d-117">Using the <xref:System.Xml.XmlReader>, <xref:System.Xml.XPath.XPathNavigator>, and <xref:System.Xml.XmlWriter> classes in conjunction with LINQ to XML, you can invoke XSLT to transform XML trees.</span></span>  
  
 <span data-ttu-id="cab0d-118">Jeśli pracujesz w środowisku mniej bezpieczne, istnieje szereg problemów z zabezpieczeniami, które są skojarzone z językiem XML i korzystanie z klas w <xref:System.Xml?displayProperty=nameWithType>, <xref:System.Xml.Schema?displayProperty=nameWithType>, <xref:System.Xml.XPath?displayProperty=nameWithType>, i <xref:System.Xml.Xsl?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="cab0d-118">If you are operating in a less secure environment, there are a number of security issues that are associated with XML and the use of the classes in <xref:System.Xml?displayProperty=nameWithType>, <xref:System.Xml.Schema?displayProperty=nameWithType>, <xref:System.Xml.XPath?displayProperty=nameWithType>, and <xref:System.Xml.Xsl?displayProperty=nameWithType>.</span></span> <span data-ttu-id="cab0d-119">Te problemy obejmują, ale nie są ograniczone do następujących:</span><span class="sxs-lookup"><span data-stu-id="cab0d-119">These issues include, but are not limited to, the following:</span></span>  
  
-   <span data-ttu-id="cab0d-120">XSD, XPath i XSLT są oparte na ciągach języki, w których można określić operacji używające dużo czasu i pamięci.</span><span class="sxs-lookup"><span data-stu-id="cab0d-120">XSD, XPath, and XSLT are string-based languages in which you can specify operations that consume a lot of time or memory.</span></span> <span data-ttu-id="cab0d-121">Jest odpowiedzialny za programistów aplikacji, którzy pobierają ciągi XSD, XPath lub XSLT ze źródeł niezaufanych, aby sprawdzić, czy ciągi nie są złośliwe, lub do monitorowania i ograniczyć możliwość, że te ciągi oceny doprowadzi do nadmiernego systemu zużycie zasobów.</span><span class="sxs-lookup"><span data-stu-id="cab0d-121">It is the responsibility of application programmers who take XSD, XPath, or XSLT strings from untrusted sources to validate that the strings are not malicious, or to monitor and mitigate the possibility that evaluating these strings will lead to excessive system resource consumption.</span></span>  
  
-   <span data-ttu-id="cab0d-122">Schematy XSD (w tym wbudowane schematy) są z założenia podatne na ataki; nie powinna obsługiwać schematy ze źródeł niezaufanych.</span><span class="sxs-lookup"><span data-stu-id="cab0d-122">XSD schemas (including inline schemas) are inherently vulnerable to denial of service attacks; you should not accept schemas from untrusted sources.</span></span>  
  
-   <span data-ttu-id="cab0d-123">XSD i XSLT może zawierać odwołania do innych plików, a tych odwołań może spowodować ataków między strefy i między domenami.</span><span class="sxs-lookup"><span data-stu-id="cab0d-123">XSD and XSLT can include references to other files, and such references can result in cross-zone and cross-domain attacks.</span></span>  
  
-   <span data-ttu-id="cab0d-124">Jednostek zewnętrznych w definicji DTD może spowodować ataków między strefy i między domenami.</span><span class="sxs-lookup"><span data-stu-id="cab0d-124">External entities in DTDs can result in cross-zone and cross-domain attacks.</span></span>  
  
-   <span data-ttu-id="cab0d-125">Definicje DTD są narażone na ataki.</span><span class="sxs-lookup"><span data-stu-id="cab0d-125">DTDs are vulnerable to denial of service attacks.</span></span>  
  
-   <span data-ttu-id="cab0d-126">Dokumenty XML wyjątkowo głębokiego mogą wiązać się z odmowa problemów dotyczących usługi; można ograniczyć liczbę dokumentów XML.</span><span class="sxs-lookup"><span data-stu-id="cab0d-126">Exceptionally deep XML documents can pose denial of service issues; you might want to limit the depth of XML documents.</span></span>  
  
-   <span data-ttu-id="cab0d-127">Nie akceptuj pomocnicze składniki, takie jak <xref:System.Xml.NameTable>, <xref:System.Xml.XmlNamespaceManager>, i <xref:System.Xml.XmlResolver> obiektów z niezaufanych zestawów.</span><span class="sxs-lookup"><span data-stu-id="cab0d-127">Do not accept supporting components, such as <xref:System.Xml.NameTable>, <xref:System.Xml.XmlNamespaceManager>, and <xref:System.Xml.XmlResolver> objects, from untrusted assemblies.</span></span>  
  
-   <span data-ttu-id="cab0d-128">Przeczytaj dane fragmentów w osłabianiu ataków dużego dokumentu.</span><span class="sxs-lookup"><span data-stu-id="cab0d-128">Read data in chunks to mitigate large document attacks.</span></span>  
  
-   <span data-ttu-id="cab0d-129">Bloki skryptu w arkuszach stylów XSLT mogą uwidaczniać liczby ataków.</span><span class="sxs-lookup"><span data-stu-id="cab0d-129">Script blocks in XSLT style sheets can expose a number of attacks.</span></span>  
  
-   <span data-ttu-id="cab0d-130">Sprawdź dokładnie, przed tworzenia dynamicznej wyrażenia XPath.</span><span class="sxs-lookup"><span data-stu-id="cab0d-130">Validate carefully before constructing dynamic XPath expressions.</span></span>  
  
## <a name="linq-to-xml-security-issues"></a><span data-ttu-id="cab0d-131">LINQ do XML problemy dotyczące zabezpieczeń</span><span class="sxs-lookup"><span data-stu-id="cab0d-131">LINQ to XML Security Issues</span></span>  
 <span data-ttu-id="cab0d-132">Problemy dotyczące zabezpieczeń w tym temacie nie są prezentowane w określonej kolejności.</span><span class="sxs-lookup"><span data-stu-id="cab0d-132">The security issues in this topic are not presented in any particular order.</span></span> <span data-ttu-id="cab0d-133">Wszystkie problemy są ważne i powinny być kierowane jako odpowiednie.</span><span class="sxs-lookup"><span data-stu-id="cab0d-133">All issues are important and should be addressed as appropriate.</span></span>  
  
 <span data-ttu-id="cab0d-134">Pomyślne atakom podniesienie uprawnień zapewnia złośliwego zestawu większą kontrolę nad środowiskiem.</span><span class="sxs-lookup"><span data-stu-id="cab0d-134">A successful elevation of privilege attack gives a malicious assembly more control over its environment.</span></span> <span data-ttu-id="cab0d-135">Pomyślne atakom podniesienie uprawnień może spowodować ujawnienie danych, odmowę usługi i inne.</span><span class="sxs-lookup"><span data-stu-id="cab0d-135">A successful elevation of privilege attack can result in disclosure of data, denial of service, and more.</span></span>  
  
 <span data-ttu-id="cab0d-136">Aplikacje nie powinny ujawniać dane do użytkowników, którzy nie jest uprawniony do wyświetlenia danych.</span><span class="sxs-lookup"><span data-stu-id="cab0d-136">Applications should not disclose data to users who are not authorized to see that data.</span></span>  
  
 <span data-ttu-id="cab0d-137">Ataki powodować analizatora składni XML lub LINQ XML zużyje nadmiernej ilości pamięci lub czasu procesora CPU.</span><span class="sxs-lookup"><span data-stu-id="cab0d-137">Denial of service attacks cause the XML parser or LINQ to XML to consume excessive amounts of memory or CPU time.</span></span> <span data-ttu-id="cab0d-138">Ataki są uważane za mniej poważne niż atakom powodującym podniesienie uprawnień lub ujawnieniem danych ataków.</span><span class="sxs-lookup"><span data-stu-id="cab0d-138">Denial of service attacks are considered to be less severe than elevation of privilege attacks or disclosure of data attacks.</span></span> <span data-ttu-id="cab0d-139">Są ważne w scenariuszu, w których serwer wymaga przetwarzania dokumentów XML ze źródeł niezaufanych.</span><span class="sxs-lookup"><span data-stu-id="cab0d-139">However, they are important in a scenario where a server needs to process XML documents from untrusted sources.</span></span>  
  
### <a name="exceptions-and-error-messages-might-reveal-data"></a><span data-ttu-id="cab0d-140">Wyjątki i komunikaty o błędach może ujawnić dane</span><span class="sxs-lookup"><span data-stu-id="cab0d-140">Exceptions and Error Messages Might Reveal Data</span></span>  
 <span data-ttu-id="cab0d-141">Opis błędu może ujawnić dane, takie jak transformacji danych, plików, nazwy lub szczegóły implementacji.</span><span class="sxs-lookup"><span data-stu-id="cab0d-141">The description of an error might reveal data, such as the data being transformed, file names, or implementation details.</span></span> <span data-ttu-id="cab0d-142">Nie należy uwidaczniać komunikaty o błędach dotyczące obiektów wywołujących, które nie są zaufane.</span><span class="sxs-lookup"><span data-stu-id="cab0d-142">Error messages should not be exposed to callers that are not trusted.</span></span> <span data-ttu-id="cab0d-143">Należy przechwytywać wszystkie błędy i przesłania raportów o błędach z własnych niestandardowych komunikatów o błędach.</span><span class="sxs-lookup"><span data-stu-id="cab0d-143">You should catch all errors and report errors with your own custom error messages.</span></span>  
  
### <a name="do-not-call-codeaccesspermissionsassert-in-an-event-handler"></a><span data-ttu-id="cab0d-144">Nie wywołuj CodeAccessPermissions.Assert w obsłudze zdarzeń</span><span class="sxs-lookup"><span data-stu-id="cab0d-144">Do Not Call CodeAccessPermissions.Assert in an Event Handler</span></span>  
 <span data-ttu-id="cab0d-145">Zestaw może mieć mniejsze lub większe uprawnienia.</span><span class="sxs-lookup"><span data-stu-id="cab0d-145">An assembly can have lesser or greater permissions.</span></span> <span data-ttu-id="cab0d-146">Zestaw, który ma większe uprawnienia ma większą kontrolę nad komputerem i środowiska.</span><span class="sxs-lookup"><span data-stu-id="cab0d-146">An assembly that has greater permissions has greater control over the computer and its environments.</span></span>  
  
 <span data-ttu-id="cab0d-147">Jeśli kod w zestawie większe uprawnienia wywołuje <xref:System.Security.CodeAccessPermission.Assert%2A?displayProperty=nameWithType> w program obsługi zdarzeń, a następnie XML drzewa są przekazywane do zestawu złośliwego czy ma ograniczone uprawnienia, złośliwy zestawu może spowodować się zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="cab0d-147">If code in an assembly with greater permissions calls <xref:System.Security.CodeAccessPermission.Assert%2A?displayProperty=nameWithType> in an event handler, and then the XML tree is passed to a malicious assembly that has restricted permissions, the malicious assembly can cause an event to be raised.</span></span> <span data-ttu-id="cab0d-148">Ponieważ zdarzenia uruchamia kod w zestawie większe uprawnienia, złośliwego zestawu następnie czy działających z podniesionymi uprawnieniami.</span><span class="sxs-lookup"><span data-stu-id="cab0d-148">Because the event runs code that is in the assembly with greater permissions, the malicious assembly would then be operating with elevated privileges.</span></span>  
  
 <span data-ttu-id="cab0d-149">Firma Microsoft zaleca się, że nigdy nie wywołania <xref:System.Security.CodeAccessPermission.Assert%2A?displayProperty=nameWithType> w obsłudze zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="cab0d-149">Microsoft recommends that you never call <xref:System.Security.CodeAccessPermission.Assert%2A?displayProperty=nameWithType> in an event handler.</span></span>  
  
### <a name="dtds-are-not-secure"></a><span data-ttu-id="cab0d-150">Definicje DTD są nie zabezpieczyć</span><span class="sxs-lookup"><span data-stu-id="cab0d-150">DTDs are Not Secure</span></span>  
 <span data-ttu-id="cab0d-151">Jednostki w definicji DTD z założenia nie jest bezpieczna.</span><span class="sxs-lookup"><span data-stu-id="cab0d-151">Entities in DTDs are inherently not secure.</span></span> <span data-ttu-id="cab0d-152">Istnieje możliwość złośliwego dokument XML, który zawiera DTD spowodować analizator użycie wszystkich pamięci i czasu Procesora, co doprowadzi do odmowy usługi.</span><span class="sxs-lookup"><span data-stu-id="cab0d-152">It is possible for a malicious XML document that contains a DTD to cause the parser to use all memory and CPU time, causing a denial of service attack.</span></span> <span data-ttu-id="cab0d-153">W związku z tym w składniku LINQ to XML, przetwarzanie elementu DTD jest domyślnie wyłączona.</span><span class="sxs-lookup"><span data-stu-id="cab0d-153">Therefore, in LINQ to XML, DTD processing is turned off by default.</span></span> <span data-ttu-id="cab0d-154">Definicje DTD ze źródeł niezaufanych nie powinna obsługiwać.</span><span class="sxs-lookup"><span data-stu-id="cab0d-154">You should not accept DTDs from untrusted sources.</span></span>  
  
 <span data-ttu-id="cab0d-155">Przykładem akceptowania elementów DTD ze źródeł niezaufanych jest aplikacji sieci Web, która umożliwia użytkownikom sieci Web przekazać plik XML, który odwołuje się do definicji DTD i plik definicji DTD.</span><span class="sxs-lookup"><span data-stu-id="cab0d-155">One example of accepting DTDs from untrusted sources is a Web application that allows Web users to upload an XML file that references a DTD and a DTD file.</span></span> <span data-ttu-id="cab0d-156">Podczas sprawdzania poprawności pliku złośliwy DTD można wykonać odmowa usługi ataków na serwerze.</span><span class="sxs-lookup"><span data-stu-id="cab0d-156">Upon validation of the file, a malicious DTD could execute a denial of service attack on your server.</span></span> <span data-ttu-id="cab0d-157">Innym przykładem akceptowania elementów DTD ze źródeł niezaufanych jest odwołanie DTD na udział sieciowy, który umożliwia także anonimowy dostęp do.</span><span class="sxs-lookup"><span data-stu-id="cab0d-157">Another example of accepting DTDs from untrusted sources is to reference a DTD on a network share that also allows anonymous FTP access.</span></span>  
  
### <a name="avoid-excessive-buffer-allocation"></a><span data-ttu-id="cab0d-158">Unikaj nadmiernego buforu alokacji</span><span class="sxs-lookup"><span data-stu-id="cab0d-158">Avoid Excessive Buffer Allocation</span></span>  
 <span data-ttu-id="cab0d-159">Deweloperzy aplikacji należy zwrócić uwagę, że bardzo dużych źródeł danych może doprowadzić do wyczerpania zasobów i ataki.</span><span class="sxs-lookup"><span data-stu-id="cab0d-159">Application developers should be aware that extremely large data sources can lead to resource exhaustion and denial of service attacks.</span></span>  
  
 <span data-ttu-id="cab0d-160">Złośliwy użytkownik przesyła lub przekazuje bardzo dużych dokument XML, program może spowodować LINQ do XML zużywanie zasobów systemowych nadmiernego.</span><span class="sxs-lookup"><span data-stu-id="cab0d-160">If a malicious user submits or uploads a very large XML document, it could cause LINQ to XML to consume excessive system resources.</span></span> <span data-ttu-id="cab0d-161">Może to stanowić typu "odmowa usługi".</span><span class="sxs-lookup"><span data-stu-id="cab0d-161">This can constitute a denial of service attack.</span></span> <span data-ttu-id="cab0d-162">Aby tego uniknąć, można ustawić <xref:System.Xml.XmlReaderSettings.MaxCharactersInDocument%2A?displayProperty=nameWithType> właściwości i Utwórz reader, który następnie jest ograniczona w rozmiar dokumentu, który można go załadować.</span><span class="sxs-lookup"><span data-stu-id="cab0d-162">To prevent this, you can set the <xref:System.Xml.XmlReaderSettings.MaxCharactersInDocument%2A?displayProperty=nameWithType> property, and create a reader that is then limited in the size of document that it can load.</span></span> <span data-ttu-id="cab0d-163">Czytnik jest następnie używać do tworzenia drzewa XML.</span><span class="sxs-lookup"><span data-stu-id="cab0d-163">You then use the reader to create the XML tree.</span></span>  
  
 <span data-ttu-id="cab0d-164">Na przykład, jeśli wiadomo, że maksymalna oczekiwany rozmiar dokumentów XML pochodzące z niezaufanego źródła będą mieć mniej niż 50 KB, ustaw <xref:System.Xml.XmlReaderSettings.MaxCharactersInDocument%2A?displayProperty=nameWithType> do 100 000.</span><span class="sxs-lookup"><span data-stu-id="cab0d-164">For example, if you know that the maximum expected size of your XML documents coming from an untrusted source will be less than 50K bytes, set <xref:System.Xml.XmlReaderSettings.MaxCharactersInDocument%2A?displayProperty=nameWithType> to 100,000.</span></span> <span data-ttu-id="cab0d-165">To nie będzie obciążać przetwarzania dokumentów XML, a jednocześnie ograniczy zagrożenie odmowa, gdzie może przekazać dokumenty zagrożeń usługi używające duże ilości pamięci.</span><span class="sxs-lookup"><span data-stu-id="cab0d-165">This will not encumber your processing of XML documents, and at the same time it will mitigate denial of service threats where documents might be uploaded that would consume large amounts of memory.</span></span>  
  
### <a name="avoid-excess-entity-expansion"></a><span data-ttu-id="cab0d-166">Unikaj rozszerzenia nadmiarowe jednostki</span><span class="sxs-lookup"><span data-stu-id="cab0d-166">Avoid Excess Entity Expansion</span></span>  
 <span data-ttu-id="cab0d-167">Jednym z znane ataki odmowy usługi przy użyciu DTD jest dokument, który powoduje, że rozszerzenia nadmiernego jednostki.</span><span class="sxs-lookup"><span data-stu-id="cab0d-167">One of the known denial of service attacks when using a DTD is a document that causes excessive entity expansion.</span></span> <span data-ttu-id="cab0d-168">Aby tego uniknąć, można ustawić <xref:System.Xml.XmlReaderSettings.MaxCharactersFromEntities%2A?displayProperty=nameWithType> właściwości i Utwórz reader, który następnie jest ograniczona w liczbie znaków, wynikających z rozszerzenia jednostki.</span><span class="sxs-lookup"><span data-stu-id="cab0d-168">To prevent this, you can set the <xref:System.Xml.XmlReaderSettings.MaxCharactersFromEntities%2A?displayProperty=nameWithType> property, and create a reader that is then limited in the number of characters that result from entity expansion.</span></span> <span data-ttu-id="cab0d-169">Czytnik jest następnie używać do tworzenia drzewa XML.</span><span class="sxs-lookup"><span data-stu-id="cab0d-169">You then use the reader to create the XML tree.</span></span>  
  
### <a name="limit-the-depth-of-the-xml-hierarchy"></a><span data-ttu-id="cab0d-170">Limit głębokość hierarchii XML</span><span class="sxs-lookup"><span data-stu-id="cab0d-170">Limit the Depth of the XML Hierarchy</span></span>  
 <span data-ttu-id="cab0d-171">Jeden możliwy atak odmowy usług jest po przesłaniu dokumentu, który ma nadmiernego głębokość hierarchii.</span><span class="sxs-lookup"><span data-stu-id="cab0d-171">One possible denial of service attack is when a document is submitted that has excessive depth of hierarchy.</span></span> <span data-ttu-id="cab0d-172">Aby tego uniknąć, można zawijać <xref:System.Xml.XmlReader> własne klasy, które zlicza głębokość elementów.</span><span class="sxs-lookup"><span data-stu-id="cab0d-172">To prevent this, you can wrap a <xref:System.Xml.XmlReader> in your own class that counts the depth of elements.</span></span> <span data-ttu-id="cab0d-173">Głębokość przekracza ustalonej rozsądnym poziomie, po zakończeniu przetwarzania złośliwy dokument.</span><span class="sxs-lookup"><span data-stu-id="cab0d-173">If the depth exceeds a predetermined reasonable level, you can terminate the processing of the malicious document.</span></span>  
  
### <a name="protect-against-untrusted-xmlreader-or-xmlwriter-implementations"></a><span data-ttu-id="cab0d-174">Ochronę przed niezaufanych, XmlReader lub XmlWriter implementacji</span><span class="sxs-lookup"><span data-stu-id="cab0d-174">Protect Against Untrusted XmlReader or XmlWriter Implementations</span></span>  
 <span data-ttu-id="cab0d-175">Administratorzy powinni sprawdzić wszelkie zewnętrznie dostarczenia <xref:System.Xml.XmlReader> lub <xref:System.Xml.XmlWriter> implementacje mieć silne nazwy i został zarejestrowany w konfiguracji komputera.</span><span class="sxs-lookup"><span data-stu-id="cab0d-175">Administrators should verify that any externally supplied <xref:System.Xml.XmlReader> or <xref:System.Xml.XmlWriter> implementations have strong names and have been registered in the machine configuration.</span></span> <span data-ttu-id="cab0d-176">Zapobiega to złośliwy kod udający odczytywania lub zapisywania z ładowany.</span><span class="sxs-lookup"><span data-stu-id="cab0d-176">This prevents malicious code masquerading as a reader or writer from being loaded.</span></span>  
  
### <a name="periodically-free-objects-that-reference-xname"></a><span data-ttu-id="cab0d-177">Obiekty okresowo wolnego XName tego odwołania</span><span class="sxs-lookup"><span data-stu-id="cab0d-177">Periodically Free Objects that Reference XName</span></span>  
 <span data-ttu-id="cab0d-178">Aby zapewnić ochronę przed określonych rodzajów ataków, programistom powinna zwolnić wszystkie obiekty, które odwołują się <xref:System.Xml.Linq.XName> obiektu w domenie aplikacji na bieżąco.</span><span class="sxs-lookup"><span data-stu-id="cab0d-178">To protect against certain kinds of attacks, application programmers should free all objects that reference an <xref:System.Xml.Linq.XName> object in the application domain on a regular basis.</span></span>  
  
### <a name="protect-against-random-xml-names"></a><span data-ttu-id="cab0d-179">Ochrona przed nazwy losowe XML</span><span class="sxs-lookup"><span data-stu-id="cab0d-179">Protect Against Random XML Names</span></span>  
 <span data-ttu-id="cab0d-180">Aplikacje, które pobierają dane ze źródeł niezaufanych należy rozważyć użycie <xref:System.Xml.XmlReader> czyli opakowana w kodu niestandardowego do sprawdzenia pod kątem możliwości losowe nazw XML i przestrzenie nazw.</span><span class="sxs-lookup"><span data-stu-id="cab0d-180">Applications that take data from untrusted sources should consider using an <xref:System.Xml.XmlReader> that is wrapped in custom code to inspect for the possibility of random XML names and namespaces.</span></span> <span data-ttu-id="cab0d-181">Jeśli takie losowe nazw XML i przestrzenie nazw są wykrywane, aplikacji można następnie przerwanie przetwarzania złośliwy dokument.</span><span class="sxs-lookup"><span data-stu-id="cab0d-181">If such random XML names and namespaces are detected, the application can then terminate the processing of the malicious document.</span></span>  
  
 <span data-ttu-id="cab0d-182">Można ograniczyć liczbę nazw w danej przestrzeni nazw (w tym nazwy w bez przestrzeni nazw) do limitu uzasadnione.</span><span class="sxs-lookup"><span data-stu-id="cab0d-182">You might want to limit the number of names in any given namespace (including names in no namespace) to a reasonable limit.</span></span>  
  
### <a name="annotations-are-accessible-by-software-components-that-share-a-linq-to-xml-tree"></a><span data-ttu-id="cab0d-183">Adnotacje są dostępne dla składników oprogramowania, które współużytkują LINQ do XML drzewa</span><span class="sxs-lookup"><span data-stu-id="cab0d-183">Annotations Are Accessible by Software Components that Share a LINQ to XML Tree</span></span>  
 <span data-ttu-id="cab0d-184">LINQ do XML może służyć do tworzenia potoków przetwarzania, w których składniki różnych aplikacji obciążenia, sprawdzanie poprawności zapytania, przekształcania, aktualizacji i zapisać dane XML, które są przekazywane między składnikami jako drzewa XML.</span><span class="sxs-lookup"><span data-stu-id="cab0d-184">LINQ to XML could be used to build processing pipelines in which different application components load, validate, query, transform, update, and save XML data that is passed between components as XML trees.</span></span> <span data-ttu-id="cab0d-185">Może to ułatwić optymalizacji wydajności, ponieważ koszty ładowania i serializacji obiektów do tekstu XML jest wykonywana tylko na końcu potoku.</span><span class="sxs-lookup"><span data-stu-id="cab0d-185">This can help optimize performance, because the overhead of loading and serializing objects to XML text is done only at the ends of the pipeline.</span></span> <span data-ttu-id="cab0d-186">Deweloperzy muszą należy jednak pamiętać, że wszystkie adnotacje i obsługi zdarzeń utworzonych przez jeden składnik są dostępne dla innych składników.</span><span class="sxs-lookup"><span data-stu-id="cab0d-186">Developers must be aware, however, that all annotations and event handlers created by one component are accessible to other components.</span></span> <span data-ttu-id="cab0d-187">Można utworzyć liczbę luk w zabezpieczeniach, jeśli składniki mają różne poziomy zaufania.</span><span class="sxs-lookup"><span data-stu-id="cab0d-187">This can create a number of vulnerabilities if the components have different levels of trust.</span></span> <span data-ttu-id="cab0d-188">Do tworzenia bezpiecznego potoki przez składniki zaufany, musi serializować LINQ do obiektów XML do tekstu XML przed przekazaniem ich do niezaufany składnik.</span><span class="sxs-lookup"><span data-stu-id="cab0d-188">To build secure pipelines across less trusted components, you must serialize LINQ to XML objects to XML text before passing the data to an untrusted component.</span></span>  
  
 <span data-ttu-id="cab0d-189">Niektóre zabezpieczeń są udostępniane przez środowisko uruchomieniowe języka wspólnego (CLR).</span><span class="sxs-lookup"><span data-stu-id="cab0d-189">Some security is provided by the common language runtime (CLR).</span></span> <span data-ttu-id="cab0d-190">Na przykład składnik, który nie zawiera klasy prywatnego nie może uzyskać dostępu adnotacje, wyznaczaną przez tę klasę.</span><span class="sxs-lookup"><span data-stu-id="cab0d-190">For example, a component that does not include a private class cannot access annotations keyed by that class.</span></span> <span data-ttu-id="cab0d-191">Jednak adnotacje mogą zostać usunięte przez składniki, których nie można ich odczytać.</span><span class="sxs-lookup"><span data-stu-id="cab0d-191">However, annotations can be deleted by components that cannot read them.</span></span> <span data-ttu-id="cab0d-192">Może to służyć jako manipulacją ataku.</span><span class="sxs-lookup"><span data-stu-id="cab0d-192">This could be used as a tampering attack.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="cab0d-193">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="cab0d-193">See Also</span></span>  
 [<span data-ttu-id="cab0d-194">Przewodnik programowania w języku (LINQ do XML) (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="cab0d-194">Programming Guide (LINQ to XML) (Visual Basic)</span></span>](../../../../visual-basic/programming-guide/concepts/linq/programming-guide-linq-to-xml.md)
