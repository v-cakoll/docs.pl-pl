---
title: PropertyPath — Składnia XAML
ms.date: 03/30/2017
helpviewer_keywords:
- PropertyPath object [WPF]
- XAML [WPF], PropertyPath object
ms.assetid: 0e3cdf07-abe6-460a-a9af-3764b4fd707f
ms.openlocfilehash: 817ce750cdad58e504eef5144cfb8a2813bca596
ms.sourcegitcommit: 839777281a281684a7e2906dccb3acd7f6a32023
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/24/2020
ms.locfileid: "82141290"
---
# <a name="propertypath-xaml-syntax"></a><span data-ttu-id="a9c50-102">PropertyPath — Składnia XAML</span><span class="sxs-lookup"><span data-stu-id="a9c50-102">PropertyPath XAML Syntax</span></span>

<span data-ttu-id="a9c50-103"><xref:System.Windows.PropertyPath> Obiekt obsługuje złożoną wbudowaną [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] składnię do ustawiania różnych właściwości, które <xref:System.Windows.PropertyPath> przyjmują typ jako wartość.</span><span class="sxs-lookup"><span data-stu-id="a9c50-103">The <xref:System.Windows.PropertyPath> object supports a complex inline [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] syntax for setting various properties that take the <xref:System.Windows.PropertyPath> type as their value.</span></span> <span data-ttu-id="a9c50-104">W tym temacie opisano <xref:System.Windows.PropertyPath> składnię stosowaną do składni powiązań i animacji.</span><span class="sxs-lookup"><span data-stu-id="a9c50-104">This topic documents the <xref:System.Windows.PropertyPath> syntax as applied to binding and animation syntaxes.</span></span>

<a name="where"></a>

## <a name="where-propertypath-is-used"></a><span data-ttu-id="a9c50-105">Gdzie jest używany PropertyPath</span><span class="sxs-lookup"><span data-stu-id="a9c50-105">Where PropertyPath Is Used</span></span>

<span data-ttu-id="a9c50-106"><xref:System.Windows.PropertyPath>jest wspólnym obiektem, który jest używany w [!INCLUDE[TLA#tla_winclient](../../../../includes/tlasharptla-winclient-md.md)] kilku funkcjach.</span><span class="sxs-lookup"><span data-stu-id="a9c50-106"><xref:System.Windows.PropertyPath> is a common object that is used in several [!INCLUDE[TLA#tla_winclient](../../../../includes/tlasharptla-winclient-md.md)] features.</span></span> <span data-ttu-id="a9c50-107">Mimo korzystania ze wspólnych <xref:System.Windows.PropertyPath> do przekazywania informacji o ścieżce właściwości użycia dla każdego obszaru funkcji, gdzie <xref:System.Windows.PropertyPath> jest używany jako typ.</span><span class="sxs-lookup"><span data-stu-id="a9c50-107">Despite using the common <xref:System.Windows.PropertyPath> to convey property path information, the usages for each feature area where <xref:System.Windows.PropertyPath> is used as a type vary.</span></span> <span data-ttu-id="a9c50-108">W związku z tym bardziej praktyczne jest udokumentowanie składni dla poszczególnych funkcji.</span><span class="sxs-lookup"><span data-stu-id="a9c50-108">Therefore, it is more practical to document the syntaxes on a per-feature basis.</span></span>

<span data-ttu-id="a9c50-109">Program [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] używa <xref:System.Windows.PropertyPath> głównie do opisywania ścieżek modelu obiektów do przechodzenia przez właściwości źródła danych obiektu oraz do opisywania ścieżki docelowej dla animacji ukierunkowanych.</span><span class="sxs-lookup"><span data-stu-id="a9c50-109">Primarily, [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] uses <xref:System.Windows.PropertyPath> to describe object-model paths for traversing the properties of an object data source, and to describe the target path for targeted animations.</span></span>

<span data-ttu-id="a9c50-110">Niektóre właściwości stylu i szablonu, takie <xref:System.Windows.Setter.Property%2A?displayProperty=nameWithType> jak Take kwalifikowana nazwa właściwości, która jest podobna do <xref:System.Windows.PropertyPath>.</span><span class="sxs-lookup"><span data-stu-id="a9c50-110">Some style and template properties such as <xref:System.Windows.Setter.Property%2A?displayProperty=nameWithType> take a qualified property name that superficially resembles a <xref:System.Windows.PropertyPath>.</span></span> <span data-ttu-id="a9c50-111">Nie jest to jednak prawdziwe <xref:System.Windows.PropertyPath>; Zamiast tego jest to kwalifikowany *właściciel.* użycie formatu ciągu właściwości, które jest włączone przez procesor [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] WPF w połączeniu z konwerterem typów dla <xref:System.Windows.DependencyProperty>.</span><span class="sxs-lookup"><span data-stu-id="a9c50-111">But this is not a true <xref:System.Windows.PropertyPath>; instead it is a qualified *owner.property* string format usage that is enabled by the WPF [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] processor in combination with the type converter for <xref:System.Windows.DependencyProperty>.</span></span>

<a name="databinding_s"></a>

## <a name="propertypath-for-objects-in-data-binding"></a><span data-ttu-id="a9c50-112">PropertyPath dla obiektów w powiązaniu danych</span><span class="sxs-lookup"><span data-stu-id="a9c50-112">PropertyPath for Objects in Data Binding</span></span>

<span data-ttu-id="a9c50-113">Powiązanie danych to [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] funkcja, za pomocą której można powiązać z wartością docelową dowolnej właściwości zależności.</span><span class="sxs-lookup"><span data-stu-id="a9c50-113">Data binding is a [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] feature whereby you can bind to the target value of any dependency property.</span></span> <span data-ttu-id="a9c50-114">Jednak Źródło takich powiązań danych nie musi być właściwością zależności; może to być dowolny typ właściwości, który jest rozpoznawany przez odpowiedniego dostawcę danych.</span><span class="sxs-lookup"><span data-stu-id="a9c50-114">However, the source of such a data binding need not be a dependency property; it can be any property type that is recognized by the applicable data provider.</span></span> <span data-ttu-id="a9c50-115">Ścieżki właściwości są szczególnie stosowane dla programu <xref:System.Windows.Data.ObjectDataProvider>, który jest używany do uzyskiwania źródeł powiązań z obiektów środowiska uruchomieniowego języka wspólnego (CLR) i ich właściwości.</span><span class="sxs-lookup"><span data-stu-id="a9c50-115">Property paths are particularly used for the <xref:System.Windows.Data.ObjectDataProvider>, which is used for obtaining binding sources from common language runtime (CLR) objects and their properties.</span></span>

<span data-ttu-id="a9c50-116">Należy zauważyć, że powiązanie danych z XML nie <xref:System.Windows.PropertyPath>używa, ponieważ nie jest używane <xref:System.Windows.Data.Binding.Path%2A> w <xref:System.Windows.Data.Binding>.</span><span class="sxs-lookup"><span data-stu-id="a9c50-116">Note that data binding to XML does not use <xref:System.Windows.PropertyPath>, because it does not use <xref:System.Windows.Data.Binding.Path%2A> in the <xref:System.Windows.Data.Binding>.</span></span> <span data-ttu-id="a9c50-117">Zamiast tego należy użyć <xref:System.Windows.Data.Binding.XPath%2A> i określić prawidłową składnię XPath w Document Object Model XML (dom) danych.</span><span class="sxs-lookup"><span data-stu-id="a9c50-117">Instead, you use <xref:System.Windows.Data.Binding.XPath%2A> and specify valid XPath syntax into the XML Document Object Model (DOM) of the data.</span></span> <span data-ttu-id="a9c50-118"><xref:System.Windows.Data.Binding.XPath%2A>jest również określony jako ciąg, ale nie jest tutaj udokumentowany. Zobacz [powiąż z danymi XML przy użyciu XmlDataProvider i zapytań XPath](../data/how-to-bind-to-xml-data-using-an-xmldataprovider-and-xpath-queries.md).</span><span class="sxs-lookup"><span data-stu-id="a9c50-118"><xref:System.Windows.Data.Binding.XPath%2A> is also specified as a string, but is not documented here; see [Bind to XML Data Using an XMLDataProvider and XPath Queries](../data/how-to-bind-to-xml-data-using-an-xmldataprovider-and-xpath-queries.md).</span></span>

<span data-ttu-id="a9c50-119">Kluczem do interpretacji ścieżek właściwości w powiązaniu danych jest możliwość kierowania powiązania do pojedynczej wartości właściwości lub można utworzyć powiązanie z właściwościami docelowymi, które pobierają listy lub kolekcje.</span><span class="sxs-lookup"><span data-stu-id="a9c50-119">A key to understanding property paths in data binding is that you can target the binding to an individual property value, or you can instead bind to target properties that take lists or collections.</span></span> <span data-ttu-id="a9c50-120">Jeśli kolekcje są wiążące, na potrzeby powiązania wystąpienia <xref:System.Windows.Controls.ListBox> a, które zostanie rozwinięte w zależności od liczby elementów danych w kolekcji, ścieżka właściwości powinna odwoływać się do obiektu kolekcji, a nie do poszczególnych elementów kolekcji.</span><span class="sxs-lookup"><span data-stu-id="a9c50-120">If you are binding collections, for instance binding a <xref:System.Windows.Controls.ListBox> that will expand depending on how many data items are in the collection, then your property path should reference the collection object, not individual collection items.</span></span> <span data-ttu-id="a9c50-121">Aparat powiązań danych będzie pasował do kolekcji używanej jako źródło danych dla typu elementu docelowego powiązania automatycznie, co skutkuje zachowaniem, takim jak wypełnienie <xref:System.Windows.Controls.ListBox> tabeli Items.</span><span class="sxs-lookup"><span data-stu-id="a9c50-121">The data binding engine will match the collection used as the data source to the type of the binding target automatically, resulting in behavior such as populating a <xref:System.Windows.Controls.ListBox> with an items array.</span></span>

<a name="singlecurrent"></a>

### <a name="single-property-on-the-immediate-object-as-data-context"></a><span data-ttu-id="a9c50-122">Pojedyncza właściwość w obiekcie bezpośrednim jako kontekst danych</span><span class="sxs-lookup"><span data-stu-id="a9c50-122">Single Property on the Immediate Object as Data Context</span></span>

```xml
<Binding Path="propertyName" ... />
```

<span data-ttu-id="a9c50-123">Funkcja *PropertyName* musi zostać rozpoznana jako nazwa właściwości, która znajduje się w bieżącej <xref:System.Windows.FrameworkElement.DataContext%2A> wartości <xref:System.Windows.Data.Binding.Path%2A> użycia.</span><span class="sxs-lookup"><span data-stu-id="a9c50-123">*propertyName* must resolve to be the name of a property that is in the current <xref:System.Windows.FrameworkElement.DataContext%2A> for a <xref:System.Windows.Data.Binding.Path%2A> usage.</span></span> <span data-ttu-id="a9c50-124">Jeśli powiązanie aktualizuje źródło, ta właściwość musi mieć wartość odczyt/zapis, a obiekt źródłowy musi być modyfikowalny.</span><span class="sxs-lookup"><span data-stu-id="a9c50-124">If your binding updates the source, that property must be read/write and the source object must be mutable.</span></span>

<a name="singleindex"></a>

### <a name="single-indexer-on-the-immediate-object-as-data-context"></a><span data-ttu-id="a9c50-125">Pojedynczy indeksator na obiekcie bezpośrednim jako kontekst danych</span><span class="sxs-lookup"><span data-stu-id="a9c50-125">Single Indexer on the Immediate Object as Data Context</span></span>

```xml
<Binding Path="[key]" ... />
```

<span data-ttu-id="a9c50-126">`key`musi być to indeks z określonym typem w słowniku lub w tabeli skrótów albo indeksem liczb całkowitych tablicy.</span><span class="sxs-lookup"><span data-stu-id="a9c50-126">`key` must be either the typed index to a dictionary or hash table, or the integer index of an array.</span></span> <span data-ttu-id="a9c50-127">Ponadto wartość klucza musi być typem, który jest bezpośrednio powiązany z właściwością, w której jest stosowany.</span><span class="sxs-lookup"><span data-stu-id="a9c50-127">Also, the value of the key must be a type that is directly bindable to the property where it is applied.</span></span> <span data-ttu-id="a9c50-128">Na przykład można użyć tabeli skrótów zawierającej klucze ciągów i wartości ciągu, aby powiązać z tekstem dla <xref:System.Windows.Controls.TextBox>.</span><span class="sxs-lookup"><span data-stu-id="a9c50-128">For instance, a hash table that contains string keys and string values can be used this way to bind to Text for a <xref:System.Windows.Controls.TextBox>.</span></span> <span data-ttu-id="a9c50-129">Lub, jeśli klucz wskazuje kolekcję lub indeks, można użyć tej składni do powiązania z właściwością kolekcji docelowej.</span><span class="sxs-lookup"><span data-stu-id="a9c50-129">Or, if the key points to a collection or subindex, you could use this syntax to bind to a target collection property.</span></span> <span data-ttu-id="a9c50-130">W przeciwnym razie należy odwołać się do określonej właściwości za pomocą składni takiej jak `<Binding Path="[key].propertyName" .../>`.</span><span class="sxs-lookup"><span data-stu-id="a9c50-130">Otherwise, you need to reference a specific property, through a syntax such as `<Binding Path="[key].propertyName" .../>`.</span></span>

<span data-ttu-id="a9c50-131">W razie potrzeby można określić typ indeksu.</span><span class="sxs-lookup"><span data-stu-id="a9c50-131">You can specify the type of the index if necessary.</span></span> <span data-ttu-id="a9c50-132">Aby uzyskać szczegółowe informacje na temat tego aspektu ścieżki właściwości indeksowanej, zobacz <xref:System.Windows.Data.Binding.Path%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="a9c50-132">For details on this aspect of an indexed property path, see <xref:System.Windows.Data.Binding.Path%2A?displayProperty=nameWithType>.</span></span>

<a name="multipleindirect"></a>

### <a name="multiple-property-indirect-property-targeting"></a><span data-ttu-id="a9c50-133">Wiele właściwości (Określanie wartości docelowej właściwości pośredniej)</span><span class="sxs-lookup"><span data-stu-id="a9c50-133">Multiple Property (Indirect Property Targeting)</span></span>

```xml
<Binding Path="propertyName.propertyName2" ... />
```

<span data-ttu-id="a9c50-134">`propertyName`musi być rozpoznawana jako nazwa właściwości, która jest aktualna <xref:System.Windows.FrameworkElement.DataContext%2A>.</span><span class="sxs-lookup"><span data-stu-id="a9c50-134">`propertyName` must resolve to be the name of a property that is the current <xref:System.Windows.FrameworkElement.DataContext%2A>.</span></span> <span data-ttu-id="a9c50-135">Właściwości `propertyName` ścieżki i `propertyName2` mogą być wszelkimi właściwościami, które istnieją w relacji, `propertyName2` gdzie jest właściwością, która istnieje w typie, który jest wartością `propertyName`.</span><span class="sxs-lookup"><span data-stu-id="a9c50-135">The path properties `propertyName` and `propertyName2` can be any properties that exist in a relationship, where `propertyName2` is a property that exists on the type that is the value of `propertyName`.</span></span>

<a name="singleattached"></a>

### <a name="single-property-attached-or-otherwise-type-qualified"></a><span data-ttu-id="a9c50-136">Pojedyncza właściwość, dołączona lub w inny sposób kwalifikowana dla typu</span><span class="sxs-lookup"><span data-stu-id="a9c50-136">Single Property, Attached or Otherwise Type-Qualified</span></span>

```xml
<object property="(ownerType.propertyName)" ... />
```

<span data-ttu-id="a9c50-137">Nawiasy wskazują, że właściwość w a <xref:System.Windows.PropertyPath> powinna być skonstruowana przy użyciu częściowej kwalifikacji.</span><span class="sxs-lookup"><span data-stu-id="a9c50-137">The parentheses indicate that this property in a <xref:System.Windows.PropertyPath> should be constructed using a partial qualification.</span></span> <span data-ttu-id="a9c50-138">Może użyć przestrzeni nazw XML, aby znaleźć typ z odpowiednim mapowaniem.</span><span class="sxs-lookup"><span data-stu-id="a9c50-138">It can use an XML namespace to find the type with an appropriate mapping.</span></span> <span data-ttu-id="a9c50-139">Typy `ownerType` wyszukiwania, do których [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] procesor ma dostęp, za pomocą <xref:System.Windows.Markup.XmlnsDefinitionAttribute> deklaracji w poszczególnych zestawach.</span><span class="sxs-lookup"><span data-stu-id="a9c50-139">The `ownerType` searches types that a [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] processor has access to, through the <xref:System.Windows.Markup.XmlnsDefinitionAttribute> declarations in each assembly.</span></span> <span data-ttu-id="a9c50-140">Większość aplikacji ma domyślną przestrzeń nazw XML zamapowana na `http://schemas.microsoft.com/winfx/2006/xaml/presentation` przestrzeń nazw, więc prefiks jest zwykle niezbędny tylko w przypadku typów niestandardowych lub typów poza tą przestrzenią nazw.</span><span class="sxs-lookup"><span data-stu-id="a9c50-140">Most applications have the default XML namespace mapped to the `http://schemas.microsoft.com/winfx/2006/xaml/presentation` namespace, so a prefix is usually only necessary for custom types or types otherwise outside that namespace.</span></span>  <span data-ttu-id="a9c50-141">`propertyName`musi być rozpoznawana jako nazwa właściwości istniejącej w `ownerType`.</span><span class="sxs-lookup"><span data-stu-id="a9c50-141">`propertyName` must resolve to be the name of a property existing on the `ownerType`.</span></span> <span data-ttu-id="a9c50-142">Ta składnia jest zwykle używana w jednym z następujących przypadków:</span><span class="sxs-lookup"><span data-stu-id="a9c50-142">This syntax is generally used for one of the following cases:</span></span>

- <span data-ttu-id="a9c50-143">Ścieżka jest określona w [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] , która znajduje się w stylu lub szablonie, który nie ma określonego typu docelowego.</span><span class="sxs-lookup"><span data-stu-id="a9c50-143">The path is specified in [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] that is in a style or template that does not have a specified Target Type.</span></span> <span data-ttu-id="a9c50-144">Kwalifikowane użycie zwykle nie jest prawidłowe w przypadku przypadków innych niż to, ponieważ w przypadku braku szablonów, właściwość istnieje w wystąpieniu, a nie w typie.</span><span class="sxs-lookup"><span data-stu-id="a9c50-144">A qualified usage is generally not valid for cases other than this, because in non-style, non-template cases, the property exists on an instance, not a type.</span></span>

- <span data-ttu-id="a9c50-145">Właściwość jest dołączoną właściwością.</span><span class="sxs-lookup"><span data-stu-id="a9c50-145">The property is an attached property.</span></span>

- <span data-ttu-id="a9c50-146">Tworzysz powiązanie z właściwością statyczną.</span><span class="sxs-lookup"><span data-stu-id="a9c50-146">You are binding to a static property.</span></span>

<span data-ttu-id="a9c50-147">Aby można było użyć jako elementu docelowego scenorysu, `propertyName` właściwość określona jako <xref:System.Windows.DependencyProperty>musi być.</span><span class="sxs-lookup"><span data-stu-id="a9c50-147">For use as storyboard target, the property specified as `propertyName` must be a <xref:System.Windows.DependencyProperty>.</span></span>

<a name="sourcetraversal"></a>

### <a name="source-traversal-binding-to-hierarchies-of-collections"></a><span data-ttu-id="a9c50-148">Przechodzenie źródłowe (powiązanie z hierarchiami kolekcji)</span><span class="sxs-lookup"><span data-stu-id="a9c50-148">Source Traversal (Binding to Hierarchies of Collections)</span></span>

```xml
<object Path="propertyName/propertyNameX" ... />
```

<span data-ttu-id="a9c50-149">Składnia/w tej składni jest używana do nawigowania w obrębie hierarchicznego obiektu źródła danych, a wiele kroków do hierarchii z kolejnymi/znakami są obsługiwane.</span><span class="sxs-lookup"><span data-stu-id="a9c50-149">The / in this syntax is used to navigate within a hierarchical data source object, and multiple steps into the hierarchy with successive / characters are supported.</span></span> <span data-ttu-id="a9c50-150">Źródłowe konta przechodzenia dla bieżącego położenia wskaźnika rekordu, które są określane przez synchronizację danych z interfejsem użytkownika widoku.</span><span class="sxs-lookup"><span data-stu-id="a9c50-150">The source traversal accounts for the current record pointer position, which is determined by synchronizing the data with the UI of its view.</span></span> <span data-ttu-id="a9c50-151">Aby uzyskać szczegółowe informacje na temat powiązania z obiektami hierarchicznych źródeł danych i koncepcji bieżącego wskaźnika rekordu w powiązaniu danych, zobacz [Korzystanie ze wzorca wzorzec-szczegóły z danymi hierarchicznymi lub z](../data/how-to-use-the-master-detail-pattern-with-hierarchical-data.md) [omówieniem powiązań danych](../../../desktop-wpf/data/data-binding-overview.md).</span><span class="sxs-lookup"><span data-stu-id="a9c50-151">For details on binding with hierarchical data source objects, and the concept of current record pointer in data binding, see [Use the Master-Detail Pattern with Hierarchical Data](../data/how-to-use-the-master-detail-pattern-with-hierarchical-data.md) or [Data Binding Overview](../../../desktop-wpf/data/data-binding-overview.md).</span></span>

> [!NOTE]
> <span data-ttu-id="a9c50-152">Niemniej ta składnia przypomina wyrażenie XPath.</span><span class="sxs-lookup"><span data-stu-id="a9c50-152">Superficially, this syntax resembles XPath.</span></span> <span data-ttu-id="a9c50-153">Prawdziwe wyrażenie XPath dla powiązania ze źródłem danych XML nie jest używane jako <xref:System.Windows.Data.Binding.Path%2A> wartość i powinno być używane dla wzajemnie wykluczających <xref:System.Windows.Data.Binding.XPath%2A> się właściwości.</span><span class="sxs-lookup"><span data-stu-id="a9c50-153">A true XPath expression for binding to an XML data source is not used as a <xref:System.Windows.Data.Binding.Path%2A> value and should instead be used for the mutually exclusive <xref:System.Windows.Data.Binding.XPath%2A> property.</span></span>

### <a name="collection-views"></a><span data-ttu-id="a9c50-154">Widoki kolekcji</span><span class="sxs-lookup"><span data-stu-id="a9c50-154">Collection Views</span></span>

<span data-ttu-id="a9c50-155">Aby odwołać się do widoku nazwanego kolekcji, poprzedź nazwę widoku kolekcji znakiem skrótu`#`().</span><span class="sxs-lookup"><span data-stu-id="a9c50-155">To reference a named collection view, prefix the collection view name with the hash character (`#`).</span></span>

### <a name="current-record-pointer"></a><span data-ttu-id="a9c50-156">Bieżący wskaźnik rekordu</span><span class="sxs-lookup"><span data-stu-id="a9c50-156">Current Record Pointer</span></span>

<span data-ttu-id="a9c50-157">Aby odwołać się do bieżącego wskaźnika rekordu dla scenariusza widok kolekcji lub wzorzec powiązania danych szczegółów, należy uruchomić ciąg ścieżki z ukośnikiem (`/`).</span><span class="sxs-lookup"><span data-stu-id="a9c50-157">To reference the current record pointer for a collection view or master detail data binding scenario, start the path string with a forward slash (`/`).</span></span> <span data-ttu-id="a9c50-158">Wszystkie ścieżki poprzedzające ukośnik są przenoszone od bieżącego wskaźnika rekordu.</span><span class="sxs-lookup"><span data-stu-id="a9c50-158">Any path past the forward slash is traversed starting from the current record pointer.</span></span>

### <a name="multiple-indexers"></a><span data-ttu-id="a9c50-159">Wiele indeksatorów</span><span class="sxs-lookup"><span data-stu-id="a9c50-159">Multiple Indexers</span></span>

```xaml
<object Path="[index1,index2...]" ... />
```

<span data-ttu-id="a9c50-160">lub</span><span class="sxs-lookup"><span data-stu-id="a9c50-160">or</span></span>

```xaml
<object Path="propertyName[index,index2...]" ... />
```

<span data-ttu-id="a9c50-161">Jeśli dany obiekt obsługuje wiele indeksatorów, te indeksatory mogą być określone w kolejności podobnej do składni odwołującej się do tablicy.</span><span class="sxs-lookup"><span data-stu-id="a9c50-161">If a given object supports multiple indexers, those indexers can be specified in order, similar to an array referencing syntax.</span></span> <span data-ttu-id="a9c50-162">Dany obiekt może być bieżącym kontekstem lub wartością właściwości, która zawiera obiekt z wieloma indeksami.</span><span class="sxs-lookup"><span data-stu-id="a9c50-162">The object in question can be either the current context or the value of a property that contains a multiple index object.</span></span>

<span data-ttu-id="a9c50-163">Domyślnie wartości indeksatora są wpisywane przy użyciu cech obiektu bazowego.</span><span class="sxs-lookup"><span data-stu-id="a9c50-163">By default, the indexer values are typed by using the characteristics of the underlying object.</span></span> <span data-ttu-id="a9c50-164">W razie potrzeby można określić typ indeksu.</span><span class="sxs-lookup"><span data-stu-id="a9c50-164">You can specify the type of the index if necessary.</span></span> <span data-ttu-id="a9c50-165">Aby uzyskać szczegółowe informacje na temat wpisywania indeksatorów, zobacz <xref:System.Windows.Data.Binding.Path%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="a9c50-165">For details on typing the indexers, see <xref:System.Windows.Data.Binding.Path%2A?displayProperty=nameWithType>.</span></span>

<a name="mixing"></a>

### <a name="mixing-syntaxes"></a><span data-ttu-id="a9c50-166">Mieszanie składni</span><span class="sxs-lookup"><span data-stu-id="a9c50-166">Mixing Syntaxes</span></span>

<span data-ttu-id="a9c50-167">Każda składnia pokazana powyżej może zostać przeplatana.</span><span class="sxs-lookup"><span data-stu-id="a9c50-167">Each of the syntaxes shown above can be interspersed.</span></span> <span data-ttu-id="a9c50-168">Na przykład, poniżej można utworzyć ścieżkę właściwości do koloru na określonej x, y `ColorGrid` właściwości, która zawiera tablicę siatki pikseli <xref:System.Windows.Media.SolidColorBrush> obiektów:</span><span class="sxs-lookup"><span data-stu-id="a9c50-168">For instance, the following is an example that creates a property path to the color at a particular x,y of a `ColorGrid` property that contains a pixel grid array of <xref:System.Windows.Media.SolidColorBrush> objects:</span></span>

```xml
<Rectangle Fill="{Binding ColorGrid[20,30].SolidColorBrushResult}" ... />
```

### <a name="escapes-for-property-path-strings"></a><span data-ttu-id="a9c50-169">Ucieczki ciągów ścieżki właściwości</span><span class="sxs-lookup"><span data-stu-id="a9c50-169">Escapes for Property Path Strings</span></span>

<span data-ttu-id="a9c50-170">W przypadku niektórych obiektów firmy można napotkać przypadek, w którym ciąg ścieżki właściwości wymaga sekwencji ucieczki w celu poprawnego przeanalizowania.</span><span class="sxs-lookup"><span data-stu-id="a9c50-170">For certain business objects, you might encounter a case where the property path string requires an escape sequence in order to parse correctly.</span></span> <span data-ttu-id="a9c50-171">Konieczność ucieczki powinna być rzadki, ponieważ wiele z tych znaków ma podobne problemy z interakcją nazewnictwa w językach, które zwykle są używane do definiowania obiektu biznesowego.</span><span class="sxs-lookup"><span data-stu-id="a9c50-171">The need to escape should be rare, because many of these characters have similar naming-interaction issues in languages that would typically be used to define the business object.</span></span>

- <span data-ttu-id="a9c50-172">Wewnątrz indeksatorów ([]), znak karetki (^) wyprowadza następny znak.</span><span class="sxs-lookup"><span data-stu-id="a9c50-172">Inside indexers ([ ]), the caret character (^) escapes the next character.</span></span>

- <span data-ttu-id="a9c50-173">Należy wyjść z określonych znaków (przy użyciu jednostek XML), które są specyficzne dla definicji języka XML.</span><span class="sxs-lookup"><span data-stu-id="a9c50-173">You must escape (using XML entities) certain characters that are special to the XML language definition.</span></span> <span data-ttu-id="a9c50-174">Użyj `&` do ucieczki znaku "&".</span><span class="sxs-lookup"><span data-stu-id="a9c50-174">Use `&` to escape the character "&".</span></span> <span data-ttu-id="a9c50-175">Użyj `>` do ucieczki znacznika końcowego ">".</span><span class="sxs-lookup"><span data-stu-id="a9c50-175">Use `>` to escape the end tag ">".</span></span>

- <span data-ttu-id="a9c50-176">Należy wypróbować (przy użyciu `\`ukośnika odwrotnego), które są specyficzne dla zachowań analizatora XAML WPF do przetwarzania rozszerzenia znaczników.</span><span class="sxs-lookup"><span data-stu-id="a9c50-176">You must escape (using backslash `\`) characters that are special to the WPF XAML parser behavior for processing a markup extension.</span></span>

  - <span data-ttu-id="a9c50-177">Ukośnik odwrotny`\`() jest samym znakiem ucieczki.</span><span class="sxs-lookup"><span data-stu-id="a9c50-177">Backslash (`\`) is the escape character itself.</span></span>

  - <span data-ttu-id="a9c50-178">Znak równości (`=`) oddziela nazwę właściwości od wartości właściwości.</span><span class="sxs-lookup"><span data-stu-id="a9c50-178">The equal sign (`=`) separates property name from property value.</span></span>

  - <span data-ttu-id="a9c50-179">Przecinek`,`() oddziela właściwości.</span><span class="sxs-lookup"><span data-stu-id="a9c50-179">Comma (`,`) separates properties.</span></span>

  - <span data-ttu-id="a9c50-180">Prawy nawias klamrowy (`}`) jest końcem rozszerzenia znacznika.</span><span class="sxs-lookup"><span data-stu-id="a9c50-180">The right curly brace (`}`) is the end of a markup extension.</span></span>

> [!NOTE]
> <span data-ttu-id="a9c50-181">Technicznie te wyjścia dla ścieżki właściwości scenorysu również są zwykle przenoszone przez modele obiektów dla istniejących obiektów WPF, a ucieczki nie powinny być potrzebne.</span><span class="sxs-lookup"><span data-stu-id="a9c50-181">Technically, these escapes work for a storyboard property path also, but you are usually traversing object models for existing WPF objects, and escaping should be unnecessary.</span></span>

<a name="databinding_sa"></a>

## <a name="propertypath-for-animation-targets"></a><span data-ttu-id="a9c50-182">PropertyPath dla celów animacji</span><span class="sxs-lookup"><span data-stu-id="a9c50-182">PropertyPath for Animation Targets</span></span>

<span data-ttu-id="a9c50-183">Właściwość Target animacji musi być właściwością zależności, która przyjmuje albo <xref:System.Windows.Freezable> lub typ pierwotny.</span><span class="sxs-lookup"><span data-stu-id="a9c50-183">The target property of an animation must be a dependency property that takes either a <xref:System.Windows.Freezable> or a primitive type.</span></span> <span data-ttu-id="a9c50-184">Jednak właściwość target typu i ostateczna Właściwość animowana może istnieć w różnych obiektach.</span><span class="sxs-lookup"><span data-stu-id="a9c50-184">However, the targeted property on a type and the eventual animated property can exist on different objects.</span></span> <span data-ttu-id="a9c50-185">W przypadku animacji ścieżka właściwości jest używana do definiowania połączenia między właściwością nazwanego obiektu docelowego animacji a zamierzoną właściwością animacji docelowej, przez przechodzenie między relacjami obiektu i właściwości w wartościach właściwości.</span><span class="sxs-lookup"><span data-stu-id="a9c50-185">For animations, a property path is used to define the connection between the named animation target object's property and the intended target animation property, by traversing object-property relationships in the property values.</span></span>

<a name="general"></a>

### <a name="general-object-property-considerations-for-animations"></a><span data-ttu-id="a9c50-186">Ogólne zagadnienia dotyczące właściwości obiektów dla animacji</span><span class="sxs-lookup"><span data-stu-id="a9c50-186">General Object-Property Considerations for Animations</span></span>

<span data-ttu-id="a9c50-187">Aby uzyskać więcej informacji na temat ogólnych pojęć dotyczących animacji, zobacz Omówienie [scenorysów](../graphics-multimedia/storyboards-overview.md) i Omówienie [animacji](../graphics-multimedia/animation-overview.md).</span><span class="sxs-lookup"><span data-stu-id="a9c50-187">For more information on animation concepts in general, see [Storyboards Overview](../graphics-multimedia/storyboards-overview.md) and [Animation Overview](../graphics-multimedia/animation-overview.md).</span></span>

<span data-ttu-id="a9c50-188">Typ wartości lub animowana właściwość musi być <xref:System.Windows.Freezable> typem lub elementem pierwotnym.</span><span class="sxs-lookup"><span data-stu-id="a9c50-188">The value type or the property being animated must be either a <xref:System.Windows.Freezable> type or a primitive.</span></span> <span data-ttu-id="a9c50-189">Właściwość rozpoczynająca ścieżkę musi być rozpoznawana jako nazwa właściwości zależności, która istnieje w określonym <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> typie.</span><span class="sxs-lookup"><span data-stu-id="a9c50-189">The property that starts the path must resolve to be the name of a dependency property that exists on the specified <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> type.</span></span>

<span data-ttu-id="a9c50-190">Aby można było obsłużyć klonowanie dla animacji <xref:System.Windows.Freezable> , która jest już zamrożona, obiekt <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> określony przez musi <xref:System.Windows.FrameworkElement> być <xref:System.Windows.FrameworkContentElement> klasą pochodną lub.</span><span class="sxs-lookup"><span data-stu-id="a9c50-190">In order to support cloning for animating a <xref:System.Windows.Freezable> that is already frozen, the object specified by <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> must be a <xref:System.Windows.FrameworkElement> or <xref:System.Windows.FrameworkContentElement> derived class.</span></span>

<a name="singlestepanim"></a>

### <a name="single-property-on-the-target-object"></a><span data-ttu-id="a9c50-191">Pojedyncza właściwość w obiekcie docelowym</span><span class="sxs-lookup"><span data-stu-id="a9c50-191">Single Property on the Target Object</span></span>

```xml
<animation Storyboard.TargetProperty="propertyName" ... />
```

<span data-ttu-id="a9c50-192">`propertyName`należy rozpoznać jako nazwę właściwości zależności, która istnieje w określonym <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> typie.</span><span class="sxs-lookup"><span data-stu-id="a9c50-192">`propertyName` must resolve to be the name of a dependency property that exists on the specified <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> type.</span></span>

<a name="indirectanim"></a>

### <a name="indirect-property-targeting"></a><span data-ttu-id="a9c50-193">Określanie wartości docelowej właściwości pośredniej</span><span class="sxs-lookup"><span data-stu-id="a9c50-193">Indirect Property Targeting</span></span>

```xml
<animation Storyboard.TargetProperty="propertyName.propertyName2" ... />
```

<span data-ttu-id="a9c50-194">`propertyName`musi być właściwością, która jest albo <xref:System.Windows.Freezable> typem wartości, albo elementem pierwotnym, który istnieje w <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> określonym typie.</span><span class="sxs-lookup"><span data-stu-id="a9c50-194">`propertyName` must be a property that is either a <xref:System.Windows.Freezable> value type or a primitive, which exists on the specified <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> type.</span></span>

<span data-ttu-id="a9c50-195">`propertyName2`musi być nazwą właściwości zależności, która istnieje w obiekcie, który jest wartością `propertyName`.</span><span class="sxs-lookup"><span data-stu-id="a9c50-195">`propertyName2` must be the name of a dependency property that exists on the object that is the value of `propertyName`.</span></span> <span data-ttu-id="a9c50-196">Innymi słowy, `propertyName2` musi istnieć jako właściwość zależności w typie, który jest `propertyName` <xref:System.Windows.DependencyProperty.PropertyType%2A>.</span><span class="sxs-lookup"><span data-stu-id="a9c50-196">In other words, `propertyName2` must exist as a dependency property on the type that is the `propertyName` <xref:System.Windows.DependencyProperty.PropertyType%2A>.</span></span>

<span data-ttu-id="a9c50-197">Pośredni cel animacji jest konieczny ze względu na zastosowane style i szablony.</span><span class="sxs-lookup"><span data-stu-id="a9c50-197">Indirect targeting of animations is necessary because of applied styles and templates.</span></span> <span data-ttu-id="a9c50-198">Aby można było utworzyć animację, musisz być <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> obiektem docelowym, a nazwa jest określana przez [x:Name](../../../desktop-wpf/xaml-services/xname-directive.md) lub. <xref:System.Windows.FrameworkElement.Name%2A></span><span class="sxs-lookup"><span data-stu-id="a9c50-198">In order to target an animation, you need a <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> on a target object, and that name is established by [x:Name](../../../desktop-wpf/xaml-services/xname-directive.md) or <xref:System.Windows.FrameworkElement.Name%2A>.</span></span> <span data-ttu-id="a9c50-199">Chociaż elementy szablonu i stylu mogą także mieć nazwy, te nazwy są prawidłowe tylko w namescope stylu i szablonu.</span><span class="sxs-lookup"><span data-stu-id="a9c50-199">Although template and style elements also can have names, those names are only valid within the namescope of the style and template.</span></span> <span data-ttu-id="a9c50-200">(Jeśli szablony i style współużytkują Zakresy nazw WPF z znacznikiem aplikacji, nazwy nie mogły być unikatowe.</span><span class="sxs-lookup"><span data-stu-id="a9c50-200">(If templates and styles did share namescopes with application markup, names couldn't be unique.</span></span> <span data-ttu-id="a9c50-201">Style i szablony są dosłownie współużytkowane przez wystąpienia i perpetuate zduplikowane nazwy.) W takim przypadku, jeśli poszczególne właściwości elementu, który można animować, pochodzą z stylu lub szablonu, należy zacząć od wystąpienia nazwanego elementu, który nie jest z szablonu stylu, a następnie kierować do drzewa wizualnego styl lub szablon, aby dotrzeć do właściwości, którą chcesz animować.</span><span class="sxs-lookup"><span data-stu-id="a9c50-201">The styles and templates are literally shared between instances and would perpetuate duplicate names.) Thus, if the individual properties of an element that you might wish to animate came from a style or template, you need to start with a named element instance that is not from a style template, and then target into the style or template visual tree to arrive at the property you wish to animate.</span></span>

<span data-ttu-id="a9c50-202">Na przykład <xref:System.Windows.Controls.Panel.Background%2A> Właściwość a <xref:System.Windows.Controls.Panel> jest kompletna <xref:System.Windows.Media.Brush> (faktycznie a <xref:System.Windows.Media.SolidColorBrush>), która pochodzi z szablonu motywu.</span><span class="sxs-lookup"><span data-stu-id="a9c50-202">For instance, the <xref:System.Windows.Controls.Panel.Background%2A> property of a <xref:System.Windows.Controls.Panel> is a complete <xref:System.Windows.Media.Brush> (actually a <xref:System.Windows.Media.SolidColorBrush>) that came from a theme template.</span></span> <span data-ttu-id="a9c50-203">Aby animować <xref:System.Windows.Media.Brush> całkowicie, należy mieć BrushAnimation (prawdopodobnie jeden dla każdego <xref:System.Windows.Media.Brush> typu) i nie istnieje taki typ.</span><span class="sxs-lookup"><span data-stu-id="a9c50-203">To animate a <xref:System.Windows.Media.Brush> completely, there would need to be a BrushAnimation (probably one for every <xref:System.Windows.Media.Brush> type) and there is no such type.</span></span> <span data-ttu-id="a9c50-204">Aby animować pędzla, należy zamiast tego animować właściwości określonego <xref:System.Windows.Media.Brush> typu.</span><span class="sxs-lookup"><span data-stu-id="a9c50-204">To animate a Brush, you instead animate properties of a particular <xref:System.Windows.Media.Brush> type.</span></span> <span data-ttu-id="a9c50-205">Aby to zrobić, musisz <xref:System.Windows.Media.SolidColorBrush> uzyskać od <xref:System.Windows.Media.SolidColorBrush.Color%2A> <xref:System.Windows.Media.Animation.ColorAnimation> użytkownika.</span><span class="sxs-lookup"><span data-stu-id="a9c50-205">You need to get from <xref:System.Windows.Media.SolidColorBrush> to its <xref:System.Windows.Media.SolidColorBrush.Color%2A> to apply a <xref:System.Windows.Media.Animation.ColorAnimation> there.</span></span> <span data-ttu-id="a9c50-206">Ścieżka właściwości dla tego przykładu `Background.Color`.</span><span class="sxs-lookup"><span data-stu-id="a9c50-206">The property path for this example would be `Background.Color`.</span></span>

<a name="attachedanim"></a>

### <a name="attached-properties"></a><span data-ttu-id="a9c50-207">Dołączone właściwości</span><span class="sxs-lookup"><span data-stu-id="a9c50-207">Attached Properties</span></span>

```xml
<animation Storyboard.TargetProperty="(ownerType.propertyName)" ... />
```

<span data-ttu-id="a9c50-208">Nawiasy wskazują, że właściwość w a <xref:System.Windows.PropertyPath> powinna być skonstruowana przy użyciu częściowej kwalifikacji.</span><span class="sxs-lookup"><span data-stu-id="a9c50-208">The parentheses indicate that this property in a <xref:System.Windows.PropertyPath> should be constructed using a partial qualification.</span></span> <span data-ttu-id="a9c50-209">Można użyć przestrzeni nazw XML, aby znaleźć typ.</span><span class="sxs-lookup"><span data-stu-id="a9c50-209">It can use an XML namespace to find the type.</span></span> <span data-ttu-id="a9c50-210">Typy `ownerType` wyszukiwania, do których [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] procesor ma dostęp, za pomocą <xref:System.Windows.Markup.XmlnsDefinitionAttribute> deklaracji w poszczególnych zestawach.</span><span class="sxs-lookup"><span data-stu-id="a9c50-210">The `ownerType` searches types that a [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] processor has access to, through the <xref:System.Windows.Markup.XmlnsDefinitionAttribute> declarations in each assembly.</span></span> <span data-ttu-id="a9c50-211">Większość aplikacji ma domyślną przestrzeń nazw XML zamapowana na `http://schemas.microsoft.com/winfx/2006/xaml/presentation` przestrzeń nazw, więc prefiks jest zwykle niezbędny tylko w przypadku typów niestandardowych lub typów poza tą przestrzenią nazw.</span><span class="sxs-lookup"><span data-stu-id="a9c50-211">Most applications have the default XML namespace mapped to the `http://schemas.microsoft.com/winfx/2006/xaml/presentation` namespace, so a prefix is usually only necessary for custom types or types otherwise outside that namespace.</span></span> <span data-ttu-id="a9c50-212">`propertyName`musi być rozpoznawana jako nazwa właściwości istniejącej w `ownerType`.</span><span class="sxs-lookup"><span data-stu-id="a9c50-212">`propertyName` must resolve to be the name of a property existing on the `ownerType`.</span></span> <span data-ttu-id="a9c50-213">Właściwość określona jako `propertyName` musi być <xref:System.Windows.DependencyProperty>.</span><span class="sxs-lookup"><span data-stu-id="a9c50-213">The property specified as `propertyName` must be a <xref:System.Windows.DependencyProperty>.</span></span> <span data-ttu-id="a9c50-214">(Wszystkie [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] dołączone właściwości są implementowane jako właściwości zależności, więc ten problem dotyczy tylko niestandardowych dołączanych właściwości).</span><span class="sxs-lookup"><span data-stu-id="a9c50-214">(All [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] attached properties are implemented as dependency properties, so this issue is only of concern for custom attached properties.)</span></span>

<a name="indexanim"></a>

### <a name="indexers"></a><span data-ttu-id="a9c50-215">Indexers (Indeksatory)</span><span class="sxs-lookup"><span data-stu-id="a9c50-215">Indexers</span></span>

```xml
<animation Storyboard.TargetProperty="propertyName.propertyName2[index].propertyName3" ... />
```

<span data-ttu-id="a9c50-216">Większość właściwości zależności lub <xref:System.Windows.Freezable> typów nie obsługuje indeksatora.</span><span class="sxs-lookup"><span data-stu-id="a9c50-216">Most dependency properties or <xref:System.Windows.Freezable> types do not support an indexer.</span></span> <span data-ttu-id="a9c50-217">W związku z tym jedynym sposobem użycia indeksatora w ścieżce animacji jest pośrednia pozycja między właściwością, która uruchamia łańcuch w nazwanym miejscu docelowym i Właściwość animowane.</span><span class="sxs-lookup"><span data-stu-id="a9c50-217">Therefore, the only usage for an indexer in an animation path is at an intermediate position between the property that starts the chain on the named target and the eventual animated property.</span></span> <span data-ttu-id="a9c50-218">W podanej składni, czyli `propertyName2`.</span><span class="sxs-lookup"><span data-stu-id="a9c50-218">In the provided syntax, that is `propertyName2`.</span></span> <span data-ttu-id="a9c50-219">Na przykład użycie indeksatora może być konieczne, jeśli właściwość pośrednia jest kolekcją, taką jak <xref:System.Windows.Media.TransformGroup>, w ścieżce właściwości, takiej jak `RenderTransform.Children[1].Angle`.</span><span class="sxs-lookup"><span data-stu-id="a9c50-219">For instance, an indexer usage might be necessary if the intermediate property is a collection such as <xref:System.Windows.Media.TransformGroup>, in a property path such as `RenderTransform.Children[1].Angle`.</span></span>

<a name="ppincode"></a>

## <a name="propertypath-in-code"></a><span data-ttu-id="a9c50-220">PropertyPath w kodzie</span><span class="sxs-lookup"><span data-stu-id="a9c50-220">PropertyPath in Code</span></span>

<span data-ttu-id="a9c50-221">Użycie kodu dla <xref:System.Windows.PropertyPath>, w tym sposób konstruowania <xref:System.Windows.PropertyPath>, jest udokumentowane w temacie referencyjnym <xref:System.Windows.PropertyPath>dla.</span><span class="sxs-lookup"><span data-stu-id="a9c50-221">Code usage for <xref:System.Windows.PropertyPath>, including how to construct a <xref:System.Windows.PropertyPath>, is documented in the reference topic for <xref:System.Windows.PropertyPath>.</span></span>

<span data-ttu-id="a9c50-222">Ogólnie rzecz biorąc <xref:System.Windows.PropertyPath> , jest przeznaczony do korzystania z dwóch różnych konstruktorów, jednej do użycia powiązań i najprostszych zastosowań animacji oraz jeden dla złożonych zastosowań animacji.</span><span class="sxs-lookup"><span data-stu-id="a9c50-222">In general, <xref:System.Windows.PropertyPath> is designed to use two different constructors, one for the binding usages and simplest animation usages, and one for the complex animation usages.</span></span> <span data-ttu-id="a9c50-223">Użyj <xref:System.Windows.PropertyPath.%23ctor%28System.Object%29> podpisu do powiązań użycia, gdzie obiekt jest ciągiem.</span><span class="sxs-lookup"><span data-stu-id="a9c50-223">Use the <xref:System.Windows.PropertyPath.%23ctor%28System.Object%29> signature for binding usages, where the object is a string.</span></span> <span data-ttu-id="a9c50-224">Użyj <xref:System.Windows.PropertyPath.%23ctor%28System.Object%29> podpisu dla ścieżek animacji jednoetapowych, gdzie obiekt jest <xref:System.Windows.DependencyProperty>.</span><span class="sxs-lookup"><span data-stu-id="a9c50-224">Use the <xref:System.Windows.PropertyPath.%23ctor%28System.Object%29> signature for one-step animation paths, where the object is a <xref:System.Windows.DependencyProperty>.</span></span> <span data-ttu-id="a9c50-225">Użyj <xref:System.Windows.PropertyPath.%23ctor%28System.String%2CSystem.Object%5B%5D%29> podpisu dla złożonych animacji.</span><span class="sxs-lookup"><span data-stu-id="a9c50-225">Use the <xref:System.Windows.PropertyPath.%23ctor%28System.String%2CSystem.Object%5B%5D%29> signature for complex animations.</span></span> <span data-ttu-id="a9c50-226">Ten drugi Konstruktor używa ciągu tokenu dla pierwszego parametru i tablicy obiektów, które wypełniają pozycje w ciągu tokenu, aby zdefiniować relację ścieżki właściwości.</span><span class="sxs-lookup"><span data-stu-id="a9c50-226">This latter constructor uses a token string for the first parameter and an array of objects that fill positions in the token string to define a property path relationship.</span></span>

## <a name="see-also"></a><span data-ttu-id="a9c50-227">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="a9c50-227">See also</span></span>

- <xref:System.Windows.PropertyPath>
- [<span data-ttu-id="a9c50-228">Przegląd powiązań danych</span><span class="sxs-lookup"><span data-stu-id="a9c50-228">Data Binding Overview</span></span>](../../../desktop-wpf/data/data-binding-overview.md)
- [<span data-ttu-id="a9c50-229">Przegląd Scenorysy</span><span class="sxs-lookup"><span data-stu-id="a9c50-229">Storyboards Overview</span></span>](../graphics-multimedia/storyboards-overview.md)
