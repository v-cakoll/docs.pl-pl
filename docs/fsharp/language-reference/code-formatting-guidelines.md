---
title: "Wskazówki dotyczące formatowania kodu (F#)"
description: "Dowiedz się wskazówki dotyczące formatowania wcięcia kodu dla języka czytelności, estetykę, normalizacji i kompilacji programowania w języku F #."
keywords: "Visual f #, f #, funkcjonalności programowania"
author: cartermp
ms.author: phcart
ms.date: 05/16/2016
ms.topic: language-reference
ms.prod: .net
ms.technology: devlang-fsharp
ms.devlang: fsharp
ms.assetid: 3f79717c-f84e-448d-9ce4-90e40a644ba1
ms.openlocfilehash: cc56c67f356b99defd8dc28770f87be1f58443c5
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 10/18/2017
---
# <a name="code-formatting-guidelines"></a><span data-ttu-id="ad13d-104">Wskazówki dotyczące formatowania kodu</span><span class="sxs-lookup"><span data-stu-id="ad13d-104">Code Formatting Guidelines</span></span>

<span data-ttu-id="ad13d-105">W tym temacie przedstawiono wskazówki wcięcia kodu języka F #.</span><span class="sxs-lookup"><span data-stu-id="ad13d-105">This topic summarizes code indentation guidelines for F#.</span></span> <span data-ttu-id="ad13d-106">Ponieważ języka F # jest wrażliwe na podziały wiersza i wcięcia, go nie jest tylko problem czytelności, estetycznych problem lub kodowania problem normalizacji poprawnie formatowania kodu.</span><span class="sxs-lookup"><span data-stu-id="ad13d-106">Because the F# language is sensitive to line breaks and indentation, it is not just a readability issue, aesthetic issue, or coding standardization issue to format your code correctly.</span></span> <span data-ttu-id="ad13d-107">Należy sformatować kodu poprawnie na jej poprawnej kompilacji.</span><span class="sxs-lookup"><span data-stu-id="ad13d-107">You must format your code correctly for it to compile correctly.</span></span>


## <a name="general-rules-for-indentation"></a><span data-ttu-id="ad13d-108">Zasady ogólne dotyczące wcięcie</span><span class="sxs-lookup"><span data-stu-id="ad13d-108">General Rules for Indentation</span></span>
<span data-ttu-id="ad13d-109">Gdy wymagana jest wcięcie, należy użyć spacje, tabulatory nie.</span><span class="sxs-lookup"><span data-stu-id="ad13d-109">When indentation is required, you must use spaces, not tabs.</span></span> <span data-ttu-id="ad13d-110">Wymagana jest co najmniej jedną przestrzeń.</span><span class="sxs-lookup"><span data-stu-id="ad13d-110">At least one space is required.</span></span> <span data-ttu-id="ad13d-111">Organizacja może utworzyć standardów kodowania, aby określić liczbę spacji dla wcięć; Typowy jest trzech lub czterech spacji wcięcia na każdym poziomie, gdzie występuje wcięcia.</span><span class="sxs-lookup"><span data-stu-id="ad13d-111">Your organization can create coding standards to specify the number of spaces to use for indentation; three or four spaces of indentation at each level where indentation occurs is typical.</span></span> <span data-ttu-id="ad13d-112">Można skonfigurować programu Visual Studio, aby dopasować standardów wcięcia w organizacji, zmieniając opcje w `Options` okno dialogowe, które jest dostępne po `Tools` menu.</span><span class="sxs-lookup"><span data-stu-id="ad13d-112">You can configure Visual Studio to match your organization's indentation standards by changing the options in the `Options` dialog box, which is available from the `Tools` menu.</span></span> <span data-ttu-id="ad13d-113">W `Text Editor` węzła, rozwiń węzeł `F#` , a następnie kliknij przycisk `Tabs`.</span><span class="sxs-lookup"><span data-stu-id="ad13d-113">In the `Text Editor` node, expand `F#` and then click `Tabs`.</span></span> <span data-ttu-id="ad13d-114">Opis dostępnych opcji, zobacz [opcje, Edytor tekstu, wszystkie języki, karty](https://msdn.microsoft.com/library/7sffa753.aspx).</span><span class="sxs-lookup"><span data-stu-id="ad13d-114">For a description of the available options, see [Options, Text Editor, All Languages, Tabs](https://msdn.microsoft.com/library/7sffa753.aspx).</span></span>

<span data-ttu-id="ad13d-115">Ogólnie rzecz biorąc gdy kompilator kodu, przechowuje wewnętrznego stosu, który wskazuje bieżący poziom zagnieżdżenia.</span><span class="sxs-lookup"><span data-stu-id="ad13d-115">In general, when the compiler parses your code, it maintains an internal stack that indicates the current level of nesting.</span></span> <span data-ttu-id="ad13d-116">Gdy tworzone jest wcięcie kodu, nowy poziom zagnieżdżenia jest utworzone lub wypychana na tym stosu wewnętrznego.</span><span class="sxs-lookup"><span data-stu-id="ad13d-116">When code is indented, a new level of nesting is created, or pushed onto this internal stack.</span></span> <span data-ttu-id="ad13d-117">Po zakończeniu konstrukcję poziom jest zdjęte ze stosu.</span><span class="sxs-lookup"><span data-stu-id="ad13d-117">When a construct ends, the level is popped.</span></span> <span data-ttu-id="ad13d-118">Wcięcie jest jednym ze sposobów sygnalizuje koniec poziomu i pop stosu wewnętrznego, ale niektórych tokenów również zostać zdjęte ze stosu, takich jak poziom `end` — słowo kluczowe, lub zamykający nawias klamrowy lub nawias.</span><span class="sxs-lookup"><span data-stu-id="ad13d-118">Indentation is one way to signal the end of a level and pop the internal stack, but certain tokens also cause the level to be popped, such as the `end` keyword, or a closing brace or parenthesis.</span></span>

<span data-ttu-id="ad13d-119">Kod w wielowierszowym konstrukcji, takich jak definicją typu definicji funkcji `try...with` konstrukcję i zapętlenia konstrukcje, musi wcięty względem wiersza otwierania konstrukcji.</span><span class="sxs-lookup"><span data-stu-id="ad13d-119">Code in a multiline construct, such as a type definition, function definition, `try...with` construct, and looping constructs, must be indented relative to the opening line of the construct.</span></span> <span data-ttu-id="ad13d-120">Pierwszy wiersz z wcięciami ustanawia pozycję w kolumnie dla kolejnych kodu w tej samej konstrukcji.</span><span class="sxs-lookup"><span data-stu-id="ad13d-120">The first indented line establishes a column position for subsequent code in the same construct.</span></span> <span data-ttu-id="ad13d-121">Poziom wcięcia jest nazywany *kontekstu*.</span><span class="sxs-lookup"><span data-stu-id="ad13d-121">The indentation level is called a *context*.</span></span> <span data-ttu-id="ad13d-122">Pozycja kolumny Ustawia minimalną kolumny, określany jako *wiersza poza*, dla kolejnych wierszy kodu, które znajdują się w tym samym kontekście.</span><span class="sxs-lookup"><span data-stu-id="ad13d-122">The column position sets a minimum column, referred to as an *offside line*, for subsequent lines of code that are in the same context.</span></span> <span data-ttu-id="ad13d-123">W przypadku napotkania wiersz kodu wcięta mniej niż ta pozycja kolumny ustalonych, kompilator zakłada zakończył kontekstu i czy możesz są teraz generowanie kodu na następny poziom, poprzedni kontekst.</span><span class="sxs-lookup"><span data-stu-id="ad13d-123">When a line of code is encountered that is indented less than this established column position, the compiler assumes that the context has ended and that you are now coding at the next level up, in the previous context.</span></span> <span data-ttu-id="ad13d-124">Termin *poza* jest używany do opisu stanu, w którym wiersz kodu wyzwala koniec konstrukcję, ponieważ nie jest wystarczająco daleko wcięcia.</span><span class="sxs-lookup"><span data-stu-id="ad13d-124">The term *offside* is used to describe the condition in which a line of code triggers the end of a construct because it is not indented far enough.</span></span> <span data-ttu-id="ad13d-125">Innymi słowy kod z lewej strony poza linii jest poza.</span><span class="sxs-lookup"><span data-stu-id="ad13d-125">In other words, code to the left of an offside line is offside.</span></span> <span data-ttu-id="ad13d-126">W kodzie poprawnie wcięta możesz korzystać z reguły poza aby odróżniać koniec konstrukcji.</span><span class="sxs-lookup"><span data-stu-id="ad13d-126">In correctly indented code, you take advantage of the offside rule in order to delineate the end of constructs.</span></span> <span data-ttu-id="ad13d-127">Jeśli używasz wcięcia nieprawidłowo, poza warunek może spowodować, że kompilator ostrzeżenie lub może doprowadzić do nieprawidłowej interpretacji kodu.</span><span class="sxs-lookup"><span data-stu-id="ad13d-127">If you use indentation improperly, an offside condition can cause the compiler to issue a warning or can lead to the incorrect interpretation of your code.</span></span>

<span data-ttu-id="ad13d-128">Poza wiersze są określane w następujący sposób.</span><span class="sxs-lookup"><span data-stu-id="ad13d-128">Offside lines are determined as follows.</span></span>


- <span data-ttu-id="ad13d-129">`=` Token skojarzony z `let` wprowadza poza wiersza w kolumnie pierwszego tokena po `=` logowania.</span><span class="sxs-lookup"><span data-stu-id="ad13d-129">An `=` token associated with a `let` introduces an offside line at the column of the first token after the `=` sign.</span></span>


- <span data-ttu-id="ad13d-130">W `if...then...else` wyrażenia, pozycja kolumny pierwszy token po `then` — słowo kluczowe lub `else` — słowo kluczowe wprowadza poza wiersza.</span><span class="sxs-lookup"><span data-stu-id="ad13d-130">In an `if...then...else` expression, the column position of the first token after the `then` keyword or the `else` keyword introduces an offside line.</span></span>


- <span data-ttu-id="ad13d-131">W `try...with` wyrażenia, pierwszy token po `try` wprowadza poza wiersza.</span><span class="sxs-lookup"><span data-stu-id="ad13d-131">In a `try...with` expression, the first token after `try` introduces an offside line.</span></span>


- <span data-ttu-id="ad13d-132">W `match` wyrażenia, pierwszy token po `with` i pierwszy token po każdym poleceniu `->` wprowadzenie poza wierszy.</span><span class="sxs-lookup"><span data-stu-id="ad13d-132">In a `match` expression, the first token after `with` and the first token after each `->` introduce offside lines.</span></span>


- <span data-ttu-id="ad13d-133">Pierwszy token po `with` jako typ rozszerzenia wprowadza poza wiersza.</span><span class="sxs-lookup"><span data-stu-id="ad13d-133">The first token after `with` in a type extension introduces an offside line.</span></span>


- <span data-ttu-id="ad13d-134">Pierwszy token po otwierający nawias klamrowy lub nawias lub po `begin` — słowo kluczowe, wprowadza poza wiersza.</span><span class="sxs-lookup"><span data-stu-id="ad13d-134">The first token after an opening brace or parenthesis, or after the `begin` keyword, introduces an offside line.</span></span>


- <span data-ttu-id="ad13d-135">Pierwszy znak w słowa kluczowe `let`, `if`, i `module` wprowadzenie poza wierszy.</span><span class="sxs-lookup"><span data-stu-id="ad13d-135">The first character in the keywords `let`, `if`, and `module` introduce offside lines.</span></span>


<span data-ttu-id="ad13d-136">Poniższe przykłady kodu przedstawiają reguły wcięcia.</span><span class="sxs-lookup"><span data-stu-id="ad13d-136">The following code examples illustrate the indentation rules.</span></span> <span data-ttu-id="ad13d-137">W tym miejscu wydruku instrukcje korzystają z wcięcie Aby skojarzyć je z odpowiedniego kontekstu.</span><span class="sxs-lookup"><span data-stu-id="ad13d-137">Here, the print statements rely on indentation to associate them with the appropriate context.</span></span> <span data-ttu-id="ad13d-138">Za każdym razem, gdy wcięcie przewiduje, kontekst jest zdjęte ze stosu, jak i przywraca poprzedni kontekst.</span><span class="sxs-lookup"><span data-stu-id="ad13d-138">Every time the indentation shifts, the context is popped and returns to the previous context.</span></span> <span data-ttu-id="ad13d-139">W związku z tym miejscem jest wydrukowany na końcu każdej iteracji; "Gotowe"!</span><span class="sxs-lookup"><span data-stu-id="ad13d-139">Therefore, a space is printed at the end of each iteration; "Done!"</span></span> <span data-ttu-id="ad13d-140">tylko jest drukowany jeden raz, ponieważ poza Wcięcie określa, że nie jest w pętli.</span><span class="sxs-lookup"><span data-stu-id="ad13d-140">is only printed one time because the offside indentation establishes that it is not part of the loop.</span></span> <span data-ttu-id="ad13d-141">Drukowanie ciągu "Context najwyższego poziomu" nie jest częścią funkcji.</span><span class="sxs-lookup"><span data-stu-id="ad13d-141">The printing of the string "Top-level context" is not part of the function.</span></span> <span data-ttu-id="ad13d-142">W związku z tym wydrukowaniem najpierw podczas statycznego inicjowania przed wywołaniem funkcji.</span><span class="sxs-lookup"><span data-stu-id="ad13d-142">Therefore, it is printed first, during the static initialization, before the function is called.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet1.fs)]

<span data-ttu-id="ad13d-143">Dane wyjściowe są następujące:</span><span class="sxs-lookup"><span data-stu-id="ad13d-143">The output is as follows.</span></span>

```
Top-level context

(Negative number) Zero 1 2 3 Done!
```

<span data-ttu-id="ad13d-144">Po przerwaniu długie wiersze kontynuacji wiersza musi wcięty między konstrukcja otaczającej.</span><span class="sxs-lookup"><span data-stu-id="ad13d-144">When you break long lines, the continuation of the line must be indented farther than the enclosing construct.</span></span> <span data-ttu-id="ad13d-145">Na przykład argumenty funkcji muszą wcięty między pierwszym znakiem nazwy funkcji, jak pokazano w poniższym kodzie.</span><span class="sxs-lookup"><span data-stu-id="ad13d-145">For example, function arguments must be indented farther than the first character of the function name, as shown in the following code.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet2.fs)]

<span data-ttu-id="ad13d-146">Istnieją wyjątki od tych reguł, zgodnie z opisem w następnej sekcji.</span><span class="sxs-lookup"><span data-stu-id="ad13d-146">There are exceptions to these rules, as described in the next section.</span></span>


## <a name="indentation-in-modules"></a><span data-ttu-id="ad13d-147">Wcięcia w modułach</span><span class="sxs-lookup"><span data-stu-id="ad13d-147">Indentation in Modules</span></span>
<span data-ttu-id="ad13d-148">Kod w lokalnym module musi być wcięty względem modułu, ale kod w module najwyższego poziomu nie musi być wcięta.</span><span class="sxs-lookup"><span data-stu-id="ad13d-148">Code in a local module must be indented relative to the module, but code in a top-level module does not have to be indented.</span></span> <span data-ttu-id="ad13d-149">Nie masz elementy Namespace wcięcia.</span><span class="sxs-lookup"><span data-stu-id="ad13d-149">Namespace elements do not have to be indented.</span></span>

<span data-ttu-id="ad13d-150">W poniższych przykładach kodu ilustrację.</span><span class="sxs-lookup"><span data-stu-id="ad13d-150">The following code examples illustrate this.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet3.fs)]
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet4.fs)]

<span data-ttu-id="ad13d-151">Aby uzyskać więcej informacji, zobacz [modułów](modules.md).</span><span class="sxs-lookup"><span data-stu-id="ad13d-151">For more information, see [Modules](modules.md).</span></span>


## <a name="exceptions-to-the-basic-indentation-rules"></a><span data-ttu-id="ad13d-152">Wyjątki reguł wcięcia podstawowe</span><span class="sxs-lookup"><span data-stu-id="ad13d-152">Exceptions to the Basic Indentation Rules</span></span>
<span data-ttu-id="ad13d-153">Zasady, zgodnie z opisem w poprzedniej sekcji, to czy kod w wielowierszowym konstrukcje musi być wcięty względem wcięcie pierwszego wiersza konstrukcji i koniec konstrukcja jest określany przez podczas pierwszego wiersza poza.</span><span class="sxs-lookup"><span data-stu-id="ad13d-153">The general rule, as described in the previous section, is that code in multiline constructs must be indented relative to the indentation of the first line of the construct, and that the end of the construct is determined by when the first offside line occurs.</span></span> <span data-ttu-id="ad13d-154">Wyjątek od reguł dotyczących gdy end sytuacjach jest, że niektóre konstrukcje, takich jak `try...with` wyrażenia `if...then...else` wyrażenie i stosowania `and` Składnia deklaracji wzajemnie funkcje rekursywne lub typów, ma wiele części.</span><span class="sxs-lookup"><span data-stu-id="ad13d-154">An exception to the rule about when contexts end is that some constructs, such as the `try...with` expression, the `if...then...else` expression, and the use of `and` syntax for declaring mutually recursive functions or types, have multiple parts.</span></span> <span data-ttu-id="ad13d-155">Wcięcie nowsze składniki, takie jak `then` i `else` w `if...then...else` wyrażenie w tym samym poziomie jako token, który rozpoczyna się wyrażenie, ale zamiast wskazujący punktu końcowego do kontekstu, reprezentuje następnej części tego samego kontekstu.</span><span class="sxs-lookup"><span data-stu-id="ad13d-155">You indent the later parts, such as `then` and `else` in an `if...then...else` expression, at the same level as the token that starts the expression, but instead of indicating an end to the context, it represents the next part of the same context.</span></span> <span data-ttu-id="ad13d-156">W związku z tym `if...then...else` wyrażenie można napisać tak jak w poniższym przykładzie kodu.</span><span class="sxs-lookup"><span data-stu-id="ad13d-156">Therefore, an `if...then...else` expression can be written as in the following code example.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet5.fs)]

<span data-ttu-id="ad13d-157">Wyjątek poza reguły dotyczą tylko `then` i `else` słów kluczowych.</span><span class="sxs-lookup"><span data-stu-id="ad13d-157">The exception to the offside rule applies only to the `then` and `else` keywords.</span></span> <span data-ttu-id="ad13d-158">W związku z tym mimo że nie jest błąd wcięcia `then` i `else` Ponadto nieudanym wcięcie wierszy kodu w `then` bloku generuje ostrzeżenie.</span><span class="sxs-lookup"><span data-stu-id="ad13d-158">Therefore, although it is not an error to indent the `then` and `else` further, failing to indent the lines of code in a `then` block produces a warning.</span></span> <span data-ttu-id="ad13d-159">Jest to zilustrowane w następujących wierszach kodu.</span><span class="sxs-lookup"><span data-stu-id="ad13d-159">This is illustrated in the following lines of code.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet6.fs)]
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet7.fs)]

<span data-ttu-id="ad13d-160">Dla kodu `else` dotyczy blok, dodatkowe reguły specjalne.</span><span class="sxs-lookup"><span data-stu-id="ad13d-160">For code in an `else` block, an additional special rule applies.</span></span> <span data-ttu-id="ad13d-161">Ostrzeżenie w poprzednim przykładzie występuje tylko w kodzie w `then` bloku, a nie na kod w `else` bloku.</span><span class="sxs-lookup"><span data-stu-id="ad13d-161">The warning in the previous example occurs only on the code in the `then` block, not on the code in the `else` block.</span></span> <span data-ttu-id="ad13d-162">Dzięki temu można napisać kod, który sprawdza bez wymuszania reszty kod funkcję, która może znajdować się w różnych warunków na początku funkcji `else` bloku wcięcia.</span><span class="sxs-lookup"><span data-stu-id="ad13d-162">This allows you to write code that checks for various conditions at the beginning of a function without forcing the rest of the code for the function, which might be in an `else` block, to be indented.</span></span> <span data-ttu-id="ad13d-163">W związku z tym można napisać następujące bez tworzenia ostrzeżenie.</span><span class="sxs-lookup"><span data-stu-id="ad13d-163">Thus, you can write the following without producing a warning.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet8.fs)]

<span data-ttu-id="ad13d-164">Kolejny wyjątek do reguły, który zakończy się kontekst wiersza nie jest wcięty, ile poprzedniego wiersza jest infiksu operatorów, takich jak `+` i `|>`.</span><span class="sxs-lookup"><span data-stu-id="ad13d-164">Another exception to the rule that contexts end when a line is not indented as far as a previous line is for infix operators, such as `+` and `|>`.</span></span> <span data-ttu-id="ad13d-165">Wiersze rozpoczynające się infiksu operatory są dozwolone, aby rozpocząć `(1 + oplength)` kolumn przed normalnej pozycji bez wyzwalania punktu końcowego do kontekstu, gdzie `oplength` jest liczba znaków, które tworzą operatora.</span><span class="sxs-lookup"><span data-stu-id="ad13d-165">Lines that start with infix operators are permitted to begin `(1 + oplength)` columns before the normal position without triggering an end to the context, where `oplength` is the number of characters that make up the operator.</span></span> <span data-ttu-id="ad13d-166">Powoduje to pierwszy token po operatorze, aby były wyrównane z poprzedniego wiersza.</span><span class="sxs-lookup"><span data-stu-id="ad13d-166">This causes the first token after the operator to align with the previous line.</span></span>

<span data-ttu-id="ad13d-167">Na przykład w poniższym kodzie `+` symboli może być wcięta dwie kolumny krótszym niż poprzedniego wiersza.</span><span class="sxs-lookup"><span data-stu-id="ad13d-167">For example, in the following code, the `+` symbol is permitted to be indented two columns less than the previous line.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet9.fs)]

<span data-ttu-id="ad13d-168">Chociaż wcięcia zwykle zwiększa się wraz ze wzrostem wyższy poziom zagnieżdżenia, istnieje kilka konstrukcji, w których kompilator pozwala na zresetowanie wcięcie do dolnej położenie kolumny.</span><span class="sxs-lookup"><span data-stu-id="ad13d-168">Although indentation usually increases as the level of nesting becomes higher, there are several constructs in which the compiler allows you to reset the indentation to a lower column position.</span></span>

<span data-ttu-id="ad13d-169">Konstrukcje, które zezwala na resetowanie położenia kolumny są następujące:</span><span class="sxs-lookup"><span data-stu-id="ad13d-169">The constructs that permit a reset of column position are as follows:</span></span>


- <span data-ttu-id="ad13d-170">Treść funkcji anonimowych.</span><span class="sxs-lookup"><span data-stu-id="ad13d-170">Bodies of anonymous functions.</span></span> <span data-ttu-id="ad13d-171">W poniższym kodzie wydruku wyrażenie rozpoczyna się od pozycji w kolumnie, która jest dostępne na lewo od `fun` — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="ad13d-171">In the following code, the print expression starts at a column position that is farther to the left than the `fun` keyword.</span></span> <span data-ttu-id="ad13d-172">Jednak wiersz nie może rozpoczynać się w kolumnie z lewej strony początek poprzedniego poziomu wcięcia (to znaczy po lewej `L` w `List`).</span><span class="sxs-lookup"><span data-stu-id="ad13d-172">However, the line must not start at a column to the left of the start of the previous indentation level (that is, to the left of the `L` in `List`).</span></span>
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet10.fs)]

- <span data-ttu-id="ad13d-173">Konstrukcje ujęta w nawiasy lub przez `begin` i `end` w `then` lub `else` zablokować z `if...then...else` wyrażenia, pod warunkiem wcięcie nie jest nie mniejszy niż pozycja kolumny `if` — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="ad13d-173">Constructs enclosed by parentheses or by `begin` and `end` in a `then` or `else` block of an `if...then...else` expression, provided the indentation is no less than the column position of the `if` keyword.</span></span> <span data-ttu-id="ad13d-174">Ten wyjątek umożliwia stylu kodowania, w którym nawias otwierający lub `begin` jest używany na końcu wiersza po `then` lub `else`.</span><span class="sxs-lookup"><span data-stu-id="ad13d-174">This exception allows for a coding style in which an opening parenthesis or `begin` is used at the end of a line after `then` or `else`.</span></span>


- <span data-ttu-id="ad13d-175">Treści modułów, klasy, interfejsy i struktur rozdzielone `begin...end`, `{...}`, `class...end`, lub `interface...end`.</span><span class="sxs-lookup"><span data-stu-id="ad13d-175">Bodies of modules, classes, interfaces, and structures delimited by `begin...end`, `{...}`, `class...end`, or `interface...end`.</span></span> <span data-ttu-id="ad13d-176">Dzięki temu stylu, w którym kluczowego otwarcia definicji typu może być na tym samym wierszu co nazwa typu bez wymuszania całej treści być wcięta między otwierania — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="ad13d-176">This allows for a style in which the opening keyword of a type definition can be on the same line as the type name without forcing the whole body to be indented farther than the opening keyword.</span></span>
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet13.fs)]


## <a name="see-also"></a><span data-ttu-id="ad13d-177">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="ad13d-177">See Also</span></span>
[<span data-ttu-id="ad13d-178">Dokumentacja języka F #</span><span class="sxs-lookup"><span data-stu-id="ad13d-178">F# Language Reference</span></span>](index.md)
