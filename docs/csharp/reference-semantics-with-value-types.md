---
title: "Semantykę odwołania z typami wartości"
description: "Zrozumieć funkcje językowe, co minimalizuje bezpiecznie kopiowania struktury"
author: billwagner
ms.author: wiwagn
ms.date: 11/10/2017
ms.topic: article
ms.prod: .net
ms.technology: devlang-csharp
ms.devlang: csharp
ms.custom: mvc
ms.openlocfilehash: 9eeaf201c1f5a58044db62e356199b609c4c035a
ms.sourcegitcommit: 7e99f66ef09d2903e22c789c67ff5a10aa953b2f
ms.translationtype: HT
ms.contentlocale: pl-PL
ms.lasthandoff: 11/18/2017
---
# <a name="reference-semantics-with-value-types"></a><span data-ttu-id="fdde2-103">Semantykę odwołania z typami wartości</span><span class="sxs-lookup"><span data-stu-id="fdde2-103">Reference semantics with value types</span></span>

<span data-ttu-id="fdde2-104">Zaletą używania typów wartości jest, aby uniknąć często Alokacje sterty.</span><span class="sxs-lookup"><span data-stu-id="fdde2-104">An advantage to using value types is that they often avoid heap allocations.</span></span>
<span data-ttu-id="fdde2-105">Odpowiednie Wada polega na tym, że są one kopiowane wartości.</span><span class="sxs-lookup"><span data-stu-id="fdde2-105">The corresponding disadvantage is that they are copied by value.</span></span> <span data-ttu-id="fdde2-106">Ta zależnościami utrudnia zoptymalizować algorytmy, które pracują w dużych ilości danych.</span><span class="sxs-lookup"><span data-stu-id="fdde2-106">This tradeoff makes it harder to optimize algorithms that operate on large amounts of data.</span></span> <span data-ttu-id="fdde2-107">Nowe funkcje języka C# 7.2 zapewniają mechanizmy, które Włącz semantykę przekazywany przez odwołanie z typami wartości.</span><span class="sxs-lookup"><span data-stu-id="fdde2-107">New language features in C# 7.2 provide mechanisms that enable pass-by-reference semantics with value types.</span></span> <span data-ttu-id="fdde2-108">Rozsądny sposób używania tych funkcji można zminimalizować zarówno alokacji i operacje kopiowania.</span><span class="sxs-lookup"><span data-stu-id="fdde2-108">If you use these features wisely you can minimize both allocations and copy operations.</span></span> <span data-ttu-id="fdde2-109">Ten artykuł opisuje te nowe funkcje.</span><span class="sxs-lookup"><span data-stu-id="fdde2-109">This article explores those new features.</span></span>

<span data-ttu-id="fdde2-110">Większość przykładowy kod w tym artykule przedstawiono funkcje dodane w języku C# 7.2.</span><span class="sxs-lookup"><span data-stu-id="fdde2-110">Much of the sample code in this article demonstrates features added in C# 7.2.</span></span> <span data-ttu-id="fdde2-111">Aby można było używać tych funkcji, należy skonfigurować projekt do używania języka C# 7,2 lub nowszej w projekcie.</span><span class="sxs-lookup"><span data-stu-id="fdde2-111">In order to use those features, you have to configure your project to use C# 7.2 or later in your project.</span></span> <span data-ttu-id="fdde2-112">Visual Studio można użyć, aby go wybrać.</span><span class="sxs-lookup"><span data-stu-id="fdde2-112">You can use Visual Studio to select it.</span></span> <span data-ttu-id="fdde2-113">Dla każdego projektu, zaznacz **projektu** z menu, następnie **właściwości**.</span><span class="sxs-lookup"><span data-stu-id="fdde2-113">For each project, select **Project** from the menu, then **Properties**.</span></span> <span data-ttu-id="fdde2-114">Wybierz **kompilacji** i kliknij polecenie **zaawansowane**.</span><span class="sxs-lookup"><span data-stu-id="fdde2-114">Select the **Build** tab and click **Advanced**.</span></span> <span data-ttu-id="fdde2-115">Z tego miejsca możesz skonfigurować wersji językowej.</span><span class="sxs-lookup"><span data-stu-id="fdde2-115">From there, you can configure the language version.</span></span> <span data-ttu-id="fdde2-116">Wybierz opcję "7.2" lub "najnowszej".</span><span class="sxs-lookup"><span data-stu-id="fdde2-116">Choose either "7.2", or "latest".</span></span>  <span data-ttu-id="fdde2-117">W razie potrzeby można edytować *csproj* plik i dodać następującego węzła:</span><span class="sxs-lookup"><span data-stu-id="fdde2-117">Or you can edit the *csproj* file and add the following node:</span></span>

```XML
  <PropertyGroup>
    <LangVersion>7.2</LangVersion>
  </PropertyGroup>
```

<span data-ttu-id="fdde2-118">Można użyć "7,2" lub "najnowszej" dla wartości.</span><span class="sxs-lookup"><span data-stu-id="fdde2-118">You can use either "7.2" or "latest" for the value.</span></span>

## <a name="specifying-in-parameters"></a><span data-ttu-id="fdde2-119">Określanie `in` parametrów</span><span class="sxs-lookup"><span data-stu-id="fdde2-119">Specifying `in` parameters</span></span>

<span data-ttu-id="fdde2-120">C# 7.2 dodaje `in` — słowo kluczowe, aby mogła uzupełniać istniejące `ref` i `out` słów kluczowych podczas pisania metodę, która przekazuje argumenty przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="fdde2-120">C# 7.2 adds the `in` keyword to complement the existing `ref` and `out` keywords when you write a method that passes arguments by reference.</span></span> <span data-ttu-id="fdde2-121">`in` — Słowo kluczowe Określa, że parametr jest przekazywany przez odwołanie, i wywołana metoda nie modyfikuje wartość przekazana do niej.</span><span class="sxs-lookup"><span data-stu-id="fdde2-121">The `in` keyword specifies that you are passing the parameter by reference and the called method does not modify the value passed to it.</span></span> 

<span data-ttu-id="fdde2-122">To dodawanie zapewnia pełne słownictwa Express z celem projektu.</span><span class="sxs-lookup"><span data-stu-id="fdde2-122">This addition provides a full vocabulary to express your design intent.</span></span> <span data-ttu-id="fdde2-123">Typy wartości są kopiowane, gdy przekazywany do metody wywoływane, gdy nie określaj żadnego z następujących modyfikatorów.</span><span class="sxs-lookup"><span data-stu-id="fdde2-123">Value types are copied when passed to a called method when you do not specify any of the following modifiers.</span></span> <span data-ttu-id="fdde2-124">Określ każdy z tych modyfikatory, że typ wartości jest przekazywana przez odwołanie, unikając kopiowania.</span><span class="sxs-lookup"><span data-stu-id="fdde2-124">Each of these modifiers specify that a value type is passed by reference, avoiding the copy.</span></span> <span data-ttu-id="fdde2-125">Każdy modyfikator wyraża innego zamiaru:</span><span class="sxs-lookup"><span data-stu-id="fdde2-125">Each modifier expresses a different intent:</span></span>

- <span data-ttu-id="fdde2-126">`out`: Ta metoda określa wartość argumentu używany jako parametr.</span><span class="sxs-lookup"><span data-stu-id="fdde2-126">`out`: This method sets the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="fdde2-127">`ref`: Ta metoda może ustawić wartość argumentu używany jako parametr.</span><span class="sxs-lookup"><span data-stu-id="fdde2-127">`ref`: This method may set the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="fdde2-128">`in`: Ta metoda nie modyfikuje wartość argumentu używany jako parametr.</span><span class="sxs-lookup"><span data-stu-id="fdde2-128">`in`: This method does not modify the value of the argument used as this parameter.</span></span>

<span data-ttu-id="fdde2-129">Po dodaniu `in` modyfikator do przekazywania argumentu przez odwołanie, należy zadeklarować z celem projektu jest przekazywać argumentów przez odwołanie, aby uniknąć niepotrzebnych kopiowania.</span><span class="sxs-lookup"><span data-stu-id="fdde2-129">When you add the `in` modifier to pass an argument by reference, you declare your design intent is to pass arguments by reference to avoid unnecessary copying.</span></span> <span data-ttu-id="fdde2-130">Nie chcesz zmodyfikować obiekt używany jako tego argumentu.</span><span class="sxs-lookup"><span data-stu-id="fdde2-130">You do not intend to modify the object used as that argument.</span></span> <span data-ttu-id="fdde2-131">Poniższy kod przedstawia przykład metodę, która oblicza odległość między dwoma punktami w przestrzeni 3D.</span><span class="sxs-lookup"><span data-stu-id="fdde2-131">The following code shows an example of a method that calculates the distance between two points in 3D space.</span></span> 

[!code-csharp[InArgument](../../samples/csharp/reference-semantics/Program.cs#InArgument "Specifying an In argument")]

<span data-ttu-id="fdde2-132">Argumenty są dwie struktury, że każdy zawiera trzy symulacyjnych.</span><span class="sxs-lookup"><span data-stu-id="fdde2-132">The arguments are two structures that each contain three doubles.</span></span> <span data-ttu-id="fdde2-133">Wartość o podwójnej precyzji jest 8 bajtów, dzięki czemu każdy argument jest 24 bajty.</span><span class="sxs-lookup"><span data-stu-id="fdde2-133">A double is 8 bytes, so each argument is 24 bytes.</span></span> <span data-ttu-id="fdde2-134">Określając `in` modyfikator, należy przekazać 4-bajtowych lub 8-bajtowych odwołania do tych argumentów, w zależności od architektury komputera.</span><span class="sxs-lookup"><span data-stu-id="fdde2-134">By specifying the `in` modifier, you pass 4-byte or 8-byte reference to those arguments, depending on the architecture of the machine.</span></span> <span data-ttu-id="fdde2-135">Różnica w rozmiarze jest mały, ale jego można szybko dodać po aplikacji wywołuje tę metodę w pętli ścisłej przy użyciu wielu różnych wartości.</span><span class="sxs-lookup"><span data-stu-id="fdde2-135">The difference in size is small, but it can quickly add up when your application calls this method in a tight loop using many different values.</span></span>
 
<span data-ttu-id="fdde2-136">`in` Uzupełnia modyfikator `out` i `ref` także inne sposoby.</span><span class="sxs-lookup"><span data-stu-id="fdde2-136">The `in` modifier complements `out` and `ref` in other ways as well.</span></span> <span data-ttu-id="fdde2-137">Nie można utworzyć przeciążenia metody, które różnią się jedynie w związku z występowaniem `in`, `out` lub `ref`.</span><span class="sxs-lookup"><span data-stu-id="fdde2-137">You cannot create overloads of a method that differ only in the presence of `in`, `out` or `ref`.</span></span> <span data-ttu-id="fdde2-138">Te nowe reguły rozszerzania zawsze ma zdefiniowany dla tego samego zachowania `out` i `ref` parametrów.</span><span class="sxs-lookup"><span data-stu-id="fdde2-138">These new rules extend the same behavior that had always been defined for `out` and `ref` parameters.</span></span>

<span data-ttu-id="fdde2-139">`in` Modyfikator mogą być stosowane do dowolnego członka, który przyjmuje parametry: metody, delegatów, wyrażenia lambda, funkcje lokalne, indeksatorów, operatorów.</span><span class="sxs-lookup"><span data-stu-id="fdde2-139">The `in` modifier may be applied to any member that takes parameters: methods, delegates, lambdas, local functions, indexers, operators.</span></span>

<span data-ttu-id="fdde2-140">W odróżnieniu od `ref` i `out` argumenty, można użyć wartości literałów ani stałe dla argumentu `in` parametru.</span><span class="sxs-lookup"><span data-stu-id="fdde2-140">Unlike `ref` and `out` arguments, you may use literal values or constants for the argument to an `in` parameter.</span></span> <span data-ttu-id="fdde2-141">Ponadto, w przeciwieństwie do `ref` lub `out` parametr, nie trzeba zastosować `in` modyfikator w miejsce wywołania.</span><span class="sxs-lookup"><span data-stu-id="fdde2-141">Also, unlike a `ref` or `out` parameter, you don't need to apply the `in` modifier at the call site.</span></span> <span data-ttu-id="fdde2-142">Poniższy kod przedstawia dwa przykłady wywołania metody `CalculateDistance` metody.</span><span class="sxs-lookup"><span data-stu-id="fdde2-142">The following code shows you two examples of calling the `CalculateDistance` method.</span></span> <span data-ttu-id="fdde2-143">Pierwszy korzysta z dwóch zmiennych lokalnych przekazywana przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="fdde2-143">The first uses two local variables passed by reference.</span></span> <span data-ttu-id="fdde2-144">Drugi zawiera zmiennej tymczasowej utworzone jako część wywołania metody.</span><span class="sxs-lookup"><span data-stu-id="fdde2-144">The second includes a temporary variable created as part of the method call.</span></span> 

[!code-csharp[UseInArgument](../../samples/csharp/reference-semantics/Program.cs#UseInArgument "Specifying an In argument")]

<span data-ttu-id="fdde2-145">Istnieje kilka sposobów, w których kompilator zapewnia, które tylko do odczytu rodzaj `in` argument jest wymuszana.</span><span class="sxs-lookup"><span data-stu-id="fdde2-145">There are several ways in which the compiler ensures that the read-only nature of an `in` argument is enforced.</span></span>  <span data-ttu-id="fdde2-146">Przede wszystkim wywołaną metodę nie można przypisać bezpośrednio do `in` parametru.</span><span class="sxs-lookup"><span data-stu-id="fdde2-146">First of all, the called method can't directly assign to an `in` parameter.</span></span> <span data-ttu-id="fdde2-147">Bezpośrednio nie można przypisać do dowolnego pola `in` parametru.</span><span class="sxs-lookup"><span data-stu-id="fdde2-147">It can't directly assign to any field of an `in` parameter.</span></span> <span data-ttu-id="fdde2-148">Ponadto nie można przekazać `in` parametr do dowolnej metody wymagających `ref` lub `out` modyfikator.</span><span class="sxs-lookup"><span data-stu-id="fdde2-148">In addition, you cannot pass an `in` parameter to any method demanding the `ref` or `out` modifier.</span></span>
<span data-ttu-id="fdde2-149">Wymusza kompilator, który `in` argument jest zmienną tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="fdde2-149">The compiler enforces that the `in` argument is a readonly variable.</span></span> <span data-ttu-id="fdde2-150">Możesz wywołać dowolnej metody wystąpienia, która używa semantyki przebiegu wartości.</span><span class="sxs-lookup"><span data-stu-id="fdde2-150">You can call any instance method that uses pass-by-value semantics.</span></span> <span data-ttu-id="fdde2-151">W tych przypadkach kopię `in` utworzeniu parametru.</span><span class="sxs-lookup"><span data-stu-id="fdde2-151">In those instances, a copy of the `in` parameter is created.</span></span> <span data-ttu-id="fdde2-152">Ponieważ kompilator można utworzyć zmiennej tymczasowej dla każdego `in` parametru, można również określić wartości domyślnej dla żadnego `in` parametru.</span><span class="sxs-lookup"><span data-stu-id="fdde2-152">Because the compiler can create a temporary variable for any `in` parameter, you can also specify default values for any `in` parameter.</span></span> <span data-ttu-id="fdde2-153">Wykonaj kodu użyty do określenia pochodzenia (punkt 0,0) z wartością domyślną dla drugiego punktu:</span><span class="sxs-lookup"><span data-stu-id="fdde2-153">The follow code uses that to specify the origin (point 0,0) as the default value for the second point:</span></span>

[!code-csharp[InArgumentDefault](../../samples/csharp/reference-semantics/Program.cs#InArgumentDefault "Specifying defaults for an in parameter")]

<span data-ttu-id="fdde2-154">`in` Można również określenie parametru używane z typami odwołanie lub wbudowane wartości liczbowych.</span><span class="sxs-lookup"><span data-stu-id="fdde2-154">The `in` parameter designation can also be used with reference types or built in numeric values.</span></span> <span data-ttu-id="fdde2-155">Jednak korzyści w obu przypadkach są minimalne, jeśli istnieje.</span><span class="sxs-lookup"><span data-stu-id="fdde2-155">However, the benefits in both cases are minimal, if any.</span></span>

## <a name="ref-readonly-returns"></a><span data-ttu-id="fdde2-156">`ref readonly`Zwraca</span><span class="sxs-lookup"><span data-stu-id="fdde2-156">`ref readonly` returns</span></span>

<span data-ttu-id="fdde2-157">Można również zwrócić przez odwołanie typu wartości, ale nie zezwalaj na obiekt wywołujący modyfikowanie tej wartości.</span><span class="sxs-lookup"><span data-stu-id="fdde2-157">You may also want to return a value type by reference, but disallow the caller from modifying that value.</span></span> <span data-ttu-id="fdde2-158">Użyj `ref readonly` modyfikator Express celem tego projektu.</span><span class="sxs-lookup"><span data-stu-id="fdde2-158">Use the `ref readonly` modifier to express that design intent.</span></span> <span data-ttu-id="fdde2-159">Czy są zwracane jest odwołanie do istniejących danych, ale nie zezwala na modyfikowanie powiadomi czytników.</span><span class="sxs-lookup"><span data-stu-id="fdde2-159">It notifies readers that you are returning a reference to existing data, but not allowing modification.</span></span> 

<span data-ttu-id="fdde2-160">Kompilator wymusza, że obiekt wywołujący nie można zmodyfikować odwołania.</span><span class="sxs-lookup"><span data-stu-id="fdde2-160">The compiler enforces that the caller cannot modify the reference.</span></span> <span data-ttu-id="fdde2-161">Próby bezpośrednio przypisać wartość generuje błąd kompilacji.</span><span class="sxs-lookup"><span data-stu-id="fdde2-161">Attempts to assign to the value directly generate a compile-time error.</span></span> <span data-ttu-id="fdde2-162">Jednak kompilator nie wiadomo, jeśli metoda dowolnego elementu członkowskiego modyfikuje stan struktury.</span><span class="sxs-lookup"><span data-stu-id="fdde2-162">However, the compiler cannot know if any member method modifies the state of the struct.</span></span>
<span data-ttu-id="fdde2-163">Aby upewnić się, że obiekt nie jest modyfikowany, kompilator tworzy kopię i wywołuje element członkowski odwołań za pomocą tej kopii.</span><span class="sxs-lookup"><span data-stu-id="fdde2-163">To ensure that the object is not modified, the compiler creates a copy and calls member references using that copy.</span></span> <span data-ttu-id="fdde2-164">Wszystkie modyfikacje są obrony ją.</span><span class="sxs-lookup"><span data-stu-id="fdde2-164">Any modifications are to that defensive copy.</span></span> 

<span data-ttu-id="fdde2-165">Istnieje prawdopodobieństwo, że przy użyciu biblioteki `Point3D` często użyje źródła w całym kodzie.</span><span class="sxs-lookup"><span data-stu-id="fdde2-165">It's likely that the library using `Point3D` would often use the origin throughout the code.</span></span> <span data-ttu-id="fdde2-166">Każde wystąpienie tworzy nowy obiekt na stosie.</span><span class="sxs-lookup"><span data-stu-id="fdde2-166">Every instance creates a new object on the stack.</span></span> <span data-ttu-id="fdde2-167">Może być korzystne utworzyć stałą i zwracać jej przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="fdde2-167">It may be advantageous to create a constant and return it by reference.</span></span> <span data-ttu-id="fdde2-168">Jednak jeśli zwraca odwołanie do wewnętrznej pamięci masowej, może zajść potrzeba wymuszenia, że obiekt wywołujący nie można zmodyfikować magazynu, do którego istnieje odwołanie.</span><span class="sxs-lookup"><span data-stu-id="fdde2-168">But, if you return a reference to internal storage, you may want to enforce that the caller cannot modify the referenced storage.</span></span> <span data-ttu-id="fdde2-169">Poniższy kod definiuje właściwości tylko do odczytu, która zwraca `readonly ref` do `Point3D` , który określa punkt początkowy.</span><span class="sxs-lookup"><span data-stu-id="fdde2-169">The following code defines a read-only property that returns a `readonly ref` to a `Point3D` that specifies the origin.</span></span>

[!code-csharp[OriginReference](../../samples/csharp/reference-semantics/Point3D.cs#OriginReference "Creating a readonly Origin reference")]

<span data-ttu-id="fdde2-170">Tworzenie kopii tylko do odczytu ref zwracany jest prosty: tylko przypisz go do zmiennej, nie jest zadeklarowana z `ref readonly` modyfikator.</span><span class="sxs-lookup"><span data-stu-id="fdde2-170">Creating a copy of a ref readonly return is easy: Just assign it to a variable not declared with the `ref readonly` modifier.</span></span> <span data-ttu-id="fdde2-171">Kompilator generuje kod, aby skopiować obiekt w ramach przypisania.</span><span class="sxs-lookup"><span data-stu-id="fdde2-171">The compiler generates code to copy the object as part of the assignment.</span></span> 

<span data-ttu-id="fdde2-172">Po przypisaniu zmiennej `ref readonly return`, można określić `ref readonly` zmiennej lub kopię przez wartość odwołania tylko do odczytu:</span><span class="sxs-lookup"><span data-stu-id="fdde2-172">When you assign a variable to a `ref readonly return`, you can specify either a `ref readonly` variable, or a by-value copy of the readonly reference:</span></span>

[!code-csharp[AssignRefReadonly](../../samples/csharp/reference-semantics/Program.cs#AssignRefReadonly "Assigning a ref readonly")]

<span data-ttu-id="fdde2-173">Pierwsze przypisanie w poprzednim kodzie tworzy kopię `Origin` stała i przypisuje, które skopiować.</span><span class="sxs-lookup"><span data-stu-id="fdde2-173">The first assignment in the preceding code makes a copy of the `Origin` constant and assigns that copy.</span></span> <span data-ttu-id="fdde2-174">Drugi przypisuje odwołanie.</span><span class="sxs-lookup"><span data-stu-id="fdde2-174">The second assigns a reference.</span></span> <span data-ttu-id="fdde2-175">Zwróć uwagę, że `readonly` modyfikator musi być częścią deklaracja zmiennej.</span><span class="sxs-lookup"><span data-stu-id="fdde2-175">Notice that the `readonly` modifier must be part of the declaration of the variable.</span></span> <span data-ttu-id="fdde2-176">Nie można zmodyfikować odwołania, do którego się odwołuje.</span><span class="sxs-lookup"><span data-stu-id="fdde2-176">The reference to which it refers cannot be modified.</span></span> <span data-ttu-id="fdde2-177">Próbuje zrobić spowodować błąd kompilacji.</span><span class="sxs-lookup"><span data-stu-id="fdde2-177">Attempts to do so result in a compile-time error.</span></span>

## <a name="readonly-struct-type"></a><span data-ttu-id="fdde2-178">`readonly struct`Typ</span><span class="sxs-lookup"><span data-stu-id="fdde2-178">`readonly struct` type</span></span>

<span data-ttu-id="fdde2-179">Stosowanie `ref readonly` na wykorzystanie dużym natężeniu ruchu struktury mogą być wystarczające.</span><span class="sxs-lookup"><span data-stu-id="fdde2-179">Applying `ref readonly` to high-traffic uses of a struct may be sufficient.</span></span>
<span data-ttu-id="fdde2-180">Innym razem, można utworzyć niezmienialny struktury.</span><span class="sxs-lookup"><span data-stu-id="fdde2-180">Other times, you may want to create an immutable struct.</span></span> <span data-ttu-id="fdde2-181">Zawsze można następnie przekazać przez odwołanie tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="fdde2-181">Then you can always pass by readonly reference.</span></span> <span data-ttu-id="fdde2-182">Czy rozwiązanie usuwa ataku kopiuje tego miejsce, gdy uzyskujesz dostęp do metody struktury używane jako `in` parametru.</span><span class="sxs-lookup"><span data-stu-id="fdde2-182">That practice removes the defensive copies that take place when you access methods of a struct used as an `in` parameter.</span></span>

<span data-ttu-id="fdde2-183">Możesz to zrobić, tworząc `readonly struct` typu.</span><span class="sxs-lookup"><span data-stu-id="fdde2-183">You can do that by creating a `readonly struct` type.</span></span> <span data-ttu-id="fdde2-184">Możesz dodać `readonly` modyfikatora w deklaracji struktury.</span><span class="sxs-lookup"><span data-stu-id="fdde2-184">You can add the `readonly` modifier to a struct declaration.</span></span> <span data-ttu-id="fdde2-185">Kompilator wymusza, że wszystkie elementy członkowskie struktury są `readonly`; `struct` musi być niezmienialne.</span><span class="sxs-lookup"><span data-stu-id="fdde2-185">The compiler enforces that all members of the struct are `readonly`; the `struct` must be immutable.</span></span>

<span data-ttu-id="fdde2-186">Istnieją inne optymalizacje dla `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="fdde2-186">There are other optimizations for a `readonly struct`.</span></span> <span data-ttu-id="fdde2-187">Można użyć `in` modyfikator w każdej lokalizacji gdzie `readonly struct` jest argumentem.</span><span class="sxs-lookup"><span data-stu-id="fdde2-187">You can use the `in` modifier at every location where a `readonly struct` is an argument.</span></span> <span data-ttu-id="fdde2-188">Ponadto można zwrócić `readonly struct` jako `ref return` po zwróconego obiektu, którego okres istnienia wykracza poza zakres metoda zwraca obiekt.</span><span class="sxs-lookup"><span data-stu-id="fdde2-188">In addition, you can return a `readonly struct` as a `ref return` when you are returning an object whose lifetime extends beyond the scope of the method returning the object.</span></span>

<span data-ttu-id="fdde2-189">Na koniec kompilator generuje kod efektywniejsze podczas wywoływania członkami `readonly struct`: `this` odwołania, zamiast kopiowania odbiornika, jest zawsze `in` parametr przekazywany przez odwołanie do elementu członkowskiego.</span><span class="sxs-lookup"><span data-stu-id="fdde2-189">Finally, the compiler generates more efficient code when you call members of a `readonly struct`: The `this` reference, instead of a copy of the receiver, is always an `in` parameter passed by reference to the member method.</span></span> <span data-ttu-id="fdde2-190">Tego rodzaju optymalizacji zapisuje więcej kopiowania, korzystając z `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="fdde2-190">This optimization saves more copying when you use a `readonly struct`.</span></span> <span data-ttu-id="fdde2-191">`Point3D` Jest doskonałym kandydatem do tej zmiany.</span><span class="sxs-lookup"><span data-stu-id="fdde2-191">The `Point3D` is a great candidate for this change.</span></span> <span data-ttu-id="fdde2-192">Poniższy kod przedstawia zaktualizowaną `ReadonlyPoint3D` struktury:</span><span class="sxs-lookup"><span data-stu-id="fdde2-192">The following code shows an updated `ReadonlyPoint3D` structure:</span></span>

[!code-csharp[ReadonlyOnlyPoint3D](../../samples/csharp/reference-semantics/Point3D.cs#ReadonlyOnlyPoint3D "Defining an immutable structure")]

## <a name="ref-struct-type"></a><span data-ttu-id="fdde2-193">`ref struct`Typ</span><span class="sxs-lookup"><span data-stu-id="fdde2-193">`ref struct` type</span></span>

<span data-ttu-id="fdde2-194">Inna funkcja języka powiązane jest możliwość zadeklarować typu wartości, który musi znajdować się na stosie.</span><span class="sxs-lookup"><span data-stu-id="fdde2-194">Another related language feature is the ability to declare a value type that must be stack allocated.</span></span> <span data-ttu-id="fdde2-195">Innymi słowy te typy nigdy nie można utworzyć na stercie członkiem innej klasy.</span><span class="sxs-lookup"><span data-stu-id="fdde2-195">In other words, these types can never be created on the heap as a member of another class.</span></span> <span data-ttu-id="fdde2-196">Główną motywacją do tej funkcji został <xref:System.Span%601> i powiązanych struktury.</span><span class="sxs-lookup"><span data-stu-id="fdde2-196">The primary motivation for this feature was <xref:System.Span%601> and related structures.</span></span> <span data-ttu-id="fdde2-197"><xref:System.Span%601>może zawierać wskaźnika zarządzane jako jeden z jego elementów członkowskich, inne są długość zakresu.</span><span class="sxs-lookup"><span data-stu-id="fdde2-197"><xref:System.Span%601> may contain a managed pointer as one of its members, the other being the length of the span.</span></span> <span data-ttu-id="fdde2-198">Faktycznie wdrażana jest nieco inaczej ponieważ C# nie obsługuje wskaźników do pamięci zarządzanej poza niebezpiecznym kontekście.</span><span class="sxs-lookup"><span data-stu-id="fdde2-198">It's actually implemented a bit differently because C# doesn't support pointers to managed memory outside of an unsafe context.</span></span> <span data-ttu-id="fdde2-199">Wszelkie zapisu, która zmienia wskaźnika oraz długość nie jest atomic.</span><span class="sxs-lookup"><span data-stu-id="fdde2-199">Any write that changes the pointer and the length is not atomic.</span></span> <span data-ttu-id="fdde2-200">Oznacza to, że <xref:System.Span%601> będzie podlegać poza zakresem błędy lub innego typu naruszenia bezpieczeństwa zostały nie ograniczone do ramki stosu pojedynczego.</span><span class="sxs-lookup"><span data-stu-id="fdde2-200">That means a <xref:System.Span%601> would be subject to out of range errors or other type safety violations were it not constrained to a single stack frame.</span></span> <span data-ttu-id="fdde2-201">Ponadto zwykle umieszczenie wskaźnika zarządzanego na stercie GC ulega awarii podczas JIT.</span><span class="sxs-lookup"><span data-stu-id="fdde2-201">In addition, putting a managed pointer on the GC heap typically crashes at JIT time.</span></span>

<span data-ttu-id="fdde2-202">Może być podobne wymagania dotyczące pracy z pamięci utworzone za pomocą [ `stackalloc` ](language-reference/keywords/stackalloc.md) lub w przypadku używania pamięci z międzyoperacyjnego interfejsów API.</span><span class="sxs-lookup"><span data-stu-id="fdde2-202">You may have similar requirements working with memory created using [`stackalloc`](language-reference/keywords/stackalloc.md) or when using memory from interop APIs.</span></span> <span data-ttu-id="fdde2-203">Można definiować własnych `ref struct` typy dla tych potrzeb.</span><span class="sxs-lookup"><span data-stu-id="fdde2-203">You can define your own `ref struct` types for those needs.</span></span> <span data-ttu-id="fdde2-204">W tym artykule, zobacz przykłady użycia `Span<T>` dla uproszczenia.</span><span class="sxs-lookup"><span data-stu-id="fdde2-204">In this article, you see examples using `Span<T>` for simplicity.</span></span>

<span data-ttu-id="fdde2-205">`ref struct` Deklaracji deklaruje, że struktura tego typu musi być na stosie.</span><span class="sxs-lookup"><span data-stu-id="fdde2-205">The `ref struct` declaration declares that a struct of this type must be on the stack.</span></span> <span data-ttu-id="fdde2-206">Reguły języka zapewnienia bezpiecznego korzystania z tych typów.</span><span class="sxs-lookup"><span data-stu-id="fdde2-206">The language rules ensure the safe use of these types.</span></span> <span data-ttu-id="fdde2-207">Inne typy zadeklarowane jako `ref struct` obejmują <xref:System.ReadOnlySpan%601>.</span><span class="sxs-lookup"><span data-stu-id="fdde2-207">Other types declared as `ref struct` include <xref:System.ReadOnlySpan%601>.</span></span> 

<span data-ttu-id="fdde2-208">Celem porządkowania `ref struct` typu jako zmienną przydzielony stos wprowadzono kilka reguł, które kompilator wymusza dla wszystkich `ref struct` typów.</span><span class="sxs-lookup"><span data-stu-id="fdde2-208">The goal of keeping a `ref struct` type as a stack-allocated variable introduces several rules that the compiler enforces for all `ref struct` types.</span></span>

- <span data-ttu-id="fdde2-209">Nie można polu `ref struct`.</span><span class="sxs-lookup"><span data-stu-id="fdde2-209">You can't box a `ref struct`.</span></span> <span data-ttu-id="fdde2-210">Nie można przypisać `ref struct` typ zmiennej typu `object`, `dynamic`, albo typu interfejsu.</span><span class="sxs-lookup"><span data-stu-id="fdde2-210">You cannot assign a `ref struct` type to a variable of type `object`, `dynamic`, or any interface type.</span></span>
- <span data-ttu-id="fdde2-211">Nie można zadeklarować `ref struct` jako element członkowski klasy lub struktury normalnego.</span><span class="sxs-lookup"><span data-stu-id="fdde2-211">You can't declare a `ref struct` as a member of a class or a normal struct.</span></span>
- <span data-ttu-id="fdde2-212">Nie można deklarować zmiennych lokalnych, które są `ref struct` typów w metodach asynchronicznych.</span><span class="sxs-lookup"><span data-stu-id="fdde2-212">You cannot declare local variables that are `ref struct` types in async methods.</span></span> <span data-ttu-id="fdde2-213">Można je zadeklarować w metod synchronicznych, które zwracają `Task`, `Task<T>` lub typów zadań.</span><span class="sxs-lookup"><span data-stu-id="fdde2-213">You can declare them in synchronous methods that return `Task`, `Task<T>` or Task-like types.</span></span>
- <span data-ttu-id="fdde2-214">Nie można zadeklarować `ref struct` zmiennych lokalnych w iteratorach.</span><span class="sxs-lookup"><span data-stu-id="fdde2-214">You cannot declare `ref struct` local variables in iterators.</span></span>
- <span data-ttu-id="fdde2-215">Nie można przechwycić `ref struct` zmiennych w wyrażeniach lambda lub funkcje lokalne.</span><span class="sxs-lookup"><span data-stu-id="fdde2-215">You cannot capture `ref struct` variables in lambda expressions or local functions.</span></span>

<span data-ttu-id="fdde2-216">Te ograniczenia upewnij się, że przypadkowo nieużywanie `ref struct` w taki sposób, który można podwyższyć sterty zarządzanej.</span><span class="sxs-lookup"><span data-stu-id="fdde2-216">These restrictions ensure that you do not accidentally use a `ref struct` in a manner that could promote it to the managed heap.</span></span>

## <a name="conclusions"></a><span data-ttu-id="fdde2-217">Wnioski</span><span class="sxs-lookup"><span data-stu-id="fdde2-217">Conclusions</span></span>

<span data-ttu-id="fdde2-218">Te ulepszenia języka C# są przeznaczone dla algorytmów krytyczne wydajności, których alokacji pamięci może być krytyczne znaczenie dla osiągnięcia niezbędne wydajności.</span><span class="sxs-lookup"><span data-stu-id="fdde2-218">These enhancements to the C# language are designed for performance critical algorithms where memory allocations can be critical to achieving the necessary performance.</span></span> <span data-ttu-id="fdde2-219">Może się okazać, że nie są często używane te funkcje w kodzie zostanie zapisany.</span><span class="sxs-lookup"><span data-stu-id="fdde2-219">You may find that you don't often use these features in the code you write.</span></span> <span data-ttu-id="fdde2-220">Jednak te ulepszenia zostały przyjęte w wielu lokalizacjach w programie .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="fdde2-220">However, these enhancements have been adopted in many locations in the .NET Framework.</span></span> <span data-ttu-id="fdde2-221">Jak coraz więcej interfejsów API należy używać tych funkcji, zobaczysz poprawy wydajności własnych aplikacji.</span><span class="sxs-lookup"><span data-stu-id="fdde2-221">As more and more APIs make use of these features, you'll see the performance of your own applications improve.</span></span>
