---
title: Ograniczenia parametrów typu - Przewodnik programowania C#
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 76cd00b9c84f128d2a181115293df910d8deb6cb
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/14/2020
ms.locfileid: "79399799"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="beadc-102">Ograniczenia parametrów typu (Przewodnik programowania C#)</span><span class="sxs-lookup"><span data-stu-id="beadc-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="beadc-103">Ograniczenia informują kompilator o możliwościach, które musi mieć argument typu.</span><span class="sxs-lookup"><span data-stu-id="beadc-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="beadc-104">Bez żadnych ograniczeń argument typu może być dowolny typ.</span><span class="sxs-lookup"><span data-stu-id="beadc-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="beadc-105">Kompilator może tylko <xref:System.Object?displayProperty=nameWithType>przyjąć członków , który jest ostateczną klasą podstawową dla dowolnego typu .NET.</span><span class="sxs-lookup"><span data-stu-id="beadc-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="beadc-106">Aby uzyskać więcej informacji, zobacz [Dlaczego warto używać ograniczeń](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="beadc-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="beadc-107">Jeśli kod klienta próbuje utworzyć wystąpienie klasy przy użyciu typu, który nie jest dozwolony przez ograniczenie, wynik jest błąd w czasie kompilacji.</span><span class="sxs-lookup"><span data-stu-id="beadc-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="beadc-108">Ograniczenia są określane przy `where` użyciu kontekstowe słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="beadc-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="beadc-109">W poniższej tabeli wymieniono siedem typów ograniczeń:</span><span class="sxs-lookup"><span data-stu-id="beadc-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="beadc-110">Ograniczenie</span><span class="sxs-lookup"><span data-stu-id="beadc-110">Constraint</span></span>|<span data-ttu-id="beadc-111">Opis</span><span class="sxs-lookup"><span data-stu-id="beadc-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="beadc-112">Argument typu musi być typem wartości niepodlegających wartości null.</span><span class="sxs-lookup"><span data-stu-id="beadc-112">The type argument must be a non-nullable value type.</span></span> <span data-ttu-id="beadc-113">Aby uzyskać informacje o typach wartości nullable, zobacz [Typy wartości nullable](../../language-reference/builtin-types/nullable-value-types.md).</span><span class="sxs-lookup"><span data-stu-id="beadc-113">For information about nullable value types, see [Nullable value types](../../language-reference/builtin-types/nullable-value-types.md).</span></span> <span data-ttu-id="beadc-114">Ponieważ wszystkie typy wartości mają dostępny konstruktor bezparametrów, `struct` ograniczenie implikuje `new()` ograniczenie i `new()` nie można połączyć z ograniczeniem.</span><span class="sxs-lookup"><span data-stu-id="beadc-114">Because all value types have an accessible parameterless constructor, the `struct` constraint implies the `new()` constraint and can't be combined with the `new()` constraint.</span></span> <span data-ttu-id="beadc-115">Nie można również `struct` połączyć ograniczenia `unmanaged` z ograniczeniem.</span><span class="sxs-lookup"><span data-stu-id="beadc-115">You also cannot combine the `struct` constraint with the `unmanaged` constraint.</span></span>|
|`where T : class`|<span data-ttu-id="beadc-116">Argument typu musi być typem odwołania.</span><span class="sxs-lookup"><span data-stu-id="beadc-116">The type argument must be a reference type.</span></span> <span data-ttu-id="beadc-117">To ograniczenie dotyczy również dowolnej klasy, interfejsu, delegata lub typu tablicy.</span><span class="sxs-lookup"><span data-stu-id="beadc-117">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="beadc-118">Argument typu musi być typem niepodlegającym null.</span><span class="sxs-lookup"><span data-stu-id="beadc-118">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="beadc-119">Argument może być typem odwołania niedopuszczającym do wartości null w języku C# 8.0 lub nowszym lub typem wartości niepodlegających zerwaniu.</span><span class="sxs-lookup"><span data-stu-id="beadc-119">The argument can be a non-nullable reference type in C# 8.0 or later, or a not nullable value type.</span></span> <span data-ttu-id="beadc-120">To ograniczenie dotyczy również dowolnej klasy, interfejsu, delegata lub typu tablicy.</span><span class="sxs-lookup"><span data-stu-id="beadc-120">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="beadc-121">Argument typu musi być [typem niepodlegającym](../../language-reference/builtin-types/unmanaged-types.md)null.</span><span class="sxs-lookup"><span data-stu-id="beadc-121">The type argument must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="beadc-122">Ograniczenie `unmanaged` oznacza `struct` ograniczenie i nie można łączyć z `struct` lub `new()` ograniczenia.</span><span class="sxs-lookup"><span data-stu-id="beadc-122">The `unmanaged` constraint implies the `struct` constraint and can't be combined with either the `struct` or `new()` constraints.</span></span>|
|`where T : new()`|<span data-ttu-id="beadc-123">Argument typu musi mieć konstruktora public parameterless.</span><span class="sxs-lookup"><span data-stu-id="beadc-123">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="beadc-124">W połączeniu `new()` z innymi ograniczeniami ograniczenie musi być określone jako ostatnie.</span><span class="sxs-lookup"><span data-stu-id="beadc-124">When used together with other constraints, the `new()` constraint must be specified last.</span></span> <span data-ttu-id="beadc-125">Ograniczenia `new()` nie można łączyć z ograniczeniami `struct` `unmanaged` i.</span><span class="sxs-lookup"><span data-stu-id="beadc-125">The `new()` constraint can't be combined with the `struct` and `unmanaged` constraints.</span></span>|
|<span data-ttu-id="beadc-126">`where T :`*nazwa klasy podstawowej>\<*</span><span class="sxs-lookup"><span data-stu-id="beadc-126">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="beadc-127">Argument typu musi być lub pochodzić z określonej klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="beadc-127">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="beadc-128">`where T :`nazwa interfejsu>\* \<\*</span><span class="sxs-lookup"><span data-stu-id="beadc-128">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="beadc-129">Argument typu musi być lub zaimplementować określony interfejs.</span><span class="sxs-lookup"><span data-stu-id="beadc-129">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="beadc-130">Można określić wiele ograniczeń interfejsu.</span><span class="sxs-lookup"><span data-stu-id="beadc-130">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="beadc-131">Interfejs ograniczający może być również ogólny.</span><span class="sxs-lookup"><span data-stu-id="beadc-131">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="beadc-132">Argument typu podany dla T musi być lub pochodzić z argumentu dostarczonego dla U.</span><span class="sxs-lookup"><span data-stu-id="beadc-132">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

## <a name="why-use-constraints"></a><span data-ttu-id="beadc-133">Dlaczego warto używać ograniczeń</span><span class="sxs-lookup"><span data-stu-id="beadc-133">Why use constraints</span></span>

<span data-ttu-id="beadc-134">Ograniczając parametr type, można zwiększyć liczbę dozwolonych operacji i wywołań metody do tych obsługiwanych przez typ ograniczający i wszystkie typy w hierarchii dziedziczenia.</span><span class="sxs-lookup"><span data-stu-id="beadc-134">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="beadc-135">Podczas projektowania ogólnych klas lub metod, jeśli będziesz wykonywać dowolną operację na członków ogólnych <xref:System.Object?displayProperty=nameWithType>poza proste przypisania lub wywołanie jakichkolwiek metod nie obsługiwane przez , należy zastosować ograniczenia do parametru typu.</span><span class="sxs-lookup"><span data-stu-id="beadc-135">When you design generic classes or methods, if you'll be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="beadc-136">Na przykład ograniczenie klasy podstawowej informuje kompilator, że tylko obiekty tego typu lub pochodzące z tego typu będą używane jako argumenty typu.</span><span class="sxs-lookup"><span data-stu-id="beadc-136">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="beadc-137">Gdy kompilator ma tę gwarancję, może zezwolić na metody tego typu, które mają być wywoływane w klasie ogólnej.</span><span class="sxs-lookup"><span data-stu-id="beadc-137">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="beadc-138">W poniższym przykładzie kodu przedstawiono funkcje, `GenericList<T>` które można dodać do klasy (w [wprowadzenie do ogólnych)](../../../standard/generics/index.md)przez zastosowanie ograniczenia klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="beadc-138">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](../../../standard/generics/index.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="beadc-139">Ograniczenie umożliwia klasy ogólnej do `Employee.Name` korzystania z właściwości.</span><span class="sxs-lookup"><span data-stu-id="beadc-139">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="beadc-140">Ograniczenie określa, że wszystkie elementy `T` typu są gwarantowane jako `Employee` obiekt lub obiekt, `Employee`który dziedziczy z .</span><span class="sxs-lookup"><span data-stu-id="beadc-140">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="beadc-141">Wiele ograniczeń można zastosować do tego samego parametru typu, a same ograniczenia mogą być typami ogólnymi w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="beadc-141">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="beadc-142">Podczas stosowania `where T : class` ograniczenia, należy `==` unikać `!=` i operatorów na parametr typu, ponieważ te operatory będą testować tylko dla tożsamości odniesienia, a nie dla równości wartości.</span><span class="sxs-lookup"><span data-stu-id="beadc-142">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="beadc-143">To zachowanie występuje, nawet jeśli te operatory są przeciążone w typie, który jest używany jako argument.</span><span class="sxs-lookup"><span data-stu-id="beadc-143">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="beadc-144">Poniższy kod ilustruje ten punkt; dane wyjściowe jest false, mimo <xref:System.String> `==` że klasa przeciąża operatora.</span><span class="sxs-lookup"><span data-stu-id="beadc-144">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="beadc-145">Kompilator wie `T` tylko, że jest typem odwołania w czasie kompilacji i musi używać domyślnych operatorów, które są prawidłowe dla wszystkich typów odwołań.</span><span class="sxs-lookup"><span data-stu-id="beadc-145">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="beadc-146">Jeśli należy przetestować równości wartości, zalecanym sposobem `where T : IEquatable<T>` jest `where T : IComparable<T>` również zastosowanie lub ograniczenie i zaimplementować interfejs w dowolnej klasie, która będzie używana do konstruowania klasy ogólnej.</span><span class="sxs-lookup"><span data-stu-id="beadc-146">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="beadc-147">Ograniczanie wielu parametrów</span><span class="sxs-lookup"><span data-stu-id="beadc-147">Constraining multiple parameters</span></span>

<span data-ttu-id="beadc-148">Ograniczenia do wielu parametrów i wiele wiązań można zastosować do jednego parametru, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="beadc-148">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="beadc-149">Parametry typu niezwiązanego</span><span class="sxs-lookup"><span data-stu-id="beadc-149">Unbounded type parameters</span></span>

 <span data-ttu-id="beadc-150">Parametry typu, które nie mają żadnych ograniczeń, takie jak T w klasie `SampleClass<T>{}`publicznej, są nazywane parametrami typu niezwiązanego.</span><span class="sxs-lookup"><span data-stu-id="beadc-150">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="beadc-151">Parametry typu niezwiązanego mają następujące reguły:</span><span class="sxs-lookup"><span data-stu-id="beadc-151">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="beadc-152">`!=` Operatory `==` i nie mogą być używane, ponieważ nie ma gwarancji, że konkretny argument typu będzie obsługiwać te operatory.</span><span class="sxs-lookup"><span data-stu-id="beadc-152">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="beadc-153">Można je konwertować na `System.Object` i z lub jawnie konwertowane na dowolny typ interfejsu.</span><span class="sxs-lookup"><span data-stu-id="beadc-153">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="beadc-154">Można porównać je z [wartością null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="beadc-154">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="beadc-155">Jeśli parametr niezwiązany jest `null`porównywany do , porównanie zawsze zwraca false, jeśli argument typu jest typem wartości.</span><span class="sxs-lookup"><span data-stu-id="beadc-155">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="beadc-156">Wpisywanie parametrów jako ograniczeń</span><span class="sxs-lookup"><span data-stu-id="beadc-156">Type parameters as constraints</span></span>

<span data-ttu-id="beadc-157">Użycie parametru typu ogólnego jako ograniczenia jest przydatne, gdy funkcja elementu członkowskiego z własnym parametrem type musi ograniczyć ten parametr do parametru typu typu zawierającego, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="beadc-157">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="beadc-158">W poprzednim przykładzie `T` jest ograniczenie typu w kontekście `Add` metody i parametr typu nieograniczonego w `List` kontekście klasy.</span><span class="sxs-lookup"><span data-stu-id="beadc-158">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="beadc-159">Parametry typu mogą być również używane jako ograniczenia w ogólnych definicjach klas.</span><span class="sxs-lookup"><span data-stu-id="beadc-159">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="beadc-160">Parametr typu musi być zadeklarowany w nawiasach kątowych wraz z innymi parametrami typu:</span><span class="sxs-lookup"><span data-stu-id="beadc-160">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="beadc-161">Przydatność parametrów typu jako ograniczeń z klasami ogólnymi jest ograniczona, ponieważ kompilator nie może zakładać nic o parametrze typu, z wyjątkiem, że pochodzi z `System.Object`.</span><span class="sxs-lookup"><span data-stu-id="beadc-161">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="beadc-162">Użyj parametrów typu jako ograniczeń dla klas ogólnych w scenariuszach, w których chcesz wymusić relację dziedziczenia między dwoma parametrami typu.</span><span class="sxs-lookup"><span data-stu-id="beadc-162">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="beadc-163">Ograniczenie NotNull</span><span class="sxs-lookup"><span data-stu-id="beadc-163">NotNull constraint</span></span>

<span data-ttu-id="beadc-164">Począwszy od języka C# 8.0, można użyć `notnull` ograniczenia, aby określić, że argument typu musi być typem wartości niepodlegającym wartości null lub typem odwołania niepodlegającym zerowaniu.</span><span class="sxs-lookup"><span data-stu-id="beadc-164">Beginning with C# 8.0, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="beadc-165">Ograniczenie `notnull` może być używane tylko `nullable enable` w kontekście.</span><span class="sxs-lookup"><span data-stu-id="beadc-165">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="beadc-166">Kompilator generuje ostrzeżenie, jeśli `notnull` dodasz ograniczenie w kontekście nullable nieświadomych.</span><span class="sxs-lookup"><span data-stu-id="beadc-166">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span>

<span data-ttu-id="beadc-167">W przeciwieństwie do innych ograniczeń, `notnull` gdy argument typu narusza ograniczenie, kompilator generuje `nullable enable` ostrzeżenie, gdy ten kod jest kompilowany w kontekście.</span><span class="sxs-lookup"><span data-stu-id="beadc-167">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="beadc-168">Jeśli kod jest kompilowany w kontekście nullable nieświadomych, kompilator nie generuje żadnych ostrzeżeń lub błędów.</span><span class="sxs-lookup"><span data-stu-id="beadc-168">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="beadc-169">Ograniczenie niezarządzane</span><span class="sxs-lookup"><span data-stu-id="beadc-169">Unmanaged constraint</span></span>

<span data-ttu-id="beadc-170">Począwszy od języka C# 7.3, można użyć `unmanaged` ograniczenia, aby określić, że parametr type musi być [typem niepodlegającym](../../language-reference/builtin-types/unmanaged-types.md)null.</span><span class="sxs-lookup"><span data-stu-id="beadc-170">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="beadc-171">Ograniczenie `unmanaged` umożliwia pisanie procedur wielokrotnego użytku do pracy z typami, które mogą być manipulowane jako bloki pamięci, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="beadc-171">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="beadc-172">Powyższa metoda musi być skompilowana w `unsafe` `sizeof` kontekście, ponieważ używa operatora na typ nie wiadomo, że jest typem wbudowanym.</span><span class="sxs-lookup"><span data-stu-id="beadc-172">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="beadc-173">Bez `unmanaged` ograniczenia `sizeof` operator jest niedostępny.</span><span class="sxs-lookup"><span data-stu-id="beadc-173">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

<span data-ttu-id="beadc-174">Ograniczenie `unmanaged` oznacza `struct` ograniczenie i nie można z nim łączyć.</span><span class="sxs-lookup"><span data-stu-id="beadc-174">The `unmanaged` constraint implies the `struct` constraint and can't be combined with it.</span></span> <span data-ttu-id="beadc-175">Ponieważ `struct` ograniczenie implikuje `new()` ograniczenie, `unmanaged` ograniczenia nie można również łączyć z `new()` ograniczeniem.</span><span class="sxs-lookup"><span data-stu-id="beadc-175">Because the `struct` constraint implies the `new()` constraint, the `unmanaged` constraint can't be combined with the `new()` constraint as well.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="beadc-176">Delegowanie ograniczeń</span><span class="sxs-lookup"><span data-stu-id="beadc-176">Delegate constraints</span></span>

<span data-ttu-id="beadc-177">Również począwszy od Języka C# <xref:System.Delegate?displayProperty=nameWithType> 7.3, można użyć lub <xref:System.MulticastDelegate?displayProperty=nameWithType> jako ograniczenie klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="beadc-177">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="beadc-178">CLR zawsze dozwolone to ograniczenie, ale język C# nie dozwolone.</span><span class="sxs-lookup"><span data-stu-id="beadc-178">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="beadc-179">Ograniczenie `System.Delegate` umożliwia pisanie kodu, który działa z delegatów w sposób bezpieczny dla typu.</span><span class="sxs-lookup"><span data-stu-id="beadc-179">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="beadc-180">Poniższy kod definiuje metodę rozszerzenia, która łączy dwóch delegatów, pod warunkiem, że są one tego samego typu:</span><span class="sxs-lookup"><span data-stu-id="beadc-180">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="beadc-181">Powyższa metoda służy do łączenia delegatów, które są tego samego typu:</span><span class="sxs-lookup"><span data-stu-id="beadc-181">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="beadc-182">Jeśli odkomentujesz ostatni wiersz, nie zostanie on skompilowany.</span><span class="sxs-lookup"><span data-stu-id="beadc-182">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="beadc-183">Oba `first` `test` typy i są delegowane, ale są to różne typy delegatów.</span><span class="sxs-lookup"><span data-stu-id="beadc-183">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="beadc-184">Ograniczenia wyliczenia</span><span class="sxs-lookup"><span data-stu-id="beadc-184">Enum constraints</span></span>

<span data-ttu-id="beadc-185">Począwszy od C# 7.3, <xref:System.Enum?displayProperty=nameWithType> można również określić typ jako ograniczenie klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="beadc-185">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="beadc-186">CLR zawsze dozwolone to ograniczenie, ale język C# nie dozwolone.</span><span class="sxs-lookup"><span data-stu-id="beadc-186">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="beadc-187">Rodzajowe `System.Enum` przy użyciu zapewniają programowanie bezpieczne dla typu `System.Enum`do buforowania wyników przy użyciu metod statycznych w programie .</span><span class="sxs-lookup"><span data-stu-id="beadc-187">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="beadc-188">Poniższy przykład znajduje wszystkie prawidłowe wartości dla typu wyliczenia, a następnie tworzy słownik, który mapuje te wartości na jego reprezentację ciągu.</span><span class="sxs-lookup"><span data-stu-id="beadc-188">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the enum constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="beadc-189">Metody używane do korzystania z odbicia, co ma wpływ na wydajność.</span><span class="sxs-lookup"><span data-stu-id="beadc-189">The methods used to make use of reflection, which has performance implications.</span></span> <span data-ttu-id="beadc-190">Można wywołać tę metodę do tworzenia kolekcji, która jest buforowana i ponownie używane, a nie powtarzanie wywołań, które wymagają odbicia.</span><span class="sxs-lookup"><span data-stu-id="beadc-190">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="beadc-191">Można go użyć, jak pokazano w poniższym przykładzie, aby utworzyć wyliczenie i utworzyć słownik jego wartości i nazw:</span><span class="sxs-lookup"><span data-stu-id="beadc-191">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[enum definition](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the enum constrained method](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="beadc-192">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="beadc-192">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="beadc-193">Przewodnik programowania języka C#</span><span class="sxs-lookup"><span data-stu-id="beadc-193">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="beadc-194">Wprowadzenie do typów ogólnych</span><span class="sxs-lookup"><span data-stu-id="beadc-194">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="beadc-195">Klasy ogólne</span><span class="sxs-lookup"><span data-stu-id="beadc-195">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="beadc-196">nowe ograniczenie</span><span class="sxs-lookup"><span data-stu-id="beadc-196">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
