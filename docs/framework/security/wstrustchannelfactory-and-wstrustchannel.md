---
title: Obiekt WSTrustChannelFactory i WSTrustChannel
ms.date: 03/30/2017
ms.assetid: 96cec467-e963-4132-b18b-7d0b3a2e979f
author: BrucePerlerMS
manager: mbaldwin
ms.openlocfilehash: 3fabd7e3cad76e17061751d0fe0bb4ae06bf6fb6
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 05/04/2018
---
# <a name="wstrustchannelfactory-and-wstrustchannel"></a><span data-ttu-id="30abb-102">Obiekt WSTrustChannelFactory i WSTrustChannel</span><span class="sxs-lookup"><span data-stu-id="30abb-102">WSTrustChannelFactory and WSTrustChannel</span></span>
<span data-ttu-id="30abb-103">Jeśli znasz już z programu Windows Communication Foundation (WCF), wiesz klienta WCF jest już pamiętać federacyjnego.</span><span class="sxs-lookup"><span data-stu-id="30abb-103">If you are already familiar with Windows Communication Foundation (WCF), you know that a WCF client is already federation aware.</span></span> <span data-ttu-id="30abb-104">Konfigurując klienta WCF z <xref:System.ServiceModel.WSFederationHttpBinding> lub podobne niestandardowe powiązanie, można włączyć uwierzytelnianie Sfederowane z usługą.</span><span class="sxs-lookup"><span data-stu-id="30abb-104">By configuring a WCF client with a <xref:System.ServiceModel.WSFederationHttpBinding> or similar custom binding, you can enable federated authentication to a service.</span></span>  
  
 <span data-ttu-id="30abb-105">Usługi WCF uzyskuje token wystawiony przez usługę tokenu zabezpieczającego (STS) w tle, który używa tego tokenu do uwierzytelnienia w usłudze.</span><span class="sxs-lookup"><span data-stu-id="30abb-105">WCF obtains the token that is issued by the security token service (STS) behind the scenes and uses this token to authenticate to the service.</span></span> <span data-ttu-id="30abb-106">Główne ograniczenia tego podejścia jest nie wgląd w komunikacji klienta z serwerem.</span><span class="sxs-lookup"><span data-stu-id="30abb-106">The main limitation to this approach is that there is no visibility into the client’s communications with the server.</span></span> <span data-ttu-id="30abb-107">Usługi WCF automatycznie generuje żądanie tokenu zabezpieczającego (RST) do usługi STS na podstawie parametrów wystawionego tokenu w powiązaniu.</span><span class="sxs-lookup"><span data-stu-id="30abb-107">WCF automatically generates the request security token (RST) to the STS based on the issued token parameters on the binding.</span></span> <span data-ttu-id="30abb-108">To oznacza, że klient nie różnią się parametrami RST na żądanie, sprawdzić odpowiedzi tokenu zabezpieczeń (RSTR), aby uzyskać informacje, takie jak wyświetlanie oświadczeń żądania lub pamięci podręcznej tokenu do użytku w przyszłości.</span><span class="sxs-lookup"><span data-stu-id="30abb-108">This means that the client cannot vary the RST parameters per request, inspect the request security token response (RSTR) to get information such as display claims, or cache the token for future use.</span></span>  
  
 <span data-ttu-id="30abb-109">Obecnie klienta WCF jest odpowiednia dla Federacji podstawowe scenariusze.</span><span class="sxs-lookup"><span data-stu-id="30abb-109">Currently, the WCF client is suitable for basic federation scenarios.</span></span> <span data-ttu-id="30abb-110">Jednak jeden z głównych scenariuszy obsługiwanych przez usługę Windows Identity Foundation (WIF) wymaga kontrolę nad RST na poziomie, który WCF nie pozwolą.</span><span class="sxs-lookup"><span data-stu-id="30abb-110">However, one of the major scenarios that Windows Identity Foundation (WIF) supports requires control over the RST at a level that WCF does not easily allow.</span></span> <span data-ttu-id="30abb-111">W związku z tym WIF dodatkowe funkcje, które zapewniają większą kontrolę nad komunikacji z STS.</span><span class="sxs-lookup"><span data-stu-id="30abb-111">Therefore, WIF adds features that give you more control over communication with the STS.</span></span>  
  
 <span data-ttu-id="30abb-112">WIF obsługuje następujące scenariusze federacyjnej:</span><span class="sxs-lookup"><span data-stu-id="30abb-112">WIF supports the following federation scenarios:</span></span>  
  
-   <span data-ttu-id="30abb-113">Za pomocą klienta WCF bez zależności WIF do uwierzytelniania usługi federacyjnej</span><span class="sxs-lookup"><span data-stu-id="30abb-113">Using a WCF client without any WIF dependencies to authenticate to a federated service</span></span>  
  
-   <span data-ttu-id="30abb-114">Włączanie programu WIF na klienta WCF można wstawić elementu ActAs lub OnBehalfOf do RST do usługi STS</span><span class="sxs-lookup"><span data-stu-id="30abb-114">Enabling WIF on a WCF client to insert an ActAs or OnBehalfOf element into the RST to the STS</span></span>  
  
-   <span data-ttu-id="30abb-115">Przy użyciu programu WIF wyłącznie do uzyskania tokenu z STS, a następnie włączyć klienta programu WCF do uwierzytelniania za pomocą tego tokenu.</span><span class="sxs-lookup"><span data-stu-id="30abb-115">Using WIF alone to obtain a token from the STS and then enable a WCF client to authenticate with this token.</span></span> <span data-ttu-id="30abb-116">Aby uzyskać więcej informacji, zobacz [ClaimsAwareWebService](http://go.microsoft.com/fwlink/?LinkID=248406) próbki.</span><span class="sxs-lookup"><span data-stu-id="30abb-116">For more information, see [ClaimsAwareWebService](http://go.microsoft.com/fwlink/?LinkID=248406) sample.</span></span>  
  
 <span data-ttu-id="30abb-117">Pierwszy scenariusz jest oczywiste: WCF istniejący klienci będą nadal działać z WIF jednostek uzależnionych i STSs.</span><span class="sxs-lookup"><span data-stu-id="30abb-117">The first scenario is self-explanatory: Existing WCF clients will continue to work with WIF relying parties and STSs.</span></span> <span data-ttu-id="30abb-118">W tym temacie omówiono dwa scenariusze pozostałych.</span><span class="sxs-lookup"><span data-stu-id="30abb-118">This topic discusses the remaining two scenarios.</span></span>  
  
## <a name="enhancing-an-existing-wcf-client-with-actas--onbehalfof"></a><span data-ttu-id="30abb-119">Udoskonalanie istniejącego klienta WCF z ActAs / OnBehalfOf</span><span class="sxs-lookup"><span data-stu-id="30abb-119">Enhancing an Existing WCF Client with ActAs / OnBehalfOf</span></span>  
 <span data-ttu-id="30abb-120">W typowy scenariusz delegowania tożsamości klient wywołuje usługi warstwy środkowej, który wywołuje usługi zaplecza.</span><span class="sxs-lookup"><span data-stu-id="30abb-120">In a typical identity delegation scenario, a client calls a middle-tier service, which then calls a back-end service.</span></span> <span data-ttu-id="30abb-121">Usługa warstwy środkowej zachowuje się jak lub działa w imieniu klienta.</span><span class="sxs-lookup"><span data-stu-id="30abb-121">The middle-tier service acts as, or acts on behalf of, the client.</span></span>  
  
> [!TIP]
>  <span data-ttu-id="30abb-122">Jaka jest różnica między ActAs i OnBehalfOf?</span><span class="sxs-lookup"><span data-stu-id="30abb-122">What is the difference between ActAs and OnBehalfOf?</span></span>  
>   
>  <span data-ttu-id="30abb-123">Z punktu widzenia protokołu WS-Trust:</span><span class="sxs-lookup"><span data-stu-id="30abb-123">From the WS-Trust procotol standpoint:</span></span>  
>   
>  1.  <span data-ttu-id="30abb-124">Element ActAs RST wskazuje, czy obiekt żądający oczekuje, że token zawiera oświadczenia około dwie różne jednostki: obiekt żądający i reprezentowany przez tokenu w elemencie ActAs jednostki zewnętrznej.</span><span class="sxs-lookup"><span data-stu-id="30abb-124">An ActAs RST element indicates that the requestor wants a token that contains claims about two distinct entities: the requestor, and an external entity represented by the token in the ActAs element.</span></span>  
> 2.  <span data-ttu-id="30abb-125">Element OnBehalfOf RST wskazuje, czy obiekt żądający oczekuje, że token zawiera oświadczenia dotyczące tylko jednego jednostkę: zewnętrznej jednostki reprezentowanej przez tokenu w elemencie OnBehalfOf.</span><span class="sxs-lookup"><span data-stu-id="30abb-125">An OnBehalfOf RST element indicates that the requestor wants a token that contains claims only about one entity: the external entity represented by the token in the OnBehalfOf element.</span></span>  
>   
>  <span data-ttu-id="30abb-126">Funkcja ActAs jest zwykle używanych w scenariuszach wymagających złożonego delegowania, gdzie końcowym odbiorcą wystawionego tokenu sprawdzić delegowania całego łańcucha zobaczyć, a nie tylko klienta, ale wszystkie pośredników.</span><span class="sxs-lookup"><span data-stu-id="30abb-126">The ActAs feature is typically used in scenarios that require composite delegation, where the final recipient of the issued token can inspect the entire delegation chain and see not just the client, but all intermediaries.</span></span> <span data-ttu-id="30abb-127">W ten sposób wykonywania kontroli dostępu, inspekcji i inne powiązane działania oparte na tożsamości całego łańcucha delegowania.</span><span class="sxs-lookup"><span data-stu-id="30abb-127">This lets it perform access control, auditing and other related activities based on the entire identity delegation chain.</span></span> <span data-ttu-id="30abb-128">Funkcja ActAs jest najczęściej używany w systemach wielowarstwowych do uwierzytelniania i przekazywania informacji o tożsamości między warstwami bez konieczności przekazać te informacje w warstwie logiki aplikacji/biznesowej.</span><span class="sxs-lookup"><span data-stu-id="30abb-128">The ActAs feature is commonly used in multi-tiered systems to authenticate and pass information about identities between the tiers without having to pass this information at the application/business logic layer.</span></span>  
>   
>  <span data-ttu-id="30abb-129">Funkcja OnBehalfOf jest używana w scenariuszach, w którym tożsamości klienta oryginalnym jest ważne i efektywnie jest taka sama jak tożsamość personifikacji funkcji dostępnych w systemie Windows.</span><span class="sxs-lookup"><span data-stu-id="30abb-129">The OnBehalfOf feature is used in scenarios where only the identity of the original client is important and is effectively the same as the identity impersonation feature available in Windows.</span></span> <span data-ttu-id="30abb-130">Użycie OnBehalfOf końcowym odbiorcą wystawiony token może zobaczyć tylko oświadczenia dotyczące klienta oryginalnym i informacji o pośredników nie są zachowywane.</span><span class="sxs-lookup"><span data-stu-id="30abb-130">When OnBehalfOf is used, the final recipient of the issued token can only see claims about the original client, and the information about intermediaries is not preserved.</span></span> <span data-ttu-id="30abb-131">Jednego z typowych wzorzec, gdy jest używana funkcja OnBehalfOf wzorzec serwera proxy, gdy klient nie może uzyskać dostępu usługi STS bezpośrednio, ale zamiast tego komunikuje się za pośrednictwem bramy serwera proxy.</span><span class="sxs-lookup"><span data-stu-id="30abb-131">One common pattern where the OnBehalfOf feature is used is the proxy pattern where the client cannot access the STS directly but instead communicates through a proxy gateway.</span></span> <span data-ttu-id="30abb-132">Brama proxy uwierzytelnia wywołującego i umieszcza informacje o elemencie wywołującym element OnBehalfOf wiadomości RST następnie wysyła go do rzeczywistego STS do przetwarzania.</span><span class="sxs-lookup"><span data-stu-id="30abb-132">The proxy gateway authenticates the caller and puts information about the caller into the OnBehalfOf element of the RST message that it then sends to the real STS for processing.</span></span> <span data-ttu-id="30abb-133">Wynikowy token zawiera tylko oświadczenia dotyczące klienta serwera proxy, co serwer proxy całkowicie niewidoczne odbiorcy wystawionego tokenu. Należy pamiętać, że WIF nie obsługuje \<wsse:SecurityTokenReference > lub \<wsa:EndpointReferences > jako element podrzędny \<wst:OnBehalfOf >.</span><span class="sxs-lookup"><span data-stu-id="30abb-133">The resulting token contains only claims related to the client of the proxy, making the proxy completely transparent to the receiver of the issued token.Note that WIF does not support \<wsse:SecurityTokenReference> or \<wsa:EndpointReferences> as a child of \<wst:OnBehalfOf>.</span></span> <span data-ttu-id="30abb-134">Specyfikacja WS-Trust zezwala na trzy sposoby można określić oryginalnego obiektu żądającego (imieniu którego serwer proxy działa).</span><span class="sxs-lookup"><span data-stu-id="30abb-134">The WS-Trust specification allows for three ways to identify the original requestor (on behalf of whom the proxy is acting).</span></span> <span data-ttu-id="30abb-135">Są to:</span><span class="sxs-lookup"><span data-stu-id="30abb-135">These are:</span></span>  
>   
>  -   <span data-ttu-id="30abb-136">Odwołania do tokenu zabezpieczeń.</span><span class="sxs-lookup"><span data-stu-id="30abb-136">Security token reference.</span></span> <span data-ttu-id="30abb-137">Odwołanie do tokenu w wiadomości lub prawdopodobnie pobrane poza pasmem).</span><span class="sxs-lookup"><span data-stu-id="30abb-137">A reference to a token, either in the message, or possibly retrieved out of band).</span></span>  
> -   <span data-ttu-id="30abb-138">Odwołanie do punktu końcowego.</span><span class="sxs-lookup"><span data-stu-id="30abb-138">Endpoint reference.</span></span> <span data-ttu-id="30abb-139">Używany jako klucz, aby wyszukiwać dane ponownie poza pasmem.</span><span class="sxs-lookup"><span data-stu-id="30abb-139">Used as a key to look up data, again out of band.</span></span>  
> -   <span data-ttu-id="30abb-140">Token zabezpieczający.</span><span class="sxs-lookup"><span data-stu-id="30abb-140">Security token.</span></span> <span data-ttu-id="30abb-141">Identyfikuje bezpośrednio oryginalnego obiektu żądającego.</span><span class="sxs-lookup"><span data-stu-id="30abb-141">Identifies the original requestor directly.</span></span>  
>   
>  <span data-ttu-id="30abb-142">WIF obsługuje tylko tokenów, szyfrowane albo niezaszyfrowanej jako elementu podrzędnego bezpośredniego \<wst:OnBehalfOf >.</span><span class="sxs-lookup"><span data-stu-id="30abb-142">WIF supports only security tokens, either encrypted or unencrypted, as a direct child element of \<wst:OnBehalfOf>.</span></span>  
  
 <span data-ttu-id="30abb-143">Ta informacja jest przekazywana do przy użyciu elementów tokenów ActAs i OnBehalfOf w RST wystawcy WS-Trust.</span><span class="sxs-lookup"><span data-stu-id="30abb-143">This information is conveyed to a WS-Trust issuer using the ActAs and OnBehalfOf token elements in the RST.</span></span>  
  
 <span data-ttu-id="30abb-144">Usługa WCF umożliwia rozszerzalności punkcie powiązania, które umożliwia dowolnego elementów XML w celu dodania do RST.</span><span class="sxs-lookup"><span data-stu-id="30abb-144">WCF exposes an extensibility point on the binding that allows arbitrary XML elements to be added to the RST.</span></span> <span data-ttu-id="30abb-145">Jednak ponieważ punkcie rozszerzenia jest związany z powiązania, scenariusze, które wymagają zawartość RST różnicującej na wywołanie musi odtworzyć klienta dla każdego wywołania, co obniża wydajność.</span><span class="sxs-lookup"><span data-stu-id="30abb-145">However, because the extensibility point is tied to the binding, scenarios that require the RST contents to vary per call must re-create the client for every call, which decreases performance.</span></span> <span data-ttu-id="30abb-146">WIF używa metody rozszerzenia na `ChannelFactory` klasa umożliwia deweloperom dołączyć dowolny uzyskany poza pasmem do RST token.</span><span class="sxs-lookup"><span data-stu-id="30abb-146">WIF uses extension methods on the `ChannelFactory` class to allow developers to attach any token that is obtained out of band to the RST.</span></span> <span data-ttu-id="30abb-147">Poniższy przykład kodu pokazuje, jak można zająć tokenu, który reprezentuje klienta (na przykład X.509, nazwa użytkownika lub token potwierdzenia języka SAML (Security Markup)) i dołączenie go do RST, wysyłany do wystawcy.</span><span class="sxs-lookup"><span data-stu-id="30abb-147">The following code example shows how to take a token that represents the client (such as an X.509, username, or Security Assertion Markup Language (SAML) token) and attach it to the RST that is sent to the issuer.</span></span>  
  
```  
IHelloService serviceChannel = channelFactory.CreateChannelActingAs<IHelloService>( clientSamlToken );  
serviceChannel.Hello("Hi!");  
```  
  
 <span data-ttu-id="30abb-148">WIF zapewnia następujące korzyści:</span><span class="sxs-lookup"><span data-stu-id="30abb-148">WIF provides the following benefits:</span></span>  
  
-   <span data-ttu-id="30abb-149">RST można zmodyfikować na kanał; w związku z tym warstwy środkowej usług ma ponownie utworzyć fabryki kanałów dla każdego klienta, co zwiększa wydajność.</span><span class="sxs-lookup"><span data-stu-id="30abb-149">The RST can be modified per channel; therefore, middle-tier services do not have to re-create the channel factory for each client, which improves performance.</span></span>  
  
-   <span data-ttu-id="30abb-150">To działanie z istniejących klientów usługi WCF, która umożliwia łatwe ścieżki uaktualniania istniejącej usługi WCF warstwy środkowej, które chcesz włączyć semantyki delegowania tożsamości.</span><span class="sxs-lookup"><span data-stu-id="30abb-150">This works with existing WCF clients, which makes an easy upgrade path possible for existing WCF middle-tier services that want to enable identity delegation semantics.</span></span>  
  
 <span data-ttu-id="30abb-151">Ponieważ nie ma jeszcze nie wgląd w komunikacji klienta z usługi STS.</span><span class="sxs-lookup"><span data-stu-id="30abb-151">However, there is still no visibility into the client’s communication with the STS.</span></span> <span data-ttu-id="30abb-152">Zajmiemy się to w trzeci scenariusz.</span><span class="sxs-lookup"><span data-stu-id="30abb-152">We’ll examine this in the third scenario.</span></span>  
  
## <a name="communicating-directly-with-an-issuer-and-using-the-issued-token-to-authenticate"></a><span data-ttu-id="30abb-153">Komunikują się bezpośrednio z wystawcę i przy użyciu wystawionego tokenu uwierzytelniania</span><span class="sxs-lookup"><span data-stu-id="30abb-153">Communicating Directly with an Issuer and Using the Issued Token to Authenticate</span></span>  
 <span data-ttu-id="30abb-154">Dla niektórych zaawansowanych scenariuszy udoskonalanie klienta WCF jest niewystarczająca.</span><span class="sxs-lookup"><span data-stu-id="30abb-154">For some advanced scenarios, enhancing a WCF client is not enough.</span></span> <span data-ttu-id="30abb-155">Deweloperzy, którzy używają tylko WCF zwykle użyć wiadomości w / komunikatów wychodzących umów oraz obsługę po stronie klienta podczas analizowania odpowiedzi wystawcy ręcznie.</span><span class="sxs-lookup"><span data-stu-id="30abb-155">Developers who use only WCF typically use Message In / Message Out contracts and handle client-side parsing of the issuer response manually.</span></span>  
  
 <span data-ttu-id="30abb-156">Wprowadza WIF <xref:System.ServiceModel.Security.WSTrustChannelFactory> i <xref:System.ServiceModel.Security.WSTrustChannel> klasy, aby umożliwić klientowi bezpośredniego komunikowania się z wystawców WS-Trust.</span><span class="sxs-lookup"><span data-stu-id="30abb-156">WIF introduces the <xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> classes to let the client communicate directly with a WS-Trust issuer.</span></span> <span data-ttu-id="30abb-157"><xref:System.ServiceModel.Security.WSTrustChannelFactory> i <xref:System.ServiceModel.Security.WSTrustChannel> klasy włączyć silnie typizowanych obiektów RST i odpowiedź RSTR przepływ między klientem a wystawcy, jak pokazano w poniższym przykładzie kodu.</span><span class="sxs-lookup"><span data-stu-id="30abb-157">The <xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> classes enable strongly typed RST and RSTR objects to flow between the client and issuer, as shown in the following code example.</span></span>  
  
```  
WSTrustChannelFactory trustChannelFactory = new WSTrustChannelFactory( stsBinding, stsAddress );  
WSTrustChannel channel = (WSTrustChannel) trustChannelFactory.CreateChannel();  
RequestSecurityToken rst = new RequestSecurityToken(RequestTypes.Issue);  
rst.AppliesTo = new EndpointAddress(serviceAddress);  
RequestSecurityTokenResponse rstr = null;  
SecurityToken token = channel.Issue(rst, out rstr);  
```  
  
 <span data-ttu-id="30abb-158">Należy pamiętać, że `out` parametru <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> metoda zapewnia dostęp do komunikacie RSTR inspekcji po stronie klienta.</span><span class="sxs-lookup"><span data-stu-id="30abb-158">Note that the `out` parameter on the <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> method allows access to the RSTR for client-side inspection.</span></span>  
  
 <span data-ttu-id="30abb-159">Firma Microsoft do tej pory tylko przedstawiono sposób uzyskiwania tokenu.</span><span class="sxs-lookup"><span data-stu-id="30abb-159">So far, we’ve only seen how to obtain a token.</span></span> <span data-ttu-id="30abb-160">Token, który jest zwracany z <xref:System.ServiceModel.Security.WSTrustChannel> obiekt jest `GenericXmlSecurityToken` zawiera wszystkie informacje, które są niezbędne do uwierzytelniania do jednostki uzależnionej.</span><span class="sxs-lookup"><span data-stu-id="30abb-160">The token that is returned from the <xref:System.ServiceModel.Security.WSTrustChannel> object is a `GenericXmlSecurityToken` that contains all of the information that is necessary for authentication to a relying party.</span></span> <span data-ttu-id="30abb-161">Poniższy przykładowy kod przedstawia sposób użycia tego tokenu.</span><span class="sxs-lookup"><span data-stu-id="30abb-161">The following code example shows how to use this token.</span></span>  
  
```  
IHelloService serviceChannel = channelFactory.CreateChannelWithIssuedToken<IHelloService>( token ); serviceChannel.Hello("Hi!");  
```  
  
 <span data-ttu-id="30abb-162"><xref:System.ServiceModel.ChannelFactory%601.CreateChannelWithIssuedToken%2A> — Metoda rozszerzenia na `ChannelFactory` obiektu wskazuje WIF uzyskaniu tokenu poza pasmem i należy ją zatrzymać normalne wywołanie WCF wystawca i zamiast tego użyć tokenu, który został pobrany do uwierzytelniania do jednostki uzależnionej.</span><span class="sxs-lookup"><span data-stu-id="30abb-162">The <xref:System.ServiceModel.ChannelFactory%601.CreateChannelWithIssuedToken%2A> extension method on the `ChannelFactory` object indicates to WIF that you have obtained a token out of band, and that it should stop the normal WCF call to the issuer and instead use the token that you obtained to authenticate to the relying party.</span></span> <span data-ttu-id="30abb-163">Ma to następujące korzyści:</span><span class="sxs-lookup"><span data-stu-id="30abb-163">This has the following benefits:</span></span>  
  
-   <span data-ttu-id="30abb-164">Udostępnia pełną kontrolę nad procesem wydawania tokenów.</span><span class="sxs-lookup"><span data-stu-id="30abb-164">It gives you complete control over the token issuance process.</span></span>  
  
-   <span data-ttu-id="30abb-165">Obsługuje ona ActAs / scenariusze OnBehalfOf bezpośrednio ustawienie tych właściwości na RST wychodzących.</span><span class="sxs-lookup"><span data-stu-id="30abb-165">It supports ActAs / OnBehalfOf scenarios by directly setting these properties on the outgoing RST.</span></span>  
  
-   <span data-ttu-id="30abb-166">Umożliwia podjęcie decyzji na podstawie zawartości o komunikacie RSTR dynamiczne zaufania po stronie klienta.</span><span class="sxs-lookup"><span data-stu-id="30abb-166">It enables dynamic client-side trust decisions to be made based on the contents of the RSTR.</span></span>  
  
-   <span data-ttu-id="30abb-167">Dzięki temu można buforować i ponowne użycie token, który jest zwracany z <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="30abb-167">It lets you cache and reuse the token that is returned from the <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> method.</span></span>  
  
-   <span data-ttu-id="30abb-168"><xref:System.ServiceModel.Security.WSTrustChannelFactory> i <xref:System.ServiceModel.Security.WSTrustChannel> umożliwiają kontrolę nad kanału semantyki pamięci podręcznej, odporności i odzyskiwania zgodnie z najlepszymi rozwiązaniami WCF.</span><span class="sxs-lookup"><span data-stu-id="30abb-168"><xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> allow for control of channel caching, fault, and recovery semantics according to WCF best practices.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="30abb-169">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="30abb-169">See Also</span></span>  
 [<span data-ttu-id="30abb-170">Funkcje programu WIF</span><span class="sxs-lookup"><span data-stu-id="30abb-170">WIF Features</span></span>](../../../docs/framework/security/wif-features.md)
