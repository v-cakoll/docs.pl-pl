---
title: Przegląd XAML
description: Dowiedz się, jak język XAML jest skonstruowany i zaimplementowany przez Program Windows Presentation Foundation (WPF) dla platformy .NET Core.
author: thraka
ms.date: 08/08/2019
ms.author: adegeo
dev_langs:
- csharp
- vb
helpviewer_keywords:
- user interfaces [XAML]
- classes [XAML]
- root element [XAML]
- XAML [WPF], about XAML
- base classes [XAML]
- routed events [WPF]
- code-behind files [WPF], XAML
- collection properties [XAML]
- events [XAML]
- property element [XAML]
- content models [XAML]
- Extensible Application Markup Language (see XAML)
- attribute syntax [XAML]
ms.openlocfilehash: b0a9357b623fbde08731a5b1ddb8fee3a93824c2
ms.sourcegitcommit: 515469828d0f040e01bde01df6b8e4eb43630b06
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/06/2020
ms.locfileid: "82071788"
---
# <a name="xaml-overview-in-wpf"></a><span data-ttu-id="e55b3-103">Omówienie XAML w wyw.</span><span class="sxs-lookup"><span data-stu-id="e55b3-103">XAML overview in WPF</span></span>

<span data-ttu-id="e55b3-104">W tym artykule opisano funkcje języka XAML i pokazano, jak można używać XAML do pisania aplikacji Windows Presentation Foundation (WPF).</span><span class="sxs-lookup"><span data-stu-id="e55b3-104">This article describes the features of the XAML language and demonstrates how you can use XAML to write Windows Presentation Foundation (WPF) apps.</span></span> <span data-ttu-id="e55b3-105">W tym artykule opisano w szczególności XAML zaimplementowane przez WPF.</span><span class="sxs-lookup"><span data-stu-id="e55b3-105">This article specifically describes XAML as implemented by WPF.</span></span> <span data-ttu-id="e55b3-106">Sam XAML jest pojęciem większego języka niż WPF.</span><span class="sxs-lookup"><span data-stu-id="e55b3-106">XAML itself is a larger language concept than WPF.</span></span>

[!INCLUDE [desktop guide under construction](../../../includes/desktop-guide-preview-note.md)]

## <a name="what-is-xaml"></a><span data-ttu-id="e55b3-107">Co to jest XAML</span><span class="sxs-lookup"><span data-stu-id="e55b3-107">What is XAML</span></span>

<span data-ttu-id="e55b3-108">XAML jest deklaratywnym językiem znaczników.</span><span class="sxs-lookup"><span data-stu-id="e55b3-108">XAML is a declarative markup language.</span></span> <span data-ttu-id="e55b3-109">W przypadku modelu programowania .NET Core kod XAML upraszcza tworzenie interfejsu użytkownika dla aplikacji .NET Core.</span><span class="sxs-lookup"><span data-stu-id="e55b3-109">As applied to the .NET Core programming model, XAML simplifies creating a UI for a .NET Core app.</span></span> <span data-ttu-id="e55b3-110">Można utworzyć widoczne elementy interfejsu użytkownika w deklaratywne znaczniki XAML, a następnie oddzielić definicję interfejsu użytkownika od logiki czasu wykonywania przy użyciu plików związanych z kodem, które są przyłączane do znaczników za pomocą definicji klasy częściowej.</span><span class="sxs-lookup"><span data-stu-id="e55b3-110">You can create visible UI elements in the declarative XAML markup, and then separate the UI definition from the run-time logic by using code-behind files that are joined to the markup through partial class definitions.</span></span> <span data-ttu-id="e55b3-111">XAML bezpośrednio reprezentuje tworzenie wystąpienia obiektów w określonym zestawie typów kopii zdefiniowanych w zestawach.</span><span class="sxs-lookup"><span data-stu-id="e55b3-111">XAML directly represents the instantiation of objects in a specific set of backing types defined in assemblies.</span></span> <span data-ttu-id="e55b3-112">Jest to w przeciwieństwie do większości innych języków znaczników, które są zazwyczaj interpretowany język bez takiego bezpośredniego powiązania z systemem typu kopii zapasowej.</span><span class="sxs-lookup"><span data-stu-id="e55b3-112">This is unlike most other markup languages, which are typically an interpreted language without such a direct tie to a backing type system.</span></span> <span data-ttu-id="e55b3-113">XAML umożliwia przepływ pracy, gdzie oddzielne strony mogą pracować na interfejs użytkownika i logiki aplikacji, przy użyciu potencjalnie różnych narzędzi.</span><span class="sxs-lookup"><span data-stu-id="e55b3-113">XAML enables a workflow where separate parties can work on the UI and the logic of an app, using potentially different tools.</span></span>

<span data-ttu-id="e55b3-114">Pliki XAML, które są reprezentowane jako tekst, `.xaml` są plikami XML, które zazwyczaj mają rozszerzenie.</span><span class="sxs-lookup"><span data-stu-id="e55b3-114">When represented as text, XAML files are XML files that generally have the `.xaml` extension.</span></span> <span data-ttu-id="e55b3-115">Pliki mogą być kodowane przez dowolne kodowanie XML, ale kodowanie jako UTF-8 jest typowe.</span><span class="sxs-lookup"><span data-stu-id="e55b3-115">The files can be encoded by any XML encoding, but encoding as UTF-8 is typical.</span></span>

<span data-ttu-id="e55b3-116">W poniższym przykładzie pokazano, jak można utworzyć przycisk jako część interfejsu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="e55b3-116">The following example shows how you might create a button as part of a UI.</span></span> <span data-ttu-id="e55b3-117">W tym przykładzie ma na celu zapewnienie smaku jak XAML reprezentuje wspólne metafory programowania interfejsu użytkownika (nie jest kompletnym przykładem).</span><span class="sxs-lookup"><span data-stu-id="e55b3-117">This example is intended to give you a flavor of how XAML represents common UI programming metaphors (it is not a complete sample).</span></span>

[!code-xaml[XAMLOvwSupport#DirtSimple](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page2.xaml#dirtsimple)]

## <a name="xaml-syntax-in-brief"></a><span data-ttu-id="e55b3-118">Składnia XAML w skrócie</span><span class="sxs-lookup"><span data-stu-id="e55b3-118">XAML syntax in brief</span></span>

<span data-ttu-id="e55b3-119">W poniższych sekcjach wyjaśniono podstawowe formy składni XAML i podano przykład krótkiego znaczników.</span><span class="sxs-lookup"><span data-stu-id="e55b3-119">The following sections explain the basic forms of XAML syntax, and give a short markup example.</span></span> <span data-ttu-id="e55b3-120">Te sekcje nie są przeznaczone do dostarczania pełnych informacji o każdym formularzu składni, takich jak sposób ich reprezentowania w systemie typu zapasowego.</span><span class="sxs-lookup"><span data-stu-id="e55b3-120">These sections are not intended to provide complete information about each syntax form, such as how these are represented in the backing type system.</span></span> <span data-ttu-id="e55b3-121">Aby uzyskać więcej informacji na temat specyfiki składni XAML, zobacz Szczegółowo [składnię XAML](../../framework/wpf/advanced/xaml-syntax-in-detail.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-121">For more information about the specifics of XAML syntax, see [XAML Syntax In Detail](../../framework/wpf/advanced/xaml-syntax-in-detail.md).</span></span>

<span data-ttu-id="e55b3-122">Większość materiałów w następnych kilku sekcjach będzie dla Ciebie elementarna, jeśli znasz wcześniej język XML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-122">Much of the material in the next few sections will be elementary to you if you have previous familiarity with the XML language.</span></span> <span data-ttu-id="e55b3-123">Jest to konsekwencja jednej z podstawowych zasad projektowania XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-123">This is a consequence of one of the basic design principles of XAML.</span></span> <span data-ttu-id="e55b3-124">Język XAML definiuje pojęcia własne, ale te pojęcia działają w języku XML i formularzu znaczników.</span><span class="sxs-lookup"><span data-stu-id="e55b3-124">The XAML language defines concepts of its own, but these concepts work within the XML language and markup form.</span></span>

### <a name="xaml-object-elements"></a><span data-ttu-id="e55b3-125">Elementy obiektu XAML</span><span class="sxs-lookup"><span data-stu-id="e55b3-125">XAML object elements</span></span>

<span data-ttu-id="e55b3-126">Element obiektu zazwyczaj deklaruje wystąpienie typu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-126">An object element typically declares an instance of a type.</span></span> <span data-ttu-id="e55b3-127">Ten typ jest zdefiniowany w zestawach, do których odwołuje się technologia, która używa języka XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-127">That type is defined in the assemblies referenced by the technology that uses XAML as a language.</span></span>

<span data-ttu-id="e55b3-128">Składnia elementu obiektu zawsze zaczyna się od`<`nawiasu kątowego otwarcia ( ).</span><span class="sxs-lookup"><span data-stu-id="e55b3-128">Object element syntax always starts with an opening angle bracket (`<`).</span></span> <span data-ttu-id="e55b3-129">Po tym następuje nazwa typu, w którym chcesz utworzyć wystąpienie.</span><span class="sxs-lookup"><span data-stu-id="e55b3-129">This is followed by the name of the type where you want to create an instance.</span></span> <span data-ttu-id="e55b3-130">(Nazwa może zawierać prefiks, pojęcie, które zostanie wyjaśnione później.) Następnie można opcjonalnie zadeklarować atrybuty na element obiektu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-130">(The name can include a prefix, a concept that will be explained later.) After this, you can optionally declare attributes on the object element.</span></span> <span data-ttu-id="e55b3-131">Aby zakończyć znacznik elementu obiektu, zakończ`>`nawiasem kątowym zamknięcia ( ).</span><span class="sxs-lookup"><span data-stu-id="e55b3-131">To complete the object element tag, end with a closing angle bracket (`>`).</span></span> <span data-ttu-id="e55b3-132">Zamiast tego można użyć formularza samozamykającego się, który nie ma żadnej zawartości, wypełniając`/>`znacznik ukośnikiem i nawiasem kątowym zamykającym po kolei ( ).</span><span class="sxs-lookup"><span data-stu-id="e55b3-132">You can instead use a self-closing form that does not have any content, by completing the tag with a forward slash and closing angle bracket in succession (`/>`).</span></span> <span data-ttu-id="e55b3-133">Na przykład ponownie przyjrzyj się wcześniej pokazanej fragmentowi kodu znaczników.</span><span class="sxs-lookup"><span data-stu-id="e55b3-133">For example, look at the previously shown markup snippet again.</span></span>

[!code-xaml[XAMLOvwSupport#DirtSimple](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page2.xaml#dirtsimple)]

<span data-ttu-id="e55b3-134">Określa dwa elementy obiektu: `<StackPanel>` (z zawartością i tagiem zamykającym później) i `<Button .../>` (formularz samozamykania się z kilkoma atrybutami).</span><span class="sxs-lookup"><span data-stu-id="e55b3-134">This specifies two object elements: `<StackPanel>` (with content, and a closing tag later), and `<Button .../>` (the self-closing form, with several attributes).</span></span> <span data-ttu-id="e55b3-135">Elementy `StackPanel` obiektu `Button` i każdej mapie do nazwy klasy, która jest zdefiniowana przez WPF i jest częścią WPF zestawy.</span><span class="sxs-lookup"><span data-stu-id="e55b3-135">The object elements `StackPanel` and `Button` each map to the name of a class that is defined by WPF and is part of the WPF assemblies.</span></span> <span data-ttu-id="e55b3-136">Po określeniu znacznika elementu obiektu należy utworzyć instrukcję przetwarzania XAML w celu utworzenia nowego wystąpienia typu źródłowego.</span><span class="sxs-lookup"><span data-stu-id="e55b3-136">When you specify an object element tag, you create an instruction for XAML processing to create a new instance of the underlying type.</span></span> <span data-ttu-id="e55b3-137">Każde wystąpienie jest tworzone przez wywołanie konstruktora bez parametrów typu źródłowego podczas analizowania i ładowania XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-137">Each instance is created by calling the parameterless constructor of the underlying type when parsing and loading the XAML.</span></span>

### <a name="attribute-syntax-properties"></a><span data-ttu-id="e55b3-138">Składnia atrybutów (właściwości)</span><span class="sxs-lookup"><span data-stu-id="e55b3-138">Attribute syntax (properties)</span></span>

<span data-ttu-id="e55b3-139">Właściwości obiektu często mogą być wyrażone jako atrybuty elementu obiektu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-139">Properties of an object can often be expressed as attributes of the object element.</span></span> <span data-ttu-id="e55b3-140">Składnia atrybutu nazywa ustawioną właściwość obiektu, a następnie operator przypisania (=).</span><span class="sxs-lookup"><span data-stu-id="e55b3-140">The attribute syntax names the object property that is being set, followed by the assignment operator (=).</span></span> <span data-ttu-id="e55b3-141">Wartość atrybutu jest zawsze określona jako ciąg, który jest zawarty w cudzysłowie.</span><span class="sxs-lookup"><span data-stu-id="e55b3-141">The value of an attribute is always specified as a string that is contained within quotation marks.</span></span>

<span data-ttu-id="e55b3-142">Składnia atrybutów jest najbardziej usprawniona składnia ustawień właściwości i jest najbardziej intuicyjną składnią do użycia dla deweloperów, którzy używali języków znaczników w przeszłości.</span><span class="sxs-lookup"><span data-stu-id="e55b3-142">Attribute syntax is the most streamlined property setting syntax and is the most intuitive syntax to use for developers who have used markup languages in the past.</span></span> <span data-ttu-id="e55b3-143">Na przykład poniższy znacznik tworzy przycisk, który ma czerwony tekst i niebieskie `Content`tło oprócz wyświetlania tekstu określonego jako .</span><span class="sxs-lookup"><span data-stu-id="e55b3-143">For example, the following markup creates a button that has red text and a blue background in addition to display text specified as `Content`.</span></span>

[!code-xaml[XAMLOvwSupport#BlueRedButton](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/Page1.xaml#blueredbutton)]

### <a name="property-element-syntax"></a><span data-ttu-id="e55b3-144">Składnia elementu właściwości</span><span class="sxs-lookup"><span data-stu-id="e55b3-144">Property element syntax</span></span>

<span data-ttu-id="e55b3-145">W przypadku niektórych właściwości elementu obiektu składnia atrybutu nie jest możliwa, ponieważ obiekt lub informacje niezbędne do dostarczenia wartości właściwości nie mogą być odpowiednio wyrażone w cudzysłowie i ograniczeniach ciągu składni atrybutu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-145">For some properties of an object element, attribute syntax is not possible, because the object or information necessary to provide the property value cannot be adequately expressed within the quotation mark and string restrictions of attribute syntax.</span></span> <span data-ttu-id="e55b3-146">W takich przypadkach można użyć innej składni znanej jako składnia elementu właściwości.</span><span class="sxs-lookup"><span data-stu-id="e55b3-146">For these cases, a different syntax known as property element syntax can be used.</span></span>

<span data-ttu-id="e55b3-147">Składnia znacznika początkowego elementu `<TypeName.PropertyName>`właściwości to .</span><span class="sxs-lookup"><span data-stu-id="e55b3-147">The syntax for the property element start tag is `<TypeName.PropertyName>`.</span></span> <span data-ttu-id="e55b3-148">Ogólnie rzecz biorąc zawartość tego tagu jest elementem obiektu typu, który właściwość przyjmuje jako swoją wartość.</span><span class="sxs-lookup"><span data-stu-id="e55b3-148">Generally, the content of that tag is an object element of the type that the property takes as its value.</span></span> <span data-ttu-id="e55b3-149">Po określeniu zawartości należy zamknąć element właściwości tagiem końcowym.</span><span class="sxs-lookup"><span data-stu-id="e55b3-149">After specifying the content, you must close the property element with an end tag.</span></span> <span data-ttu-id="e55b3-150">Składnia znacznika końcowego `</TypeName.PropertyName>`to .</span><span class="sxs-lookup"><span data-stu-id="e55b3-150">The syntax for the end tag is `</TypeName.PropertyName>`.</span></span>

<span data-ttu-id="e55b3-151">Jeśli składnia atrybutów jest możliwa, użycie składni atrybutu jest zazwyczaj wygodniejsze i umożliwia bardziej kompaktowe znaczniki, ale często jest to tylko kwestia stylu, a nie ograniczenia techniczne.</span><span class="sxs-lookup"><span data-stu-id="e55b3-151">If an attribute syntax is possible, using the attribute syntax is typically more convenient and enables a more compact markup, but that is often just a matter of style, not a technical limitation.</span></span> <span data-ttu-id="e55b3-152">W poniższym przykładzie przedstawiono te same właściwości ustawione, co w przykładzie składni poprzedniego atrybutu, ale `Button`tym razem przy użyciu składni elementu właściwości dla wszystkich właściwości . .</span><span class="sxs-lookup"><span data-stu-id="e55b3-152">The following example shows the same properties being set as in the previous attribute syntax example, but this time by using property element syntax for all properties of the `Button`.</span></span>

[!code-xaml[XAMLOvwSupport#BlueRedButtonPE](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/Page1.xaml#blueredbuttonpe)]

### <a name="collection-syntax"></a><span data-ttu-id="e55b3-153">Składnia kolekcji</span><span class="sxs-lookup"><span data-stu-id="e55b3-153">Collection syntax</span></span>

<span data-ttu-id="e55b3-154">Język XAML zawiera pewne optymalizacje, które generują więcej znaczników czytelnych dla człowieka.</span><span class="sxs-lookup"><span data-stu-id="e55b3-154">The XAML language includes some optimizations that produce more human-readable markup.</span></span> <span data-ttu-id="e55b3-155">Jedną z takich optymalizacji jest to, że jeśli określona właściwość przyjmuje typ kolekcji, a następnie elementy, które deklarujesz w znacznikach jako elementy podrzędne w ramach tej właściwości wartości stają się częścią kolekcji.</span><span class="sxs-lookup"><span data-stu-id="e55b3-155">One such optimization is that if a particular property takes a collection type, then items that you declare in markup as child elements within that property's value become part of the collection.</span></span> <span data-ttu-id="e55b3-156">W tym przypadku kolekcja elementów obiektu podrzędnego jest wartością ustawioną na właściwość kolekcji.</span><span class="sxs-lookup"><span data-stu-id="e55b3-156">In this case a collection of child object elements is the value being set to the collection property.</span></span>

<span data-ttu-id="e55b3-157">Poniższy przykład przedstawia składnię kolekcji <xref:System.Windows.Media.GradientBrush.GradientStops%2A> dla ustawiania wartości właściwości.</span><span class="sxs-lookup"><span data-stu-id="e55b3-157">The following example shows collection syntax for setting values of the <xref:System.Windows.Media.GradientBrush.GradientStops%2A> property.</span></span>

```xaml
<LinearGradientBrush>
  <LinearGradientBrush.GradientStops>
    <!-- no explicit new GradientStopCollection, parser knows how to find or create -->
    <GradientStop Offset="0.0" Color="Red" />
    <GradientStop Offset="1.0" Color="Blue" />
  </LinearGradientBrush.GradientStops>
</LinearGradientBrush>
```

### <a name="xaml-content-properties"></a><span data-ttu-id="e55b3-158">Właściwości zawartości XAML</span><span class="sxs-lookup"><span data-stu-id="e55b3-158">XAML content properties</span></span>

<span data-ttu-id="e55b3-159">XAML określa funkcję języka, zgodnie z którą klasa może wyznaczyć dokładnie jedną z jego właściwości jako właściwość _zawartości_ XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-159">XAML specifies a language feature whereby a class can designate exactly one of its properties to be the XAML _content_ property.</span></span> <span data-ttu-id="e55b3-160">Elementy podrzędne tego elementu obiektu są używane do ustawiania wartości tej właściwości zawartości.</span><span class="sxs-lookup"><span data-stu-id="e55b3-160">Child elements of that object element are used to set the value of that content property.</span></span> <span data-ttu-id="e55b3-161">Innymi słowy dla właściwości zawartości unikatowo można pominąć element właściwości podczas ustawiania tej właściwości w znacznikach XAML i utworzyć bardziej widoczną metaforę nadrzędną/podrzędną w znacznikach.</span><span class="sxs-lookup"><span data-stu-id="e55b3-161">In other words, for the content property uniquely, you can omit a property element when setting that property in XAML markup and produce a more visible parent/child metaphor in the markup.</span></span>

<span data-ttu-id="e55b3-162">Na przykład <xref:System.Windows.Controls.Border> określa właściwość <xref:System.Windows.Controls.Decorator.Child%2A> _zawartości_ .</span><span class="sxs-lookup"><span data-stu-id="e55b3-162">For example, <xref:System.Windows.Controls.Border> specifies a _content_ property of <xref:System.Windows.Controls.Decorator.Child%2A>.</span></span> <span data-ttu-id="e55b3-163">Następujące dwa <xref:System.Windows.Controls.Border> elementy są traktowane identycznie.</span><span class="sxs-lookup"><span data-stu-id="e55b3-163">The following two <xref:System.Windows.Controls.Border> elements are treated identically.</span></span> <span data-ttu-id="e55b3-164">Pierwszy z nich korzysta ze składni właściwości zawartości `Border.Child` i pomija element właściwości.</span><span class="sxs-lookup"><span data-stu-id="e55b3-164">The first one takes advantage of the content property syntax and omits the `Border.Child` property element.</span></span> <span data-ttu-id="e55b3-165">Drugi pokazuje `Border.Child` jawnie.</span><span class="sxs-lookup"><span data-stu-id="e55b3-165">The second one shows `Border.Child` explicitly.</span></span>

```xaml
<Border>
  <TextBox Width="300"/>
</Border>
<!--explicit equivalent-->
<Border>
  <Border.Child>
    <TextBox Width="300"/>
  </Border.Child>
</Border>
```

<span data-ttu-id="e55b3-166">Z reguły języka XAML wartość właściwości zawartości XAML musi być podana całkowicie przed lub całkowicie po innych elementów właściwości w tym elemencie obiektu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-166">As a rule of the XAML language, the value of a XAML content property must be given either entirely before or entirely after any other property elements on that object element.</span></span> <span data-ttu-id="e55b3-167">Na przykład następujące znaczniki nie kompiluje.</span><span class="sxs-lookup"><span data-stu-id="e55b3-167">For instance, the following markup does not compile.</span></span>

```xaml
<Button>I am a
  <Button.Background>Blue</Button.Background>
  blue button</Button>
```

<span data-ttu-id="e55b3-168">Aby uzyskać więcej informacji na temat specyfiki składni XAML, zobacz Szczegółowo [składnię XAML](../../framework/wpf/advanced/xaml-syntax-in-detail.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-168">For more information about the specifics of XAML syntax, see [XAML Syntax In Detail](../../framework/wpf/advanced/xaml-syntax-in-detail.md).</span></span>

### <a name="text-content"></a><span data-ttu-id="e55b3-169">Zawartość tekstowa</span><span class="sxs-lookup"><span data-stu-id="e55b3-169">Text content</span></span>

<span data-ttu-id="e55b3-170">Niewielka liczba elementów XAML może bezpośrednio przetwarzać tekst jako ich zawartość.</span><span class="sxs-lookup"><span data-stu-id="e55b3-170">A small number of XAML elements can directly process text as their content.</span></span> <span data-ttu-id="e55b3-171">Aby włączyć tę funkcję, musi być spełniony jeden z następujących przypadków:</span><span class="sxs-lookup"><span data-stu-id="e55b3-171">To enable this, one of the following cases must be true:</span></span>

- <span data-ttu-id="e55b3-172">Klasa musi zadeklarować właściwość zawartości, a właściwość content musi być typu przypisywanego do ciągu (typ może być). <xref:System.Object></span><span class="sxs-lookup"><span data-stu-id="e55b3-172">The class must declare a content property, and that content property must be of a type assignable to a string (the type could be <xref:System.Object>).</span></span> <span data-ttu-id="e55b3-173">Na przykład <xref:System.Windows.Controls.ContentControl> wszelkie <xref:System.Windows.Controls.ContentControl.Content%2A> zastosowania jako jego właściwości <xref:System.Object>zawartości i jest to typ <xref:System.Windows.Controls.ContentControl> , <xref:System.Windows.Controls.Button>a `<Button>Hello</Button>`to obsługuje następujące użycie na takie jak: .</span><span class="sxs-lookup"><span data-stu-id="e55b3-173">For instance, any <xref:System.Windows.Controls.ContentControl> uses <xref:System.Windows.Controls.ContentControl.Content%2A> as its content property and it is type <xref:System.Object>, and this supports the following usage on a <xref:System.Windows.Controls.ContentControl> such as a <xref:System.Windows.Controls.Button>: `<Button>Hello</Button>`.</span></span>

- <span data-ttu-id="e55b3-174">Typ musi zadeklarować konwerter typów, w którym to przypadku zawartość tekstowa jest używana jako tekst inicjowania dla tego typu konwertera.</span><span class="sxs-lookup"><span data-stu-id="e55b3-174">The type must declare a type converter, in which case the text content is used as initialization text for that type converter.</span></span> <span data-ttu-id="e55b3-175">Na przykład `<Brush>Blue</Brush>` konwertuje wartość `Blue` zawartości na pędzel.</span><span class="sxs-lookup"><span data-stu-id="e55b3-175">For example, `<Brush>Blue</Brush>` converts the content value of `Blue` into a brush.</span></span> <span data-ttu-id="e55b3-176">Ten przypadek jest mniej powszechny w praktyce.</span><span class="sxs-lookup"><span data-stu-id="e55b3-176">This case is less common in practice.</span></span>

- <span data-ttu-id="e55b3-177">Typ musi być znany język XAML pierwotny.</span><span class="sxs-lookup"><span data-stu-id="e55b3-177">The type must be a known XAML language primitive.</span></span>

### <a name="content-properties-and-collection-syntax-combined"></a><span data-ttu-id="e55b3-178">Właściwości zawartości i składnia kolekcji połączone</span><span class="sxs-lookup"><span data-stu-id="e55b3-178">Content properties and collection syntax combined</span></span>

<span data-ttu-id="e55b3-179">Rozważmy ten przykład.</span><span class="sxs-lookup"><span data-stu-id="e55b3-179">Consider this example.</span></span>

```xaml
<StackPanel>
  <Button>First Button</Button>
  <Button>Second Button</Button>
</StackPanel>
```

<span data-ttu-id="e55b3-180">Tutaj każdy <xref:System.Windows.Controls.Button> jest elementem <xref:System.Windows.Controls.StackPanel>podrzędnym .</span><span class="sxs-lookup"><span data-stu-id="e55b3-180">Here, each <xref:System.Windows.Controls.Button> is a child element of <xref:System.Windows.Controls.StackPanel>.</span></span> <span data-ttu-id="e55b3-181">Jest to usprawniony i intuicyjny znacznik, który pomija dwa tagi z dwóch różnych powodów.</span><span class="sxs-lookup"><span data-stu-id="e55b3-181">This is a streamlined and intuitive markup that omits two tags for two different reasons.</span></span>

- <span data-ttu-id="e55b3-182">**Pominięty Element właściwości StackPanel.Children:** <xref:System.Windows.Controls.StackPanel> pochodzi od <xref:System.Windows.Controls.Panel>.</span><span class="sxs-lookup"><span data-stu-id="e55b3-182">**Omitted StackPanel.Children property element:** <xref:System.Windows.Controls.StackPanel> derives from <xref:System.Windows.Controls.Panel>.</span></span> <span data-ttu-id="e55b3-183"><xref:System.Windows.Controls.Panel>definiuje <xref:System.Windows.Controls.Panel.Children%2A?displayProperty=nameWithType> jako jego właściwość zawartości XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-183"><xref:System.Windows.Controls.Panel> defines <xref:System.Windows.Controls.Panel.Children%2A?displayProperty=nameWithType> as its XAML content property.</span></span>

- <span data-ttu-id="e55b3-184">**Pominięty element obiektu UIElementCollection:** Właściwość <xref:System.Windows.Controls.Panel.Children%2A?displayProperty=nameWithType> przyjmuje <xref:System.Windows.Controls.UIElementCollection>typ , <xref:System.Collections.IList>który implementuje .</span><span class="sxs-lookup"><span data-stu-id="e55b3-184">**Omitted UIElementCollection object element:** The <xref:System.Windows.Controls.Panel.Children%2A?displayProperty=nameWithType> property takes the type <xref:System.Windows.Controls.UIElementCollection>, which implements <xref:System.Collections.IList>.</span></span> <span data-ttu-id="e55b3-185">Tag elementu kolekcji można pominąć na podstawie reguł XAML do <xref:System.Collections.IList>przetwarzania kolekcji, takich jak .</span><span class="sxs-lookup"><span data-stu-id="e55b3-185">The collection's element tag can be omitted, based on the XAML rules for processing collections such as <xref:System.Collections.IList>.</span></span> <span data-ttu-id="e55b3-186">(W takim <xref:System.Windows.Controls.UIElementCollection> przypadku faktycznie nie można utworzyć wystąpienia, ponieważ nie udostępnia konstruktora bez parametrów i dlatego element <xref:System.Windows.Controls.UIElementCollection> obiektu jest wyświetlany skomentowane).</span><span class="sxs-lookup"><span data-stu-id="e55b3-186">(In this case, <xref:System.Windows.Controls.UIElementCollection> actually cannot be instantiated because it does not expose a parameterless constructor, and that is why the <xref:System.Windows.Controls.UIElementCollection> object element is shown commented out).</span></span>

```xaml
<StackPanel>
  <StackPanel.Children>
    <!--<UIElementCollection>-->
    <Button>First Button</Button>
    <Button>Second Button</Button>
    <!--</UIElementCollection>-->
  </StackPanel.Children>
</StackPanel>
```

### <a name="attribute-syntax-events"></a><span data-ttu-id="e55b3-187">Składnia atrybutów (zdarzenia)</span><span class="sxs-lookup"><span data-stu-id="e55b3-187">Attribute syntax (events)</span></span>

<span data-ttu-id="e55b3-188">Składnia atrybutów może być również używana dla elementów członkowskich, które są zdarzeniami, a nie właściwościami.</span><span class="sxs-lookup"><span data-stu-id="e55b3-188">Attribute syntax can also be used for members that are events rather than properties.</span></span> <span data-ttu-id="e55b3-189">W takim przypadku nazwa atrybutu jest nazwą zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="e55b3-189">In this case, the attribute's name is the name of the event.</span></span> <span data-ttu-id="e55b3-190">W implementacji WPF dla XAML wartość atrybutu jest nazwą programu obsługi, który implementuje delegata tego zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="e55b3-190">In the WPF implementation of events for XAML, the attribute's value is the name of a handler that implements that event's delegate.</span></span> <span data-ttu-id="e55b3-191">Na przykład następujące znaczniki przypisuje program <xref:System.Windows.Controls.Primitives.ButtonBase.Click> obsługi dla <xref:System.Windows.Controls.Button> zdarzenia do utworzonego w znacznikach:</span><span class="sxs-lookup"><span data-stu-id="e55b3-191">For example, the following markup assigns a handler for the <xref:System.Windows.Controls.Primitives.ButtonBase.Click> event to a <xref:System.Windows.Controls.Button> created in markup:</span></span>

[!code-xaml[XAMLOvwSupport#ButtonWithCodeBehind](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page3.xaml#buttonwithcodebehind)]

<span data-ttu-id="e55b3-192">Istnieje więcej zdarzeń i XAML w WPF niż tylko w tym przykładzie składni atrybutu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-192">There is more to events and XAML in WPF than just this example of the attribute syntax.</span></span> <span data-ttu-id="e55b3-193">Na przykład można się `ClickHandler` zastanawiać, co reprezentuje w tym miejscu, do którego odwołuje się i jak jest zdefiniowany.</span><span class="sxs-lookup"><span data-stu-id="e55b3-193">For example, you might wonder what the `ClickHandler` referenced here represents and how it is defined.</span></span> <span data-ttu-id="e55b3-194">Zostanie to wyjaśnione w nadchodzącej sekcji [Wydarzenia i XAML związane z kodem](#events-and-xaml-code-behind) tego artykułu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-194">This will be explained in the upcoming [Events and XAML code-behind](#events-and-xaml-code-behind) section of this article.</span></span>

## <a name="case-and-white-space-in-xaml"></a><span data-ttu-id="e55b3-195">Obudowa i biały znak w XAML</span><span class="sxs-lookup"><span data-stu-id="e55b3-195">Case and white space in XAML</span></span>

<span data-ttu-id="e55b3-196">Ogólnie rzecz biorąc, W języku XAML jest rozróżniana wielkość liter.</span><span class="sxs-lookup"><span data-stu-id="e55b3-196">In general, XAML is case-sensitive.</span></span> <span data-ttu-id="e55b3-197">Na potrzeby rozwiązywania typów kopii zapasowej WPF XAML jest rozróżniana wielkość liter przez te same reguły, że CLR jest rozróżniana wielkość liter.</span><span class="sxs-lookup"><span data-stu-id="e55b3-197">For purposes of resolving backing types, WPF XAML is case-sensitive by the same rules that the CLR is case-sensitive.</span></span> <span data-ttu-id="e55b3-198">Elementy obiektu, elementy właściwości i nazwy atrybutów muszą być określone przy użyciu czułej wielkości liter w porównaniu według nazwy do typu źródłowego w zestawie lub do elementu członkowskiego typu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-198">Object elements, property elements, and attribute names must all be specified by using the sensitive casing when compared by name to the underlying type in the assembly, or to a member of a type.</span></span> <span data-ttu-id="e55b3-199">W językach XAML i w językach pierwotnych rozróżniana jest również wielkość liter.</span><span class="sxs-lookup"><span data-stu-id="e55b3-199">XAML language keywords and primitives are also case-sensitive.</span></span> <span data-ttu-id="e55b3-200">W wartościach nie zawsze jest rozróżniana wielkość liter.</span><span class="sxs-lookup"><span data-stu-id="e55b3-200">Values are not always case-sensitive.</span></span> <span data-ttu-id="e55b3-201">Wielkość liter dla wartości zależy od zachowania konwertera typu skojarzonego z właściwością, która przyjmuje wartość lub typem wartości właściwości.</span><span class="sxs-lookup"><span data-stu-id="e55b3-201">Case sensitivity for values will depend on the type converter behavior associated with the property that takes the value, or the property value type.</span></span> <span data-ttu-id="e55b3-202">Na przykład właściwości, które <xref:System.Boolean> przyjmują typ `true` `True` może mieć jedną lub równoważne wartości, ale tylko dlatego, że <xref:System.Boolean> natywne WPF XAML konwersji typu parsera dla ciągu już zezwala na te jako odpowiedniki.</span><span class="sxs-lookup"><span data-stu-id="e55b3-202">For example, properties that take the <xref:System.Boolean> type can take either `true` or `True` as equivalent values, but only because the native WPF XAML parser type conversion for string to <xref:System.Boolean> already permits these as equivalents.</span></span>

<span data-ttu-id="e55b3-203">Procesory I serializatory WPF XAML ignorują lub upuszczają wszystkie nieistotne białe znaki i normalizują wszelkie znaczące odstępy.</span><span class="sxs-lookup"><span data-stu-id="e55b3-203">WPF XAML processors and serializers will ignore or drop all nonsignificant white space, and will normalize any significant white space.</span></span> <span data-ttu-id="e55b3-204">Jest to zgodne z domyślnymi zaleceniami zachowania odstępu specyfikacji XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-204">This is consistent with the default white-space behavior recommendations of the XAML specification.</span></span> <span data-ttu-id="e55b3-205">To zachowanie jest tylko konsekwencji podczas określania ciągów we właściwościach zawartości XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-205">This behavior is only of consequence when you specify strings within XAML content properties.</span></span> <span data-ttu-id="e55b3-206">W najprostszych słowach XAML konwertuje spację, wysuw liniowy i znaki tabulacji na spacje, a następnie zachowuje jedną spację, jeśli zostanie znaleziony na obu końcach ciągłego ciągu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-206">In simplest terms, XAML converts space, linefeed, and tab characters into spaces, and then preserves one space if found at either end of a contiguous string.</span></span> <span data-ttu-id="e55b3-207">Pełne wyjaśnienie obsługi odstępu XAML nie jest omówione w tym artykule.</span><span class="sxs-lookup"><span data-stu-id="e55b3-207">The full explanation of XAML white-space handling is not covered in this article.</span></span> <span data-ttu-id="e55b3-208">Aby uzyskać więcej informacji, zobacz [Przetwarzanie odstępów w języku XAML](../xaml-services/white-space-processing.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-208">For more information, see [White space processing in XAML](../xaml-services/white-space-processing.md).</span></span>

## <a name="markup-extensions"></a><span data-ttu-id="e55b3-209">Rozszerzenia struktury znaczników</span><span class="sxs-lookup"><span data-stu-id="e55b3-209">Markup extensions</span></span>

<span data-ttu-id="e55b3-210">Rozszerzenia znaczników są pojęciem języka XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-210">Markup extensions are a XAML language concept.</span></span> <span data-ttu-id="e55b3-211">Używane do dostarczania wartości składni atrybutów, nawiasy klamrowe (`{` i `}`) wskazują użycie rozszerzenia znaczników.</span><span class="sxs-lookup"><span data-stu-id="e55b3-211">When used to provide the value of an attribute syntax, curly braces (`{` and `}`) indicate a markup extension usage.</span></span> <span data-ttu-id="e55b3-212">To użycie kieruje przetwarzania XAML do ucieczki od ogólnego traktowania wartości atrybutów jako ciąg literał lub wartość konwersacyjną ciąg.</span><span class="sxs-lookup"><span data-stu-id="e55b3-212">This usage directs the XAML processing to escape from the general treatment of attribute values as either a literal string or a string-convertible value.</span></span>

<span data-ttu-id="e55b3-213">Najczęstsze rozszerzenia znaczników używane w programowaniu [`Binding`](../../framework/wpf/advanced/binding-markup-extension.md)aplikacji WPF są używane do wyrażeń wiązania danych oraz odwołań [`StaticResource`](../../framework/wpf/advanced/staticresource-markup-extension.md) do zasobów i [`DynamicResource`](../../framework/wpf/advanced/dynamicresource-markup-extension.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-213">The most common markup extensions used in WPF app programming are [`Binding`](../../framework/wpf/advanced/binding-markup-extension.md), used for data binding expressions, and the resource references [`StaticResource`](../../framework/wpf/advanced/staticresource-markup-extension.md) and [`DynamicResource`](../../framework/wpf/advanced/dynamicresource-markup-extension.md).</span></span> <span data-ttu-id="e55b3-214">Za pomocą rozszerzeń znaczników, można użyć składni atrybutów, aby podać wartości właściwości, nawet jeśli ta właściwość nie obsługuje składni atrybutu w ogóle.</span><span class="sxs-lookup"><span data-stu-id="e55b3-214">By using markup extensions, you can use attribute syntax to provide values for properties even if that property does not support an attribute syntax in general.</span></span> <span data-ttu-id="e55b3-215">Rozszerzenia znaczników często używają typów wyrażeń pośrednich, aby włączyć funkcje, takie jak odracanie wartości lub odwoływanie się do innych obiektów, które są obecne tylko w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="e55b3-215">Markup extensions often use intermediate expression types to enable features such as deferring values or referencing other objects that are only present at run-time.</span></span>

<span data-ttu-id="e55b3-216">Na przykład następujące znaczniki ustawia wartość <xref:System.Windows.FrameworkElement.Style%2A> właściwości przy użyciu składni atrybutu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-216">For example, the following markup sets the value of the <xref:System.Windows.FrameworkElement.Style%2A> property using attribute syntax.</span></span> <span data-ttu-id="e55b3-217">Właściwość <xref:System.Windows.FrameworkElement.Style%2A> przyjmuje wystąpienie <xref:System.Windows.Style> klasy, które domyślnie nie może być tworzone przez ciąg składni atrybutu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-217">The <xref:System.Windows.FrameworkElement.Style%2A> property takes an instance of the <xref:System.Windows.Style> class, which by default could not be instantiated by an attribute syntax string.</span></span> <span data-ttu-id="e55b3-218">Ale w tym przypadku atrybut odwołuje się do `StaticResource`określonego rozszerzenia znaczników, .</span><span class="sxs-lookup"><span data-stu-id="e55b3-218">But in this case, the attribute references a particular markup extension, `StaticResource`.</span></span> <span data-ttu-id="e55b3-219">Podczas przetwarzania tego rozszerzenia znaczników zwraca odwołanie do stylu, który został wcześniej szebrany jako zasób z kluczem w słowniku zasobów.</span><span class="sxs-lookup"><span data-stu-id="e55b3-219">When that markup extension is processed, it returns a reference to a style that was previously instantiated as a keyed resource in a resource dictionary.</span></span>

[!code-xaml[FEResourceSH_snip#XAMLOvwShortResources](~/samples/snippets/csharp/VS_Snippets_Wpf/FEResourceSH_snip/CS/page1.xaml#xamlovwshortresources)]
[!code-xaml[FEResourceSH_snip#XAMLOvwShortResources2](~/samples/snippets/csharp/VS_Snippets_Wpf/FEResourceSH_snip/CS/page1.xaml#xamlovwshortresources2)]
[!code-xaml[FEResourceSH_snip#XAMLOvwShortResources3](~/samples/snippets/csharp/VS_Snippets_Wpf/FEResourceSH_snip/CS/page1.xaml#xamlovwshortresources3)]

<span data-ttu-id="e55b3-220">Aby uzyskać listę odwołań wszystkich rozszerzeń znaczników dla XAML zaimplementowanych specjalnie w WPF, zobacz [rozszerzenia WPF XAML](../../framework/wpf/advanced/wpf-xaml-extensions.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-220">For a reference listing of all markup extensions for XAML implemented specifically in WPF, see [WPF XAML Extensions](../../framework/wpf/advanced/wpf-xaml-extensions.md).</span></span> <span data-ttu-id="e55b3-221">Aby uzyskać listę odwołań do rozszerzeń znaczników zdefiniowanych przez system.Xaml i które są szerzej dostępne dla implementacji XAML .NET Core, zobacz [obszar nazw XAML (x:) Funkcje językowe](../xaml-services/namespace-language-features.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-221">For a reference listing of the markup extensions that are defined by System.Xaml and are more widely available for .NET Core XAML implementations, see [XAML Namespace (x:) Language Features](../xaml-services/namespace-language-features.md).</span></span> <span data-ttu-id="e55b3-222">Aby uzyskać więcej informacji na temat pojęć rozszerzenia znaczników, zobacz [Rozszerzenia znaczników i WPF XAML](../../framework/wpf/advanced/markup-extensions-and-wpf-xaml.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-222">For more information about markup extension concepts, see [Markup Extensions and WPF XAML](../../framework/wpf/advanced/markup-extensions-and-wpf-xaml.md).</span></span>

## <a name="type-converters"></a><span data-ttu-id="e55b3-223">Konwertery typów</span><span class="sxs-lookup"><span data-stu-id="e55b3-223">Type converters</span></span>

<span data-ttu-id="e55b3-224">W [składni XAML w](#xaml-syntax-in-brief) sekcji Krótki stwierdzono, że wartość atrybutu musi być ustawiona przez ciąg.</span><span class="sxs-lookup"><span data-stu-id="e55b3-224">In the [XAML Syntax in Brief](#xaml-syntax-in-brief) section, it was stated that the attribute value must be able to be set by a string.</span></span> <span data-ttu-id="e55b3-225">Podstawowa, natywna obsługa sposobu konwertowania ciągów na inne <xref:System.String> typy obiektów lub wartości pierwotne jest <xref:System.DateTime> <xref:System.Uri>oparta na samym typie, oprócz przetwarzania natywnego dla niektórych typów, takich jak lub .</span><span class="sxs-lookup"><span data-stu-id="e55b3-225">The basic, native handling of how strings are converted into other object types or primitive values is based on the <xref:System.String> type itself, in addition to native processing for certain types such as <xref:System.DateTime> or <xref:System.Uri>.</span></span> <span data-ttu-id="e55b3-226">Ale wiele typów WPF lub członków tych typów rozszerzyć podstawowe zachowanie przetwarzania atrybutów ciągu w taki sposób, że wystąpienia bardziej złożonych typów obiektów można określić jako ciągi i atrybuty.</span><span class="sxs-lookup"><span data-stu-id="e55b3-226">But many WPF types or members of those types extend the basic string attribute processing behavior in such a way that instances of more complex object types can be specified as strings and attributes.</span></span>

<span data-ttu-id="e55b3-227">Struktura <xref:System.Windows.Thickness> jest przykładem typu, który ma włączoną konwersję typu dla użycia XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-227">The <xref:System.Windows.Thickness> structure is an example of a type that has a type conversion enabled for XAML usages.</span></span> <span data-ttu-id="e55b3-228"><xref:System.Windows.Thickness>wskazuje pomiary w zagnieżdżonym prostokącie i jest <xref:System.Windows.FrameworkElement.Margin%2A>używana jako wartość właściwości, takich jak .</span><span class="sxs-lookup"><span data-stu-id="e55b3-228"><xref:System.Windows.Thickness> indicates measurements within a nested rectangle and is used as the value for properties such as <xref:System.Windows.FrameworkElement.Margin%2A>.</span></span> <span data-ttu-id="e55b3-229">Umieszczając konwerter typów <xref:System.Windows.Thickness>na , wszystkie <xref:System.Windows.Thickness> właściwości, które używają są łatwiejsze do określenia w XAML, ponieważ mogą być określone jako atrybuty.</span><span class="sxs-lookup"><span data-stu-id="e55b3-229">By placing a type converter on <xref:System.Windows.Thickness>, all properties that use a <xref:System.Windows.Thickness> are easier to specify in XAML because they can be specified as attributes.</span></span> <span data-ttu-id="e55b3-230">W poniższym przykładzie użyto składni konwersji typu i <xref:System.Windows.FrameworkElement.Margin%2A>atrybutu, aby podać wartość dla:</span><span class="sxs-lookup"><span data-stu-id="e55b3-230">The following example uses a type conversion and attribute syntax to provide a value for a <xref:System.Windows.FrameworkElement.Margin%2A>:</span></span>

[!code-xaml[XAMLOvwSupport#MarginTCE](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page7.xaml#margintce)]

<span data-ttu-id="e55b3-231">Przykład składni poprzedniego atrybutu jest odpowiednikiem następującego przykładu składni bardziej <xref:System.Windows.FrameworkElement.Margin%2A> szczegółowej, gdzie zamiast tego jest <xref:System.Windows.Thickness> ustawiane za pomocą składni elementu właściwości zawierającego element obiektu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-231">The previous attribute syntax example is equivalent to the following more verbose syntax example, where the <xref:System.Windows.FrameworkElement.Margin%2A> is instead set through property element syntax containing a <xref:System.Windows.Thickness> object element.</span></span> <span data-ttu-id="e55b3-232">Cztery kluczowe właściwości <xref:System.Windows.Thickness> są ustawiane jako atrybuty w nowym wystąpieniu:</span><span class="sxs-lookup"><span data-stu-id="e55b3-232">The four key properties of <xref:System.Windows.Thickness> are set as attributes on the new instance:</span></span>

[!code-xaml[XAMLOvwSupport#MarginVerbose](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page7.xaml#marginverbose)]

> [!NOTE]
> <span data-ttu-id="e55b3-233">Istnieje również ograniczona liczba obiektów, gdzie konwersja typu jest jedynym publicznym sposobem, aby ustawić właściwość do tego typu bez angażowania podklasy, ponieważ sam typ nie ma konstruktora bez parametrów.</span><span class="sxs-lookup"><span data-stu-id="e55b3-233">There are also a limited number of objects where the type conversion is the only public way to set a property to that type without involving a subclass, because the type itself does not have a parameterless constructor.</span></span> <span data-ttu-id="e55b3-234">Może to być na przykład <xref:System.Windows.Input.Cursor>.</span><span class="sxs-lookup"><span data-stu-id="e55b3-234">An example is <xref:System.Windows.Input.Cursor>.</span></span>

<span data-ttu-id="e55b3-235">Aby uzyskać więcej informacji na temat konwersji typów, zobacz [TypeConverters i XAML](../../framework/wpf/advanced/typeconverters-and-xaml.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-235">For more information on type conversion, see [TypeConverters and XAML](../../framework/wpf/advanced/typeconverters-and-xaml.md).</span></span>

## <a name="xaml-root-elements-and-xaml-namespaces"></a><span data-ttu-id="e55b3-236">Elementy główne XAML i przestrzenie nazw XAML</span><span class="sxs-lookup"><span data-stu-id="e55b3-236">XAML root elements and XAML namespaces</span></span>

<span data-ttu-id="e55b3-237">Plik XAML musi mieć tylko jeden element główny, aby był zarówno dobrze sformułowanym plikiem XML, jak i prawidłowym plikiem XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-237">A XAML file must have only one root element, in order to be both a well-formed XML file and a valid XAML file.</span></span> <span data-ttu-id="e55b3-238">W typowych scenariuszach WPF należy użyć elementu głównego, który ma znaczenie widoczne <xref:System.Windows.Window> <xref:System.Windows.Controls.Page> w modelu <xref:System.Windows.ResourceDictionary> aplikacji WPF (na <xref:System.Windows.Application> przykład lub dla strony, dla słownika zewnętrznego lub definicji aplikacji).</span><span class="sxs-lookup"><span data-stu-id="e55b3-238">For typical WPF scenarios, you use a root element that has a prominent meaning in the WPF app model (for example, <xref:System.Windows.Window> or <xref:System.Windows.Controls.Page> for a page, <xref:System.Windows.ResourceDictionary> for an external dictionary, or <xref:System.Windows.Application> for the app definition).</span></span> <span data-ttu-id="e55b3-239">Poniższy przykład przedstawia element główny typowego pliku XAML dla strony WPF, z elementem głównym <xref:System.Windows.Controls.Page>.</span><span class="sxs-lookup"><span data-stu-id="e55b3-239">The following example shows the root element of a typical XAML file for a WPF page, with the root element of <xref:System.Windows.Controls.Page>.</span></span>

[!code-xaml[XAMLOvwSupport#RootOnly](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page2.xaml#rootonly)]
[!code-xaml[XAMLOvwSupport#RootOnly2](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page2.xaml#rootonly2)]

<span data-ttu-id="e55b3-240">Element główny zawiera również `xmlns` `xmlns:x`atrybuty i .</span><span class="sxs-lookup"><span data-stu-id="e55b3-240">The root element also contains the attributes `xmlns` and `xmlns:x`.</span></span> <span data-ttu-id="e55b3-241">Te atrybuty wskazują procesorowi XAML, które obszary nazw XAML zawierają definicje typów typów kopii zapasowych, do których znaczniki będą odwoływać się jako elementy.</span><span class="sxs-lookup"><span data-stu-id="e55b3-241">These attributes indicate to a XAML processor which XAML namespaces contain the type definitions for backing types that the markup will reference as elements.</span></span> <span data-ttu-id="e55b3-242">Atrybut `xmlns` wskazuje w szczególności domyślną przestrzeń nazw XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-242">The `xmlns` attribute specifically indicates the default XAML namespace.</span></span> <span data-ttu-id="e55b3-243">W domyślnej przestrzeni nazw XAML elementy obiektów w znacznikach można określić bez prefiksu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-243">Within the default XAML namespace, object elements in the markup can be specified without a prefix.</span></span> <span data-ttu-id="e55b3-244">W przypadku większości scenariuszy aplikacji WPF i dla prawie wszystkich przykładów podanych w sekcjach WPF SDK domyślny obszar `http://schemas.microsoft.com/winfx/2006/xaml/presentation`nazw XAML jest mapowany do obszaru nazw WPF .</span><span class="sxs-lookup"><span data-stu-id="e55b3-244">For most WPF app scenarios, and for almost all of the examples given in the WPF sections of the SDK, the default XAML namespace is mapped to the WPF namespace `http://schemas.microsoft.com/winfx/2006/xaml/presentation`.</span></span> <span data-ttu-id="e55b3-245">Atrybut `xmlns:x` wskazuje dodatkową przestrzeń nazw XAML, która mapuje obszar `http://schemas.microsoft.com/winfx/2006/xaml`nazw języka XAML .</span><span class="sxs-lookup"><span data-stu-id="e55b3-245">The `xmlns:x` attribute indicates an additional XAML namespace, which maps the XAML language namespace `http://schemas.microsoft.com/winfx/2006/xaml`.</span></span>

<span data-ttu-id="e55b3-246">To użycie `xmlns` do definiowania zakresu użycia i mapowania namescope jest zgodne ze specyfikacją XML 1.0.</span><span class="sxs-lookup"><span data-stu-id="e55b3-246">This usage of `xmlns` to define a scope for usage and mapping of a namescope is consistent with the XML 1.0 specification.</span></span> <span data-ttu-id="e55b3-247">Namescopes XAML różnią się od nazw XML tylko w tym, że xaml namescope oznacza również coś o tym, jak namescope elementy są obsługiwane przez typy, jeśli chodzi o rozpoznawanie typów i analizowanie XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-247">XAML namescopes are different from XML namescopes only in that a XAML namescope also implies something about how the namescope's elements are backed by types when it comes to type resolution and parsing the XAML.</span></span>

<span data-ttu-id="e55b3-248">Atrybuty `xmlns` są ściśle niezbędne tylko w elemencie głównym każdego pliku XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-248">The `xmlns` attributes are only strictly necessary on the root element of each XAML file.</span></span> <span data-ttu-id="e55b3-249">`xmlns`definicje będą miały zastosowanie do wszystkich elementów podrzędnych elementu głównego (to zachowanie jest `xmlns`ponownie zgodne ze specyfikacją XML 1.0 dla .) `xmlns` atrybuty są również dozwolone na innych elementów pod katalogiem głównym i będzie stosowany do wszystkich elementów podrzędnych elementu definiującego.</span><span class="sxs-lookup"><span data-stu-id="e55b3-249">`xmlns` definitions will apply to all descendant elements of the root element (this behavior is again consistent with the XML 1.0 specification for `xmlns`.) `xmlns` attributes are also permitted on other elements underneath the root, and would apply to any descendant elements of the defining element.</span></span> <span data-ttu-id="e55b3-250">Jednak częste definiowanie lub ponowne definiowanie obszarów nazw XAML może spowodować styl znaczników XAML, który jest trudny do odczytania.</span><span class="sxs-lookup"><span data-stu-id="e55b3-250">However, frequent definition or redefinition of XAML namespaces can result in a XAML markup style that is difficult to read.</span></span>

<span data-ttu-id="e55b3-251">Implementacja WPF jego procesor XAML zawiera infrastrukturę, która ma świadomość WPF zestawy rdzenia.</span><span class="sxs-lookup"><span data-stu-id="e55b3-251">The WPF implementation of its XAML processor includes an infrastructure that has awareness of the WPF core assemblies.</span></span> <span data-ttu-id="e55b3-252">WPF core zestawy są znane zawierają typy, które obsługują mapowania WPF do domyślnego obszaru nazw XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-252">The WPF core assemblies are known to contain the types that support the WPF mappings to the default XAML namespace.</span></span> <span data-ttu-id="e55b3-253">Jest to włączone za pośrednictwem konfiguracji, która jest częścią pliku kompilacji projektu i systemów kompilacji i projektu WPF.</span><span class="sxs-lookup"><span data-stu-id="e55b3-253">This is enabled through configuration that is part of your project build file and the WPF build and project systems.</span></span> <span data-ttu-id="e55b3-254">W związku z tym deklarowanie domyślnej przestrzeni `xmlns` nazw XAML jako domyślne jest wszystko, co jest niezbędne do odwoływania się do elementów XAML, które pochodzą z wpf.</span><span class="sxs-lookup"><span data-stu-id="e55b3-254">Therefore, declaring the default XAML namespace as the default `xmlns` is all that is necessary in order to reference XAML elements that come from WPF assemblies.</span></span>

### <a name="the-x-prefix"></a><span data-ttu-id="e55b3-255">Prefiks x:</span><span class="sxs-lookup"><span data-stu-id="e55b3-255">The x: prefix</span></span>

<span data-ttu-id="e55b3-256">W przykładzie poprzedniego elementu głównego `x:` prefiks został użyty `http://schemas.microsoft.com/winfx/2006/xaml`do mapowania obszaru nazw XAML, który jest dedykowaną przestrzenią nazw XAML obsługującą konstrukcje języka XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-256">In the previous root element example, the prefix `x:` was used to map the XAML namespace `http://schemas.microsoft.com/winfx/2006/xaml`, which is the dedicated XAML namespace that supports XAML language constructs.</span></span> <span data-ttu-id="e55b3-257">Ten `x:` prefiks jest używany do mapowania tej przestrzeni nazw XAML w szablonach dla projektów, w przykładach i w dokumentacji w tym zestawie SDK.</span><span class="sxs-lookup"><span data-stu-id="e55b3-257">This `x:` prefix is used for mapping this XAML namespace in the templates for projects, in examples, and in documentation throughout this SDK.</span></span> <span data-ttu-id="e55b3-258">Obszar nazw XAML dla języka XAML zawiera kilka konstrukcji programowania, które będą często używane w języku XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-258">The XAML namespace for the XAML language contains several programming constructs that you will use frequently in your XAML.</span></span> <span data-ttu-id="e55b3-259">Poniżej znajduje się lista `x:` najczęściej używanych konstrukcji programowania prefiksów:</span><span class="sxs-lookup"><span data-stu-id="e55b3-259">The following is a listing of the most common `x:` prefix programming constructs you will use:</span></span>

- <span data-ttu-id="e55b3-260">[x:Key:](../xaml-services/xkey-directive.md)Ustawia unikatowy klucz dla <xref:System.Windows.ResourceDictionary> każdego zasobu w (lub podobnych pojęć słownika w innych ramach).</span><span class="sxs-lookup"><span data-stu-id="e55b3-260">[x:Key](../xaml-services/xkey-directive.md): Sets a unique key for each resource in a <xref:System.Windows.ResourceDictionary> (or similar dictionary concepts in other frameworks).</span></span> <span data-ttu-id="e55b3-261">`x:Key`prawdopodobnie będzie stanowić 90 `x:` procent użycia, które zobaczysz w typowych znaczników aplikacji WPF.</span><span class="sxs-lookup"><span data-stu-id="e55b3-261">`x:Key` will probably account for 90 percent of the `x:` usages you will see in a typical WPF app's markup.</span></span>

- <span data-ttu-id="e55b3-262">[x:Klasa:](../xaml-services/xclass-directive.md)Określa obszar nazw CLR i nazwę klasy dla klasy, która udostępnia kod dla strony XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-262">[x:Class](../xaml-services/xclass-directive.md): Specifies the CLR namespace and class name for the class that provides code-behind for a XAML page.</span></span> <span data-ttu-id="e55b3-263">Musisz mieć taką klasę do obsługi związane z kodem na modelu programowania `x:` WPF i dlatego prawie zawsze widzisz mapowane, nawet jeśli nie ma żadnych zasobów.</span><span class="sxs-lookup"><span data-stu-id="e55b3-263">You must have such a class to support code-behind per the WPF programming model, and therefore you almost always see `x:` mapped, even if there are no resources.</span></span>

- <span data-ttu-id="e55b3-264">[x:Name](../xaml-services/xname-directive.md): Określa nazwę obiektu w czasie wykonywania dla wystąpienia, które istnieje w kodzie w czasie wykonywania po przetworzyniu elementu obiektu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-264">[x:Name](../xaml-services/xname-directive.md): Specifies a run-time object name for the instance that exists in run-time code after an object element is processed.</span></span> <span data-ttu-id="e55b3-265">Ogólnie rzecz biorąc, często będziesz używać właściwości równoważnej zdefiniowanej przez WPF dla [x:Name](../xaml-services/xname-directive.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-265">In general, you will frequently use a WPF-defined equivalent property for [x:Name](../xaml-services/xname-directive.md).</span></span> <span data-ttu-id="e55b3-266">Takie właściwości mapują specjalnie do właściwości kopii CLR i w związku z tym są wygodniejsze w programowaniu aplikacji, gdzie często używasz kodu czasu wykonywania, aby znaleźć nazwane elementy z zainicjowanego XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-266">Such properties map specifically to a CLR backing property and are thus more convenient for app programming, where you frequently use run-time code to find the named elements from initialized XAML.</span></span> <span data-ttu-id="e55b3-267">Najczęstszą taką właściwością jest <xref:System.Windows.FrameworkElement.Name%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="e55b3-267">The most common such property is <xref:System.Windows.FrameworkElement.Name%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="e55b3-268">Nadal można użyć [x:Name,](../xaml-services/xname-directive.md) gdy równoważne <xref:System.Windows.FrameworkElement.Name%2A> WPF framework-level właściwość nie jest obsługiwana w określonym typie.</span><span class="sxs-lookup"><span data-stu-id="e55b3-268">You might still use [x:Name](../xaml-services/xname-directive.md) when the equivalent WPF framework-level <xref:System.Windows.FrameworkElement.Name%2A> property is not supported in a particular type.</span></span> <span data-ttu-id="e55b3-269">Dzieje się tak w niektórych scenariuszach animacji.</span><span class="sxs-lookup"><span data-stu-id="e55b3-269">This occurs in certain animation scenarios.</span></span>

- <span data-ttu-id="e55b3-270">[x:Static](../xaml-services/xstatic-markup-extension.md): Włącza odwołanie, które zwraca wartość statyczną, która nie jest w przeciwnym razie właściwością kompatybilną z XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-270">[x:Static](../xaml-services/xstatic-markup-extension.md): Enables a reference that returns a static value that is not otherwise a XAML-compatible property.</span></span>

- <span data-ttu-id="e55b3-271">[x:Type](../xaml-services/xtype-markup-extension.md): Tworzy <xref:System.Type> odwołanie na podstawie nazwy typu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-271">[x:Type](../xaml-services/xtype-markup-extension.md): Constructs a <xref:System.Type> reference based on a type name.</span></span> <span data-ttu-id="e55b3-272">Służy do określania atrybutów, <xref:System.Type> <xref:System.Windows.Style.TargetType%2A?displayProperty=nameWithType>które zajmują , takich jak ,<xref:System.Type> chociaż często właściwość ma natywnego ciąg do konwersji w taki sposób, że [x:Type](../xaml-services/xtype-markup-extension.md) znaczników rozszerzenia rozszerzenia jest opcjonalne.</span><span class="sxs-lookup"><span data-stu-id="e55b3-272">This is used to specify attributes that take <xref:System.Type>, such as <xref:System.Windows.Style.TargetType%2A?displayProperty=nameWithType>, although frequently the property has native string-to-<xref:System.Type> conversion in such a way that the [x:Type](../xaml-services/xtype-markup-extension.md) markup extension usage is optional.</span></span>

<span data-ttu-id="e55b3-273">Istnieją dodatkowe konstrukcje programowania `x:` w przestrzeni nazw prefiks/XAML, które nie są tak powszechne.</span><span class="sxs-lookup"><span data-stu-id="e55b3-273">There are additional programming constructs in the `x:` prefix/XAML namespace, which are not as common.</span></span> <span data-ttu-id="e55b3-274">Aby uzyskać szczegółowe informacje, zobacz [Obszar nazw XAML (x:) Funkcje językowe](../xaml-services/namespace-language-features.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-274">For details, see [XAML Namespace (x:) Language Features](../xaml-services/namespace-language-features.md).</span></span>

## <a name="custom-prefixes-and-custom-types-in-xaml"></a><span data-ttu-id="e55b3-275">Niestandardowe prefiksy i typy niestandardowe w języku XAML</span><span class="sxs-lookup"><span data-stu-id="e55b3-275">Custom prefixes and custom types in XAML</span></span>

<span data-ttu-id="e55b3-276">Dla własnych zestawów niestandardowych lub dla zestawów poza rdzeniem WPF **PresentationCore**, **PresentationFramework** i **WindowsBase**, można określić zestaw jako część mapowania niestandardowego. `xmlns`</span><span class="sxs-lookup"><span data-stu-id="e55b3-276">For your own custom assemblies, or for assemblies outside the WPF core of **PresentationCore**, **PresentationFramework** and **WindowsBase**, you can specify the assembly as part of a custom `xmlns` mapping.</span></span> <span data-ttu-id="e55b3-277">Następnie można odwoływać się do typów z tego zestawu w xaml, tak długo, jak ten typ jest poprawnie zaimplementowana do obsługi użycia XAML, które próbujesz.</span><span class="sxs-lookup"><span data-stu-id="e55b3-277">You can then reference types from that assembly in your XAML, so long as that type is correctly implemented to support the XAML usages you are attempting.</span></span>

<span data-ttu-id="e55b3-278">Poniżej przedstawiono podstawowy przykład działania prefiksów niestandardowych w znacznikach XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-278">The following is a basic example of how custom prefixes work in XAML markup.</span></span> <span data-ttu-id="e55b3-279">Prefiks `custom` jest zdefiniowany w tagu elementu głównego i mapowane do określonego zestawu, który jest spakowany i dostępny z aplikacją.</span><span class="sxs-lookup"><span data-stu-id="e55b3-279">The prefix `custom` is defined in the root element tag, and mapped to a specific assembly that is packaged and available with the app.</span></span> <span data-ttu-id="e55b3-280">Ten zestaw zawiera `NumericUpDown`typ , który jest implementowany do obsługi ogólnego użycia XAML, a także przy użyciu dziedziczenia klasy, który umożliwia jego wstawienie w tym określonym punkcie w modelu zawartości WPF XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-280">This assembly contains a type `NumericUpDown`, which is implemented to support general XAML usage as well as using a class inheritance that permits its insertion at this particular point in a WPF XAML content model.</span></span> <span data-ttu-id="e55b3-281">Wystąpienie tego `NumericUpDown` formantu jest zadeklarowany jako element obiektu, przy użyciu prefiksu, tak aby analizator XAML wie, który obszar nazw XAML zawiera typ i dlatego, gdzie zestaw zapasowy jest, który zawiera definicję typu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-281">An instance of this `NumericUpDown` control is declared as an object element, using the prefix so that a XAML parser knows which XAML namespace contains the type, and therefore where the backing assembly is that contains the type definition.</span></span>

```xaml
<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:custom="clr-namespace:NumericUpDownCustomControl;assembly=CustomLibrary"
    >
  <StackPanel Name="LayoutRoot">
    <custom:NumericUpDown Name="numericCtrl1" Width="100" Height="60"/>
...
  </StackPanel>
</Page>
```

<span data-ttu-id="e55b3-282">Aby uzyskać więcej informacji na temat typów niestandardowych w języku XAML, zobacz [XAML i Klasy niestandardowe dla WPF](../../framework/wpf/advanced/xaml-and-custom-classes-for-wpf.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-282">For more information about custom types in XAML, see [XAML and Custom Classes for WPF](../../framework/wpf/advanced/xaml-and-custom-classes-for-wpf.md).</span></span>

<span data-ttu-id="e55b3-283">Aby uzyskać więcej informacji na temat powiązanych obszarów nazw XML i obszarów nazw kodu w zestawach, zobacz [XAML Obszary nazw i Mapowanie obszaru nazw dla WPF XAML](../../framework/wpf/advanced/xaml-namespaces-and-namespace-mapping-for-wpf-xaml.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-283">For more information about how XML namespaces and code namespaces in assemblies are related, see [XAML Namespaces and Namespace Mapping for WPF XAML](../../framework/wpf/advanced/xaml-namespaces-and-namespace-mapping-for-wpf-xaml.md).</span></span>

## <a name="events-and-xaml-code-behind"></a><span data-ttu-id="e55b3-284">Zdarzenia i kod XAML</span><span class="sxs-lookup"><span data-stu-id="e55b3-284">Events and XAML code-behind</span></span>

<span data-ttu-id="e55b3-285">Większość aplikacji WPF składa się zarówno z znaczników XAML i związanych z kodem.</span><span class="sxs-lookup"><span data-stu-id="e55b3-285">Most WPF apps consist of both XAML markup and code-behind.</span></span> <span data-ttu-id="e55b3-286">W ramach projektu kod XAML jest `.xaml` zapisywany jako plik, a język CLR, taki jak Microsoft Visual Basic lub C#, jest używany do pisania pliku zawierającego kod.</span><span class="sxs-lookup"><span data-stu-id="e55b3-286">Within a project, the XAML is written as a `.xaml` file, and a CLR language such as Microsoft Visual Basic or C# is used to write a code-behind file.</span></span> <span data-ttu-id="e55b3-287">Gdy plik XAML jest znaczników skompilowanych jako część programowania WPF i modeli aplikacji, lokalizacja pliku związanego z kodem XAML dla pliku `x:Class` XAML jest identyfikowany przez określenie obszaru nazw i klasy jako atrybut elementu głównego XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-287">When a XAML file is markup compiled as part of the WPF programming and application models, the location of the XAML code-behind file for a XAML file is identified by specifying a namespace and class as the `x:Class` attribute of the root element of the XAML.</span></span>

<span data-ttu-id="e55b3-288">W przykładach do tej pory widziałeś kilka przycisków, ale żaden z tych przycisków nie miał jeszcze żadnego logicznego zachowania skojarzonego z nimi.</span><span class="sxs-lookup"><span data-stu-id="e55b3-288">In the examples so far, you have seen several buttons, but none of these buttons had any logical behavior associated with them yet.</span></span> <span data-ttu-id="e55b3-289">Podstawowy mechanizm na poziomie aplikacji do dodawania zachowania dla elementu obiektu jest użycie istniejącego zdarzenia klasy elementu i napisać określonego programu obsługi dla tego zdarzenia, który jest wywoływany, gdy to zdarzenie jest wywoływane w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="e55b3-289">The primary application-level mechanism for adding a behavior for an object element is to use an existing event of the element class, and to write a specific handler for that event that is invoked when that event is raised at run-time.</span></span> <span data-ttu-id="e55b3-290">Nazwa zdarzenia i nazwa programu obsługi do użycia są określone w znacznikach, podczas gdy kod, który implementuje program obsługi jest zdefiniowany w kodzie.</span><span class="sxs-lookup"><span data-stu-id="e55b3-290">The event name and the name of the handler to use are specified in the markup, whereas the code that implements your handler is defined in the code-behind.</span></span>

[!code-xaml[XAMLOvwSupport#ButtonWithCodeBehind](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page3.xaml#buttonwithcodebehind)]

[!code-csharp[XAMLOvwSupport#ButtonWithCodeBehindHandler](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page3.xaml.cs#buttonwithcodebehindhandler)]
[!code-vb[XAMLOvwSupport#ButtonWithCodeBehindHandler](~/samples/snippets/visualbasic/VS_Snippets_Wpf/XAMLOvwSupport/VisualBasic/Page1.xaml.vb#buttonwithcodebehindhandler)]

<span data-ttu-id="e55b3-291">Należy zauważyć, że plik związany z kodem używa obszaru `ExampleNamespace` nazw CLR i deklaruje `ExamplePage` jako klasę częściową w tym obszarze nazw.</span><span class="sxs-lookup"><span data-stu-id="e55b3-291">Notice that the code-behind file uses the CLR namespace `ExampleNamespace` and declares `ExamplePage` as a partial class within that namespace.</span></span> <span data-ttu-id="e55b3-292">Jest to `x:Class` równoległe do `ExampleNamespace`wartości atrybutu .`ExamplePage`</span><span class="sxs-lookup"><span data-stu-id="e55b3-292">This parallels the `x:Class` attribute value of `ExampleNamespace`.`ExamplePage`</span></span> <span data-ttu-id="e55b3-293">który został podany w katalogu głównym znaczników.</span><span class="sxs-lookup"><span data-stu-id="e55b3-293">that was provided in the markup root.</span></span> <span data-ttu-id="e55b3-294">Kompilator znaczników WPF utworzy klasę częściową dla dowolnego skompilowanego pliku XAML, wyprowadzając klasę z typu elementu głównego.</span><span class="sxs-lookup"><span data-stu-id="e55b3-294">The WPF markup compiler will create a partial class for any compiled XAML file, by deriving a class from the root element type.</span></span> <span data-ttu-id="e55b3-295">Po podaniu kodu, który definiuje również tę samą klasę częściową, wynikowy kod jest łączony w tym samym obszarze nazw i klasy skompilowaną aplikację.</span><span class="sxs-lookup"><span data-stu-id="e55b3-295">When you provide code-behind that also defines the same partial class, the resulting code is combined within the same namespace and class of the compiled app.</span></span>

<span data-ttu-id="e55b3-296">Aby uzyskać więcej informacji na temat wymagań dotyczących programowania bez kodu w WPF, zobacz [Code-behind, Event Handler i Częściowe wymagania klasy w WPF.](../../framework/wpf/advanced/code-behind-and-xaml-in-wpf.md#code-behind-event-handler-and-partial-class-requirements-in-wpf)</span><span class="sxs-lookup"><span data-stu-id="e55b3-296">For more information about requirements for code-behind programming in WPF, see [Code-behind, Event Handler, and Partial Class Requirements in WPF](../../framework/wpf/advanced/code-behind-and-xaml-in-wpf.md#code-behind-event-handler-and-partial-class-requirements-in-wpf).</span></span>

<span data-ttu-id="e55b3-297">Jeśli nie chcesz utworzyć oddzielny plik związany z kodem, można również wbudowany kod w pliku XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-297">If you do not want to create a separate code-behind file, you can also inline your code in a XAML file.</span></span> <span data-ttu-id="e55b3-298">Jednak kod wbudowany jest mniej wszechstronną techniką, która ma znaczne ograniczenia.</span><span class="sxs-lookup"><span data-stu-id="e55b3-298">However, inline code is a less versatile technique that has substantial limitations.</span></span> <span data-ttu-id="e55b3-299">Aby uzyskać więcej informacji, zobacz [Code-Behind i XAML w WPF](../../framework/wpf/advanced/code-behind-and-xaml-in-wpf.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-299">For more informaiton, see [Code-Behind and XAML in WPF](../../framework/wpf/advanced/code-behind-and-xaml-in-wpf.md).</span></span>

### <a name="routed-events"></a><span data-ttu-id="e55b3-300">Kierowane zdarzenia</span><span class="sxs-lookup"><span data-stu-id="e55b3-300">Routed events</span></span>

<span data-ttu-id="e55b3-301">Funkcja określonego zdarzenia, która ma podstawowe dla WPF jest kierowane zdarzenie.</span><span class="sxs-lookup"><span data-stu-id="e55b3-301">A particular event feature that is fundamental to WPF is a routed event.</span></span> <span data-ttu-id="e55b3-302">Kierowane zdarzenia umożliwiają element do obsługi zdarzenia, które zostało podniesione przez inny element, tak długo, jak elementy są połączone za pośrednictwem relacji drzewa.</span><span class="sxs-lookup"><span data-stu-id="e55b3-302">Routed events enable an element to handle an event that was raised by a different element, as long as the elements are connected through a tree relationship.</span></span> <span data-ttu-id="e55b3-303">Podczas określania obsługi zdarzeń z atrybutem XAML, kierowane zdarzenie można nasłuchiwał i obsługiwał na dowolnym elemencie, w tym elementy, które nie zawierają tej konkretnej zdarzenia w tabeli członków klasy.</span><span class="sxs-lookup"><span data-stu-id="e55b3-303">When specifying event handling with a XAML attribute, the routed event can be listened for and handled on any element, including elements that do not list that particular event in the class members table.</span></span> <span data-ttu-id="e55b3-304">Jest to realizowane przez zakwalifikowanie atrybutu nazwa zdarzenia z nazwą klasy własnej.</span><span class="sxs-lookup"><span data-stu-id="e55b3-304">This is accomplished by qualifying the event name attribute with the owning class name.</span></span> <span data-ttu-id="e55b3-305">Na przykład element `StackPanel` nadrzędny `StackPanel`  /  `Button` w trwającym przykładzie może zarejestrować program obsługi dla `Button.Click` zdarzenia `StackPanel` przycisku elementu podrzędnego, <xref:System.Windows.Controls.Primitives.ButtonBase.Click> określając atrybut elementu obiektu, z nazwą programu obsługi jako wartością atrybutu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-305">For instance, the parent `StackPanel` in the ongoing `StackPanel` / `Button` example could register a handler for the child element button's <xref:System.Windows.Controls.Primitives.ButtonBase.Click> event by specifying the attribute `Button.Click` on the `StackPanel` object element, with your handler name as the attribute value.</span></span> <span data-ttu-id="e55b3-306">Aby uzyskać więcej informacji, zobacz [Omówienie zdarzeń trasowych](../../framework/wpf/advanced/routed-events-overview.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-306">For more information, see [Routed Events Overview](../../framework/wpf/advanced/routed-events-overview.md).</span></span>

## <a name="xaml-named-elements"></a><span data-ttu-id="e55b3-307">Elementy o nazwie XAML</span><span class="sxs-lookup"><span data-stu-id="e55b3-307">XAML named elements</span></span>

<span data-ttu-id="e55b3-308">Domyślnie wystąpienie obiektu utworzone na wykresie obiektu przez przetworzenie elementu obiektu XAML nie posiada unikatowego identyfikatora ani odwołania do obiektu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-308">By default, the object instance that is created in an object graph by processing a XAML object element does not possess a unique identifier or object reference.</span></span> <span data-ttu-id="e55b3-309">Z drugiej strony, jeśli wywołasz konstruktora w kodzie, prawie zawsze używasz wyniku konstruktora, aby ustawić zmienną na skonstruowane wystąpienie, dzięki czemu można odwoływać się do wystąpienia później w kodzie.</span><span class="sxs-lookup"><span data-stu-id="e55b3-309">In contrast, if you call a constructor in code, you almost always use the constructor result to set a variable to the constructed instance, so that you can reference the instance later in your code.</span></span> <span data-ttu-id="e55b3-310">Aby zapewnić znormalizowany dostęp do obiektów utworzonych za pomocą definicji znaczników, XAML definiuje [atrybut x:Name](../xaml-services/xname-directive.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-310">In order to provide standardized access to objects that were created through a markup definition, XAML defines the [x:Name attribute](../xaml-services/xname-directive.md).</span></span> <span data-ttu-id="e55b3-311">Można ustawić wartość atrybutu `x:Name` na dowolnym elemencie obiektu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-311">You can set the value of the `x:Name` attribute on any object element.</span></span> <span data-ttu-id="e55b3-312">W przypadku kodu, który wybierzesz identyfikator jest odpowiednikiem zmiennej wystąpienia, która odwołuje się do skonstruowanego wystąpienia.</span><span class="sxs-lookup"><span data-stu-id="e55b3-312">In your code-behind, the identifier you choose is equivalent to an instance variable that refers to the constructed instance.</span></span> <span data-ttu-id="e55b3-313">Pod każdym względem nazwane elementy działają tak, jakby były wystąpieniami obiektów (nazwy odwołań do tego wystąpienia), a kod związany może odwoływać się do nazwanych elementów do obsługi interakcji w czasie wykonywania w aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e55b3-313">In all respects, named elements function as if they were object instances (the name references that instance), and your code-behind can reference the named elements to handle run-time interactions within the app.</span></span> <span data-ttu-id="e55b3-314">To połączenie między wystąpieniami i zmiennymi jest realizowane przez kompilator znaczników WPF XAML, <xref:System.Windows.Markup.IComponentConnector.InitializeComponent%2A> a w szczególności obejmują funkcje i wzorce, takie jak te nie zostaną omówione szczegółowo w tym artykule.</span><span class="sxs-lookup"><span data-stu-id="e55b3-314">This connection between instances and variables is accomplished by the WPF XAML markup compiler, and more specifically involve features and patterns such as <xref:System.Windows.Markup.IComponentConnector.InitializeComponent%2A> that will not be discussed in detail in this article.</span></span>

<span data-ttu-id="e55b3-315">Elementy XAML na poziomie struktury <xref:System.Windows.FrameworkElement.Name%2A> WPF dziedziczą właściwość, `x:Name` która jest równoważna atrybutowi zdefiniowanemu przez XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-315">WPF framework-level XAML elements inherit a <xref:System.Windows.FrameworkElement.Name%2A> property, which is equivalent to the XAML defined `x:Name` attribute.</span></span> <span data-ttu-id="e55b3-316">Niektóre inne klasy również zapewniają odpowiedniki na poziomie właściwości `x:Name`dla `Name` programu , który jest również ogólnie zdefiniowany jako właściwość.</span><span class="sxs-lookup"><span data-stu-id="e55b3-316">Certain other classes also provide property-level equivalents for `x:Name`, which is also generally defined as a `Name` property.</span></span> <span data-ttu-id="e55b3-317">Ogólnie rzecz biorąc, jeśli nie `Name` można znaleźć właściwości w tabeli członków `x:Name` dla wybranego elementu/typu, użyj zamiast tego.</span><span class="sxs-lookup"><span data-stu-id="e55b3-317">Generally speaking, if you cannot find a `Name` property in the members table for your chosen element/type, use `x:Name` instead.</span></span> <span data-ttu-id="e55b3-318">Wartości `x:Name` będą stanowić identyfikator elementu XAML, który może być używany w czasie wykonywania, przez określone podsystemy lub za pomocą metod narzędziowych, takich jak <xref:System.Windows.FrameworkElement.FindName%2A>.</span><span class="sxs-lookup"><span data-stu-id="e55b3-318">The `x:Name` values will provide an identifier to a XAML element that can be used at run-time, either by specific subsystems or by utility methods such as <xref:System.Windows.FrameworkElement.FindName%2A>.</span></span>

<span data-ttu-id="e55b3-319">Poniższy przykład <xref:System.Windows.FrameworkElement.Name%2A> ustawia <xref:System.Windows.Controls.StackPanel> na element.</span><span class="sxs-lookup"><span data-stu-id="e55b3-319">The following example sets <xref:System.Windows.FrameworkElement.Name%2A> on a <xref:System.Windows.Controls.StackPanel> element.</span></span> <span data-ttu-id="e55b3-320">Następnie program obsługi <xref:System.Windows.Controls.Button> w <xref:System.Windows.Controls.StackPanel> obrębie, <xref:System.Windows.Controls.StackPanel> który odwołuje `buttonContainer` się do <xref:System.Windows.FrameworkElement.Name%2A>odwołania poprzez jego wystąpienie, zgodnie z zestawem przez .</span><span class="sxs-lookup"><span data-stu-id="e55b3-320">Then, a handler on a <xref:System.Windows.Controls.Button> within that <xref:System.Windows.Controls.StackPanel> references the <xref:System.Windows.Controls.StackPanel> through its instance reference `buttonContainer` as set by <xref:System.Windows.FrameworkElement.Name%2A>.</span></span>

[!code-xaml[XAMLOvwSupport#NamedE](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page7.xaml#namede)]
[!code-xaml[XAMLOvwSupport#NamedE2](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page7.xaml#namede2)]

[!code-csharp[XAMLOvwSupport#NameCode](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page7.xaml.cs#namecode)]
[!code-vb[XAMLOvwSupport#NameCode](~/samples/snippets/visualbasic/VS_Snippets_Wpf/XAMLOvwSupport/VisualBasic/Page1.xaml.vb#namecode)]

<span data-ttu-id="e55b3-321">Podobnie jak zmienna, nazwa XAML dla wystąpienia jest regulowana przez pojęcie zakresu, dzięki czemu nazwy mogą być wymuszane jako unikatowe w określonym zakresie, który jest przewidywalny.</span><span class="sxs-lookup"><span data-stu-id="e55b3-321">Just like a variable, the XAML name for an instance is governed by a concept of scope, so that names can be enforced to be unique within a certain scope that is predictable.</span></span> <span data-ttu-id="e55b3-322">Znacznik podstawowy definiujący stronę oznacza jeden unikatowy zakres nazw XAML, przy czym granica xaml namescope jest elementem głównym tej strony.</span><span class="sxs-lookup"><span data-stu-id="e55b3-322">The primary markup that defines a page denotes one unique XAML namescope, with the XAML namescope boundary being the root element of that page.</span></span> <span data-ttu-id="e55b3-323">Jednak inne źródła znaczników mogą wchodzić w interakcje ze stroną w czasie wykonywania, takie jak style lub szablony w stylach, a takie źródła znaczników często mają własne identyfikatory nazw XAML, które niekoniecznie łączą się z identyfikatorem XAML strony.</span><span class="sxs-lookup"><span data-stu-id="e55b3-323">However, other markup sources can interact with a page at run-time, such as styles or templates within styles, and such markup sources often have their own XAML namescopes that do not necessarily connect with the XAML namescope of the page.</span></span> <span data-ttu-id="e55b3-324">Aby uzyskać `x:Name` więcej informacji na temat i <xref:System.Windows.FrameworkElement.Name%2A>xAML namescopes, zobacz , [x:Name Directive](../xaml-services/xname-directive.md)lub [WPF XAML Namescopes](../../framework/wpf/advanced/wpf-xaml-namescopes.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-324">For more information on `x:Name` and XAML namescopes, see <xref:System.Windows.FrameworkElement.Name%2A>, [x:Name Directive](../xaml-services/xname-directive.md), or [WPF XAML Namescopes](../../framework/wpf/advanced/wpf-xaml-namescopes.md).</span></span>

## <a name="attached-properties-and-attached-events"></a><span data-ttu-id="e55b3-325">Dołączone właściwości i dołączone zdarzenia</span><span class="sxs-lookup"><span data-stu-id="e55b3-325">Attached properties and attached events</span></span>

<span data-ttu-id="e55b3-326">XAML określa funkcję języka, która umożliwia określenie niektórych właściwości lub zdarzeń na dowolnym elemencie, niezależnie od tego, czy właściwość lub zdarzenie istnieje w definicjach typu dla elementu, na który jest ustawiany.</span><span class="sxs-lookup"><span data-stu-id="e55b3-326">XAML specifies a language feature that enables certain properties or events to be specified on any element, regardless of whether the property or event exists in the type's definitions for the element it is being set on.</span></span> <span data-ttu-id="e55b3-327">Wersja właściwości tej funkcji jest nazywana dołączoną właściwością, wersja zdarzeń jest nazywana dołączonym zdarzeniem.</span><span class="sxs-lookup"><span data-stu-id="e55b3-327">The properties version of this feature is called an attached property, the events version is called an attached event.</span></span> <span data-ttu-id="e55b3-328">Koncepcyjnie można myśleć o dołączonych właściwości i dołączonych zdarzeń jako globalnych elementów członkowskich, które można ustawić na dowolnym wystąpieniu elementu XAML/obiektu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-328">Conceptually, you can think of attached properties and attached events as global members that can be set on any XAML element/object instance.</span></span> <span data-ttu-id="e55b3-329">Jednak ten element/klasa lub większa infrastruktura musi obsługiwać zapas właściwości kopii zapasowej dla dołączonych wartości.</span><span class="sxs-lookup"><span data-stu-id="e55b3-329">However, that element/class or a larger infrastructure must support a backing property store for the attached values.</span></span>

<span data-ttu-id="e55b3-330">Dołączone właściwości w XAML są zwykle używane za pośrednictwem składni atrybutów.</span><span class="sxs-lookup"><span data-stu-id="e55b3-330">Attached properties in XAML are typically used through attribute syntax.</span></span> <span data-ttu-id="e55b3-331">W składni atrybutu należy określić dołączoną `ownerType.propertyName`właściwość w formularzu .</span><span class="sxs-lookup"><span data-stu-id="e55b3-331">In attribute syntax, you specify an attached property in the form `ownerType.propertyName`.</span></span>

<span data-ttu-id="e55b3-332">Powierzchownie przypomina użycie elementu właściwości, ale w `ownerType` tym przypadku określić jest zawsze inny typ niż element obiektu, w którym jest ustawiona dołączone właściwość.</span><span class="sxs-lookup"><span data-stu-id="e55b3-332">Superficially, this resembles a property element usage, but in this case the `ownerType` you specify is always a different type than the object element where the attached property is being set.</span></span> <span data-ttu-id="e55b3-333">`ownerType`jest typem, który udostępnia metody akcesora, które są wymagane przez procesor XAML w celu uzyskania lub ustawić wartość dołączonej właściwości.</span><span class="sxs-lookup"><span data-stu-id="e55b3-333">`ownerType` is the type that provides the accessor methods that are required by a XAML processor in order to get or set the attached property value.</span></span>

<span data-ttu-id="e55b3-334">Najbardziej typowym scenariuszem dla dołączonych właściwości jest włączenie elementów podrzędnych do raportowania wartości właściwości do ich elementu nadrzędnego.</span><span class="sxs-lookup"><span data-stu-id="e55b3-334">The most common scenario for attached properties is to enable child elements to report a property value to their parent element.</span></span>

<span data-ttu-id="e55b3-335">Poniższy przykład ilustruje dołączoną <xref:System.Windows.Controls.DockPanel.Dock%2A?displayProperty=nameWithType> właściwość.</span><span class="sxs-lookup"><span data-stu-id="e55b3-335">The following example illustrates the <xref:System.Windows.Controls.DockPanel.Dock%2A?displayProperty=nameWithType> attached property.</span></span> <span data-ttu-id="e55b3-336">Klasa <xref:System.Windows.Controls.DockPanel> definiuje akcesory dla <xref:System.Windows.Controls.DockPanel.Dock%2A?displayProperty=nameWithType> i dlatego jest właścicielem dołączonej właściwości.</span><span class="sxs-lookup"><span data-stu-id="e55b3-336">The <xref:System.Windows.Controls.DockPanel> class defines the accessors for <xref:System.Windows.Controls.DockPanel.Dock%2A?displayProperty=nameWithType> and therefore owns the attached property.</span></span> <span data-ttu-id="e55b3-337">Klasa <xref:System.Windows.Controls.DockPanel> zawiera również logikę, która iteruje jego elementy podrzędne <xref:System.Windows.Controls.DockPanel.Dock%2A?displayProperty=nameWithType>i w szczególności sprawdza każdy element dla ustawionej wartości .</span><span class="sxs-lookup"><span data-stu-id="e55b3-337">The <xref:System.Windows.Controls.DockPanel> class also includes logic that iterates its child elements and specifically checks each element for a set value of <xref:System.Windows.Controls.DockPanel.Dock%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="e55b3-338">Jeśli wartość zostanie znaleziona, ta wartość jest używana podczas układu do położenia elementów podrzędnych.</span><span class="sxs-lookup"><span data-stu-id="e55b3-338">If a value is found, that value is used during layout to position the child elements.</span></span> <span data-ttu-id="e55b3-339">Korzystanie z <xref:System.Windows.Controls.DockPanel.Dock%2A?displayProperty=nameWithType> dołączonej właściwości i tej zdolności pozycjonowania <xref:System.Windows.Controls.DockPanel> jest w rzeczywistości scenariusz motywacyjny dla klasy.</span><span class="sxs-lookup"><span data-stu-id="e55b3-339">Use of the <xref:System.Windows.Controls.DockPanel.Dock%2A?displayProperty=nameWithType> attached property and this positioning capability is in fact the motivating scenario for the <xref:System.Windows.Controls.DockPanel> class.</span></span>

[!code-xaml[XAMLOvwSupport#DockAP](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page8.xaml#dockap)]

<span data-ttu-id="e55b3-340">W WPF WPF większość lub wszystkie dołączone właściwości są również implementowane jako właściwości zależności.</span><span class="sxs-lookup"><span data-stu-id="e55b3-340">In WPF, most or all the attached properties are also implemented as dependency properties.</span></span> <span data-ttu-id="e55b3-341">Aby uzyskać więcej informacji, zobacz [Omówienie załączonych właściwości](../../framework/wpf/advanced/attached-properties-overview.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-341">For more information, see [Attached Properties Overview](../../framework/wpf/advanced/attached-properties-overview.md).</span></span>

<span data-ttu-id="e55b3-342">Dołączone zdarzenia używają `ownerType.eventName` podobnej formy składni atrybutów.</span><span class="sxs-lookup"><span data-stu-id="e55b3-342">Attached events use a similar `ownerType.eventName` form of attribute syntax.</span></span> <span data-ttu-id="e55b3-343">Podobnie jak zdarzenia niezałączone, wartość atrybutu dla dołączonego zdarzenia w XAML określa nazwę metody obsługi, która jest wywoływana, gdy zdarzenie jest obsługiwane w elemencie.</span><span class="sxs-lookup"><span data-stu-id="e55b3-343">Just like the non-attached events, the attribute value for an attached event in XAML specifies the name of the handler method that is invoked when the event is handled on the element.</span></span> <span data-ttu-id="e55b3-344">Dołączone użycia zdarzeń w WPF XAML są mniej powszechne.</span><span class="sxs-lookup"><span data-stu-id="e55b3-344">Attached event usages in WPF XAML are less common.</span></span> <span data-ttu-id="e55b3-345">Aby uzyskać więcej informacji, zobacz [Omówienie załączonych zdarzeń](../../framework/wpf/advanced/attached-events-overview.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-345">For more information, see [Attached Events Overview](../../framework/wpf/advanced/attached-events-overview.md).</span></span>

## <a name="base-types-and-xaml"></a><span data-ttu-id="e55b3-346">Typy podstawowe i kod XAML</span><span class="sxs-lookup"><span data-stu-id="e55b3-346">Base types and XAML</span></span>

<span data-ttu-id="e55b3-347">Podstawowe WPF XAML i jego przestrzeni nazw XAML jest zbiorem typów, które odpowiadają clr obiektów oprócz elementów znaczników dla XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-347">Underlying WPF XAML and its XAML namespace is a collection of types that correspond to CLR objects in addition to markup elements for XAML.</span></span> <span data-ttu-id="e55b3-348">Jednak nie wszystkie klasy mogą być mapowane na elementy.</span><span class="sxs-lookup"><span data-stu-id="e55b3-348">However, not all classes can be mapped to elements.</span></span> <span data-ttu-id="e55b3-349">Klasy abstrakcyjne, <xref:System.Windows.Controls.Primitives.ButtonBase>takie jak , i niektóre nieseparskie klasy podstawowe, są używane do dziedziczenia w modelu obiektów CLR.</span><span class="sxs-lookup"><span data-stu-id="e55b3-349">Abstract classes, such as <xref:System.Windows.Controls.Primitives.ButtonBase>, and certain non-abstract base classes, are used for inheritance in the CLR objects model.</span></span> <span data-ttu-id="e55b3-350">Klasy podstawowe, w tym abstrakcyjne, są nadal ważne dla rozwoju XAML, ponieważ każdy z konkretnych elementów XAML dziedziczy członków z niektórych klas podstawowych w hierarchii.</span><span class="sxs-lookup"><span data-stu-id="e55b3-350">Base classes, including abstract ones, are still important to XAML development because each of the concrete XAML elements inherits members from some base class in its hierarchy.</span></span> <span data-ttu-id="e55b3-351">Często te elementy członkowskie obejmują właściwości, które można ustawić jako atrybuty elementu lub zdarzenia, które mogą być obsługiwane.</span><span class="sxs-lookup"><span data-stu-id="e55b3-351">Often these members include properties that can be set as attributes on the element, or events that can be handled.</span></span> <span data-ttu-id="e55b3-352"><xref:System.Windows.FrameworkElement>jest betonową bazą UI klasy WPF na poziomie framework WPF.</span><span class="sxs-lookup"><span data-stu-id="e55b3-352"><xref:System.Windows.FrameworkElement> is the concrete base UI class of WPF at the WPF framework level.</span></span> <span data-ttu-id="e55b3-353">Podczas projektowania interfejsu użytkownika, można użyć różnych kształtów, panel, dekorator, lub klasy sterowania, które pochodzą z <xref:System.Windows.FrameworkElement>.</span><span class="sxs-lookup"><span data-stu-id="e55b3-353">When designing UI, you will use various shape, panel, decorator, or control classes, which all derive from <xref:System.Windows.FrameworkElement>.</span></span> <span data-ttu-id="e55b3-354">Powiązana klasa <xref:System.Windows.FrameworkContentElement>podstawowa obsługuje elementy zorientowane na dokument, które działają dobrze w przypadku prezentacji układu <xref:System.Windows.FrameworkElement>przepływu, przy użyciu interfejsów API, które celowo odzwierciedlają interfejsy API w programie .</span><span class="sxs-lookup"><span data-stu-id="e55b3-354">A related base class, <xref:System.Windows.FrameworkContentElement>, supports document-oriented elements that work well for a flow layout presentation, using APIs that deliberately mirror the APIs in <xref:System.Windows.FrameworkElement>.</span></span> <span data-ttu-id="e55b3-355">Kombinacja atrybutów na poziomie elementu i modelu obiektu CLR zapewnia zestaw wspólnych właściwości, które są ustawione na większości elementów XAML betonu, niezależnie od określonego elementu XAML i jego typ podstawowy.</span><span class="sxs-lookup"><span data-stu-id="e55b3-355">The combination of attributes at the element level and a CLR object model provides you with a set of common properties that are settable on most concrete XAML elements, regardless of the specific XAML element and its underlying type.</span></span>

## <a name="xaml-security"></a><span data-ttu-id="e55b3-356">Zabezpieczenia XAML</span><span class="sxs-lookup"><span data-stu-id="e55b3-356">XAML security</span></span>

<span data-ttu-id="e55b3-357">XAML jest językiem znaczników, który bezpośrednio reprezentuje tworzenie i wykonywanie wystąpienia i obiektu.</span><span class="sxs-lookup"><span data-stu-id="e55b3-357">XAML is a markup language that directly represents object instantiation and execution.</span></span> <span data-ttu-id="e55b3-358">W związku z tym elementy utworzone w XAML mają taką samą możliwość interakcji z zasobami systemowymi (dostęp do sieci, system plików We/Wy, na przykład) jak równoważny wygenerowany kod.</span><span class="sxs-lookup"><span data-stu-id="e55b3-358">Therefore, elements created in XAML have the same ability to interact with system resources (network access, file system IO, for example) as the equivalent generated code does.</span></span> <span data-ttu-id="e55b3-359">XAML załadowany do w pełni zaufanej aplikacji ma taki sam dostęp do zasobów systemowych, jak aplikacja hostingowa.</span><span class="sxs-lookup"><span data-stu-id="e55b3-359">XAML loaded in to a fully trusted app has the same access to the system resources as the hosting app does.</span></span>

### <a name="code-access-security-cas-in-wpf"></a><span data-ttu-id="e55b3-360">Zabezpieczenia dostępu do kodu (CAS) w WPF</span><span class="sxs-lookup"><span data-stu-id="e55b3-360">Code Access Security (CAS) in WPF</span></span>

<span data-ttu-id="e55b3-361">**Ta sekcja dotyczy tylko programu .NET Framework. WPF WPF dla platformy .NET Core nie obsługuje cas. Aby uzyskać więcej informacji, zobacz [Różnice zabezpieczeń dostępu do kodu](../migration/differences-from-net-framework.md#code-access-security).**</span><span class="sxs-lookup"><span data-stu-id="e55b3-361">**This section only applies to .NET Framework. WPF for .NET Core doesn't support CAS. For more information, see [Code Access Security differences](../migration/differences-from-net-framework.md#code-access-security).**</span></span>

<span data-ttu-id="e55b3-362">WPF DLA platformy .NET Framework obsługuje zabezpieczenia dostępu do kodu (CAS).</span><span class="sxs-lookup"><span data-stu-id="e55b3-362">WPF for .NET Framework supports Code Access Security (CAS).</span></span> <span data-ttu-id="e55b3-363">Oznacza to, że zawartość WPF uruchomiona w strefie internetowej zmniejszyła uprawnienia do wykonywania.</span><span class="sxs-lookup"><span data-stu-id="e55b3-363">This means that WPF content running in the internet zone has reduced execution permissions.</span></span> <span data-ttu-id="e55b3-364">"Loose XAML" (strony niekompilowanego XAML interpretowane w czasie ładowania przez przeglądarkę XAML) i XBAP są zwykle uruchamiane w tej strefie internetowej i używają tego samego zestawu uprawnień.</span><span class="sxs-lookup"><span data-stu-id="e55b3-364">"Loose XAML" (pages of noncompiled XAML interpreted at load time by a XAML viewer) and XBAP are usually run in this internet zone and use the same permission set.</span></span> <span data-ttu-id="e55b3-365">Jednak XAML załadowany do w pełni zaufanej aplikacji ma taki sam dostęp do zasobów systemowych, jak aplikacja hostingowa.</span><span class="sxs-lookup"><span data-stu-id="e55b3-365">However, XAML loaded in to a fully trusted application has the same access to the system resources as the hosting application does.</span></span> <span data-ttu-id="e55b3-366">Aby uzyskać więcej informacji, zobacz [WPF Częściowe zabezpieczenia zaufania](../../framework/wpf/wpf-partial-trust-security.md).</span><span class="sxs-lookup"><span data-stu-id="e55b3-366">For more information, see [WPF Partial Trust Security](../../framework/wpf/wpf-partial-trust-security.md).</span></span>

## <a name="loading-xaml-from-code"></a><span data-ttu-id="e55b3-367">Ładowanie kodu XAML z kodu</span><span class="sxs-lookup"><span data-stu-id="e55b3-367">Loading XAML from code</span></span>

<span data-ttu-id="e55b3-368">XAML może służyć do definiowania wszystkich interfejsu użytkownika, ale czasami jest również odpowiednie do zdefiniowania tylko kawałek interfejsu użytkownika w języku XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-368">XAML can be used to define all of the UI, but it is sometimes also appropriate to define just a piece of the UI in XAML.</span></span> <span data-ttu-id="e55b3-369">Ta funkcja może służyć do umożliwienia częściowego dostosowywania, lokalnego przechowywania informacji, przy użyciu XAML w celu zapewnienia obiektu biznesowego lub różnych możliwych scenariuszy.</span><span class="sxs-lookup"><span data-stu-id="e55b3-369">This capability could be used to enable partial customization, local storage of information, using XAML to provide a business object, or a variety of possible scenarios.</span></span> <span data-ttu-id="e55b3-370">Kluczem do tych scenariuszy <xref:System.Windows.Markup.XamlReader> jest <xref:System.Windows.Markup.XamlReader.Load%2A> klasa i jej metoda.</span><span class="sxs-lookup"><span data-stu-id="e55b3-370">The key to these scenarios is the <xref:System.Windows.Markup.XamlReader> class and its <xref:System.Windows.Markup.XamlReader.Load%2A> method.</span></span> <span data-ttu-id="e55b3-371">Dane wejściowe są plikiem XAML, a dane wyjściowe są obiektem reprezentującym wszystkie drzewa wykonywania obiektów utworzonych na podstawie tego znaczników.</span><span class="sxs-lookup"><span data-stu-id="e55b3-371">The input is a XAML file, and the output is an object that represents all of the run-time tree of objects that was created from that markup.</span></span> <span data-ttu-id="e55b3-372">Następnie można wstawić obiekt jako właściwość innego obiektu, który już istnieje w aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e55b3-372">You then can insert the object to be a property of another object that already exists in the app.</span></span> <span data-ttu-id="e55b3-373">Tak długo, jak właściwość jest odpowiednią właściwością w modelu zawartości, która ma możliwości wyświetlania, które mają możliwość wyświetlania i która powiadomi aparat wykonywania, że nowa zawartość została dodana do aplikacji, można łatwo zmodyfikować zawartość uruchomionej aplikacji, ładując w języku XAML.</span><span class="sxs-lookup"><span data-stu-id="e55b3-373">So long as the property is an appropriate property in the content model that has eventual display capabilities and that will notify the execution engine that new content has been added into the app, you can modify a running app's contents easily by loading in XAML.</span></span> <span data-ttu-id="e55b3-374">W przypadku platformy .NET Framework ta funkcja jest ogólnie dostępna tylko w aplikacjach pełnego zaufania, ze względu na oczywiste implikacje bezpieczeństwa ładowania plików do aplikacji podczas ich uruchamiania.</span><span class="sxs-lookup"><span data-stu-id="e55b3-374">For .NET Framework, this capability is generally only available in full-trust applications, because of the obvious security implications of loading files into applications as they run.</span></span>

## <a name="see-also"></a><span data-ttu-id="e55b3-375">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="e55b3-375">See also</span></span>

- [<span data-ttu-id="e55b3-376">Szczegóły składni XAML</span><span class="sxs-lookup"><span data-stu-id="e55b3-376">XAML Syntax In Detail</span></span>](../../framework/wpf/advanced/xaml-syntax-in-detail.md)
- [<span data-ttu-id="e55b3-377">Klasy XAML i niestandardowe dla WPF</span><span class="sxs-lookup"><span data-stu-id="e55b3-377">XAML and Custom Classes for WPF</span></span>](../../framework/wpf/advanced/xaml-and-custom-classes-for-wpf.md)
- [<span data-ttu-id="e55b3-378">Przestrzeń nazw XAML (x:) Funkcje językowe</span><span class="sxs-lookup"><span data-stu-id="e55b3-378">XAML Namespace (x:) Language Features</span></span>](../xaml-services/namespace-language-features.md)
- [<span data-ttu-id="e55b3-379">Rozszerzenia WPF XAML</span><span class="sxs-lookup"><span data-stu-id="e55b3-379">WPF XAML Extensions</span></span>](../../framework/wpf/advanced/wpf-xaml-extensions.md)
- [<span data-ttu-id="e55b3-380">Przegląd Elementy bazy</span><span class="sxs-lookup"><span data-stu-id="e55b3-380">Base Elements Overview</span></span>](../../framework/wpf/advanced/base-elements-overview.md)
- [<span data-ttu-id="e55b3-381">Drzewa w WPF</span><span class="sxs-lookup"><span data-stu-id="e55b3-381">Trees in WPF</span></span>](../../framework/wpf/advanced/trees-in-wpf.md)
