---
title: Stan i dane w aplikacjach platformy Docker
description: Stan i zarządzanie danymi w aplikacjach platformy Docker. Mikrousługi są dostarczane, ale dane są, jak obsługiwać to korzystanie z mikrousług.
author: CESARDELATORRE
ms.author: wiwagn
ms.date: 09/20/2018
ms.openlocfilehash: 70c3cee8c5fd1e63f2ff869f49b1fb02ab8f59dd
ms.sourcegitcommit: ccd8c36b0d74d99291d41aceb14cf98d74dc9d2b
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 12/10/2018
ms.locfileid: "53152659"
---
# <a name="state-and-data-in-docker-applications"></a><span data-ttu-id="51a94-104">Stan i dane w aplikacjach platformy Docker</span><span class="sxs-lookup"><span data-stu-id="51a94-104">State and data in Docker applications</span></span>

<span data-ttu-id="51a94-105">W większości przypadków można traktować jako wystąpienie procesu kontenera.</span><span class="sxs-lookup"><span data-stu-id="51a94-105">In most cases, you can think of a container as an instance of a process.</span></span> <span data-ttu-id="51a94-106">Proces nie zapamiętywania.</span><span class="sxs-lookup"><span data-stu-id="51a94-106">A process doesn't maintain persistent state.</span></span> <span data-ttu-id="51a94-107">Gdy kontener może zapisywać jej magazynu lokalnego, przy założeniu, że wystąpienie będzie wokół przez czas nieokreślony oznaczałoby przy założeniu, że będą trwałe jednej lokalizacji w pamięci.</span><span class="sxs-lookup"><span data-stu-id="51a94-107">While a container can write to its local storage, assuming that an instance will be around indefinitely would be like assuming that a single location in memory will be durable.</span></span> <span data-ttu-id="51a94-108">Należy przyjąć, że obrazy kontenera, takich jak procesy, istnieje wiele wystąpień lub po pewnym czasie zostaną skasowane.</span><span class="sxs-lookup"><span data-stu-id="51a94-108">You should assume that container images, like processes, have multiple instances or will eventually be killed.</span></span> <span data-ttu-id="51a94-109">Jeśli są one zarządzane z koordynatorem kontenera, należy przyjąć, że może uzyskać z jednego węzła lub maszyny Wirtualnej przeniesione do innego.</span><span class="sxs-lookup"><span data-stu-id="51a94-109">If they're managed with a container orchestrator, you should assume that they might get moved from one node or VM to another.</span></span>

<span data-ttu-id="51a94-110">Następujące rozwiązania są używane do zarządzania trwałości danych w aplikacji platformy Docker:</span><span class="sxs-lookup"><span data-stu-id="51a94-110">The following solutions are used to manage persistent data in Docker applications:</span></span>

<span data-ttu-id="51a94-111">Z hosta platformy Docker jako [woluminów Docker](https://docs.docker.com/engine/admin/volumes/):</span><span class="sxs-lookup"><span data-stu-id="51a94-111">From the Docker host, as [Docker Volumes](https://docs.docker.com/engine/admin/volumes/):</span></span>

- <span data-ttu-id="51a94-112">**Woluminy** są przechowywane w obszarze systemu plików hosta, który jest zarządzany przez platformę Docker.</span><span class="sxs-lookup"><span data-stu-id="51a94-112">**Volumes** are stored in an area of the host filesystem that's managed by Docker.</span></span>

- <span data-ttu-id="51a94-113">**Powiąż instaluje** można mapować do dowolnego folderu systemu plików hosta, więc dostępu nie mogą być kontrolowane z procesu platformy Docker i może stanowić zagrożenie bezpieczeństwa, ponieważ kontener może uzyskać dostępu do poufnych folderów systemu operacyjnego.</span><span class="sxs-lookup"><span data-stu-id="51a94-113">**Bind mounts** can map to any folder in the host filesystem, so access can't be controlled from Docker process and can pose a security risk as a container could access sensitive OS folders.</span></span>

- <span data-ttu-id="51a94-114">**Instaluje tmpfs** są, takich jak foldery wirtualne, które tylko istnieje w pamięci hosta i nigdy nie są zapisywane w systemie plików.</span><span class="sxs-lookup"><span data-stu-id="51a94-114">**tmpfs mounts** are like virtual folders that only exist in the host's memory and are never written to the filesystem.</span></span>

<span data-ttu-id="51a94-115">Z magazynu zdalnego:</span><span class="sxs-lookup"><span data-stu-id="51a94-115">From remote storage:</span></span>

- <span data-ttu-id="51a94-116">[Usługa Azure Storage](https://azure.microsoft.com/documentation/services/storage/), zapewniającą dystrybucyjny geograficzną storage zapewniająca dobrym rozwiązaniem długoterminowym stanów trwałych dla kontenerów.</span><span class="sxs-lookup"><span data-stu-id="51a94-116">[Azure Storage](https://azure.microsoft.com/documentation/services/storage/), which provides geo-distributable storage, providing a good long-term persistence solution for containers.</span></span>

- <span data-ttu-id="51a94-117">Zdalne relacyjnych baz danych, takich jak [usługi Azure SQL Database](https://azure.microsoft.com/services/sql-database/) lub bazy danych NoSQL, takie jak [usługi Azure Cosmos DB](https://docs.microsoft.com/azure/cosmos-db/introduction), lub usług, takich jak buforowanie [Redis](https://redis.io/).</span><span class="sxs-lookup"><span data-stu-id="51a94-117">Remote relational databases like [Azure SQL Database](https://azure.microsoft.com/services/sql-database/) or NoSQL databases like [Azure Cosmos DB](https://docs.microsoft.com/azure/cosmos-db/introduction), or cache services like [Redis](https://redis.io/).</span></span>

<span data-ttu-id="51a94-118">Z kontenera platformy Docker:</span><span class="sxs-lookup"><span data-stu-id="51a94-118">From the Docker container:</span></span>

> <span data-ttu-id="51a94-119">Środowisko docker zawiera funkcję o nazwie *nakładki system plików*.</span><span class="sxs-lookup"><span data-stu-id="51a94-119">Docker provides a feature named the *overlay file system*.</span></span> <span data-ttu-id="51a94-120">Implementuje to zadanie kopii przy zapisie czy magazyny zaktualizowane informacje do katalogu głównego systemu plików kontenera.</span><span class="sxs-lookup"><span data-stu-id="51a94-120">This implements a copy-on-write task that stores updated information to the root file system of the container.</span></span> <span data-ttu-id="51a94-121">Informacje te są dodatkowo do oryginalnego obrazu, na którym bazuje kontenera.</span><span class="sxs-lookup"><span data-stu-id="51a94-121">That information is in addition to the original image on which the container is based.</span></span> <span data-ttu-id="51a94-122">Jeśli kontener zostanie usunięty z systemu, te zmiany zostaną utracone.</span><span class="sxs-lookup"><span data-stu-id="51a94-122">If the container is deleted from the system, those changes are lost.</span></span> <span data-ttu-id="51a94-123">W związku z tym mimo że jest możliwe do zapisywania stanu kontener w ramach jego magazynu lokalnego, projektowania systemu wokół tego spowodowałoby to konflikt z założenia projektowania kontenera, która domyślnie jest bezstanowy.</span><span class="sxs-lookup"><span data-stu-id="51a94-123">Therefore, while it's possible to save the state of a container within its local storage, designing a system around this would conflict with the premise of container design, which by default is stateless.</span></span>
>
> <span data-ttu-id="51a94-124">Jednak wcześniej wprowadzonych woluminy platformy Docker jest teraz preferowanym sposobem obsługi danych lokalnych platformy Docker.</span><span class="sxs-lookup"><span data-stu-id="51a94-124">However, the previously introduced Docker Volumes is now the preferred way to handling local data Docker.</span></span> <span data-ttu-id="51a94-125">Jeśli potrzebujesz więcej informacji na temat magazynu w kontenerach wyboru na [sterowników pamięci masowej Docker](https://docs.docker.com/storage/storagedriver/select-storage-driver/) i [o sterownikach pamięci masowej](https://docs.docker.com/storage/storagedriver/).</span><span class="sxs-lookup"><span data-stu-id="51a94-125">If you need more information about storage in containers check on [Docker storage drivers](https://docs.docker.com/storage/storagedriver/select-storage-driver/) and [About storage drivers](https://docs.docker.com/storage/storagedriver/).</span></span>

<span data-ttu-id="51a94-126">Poniżej znajduje się więcej na temat tych opcji:</span><span class="sxs-lookup"><span data-stu-id="51a94-126">The following provides more detail about these options:</span></span>

<span data-ttu-id="51a94-127">**Woluminy** są mapowane z systemem operacyjnym hosta do katalogów w kontenerach katalogów.</span><span class="sxs-lookup"><span data-stu-id="51a94-127">**Volumes** are directories mapped from the host OS to directories in containers.</span></span> <span data-ttu-id="51a94-128">Gdy kod w kontenerze ma dostęp do katalogu, dostęp jest faktycznie do katalogu na hoście systemu operacyjnego.</span><span class="sxs-lookup"><span data-stu-id="51a94-128">When code in the container has access to the directory, that access is actually to a directory on the host OS.</span></span> <span data-ttu-id="51a94-129">Ten katalog nie jest związany z okresem istnienia samego kontenera, a katalog jest zarządzane przez platformę Docker i odizolowanych od podstawowych funkcji maszyny hosta.</span><span class="sxs-lookup"><span data-stu-id="51a94-129">This directory is not tied to the lifetime of the container itself, and the directory is managed by Docker and isolated from the core functionality of the host machine.</span></span> <span data-ttu-id="51a94-130">W efekcie woluminów danych są przeznaczone do utrwalenia danych niezależnie od cyklu życia kontenera.</span><span class="sxs-lookup"><span data-stu-id="51a94-130">Thus, data volumes are designed to persist data independently of the life of the container.</span></span> <span data-ttu-id="51a94-131">Czy usunąć kontener obrazu z hosta platformy Docker, dane utrwalone w ilości danych nie jest usuwany.</span><span class="sxs-lookup"><span data-stu-id="51a94-131">If you delete a container or an image from the Docker host, the data persisted in the data volume isn't deleted.</span></span>

<span data-ttu-id="51a94-132">Woluminy mogą być nazwane i anonimowe (ustawienie domyślne).</span><span class="sxs-lookup"><span data-stu-id="51a94-132">Volumes can be named or anonymous (the default).</span></span> <span data-ttu-id="51a94-133">Nazwane woluminy są dostępne w rozwój **kontenery woluminów danych** , co ułatwia udostępnianie danych między kontenerów.</span><span class="sxs-lookup"><span data-stu-id="51a94-133">Named volumes are the evolution of **Data Volume Containers** and make it easy to share data between containers.</span></span> <span data-ttu-id="51a94-134">Woluminy obsługują także sterowników woluminów, które umożliwiają przechowywanie danych na zdalnym hosty, między innymi opcjami.</span><span class="sxs-lookup"><span data-stu-id="51a94-134">Volumes also support volume drivers, that allow you to store data on remote hosts, among other options.</span></span>

<span data-ttu-id="51a94-135">**Powiąż instaluje** są dostępne od dawno temu i pozostawić mapowanie dowolnego folderu w punkcie instalacji w kontenerze.</span><span class="sxs-lookup"><span data-stu-id="51a94-135">**Bind mounts** are available since a long time ago and allow the mapping of any folder to a mount point in a container.</span></span> <span data-ttu-id="51a94-136">Instaluje powiązania ma więcej ograniczeń niż woluminy i niektóre problemy dotyczące zabezpieczeń istotne, aby woluminy są to zalecana opcja.</span><span class="sxs-lookup"><span data-stu-id="51a94-136">Bind mounts have more limitations than volumes and some important security issues, so volumes are the recommended option.</span></span>

<span data-ttu-id="51a94-137">**Instaluje tmpfs** są zasadniczo wirtualnych folderów, które znajdują się tylko w pamięci hosta i nigdy nie są zapisywane w systemie plików.</span><span class="sxs-lookup"><span data-stu-id="51a94-137">**tmpfs mounts** are basically virtual folders that live only in the host's memory and are never written to the filesystem.</span></span> <span data-ttu-id="51a94-138">Są one szybki i bezpieczny, ale użycia pamięci i są przeznaczone tylko dla danych nietrwałe.</span><span class="sxs-lookup"><span data-stu-id="51a94-138">They are fast and secure but use memory and are only meant for non-persistent data.</span></span>

<span data-ttu-id="51a94-139">Jak pokazano na rysunku 4-5, regularne woluminów Docker mogą być przechowywane na zewnątrz kontenerów, samodzielnie, ale w granicach fizycznego serwera hosta lub maszyny Wirtualnej.</span><span class="sxs-lookup"><span data-stu-id="51a94-139">As shown in Figure 4-5, regular Docker volumes can be stored outside of the containers themselves but within the physical boundaries of the host server or VM.</span></span> <span data-ttu-id="51a94-140">Jednak kontenerów platformy Docker nie może uzyskać dostęp do woluminu z jednego hosta serwera lub maszyny Wirtualnej do innego.</span><span class="sxs-lookup"><span data-stu-id="51a94-140">However, Docker containers can't access a volume from one host server or VM to another.</span></span> <span data-ttu-id="51a94-141">Innymi słowy za pomocą tych woluminów nie jest możliwe do zarządzania danymi udostępniane między kontenerów, które można uruchomić na różnych hostach Docker, mimo że można osiągnąć za pomocą sterownika woluminu, który obsługuje hostów zdalnych.</span><span class="sxs-lookup"><span data-stu-id="51a94-141">In other words, with these volumes, it isn't possible to manage data shared between containers that run on different Docker hosts, although it could be achieved with a volume driver that supports remote hosts.</span></span>

![<span data-ttu-id="51a94-142">Woluminy mogą być współużytkowane między kontenerów, ale tylko w tym samym hoście, chyba że w przypadku używania zdalnego sterownika, która obsługuje hostów zdalnych.</span><span class="sxs-lookup"><span data-stu-id="51a94-142">Volumes can be shared between containers, but only in the same host, unless you use a remote driver that supports remote hosts.</span></span> ](./media/image5.png)

<span data-ttu-id="51a94-143">**Rysunek 4-5**.</span><span class="sxs-lookup"><span data-stu-id="51a94-143">**Figure 4-5**.</span></span> <span data-ttu-id="51a94-144">Woluminy i zewnętrznych źródeł danych dla aplikacji opartych na kontenerach</span><span class="sxs-lookup"><span data-stu-id="51a94-144">Volumes and external data sources for container-based applications</span></span>

<span data-ttu-id="51a94-145">Ponadto gdy kontenerów platformy Docker są zarządzane przez program orchestrator, kontenerów może "Przenieś" między hostami w zależności od optymalizacje wykonywane przez klaster.</span><span class="sxs-lookup"><span data-stu-id="51a94-145">In addition, when Docker containers are managed by an orchestrator, containers might "move" between hosts, depending on the optimizations performed by the cluster.</span></span> <span data-ttu-id="51a94-146">W związku z tym nie jest zalecane, użyć woluminów danych dla danych biznesowych.</span><span class="sxs-lookup"><span data-stu-id="51a94-146">Therefore, it isn't recommended that you use data volumes for business data.</span></span> <span data-ttu-id="51a94-147">Ale są one dobrym mechanizm do pracy z plikami śledzenia, pliki danych czasowych lub podobne, nie ma wpływu na spójność danych biznesowych.</span><span class="sxs-lookup"><span data-stu-id="51a94-147">But they're a good mechanism to work with trace files, temporal files, or similar that will not impact business data consistency.</span></span>

<span data-ttu-id="51a94-148">**Zdalnych źródeł danych i pamięć podręczna** aplikacji kontenerowych nimi narzędzi, takich jak Azure SQL Database, Azure Cosmos DB lub zdalnego pamięci podręcznej, takich jak Redis można używać w taki sam sposób, są one używane podczas tworzenia bez kontenerów.</span><span class="sxs-lookup"><span data-stu-id="51a94-148">**Remote data sources and cache** tools like Azure SQL Database, Azure Cosmos DB, or a remote cache like Redis can be used in containerized applications the same way they are used when developing without containers.</span></span> <span data-ttu-id="51a94-149">To jest sprawdzonym sposobem przechowywania danych aplikacji biznesowych.</span><span class="sxs-lookup"><span data-stu-id="51a94-149">This is a proven way to store business application data.</span></span>

<span data-ttu-id="51a94-150">**Usługa Azure Storage.**</span><span class="sxs-lookup"><span data-stu-id="51a94-150">**Azure Storage.**</span></span> <span data-ttu-id="51a94-151">Dane biznesowe zwykle należy można umieścić w zasoby zewnętrzne lub baz danych, takich jak Azure Storage.</span><span class="sxs-lookup"><span data-stu-id="51a94-151">Business data usually will need to be placed in external resources or databases, like Azure Storage.</span></span> <span data-ttu-id="51a94-152">Usługa Azure Storage w konkretny, udostępnia następujące usługi w chmurze:</span><span class="sxs-lookup"><span data-stu-id="51a94-152">Azure Storage, in concrete, provides the following services in the cloud:</span></span>

- <span data-ttu-id="51a94-153">Magazyn obiektów blob przechowuje dane obiektów bez struktury.</span><span class="sxs-lookup"><span data-stu-id="51a94-153">Blob storage stores unstructured object data.</span></span> <span data-ttu-id="51a94-154">Obiekt blob może być dowolnego typu dane tekstowe lub binarne, takie jak dokument lub nośnik pliki (obrazy, audio i wideo).</span><span class="sxs-lookup"><span data-stu-id="51a94-154">A blob can be any type of text or binary data, such as document or media files (images, audio, and video files).</span></span> <span data-ttu-id="51a94-155">Magazyn obiektów blob jest również określany jako magazyn obiektów.</span><span class="sxs-lookup"><span data-stu-id="51a94-155">Blob storage is also referred to as Object storage.</span></span>

- <span data-ttu-id="51a94-156">Usługa File storage udostępnia współużytkowany magazyn dla starszych aplikacji używających standardowego protokołu SMB.</span><span class="sxs-lookup"><span data-stu-id="51a94-156">File storage offers shared storage for legacy applications using standard SMB protocol.</span></span> <span data-ttu-id="51a94-157">Usługa Azure virtual machines i usług w chmurze mogą współużytkować dane plików między składnikami aplikacji za pośrednictwem zainstalowanych udziałów.</span><span class="sxs-lookup"><span data-stu-id="51a94-157">Azure virtual machines and cloud services can share file data across application components via mounted shares.</span></span> <span data-ttu-id="51a94-158">Aplikacje lokalne mogą uzyskać dostęp do danych plików w udziale za pośrednictwem interfejsu API REST usługi plików.</span><span class="sxs-lookup"><span data-stu-id="51a94-158">On-premises applications can access file data in a share via the File service REST API.</span></span>

- <span data-ttu-id="51a94-159">Usługa TABLE storage przechowuje zestawy danych ze strukturą.</span><span class="sxs-lookup"><span data-stu-id="51a94-159">Table storage stores structured datasets.</span></span> <span data-ttu-id="51a94-160">Magazyn tabel jest atrybutem klucza magazynu NoSQL, która umożliwia szybkie opracowywanie i szybki dostęp do dużych ilości danych.</span><span class="sxs-lookup"><span data-stu-id="51a94-160">Table storage is a NoSQL key-attribute data store, which allows rapid development and fast access to large quantities of data.</span></span>

<span data-ttu-id="51a94-161">**Relacyjne bazy danych i bazy danych NoSQL.**</span><span class="sxs-lookup"><span data-stu-id="51a94-161">**Relational databases and NoSQL databases.**</span></span> <span data-ttu-id="51a94-162">Istnieje wiele opcji dla zewnętrznych baz danych z relacyjnej bazy danych, takich jak SQL Server, PostgreSQL, Oracle lub NoSQL bazy danych, takich jak usługi Azure Cosmos DB, MongoDB itp. Te bazy danych nie będą wyjaśnione w ramach tego przewodnika, ponieważ są one w całkowicie różnych tematów.</span><span class="sxs-lookup"><span data-stu-id="51a94-162">There are many choices for external databases, from relational databases like SQL Server, PostgreSQL, Oracle, or NoSQL databases like Azure Cosmos DB, MongoDB, etc. These databases are not going to be explained as part of this guide since they are in a completely different subject.</span></span>

>[!div class="step-by-step"]
><span data-ttu-id="51a94-163">[Poprzednie](containerize-monolithic-applications.md)
>[dalej](service-oriented-architecture.md)</span><span class="sxs-lookup"><span data-stu-id="51a94-163">[Previous](containerize-monolithic-applications.md)
[Next](service-oriented-architecture.md)</span></span>