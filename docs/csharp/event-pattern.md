---
title: Standardowe wzorce zdarzenia platformy .NET
description: Więcej informacji na temat wzorców zdarzeń .NET i tworzenie źródła zdarzeń w wersji standard i subskrypcji i przetworzyć standardowych zdarzeń w kodzie.
ms.date: 06/20/2016
ms.assetid: 8a3133d6-4ef2-46f9-9c8d-a8ea8898e4c9
ms.openlocfilehash: 9bd9f71726647966dd1e4426b260484decb048c6
ms.sourcegitcommit: d955cb4c681d68cf301d410925d83f25172ece86
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/07/2018
ms.locfileid: "34827251"
---
# <a name="standard-net-event-patterns"></a><span data-ttu-id="ab55e-103">Standardowe wzorce zdarzenia platformy .NET</span><span class="sxs-lookup"><span data-stu-id="ab55e-103">Standard .NET event patterns</span></span>

[<span data-ttu-id="ab55e-104">Poprzednie</span><span class="sxs-lookup"><span data-stu-id="ab55e-104">Previous</span></span>](events-overview.md)

<span data-ttu-id="ab55e-105">Zdarzenia platformy .NET zazwyczaj należy wykonać kilka znane wzorce.</span><span class="sxs-lookup"><span data-stu-id="ab55e-105">.NET events generally follow a few known patterns.</span></span> <span data-ttu-id="ab55e-106">Standaryzacja te wzorce oznacza, że deweloperzy mogą korzystać z wiedzę na temat tych standardowe wzorce, które mogą być stosowane do dowolnego programu zdarzenia platformy .NET.</span><span class="sxs-lookup"><span data-stu-id="ab55e-106">Standardizing on these patterns means that developers can leverage knowledge of those standard patterns, which can be applied to any .NET event program.</span></span>

<span data-ttu-id="ab55e-107">Przejdźmy przy użyciu tych wzorców standardowe, konieczne będzie wiedzy, które należy utworzyć źródła zdarzeń w wersji standard i subskrybowanie i przetworzyć standardowych zdarzeń w kodzie.</span><span class="sxs-lookup"><span data-stu-id="ab55e-107">Let's go through these standard patterns so you will have all the knowledge you need to create standard event sources, and subscribe and process standard events in your code.</span></span>

## <a name="event-delegate-signatures"></a><span data-ttu-id="ab55e-108">Podpisy delegata zdarzenia</span><span class="sxs-lookup"><span data-stu-id="ab55e-108">Event Delegate Signatures</span></span>

<span data-ttu-id="ab55e-109">Standardowa podpis dla delegata zdarzenia .NET jest:</span><span class="sxs-lookup"><span data-stu-id="ab55e-109">The standard signature for a .NET event delegate is:</span></span>

```csharp
void OnEventRaised(object sender, EventArgs args);
```

<span data-ttu-id="ab55e-110">Typ zwracany jest typ void.</span><span class="sxs-lookup"><span data-stu-id="ab55e-110">The return type is void.</span></span> <span data-ttu-id="ab55e-111">Zdarzenia są oparte na delegatów i są obiekty delegowane multiemisji.</span><span class="sxs-lookup"><span data-stu-id="ab55e-111">Events are based on delegates and are multicast delegates.</span></span> <span data-ttu-id="ab55e-112">Obsługujący wiele subskrybentów dla dowolnego źródła zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="ab55e-112">That supports multiple subscribers for any event source.</span></span> <span data-ttu-id="ab55e-113">Pojedynczy wartość zwracana z metody nie skalować do wielu subskrybentów zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="ab55e-113">The single return value from a method doesn't scale to multiple event subscribers.</span></span> <span data-ttu-id="ab55e-114">Które zwracają wartość oznacza Zobacz źródło zdarzeń po wywołaniem zdarzenia?</span><span class="sxs-lookup"><span data-stu-id="ab55e-114">Which return value does the event source see after raising an event?</span></span> <span data-ttu-id="ab55e-115">W dalszej części tego artykułu zobaczysz sposobu tworzenia zdarzeń protokołów, które obsługują subskrybentów zdarzeń informacji raportu ze źródłem zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="ab55e-115">Later in this article you'll see how to create event protocols that support event subscribers that report information to the event source.</span></span>

<span data-ttu-id="ab55e-116">Lista argumentów zawiera dwa argumenty: nadawcy i argumenty zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="ab55e-116">The argument list contains two arguments: the sender, and the event arguments.</span></span> <span data-ttu-id="ab55e-117">Typ czasu kompilacji `sender` jest `System.Object`, nawet jeśli wiesz, prawdopodobnie bardziej pochodny typ, który zawsze są poprawne.</span><span class="sxs-lookup"><span data-stu-id="ab55e-117">The compile time type of `sender` is `System.Object`, even though you likely know a more derived type that would always be correct.</span></span> <span data-ttu-id="ab55e-118">Konwencja, użyj `object`.</span><span class="sxs-lookup"><span data-stu-id="ab55e-118">By convention, use `object`.</span></span>

<span data-ttu-id="ab55e-119">Drugi argument zwykle został typ, który pochodzi od `System.EventArgs`.</span><span class="sxs-lookup"><span data-stu-id="ab55e-119">The second argument has typically been a type that is derived from `System.EventArgs`.</span></span> <span data-ttu-id="ab55e-120">(Będzie [następnej sekcji](modern-events.md) która Konwencja przestaną być wymuszane.) Jeśli danego typu zdarzenia nie wymaga żadnych dodatkowych argumentów, nadal zapewni oba argumenty.</span><span class="sxs-lookup"><span data-stu-id="ab55e-120">(You'll see in the [next section](modern-events.md) that this convention is no longer enforced.) If your event type does not need any additional arguments, you will still provide both arguments.</span></span>
<span data-ttu-id="ab55e-121">Jest wartością specjalną `EventArgs.Empty` należy używać do określenia, że zdarzenie nie zawiera żadnych dodatkowych informacji.</span><span class="sxs-lookup"><span data-stu-id="ab55e-121">There is a special value, `EventArgs.Empty` that you should use to denote that your event does not contain any additional information.</span></span>

<span data-ttu-id="ab55e-122">Utworzymy klasę, która zawiera listę plików w katalogu lub jego podkatalogach przestrzeganie wzorca.</span><span class="sxs-lookup"><span data-stu-id="ab55e-122">Let's build a class that lists files in a directory, or any of its subdirectories that follow a pattern.</span></span> <span data-ttu-id="ab55e-123">Ten składnik wywołuje zdarzenie, dla każdego pliku znaleźć odpowiadającego wzorzec.</span><span class="sxs-lookup"><span data-stu-id="ab55e-123">This component raises an event for each file found that matches the pattern.</span></span>

<span data-ttu-id="ab55e-124">Przy użyciu modelu zdarzeń zawiera niektóre zalety projektu.</span><span class="sxs-lookup"><span data-stu-id="ab55e-124">Using an event model provides some design advantages.</span></span> <span data-ttu-id="ab55e-125">Możesz utworzyć wiele odbiorników zdarzeń, wykonujących różne akcje po znalezieniu używanych plików.</span><span class="sxs-lookup"><span data-stu-id="ab55e-125">You can create multiple event listeners that perform different actions when a sought file is found.</span></span> <span data-ttu-id="ab55e-126">Łączenie różnych odbiorników można tworzyć bardziej niezawodne algorytmów.</span><span class="sxs-lookup"><span data-stu-id="ab55e-126">Combining the different listeners can create more robust algorithms.</span></span>

<span data-ttu-id="ab55e-127">Oto deklaracji argument początkowej zdarzenia do znajdowania używanych plików:</span><span class="sxs-lookup"><span data-stu-id="ab55e-127">Here is the initial event argument declaration for finding a sought file:</span></span> 

[!code-csharp[EventArgs](../../samples/csharp/events/Program.cs#EventArgsV1 "Define event arguments")]

<span data-ttu-id="ab55e-128">Mimo że ten typ wygląda jak typ małe, tylko dane, postępuj zgodnie z Konwencją i odnieść (`class`) typu.</span><span class="sxs-lookup"><span data-stu-id="ab55e-128">Even though this type looks like a small, data-only type, you should follow the convention and make it a reference (`class`) type.</span></span> <span data-ttu-id="ab55e-129">Oznacza to obiekt argument zostanie przekazany przez odwołanie, a wszelkie zmiany danych ma być wyświetlana przez wszystkich subskrybentów.</span><span class="sxs-lookup"><span data-stu-id="ab55e-129">That means the argument object will be passed by reference, and any updates to the data will be viewed by all subscribers.</span></span> <span data-ttu-id="ab55e-130">Pierwsza wersja jest niezmienialny obiektu.</span><span class="sxs-lookup"><span data-stu-id="ab55e-130">The first version is an immutable object.</span></span> <span data-ttu-id="ab55e-131">Należy wolisz właściwości były czcionką argument zdarzenia niezmienialny.</span><span class="sxs-lookup"><span data-stu-id="ab55e-131">You should prefer to make the properties in your event argument type immutable.</span></span> <span data-ttu-id="ab55e-132">Dzięki temu jeden subskrybent nie można zmienić wartości, przed subskrybenta innego będzie je widział.</span><span class="sxs-lookup"><span data-stu-id="ab55e-132">That way, one subscriber cannot change the values before another subscriber sees them.</span></span> <span data-ttu-id="ab55e-133">(Istnieją wyjątki od tej reguły, jak można zauważyć poniżej.)</span><span class="sxs-lookup"><span data-stu-id="ab55e-133">(There are exceptions to this, as you'll see below.)</span></span>  

<span data-ttu-id="ab55e-134">Następnie należy utworzyć deklaracji zdarzenia w klasie FileSearcher.</span><span class="sxs-lookup"><span data-stu-id="ab55e-134">Next, we need to create the event declaration in the FileSearcher class.</span></span> <span data-ttu-id="ab55e-135">Wykorzystanie `EventHandler<T>` typ oznacza, że nie trzeba tworzyć jeszcze innej definicji typu.</span><span class="sxs-lookup"><span data-stu-id="ab55e-135">Leveraging the `EventHandler<T>` type means that you don't need to create yet another type definition.</span></span> <span data-ttu-id="ab55e-136">Po prostu użyć specjalizacji ogólnego.</span><span class="sxs-lookup"><span data-stu-id="ab55e-136">You simply use a generic specialization.</span></span>

<span data-ttu-id="ab55e-137">Umożliwia wypełnianie klasy FileSearcher do wyszukiwania plików zgodnych ze wzorcem i wywołaj zdarzenie prawidłowe, gdy zostanie wykryta dopasowania.</span><span class="sxs-lookup"><span data-stu-id="ab55e-137">Let's fill out the FileSearcher class to search for files that match a pattern, and raise the correct event when a match is discovered.</span></span>

[!code-csharp[FileSearxcher](../../samples/csharp/events/Program.cs#FileSearcherV1 "Create the initial file searcher")]

## <a name="definining-and-raising-field-like-events"></a><span data-ttu-id="ab55e-138">Definining i wywoływanie zdarzeń podobnych pola</span><span class="sxs-lookup"><span data-stu-id="ab55e-138">Definining and Raising Field-Like Events</span></span>

<span data-ttu-id="ab55e-139">Najprostszym sposobem, aby dodać zdarzenie do własnej klasy jest można zadeklarować tego zdarzenia jako pole publiczne, tak jak w poprzednim przykładzie:</span><span class="sxs-lookup"><span data-stu-id="ab55e-139">The simplest way to add an event to your class is to declare that event as a public field, as in the preceding example:</span></span>

[!code-csharp[DeclareEvent](../../samples/csharp/events/Program.cs#DeclareEvent "Declare the file found event")]

<span data-ttu-id="ab55e-140">To prawdopodobnie jest deklarowanie publiczne pola, które wydają się być zły zorientowane obiektowo rozwiązaniem.</span><span class="sxs-lookup"><span data-stu-id="ab55e-140">This looks like it's declaring a public field, which would appear to be bad object-oriented practice.</span></span> <span data-ttu-id="ab55e-141">Chcesz chronić dostęp do danych za pośrednictwem właściwości lub metody.</span><span class="sxs-lookup"><span data-stu-id="ab55e-141">You want to protect data access through properties, or methods.</span></span> <span data-ttu-id="ab55e-142">Gdy to, że wygląda jak rozwiązaniem zły kod wygenerowany przez kompilator tworzenie otok tak, aby obiekty zdarzeń można uzyskać tylko w sposób bezpieczne.</span><span class="sxs-lookup"><span data-stu-id="ab55e-142">While this make look like a bad practice, the code generated by the compiler does create wrappers so that the event objects can only be accessed in safe ways.</span></span> <span data-ttu-id="ab55e-143">Tylko operacje dostępne dla zdarzeń podobnych pola są Dodaj program obsługi:</span><span class="sxs-lookup"><span data-stu-id="ab55e-143">The only operations available on a field-like event are add handler:</span></span>

[!code-csharp[DeclareEventHandler](../../samples/csharp/events/Program.cs#DeclareEventHandler "Declare the file found event handler")]

<span data-ttu-id="ab55e-144">i usunąć program obsługi:</span><span class="sxs-lookup"><span data-stu-id="ab55e-144">and remove handler:</span></span>

[!code-csharp[RemoveEventHandler](../../samples/csharp/events/Program.cs#RemoveHandler "Remove the event handler")]

<span data-ttu-id="ab55e-145">Należy pamiętać, że zmienna lokalna dla programu obsługi.</span><span class="sxs-lookup"><span data-stu-id="ab55e-145">Note that there's a local variable for the handler.</span></span> <span data-ttu-id="ab55e-146">Jeśli używasz treści Wyrażenie lambda, Usuń nie będzie działać poprawnie.</span><span class="sxs-lookup"><span data-stu-id="ab55e-146">If you used the body of the lambda, the remove would not work correctly.</span></span> <span data-ttu-id="ab55e-147">Może być inne wystąpienie delegata i dyskretnej nic nie rób.</span><span class="sxs-lookup"><span data-stu-id="ab55e-147">It would be a different instance of the delegate, and silently do nothing.</span></span>

<span data-ttu-id="ab55e-148">Kod poza klasy nie mogą wywoływać zdarzeń ani nie można wykonać innych operacji.</span><span class="sxs-lookup"><span data-stu-id="ab55e-148">Code outside the class cannot raise the event, nor can it perform any other operations.</span></span>

## <a name="returning-values-from-event-subscribers"></a><span data-ttu-id="ab55e-149">Zwracanie wartości z subskrybentów zdarzeń</span><span class="sxs-lookup"><span data-stu-id="ab55e-149">Returning Values from Event Subscribers</span></span>

<span data-ttu-id="ab55e-150">Proste wersji działa prawidłowo.</span><span class="sxs-lookup"><span data-stu-id="ab55e-150">Your simple version is working fine.</span></span> <span data-ttu-id="ab55e-151">Dodajmy inna funkcja: anulowania.</span><span class="sxs-lookup"><span data-stu-id="ab55e-151">Let's add another feature: Cancellation.</span></span>

<span data-ttu-id="ab55e-152">Po podniesieniu zdarzenia znaleziono odbiorników powinno być możliwe zatrzymać dalsze przetwarzanie, jeśli ten plik jest, że ostatni poszukiwania.</span><span class="sxs-lookup"><span data-stu-id="ab55e-152">When you raise the found event, listeners should be able to stop further processing, if this file is that last one sought.</span></span>

<span data-ttu-id="ab55e-153">Obsługi zdarzeń nie zwraca wartości, więc należy do komunikowania się, że w inny sposób.</span><span class="sxs-lookup"><span data-stu-id="ab55e-153">The event handlers do not return a value, so you need to communicate that in another way.</span></span> <span data-ttu-id="ab55e-154">Wzorzec zdarzeń w wersji standard używa obiektu EventArgs zawierać pól, które subskrybenci zdarzeń mogą używać do komunikowania się Anuluj.</span><span class="sxs-lookup"><span data-stu-id="ab55e-154">The standard event pattern uses the EventArgs object to include fields that event subscribers can use to communicate cancel.</span></span>

<span data-ttu-id="ab55e-155">Istnieją dwa różne wzorce, które mogłyby zostać użyte, oparte na semantykę kontraktu Anuluj.</span><span class="sxs-lookup"><span data-stu-id="ab55e-155">There are two different patterns that could be used, based on the semantics of the Cancel contract.</span></span> <span data-ttu-id="ab55e-156">W obu przypadkach warto polem EventArguments zdarzenia znaleziony plik.</span><span class="sxs-lookup"><span data-stu-id="ab55e-156">In both cases, you'll add a boolean field to the EventArguments for the found file event.</span></span> 

<span data-ttu-id="ab55e-157">Wzorzec co pozwoliłoby żadnych jednego abonenta anulować operację.</span><span class="sxs-lookup"><span data-stu-id="ab55e-157">One pattern would allow any one subscriber to cancel the operation.</span></span>
<span data-ttu-id="ab55e-158">Dla tego wzorca nowego pola jest ustawiana na `false`.</span><span class="sxs-lookup"><span data-stu-id="ab55e-158">For this pattern, the new field is initialized to `false`.</span></span> <span data-ttu-id="ab55e-159">Wszelkie subskrybenta można go zmienić `true`.</span><span class="sxs-lookup"><span data-stu-id="ab55e-159">Any subscriber can change it to `true`.</span></span> <span data-ttu-id="ab55e-160">Po wszystkich subskrybentów jak już wspomniano Zdarzenie wywoływane, składnik FileSearcher sprawdza, czy wartość logiczną i podejmuje działania.</span><span class="sxs-lookup"><span data-stu-id="ab55e-160">After all subscribers have seen the event raised, the FileSearcher component examines the boolean value and takes action.</span></span>

<span data-ttu-id="ab55e-161">Drugi wzorzec tylko czy anulować operację, jeśli operacja została anulowana wszystkich subskrybentów.</span><span class="sxs-lookup"><span data-stu-id="ab55e-161">The second pattern would only cancel the operation if all subscribers wanted the operation cancelled.</span></span> <span data-ttu-id="ab55e-162">W tym wzorcu nowego pola jest ustawiana na wskazują, należy anulować operacji oraz wszystkie subskrybent może zmienić wskazująca, że należy kontynuować operacji.</span><span class="sxs-lookup"><span data-stu-id="ab55e-162">In this pattern, the new field is initialized to indicate the operation should cancel, and any subscriber could change it to indicate the operation should continue.</span></span>
<span data-ttu-id="ab55e-163">Po wszystkich subskrybentów jak już wspomniano Zdarzenie wywoływane, składnik FileSearcher sprawdza typu boolean i podejmuje działania.</span><span class="sxs-lookup"><span data-stu-id="ab55e-163">After all subscribers have seen the event raised, the FileSearcher component examines the boolean and takes action.</span></span> <span data-ttu-id="ab55e-164">Brak jednego dodatkowego kroku w tym wzorcu: składnik musi wiedzieć, jeśli zdarzenie przejrzane żadnych subskrybentów.</span><span class="sxs-lookup"><span data-stu-id="ab55e-164">There is one extra step in this pattern: the component needs to know if any subscribers have seen the event.</span></span> <span data-ttu-id="ab55e-165">Jeśli nie ma żadnych subskrybentów, pole wskazuje cancel niepoprawnie.</span><span class="sxs-lookup"><span data-stu-id="ab55e-165">If there are no subscribers, the field would indicate a cancel incorrectly.</span></span>

<span data-ttu-id="ab55e-166">Umożliwia wdrożenie pierwszej wersji dla tego przykładu.</span><span class="sxs-lookup"><span data-stu-id="ab55e-166">Let's implement the first version for this sample.</span></span> <span data-ttu-id="ab55e-167">Konieczne jest dodanie polem o nazwie `CancelRequested` do `FileFoundArgs` typu:</span><span class="sxs-lookup"><span data-stu-id="ab55e-167">You need to add a boolean field named `CancelRequested` to the `FileFoundArgs` type:</span></span>

[!code-csharp[EventArgs](../../samples/csharp/events/Program.cs#EventArgs "Update event arguments")]

<span data-ttu-id="ab55e-168">To nowe pole jest automatycznie ustawiana na `false`, wartością domyślną dla polem, więc nie można anulować przypadkowo.</span><span class="sxs-lookup"><span data-stu-id="ab55e-168">This new Field is automatically initialized to `false`, the default value for a Boolean field, so you don't cancel accidentally.</span></span> <span data-ttu-id="ab55e-169">Jedyną inną zmianą do składnika jest sprawdzanie flagę po wywoływanie zdarzeń, aby zobaczyć, jeśli dowolne subskrybentów żądanych anulowania:</span><span class="sxs-lookup"><span data-stu-id="ab55e-169">The only other change to the component is to check the flag after raising the event to see if any of the subscribers have requested a cancellation:</span></span>

```csharp
public void List(string directory, string searchPattern)
{
    foreach (var file in Directory.EnumerateFiles(directory, searchPattern))
    {
        var args = new FileFoundArgs(file);
        FileFound?.Invoke(this, args);
        if (args.CancelRequested)
            break;
    }
}
```

<span data-ttu-id="ab55e-170">Jedną z zalet tego wzorca jest, że nie jest istotne zmiany.</span><span class="sxs-lookup"><span data-stu-id="ab55e-170">One advantage of this pattern is that it isn't a breaking change.</span></span>
<span data-ttu-id="ab55e-171">Brak subskrybentów żądanie anulowania przed i nadal nie są one.</span><span class="sxs-lookup"><span data-stu-id="ab55e-171">None of the subscribers requested a cancellation before, and they still are not.</span></span> <span data-ttu-id="ab55e-172">Żaden kod subskrybenta nie musi aktualizacji, chyba że chcą, aby obsługiwać nowy protokół Anuluj.</span><span class="sxs-lookup"><span data-stu-id="ab55e-172">None of the subscriber code needs updating unless they want to support the new cancel protocol.</span></span> <span data-ttu-id="ab55e-173">Bardzo luźno jest powiązane.</span><span class="sxs-lookup"><span data-stu-id="ab55e-173">It's very loosely coupled.</span></span>

<span data-ttu-id="ab55e-174">Teraz należy zaktualizować subskrybenta, tak aby żądania anulowania po znalezieniu pierwszy plik wykonywalny:</span><span class="sxs-lookup"><span data-stu-id="ab55e-174">Let's update the subscriber so that it requests a cancellation once it finds the first executable:</span></span>

```csharp
EventHandler<FileFoundArgs> onFileFound = (sender, eventArgs) =>
{
    Console.WriteLine(eventArgs.FoundFile);
    eventArgs.CancelRequested = true;
};
```

## <a name="adding-another-event-declaration"></a><span data-ttu-id="ab55e-175">Dodawanie innego deklaracja zdarzenia</span><span class="sxs-lookup"><span data-stu-id="ab55e-175">Adding Another Event Declaration</span></span>

<span data-ttu-id="ab55e-176">Teraz Dodaj jedną funkcję więcej i Wykaż innych idioms języka dla zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="ab55e-176">Let's add one more feature, and demonstrate other language idioms for events.</span></span> <span data-ttu-id="ab55e-177">Dodajmy przeciążenia `Search()` metody, który przechodzi przez wszystkie podkatalogi w poszukiwaniu plików.</span><span class="sxs-lookup"><span data-stu-id="ab55e-177">Let's add an overload of the `Search()` method that traverses all subdirectories in search of files.</span></span>

<span data-ttu-id="ab55e-178">Ten można pobrać jako długotrwałej operacji w katalogu za dużo podkatalogów.</span><span class="sxs-lookup"><span data-stu-id="ab55e-178">This could get to be a lengthy operation in a directory with many sub-directories.</span></span> <span data-ttu-id="ab55e-179">Dodajmy zdarzenie, które pobiera wywoływane, gdy rozpoczyna się każdego nowego wyszukiwania w katalogu.</span><span class="sxs-lookup"><span data-stu-id="ab55e-179">Let's add an event that gets raised when each new directory search begins.</span></span> <span data-ttu-id="ab55e-180">Dzięki temu subskrybentów śledzić postęp i zaktualizować użytkownika dotyczące postępu.</span><span class="sxs-lookup"><span data-stu-id="ab55e-180">This enables subscribers to track progress, and update the user as to progress.</span></span> <span data-ttu-id="ab55e-181">Wszystkie przykłady dotąd utworzony są publiczne.</span><span class="sxs-lookup"><span data-stu-id="ab55e-181">All the samples you've created so far are public.</span></span> <span data-ttu-id="ab55e-182">Upewnijmy tego Zdarzenie wewnętrzne.</span><span class="sxs-lookup"><span data-stu-id="ab55e-182">Let's make this one an internal event.</span></span> <span data-ttu-id="ab55e-183">Oznacza to, że należy również wybrać typy używane dla argumentów wewnętrzny również.</span><span class="sxs-lookup"><span data-stu-id="ab55e-183">That means you can also make the types used for the arguments internal as well.</span></span>

<span data-ttu-id="ab55e-184">Będzie rozpoczyna się od utworzenia nowej klasy EventArgs pochodnych dla raportowania nowy katalog i postępu.</span><span class="sxs-lookup"><span data-stu-id="ab55e-184">You'll start by creating the new EventArgs derived class for reporting the new directory and progress.</span></span> 

[!code-csharp[DirEventArgs](../../samples/csharp/events/Program.cs#SearchDirEventArgs "Define search directory event arguments")]

<span data-ttu-id="ab55e-185">Ponownie mogą postępować zgodnie z zaleceniami aby typu niezmienne odwołania dla argumentów zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="ab55e-185">Again, you can follow the recommendations to make an immutable reference type for the event arguments.</span></span>

<span data-ttu-id="ab55e-186">Następnie określ zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="ab55e-186">Next, define the event.</span></span> <span data-ttu-id="ab55e-187">Tym razem użyjesz innej składni.</span><span class="sxs-lookup"><span data-stu-id="ab55e-187">This time, you'll use a different syntax.</span></span> <span data-ttu-id="ab55e-188">Oprócz przy użyciu składni pola, można jawnie utworzyć właściwości, z Dodaj i usuń programy obsługi.</span><span class="sxs-lookup"><span data-stu-id="ab55e-188">In addition to using the field syntax, you can explicitly create the property, with add and remove handlers.</span></span> <span data-ttu-id="ab55e-189">W tym przykładzie nie wymaga dodatkowego kodu w tych programów obsługi, ale oznacza to, jak należy je utworzyć.</span><span class="sxs-lookup"><span data-stu-id="ab55e-189">In this sample, you won't need extra code in those handlers, but this shows how you would create them.</span></span>

[!code-csharp[Declare event with add and remove handlers](../../samples/csharp/events/Program.cs#DeclareSearchEvent "Declare the event with add and remove handlers")]

<span data-ttu-id="ab55e-190">Na wiele sposobów kod, który można zapisać tutaj duplikatów kodu kompilator generuje definicje zdarzeń pola przedstawiono wcześniej.</span><span class="sxs-lookup"><span data-stu-id="ab55e-190">In many ways, the code you write here mirrors the code the compiler generates for the field event definitions you've seen earlier.</span></span> <span data-ttu-id="ab55e-191">Utwórz zdarzenie przy użyciu składni bardzo podobne do celów [właściwości](properties.md).</span><span class="sxs-lookup"><span data-stu-id="ab55e-191">You create the event using syntax very similar to that used for [properties](properties.md).</span></span> <span data-ttu-id="ab55e-192">Powiadomienie, że obsługi mają różne nazwy elementu: `add` i `remove`.</span><span class="sxs-lookup"><span data-stu-id="ab55e-192">Notice that the handlers have different names: `add` and `remove`.</span></span> <span data-ttu-id="ab55e-193">Są one nazywane subskrypcji zdarzenia lub zrezygnować z zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="ab55e-193">These are called to subscribe to the event, or unsubscribe from the event.</span></span> <span data-ttu-id="ab55e-194">Zwróć uwagę, czy też należy zadeklarować polem zapasowym prywatnych do przechowywania zmiennej zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="ab55e-194">Notice that you also must declare a private backing field to store the event variable.</span></span> <span data-ttu-id="ab55e-195">Jest on zainicjowany do wartości null.</span><span class="sxs-lookup"><span data-stu-id="ab55e-195">It is initialized to null.</span></span>

<span data-ttu-id="ab55e-196">Następnie możemy dodać przeciążenia metody Search(), która jest przesyłany w podkatalogach i zgłasza obu zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="ab55e-196">Next, let's add the overload of the Search() method that traverses subdirectories and raises both events.</span></span> <span data-ttu-id="ab55e-197">W tym celu najłatwiej Użyj domyślnego argumentu, aby określić, czy chcesz wyszukać wszystkich katalogów:</span><span class="sxs-lookup"><span data-stu-id="ab55e-197">The easiest way to accomplish this is to use a default argument to specify that you want to search all directories:</span></span>

[!code-csharp[SearchImplementation](../../samples/csharp/events/Program.cs#FinalImplementation "Implementation to search directories")]

<span data-ttu-id="ab55e-198">W tym momencie można uruchomić aplikacji wywoływania przeciążenia wszystkie podkatalogi wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="ab55e-198">At this point, you can run the application calling the overload for searching all sub-directories.</span></span> <span data-ttu-id="ab55e-199">Na nowe nie ma żadnych subskrybentów `ChangeDirectory` zdarzeń, ale przy użyciu `?.Invoke()` idiom gwarantuje, że to działa prawidłowo.</span><span class="sxs-lookup"><span data-stu-id="ab55e-199">There are no subscribers on the new `ChangeDirectory` event, but using the `?.Invoke()` idiom ensures that this works correctly.</span></span>

 <span data-ttu-id="ab55e-200">Dodajmy obsługi do zapisywania wiersza, który będzie wyświetlany postęp w oknie konsoli.</span><span class="sxs-lookup"><span data-stu-id="ab55e-200">Let's add a handler to write a line that shows the progress in the console window.</span></span> 

[!code-csharp[Search](../../samples/csharp/events/Program.cs#Search "Declare event handler")]

<span data-ttu-id="ab55e-201">Przedstawiono wzorców, które zostaną wykonane przez cały ekosystem .NET.</span><span class="sxs-lookup"><span data-stu-id="ab55e-201">You've seen patterns that are followed throughout the .NET ecosystem.</span></span>
<span data-ttu-id="ab55e-202">Przez nauki tych wzorców i konwencje napiszesz idiomatyczne C# i .NET szybko.</span><span class="sxs-lookup"><span data-stu-id="ab55e-202">By learning these patterns and conventions, you'll be writing idiomatic C# and .NET quickly.</span></span>

<span data-ttu-id="ab55e-203">Następnie zostanie wyświetlony pewne zmiany w tych wzorców w najnowszej wersji programu .NET.</span><span class="sxs-lookup"><span data-stu-id="ab55e-203">Next, you'll see some changes in these patterns in the most recent release of .NET.</span></span>

[<span data-ttu-id="ab55e-204">Next</span><span class="sxs-lookup"><span data-stu-id="ab55e-204">Next</span></span>](modern-events.md)
