---
title: Migrowanie aplikacji WPF do programu .NET Core 3,0
description: Dowiedz się, jak przeprowadzić migrację aplikacji Windows Presentation Foundation (WPF) do programu .NET Core 3,0.
author: mjrousos
ms.date: 09/12/2019
ms.author: mikerou
ms.openlocfilehash: fda4f618ddb4a3edbe6f2dd9fba0b10bc618e88d
ms.sourcegitcommit: 71b8f5a2108a0f1a4ef1d8d75c5b3e129ec5ca1e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 05/29/2020
ms.locfileid: "84201556"
---
# <a name="migrating-wpf-apps-to-net-core"></a><span data-ttu-id="404b5-103">Migrowanie aplikacji WPF do platformy .NET Core</span><span class="sxs-lookup"><span data-stu-id="404b5-103">Migrating WPF apps to .NET Core</span></span>

<span data-ttu-id="404b5-104">W tym artykule opisano kroki niezbędne do przeprowadzenia migracji aplikacji Windows Presentation Foundation (WPF) z .NET Framework do programu .NET Core 3,0.</span><span class="sxs-lookup"><span data-stu-id="404b5-104">This article covers the steps necessary to migrate a Windows Presentation Foundation (WPF) app from .NET Framework to .NET Core 3.0.</span></span> <span data-ttu-id="404b5-105">Jeśli nie masz dostępnej aplikacji WPF do portu, ale chcesz wypróbować ten proces, możesz użyć przykładowej aplikacji dla **handlowców** w witrynie [GitHub](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader).</span><span class="sxs-lookup"><span data-stu-id="404b5-105">If you don't have a WPF app on hand to port, but would like to try out the process, you can use the **Bean Trader** sample app available on [GitHub](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader).</span></span> <span data-ttu-id="404b5-106">Oryginalna aplikacja (docelowa .NET Framework 4.7.2) jest dostępna w folderze NetFx\BeanTraderClient.</span><span class="sxs-lookup"><span data-stu-id="404b5-106">The original app (targeting .NET Framework 4.7.2) is available in the NetFx\BeanTraderClient folder.</span></span> <span data-ttu-id="404b5-107">Najpierw wyjaśnimy kroki niezbędne do przetestowania aplikacji w ogólny sposób, a następnie przeprowadzimy Cię przez szczegółowe zmiany, które dotyczą przykładu **handlowca** .</span><span class="sxs-lookup"><span data-stu-id="404b5-107">First we'll explain the steps necessary to port apps in general, and then we'll walk through the specific changes that apply to the **Bean Trader** sample.</span></span>

[!INCLUDE [desktop guide under construction](../../../includes/desktop-guide-preview-note.md)]

<span data-ttu-id="404b5-108">Aby przeprowadzić migrację do platformy .NET Core, należy najpierw:</span><span class="sxs-lookup"><span data-stu-id="404b5-108">To migrate to .NET Core, you must first:</span></span>

01. <span data-ttu-id="404b5-109">Zrozumienie i aktualizowanie zależności NuGet:</span><span class="sxs-lookup"><span data-stu-id="404b5-109">Understand and update NuGet dependencies:</span></span>

    01. <span data-ttu-id="404b5-110">Uaktualnij zależności NuGet, aby użyć tego `<PackageReference>` formatu.</span><span class="sxs-lookup"><span data-stu-id="404b5-110">Upgrade NuGet dependencies to use the `<PackageReference>` format.</span></span>
    01. <span data-ttu-id="404b5-111">Przejrzyj zależności NuGet najwyższego poziomu dla platformy .NET Core lub .NET Standard zgodności.</span><span class="sxs-lookup"><span data-stu-id="404b5-111">Review top-level NuGet dependencies for .NET Core or .NET Standard compatibility.</span></span>
    01. <span data-ttu-id="404b5-112">Uaktualnij pakiety NuGet do nowszych wersji.</span><span class="sxs-lookup"><span data-stu-id="404b5-112">Upgrade NuGet packages to newer versions.</span></span>
    01. <span data-ttu-id="404b5-113">Użyj [analizatora przenośności platformy .NET](../../standard/analyzers/portability-analyzer.md) , aby zrozumieć zależności platformy .NET.</span><span class="sxs-lookup"><span data-stu-id="404b5-113">Use the [.NET Portability Analyzer](../../standard/analyzers/portability-analyzer.md) to understand .NET dependencies.</span></span>

01. <span data-ttu-id="404b5-114">Migruj plik projektu do nowego formatu w stylu zestawu SDK:</span><span class="sxs-lookup"><span data-stu-id="404b5-114">Migrate the project file to the new SDK-style format:</span></span>

    01. <span data-ttu-id="404b5-115">Zdecyduj, czy ma być przeznaczony zarówno dla platformy .NET Core, jak i .NET Framework, czy tylko na platformie .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-115">Choose whether to target both .NET Core and .NET Framework, or only .NET Core.</span></span>
    01. <span data-ttu-id="404b5-116">Skopiuj odpowiednie właściwości pliku projektu i elementów do nowego pliku projektu.</span><span class="sxs-lookup"><span data-stu-id="404b5-116">Copy relevant project file properties and items to the new project file.</span></span>

01. <span data-ttu-id="404b5-117">Rozwiąż problemy z kompilacją:</span><span class="sxs-lookup"><span data-stu-id="404b5-117">Fix build issues:</span></span>

    01. <span data-ttu-id="404b5-118">Dodaj odwołanie do pakietu [Microsoft. Windows. Compatibility](https://www.nuget.org/packages/Microsoft.Windows.Compatibility/) .</span><span class="sxs-lookup"><span data-stu-id="404b5-118">Add a reference to the [Microsoft.Windows.Compatibility](https://www.nuget.org/packages/Microsoft.Windows.Compatibility/) package.</span></span>
    01. <span data-ttu-id="404b5-119">Znajdź i napraw różnice poziomu interfejsu API.</span><span class="sxs-lookup"><span data-stu-id="404b5-119">Find and fix API-level differences.</span></span>
    01. <span data-ttu-id="404b5-120">Usuń sekcje *App. config* inne niż `appSettings` lub `connectionStrings` .</span><span class="sxs-lookup"><span data-stu-id="404b5-120">Remove *app.config* sections other than `appSettings` or `connectionStrings`.</span></span>
    01. <span data-ttu-id="404b5-121">W razie potrzeby ponownie Generuj wygenerowany kod.</span><span class="sxs-lookup"><span data-stu-id="404b5-121">Regenerate generated code, if necessary.</span></span>

01. <span data-ttu-id="404b5-122">Testowanie środowiska uruchomieniowego:</span><span class="sxs-lookup"><span data-stu-id="404b5-122">Runtime testing:</span></span>

    01. <span data-ttu-id="404b5-123">Potwierdź, że port działa zgodnie z oczekiwaniami.</span><span class="sxs-lookup"><span data-stu-id="404b5-123">Confirm the ported app works as expected.</span></span>
    01. <span data-ttu-id="404b5-124">Uważaj na <xref:System.NotSupportedException> wyjątki.</span><span class="sxs-lookup"><span data-stu-id="404b5-124">Beware of <xref:System.NotSupportedException> exceptions.</span></span>

## <a name="about-the-sample"></a><span data-ttu-id="404b5-125">Informacje o przykładzie</span><span class="sxs-lookup"><span data-stu-id="404b5-125">About the sample</span></span>

<span data-ttu-id="404b5-126">Ten artykuł odwołuje się do [przykładowej aplikacji handlowca w języku fasoli](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader) , ponieważ używa wielu zależności podobnych do tych, które mogą mieć rzeczywiste aplikacje WPF.</span><span class="sxs-lookup"><span data-stu-id="404b5-126">This article references the [Bean Trader sample app](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader) because it uses a variety of dependencies similar to those that real-world WPF apps might have.</span></span> <span data-ttu-id="404b5-127">Aplikacja nie jest duża, ale ma stanowić krok w górę od "Hello world" pod względem złożoności.</span><span class="sxs-lookup"><span data-stu-id="404b5-127">The app isn't large, but is meant to be a step up from 'Hello World' in terms of complexity.</span></span> <span data-ttu-id="404b5-128">W aplikacji przedstawiono niektóre problemy, które użytkownicy mogą napotkać podczas przenoszenia prawdziwych aplikacji.</span><span class="sxs-lookup"><span data-stu-id="404b5-128">The app demonstrates some issues users may encounter while porting real apps.</span></span> <span data-ttu-id="404b5-129">Aplikacja komunikuje się z usługą WCF, dlatego aby działała prawidłowo, należy również uruchomić projekt BeanTraderServer (dostępny w tym samym repozytorium GitHub) i upewnić się, że konfiguracja BeanTraderClient wskazuje właściwy punkt końcowy.</span><span class="sxs-lookup"><span data-stu-id="404b5-129">The app communicates with a WCF service, so for it to run properly, you'll also need to run the BeanTraderServer project (available in the same GitHub repository) and make sure the BeanTraderClient configuration points to the correct endpoint.</span></span> <span data-ttu-id="404b5-130">(Domyślnie przykład zakłada, że serwer jest uruchomiony na tym samym komputerze `http://localhost:8090` , co ma wartość true, jeśli uruchomisz lokalnie BeanTraderServer).</span><span class="sxs-lookup"><span data-stu-id="404b5-130">(By default, the sample assumes the server is running on the same machine at `http://localhost:8090`, which will be true if you launch BeanTraderServer locally.)</span></span>

<span data-ttu-id="404b5-131">Należy pamiętać, że ta Przykładowa aplikacja ma na celu przedstawienie wyzwań i rozwiązań związanych z portem .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-131">Keep in mind that this sample app is meant to demonstrate .NET Core porting challenges and solutions.</span></span> <span data-ttu-id="404b5-132">Nie ma potrzeby przedstawienia najlepszych rozwiązań WPF.</span><span class="sxs-lookup"><span data-stu-id="404b5-132">It's not meant to demonstrate WPF best practices.</span></span> <span data-ttu-id="404b5-133">W rzeczywistości z tego względu zawiera pewne antywzorce, aby upewnić się, że będziesz mieć co najmniej kilka interesujących wyzwań podczas przenoszenia.</span><span class="sxs-lookup"><span data-stu-id="404b5-133">In fact, it deliberately includes some anti-patterns to make sure you come across at least a couple of interesting challenges while porting.</span></span>

## <a name="getting-ready"></a><span data-ttu-id="404b5-134">Przygotowanie</span><span class="sxs-lookup"><span data-stu-id="404b5-134">Getting ready</span></span>

<span data-ttu-id="404b5-135">Podstawowym wyzwaniem migrowania aplikacji .NET Framework do programu .NET Core jest to, że jego zależności mogą współpracować inaczej lub wcale.</span><span class="sxs-lookup"><span data-stu-id="404b5-135">The primary challenge of migrating a .NET Framework app to .NET Core is that its dependencies may work differently or not at all.</span></span> <span data-ttu-id="404b5-136">Migracja jest znacznie łatwiejsza niż jej użycie; wiele pakietów NuGet teraz docelowo .NET Standard.</span><span class="sxs-lookup"><span data-stu-id="404b5-136">Migration is much easier than it used to be; many NuGet packages now target .NET Standard.</span></span> <span data-ttu-id="404b5-137">Począwszy od platformy .NET Core 2,0, obszary powierzchni .NET Framework i .NET Core stają się podobne.</span><span class="sxs-lookup"><span data-stu-id="404b5-137">Starting with .NET Core 2.0, the .NET Framework and .NET Core surface areas have become similar.</span></span> <span data-ttu-id="404b5-138">Nawet dlatego niektóre różnice (zarówno w przypadku obsługi pakietów NuGet, jak i dostępnych interfejsów API platformy .NET) pozostają.</span><span class="sxs-lookup"><span data-stu-id="404b5-138">Even so, some differences (both in support from NuGet packages and in available .NET APIs) remain.</span></span> <span data-ttu-id="404b5-139">Pierwszym krokiem w migrowaniu jest sprawdzenie zależności aplikacji i upewnienie się, że odwołania znajdują się w formacie, który można łatwo zmigrować do programu .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-139">The first step in migrating is to review the app's dependencies and make sure references are in a format that's easily migrated to .NET Core.</span></span>

### <a name="upgrade-to-packagereference-nuget-references"></a><span data-ttu-id="404b5-140">Uaktualnij do `<PackageReference>` odwołań NuGet</span><span class="sxs-lookup"><span data-stu-id="404b5-140">Upgrade to `<PackageReference>` NuGet references</span></span>

<span data-ttu-id="404b5-141">Starsze projekty .NET Framework zwykle wykazują zależności NuGet w pliku *Packages. config* .</span><span class="sxs-lookup"><span data-stu-id="404b5-141">Older .NET Framework projects typically list their NuGet dependencies in a *packages.config* file.</span></span> <span data-ttu-id="404b5-142">Nowy format pliku projektu w stylu zestawu SDK odwołuje się do pakietów NuGet jako [`<PackageReference>`](/nuget/consume-packages/package-references-in-project-files) elementy w samym pliku csproj, a nie w osobnym pliku konfiguracyjnym.</span><span class="sxs-lookup"><span data-stu-id="404b5-142">The new SDK-style project file format references NuGet packages as [`<PackageReference>`](/nuget/consume-packages/package-references-in-project-files) elements in the csproj file itself rather than in a separate config file.</span></span>

<span data-ttu-id="404b5-143">Podczas migracji istnieją dwie zalety korzystania z odwołań do `<PackageReference>` stylów:</span><span class="sxs-lookup"><span data-stu-id="404b5-143">When migrating, there are two advantages to using `<PackageReference>`-style references:</span></span>

- <span data-ttu-id="404b5-144">Jest to styl odwołania NuGet, który jest wymagany dla nowego pliku projektu .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-144">This is the style of NuGet reference that is required for the new .NET Core project file.</span></span> <span data-ttu-id="404b5-145">Jeśli już używasz `<PackageReference>` , te elementy pliku projektu można skopiować i wkleić bezpośrednio do nowego projektu.</span><span class="sxs-lookup"><span data-stu-id="404b5-145">If you're already using `<PackageReference>`, those project file elements can be copied and pasted directly into the new project.</span></span>
- <span data-ttu-id="404b5-146">W przeciwieństwie do pliku Packages. config `<PackageReference>` elementy odnoszą się tylko do zależności najwyższego poziomu, od których projekt zależy bezpośrednio.</span><span class="sxs-lookup"><span data-stu-id="404b5-146">Unlike a packages.config file, `<PackageReference>` elements only refer to the top-level dependencies that your project depends on directly.</span></span> <span data-ttu-id="404b5-147">Wszystkie inne przechodnie pakiety NuGet zostaną określone w czasie przywracania i zapisane w pliku obj\project.assets.JSON.</span><span class="sxs-lookup"><span data-stu-id="404b5-147">All other transitive NuGet packages will be determined at restore time and recorded in the autogenerated obj\project.assets.json file.</span></span> <span data-ttu-id="404b5-148">Dzięki temu można znacznie łatwiej określić, jakie zależności ma projekt, co jest przydatne podczas określania, czy wymagane zależności będą działały na platformie .NET Core, czy nie.</span><span class="sxs-lookup"><span data-stu-id="404b5-148">This makes it much easier to determine what dependencies your project has, which is useful when determining whether the necessary dependencies will work on .NET Core or not.</span></span>

<span data-ttu-id="404b5-149">Pierwszym krokiem migrowania aplikacji .NET Framework do programu .NET Core jest zaktualizowanie jej do korzystania z `<PackageReference>` odwołań NuGet.</span><span class="sxs-lookup"><span data-stu-id="404b5-149">The first step to migrating a .NET Framework app to .NET Core is to update it to use `<PackageReference>` NuGet references.</span></span> <span data-ttu-id="404b5-150">Program Visual Studio ułatwia to proste.</span><span class="sxs-lookup"><span data-stu-id="404b5-150">Visual Studio makes this simple.</span></span> <span data-ttu-id="404b5-151">Po prostu kliknij prawym przyciskiem myszy plik *Packages. config* projektu w **Eksplorator rozwiązań**programu Visual Studio, a następnie wybierz polecenie **Migruj pakiety. config do PackageReference**.</span><span class="sxs-lookup"><span data-stu-id="404b5-151">Just right-click the project's *packages.config* file in Visual Studio's **Solution Explorer**, and then select **Migrate packages.config to PackageReference**.</span></span>

![Uaktualnianie do PackageReference](./media/convert-project-from-net-framework/package-reference-migration.png)

<span data-ttu-id="404b5-153">Zostanie wyświetlone okno dialogowe z obliczonymi zależnościami programu NuGet najwyższego poziomu i pytaniem, które inne pakiety NuGet mają być podwyższenie poziomu.</span><span class="sxs-lookup"><span data-stu-id="404b5-153">A dialog appears showing calculated top-level NuGet dependencies and asking which other NuGet packages should be promoted to top-level.</span></span> <span data-ttu-id="404b5-154">Żaden z tych pozostałych pakietów nie musi być najwyższego poziomu dla przykładu handlowca fasoli, więc można usunąć zaznaczenie wszystkich pól.</span><span class="sxs-lookup"><span data-stu-id="404b5-154">None of these other packages need to be top-level for the Bean Trader sample, so you can uncheck all of those boxes.</span></span> <span data-ttu-id="404b5-155">Następnie kliknij przycisk **OK** , a plik *Packages. config* zostanie usunięty, a `<PackageReference>` elementy zostaną dodane do pliku projektu.</span><span class="sxs-lookup"><span data-stu-id="404b5-155">Then, click **Ok** and the *packages.config* file is removed and `<PackageReference>` elements are added to the project file.</span></span>

<span data-ttu-id="404b5-156">`<PackageReference>`-Style References nie przechowują pakietów NuGet lokalnie w folderze Packages.</span><span class="sxs-lookup"><span data-stu-id="404b5-156">`<PackageReference>`-style references don't store NuGet packages locally in a packages folder.</span></span> <span data-ttu-id="404b5-157">Zamiast tego są one przechowywane globalnie jako Optymalizacja.</span><span class="sxs-lookup"><span data-stu-id="404b5-157">Instead, they're stored globally as an optimization.</span></span> <span data-ttu-id="404b5-158">Po zakończeniu migracji Edytuj plik CSPROJ i Usuń wszystkie `<Analyzer>` elementy odwołujące się do analizatorów, które wcześniej pochodziły z *.. Katalog \packages* .</span><span class="sxs-lookup"><span data-stu-id="404b5-158">After the migration completes, edit the csproj file and remove any `<Analyzer>` elements referring to the analyzers that previously came from the *..\packages* directory.</span></span> <span data-ttu-id="404b5-159">Nie martw się; ponieważ nadal masz odwołania do pakietu NuGet, analizatory zostaną dołączone do projektu.</span><span class="sxs-lookup"><span data-stu-id="404b5-159">Don't worry; since you still have the NuGet package references, the analyzers will be included in the project.</span></span> <span data-ttu-id="404b5-160">Wystarczy wyczyścić stare elementy style Packages. config `<Analyzer>` .</span><span class="sxs-lookup"><span data-stu-id="404b5-160">You just need to clean up the old packages.config-style `<Analyzer>` elements.</span></span>

### <a name="review-nuget-packages"></a><span data-ttu-id="404b5-161">Przejrzyj pakiety NuGet</span><span class="sxs-lookup"><span data-stu-id="404b5-161">Review NuGet packages</span></span>

<span data-ttu-id="404b5-162">Teraz, gdy widzisz pakiety NuGet najwyższego poziomu, od których zależy projekt, możesz sprawdzić, czy te pakiety są dostępne w środowisku .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-162">Now that you can see the top-level NuGet packages that the project depends on, you can review whether those packages are available on .NET Core.</span></span> <span data-ttu-id="404b5-163">Można określić, czy pakiet obsługuje platformę .NET Core, sprawdzając jego zależności od [NuGet.org](https://www.nuget.org/). Witryna [fuget.org](https://www.fuget.org/) utworzona przez społeczność ukazuje te informacje w widocznym miejscu w górnej części strony informacji o pakiecie.</span><span class="sxs-lookup"><span data-stu-id="404b5-163">You can determine whether a package supports .NET Core by looking at its dependencies on [nuget.org](https://www.nuget.org/). The community-created [fuget.org](https://www.fuget.org/) site shows this information prominently at the top of the package information page.</span></span>

<span data-ttu-id="404b5-164">W przypadku określania platformy .NET Core 3,0 wszystkie pakiety przeznaczone dla platformy .NET Core lub .NET Standard powinny funkcjonować (ponieważ platforma .NET Core implementuje obszar powierzchni .NET Standard).</span><span class="sxs-lookup"><span data-stu-id="404b5-164">When targeting .NET Core 3.0, any packages targeting .NET Core or .NET Standard should work (since .NET Core implements the .NET Standard surface area).</span></span> <span data-ttu-id="404b5-165">W niektórych przypadkach określona wersja pakietu, która jest używana, nie będzie ukierunkowana na platformę .NET Core lub .NET Standard, ale będzie dostępna nowsza wersja.</span><span class="sxs-lookup"><span data-stu-id="404b5-165">In some cases, the specific version of a package that's used won't target .NET Core or .NET Standard, but newer versions will.</span></span> <span data-ttu-id="404b5-166">W takim przypadku należy rozważyć uaktualnienie do najnowszej wersji pakietu.</span><span class="sxs-lookup"><span data-stu-id="404b5-166">In this case, you should consider upgrading to the latest version of the package.</span></span>

<span data-ttu-id="404b5-167">Można używać pakietów przeznaczonych do .NET Framework, a także, które wprowadzają pewne ryzyko.</span><span class="sxs-lookup"><span data-stu-id="404b5-167">You can use packages targeting .NET Framework, as well, but that introduces some risk.</span></span> <span data-ttu-id="404b5-168">Elementy .NET Core do .NET Framework zależności są dozwolone, ponieważ obszary powierzchni .NET Core i .NET Framework Surface są podobne, że takie zależności *często* działają.</span><span class="sxs-lookup"><span data-stu-id="404b5-168">.NET Core to .NET Framework dependencies are allowed because .NET Core and .NET Framework surface areas are similar enough that such dependencies *often* work.</span></span> <span data-ttu-id="404b5-169">Jeśli jednak pakiet próbuje użyć interfejsu API platformy .NET, który nie jest obecny w środowisku .NET Core, wystąpi wyjątek w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="404b5-169">However, if the package tries to use a .NET API that isn't present in .NET Core, you'll encounter a runtime exception.</span></span> <span data-ttu-id="404b5-170">Z tego względu należy odwoływać się tylko do .NET Framework pakietów, gdy nie są dostępne żadne inne opcje i nie rozumieją, że nie nastąpi obciążenie testem.</span><span class="sxs-lookup"><span data-stu-id="404b5-170">Because of that, you should only reference .NET Framework packages when no other options are available and understand that doing so imposes a test burden.</span></span>

<span data-ttu-id="404b5-171">Jeśli istnieją pakiety, które nie są przeznaczone do programu .NET Core lub .NET Standard, należy wziąć pod uwagę inne alternatywy:</span><span class="sxs-lookup"><span data-stu-id="404b5-171">If there are packages referenced that don't target .NET Core or .NET Standard, you'll have to think about other alternatives:</span></span>

- <span data-ttu-id="404b5-172">Czy istnieją inne podobne pakiety, których można użyć zamiast tego?</span><span class="sxs-lookup"><span data-stu-id="404b5-172">Are there other similar packages that can be used instead?</span></span> <span data-ttu-id="404b5-173">Czasami autorzy NuGet publikują oddzielne ". Podstawowe wersje ich bibliotek przeznaczonych dla platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-173">Sometimes NuGet authors publish separate '.Core' versions of their libraries specifically targeting .NET Core.</span></span> <span data-ttu-id="404b5-174">Pakiety biblioteki przedsiębiorstwa to przykład publikacji społecznościowej. Rozwiązania alternatywne.</span><span class="sxs-lookup"><span data-stu-id="404b5-174">Enterprise Library packages are an example of the community publishing ".NetCore" alternatives.</span></span> <span data-ttu-id="404b5-175">W innych przypadkach dla .NET Standard są dostępne nowsze zestawy SDK dla określonej usługi (czasami z różnymi nazwami pakietów).</span><span class="sxs-lookup"><span data-stu-id="404b5-175">In other cases, newer SDKs for a particular service (sometimes with different package names) are available for .NET Standard.</span></span> <span data-ttu-id="404b5-176">Jeśli nie są dostępne żadne alternatywy, możesz korzystać z do.NET Frameworkch pakietów, biorąc pod uwagę, że należy przetestować je dokładnie po uruchomieniu na platformie .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-176">If no alternatives are available, you can proceed using the .NET Framework-targeted packages, bearing in mind that you'll need to test them thoroughly once running on .NET Core.</span></span>

<span data-ttu-id="404b5-177">Przykład handlowy ziarna fasoli ma następujące zależności najwyższego poziomu NuGet:</span><span class="sxs-lookup"><span data-stu-id="404b5-177">The Bean Trader sample has the following top-level NuGet dependencies:</span></span>

- [<span data-ttu-id="404b5-178">**Castle. Windsor, wersja 4.1.1**</span><span class="sxs-lookup"><span data-stu-id="404b5-178">**Castle.Windsor, version 4.1.1**</span></span>](https://www.castleproject.org/projects/windsor/)  

  <span data-ttu-id="404b5-179">Ten pakiet jest przeznaczony dla .NET Standard 1,6, dlatego działa w środowisku .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-179">This package targets .NET Standard 1.6, so it works on .NET Core.</span></span>

- [<span data-ttu-id="404b5-180">**Microsoft. CodeAnalysis. FxCopAnalyzers, wersja 2.6.3**</span><span class="sxs-lookup"><span data-stu-id="404b5-180">**Microsoft.CodeAnalysis.FxCopAnalyzers, version 2.6.3**</span></span>](https://www.nuget.org/packages/Microsoft.CodeAnalysis.FxCopAnalyzers/2.6.3)  
  <span data-ttu-id="404b5-181">Jest to Meta-pakiet, dlatego nie jest od razu oczywisty, które platformy obsługują, ale [Dokumentacja](https://github.com/dotnet/roslyn-analyzers#microsoftcodeanalysisfxcopanalyzers) wskazuje, że jej Najnowsza wersja (2.9.2) będzie działała zarówno dla .NET Framework, jak i .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-181">This is a meta-package, so it's not immediately obvious which platforms it supports, but [documentation](https://github.com/dotnet/roslyn-analyzers#microsoftcodeanalysisfxcopanalyzers) indicates that its newest version (2.9.2) will work for both .NET Framework and .NET Core.</span></span>

- [<span data-ttu-id="404b5-182">**Nito. AsyncEx, wersja 4.0.1**</span><span class="sxs-lookup"><span data-stu-id="404b5-182">**Nito.AsyncEx, version 4.0.1**</span></span>](https://www.nuget.org/packages/Nito.AsyncEx/4.0.1)  

  <span data-ttu-id="404b5-183">Ten pakiet nie jest przeznaczony dla platformy .NET Core, ale nowsza wersja 5,0.</span><span class="sxs-lookup"><span data-stu-id="404b5-183">This package doesn't target .NET Core, but the newer 5.0 version does.</span></span> <span data-ttu-id="404b5-184">Jest to typowe w przypadku migrowania, ponieważ wiele pakietów NuGet .NET Standard obsługiwać ostatnio, ale starsze wersje projektu będą wskazywać tylko .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="404b5-184">This is common when migrating because many NuGet packages have added .NET Standard support recently, but older project versions will only target .NET Framework.</span></span> <span data-ttu-id="404b5-185">Jeśli różnica wersji jest tylko niewielką różnicą wersji, często można łatwo przeprowadzić uaktualnienie do nowszej wersji.</span><span class="sxs-lookup"><span data-stu-id="404b5-185">If the version difference is only a minor version difference, it's often easy to upgrade to the newer version.</span></span> <span data-ttu-id="404b5-186">Ze względu na to, że jest to główna zmiana wersji, należy zachować ostrożność uaktualniania, ponieważ w pakiecie mogą wystąpić pewne zmiany.</span><span class="sxs-lookup"><span data-stu-id="404b5-186">Because this is a major version change, you need to be cautious upgrading since there could be breaking changes in the package.</span></span> <span data-ttu-id="404b5-187">Istnieje ścieżka do przodu, ale jest dobry.</span><span class="sxs-lookup"><span data-stu-id="404b5-187">There is a path forward, though, which is good.</span></span>

- [<span data-ttu-id="404b5-188">**MahApps. Metro, wersja 1.6.5**</span><span class="sxs-lookup"><span data-stu-id="404b5-188">**MahApps.Metro, version 1.6.5**</span></span>](https://www.nuget.org/packages/MahApps.Metro/1.6.5)  

  <span data-ttu-id="404b5-189">Ten pakiet nie jest również przeznaczony dla programu .NET Core, ale ma nowszą wersję wstępną (2,0-Alpha).</span><span class="sxs-lookup"><span data-stu-id="404b5-189">This package also doesn't target .NET Core, but has a newer pre-release (2.0-alpha) that does.</span></span> <span data-ttu-id="404b5-190">Ponownie musisz wyszukać istotne zmiany, ale nowy pakiet zachęca.</span><span class="sxs-lookup"><span data-stu-id="404b5-190">Again, you have to look out for breaking changes, but the newer package is encouraging.</span></span>

<span data-ttu-id="404b5-191">Pakiet NuGet przykładowego elementu danych firmy fasoli jest zależny od wszystkich elementów docelowych .NET Standard/. NET Core lub mających nowsze wersje, co oznacza, że w tym miejscu nie można znaleźć żadnych problemów z blokowaniem.</span><span class="sxs-lookup"><span data-stu-id="404b5-191">The Bean Trader sample's NuGet dependencies all either target .NET Standard/.NET Core or have newer versions that do, so there are unlikely to be any blocking issues here.</span></span>

### <a name="upgrade-nuget-packages"></a><span data-ttu-id="404b5-192">Uaktualnij pakiety NuGet</span><span class="sxs-lookup"><span data-stu-id="404b5-192">Upgrade NuGet packages</span></span>

<span data-ttu-id="404b5-193">Jeśli to możliwe, warto uaktualnić wersje pakietów, które są przeznaczone tylko dla platformy .NET Core lub .NET Standard z nowszymi wersjami w tym momencie (z projektem, który ma nadal służyć .NET Framework), aby odszukać i rozwiązać wszystkie istotne zmiany na początku.</span><span class="sxs-lookup"><span data-stu-id="404b5-193">If possible, it would be good to upgrade the versions of any packages that only target .NET Core or .NET Standard with more recent versions at this point (with the project still targeting .NET Framework) to discover and address any breaking changes early.</span></span>

<span data-ttu-id="404b5-194">Jeśli wolisz nie wprowadzać żadnych istotnych zmian do istniejącej wersji .NET Framework aplikacji, może to potrwać do momentu, gdy nowy plik projektu jest przeznaczony dla platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-194">If you would rather not make any material changes to the existing .NET Framework version of the app, this can wait until you have a new project file targeting .NET Core.</span></span> <span data-ttu-id="404b5-195">Jednak Uaktualnienie pakietów NuGet do wersji programu .NET Core zgodnych ze standardem czasu sprawia, że proces migracji będzie jeszcze łatwiejszy po utworzeniu nowego pliku projektu i zmniejszeniu liczby różnic między wersjami .NET Framework i .NET Core aplikacji.</span><span class="sxs-lookup"><span data-stu-id="404b5-195">However, upgrading the NuGet packages to .NET Core-compatible versions ahead of time makes the migration process even easier once you create the new project file and reduces the number of differences between the .NET Framework and .NET Core versions of the app.</span></span>

<span data-ttu-id="404b5-196">Za pomocą przykładu handlowca można łatwo wykonać wszystkie niezbędne uaktualnienia (przy użyciu Menedżera pakietów NuGet programu Visual Studio) z jednym wyjątkiem: uaktualnienie z **MahApps. Metro 1.6.5** do **2,0** ujawnia istotne zmiany związane z interfejsami API zarządzania motywem i akcentem.</span><span class="sxs-lookup"><span data-stu-id="404b5-196">With the Bean Trader sample, all of the necessary upgrades can be made easily (using Visual Studio's NuGet package manager) with one exception: upgrading from **MahApps.Metro 1.6.5** to **2.0** reveals breaking changes related to theme and accent management APIs.</span></span>

<span data-ttu-id="404b5-197">W idealnym przypadku aplikacja zostanie zaktualizowana tak, aby korzystała z nowszej wersji pakietu (ponieważ najprawdopodobniej będzie działać na platformie .NET Core).</span><span class="sxs-lookup"><span data-stu-id="404b5-197">Ideally, the app would be updated to use the newer version of the package (since that is more likely to work on .NET Core).</span></span> <span data-ttu-id="404b5-198">Jednak w niektórych przypadkach może to nie być możliwe.</span><span class="sxs-lookup"><span data-stu-id="404b5-198">In some cases, however, that may not be feasible.</span></span> <span data-ttu-id="404b5-199">W takich przypadkach nie należy aktualizować **MahApps. Metro** , ponieważ niezbędne zmiany nie są proste i ten samouczek koncentruje się na migrowaniu do platformy .NET Core 3, a nie do **MahApps. Metro 2.**</span><span class="sxs-lookup"><span data-stu-id="404b5-199">In these cases, don't upgrade **MahApps.Metro** because the necessary changes are non-trivial and this tutorial focuses on migrating to .NET Core 3, not to **MahApps.Metro 2.**</span></span> <span data-ttu-id="404b5-200">Jest to również zależność .NET Framework o niskim ryzyku, ponieważ aplikacja handlowca jest oparta tylko na niewielkiej części **MahApps. Metro**.</span><span class="sxs-lookup"><span data-stu-id="404b5-200">Also, this is a low-risk .NET Framework dependency because the Bean Trader app only exercises a small part of **MahApps.Metro**.</span></span> <span data-ttu-id="404b5-201">W takim przypadku należy wymagać testowania, aby upewnić się, że wszystko działa po zakończeniu migracji.</span><span class="sxs-lookup"><span data-stu-id="404b5-201">It will, of course, require testing to make sure everything's working once the migration is complete.</span></span> <span data-ttu-id="404b5-202">Jeśli był to rzeczywisty scenariusz, warto zaplikować problem, aby śledzić pracę, która zostanie przeniesiona do **MahApps. Metro** w wersji 2,0, ponieważ nie spowoduje to przeprowadzenia migracji.</span><span class="sxs-lookup"><span data-stu-id="404b5-202">If this were a real-world scenario, it would be good to file an issue to track the work to move to **MahApps.Metro** version 2.0 since not doing the migration now leaves behind some technical debt.</span></span>

<span data-ttu-id="404b5-203">Gdy pakiety NuGet zostaną zaktualizowane do najnowszych wersji, `<PackageReference>` Grupa elementów w pliku projektu przykładowej firmy ziarna fasoli powinna wyglądać następująco.</span><span class="sxs-lookup"><span data-stu-id="404b5-203">Once the NuGet packages are updated to recent versions, the `<PackageReference>` item group in the Bean Trader sample's project file should look like this.</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Castle.Windsor">
    <Version>4.1.1</Version>
  </PackageReference>
  <PackageReference Include="MahApps.Metro">
    <Version>1.6.5</Version>
  </PackageReference>
  <PackageReference Include="Microsoft.CodeAnalysis.FxCopAnalyzers">
    <Version>2.9.2</Version>
  </PackageReference>
  <PackageReference Include="Nito.AsyncEx">
    <Version>5.0.0</Version>
  </PackageReference>
</ItemGroup>
```

### <a name="net-framework-portability-analysis"></a><span data-ttu-id="404b5-204">.NET Framework analiza przenośności</span><span class="sxs-lookup"><span data-stu-id="404b5-204">.NET Framework portability analysis</span></span>

<span data-ttu-id="404b5-205">Gdy zrozumiesz stan zależności NuGet projektu, następnym krokiem, który należy wziąć pod uwagę, jest .NET Framework zależności interfejsu API.</span><span class="sxs-lookup"><span data-stu-id="404b5-205">Once you understand the state of your project's NuGet dependencies, the next thing to consider is .NET Framework API dependencies.</span></span> <span data-ttu-id="404b5-206">Narzędzie [analizatora przenośności platformy .NET](../../standard/analyzers/portability-analyzer.md) jest przydatne w celu ustalenia, które z interfejsów API platformy .NET są używane przez projekt, są dostępne na innych platformach platformy .NET.</span><span class="sxs-lookup"><span data-stu-id="404b5-206">The [.NET Portability Analyzer](../../standard/analyzers/portability-analyzer.md) tool is useful for understanding which of the .NET APIs your project uses are available on other .NET platforms.</span></span>

<span data-ttu-id="404b5-207">Narzędzie jest dostępne jako [wtyczka programu Visual Studio](https://marketplace.visualstudio.com/items?itemName=ConnieYau.NETPortabilityAnalyzer), [Narzędzie wiersza polecenia](https://github.com/Microsoft/dotnet-apiport/releases)lub opakowane w [prosty graficzny interfejs użytkownika](https://github.com/Microsoft/dotnet-apiport-ui), co upraszcza jego opcje.</span><span class="sxs-lookup"><span data-stu-id="404b5-207">The tool comes as a [Visual Studio plugin](https://marketplace.visualstudio.com/items?itemName=ConnieYau.NETPortabilityAnalyzer), a [command-line tool](https://github.com/Microsoft/dotnet-apiport/releases), or wrapped in a [simple GUI](https://github.com/Microsoft/dotnet-apiport-ui), which simplifies its options.</span></span> <span data-ttu-id="404b5-208">Więcej informacji o używaniu analizatora przenośności platformy .NET (port interfejsu API) przy użyciu graficznego interfejsu użytkownika w [usłudze portów aplikacji klasycznych na platformie .NET Core](https://devblogs.microsoft.com/dotnet/porting-desktop-apps-to-net-core/) .</span><span class="sxs-lookup"><span data-stu-id="404b5-208">You can read more about using the .NET Portability Analyzer (API Port) using the GUI in the [Porting desktop apps to .NET Core](https://devblogs.microsoft.com/dotnet/porting-desktop-apps-to-net-core/) blog post.</span></span> <span data-ttu-id="404b5-209">Jeśli wolisz używać wiersza polecenia, konieczne są następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="404b5-209">If you prefer to use the command line, the necessary steps are:</span></span>

1. <span data-ttu-id="404b5-210">Pobierz [Analizator przenośności platformy .NET](https://github.com/Microsoft/dotnet-apiport/releases) , jeśli jeszcze go nie masz.</span><span class="sxs-lookup"><span data-stu-id="404b5-210">Download the [.NET Portability Analyzer](https://github.com/Microsoft/dotnet-apiport/releases) if you don't already have it.</span></span>
1. <span data-ttu-id="404b5-211">Upewnij się, że aplikacja .NET Framework, która ma zostać przeniesiena do portu, została pomyślnie skompilowana (dobrym pomysłem przed migracją).</span><span class="sxs-lookup"><span data-stu-id="404b5-211">Make sure the .NET Framework app to be ported builds successfully (this is a good idea prior to migration regardless).</span></span>
1. <span data-ttu-id="404b5-212">Uruchom port interfejsu API z wierszem polecenia podobnym do tego.</span><span class="sxs-lookup"><span data-stu-id="404b5-212">Run API Port with a command line like this.</span></span>

    ```console
    ApiPort.exe analyze -f <PathToBeanTraderBinaries> -r html -r excel -t ".NET Core"
    ```

    <span data-ttu-id="404b5-213">`-f`Argument określa ścieżkę zawierającą pliki binarne do analizy.</span><span class="sxs-lookup"><span data-stu-id="404b5-213">The `-f` argument specifies the path containing the binaries to analyze.</span></span> <span data-ttu-id="404b5-214">`-r`Argument określa format pliku wyjściowego.</span><span class="sxs-lookup"><span data-stu-id="404b5-214">The `-r` argument specifies which output file format you want.</span></span> <span data-ttu-id="404b5-215">`-t`Argument określa platformę .NET, w której ma być analizowane użycie interfejsu API.</span><span class="sxs-lookup"><span data-stu-id="404b5-215">The `-t` argument specifies which .NET platform to analyze API usage against.</span></span> <span data-ttu-id="404b5-216">W takim przypadku potrzebny jest program .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-216">In this case, you want .NET Core.</span></span>

<span data-ttu-id="404b5-217">Po otwarciu raportu HTML w pierwszej sekcji zostanie wystawiona lista wszystkich przeanalizowanych plików binarnych i jaki procent interfejsów API platformy .NET są dostępne na platformie dostosowanej.</span><span class="sxs-lookup"><span data-stu-id="404b5-217">When you open the HTML report, the first section will list all of the analyzed binaries and what percentage of the .NET APIs they use are available on the targeted platform.</span></span> <span data-ttu-id="404b5-218">Wartość procentowa nie jest istotna dla siebie.</span><span class="sxs-lookup"><span data-stu-id="404b5-218">The percentage is not meaningful by itself.</span></span> <span data-ttu-id="404b5-219">To, co jest bardziej przydatne, jest wyświetlenie określonych interfejsów API, które nie istnieją.</span><span class="sxs-lookup"><span data-stu-id="404b5-219">What's more useful is to see the specific APIs that are missing.</span></span> <span data-ttu-id="404b5-220">W tym celu wybierz nazwę zestawu lub przewiń w dół do raportów dla poszczególnych zestawów.</span><span class="sxs-lookup"><span data-stu-id="404b5-220">To do that, either select an assembly name or scroll down to the reports for individual assemblies.</span></span>

<span data-ttu-id="404b5-221">Skup się na zestawach, do których należy kod źródłowy.</span><span class="sxs-lookup"><span data-stu-id="404b5-221">Focus on the assemblies that you own the source code for.</span></span> <span data-ttu-id="404b5-222">Na przykład w raporcie fasola handlowa ApiPort istnieje wiele plików binarnych, ale większość z nich należy do pakietów NuGet.</span><span class="sxs-lookup"><span data-stu-id="404b5-222">In the Bean Trader ApiPort report, for example, there are many binaries listed, but most of them belong to NuGet packages.</span></span> <span data-ttu-id="404b5-223">`Castle.Windsor`pokazuje, że zależy od niektórych interfejsów API System. Web, których brakuje w programie .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-223">`Castle.Windsor` shows that it depends on some System.Web APIs that are missing in .NET Core.</span></span> <span data-ttu-id="404b5-224">Nie jest to problem, ponieważ został wcześniej zweryfikowany, który `Castle.Windsor` obsługuje program .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-224">This isn't a concern because you previously verified that `Castle.Windsor` supports .NET Core.</span></span> <span data-ttu-id="404b5-225">Pakiety NuGet często korzystają z różnych plików binarnych, które mogą być używane z różnymi platformami .NET, więc czy .NET Framework wersja `Castle.Windsor` używa interfejsów API System. Web, czy nie ma znaczenia, o ile pakiet jest również przeznaczony dla .NET Standard lub .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-225">It is common for NuGet packages to have different binaries for use with different .NET platforms, so whether the .NET Framework version of `Castle.Windsor` uses System.Web APIs or not is irrelevant as long as the package also targets .NET Standard or .NET Core (which it does).</span></span>

<span data-ttu-id="404b5-226">Za pomocą przykładu handlowca, jedyną binarną, którą należy wziąć pod uwagę, jest **BeanTraderClient** , a raport pokazuje, że brakuje tylko dwóch interfejsów API platformy .NET: `System.ServiceModel.ClientBase<T>.Close` i `System.ServiceModel.ClientBase<T>.Open` .</span><span class="sxs-lookup"><span data-stu-id="404b5-226">With the Bean Trader sample, the only binary that you need to consider is **BeanTraderClient** and the report shows that only two .NET APIs are missing: `System.ServiceModel.ClientBase<T>.Close` and `System.ServiceModel.ClientBase<T>.Open`.</span></span>

![BeanTraderClient przenośności](./media/convert-project-from-net-framework/portability-report.png)

<span data-ttu-id="404b5-228">Te problemy nie mogą być blokowane, ponieważ interfejsy API klienta WCF są (głównie) obsługiwane przez platformę .NET Core, dlatego dla tych centralnych interfejsów API muszą być dostępne alternatywy.</span><span class="sxs-lookup"><span data-stu-id="404b5-228">These are unlikely to be blocking issues because WCF Client APIs are (mostly) supported on .NET Core, so there must be alternatives available for these central APIs.</span></span> <span data-ttu-id="404b5-229">W rzeczywistości szukasz `System.ServiceModel` obszaru powierzchni .NET Core (przy użyciu <https://apisof.net> ), zobaczysz, że zamiast tego istnieją asynchroniczne alternatywy w programie .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-229">In fact, looking at `System.ServiceModel`'s .NET Core surface area (using <https://apisof.net>), you see that there are async alternatives in .NET Core instead.</span></span>

<span data-ttu-id="404b5-230">W oparciu o ten raport i poprzednią analizę zależności NuGet wygląda na to, że nie powinno być żadnych poważnych problemów z migracją przykładowego handlowca do programu .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-230">Based on this report and the previous NuGet dependency analysis, it looks like there should be no major issues migrating the Bean Trader sample to .NET Core.</span></span> <span data-ttu-id="404b5-231">Jesteś gotowy do następnego kroku, w którym faktycznie rozpocznie się migracja.</span><span class="sxs-lookup"><span data-stu-id="404b5-231">You're ready for the next step in which you'll actually start the migration.</span></span>

## <a name="migrating-the-project-file"></a><span data-ttu-id="404b5-232">Migrowanie pliku projektu</span><span class="sxs-lookup"><span data-stu-id="404b5-232">Migrating the project file</span></span>

<span data-ttu-id="404b5-233">Jeśli aplikacja nie korzysta z nowego [formatu pliku projektu w stylu zestawu SDK](../../core/tools/csproj.md), potrzebny będzie nowy plik projektu do docelowego środowiska .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-233">If your app isn't using the new [SDK-style project file format](../../core/tools/csproj.md), you'll need a new project file to target .NET Core.</span></span> <span data-ttu-id="404b5-234">Można zastąpić istniejący plik CSPROJ lub, jeśli wolisz zachować istniejący projekt bez zmian w bieżącym stanie, można dodać nowy plik CSPROJ przeznaczony dla platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-234">You can replace the existing csproj file or, if you prefer to keep the existing project untouched in its current state, you can add a new csproj file targeting .NET Core.</span></span> <span data-ttu-id="404b5-235">Możesz tworzyć wersje aplikacji dla .NET Framework i .NET Core z pojedynczym plikiem projektu w stylu zestawu SDK z [wieloma](../../standard/library-guidance/cross-platform-targeting.md) obiektami docelowymi (określając wiele `<TargetFrameworks>` obiektów docelowych).</span><span class="sxs-lookup"><span data-stu-id="404b5-235">You can build versions of the app for .NET Framework and .NET Core with a single SDK-style project file with [multi-targeting](../../standard/library-guidance/cross-platform-targeting.md) (specifying multiple `<TargetFrameworks>` targets).</span></span>

<span data-ttu-id="404b5-236">Aby utworzyć nowy plik projektu, można utworzyć nowy projekt WPF w programie Visual Studio lub użyć `dotnet new wpf` polecenia w katalogu tymczasowym do wygenerowania pliku projektu, a następnie skopiować go lub zmienić jego nazwę na poprawną lokalizację.</span><span class="sxs-lookup"><span data-stu-id="404b5-236">To create the new project file, you can create a new WPF project in Visual Studio or use the `dotnet new wpf` command in a temporary directory to generate the project file and then copy/rename it to the correct location.</span></span> <span data-ttu-id="404b5-237">Istnieje również narzędzie stworzone przez społeczność, [CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017), które umożliwia automatyzację niektórych migracji plików projektu.</span><span class="sxs-lookup"><span data-stu-id="404b5-237">There is also a community-created tool, [CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017), that can automate some of the project file migration.</span></span> <span data-ttu-id="404b5-238">Narzędzie jest przydatne, ale nadal wymaga od ludzi sprawdzenia wyników, aby upewnić się, że wszystkie szczegóły migracji są poprawne.</span><span class="sxs-lookup"><span data-stu-id="404b5-238">The tool is helpful but still needs a human to review the results to make sure all the details of the migration are correct.</span></span> <span data-ttu-id="404b5-239">Jednym z obszarów, które narzędzie nie obsługuje optymalnie, jest Migrowanie pakietów NuGet z plików *Packages. config* .</span><span class="sxs-lookup"><span data-stu-id="404b5-239">One particular area that the tool doesn't handle optimally is migrating NuGet packages from *packages.config* files.</span></span> <span data-ttu-id="404b5-240">Jeśli narzędzie działa w pliku projektu, który nadal używa pliku *Packages. config* do odwoływania się do pakietów NuGet, zostanie automatycznie zmigrowane do `<PackageReference>` elementów, ale doda `<PackageReference>` elementy dla *wszystkich* pakietów, a nie tylko najwyższego poziomu.</span><span class="sxs-lookup"><span data-stu-id="404b5-240">If the tool runs on a project file that still uses a *packages.config* file to reference NuGet packages, it will migrate to `<PackageReference>` elements automatically, but will add `<PackageReference>` elements for *all* of the packages instead of just top-level ones.</span></span> <span data-ttu-id="404b5-241">Jeśli przeprowadzono już migrację do `<PackageReference>` elementów z programem Visual Studio (jak zostało to zrobione w tym przykładzie), narzędzie może pomóc w pozostałej części konwersji.</span><span class="sxs-lookup"><span data-stu-id="404b5-241">If you have already migrated to`<PackageReference>` elements with Visual Studio (as you've done in this sample), then the tool can help with the rest of the conversion.</span></span> <span data-ttu-id="404b5-242">Podobnie jak Scott Hanselman zaleca w [swoim wpisie w blogu dotyczącym migrowania plików csproj](https://www.hanselman.com/blog/UpgradingAnExistingNETProjectFilesToTheLeanNewCSPROJFormatFromNETCore.aspx), przenoszenie odbywa się w sposób edukacyjny i zapewni lepsze wyniki, jeśli masz tylko kilka projektów do portów.</span><span class="sxs-lookup"><span data-stu-id="404b5-242">Like Scott Hanselman recommends in [his blog post on migrating csproj files](https://www.hanselman.com/blog/UpgradingAnExistingNETProjectFilesToTheLeanNewCSPROJFormatFromNETCore.aspx), porting by hand is educational and will give better results if you only have a few projects to port.</span></span> <span data-ttu-id="404b5-243">Ale jeśli przenosisz dziesiątki lub setki plików projektu, to narzędzie takie jak [CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017) może być pomoc.</span><span class="sxs-lookup"><span data-stu-id="404b5-243">But if you're porting dozens or hundreds of project files, then a tool like [CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017) can be a help.</span></span>

<span data-ttu-id="404b5-244">Aby utworzyć nowy plik projektu dla przykładowego handlowca, uruchom `dotnet new wpf` w katalogu tymczasowym i Przenieś wygenerowany plik *csproj* do folderu *BeanTraderClient* i zmień jego nazwę na **BeanTraderClient. Core. csproj**.</span><span class="sxs-lookup"><span data-stu-id="404b5-244">To create a new project file for the Bean Trader sample, run `dotnet new wpf` in a temporary directory and move the generated *.csproj* file into the *BeanTraderClient* folder and rename it **BeanTraderClient.Core.csproj**.</span></span>

<span data-ttu-id="404b5-245">Ponieważ nowy format pliku projektu automatycznie zawiera pliki języka C#, pliki *resx* i pliki XAML, które znajdują się w lub w katalogu, plik projektu jest już prawie ukończony.</span><span class="sxs-lookup"><span data-stu-id="404b5-245">Because the new project file format automatically includes C# files, *resx* files, and XAML files that it finds in or under its directory, the project file is already almost complete!</span></span> <span data-ttu-id="404b5-246">Aby zakończyć migrację, Otwórz stare i nowe pliki projektu obok siebie i zapoznaj się ze starym, aby sprawdzić, czy zawarte w nim informacje wymagają migracji.</span><span class="sxs-lookup"><span data-stu-id="404b5-246">To finish the migration, open the old and new project files side-by-side and look through the old one to see if any information it contains needs to be migrated.</span></span> <span data-ttu-id="404b5-247">W przypadku przykładowego podmiotu gospodarczego fasoli następujące elementy powinny zostać skopiowane do nowego projektu:</span><span class="sxs-lookup"><span data-stu-id="404b5-247">In the Bean Trader sample case, the following items should be copied to the new project:</span></span>

- <span data-ttu-id="404b5-248">`<RootNamespace>` `<AssemblyName>` Wszystkie właściwości, i `<ApplicationIcon>` powinny być kopiowane.</span><span class="sxs-lookup"><span data-stu-id="404b5-248">The `<RootNamespace>`, `<AssemblyName>`, and `<ApplicationIcon>` properties should all be copied.</span></span>

- <span data-ttu-id="404b5-249">Należy również dodać `<GenerateAssemblyInfo>false</GenerateAssemblyInfo>` Właściwość do nowego pliku projektu, ponieważ przykład handlowca fasoli zawiera atrybuty na poziomie zestawu (takie jak `[AssemblyTitle]` ) w pliku AssemblyInfo.cs.</span><span class="sxs-lookup"><span data-stu-id="404b5-249">You also need to add a `<GenerateAssemblyInfo>false</GenerateAssemblyInfo>` property to the new project file since the Bean Trader sample includes assembly-level attributes (like `[AssemblyTitle]`) in an AssemblyInfo.cs file.</span></span> <span data-ttu-id="404b5-250">Domyślnie nowe projekty w stylu zestawu SDK automatycznie generują te atrybuty na podstawie właściwości w pliku csproj.</span><span class="sxs-lookup"><span data-stu-id="404b5-250">By default, new SDK-style projects will autogenerate these attributes based on properties in the csproj file.</span></span> <span data-ttu-id="404b5-251">Ponieważ nie chcesz tego zrobić w tym przypadku (automatycznie generowane atrybuty byłyby sprzeczne z tymi z AssemblyInfo.cs), wygenerowane automatycznie atrybuty można wyłączyć za pomocą `<GenerateAssemblyInfo>` .</span><span class="sxs-lookup"><span data-stu-id="404b5-251">Because you don't want that to happen in this case (the autogenerated attributes would conflict with those from AssemblyInfo.cs), you disable the autogenerated attributes with `<GenerateAssemblyInfo>`.</span></span>

- <span data-ttu-id="404b5-252">Mimo że pliki *resx* są automatycznie dołączane jako zasoby osadzone, inne `<Resource>` elementy, takie jak obrazy, nie są obsługiwane.</span><span class="sxs-lookup"><span data-stu-id="404b5-252">Although *resx* files are automatically included as embedded resources, other `<Resource>` items like images are not.</span></span> <span data-ttu-id="404b5-253">Skopiuj `<Resource>` elementy do osadzania plików obrazów i ikon.</span><span class="sxs-lookup"><span data-stu-id="404b5-253">So, copy the `<Resource>` elements for embedding image and icon files.</span></span> <span data-ttu-id="404b5-254">Można uprościć odwołania PNG do pojedynczego wiersza przy użyciu nowego formatu pliku projektu do obsługi wzorców obsługi symboli wieloznacznych: `<Resource Include="**\*.png" />` .</span><span class="sxs-lookup"><span data-stu-id="404b5-254">You can simplify the png references to a single line by using the new project file format's support for globbing patterns: `<Resource Include="**\*.png" />`.</span></span>

- <span data-ttu-id="404b5-255">Podobnie `<None>` elementy są uwzględniane automatycznie, ale nie są domyślnie kopiowane do katalogu wyjściowego.</span><span class="sxs-lookup"><span data-stu-id="404b5-255">Similarly, `<None>` items are included automatically, but they aren't copied to the output directory, by default.</span></span> <span data-ttu-id="404b5-256">Ponieważ projekt handlowca zawiera `<None>` element, który *jest* kopiowany do katalogu wyjściowego (przy użyciu `PreserveNewest` zachowań), należy zaktualizować automatycznie wypełniony `<None>` element dla tego pliku, na przykład.</span><span class="sxs-lookup"><span data-stu-id="404b5-256">Because the Bean Trader project includes a `<None>` item that *is* copied to the output directory (using `PreserveNewest` behaviors), you need to update the automatically populated `<None>` item for that file, like this.</span></span>

  ```xml
  <None Update="BeanTrader.pfx">
    <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </None>
  ```

- <span data-ttu-id="404b5-257">Przykład handlowy fasoli zawiera plik XAML (default. akcent. XAML) jako `Content` (a nie jako `Page` ), ponieważ motywy i akcenty zdefiniowane w tym pliku są ładowane z pliku XAML w środowisku uruchomieniowym, a nie osadzane w samej aplikacji.</span><span class="sxs-lookup"><span data-stu-id="404b5-257">The Bean Trader sample includes a XAML file (Default.Accent.xaml) as `Content` (rather than as a `Page`) because themes and accents defined in this file are loaded from the file's XAML at runtime, rather than being embedded in the app itself.</span></span> <span data-ttu-id="404b5-258">Nowy system projektu automatycznie uwzględnia ten plik jako `<Page>` plik XAML.</span><span class="sxs-lookup"><span data-stu-id="404b5-258">The new project system automatically includes this file as a `<Page>`, however, since it's a XAML file.</span></span> <span data-ttu-id="404b5-259">Należy więc zarówno usunąć plik XAML jako stronę (), jak `<Page Remove="**\Default.Accent.xaml" />` i dodać go jako zawartość.</span><span class="sxs-lookup"><span data-stu-id="404b5-259">So, you need to both remove the XAML file as a page (`<Page Remove="**\Default.Accent.xaml" />`) and add it as content.</span></span>

  ```xml
  <Content Include="Resources\Themes\Default.Accent.xaml">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </Content>
  ```

- <span data-ttu-id="404b5-260">Na koniec Dodaj odwołania NuGet poprzez skopiowanie `<ItemGroup>` elementu z wszystkimi `<PackageReference>` elementami.</span><span class="sxs-lookup"><span data-stu-id="404b5-260">Finally, add NuGet references by copying the `<ItemGroup>` with all the `<PackageReference>` elements.</span></span> <span data-ttu-id="404b5-261">Jeśli nie poprzednio uaktualnione pakiety NuGet do wersji zgodnych z platformą .NET Core, można to zrobić teraz, gdy odwołania do pakietu znajdują się w projekcie specyficznym dla platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-261">If you hadn't previously upgraded the NuGet packages to .NET Core-compatible versions, you could do that now that the package references are in a .NET Core-specific project.</span></span>

<span data-ttu-id="404b5-262">W tym momencie powinno być możliwe dodanie nowego projektu do rozwiązania BeanTrader i otwarcie go w programie Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="404b5-262">At this point, it should be possible to add the new project to the BeanTrader solution and open it in Visual Studio.</span></span> <span data-ttu-id="404b5-263">Projekt powinien wyglądać poprawnie w **Eksplorator rozwiązań**i `dotnet restore BeanTraderClient.Core.csproj` powinien pomyślnie przywrócić pakiety (z dwoma oczekiwanymi ostrzeżeniami związanymi z wersją MahApps. Metro, do której korzystasz .NET Framework).</span><span class="sxs-lookup"><span data-stu-id="404b5-263">The project should look correct in **Solution Explorer**, and `dotnet restore BeanTraderClient.Core.csproj` should successfully restore packages (with two expected warnings related to the MahApps.Metro version you're using targeting .NET Framework).</span></span>

<span data-ttu-id="404b5-264">Chociaż istnieje możliwość równoczesnego zachowania obu plików projektu (i może być pożądane, jeśli chcesz zachować stary projekt dokładnie tak, jak był), komplikuje proces migracji (te dwa projekty będą próbować korzystać z tych samych folderów bin i obj) i zwykle nie jest to konieczne.</span><span class="sxs-lookup"><span data-stu-id="404b5-264">Although it's possible to keep both project files side-by-side (and may even be desirable if you want to keep building the old project exactly as it was), it complicates the migration process (the two projects will try to use the same bin and obj folders) and usually isn't necessary.</span></span> <span data-ttu-id="404b5-265">Jeśli chcesz skompilować zarówno dla programu .NET Core, jak i .NET Framework obiektów docelowych, możesz `<TargetFramework>netcoreapp3.0</TargetFramework>` zamiast tego zastąpić właściwość w nowym pliku projektu `<TargetFrameworks>netcoreapp3.0;net472</TargetFrameworks>` .</span><span class="sxs-lookup"><span data-stu-id="404b5-265">If you want to build for both .NET Core and .NET Framework targets, you can replace the `<TargetFramework>netcoreapp3.0</TargetFramework>` property in the new project file with `<TargetFrameworks>netcoreapp3.0;net472</TargetFrameworks>` instead.</span></span> <span data-ttu-id="404b5-266">W przypadku przykładowego ziarna fasoli Usuń stary plik projektu (BeanTraderClient. csproj), ponieważ nie jest już wymagany.</span><span class="sxs-lookup"><span data-stu-id="404b5-266">For the Bean Trader sample, delete the old project file (BeanTraderClient.csproj) since it's no longer needed.</span></span> <span data-ttu-id="404b5-267">Jeśli wolisz zachować oba pliki projektu, upewnij się, że są one kompilowane do różnych wyjściowych i pośrednich ścieżek wyjściowych.</span><span class="sxs-lookup"><span data-stu-id="404b5-267">If you prefer to keep both project files, be sure to have them build to different output and intermediate output paths.</span></span>

## <a name="fix-build-issues"></a><span data-ttu-id="404b5-268">Rozwiązywanie problemów z kompilacją</span><span class="sxs-lookup"><span data-stu-id="404b5-268">Fix build issues</span></span>

<span data-ttu-id="404b5-269">Trzeci krok procesu przenoszenia pozwala uzyskać projekt do skompilowania.</span><span class="sxs-lookup"><span data-stu-id="404b5-269">The third step of the porting process is getting the project to build.</span></span> <span data-ttu-id="404b5-270">Niektóre aplikacje zostaną już pomyślnie skompilowane, gdy plik projektu zostanie przekonwertowany na projekt w stylu zestawu SDK.</span><span class="sxs-lookup"><span data-stu-id="404b5-270">Some apps will already build successfully once the project file is converted to an SDK-style project.</span></span> <span data-ttu-id="404b5-271">Jeśli tak, to w przypadku Twojej aplikacji gratulacje!</span><span class="sxs-lookup"><span data-stu-id="404b5-271">If that's the case for your app, congratulations!</span></span> <span data-ttu-id="404b5-272">Możesz przejść do kroku 4.</span><span class="sxs-lookup"><span data-stu-id="404b5-272">You can go on to Step 4.</span></span> <span data-ttu-id="404b5-273">Inne aplikacje będą potrzebować niektórych aktualizacji w celu ich skompilowania dla platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-273">Other apps will need some updates to get them building for .NET Core.</span></span> <span data-ttu-id="404b5-274">Jeśli spróbujesz uruchomić `dotnet build` w przykładowym projekcie w języku ziarna fasoli teraz, na przykład (lub skompilować go w programie Visual Studio), wystąpi wiele błędów, ale nastąpi szybkie naprawienie.</span><span class="sxs-lookup"><span data-stu-id="404b5-274">If you try to run `dotnet build` on the Bean Trader sample project now, for example, (or build it in Visual Studio), there will be many errors, but you'll get them fixed quickly.</span></span>

### <a name="systemservicemodel-references-and-microsoftwindowscompatibility"></a><span data-ttu-id="404b5-275">Odwołania do elementu System. ServiceModel i Microsoft. Windows. Compatibility</span><span class="sxs-lookup"><span data-stu-id="404b5-275">System.ServiceModel references and Microsoft.Windows.Compatibility</span></span>

<span data-ttu-id="404b5-276">W przypadku wszystkich interfejsów API, które są dostępne dla platformy .NET Core, ale nie są automatycznie dołączane do pakietu aplikacji .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-276">A common source of errors is missing references for APIs that are available for .NET Core but not automatically included in the .NET Core app metapackage.</span></span> <span data-ttu-id="404b5-277">Aby rozwiązać ten konieczność, należy odwołać się do `Microsoft.Windows.Compatibility` pakietu.</span><span class="sxs-lookup"><span data-stu-id="404b5-277">To address this, you should reference the `Microsoft.Windows.Compatibility` package.</span></span> <span data-ttu-id="404b5-278">Pakiet zgodności zawiera szeroki zestaw interfejsów API, które są wspólne w aplikacjach klasycznych systemu Windows, takich jak klient WCF, usługi katalogowe, rejestr, konfiguracja, interfejsy API list ACL i inne.</span><span class="sxs-lookup"><span data-stu-id="404b5-278">The compatibility package includes a broad set of APIs that are common in Windows desktop apps, such as WCF client, directory services, registry, configuration, ACLs APIs, and more.</span></span>

<span data-ttu-id="404b5-279">Z próbką handlowca fasoli większość błędów kompilacji wynika z brakujących <xref:System.ServiceModel> typów.</span><span class="sxs-lookup"><span data-stu-id="404b5-279">With the Bean Trader sample, the majority of the build errors are due to missing <xref:System.ServiceModel> types.</span></span> <span data-ttu-id="404b5-280">Można je rozwiązać, odwołując się do niezbędnych pakietów NuGet WCF.</span><span class="sxs-lookup"><span data-stu-id="404b5-280">These could be addressed by referencing the necessary WCF NuGet packages.</span></span> <span data-ttu-id="404b5-281">Interfejsy API klienta programu WCF znajdują się wśród tych, które znajdują się w `Microsoft.Windows.Compatibility` pakiecie, chociaż odwołujące się do pakietu zgodności jest jeszcze lepszym rozwiązaniem (ponieważ dotyczy to również wszystkich problemów związanych z interfejsami API, a także rozwiązań dotyczących problemów z usługą WCF, które udostępnia pakiet zgodności).</span><span class="sxs-lookup"><span data-stu-id="404b5-281">WCF client APIs are among those present in the `Microsoft.Windows.Compatibility` package, though, so referencing the compatibility package is an even better solution (since it also addresses any issues related to APIs as well as solutions to the WCF issues that the compatibility package makes available).</span></span> <span data-ttu-id="404b5-282">`Microsoft.Windows.Compatibility`Pakiet jest pomocny w większości scenariuszy obejmujących platformy .NET Core 3,0 i WinForms.</span><span class="sxs-lookup"><span data-stu-id="404b5-282">The `Microsoft.Windows.Compatibility` package helps in most .NET Core 3.0 WPF and WinForms porting scenarios.</span></span> <span data-ttu-id="404b5-283">Po dodaniu odwołania do programu NuGet do programu `Microsoft.Windows.Compatibility` pozostanie tylko jeden błąd kompilacji!</span><span class="sxs-lookup"><span data-stu-id="404b5-283">After adding the NuGet reference to `Microsoft.Windows.Compatibility`, only one build error remains!</span></span>

### <a name="cleaning-up-unused-files"></a><span data-ttu-id="404b5-284">Czyszczenie nieużywanych plików</span><span class="sxs-lookup"><span data-stu-id="404b5-284">Cleaning up unused files</span></span>

<span data-ttu-id="404b5-285">Jeden z typów problemów migracji, które są często odnoszą się do plików języka C# i XAML, które nie zostały wcześniej dołączone do kompilacji, które zostały pobrane przez nowe projekty w stylu zestawu SDK, które zawierają automatycznie *wszystkie* źródła.</span><span class="sxs-lookup"><span data-stu-id="404b5-285">One type of migration issue that comes up often relates to C# and XAML files that weren't previously included in the build getting picked up by the new SDK-style projects that include *all* source automatically.</span></span>

<span data-ttu-id="404b5-286">Następny błąd kompilacji widoczny w przykładzie handlowca w języku fasoli odnosi się do nieprawidłowej implementacji interfejsu w *OldUnusedViewModel.cs*.</span><span class="sxs-lookup"><span data-stu-id="404b5-286">The next build error you see in the Bean Trader sample refers to a bad interface implementation in *OldUnusedViewModel.cs*.</span></span> <span data-ttu-id="404b5-287">Nazwa pliku jest wskazówką, ale po inspekcji znajdziesz ten plik źródłowy jest nieprawidłowy.</span><span class="sxs-lookup"><span data-stu-id="404b5-287">The file name is a hint, but on inspection, you'll find that this source file is incorrect.</span></span> <span data-ttu-id="404b5-288">Nie powodowały one wcześniej problemów, ponieważ nie zostały uwzględnione w oryginalnym projekcie .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="404b5-288">It didn't cause issues previously because it wasn't included in the original .NET Framework project.</span></span> <span data-ttu-id="404b5-289">Pliki źródłowe, które znajdowały się na dysku, ale nie zostały uwzględnione w starym *csproj* , są teraz automatycznie dostępne.</span><span class="sxs-lookup"><span data-stu-id="404b5-289">Source files that were present on disk but not included in the old *csproj* are included automatically now.</span></span>

<span data-ttu-id="404b5-290">W przypadku jednorazowych problemów, takich jak ten, można łatwo porównać z poprzednią *csproją* , aby potwierdzić, że plik nie jest wymagany, a następnie `<Compile Remove="" />` lub, jeśli plik źródłowy nie jest już wymagany, usuń go.</span><span class="sxs-lookup"><span data-stu-id="404b5-290">For one-off issues like this, it's easy to compare to the previous *csproj* to confirm that the file isn't needed, and then either `<Compile Remove="" />` it or, if the source file isn't needed anywhere anymore, delete it.</span></span> <span data-ttu-id="404b5-291">W takim przypadku można bezpiecznie usunąć *OldUnusedViewModel.cs*.</span><span class="sxs-lookup"><span data-stu-id="404b5-291">In this case, it's safe to just delete *OldUnusedViewModel.cs*.</span></span>

<span data-ttu-id="404b5-292">Jeśli masz wiele plików źródłowych, które należy wykluczyć w ten sposób, możesz wyłączyć funkcję dołączania plików języka C#, ustawiając `<EnableDefaultCompileItems>` Właściwość na false w pliku projektu.</span><span class="sxs-lookup"><span data-stu-id="404b5-292">If you have many source files that would need to be excluded this way, you can disable auto-inclusion of C# files by setting the `<EnableDefaultCompileItems>` property to false in the project file.</span></span> <span data-ttu-id="404b5-293">Następnie można skopiować `<Compile Include>` elementy ze starego pliku projektu do nowego, aby utworzyć tylko źródła przeznaczone do uwzględnienia.</span><span class="sxs-lookup"><span data-stu-id="404b5-293">Then, you can copy `<Compile Include>` items from the old project file to the new one in order to only build sources you intended to include.</span></span> <span data-ttu-id="404b5-294">Podobnie, `<EnableDefaultPageItems>` można użyć, aby wyłączyć Autouzupełnianie stron XAML i `<EnableDefaultItems>` kontrolować obydwa właściwości.</span><span class="sxs-lookup"><span data-stu-id="404b5-294">Similarly, `<EnableDefaultPageItems>` can be used to turn off auto-inclusion of XAML pages and `<EnableDefaultItems>` can control both with a single property.</span></span>

### <a name="a-brief-aside-on-multi-pass-compilers"></a><span data-ttu-id="404b5-295">W odniesieniu do kompilatora wieloprzebiegowego</span><span class="sxs-lookup"><span data-stu-id="404b5-295">A brief aside on multi-pass compilers</span></span>

<span data-ttu-id="404b5-296">Po usunięciu błędnego pliku z przykładu handlowca w postaci ziarna można ponownie skompilować i uzyskać cztery błędy.</span><span class="sxs-lookup"><span data-stu-id="404b5-296">After removing the offending file from the Bean Trader sample, you can re-build and will get four errors.</span></span> <span data-ttu-id="404b5-297">Nie masz tego wcześniej?</span><span class="sxs-lookup"><span data-stu-id="404b5-297">Didn't you have one before?</span></span> <span data-ttu-id="404b5-298">Dlaczego została wystawiona liczba błędów?</span><span class="sxs-lookup"><span data-stu-id="404b5-298">Why did the number of errors go up?</span></span> <span data-ttu-id="404b5-299">Kompilator języka C# jest [kompilatorem wielodostępnym](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes).</span><span class="sxs-lookup"><span data-stu-id="404b5-299">The C# compiler is a [multi-pass compiler](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes).</span></span> <span data-ttu-id="404b5-300">Oznacza to, że poszczególne pliki źródłowe przechodzą dwa razy.</span><span class="sxs-lookup"><span data-stu-id="404b5-300">This means that it goes through each source file twice.</span></span> <span data-ttu-id="404b5-301">Najpierw kompilator analizuje metadane i deklaracje w każdym pliku źródłowym i identyfikuje wszelkie problemy na poziomie deklaracji.</span><span class="sxs-lookup"><span data-stu-id="404b5-301">First, the compiler just looks at metadata and declarations in each source file and identifies any declaration-level problems.</span></span> <span data-ttu-id="404b5-302">Są to błędy, które zostały naprawione.</span><span class="sxs-lookup"><span data-stu-id="404b5-302">Those are the errors you've fixed.</span></span> <span data-ttu-id="404b5-303">Następnie ponownie przechodzi przez kod w celu skompilowania źródła C# do języka IL; są to drugi zestaw błędów, które są teraz wyświetlane.</span><span class="sxs-lookup"><span data-stu-id="404b5-303">Then it goes through the code again to build the C# source into IL; those are the second set of errors that you're seeing now.</span></span>

> [!NOTE]
> <span data-ttu-id="404b5-304">Kompilator języka C# wykonuje [więcej niż dwa przebiegi](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes), ale wynikiem jest to, że błędy kompilatora dla dużych zmian kodu, takie jak ta, zachodzą na dwie fale.</span><span class="sxs-lookup"><span data-stu-id="404b5-304">The C# compiler does [more than just two passes](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes), but the end result is that compiler errors for large code changes like this tend to come in two waves.</span></span>

### <a name="third-party-dependency-fixes-castlewindsor"></a><span data-ttu-id="404b5-305">Poprawki zależności innych firm (Castle. Windsor)</span><span class="sxs-lookup"><span data-stu-id="404b5-305">Third-party dependency fixes (Castle.Windsor)</span></span>

<span data-ttu-id="404b5-306">Inną klasą problemu, który występuje w niektórych scenariuszach migracji, jest różnice interfejsu API między wersjami zależności .NET Framework i .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-306">Another class of issue that comes up in some migration scenarios is API differences between .NET Framework and .NET Core versions of dependencies.</span></span> <span data-ttu-id="404b5-307">Nawet jeśli pakiet NuGet jest przeznaczony dla .NET Framework i .NET Standard lub .NET Core, mogą istnieć różne biblioteki do użycia z różnymi obiektami docelowymi platformy .NET.</span><span class="sxs-lookup"><span data-stu-id="404b5-307">Even if a NuGet package targets both .NET Framework and .NET Standard or .NET Core, there may be different libraries for use with different .NET targets.</span></span> <span data-ttu-id="404b5-308">Dzięki temu pakiety obsługują wiele różnych platform .NET, które mogą wymagać innych implementacji.</span><span class="sxs-lookup"><span data-stu-id="404b5-308">This allows the packages to support many different .NET platforms, which may require different implementations.</span></span> <span data-ttu-id="404b5-309">Oznacza to również, że w bibliotekach może być małych różnic interfejsu API w przypadku różnych platform .NET.</span><span class="sxs-lookup"><span data-stu-id="404b5-309">It also means that there may be small API differences in the libraries when targeting different .NET platforms.</span></span>

<span data-ttu-id="404b5-310">Następny zestaw błędów, które zobaczysz w próbce handlowca fasoli, jest związany z `Castle.Windsor` interfejsami API.</span><span class="sxs-lookup"><span data-stu-id="404b5-310">The next set of errors you'll see in the Bean Trader sample are related to `Castle.Windsor` APIs.</span></span> <span data-ttu-id="404b5-311">Projekt platformy .NET Core fasoli korzysta z tej samej wersji programu `Castle.Windsor` co projekt .NET Framework (4.1.1), ale implementacje tych dwóch platform są nieco inne.</span><span class="sxs-lookup"><span data-stu-id="404b5-311">The .NET Core Bean Trader project uses the same version of `Castle.Windsor` as the .NET Framework-targeted project (4.1.1), but the implementations for those two platforms are slightly different.</span></span>

<span data-ttu-id="404b5-312">W takim przypadku zobaczysz następujące problemy, które muszą zostać naprawione:</span><span class="sxs-lookup"><span data-stu-id="404b5-312">In this case, you see the following issues that need to be fixed:</span></span>

1. <span data-ttu-id="404b5-313">`Castle.MicroKernel.Registration.Classes.FromThisAssembly`nie jest dostępny w programie .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-313">`Castle.MicroKernel.Registration.Classes.FromThisAssembly` isn't available on .NET Core.</span></span> <span data-ttu-id="404b5-314">Istnieje jednak podobny dostępny interfejs API `Classes.FromAssemblyContaining` , dlatego można zastąpić oba zastosowania `Classes.FromThisAssembly()` z wywołaniami do `Classes.FromAssemblyContaining(t)` , gdzie `t` jest typem wywołującym wywołanie.</span><span class="sxs-lookup"><span data-stu-id="404b5-314">There is, however, the similar API `Classes.FromAssemblyContaining` available, so we can replace both uses of `Classes.FromThisAssembly()` with calls to `Classes.FromAssemblyContaining(t)`, where `t` is the type making the call.</span></span>
1. <span data-ttu-id="404b5-315">Podobnie, w *Bootstrapper.cs*, `Castle.Windsor.Installer.FromAssembly` . Jest to niedostępne w programie .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-315">Similarly, in *Bootstrapper.cs*, `Castle.Windsor.Installer.FromAssembly`.This is unavailable on .NET Core.</span></span> <span data-ttu-id="404b5-316">Zamiast tego to wywołanie może zostać zastąpione przez `FromAssembly.Containing(typeof(Bootstrapper))` .</span><span class="sxs-lookup"><span data-stu-id="404b5-316">Instead, that call can be replaced with `FromAssembly.Containing(typeof(Bootstrapper))`.</span></span>

### <a name="updating-wcf-client-usage"></a><span data-ttu-id="404b5-317">Aktualizowanie użycia klienta WCF</span><span class="sxs-lookup"><span data-stu-id="404b5-317">Updating WCF client usage</span></span>

<span data-ttu-id="404b5-318">Po naprawieniu `Castle.Windsor` różnic, ostatni pozostały błąd kompilacji w projekcie handlowca programu .NET Core to, że `BeanTraderServiceClient` (który pochodzi z `DuplexClientBase` ) nie ma `Open` metody.</span><span class="sxs-lookup"><span data-stu-id="404b5-318">Having fixed the `Castle.Windsor` differences, the last remaining build error in the .NET Core Bean Trader project is that `BeanTraderServiceClient` (which derives from `DuplexClientBase`) doesn't have an `Open` method.</span></span> <span data-ttu-id="404b5-319">Nie jest to zaskakujące, ponieważ jest to interfejs API, który został wyróżniony przez analizator przenośności platformy .NET na początku tego procesu migracji.</span><span class="sxs-lookup"><span data-stu-id="404b5-319">This isn't surprising since this is an API that was highlighted by the .NET Portability Analyzer at the beginning of this migration process.</span></span> <span data-ttu-id="404b5-320">Spojrzenie na `BeanTraderServiceClient` to, co jest bardziej związane z większą ilością problemu.</span><span class="sxs-lookup"><span data-stu-id="404b5-320">Looking at `BeanTraderServiceClient` draws our attention to a larger issue, though.</span></span> <span data-ttu-id="404b5-321">Ten klient WCF został automatycznie wygenerowany przez narzędzie [Svcutil. exe](../../framework/wcf/servicemodel-metadata-utility-tool-svcutil-exe.md) .</span><span class="sxs-lookup"><span data-stu-id="404b5-321">This WCF client was autogenerated by the [Svcutil.exe](../../framework/wcf/servicemodel-metadata-utility-tool-svcutil-exe.md) tool.</span></span>

<span data-ttu-id="404b5-322">**Klienci WCF wygenerowane przez Svcutil są przeznaczone do użycia w .NET Framework.**</span><span class="sxs-lookup"><span data-stu-id="404b5-322">**WCF clients generated by Svcutil are meant for use on .NET Framework.**</span></span>

<span data-ttu-id="404b5-323">Rozwiązania, które korzystają z klientów WCF generowanych przez Svcutil, muszą ponownie wygenerować klientów zgodnych z .NET Standardymi do użycia z platformą .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-323">Solutions that use svcutil-generated WCF clients will need to regenerate .NET Standard-compatible clients for use with .NET Core.</span></span> <span data-ttu-id="404b5-324">Jednym z głównych powodów, dla których stara klienci nie będzie działały, jest to, że zależą od konfiguracji aplikacji w celu zdefiniowania powiązań i punktów końcowych usługi WCF.</span><span class="sxs-lookup"><span data-stu-id="404b5-324">One of the main reasons the old clients won't work is that they depend on app configuration for defining WCF bindings and endpoints.</span></span> <span data-ttu-id="404b5-325">Ponieważ .NET Standard interfejsów API WCF mogą współdziałać na wielu platformach (w których nie są dostępne interfejsy API System. Configuration), klienci programu WCF dla scenariuszy .NET Core i .NET Standard muszą definiować powiązania i punkty końcowe zamiast w konfiguracji.</span><span class="sxs-lookup"><span data-stu-id="404b5-325">Because .NET Standard WCF APIs can work cross-platform (where System.Configuration APIs aren't available), WCF clients for .NET Core and .NET Standard scenarios must define bindings and endpoints programmatically instead of in configuration.</span></span>

<span data-ttu-id="404b5-326">W rzeczywistości każde użycie klienta programu WCF zależne od `<system.serviceModel>` sekcji App. config (niezależnie od tego, czy utworzone za pomocą Svcutil lub ręcznie) będzie musiało zostać zmienione w celu pracy z platformą .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-326">In fact, any WCF client usage that depends on the `<system.serviceModel>` app.config section (whether created with Svcutil or manually) will need to be changed to work on .NET Core.</span></span>

<span data-ttu-id="404b5-327">Istnieją dwa sposoby automatycznego generowania klientów WCF zgodnych z .NET Standard:</span><span class="sxs-lookup"><span data-stu-id="404b5-327">There are two ways to automatically generate .NET Standard-compatible WCF clients:</span></span>

- <span data-ttu-id="404b5-328">`dotnet-svcutil`Narzędzie jest narzędziem platformy .NET, które generuje klientów WCF w taki sposób, który jest podobny do tego, jak Svcutil działał wcześniej.</span><span class="sxs-lookup"><span data-stu-id="404b5-328">The `dotnet-svcutil` tool is a .NET tool that generates WCF clients in a way that is similar to how Svcutil worked previously.</span></span>
- <span data-ttu-id="404b5-329">Program Visual Studio może generować klientów WCF przy użyciu opcji [referencyjnej usługi sieci Web programu WCF](../../core/additional-tools/wcf-web-service-reference-guide.md) funkcji usługi połączone.</span><span class="sxs-lookup"><span data-stu-id="404b5-329">Visual Studio can generate WCF clients using the [WCF Web Service Reference](../../core/additional-tools/wcf-web-service-reference-guide.md) option of its Connected Services feature.</span></span>

<span data-ttu-id="404b5-330">Jedno z tych rozwiązań działa prawidłowo.</span><span class="sxs-lookup"><span data-stu-id="404b5-330">Either approach works well.</span></span> <span data-ttu-id="404b5-331">Alternatywnie możesz napisać kod klienta WCF samodzielnie.</span><span class="sxs-lookup"><span data-stu-id="404b5-331">Alternatively, of course, you could write the WCF client code yourself.</span></span> <span data-ttu-id="404b5-332">W tym przykładzie wybrano opcję korzystania z funkcji usługi połączonej programu Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="404b5-332">For this sample, I chose to use the Visual Studio Connected Service feature.</span></span> <span data-ttu-id="404b5-333">Aby to zrobić, kliknij prawym przyciskiem myszy projekt *BeanTraderClient. Core* w Eksploratorze rozwiązań programu Visual Studio i wybierz polecenie **Dodaj**  >  **podłączoną usługę**.</span><span class="sxs-lookup"><span data-stu-id="404b5-333">To do that, right-click on the *BeanTraderClient.Core* project in Visual Studio's solution explorer and select **Add** > **Connected Service**.</span></span> <span data-ttu-id="404b5-334">Następnie wybierz dostawcę odwołań usługi sieci Web programu WCF.</span><span class="sxs-lookup"><span data-stu-id="404b5-334">Next, choose the WCF Web Service Reference Provider.</span></span> <span data-ttu-id="404b5-335">Spowoduje to wyświetlenie okna dialogowego, w którym można określić adres usługi sieci Web handlowca zaplecza ( `localhost:8080` Jeśli serwer jest uruchomiony lokalnie), a przestrzeń nazw, która wygenerowała typy powinna być używana (**BeanTrader. Service**, na przykład).</span><span class="sxs-lookup"><span data-stu-id="404b5-335">This will bring up a dialog where you can specify the address of the backend Bean Trader web service (`localhost:8080` if you are running the server locally) and the namespace that generated types should use (**BeanTrader.Service**, for example).</span></span>

![Okno dialogowe usługi połączonej z usługą sieci Web WCF](./media/convert-project-from-net-framework/connected-service-dialog.png)

<span data-ttu-id="404b5-337">Po wybraniu przycisku **Zakończ** do projektu zostanie dodany nowy węzeł usługi połączone, a w tym węźle zostanie dodany plik Reference.cs zawierający nowy klient WCF .NET Standard na potrzeby uzyskiwania dostępu do usługi handlowca.</span><span class="sxs-lookup"><span data-stu-id="404b5-337">After you select the **Finish** button, a new Connected Services node is added to the project and a Reference.cs file is added under that node containing the new .NET Standard WCF client for accessing the Bean Trader service.</span></span> <span data-ttu-id="404b5-338">Jeśli przeszukasz `GetEndpointAddress` metody lub `GetBindingForEndpoint` w tym pliku, zobaczysz, że powiązania i punkty końcowe są teraz generowane programowo (a nie za pomocą konfiguracji aplikacji).</span><span class="sxs-lookup"><span data-stu-id="404b5-338">If you look at the `GetEndpointAddress` or `GetBindingForEndpoint` methods in that file, you'll see that bindings and endpoints are now generated programmatically (instead of via app config).</span></span> <span data-ttu-id="404b5-339">Funkcja "Dodaj połączone usługi" może także dodawać odwołania do niektórych pakietów system. ServiceModel w pliku projektu, które nie są potrzebne, ponieważ wszystkie niezbędne pakiety WCF są dołączone do firmy Microsoft. Windows. Compatibility.</span><span class="sxs-lookup"><span data-stu-id="404b5-339">The 'Add Connected Services' feature may also add references to some System.ServiceModel packages in the project file, which aren't needed since all necessary WCF packages are included via Microsoft.Windows.Compatibility.</span></span> <span data-ttu-id="404b5-340">Sprawdź csproj, aby sprawdzić, czy dodaliśmy dodatkowe elementy system. ServiceModel `<PackageReference>` , i jeśli tak, usuń je.</span><span class="sxs-lookup"><span data-stu-id="404b5-340">Check the csproj to see if any extra System.ServiceModel `<PackageReference>` items have been added, and if so, remove them.</span></span>

<span data-ttu-id="404b5-341">Nasz projekt ma teraz nowe klasy klienta WCF (w *Reference.cs*), ale również nadal ma stare (w BeanTrader.cs).</span><span class="sxs-lookup"><span data-stu-id="404b5-341">Our project has new WCF client classes now (in *Reference.cs*), but it also still has the old ones (in BeanTrader.cs).</span></span> <span data-ttu-id="404b5-342">W tym momencie dostępne są dwie opcje:</span><span class="sxs-lookup"><span data-stu-id="404b5-342">There are two options at this point:</span></span>

- <span data-ttu-id="404b5-343">Jeśli chcesz mieć możliwość kompilowania oryginalnego projektu .NET Framework (wraz z nowym celem platformy .NET Core), możesz użyć `<Compile Remove="BeanTrader.cs" />` elementu w pliku csproj projektu .NET Core, aby .NET Framework i .NET Core wersji aplikacji używały różnych klientów programu WCF.</span><span class="sxs-lookup"><span data-stu-id="404b5-343">If you want to be able to build the original .NET Framework project (alongside the new .NET Core-targeted one), you can use a `<Compile Remove="BeanTrader.cs" />` item in the .NET Core project's csproj file so that the .NET Framework and .NET Core versions of the app use different WCF clients.</span></span> <span data-ttu-id="404b5-344">Jest to zaletą pozostawienia istniejącego projektu .NET Framework bez zmian. jednak ma wadą, że kod korzystający z wygenerowanych klientów WCF może być nieznacznie różny w przypadku platformy .NET Core niż w projekcie .NET Framework, więc prawdopodobnie trzeba będzie użyć `#if` dyrektyw w celu warunkowego skompilowania niektórych zastosowań klienta WCF (na przykład w przypadku tworzenia klientów) w taki sposób, aby działały w sposób oparty na platformie .NET Core i w inny sposób po skompilowaniu dla .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="404b5-344">This has the advantage of leaving the existing .NET Framework project unchanged, but has the disadvantage that code using the generated WCF clients may need to be slightly different in the .NET Core case than it was in the .NET Framework project, so you'll likely need to use `#if` directives to conditionally compile some WCF client usage (creating clients, for example) to work one way when built for .NET Core and another way when built for .NET Framework.</span></span>

- <span data-ttu-id="404b5-345">Jeśli z drugiej strony, niektóre zmiany kodu w istniejącym .NET Framework projekcie są akceptowalne, można usunąć wszystkie razem *BeanTrader.cs* .</span><span class="sxs-lookup"><span data-stu-id="404b5-345">If, on the other hand, some code churn in the existing .NET Framework project is acceptable, you can remove *BeanTrader.cs* all together.</span></span> <span data-ttu-id="404b5-346">Ponieważ nowy klient WCF został skompilowany dla .NET Standard, będzie działał zarówno w scenariuszach .NET Core, jak i .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="404b5-346">Because the new WCF client is built for .NET Standard, it will work in both .NET Core and .NET Framework scenarios.</span></span> <span data-ttu-id="404b5-347">Jeśli tworzysz dla .NET Framework oprócz platformy .NET Core (przez wiele obiektów docelowych lub mających dwa pliki csproj), możesz użyć tego nowego pliku *Reference.cs* dla obu celów.</span><span class="sxs-lookup"><span data-stu-id="404b5-347">If you are building for .NET Framework in addition to .NET Core (either by multi-targeting or by having two csproj files), you can use this new *Reference.cs* file for both targets.</span></span> <span data-ttu-id="404b5-348">Takie podejście ma zaletę, że kod nie musi bifurcate do obsługi dwóch różnych klientów WCF. ten sam kod będzie używany wszędzie.</span><span class="sxs-lookup"><span data-stu-id="404b5-348">This approach has the advantage that the code won't need to bifurcate to support two different WCF clients; the same code will be used everywhere.</span></span> <span data-ttu-id="404b5-349">Wadą jest to, że wymaga zmiany (najprawdopodobniej stabilnej) .NET Framework projektu.</span><span class="sxs-lookup"><span data-stu-id="404b5-349">The drawback is that it involves changing the (presumably stable) .NET Framework project.</span></span>

<span data-ttu-id="404b5-350">W przypadku przykładu handlowca w postaci ziarna można wprowadzać niewielkie zmiany w oryginalnym projekcie, jeśli ułatwia to migrację, więc wykonaj następujące kroki, aby uzgodnić użycie klienta WCF:</span><span class="sxs-lookup"><span data-stu-id="404b5-350">In the case of the Bean Trader sample, you can make small changes to the original project if it makes migration easier, so follow these steps to reconcile WCF client usage:</span></span>

01. <span data-ttu-id="404b5-351">Dodaj nowy plik Reference.cs do projektu .NET Framework *BeanTraderClient. csproj* za pomocą menu kontekstowego "Dodaj istniejący element" z Eksploratora rozwiązań.</span><span class="sxs-lookup"><span data-stu-id="404b5-351">Add the new Reference.cs file to the .NET Framework *BeanTraderClient.csproj* project using the 'Add existing item' context menu from the solution explorer.</span></span> <span data-ttu-id="404b5-352">Pamiętaj, aby dodać "jako link", aby ten sam plik był używany przez oba projekty (w przeciwieństwie do kopiowania pliku C#).</span><span class="sxs-lookup"><span data-stu-id="404b5-352">Be sure to add 'as link' so that the same file is used by both projects (as opposed to copying the C# file).</span></span> <span data-ttu-id="404b5-353">W przypadku kompilowania programów .NET Core i .NET Framework przy użyciu jednego csproj (z użyciem wieloelementowego) ten krok nie jest konieczny.</span><span class="sxs-lookup"><span data-stu-id="404b5-353">If you are building for both .NET Core and .NET Framework with a single csproj (using multi-targeting) then this step isn't necessary.</span></span>

01. <span data-ttu-id="404b5-354">Usuń *BeanTrader.cs*.</span><span class="sxs-lookup"><span data-stu-id="404b5-354">Delete *BeanTrader.cs*.</span></span>

01. <span data-ttu-id="404b5-355">Nowy klient WCF jest podobny do starego, ale liczba przestrzeni nazw w wygenerowanym kodzie jest różna.</span><span class="sxs-lookup"><span data-stu-id="404b5-355">The new WCF client is similar to the old one, but a number of namespaces in the generated code are different.</span></span> <span data-ttu-id="404b5-356">W związku z tym konieczne jest zaktualizowanie projektu tak, aby typy klientów WCF były używane z BeanTrader. Service (lub dowolnej wybranej nazwy przestrzeni nazw) zamiast BeanTrader. model lub bez przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="404b5-356">Because of this, it is necessary to update the project so that WCF client types are used from BeanTrader.Service (or whatever namespace name you chose) instead of BeanTrader.Model or without a namespace.</span></span> <span data-ttu-id="404b5-357">Kompilowanie *BeanTraderClient. Core. csproj* pomoże ustalić, gdzie te zmiany muszą zostać wykonane.</span><span class="sxs-lookup"><span data-stu-id="404b5-357">Building *BeanTraderClient.Core.csproj* will help to identify where these changes need to be made.</span></span> <span data-ttu-id="404b5-358">Poprawki będą wymagały zarówno w języku C#, jak i w plikach źródłowych XAML.</span><span class="sxs-lookup"><span data-stu-id="404b5-358">Fixes will be needed both in C# and in XAML source files.</span></span>

01. <span data-ttu-id="404b5-359">Na koniec wykryjesz, że wystąpił błąd w *BeanTraderServiceClientFactory.cs* , ponieważ dostępne są konstruktory dla tego `BeanTraderServiceClient` typu.</span><span class="sxs-lookup"><span data-stu-id="404b5-359">Finally, you'll discover that there is an error in *BeanTraderServiceClientFactory.cs* because the available constructors for the `BeanTraderServiceClient` type have changed.</span></span> <span data-ttu-id="404b5-360">Jest to możliwe, aby podać `InstanceContext` argument (który został utworzony przy użyciu `CallbackHandler` z `Castle.Windsor` kontenera IOC).</span><span class="sxs-lookup"><span data-stu-id="404b5-360">It used to be possible to supply an `InstanceContext` argument (which was created using a `CallbackHandler` from the `Castle.Windsor` IoC container).</span></span> <span data-ttu-id="404b5-361">Nowe konstruktory tworzą nowe `CallbackHandler` s.</span><span class="sxs-lookup"><span data-stu-id="404b5-361">The new constructors create new `CallbackHandler`s.</span></span> <span data-ttu-id="404b5-362">Istnieją jednak konstruktory w `BeanTraderServiceClient` typie podstawowym, które pasują do tego, czego potrzebujesz.</span><span class="sxs-lookup"><span data-stu-id="404b5-362">There are, however, constructors in `BeanTraderServiceClient`'s base type that match what you want.</span></span> <span data-ttu-id="404b5-363">Ponieważ wygenerowany automatycznie kod klienta WCF istnieje w klasach częściowych, można go łatwo zwiększyć.</span><span class="sxs-lookup"><span data-stu-id="404b5-363">Since the autogenerated WCF client code all exists in partial classes, you can easily extend it.</span></span> <span data-ttu-id="404b5-364">W tym celu Utwórz nowy plik o nazwie *BeanTraderServiceClient.cs* , a następnie Utwórz klasę częściową o tej samej nazwie (przy użyciu przestrzeni nazw BeanTrader. Service).</span><span class="sxs-lookup"><span data-stu-id="404b5-364">To do this, create a new file called *BeanTraderServiceClient.cs* and then create a partial class with that same name (using the BeanTrader.Service namespace).</span></span> <span data-ttu-id="404b5-365">Następnie Dodaj jeden Konstruktor do typu częściowego, jak pokazano poniżej.</span><span class="sxs-lookup"><span data-stu-id="404b5-365">Then, add one constructor to the partial type as shown here.</span></span>

    ```csharp
    public BeanTraderServiceClient(System.ServiceModel.InstanceContext callbackInstance) :
        base(callbackInstance, EndpointConfiguration.NetTcpBinding_BeanTraderService)
            { }
    ```

<span data-ttu-id="404b5-366">Po wprowadzeniu tych zmian przykładowa wartość handlowa fasoli będzie używać nowego klienta WCF zgodnego z .NET Standard i można wprowadzić ostateczną poprawkę zmiany `Open` wywołania w *TradingService.cs* `await OpenAsync` .</span><span class="sxs-lookup"><span data-stu-id="404b5-366">With those changes made, the Bean Trader sample will now be using a new .NET Standard-compatible WCF client and you can make the final fix of changing the `Open` call in *TradingService.cs* to use `await OpenAsync` instead.</span></span>

<span data-ttu-id="404b5-367">Dzięki rozwiązanym problemom z programem WCF wersja platformy .NET Core klasy handlowca jest teraz kompiluje się prawidłowo!</span><span class="sxs-lookup"><span data-stu-id="404b5-367">With the WCF issues addressed, the .NET Core version of the Bean Trader sample now builds cleanly!</span></span>

## <a name="runtime-testing"></a><span data-ttu-id="404b5-368">Testowanie środowiska uruchomieniowego</span><span class="sxs-lookup"><span data-stu-id="404b5-368">Runtime testing</span></span>

<span data-ttu-id="404b5-369">Można łatwo zapomnieć, że prace migracji nie są wykonywane, gdy tylko projekt zostanie oczyszczony w porównaniu z platformą .NET Core.</span><span class="sxs-lookup"><span data-stu-id="404b5-369">It's easy to forget that migration work isn't done as soon as the project builds cleanly against .NET Core.</span></span> <span data-ttu-id="404b5-370">Ważne jest, aby również pozostawić czas na przetestowanie aplikacji przez port.</span><span class="sxs-lookup"><span data-stu-id="404b5-370">It's important to leave time for testing the ported app, too.</span></span> <span data-ttu-id="404b5-371">Po pomyślnym skompilowaniu aplikacji upewnij się, że aplikacja działa i działa zgodnie z oczekiwaniami, zwłaszcza jeśli używasz dowolnych pakietów przeznaczonych do .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="404b5-371">Once things build successfully, make sure the app runs and works as expected, especially if you are using any packages targeting .NET Framework.</span></span>

<span data-ttu-id="404b5-372">Spróbujmy uruchomić aplikację sprzedawca z portem fasoli i zobaczyć, co się dzieje.</span><span class="sxs-lookup"><span data-stu-id="404b5-372">Let's try launching the ported Bean Trader app and see what happens.</span></span> <span data-ttu-id="404b5-373">Aplikacja nie jest daleko przed niepowodzeniem z powodu następującego wyjątku.</span><span class="sxs-lookup"><span data-stu-id="404b5-373">The app doesn't get far before failing with the following exception.</span></span>

```output
System.Configuration.ConfigurationErrorsException: 'Configuration system failed to initialize'

Inner Exception
ConfigurationErrorsException: Unrecognized configuration section system.serviceModel.
```

<span data-ttu-id="404b5-374">Jest to uzasadnione.</span><span class="sxs-lookup"><span data-stu-id="404b5-374">This makes sense, of course.</span></span> <span data-ttu-id="404b5-375">Należy pamiętać, że usługa WCF nie korzysta już z konfiguracji aplikacji, więc należy usunąć starą sekcję system. serviceModel pliku App. config.</span><span class="sxs-lookup"><span data-stu-id="404b5-375">Remember that WCF no longer uses app configuration, so the old system.serviceModel section of the app.config file needs to be removed.</span></span> <span data-ttu-id="404b5-376">Zaktualizowany klient WCF zawiera wszystkie te same informacje w jego kodzie, więc sekcja config nie jest już wymagana.</span><span class="sxs-lookup"><span data-stu-id="404b5-376">The updated WCF client includes all of the same information in its code, so the config section isn't needed anymore.</span></span> <span data-ttu-id="404b5-377">Jeśli chcesz, aby punkt końcowy WCF był konfigurowalny w pliku App. config, możesz go dodać jako ustawienie aplikacji i zaktualizować kod klienta WCF w celu pobrania punktu końcowego usługi WCF z konfiguracji.</span><span class="sxs-lookup"><span data-stu-id="404b5-377">If you wanted the WCF endpoint to be configurable in app.config, you could add it as an app setting and update the WCF client code to retrieve the WCF service endpoint from configuration.</span></span>

<span data-ttu-id="404b5-378">Po usunięciu sekcji System. serviceModel *pliku App. config*aplikacja zostanie uruchomiona, ale nie powiedzie się z innym wyjątkiem po zalogowaniu się użytkownika.</span><span class="sxs-lookup"><span data-stu-id="404b5-378">After removing the system.serviceModel section of *app.config*, the app launches but fails with another exception when a user signs in.</span></span>

```output
System.PlatformNotSupportedException: 'Operation is not supported on this platform.'
```

<span data-ttu-id="404b5-379">Nieobsługiwany interfejs API to `Func<T>.BeginInvoke` .</span><span class="sxs-lookup"><span data-stu-id="404b5-379">The unsupported API is `Func<T>.BeginInvoke`.</span></span> <span data-ttu-id="404b5-380">Zgodnie z objaśnieniem [dotnet/corefx # 5940](https://github.com/dotnet/corefx/issues/5940), platforma .NET Core nie `BeginInvoke` obsługuje `EndInvoke` metod i typów delegatów z powodu podstawowych zależności zdalnych.</span><span class="sxs-lookup"><span data-stu-id="404b5-380">As explained in [dotnet/corefx#5940](https://github.com/dotnet/corefx/issues/5940), .NET Core doesn't support the `BeginInvoke` and `EndInvoke` methods on delegate types due to underlying remoting dependencies.</span></span> <span data-ttu-id="404b5-381">Ten problem i jego rozwiązanie zostały omówione bardziej szczegółowo w sekcji [Migrowanie delegatów. BeginInvoke wywołań dla platformy .NET Core](https://devblogs.microsoft.com/dotnet/migrating-delegate-begininvoke-calls-for-net-core/) , ale jest to, `BeginInvoke` że `EndInvoke` wywołania powinny być zastępowane `Task.Run` (lub asynchroniczne alternatywy, jeśli to możliwe).</span><span class="sxs-lookup"><span data-stu-id="404b5-381">This issue and its fix are explained in more detail in the [Migrating Delegate.BeginInvoke Calls for .NET Core](https://devblogs.microsoft.com/dotnet/migrating-delegate-begininvoke-calls-for-net-core/) blog post, but the gist is that `BeginInvoke` and `EndInvoke` calls should be replaced with `Task.Run` (or async alternatives, if possible).</span></span> <span data-ttu-id="404b5-382">Jeśli w tym miejscu zostanie zastosowane ogólne rozwiązanie, `BeginInvoke` wywołanie może zostać zastąpione `Invoke` wywołaniem uruchomionym przez `Task.Run` .</span><span class="sxs-lookup"><span data-stu-id="404b5-382">Applying the general solution here, the `BeginInvoke` call can be replaced with an `Invoke` call launched by `Task.Run`.</span></span>

```csharp
Task.Run(() =>
{
    return userInfoRetriever.Invoke();
}).ContinueWith(result =>
{
    // BeginInvoke's callback is replaced with ContinueWith
    var task = result.ConfigureAwait(false);
    CurrentTrader = task.GetAwaiter().GetResult();
}, TaskScheduler.Default);
```

<span data-ttu-id="404b5-383">Po usunięciu `BeginInvoke` użycia aplikacja handlowca języka fasoli zostanie pomyślnie uruchomiona na platformie .NET Core!</span><span class="sxs-lookup"><span data-stu-id="404b5-383">After removing the `BeginInvoke` usage, the Bean Trader app runs successfully on .NET Core!</span></span>

![Podmiot gospodarczy języka fasoli działającej na platformie .NET Core](./media/convert-project-from-net-framework/running-on-core.png)

<span data-ttu-id="404b5-385">Wszystkie aplikacje są inne, więc określone kroki wymagane do przeprowadzenia migracji własnych aplikacji do programu .NET Core będą się różnić.</span><span class="sxs-lookup"><span data-stu-id="404b5-385">All apps are different, so the specific steps needed to migrate your own apps to .NET Core will vary.</span></span> <span data-ttu-id="404b5-386">Ale miejmy nadzieję w przykładzie handlowca prezentuje ogólny przepływ pracy i typy problemów, które mogą być oczekiwane.</span><span class="sxs-lookup"><span data-stu-id="404b5-386">But hopefully the Bean Trader sample demonstrates the general workflow and the types of issues that can be expected.</span></span> <span data-ttu-id="404b5-387">Mimo że pomimo tego artykułu, rzeczywiste zmiany, które trzeba wykonać na próbce handlowca ziarna, aby działały na platformie .NET Core, były dość ograniczone.</span><span class="sxs-lookup"><span data-stu-id="404b5-387">And, despite this article's length, the actual changes needed in the Bean Trader sample to make it work on .NET Core were fairly limited.</span></span> <span data-ttu-id="404b5-388">Wiele aplikacji jest migrowanych do programu .NET Core w ten sam sposób; bez konieczności wprowadzania zmian w kodzie.</span><span class="sxs-lookup"><span data-stu-id="404b5-388">Many apps migrate to .NET Core in this same way; with limited or even no code changes needed.</span></span>
