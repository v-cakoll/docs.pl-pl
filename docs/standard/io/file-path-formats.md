---
title: Formaty ścieżki plików w systemach Windows
ms.date: 06/28/2018
ms.technology: dotnet-standard
ms.topic: article
helpviewer_keywords:
- I/O, long paths
- long paths
- path formats, Windows
author: rpetrusha
ms.author: ronpet
ms.workload:
- dotnet
- dotnetcore
ms.openlocfilehash: 0efef54abd1da9631b5a560b49c6587d726e9193
ms.sourcegitcommit: 2eceb05f1a5bb261291a1f6a91c5153727ac1c19
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/04/2018
ms.locfileid: "43535455"
---
# <a name="file-path-formats-on-windows-systems"></a><span data-ttu-id="6cffa-102">Formaty ścieżki plików w systemach Windows</span><span class="sxs-lookup"><span data-stu-id="6cffa-102">File path formats on Windows systems</span></span>

<span data-ttu-id="6cffa-103">Wiele typów elementów członkowskich <xref:System.IO> obejmują przestrzeni nazw `path` parametr, który pozwala określić ścieżkę bezwzględną lub względną do zasobu systemu plików.</span><span class="sxs-lookup"><span data-stu-id="6cffa-103">Members of many of the types in the <xref:System.IO> namespace include a `path` parameter that lets you specify an absolute or relative path to a file system resource.</span></span> <span data-ttu-id="6cffa-104">Ta ścieżka jest następnie przekazywany do [Windows plik systemowy interfejsów API](https://msdn.microsoft.com/library/windows/desktop/aa364407(v=vs.85).aspx).</span><span class="sxs-lookup"><span data-stu-id="6cffa-104">This path is then passed to [Windows file sysem APIs](https://msdn.microsoft.com/library/windows/desktop/aa364407(v=vs.85).aspx).</span></span> <span data-ttu-id="6cffa-105">W tym temacie opisano formaty ścieżki plików, których można użyć w systemach Windows.</span><span class="sxs-lookup"><span data-stu-id="6cffa-105">This topic discusses the formats for file paths that you can use on Windows systems.</span></span>

## <a name="traditional-dos-paths"></a><span data-ttu-id="6cffa-106">Tradycyjne ścieżki DOS</span><span class="sxs-lookup"><span data-stu-id="6cffa-106">Traditional DOS paths</span></span>

<span data-ttu-id="6cffa-107">Standardowa ścieżki DOS może składać się z trzech składników:</span><span class="sxs-lookup"><span data-stu-id="6cffa-107">A standard DOS path can consist of three components:</span></span>

- <span data-ttu-id="6cffa-108">Woluminu lub litery dysku, a następnie separator woluminu (`:`).</span><span class="sxs-lookup"><span data-stu-id="6cffa-108">A volume or drive letter followed by the volume separator (`:`).</span></span>
- <span data-ttu-id="6cffa-109">Nazwa katalogu.</span><span class="sxs-lookup"><span data-stu-id="6cffa-109">A directory name.</span></span> <span data-ttu-id="6cffa-110">[Znakiem separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>) oddziela podkatalogów w hierarchii katalogów zagnieżdżonych.</span><span class="sxs-lookup"><span data-stu-id="6cffa-110">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates subdirectories within the nested directory hierarchy.</span></span>
- <span data-ttu-id="6cffa-111">Opcjonalnie nazwy pliku.</span><span class="sxs-lookup"><span data-stu-id="6cffa-111">An optional filename.</span></span> <span data-ttu-id="6cffa-112">[Znakiem separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>) oddziela ścieżkę pliku i nazwa pliku.</span><span class="sxs-lookup"><span data-stu-id="6cffa-112">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates the file path and the filename.</span></span>

<span data-ttu-id="6cffa-113">Jeśli wszystkie trzy składniki są zainstalowane, ścieżka jest bezwzględna.</span><span class="sxs-lookup"><span data-stu-id="6cffa-113">If all three components are present, the path is absolute.</span></span> <span data-ttu-id="6cffa-114">Jeśli nie określono żadnych woluminu lub litery dysku i nazwy katalogów rozpoczyna się od [znakiem separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>), ścieżka jest względna od katalogu głównego na bieżącym dysku.</span><span class="sxs-lookup"><span data-stu-id="6cffa-114">If no volume or drive letter is specified and the directory names begins with the [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>), the path is relative from the root of the current drive.</span></span> <span data-ttu-id="6cffa-115">W przeciwnym razie ścieżki jest określana względem bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="6cffa-115">Otherwise, the path is relative to the current directory.</span></span> <span data-ttu-id="6cffa-116">W poniższej tabeli przedstawiono niektóre możliwe katalogu i ścieżek plików.</span><span class="sxs-lookup"><span data-stu-id="6cffa-116">The following table shows some possible directory and file paths.</span></span>

|<span data-ttu-id="6cffa-117">Ścieżka</span><span class="sxs-lookup"><span data-stu-id="6cffa-117">Path</span></span>  |<span data-ttu-id="6cffa-118">Opis</span><span class="sxs-lookup"><span data-stu-id="6cffa-118">Description</span></span>  |
| -- | -- |
| `C:\Documents\Newsletters\Summer2018.pdf` | <span data-ttu-id="6cffa-119">Ścieżka bezwzględna do pliku w folderze głównym dysku C:</span><span class="sxs-lookup"><span data-stu-id="6cffa-119">An absolute file path from the root of drive C:</span></span> |
| `\Program Files\Custom Utilities\StringFinder.exe` | <span data-ttu-id="6cffa-120">Ścieżka bezwzględna w katalogu głównym na bieżącym dysku.</span><span class="sxs-lookup"><span data-stu-id="6cffa-120">An absolute path from the root of the current drive.</span></span> |
| `2018\January.xlsx` | <span data-ttu-id="6cffa-121">Ścieżka względna do pliku w podkatalogu bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="6cffa-121">A relative path to a file in a subdirectory of the current directory.</span></span> |
| `..\Publications\TravelBrochure.pdf` | <span data-ttu-id="6cffa-122">Ścieżka względna do pliku w katalogu, który jest równorzędny bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="6cffa-122">A relative path to file in a directory that is a peer of the current directory.</span></span> |
| `C:\Projects\apilibrary\apilibrary.sln` | <span data-ttu-id="6cffa-123">Ścieżka bezwzględna do pliku w folderze głównym dysku C:</span><span class="sxs-lookup"><span data-stu-id="6cffa-123">An absolute path to a file from the root of drive C:</span></span> |
| `C:Projects\apilibrary\apilibrary.sln` | <span data-ttu-id="6cffa-124">Ścieżka względna od bieżącego katalogu na dysku C:.</span><span class="sxs-lookup"><span data-stu-id="6cffa-124">A relative path from the current directory of the C: drive.</span></span> |

> [!IMPORTANT]
> <span data-ttu-id="6cffa-125">Należy zauważyć różnicę między ostatnich dwóch ścieżek.</span><span class="sxs-lookup"><span data-stu-id="6cffa-125">Note the difference between the last two paths.</span></span> <span data-ttu-id="6cffa-126">Zarówno określić specyfikator opcjonalne woluminu (C: w obu przypadkach), ale pierwszy zaczyna się od katalogu głównego określonego woluminu, natomiast druga nie.</span><span class="sxs-lookup"><span data-stu-id="6cffa-126">Both specify the optional volume specifier (C: in both cases), but the first begins with the root of the specified volume, whereas the second does not.</span></span> <span data-ttu-id="6cffa-127">W wyniku pierwszy jest ścieżką bezwzględną w katalogu głównym dysku C:, natomiast druga jest ścieżką względną z bieżącego katalogu dysku C:.</span><span class="sxs-lookup"><span data-stu-id="6cffa-127">As result, the first is an absolute path from the root directory of drive C:, whereas the second is a relative path from the current directory of drive C:.</span></span> <span data-ttu-id="6cffa-128">Użyj drugiego formularza po pierwszym jest przeznaczony jest źródłem typowych błędów, które obejmują ścieżki plików Windows.</span><span class="sxs-lookup"><span data-stu-id="6cffa-128">Use of the second form when the first is intended is a common source of bugs that involve Windows file paths.</span></span>

<span data-ttu-id="6cffa-129">Można określić, czy ścieżka pliku jest w pełni kwalifikowany (oznacza to, jego ścieżka jest niezależna od bieżącego katalogu i nie ulega zmianie, gdy zmienia bieżący katalog) przez wywołanie metody <xref:System.IO.Path.IsPathFullyQualified%2A?displayProperty=nameWthType> metody.</span><span class="sxs-lookup"><span data-stu-id="6cffa-129">You can determine whether a file path is fully qualified (that is, it the path is independent of the current directory and does not change when the current directory changes) by calling the <xref:System.IO.Path.IsPathFullyQualified%2A?displayProperty=nameWthType> method.</span></span> <span data-ttu-id="6cffa-130">Należy pamiętać, że znajdzie takiej ścieżki może zawierać segmentów względna katalogu (`.` i `..`) i nadal być w pełni kwalifikowana Jeśli rozpoznana ścieżka zawsze wskazuje w tej samej lokalizacji.</span><span class="sxs-lookup"><span data-stu-id="6cffa-130">Note that such a path can include relative directory segments (`.` and `..`) and still be fully qualified if the resolved path always points to the same location.</span></span>

<span data-ttu-id="6cffa-131">Poniższy przykład ilustruje różnicę między ścieżki względne i bezwzględne.</span><span class="sxs-lookup"><span data-stu-id="6cffa-131">The following example illustrates the difference between absolute and relative paths.</span></span> <span data-ttu-id="6cffa-132">Przyjęto założenie, że katalog D:\FY2018\ istnieje i czy nie został ustawiony dowolny bieżące katalog dla D:\ w wierszu polecenia przed uruchomieniem przykładu.</span><span class="sxs-lookup"><span data-stu-id="6cffa-132">It assumes that the directory D:\FY2018\ exists, and that you haven't set any curent directory for D:\ from the command prompt before running the example.</span></span>

[!code-csharp[absolute-and-relative-paths](~/samples/snippets/standard/io/file-names/cs/paths.cs)]
[!code-vb[absolute-and-relative-paths](~/samples/snippets/standard/io/file-names/vb/paths.vb)]

## <a name="unc-paths"></a><span data-ttu-id="6cffa-133">Ścieżki UNC</span><span class="sxs-lookup"><span data-stu-id="6cffa-133">UNC paths</span></span>

<span data-ttu-id="6cffa-134">Universal naming convention (UNC) ścieżki, które są używane do dostępu do zasobów sieciowych, mają następujący format:</span><span class="sxs-lookup"><span data-stu-id="6cffa-134">Universal naming convention (UNC) paths, which are used to access network resources, have the following format:</span></span>

- <span data-ttu-id="6cffa-135">Nazwa serwera lub hosta, który jest poprzedzony przez \\ \\.</span><span class="sxs-lookup"><span data-stu-id="6cffa-135">A server or host name, which is prefaced by \\\\.</span></span> <span data-ttu-id="6cffa-136">Nazwa serwera może być nazwą komputera NetBIOS lub adres IP/nazwę FQDN (IPv4 i IPv6 są obsługiwane).</span><span class="sxs-lookup"><span data-stu-id="6cffa-136">The server name can be a NetBIOS machine name or an IP/FQDN address (IPv4 as well as v6 are supported).</span></span>
- <span data-ttu-id="6cffa-137">Nazwa udziału jest oddzielony od nazwy hosta przez \\.</span><span class="sxs-lookup"><span data-stu-id="6cffa-137">A share name, which is separated from the host name by \\.</span></span> <span data-ttu-id="6cffa-138">Ze sobą serwera i nazwa udziału tworzą woluminu.</span><span class="sxs-lookup"><span data-stu-id="6cffa-138">Together, the server and share name make up the volume.</span></span>
- <span data-ttu-id="6cffa-139">Nazwa katalogu.</span><span class="sxs-lookup"><span data-stu-id="6cffa-139">A directory name.</span></span> <span data-ttu-id="6cffa-140">[Znakiem separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>) oddziela podkatalogów w hierarchii katalogów zagnieżdżonych.</span><span class="sxs-lookup"><span data-stu-id="6cffa-140">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates subdirectories within the nested directory hierarchy.</span></span>
- <span data-ttu-id="6cffa-141">Opcjonalnie nazwy pliku.</span><span class="sxs-lookup"><span data-stu-id="6cffa-141">An optional filename.</span></span> <span data-ttu-id="6cffa-142">[Znakiem separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>) oddziela ścieżkę pliku i nazwa pliku.</span><span class="sxs-lookup"><span data-stu-id="6cffa-142">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates the file path and the filename.</span></span>

<span data-ttu-id="6cffa-143">Poniżej przedstawiono kilka przykładów ścieżki UNC:</span><span class="sxs-lookup"><span data-stu-id="6cffa-143">The following are some examples of UNC paths:</span></span>

|<span data-ttu-id="6cffa-144">Ścieżka</span><span class="sxs-lookup"><span data-stu-id="6cffa-144">Path</span></span>  |<span data-ttu-id="6cffa-145">Opis</span><span class="sxs-lookup"><span data-stu-id="6cffa-145">Description</span></span>  |
| -- | -- |
| `\\system07\C$\` | <span data-ttu-id="6cffa-146">Katalog główny dysku c: dysków na `system07`.</span><span class="sxs-lookup"><span data-stu-id="6cffa-146">The root directory of the C: drive on `system07`.</span></span> |
| `\\Server2\Share\Test\Foo.txt` | <span data-ttu-id="6cffa-147">Plik Foo.txt w katalogu testu \\ \\Serwer2\\udostępnianie woluminów.</span><span class="sxs-lookup"><span data-stu-id="6cffa-147">The Foo.txt file in the Test directory of the \\\\Server2\\Share volume.</span></span>|

<span data-ttu-id="6cffa-148">Musi być zawsze w pełni kwalifikowaną ścieżkę UNC.</span><span class="sxs-lookup"><span data-stu-id="6cffa-148">UNC paths must always be fully qualified.</span></span> <span data-ttu-id="6cffa-149">Mogą zawierać segmentów względna katalogu (`.` i `..`), ale te muszą być częścią w pełni kwalifikowaną ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="6cffa-149">They can include relative directory segments (`.` and `..`), but these must be part of a fully qualified path.</span></span> <span data-ttu-id="6cffa-150">Tylko przez mapowanie ścieżki UNC na literę dysku, można użyć ścieżek względnych.</span><span class="sxs-lookup"><span data-stu-id="6cffa-150">You can use relative paths only by mapping a UNC path to a drive letter.</span></span>

## <a name="dos-device-paths"></a><span data-ttu-id="6cffa-151">Ścieżki urządzenia systemu DOS</span><span class="sxs-lookup"><span data-stu-id="6cffa-151">DOS device paths</span></span>

<span data-ttu-id="6cffa-152">System operacyjny Windows ma modelu ujednoliconego obiektu, który wskazuje wszystkie zasoby, łącznie z plikami.</span><span class="sxs-lookup"><span data-stu-id="6cffa-152">The Windows operating system has a unified object model that points to all resources, including files.</span></span> <span data-ttu-id="6cffa-153">Te ścieżki obiektów są dostępne z okna konsoli i są narażone na warstwie systemu Win32 za pośrednictwem specjalnego folderu łączy symbolicznych, które mapowania starszych ścieżek systemu DOS i UNC.</span><span class="sxs-lookup"><span data-stu-id="6cffa-153">These object paths are accessible from the console window and are exposed to the Win32 layer through a special folder of symbolic links that legacy DOS and UNC paths are mapped to.</span></span> <span data-ttu-id="6cffa-154">Ten specjalny folder jest dostępna za pośrednictwem składnia ścieżki urządzenia systemu DOS, który będzie miał jedną z:</span><span class="sxs-lookup"><span data-stu-id="6cffa-154">This special folder is accessed via the DOS device path syntax, which is one of:</span></span>

`\\.\C:\Test\Foo.txt`  
`\\?\C:\Test\Foo.txt`

> [!NOTE]
> <span data-ttu-id="6cffa-155">Składnia ścieżki urządzenia systemu DOS jest obsługiwana w implementacji platformy .NET w systemie Windows, począwszy od programu .NET Core 1.1 i .NET Framework 4.6.2.</span><span class="sxs-lookup"><span data-stu-id="6cffa-155">DOS device path syntax is supported on .NET implementations running on Windows starting with .NET Core 1.1 and .NET Framework 4.6.2.</span></span>

<span data-ttu-id="6cffa-156">Ścieżka urządzenia systemu DOS składa się z następujących składników:</span><span class="sxs-lookup"><span data-stu-id="6cffa-156">The DOS device path consists of the following components:</span></span>

- <span data-ttu-id="6cffa-157">Specyfikator ścieżki urządzenia (`\\.\` lub `\\?\`), który identyfikuje ścieżkę jako ścieżki urządzenia systemu DOS.</span><span class="sxs-lookup"><span data-stu-id="6cffa-157">The device path specifier (`\\.\` or `\\?\`), which identifies the path as a DOS device path.</span></span>

   > [!NOTE]
   > <span data-ttu-id="6cffa-158">`\\?\` Jest obsługiwana we wszystkich wersjach programu .NET Core i .NET Framework, począwszy od wersji 4.6.2.</span><span class="sxs-lookup"><span data-stu-id="6cffa-158">The `\\?\` is supported in all versions of .NET Core and in the .NET Framework starting with version 4.6.2.</span></span>
   
- <span data-ttu-id="6cffa-159">Link symboliczny do obiektu urządzenia "członu real" (C: w tym przypadku).</span><span class="sxs-lookup"><span data-stu-id="6cffa-159">A symbolic link to the "real" device object (C: in this case).</span></span>

   <span data-ttu-id="6cffa-160">Pierwszy segment ścieżki urządzenia systemu DOS, po specyfikatorze ścieżki urządzenia identyfikuje woluminu lub dysku.</span><span class="sxs-lookup"><span data-stu-id="6cffa-160">The first segment of the DOS device path after the device path specifier identifies the volume or drive.</span></span> <span data-ttu-id="6cffa-161">(Na przykład `\\?\C:\` i `\\.\BootPartition\`.)</span><span class="sxs-lookup"><span data-stu-id="6cffa-161">(For example, `\\?\C:\` and `\\.\BootPartition\`.)</span></span>

   <span data-ttu-id="6cffa-162">Istnieje jedno łącze UNC, która jest wywoływana, nie zaskakująco `UNC`.</span><span class="sxs-lookup"><span data-stu-id="6cffa-162">There is a specific link for UNCs that is called, not surprisingly, `UNC`.</span></span> <span data-ttu-id="6cffa-163">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="6cffa-163">For example:</span></span>

      `\\.\UNC\Server\Share\Test\Foo.txt`
      `\\?\UNC\Server\Share\Test\Foo.txt`

    <span data-ttu-id="6cffa-164">Dla urządzenia UNC fragment serwera/udział jest formularzy woluminu.</span><span class="sxs-lookup"><span data-stu-id="6cffa-164">For device UNCs, the server/share portion is forms the volume.</span></span> <span data-ttu-id="6cffa-165">Na przykład w `\\?\server1\e:\utilities\\filecomparer\`, fragment serwera/udział jest server1\utilities.</span><span class="sxs-lookup"><span data-stu-id="6cffa-165">For example, in `\\?\server1\e:\utilities\\filecomparer\`, the server/share portion is server1\utilities.</span></span> <span data-ttu-id="6cffa-166">Jest to istotne podczas wywoływania metody takie jak <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> segmentów względna katalogu; nigdy nie jest możliwe do nawigacji w przeszłości woluminu.</span><span class="sxs-lookup"><span data-stu-id="6cffa-166">This is significant when calling a method such as <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> with relative directory segments; it is never possible to navigate past the volume.</span></span> 

<span data-ttu-id="6cffa-167">Ścieżki urządzenia DOS są w pełni kwalifikowane przez definicję.</span><span class="sxs-lookup"><span data-stu-id="6cffa-167">DOS device paths are fully qualified by definition.</span></span> <span data-ttu-id="6cffa-168">Segmenty względna katalogu (`.` i `..`) nie są dozwolone.</span><span class="sxs-lookup"><span data-stu-id="6cffa-168">Relative directory segments (`.` and `..`) are not allowed.</span></span> <span data-ttu-id="6cffa-169">Bieżący katalog nigdy nie wprowadzają do ich użycia.</span><span class="sxs-lookup"><span data-stu-id="6cffa-169">Current directories never enter into their usage.</span></span>

## <a name="example-ways-to-refer-to-the-same-file"></a><span data-ttu-id="6cffa-170">Przykład: Sposobów, aby odwołać się do tego samego pliku</span><span class="sxs-lookup"><span data-stu-id="6cffa-170">Example: Ways to refer to the same file</span></span>

<span data-ttu-id="6cffa-171">W poniższym przykładzie pokazano kilka sposobów, w którym możesz zapoznać się z plikiem przy użyciu interfejsów API w <xref:System.IO> przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="6cffa-171">The following example illustrates some of the ways in which you can refer to a file when using the APIs in the <xref:System.IO> namespace.</span></span> <span data-ttu-id="6cffa-172">Przykład tworzy <xref:System.IO.FileInfo> obiektu i zastosowań jego <xref:System.IO.FileInfo.Name> i <xref:System.IO.FileInfo.Length> właściwości, aby wyświetlić nazwę pliku i długość pliku.</span><span class="sxs-lookup"><span data-stu-id="6cffa-172">The example instantiates a <xref:System.IO.FileInfo> object and uses its <xref:System.IO.FileInfo.Name> and <xref:System.IO.FileInfo.Length> properties to display the filename and the length of the file.</span></span>

[!code-csharp[referring-to-the-same-file](~/samples/snippets/standard/io/file-names/cs/file-refs.cs)]
[!code-vb[referring-to-the-same-file](~/samples/snippets/standard/io/file-names/vb/file-refs.vb)]

## <a name="path-normalization"></a><span data-ttu-id="6cffa-173">Ścieżka normalizacji</span><span class="sxs-lookup"><span data-stu-id="6cffa-173">Path normalization</span></span>

<span data-ttu-id="6cffa-174">Prawie wszystkie ścieżki przekazany do Windows API są znormalizowane.</span><span class="sxs-lookup"><span data-stu-id="6cffa-174">Almost all paths passed to Windows APIs are normalized.</span></span> <span data-ttu-id="6cffa-175">Podczas normalizacji Windows wykonuje następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="6cffa-175">During normalization, Windows performs the following steps:</span></span>

- <span data-ttu-id="6cffa-176">Określa ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="6cffa-176">Identifies the path.</span></span>
- <span data-ttu-id="6cffa-177">Częściowo kwalifikowane (względnej) ścieżki dotyczy bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="6cffa-177">Applies the current directory to partially qualified (relative) paths.</span></span>
- <span data-ttu-id="6cffa-178">Canonicalizes separatory składnika i katalog.</span><span class="sxs-lookup"><span data-stu-id="6cffa-178">Canonicalizes component and directory separators.</span></span>
- <span data-ttu-id="6cffa-179">Ocenia składniki względna katalogu (`.` dla bieżącego katalogu i `..` dla katalogu nadrzędnego).</span><span class="sxs-lookup"><span data-stu-id="6cffa-179">Evaluates relative directory components (`.` for the current directory and `..` for the parent directory).</span></span>
- <span data-ttu-id="6cffa-180">Przycina niektórych znaków.</span><span class="sxs-lookup"><span data-stu-id="6cffa-180">Trims certain characters.</span></span>

<span data-ttu-id="6cffa-181">Ta normalizacji lubi niejawnie, ale możesz zrobić to jawnie przez wywołanie metody <xref:System.IO.Path.GetFullPath%2A?displayProperty=nameWithType> metody, która zawija wywołanie do [funkcja GetFullPathName()](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).aspx).</span><span class="sxs-lookup"><span data-stu-id="6cffa-181">This normalization happens implicitly, but you can do it explicitly by calling the <xref:System.IO.Path.GetFullPath%2A?displayProperty=nameWithType> method, which wraps a call to the  [GetFullPathName() function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).aspx).</span></span> <span data-ttu-id="6cffa-182">Można również wywołać Windows [funkcja GetFullPathName()](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).aspx) bezpośrednio przy użyciu metody P/Invoke.</span><span class="sxs-lookup"><span data-stu-id="6cffa-182">You can also call the Windows [GetFullPathName() function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).aspx) directly using P/Invoke.</span></span> <span data-ttu-id="6cffa-183">Można również wywołać</span><span class="sxs-lookup"><span data-stu-id="6cffa-183">You can also call the</span></span> 

### <a name="identifying-the-path"></a><span data-ttu-id="6cffa-184">Identyfikowanie ścieżki</span><span class="sxs-lookup"><span data-stu-id="6cffa-184">Identifying the path</span></span>

<span data-ttu-id="6cffa-185">Pierwszym krokiem podczas normalizacji ścieżki identyfikuje typ ścieżki.</span><span class="sxs-lookup"><span data-stu-id="6cffa-185">The first step in path normalization is identifying the type of path.</span></span> <span data-ttu-id="6cffa-186">Ścieżki można podzielić na jeden z kilku kategorii:</span><span class="sxs-lookup"><span data-stu-id="6cffa-186">Paths fall into one of a few categories:</span></span>

- <span data-ttu-id="6cffa-187">Są one ścieżki urządzenia. oznacza to, że zaczynają się z dwóch separatory i znak zapytania lub okres (`\\?` lub `\\.`).</span><span class="sxs-lookup"><span data-stu-id="6cffa-187">They are device paths; that is, they begin with two separators and a question mark or period (`\\?` or `\\.`).</span></span>
- <span data-ttu-id="6cffa-188">Są one ścieżki UNC. oznacza to, że ich zaczyna się od dwóch separatory bez znaku zapytania lub okres.</span><span class="sxs-lookup"><span data-stu-id="6cffa-188">They are UNC paths; that is, they begin with two separators without a question mark or period.</span></span> 
- <span data-ttu-id="6cffa-189">Są one w pełni kwalifikowanej ścieżki DOS. oznacza to, że ich zaczynają się od litery dysku, separator woluminu i separator składników (`C:\`).</span><span class="sxs-lookup"><span data-stu-id="6cffa-189">They are fully qualified DOS paths; that is, they begin with a drive letter, a volume separator, and a component separator (`C:\`).</span></span>
- <span data-ttu-id="6cffa-190">Wyznaczają starszego urządzenia (`CON`, `LPT1`).</span><span class="sxs-lookup"><span data-stu-id="6cffa-190">They designate a legacy device (`CON`, `LPT1`).</span></span>
- <span data-ttu-id="6cffa-191">Są one względem katalogu głównego na bieżącym dysku; oznacza to, że zaczynają się separatorem pojedynczego składnika (`\`).</span><span class="sxs-lookup"><span data-stu-id="6cffa-191">They are relative to the root of the current drive; that is, they begin with a single component separator (`\`).</span></span>
- <span data-ttu-id="6cffa-192">Są one względem bieżącego katalogu określonego dysku; oznacza to, że ich zaczynają się od litery dysku, separator woluminu i nie separator składników (`C:`).</span><span class="sxs-lookup"><span data-stu-id="6cffa-192">They are relative to the current directory of a specified drive; that is, they begin with a drive letter, a volume separator, and no component separator (`C:`).</span></span>
- <span data-ttu-id="6cffa-193">Są one względem bieżącego katalogu; oznacza to, że zaczynają się inaczej (`temp\testfile.txt`).</span><span class="sxs-lookup"><span data-stu-id="6cffa-193">They are relative to the current directory; that is, they begin with anything else (`temp\testfile.txt`).</span></span>

<span data-ttu-id="6cffa-194">Typ ścieżki Określa, czy bieżący katalog jest stosowana w jakiś sposób.</span><span class="sxs-lookup"><span data-stu-id="6cffa-194">The type of the path determines whether or not a current directory is applied in some way.</span></span> <span data-ttu-id="6cffa-195">Określa również, co to jest "root" ścieżka.</span><span class="sxs-lookup"><span data-stu-id="6cffa-195">It also determines what the "root" of the path is.</span></span>

### <a name="handling-legacy-devices"></a><span data-ttu-id="6cffa-196">Obsługa starszych urządzeń</span><span class="sxs-lookup"><span data-stu-id="6cffa-196">Handling legacy devices</span></span>

<span data-ttu-id="6cffa-197">Jeśli ścieżka jest starszego urządzenia systemu DOS na takie `CON`, `COM1`, lub `LPT1`, jest konwertowana na ścieżki urządzenia przez poprzedzenie jej `\\.\` i zwrócone.</span><span class="sxs-lookup"><span data-stu-id="6cffa-197">If the path is a legacy DOS device such as `CON`, `COM1`, or `LPT1`, it is converted into a device path by prepending `\\.\` and returned.</span></span> 

<span data-ttu-id="6cffa-198">Ścieżkę, która zaczyna się od nazwy starszego urządzenia jest zawsze interpretowane jako starszego urządzenia przez <xref:System.IO.Path.GetFullPath(System.String)?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="6cffa-198">A path that begins with a legacy device name is always interpreted as a legacy device by the <xref:System.IO.Path.GetFullPath(System.String)?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="6cffa-199">Na przykład ścieżka urządzenia systemu DOS dla `CON.TXT` jest `\\.\CON`, ścieżkę urządzenia systemu DOS i `COM1.TXT\file1.txt` jest `\\.\COM1`.</span><span class="sxs-lookup"><span data-stu-id="6cffa-199">For example, the DOS device path for `CON.TXT` is `\\.\CON`, and the DOS device path for `COM1.TXT\file1.txt` is `\\.\COM1`.</span></span>

### <a name="applying-the-current-directory"></a><span data-ttu-id="6cffa-200">Stosowanie bieżącego katalogu</span><span class="sxs-lookup"><span data-stu-id="6cffa-200">Applying the current directory</span></span>

<span data-ttu-id="6cffa-201">Jeśli ścieżka nie jest w pełni kwalifikowaną, Windows stosuje bieżący katalog do niego.</span><span class="sxs-lookup"><span data-stu-id="6cffa-201">If a path isn't fully qualified, Windows applies the current directory to it.</span></span> <span data-ttu-id="6cffa-202">UNC i ścieżki urządzenia nie ma bieżącego katalogu, które są stosowane.</span><span class="sxs-lookup"><span data-stu-id="6cffa-202">UNCs and device paths do not have the current directory applied.</span></span> <span data-ttu-id="6cffa-203">Żadna z nich nie jest pełną dysku z separatorem C:\\.</span><span class="sxs-lookup"><span data-stu-id="6cffa-203">Neither does a full drive with separator C:\\.</span></span>

<span data-ttu-id="6cffa-204">Jeśli ścieżka zaczyna się od separatora pojedynczego składnika, stosowany jest dysk z bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="6cffa-204">If the path starts with a single component separator, the drive from the current directory is applied.</span></span> <span data-ttu-id="6cffa-205">Na przykład, jeśli ścieżka pliku jest `\utilities` i bieżący katalog to `C:\temp\`, tworzy normalizacji `C:\utilities`.</span><span class="sxs-lookup"><span data-stu-id="6cffa-205">For example, if the file path is `\utilities` and the current directory is `C:\temp\`, normalization produces `C:\utilities`.</span></span>

<span data-ttu-id="6cffa-206">Jeśli ścieżka zaczyna się od litery dysku, separator woluminu i nie separator składników, stosowany jest ostatni bieżącego katalogu z powłoki poleceń określonego dysku.</span><span class="sxs-lookup"><span data-stu-id="6cffa-206">If the path starts with a drive letter, volume separator, and no component separator, the last current directory set from the command shell for the specified drive is applied.</span></span> <span data-ttu-id="6cffa-207">Jeśli nie ustawiono ostatniego bieżącego katalogu, jest stosowana stacji autonomicznej.</span><span class="sxs-lookup"><span data-stu-id="6cffa-207">If the last current directory was not set, the drive alone is applied.</span></span> <span data-ttu-id="6cffa-208">Na przykład, jeśli ścieżka pliku jest `D:sources`, bieżący katalog to `C:\Documents\`, i był ostatni bieżącego katalogu na dysku D: `D:\sources\`, wynik jest `D:\sources\sources`.</span><span class="sxs-lookup"><span data-stu-id="6cffa-208">For example, if the file path is `D:sources`, the current directory is `C:\Documents\`, and the last current directory on drive D: was `D:\sources\`, the result is `D:\sources\sources`.</span></span> <span data-ttu-id="6cffa-209">Te ścieżki "dysku względna" są wspólne źródło błędy logiczne programów i skryptów.</span><span class="sxs-lookup"><span data-stu-id="6cffa-209">These "drive relative" paths are a common source of program and script logic errors.</span></span> <span data-ttu-id="6cffa-210">Przy założeniu, że ścieżki rozpoczynającej się od litery i dwukropek jest względna oczywiście nie jest prawidłowy.</span><span class="sxs-lookup"><span data-stu-id="6cffa-210">Assuming that a path beginning with a letter and a colon isn't relative is obviously not correct.</span></span>

<span data-ttu-id="6cffa-211">Jeśli ścieżka zaczyna się coś innego niż separatora, są stosowane bieżący dysk i katalog bieżący.</span><span class="sxs-lookup"><span data-stu-id="6cffa-211">If the path starts with something other than a separator, the current drive and current directory are applied.</span></span> <span data-ttu-id="6cffa-212">Na przykład, jeśli ścieżka jest `filecompare` i bieżący katalog to `C:\utilities\`, wynik jest `C:\utilities\filecompare\`.</span><span class="sxs-lookup"><span data-stu-id="6cffa-212">For example, if the path is `filecompare` and the current directory is `C:\utilities\`, the result is `C:\utilities\filecompare\`.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="6cffa-213">Ścieżki względne są niebezpiecznych w aplikacjach wielowątkowych (oznacza to, że większość aplikacji), ponieważ bieżący katalog to ustawienie na proces.</span><span class="sxs-lookup"><span data-stu-id="6cffa-213">Relative paths are dangerous in multithreaded applications (that is, most applications) because the current directory is a per-process setting.</span></span> <span data-ttu-id="6cffa-214">Jednym z wątków można zmienić bieżący katalog, w dowolnym momencie.</span><span class="sxs-lookup"><span data-stu-id="6cffa-214">Any thread can change the current directory at any time.</span></span> <span data-ttu-id="6cffa-215">Począwszy od platformy .NET Core 2.1, możesz wywołać <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> metodę, aby uzyskać ścieżkę bezwzględną ścieżką względną i ścieżki podstawowej (bieżący katalog), który chcesz rozwiązać go przed.</span><span class="sxs-lookup"><span data-stu-id="6cffa-215">Starting with .NET Core 2.1, you can call the <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> method to get an absolute path from a relative path and the base path (the current directory) that you want to resolve it against.</span></span> 

### <a name="canonicalizing-separators"></a><span data-ttu-id="6cffa-216">Separatory przekształcania w formę kanoniczną</span><span class="sxs-lookup"><span data-stu-id="6cffa-216">Canonicalizing separators</span></span>

<span data-ttu-id="6cffa-217">Wszystkie kreski ułamkowe (`/`) są konwertowane na standardowa separator Windows kreski ułamkowej odwróconej (`\`).</span><span class="sxs-lookup"><span data-stu-id="6cffa-217">All forward slashes (`/`) are converted into the standard Windows separator, the back slash (`\`).</span></span> <span data-ttu-id="6cffa-218">Jeśli są obecne, szereg ukośniki, których wykonaj pierwsze dwa ukośniki są zwinięte do pojedynczego ukośnika.</span><span class="sxs-lookup"><span data-stu-id="6cffa-218">If they are present, a series of slashes that follow the first two slashes are collapsed into a single slash.</span></span>

### <a name="evaluating-relative-components"></a><span data-ttu-id="6cffa-219">Ocena względne składników</span><span class="sxs-lookup"><span data-stu-id="6cffa-219">Evaluating relative components</span></span>

<span data-ttu-id="6cffa-220">Ścieżka jest przetwarzany, wszelkie składniki i segmentów, które składają się z pojedynczego lub podwójnego kropki (`.` lub `..`) są obliczane:</span><span class="sxs-lookup"><span data-stu-id="6cffa-220">As the path is processed, any components or segments that are composed of a single or a double period (`.` or `..`) are evaluated:</span></span> 

- <span data-ttu-id="6cffa-221">W zadanym przedziale bieżący segment zostanie usunięty, ponieważ odwołuje się on w bieżącym katalogu.</span><span class="sxs-lookup"><span data-stu-id="6cffa-221">For a single period, the current segment is removed, since it refers to the current directory.</span></span>

- <span data-ttu-id="6cffa-222">W okresie double bieżącego segmentu i nadrzędne są usuwane, ponieważ okres double odwołuje się do katalogu nadrzędnego.</span><span class="sxs-lookup"><span data-stu-id="6cffa-222">For a double period, the current segment and the parent segment are removed, since the double period refers to the parent directory.</span></span>

   <span data-ttu-id="6cffa-223">Katalogi nadrzędne są usuwane tylko wtedy, jeśli nie są one poza katalog główny ścieżki.</span><span class="sxs-lookup"><span data-stu-id="6cffa-223">Parent directories are only removed if they aren't past the root of the path.</span></span> <span data-ttu-id="6cffa-224">Katalog główny ścieżki zależy od typu ścieżki.</span><span class="sxs-lookup"><span data-stu-id="6cffa-224">The root of the path depends on the type of path.</span></span> <span data-ttu-id="6cffa-225">Jest dysk (`C:\`) dla ścieżki DOS, serwer/udostępniania dla UNC (`\\Server\Share`) oraz prefiks ścieżki urządzenia dla urządzenia ścieżek (`\\?\` lub `\\.\`).</span><span class="sxs-lookup"><span data-stu-id="6cffa-225">It is the drive (`C:\`) for DOS paths, the server/share for UNCs (`\\Server\Share`), and the device path prefix for device paths (`\\?\` or `\\.\`).</span></span>

### <a name="trimming-characters"></a><span data-ttu-id="6cffa-226">Przycinanie znaków</span><span class="sxs-lookup"><span data-stu-id="6cffa-226">Trimming characters</span></span>

<span data-ttu-id="6cffa-227">Wraz z uruchomienia separatory i segmentów względne usunięty wcześniej występują dodatkowe znaki są usuwane podczas normalizacji:</span><span class="sxs-lookup"><span data-stu-id="6cffa-227">Along with the runs of separators and relative segments removed earlier, some additional characters are removed during normalization:</span></span>

- <span data-ttu-id="6cffa-228">Segment kończy się na jednej kropki, tego okresu zostanie usunięta.</span><span class="sxs-lookup"><span data-stu-id="6cffa-228">If a segment ends in a single period, that period is removed.</span></span> <span data-ttu-id="6cffa-229">(Segment okresu pojedynczym lub podwójnym znormalizowane w poprzednim kroku.</span><span class="sxs-lookup"><span data-stu-id="6cffa-229">(A segment of a single or double period is normalized in the previous step.</span></span> <span data-ttu-id="6cffa-230">Segment co najmniej trzy kropki nie jest znormalizowana i jest faktycznie nazwą prawidłową pliku lub katalogu.)</span><span class="sxs-lookup"><span data-stu-id="6cffa-230">A segment of three or more periods is not normalized and is actually a valid file/directory name.)</span></span>

- <span data-ttu-id="6cffa-231">Jeśli ścieżka nie kończy się separatorem, wszystkie końcowe kropek i spacji (U + 0020), są usuwane.</span><span class="sxs-lookup"><span data-stu-id="6cffa-231">If the path doesn't end in a separator, all trailing periods and spaces (U+0020) are removed.</span></span> <span data-ttu-id="6cffa-232">Jeśli ostatni element jest po prostu okres pojedyncze lub podwójne, znajduje się w obszarze reguła względne składniki powyżej.</span><span class="sxs-lookup"><span data-stu-id="6cffa-232">If the last segment is simply a single or double period, it falls under the relative components rule above.</span></span> 

   <span data-ttu-id="6cffa-233">Ta reguła oznacza, że nazwę katalogu można utworzyć ze spacją końcową, dodając końcowe separator po miejsce.</span><span class="sxs-lookup"><span data-stu-id="6cffa-233">This rule means that you can create a directory name with a trailing space by adding a trailing separator after the space.</span></span>  

   > [!IMPORTANT]
   > <span data-ttu-id="6cffa-234">Należy **nigdy nie** Utwórz nazwę pliku lub katalogu ze spacją końcową.</span><span class="sxs-lookup"><span data-stu-id="6cffa-234">You should **never** create a directory or filename with a trailing space.</span></span> <span data-ttu-id="6cffa-235">Spacje końcowe może utrudnić lub niemożliwe do dostępu do katalogu i aplikacji często się niepowodzeniem podczas próby obsługi katalogów lub plików, których nazwy zawierają spacje końcowe.</span><span class="sxs-lookup"><span data-stu-id="6cffa-235">Trailing spaces can make it difficult or impossible to access a directory, and applications commonly fail when attempting to handle directories or files whose names include trailing spaces.</span></span>

## <a name="skipping-normalization"></a><span data-ttu-id="6cffa-236">Pomijanie normalizacji</span><span class="sxs-lookup"><span data-stu-id="6cffa-236">Skipping normalization</span></span>

<span data-ttu-id="6cffa-237">Zwykle dowolną ścieżkę przekazywane do interfejsu API programu Windows (skutecznie) jest przekazywana do [funkcja GetFullPathName](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) i znormalizowana.</span><span class="sxs-lookup"><span data-stu-id="6cffa-237">Normally, any path passed to a Windows API is (effectively) passed to the [GetFullPathName function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) and normalized.</span></span> <span data-ttu-id="6cffa-238">Istnieje jeden ważny wyjątek: ścieżka urządzenia, która rozpoczyna się od znaku zapytania, a nie przez okres.</span><span class="sxs-lookup"><span data-stu-id="6cffa-238">There is one important exception: a device path that begins with a question mark instead of a period.</span></span> <span data-ttu-id="6cffa-239">Chyba, że ścieżka zaczyna dokładnie `\\?\` (Uwaga użytkowania canonical ukośnik odwrotny), jej jest znormalizować.</span><span class="sxs-lookup"><span data-stu-id="6cffa-239">Unless the path starts exactly with `\\?\` (note the use of the canonical backslash), it is normalized.</span></span>

<span data-ttu-id="6cffa-240">Dlaczego czy chcesz pominąć normalizacji?</span><span class="sxs-lookup"><span data-stu-id="6cffa-240">Why would you want to skip normalization?</span></span> <span data-ttu-id="6cffa-241">Istnieją trzy główne przyczyny:</span><span class="sxs-lookup"><span data-stu-id="6cffa-241">There are three major reasons:</span></span>

1. <span data-ttu-id="6cffa-242">Aby uzyskać dostęp do ścieżki, które są zwykle niedostępne, ale także są dopuszczalne.</span><span class="sxs-lookup"><span data-stu-id="6cffa-242">To get access to paths that are normally unavailable but are legal.</span></span> <span data-ttu-id="6cffa-243">Plik lub katalog o nazwie `hidden.`, na przykład, istnieje możliwość dostępu w jakikolwiek inny sposób.</span><span class="sxs-lookup"><span data-stu-id="6cffa-243">A file or directory called `hidden.`, for example, is impossible to access in any other way.</span></span> 

1. <span data-ttu-id="6cffa-244">Aby zwiększyć wydajność przez pominięcie normalizacji, jeśli została już znormalizowane.</span><span class="sxs-lookup"><span data-stu-id="6cffa-244">To improve performance by skipping normalization if you've already normalized.</span></span>

1. <span data-ttu-id="6cffa-245">Od programu .NET Framework, aby pominąć `MAX_PATH` Sprawdź, czy długość ścieżki umożliwić ścieżek, które są większe niż 259 znaków.</span><span class="sxs-lookup"><span data-stu-id="6cffa-245">On the .NET Framework only, to skip the `MAX_PATH` check for path length to allow for paths that are greater than 259 characters.</span></span> <span data-ttu-id="6cffa-246">Większość interfejsów API Zezwól na to, z pewnymi wyjątkami.</span><span class="sxs-lookup"><span data-stu-id="6cffa-246">Most APIs allow this, with some exceptions.</span></span>

> [!NOTE]
> <span data-ttu-id="6cffa-247">.NET core obsługuje długich ścieżek niejawnie i nie wykonuje `MAX_PATH` Sprawdź.</span><span class="sxs-lookup"><span data-stu-id="6cffa-247">.NET Core handles long paths implicitly and does not perform a `MAX_PATH` check.</span></span> <span data-ttu-id="6cffa-248">`MAX_PATH` Sprawdzenie jest stosowane tylko do programu .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="6cffa-248">The `MAX_PATH` check applies only to the .NET Framework.</span></span>

<span data-ttu-id="6cffa-249">Pomijanie kontroli ścieżki normalizacji i maksymalna jest jedyną różnicą między składni ścieżki dwóch urządzeń; w przeciwnym razie są identyczne.</span><span class="sxs-lookup"><span data-stu-id="6cffa-249">Skipping normalization and max path checks is the only difference between the two device path syntaxes; they are otherwise identical.</span></span> <span data-ttu-id="6cffa-250">Należy zachować ostrożność w przypadku pominięcia normalizacji, ponieważ łatwo można utworzyć ścieżki, które są trudne do przeciwdziałania "normal" aplikacji.</span><span class="sxs-lookup"><span data-stu-id="6cffa-250">Be careful with skipping normalization, since you can easily create paths that are difficult for "normal" applications to deal with.</span></span>

<span data-ttu-id="6cffa-251">Ścieżki, które zaczyna się `\\?\` nadal są znormalizowane, jeśli jawnie przekażesz je do [funkcja GetFullPathName](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).</span><span class="sxs-lookup"><span data-stu-id="6cffa-251">Paths that start with `\\?\` are still normalized if you explicitly pass them to the [GetFullPathName function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).</span></span>

<span data-ttu-id="6cffa-252">Należy pamiętać o możliwości ścieżki ponad `MAX_PATH` znaków [GetFullPathName](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) bez `\\?\`.</span><span class="sxs-lookup"><span data-stu-id="6cffa-252">Note that you can paths of more than `MAX_PATH` characters to [GetFullPathName](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) without `\\?\`.</span></span> <span data-ttu-id="6cffa-253">Obsługuje ona ścieżki dowolnej długości maksymalnie rozmiar maksymalny ciągu, która może obsłużyć Windows.</span><span class="sxs-lookup"><span data-stu-id="6cffa-253">It supports arbitrary length paths up to the maximum string size that Windows can handle.</span></span>

## <a name="case-and-the-windows-file-system"></a><span data-ttu-id="6cffa-254">Wielkość liter i system plików Windows</span><span class="sxs-lookup"><span data-stu-id="6cffa-254">Case and the Windows file system</span></span>

<span data-ttu-id="6cffa-255">Cecha systemu plików Windows, który użytkowników innych niż Windows i deweloperów Znajdź mylące jest tej ścieżki i nazwy katalogów jest rozróżniana wielkość liter.</span><span class="sxs-lookup"><span data-stu-id="6cffa-255">A peculiarity of the Windows file system that non-Windows users and developers find confusing is that path and directory names are case-insensitive.</span></span> <span data-ttu-id="6cffa-256">Oznacza to, że nazwy katalogów i plików uwzględniają wielkość liter w wyrazie ciągi tekstowe używane podczas ich tworzenia.</span><span class="sxs-lookup"><span data-stu-id="6cffa-256">That is, directory and file names reflect the casing of the strings used when they are created.</span></span> <span data-ttu-id="6cffa-257">Na przykład wywołanie metody</span><span class="sxs-lookup"><span data-stu-id="6cffa-257">For example, the method call</span></span>

```csharp
Directory.Create(TeStDiReCtOrY);
```
<span data-ttu-id="6cffa-258">Tworzy katalog o nazwie katalog testowy.</span><span class="sxs-lookup"><span data-stu-id="6cffa-258">creates a directory named TeStDiReCtOrY.</span></span> <span data-ttu-id="6cffa-259">W przypadku zmiany nazwy, katalog lub plik, aby zmienić jego przypadek, nazwa pliku lub katalogu odzwierciedla wielkość liter w ciągu używany, gdy można zmienić jego nazwę.</span><span class="sxs-lookup"><span data-stu-id="6cffa-259">If you rename a directory or file to change its case, the directory or file name reflects the case of the string used when you rename it.</span></span> <span data-ttu-id="6cffa-260">Na przykład poniższy kod zmienia nazwę pliku o nazwie jako się jako:</span><span class="sxs-lookup"><span data-stu-id="6cffa-260">For example, the following code renames a file named test.txt to Test.txt:</span></span>

```csharp
using System;
using System.IO;

class Example
{
   public static void Main()
   {
      var fi = new FileInfo(@".\test.txt");
      fi.MoveTo(@".\Test.txt");
   }
}
``` 
```vb
Imports System.IO

Module Example
   Public Sub Main()
      Dim fi As New FileInfo(".\test.txt")
      fi.MoveTo(".\Test.txt")
   End Sub
End Module
```

<span data-ttu-id="6cffa-261">Jednak bez uwzględniania wielkości liter podczas porównywania nazw katalogów i plików.</span><span class="sxs-lookup"><span data-stu-id="6cffa-261">However, directory and file name comparisons are case-insensitive.</span></span> <span data-ttu-id="6cffa-262">Jeśli wyszukasz plik o nazwie "jako" interfejsów API systemu plików .NET zignorować przypadek, w porównaniu.</span><span class="sxs-lookup"><span data-stu-id="6cffa-262">If you search for a file named "test.txt", .NET file system APIs ignore case in the comparison.</span></span> <span data-ttu-id="6cffa-263">Jako, TEST. TXT, test. TXT i wszelkich innych kombinacji wielkich i małych liter, będą zgodne "jako".</span><span class="sxs-lookup"><span data-stu-id="6cffa-263">Test.txt, TEST.TXT, test.TXT, and any other combination of upper- and lowercase letters will match "test.txt".</span></span>
