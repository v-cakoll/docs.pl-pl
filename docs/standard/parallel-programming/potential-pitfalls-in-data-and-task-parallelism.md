---
title: Potencjalne pułapki związane z równoległością danych i zadań
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- parallel programming, pitfalls
ms.assetid: 1e357177-e699-4b8f-9e49-56d3513ed128
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: f6910dfba0889b4eaf601960d13dfe87a3b8c2fa
ms.sourcegitcommit: ad99773e5e45068ce03b99518008397e1299e0d1
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/23/2018
ms.locfileid: "46706440"
---
# <a name="potential-pitfalls-in-data-and-task-parallelism"></a><span data-ttu-id="cdd46-102">Potencjalne pułapki związane z równoległością danych i zadań</span><span class="sxs-lookup"><span data-stu-id="cdd46-102">Potential Pitfalls in Data and Task Parallelism</span></span>
<span data-ttu-id="cdd46-103">W wielu przypadkach <xref:System.Threading.Tasks.Parallel.For%2A?displayProperty=nameWithType> i <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> może zapewnić znaczne ulepszenia wydajności za pośrednictwem zwykłych sekwencyjne pętli.</span><span class="sxs-lookup"><span data-stu-id="cdd46-103">In many cases, <xref:System.Threading.Tasks.Parallel.For%2A?displayProperty=nameWithType> and <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> can provide significant performance improvements over ordinary sequential loops.</span></span> <span data-ttu-id="cdd46-104">Jednak pracy zrównoleglić pętlę wprowadza złożoności, który może prowadzić do problemów, które sekwencyjnego kodu nie są jako wspólne lub nie zostaną napotkane w ogóle.</span><span class="sxs-lookup"><span data-stu-id="cdd46-104">However, the work of parallelizing the loop introduces complexity that can lead to problems that, in sequential code, are not as common or are not encountered at all.</span></span> <span data-ttu-id="cdd46-105">W tym temacie wymieniono niektóre rozwiązania, aby uniknąć podczas pisania pętli równoległych.</span><span class="sxs-lookup"><span data-stu-id="cdd46-105">This topic lists some practices to avoid when you write parallel loops.</span></span>  
  
## <a name="do-not-assume-that-parallel-is-always-faster"></a><span data-ttu-id="cdd46-106">Nie należy zakładać, że równoległa jest zawsze szybciej</span><span class="sxs-lookup"><span data-stu-id="cdd46-106">Do Not Assume That Parallel Is Always Faster</span></span>  
 <span data-ttu-id="cdd46-107">W niektórych przypadkach pętlę równoległą może działać wolniej niż równoważnik sekwencyjne.</span><span class="sxs-lookup"><span data-stu-id="cdd46-107">In certain cases a parallel loop might run slower than its sequential equivalent.</span></span> <span data-ttu-id="cdd46-108">Podstawowa zasada mówi jest możliwość równoległego pętli, które mają kilka iteracji i delegatów użytkownika szybkie, prawdopodobnie nie będzie przyspieszenie znacznie.</span><span class="sxs-lookup"><span data-stu-id="cdd46-108">The basic rule of thumb is that parallel loops that have few iterations and fast user delegates are unlikely to speedup much.</span></span> <span data-ttu-id="cdd46-109">Ponieważ wiele czynników są zaangażowane w wydajności, firma Microsoft zaleca jednak zawsze pomiaru rzeczywiste wyniki.</span><span class="sxs-lookup"><span data-stu-id="cdd46-109">However, because many factors are involved in performance, we recommend that you always measure actual results.</span></span>  
  
## <a name="avoid-writing-to-shared-memory-locations"></a><span data-ttu-id="cdd46-110">Należy unikać pisania do lokalizacji udostępnionej pamięci</span><span class="sxs-lookup"><span data-stu-id="cdd46-110">Avoid Writing to Shared Memory Locations</span></span>  
 <span data-ttu-id="cdd46-111">W kodzie sekwencyjnych nie jest niczym niezwykłym, aby odczytać lub zapisać zmienne statyczne lub pola klasy.</span><span class="sxs-lookup"><span data-stu-id="cdd46-111">In sequential code, it is not uncommon to read from or write to static variables or class fields.</span></span> <span data-ttu-id="cdd46-112">Zawsze, gdy wiele wątków uzyskują dostęp do takich zmiennych jednocześnie, istnieje jednak duże prawdopodobieństwo jednoczesnego wyścigu.</span><span class="sxs-lookup"><span data-stu-id="cdd46-112">However, whenever multiple threads are accessing such variables concurrently, there is a big potential for race conditions.</span></span> <span data-ttu-id="cdd46-113">Mimo że blokad służy do synchronizowania dostępu do zmiennej, koszt synchronizacji może obniżyć wydajność.</span><span class="sxs-lookup"><span data-stu-id="cdd46-113">Even though you can use locks to synchronize access to the variable, the cost of synchronization can hurt performance.</span></span> <span data-ttu-id="cdd46-114">Dlatego zaleca się że uniknąć, lub co najmniej ograniczyć, dostęp do udostępnionego stanu w miarę możliwości pętli równoległej.</span><span class="sxs-lookup"><span data-stu-id="cdd46-114">Therefore, we recommend that you avoid, or at least limit, access to shared state in a parallel loop as much as possible.</span></span> <span data-ttu-id="cdd46-115">Najlepszym sposobem, w tym celu jest użycie przeciążenia <xref:System.Threading.Tasks.Parallel.For%2A?displayProperty=nameWithType> i <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> używające <xref:System.Threading.ThreadLocal%601?displayProperty=nameWithType> zmienną do przechowywania stanu lokalnej wątku podczas wykonywania pętli.</span><span class="sxs-lookup"><span data-stu-id="cdd46-115">The best way to do this is to use the overloads of <xref:System.Threading.Tasks.Parallel.For%2A?displayProperty=nameWithType> and <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> that use a <xref:System.Threading.ThreadLocal%601?displayProperty=nameWithType> variable to store thread-local state during loop execution.</span></span> <span data-ttu-id="cdd46-116">Aby uzyskać więcej informacji, zobacz [porady: zapisywanie pętli Parallel.For ze zmiennymi lokalnymi wątku](../../../docs/standard/parallel-programming/how-to-write-a-parallel-for-loop-with-thread-local-variables.md) i [porady: zapisywanie pętli Parallel.ForEach ze zmiennymi lokalnymi partycji](../../../docs/standard/parallel-programming/how-to-write-a-parallel-foreach-loop-with-partition-local-variables.md).</span><span class="sxs-lookup"><span data-stu-id="cdd46-116">For more information, see [How to: Write a Parallel.For Loop with Thread-Local Variables](../../../docs/standard/parallel-programming/how-to-write-a-parallel-for-loop-with-thread-local-variables.md) and [How to: Write a Parallel.ForEach Loop with Partition-Local Variables](../../../docs/standard/parallel-programming/how-to-write-a-parallel-foreach-loop-with-partition-local-variables.md).</span></span>  
  
## <a name="avoid-over-parallelization"></a><span data-ttu-id="cdd46-117">Unikaj nadmiernego przetwarzania równoległego</span><span class="sxs-lookup"><span data-stu-id="cdd46-117">Avoid Over-Parallelization</span></span>  
 <span data-ttu-id="cdd46-118">Za pomocą pętli równoległej wiąże się z kosztów ogólnych partycjonowania kolekcji źródłowej i synchronizacji wątków roboczych.</span><span class="sxs-lookup"><span data-stu-id="cdd46-118">By using parallel loops, you incur the overhead costs of partitioning the source collection and synchronizing the worker threads.</span></span> <span data-ttu-id="cdd46-119">Korzyści wynikające z przetwarzaniem równoległym dalsze są ograniczone przez liczbę procesorów w komputerze.</span><span class="sxs-lookup"><span data-stu-id="cdd46-119">The benefits of parallelization are further limited by the number of processors on the computer.</span></span> <span data-ttu-id="cdd46-120">Nie ma żadnych przyspieszenie, które można uzyskać, uruchamiając wiele wątków powiązany obliczeniowych w tylko jednym procesorze.</span><span class="sxs-lookup"><span data-stu-id="cdd46-120">There is no speedup to be gained by running multiple compute-bound threads on just one processor.</span></span> <span data-ttu-id="cdd46-121">W związku z tym należy uważać, aby nie nadmiernie zrównoleglić pętlę.</span><span class="sxs-lookup"><span data-stu-id="cdd46-121">Therefore, you must be careful not to over-parallelize a loop.</span></span>  
  
 <span data-ttu-id="cdd46-122">Najbardziej typowym scenariuszem, w których nadmiernego przetwarzania równoległego może wystąpić znajduje się w pętli zagnieżdżonych.</span><span class="sxs-lookup"><span data-stu-id="cdd46-122">The most common scenario in which over-parallelization can occur is in nested loops.</span></span> <span data-ttu-id="cdd46-123">W większości przypadków najlepiej jest równoległe przetwarzanie tylko zewnętrzna pętla, chyba że zastosować co najmniej jeden z następujących warunków:</span><span class="sxs-lookup"><span data-stu-id="cdd46-123">In most cases, it is best to parallelize only the outer loop unless one or more of the following conditions apply:</span></span>  
  
-   <span data-ttu-id="cdd46-124">Wiadomo, że wewnętrzną pętlę być bardzo długie.</span><span class="sxs-lookup"><span data-stu-id="cdd46-124">The inner loop is known to be very long.</span></span>  
  
-   <span data-ttu-id="cdd46-125">Kosztowne obliczenia są wykonywane na każde zamówienie.</span><span class="sxs-lookup"><span data-stu-id="cdd46-125">You are performing an expensive computation on each order.</span></span> <span data-ttu-id="cdd46-126">(Operacja pokazano w przykładzie nie jest kosztowna).</span><span class="sxs-lookup"><span data-stu-id="cdd46-126">(The operation shown in the example is not expensive.)</span></span>  
  
-   <span data-ttu-id="cdd46-127">System docelowy jest znana wystarczającej liczby procesorów, którą należy obsługiwać liczbę wątków, które zostaną wygenerowane przez zrównoleglić zapytanie na `cust.Orders`.</span><span class="sxs-lookup"><span data-stu-id="cdd46-127">The target system is known to have enough processors to handle the number of threads that will be produced by parallelizing the query on `cust.Orders`.</span></span>  
  
 <span data-ttu-id="cdd46-128">We wszystkich przypadkach najlepszym sposobem określenia kształtu optymalne zapytania jest do testowania i miar.</span><span class="sxs-lookup"><span data-stu-id="cdd46-128">In all cases, the best way to determine the optimum query shape is to test and measure.</span></span>  
  
## <a name="avoid-calls-to-non-thread-safe-methods"></a><span data-ttu-id="cdd46-129">Należy unikać wywołania metod innych niż wątkowo</span><span class="sxs-lookup"><span data-stu-id="cdd46-129">Avoid Calls to Non-Thread-Safe Methods</span></span>  
 <span data-ttu-id="cdd46-130">Zapisywanie metod wystąpienia innych wątkowo pętlę równoległą może prowadzić do uszkodzenia danych, które mogą lub nie może być wykryte w programach.</span><span class="sxs-lookup"><span data-stu-id="cdd46-130">Writing to non-thread-safe instance methods from a parallel loop can lead to data corruption which may or may not go undetected in your program.</span></span> <span data-ttu-id="cdd46-131">Może to również prowadzić do wyjątków.</span><span class="sxs-lookup"><span data-stu-id="cdd46-131">It can also lead to exceptions.</span></span> <span data-ttu-id="cdd46-132">W poniższym przykładzie wielu wątków będzie próba wywołania <xref:System.IO.FileStream.WriteByte%2A?displayProperty=nameWithType> metoda równocześnie, które nie jest obsługiwane przez klasę.</span><span class="sxs-lookup"><span data-stu-id="cdd46-132">In the following example, multiple threads would be attempting to call the <xref:System.IO.FileStream.WriteByte%2A?displayProperty=nameWithType> method simultaneously, which is not supported by the class.</span></span>  
  
 [!code-csharp[TPL_Pitfalls#04](../../../samples/snippets/csharp/VS_Snippets_Misc/tpl_pitfalls/cs/pitfalls.cs#04)]
 [!code-vb[TPL_Pitfalls#04](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpl_pitfalls/vb/pitfalls_vb.vb#04)]  
  
## <a name="limit-calls-to-thread-safe-methods"></a><span data-ttu-id="cdd46-133">Ogranicz wywołania metod metodą o bezpiecznych wątkach</span><span class="sxs-lookup"><span data-stu-id="cdd46-133">Limit Calls to Thread-Safe Methods</span></span>  
 <span data-ttu-id="cdd46-134">Najbardziej statycznych metod .NET Framework są odporne na wątki i może być wywoływana z wielu wątków jednocześnie.</span><span class="sxs-lookup"><span data-stu-id="cdd46-134">Most static methods in the .NET Framework are thread-safe and can be called from multiple threads concurrently.</span></span> <span data-ttu-id="cdd46-135">Jednak nawet w takich przypadkach zaangażowane synchronizacji może prowadzić do znaczne spowolnienie w zapytaniu.</span><span class="sxs-lookup"><span data-stu-id="cdd46-135">However, even in these cases, the synchronization involved can lead to significant slowdown in the query.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="cdd46-136">Możesz sprawdzić to samodzielnie, wstawiając wywołania niektórych <xref:System.Console.WriteLine%2A> w zapytaniach.</span><span class="sxs-lookup"><span data-stu-id="cdd46-136">You can test for this yourself by inserting some calls to <xref:System.Console.WriteLine%2A> in your queries.</span></span> <span data-ttu-id="cdd46-137">Chociaż ta metoda jest używana w przykładach dokumentacji w celach demonstracyjnych, nie należy używać go w pętlach równoległych Jeśli to konieczne.</span><span class="sxs-lookup"><span data-stu-id="cdd46-137">Although this method is used in the documentation examples for demonstration purposes, do not use it in parallel loops unless necessary.</span></span>  
  
## <a name="be-aware-of-thread-affinity-issues"></a><span data-ttu-id="cdd46-138">Należy pamiętać o problemów koligacji wątku</span><span class="sxs-lookup"><span data-stu-id="cdd46-138">Be Aware of Thread Affinity Issues</span></span>  
 <span data-ttu-id="cdd46-139">Niektóre technologie, na przykład współdziałanie COM dla składników Single-Threaded apartamentu (STA), Windows Forms i Windows Presentation Foundation (WPF), nakładają ograniczenia koligacji wątku, które wymagają kod wymagany do uruchomienia w określonym wątku.</span><span class="sxs-lookup"><span data-stu-id="cdd46-139">Some technologies, for example, COM interoperability for Single-Threaded Apartment (STA) components, Windows Forms, and Windows Presentation Foundation (WPF), impose thread affinity restrictions that require code to run on a specific thread.</span></span> <span data-ttu-id="cdd46-140">Na przykład w Windows Forms i WPF formant może zostać oceniony jedynie w wątku, na którym została utworzona.</span><span class="sxs-lookup"><span data-stu-id="cdd46-140">For example, in both Windows Forms and WPF, a control can only be accessed on the thread on which it was created.</span></span> <span data-ttu-id="cdd46-141">Oznacza to, na przykład nie można zaktualizować formantu listy z równoległą pętlą o ile nie skonfigurowano harmonogramu wątków do zaplanowania pracy tylko w wątku interfejsu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="cdd46-141">This means, for example, that you cannot update a list control from a parallel loop unless you configure the thread scheduler to schedule work only on the UI thread.</span></span> <span data-ttu-id="cdd46-142">Aby uzyskać więcej informacji, zobacz [jak: harmonogram pracy w wątku interfejsu użytkownika (UI)](https://msdn.microsoft.com/library/32a846a5-d628-4933-907b-4888ff72c663).</span><span class="sxs-lookup"><span data-stu-id="cdd46-142">For more information, see [How to: Schedule Work on the User Interface (UI) Thread](https://msdn.microsoft.com/library/32a846a5-d628-4933-907b-4888ff72c663).</span></span>  
  
## <a name="use-caution-when-waiting-in-delegates-that-are-called-by-parallelinvoke"></a><span data-ttu-id="cdd46-143">Należy zachować ostrożność podczas oczekiwania w delegatów, które są wywoływane przez elementu Parallel.Invoke</span><span class="sxs-lookup"><span data-stu-id="cdd46-143">Use Caution When Waiting in Delegates That Are Called by Parallel.Invoke</span></span>  
 <span data-ttu-id="cdd46-144">W pewnych okolicznościach Biblioteka zadań równoległych będą wbudowane zadanie, co oznacza, że jest ono uruchamiane zadanie na aktualnie wykonywany wątek.</span><span class="sxs-lookup"><span data-stu-id="cdd46-144">In certain circumstances, the Task Parallel Library will inline a task, which means it runs on the task on the currently executing thread.</span></span> <span data-ttu-id="cdd46-145">(Aby uzyskać więcej informacji, zobacz [harmonogramów zadań](https://msdn.microsoft.com/library/638f8ea5-21db-47a2-a934-86e1e961bf65).) Tego rodzaju optymalizacji wydajności może prowadzić do zakleszczenia w niektórych przypadkach.</span><span class="sxs-lookup"><span data-stu-id="cdd46-145">(For more information, see [Task Schedulers](https://msdn.microsoft.com/library/638f8ea5-21db-47a2-a934-86e1e961bf65).) This performance optimization can lead to deadlock in certain cases.</span></span> <span data-ttu-id="cdd46-146">Na przykład dwa zadania może uruchomić tego samego obiektu delegowanego kodu, które sygnalizują, gdy wystąpi zdarzenie, a następnie czeka na innych zadań w celu sygnalizowania, że.</span><span class="sxs-lookup"><span data-stu-id="cdd46-146">For example, two tasks might run the same delegate code, which signals when an event occurs, and then waits for the other task to signal.</span></span> <span data-ttu-id="cdd46-147">Jeśli drugie zadanie jest śródwierszowa, w tym samym wątku, który jako pierwszy, a pierwszy przechodzi w stan oczekiwania, drugie zadanie nigdy nie będą mogli sygnalizują jego zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="cdd46-147">If the second task is inlined on the same thread as the first, and the first goes into a Wait state, the second task will never be able to signal its event.</span></span> <span data-ttu-id="cdd46-148">Aby uniknąć wystąpienia zdarzenia, można określić limit czasu operacji oczekiwania, lub użyj wątku jawne konstruktory ułatwiające, upewnij się, że jedno zadanie nie można zablokować, drugi.</span><span class="sxs-lookup"><span data-stu-id="cdd46-148">To avoid such an occurrence, you can specify a timeout on the Wait operation, or use explicit thread constructors to help ensure that one task cannot block the other.</span></span>  
  
## <a name="do-not-assume-that-iterations-of-foreach-for-and-forall-always-execute-in-parallel"></a><span data-ttu-id="cdd46-149">Nie należy zakładać, że iteracji ForEach, dla i równolegle uruchomić zawsze ForAll</span><span class="sxs-lookup"><span data-stu-id="cdd46-149">Do Not Assume that Iterations of ForEach, For and ForAll Always Execute in Parallel</span></span>  
 <span data-ttu-id="cdd46-150">Ważne jest, aby pamiętać, że poszczególnych iteracji w <xref:System.Threading.Tasks.Parallel.For%2A>, <xref:System.Threading.Tasks.Parallel.ForEach%2A> lub <xref:System.Linq.ParallelEnumerable.ForAll%2A> pętli może, ale nie trzeba wykonywać równolegle.</span><span class="sxs-lookup"><span data-stu-id="cdd46-150">It is important to keep in mind that individual iterations in a <xref:System.Threading.Tasks.Parallel.For%2A>, <xref:System.Threading.Tasks.Parallel.ForEach%2A> or <xref:System.Linq.ParallelEnumerable.ForAll%2A> loop may but do not have to execute in parallel.</span></span> <span data-ttu-id="cdd46-151">W związku z tym należy unikać pisania żadnego kodu, który zależy od pod kątem poprawności na równoległe wykonywanie iteracji lub wykonywania iteracji w określonej kolejności.</span><span class="sxs-lookup"><span data-stu-id="cdd46-151">Therefore, you should avoid writing any code that depends for correctness on parallel execution of iterations or on the execution of iterations in any particular order.</span></span> <span data-ttu-id="cdd46-152">Na przykład ten kod jest prawdopodobnie zakleszczenie:</span><span class="sxs-lookup"><span data-stu-id="cdd46-152">For example, this code is likely to deadlock:</span></span>  
  
 [!code-csharp[TPL_Pitfalls#01](../../../samples/snippets/csharp/VS_Snippets_Misc/tpl_pitfalls/cs/pitfalls.cs#01)]
 [!code-vb[TPL_Pitfalls#01](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpl_pitfalls/vb/pitfalls_vb.vb#01)]  
  
 <span data-ttu-id="cdd46-153">W tym przykładzie jednej iteracji ustawia zdarzenie, a wszystkie inne iteracje oczekiwania na zdarzenie.</span><span class="sxs-lookup"><span data-stu-id="cdd46-153">In this example, one iteration sets an event, and all other iterations wait on the event.</span></span> <span data-ttu-id="cdd46-154">Brak iteracji oczekiwania można wykonać przed ukończeniem iteracji ustawienie zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="cdd46-154">None of the waiting iterations can complete until the event-setting iteration has completed.</span></span> <span data-ttu-id="cdd46-155">Jednak jest możliwe, że iteracji oczekujące blokuje wszystkie wątki, które są używane do wykonywania pętli równoległej, zanim iteracji ustawienie zdarzenia miała szansę, aby wykonać.</span><span class="sxs-lookup"><span data-stu-id="cdd46-155">However, it is possible that the waiting iterations block all threads that are used to execute the parallel loop, before the event-setting iteration has had a chance to execute.</span></span> <span data-ttu-id="cdd46-156">Skutkuje to zakleszczenie — iteracji ustawienie zdarzenia nigdy nie zostanie wykonana i iteracje oczekiwania nigdy nie będzie wznawiania.</span><span class="sxs-lookup"><span data-stu-id="cdd46-156">This results in a deadlock – the event-setting iteration will never execute, and the waiting iterations will never wake up.</span></span>  
  
 <span data-ttu-id="cdd46-157">W szczególności jednej iteracji pętli równoległej nigdy nie należy czekać na innej iteracji pętli postępu.</span><span class="sxs-lookup"><span data-stu-id="cdd46-157">In particular, one iteration of a parallel loop should never wait on another iteration of the loop to make progress.</span></span> <span data-ttu-id="cdd46-158">Jeśli pętli równoległej decyduje zaplanować iteracje kolejno, ale w kolejności przeciwnej, nastąpi zakleszczenia.</span><span class="sxs-lookup"><span data-stu-id="cdd46-158">If the parallel loop decides to schedule the iterations sequentially but in the opposite order, a deadlock will occur.</span></span>  
  
## <a name="avoid-executing-parallel-loops-on-the-ui-thread"></a><span data-ttu-id="cdd46-159">Należy unikać wykonywania pętli równoległych w wątku interfejsu użytkownika</span><span class="sxs-lookup"><span data-stu-id="cdd46-159">Avoid Executing Parallel Loops on the UI Thread</span></span>  
 <span data-ttu-id="cdd46-160">Należy zachować elastyczny interfejs użytkownika (UI).</span><span class="sxs-lookup"><span data-stu-id="cdd46-160">It is important to keep your application's user interface (UI) responsive.</span></span> <span data-ttu-id="cdd46-161">Jeśli operacja zawiera wystarczająco dużo pracy, aby gwarantowało przetwarzania równoległego, następnie go prawdopodobnie nie powinna być uruchamiana tej operacji w wątku interfejsu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="cdd46-161">If an operation contains enough work to warrant parallelization, then it likely should not be run that operation on the UI thread.</span></span>  <span data-ttu-id="cdd46-162">Powinien on zamiast tego należy obciążyć tej operacji należy uruchomić na wątku w tle.</span><span class="sxs-lookup"><span data-stu-id="cdd46-162">Instead, it should offload that operation to be run on a background thread.</span></span> <span data-ttu-id="cdd46-163">Na przykład jeśli chcesz użyć pętli równoległej do obliczenia części danych, które następnie powinna być renderowana na kontrolkę interfejsu użytkownika, należy rozważyć wykonywanie pętli w ramach wystąpienie zadania, a nie bezpośrednio na program obsługi zdarzeń w interfejsie użytkownika.</span><span class="sxs-lookup"><span data-stu-id="cdd46-163">For example, if you want to use a parallel loop to compute some data that should then be rendered into a UI control, you should consider executing the loop within a task instance rather than directly in a UI event handler.</span></span>  <span data-ttu-id="cdd46-164">Tylko wtedy, gdy obliczenie core została zakończona powinien można następnie kierować aktualizacji interfejsu użytkownika do wątku interfejsu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="cdd46-164">Only when the core computation has completed should you then marshal the UI update back to the UI thread.</span></span>  
  
 <span data-ttu-id="cdd46-165">Po uruchomieniu pętli równoległych w wątku interfejsu użytkownika, należy zachować ostrożność uniknąć aktualizowanie formantów interfejsu użytkownika z w ramach pętli.</span><span class="sxs-lookup"><span data-stu-id="cdd46-165">If you do run parallel loops on the UI thread, be careful to avoid updating UI controls from within the loop.</span></span> <span data-ttu-id="cdd46-166">Podjęto próbę zaktualizowania interfejsu użytkownika kontrolki z w ramach pętli równoległej, które jest wykonywane w wątku interfejsu użytkownika może spowodować uszkodzenie stanu, wyjątki, opóźnione aktualizacje i nawet zakleszczenia, w zależności od tego, jak jest wywoływany aktualizacji interfejsu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="cdd46-166">Attempting to update UI controls from within a parallel loop that is executing on the UI thread can lead to state corruption, exceptions, delayed updates, and even deadlocks, depending on how the UI update is invoked.</span></span> <span data-ttu-id="cdd46-167">W poniższym przykładzie pętli równoległej blokuje wątek interfejsu użytkownika, na którym jest wykonywany, dopiero po zakończeniu wszystkich iteracji.</span><span class="sxs-lookup"><span data-stu-id="cdd46-167">In the following example, the parallel loop blocks the UI thread on which it’s executing until all iterations are complete.</span></span> <span data-ttu-id="cdd46-168">Jednak jeśli iteracji pętli jest uruchomiona w wątku tła (jako <xref:System.Threading.Tasks.Parallel.For%2A> może wykonać), wiadomości do wysłania do wątku interfejsu użytkownika i bloków oczekiwanie na przetworzenie tego komunikatu powoduje, że wywołanie metody Invoke.</span><span class="sxs-lookup"><span data-stu-id="cdd46-168">However, if an iteration of the loop is running on a background thread (as <xref:System.Threading.Tasks.Parallel.For%2A> may do), the call to Invoke causes a message to be submitted to the UI thread and blocks waiting for that message to be processed.</span></span> <span data-ttu-id="cdd46-169">Ponieważ wątku interfejsu użytkownika jest zablokowane, uruchamianie <xref:System.Threading.Tasks.Parallel.For%2A>, nigdy nie można przetworzyć wiadomości, a zakleszczenia wątku interfejsu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="cdd46-169">Since the UI thread is blocked running the <xref:System.Threading.Tasks.Parallel.For%2A>, the message can never be processed, and the UI thread deadlocks.</span></span>  
  
 [!code-csharp[TPL_Pitfalls#02](../../../samples/snippets/csharp/VS_Snippets_Misc/tpl_pitfalls/cs/pitfalls.cs#02)]
 [!code-vb[TPL_Pitfalls#02](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpl_pitfalls/vb/pitfalls_vb.vb#02)]  
  
 <span data-ttu-id="cdd46-170">Poniższy przykład pokazuje, jak w celu uniknięcia zakleszczenia, uruchamiając pętli wewnątrz wystąpienia zadania.</span><span class="sxs-lookup"><span data-stu-id="cdd46-170">The following example shows how to avoid the deadlock, by running the loop inside a task instance.</span></span> <span data-ttu-id="cdd46-171">Wątek interfejsu użytkownika nie jest blokowany przez pętli, a wiadomości, które mogą być przetwarzane.</span><span class="sxs-lookup"><span data-stu-id="cdd46-171">The UI thread is not blocked by the loop, and the message can be processed.</span></span>  
  
 [!code-csharp[TPL_Pitfalls#03](../../../samples/snippets/csharp/VS_Snippets_Misc/tpl_pitfalls/cs/pitfalls.cs#03)]
 [!code-vb[TPL_Pitfalls#03](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpl_pitfalls/vb/pitfalls_vb.vb#03)]  
  
## <a name="see-also"></a><span data-ttu-id="cdd46-172">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="cdd46-172">See also</span></span>

- [<span data-ttu-id="cdd46-173">Programowanie równoległe</span><span class="sxs-lookup"><span data-stu-id="cdd46-173">Parallel Programming</span></span>](../../../docs/standard/parallel-programming/index.md)  
- [<span data-ttu-id="cdd46-174">Potencjalne pułapki związane z PLINQ</span><span class="sxs-lookup"><span data-stu-id="cdd46-174">Potential Pitfalls with PLINQ</span></span>](../../../docs/standard/parallel-programming/potential-pitfalls-with-plinq.md)  
- [<span data-ttu-id="cdd46-175">Wzorce programowania równoległego: rozumienie i stosowanie równoległych wzorców z programu .NET Framework 4</span><span class="sxs-lookup"><span data-stu-id="cdd46-175">Patterns for Parallel Programming: Understanding and Applying Parallel Patterns with the .NET Framework 4</span></span>](https://www.microsoft.com/download/details.aspx?id=19222)
