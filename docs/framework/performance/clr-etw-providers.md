---
title: Dostawcy CLR ETW
ms.date: 03/30/2017
helpviewer_keywords:
- ETW, CLR providers
- CLR ETW providers
ms.assetid: 0beafad4-b2c8-47f4-b342-83411d57a51f
author: mairaw
ms.author: mairaw
ms.openlocfilehash: 639ebe1552fd3950bd77acd7b5730b0d3bdb150f
ms.sourcegitcommit: 558d78d2a68acd4c95ef23231c8b4e4c7bac3902
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/09/2019
ms.locfileid: "59302624"
---
# <a name="clr-etw-providers"></a><span data-ttu-id="12407-102">Dostawcy CLR ETW</span><span class="sxs-lookup"><span data-stu-id="12407-102">CLR ETW Providers</span></span>
<span data-ttu-id="12407-103">Środowisko uruchomieniowe języka wspólnego (CLR) ma dwóch dostawców: dostawcę środowiska uruchomieniowego i dostawcę podsumowania.</span><span class="sxs-lookup"><span data-stu-id="12407-103">The common language runtime (CLR) has two providers: the runtime provider and the rundown provider.</span></span>  
  
 <span data-ttu-id="12407-104">Dostawca środowiska uruchomieniowego wywołuje zdarzenia, w zależności od tego, które są włączone słów kluczowych (kategorie zdarzeń).</span><span class="sxs-lookup"><span data-stu-id="12407-104">The runtime provider raises events, depending on which keywords (categories of events) are enabled.</span></span> <span data-ttu-id="12407-105">Na przykład, można zbierać zdarzenia ładowania, włączając `LoaderKeyword` — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="12407-105">For example, you can collect loader events by enabling the `LoaderKeyword` keyword.</span></span>  
  
 <span data-ttu-id="12407-106">Zdarzenia śledzenia dla zdarzeń Windows (ETW) są rejestrowane w pliku z rozszerzeniem .etl, które później mogą być używane po tej operacji przetwarzane w plikach wartości rozdzielanych przecinkami (CSV), zgodnie z potrzebami.</span><span class="sxs-lookup"><span data-stu-id="12407-106">Event tracking for Windows (ETW) events are logged into a file that has an .etl extension, which can later be post-processed in comma-separated value (.csv) files as needed.</span></span> <span data-ttu-id="12407-107">Aby uzyskać informacje o sposobie konwertowania pliku etl do pliku CSV, zobacz [kontrolowanie rejestrowania .NET Framework](../../../docs/framework/performance/controlling-logging.md).</span><span class="sxs-lookup"><span data-stu-id="12407-107">For information about how to convert the .etl file to a .csv file, see [Controlling .NET Framework Logging](../../../docs/framework/performance/controlling-logging.md).</span></span>  
  
## <a name="the-runtime-provider"></a><span data-ttu-id="12407-108">Dostawca środowiska uruchomieniowego</span><span class="sxs-lookup"><span data-stu-id="12407-108">The Runtime Provider</span></span>  
 <span data-ttu-id="12407-109">Dostawca środowiska uruchomieniowego jest głównym dostawcą CLR ETW.</span><span class="sxs-lookup"><span data-stu-id="12407-109">The runtime provider is the main CLR ETW provider.</span></span>  
  
 <span data-ttu-id="12407-110">Identyfikator GUID dostawcy środowiska uruchomieniowego CLR to e13c0d23-ccbc-4e12-931b-d9cc2eee27e4.</span><span class="sxs-lookup"><span data-stu-id="12407-110">The CLR runtime provider GUID is e13c0d23-ccbc-4e12-931b-d9cc2eee27e4.</span></span>  
  
 <span data-ttu-id="12407-111">Aby zapoznać się z przykładami sposobu rejestrowania i przeglądania zdarzeń CLR ETW przy użyciu powszechnie dostępnych narzędzi, zobacz [kontrolowanie rejestrowania .NET Framework](../../../docs/framework/performance/controlling-logging.md).</span><span class="sxs-lookup"><span data-stu-id="12407-111">For examples of how to log and view CLR ETW events by using commonly available tools, see [Controlling .NET Framework Logging](../../../docs/framework/performance/controlling-logging.md).</span></span>  
  
 <span data-ttu-id="12407-112">Oprócz używania słów kluczowych, takich jak `LoaderKeyword`, może zajść potrzeba włączenia słów kluczowych do rejestrowania zdarzeń, które mogą być wywoływane zbyt często.</span><span class="sxs-lookup"><span data-stu-id="12407-112">In addition to using keywords such as `LoaderKeyword`, you may have to enable keywords for logging events that may be raised too frequently.</span></span> <span data-ttu-id="12407-113">`StartEnumerationKeyword` i `EndEnumerationKeyword` słowa kluczowe włączają te zdarzenia i są podsumowane w [słowa kluczowe CLR ETW i poziomy](../../../docs/framework/performance/clr-etw-keywords-and-levels.md).</span><span class="sxs-lookup"><span data-stu-id="12407-113">The `StartEnumerationKeyword` and the `EndEnumerationKeyword` keywords enable these events and are summarized in [CLR ETW Keywords and Levels](../../../docs/framework/performance/clr-etw-keywords-and-levels.md).</span></span>  
  
## <a name="the-rundown-provider"></a><span data-ttu-id="12407-114">Dostawca podsumowań</span><span class="sxs-lookup"><span data-stu-id="12407-114">The Rundown Provider</span></span>  
 <span data-ttu-id="12407-115">Dostawca podsumowań musi być włączona dla niektórych zastosowań specjalnych.</span><span class="sxs-lookup"><span data-stu-id="12407-115">The rundown provider must be turned on for certain special-purpose uses.</span></span> <span data-ttu-id="12407-116">Jednak w przypadku większości użytkowników dostawca środowiska uruchomieniowego powinien wystarczyć.</span><span class="sxs-lookup"><span data-stu-id="12407-116">However, for a majority of users, the runtime provider should suffice.</span></span>  
  
 <span data-ttu-id="12407-117">Identyfikator GUID dostawcy podsumowań CLR jest A669021C-C450-4609-A035-5AF59AF4DF18.</span><span class="sxs-lookup"><span data-stu-id="12407-117">The CLR rundown provider GUID is A669021C-C450-4609-A035-5AF59AF4DF18.</span></span>  
  
 <span data-ttu-id="12407-118">Zwykle rejestrowanie ETW jest włączane, zanim proces zostanie uruchomiony, a rejestrowanie jest wyłączone, po zakończeniu procesu.</span><span class="sxs-lookup"><span data-stu-id="12407-118">Normally, ETW logging is enabled before a process launches, and the logging is turned off after the process exits.</span></span> <span data-ttu-id="12407-119">Jednakże jeśli rejestrowanie ETW jest włączona, gdy proces jest wykonywany, potrzebne są dodatkowe informacje o procesie.</span><span class="sxs-lookup"><span data-stu-id="12407-119">However, if ETW logging is turned on while the process is executing, additional information is needed about the process.</span></span> <span data-ttu-id="12407-120">Na przykład dla rozpoznawania symboli należy rejestrować zdarzenia metody dla metod, które zostały już załadowane przed włączeniem rejestrowania.</span><span class="sxs-lookup"><span data-stu-id="12407-120">For example, for symbol resolution you have to log method events for methods that were already loaded before logging was turned on.</span></span>  
  
 <span data-ttu-id="12407-121">`DCStart` i `DCEnd` zdarzenia przechwytują stan procesu, gdy zbieranie danych zostało uruchomione i zatrzymane.</span><span class="sxs-lookup"><span data-stu-id="12407-121">The `DCStart` and `DCEnd` events capture the state of the process when data collection was started and stopped.</span></span> <span data-ttu-id="12407-122">(Stan odnosi się do informacji na wysokim poziomie, łącznie z metod, które zostały już just-in-time (JIT) skompilowane i zestawy, które zostały załadowane). Te dwa zdarzenia mogą dostarczyć informacji o tym co już stało się w procesie; na przykład, które metody były skompilowane JIT, i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="12407-122">(State refers to information at a high level, including the methods that were already just-in-time (JIT) compiled and assemblies that were loaded.) These two events can provide information about what has already happened in the process; for example, which methods were JIT- compiled, and so on.</span></span>  
  
 <span data-ttu-id="12407-123">Tylko zdarzenia o `DC`, `DCStart`, `DCEnd`, lub `DCInit` w nazwach są wywoływane w obszarze dostawcy podsumowań.</span><span class="sxs-lookup"><span data-stu-id="12407-123">Only the events with `DC`, `DCStart`, `DCEnd`, or `DCInit` in their names are raised under the rundown provider.</span></span> <span data-ttu-id="12407-124">Ponadto te zdarzenia są wywoływane tylko w obszarze dostawcy podsumowań.</span><span class="sxs-lookup"><span data-stu-id="12407-124">Additionally, these events are raised only under the rundown provider.</span></span>  
  
 <span data-ttu-id="12407-125">Oprócz filtrów słów kluczowych zdarzeń dostawca podsumowań obsługuje także `StartRundownKeyword` i `EndRundownKeyword` słów kluczowych, aby zapewnić ukierunkowane filtrowanie.</span><span class="sxs-lookup"><span data-stu-id="12407-125">In addition to the event keyword filters, the rundown provider also supports the `StartRundownKeyword` and `EndRundownKeyword` keywords to provide targeted filtering.</span></span>  
  
### <a name="start-rundown"></a><span data-ttu-id="12407-126">Początek podsumowania</span><span class="sxs-lookup"><span data-stu-id="12407-126">Start Rundown</span></span>  
 <span data-ttu-id="12407-127">Początek podsumowania jest wyzwalany, gdy jest włączone rejestrowanie dostawcy podsumowań, ze `StartRundownKeyword` — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="12407-127">A start rundown is triggered when logging under the rundown provider is enabled with the `StartRundownKeyword` keyword.</span></span> <span data-ttu-id="12407-128">Powoduje to, że `DCStart` zdarzenia i przechwytuje stan systemu.</span><span class="sxs-lookup"><span data-stu-id="12407-128">This causes the `DCStart` event to be raised, and captures the state of the system.</span></span> <span data-ttu-id="12407-129">Przed rozpoczęciem wyliczania `DCStartInit` zdarzenie jest wywoływane.</span><span class="sxs-lookup"><span data-stu-id="12407-129">Before the start of the enumeration, the `DCStartInit` event is raised.</span></span> <span data-ttu-id="12407-130">Na koniec wyliczenia `DCStartComplete` zdarzenie jest zgłaszane, aby powiadomić kontrolera że zbieranie danych zostało zakończone normalnie.</span><span class="sxs-lookup"><span data-stu-id="12407-130">At the end of the enumeration, the `DCStartComplete` event is raised to notify the controller that data collection terminated normally.</span></span>  
  
### <a name="end-rundown"></a><span data-ttu-id="12407-131">Koniec podsumowania</span><span class="sxs-lookup"><span data-stu-id="12407-131">End Rundown</span></span>  
 <span data-ttu-id="12407-132">Koniec podsumowania jest wyzwalany, gdy jest włączone rejestrowanie dostawcy podsumowań, ze `EndRundownKeyword` — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="12407-132">An end rundown is triggered when logging under the rundown provider is enabled with the `EndRundownKeyword` keyword.</span></span> <span data-ttu-id="12407-133">Koniec podsumowania zatrzymuje profilowanie procesu, którego wykonywanie będzie kontynuowane.</span><span class="sxs-lookup"><span data-stu-id="12407-133">End rundown stops profiling on a process that continues to execute.</span></span> <span data-ttu-id="12407-134">`DCEnd` Zdarzenia Przechwytywanie stanu systemu po zatrzymaniu profilowania.</span><span class="sxs-lookup"><span data-stu-id="12407-134">The `DCEnd` events capture the state of the system when profiling is stopped.</span></span>  
  
 <span data-ttu-id="12407-135">Przed rozpoczęciem wyliczania `DCEndInit` zdarzenie jest wywoływane.</span><span class="sxs-lookup"><span data-stu-id="12407-135">Before the start of the enumeration, the `DCEndInit` event is raised.</span></span> <span data-ttu-id="12407-136">Na koniec wyliczenia `DCEndComplete` zdarzenie jest zgłaszane, aby powiadomić konsumenta, że zbieranie danych zostało zakończone normalnie.</span><span class="sxs-lookup"><span data-stu-id="12407-136">At the end of the enumeration, the `DCEndComplete` event is raised to notify the consumer that data collection terminated normally.</span></span> <span data-ttu-id="12407-137">Początek podsumowania i koniec podsumowania są głównie używane do rozpoznawania symboli zarządzanych.</span><span class="sxs-lookup"><span data-stu-id="12407-137">Start rundown and end rundown are primarily used for managed symbol resolution.</span></span> <span data-ttu-id="12407-138">Początek podsumowania może dostarczyć informacji o zakresie adresów dla metod, które zostały już skompilowane JIT przed uruchomieniem sesji profilowania.</span><span class="sxs-lookup"><span data-stu-id="12407-138">Start rundown can provide address range information for methods that were already JIT-compiled before the profiling session was started.</span></span> <span data-ttu-id="12407-139">Koniec podsumowania może dostarczyć informacji o zakresie adresów dla wszystkich metod, które zostały skompilowane JIT gdy profilowanie ma być wyłączona.</span><span class="sxs-lookup"><span data-stu-id="12407-139">End rundown can provide address range information for all methods that have been JIT-compiled when profiling is about to be turned off.</span></span>  
  
 <span data-ttu-id="12407-140">Koniec podsumowania nie odbywa się automatycznie po zatrzymaniu sesji profilowania.</span><span class="sxs-lookup"><span data-stu-id="12407-140">End rundown does not happen automatically when a profiling session is stopped.</span></span> <span data-ttu-id="12407-141">Zamiast tego narzędzia, które dąży do wykonania rozpoznania symboli zarządzanych musi jawnie wywołać sesję dostawcy podsumowań CLR z `EndRundownKeyword` — słowo kluczowe włączone, przed zatrzymaniem profilowania.</span><span class="sxs-lookup"><span data-stu-id="12407-141">Instead, a tool that is seeking to perform managed symbol resolution has to explicitly invoke a CLR rundown provider session with the `EndRundownKeyword` keyword enabled, just before profiling is stopped.</span></span>  
  
 <span data-ttu-id="12407-142">Chociaż Początek podsumowania i koniec podsumowania zapewniają informacje dotyczące zakresów adresów metody dla rozpoznawania symboli zarządzanych, zaleca się używanie `EndRundownKeyword` — słowo kluczowe (które dostarcza `DCEnd` zdarzenia) zamiast `StartRundownKeyword` — słowo kluczowe (który dostarcza `DCStart` zdarzenia).</span><span class="sxs-lookup"><span data-stu-id="12407-142">Although either start rundown or end rundown can provide method address range information for managed symbol resolution, we recommend that you use the `EndRundownKeyword` keyword (which supplies `DCEnd` events) instead of the `StartRundownKeyword` keyword (which supplies `DCStart` events).</span></span> <span data-ttu-id="12407-143">Za pomocą `StartRundownKeyword` powoduje wywołanie podsumowania podczas sesji profilowania, co może zakłócać profilowany scenariusz.</span><span class="sxs-lookup"><span data-stu-id="12407-143">Using `StartRundownKeyword` causes the rundown to happen during the profiling session, which may disturb the profiled scenario.</span></span>  
  
## <a name="etw-data-collection-using-runtime-and-rundown-providers"></a><span data-ttu-id="12407-144">Zbieranie danych ETW za pomocą środowiska uruchomieniowego i podsumowania dostawców</span><span class="sxs-lookup"><span data-stu-id="12407-144">ETW Data Collection Using Runtime and Rundown Providers</span></span>  
 <span data-ttu-id="12407-145">Poniższy przykład pokazuje sposób użycia dostawcy podsumowania CLR w sposób umożliwiający rozpoznawanie symboli procesów zarządzanych z minimalnym wpływem, niezależnie od tego, czy procesy rozpoczęły lub zakończyły się wewnątrz lub poza oknem profilowania.</span><span class="sxs-lookup"><span data-stu-id="12407-145">The following example demonstrates how to use the CLR rundown provider in a way that allows symbol resolution of managed processes with minimal impact, regardless of whether the processes start or end inside or outside the profiled window.</span></span>  
  
1. <span data-ttu-id="12407-146">Włącz rejestrowanie ETW za pomocą dostawcy środowiska uruchomieniowego CLR:</span><span class="sxs-lookup"><span data-stu-id="12407-146">Turn on ETW logging by using the CLR runtime provider:</span></span>  
  
    ```  
    xperf -start clr -on e13c0d23-ccbc-4e12-931b-d9cc2eee27e4:0x1CCBD:0x5 -f clr1.etl      
    ```  
  
     <span data-ttu-id="12407-147">Dziennik zostanie zapisany do pliku clr1.etl.</span><span class="sxs-lookup"><span data-stu-id="12407-147">The log will be saved to the clr1.etl file.</span></span>  
  
2. <span data-ttu-id="12407-148">Aby zatrzymać profilowanie, podczas gdy proces kontynuuje wykonywanie, Rozpocznij dostawcę podsumowań do przechwytywania `DCEnd` zdarzenia:</span><span class="sxs-lookup"><span data-stu-id="12407-148">To stop profiling while the process continues to execute, start the rundown provider to capture the `DCEnd` events:</span></span>  
  
    ```  
    xperf -start clrRundown -on A669021C-C450-4609-A035-5AF59AF4DF18:0xB8:0x5 -f clr2.etl      
    ```  
  
     <span data-ttu-id="12407-149">Umożliwia to kolekcja `DCEnd` zdarzenia, które można uruchomić sesji podsumowania.</span><span class="sxs-lookup"><span data-stu-id="12407-149">This enables the collection of `DCEnd` events to start a rundown session.</span></span> <span data-ttu-id="12407-150">Należy poczekać 30 do 60 sekund, aby wszystkie zdarzenia mają być zbierane.</span><span class="sxs-lookup"><span data-stu-id="12407-150">You may need to wait 30 to 60 seconds for all events to be collected.</span></span> <span data-ttu-id="12407-151">Dziennik zostanie zapisany do pliku clr1.et2.</span><span class="sxs-lookup"><span data-stu-id="12407-151">The log will be saved to the clr1.et2 file.</span></span>  
  
3. <span data-ttu-id="12407-152">Wyłącz wszystkie profilowania ETW:</span><span class="sxs-lookup"><span data-stu-id="12407-152">Turn off all ETW profiling:</span></span>  
  
    ```  
    xperf -stop clrRundown   
    xperf -stop clr  
    ```  
  
4. <span data-ttu-id="12407-153">Scal profile Aby utworzyć jeden plik dziennika:</span><span class="sxs-lookup"><span data-stu-id="12407-153">Merge the profiles to create one log file:</span></span>  
  
    ```  
    xperf -merge clr1.etl clr2.etl merged.etl  
    ```  
  
     <span data-ttu-id="12407-154">Plik merged.etl zawiera zdarzenia środowiska uruchomieniowego i sesje dostawcy podsumowań.</span><span class="sxs-lookup"><span data-stu-id="12407-154">The merged.etl file will contain the events from the runtime and the rundown provider sessions.</span></span>  
  
 <span data-ttu-id="12407-155">Narzędzie może wykonać kroki 2 i 3 (uruchamianie sesji podsumowania, a następnie zakończenie profilowania) zamiast natychmiastowego wyłączenia profilowania, gdy użytkownik żądania zatrzymania profilowania.</span><span class="sxs-lookup"><span data-stu-id="12407-155">A tool can execute steps 2 and 3 (starting a rundown session and then terminating profiling) instead of immediately turning off profiling when a user requests profiling to be stopped.</span></span> <span data-ttu-id="12407-156">To narzędzie może również wykonać krok 4.</span><span class="sxs-lookup"><span data-stu-id="12407-156">A tool can also execute step 4.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="12407-157">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="12407-157">See also</span></span>

- [<span data-ttu-id="12407-158">Zdarzenia ETW w środowisku CLR</span><span class="sxs-lookup"><span data-stu-id="12407-158">ETW Events in the Common Language Runtime</span></span>](../../../docs/framework/performance/etw-events-in-the-common-language-runtime.md)
