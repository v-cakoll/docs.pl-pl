---
title: Napisać niestandardowego hosta środowiska uruchomieniowego .NET Core
description: Dowiedz się, obsługa środowiska uruchomieniowego .NET Core z kodu natywnego, aby wspierać zaawansowane scenariusze, które wymagają kontroli, jak działa środowisko uruchomieniowe platformy .NET Core.
author: mjrousos
ms.date: 12/21/2018
ms.custom: seodec18
ms.openlocfilehash: 5b783bf7a5da55a3b5dada8ed024069f5fe3d3ba
ms.sourcegitcommit: 4c41ec195caf03d98b7900007c3c8e24eba20d34
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/20/2019
ms.locfileid: "67267848"
---
# <a name="write-a-custom-net-core-host-to-control-the-net-runtime-from-your-native-code"></a><span data-ttu-id="a4f8a-103">Napisać niestandardowego hosta platformy .NET Core do kontrolowania środowiska uruchomieniowego .NET z kodu natywnego</span><span class="sxs-lookup"><span data-stu-id="a4f8a-103">Write a custom .NET Core host to control the .NET runtime from your native code</span></span>

<span data-ttu-id="a4f8a-104">Podobnie jak wszystkie kodu zarządzanego aplikacje platformy .NET Core są wykonywane przez hosta.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-104">Like all managed code, .NET Core applications are executed by a host.</span></span> <span data-ttu-id="a4f8a-105">Host jest odpowiedzialny za uruchamianie środowiska uruchomieniowego (takie jak składników, takich jak JIT i moduł wyrzucania elementów bezużytecznych) i wywołując punkty wejścia zarządzanych.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-105">The host is responsible for starting the runtime (including components like the JIT and garbage collector) and invoking managed entry points.</span></span>

<span data-ttu-id="a4f8a-106">Hostowanie środowiska uruchomieniowego .NET Core to zaawansowany scenariusz, i w większości przypadków platformy .NET Core, deweloperzy nie muszą się martwić o hostingu, ponieważ procesy kompilacji platformy .NET Core zapewniają domyślnego hosta do uruchamiania aplikacji .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-106">Hosting the .NET Core runtime is an advanced scenario and, in most cases, .NET Core developers don't need to worry about hosting because .NET Core build processes provide a default host to run .NET Core applications.</span></span> <span data-ttu-id="a4f8a-107">W niektórych sytuacjach specjalistyczne jednak może być przydatne do jawnie hosta środowiska uruchomieniowego .NET Core, jako środek wywołanie kodu zarządzanego, natywnego procesu lub Aby uzyskać większą kontrolę nad jak działa środowisko uruchomieniowe.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-107">In some specialized circumstances, though, it can be useful to explicitly host the .NET Core runtime, either as a means of invoking managed code in a native process or in order to gain more control over how the runtime works.</span></span>

<span data-ttu-id="a4f8a-108">Ten artykuł zawiera omówienie kroków niezbędnych do środowiska uruchomieniowego .NET Core z kodu natywnego, a wykonanie kodu zarządzanego w nim.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-108">This article gives an overview of the steps necessary to start the .NET Core runtime from native code and execute managed code in it.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="a4f8a-109">Wymagania wstępne</span><span class="sxs-lookup"><span data-stu-id="a4f8a-109">Prerequisites</span></span>

<span data-ttu-id="a4f8a-110">Ponieważ hosty znajdują się aplikacje natywne, w tym samouczku opisano tworzenie hosta platformy .NET Core w języku C++ aplikacji.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-110">Because hosts are native applications, this tutorial will cover constructing a C++ application to host .NET Core.</span></span> <span data-ttu-id="a4f8a-111">Konieczne będzie środowisko programistyczne języka C++ (takim jak udostępniany przez [programu Visual Studio](https://aka.ms/vsdownload?utm_source=mscom&utm_campaign=msdocs)).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-111">You will need a C++ development environment (such as that provided by [Visual Studio](https://aka.ms/vsdownload?utm_source=mscom&utm_campaign=msdocs)).</span></span>

<span data-ttu-id="a4f8a-112">Możesz też prostą aplikację platformy .NET Core, aby przetestować hosta, więc należy zainstalować [zestawu .NET Core SDK](https://www.microsoft.com/net/core) i [tworzenie małych testowanie aplikacji platformy .NET Core](../../core/tutorials/with-visual-studio.md) (np. aplikacji "Hello World").</span><span class="sxs-lookup"><span data-stu-id="a4f8a-112">You will also want a simple .NET Core application to test the host with, so you should install the [.NET Core SDK](https://www.microsoft.com/net/core) and [build a small .NET Core test app](../../core/tutorials/with-visual-studio.md) (such as a 'Hello World' app).</span></span> <span data-ttu-id="a4f8a-113">Aplikacja "Hello World" utworzony przez nowy szablon Projekt konsoli .NET Core jest wystarczająca.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-113">The 'Hello World' app created by the new .NET Core console project template is sufficient.</span></span>

## <a name="hosting-apis"></a><span data-ttu-id="a4f8a-114">Interfejsy API hostingu</span><span class="sxs-lookup"><span data-stu-id="a4f8a-114">Hosting APIs</span></span>
<span data-ttu-id="a4f8a-115">Istnieją trzy różne interfejsy API, które mogą służyć do hosta platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-115">There are three different APIs that can be used to host .NET Core.</span></span> <span data-ttu-id="a4f8a-116">Ten dokument (i skojarzone [przykłady](https://github.com/dotnet/samples/tree/master/core/hosting)) obejmuje wszystkie opcje.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-116">This document (and its associated [samples](https://github.com/dotnet/samples/tree/master/core/hosting)) cover all options.</span></span>

* <span data-ttu-id="a4f8a-117">Hostowanie środowiska uruchomieniowego .NET Core w środowisku .NET Core 3.0 i nowszych preferowaną metodą jest `nethost` i `hostfxr` bibliotek interfejsów API.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-117">The preferred method of hosting the .NET Core runtime in .NET Core 3.0 and above is with the `nethost` and `hostfxr` libraries' APIs.</span></span> <span data-ttu-id="a4f8a-118">Te punkty wejścia złożoności znajdowania i konfigurowania rutime inicjowania obsługi i zezwolić na uruchamianie aplikacji zarządzanych i wywoływanie statycznej metody zarządzanych.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-118">These entry points handle the complexity of finding and setting up the rutime for initialization and allow both launching a managed application and calling into a static managed method.</span></span>
* <span data-ttu-id="a4f8a-119">Hostowanie środowiska uruchomieniowego .NET Core, .NET Core 3.0 — przed preferowaną metodą jest [CoreClrHost.h](https://github.com/dotnet/coreclr/blob/master/src/coreclr/hosts/inc/coreclrhost.h) interfejsu API.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-119">The preferred method of hosting the .NET Core runtime prior to .NET Core 3.0 is with the [CoreClrHost.h](https://github.com/dotnet/coreclr/blob/master/src/coreclr/hosts/inc/coreclrhost.h) API.</span></span> <span data-ttu-id="a4f8a-120">Ten interfejs API udostępnia funkcje łatwe uruchamianie i zatrzymywanie środowiska uruchomieniowego i wywoływania zarządzanego kodu, (albo poprzez uruchomienie zarządzanego pliku exe albo przez wywołanie metody statyczne zarządzanych).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-120">This API exposes functions for easily starting and stopping the runtime and invoking managed code (either by launching a managed exe or by calling static managed methods).</span></span>
* <span data-ttu-id="a4f8a-121">.NET core może też być hostowana przy użyciu `ICLRRuntimeHost4` interfejsu w [mscoree.h](https://github.com/dotnet/coreclr/blob/master/src/pal/prebuilt/inc/mscoree.h).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-121">.NET Core can also be hosted with the `ICLRRuntimeHost4` interface in [mscoree.h](https://github.com/dotnet/coreclr/blob/master/src/pal/prebuilt/inc/mscoree.h).</span></span> <span data-ttu-id="a4f8a-122">Ten interfejs API został wokół dłużej niż CoreClrHost.h, więc widoczne starsze hosty korzystania z niego.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-122">This API has been around longer than CoreClrHost.h, so you may have seen older hosts using it.</span></span> <span data-ttu-id="a4f8a-123">Nadal działa i umożliwia nieco większą kontrolę nad procesu hostingu niż CoreClrHost.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-123">It still works and allows a bit more control over the hosting process than CoreClrHost.</span></span> <span data-ttu-id="a4f8a-124">W przypadku większości scenariuszy jednak CoreClrHost.h jest preferowana, teraz ze względu na jej prostsza interfejsów API.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-124">For most scenarios, though, CoreClrHost.h is preferred now because of its simpler APIs.</span></span>

## <a name="sample-hosts"></a><span data-ttu-id="a4f8a-125">Przykładowy Hosts</span><span class="sxs-lookup"><span data-stu-id="a4f8a-125">Sample Hosts</span></span>
<span data-ttu-id="a4f8a-126">[Przykładowy hosts](https://github.com/dotnet/samples/tree/master/core/hosting) ukazujące czynności opisanych w samouczkach poniżej są dostępne w repozytorium dotnet/samples w witrynie GitHub.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-126">[Sample hosts](https://github.com/dotnet/samples/tree/master/core/hosting) demonstrating the steps outlined in the tutorials below are available in the dotnet/samples GitHub repository.</span></span> <span data-ttu-id="a4f8a-127">Komentarze w przykładach wyraźnie skojarzyć ponumerowanych kroków z tych samouczków, z której jest przeprowadzane w próbce.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-127">Comments in the samples clearly associate the numbered steps from these tutorials with where they're performed in the sample.</span></span> <span data-ttu-id="a4f8a-128">Aby uzyskać instrukcje pobierania, zobacz [przykłady i samouczki](../../samples-and-tutorials/index.md#viewing-and-downloading-samples).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-128">For download instructions, see [Samples and Tutorials](../../samples-and-tutorials/index.md#viewing-and-downloading-samples).</span></span>

<span data-ttu-id="a4f8a-129">Należy pamiętać, że hosty próbki są przeznaczone do służyć do celów, szkoleniowych, dzięki czemu są światło na sprawdzanie błędów i są przeznaczone do wyróżnienia czytelności za pośrednictwem wydajność.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-129">Keep in mind that the sample hosts are meant to be used for learning purposes, so they are light on error checking and are designed to emphasize readability over efficiency.</span></span>

## <a name="create-a-host-using-nethosth-and-hostfxrh"></a><span data-ttu-id="a4f8a-130">Tworzenie hosta przy użyciu NetHost.h i HostFxr.h</span><span class="sxs-lookup"><span data-stu-id="a4f8a-130">Create a host using NetHost.h and HostFxr.h</span></span>

<span data-ttu-id="a4f8a-131">Poniższe kroki zawierają szczegółowe instrukcje dotyczące `nethost` i `hostfxr` biblioteki, aby uruchomić środowisko uruchomieniowe platformy .NET Core w aplikacji natywnej i wywołania do zarządzanej metody statycznej.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-131">The following steps detail how to use the `nethost` and `hostfxr` libraries to start the .NET Core runtime in a native application and call into a managed static method.</span></span> <span data-ttu-id="a4f8a-132">[Przykładowe](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithHostFxr) używa `nethost` nagłówków i bibliotek, instalowane przy użyciu zestawu SDK platformy .NET i kopie [ `coreclr_delegates.h` ](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/coreclr_delegates.h) i [ `hostfxr.h` ](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) pliki z [core-dotnet-setup](https://github.com/dotnet/core-setup) repozytorium.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-132">The [sample](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithHostFxr) uses the `nethost` header and library installed with the .NET SDK and copies of the [`coreclr_delegates.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/coreclr_delegates.h) and [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) files from the [dotnet/core-setup](https://github.com/dotnet/core-setup) repository.</span></span>

### <a name="step-1---load-hostfxr-and-get-exported-hosting-functions"></a><span data-ttu-id="a4f8a-133">Krok 1 — ładowanie HostFxr i eksportowane funkcje hostingu</span><span class="sxs-lookup"><span data-stu-id="a4f8a-133">Step 1 - Load HostFxr and get exported hosting functions</span></span>

<span data-ttu-id="a4f8a-134">`nethost` Biblioteka zapewnia `get_hostfxr_path` funkcji do lokalizowania `hostfxr` biblioteki.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-134">The `nethost` library provides the `get_hostfxr_path` function for locating the `hostfxr` library.</span></span> <span data-ttu-id="a4f8a-135">`hostfxr` Biblioteki udostępnia funkcje hostingu środowiska uruchomieniowego .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-135">The `hostfxr` library exposes functions for hosting the .NET Core runtime.</span></span> <span data-ttu-id="a4f8a-136">Pełną listę funkcji można znaleźć w [ `hostfxr.h` ](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) i [dokumentu macierzystego projektowania hostingu](https://github.com/dotnet/core-setup/blob/master/Documentation/design-docs/native-hosting.md).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-136">The full list of functions can be found in [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) and the [native hosting design document](https://github.com/dotnet/core-setup/blob/master/Documentation/design-docs/native-hosting.md).</span></span> <span data-ttu-id="a4f8a-137">Próbki i w tym samouczku należy użyć następującego polecenia:</span><span class="sxs-lookup"><span data-stu-id="a4f8a-137">The sample and this tutorial use the following:</span></span>
* <span data-ttu-id="a4f8a-138">`hostfxr_initialize_for_runtime_config`: Inicjalizuje kontekst hosta i przygotowanie do inicjowania środowiska uruchomieniowego .NET Core za pomocą konfiguracji określonego środowiska uruchomieniowego.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-138">`hostfxr_initialize_for_runtime_config`: Initializes a host context and prepares for initialization of the .NET Core runtime using the specified runtime configuration.</span></span>
* <span data-ttu-id="a4f8a-139">`hostfxr_get_runtime_delegate`: Pobiera obiekt delegowany funkcjonalność aparatu plików wykonywalnych.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-139">`hostfxr_get_runtime_delegate`: Gets a delegate for runtime functionality.</span></span>
* <span data-ttu-id="a4f8a-140">`hostfxr_close`: Zamyka kontekstu hosta.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-140">`hostfxr_close`: Closes a host context.</span></span>

<span data-ttu-id="a4f8a-141">`hostfxr` Biblioteki znajduje się za pomocą `get_hostfxr_path`.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-141">The `hostfxr` library is found using `get_hostfxr_path`.</span></span> <span data-ttu-id="a4f8a-142">Następnie załadowaniu i wywozu są pobierane.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-142">It is then loaded and its exports are retrieved.</span></span>

[!code-cpp[HostFxrHost#LoadHostFxr](~/samples/core/hosting/HostWithHostFxr/src/NativeHost/nativehost.cpp#LoadHostFxr)]

### <a name="step-2---initialize-and-start-the-net-core-runtime"></a><span data-ttu-id="a4f8a-143">Krok 2 — inicjowanie i uruchom środowisko uruchomieniowe platformy .NET Core</span><span class="sxs-lookup"><span data-stu-id="a4f8a-143">Step 2 - Initialize and start the .NET Core runtime</span></span>

<span data-ttu-id="a4f8a-144">`hostfxr_initialize_for_runtime_config` i `hostfxr_get_runtime_delegate` funkcje inicjuje i uruchamia środowisko uruchomieniowe platformy .NET Core za pomocą konfiguracji czasu wykonywania zarządzanego składnika, który zostanie załadowany.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-144">The `hostfxr_initialize_for_runtime_config` and `hostfxr_get_runtime_delegate` functions initialize and start the .NET Core runtime using the runtime configuration for the managed component that will be loaded.</span></span> <span data-ttu-id="a4f8a-145">`hostfxr_get_runtime_delegate` Funkcja jest używana do pobierania delegata obsługi, który umożliwia ładowanie zestawu zarządzanego i wprowadzenie wskaźnik funkcji na metodę statyczną, w tym zestawie.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-145">The `hostfxr_get_runtime_delegate` function is used to get a runtime delegate that allows loading a managed assembly and getting a function pointer to a static method in that assembly.</span></span>

[!code-cpp[HostFxrHost#Initialize](~/samples/core/hosting/HostWithHostFxr/src/NativeHost/nativehost.cpp#Initialize)]

### <a name="step-3---load-managed-assembly-and-get-function-pointer-to-a-managed-method"></a><span data-ttu-id="a4f8a-146">Krok 3 — ładowanie zestawu zarządzanego i wskaźnik funkcji zarządzanej metody get</span><span class="sxs-lookup"><span data-stu-id="a4f8a-146">Step 3 - Load managed assembly and get function pointer to a managed method</span></span>

<span data-ttu-id="a4f8a-147">Delegata środowiska uruchomieniowego jest wywoływana, aby załadować zestaw zarządzany i Uzyskaj wskaźnik funkcji zarządzanej metody.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-147">The runtime delegate is called to load the managed assembly and get a function pointer to a managed method.</span></span> <span data-ttu-id="a4f8a-148">Delegat wymaga ścieżka zestawu, wpisz nazwę i nazwę metody jako dane wejściowe i zwraca wskaźnik funkcji, który może służyć do wywołania metody zarządzanych.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-148">The delegate requires the assembly path, type name, and method name as inputs and returns a function pointer that can be used to invoke the managed method.</span></span>

[!code-cpp[HostFxrHost#LoadAndGet](~/samples/core/hosting/HostWithHostFxr/src/NativeHost/nativehost.cpp#LoadAndGet)]

<span data-ttu-id="a4f8a-149">Przekazując `nullptr` jako nazwy typu delegata podczas wywoływania delegata środowiska uruchomieniowego, w przykładzie użyto domyślny podpis zarządzanej metody:</span><span class="sxs-lookup"><span data-stu-id="a4f8a-149">By passing `nullptr` as the delegate type name when calling the runtime delegate, the sample uses a default signature for the managed method:</span></span>
```C#
public delegate int ComponentEntryPoint(IntPtr args, int sizeBytes);
```
<span data-ttu-id="a4f8a-150">Inny podpis mogą służyć podczas wywoływania delegata środowiska uruchomieniowego, określając nazwę typu delegata.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-150">A different signature can be used by specifying the delegate type name when calling the runtime delegate.</span></span>

### <a name="step-4---run-managed-code"></a><span data-ttu-id="a4f8a-151">Krok 4 — przebieg zarządzanego kodu!</span><span class="sxs-lookup"><span data-stu-id="a4f8a-151">Step 4 - Run managed code!</span></span>

<span data-ttu-id="a4f8a-152">Natywne hosta teraz można wywołać metodę zarządzanych i przekaż go w odpowiednie parametry.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-152">The native host can now call the managed method and pass it the desired parameters.</span></span>

[!code-cpp[HostFxrHost#CallManaged](~/samples/core/hosting/HostWithHostFxr/src/NativeHost/nativehost.cpp#CallManaged)]

## <a name="create-a-host-using-coreclrhosth"></a><span data-ttu-id="a4f8a-153">Tworzenie hosta przy użyciu CoreClrHost.h</span><span class="sxs-lookup"><span data-stu-id="a4f8a-153">Create a host using CoreClrHost.h</span></span>

<span data-ttu-id="a4f8a-154">Poniższe kroki zawierają szczegółowe instrukcje dotyczące interfejsu API CoreClrHost.h można uruchomić środowiska uruchomieniowego .NET Core w aplikacji natywnej i wywołania do zarządzanej metody statycznej.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-154">The following steps detail how to use the CoreClrHost.h API to start the .NET Core runtime in a native application and call into a managed static method.</span></span> <span data-ttu-id="a4f8a-155">Fragmenty kodu, w tym dokumencie przy użyciu niektórych interfejsów API specyficznych dla Windows, ale [hosta pełny przykład](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithCoreClrHost) zawiera ścieżki kodu systemów Windows i Linux.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-155">The code snippets in this document use some Windows-specific APIs, but the [full sample host](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithCoreClrHost) shows both Windows and Linux code paths.</span></span>

<span data-ttu-id="a4f8a-156">[Hosta Unix CoreRun](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts/unixcorerun) przedstawiono przykład bardziej złożone, rzeczywistych hostingu za pomocą coreclrhost.h.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-156">The [Unix CoreRun host](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts/unixcorerun) shows a more complex, real-world example of hosting using coreclrhost.h.</span></span>

### <a name="step-1---find-and-load-coreclr"></a><span data-ttu-id="a4f8a-157">Krok 1 — Znajdź i załaduj CoreCLR</span><span class="sxs-lookup"><span data-stu-id="a4f8a-157">Step 1 - Find and load CoreCLR</span></span>

<span data-ttu-id="a4f8a-158">Środowisko uruchomieniowe platformy .NET Core interfejsów API znajdują się w *coreclr.dll* (na Windows) w polu *libcoreclr.so* (w systemie Linux) lub *libcoreclr.dylib* (w systemie macOS).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-158">The .NET Core runtime APIs are in *coreclr.dll* (on Windows), in *libcoreclr.so* (on Linux), or in *libcoreclr.dylib* (on macOS).</span></span> <span data-ttu-id="a4f8a-159">Pierwszym krokiem do hostowanie platformy .NET Core jest można załadować biblioteki CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-159">The first step to hosting .NET Core is to load the CoreCLR library.</span></span> <span data-ttu-id="a4f8a-160">Niektóre hosty sondowania różnych ścieżek, lub użyj parametrów wejściowych, aby znaleźć biblioteki, podczas gdy inne osoby, aby go załadować z określoną ścieżką (obok hosta, na przykład, lub z lokalizacji komputera).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-160">Some hosts probe different paths or use input parameters to find the library while others know to load it from a certain path (next to the host, for example, or from a machine-wide location).</span></span>

<span data-ttu-id="a4f8a-161">Gdy zostanie znaleziony, biblioteki jest ładowany z `LoadLibraryEx` (na Windows) lub `dlopen` (w systemie Linux lub Mac).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-161">Once found, the library is loaded with `LoadLibraryEx` (on Windows) or `dlopen` (on Linux/Mac).</span></span>

[!code-cpp[CoreClrHost#1](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#1)]

### <a name="step-2---get-net-core-hosting-functions"></a><span data-ttu-id="a4f8a-162">Krok 2 — Get platformy .NET Core funkcje hostingu</span><span class="sxs-lookup"><span data-stu-id="a4f8a-162">Step 2 - Get .NET Core hosting functions</span></span>

<span data-ttu-id="a4f8a-163">CoreClrHost ma kilka ważnych metod przydatne hostowanie platformy .NET Core:</span><span class="sxs-lookup"><span data-stu-id="a4f8a-163">CoreClrHost has several important methods useful for hosting .NET Core:</span></span>

* <span data-ttu-id="a4f8a-164">`coreclr_initialize`: Uruchamia środowisko uruchomieniowe platformy .NET Core i ustawia domyślne (i tylko) obiektu AppDomain.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-164">`coreclr_initialize`: Starts the .NET Core runtime and sets up the default (and only) AppDomain.</span></span>
* <span data-ttu-id="a4f8a-165">`coreclr_execute_assembly`: Wykonuje zestaw zarządzany.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-165">`coreclr_execute_assembly`: Executes a managed assembly.</span></span>
* <span data-ttu-id="a4f8a-166">`coreclr_create_delegate`: Tworzy wskaźnik funkcji do zarządzanej metody.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-166">`coreclr_create_delegate`: Creates a function pointer to a managed method.</span></span>
* <span data-ttu-id="a4f8a-167">`coreclr_shutdown`: Zamyka środowisko uruchomieniowe platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-167">`coreclr_shutdown`: Shuts down the .NET Core runtime.</span></span>
* <span data-ttu-id="a4f8a-168">`coreclr_shutdown_2`: Podobnie jak `coreclr_shutdown`, ale również pobiera kod zarządzany kod zakończenia.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-168">`coreclr_shutdown_2`: Like `coreclr_shutdown`, but also retrieves the managed code's exit code.</span></span>

<span data-ttu-id="a4f8a-169">Po załadowaniu biblioteki CoreCLR, następnym krokiem jest pobieranie odwołań do tych funkcji przy użyciu `GetProcAddress` (na Windows) lub `dlsym` (w systemie Linux lub Mac).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-169">After loading the CoreCLR library, the next step is to get references to these functions using `GetProcAddress` (on Windows) or `dlsym` (on Linux/Mac).</span></span>

[!code-cpp[CoreClrHost#2](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#2)]

### <a name="step-3---prepare-runtime-properties"></a><span data-ttu-id="a4f8a-170">Krok 3 — Przygotowanie właściwości środowiska uruchomieniowego</span><span class="sxs-lookup"><span data-stu-id="a4f8a-170">Step 3 - Prepare runtime properties</span></span>

<span data-ttu-id="a4f8a-171">Przed rozpoczęciem środowisko uruchomieniowe, jest wymagane do przygotowania niektóre właściwości, aby określić zachowanie (szczególnie dotyczące moduł ładujący zestaw).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-171">Before starting the runtime, it is necessary to prepare some properties to specify behavior (especially concerning the assembly loader).</span></span>

<span data-ttu-id="a4f8a-172">Wspólne właściwości obejmują:</span><span class="sxs-lookup"><span data-stu-id="a4f8a-172">Common properties include:</span></span>

* <span data-ttu-id="a4f8a-173">`TRUSTED_PLATFORM_ASSEMBLIES` Jest to lista zestawu ścieżek (rozdzielonych przez ';' na Windows i ":" w systemie Linux), której środowisko uruchomieniowe będzie mieć możliwość rozpoznania domyślnie.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-173">`TRUSTED_PLATFORM_ASSEMBLIES` This is a list of assembly paths (delimited by ';' on Windows and ':' on Linux) which the runtime will be able to resolve by default.</span></span> <span data-ttu-id="a4f8a-174">Niektóre hosty ma ustaloną manifesty listę zestawów, które mogą oni ładować.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-174">Some hosts have hard-coded manifests listing assemblies they can load.</span></span> <span data-ttu-id="a4f8a-175">Inne umieści wszystkie biblioteki w określonych lokalizacjach (obok *coreclr.dll*, na przykład) na tej liście.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-175">Others will put any library in certain locations (next to *coreclr.dll*, for example) on this list.</span></span>
* <span data-ttu-id="a4f8a-176">`APP_PATHS` To jest lista ścieżek do sondowania w dla zestawu, jeśli nie można znaleźć na liście zestawów (TPA) TPM.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-176">`APP_PATHS` This is a list of paths to probe in for an assembly if it can't be found in the trusted platform assemblies (TPA) list.</span></span> <span data-ttu-id="a4f8a-177">Ponieważ host nie ma większą kontrolę nad tym, którzy zestawy są ładowane przy użyciu listy elementu TPA, jest najlepszym rozwiązaniem dla hostów, aby określić zestawy, które spełniają oczekiwane obciążenia i wyświetlać je w sposób jawny.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-177">Because the host has more control over which assemblies are loaded using the TPA list, it is a best practice for hosts to determine which assemblies they expect to load and list them explicitly.</span></span> <span data-ttu-id="a4f8a-178">Jeśli potrzebne jest badania w czasie wykonywania, jednak tej właściwości można włączyć tego scenariusza.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-178">If probing at runtime is needed, however, this property can enable that scenario.</span></span>
* <span data-ttu-id="a4f8a-179">`APP_NI_PATHS` Ta lista jest podobne do APP_PATHS, z tą różnicą, że oznaczało ma być ścieżek, które będzie sondowany dla obrazów natywnych.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-179">`APP_NI_PATHS` This list is similar to APP_PATHS except that it's meant to be paths that will be probed for native images.</span></span>
* <span data-ttu-id="a4f8a-180">`NATIVE_DLL_SEARCH_DIRECTORIES` Ta właściwość jest lista ścieżek, które moduł ładujący powinien sondowania podczas wyszukiwania dla bibliotek natywnych wywoływanym za pośrednictwem p/invoke.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-180">`NATIVE_DLL_SEARCH_DIRECTORIES` This property is a list of paths the loader should probe when looking for native libraries called via p/invoke.</span></span>
* <span data-ttu-id="a4f8a-181">`PLATFORM_RESOURCE_ROOTS` Ta lista zawiera ścieżki do sondowania w dla zestawów satelickich zasobów (w podfolderach specyficzne dla kultury).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-181">`PLATFORM_RESOURCE_ROOTS` This list includes paths to probe in for resource satellite assemblies (in culture-specific sub-directories).</span></span>

<span data-ttu-id="a4f8a-182">Na tym hoście próbki na liście elementu TPA jest tworzony po prostu wyświetlając wszystkie biblioteki w bieżącym katalogu:</span><span class="sxs-lookup"><span data-stu-id="a4f8a-182">In this sample host, the TPA list is constructed by simply listing all libraries in the current directory:</span></span>

[!code-cpp[CoreClrHost#7](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#7)]

<span data-ttu-id="a4f8a-183">Ponieważ plik jest proste, wymaga tylko `TRUSTED_PLATFORM_ASSEMBLIES` właściwości:</span><span class="sxs-lookup"><span data-stu-id="a4f8a-183">Because the sample is simple, it only needs the `TRUSTED_PLATFORM_ASSEMBLIES` property:</span></span>

[!code-cpp[CoreClrHost#3](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#3)]

### <a name="step-4---start-the-runtime"></a><span data-ttu-id="a4f8a-184">Krok 4 — uruchamianie runtime</span><span class="sxs-lookup"><span data-stu-id="a4f8a-184">Step 4 - Start the runtime</span></span>

<span data-ttu-id="a4f8a-185">W odróżnieniu od mscoree.h interfejsu API (opisanych poniżej), interfejsy API CoreCLRHost.h Uruchom środowisko uruchomieniowe i Utwórz domyślnej domeny aplikacji wszystko z jednego wywołania.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-185">Unlike the mscoree.h hosting API (described below), CoreCLRHost.h APIs start the runtime and create the default AppDomain all with a single call.</span></span> <span data-ttu-id="a4f8a-186">`coreclr_initialize` Funkcja przyjmuje ścieżki podstawowej, nazwę i właściwości opisanych wcześniej, i zwraca uchwyt z powrotem do hosta za pośrednictwem `hostHandle` parametru.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-186">The `coreclr_initialize` function takes a base path, name, and the properties described earlier and returns back a handle to the host via the `hostHandle` parameter.</span></span>

[!code-cpp[CoreClrHost#4](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#4)]

### <a name="step-5---run-managed-code"></a><span data-ttu-id="a4f8a-187">Krok 5 — wykonywania kodu zarządzanego!</span><span class="sxs-lookup"><span data-stu-id="a4f8a-187">Step 5 - Run managed code!</span></span>

<span data-ttu-id="a4f8a-188">W środowisku uruchomieniowym do hosta może wywoływać kod zarządzany.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-188">With the runtime started, the host can call managed code.</span></span> <span data-ttu-id="a4f8a-189">Można to zrobić na kilka różnych sposobów.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-189">This can be done in a couple of different ways.</span></span> <span data-ttu-id="a4f8a-190">Przykładowy kod powiązany ten samouczek używa `coreclr_create_delegate` funkcji, aby utworzyć obiekt delegowany do statycznej metody zarządzanych.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-190">The sample code linked to this tutorial uses the `coreclr_create_delegate` function to create a delegate to a static managed method.</span></span> <span data-ttu-id="a4f8a-191">Ten interfejs API [nazwy zestawu](../../framework/app-domains/assembly-names.md), nazwę typu kwalifikowanego na przestrzeń nazw i nazwę metody, jako danych wejściowych i zwraca obiekt delegowany, który może służyć do wywołania metody.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-191">This API takes the [assembly name](../../framework/app-domains/assembly-names.md), namespace-qualified type name, and method name as inputs and returns a delegate that can be used to invoke the method.</span></span>

[!code-cpp[CoreClrHost#5](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#5)]

<span data-ttu-id="a4f8a-192">W tym przykładzie hosta teraz można wywołać `managedDelegate` do uruchomienia `ManagedWorker.DoWork` metody.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-192">In this sample, the host can now call `managedDelegate` to run the `ManagedWorker.DoWork` method.</span></span>

<span data-ttu-id="a4f8a-193">Alternatywnie `coreclr_execute_assembly` funkcja może służyć do uruchamiania zarządzanego pliku wykonywalnego.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-193">Alternatively, the `coreclr_execute_assembly` function can be used to launch a managed executable.</span></span> <span data-ttu-id="a4f8a-194">Ten interfejs API ma ścieżkę zestawu i tablicę argumentów jako parametry wejściowe.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-194">This API takes an assembly path and array of arguments as input parameters.</span></span> <span data-ttu-id="a4f8a-195">Ładuje zestaw w tej ścieżce i wywołuje jego głównej metody.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-195">It loads the assembly at that path and invokes its main method.</span></span>

```C++
int hr = executeAssembly(
        hostHandle,
        domainId,
        argumentCount,
        arguments,
        "HelloWorld.exe",
        (unsigned int*)&exitCode);
```

### <a name="step-6---shutdown-and-clean-up"></a><span data-ttu-id="a4f8a-196">Krok 6 — zamykanie i oczyścić</span><span class="sxs-lookup"><span data-stu-id="a4f8a-196">Step 6 - Shutdown and clean up</span></span>

<span data-ttu-id="a4f8a-197">Na koniec, po zakończeniu uruchamiania kodu zarządzanego hosta środowiska uruchomieniowego .NET Core zostanie zamknięta za pomocą `coreclr_shutdown` lub `coreclr_shutdown_2`.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-197">Finally, when the host is done running managed code, the .NET Core runtime is shut down with `coreclr_shutdown` or `coreclr_shutdown_2`.</span></span>

[!code-cpp[CoreClrHost#6](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#6)]

<span data-ttu-id="a4f8a-198">CoreCLR nepodporuje ponowne inicjowanie lub zwalnianie.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-198">CoreCLR does not support reinitialization or unloading.</span></span> <span data-ttu-id="a4f8a-199">Nie wywołuj `coreclr_initialize` ponownie lub zwalnianie biblioteki CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-199">Do not call `coreclr_initialize` again or unload the CoreCLR library.</span></span>

## <a name="create-a-host-using-mscoreeh"></a><span data-ttu-id="a4f8a-200">Tworzenie hosta przy użyciu Mscoree.h</span><span class="sxs-lookup"><span data-stu-id="a4f8a-200">Create a host using Mscoree.h</span></span>

<span data-ttu-id="a4f8a-201">Jak wspomniano wcześniej, CoreClrHost.h jest teraz preferowana metoda hostowanie środowiska uruchomieniowego .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-201">As mentioned previously, CoreClrHost.h is now the preferred method of hosting the .NET Core runtime.</span></span> <span data-ttu-id="a4f8a-202">`ICLRRuntimeHost4` Interfejsu nadal można używać, jednak jeśli interfejsy CoreClrHost.h nie są wystarczające (w razie uruchamiania niestandardowej flagi są na przykład lub AppDomainManager jest wymagana w domyślnej domeny).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-202">The `ICLRRuntimeHost4` interface can still be used, though, if the CoreClrHost.h interfaces aren't sufficient (if non-standard startup flags are needed, for example, or if an AppDomainManager is needed on the default domain).</span></span> <span data-ttu-id="a4f8a-203">Te instrukcje poprowadzi Cię hostowanie platformy .NET Core przy użyciu mscoree.h.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-203">These instructions will guide you through hosting .NET Core using mscoree.h.</span></span>

<span data-ttu-id="a4f8a-204">[Hosta CoreRun](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts/corerun) przedstawiono przykład bardziej złożone, rzeczywistych hostingu za pomocą mscoree.h.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-204">The [CoreRun host](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts/corerun) shows a more complex, real-world example of hosting using mscoree.h.</span></span>

### <a name="a-note-about-mscoreeh"></a><span data-ttu-id="a4f8a-205">Uwaga dotycząca mscoree.h</span><span class="sxs-lookup"><span data-stu-id="a4f8a-205">A note about mscoree.h</span></span>
<span data-ttu-id="a4f8a-206">`ICLRRuntimeHost4` Platformy .NET Core hostingu interfejsu jest zdefiniowany w [MSCOREE. IDL](https://github.com/dotnet/coreclr/blob/master/src/inc/MSCOREE.IDL).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-206">The `ICLRRuntimeHost4` .NET Core hosting interface is defined in [MSCOREE.IDL](https://github.com/dotnet/coreclr/blob/master/src/inc/MSCOREE.IDL).</span></span> <span data-ttu-id="a4f8a-207">Nagłówek wersję tego pliku (mscoree.h), hosta należy do odwołania, jest generowany za pośrednictwem MIDL podczas [środowisko uruchomieniowe programu .NET Core](https://github.com/dotnet/coreclr/) jest wbudowana.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-207">A header version of this file (mscoree.h), which your host will need to reference, is produced via MIDL when the [.NET Core runtime](https://github.com/dotnet/coreclr/) is built.</span></span> <span data-ttu-id="a4f8a-208">Jeśli nie chcesz tworzyć środowiska uruchomieniowego .NET Core, mscoree.h jest również dostępny jako [wstępnie skompilowany nagłówek](https://github.com/dotnet/coreclr/tree/master/src/pal/prebuilt/inc) w repozytorium dotnet/coreclr.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-208">If you do not want to build the .NET Core runtime, mscoree.h is also available as a [pre-built header](https://github.com/dotnet/coreclr/tree/master/src/pal/prebuilt/inc) in the dotnet/coreclr repository.</span></span> <span data-ttu-id="a4f8a-209">[Instrukcje dotyczące tworzenia środowiska uruchomieniowego .NET Core](https://github.com/dotnet/coreclr#building-the-repository) znajdują się w repozytorium GitHub.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-209">[Instructions on building the .NET Core runtime](https://github.com/dotnet/coreclr#building-the-repository) can be found in its GitHub repository.</span></span>

### <a name="step-1---identify-the-managed-entry-point"></a><span data-ttu-id="a4f8a-210">Krok 1 — ustalenie zarządzany punkt wejścia</span><span class="sxs-lookup"><span data-stu-id="a4f8a-210">Step 1 - Identify the managed entry point</span></span>
<span data-ttu-id="a4f8a-211">Po odwołujące się do niezbędne nagłówki ([mscoree.h](https://github.com/dotnet/coreclr/tree/master/src/pal/prebuilt/inc/mscoree.h) i stdio.h, na przykład), jest jednym z pierwszych czynności należy wykonać na hoście platformy .NET Core, zlokalizuj zarządzany punkt wejścia będzie używana.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-211">After referencing necessary headers ([mscoree.h](https://github.com/dotnet/coreclr/tree/master/src/pal/prebuilt/inc/mscoree.h) and stdio.h, for example), one of the first things a .NET Core host must do is locate the managed entry point it will be using.</span></span> <span data-ttu-id="a4f8a-212">W naszych przykładowych hosta, to wykonując tylko pierwszy argument wiersza polecenia do naszych hosta jako ścieżkę do zarządzanych danych binarnych którego `main` będzie można wykonać metody.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-212">In our sample host, this is done by just taking the first command line argument to our host as the path to a managed binary whose `main` method will be executed.</span></span>

[!code-cpp[NetCoreHost#1](~/samples/core/hosting/HostWithMscoree/host.cpp#1)]

### <a name="step-2---find-and-load-coreclr"></a><span data-ttu-id="a4f8a-213">Krok 2 — Znajdź i załaduj CoreCLR</span><span class="sxs-lookup"><span data-stu-id="a4f8a-213">Step 2 - Find and load CoreCLR</span></span>
<span data-ttu-id="a4f8a-214">Środowisko uruchomieniowe platformy .NET Core interfejsów API znajdują się w *CoreCLR.dll* (na Windows).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-214">The .NET Core runtime APIs are in *CoreCLR.dll* (on Windows).</span></span> <span data-ttu-id="a4f8a-215">Można pobrać interfejsu hostingu (`ICLRRuntimeHost4`), należy znaleźć i załadować *CoreCLR.dll*.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-215">To get our hosting interface (`ICLRRuntimeHost4`), it's necessary to find and load *CoreCLR.dll*.</span></span> <span data-ttu-id="a4f8a-216">Zależy od hosta, aby zdefiniować Konwencję dotyczącą sposobu zlokalizuje *CoreCLR.dll*.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-216">It is up to the host to define a convention for how it will locate *CoreCLR.dll*.</span></span> <span data-ttu-id="a4f8a-217">Niektóre hosty oczekiwać pliku do dobrze znanej lokalizacji komputera (takich jak *%programfiles%\dotnet\shared\Microsoft.NETCore.App\2.1.6*).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-217">Some hosts expect the file to be present in a well-known machine-wide location (such as *%programfiles%\dotnet\shared\Microsoft.NETCore.App\2.1.6*).</span></span> <span data-ttu-id="a4f8a-218">Inne oczekuje, że *CoreCLR.dll* zostaną załadowane z miejsca obok hosta autonomiczną usługą Intune a aplikacja będzie hostowana.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-218">Others expect that *CoreCLR.dll* will be loaded from a location next to either the host itself or the app to be hosted.</span></span> <span data-ttu-id="a4f8a-219">Nadal inne osoby mogą można znaleźć w zmiennej środowiskowej, aby znaleźć biblioteki.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-219">Still others might consult an environment variable to find the library.</span></span>

<span data-ttu-id="a4f8a-220">W systemie Linux lub Mac jest podstawowej biblioteki środowiska uruchomieniowego *libcoreclr.so* lub *libcoreclr.dylib*, odpowiednio.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-220">On Linux or Mac, the core runtime library is *libcoreclr.so* or *libcoreclr.dylib*, respectively.</span></span>

<span data-ttu-id="a4f8a-221">Nasze przykładowe hosta sondy kilka typowe lokalizacje dla *CoreCLR.dll*.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-221">Our sample host probes a few common locations for *CoreCLR.dll*.</span></span> <span data-ttu-id="a4f8a-222">Gdy zostanie znaleziony, zostać załadowany za pośrednictwem `LoadLibrary` (lub `dlopen` na system Linux lub Mac).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-222">Once found, it must be loaded via `LoadLibrary` (or `dlopen` on Linux/Mac).</span></span>

[!code-cpp[NetCoreHost#2](~/samples/core/hosting/HostWithMscoree/host.cpp#2)]

### <a name="step-3---get-an-iclrruntimehost4-instance"></a><span data-ttu-id="a4f8a-223">Krok 3 — Pobierz wystąpienie ICLRRuntimeHost4</span><span class="sxs-lookup"><span data-stu-id="a4f8a-223">Step 3 - Get an ICLRRuntimeHost4 Instance</span></span>
<span data-ttu-id="a4f8a-224">`ICLRRuntimeHost4` Interfejs hosta jest pobierany przez wywołanie metody `GetProcAddress` (lub `dlsym` na system Linux lub Mac) na `GetCLRRuntimeHost`, a następnie wywoływania tej funkcji.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-224">The `ICLRRuntimeHost4` hosting interface is retrieved by calling `GetProcAddress` (or `dlsym` on Linux/Mac) on `GetCLRRuntimeHost`, and then invoking that function.</span></span>

[!code-cpp[NetCoreHost#3](~/samples/core/hosting/HostWithMscoree/host.cpp#3)]

### <a name="step-4---set-startup-flags-and-start-the-runtime"></a><span data-ttu-id="a4f8a-225">Krok 4 — Ustaw flagi uruchamiania i uruchomić środowiska uruchomieniowego</span><span class="sxs-lookup"><span data-stu-id="a4f8a-225">Step 4 - Set startup flags and start the runtime</span></span>
<span data-ttu-id="a4f8a-226">Za pomocą `ICLRRuntimeHost4` dostępnych, możemy teraz określić flagi uruchamianie całego środowiska uruchomieniowego i uruchomić środowiska uruchomieniowego.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-226">With an `ICLRRuntimeHost4` in-hand, we can now specify runtime-wide startup flags and start the runtime.</span></span> <span data-ttu-id="a4f8a-227">Flagi uruchamiania określają, które moduł wyrzucania elementów bezużytecznych (GC) do użycia (współbieżnych lub serwera), czy użyto pojedynczego elementu AppDomain lub wielu domen aplikacji i jakie zasady optymalizacji modułu ładującego do użytku (niezależne od domeny ładowania zestawów).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-227">Startup flags determine which garbage collector (GC) to use (concurrent or server), whether we will use a single AppDomain or multiple AppDomains, and what loader optimization policy to use (for domain-neutral loading of assemblies).</span></span>

[!code-cpp[NetCoreHost#4](~/samples/core/hosting/HostWithMscoree/host.cpp#4)]

<span data-ttu-id="a4f8a-228">Środowisko uruchomieniowe zostanie uruchomiony przy użyciu wywołania `Start` funkcji.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-228">The runtime is started with a call to the `Start` function.</span></span>

```C++
hr = runtimeHost->Start();
```

### <a name="step-5---preparing-appdomain-settings"></a><span data-ttu-id="a4f8a-229">Krok 5 — ustawienia przygotowywanie elementu AppDomain</span><span class="sxs-lookup"><span data-stu-id="a4f8a-229">Step 5 - Preparing AppDomain settings</span></span>
<span data-ttu-id="a4f8a-230">Po uruchomieniu środowiska uruchomieniowego, chcemy do skonfigurowania elementu AppDomain.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-230">Once the runtime is started, we will want to set up an AppDomain.</span></span> <span data-ttu-id="a4f8a-231">Istnieje kilka opcji, które należy określić podczas tworzenia .NET elementu AppDomain, jednak tak jest przygotować je w pierwszej kolejności.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-231">There are a number of options that must be specified when creating a .NET AppDomain, however, so it's necessary to prepare those first.</span></span>

<span data-ttu-id="a4f8a-232">Flagi AppDomain Określ AppDomain zachowania związane z zabezpieczeniami i współdziałania.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-232">AppDomain flags specify AppDomain behaviors related to security and interop.</span></span> <span data-ttu-id="a4f8a-233">Starsze hosty programu Silverlight używany te ustawienia mają być piaskownicy kod użytkownika, ale większości współczesnych hostów platformy .NET Core uruchamiają kod użytkownika jako pełne zaufanie i włączyć współdziałanie.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-233">Older Silverlight hosts used these settings to sandbox user code, but most modern .NET Core hosts run user code as full trust and enable interop.</span></span>

[!code-cpp[NetCoreHost#5](~/samples/core/hosting/HostWithMscoree/host.cpp#5)]

<span data-ttu-id="a4f8a-234">Po podjęciu decyzji, które AppDomain flagi do użycia, należy zdefiniować właściwości elementu AppDomain.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-234">After deciding which AppDomain flags to use, AppDomain properties must be defined.</span></span> <span data-ttu-id="a4f8a-235">Właściwości to pary klucz/wartość ciągów.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-235">The properties are key/value pairs of strings.</span></span> <span data-ttu-id="a4f8a-236">Wiele właściwości odnoszą się do jak element AppDomain zostanie załadowany zestawów.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-236">Many of the properties relate to how the AppDomain will load assemblies.</span></span>

<span data-ttu-id="a4f8a-237">Wspólne właściwości elementu AppDomain obejmują:</span><span class="sxs-lookup"><span data-stu-id="a4f8a-237">Common AppDomain properties include:</span></span>

* <span data-ttu-id="a4f8a-238">`TRUSTED_PLATFORM_ASSEMBLIES` Jest to lista zestawu ścieżek (rozdzielone `;` na Windows i `:` na system Linux lub Mac) który element AppDomain nadać priorytet ładowanie i przyznać pełne zaufanie (nawet w częściowo zaufanych domen).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-238">`TRUSTED_PLATFORM_ASSEMBLIES` This is a list of assembly paths (delimited by `;` on Windows and `:` on Linux/Mac) which the AppDomain should prioritize loading and give full trust to (even in partially-trusted domains).</span></span> <span data-ttu-id="a4f8a-239">Ta lista jest przeznaczona do zawierają zestawy "Framework" i innych zaufanych modułów, podobne do globalnej pamięci podręcznej zestawów w scenariuszach .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-239">This list is meant to contain 'Framework' assemblies and other trusted modules, similar to the GAC in .NET Framework scenarios.</span></span> <span data-ttu-id="a4f8a-240">Niektóre hosty będą obok Umieść każdą bibliotekę *coreclr.dll* na tej liście inne ma ustaloną manifesty listę zaufanych zestawów dla swoich potrzeb.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-240">Some hosts will put any library next to *coreclr.dll* on this list, others have hard-coded manifests listing trusted assemblies for their purposes.</span></span>
* <span data-ttu-id="a4f8a-241">`APP_PATHS` To jest lista ścieżek do sondowania w dla zestawu, jeśli nie można znaleźć na liście zestawów (TPA) TPM.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-241">`APP_PATHS` This is a list of paths to probe in for an assembly if it can't be found in the trusted platform assemblies (TPA) list.</span></span> <span data-ttu-id="a4f8a-242">Ponieważ host nie ma większą kontrolę nad tym, którzy zestawy są ładowane przy użyciu listy elementu TPA, jest najlepszym rozwiązaniem dla hostów, aby określić zestawy, które spełniają oczekiwane obciążenia i wyświetlać je w sposób jawny.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-242">Because the host has more control over which assemblies are loaded using the TPA list, it is a best practice for hosts to determine which assemblies they expect to load and list them explicitly.</span></span> <span data-ttu-id="a4f8a-243">Jeśli potrzebne jest badania w czasie wykonywania, jednak tej właściwości można włączyć tego scenariusza.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-243">If probing at runtime is needed, however, this property can enable that scenario.</span></span>
* <span data-ttu-id="a4f8a-244">`APP_NI_PATHS` Ta lista jest bardzo podobne do APP_PATHS, chyba że ma ją traktować jako ścieżek, które będzie sondowany dla obrazów natywnych.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-244">`APP_NI_PATHS` This list is very similar to APP_PATHS except that it's meant to be paths that will be probed for native images.</span></span>
* <span data-ttu-id="a4f8a-245">`NATIVE_DLL_SEARCH_DIRECTORIES` Ta właściwość jest lista ścieżek, które moduł ładujący powinien sondowania, gdy szukasz natywnych bibliotek DLL wywoływanym za pośrednictwem p/invoke.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-245">`NATIVE_DLL_SEARCH_DIRECTORIES` This property is a list of paths the loader should probe when looking for native DLLs called via p/invoke.</span></span>
* <span data-ttu-id="a4f8a-246">`PLATFORM_RESOURCE_ROOTS` Ta lista zawiera ścieżki do sondowania w dla zestawów satelickich zasobów (w podfolderach specyficzne dla kultury).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-246">`PLATFORM_RESOURCE_ROOTS` This list includes paths to probe in for resource satellite assemblies (in culture-specific sub-directories).</span></span>

<span data-ttu-id="a4f8a-247">W naszym [hosta prostych przykładowych](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithMscoree), te właściwości są skonfigurowane w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="a4f8a-247">In our [simple sample host](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithMscoree), these properties are set up as follows:</span></span>

[!code-cpp[NetCoreHost#6](~/samples/core/hosting/HostWithMscoree/host.cpp#6)]

### <a name="step-6---create-the-appdomain"></a><span data-ttu-id="a4f8a-248">Krok 6 — Tworzenie domeny aplikacji</span><span class="sxs-lookup"><span data-stu-id="a4f8a-248">Step 6 - Create the AppDomain</span></span>
<span data-ttu-id="a4f8a-249">Po przygotowaniu wszystkie flagi domeny aplikacji i właściwości `ICLRRuntimeHost4::CreateAppDomainWithManager` może służyć do konfigurowania elementu AppDomain.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-249">Once all AppDomain flags and properties are prepared, `ICLRRuntimeHost4::CreateAppDomainWithManager` can be used to set up the AppDomain.</span></span> <span data-ttu-id="a4f8a-250">Ta funkcja przyjmuje opcjonalnie, w pełni kwalifikowanej nazwy zestawu i nazwa typu do użycia jako Menedżer ds. domeny AppDomain.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-250">This function optionally takes a fully qualified assembly name and type name to use as the domain's AppDomain manager.</span></span> <span data-ttu-id="a4f8a-251">Menedżer domeny aplikacji umożliwia hosta kontrolować niektóre aspekty zachowanie domeny aplikacji i może zapewnić punkty wejścia uruchamiania kodu zarządzanego, jeśli host nie zamierzasz bezpośrednio Wywołaj kod użytkownika.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-251">An AppDomain manager can allow a host to control some aspects of AppDomain behavior and may provide entry points for launching managed code if the host doesn't intend to invoke user code directly.</span></span>

[!code-cpp[NetCoreHost#7](~/samples/core/hosting/HostWithMscoree/host.cpp#7)]

### <a name="step-7---run-managed-code"></a><span data-ttu-id="a4f8a-252">Krok 7 — wykonywania kodu zarządzanego!</span><span class="sxs-lookup"><span data-stu-id="a4f8a-252">Step 7 - Run managed code!</span></span>
<span data-ttu-id="a4f8a-253">Za pomocą elementu AppDomain skonfigurowaniu i uruchomieniu hosta można teraz rozpocząć wykonywanie kodu zarządzanego.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-253">With an AppDomain up and running, the host can now start executing managed code.</span></span> <span data-ttu-id="a4f8a-254">W tym celu najłatwiej używać `ICLRRuntimeHost4::ExecuteAssembly` do wywołania metody punktu wejścia zestaw zarządzany.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-254">The easiest way to do this is to use `ICLRRuntimeHost4::ExecuteAssembly` to invoke a managed assembly's entry point method.</span></span> <span data-ttu-id="a4f8a-255">Należy pamiętać, że ta funkcja działa tylko w scenariuszach jednej domeny.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-255">Note that this function only works in single-domain scenarios.</span></span>

[!code-cpp[NetCoreHost#8](~/samples/core/hosting/HostWithMscoree/host.cpp#8)]

<span data-ttu-id="a4f8a-256">Innej opcji, jeśli `ExecuteAssembly` nie odpowiada potrzebom użytkownika hosta, jest użycie `CreateDelegate` utworzyć wskaźnik funkcji na statyczną zarządzane metody.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-256">Another option, if `ExecuteAssembly` doesn't meet your host's needs, is to use `CreateDelegate` to create a function pointer to a static managed method.</span></span> <span data-ttu-id="a4f8a-257">Wymaga to hosta wiedzieć sygnatura metody jest wywoływanie (Aby utworzyć typ wskaźnika funkcji), ale pozwala hostom elastyczność wywołać kod inny niż wpisu zestawu punktu.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-257">This requires the host to know the signature of the method it is calling into (in order to create the function pointer type) but allows hosts the flexibility to invoke code other than an assembly's entry point.</span></span> <span data-ttu-id="a4f8a-258">Nazwa zestawu, pod warunkiem w drugi parametr jest [nazwy pełnego zestawu zarządzanego](../../framework/app-domains/assembly-names.md) biblioteki do załadowania.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-258">The assembly name provided in the second parameter is the [full managed assembly name](../../framework/app-domains/assembly-names.md) of the library to load.</span></span>

```C++
void *pfnDelegate = NULL;
hr = runtimeHost->CreateDelegate(
    domainId,
    L"HW, Version=1.0.0.0, Culture=neutral", // Target managed assembly
    L"ConsoleApplication.Program",           // Target managed type
    L"Main",                                 // Target entry point (static method)
    (INT_PTR*)&pfnDelegate);

((MainMethodFp*)pfnDelegate)(NULL);
```

### <a name="step-8---clean-up"></a><span data-ttu-id="a4f8a-259">Krok 8 — czyszczenia</span><span class="sxs-lookup"><span data-stu-id="a4f8a-259">Step 8 - Clean up</span></span>
<span data-ttu-id="a4f8a-260">Na koniec hosta powinno wyczyścić zasoby po sam zwalnianie domen aplikacji, zatrzymywanie środowiska uruchomieniowego i zwalniania `ICLRRuntimeHost4` odwołania.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-260">Finally, the host should clean up after itself by unloading AppDomains, stopping the runtime, and releasing the `ICLRRuntimeHost4` reference.</span></span>

[!code-cpp[NetCoreHost#9](~/samples/core/hosting/HostWithMscoree/host.cpp#9)]

<span data-ttu-id="a4f8a-261">CoreCLR nepodporuje zwolnienie.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-261">CoreCLR does not support unloading.</span></span> <span data-ttu-id="a4f8a-262">Nie zwolnienia biblioteki CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-262">Do not unload the CoreCLR library.</span></span>

## <a name="conclusion"></a><span data-ttu-id="a4f8a-263">Wniosek</span><span class="sxs-lookup"><span data-stu-id="a4f8a-263">Conclusion</span></span>
<span data-ttu-id="a4f8a-264">Po skompilowaniu hosta może zostać przetestowany, uruchamiając go z poziomu wiersza polecenia, a następnie przekazując argumenty hosta oczekuje (np. aplikacji zarządzanej można uruchomić hosta przykład mscoree).</span><span class="sxs-lookup"><span data-stu-id="a4f8a-264">Once your host is built, it can be tested by running it from the command line and passing any arguments the host expects (like the managed app to run for the mscoree example host).</span></span> <span data-ttu-id="a4f8a-265">Podczas określania aplikacji .NET Core dla hosta do uruchamiania, należy użyć pliku .dll, który jest wytwarzany przez `dotnet build`.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-265">When specifying the .NET Core app for the host to run, be sure to use the .dll that is produced by `dotnet build`.</span></span> <span data-ttu-id="a4f8a-266">Pliki wykonywalne (pliki .exe) generowany przez `dotnet publish` aplikacje samodzielne są faktycznie domyślne platformy .NET Core hosta (tak, aby aplikację można uruchomić bezpośrednio z wiersza polecenia w scenariuszach linii głównej); użytkownika kod jest kompilowany do biblioteki dll o takiej samej nazwie.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-266">Executables (.exe files) produced by `dotnet publish` for self-contained applications are actually the default .NET Core host (so that the app can be launched directly from the command line in mainline scenarios); user code is compiled into a dll of the same name.</span></span>

<span data-ttu-id="a4f8a-267">Jeśli elementy nie działa początkowo, dokładnie sprawdzić, które *coreclr.dll* jest dostępny w lokalizacji, oczekiwanego przez hosta, wszystkie wymagane biblioteki Framework znajdują się na liście elementu TPA, która pasuje do tego CoreCLR bitowych (32 - lub 64-bitowy) jak Host został skompilowany.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-267">If things don't work initially, double-check that *coreclr.dll* is available in the location expected by the host, that all necessary Framework libraries are in the TPA list, and that CoreCLR's bitness (32- or 64-bit) matches how the host was built.</span></span>

<span data-ttu-id="a4f8a-268">Hostowanie środowiska uruchomieniowego .NET Core to zaawansowany scenariusz nie będzie wymagać wielu programistów, że dla tych, którzy muszą do uruchomienia kodu zarządzanego z macierzysty proces lub potrzebują większą kontrolę nad zachowanie środowiska uruchomieniowego .NET Core, może być bardzo przydatne.</span><span class="sxs-lookup"><span data-stu-id="a4f8a-268">Hosting the .NET Core runtime is an advanced scenario that many developers won't require, but for those who need to launch managed code from a native process, or who need more control over the .NET Core runtime's behavior, it can be very useful.</span></span>
