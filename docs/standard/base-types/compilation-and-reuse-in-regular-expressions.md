---
title: Kompilacja i ponowne użycie w wyrażeniach regularnych
ms.date: 03/30/2017
ms.technology: dotnet-standard
helpviewer_keywords:
- parsing text with regular expressions, compilation
- searching with regular expressions, compilation
- .NET Framework regular expressions, engines
- .NET Framework regular expressions, compilation
- regular expressions, compilation
- compilation, regular expressions
- pattern-matching with regular expressions, compilation
- regular expressions, engines
ms.assetid: 182ec76d-5a01-4d73-996c-0b0d14fcea18
ms.openlocfilehash: 3e1dfe8373145286b03e503f038e267ff0d4c4f3
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/15/2020
ms.locfileid: "73091733"
---
# <a name="compilation-and-reuse-in-regular-expressions"></a><span data-ttu-id="f95a0-102">Kompilacja i ponowne użycie w wyrażeniach regularnych</span><span class="sxs-lookup"><span data-stu-id="f95a0-102">Compilation and Reuse in Regular Expressions</span></span>
<span data-ttu-id="f95a0-103">Można zoptymalizować wydajność aplikacji, które intensywnie korzystają z wyrażeń regularnych, rozumiejąc, jak aparat wyrażeń regularnych kompiluje wyrażenia i rozumiejąc, jak są buforowane wyrażenia regularne.</span><span class="sxs-lookup"><span data-stu-id="f95a0-103">You can optimize the performance of applications that make extensive use of regular expressions by understanding how the regular expression engine compiles expressions and by understanding how regular expressions are cached.</span></span> <span data-ttu-id="f95a0-104">W tym temacie omówiono kompilacji i buforowania.</span><span class="sxs-lookup"><span data-stu-id="f95a0-104">This topic discusses both compilation and caching.</span></span>  
  
## <a name="compiled-regular-expressions"></a><span data-ttu-id="f95a0-105">Skompilowane wyrażenia regularne</span><span class="sxs-lookup"><span data-stu-id="f95a0-105">Compiled Regular Expressions</span></span>  
 <span data-ttu-id="f95a0-106">Domyślnie aparat wyrażeń regularnych kompiluje wyrażenie regularne do sekwencji instrukcji wewnętrznych (są to kody wysokiego poziomu, które różnią się od języka pośredniego firmy Microsoft lub MSIL).</span><span class="sxs-lookup"><span data-stu-id="f95a0-106">By default, the regular expression engine compiles a regular expression to a sequence of internal instructions (these are high-level codes that are different from Microsoft intermediate language, or MSIL).</span></span> <span data-ttu-id="f95a0-107">Gdy aparat wykonuje wyrażenie regularne, interpretuje kody wewnętrzne.</span><span class="sxs-lookup"><span data-stu-id="f95a0-107">When the engine executes a regular expression, it interprets the internal codes.</span></span>  
  
 <span data-ttu-id="f95a0-108">Jeśli <xref:System.Text.RegularExpressions.Regex> obiekt jest konstruowany z opcją, <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> kompiluje wyrażenie regularne do jawnego kodu MSIL zamiast instrukcji wewnętrznych wyrażeń regularnych wysokiego poziomu.</span><span class="sxs-lookup"><span data-stu-id="f95a0-108">If a <xref:System.Text.RegularExpressions.Regex> object is constructed with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option, it compiles the regular expression to explicit MSIL code instead of high-level regular expression internal instructions.</span></span> <span data-ttu-id="f95a0-109">Pozwala to . Net just-in-time (JIT) kompilator do konwersji wyrażenia do natywnego kodu maszynowego w celu uzyskania wyższej wydajności.</span><span class="sxs-lookup"><span data-stu-id="f95a0-109">This allows .NET's just-in-time (JIT) compiler to convert the expression to native machine code for higher performance.</span></span>  
  
<span data-ttu-id="f95a0-110">Jednak wygenerowany MSIL nie może być rozładowany.</span><span class="sxs-lookup"><span data-stu-id="f95a0-110">However, generated MSIL cannot be unloaded.</span></span> <span data-ttu-id="f95a0-111">Jedynym sposobem, aby zwolnić kod jest zwolnienie całej domeny aplikacji (to znaczy, aby zwolnić cały kod aplikacji.).</span><span class="sxs-lookup"><span data-stu-id="f95a0-111">The only way to unload code is to unload an entire application domain (that is, to unload all of your application's code.).</span></span> <span data-ttu-id="f95a0-112">Skutecznie po skompilowaniu wyrażenia regularnego <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> z opcją nigdy nie zwalnia zasobów używanych przez skompilowane wyrażenie, <xref:System.Text.RegularExpressions.Regex> nawet jeśli wyrażenie regularne zostało utworzone przez obiekt, który sam jest zwolniony do wyrzucania elementów bezużytecznych.</span><span class="sxs-lookup"><span data-stu-id="f95a0-112">Effectively, once a regular expression is compiled with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option,   never releases the resources used by the compiled expression, even if the regular expression was created by a <xref:System.Text.RegularExpressions.Regex> object that is itself released to garbage collection.</span></span>  
  
 <span data-ttu-id="f95a0-113">Należy zachować ostrożność, aby ograniczyć liczbę różnych wyrażeń regularnych, które można skompilować z opcją, <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> aby uniknąć zużywa zbyt wiele zasobów.</span><span class="sxs-lookup"><span data-stu-id="f95a0-113">You must be careful to limit the number of different regular expressions you compile with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option to avoid consuming too many resources.</span></span> <span data-ttu-id="f95a0-114">Jeśli aplikacja musi używać dużej lub nieograniczonej liczby wyrażeń regularnych, każde wyrażenie powinno być interpretowane, a nie kompilowane.</span><span class="sxs-lookup"><span data-stu-id="f95a0-114">If an application must use a large or unbounded number of regular expressions, each expression should be interpreted, not compiled.</span></span> <span data-ttu-id="f95a0-115">Jednak jeśli niewielka liczba wyrażeń regularnych są używane wielokrotnie, <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> powinny być kompilowane z dla lepszej wydajności.</span><span class="sxs-lookup"><span data-stu-id="f95a0-115">However, if a small number of regular expressions are used repeatedly, they should be compiled with <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> for better performance.</span></span> <span data-ttu-id="f95a0-116">Alternatywą jest użycie wstępnie skompilowanych wyrażeń regularnych.</span><span class="sxs-lookup"><span data-stu-id="f95a0-116">An alternative is to use precompiled regular expressions.</span></span> <span data-ttu-id="f95a0-117">Można skompilować wszystkie wyrażenia do dll wielokrotnego użytku <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A> przy użyciu metody.</span><span class="sxs-lookup"><span data-stu-id="f95a0-117">You can compile all of your expressions into a reusable DLL by using the <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A> method.</span></span> <span data-ttu-id="f95a0-118">Pozwala to uniknąć konieczności kompilowania w czasie wykonywania, a jednocześnie korzystających z szybkości skompilowanych wyrażeń regularnych.</span><span class="sxs-lookup"><span data-stu-id="f95a0-118">This avoids the need to compile at runtime while still benefiting from the speed of compiled regular expressions.</span></span>  
  
## <a name="the-regular-expressions-cache"></a><span data-ttu-id="f95a0-119">Pamięć podręczna wyrażeń regularnych</span><span class="sxs-lookup"><span data-stu-id="f95a0-119">The Regular Expressions Cache</span></span>  
 <span data-ttu-id="f95a0-120">Aby zwiększyć wydajność, aparat wyrażeń regularnych utrzymuje pamięć podręczną skompilowanych wyrażeń regularnych w całej aplikacji.</span><span class="sxs-lookup"><span data-stu-id="f95a0-120">To improve performance, the regular expression engine maintains an application-wide cache of compiled regular expressions.</span></span> <span data-ttu-id="f95a0-121">Pamięć podręczna przechowuje wzorce wyrażeń regularnych, które są używane tylko w wywołaniach metod statycznych.</span><span class="sxs-lookup"><span data-stu-id="f95a0-121">The cache stores regular expression patterns that are used only in static method calls.</span></span> <span data-ttu-id="f95a0-122">(Wzorce wyrażeń regularnych dostarczane do metod wystąpienia nie są buforowane). Pozwala to uniknąć konieczności ponownej analizy wyrażenia do kodu bajtu wysokiego poziomu za każdym razem, gdy jest używany.</span><span class="sxs-lookup"><span data-stu-id="f95a0-122">(Regular expression patterns supplied to instance methods are not cached.) This avoids the need to reparse an expression into high-level byte code each time it is used.</span></span>  
  
 <span data-ttu-id="f95a0-123">Maksymalna liczba buforowanych wyrażeń regularnych jest `static` `Shared` określana przez <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> wartość właściwości ( w języku Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="f95a0-123">The maximum number of cached regular expressions is determined by the value of the `static` (`Shared` in Visual Basic) <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="f95a0-124">Domyślnie aparat wyrażeń regularnych buforuje do 15 skompilowanych wyrażeń regularnych.</span><span class="sxs-lookup"><span data-stu-id="f95a0-124">By default, the regular expression engine caches up to 15 compiled regular expressions.</span></span> <span data-ttu-id="f95a0-125">Jeśli liczba skompilowanych wyrażeń regularnych przekracza rozmiar pamięci podręcznej, najmniej ostatnio używane wyrażenie regularne jest odrzucane, a nowe wyrażenie regularne jest buforowane.</span><span class="sxs-lookup"><span data-stu-id="f95a0-125">If the number of compiled regular expressions exceeds the cache size, the least recently used regular expression is discarded and the new regular expression is cached.</span></span>  
  
 <span data-ttu-id="f95a0-126">Aplikacja może korzystać ze wstępnie skompilowanych wyrażeń regularnych na jeden z następujących dwóch sposobów:</span><span class="sxs-lookup"><span data-stu-id="f95a0-126">Your application can take advantage of precompiled regular expressions in one of the following two ways:</span></span>  
  
- <span data-ttu-id="f95a0-127">Za pomocą metody statycznej <xref:System.Text.RegularExpressions.Regex> obiektu do definiowania wyrażenia regularnego.</span><span class="sxs-lookup"><span data-stu-id="f95a0-127">By using a static method of the <xref:System.Text.RegularExpressions.Regex> object to define the regular expression.</span></span> <span data-ttu-id="f95a0-128">Jeśli używasz wzorca wyrażenia regularnego, który został już zdefiniowany w innym wywołaniu metody statycznej, aparat wyrażeń regularnych pobierze go z pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="f95a0-128">If you are using a regular expression pattern that has already been defined in another static method call, the regular expression engine will retrieve it from the cache.</span></span> <span data-ttu-id="f95a0-129">Jeśli nie, aparat skompiluje wyrażenie regularne i dodać go do pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="f95a0-129">If not, the engine will compile the regular expression and add it to the cache.</span></span>  
  
- <span data-ttu-id="f95a0-130">Przez ponowne użycie <xref:System.Text.RegularExpressions.Regex> istniejącego obiektu tak długo, jak jego wzorzec wyrażenia regularnego jest potrzebny.</span><span class="sxs-lookup"><span data-stu-id="f95a0-130">By reusing an existing <xref:System.Text.RegularExpressions.Regex> object as long as its regular expression pattern is needed.</span></span>  
  
 <span data-ttu-id="f95a0-131">Ze względu na obciążenie wystąpienia obiektu i kompilacji wyrażenia regularnego, <xref:System.Text.RegularExpressions.Regex> tworzenie i szybkie niszczenie wielu obiektów jest bardzo kosztowny proces.</span><span class="sxs-lookup"><span data-stu-id="f95a0-131">Because of the overhead of object instantiation and regular expression compilation, creating and rapidly destroying numerous <xref:System.Text.RegularExpressions.Regex> objects is a very expensive process.</span></span> <span data-ttu-id="f95a0-132">W przypadku aplikacji, które używają dużej liczby różnych wyrażeń `Regex` regularnych, można zoptymalizować wydajność przy użyciu wywołań metod statycznych i ewentualnie zwiększając rozmiar pamięci podręcznej wyrażeń regularnych.</span><span class="sxs-lookup"><span data-stu-id="f95a0-132">For applications that use a large number of different regular expressions, you can optimize performance by using calls to static `Regex` methods and possibly by increasing the size of the regular expression cache.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="f95a0-133">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="f95a0-133">See also</span></span>

- [<span data-ttu-id="f95a0-134">Wyrażenia regularne .NET</span><span class="sxs-lookup"><span data-stu-id="f95a0-134">.NET Regular Expressions</span></span>](../../../docs/standard/base-types/regular-expressions.md)
