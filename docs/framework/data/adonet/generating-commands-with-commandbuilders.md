---
title: Generowanie poleceń za pomocą CommandBuilders
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: 6e3fb8b5-373b-4f9e-ab03-a22693df8e91
ms.openlocfilehash: 7cc8ff5391fca7c3315dda433785a182f476bca7
ms.sourcegitcommit: d2e1dfa7ef2d4e9ffae3d431cf6a4ffd9c8d378f
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/07/2019
ms.locfileid: "70783878"
---
# <a name="generating-commands-with-commandbuilders"></a><span data-ttu-id="e7a6c-102">Generowanie poleceń za pomocą CommandBuilders</span><span class="sxs-lookup"><span data-stu-id="e7a6c-102">Generating Commands with CommandBuilders</span></span>
<span data-ttu-id="e7a6c-103">Gdy właściwość jest określana dynamicznie w czasie wykonywania, na przykład za pomocą narzędzia zapytania pobierającego polecenie tekstowe od użytkownika, nie można określić odpowiedniego `InsertCommand`, `UpdateCommand`lub `DeleteCommand` w czasie projektowania. `SelectCommand`</span><span class="sxs-lookup"><span data-stu-id="e7a6c-103">When the `SelectCommand` property is dynamically specified at run time, such as through a query tool that takes a textual command from the user, you may not be able to specify the appropriate `InsertCommand`, `UpdateCommand`, or `DeleteCommand` at design time.</span></span> <span data-ttu-id="e7a6c-104">`UpdateCommand` `DeleteCommand` <xref:System.Data.Common.DbCommandBuilder> <xref:System.Data.Common.DbDataAdapter> `InsertCommand`Jeśli mapy lub są generowane na podstawie pojedynczej tabeli bazy danych, można skorzystać z obiektu, aby automatycznie generować, i. <xref:System.Data.DataTable></span><span class="sxs-lookup"><span data-stu-id="e7a6c-104">If your <xref:System.Data.DataTable> maps to or is generated from a single database table, you can take advantage of the <xref:System.Data.Common.DbCommandBuilder> object to automatically generate the `DeleteCommand`, `InsertCommand`, and `UpdateCommand` of the <xref:System.Data.Common.DbDataAdapter>.</span></span>  
  
 <span data-ttu-id="e7a6c-105">Minimalnym wymaganiem jest ustawienie `SelectCommand` właściwości w kolejności, w jakiej funkcja automatycznego generowania poleceń będzie działała.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-105">As a minimum requirement, you must set the `SelectCommand` property in order for automatic command generation to work.</span></span> <span data-ttu-id="e7a6c-106">Schemat tabeli pobrany przez `SelectCommand` Właściwość określa składnię automatycznie generowanych instrukcji INSERT, Update i DELETE.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-106">The table schema retrieved by the `SelectCommand` property determines the syntax of the automatically generated INSERT, UPDATE, and DELETE statements.</span></span>  
  
 <span data-ttu-id="e7a6c-107">Aby można było zwrócić `SelectCommand` metadane niezbędne do skonstruowania poleceń INSERT, Update i DELETE SQL, należywykonaćpolecenie.<xref:System.Data.Common.DbCommandBuilder></span><span class="sxs-lookup"><span data-stu-id="e7a6c-107">The <xref:System.Data.Common.DbCommandBuilder> must execute the `SelectCommand` in order to return the metadata necessary to construct the INSERT, UPDATE, and DELETE SQL commands.</span></span> <span data-ttu-id="e7a6c-108">W związku z tym konieczna jest dodatkowa podróż do źródła danych, co może utrudnić działanie.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-108">As a result, an extra trip to the data source is necessary, and this can hinder performance.</span></span> <span data-ttu-id="e7a6c-109">Aby osiągnąć optymalną wydajność, należy jawnie określić polecenia zamiast używać <xref:System.Data.Common.DbCommandBuilder>.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-109">To achieve optimal performance, specify your commands explicitly rather than using the <xref:System.Data.Common.DbCommandBuilder>.</span></span>  
  
 <span data-ttu-id="e7a6c-110">`SelectCommand` Należy również zwrócić co najmniej jeden klucz podstawowy lub unikatową kolumnę.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-110">The `SelectCommand` must also return at least one primary key or unique column.</span></span> <span data-ttu-id="e7a6c-111">Jeśli nie są obecne, `InvalidOperation` generowany jest wyjątek, a polecenia nie są generowane.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-111">If none are present, an `InvalidOperation` exception is generated, and the commands are not generated.</span></span>  
  
 <span data-ttu-id="e7a6c-112">Gdy jest skojarzona `DataAdapter`z <xref:System.Data.Common.DbCommandBuilder> , automatycznie generuje `InsertCommand`właściwości `UpdateCommand` `DeleteCommand`,i , Jeślisąodwołaniamiowartościnull.`DataAdapter`</span><span class="sxs-lookup"><span data-stu-id="e7a6c-112">When associated with a `DataAdapter`, the <xref:System.Data.Common.DbCommandBuilder> automatically generates the `InsertCommand`, `UpdateCommand`, and `DeleteCommand` properties of the `DataAdapter` if they are null references.</span></span> <span data-ttu-id="e7a6c-113">Jeśli istnieje `Command` już dla właściwości, używana jest istniejąca. `Command`</span><span class="sxs-lookup"><span data-stu-id="e7a6c-113">If a `Command` already exists for a property, the existing `Command` is used.</span></span>  
  
 <span data-ttu-id="e7a6c-114">Widoki bazy danych tworzone przez Sprzęganie dwóch lub więcej tabel nie są traktowane jako jedna tabela bazy danych.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-114">Database views that are created by joining two or more tables together are not considered a single database table.</span></span> <span data-ttu-id="e7a6c-115">W tym wystąpieniu nie można użyć <xref:System.Data.Common.DbCommandBuilder> do automatycznego generowania poleceń. należy jawnie określić polecenia.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-115">In this instance you cannot use the <xref:System.Data.Common.DbCommandBuilder> to automatically generate commands; you must specify your commands explicitly.</span></span> <span data-ttu-id="e7a6c-116">Aby uzyskać informacje na temat jawnego ustawiania poleceń w celu `DataSet` rozwiązywania aktualizacji z powrotem do źródła danych, zobacz [Aktualizowanie źródeł danych za pomocą kart DataAdapters](updating-data-sources-with-dataadapters.md).</span><span class="sxs-lookup"><span data-stu-id="e7a6c-116">For information about explicitly setting commands to resolve updates to a `DataSet` back to the data source, see [Updating Data Sources with DataAdapters](updating-data-sources-with-dataadapters.md).</span></span>  
  
 <span data-ttu-id="e7a6c-117">Możesz chcieć zamapować parametry wyjściowe z powrotem do zaktualizowanego wiersza `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-117">You might want to map output parameters back to the updated row of a `DataSet`.</span></span> <span data-ttu-id="e7a6c-118">Jedno z typowych zadań Pobiera wartość automatycznie wygenerowanego pola tożsamości lub sygnatury czasowej ze źródła danych.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-118">One common task would be retrieving the value of an automatically generated identity field or time stamp from the data source.</span></span> <span data-ttu-id="e7a6c-119"><xref:System.Data.Common.DbCommandBuilder> Program nie będzie domyślnie mapować parametrów wyjściowych do kolumn w zaktualizowanym wierszu.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-119">The <xref:System.Data.Common.DbCommandBuilder> will not map output parameters to columns in an updated row by default.</span></span> <span data-ttu-id="e7a6c-120">W tym wystąpieniu należy jawnie określić polecenie.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-120">In this instance you must specify your command explicitly.</span></span> <span data-ttu-id="e7a6c-121">Aby zapoznać się z przykładem mapowania automatycznie generowanego pola tożsamości z powrotem do kolumny wstawionego wiersza, zobacz [pobieranie tożsamości lub wartości AutoNumber](retrieving-identity-or-autonumber-values.md).</span><span class="sxs-lookup"><span data-stu-id="e7a6c-121">For an example of mapping an automatically generated identity field back to a column of an inserted row, see [Retrieving Identity or Autonumber Values](retrieving-identity-or-autonumber-values.md).</span></span>  
  
## <a name="rules-for-automatically-generated-commands"></a><span data-ttu-id="e7a6c-122">Reguły dla automatycznie generowanych poleceń</span><span class="sxs-lookup"><span data-stu-id="e7a6c-122">Rules for Automatically Generated Commands</span></span>  
 <span data-ttu-id="e7a6c-123">W poniższej tabeli przedstawiono reguły generowania generowanych automatycznie poleceń.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-123">The following table shows the rules for how automatically generated commands are generated.</span></span>  
  
|<span data-ttu-id="e7a6c-124">Polecenie</span><span class="sxs-lookup"><span data-stu-id="e7a6c-124">Command</span></span>|<span data-ttu-id="e7a6c-125">Reguła</span><span class="sxs-lookup"><span data-stu-id="e7a6c-125">Rule</span></span>|  
|-------------|----------|  
|`InsertCommand`|<span data-ttu-id="e7a6c-126">Wstawia wiersz w źródle danych dla wszystkich wierszy w tabeli z <xref:System.Data.DataRow.RowState%2A>. <xref:System.Data.DataRowState.Added></span><span class="sxs-lookup"><span data-stu-id="e7a6c-126">Inserts a row at the data source for all rows in the table with a <xref:System.Data.DataRow.RowState%2A> of <xref:System.Data.DataRowState.Added>.</span></span> <span data-ttu-id="e7a6c-127">Wstawia wartości dla wszystkich kolumn, które są aktualizowalne (ale nie kolumn, takich jak tożsamości, wyrażenia lub sygnatury czasowe).</span><span class="sxs-lookup"><span data-stu-id="e7a6c-127">Inserts values for all columns that are updateable (but not columns such as identities, expressions, or timestamps).</span></span>|  
|`UpdateCommand`|<span data-ttu-id="e7a6c-128">Aktualizuje wiersze w źródle danych dla wszystkich wierszy w tabeli z `RowState`. <xref:System.Data.DataRowState.Modified></span><span class="sxs-lookup"><span data-stu-id="e7a6c-128">Updates rows at the data source for all rows in the table with a `RowState` of <xref:System.Data.DataRowState.Modified>.</span></span> <span data-ttu-id="e7a6c-129">Aktualizuje wartości wszystkich kolumn z wyjątkiem kolumn, które nie są aktualizowalne, takich jak tożsamości lub wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-129">Updates the values of all columns except for columns that are not updateable, such as identities or expressions.</span></span> <span data-ttu-id="e7a6c-130">Aktualizuje wszystkie wiersze, w których wartości kolumn w źródle danych pasują do wartości kolumn klucza podstawowego w wierszu i gdzie pozostałe kolumny w źródle danych pasują do oryginalnych wartości wiersza.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-130">Updates all rows where the column values at the data source match the primary key column values of the row, and where the remaining columns at the data source match the original values of the row.</span></span> <span data-ttu-id="e7a6c-131">Aby uzyskać więcej informacji, zobacz "optymistyczny model współbieżności dla aktualizacji i usunięć" w dalszej części tego tematu.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-131">For more information, see "Optimistic Concurrency Model for Updates and Deletes," later in this topic.</span></span>|  
|`DeleteCommand`|<span data-ttu-id="e7a6c-132">Usuwa wiersze ze źródła danych dla wszystkich wierszy w tabeli z `RowState`. <xref:System.Data.DataRowState.Deleted></span><span class="sxs-lookup"><span data-stu-id="e7a6c-132">Deletes rows at the data source for all rows in the table with a `RowState` of <xref:System.Data.DataRowState.Deleted>.</span></span> <span data-ttu-id="e7a6c-133">Usuwa wszystkie wiersze, w których wartości kolumn pasują do wartości kolumn klucza podstawowego wiersza i gdzie pozostałe kolumny w źródle danych pasują do oryginalnych wartości wiersza.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-133">Deletes all rows where the column values match the primary key column values of the row, and where the remaining columns at the data source match the original values of the row.</span></span> <span data-ttu-id="e7a6c-134">Aby uzyskać więcej informacji, zobacz "optymistyczny model współbieżności dla aktualizacji i usunięć" w dalszej części tego tematu.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-134">For more information, see "Optimistic Concurrency Model for Updates and Deletes," later in this topic.</span></span>|  
  
## <a name="optimistic-concurrency-model-for-updates-and-deletes"></a><span data-ttu-id="e7a6c-135">Optymistyczny model współbieżności dla aktualizacji i usunięć</span><span class="sxs-lookup"><span data-stu-id="e7a6c-135">Optimistic Concurrency Model for Updates and Deletes</span></span>  
 <span data-ttu-id="e7a6c-136">Logika do generowania poleceń automatycznie dla instrukcji UPDATE i DELETE jest oparta na *optymistycznej współbieżności*— to oznacza, że rekordy nie są zablokowane do edycji i mogą być modyfikowane przez innych użytkowników lub procesy w dowolnym momencie.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-136">The logic for generating commands automatically for UPDATE and DELETE statements is based on *optimistic concurrency*--that is, records are not locked for editing and can be modified by other users or processes at any time.</span></span> <span data-ttu-id="e7a6c-137">Ponieważ rekord mógł zostać zmodyfikowany po zwróceniu z instrukcji SELECT, ale przed wystawieniem instrukcji UPDATE lub DELETE, automatycznie wygenerowana instrukcja UPDATE lub DELETE zawiera klauzulę WHERE, określając, że wiersz jest aktualizowany tylko wtedy, gdy zawiera wszystkie oryginalne wartości i nie został usunięty ze źródła danych.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-137">Because a record could have been modified after it was returned from the SELECT statement, but before the UPDATE or DELETE statement is issued, the automatically generated UPDATE or DELETE statement contains a WHERE clause, specifying that a row is only updated if it contains all original values and has not been deleted from the data source.</span></span> <span data-ttu-id="e7a6c-138">Dzieje się tak, aby uniknąć zastępowania nowych danych.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-138">This is done to avoid overwriting new data.</span></span> <span data-ttu-id="e7a6c-139">Gdy automatycznie wygenerowana aktualizacja próbuje zaktualizować wiersz, który został usunięty lub który nie zawiera oryginalnych wartości znalezionych w <xref:System.Data.DataSet>, polecenie nie ma wpływu na żadne rekordy <xref:System.Data.DBConcurrencyException> i zostanie zgłoszone.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-139">Where an automatically generated update attempts to update a row that has been deleted or that does not contain the original values found in the <xref:System.Data.DataSet>, the command does not affect any records, and a <xref:System.Data.DBConcurrencyException> is thrown.</span></span>  
  
 <span data-ttu-id="e7a6c-140">Jeśli chcesz, aby aktualizacje lub usuwanie zostały wykonane niezależnie od oryginalnych wartości, musisz jawnie ustawić `UpdateCommand` `DataAdapter` dla i nie opierać się na automatycznym generowaniu polecenia.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-140">If you want the UPDATE or DELETE to complete regardless of original values, you must explicitly set the `UpdateCommand` for the `DataAdapter` and not rely on automatic command generation.</span></span>  
  
## <a name="limitations-of-automatic-command-generation-logic"></a><span data-ttu-id="e7a6c-141">Ograniczenia automatycznej logiki generowania poleceń</span><span class="sxs-lookup"><span data-stu-id="e7a6c-141">Limitations of Automatic Command Generation Logic</span></span>  
 <span data-ttu-id="e7a6c-142">Do automatycznego generowania poleceń mają zastosowanie następujące ograniczenia.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-142">The following limitations apply to automatic command generation.</span></span>  
  
### <a name="unrelated-tables-only"></a><span data-ttu-id="e7a6c-143">Tylko niepowiązane tabele</span><span class="sxs-lookup"><span data-stu-id="e7a6c-143">Unrelated Tables Only</span></span>  
 <span data-ttu-id="e7a6c-144">Logika automatycznego generowania poleceń generuje instrukcje INSERT, UPDATE lub DELETE dla tabel autonomicznych bez uwzględniania jakichkolwiek relacji z innymi tabelami w źródle danych.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-144">The automatic command generation logic generates INSERT, UPDATE, or DELETE statements for stand-alone tables without taking into account any relationships to other tables at the data source.</span></span> <span data-ttu-id="e7a6c-145">W związku z tym może wystąpić błąd podczas wywoływania `Update` , aby przesłać zmiany dla kolumny, która uczestniczy w ograniczeniu klucza obcego w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-145">As a result, you may encounter a failure when calling `Update` to submit changes for a column that participates in a foreign key constraint in the database.</span></span> <span data-ttu-id="e7a6c-146">Aby uniknąć tego wyjątku, nie należy używać <xref:System.Data.Common.DbCommandBuilder> do aktualizowania kolumn związanych z ograniczeniem klucza obcego; w zamian jawnie określić instrukcje używane do wykonania operacji.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-146">To avoid this exception, do not use the <xref:System.Data.Common.DbCommandBuilder> for updating columns involved in a foreign key constraint; instead, explicitly specify the statements used to perform the operation.</span></span>  
  
### <a name="table-and-column-names"></a><span data-ttu-id="e7a6c-147">Nazwy tabel i kolumn</span><span class="sxs-lookup"><span data-stu-id="e7a6c-147">Table and Column Names</span></span>  
 <span data-ttu-id="e7a6c-148">Automatyczna logika generowania poleceń może zakończyć się niepowodzeniem, jeśli nazwy kolumn lub tabel zawierają dowolne znaki specjalne, takie jak spacje, kropki, znaki cudzysłowu lub inne znaki niealfanumeryczne, nawet jeśli są rozdzielone nawiasami.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-148">Automatic command generation logic may fail if column names or table names contain any special characters, such as spaces, periods, quotation marks, or other nonalphanumeric characters, even if delimited by brackets.</span></span> <span data-ttu-id="e7a6c-149">W zależności od dostawcy ustawienie parametrów QuotePrefix i QuoteSuffix może pozwolić logiki generowania na przetwarzanie spacji, ale nie może wyjść ze znaków specjalnych.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-149">Depending on the provider, setting the QuotePrefix and QuoteSuffix parameters may allow the generation logic to process spaces, but it cannot escape special characters.</span></span> <span data-ttu-id="e7a6c-150">W pełni kwalifikowane nazwy tabel w postaci *wykazu. Schema. Table* .</span><span class="sxs-lookup"><span data-stu-id="e7a6c-150">Fully qualified table names in the form of *catalog.schema.table* are supported.</span></span>  
  
## <a name="using-the-commandbuilder-to-automatically-generate-an-sql-statement"></a><span data-ttu-id="e7a6c-151">Automatyczne generowanie instrukcji SQL przy użyciu CommandBuilder</span><span class="sxs-lookup"><span data-stu-id="e7a6c-151">Using the CommandBuilder to Automatically Generate an SQL Statement</span></span>  
 <span data-ttu-id="e7a6c-152">Aby automatycznie generować instrukcje SQL dla elementu `DataAdapter`, należy najpierw `SelectCommand` ustawić właściwość `DataAdapter`, a następnie utworzyć `CommandBuilder` `CommandBuilder` obiekt i określić jako argument, `DataAdapter` dla którego będzie automatycznie Generuj instrukcje SQL.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-152">To automatically generate SQL statements for a `DataAdapter`, first set the `SelectCommand` property of the `DataAdapter`, then create a `CommandBuilder` object, and specify as an argument the `DataAdapter` for which the `CommandBuilder` will automatically generate SQL statements.</span></span>  
  
```vb  
' Assumes that connection is a valid SqlConnection object   
' inside of a Using block.  
Dim adapter As SqlDataAdapter = New SqlDataAdapter( _  
  "SELECT * FROM dbo.Customers", connection)  
Dim builder As SqlCommandBuilder = New SqlCommandBuilder(adapter)  
builder.QuotePrefix = "["  
builder.QuoteSuffix = "]"  
```  
  
```csharp  
// Assumes that connection is a valid SqlConnection object  
// inside of a using block.  
SqlDataAdapter adapter = new SqlDataAdapter(  
  "SELECT * FROM dbo.Customers", connection);  
SqlCommandBuilder builder = new SqlCommandBuilder(adapter);  
builder.QuotePrefix = "[";  
builder.QuoteSuffix = "]";  
```  
  
## <a name="modifying-the-selectcommand"></a><span data-ttu-id="e7a6c-153">Modyfikowanie elementu SelectCommand</span><span class="sxs-lookup"><span data-stu-id="e7a6c-153">Modifying the SelectCommand</span></span>  
 <span data-ttu-id="e7a6c-154">W przypadku zmodyfikowania `CommandText` `SelectCommand` automatycznie generowanych poleceń INSERT, Update lub DELETE może wystąpić wyjątek.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-154">If you modify the `CommandText` of the `SelectCommand` after the INSERT, UPDATE, or DELETE commands have been automatically generated, an exception may occur.</span></span> <span data-ttu-id="e7a6c-155">Jeśli zmodyfikowane `SelectCommand.CommandText` zawiera informacje o schemacie, które są niespójne `SelectCommand.CommandText` z użyciem podczas automatycznego generowania poleceń INSERT, Update lub DELETE `DataAdapter.Update` , przyszłe wywołania metody mogą próbować uzyskać dostęp do kolumn, które nie istnieje już w bieżącej tabeli `SelectCommand`, do której odwołuje się i zostanie zgłoszony wyjątek.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-155">If the modified `SelectCommand.CommandText` contains schema information that is inconsistent with the `SelectCommand.CommandText` used when the insert, update, or delete commands were automatically generated, future calls to the `DataAdapter.Update` method may attempt to access columns that no longer exist in the current table referenced by the `SelectCommand`, and an exception will be thrown.</span></span>  
  
 <span data-ttu-id="e7a6c-156">Można odświeżyć informacje o schemacie, które `CommandBuilder` są używane przez program do automatycznego generowania `RefreshSchema` poleceń przez wywołanie `CommandBuilder`metody.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-156">You can refresh the schema information used by the `CommandBuilder` to automatically generate commands by calling the `RefreshSchema` method of the `CommandBuilder`.</span></span>  
  
 <span data-ttu-id="e7a6c-157">Jeśli chcesz wiedzieć, jakie polecenie zostało automatycznie wygenerowane, możesz `GetInsertCommand`uzyskać odwołanie do automatycznie generowanych poleceń przy użyciu metod `CommandBuilder` , `GetUpdateCommand`, i `GetDeleteCommand` , sprawdzając `CommandText`właściwości skojarzonego polecenia.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-157">If you want to know what command was automatically generated, you can obtain a reference to the automatically generated commands by using the `GetInsertCommand`, `GetUpdateCommand`, and `GetDeleteCommand` methods of the `CommandBuilder` object and checking the `CommandText` property of the associated command.</span></span>  
  
 <span data-ttu-id="e7a6c-158">Poniższy przykład kodu zapisuje w konsoli polecenie Update, które zostało automatycznie wygenerowane.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-158">The following code example writes to the console the update command that was automatically generated.</span></span>  
  
```vb
Console.WriteLine(builder.GetUpdateCommand().CommandText)  
```

```csharp
Console.WriteLine(builder.GetUpdateCommand().CommandText);
```
  
 <span data-ttu-id="e7a6c-159">Poniższy przykład odtworzy `Customers` tabelę `custDS` w zestawie danych.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-159">The following example recreates the `Customers` table in the `custDS` dataset.</span></span> <span data-ttu-id="e7a6c-160">Metoda **RefreshSchema** jest wywoływana w celu odświeżenia automatycznie generowanych poleceń przy użyciu tej nowej informacji o kolumnie.</span><span class="sxs-lookup"><span data-stu-id="e7a6c-160">The **RefreshSchema** method is called to refresh the automatically generated commands with this new column information.</span></span>  
  
```vb  
' Assumes an open SqlConnection and SqlDataAdapter inside of a Using block.  
adapter.SelectCommand.CommandText = _  
  "SELECT CustomerID, ContactName FROM dbo.Customers"  
builder.RefreshSchema()  
  
custDS.Tables.Remove(custDS.Tables("Customers"))  
adapter.Fill(custDS, "Customers")  
```  
  
```csharp  
// Assumes an open SqlConnection and SqlDataAdapter inside of a using block.  
adapter.SelectCommand.CommandText =   
  "SELECT CustomerID, ContactName FROM dbo.Customers";  
builder.RefreshSchema();  
  
custDS.Tables.Remove(custDS.Tables["Customers"]);  
adapter.Fill(custDS, "Customers");  
```  
  
## <a name="see-also"></a><span data-ttu-id="e7a6c-161">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="e7a6c-161">See also</span></span>

- [<span data-ttu-id="e7a6c-162">Polecenia i parametry</span><span class="sxs-lookup"><span data-stu-id="e7a6c-162">Commands and Parameters</span></span>](commands-and-parameters.md)
- [<span data-ttu-id="e7a6c-163">Wykonywanie polecenia</span><span class="sxs-lookup"><span data-stu-id="e7a6c-163">Executing a Command</span></span>](executing-a-command.md)
- [<span data-ttu-id="e7a6c-164">DbConnection, DbCommand i DbException</span><span class="sxs-lookup"><span data-stu-id="e7a6c-164">DbConnection, DbCommand and DbException</span></span>](dbconnection-dbcommand-and-dbexception.md)
- [<span data-ttu-id="e7a6c-165">Omówienie ADO.NET</span><span class="sxs-lookup"><span data-stu-id="e7a6c-165">ADO.NET Overview</span></span>](ado-net-overview.md)
