---
title: Praca z zestawu SDK platformy kompilatora .NET semantycznego modelu analizy biznesowej
description: Ten przegląd zawiera opis typów, używaną do manipulowania semantycznego modelu kodu.
ms.date: 10/15/2017
ms.custom: mvc
ms.openlocfilehash: cf34e2ab9688325f58cb54755db4142a883fca77
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/23/2019
ms.locfileid: "61706640"
---
# <a name="work-with-semantics"></a><span data-ttu-id="ce573-103">Korzystanie z semantyki</span><span class="sxs-lookup"><span data-stu-id="ce573-103">Work with semantics</span></span>

<span data-ttu-id="ce573-104">[Drzewa składni](work-with-syntax.md) reprezentują leksykalne i składniowych struktury kodu źródłowego.</span><span class="sxs-lookup"><span data-stu-id="ce573-104">[Syntax trees](work-with-syntax.md) represent the lexical and syntactic structure of source code.</span></span> <span data-ttu-id="ce573-105">Mimo że te informacje tylko jest wystarczająca do opisywania, deklaracje i logiki w źródle, nie jest wystarczająco dużo informacji do identyfikacji, co jest ono przywoływane.</span><span class="sxs-lookup"><span data-stu-id="ce573-105">Although this information alone is enough to describe all the declarations and logic in the source, it is not enough information to identify what is being referenced.</span></span> <span data-ttu-id="ce573-106">Nazwa może reprezentować:</span><span class="sxs-lookup"><span data-stu-id="ce573-106">A name may represent:</span></span>

- <span data-ttu-id="ce573-107">Typ</span><span class="sxs-lookup"><span data-stu-id="ce573-107">a type</span></span>
- <span data-ttu-id="ce573-108">Pola</span><span class="sxs-lookup"><span data-stu-id="ce573-108">a field</span></span>
- <span data-ttu-id="ce573-109">Metoda</span><span class="sxs-lookup"><span data-stu-id="ce573-109">a method</span></span>
- <span data-ttu-id="ce573-110">Zmienna lokalna</span><span class="sxs-lookup"><span data-stu-id="ce573-110">a local variable</span></span>

<span data-ttu-id="ce573-111">Mimo że każdy z nich jest jednoznacznie inny, określania, który z nich odpowiadającym faktycznie odnosi się do często wymaga głębokiego zrozumienie reguły języka.</span><span class="sxs-lookup"><span data-stu-id="ce573-111">Although each of these is uniquely different, determining which one an identifier actually refers to often requires a deep understanding of the language rules.</span></span> 

<span data-ttu-id="ce573-112">Istnieją elementy programu reprezentowane w kodzie źródłowym i programów znajdują się wcześniej skompilowanych bibliotek, spakowane w zestawie plików.</span><span class="sxs-lookup"><span data-stu-id="ce573-112">There are program elements represented in source code, and programs can also refer to previously compiled libraries, packaged in assembly files.</span></span> <span data-ttu-id="ce573-113">Brak kodu źródłowego i w związku z tym żadnych węzłów składni lub drzewa, są dostępne dla zestawów, programy nadal mogą odwoływać się do elementów wewnątrz nich.</span><span class="sxs-lookup"><span data-stu-id="ce573-113">Although no source code, and therefore no syntax nodes or trees, are available for assemblies, programs can still refer to elements inside them.</span></span>

<span data-ttu-id="ce573-114">W przypadku tych zadań należy **semantycznego modelu analizy biznesowej**.</span><span class="sxs-lookup"><span data-stu-id="ce573-114">For those tasks, you need the **Semantic model**.</span></span>

<span data-ttu-id="ce573-115">Oprócz modelu składni kodu źródłowego modelu semantycznego hermetyzuje reguły języka, co zapewnia łatwy sposób poprawnie zgodne identyfikatory z elementem odpowiedniego programu, do którego nastąpiło odwołanie.</span><span class="sxs-lookup"><span data-stu-id="ce573-115">In addition to a syntactic model of the source code, a semantic model encapsulates the language rules, giving you an easy way to correctly match identifiers with the correct program element being referenced.</span></span>

## <a name="compilation"></a><span data-ttu-id="ce573-116">Kompilacja</span><span class="sxs-lookup"><span data-stu-id="ce573-116">Compilation</span></span>

<span data-ttu-id="ce573-117">Kompilacja jest reprezentacją wszystko, co jest potrzebne do kompilowania C# program lub Visual Basic, który zawiera wszystkie odwołania do zestawów, opcje kompilatora i plików źródłowych.</span><span class="sxs-lookup"><span data-stu-id="ce573-117">A compilation is a representation of everything needed to compile a C# or Visual Basic program, which includes all the assembly references, compiler options, and source files.</span></span> 

<span data-ttu-id="ce573-118">Ponieważ te informacje w jednym miejscu, elementy zawarte w kodzie źródłowym można opisane bardziej szczegółowo.</span><span class="sxs-lookup"><span data-stu-id="ce573-118">Because all this information is in one place, the elements contained in the source code can be described in more detail.</span></span> <span data-ttu-id="ce573-119">Kompilacja reprezentuje każdego deklarowanego typu, elementu członkowskiego lub zmiennej jako symbol separatora.</span><span class="sxs-lookup"><span data-stu-id="ce573-119">The compilation represents each declared type, member, or variable as a symbol.</span></span> <span data-ttu-id="ce573-120">Kompilacja zawiera szereg metod, które ułatwiają znajdowanie i odnoszą się symboli, które mają została zadeklarowana w kodzie źródłowym lub importowany jako metadane z zestawu.</span><span class="sxs-lookup"><span data-stu-id="ce573-120">The compilation contains a variety of methods that help you find and relate the symbols that have either been declared in the source code or imported as metadata from an assembly.</span></span>

<span data-ttu-id="ce573-121">Podobnie jak drzewa składni, kompilacje są niezmienne.</span><span class="sxs-lookup"><span data-stu-id="ce573-121">Similar to syntax trees, compilations are immutable.</span></span> <span data-ttu-id="ce573-122">Po utworzeniu kompilacji, nie można zmienić przez nikogo, który może być używany z.</span><span class="sxs-lookup"><span data-stu-id="ce573-122">After you create a compilation, it cannot be changed by you or anyone else you might be sharing it with.</span></span> <span data-ttu-id="ce573-123">Jednak można utworzyć nowej kompilacji z istniejącej kompilacji, określając zmiany, jak można to zrobić.</span><span class="sxs-lookup"><span data-stu-id="ce573-123">However, you can create a new compilation from an existing compilation, specifying a change as you do so.</span></span> <span data-ttu-id="ce573-124">Na przykład może utworzyć kompilacji, która jest taka sama w sposób, co jako istniejące kompilacji, z wyjątkiem może zawierać dodatkowe pliku lub zestawu odwołania do źródła.</span><span class="sxs-lookup"><span data-stu-id="ce573-124">For example, you might create a compilation that is the same in every way as an existing compilation, except it may include an additional source file or assembly reference.</span></span>

## <a name="symbols"></a><span data-ttu-id="ce573-125">Symbole</span><span class="sxs-lookup"><span data-stu-id="ce573-125">Symbols</span></span>

<span data-ttu-id="ce573-126">Symbol reprezentuje element distinct zadeklarowane na podstawie kodu źródłowego lub importowane z zestawu jako metadane.</span><span class="sxs-lookup"><span data-stu-id="ce573-126">A symbol represents a distinct element declared by the source code or imported from an assembly as metadata.</span></span> <span data-ttu-id="ce573-127">Co przestrzeni nazw, typu, metody, właściwości, pola, zdarzenia, parametr lub zmienna lokalna jest reprezentowany przez symbol.</span><span class="sxs-lookup"><span data-stu-id="ce573-127">Every namespace, type, method, property, field, event, parameter, or local variable is represented by a symbol.</span></span> 

<span data-ttu-id="ce573-128">Różne metody i właściwości <xref:Microsoft.CodeAnalysis.Compilation> wpisz pomóc w znalezieniu symboli.</span><span class="sxs-lookup"><span data-stu-id="ce573-128">A variety of methods and properties on the <xref:Microsoft.CodeAnalysis.Compilation> type help you find symbols.</span></span> <span data-ttu-id="ce573-129">Na przykład można znaleźć symbolu dla zadeklarowanym typem według nazwy pospolitej metadanych.</span><span class="sxs-lookup"><span data-stu-id="ce573-129">For example, you can find a symbol for a declared type by its common metadata name.</span></span> <span data-ttu-id="ce573-130">Można również uzyskać dostęp tabeli symboli całego jako drzewo symboli umieszczone w globalnej przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="ce573-130">You can also access the entire symbol table as a tree of symbols rooted by the global namespace.</span></span>

<span data-ttu-id="ce573-131">Symbole również zawierać dodatkowe informacje, które kompilator określa ze źródła lub metadane, takie jak inne odwołania symboli.</span><span class="sxs-lookup"><span data-stu-id="ce573-131">Symbols also contain additional information that the compiler determines from the source or metadata, such as other referenced symbols.</span></span> <span data-ttu-id="ce573-132">Każdy rodzaj symbolu jest reprezentowany przez oddzielny interfejs pochodną <xref:Microsoft.CodeAnalysis.ISymbol>, każda z metod i właściwości ze szczegółami dotyczącymi informacje zebrane przez kompilator.</span><span class="sxs-lookup"><span data-stu-id="ce573-132">Each kind of symbol is represented by a separate interface derived from <xref:Microsoft.CodeAnalysis.ISymbol>, each with its own methods and properties detailing the information the compiler has gathered.</span></span> <span data-ttu-id="ce573-133">Wiele z tych właściwości bezpośrednio odwoływać innych symboli.</span><span class="sxs-lookup"><span data-stu-id="ce573-133">Many of these properties directly reference other symbols.</span></span> <span data-ttu-id="ce573-134">Na przykład <xref:Microsoft.CodeAnalysis.IMethodSymbol.ReturnType?displayProperty=nameWithType> właściwość zawiera informacje, symbol rzeczywisty typ, który odwołuje się do deklaracji metody.</span><span class="sxs-lookup"><span data-stu-id="ce573-134">For example, the <xref:Microsoft.CodeAnalysis.IMethodSymbol.ReturnType?displayProperty=nameWithType> property tells you the actual type symbol that the method declaration references.</span></span>

<span data-ttu-id="ce573-135">Symbole przedstawiają reprezentację wspólnej przestrzeni nazw, typów i członków, między kodem źródłowym i metadanych.</span><span class="sxs-lookup"><span data-stu-id="ce573-135">Symbols present a common representation of namespaces, types, and members, between source code and metadata.</span></span> <span data-ttu-id="ce573-136">Na przykład, metody, która została zadeklarowana w kodzie źródłowym i metody, które zostały zaimportowane z metadanych są zarówno reprezentowane przez <xref:Microsoft.CodeAnalysis.IMethodSymbol> z tymi samymi właściwościami.</span><span class="sxs-lookup"><span data-stu-id="ce573-136">For example, a method that was declared in source code and a method that was imported from metadata are both represented by an <xref:Microsoft.CodeAnalysis.IMethodSymbol> with the same properties.</span></span>

<span data-ttu-id="ce573-137">Symbole są podobne do systemu typów CLR reprezentowany przez <xref:System.Reflection> interfejsu API, ale one są bardziej rozbudowane, modelują one więcej niż tylko typy.</span><span class="sxs-lookup"><span data-stu-id="ce573-137">Symbols are similar in concept to the CLR type system as represented by the <xref:System.Reflection> API, yet they are richer in that they model more than just types.</span></span> <span data-ttu-id="ce573-138">Przestrzenie nazw, zmienne lokalne i etykiety są wszystkie symbole.</span><span class="sxs-lookup"><span data-stu-id="ce573-138">Namespaces, local variables, and labels are all symbols.</span></span> <span data-ttu-id="ce573-139">Ponadto symbole są reprezentacją koncepcje językowe i nie pojęcia dotyczące środowiska CLR.</span><span class="sxs-lookup"><span data-stu-id="ce573-139">In addition, symbols are a representation of language concepts, not CLR concepts.</span></span> <span data-ttu-id="ce573-140">Jest dużo nakładają się na siebie, ale istnieje wiele również istotne różnice.</span><span class="sxs-lookup"><span data-stu-id="ce573-140">There is a lot of overlap, but there are many meaningful distinctions as well.</span></span> <span data-ttu-id="ce573-141">Na przykład metoda iteratora jest w C# lub Visual Basic to pojedynczy symbol.</span><span class="sxs-lookup"><span data-stu-id="ce573-141">For instance, an iterator method in C# or Visual Basic is a single symbol.</span></span> <span data-ttu-id="ce573-142">Jednakże gdy metoda iteratora jest tłumaczona na metadanych CLR, jest typem i na wiele sposobów.</span><span class="sxs-lookup"><span data-stu-id="ce573-142">However, when the iterator method is translated to CLR metadata, it is a type and multiple methods.</span></span>

## <a name="semantic-model"></a><span data-ttu-id="ce573-143">Semantyczny model analizy biznesowej</span><span class="sxs-lookup"><span data-stu-id="ce573-143">Semantic model</span></span>

<span data-ttu-id="ce573-144">Model semantyczny reprezentuje wszystkie informacje semantyczne jednym pliku źródłowym.</span><span class="sxs-lookup"><span data-stu-id="ce573-144">A semantic model represents all the semantic information for a single source file.</span></span> <span data-ttu-id="ce573-145">Służy do odnajdywania następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="ce573-145">You can use it to discover the following:</span></span> 

* <span data-ttu-id="ce573-146">Symbole, do których odwołuje się w określonym miejscu w źródle.</span><span class="sxs-lookup"><span data-stu-id="ce573-146">The symbols referenced at a specific location in source.</span></span>
* <span data-ttu-id="ce573-147">Wynikowy typ dowolne wyrażenie.</span><span class="sxs-lookup"><span data-stu-id="ce573-147">The resultant type of any expression.</span></span>
* <span data-ttu-id="ce573-148">Testy diagnostyczne, które są błędy i ostrzeżenia.</span><span class="sxs-lookup"><span data-stu-id="ce573-148">All diagnostics, which are errors and warnings.</span></span>
* <span data-ttu-id="ce573-149">Jak zmienne przenoszone do regionów źródłowych.</span><span class="sxs-lookup"><span data-stu-id="ce573-149">How variables flow in and out of regions of source.</span></span>
* <span data-ttu-id="ce573-150">Odpowiedzi na pytania bardziej spekulacyjnego.</span><span class="sxs-lookup"><span data-stu-id="ce573-150">The answers to more speculative questions.</span></span>
