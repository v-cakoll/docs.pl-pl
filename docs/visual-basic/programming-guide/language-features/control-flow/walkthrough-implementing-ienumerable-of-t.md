---
title: Implementowanie interfejsu IEnumerable
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: 582957c91eac63cf7f72dd2f6c0cf40e627be686
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/04/2020
ms.locfileid: "84402034"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="d5c61-102">Wskazówki: wdrażanie IEnumerable(Of T) w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="d5c61-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="d5c61-103"><xref:System.Collections.Generic.IEnumerable%601>Interfejs jest implementowany przez klasy, które mogą zwracać sekwencję wartości po jednym elemencie naraz.</span><span class="sxs-lookup"><span data-stu-id="d5c61-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="d5c61-104">Zaletą zwracania danych z jednego elementu jednocześnie jest to, że nie trzeba ładować kompletnego zestawu danych do pamięci, aby z niego korzystać.</span><span class="sxs-lookup"><span data-stu-id="d5c61-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="d5c61-105">Wystarczy użyć wystarczającej ilości pamięci do załadowania pojedynczego elementu z danych.</span><span class="sxs-lookup"><span data-stu-id="d5c61-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="d5c61-106">Klasy implementujące `IEnumerable(T)` interfejs mogą być używane z `For Each` pętlami lub kwerendami LINQ.</span><span class="sxs-lookup"><span data-stu-id="d5c61-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="d5c61-107">Rozważmy na przykład aplikację, która musi odczytać duży plik tekstowy i zwrócić każdy wiersz z pliku zgodnego z określonymi kryteriami wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="d5c61-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="d5c61-108">Aplikacja używa zapytania LINQ do zwracania wierszy z pliku, które pasują do określonych kryteriów.</span><span class="sxs-lookup"><span data-stu-id="d5c61-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="d5c61-109">Aby zbadać zawartość pliku przy użyciu zapytania LINQ, aplikacja może załadować zawartość pliku do tablicy lub kolekcji.</span><span class="sxs-lookup"><span data-stu-id="d5c61-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="d5c61-110">Jednak załadowanie całego pliku do tablicy lub kolekcji spowodowałoby dużo więcej pamięci niż jest to wymagane.</span><span class="sxs-lookup"><span data-stu-id="d5c61-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="d5c61-111">Zapytanie LINQ mogło zamiast tego zbadać zawartość pliku przy użyciu klasy wyliczalnej, zwracając tylko wartości pasujące do kryteriów wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="d5c61-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="d5c61-112">Zapytania, które zwracają tylko kilka pasujących wartości, zużywają znacznie mniej pamięci.</span><span class="sxs-lookup"><span data-stu-id="d5c61-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="d5c61-113">Można utworzyć klasę, która implementuje interfejs, <xref:System.Collections.Generic.IEnumerable%601> Aby udostępnić dane źródłowe jako wyliczalne dane.</span><span class="sxs-lookup"><span data-stu-id="d5c61-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="d5c61-114">Klasa implementująca `IEnumerable(T)` interfejs będzie wymagała innej klasy implementującej <xref:System.Collections.Generic.IEnumerator%601> interfejs do iteracji przez dane źródłowe.</span><span class="sxs-lookup"><span data-stu-id="d5c61-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="d5c61-115">Te dwie klasy umożliwiają zwracanie elementów danych sekwencyjnie jako określonego typu.</span><span class="sxs-lookup"><span data-stu-id="d5c61-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="d5c61-116">W tym instruktażu utworzysz klasę, która implementuje `IEnumerable(Of String)` interfejs i klasę, która implementuje `IEnumerator(Of String)` interfejs do odczytywania pliku tekstowego po jednym wierszu naraz.</span><span class="sxs-lookup"><span data-stu-id="d5c61-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="d5c61-117">Tworzenie klasy wyliczalnej</span><span class="sxs-lookup"><span data-stu-id="d5c61-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="d5c61-118">**Utwórz wyliczalny projekt klasy**</span><span class="sxs-lookup"><span data-stu-id="d5c61-118">**Create the enumerable class project**</span></span>

1. <span data-ttu-id="d5c61-119">W Visual Basic w menu **plik** wskaż polecenie **Nowy** , a następnie kliknij pozycję **projekt**.</span><span class="sxs-lookup"><span data-stu-id="d5c61-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1. <span data-ttu-id="d5c61-120">W oknie dialogowym **Nowy projekt** w okienku **typy projektów** upewnij się, że wybrano opcję **Windows** .</span><span class="sxs-lookup"><span data-stu-id="d5c61-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="d5c61-121">W okienku **Szablony** wybierz pozycję **Biblioteka klas** .</span><span class="sxs-lookup"><span data-stu-id="d5c61-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="d5c61-122">W polu **Nazwa** wpisz `StreamReaderEnumerable` , a następnie kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="d5c61-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="d5c61-123">Zostanie wyświetlony nowy projekt.</span><span class="sxs-lookup"><span data-stu-id="d5c61-123">The new project is displayed.</span></span>

1. <span data-ttu-id="d5c61-124">W **Eksplorator rozwiązań**kliknij prawym przyciskiem myszy plik Class1. vb i kliknij polecenie **Zmień nazwę**.</span><span class="sxs-lookup"><span data-stu-id="d5c61-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="d5c61-125">Zmień nazwę pliku na `StreamReaderEnumerable.vb` i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="d5c61-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="d5c61-126">Zmiana nazwy pliku spowoduje również zmianę nazwy klasy na `StreamReaderEnumerable` .</span><span class="sxs-lookup"><span data-stu-id="d5c61-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="d5c61-127">Ta klasa będzie implementować `IEnumerable(Of String)` interfejs.</span><span class="sxs-lookup"><span data-stu-id="d5c61-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1. <span data-ttu-id="d5c61-128">Kliknij prawym przyciskiem myszy projekt StreamReaderEnumerable, wskaż polecenie **Dodaj**, a następnie kliknij pozycję **nowy element**.</span><span class="sxs-lookup"><span data-stu-id="d5c61-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="d5c61-129">Wybierz szablon **klasy** .</span><span class="sxs-lookup"><span data-stu-id="d5c61-129">Select the **Class** template.</span></span> <span data-ttu-id="d5c61-130">W polu **Nazwa** wpisz `StreamReaderEnumerator.vb` i kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="d5c61-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="d5c61-131">Pierwsza klasa w tym projekcie jest klasą wyliczalną i implementuje `IEnumerable(Of String)` interfejs.</span><span class="sxs-lookup"><span data-stu-id="d5c61-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="d5c61-132">Ten interfejs generyczny implementuje <xref:System.Collections.IEnumerable> interfejs i gwarantuje, że konsumenci tej klasy mogą uzyskać dostęp do wartości wpisanych jako `String` .</span><span class="sxs-lookup"><span data-stu-id="d5c61-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="d5c61-133">**Dodaj kod w celu zaimplementowania interfejsu IEnumerable**</span><span class="sxs-lookup"><span data-stu-id="d5c61-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="d5c61-134">Otwórz plik StreamReaderEnumerable. vb.</span><span class="sxs-lookup"><span data-stu-id="d5c61-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="d5c61-135">W wierszu po `Public Class StreamReaderEnumerable` wpisz następujące polecenie i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="d5c61-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#1)]

   <span data-ttu-id="d5c61-136">Visual Basic automatycznie wypełnia klasę elementami członkowskimi, które są wymagane przez `IEnumerable(Of String)` interfejs.</span><span class="sxs-lookup"><span data-stu-id="d5c61-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="d5c61-137">Ta klasa wyliczalna odczytaje wiersze z pliku tekstowego po jednym wierszu.</span><span class="sxs-lookup"><span data-stu-id="d5c61-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="d5c61-138">Dodaj następujący kod do klasy, aby uwidocznić konstruktora publicznego, który Pobiera ścieżkę pliku jako parametr wejściowy.</span><span class="sxs-lookup"><span data-stu-id="d5c61-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#2)]

4. <span data-ttu-id="d5c61-139">Twoja implementacja <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> metody `IEnumerable(Of String)` interfejsu zwróci nowe wystąpienie `StreamReaderEnumerator` klasy.</span><span class="sxs-lookup"><span data-stu-id="d5c61-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="d5c61-140">Można wprowadzić implementację `GetEnumerator` metody `IEnumerable` interfejsu `Private` , ponieważ trzeba uwidocznić tylko elementy członkowskie `IEnumerable(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="d5c61-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="d5c61-141">Zastąp kod, który Visual Basic wygenerowany dla `GetEnumerator` metod przy użyciu następującego kodu.</span><span class="sxs-lookup"><span data-stu-id="d5c61-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#3)]  
  
<span data-ttu-id="d5c61-142">**Dodawanie kodu w celu zaimplementowania IEnumerator**</span><span class="sxs-lookup"><span data-stu-id="d5c61-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="d5c61-143">Otwórz plik StreamReaderEnumerator. vb.</span><span class="sxs-lookup"><span data-stu-id="d5c61-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="d5c61-144">W wierszu po `Public Class StreamReaderEnumerator` wpisz następujące polecenie i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="d5c61-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#4)]

   <span data-ttu-id="d5c61-145">Visual Basic automatycznie wypełnia klasę elementami członkowskimi, które są wymagane przez `IEnumerator(Of String)` interfejs.</span><span class="sxs-lookup"><span data-stu-id="d5c61-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="d5c61-146">Klasa Enumerator otwiera plik tekstowy i wykonuje operacje we/wy pliku w celu odczytania wierszy z pliku.</span><span class="sxs-lookup"><span data-stu-id="d5c61-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="d5c61-147">Dodaj następujący kod do klasy, aby uwidocznić konstruktora publicznego, który Pobiera ścieżkę pliku jako parametr wejściowy, i Otwórz plik tekstowy do odczytu.</span><span class="sxs-lookup"><span data-stu-id="d5c61-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#5)]

4. <span data-ttu-id="d5c61-148">`Current`Właściwości obu `IEnumerator(Of String)` `IEnumerator` interfejsów i zwracają bieżący element z pliku tekstowego jako `String` .</span><span class="sxs-lookup"><span data-stu-id="d5c61-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="d5c61-149">Można wprowadzić implementację `Current` właściwości `IEnumerator` interfejsu `Private` , ponieważ trzeba uwidocznić tylko elementy członkowskie `IEnumerator(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="d5c61-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="d5c61-150">Zastąp kod, który Visual Basic wygenerowany dla `Current` właściwości przy użyciu następującego kodu.</span><span class="sxs-lookup"><span data-stu-id="d5c61-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#6)]

5. <span data-ttu-id="d5c61-151">`MoveNext`Metoda `IEnumerator` interfejsu przechodzi do następnego elementu w pliku tekstowym i aktualizuje wartość zwracaną przez `Current` Właściwość.</span><span class="sxs-lookup"><span data-stu-id="d5c61-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="d5c61-152">Jeśli nie ma więcej elementów do odczytania, `MoveNext` Metoda zwraca `False` ; w przeciwnym razie `MoveNext` Metoda zwraca `True` .</span><span class="sxs-lookup"><span data-stu-id="d5c61-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="d5c61-153">Dodaj następujący kod do metody `MoveNext`:</span><span class="sxs-lookup"><span data-stu-id="d5c61-153">Add the following code to the `MoveNext` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#7)]

6. <span data-ttu-id="d5c61-154">`Reset`Metoda `IEnumerator` interfejsu kieruje iterator, aby wskazać początek pliku tekstowego i czyści bieżącą wartość elementu.</span><span class="sxs-lookup"><span data-stu-id="d5c61-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="d5c61-155">Dodaj następujący kod do metody `Reset`:</span><span class="sxs-lookup"><span data-stu-id="d5c61-155">Add the following code to the `Reset` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#8)]

7. <span data-ttu-id="d5c61-156">`Dispose`Metoda `IEnumerator` interfejsu gwarantuje, że wszystkie niezarządzane zasoby są uwalniane przed zniszczeniem iteratora.</span><span class="sxs-lookup"><span data-stu-id="d5c61-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="d5c61-157">Dojście do pliku używane przez `StreamReader` obiekt jest zasobem niezarządzanym i musi zostać zamknięte przed zniszczeniem wystąpienia iteratora.</span><span class="sxs-lookup"><span data-stu-id="d5c61-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="d5c61-158">Zastąp kod, który Visual Basic wygenerowany dla `Dispose` metody przy użyciu następującego kodu.</span><span class="sxs-lookup"><span data-stu-id="d5c61-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#9)]
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="d5c61-159">Użycie iteratora przykładowego</span><span class="sxs-lookup"><span data-stu-id="d5c61-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="d5c61-160">Można użyć klasy wyliczalnej w kodzie wraz ze strukturami formantów, które wymagają obiektu, który implementuje `IEnumerable` , takich jak `For Next` Pętla lub zapytanie LINQ.</span><span class="sxs-lookup"><span data-stu-id="d5c61-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="d5c61-161">Poniższy przykład przedstawia `StreamReaderEnumerable` w kwerendzie LINQ.</span><span class="sxs-lookup"><span data-stu-id="d5c61-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/Module1.vb#10)]  
  
## <a name="see-also"></a><span data-ttu-id="d5c61-162">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="d5c61-162">See also</span></span>

- [<span data-ttu-id="d5c61-163">Wprowadzenie do LINQ w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="d5c61-163">Introduction to LINQ in Visual Basic</span></span>](../linq/introduction-to-linq.md)
- [<span data-ttu-id="d5c61-164">Przepływ sterowania</span><span class="sxs-lookup"><span data-stu-id="d5c61-164">Control Flow</span></span>](index.md)
- [<span data-ttu-id="d5c61-165">Struktury pętli</span><span class="sxs-lookup"><span data-stu-id="d5c61-165">Loop Structures</span></span>](loop-structures.md)
- [<span data-ttu-id="d5c61-166">For Each...Next, instrukcja</span><span class="sxs-lookup"><span data-stu-id="d5c61-166">For Each...Next Statement</span></span>](../../../language-reference/statements/for-each-next-statement.md)
