---
title: "\"#ElseIf\", \"#Else\" lub \"#End If\" musi być poprzedzona pasującego \"#If\""
ms.date: 07/20/2015
f1_keywords:
- vbc30013
- bc30013
helpviewer_keywords:
- BC30013
ms.assetid: 8fe2d23c-8b8f-46d8-90f2-7f8857ea43bb
ms.openlocfilehash: 92a481febe4d2fb4045e7fbb50eee81c9656b189
ms.sourcegitcommit: 5c1abeec15fbddcc7dbaa729fabc1f1f29f12045
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/15/2019
ms.locfileid: "58031681"
---
# <a name="elseif-else-or-end-if-must-be-preceded-by-a-matching-if"></a><span data-ttu-id="81666-102">"#ElseIf", "#Else" lub "#End If" musi być poprzedzona pasującego "#If"</span><span class="sxs-lookup"><span data-stu-id="81666-102">'#ElseIf', '#Else', or '#End If' must be preceded by a matching '#If'</span></span>
<span data-ttu-id="81666-103">`#ElseIf`, `#Else`, i `#End If` są dyrektywy kompilacji warunkowej.</span><span class="sxs-lookup"><span data-stu-id="81666-103">`#ElseIf`, `#Else`, and `#End If` are conditional compilation directives.</span></span> <span data-ttu-id="81666-104">`#ElseIf`, `#Else`, Lub `#End If` nie jest poprzedzony przez odpowiednią `#If` dyrektywy.</span><span class="sxs-lookup"><span data-stu-id="81666-104">The `#ElseIf`, `#Else`, or `#End If` is not preceded by a corresponding `#If` directive.</span></span>  
  
 <span data-ttu-id="81666-105">**Identyfikator błędu:** BC30013</span><span class="sxs-lookup"><span data-stu-id="81666-105">**Error ID:** BC30013</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="81666-106">Aby poprawić ten błąd</span><span class="sxs-lookup"><span data-stu-id="81666-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="81666-107">Sprawdź, czy zamierzony `#If` nie jest oddzielony od klauzuli zagrożona przez pośredniczące bloku kompilacja warunkowa lub nieprawidłowo umieszczony `#End If`.</span><span class="sxs-lookup"><span data-stu-id="81666-107">Check that the intended `#If` is not separated from the clause in question by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="81666-108">Tylko jeden `#Else` jest dozwolona w każdym `#If` zablokować, więc dwóch kolejnych `#Else` dyrektywy przyczyny wystąpienia tego błędu.</span><span class="sxs-lookup"><span data-stu-id="81666-108">Only one `#Else` is permitted in each `#If` block, so two successive `#Else` directives cause this error.</span></span>  
  
2.  <span data-ttu-id="81666-109">Sprawdź, czy wiodące `#` nie brakuje wcześniejszej `#If` dyrektywy.</span><span class="sxs-lookup"><span data-stu-id="81666-109">Check that the leading `#` is not missing from an earlier `#If` directive.</span></span>  
  
3.  <span data-ttu-id="81666-110">Jeśli cała reszta będzie w kolejności, należy dodać `#If` dyrektywę na początku bloku kompilacji warunkowej.</span><span class="sxs-lookup"><span data-stu-id="81666-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="81666-111">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="81666-111">See also</span></span>

- [<span data-ttu-id="81666-112">#If...Then...#Else, dyrektywy</span><span class="sxs-lookup"><span data-stu-id="81666-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
