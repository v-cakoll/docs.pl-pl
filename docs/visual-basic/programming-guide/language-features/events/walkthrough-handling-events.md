---
title: Obsługa zdarzeń (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: fe797885a9063a19efc3f35da9cdf62d7f271693
ms.sourcegitcommit: ccd8c36b0d74d99291d41aceb14cf98d74dc9d2b
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 12/10/2018
ms.locfileid: "53153387"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="439a2-102">Przewodnik: Obsługa zdarzeń (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="439a2-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="439a2-103">Jest to drugi dwa tematy, które pokazują sposób pracy ze zdarzeniami.</span><span class="sxs-lookup"><span data-stu-id="439a2-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="439a2-104">Pierwszy temat [instruktażu: Deklarujący i wywoływanie zdarzeń](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), pokazuje, jak deklarować i wywoływać zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="439a2-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="439a2-105">Ta sekcja używa formularza i klasy z tego przewodnika, aby pokazują, jak obsługiwać zdarzenia w przypadku mogą mieć miejsce.</span><span class="sxs-lookup"><span data-stu-id="439a2-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="439a2-106">`Widget` Przykład klasy korzysta z tradycyjnych instrukcji obsługi zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="439a2-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="439a2-107">Visual Basic umożliwia innych technik do pracy ze zdarzeniami.</span><span class="sxs-lookup"><span data-stu-id="439a2-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="439a2-108">W charakterze ćwiczenia, można zmodyfikować ten przykład, aby używał `AddHandler` i `Handles` instrukcji.</span><span class="sxs-lookup"><span data-stu-id="439a2-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="439a2-109">Aby obsłużyć zdarzenie PercentDone klasy elementu Widget</span><span class="sxs-lookup"><span data-stu-id="439a2-109">To handle the PercentDone event of the Widget class</span></span>  
  
1.  <span data-ttu-id="439a2-110">Umieść następujący kod w `Form1`:</span><span class="sxs-lookup"><span data-stu-id="439a2-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_1.vb)]  
  
     <span data-ttu-id="439a2-111">`WithEvents` — Słowo kluczowe Określa, że zmienna `mWidget` służy do obsługi zdarzeń obiektu.</span><span class="sxs-lookup"><span data-stu-id="439a2-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="439a2-112">Należy określić rodzaj obiektu, podając nazwę klasy, z którego obiekt zostanie utworzony.</span><span class="sxs-lookup"><span data-stu-id="439a2-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="439a2-113">Zmienna `mWidget` jest zadeklarowana w `Form1` ponieważ `WithEvents` zmienne muszą być na poziomie klasy.</span><span class="sxs-lookup"><span data-stu-id="439a2-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="439a2-114">Ta zasada obowiązuje niezależnie od typu klasy, które można umieścić w.</span><span class="sxs-lookup"><span data-stu-id="439a2-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="439a2-115">Zmienna `mblnCancel` służy do anulowania `LongTask` metody.</span><span class="sxs-lookup"><span data-stu-id="439a2-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="439a2-116">Pisanie kodu, aby obsłużyć zdarzenie</span><span class="sxs-lookup"><span data-stu-id="439a2-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="439a2-117">Jak najszybciej po zadeklarowaniu zmiennej za pomocą `WithEvents`, nazwa zmiennej jest wyświetlana na liście rozwijanej po lewej stronie klasy **Edytor kodu**.</span><span class="sxs-lookup"><span data-stu-id="439a2-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="439a2-118">Po wybraniu `mWidget`, `Widget` klasy zdarzenia są wyświetlane w prawo rozwijanej listy.</span><span class="sxs-lookup"><span data-stu-id="439a2-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="439a2-119">Wybranie event Wyświetla procedury odpowiednie zdarzenia z prefiksem `mWidget` i podkreślenia.</span><span class="sxs-lookup"><span data-stu-id="439a2-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="439a2-120">Procedury zdarzeń skojarzonych z `WithEvents` zmiennej podano nazwę zmiennej jako prefiksu.</span><span class="sxs-lookup"><span data-stu-id="439a2-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="439a2-121">Aby obsłużyć zdarzenie</span><span class="sxs-lookup"><span data-stu-id="439a2-121">To handle an event</span></span>  
  
1.  <span data-ttu-id="439a2-122">Wybierz `mWidget` na liście rozwijanej po lewej stronie w **Edytor kodu**.</span><span class="sxs-lookup"><span data-stu-id="439a2-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2.  <span data-ttu-id="439a2-123">Wybierz `PercentDone` zdarzenie z listy po prawej listy rozwijanej.</span><span class="sxs-lookup"><span data-stu-id="439a2-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="439a2-124">**Edytor kodu** otwiera `mWidget_PercentDone` procedury zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="439a2-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="439a2-125">**Edytor kodu** jest przydatna, ale nie jest to wymagane, do wstawiania nowych programów obsługi zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="439a2-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="439a2-126">W tym instruktażu jest bardziej bezpośrednie, aby po prostu skopiować procedury obsługi zdarzeń bezpośrednio w kodzie.</span><span class="sxs-lookup"><span data-stu-id="439a2-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3.  <span data-ttu-id="439a2-127">Dodaj następujący kod do `mWidget_PercentDone` program obsługi zdarzeń:</span><span class="sxs-lookup"><span data-stu-id="439a2-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_2.vb)]  
  
     <span data-ttu-id="439a2-128">Zawsze, gdy `PercentDone` zdarzenie jest zgłaszane, procedura zdarzenia Wyświetla procent wykonania w `Label` kontroli.</span><span class="sxs-lookup"><span data-stu-id="439a2-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="439a2-129">`DoEvents` Metoda umożliwia etykiety do odświeżenia, a także zapewnia możliwość kliknij **anulować** przycisku.</span><span class="sxs-lookup"><span data-stu-id="439a2-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4.  <span data-ttu-id="439a2-130">Dodaj następujący kod do `Button2_Click` program obsługi zdarzeń:</span><span class="sxs-lookup"><span data-stu-id="439a2-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_3.vb)]  
  
 <span data-ttu-id="439a2-131">Jeśli użytkownik kliknie **anulować** przycisk podczas `LongTask` jest uruchomiona, `Button2_Click` zdarzeń jest wykonywane tak szybko, jak `DoEvents` instrukcji umożliwia przetwarzanie zdarzeń, które wystąpią.</span><span class="sxs-lookup"><span data-stu-id="439a2-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="439a2-132">Zmienna poziomu klasa `mblnCancel` jest ustawiona na `True`i `mWidget_PercentDone` zdarzeń następnie ją sprawdza i ustawia `ByRef Cancel` argument `True`.</span><span class="sxs-lookup"><span data-stu-id="439a2-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="439a2-133">Łączenie zmiennej WithEvents do obiektu</span><span class="sxs-lookup"><span data-stu-id="439a2-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="439a2-134">`Form1` jest teraz skonfigurowane do obsługi `Widget` zdarzenia obiektu.</span><span class="sxs-lookup"><span data-stu-id="439a2-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="439a2-135">Pozostaje tylko można znaleźć `Widget` gdzieś.</span><span class="sxs-lookup"><span data-stu-id="439a2-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="439a2-136">Kiedy Deklarujesz zmienną `WithEvents` w czasie projektowania, żaden obiekt nie jest skojarzony z nim.</span><span class="sxs-lookup"><span data-stu-id="439a2-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="439a2-137">A `WithEvents` zmienna zachowuje się podobnie jak dowolnej zmiennej obiektu.</span><span class="sxs-lookup"><span data-stu-id="439a2-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="439a2-138">Należy utworzyć obiekt i przypisać odwołanie do niej przy użyciu `WithEvents` zmiennej.</span><span class="sxs-lookup"><span data-stu-id="439a2-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="439a2-139">Aby utworzyć obiekt i przypisać odwołanie do niej</span><span class="sxs-lookup"><span data-stu-id="439a2-139">To create an object and assign a reference to it</span></span>  
  
1.  <span data-ttu-id="439a2-140">Wybierz **(formularz Form1 zdarzenia)** na liście rozwijanej po lewej stronie w **Edytor kodu**.</span><span class="sxs-lookup"><span data-stu-id="439a2-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2.  <span data-ttu-id="439a2-141">Wybierz `Load` zdarzenie z listy po prawej listy rozwijanej.</span><span class="sxs-lookup"><span data-stu-id="439a2-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="439a2-142">**Edytor kodu** otwiera `Form1_Load` procedury zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="439a2-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3.  <span data-ttu-id="439a2-143">Dodaj następujący kod do `Form1_Load` procedurę zdarzeń w celu utworzenia `Widget`:</span><span class="sxs-lookup"><span data-stu-id="439a2-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_4.vb)]  
  
 <span data-ttu-id="439a2-144">Gdy ten kod jest wykonywany, Visual Basic tworzy `Widget` obiektu i jego zdarzeń nawiązanie połączenia z procedur zdarzeń związanych z `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="439a2-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="439a2-145">Od tego punktu, gdy `Widget` wywołuje jego `PercentDone` zdarzenia `mWidget_PercentDone` zdarzeń procedura jest wykonywana.</span><span class="sxs-lookup"><span data-stu-id="439a2-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="439a2-146">Aby wywołać metodę LongTask</span><span class="sxs-lookup"><span data-stu-id="439a2-146">To call the LongTask method</span></span>  
  
-   <span data-ttu-id="439a2-147">Dodaj następujący kod do `Button1_Click` program obsługi zdarzeń:</span><span class="sxs-lookup"><span data-stu-id="439a2-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_5.vb)]  
  
 <span data-ttu-id="439a2-148">Przed `LongTask` metoda jest wywoływana, etykieta Wyświetla procent ukończenia musi zostać zainicjowany i poziomu klasa `Boolean` Flaga dla anulowanie metody musi być równa `False`.</span><span class="sxs-lookup"><span data-stu-id="439a2-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="439a2-149">`LongTask` jest wywoływana z czas trwania zadania 12.2 sekund.</span><span class="sxs-lookup"><span data-stu-id="439a2-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="439a2-150">`PercentDone` Zdarzenie jest wywoływane po każdym jednej trzeciej części sekundy.</span><span class="sxs-lookup"><span data-stu-id="439a2-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="439a2-151">Każdorazowo, zdarzenie jest wywoływane, `mWidget_PercentDone` zdarzeń procedura jest wykonywana.</span><span class="sxs-lookup"><span data-stu-id="439a2-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="439a2-152">Gdy `LongTask` zostanie to zrobione, `mblnCancel` jest testowany, aby sprawdzić, czy `LongTask` zakończył się normalnie, czy zatrzymane, ponieważ `mblnCancel` została ustawiona na `True`.</span><span class="sxs-lookup"><span data-stu-id="439a2-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="439a2-153">Procent wykonania jest aktualizowany tylko w przypadku poprzedniej wersji portalu.</span><span class="sxs-lookup"><span data-stu-id="439a2-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="439a2-154">Aby uruchomić program</span><span class="sxs-lookup"><span data-stu-id="439a2-154">To run the program</span></span>  
  
1.  <span data-ttu-id="439a2-155">Naciśnij klawisz F5, aby przełączyć projekt w trybie uruchamiania.</span><span class="sxs-lookup"><span data-stu-id="439a2-155">Press F5 to put the project in run mode.</span></span>  
  
2.  <span data-ttu-id="439a2-156">Kliknij przycisk **Rozpocznij zadanie** przycisku.</span><span class="sxs-lookup"><span data-stu-id="439a2-156">Click the **Start Task** button.</span></span> <span data-ttu-id="439a2-157">Każdorazowo `PercentDone` zdarzenie jest zgłaszane, etykieta zostanie zaktualizowana przy użyciu procent wykonania zadania.</span><span class="sxs-lookup"><span data-stu-id="439a2-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3.  <span data-ttu-id="439a2-158">Kliknij przycisk **anulować** przycisk, aby zatrzymać zadanie.</span><span class="sxs-lookup"><span data-stu-id="439a2-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="439a2-159">Należy zauważyć, że wygląd **anulować** natychmiast po kliknięciu przycisku nie powoduje zmiany.</span><span class="sxs-lookup"><span data-stu-id="439a2-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="439a2-160">`Click` Zdarzeń nie może się zdarzyć, dopóki `My.Application.DoEvents` instrukcji umożliwia przetwarzanie zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="439a2-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="439a2-161">`My.Application.DoEvents` Metody nie przetwarza zdarzeń w taki sam sposób jak formularz.</span><span class="sxs-lookup"><span data-stu-id="439a2-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="439a2-162">Na przykład, w tym przewodniku należy kliknij **anulować** podwójnie.</span><span class="sxs-lookup"><span data-stu-id="439a2-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="439a2-163">Aby zezwolić na formularzu do obsługi zdarzeń bezpośrednio, możesz użyć wielowątkowości.</span><span class="sxs-lookup"><span data-stu-id="439a2-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="439a2-164">Aby uzyskać więcej informacji, zobacz [zarządzana wątkowość](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="439a2-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="439a2-165">Może okazać się istotne, aby uruchomić program za pomocą F11 i przejść przez kod linię w danym momencie.</span><span class="sxs-lookup"><span data-stu-id="439a2-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="439a2-166">Wyraźnie widać, jak wykonanie wprowadza `LongTask`i następnie krótko ponownie wprowadza `Form1` każdorazowo `PercentDone` zdarzenie jest wywoływane.</span><span class="sxs-lookup"><span data-stu-id="439a2-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="439a2-167">Co się stanie, jeśli podczas wykonywania w kodzie `Form1`, `LongTask` metoda była nazywana ponownie?</span><span class="sxs-lookup"><span data-stu-id="439a2-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="439a2-168">W najgorszym przypadku może wystąpić przepełnienie stosu, jeśli `LongTask` były wywoływane za każdym razem, gdy zdarzenia zostało podniesione.</span><span class="sxs-lookup"><span data-stu-id="439a2-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="439a2-169">Może spowodować zmiennej `mWidget` do obsługi zdarzeń dla innego `Widget` obiektu przez przypisywanie odwołania do nowego `Widget` do `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="439a2-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="439a2-170">W rzeczywistości, można wprowadzić kod w `Button1_Click` to zrobić, za każdym razem, gdy klikniesz przycisk.</span><span class="sxs-lookup"><span data-stu-id="439a2-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="439a2-171">Do obsługi zdarzeń dla innego elementu widget</span><span class="sxs-lookup"><span data-stu-id="439a2-171">To handle events for a different widget</span></span>  
  
-   <span data-ttu-id="439a2-172">Dodaj następujący wiersz kodu w celu `Button1_Click` procedury bezpośrednio poprzedzający wiersz zawierający ciąg `mWidget.LongTask(12.2, 0.33)`:</span><span class="sxs-lookup"><span data-stu-id="439a2-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](../../../../visual-basic/programming-guide/language-features/events/codesnippet/VisualBasic/walkthrough-handling-events_6.vb)]  
  
 <span data-ttu-id="439a2-173">Powyższy kod tworzy nową `Widget` każdym kliknięciu przycisku.</span><span class="sxs-lookup"><span data-stu-id="439a2-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="439a2-174">Jak najszybciej `LongTask` metoda zakończeniu odwołanie do `Widget` wydaniu i `Widget` zostanie zniszczony.</span><span class="sxs-lookup"><span data-stu-id="439a2-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="439a2-175">A `WithEvents` zmienna może zawierać tylko jeden obiekt odwołania w czasie, więc Jeśli przypiszesz inną `Widget` obiekt `mWidget`, poprzedni `Widget` zdarzenia obiektu będzie już obsługiwana.</span><span class="sxs-lookup"><span data-stu-id="439a2-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="439a2-176">Jeśli `mWidget` jest tylko zmienna obiekt zawierający odniesienia do starego `Widget`, obiekt zostanie zniszczony.</span><span class="sxs-lookup"><span data-stu-id="439a2-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="439a2-177">Jeśli chcesz obsługiwać zdarzenia z kilku `Widget` obiekty, używają `AddHandler` instrukcję, aby oddzielnie przetwarzania zdarzeń pochodzących z każdego obiektu.</span><span class="sxs-lookup"><span data-stu-id="439a2-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="439a2-178">Można zadeklarować dowolną liczbę `WithEvents` zmiennych, jak należy, ale tablic `WithEvents` zmienne nie są obsługiwane.</span><span class="sxs-lookup"><span data-stu-id="439a2-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="439a2-179">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="439a2-179">See Also</span></span>  
 [<span data-ttu-id="439a2-180">Wskazówki: Deklarowanie i wywoływanie zdarzeń</span><span class="sxs-lookup"><span data-stu-id="439a2-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)  
 [<span data-ttu-id="439a2-181">Zdarzenia</span><span class="sxs-lookup"><span data-stu-id="439a2-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
