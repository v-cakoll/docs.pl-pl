---
title: Tablice
ms.date: 12/06/2017
f1_keywords:
- vb.Array
helpviewer_keywords:
- arrays [Visual Basic]
- Visual Basic, arrays
ms.assetid: dbf29737-b589-4443-bee6-a27588d9c67e
ms.openlocfilehash: 5093f28f05c5b72294dce9a4e69723acafb31a9f
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/04/2020
ms.locfileid: "84413094"
---
# <a name="arrays-in-visual-basic"></a><span data-ttu-id="bd0e2-102">Tablice w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="bd0e2-102">Arrays in Visual Basic</span></span>

<span data-ttu-id="bd0e2-103">Tablica jest zestawem wartości, które są oznaczanymi *elementami*, które są logicznie powiązane ze sobą.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-103">An array is a set of values, which are termed *elements*, that are logically related to each other.</span></span> <span data-ttu-id="bd0e2-104">Na przykład tablica może zawierać liczbę studentów w każdej klasie w szkole gramatyki; Każdy element tablicy to liczba studentów w jednej klasie.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-104">For example, an array may consist of the number of students in each grade in a grammar school; each element of the array is the number of students in a single grade.</span></span> <span data-ttu-id="bd0e2-105">Podobnie tablica może składać się z kategorii uczniów dla klasy; Każdy element tablicy jest pojedynczą klasy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-105">Similarly, an array may consist of a student's grades for a class; each element of the array is a single grade.</span></span>

<span data-ttu-id="bd0e2-106">Możliwe jest, że poszczególne zmienne przechowują poszczególne elementy danych.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-106">It is possible individual variables to store each of our data items.</span></span> <span data-ttu-id="bd0e2-107">Na przykład jeśli nasza aplikacja analizuje klasy uczniów, możemy użyć osobnej zmiennej dla każdej studenta, np `englishGrade1` `englishGrade2` ., itp. Takie podejście ma trzy zasadnicze ograniczenia:</span><span class="sxs-lookup"><span data-stu-id="bd0e2-107">For example, if our application analyzes student grades, we can use a separate variable for each student's grade, such as `englishGrade1`, `englishGrade2`, etc. This approach has three major limitations:</span></span>

- <span data-ttu-id="bd0e2-108">Musimy znać czas projektowania dokładnie na liczbę potrzebnych do obsłużenia.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-108">We have to know at design time exactly how many grades we have to handle.</span></span>
- <span data-ttu-id="bd0e2-109">Obsługa dużej liczby ocen szybko zmieni się na nieporęczny.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-109">Handling large numbers of grades quickly becomes unwieldy.</span></span> <span data-ttu-id="bd0e2-110">Dzięki temu aplikacja ma znacznie większą liczbę błędów.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-110">This in turn makes an application much more likely to have serious bugs.</span></span>
- <span data-ttu-id="bd0e2-111">Jest trudno zachować.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-111">It is difficult to maintain.</span></span> <span data-ttu-id="bd0e2-112">Każda nowa dodawana Klasa wymaga, aby aplikacja została zmodyfikowana, ponownie skompilowana i wdrożona ponownie.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-112">Each new grade that we add requires that the application be modified, recompiled, and redeployed.</span></span>

<span data-ttu-id="bd0e2-113">Korzystając z tablicy, można odwoływać się do tych powiązanych wartości o tej samej nazwie i użyć numeru, który jest nazywany *indeksem* lub *dolnym podindeksem* , aby zidentyfikować pojedynczy element na podstawie jego położenia w tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-113">By using an array, you can refer to these related values by the same name, and use a number that’s called an *index* or *subscript* to identify an individual element based on its position in the array.</span></span> <span data-ttu-id="bd0e2-114">Indeksy zakresu tablicy od 0 do jeden są mniejsze od całkowitej liczby elementów w tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-114">The indexes of an array range from 0 to one less than the total number of elements in the array.</span></span> <span data-ttu-id="bd0e2-115">W przypadku użycia składni Visual Basic w celu zdefiniowania rozmiaru tablicy należy określić jej najwyższy indeks, a nie całkowitą liczbę elementów w tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-115">When you use Visual Basic syntax to define the size of an array, you specify its highest index, not the total number of elements in the array.</span></span> <span data-ttu-id="bd0e2-116">Możesz współpracować z tablicą jako jednostką, a możliwość iteracji jej elementów uwalnia użytkownika od potrzeb, aby wiedzieć dokładnie, ile elementów zawiera w czasie projektowania.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-116">You can work with the array as a unit, and the ability to iterate its elements frees you from needing to know exactly how many elements it contains at design time.</span></span>

<span data-ttu-id="bd0e2-117">Kilka szybkich przykładów przed wyjaśnieniem:</span><span class="sxs-lookup"><span data-stu-id="bd0e2-117">Some quick examples before explanation:</span></span>

```vb
' Declare a single-dimension array of 5 numbers.
Dim numbers(4) As Integer

' Declare a single-dimension array and set its 4 values.
Dim numbers = New Integer() {1, 2, 4, 8}

' Change the size of an existing array to 16 elements and retain the current values.
ReDim Preserve numbers(15)

' Redefine the size of an existing array and reset the values.
ReDim numbers(15)

' Declare a 6 x 6 multidimensional array.
Dim matrix(5, 5) As Double

' Declare a 4 x 3 multidimensional array and set array element values.
Dim matrix = New Integer(3, 2) {{1, 2, 3}, {2, 3, 4}, {3, 4, 5}, {4, 5, 6}}

' Declare a jagged array
Dim sales()() As Double = New Double(11)() {}
```

## <a name="array-elements-in-a-simple-array"></a><span data-ttu-id="bd0e2-118">Elementy tablicy w prostej tablicy</span><span class="sxs-lookup"><span data-stu-id="bd0e2-118">Array elements in a simple array</span></span>

<span data-ttu-id="bd0e2-119">Utwórzmy tablicę o nazwie `students` , aby przechowywać liczbę studentów w każdej klasie w szkole gramatyki.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-119">Let's create an array named `students` to store the number of students in each grade in a grammar school.</span></span> <span data-ttu-id="bd0e2-120">Indeksy elementów mieszczą się w zakresie od 0 do 6.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-120">The indexes of the elements range from 0 through 6.</span></span> <span data-ttu-id="bd0e2-121">Użycie tej tablicy jest prostsze niż deklarowanie siedmiu zmiennych.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-121">Using this array is simpler than declaring seven variables.</span></span>

<span data-ttu-id="bd0e2-122">Na poniższej ilustracji przedstawiono `students` tablicę.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-122">The following illustration shows the `students` array.</span></span> <span data-ttu-id="bd0e2-123">Dla każdego elementu tablicy:</span><span class="sxs-lookup"><span data-stu-id="bd0e2-123">For each element of the array:</span></span>

- <span data-ttu-id="bd0e2-124">Indeks elementu reprezentuje ocenę (indeks 0 reprezentuje przedszkole).</span><span class="sxs-lookup"><span data-stu-id="bd0e2-124">The index of the element represents the grade (index 0 represents kindergarten).</span></span>

- <span data-ttu-id="bd0e2-125">Wartość zawarta w elemencie reprezentuje liczbę uczniów w tej klasie.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-125">The value that’s contained in the element represents the number of students in that grade.</span></span>

![Diagram przedstawiający tablicę liczb uczniów](./media/index/students-array-elements.gif)

<span data-ttu-id="bd0e2-127">Poniższy przykład zawiera kod Visual Basic, który tworzy i używa tablicy:</span><span class="sxs-lookup"><span data-stu-id="bd0e2-127">The following example contains the Visual Basic code that creates and uses the array:</span></span>

[!code-vb[simple-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/simple-array.vb)]

<span data-ttu-id="bd0e2-128">Przykład wykonuje trzy czynności:</span><span class="sxs-lookup"><span data-stu-id="bd0e2-128">The example does three things:</span></span>

- <span data-ttu-id="bd0e2-129">Deklaruje `students` tablicę z siedmiu elementami.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-129">It declares a `students` array with seven elements.</span></span> <span data-ttu-id="bd0e2-130">Liczba `6` w deklaracji tablicy wskazuje ostatni indeks w tablicy; jest ona mniejsza niż liczba elementów w tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-130">The number `6` in the array declaration indicates the last index in the array; it is one less than the number of elements in the array.</span></span>
- <span data-ttu-id="bd0e2-131">Przypisuje wartości do każdego elementu w tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-131">It assigns values to each element in the array.</span></span> <span data-ttu-id="bd0e2-132">Do elementów tablicy uzyskuje się dostęp przy użyciu nazwy tablicy i w tym indeks poszczególnych elementów w nawiasach.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-132">Array elements are accessed by using the array name and including the index of the individual element in parentheses.</span></span>
- <span data-ttu-id="bd0e2-133">Wyświetla listę każdej wartości tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-133">It lists each value of the array.</span></span> <span data-ttu-id="bd0e2-134">W przykładzie używa się [`For`](../../../language-reference/statements/for-next-statement.md) instrukcji w celu uzyskania dostępu do każdego elementu tablicy według numeru indeksu.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-134">The example uses a [`For`](../../../language-reference/statements/for-next-statement.md) statement to access each element of the array by its index number.</span></span>

<span data-ttu-id="bd0e2-135">`students`Tablica w poprzednim przykładzie jest tablicą jednowymiarową, ponieważ używa jednego indeksu.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-135">The `students` array in the preceding example is a one-dimensional array because it uses one index.</span></span> <span data-ttu-id="bd0e2-136">Tablica, która używa więcej niż jednego indeksu lub indeks dolny, jest nazywana *wielowymiarowych*.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-136">An array that uses more than one index or subscript is called *multidimensional*.</span></span> <span data-ttu-id="bd0e2-137">Aby uzyskać więcej informacji, zobacz pozostała część tego artykułu i [Wymiary tablicy w Visual Basic](array-dimensions.md).</span><span class="sxs-lookup"><span data-stu-id="bd0e2-137">For more information, see the rest of this article and [Array Dimensions in Visual Basic](array-dimensions.md).</span></span>

## <a name="creating-an-array"></a><span data-ttu-id="bd0e2-138">Tworzenie tablicy</span><span class="sxs-lookup"><span data-stu-id="bd0e2-138">Creating an array</span></span>

<span data-ttu-id="bd0e2-139">Rozmiar tablicy można zdefiniować na kilka sposobów:</span><span class="sxs-lookup"><span data-stu-id="bd0e2-139">You can define the size of an array in several ways:</span></span>

- <span data-ttu-id="bd0e2-140">Możesz określić rozmiar, gdy tablica jest zadeklarowana:</span><span class="sxs-lookup"><span data-stu-id="bd0e2-140">You can specify the size when the array is declared:</span></span>

  [!code-vb[creating1](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#1)]

- <span data-ttu-id="bd0e2-141">Można użyć klauzuli, `New` Aby podać rozmiar tablicy podczas jej tworzenia:</span><span class="sxs-lookup"><span data-stu-id="bd0e2-141">You can use a `New` clause to supply the size of an array when it’s created:</span></span>

  [!code-vb[creating2](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#2)]

<span data-ttu-id="bd0e2-142">Jeśli masz istniejącą tablicę, możesz zmienić jej rozmiar przy użyciu [`ReDim`](../../../language-reference/statements/redim-statement.md) instrukcji.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-142">If you have an existing array, you can redefine its size by using the [`ReDim`](../../../language-reference/statements/redim-statement.md) statement.</span></span> <span data-ttu-id="bd0e2-143">Można określić, że `ReDim` instrukcja ma przechowywać wartości, które znajdują się w tablicy, lub można określić, że ma ona tworzyć pustą tablicę.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-143">You can specify that the `ReDim` statement keep the values that are in the array, or you can specify that it create an empty array.</span></span> <span data-ttu-id="bd0e2-144">W poniższym przykładzie pokazano różne zastosowania `ReDim` instrukcji w celu zmodyfikowania rozmiaru istniejącej tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-144">The following example shows different uses of the `ReDim` statement to modify the size of an existing array.</span></span>

[!code-vb[redimensioning](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#3)]

<span data-ttu-id="bd0e2-145">Aby uzyskać więcej informacji, zobacz [Instrukcja ReDim](../../../language-reference/statements/redim-statement.md).</span><span class="sxs-lookup"><span data-stu-id="bd0e2-145">For more information, see the [ReDim Statement](../../../language-reference/statements/redim-statement.md).</span></span>

## <a name="storing-values-in-an-array"></a><span data-ttu-id="bd0e2-146">Przechowywanie wartości w tablicy</span><span class="sxs-lookup"><span data-stu-id="bd0e2-146">Storing values in an array</span></span>

<span data-ttu-id="bd0e2-147">Możesz uzyskać dostęp do każdej lokalizacji w tablicy przy użyciu indeksu typu `Integer` .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-147">You can access each location in an array by using an index of type `Integer`.</span></span> <span data-ttu-id="bd0e2-148">Możesz przechowywać i pobierać wartości w tablicy, odwołując się do każdej lokalizacji tablicy przy użyciu indeksu ujętego w nawiasy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-148">You can store and retrieve values in an array by referencing each array location by using its index enclosed in parentheses.</span></span> <span data-ttu-id="bd0e2-149">Indeksy dla tablic wielowymiarowych są oddzielone przecinkami (,).</span><span class="sxs-lookup"><span data-stu-id="bd0e2-149">Indexes for multidimensional arrays are separated by commas (,).</span></span> <span data-ttu-id="bd0e2-150">Wymagany jest jeden indeks dla każdego wymiaru tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-150">You need one index for each array dimension.</span></span>

<span data-ttu-id="bd0e2-151">Poniższy przykład ukazuje niektóre instrukcje, które przechowują i pobierają wartości w tablicach.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-151">The following example shows some statements that store and retrieve values in arrays.</span></span>

[!code-vb[store-and-retrieve](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/store-and-retrieve.vb)]

## <a name="populating-an-array-with-array-literals"></a><span data-ttu-id="bd0e2-152">Wypełnianie tablicy przy użyciu literałów tablicowych</span><span class="sxs-lookup"><span data-stu-id="bd0e2-152">Populating an array with array literals</span></span>

<span data-ttu-id="bd0e2-153">Za pomocą literału tablicowego można wypełnić tablicę początkowym zestawem wartości w tym samym czasie, w którym została utworzona.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-153">By using an array literal, you can populate an array with an initial set of values at the same time that you create it.</span></span> <span data-ttu-id="bd0e2-154">Literał tablicowy składa się z listy wartości rozdzielonych przecinkami, które są ujęte w nawiasy klamrowe ( `{}` ).</span><span class="sxs-lookup"><span data-stu-id="bd0e2-154">An array literal consists of a list of comma-separated values that are enclosed in braces (`{}`).</span></span>

<span data-ttu-id="bd0e2-155">Podczas tworzenia tablicy przy użyciu literału tablicowego można dostarczyć typ tablicy lub użyć wnioskowania o typie, aby określić typ tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-155">When you create an array by using an array literal, you can either supply the array type or use type inference to determine the array type.</span></span> <span data-ttu-id="bd0e2-156">W poniższym przykładzie przedstawiono obie opcje.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-156">The following example shows both options.</span></span>

[!code-vb[create-with-literals](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#4)]

<span data-ttu-id="bd0e2-157">Gdy używasz wnioskowania o typie, typ tablicy jest określany przez *Typ dominujący* na liście wartości literału.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-157">When you use type inference, the type of the array is determined by the *dominant type* in the list of literal values.</span></span> <span data-ttu-id="bd0e2-158">Typ dominujący jest typem, do którego można rozszerzyć wszystkie inne typy w tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-158">The dominant type is the type to which all other types in the array can widen.</span></span> <span data-ttu-id="bd0e2-159">Jeśli nie można określić tego unikatowego typu, typ dominujący jest unikatowym typem, do którego można zawęzić wszystkie inne typy w tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-159">If this unique type can’t be determined, the dominant type is the unique type to which all other types in the array can narrow.</span></span> <span data-ttu-id="bd0e2-160">Jeśli nie można określić żadnego z tych unikatowych typów, typ dominujący to `Object` .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-160">If neither of these unique types can be determined, the dominant type is `Object`.</span></span> <span data-ttu-id="bd0e2-161">Na przykład, jeśli lista wartości, które są dostarczone do literału tablicowego zawiera wartości typu `Integer` , `Long` , i `Double` , Tablica wyników jest typu `Double` .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-161">For example, if the list of values that’s supplied to the array literal contains values of type `Integer`, `Long`, and `Double`, the resulting array is of type `Double`.</span></span> <span data-ttu-id="bd0e2-162">Ponieważ `Integer` i `Long` rozszerza tylko do `Double` , `Double` jest typem dominującym.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-162">Because `Integer` and `Long` widen only to `Double`, `Double` is the dominant type.</span></span> <span data-ttu-id="bd0e2-163">Aby uzyskać więcej informacji, zobacz [rozszerzanie i zwężanie konwersji](../data-types/widening-and-narrowing-conversions.md).</span><span class="sxs-lookup"><span data-stu-id="bd0e2-163">For more information, see [Widening and Narrowing Conversions](../data-types/widening-and-narrowing-conversions.md).</span></span>

> [!NOTE]
> <span data-ttu-id="bd0e2-164">Wnioskowanie o typie można użyć tylko dla tablic, które są zdefiniowane jako zmienne lokalne w składowej typu.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-164">You can use type inference only for arrays that are defined as local variables in a type member.</span></span> <span data-ttu-id="bd0e2-165">Jeśli nie istnieje jawna definicja typu, tablice zdefiniowane za pomocą literałów tablicowych na poziomie klasy są typu `Object[]` .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-165">If an explicit type definition is absent, arrays defined with array literals at the class level are of type `Object[]`.</span></span> <span data-ttu-id="bd0e2-166">Aby uzyskać więcej informacji, zobacz temat [wnioskowanie o typie lokalnym](../variables/local-type-inference.md).</span><span class="sxs-lookup"><span data-stu-id="bd0e2-166">For more information, see [Local type inference](../variables/local-type-inference.md).</span></span>

<span data-ttu-id="bd0e2-167">Należy zauważyć, że poprzedni przykład definiuje `values` jako tablicę typu, `Double` mimo że wszystkie literały tablicowe są typu `Integer` .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-167">Note that the previous example defines `values` as an array of type `Double` even though all the array literals are of type `Integer`.</span></span> <span data-ttu-id="bd0e2-168">Można utworzyć tę tablicę, ponieważ wartości w literale Array można rozszerzyć na `Double` wartości.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-168">You can create this array because the values in the array literal can widen to `Double` values.</span></span>

<span data-ttu-id="bd0e2-169">Możesz również utworzyć i wypełnić tablicę wielowymiarową przy użyciu *zagnieżdżonych literałów tablicowych*.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-169">You can also create and populate a multidimensional array by using *nested array literals*.</span></span> <span data-ttu-id="bd0e2-170">Zagnieżdżone literały tablicowe muszą mieć różne wymiary zgodne z tablicą wyników.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-170">Nested array literals must have a number of dimensions that’s consistent with the resulting array.</span></span> <span data-ttu-id="bd0e2-171">Poniższy przykład tworzy dwuwymiarową tablicę liczb całkowitych przy użyciu zagnieżdżonych literałów tablicowych.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-171">The following example creates a two-dimensional array of integers by using nested array literals.</span></span>

[!code-vb[nested-array-literals](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#5)]

<span data-ttu-id="bd0e2-172">W przypadku używania zagnieżdżonych literałów tablicowych do tworzenia i wypełniania tablicy, występuje błąd, jeśli liczba elementów w zagnieżdżonych literałach tablicowych nie jest zgodna.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-172">When using nested array literals to create and populate an array, an error occurs if the number of elements in the nested array literals don't match.</span></span> <span data-ttu-id="bd0e2-173">Błąd występuje również wtedy, gdy jawnie deklaruje zmienną tablicową tak, aby miała różną liczbę wymiarów niż literały tablicowe.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-173">An error also occurs if you explicitly declare the array variable to have a different number of dimensions than the array literals.</span></span>

<span data-ttu-id="bd0e2-174">Podobnie jak w przypadku tablic jednowymiarowych można polegać na wnioskach o typie podczas tworzenia wielowymiarowej tablicy z zagnieżdżonymi literałami tablicowymi.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-174">Just as you can for one-dimensional arrays, you can rely on type inference when creating a multidimensional array with nested array literals.</span></span> <span data-ttu-id="bd0e2-175">Typ wnioskowany jest typem dominującym dla wszystkich wartości we wszystkich literałach tablicowych dla wszystkich poziomów zagnieżdżenia.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-175">The inferred type is the dominant type for all the values in all the array literals for all nesting level.</span></span> <span data-ttu-id="bd0e2-176">Poniższy przykład tworzy dwuwymiarową tablicę typu `Double[,]` z wartości, które są typu `Integer` i `Double` .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-176">The following example creates a two-dimensional array of type `Double[,]` from values that are of type `Integer` and `Double`.</span></span>

[!code-vb[nested-type-inference](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#6)]

<span data-ttu-id="bd0e2-177">Aby uzyskać więcej przykładów, zobacz [How to: Initialize a Array Variable in Visual Basic](how-to-initialize-an-array-variable.md).</span><span class="sxs-lookup"><span data-stu-id="bd0e2-177">For additional examples, see [How to: Initialize an Array Variable in Visual Basic](how-to-initialize-an-array-variable.md).</span></span>

## <a name="iterating-through-an-array"></a><span data-ttu-id="bd0e2-178">Iteracja przez tablicę</span><span class="sxs-lookup"><span data-stu-id="bd0e2-178">Iterating through an array</span></span>

<span data-ttu-id="bd0e2-179">Podczas iteracji przez tablicę uzyskuje się dostęp do każdego elementu w tablicy od najniższego indeksu do najwyższej lub od najwyższego do najniższego.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-179">When you iterate through an array, you access each element in the array from the lowest index to the highest or from the highest to the lowest.</span></span> <span data-ttu-id="bd0e2-180">Zazwyczaj należy użyć [dla... Next](../../../language-reference/statements/for-next-statement.md) lub [for each... Następna instrukcja](../../../language-reference/statements/for-each-next-statement.md) do iteracji przez elementy tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-180">Typically, use either the [For...Next Statement](../../../language-reference/statements/for-next-statement.md) or the [For Each...Next Statement](../../../language-reference/statements/for-each-next-statement.md) to iterate through the elements of an array.</span></span> <span data-ttu-id="bd0e2-181">Gdy nie znasz górnych granic tablicy, możesz wywołać <xref:System.Array.GetUpperBound%2A?displayProperty=nameWithType> metodę, aby uzyskać najwyższą wartość indeksu.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-181">When you don't know the upper bounds of the array, you can call the <xref:System.Array.GetUpperBound%2A?displayProperty=nameWithType> method to get the highest value of the index.</span></span> <span data-ttu-id="bd0e2-182">Chociaż najniższa wartość indeksu jest niemal zawsze równa 0, można wywołać <xref:System.Array.GetLowerBound%2A?displayProperty=nameWithType> metodę, aby uzyskać najniższą wartość indeksu.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-182">Although lowest index value is almost always 0, you can call the <xref:System.Array.GetLowerBound%2A?displayProperty=nameWithType> method to get the lowest value of the index.</span></span>

<span data-ttu-id="bd0e2-183">Poniższy przykład wykonuje iterację przez tablicę jednowymiarową za pomocą [`For...Next`](../../../language-reference/statements/for-next-statement.md) instrukcji.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-183">The following example iterates through a one-dimensional array by using the [`For...Next`](../../../language-reference/statements/for-next-statement.md) statement.</span></span>

[!code-vb[iterate-one-dimensional-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate1d.vb)]

<span data-ttu-id="bd0e2-184">Poniższy przykład wykonuje iterację przez tablicę wielowymiarową za pomocą [`For...Next`](../../../language-reference/statements/for-next-statement.md) instrukcji.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-184">The following example iterates through a multidimensional array by using a [`For...Next`](../../../language-reference/statements/for-next-statement.md) statement.</span></span> <span data-ttu-id="bd0e2-185"><xref:System.Array.GetUpperBound%2A>Metoda ma parametr, który określa wymiar.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-185">The <xref:System.Array.GetUpperBound%2A> method has a parameter that specifies the dimension.</span></span> <span data-ttu-id="bd0e2-186">`GetUpperBound(0)`zwraca najwyższy indeks pierwszego wymiaru i `GetUpperBound(1)` zwraca najwyższy indeks drugiego wymiaru.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-186">`GetUpperBound(0)` returns the highest index of the first dimension, and `GetUpperBound(1)` returns the highest index of the second dimension.</span></span>

[!code-vb[iterate-two-dimensional-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate2d.vb)]

<span data-ttu-id="bd0e2-187">W poniższym przykładzie zastosowano [for each... Następna instrukcja](../../../language-reference/statements/for-each-next-statement.md)do iteracji przez tablicę jednowymiarową i dwuwymiarową tablicę.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-187">The following example uses a [For Each...Next Statement](../../../language-reference/statements/for-each-next-statement.md)to iterate through a one-dimensional array and a two-dimensional array.</span></span>

[!code-vb[iterate-for-each-next](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate-for-each-next.vb)]

## <a name="array-size"></a><span data-ttu-id="bd0e2-188">Rozmiar tablicy</span><span class="sxs-lookup"><span data-stu-id="bd0e2-188">Array size</span></span>

<span data-ttu-id="bd0e2-189">Rozmiar tablicy jest iloczynem długości wszystkich wymiarów.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-189">The size of an array is the product of the lengths of all its dimensions.</span></span> <span data-ttu-id="bd0e2-190">Reprezentuje łączną liczbę elementów aktualnie znajdujących się w tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-190">It represents the total number of elements currently contained in the array.</span></span>  <span data-ttu-id="bd0e2-191">Na przykład poniższy przykład deklaruje tablicę 2-wymiarową z czterema elementami w każdym wymiarze.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-191">For example, the following example declares a 2-dimensional array with four elements in each dimension.</span></span> <span data-ttu-id="bd0e2-192">Jak widać dane wyjściowe z przykładu, rozmiar tablicy wynosi 16 (lub (3 + 1) \* (3 + 1).</span><span class="sxs-lookup"><span data-stu-id="bd0e2-192">As the output from the example shows, the array's size is 16 (or (3 + 1) \* (3 + 1).</span></span>

[!code-vb[array-size](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-size.vb)]

> [!NOTE]
> <span data-ttu-id="bd0e2-193">Ta dyskusja rozmiaru tablicy nie ma zastosowania do tablic nieregularnych.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-193">This discussion of array size does not apply to jagged arrays.</span></span> <span data-ttu-id="bd0e2-194">Aby uzyskać informacje na temat tablic nieregularnych i określania rozmiaru tablicy nieregularnej, zobacz sekcję [tablice nieregularne](#jagged-arrays) .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-194">For information on jagged arrays and determining the size of a jagged array, see the [Jagged arrays](#jagged-arrays) section.</span></span>

<span data-ttu-id="bd0e2-195">Rozmiar tablicy można znaleźć za pomocą <xref:System.Array.Length%2A?displayProperty=nameWithType> właściwości.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-195">You can find the size of an array by using the <xref:System.Array.Length%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="bd0e2-196">Długość każdego wymiaru tablicy wielowymiarowej można znaleźć za pomocą <xref:System.Array.GetLength%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-196">You can find the length of each dimension of a multidimensional array by using the <xref:System.Array.GetLength%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="bd0e2-197">Można zmienić rozmiar zmiennej tablicowej, przypisując do niej nowy obiekt array lub używając instrukcji [ `ReDim` instrukcji](../../../language-reference/statements/redim-statement.md) .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-197">You can resize an array variable by assigning a new array object to it or by using the [`ReDim` Statement](../../../language-reference/statements/redim-statement.md) statement.</span></span> <span data-ttu-id="bd0e2-198">Poniższy przykład używa instrukcji, `ReDim` Aby zmienić tablicę elementów 100 do 51-elementowej tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-198">The following example uses the `ReDim` statement to change a 100-element array to a 51-element array.</span></span>

[!code-vb[resize-an-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-size2.vb)]

<span data-ttu-id="bd0e2-199">Należy pamiętać o kilku kwestiach związanych z rozmiarem tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-199">There are several things to keep in mind when dealing with the size of an array.</span></span>

|||
|---|---|
|<span data-ttu-id="bd0e2-200">Długość wymiaru</span><span class="sxs-lookup"><span data-stu-id="bd0e2-200">Dimension Length</span></span>|<span data-ttu-id="bd0e2-201">Indeks każdego wymiaru jest oparty na 0, co oznacza, że zakres od 0 do jego górnej granicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-201">The index of each dimension is 0-based, which means it ranges from 0 to its upper bound.</span></span> <span data-ttu-id="bd0e2-202">W związku z tym długość danego wymiaru jest większa niż zadeklarowana Górna granica tego wymiaru.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-202">Therefore, the length of a given dimension is one greater than the declared upper bound of that dimension.</span></span>|
|<span data-ttu-id="bd0e2-203">Limity długości</span><span class="sxs-lookup"><span data-stu-id="bd0e2-203">Length Limits</span></span>|<span data-ttu-id="bd0e2-204">Długość każdego wymiaru tablicy jest ograniczona do maksymalnej wartości `Integer` typu danych, czyli <xref:System.Int32.MaxValue?displayProperty=nameWithType> lub (2 ^ 31)-1.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-204">The length of every dimension of an array is limited to the maximum value of the `Integer` data type, which is <xref:System.Int32.MaxValue?displayProperty=nameWithType> or (2 ^ 31) - 1.</span></span> <span data-ttu-id="bd0e2-205">Jednak łączny rozmiar tablicy jest również ograniczony przez pamięć dostępną w systemie.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-205">However, the total size of an array is also limited by the memory available on your system.</span></span> <span data-ttu-id="bd0e2-206">Jeśli spróbujesz zainicjować tablicę, która przekracza ilość dostępnej pamięci, środowisko uruchomieniowe wygeneruje <xref:System.OutOfMemoryException> .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-206">If you attempt to initialize an array that exceeds the amount of available memory, the runtime throws an <xref:System.OutOfMemoryException>.</span></span>|
|<span data-ttu-id="bd0e2-207">Rozmiar i rozmiar elementu</span><span class="sxs-lookup"><span data-stu-id="bd0e2-207">Size and Element Size</span></span>|<span data-ttu-id="bd0e2-208">Rozmiar tablicy jest niezależny od typu danych jego elementów.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-208">An array's size is independent of the data type of its elements.</span></span> <span data-ttu-id="bd0e2-209">Rozmiar zawsze reprezentuje całkowitą liczbę elementów, a nie liczbę bajtów, które zużywają w pamięci.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-209">The size always represents the total number of elements, not the number of bytes that they consume in memory.</span></span>|
|<span data-ttu-id="bd0e2-210">Zużycie pamięci</span><span class="sxs-lookup"><span data-stu-id="bd0e2-210">Memory Consumption</span></span>|<span data-ttu-id="bd0e2-211">Nie można bezpiecznie wprowadzać żadnych założeń dotyczących sposobu przechowywania tablicy w pamięci.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-211">It is not safe to make any assumptions regarding how an array is stored in memory.</span></span> <span data-ttu-id="bd0e2-212">Magazyn różni się w zależności od platformy różnych szerokości danych, dlatego ta sama tablica może zużywać więcej pamięci w systemie 64-bitowym niż w systemie 32-bitowym.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-212">Storage varies on platforms of different data widths, so the same array can consume more memory on a 64-bit system than on a 32-bit system.</span></span> <span data-ttu-id="bd0e2-213">W zależności od konfiguracji systemu po zainicjowaniu tablicy środowisko uruchomieniowe języka wspólnego (CLR) może przypisywać magazyn do spakowania elementów jako blisko siebie, jak to możliwe, lub aby wyrównać je wszystkie do naturalnych granic sprzętu.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-213">Depending on system configuration when you initialize an array, the common language runtime (CLR) can assign storage either to pack elements as close together as possible, or to align them all on natural hardware boundaries.</span></span> <span data-ttu-id="bd0e2-214">Ponadto tablica wymaga nakładu pracy związanego z magazynowaniem danych sterujących, a narzuty zwiększają się przy każdym dodanym wymiarze.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-214">Also, an array requires a storage overhead for its control information, and this overhead increases with each added dimension.</span></span>|

## <a name="the-array-type"></a><span data-ttu-id="bd0e2-215">Typ tablicy</span><span class="sxs-lookup"><span data-stu-id="bd0e2-215">The array type</span></span>

<span data-ttu-id="bd0e2-216">Każda tablica ma typ danych, który różni się od typu danych jego elementów.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-216">Every array has a data type, which differs from the data type of its elements.</span></span> <span data-ttu-id="bd0e2-217">Brak pojedynczego typu danych dla wszystkich tablic.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-217">There is no single data type for all arrays.</span></span> <span data-ttu-id="bd0e2-218">Zamiast tego typ danych tablicy jest określany przez liczbę wymiarów lub *rangę*tablicy oraz typ danych elementów w tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-218">Instead, the data type of an array is determined by the number of dimensions, or *rank*, of the array, and the data type of the elements in the array.</span></span> <span data-ttu-id="bd0e2-219">Dwie zmienne tablic są tego samego typu danych tylko wtedy, gdy mają tę samą rangę, a ich elementy mają ten sam typ danych.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-219">Two array variables are of the same data type only when they have the same rank and their elements have the same data type.</span></span> <span data-ttu-id="bd0e2-220">Długość wymiarów tablicy nie ma wpływu na typ danych tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-220">The lengths of the dimensions of an array do not influence the array data type.</span></span>

<span data-ttu-id="bd0e2-221">Każda tablica dziedziczy z <xref:System.Array?displayProperty=nameWithType> klasy i można zadeklarować zmienną jako typu `Array` , ale nie można utworzyć tablicy typu `Array` .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-221">Every array inherits from the <xref:System.Array?displayProperty=nameWithType> class, and you can declare a variable to be of type `Array`, but you cannot create an array of type `Array`.</span></span> <span data-ttu-id="bd0e2-222">Na przykład, chociaż Poniższy kod deklaruje `arr` zmienną jako typ `Array` i wywołuje <xref:System.Array.CreateInstance%2A?displayProperty=nameWithType> metodę w celu utworzenia wystąpienia tablicy, typ tablicy udowadnia się obiektem [].</span><span class="sxs-lookup"><span data-stu-id="bd0e2-222">For example, although the following code declares the `arr` variable to be of type `Array` and calls the <xref:System.Array.CreateInstance%2A?displayProperty=nameWithType> method to instantiate the array, the array's type proves to be Object[].</span></span>

[!code-vb[array-class](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-class.vb)]

<span data-ttu-id="bd0e2-223">Ponadto [Instrukcja ReDim](../../../language-reference/statements/redim-statement.md) nie może działać na zmiennej zadeklarowanej jako typ `Array` .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-223">Also, the [ReDim Statement](../../../language-reference/statements/redim-statement.md) cannot operate on a variable declared as type `Array`.</span></span> <span data-ttu-id="bd0e2-224">Z tych powodów i dla bezpieczeństwa typów zaleca się zadeklarowanie każdej tablicy jako określonego typu.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-224">For these reasons, and for type safety, it is advisable to declare every array as a specific type.</span></span>

<span data-ttu-id="bd0e2-225">Możesz sprawdzić typ danych tablicy lub jej elementów na kilka sposobów.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-225">You can find out the data type of either an array or its elements in several ways.</span></span>

- <span data-ttu-id="bd0e2-226">Można wywołać <xref:System.Object.GetType%2A> metodę na zmiennej, aby uzyskać <xref:System.Type> obiekt, który reprezentuje typ czasu wykonywania zmiennej.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-226">You can call the <xref:System.Object.GetType%2A> method on the variable to get a <xref:System.Type> object that represents the run-time type of the variable.</span></span> <span data-ttu-id="bd0e2-227"><xref:System.Type>Obiekt zawiera rozbudowane informacje we właściwościach i metodach.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-227">The <xref:System.Type> object holds extensive information in its properties and methods.</span></span>
- <span data-ttu-id="bd0e2-228">Możesz przekazać zmienną do <xref:Microsoft.VisualBasic.Information.TypeName%2A> funkcji, aby uzyskać `String` nazwę typu czasu wykonywania.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-228">You can pass the variable to the <xref:Microsoft.VisualBasic.Information.TypeName%2A> function to get a `String` with the name of run-time type.</span></span>

<span data-ttu-id="bd0e2-229">Poniższy przykład wywołuje `GetType` metodę i `TypeName` funkcję, aby określić typ tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-229">The following example calls the both the `GetType` method and the `TypeName` function to determine the type of an array.</span></span> <span data-ttu-id="bd0e2-230">Typ tablicy to `Byte(,)` .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-230">The array type is `Byte(,)`.</span></span> <span data-ttu-id="bd0e2-231">Należy zauważyć, że <xref:System.Type.BaseType%2A?displayProperty=nameWithType> Właściwość wskazuje również, że typ podstawowy tablicy bajtowej jest <xref:System.Array> klasą.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-231">Note that the <xref:System.Type.BaseType%2A?displayProperty=nameWithType> property also indicates that the base type of the byte array is the <xref:System.Array> class.</span></span>

[!code-vb[array-type](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-type.vb)]

## <a name="arrays-as-return-values-and-parameters"></a><span data-ttu-id="bd0e2-232">Tablice jako wartości zwracane i parametry</span><span class="sxs-lookup"><span data-stu-id="bd0e2-232">Arrays as return values and parameters</span></span>

<span data-ttu-id="bd0e2-233">Aby zwrócić tablicę z `Function` procedury, określ typ danych Array i liczbę wymiarów jako zwracany typ [instrukcji Function](../../../language-reference/statements/function-statement.md).</span><span class="sxs-lookup"><span data-stu-id="bd0e2-233">To return an array from a `Function` procedure, specify the array data type and the number of dimensions as the return type of the [Function Statement](../../../language-reference/statements/function-statement.md).</span></span> <span data-ttu-id="bd0e2-234">W ramach funkcji deklaruj lokalną zmienną tablicową o tym samym typie danych i liczbie wymiarów.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-234">Within the function, declare a local array variable with same data type and number of dimensions.</span></span> <span data-ttu-id="bd0e2-235">W [instrukcji return](../../../language-reference/statements/return-statement.md)Uwzględnij lokalną zmienną tablicową bez nawiasów.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-235">In the [Return Statement](../../../language-reference/statements/return-statement.md), include the local array variable without parentheses.</span></span>

<span data-ttu-id="bd0e2-236">Aby określić tablicę jako parametr do `Sub` `Function` procedury lub, zdefiniuj parametr jako tablicę o określonym typie danych i liczbie wymiarów.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-236">To specify an array as a parameter to a `Sub` or `Function` procedure, define the parameter as an array with a specified data type and number of dimensions.</span></span> <span data-ttu-id="bd0e2-237">W wywołaniu procedury należy przekazać zmienną tablicową o tym samym typie danych i liczbie wymiarów.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-237">In the call to the procedure, pass an array variable with the same data type and number of dimensions.</span></span>

<span data-ttu-id="bd0e2-238">W poniższym przykładzie `GetNumbers` Funkcja zwraca `Integer()` tablicę jednowymiarową typu `Integer` .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-238">In the following example, the `GetNumbers` function returns an `Integer()`, a one-dimensional array of type `Integer`.</span></span> <span data-ttu-id="bd0e2-239">`ShowNumbers`Procedura akceptuje `Integer()` argument.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-239">The `ShowNumbers` procedure accepts an `Integer()` argument.</span></span>

[!code-vb[return-value-and-params](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/return-values-and-params.vb)]

<span data-ttu-id="bd0e2-240">W poniższym przykładzie `GetNumbersMultiDim` Funkcja zwraca `Integer(,)` tablicę dwuwymiarową typu `Integer` .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-240">In the following example, the `GetNumbersMultiDim` function returns an `Integer(,)`, a two-dimensional array of type `Integer`.</span></span>  <span data-ttu-id="bd0e2-241">`ShowNumbersMultiDim`Procedura akceptuje `Integer(,)` argument.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-241">The `ShowNumbersMultiDim` procedure accepts an `Integer(,)` argument.</span></span>

[!code-vb[multidimensional-return-value](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/return-values-and-params-2d.vb)]

## <a name="jagged-arrays"></a><span data-ttu-id="bd0e2-242">Tablice nieregularne</span><span class="sxs-lookup"><span data-stu-id="bd0e2-242">Jagged arrays</span></span>

<span data-ttu-id="bd0e2-243">Czasami struktura danych w aplikacji jest dwuwymiarowa, ale nie prostokątna.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-243">Sometimes the data structure in your application is two-dimensional but not rectangular.</span></span> <span data-ttu-id="bd0e2-244">Na przykład można użyć tablicy do przechowywania danych o wysokiej temperatury każdego dnia miesiąca.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-244">For example, you might use an array to store data about the high temperature of each day of the month.</span></span> <span data-ttu-id="bd0e2-245">Pierwszy wymiar tablicy reprezentuje miesiąc, ale drugi wymiar reprezentuje liczbę dni, a liczba dni w miesiącu nie jest jednolita.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-245">The first dimension of the array represents the month, but the second dimension represents the number of days, and the number of days in a month is not uniform.</span></span> <span data-ttu-id="bd0e2-246">*Tablica nieregularna*, nazywana również *tablicą*tablic, została zaprojektowana dla takich scenariuszy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-246">A *jagged array*, which is also called an *array of arrays*, is designed for such scenarios.</span></span> <span data-ttu-id="bd0e2-247">Tablica nieregularna jest tablicą, której elementy są również tablicami.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-247">A jagged array is an array whose elements are also arrays.</span></span> <span data-ttu-id="bd0e2-248">Tablica nieregularna i każdy element w tablicy nieregularnej może mieć co najmniej jeden wymiar.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-248">A jagged array and each element in a jagged array can have one or more dimensions.</span></span>

<span data-ttu-id="bd0e2-249">W poniższym przykładzie zastosowano tablicę miesięcy, każdy element jest tablicą dni.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-249">The following example uses an array of months, each element of which is an array of days.</span></span> <span data-ttu-id="bd0e2-250">W przykładzie zastosowano tablicę nieregularną, ponieważ różne miesiące mają różną liczbę dni.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-250">The example uses a jagged array because different months have different numbers of days.</span></span>  <span data-ttu-id="bd0e2-251">W przykładzie pokazano, jak utworzyć tablicę nieregularną, przypisać do niej wartości, a następnie pobrać i wyświetlić jej wartości.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-251">The example shows how to create a jagged array, assign values to it, and retrieve and display its values.</span></span>

[!code-vb[jagged-arrays](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged.vb)]

<span data-ttu-id="bd0e2-252">W poprzednim przykładzie wartości są przypisywane do tablicy nieregularnej przy użyciu `For...Next` pętli.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-252">The previous example assigns values to the jagged array on an element-by-element basis by using a `For...Next` loop.</span></span> <span data-ttu-id="bd0e2-253">Można również przypisać wartości do elementów tablicy nieregularnej przy użyciu zagnieżdżonych literałów tablicowych.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-253">You can also assign values to the elements of a jagged array by using nested array literals.</span></span> <span data-ttu-id="bd0e2-254">Jednak próba użycia zagnieżdżonych literałów tablicowych (na przykład) powoduje `Dim valuesjagged = {{1, 2}, {2, 3, 4}}` wygenerowanie błędu kompilatora [BC30568](../../../misc/bc30568.md).</span><span class="sxs-lookup"><span data-stu-id="bd0e2-254">However, the attempt to use nested array literals (for example, `Dim valuesjagged = {{1, 2}, {2, 3, 4}}`) generates compiler error [BC30568](../../../misc/bc30568.md).</span></span> <span data-ttu-id="bd0e2-255">Aby poprawić błąd, ujmij wewnętrzne literały tablicowe w nawiasy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-255">To correct the error, enclose the inner array literals in parentheses.</span></span> <span data-ttu-id="bd0e2-256">Nawiasy wymuszają obliczenie wyrażenia literału tablicy, a wyniki są używane z zewnętrznym literałem tablicy, jak pokazano w poniższym przykładzie.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-256">The parentheses force the array literal expression to be evaluated, and the resulting values are used with the outer array literal, as the following example shows.</span></span>

[!code-vb[jagged-array-initialization](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged-assign.vb)]

<span data-ttu-id="bd0e2-257">Tablica nieregularna jest tablicą jednowymiarową, której elementy zawierają tablice.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-257">A jagged array is a one-dimensional array whose elements contain arrays.</span></span> <span data-ttu-id="bd0e2-258">W związku z tym <xref:System.Array.Length%2A?displayProperty=nameWithType> Właściwość i `Array.GetLength(0)` Metoda zwracają liczbę elementów w tablicy jednowymiarowej i `Array.GetLength(1)` zgłasza, <xref:System.IndexOutOfRangeException> że nieregularna tablica nie jest wielowymiarowa.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-258">Therefore, the <xref:System.Array.Length%2A?displayProperty=nameWithType> property and the `Array.GetLength(0)` method return the number of elements in the one-dimensional array, and `Array.GetLength(1)` throws an <xref:System.IndexOutOfRangeException> because a jagged array is not multidimensional.</span></span> <span data-ttu-id="bd0e2-259">Należy określić liczbę elementów w każdej podtablicy, pobierając wartość każdej właściwości podtablicy <xref:System.Array.Length%2A?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-259">You determine the number of elements in each subarray by retrieving the value of each subarray's <xref:System.Array.Length%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="bd0e2-260">Poniższy przykład ilustruje, jak określić liczbę elementów w tablicy nieregularnej.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-260">The following example illustrates how to determine the number of elements in a jagged array.</span></span>

[!code-vb[jagged-array-size](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged-length.vb)]

## <a name="zero-length-arrays"></a><span data-ttu-id="bd0e2-261">Tablice o zerowej długości</span><span class="sxs-lookup"><span data-stu-id="bd0e2-261">Zero-length arrays</span></span>

<span data-ttu-id="bd0e2-262">Visual Basic odróżnia między niezainicjowaną tablicą (tablicę, której wartością `Nothing` ) i *tablicą o zerowej długości* lub tablicę pustą (tablicę, która nie ma elementów). Niezainicjowana tablica jest taka, która nie została zwymiarowana ani nie ma przypisanych żadnych wartości.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-262">Visual Basic differentiates between a uninitialized array (an array whose value is `Nothing`) and a *zero-length array* or empty array (an array that has no elements.) An uninitialized array is one that has not been dimensioned or had any values assigned to it.</span></span> <span data-ttu-id="bd0e2-263">Przykład:</span><span class="sxs-lookup"><span data-stu-id="bd0e2-263">For example:</span></span>

```vb
Dim arr() As String
```

<span data-ttu-id="bd0e2-264">Tablica o zerowej długości jest zadeklarowana z wymiarem-1.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-264">A zero-length array is declared with a dimension of -1.</span></span> <span data-ttu-id="bd0e2-265">Przykład:</span><span class="sxs-lookup"><span data-stu-id="bd0e2-265">For example:</span></span>

```vb
Dim arrZ(-1) As String
```

<span data-ttu-id="bd0e2-266">Może być konieczne utworzenie tablicy o zerowej długości w następujących okolicznościach:</span><span class="sxs-lookup"><span data-stu-id="bd0e2-266">You might need to create a zero-length array under the following circumstances:</span></span>

- <span data-ttu-id="bd0e2-267">Bez ryzyka <xref:System.NullReferenceException> wyjątku, kod musi uzyskać dostęp do elementów członkowskich <xref:System.Array> klasy, takich jak <xref:System.Array.Length%2A> lub <xref:System.Array.Rank%2A> , lub wywołać funkcję Visual Basic taką jak <xref:Microsoft.VisualBasic.Information.UBound%2A> .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-267">Without risking a <xref:System.NullReferenceException> exception, your code must access members of the <xref:System.Array> class, such as <xref:System.Array.Length%2A> or <xref:System.Array.Rank%2A>, or call a Visual Basic function such as <xref:Microsoft.VisualBasic.Information.UBound%2A>.</span></span>

- <span data-ttu-id="bd0e2-268">Chcesz zachować prosty kod, nie trzeba sprawdzać się `Nothing` jako przypadek specjalny.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-268">You want to keep your code simple by not having to check for `Nothing` as a special case.</span></span>

- <span data-ttu-id="bd0e2-269">Twój kod współdziała z interfejsem programowania aplikacji (API), który wymaga przekazania tablicy o zerowej długości do jednej lub kilku procedur lub zwraca tablicę o zerowej długości z jednej lub kilku procedur.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-269">Your code interacts with an application programming interface (API) that either requires you to pass a zero-length array to one or more procedures or returns a zero-length array from one or more procedures.</span></span>

## <a name="splitting-an-array"></a><span data-ttu-id="bd0e2-270">Dzielenie tablicy</span><span class="sxs-lookup"><span data-stu-id="bd0e2-270">Splitting an array</span></span>

<span data-ttu-id="bd0e2-271">W niektórych przypadkach może być konieczne wyodrębnienie pojedynczej tablicy do wielu tablic.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-271">In some cases, you may need to split a single array into multiple arrays.</span></span> <span data-ttu-id="bd0e2-272">Obejmuje to zidentyfikowanie punktu lub punktów, w których tablica ma zostać podzielona, a następnie Spitting tablicę do dwóch lub więcej oddzielnych tablic.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-272">This involves identifying the point or points at which the array is to be split, and then spitting the array into two or more separate arrays.</span></span>

> [!NOTE]
> <span data-ttu-id="bd0e2-273">W tej sekcji nie jest omawiany podział pojedynczego ciągu na tablicę ciągów na podstawie pewnego ogranicznika.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-273">This section does not discuss splitting a single string into a string array based on some delimiter.</span></span> <span data-ttu-id="bd0e2-274">Aby uzyskać informacje na temat dzielenia ciągu, zobacz <xref:System.String.Split%2A?displayProperty=nameWithType> metodę.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-274">For information on splitting a string, see the <xref:System.String.Split%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="bd0e2-275">Najczęstszymi kryteriami dzielenia tablicy są:</span><span class="sxs-lookup"><span data-stu-id="bd0e2-275">The most common criteria for splitting an array are:</span></span>

- <span data-ttu-id="bd0e2-276">Liczba elementów w tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-276">The number of elements in the array.</span></span> <span data-ttu-id="bd0e2-277">Na przykład możesz chcieć podzielić tablicę o więcej niż określoną liczbę elementów na liczbę w przybliżeniu równej części.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-277">For example, you might want to split an array of more than a specified number of elements into a number of approximately equal parts.</span></span> <span data-ttu-id="bd0e2-278">W tym celu można użyć wartości zwracanej przez <xref:System.Array.Length%2A?displayProperty=nameWithType> <xref:System.Array.GetLength%2A?displayProperty=nameWithType> metodę lub.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-278">For this purpose, you can use the value returned by either the <xref:System.Array.Length%2A?displayProperty=nameWithType> or <xref:System.Array.GetLength%2A?displayProperty=nameWithType> method.</span></span>

- <span data-ttu-id="bd0e2-279">Wartość elementu, który służy jako ogranicznik, który wskazuje, gdzie należy podzielić tablicę.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-279">The value of an element, which serves as a delimiter that indicates where the array should be split.</span></span> <span data-ttu-id="bd0e2-280">Konkretną wartość można wyszukać, wywołując <xref:System.Array.FindIndex%2A?displayProperty=nameWithType> metody i <xref:System.Array.FindLastIndex%2A?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="bd0e2-280">You can search for a specific value by calling the <xref:System.Array.FindIndex%2A?displayProperty=nameWithType> and <xref:System.Array.FindLastIndex%2A?displayProperty=nameWithType> methods.</span></span>

<span data-ttu-id="bd0e2-281">Po ustaleniu indeksu lub indeksów, w których Tablica powinna być podzielona, można utworzyć poszczególne tablice, wywołując <xref:System.Array.Copy%2A?displayProperty=nameWithType> metodę.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-281">Once you've determined the index or indexes at which the array should be split, you can then create the individual arrays by calling the <xref:System.Array.Copy%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="bd0e2-282">Poniższy przykład dzieli tablicę na dwie tablice o rozmiarze zbliżonym do równe.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-282">The following example splits an array into two arrays of approximately equal size.</span></span> <span data-ttu-id="bd0e2-283">(Jeśli łączna liczba elementów tablicy jest nieparzysta, pierwsza tablica ma jeden element niż drugi).</span><span class="sxs-lookup"><span data-stu-id="bd0e2-283">(If the total number of array elements is odd, the first array has one more element than the second.)</span></span>

[!code-vb[splitting-an-array-by-length](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/split1.vb)]

<span data-ttu-id="bd0e2-284">Poniższy przykład dzieli tablicę ciągów na dwie tablice na podstawie obecności elementu, którego wartością jest "ZZZ", który służy jako ogranicznik tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-284">The following example splits a string array into two arrays based on the presence of an element whose value is "zzz", which serves as the array delimiter.</span></span> <span data-ttu-id="bd0e2-285">Nowe tablice nie zawierają elementu zawierającego ogranicznik.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-285">The new arrays do not include the element that contains the delimiter.</span></span>

[!code-vb[splitting-an-array-by-delimiter](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/split2.vb)]

## <a name="joining-arrays"></a><span data-ttu-id="bd0e2-286">Sprzęganie tablic</span><span class="sxs-lookup"><span data-stu-id="bd0e2-286">Joining arrays</span></span>

<span data-ttu-id="bd0e2-287">Istnieje również możliwość łączenia wielu tablic w jedną większą tablicę.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-287">You can also combine a number of arrays into a single larger array.</span></span> <span data-ttu-id="bd0e2-288">W tym celu należy również użyć <xref:System.Array.Copy%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-288">To do this, you also use the <xref:System.Array.Copy%2A?displayProperty=nameWithType> method.</span></span>

> [!NOTE]
> <span data-ttu-id="bd0e2-289">Ta sekcja nie omawia sprzężenia tablicy ciągów w jeden ciąg.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-289">This section does not discuss joining a string array into a single string.</span></span> <span data-ttu-id="bd0e2-290">Aby uzyskać informacje na temat dołączania tablicy ciągów, zobacz <xref:System.String.Join%2A?displayProperty=nameWithType> metodę.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-290">For information on joining a string array, see the <xref:System.String.Join%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="bd0e2-291">Przed skopiowaniem elementów każdej tablicy do nowej tablicy, najpierw należy się upewnić, że zainicjowano tablicę, tak aby była wystarczająco duża, aby pomieścić nową tablicę.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-291">Before copying the elements of each array into the new array, you must first ensure that you have initialized the array so that it is large enough to accommodate the new array.</span></span> <span data-ttu-id="bd0e2-292">Można to zrobić na jeden z dwóch sposobów:</span><span class="sxs-lookup"><span data-stu-id="bd0e2-292">You can do this in one of two ways:</span></span>

- <span data-ttu-id="bd0e2-293">Użyj [`ReDim Preserve`](../../../language-reference/statements/redim-statement.md) instrukcji, aby dynamicznie rozwijać tablicę przed dodaniem do niej nowych elementów.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-293">Use the [`ReDim Preserve`](../../../language-reference/statements/redim-statement.md) statement to dynamically expand the array before adding new elements to it.</span></span> <span data-ttu-id="bd0e2-294">Jest to najprostsza technika, ale może to spowodować spadek wydajności i nadmierne użycie pamięci podczas kopiowania dużych tablic.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-294">This is the easiest technique, but it can result in performance degradation and excessive memory consumption when you are copying large arrays.</span></span>
- <span data-ttu-id="bd0e2-295">Oblicz łączną liczbę elementów wymaganą przez nową dużą tablicę, a następnie Dodaj do niej elementy każdej tablicy źródłowej.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-295">Calculate the total number of elements needed for the new large array, then add the elements of each source array to it.</span></span>

<span data-ttu-id="bd0e2-296">Poniższy przykład używa drugiego podejścia do dodawania czterech tablic zawierających dziesięć elementów każdego do jednej tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-296">The following example uses the second approach to add four arrays with ten elements each to a single array.</span></span>

[!code-vb[joining-an-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/join.vb)]

<span data-ttu-id="bd0e2-297">Ponieważ w tym przypadku tablice źródłowe są bardzo małe, możemy również dynamicznie rozwijać tablicę, dodając do niej elementy każdej nowej tablicy.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-297">Since in this case the source arrays are all small, we can also dynamically expand the array as we add the elements of each new array to it.</span></span> <span data-ttu-id="bd0e2-298">W poniższym przykładzie jest to.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-298">The following example does that.</span></span>

[!code-vb[joining-an-array-dynamically](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/join2.vb)]

## <a name="collections-as-an-alternative-to-arrays"></a><span data-ttu-id="bd0e2-299">Kolekcje jako alternatywę dla tablic</span><span class="sxs-lookup"><span data-stu-id="bd0e2-299">Collections as an alternative to arrays</span></span>

<span data-ttu-id="bd0e2-300">Tablice są najbardziej przydatne do tworzenia i pracy ze stałą liczbą obiektów silnie wpisanych.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-300">Arrays are most useful for creating and working with a fixed number of strongly typed objects.</span></span> <span data-ttu-id="bd0e2-301">Kolekcje zapewniają bardziej elastyczny sposób pracy z grupami obiektów.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-301">Collections provide a more flexible way to work with groups of objects.</span></span> <span data-ttu-id="bd0e2-302">W przeciwieństwie do tablic, które wymagają jawnie zmiany rozmiaru tablicy z [ `ReDim` instrukcją](../../../language-reference/statements/redim-statement.md), kolekcje są zwiększane i zmniejszane dynamicznie w miarę potrzeby zmiany aplikacji.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-302">Unlike arrays, which require that you explicitly change the size of an array with the [`ReDim` Statement](../../../language-reference/statements/redim-statement.md), collections grow and shrink dynamically as the needs of an application change.</span></span>

<span data-ttu-id="bd0e2-303">W przypadku użycia `ReDim` do przewymiarowania tablicy Visual Basic tworzy nową tablicę i zwalnia poprzednią.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-303">When you use `ReDim` to redimension an array, Visual Basic creates a new array and releases the previous one.</span></span> <span data-ttu-id="bd0e2-304">Trwa to czas wykonywania.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-304">This takes execution time.</span></span> <span data-ttu-id="bd0e2-305">W związku z tym, jeśli liczba elementów, które często pracujesz ze zmianami lub nie można przewidzieć maksymalnej wymaganej liczby elementów, zwykle uzyskuje się lepszą wydajność przy użyciu kolekcji.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-305">Therefore, if the number of items you are working with changes frequently, or you cannot predict the maximum number of items you need, you'll usually obtain better performance by using a collection.</span></span>

<span data-ttu-id="bd0e2-306">W przypadku niektórych kolekcji można przypisać klucz do dowolnego obiektu, który został umieszczony w kolekcji, dzięki czemu można szybko pobrać obiekt przy użyciu klucza.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-306">For some collections, you can assign a key to any object that you put into the collection so that you can quickly retrieve the object by using the key.</span></span>

<span data-ttu-id="bd0e2-307">Jeśli kolekcja zawiera elementy tylko jednego typu danych, można użyć jednej z klas w <xref:System.Collections.Generic?displayProperty=nameWithType> przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-307">If your collection contains elements of only one data type, you can use one of the classes in the <xref:System.Collections.Generic?displayProperty=nameWithType> namespace.</span></span> <span data-ttu-id="bd0e2-308">Ogólna kolekcja wymusza bezpieczeństwo typu, tak aby nie można było dodać do niego żadnego innego typu danych.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-308">A generic collection enforces type safety so that no other data type can be added to it.</span></span>

<span data-ttu-id="bd0e2-309">Aby uzyskać więcej informacji na temat kolekcji, zobacz [kolekcje](../../concepts/collections.md).</span><span class="sxs-lookup"><span data-stu-id="bd0e2-309">For more information about collections, see [Collections](../../concepts/collections.md).</span></span>

## <a name="related-topics"></a><span data-ttu-id="bd0e2-310">Powiązane tematy</span><span class="sxs-lookup"><span data-stu-id="bd0e2-310">Related topics</span></span>

|<span data-ttu-id="bd0e2-311">Termin</span><span class="sxs-lookup"><span data-stu-id="bd0e2-311">Term</span></span>|<span data-ttu-id="bd0e2-312">Definicja</span><span class="sxs-lookup"><span data-stu-id="bd0e2-312">Definition</span></span>|
|----------|----------------|
|[<span data-ttu-id="bd0e2-313">Wymiary tablic w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="bd0e2-313">Array Dimensions in Visual Basic</span></span>](array-dimensions.md)|<span data-ttu-id="bd0e2-314">Objaśnia rangę i wymiary w tablicach.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-314">Explains rank and dimensions in arrays.</span></span>|
|[<span data-ttu-id="bd0e2-315">Porady: inicjowanie zmiennej tablicy w języku Visual Basic</span><span class="sxs-lookup"><span data-stu-id="bd0e2-315">How to: Initialize an Array Variable in Visual Basic</span></span>](how-to-initialize-an-array-variable.md)|<span data-ttu-id="bd0e2-316">Opisuje sposób wypełniania tablic wartościami początkowymi.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-316">Describes how to populate arrays with initial values.</span></span>|
|[<span data-ttu-id="bd0e2-317">Porady: sortowanie tablicy w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="bd0e2-317">How to: Sort An Array in Visual Basic</span></span>](how-to-sort-an-array.md)|<span data-ttu-id="bd0e2-318">Pokazuje, jak sortować elementy tablicy alfabetycznie.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-318">Shows how to sort the elements of an array alphabetically.</span></span>|
|[<span data-ttu-id="bd0e2-319">Instrukcje: przypisywanie tablicy do innej tablicy</span><span class="sxs-lookup"><span data-stu-id="bd0e2-319">How to: Assign One Array to Another Array</span></span>](how-to-assign-one-array-to-another-array.md)|<span data-ttu-id="bd0e2-320">Opisuje reguły i kroki umożliwiające przypisanie tablicy do innej zmiennej tablicowej.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-320">Describes the rules and steps for assigning an array to another array variable.</span></span>|
|[<span data-ttu-id="bd0e2-321">Rozwiązywanie problemów związanych z tablicami</span><span class="sxs-lookup"><span data-stu-id="bd0e2-321">Troubleshooting Arrays</span></span>](troubleshooting-arrays.md)|<span data-ttu-id="bd0e2-322">W tym artykule omówiono niektóre typowe problemy, które powstają podczas pracy z tablicami.</span><span class="sxs-lookup"><span data-stu-id="bd0e2-322">Discusses some common problems that arise when working with arrays.</span></span>|

## <a name="see-also"></a><span data-ttu-id="bd0e2-323">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="bd0e2-323">See also</span></span>

- <xref:System.Array?displayProperty=nameWithType>
- [<span data-ttu-id="bd0e2-324">Dim, instrukcja</span><span class="sxs-lookup"><span data-stu-id="bd0e2-324">Dim Statement</span></span>](../../../language-reference/statements/dim-statement.md)
- [<span data-ttu-id="bd0e2-325">ReDim, instrukcja</span><span class="sxs-lookup"><span data-stu-id="bd0e2-325">ReDim Statement</span></span>](../../../language-reference/statements/redim-statement.md)
