---
title: Kompilatora (poziom 3) ostrzeżenie CS1718
ms.date: 07/20/2015
f1_keywords:
- CS1718
helpviewer_keywords:
- CS1718
ms.assetid: 7c1c11fd-4f91-482d-b8f7-efe2a361634e
ms.openlocfilehash: f563e43ca131899900060a485696b0985746d46d
ms.sourcegitcommit: 2701302a99cafbe0d86d53d540eb0fa7e9b46b36
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/28/2019
ms.locfileid: "64584368"
---
# <a name="compiler-warning-level-3-cs1718"></a><span data-ttu-id="243a5-102">Kompilatora (poziom 3) ostrzeżenie CS1718</span><span class="sxs-lookup"><span data-stu-id="243a5-102">Compiler Warning (level 3) CS1718</span></span>
<span data-ttu-id="243a5-103">Wykonano porównanie z tą samą zmienną; Czy chodziło Ci chcesz porównać coś innego?</span><span class="sxs-lookup"><span data-stu-id="243a5-103">Comparison made to same variable; did you mean to compare something else?</span></span>  
  
 <span data-ttu-id="243a5-104">Jeśli przeznaczony do porównania z innego elementu, należy po prostu Popraw instrukcję.</span><span class="sxs-lookup"><span data-stu-id="243a5-104">If you meant to compare to something else, then you should simply correct the statement.</span></span>  
  
 <span data-ttu-id="243a5-105">Ale inną możliwością jest testowanie zostały wartość PRAWDA lub FAŁSZ i robi według instrukcji takich jak `if (a == a) (true)` lub `if (a < a) (false)`.</span><span class="sxs-lookup"><span data-stu-id="243a5-105">But another possibility is that you were testing for true or false, and were doing so by statements such as `if (a == a) (true)` or `if (a < a) (false)`.</span></span> <span data-ttu-id="243a5-106">Zaleca się, że po prostu `if (true)` lub `if (false)`.</span><span class="sxs-lookup"><span data-stu-id="243a5-106">It is better to simply say `if (true)` or `if (false)`.</span></span> <span data-ttu-id="243a5-107">Istnieją dwa powody takiego:</span><span class="sxs-lookup"><span data-stu-id="243a5-107">There are two reasons for this:</span></span>  
  
- <span data-ttu-id="243a5-108">Jest łatwiejsze: jest zawsze przejrzyste wystarczy powiedzieć, co oznacza.</span><span class="sxs-lookup"><span data-stu-id="243a5-108">It is simpler: it is always clearer to simply say what you mean.</span></span>  
  
- <span data-ttu-id="243a5-109">Pomaga uniknąć nieporozumień: Nowa funkcja C# w wersji 2.0 to typy wartości null, które są analogiczne do wartości `null` języka T-SQL, język programowania używany przez program SQL Server.</span><span class="sxs-lookup"><span data-stu-id="243a5-109">It helps avoid confusion: a new feature of C# 2.0 is nullable value types, which are analogous to the value `null` in T-SQL, the programming language used by SQL Server.</span></span> <span data-ttu-id="243a5-110">Deweloperzy znajomości języka T-SQL może być zajmującym się ochroną skutki typy dopuszczające wartości null dla wyrażenia takie jak `if (a == a)`, z powodu użycia trójargumentowy logiki języka T-SQL.</span><span class="sxs-lookup"><span data-stu-id="243a5-110">Developers familiar with T-SQL might be concerned about the effect of nullable types on expressions such as `if (a == a)`, because of the use of ternary logic in T-SQL.</span></span> <span data-ttu-id="243a5-111">Jeśli używasz `true` lub `false`, można uniknąć tego potencjalne problemy.</span><span class="sxs-lookup"><span data-stu-id="243a5-111">If you use `true` or `false`, you avoid this possible confusion.</span></span>  
  
## <a name="example"></a><span data-ttu-id="243a5-112">Przykład</span><span class="sxs-lookup"><span data-stu-id="243a5-112">Example</span></span>  
 <span data-ttu-id="243a5-113">Poniższy kod generuje ostrzeżenie CS1718.</span><span class="sxs-lookup"><span data-stu-id="243a5-113">The following code generates warning CS1718.</span></span>  
  
```csharp  
// CS1718.cs  
using System;  
public class Tester   
{  
    public static void Main()   
    {   
        int i = 0;  
        if (i == i) { // CS1718.cs  
        //if (true) {   
            i++;  
        }  
    }  
}  
```
