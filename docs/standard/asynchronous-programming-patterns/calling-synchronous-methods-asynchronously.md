---
title: Wywołanie metod synchronicznych w sposób asynchroniczny
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
- cpp
helpviewer_keywords:
- asynchronous programming, delegates
- asynchronous delegates
- AsyncWaitHandle property
- callback methods
- calling synchronous methods in asynchronous manner
- WaitHandle class, code examples
- asynchronous programming, status polling
- polling asynchronous operation status
- delegates [.NET Framework], asynchronous
- synchronous calling in asynchronous manner
- waiting for asynchronous calls
- status information [.NET Framework], asynchronous operations
ms.assetid: 41972034-92ed-450a-9664-ab93fcc6f1fb
ms.openlocfilehash: 06df584f0120fbd4978e18647854a3ee844a2095
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/15/2020
ms.locfileid: "73105129"
---
# <a name="calling-synchronous-methods-asynchronously"></a><span data-ttu-id="a246b-102">Wywołanie metod synchronicznych w sposób asynchroniczny</span><span class="sxs-lookup"><span data-stu-id="a246b-102">Calling Synchronous Methods Asynchronously</span></span>

<span data-ttu-id="a246b-103">Program .NET Framework umożliwia wywołanie dowolnej metody asynchronicznie.</span><span class="sxs-lookup"><span data-stu-id="a246b-103">The .NET Framework enables you to call any method asynchronously.</span></span> <span data-ttu-id="a246b-104">W tym celu należy zdefiniować pełnomocnika z tym samym podpisem co metoda, którą chcesz wywołać; program runtime języka wspólnego `BeginInvoke` automatycznie `EndInvoke` definiuje i metody dla tego delegata, z odpowiednimi podpisami.</span><span class="sxs-lookup"><span data-stu-id="a246b-104">To do this you define a delegate with the same signature as the method you want to call; the common language runtime automatically defines `BeginInvoke` and `EndInvoke` methods for this delegate, with the appropriate signatures.</span></span>

> [!NOTE]
> <span data-ttu-id="a246b-105">Asynchroniczne wywołania delegata, `BeginInvoke` `EndInvoke` w szczególności i metody, nie są obsługiwane w .NET Compact Framework.</span><span class="sxs-lookup"><span data-stu-id="a246b-105">Asynchronous delegate calls, specifically the `BeginInvoke` and `EndInvoke` methods, are not supported in the .NET Compact Framework.</span></span>

<span data-ttu-id="a246b-106">Metoda `BeginInvoke` inicjuje wywołanie asynchroniczne.</span><span class="sxs-lookup"><span data-stu-id="a246b-106">The `BeginInvoke` method initiates the asynchronous call.</span></span> <span data-ttu-id="a246b-107">Ma te same parametry co metoda, którą chcesz wykonać asynchronicznie, plus dwa dodatkowe parametry opcjonalne.</span><span class="sxs-lookup"><span data-stu-id="a246b-107">It has the same parameters as the method that you want to execute asynchronously, plus two additional optional parameters.</span></span> <span data-ttu-id="a246b-108">Pierwszy parametr jest <xref:System.AsyncCallback> pełnomocnikiem, który odwołuje się do metody, która ma być wywoływana po zakończeniu wywołania asynchronicznego.</span><span class="sxs-lookup"><span data-stu-id="a246b-108">The first parameter is an <xref:System.AsyncCallback> delegate that references a method to be called when the asynchronous call completes.</span></span> <span data-ttu-id="a246b-109">Drugi parametr jest obiektem zdefiniowanym przez użytkownika, który przekazuje informacje do metody wywołania wywołania.</span><span class="sxs-lookup"><span data-stu-id="a246b-109">The second parameter is a user-defined object that passes information into the callback method.</span></span> <span data-ttu-id="a246b-110">`BeginInvoke`zwraca natychmiast i nie czeka na zakończenie wywołania asynchronicznego.</span><span class="sxs-lookup"><span data-stu-id="a246b-110">`BeginInvoke` returns immediately and does not wait for the asynchronous call to complete.</span></span> <span data-ttu-id="a246b-111">`BeginInvoke`zwraca <xref:System.IAsyncResult>, który może służyć do monitorowania postępu wywołania asynchronicznego.</span><span class="sxs-lookup"><span data-stu-id="a246b-111">`BeginInvoke` returns an <xref:System.IAsyncResult>, which can be used to monitor the progress of the asynchronous call.</span></span>

<span data-ttu-id="a246b-112">Metoda `EndInvoke` pobiera wyniki wywołania asynchronicznego.</span><span class="sxs-lookup"><span data-stu-id="a246b-112">The `EndInvoke` method retrieves the results of the asynchronous call.</span></span> <span data-ttu-id="a246b-113">Można go nazwać w `BeginInvoke`dowolnym momencie po .</span><span class="sxs-lookup"><span data-stu-id="a246b-113">It can be called any time after `BeginInvoke`.</span></span> <span data-ttu-id="a246b-114">Jeśli wywołanie asynchroniczne nie `EndInvoke` zostało zakończone, blokuje wątek wywołujący, dopóki nie zostanie ukończony.</span><span class="sxs-lookup"><span data-stu-id="a246b-114">If the asynchronous call has not completed, `EndInvoke` blocks the calling thread until it completes.</span></span> <span data-ttu-id="a246b-115">`EndInvoke` Parametry include `out` i `ref` parametry (i`<Out>` `ByRef` `ByRef` w języku Visual Basic) metody, która ma być wykonywana asynchronicznie, plus <xref:System.IAsyncResult> zwrócone przez `BeginInvoke`.</span><span class="sxs-lookup"><span data-stu-id="a246b-115">The parameters of `EndInvoke` include the `out` and `ref` parameters (`<Out>` `ByRef` and `ByRef` in Visual Basic) of the method that you want to execute asynchronously, plus the <xref:System.IAsyncResult> returned by `BeginInvoke`.</span></span>

> [!NOTE]
> <span data-ttu-id="a246b-116">Funkcja IntelliSense w programie Visual Studio `BeginInvoke` wyświetla `EndInvoke`parametry i .</span><span class="sxs-lookup"><span data-stu-id="a246b-116">The IntelliSense feature in Visual Studio displays the parameters of `BeginInvoke` and `EndInvoke`.</span></span> <span data-ttu-id="a246b-117">Jeśli nie używasz programu Visual Studio lub podobnego narzędzia lub używasz języka C# w programie Visual Studio, zobacz [Asynchroniczny model programowania (APM),](../../../docs/standard/asynchronous-programming-patterns/asynchronous-programming-model-apm.md) aby uzyskać opis parametrów zdefiniowanych dla tych metod.</span><span class="sxs-lookup"><span data-stu-id="a246b-117">If you're not using Visual Studio or a similar tool, or if you're using C# with Visual Studio, see [Asynchronous Programming Model (APM)](../../../docs/standard/asynchronous-programming-patterns/asynchronous-programming-model-apm.md) for a description of the parameters defined for these methods.</span></span>

<span data-ttu-id="a246b-118">Przykłady kodu w tym temacie demonstrować `BeginInvoke` `EndInvoke` cztery typowe sposoby używania i do wykonania wywołań asynchronicznych.</span><span class="sxs-lookup"><span data-stu-id="a246b-118">The code examples in this topic demonstrate four common ways to use `BeginInvoke` and `EndInvoke` to make asynchronous calls.</span></span> <span data-ttu-id="a246b-119">Po `BeginInvoke` nadzwonieniu można wykonać następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="a246b-119">After calling `BeginInvoke` you can do the following:</span></span>

- <span data-ttu-id="a246b-120">Wykonaj pracę, a `EndInvoke` następnie wywołaj, aby zablokować, dopóki połączenie nie zostanie zakończone.</span><span class="sxs-lookup"><span data-stu-id="a246b-120">Do some work and then call `EndInvoke` to block until the call completes.</span></span>

- <span data-ttu-id="a246b-121">Uzyskaj <xref:System.Threading.WaitHandle> przy <xref:System.IAsyncResult.AsyncWaitHandle%2A?displayProperty=nameWithType> użyciu właściwości, <xref:System.Threading.WaitHandle.WaitOne%2A> użyj jego metody, aby zablokować wykonanie, dopóki <xref:System.Threading.WaitHandle> nie jest sygnalizowane, a następnie wywołać `EndInvoke`.</span><span class="sxs-lookup"><span data-stu-id="a246b-121">Obtain a <xref:System.Threading.WaitHandle> using the <xref:System.IAsyncResult.AsyncWaitHandle%2A?displayProperty=nameWithType> property, use its <xref:System.Threading.WaitHandle.WaitOne%2A> method to block execution until the <xref:System.Threading.WaitHandle> is signaled, and then call `EndInvoke`.</span></span>

- <span data-ttu-id="a246b-122">Sonduj zwrócone <xref:System.IAsyncResult> przez, `BeginInvoke` aby określić, kiedy wywołanie asynchroniczne zostało zakończone, a następnie wywołać `EndInvoke`.</span><span class="sxs-lookup"><span data-stu-id="a246b-122">Poll the <xref:System.IAsyncResult> returned by `BeginInvoke` to determine when the asynchronous call has completed, and then call `EndInvoke`.</span></span>

- <span data-ttu-id="a246b-123">Przekaż pełnomocnika dla metody wywołania wywołania `BeginInvoke`do .</span><span class="sxs-lookup"><span data-stu-id="a246b-123">Pass a delegate for a callback method to `BeginInvoke`.</span></span> <span data-ttu-id="a246b-124">Metoda jest wykonywana <xref:System.Threading.ThreadPool> w wątku po zakończeniu wywołania asynchronicznego.</span><span class="sxs-lookup"><span data-stu-id="a246b-124">The method is executed on a <xref:System.Threading.ThreadPool> thread when the asynchronous call completes.</span></span> <span data-ttu-id="a246b-125">Wywołuje metodę wywołania wywołania wywołania . `EndInvoke`</span><span class="sxs-lookup"><span data-stu-id="a246b-125">The callback method calls `EndInvoke`.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="a246b-126">Bez względu na to, `EndInvoke` której techniki używasz, zawsze wywołaj, aby zakończyć wywołanie asynchroniczne.</span><span class="sxs-lookup"><span data-stu-id="a246b-126">No matter which technique you use, always call `EndInvoke` to complete your asynchronous call.</span></span>

## <a name="defining-the-test-method-and-asynchronous-delegate"></a><span data-ttu-id="a246b-127">Definiowanie metody testowej i delegata asynchronicznego</span><span class="sxs-lookup"><span data-stu-id="a246b-127">Defining the Test Method and Asynchronous Delegate</span></span>
 <span data-ttu-id="a246b-128">Przykłady kodu, które należy wykazać różne sposoby wywoływania `TestMethod`tej samej metody długotrwałego, asynchronicznie.</span><span class="sxs-lookup"><span data-stu-id="a246b-128">The code examples that follow demonstrate various ways of calling the same long-running method, `TestMethod`, asynchronously.</span></span> <span data-ttu-id="a246b-129">Metoda `TestMethod` wyświetla komunikat konsoli, aby pokazać, że rozpoczął przetwarzanie, uśpiony przez kilka sekund, a następnie kończy.</span><span class="sxs-lookup"><span data-stu-id="a246b-129">The `TestMethod` method displays a console message to show that it has begun processing, sleeps for a few seconds, and then ends.</span></span> <span data-ttu-id="a246b-130">`TestMethod`ma `out` parametr, aby wykazać sposób, w jaki `BeginInvoke` takie `EndInvoke`parametry są dodawane do podpisów i .</span><span class="sxs-lookup"><span data-stu-id="a246b-130">`TestMethod` has an `out` parameter to demonstrate the way such parameters are added to the signatures of `BeginInvoke` and `EndInvoke`.</span></span> <span data-ttu-id="a246b-131">Parametry można `ref` obsługiwać podobnie.</span><span class="sxs-lookup"><span data-stu-id="a246b-131">You can handle `ref` parameters similarly.</span></span>

 <span data-ttu-id="a246b-132">Poniższy przykład kodu pokazuje `TestMethod` definicję i `AsyncMethodCaller` nazwę delegata, `TestMethod` która może służyć do wywoływania asynchronicznie.</span><span class="sxs-lookup"><span data-stu-id="a246b-132">The following code example shows the definition of `TestMethod` and the delegate named `AsyncMethodCaller` that can be used to call `TestMethod` asynchronously.</span></span> <span data-ttu-id="a246b-133">Aby skompilować przykłady kodu, należy dołączyć `TestMethod` definicje `AsyncMethodCaller` i delegata.</span><span class="sxs-lookup"><span data-stu-id="a246b-133">To compile the code examples, you must include the definitions for `TestMethod` and the `AsyncMethodCaller` delegate.</span></span>

 [!code-cpp[AsyncDelegateExamples#1](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/TestMethod.cpp#1)]
 [!code-csharp[AsyncDelegateExamples#1](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/TestMethod.cs#1)]
 [!code-vb[AsyncDelegateExamples#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/TestMethod.vb#1)]

## <a name="waiting-for-an-asynchronous-call-with-endinvoke"></a><span data-ttu-id="a246b-134">Oczekiwanie na wywołanie asynchroniczne z EndInvoke</span><span class="sxs-lookup"><span data-stu-id="a246b-134">Waiting for an Asynchronous Call with EndInvoke</span></span>
 <span data-ttu-id="a246b-135">Najprostszym sposobem wykonania metody asynchronicznie jest rozpoczęcie wykonywania metody przez `BeginInvoke` wywołanie metody delegata, wykonanie pracy nad głównym wątkiem, a następnie wywołanie metody delegata. `EndInvoke`</span><span class="sxs-lookup"><span data-stu-id="a246b-135">The simplest way to execute a method asynchronously is to start executing the method by calling the delegate's `BeginInvoke` method, do some work on the main thread, and then call the delegate's `EndInvoke` method.</span></span> <span data-ttu-id="a246b-136">`EndInvoke`może zablokować wątek wywołujący, ponieważ nie zwraca, dopóki nie zakończy wywołanie asynchroniczne.</span><span class="sxs-lookup"><span data-stu-id="a246b-136">`EndInvoke` might block the calling thread because it does not return until the asynchronous call completes.</span></span> <span data-ttu-id="a246b-137">Jest to dobra technika do użycia w operacjach plików lub sieci.</span><span class="sxs-lookup"><span data-stu-id="a246b-137">This is a good technique to use with file or network operations.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="a246b-138">Ponieważ `EndInvoke` może blokować, nigdy nie należy wywoływać go z wątków, które obsługują interfejs użytkownika.</span><span class="sxs-lookup"><span data-stu-id="a246b-138">Because `EndInvoke` might block, you should never call it from threads that service the user interface.</span></span>

 [!code-cpp[AsyncDelegateExamples#2](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/EndInvoke.cpp#2)]
 [!code-csharp[AsyncDelegateExamples#2](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/EndInvoke.cs#2)]
 [!code-vb[AsyncDelegateExamples#2](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/EndInvoke.vb#2)]

## <a name="waiting-for-an-asynchronous-call-with-waithandle"></a><span data-ttu-id="a246b-139">Oczekiwanie na wywołanie asynchroniczne z waithandle</span><span class="sxs-lookup"><span data-stu-id="a246b-139">Waiting for an Asynchronous Call with WaitHandle</span></span>
 <span data-ttu-id="a246b-140">Można uzyskać <xref:System.Threading.WaitHandle> za pomocą <xref:System.IAsyncResult.AsyncWaitHandle%2A> właściwości <xref:System.IAsyncResult> zwróconego `BeginInvoke`przez .</span><span class="sxs-lookup"><span data-stu-id="a246b-140">You can obtain a <xref:System.Threading.WaitHandle> by using the <xref:System.IAsyncResult.AsyncWaitHandle%2A> property of the <xref:System.IAsyncResult> returned by `BeginInvoke`.</span></span> <span data-ttu-id="a246b-141">Jest <xref:System.Threading.WaitHandle> sygnalizowany po zakończeniu wywołania asynchronicznego i można na <xref:System.Threading.WaitHandle.WaitOne%2A> nie poczekać, wywołując metodę.</span><span class="sxs-lookup"><span data-stu-id="a246b-141">The <xref:System.Threading.WaitHandle> is signaled when the asynchronous call completes, and you can wait for it by calling the <xref:System.Threading.WaitHandle.WaitOne%2A> method.</span></span>

 <span data-ttu-id="a246b-142">Jeśli używasz <xref:System.Threading.WaitHandle>, można wykonać dodatkowe przetwarzanie przed lub po zakończeniu wywołania `EndInvoke` asynchronicznego, ale przed wywołaniem w celu pobrania wyników.</span><span class="sxs-lookup"><span data-stu-id="a246b-142">If you use a <xref:System.Threading.WaitHandle>, you can perform additional processing before or after the asynchronous call completes, but before calling `EndInvoke` to retrieve the results.</span></span>

> [!NOTE]
> <span data-ttu-id="a246b-143">Uchwyt oczekiwania nie jest zamykany `EndInvoke`automatycznie po wywołaniu .</span><span class="sxs-lookup"><span data-stu-id="a246b-143">The wait handle is not closed automatically when you call `EndInvoke`.</span></span> <span data-ttu-id="a246b-144">Jeśli zwolnisz wszystkie odwołania do dojścia oczekiwania, zasoby systemowe są zwalniane, gdy wyrzucanie elementów bezużytecznych odzyskuje dojście oczekiwania.</span><span class="sxs-lookup"><span data-stu-id="a246b-144">If you release all references to the wait handle, system resources are freed when garbage collection reclaims the wait handle.</span></span> <span data-ttu-id="a246b-145">Aby zwolnić zasoby systemowe, gdy tylko skończysz przy użyciu <xref:System.Threading.WaitHandle.Close%2A?displayProperty=nameWithType> uchwytu oczekiwania, zlikwiduj go, wywołując metodę.</span><span class="sxs-lookup"><span data-stu-id="a246b-145">To free the system resources as soon as you are finished using the wait handle, dispose of it by calling the <xref:System.Threading.WaitHandle.Close%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="a246b-146">Wyrzucanie elementów bezużytecznych działa wydajniej, gdy jednorazowe obiekty są jawnie usuwane.</span><span class="sxs-lookup"><span data-stu-id="a246b-146">Garbage collection works more efficiently when disposable objects are explicitly disposed.</span></span>

 [!code-cpp[AsyncDelegateExamples#3](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/waithandle.cpp#3)]
 [!code-csharp[AsyncDelegateExamples#3](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/waithandle.cs#3)]
 [!code-vb[AsyncDelegateExamples#3](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/WaitHandle.vb#3)]

## <a name="polling-for-asynchronous-call-completion"></a><span data-ttu-id="a246b-147">Sondowanie asynchronicznego zakończenia wywołania</span><span class="sxs-lookup"><span data-stu-id="a246b-147">Polling for Asynchronous Call Completion</span></span>
 <span data-ttu-id="a246b-148">Można użyć <xref:System.IAsyncResult.IsCompleted%2A> właściwości zwrócone <xref:System.IAsyncResult> przez `BeginInvoke` odkryć po zakończeniu wywołania asynchronicznego.</span><span class="sxs-lookup"><span data-stu-id="a246b-148">You can use the <xref:System.IAsyncResult.IsCompleted%2A> property of the <xref:System.IAsyncResult> returned by `BeginInvoke` to discover when the asynchronous call completes.</span></span> <span data-ttu-id="a246b-149">Można to zrobić podczas wykonywania wywołania asynchronicznego z wątku, który służy interfejs użytkownika.</span><span class="sxs-lookup"><span data-stu-id="a246b-149">You might do this when making the asynchronous call from a thread that services the user interface.</span></span> <span data-ttu-id="a246b-150">Sondowanie do zakończenia umożliwia wątku wywołującego, aby kontynuować wykonywanie <xref:System.Threading.ThreadPool> podczas wykonywania asynchronicznego wywołania w wątku.</span><span class="sxs-lookup"><span data-stu-id="a246b-150">Polling for completion allows the calling thread to continue executing while the asynchronous call executes on a <xref:System.Threading.ThreadPool> thread.</span></span>

 [!code-cpp[AsyncDelegateExamples#4](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/polling.cpp#4)]
 [!code-csharp[AsyncDelegateExamples#4](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/polling.cs#4)]
 [!code-vb[AsyncDelegateExamples#4](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/polling.vb#4)]

## <a name="executing-a-callback-method-when-an-asynchronous-call-completes"></a><span data-ttu-id="a246b-151">Wykonywanie metody wywołania zwrotu po zakończeniu wywołania asynchronicznego</span><span class="sxs-lookup"><span data-stu-id="a246b-151">Executing a Callback Method When an Asynchronous Call Completes</span></span>
 <span data-ttu-id="a246b-152">Jeśli wątek, który inicjuje wywołanie asynchroniczne nie musi być wątek, który przetwarza wyniki, można wykonać metodę wywołania wywołania po zakończeniu wywołania.</span><span class="sxs-lookup"><span data-stu-id="a246b-152">If the thread that initiates the asynchronous call does not need to be the thread that processes the results, you can execute a callback method when the call completes.</span></span> <span data-ttu-id="a246b-153">Metoda wywołania odwróconego jest wykonywana w wątku. <xref:System.Threading.ThreadPool></span><span class="sxs-lookup"><span data-stu-id="a246b-153">The callback method is executed on a <xref:System.Threading.ThreadPool> thread.</span></span>

 <span data-ttu-id="a246b-154">Aby użyć metody wywołania wywołania, należy przekazać `BeginInvoke` pełnomocnika, <xref:System.AsyncCallback> który reprezentuje metodę wywołania wywołania.</span><span class="sxs-lookup"><span data-stu-id="a246b-154">To use a callback method, you must pass `BeginInvoke` an <xref:System.AsyncCallback> delegate that represents the callback method.</span></span> <span data-ttu-id="a246b-155">Można również przekazać obiekt, który zawiera informacje, które mają być używane przez metodę wywołania wywołania.</span><span class="sxs-lookup"><span data-stu-id="a246b-155">You can also pass an object that contains information to be used by the callback method.</span></span> <span data-ttu-id="a246b-156">W metodzie wywołania wywołania <xref:System.IAsyncResult>można rzutować , który jest jedynym <xref:System.Runtime.Remoting.Messaging.AsyncResult> parametrem metody wywołania wywołania, do obiektu.</span><span class="sxs-lookup"><span data-stu-id="a246b-156">In the callback method, you can cast the <xref:System.IAsyncResult>, which is the only parameter of the callback method, to an <xref:System.Runtime.Remoting.Messaging.AsyncResult> object.</span></span> <span data-ttu-id="a246b-157">Następnie można użyć <xref:System.Runtime.Remoting.Messaging.AsyncResult.AsyncDelegate%2A?displayProperty=nameWithType> właściwości, aby uzyskać pełnomocnika, który został użyty `EndInvoke`do zainicjowania wywołania, dzięki czemu można wywołać .</span><span class="sxs-lookup"><span data-stu-id="a246b-157">You can then use the <xref:System.Runtime.Remoting.Messaging.AsyncResult.AsyncDelegate%2A?displayProperty=nameWithType> property to get the delegate that was used to initiate the call so that you can call `EndInvoke`.</span></span>

 <span data-ttu-id="a246b-158">Uwagi dotyczące przykładu:</span><span class="sxs-lookup"><span data-stu-id="a246b-158">Notes on the example:</span></span>

- <span data-ttu-id="a246b-159">Parametr `threadId` `TestMethod` jest `out` parametrem ([`<Out>` `ByRef` w języku Visual Basic), `TestMethod`więc jego wartość wejściowa nigdy nie jest używana przez .</span><span class="sxs-lookup"><span data-stu-id="a246b-159">The `threadId` parameter of `TestMethod` is an `out` parameter ([`<Out>` `ByRef` in Visual Basic), so its input value is never used by `TestMethod`.</span></span> <span data-ttu-id="a246b-160">Zmienna fikcyjna jest `BeginInvoke` przekazywana do wywołania.</span><span class="sxs-lookup"><span data-stu-id="a246b-160">A dummy variable is passed to the `BeginInvoke` call.</span></span> <span data-ttu-id="a246b-161">Jeśli `threadId` parametr był `ref` parametrem`ByRef` (w języku Visual Basic), zmienna musiałaby być polem `BeginInvoke` `EndInvoke`na poziomie klasy, aby można było przekazać je obu i .</span><span class="sxs-lookup"><span data-stu-id="a246b-161">If the `threadId` parameter were a `ref` parameter (`ByRef` in Visual Basic), the variable would have to be a class-level field so that it could be passed to both `BeginInvoke` and `EndInvoke`.</span></span>

- <span data-ttu-id="a246b-162">Informacje o stanie, `BeginInvoke` który jest przekazywany do jest ciąg formatu, który metoda wywołania wstecznego używa do formatowania komunikatu wyjściowego.</span><span class="sxs-lookup"><span data-stu-id="a246b-162">The state information that is passed to `BeginInvoke` is a format string, which the callback method uses to format an output message.</span></span> <span data-ttu-id="a246b-163">Ponieważ jest przekazywana <xref:System.Object>jako typ , informacje o stanie muszą być rzutować do jego właściwego typu, zanim będzie można go użyć.</span><span class="sxs-lookup"><span data-stu-id="a246b-163">Because it is passed as type <xref:System.Object>, the state information must be cast to its proper type before it can be used.</span></span>

- <span data-ttu-id="a246b-164">Wywołanie wsteczne jest <xref:System.Threading.ThreadPool> dokonywane w wątku.</span><span class="sxs-lookup"><span data-stu-id="a246b-164">The callback is made on a <xref:System.Threading.ThreadPool> thread.</span></span> <span data-ttu-id="a246b-165"><xref:System.Threading.ThreadPool>wątki są wątki w tle, które nie utrzymują aplikacji uruchomiony, jeśli główny wątek kończy się, więc główny wątek przykładu musi spać wystarczająco długo, aby wywołanie wstecz do końca.</span><span class="sxs-lookup"><span data-stu-id="a246b-165"><xref:System.Threading.ThreadPool> threads are background threads, which do not keep the application running if the main thread ends, so the main thread of the example has to sleep long enough for the callback to finish.</span></span>

 [!code-cpp[AsyncDelegateExamples#5](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/callback.cpp#5)]
 [!code-csharp[AsyncDelegateExamples#5](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/callback.cs#5)]
 [!code-vb[AsyncDelegateExamples#5](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/callback.vb#5)]

## <a name="see-also"></a><span data-ttu-id="a246b-166">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="a246b-166">See also</span></span>

- <xref:System.Delegate>
- [<span data-ttu-id="a246b-167">Asynchroniczny wzorzec oparty na zdarzeniach (EAP)</span><span class="sxs-lookup"><span data-stu-id="a246b-167">Event-based Asynchronous Pattern (EAP)</span></span>](../../../docs/standard/asynchronous-programming-patterns/event-based-asynchronous-pattern-eap.md)
