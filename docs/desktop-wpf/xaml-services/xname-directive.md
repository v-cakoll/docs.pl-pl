---
title: x:Name — dyrektywa
ms.date: 03/30/2017
f1_keywords:
- x:Name
- xName
- Name
helpviewer_keywords:
- x:Name attribute [XAML Services]
- XAML [XAML Services], x:Name attribute
- Name attribute in XAML [XAML Services]
ms.assetid: b7e61222-e8cf-48d2-acd0-6df3b7685d48
ms.openlocfilehash: 9f812a49a3217a563be1bd7f1d999b641c28463d
ms.sourcegitcommit: c2d9718996402993cf31541f11e95531bc68bad0
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 02/27/2020
ms.locfileid: "82071452"
---
# <a name="xname-directive"></a><span data-ttu-id="7b62d-102">x:Name — dyrektywa</span><span class="sxs-lookup"><span data-stu-id="7b62d-102">x:Name Directive</span></span>

<span data-ttu-id="7b62d-103">Jednoznacznie identyfikuje elementy zdefiniowane przez XAML w oskopie nazw XAML.</span><span class="sxs-lookup"><span data-stu-id="7b62d-103">Uniquely identifies XAML-defined elements in a XAML namescope.</span></span> <span data-ttu-id="7b62d-104">Namescopes XAML i ich modele unikatowości mogą być stosowane do obiektów wystąpienia, gdy struktury zapewniają interfejsy API lub implementują zachowania, które uzyskują dostęp do wykresu obiektów utworzonych przez XAML w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="7b62d-104">XAML namescopes and their uniqueness models can be applied to the instantiated objects, when frameworks provide APIs or implement behaviors that access the XAML-created object graph at run time.</span></span>

## <a name="xaml-attribute-usage"></a><span data-ttu-id="7b62d-105">Użycie atrybutu języka XAML</span><span class="sxs-lookup"><span data-stu-id="7b62d-105">XAML Attribute Usage</span></span>

```xaml
<object x:Name="XAMLNameValue".../>
```

## <a name="xaml-values"></a><span data-ttu-id="7b62d-106">Wartości XAML</span><span class="sxs-lookup"><span data-stu-id="7b62d-106">XAML Values</span></span>

|||
|-|-|
|`XAMLNameValue`|<span data-ttu-id="7b62d-107">Ciąg zgodny z ograniczeniami [gramatyki XamlName](xamlname-grammar.md).</span><span class="sxs-lookup"><span data-stu-id="7b62d-107">A string that conforms to the restrictions of the [XamlName Grammar](xamlname-grammar.md).</span></span>|

## <a name="remarks"></a><span data-ttu-id="7b62d-108">Uwagi</span><span class="sxs-lookup"><span data-stu-id="7b62d-108">Remarks</span></span>

<span data-ttu-id="7b62d-109">Po `x:Name` zastosowaniu do modelu programowania kopii struktury, nazwa jest odpowiednikiem zmiennej, która posiada odwołanie do obiektu lub wystąpienie zwrócone przez konstruktora.</span><span class="sxs-lookup"><span data-stu-id="7b62d-109">After `x:Name` is applied to a framework's backing programming model, the name is equivalent to the variable that holds an object reference or an instance as returned by a constructor.</span></span>

<span data-ttu-id="7b62d-110">Wartość użycia `x:Name` dyrektywy musi być unikatowa w ramach zakresu nazw XAML.</span><span class="sxs-lookup"><span data-stu-id="7b62d-110">The value of an `x:Name` directive usage must be unique within a XAML namescope.</span></span> <span data-ttu-id="7b62d-111">Domyślnie, gdy jest używany przez interfejs API usług XAML platformy .NET, podstawowy kod nazw XAML jest zdefiniowany w elemencie głównym XAML pojedynczej produkcji XAML i obejmuje elementy zawarte w tej produkcji XAML.</span><span class="sxs-lookup"><span data-stu-id="7b62d-111">By default when used by .NET XAML Services API, the primary XAML namescope is defined at the XAML root element of a single XAML production, and encompasses the elements that are contained in that XAML production.</span></span> <span data-ttu-id="7b62d-112">Dodatkowe dyskretne namescopes XAML, które mogą wystąpić w ramach jednej produkcji XAML mogą być zdefiniowane przez struktury do rozwiązania określonych scenariuszy.</span><span class="sxs-lookup"><span data-stu-id="7b62d-112">Additional discrete XAML namescopes that might occur within a single XAML production can be defined by frameworks to address specific scenarios.</span></span> <span data-ttu-id="7b62d-113">Na przykład w WPF nowe identyfikatory XAML są definiowane i tworzone przez dowolny szablon, który jest również zdefiniowany w tej produkcji XAML.</span><span class="sxs-lookup"><span data-stu-id="7b62d-113">For example, in WPF, new XAML namescopes are defined and created by any template that is also defined on that XAML production.</span></span> <span data-ttu-id="7b62d-114">Aby uzyskać więcej informacji na temat nazw XAML (napisanych dla WPF, ale istotnych dla wielu pojęć xaml namescope), zobacz [WPF XAML Namescopes](../../framework/wpf/advanced/wpf-xaml-namescopes.md).</span><span class="sxs-lookup"><span data-stu-id="7b62d-114">For more information about XAML namescopes (written for WPF but relevant for many XAML namescope concepts), see [WPF XAML Namescopes](../../framework/wpf/advanced/wpf-xaml-namescopes.md).</span></span>

<span data-ttu-id="7b62d-115">Ogólnie rzecz `x:Name` biorąc, nie powinny być `x:Key`stosowane w sytuacjach, które również używają .</span><span class="sxs-lookup"><span data-stu-id="7b62d-115">In general, `x:Name` should not be applied in situations that also use `x:Key`.</span></span> <span data-ttu-id="7b62d-116">Implementacje XAML przez konkretne istniejące struktury wprowadziły pojęcia podstawiania między `x:Key` i `x:Name`, ale nie jest to zalecana praktyka.</span><span class="sxs-lookup"><span data-stu-id="7b62d-116">XAML implementations by specific existing frameworks have introduced substitution concepts between `x:Key` and `x:Name`, but that is not a recommended practice.</span></span> <span data-ttu-id="7b62d-117">Usługi .NET XAML nie obsługują takich pojęć podstawienia <xref:System.Windows.Markup.INameScope> <xref:System.Windows.Markup.DictionaryKeyPropertyAttribute>podczas obsługi informacji o nazwie/kluczu, takich jak lub .</span><span class="sxs-lookup"><span data-stu-id="7b62d-117">.NET XAML Services does not support such substitution concepts when handling name/key information such as <xref:System.Windows.Markup.INameScope> or <xref:System.Windows.Markup.DictionaryKeyPropertyAttribute>.</span></span>

<span data-ttu-id="7b62d-118">Zasady `x:Name` zezwalania, jak również wymuszanie unikatowości nazwy są potencjalnie definiowane przez określone ramy wdrażania.</span><span class="sxs-lookup"><span data-stu-id="7b62d-118">Rules for permittance of `x:Name` as well as the name uniqueness enforcement are potentially defined by specific implementing frameworks.</span></span> <span data-ttu-id="7b62d-119">Jednak aby być użytecznym z usługami .NET XAML, definicje frameworkowe unikatowości nazw <xref:System.Windows.Markup.INameScope> XAML powinny być zgodne z definicją informacji w tej dokumentacji i powinny używać tych samych reguł dotyczących miejsca, w którym informacje są stosowane.</span><span class="sxs-lookup"><span data-stu-id="7b62d-119">However, to be usable with .NET XAML Services, the framework definitions of XAML namescope uniqueness should be consistent with the definition of <xref:System.Windows.Markup.INameScope> information in this documentation, and should use the same rules regarding where the information is applied.</span></span> <span data-ttu-id="7b62d-120">Na przykład [!INCLUDE[TLA#tla_winclient](../../../includes/tlasharptla-winclient-md.md)] implementacja dzieli różne elementy <xref:System.Windows.NameScope> znaczników na oddzielne zakresy, takie jak słowniki zasobów, drzewo logiczne utworzone przez xaml na poziomie strony, szablony i inną odroczoną zawartość, a następnie wymusza unikatowość nazwy XAML w ramach każdego z tych nazw XAML.</span><span class="sxs-lookup"><span data-stu-id="7b62d-120">For example, the [!INCLUDE[TLA#tla_winclient](../../../includes/tlasharptla-winclient-md.md)] implementation divides various markup elements into separate <xref:System.Windows.NameScope> ranges, such as resource dictionaries, the logical tree created by the page-level XAML, templates, and other deferred content, and then enforces XAML name uniqueness within each of those XAML namescopes.</span></span>

<span data-ttu-id="7b62d-121">W przypadku typów niestandardowych korzystających z modułów zapisu obiektów `x:Name` XAML usług .NET XAML można ustanowić lub zmienić właściwość mapową typu.</span><span class="sxs-lookup"><span data-stu-id="7b62d-121">For custom types that use .NET XAML Services XAML object writers, a property that maps to `x:Name` on a type can be established or changed.</span></span> <span data-ttu-id="7b62d-122">Definiujesz to zachowanie, odwołując się do <xref:System.Windows.Markup.RuntimeNamePropertyAttribute> nazwy właściwości do mapy z kodem definicji typu.</span><span class="sxs-lookup"><span data-stu-id="7b62d-122">You define this behavior by referencing the name of the property to map with the <xref:System.Windows.Markup.RuntimeNamePropertyAttribute> in the type definition code.</span></span>  <span data-ttu-id="7b62d-123"><xref:System.Windows.Markup.RuntimeNamePropertyAttribute>jest atrybutem na poziomie typu.</span><span class="sxs-lookup"><span data-stu-id="7b62d-123"><xref:System.Windows.Markup.RuntimeNamePropertyAttribute> is a type-level attribute.</span></span>

<span data-ttu-id="7b62d-124">Using.NET usług XAML logikę tworzenia kopii zapasowych dla obsługi nazw XAML można zdefiniować <xref:System.Windows.Markup.INameScope> w sposób neutralny dla struktury, implementując interfejs.</span><span class="sxs-lookup"><span data-stu-id="7b62d-124">Using.NET XAML Services, the backing logic for XAML namescope support can be defined in a framework-neutral way by implementing the <xref:System.Windows.Markup.INameScope> interface.</span></span>

## <a name="wpf-usage-notes"></a><span data-ttu-id="7b62d-125">Uwagi dotyczące użycia WPF</span><span class="sxs-lookup"><span data-stu-id="7b62d-125">WPF Usage Notes</span></span>

<span data-ttu-id="7b62d-126">W standardowej konfiguracji [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] kompilacji dla aplikacji, która używa XAML, klasy `x:Name` częściowe i związane z kodem, [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] określony staje się nazwą pola, które jest tworzone w kodzie źródłowym, gdy jest przetwarzany przez zadanie kompilacji znaczników, a to pole posiada odwołanie do obiektu.</span><span class="sxs-lookup"><span data-stu-id="7b62d-126">Under the standard build configuration for a [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] application that uses XAML, partial classes, and code-behind, the specified `x:Name` becomes the name of a field that is created in the underlying code when [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] is processed by a markup compilation build task, and that field holds a reference to the object.</span></span> <span data-ttu-id="7b62d-127">Domyślnie utworzone pole jest wewnętrzne.</span><span class="sxs-lookup"><span data-stu-id="7b62d-127">By default, the created field is internal.</span></span> <span data-ttu-id="7b62d-128">Dostęp do pól można zmienić, określając [atrybut x:FieldModifier](xfieldmodifier-directive.md).</span><span class="sxs-lookup"><span data-stu-id="7b62d-128">You can change field access by specifying the [x:FieldModifier attribute](xfieldmodifier-directive.md).</span></span> <span data-ttu-id="7b62d-129">W WPF I Silverlight sekwencji jest, że kompilacja znaczników definiuje i nazwy pola w klasie częściowej, ale wartość jest początkowo pusta.</span><span class="sxs-lookup"><span data-stu-id="7b62d-129">In WPF and Silverlight, the sequence is that the markup compile defines and names the field in a partial class, but the value is initially empty.</span></span> <span data-ttu-id="7b62d-130">Następnie wygenerowana metoda `InitializeComponent` o nazwie jest wywoływana z wewnątrz konstruktora klasy.</span><span class="sxs-lookup"><span data-stu-id="7b62d-130">Then, a generated method named `InitializeComponent` is called from within the class constructor.</span></span> <span data-ttu-id="7b62d-131">`InitializeComponent`składa `FindName` się z wywołań przy użyciu każdej z `x:Name` wartości, które istnieją w części klasy częściowej zdefiniowanej przez XAML jako ciągi wejściowe.</span><span class="sxs-lookup"><span data-stu-id="7b62d-131">`InitializeComponent` consists of `FindName` calls using each of the `x:Name` values that exist in the XAML-defined part of the partial class as input strings.</span></span> <span data-ttu-id="7b62d-132">Zwracane wartości są następnie przypisywane do odwołania do pola o podobnej nazwie, aby wypełnić wartości pól obiektami utworzonymi na podstawie analizy XAML.</span><span class="sxs-lookup"><span data-stu-id="7b62d-132">The return values are then assigned to the like-named field reference to fill the field values with objects that were created from XAML parsing.</span></span> <span data-ttu-id="7b62d-133">Wykonanie `InitializeComponent` umożliwia odwoływanie się do wykresu obiektu `x:Name` czasu wykonywania przy użyciu /field `FindName` name bezpośrednio, zamiast wywoływać jawnie w dowolnym momencie potrzebne odwołanie do obiektu zdefiniowanego przez XAML.</span><span class="sxs-lookup"><span data-stu-id="7b62d-133">The execution of `InitializeComponent` make it possible to reference the run time object graph using the `x:Name` / field name directly, rather than having to call `FindName` explicitly any time you need a reference to a XAML-defined object.</span></span>

<span data-ttu-id="7b62d-134">Dla aplikacji WPF, która używa obiektów docelowych języka Microsoft `Page` Visual Basic i zawiera pliki XAML `WithEvents` z akcji kompilacji, `x:Name`oddzielna `Handles` właściwość odwołania jest tworzony podczas kompilacji, która dodaje słowo kluczowe do wszystkich elementów, które mają , do obsługi składni dla delegatów obsługi zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="7b62d-134">For a WPF application that uses the Microsoft Visual Basic targets and includes XAML files with `Page` build action, a separate reference property is created during compilation that adds the `WithEvents` keyword to all elements that have an `x:Name`, to support `Handles` syntax for event handler delegates.</span></span> <span data-ttu-id="7b62d-135">Ta właściwość jest zawsze publiczna.</span><span class="sxs-lookup"><span data-stu-id="7b62d-135">This property is always public.</span></span> <span data-ttu-id="7b62d-136">Aby uzyskać więcej informacji, zobacz [Visual Basic i Obsługa zdarzeń WPF](../../framework/wpf/advanced/visual-basic-and-wpf-event-handling.md).</span><span class="sxs-lookup"><span data-stu-id="7b62d-136">For more information, see [Visual Basic and WPF Event Handling](../../framework/wpf/advanced/visual-basic-and-wpf-event-handling.md).</span></span>

<span data-ttu-id="7b62d-137">`x:Name`jest używany przez procesor WPF XAML do rejestrowania nazwy w xaml namescope w czasie ładowania, nawet w przypadkach, gdy strona nie jest znaczników kompilowane przez akcje kompilacji (na przykład luźne XAML słownika zasobów).</span><span class="sxs-lookup"><span data-stu-id="7b62d-137">`x:Name` is used by the WPF XAML processor to register a name into a XAML namescope at load time, even for cases where the page is not markup-compiled by build actions (for example, loose XAML of a resource dictionary).</span></span> <span data-ttu-id="7b62d-138">Jedną z przyczyn tego `x:Name` zachowania jest, <xref:System.Windows.Data.Binding.ElementName%2A> ponieważ jest potencjalnie potrzebne do wiązania.</span><span class="sxs-lookup"><span data-stu-id="7b62d-138">One reason for this behavior is because the `x:Name` is potentially needed for <xref:System.Windows.Data.Binding.ElementName%2A> binding.</span></span> <span data-ttu-id="7b62d-139">Aby uzyskać szczegółowe informacje, zobacz [Omówienie powiązania danych](../data/data-binding-overview.md).</span><span class="sxs-lookup"><span data-stu-id="7b62d-139">For details, see [Data Binding Overview](../data/data-binding-overview.md).</span></span>

<span data-ttu-id="7b62d-140">Jak wspomniano wcześniej, `x:Name` (lub) `Name`nie powinny być stosowane `x:Key`w sytuacjach, które również używają .</span><span class="sxs-lookup"><span data-stu-id="7b62d-140">As mentioned previously, `x:Name` (or `Name`) should not be applied in situations that also use `x:Key`.</span></span> <span data-ttu-id="7b62d-141">Ma [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] <xref:System.Windows.ResourceDictionary> specjalne zachowanie definiowania się jako xaml namescope, ale zwracanie <xref:System.Windows.Markup.INameScope> nie implementowane lub null wartości dla interfejsów API jako sposób wymuszania tego zachowania.</span><span class="sxs-lookup"><span data-stu-id="7b62d-141">The [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] <xref:System.Windows.ResourceDictionary> has a special behavior of defining itself as a XAML namescope but returning Not Implemented or null values for <xref:System.Windows.Markup.INameScope> APIs as a way to enforce this behavior.</span></span> <span data-ttu-id="7b62d-142">Jeśli parser WPF XAML `Name` `x:Name` napotka lub w <xref:System.Windows.ResourceDictionary>xaml zdefiniowane , nazwa nie jest dodawany do żadnych nazw XAML.</span><span class="sxs-lookup"><span data-stu-id="7b62d-142">If the WPF XAML parser encounters `Name` or `x:Name` in a XAML-defined <xref:System.Windows.ResourceDictionary>, the name is not added to any XAML namescope.</span></span> <span data-ttu-id="7b62d-143">Próba znalezienia tej nazwy z dowolnego kodu `FindName` nazw XAML i metody nie zwróci prawidłowe wyniki.</span><span class="sxs-lookup"><span data-stu-id="7b62d-143">Attempting to find that name from any XAML namescope and the `FindName` methods will not return valid results.</span></span>

### <a name="xname-and-name"></a><span data-ttu-id="7b62d-144">x:Nazwa i nazwa</span><span class="sxs-lookup"><span data-stu-id="7b62d-144">x:Name and Name</span></span>

<span data-ttu-id="7b62d-145">Wiele scenariuszy aplikacji WPF można `x:Name` uniknąć użycia atrybutu, ponieważ właściwość `Name` zależności, jak określono w domyślnej przestrzeni <xref:System.Windows.FrameworkElement> nazw <xref:System.Windows.FrameworkContentElement> XAML dla kilku ważnych klas podstawowych, takich jak i spełnia ten sam cel.</span><span class="sxs-lookup"><span data-stu-id="7b62d-145">Many WPF application scenarios can avoid any use of the `x:Name` attribute, because the `Name` dependency property as specified in the default XAML namespace for several of the important base classes such as <xref:System.Windows.FrameworkElement> and <xref:System.Windows.FrameworkContentElement> satisfies this same purpose.</span></span> <span data-ttu-id="7b62d-146">Nadal istnieją niektóre typowe scenariusze XAML i WPF, `Name` gdzie dostęp do kodu do elementu bez właściwości na poziomie struktury jest ważne.</span><span class="sxs-lookup"><span data-stu-id="7b62d-146">There are still some common XAML and WPF scenarios where code access to an element with no `Name` property at the framework level is important.</span></span> <span data-ttu-id="7b62d-147">Na przykład niektóre klasy obsługi animacji i `Name` scenorysu nie obsługują właściwości, ale często muszą być odwoływane w kodzie, aby kontrolować animację.</span><span class="sxs-lookup"><span data-stu-id="7b62d-147">For example, certain animation and storyboard support classes do not support a `Name` property, but they often need to be referenced in code in order to control the animation.</span></span> <span data-ttu-id="7b62d-148">Należy określić `x:Name` jako atrybut na osiach czasu i przekształcenia, które są tworzone w XAML, jeśli zamierzasz odwoływać się do nich z kodu później.</span><span class="sxs-lookup"><span data-stu-id="7b62d-148">You should specify `x:Name` as an attribute on timelines and transforms that are created in XAML, if you intend to reference them from code later.</span></span>

<span data-ttu-id="7b62d-149">Jeśli <xref:System.Windows.FrameworkElement.Name%2A> jest dostępny jako właściwość <xref:System.Windows.FrameworkElement.Name%2A> `x:Name` w klasie i może być używany zamiennie jako atrybuty, ale wyjątek analizy spowoduje, jeśli oba są określone w tym samym elemencie.</span><span class="sxs-lookup"><span data-stu-id="7b62d-149">If <xref:System.Windows.FrameworkElement.Name%2A> is available as a property on the class, <xref:System.Windows.FrameworkElement.Name%2A> and `x:Name` can be used interchangeably as attributes, but a parse exception will result if both are specified on the same element.</span></span> <span data-ttu-id="7b62d-150">Jeśli XAML jest znaczników skompilowany, wyjątek wystąpi na kompilacji znaczników, w przeciwnym razie występuje na obciążenie.</span><span class="sxs-lookup"><span data-stu-id="7b62d-150">If the XAML is markup compiled, the exception will occur on the markup compile, otherwise it occurs on load.</span></span>

<span data-ttu-id="7b62d-151"><xref:System.Windows.FrameworkElement.Name%2A>można ustawić przy użyciu składni atrybutu XAML, a w kodzie przy użyciu <xref:System.Windows.DependencyObject.SetValue%2A>; należy jednak zauważyć, że ustawienie <xref:System.Windows.FrameworkElement.Name%2A> właściwości w kodzie nie tworzy reprezentatywne odwołanie do pola w obszarze nazw XAML w większości przypadków, gdy kod XAML jest już załadowany.</span><span class="sxs-lookup"><span data-stu-id="7b62d-151"><xref:System.Windows.FrameworkElement.Name%2A> can be set using XAML attribute syntax, and in code using <xref:System.Windows.DependencyObject.SetValue%2A>; note however that setting the <xref:System.Windows.FrameworkElement.Name%2A> property in code does not create the representative field reference within the XAML namescope in most circumstances where the XAML is already loaded.</span></span> <span data-ttu-id="7b62d-152">Zamiast próbować ustawić <xref:System.Windows.FrameworkElement.Name%2A> w kodzie, <xref:System.Windows.NameScope> użyj metod z kodu, względem odpowiedniego namescope.</span><span class="sxs-lookup"><span data-stu-id="7b62d-152">Instead of attempting to set <xref:System.Windows.FrameworkElement.Name%2A> in code, use <xref:System.Windows.NameScope> methods from code, against the appropriate namescope.</span></span>

<span data-ttu-id="7b62d-153"><xref:System.Windows.FrameworkElement.Name%2A>można również ustawić przy użyciu składni elementu właściwości z tekstem wewnętrznym, ale jest to rzadkością.</span><span class="sxs-lookup"><span data-stu-id="7b62d-153"><xref:System.Windows.FrameworkElement.Name%2A> can also be set using property element syntax with inner text, but that is uncommon.</span></span> <span data-ttu-id="7b62d-154">Natomiast `x:Name` nie można ustawić w składni elementu właściwości XAML <xref:System.Windows.DependencyObject.SetValue%2A>ani w kodzie przy użyciu ; można go ustawić tylko przy użyciu składni atrybutów na obiektach, ponieważ jest to dyrektywa.</span><span class="sxs-lookup"><span data-stu-id="7b62d-154">In contrast, `x:Name` cannot be set in XAML property element syntax, or in code using <xref:System.Windows.DependencyObject.SetValue%2A>; it can only be set using attribute syntax on objects because it is a directive.</span></span>

## <a name="silverlight-usage-notes"></a><span data-ttu-id="7b62d-155">Uwagi dotyczące użycia programu Silverlight</span><span class="sxs-lookup"><span data-stu-id="7b62d-155">Silverlight Usage Notes</span></span>

<span data-ttu-id="7b62d-156">`x:Name`dla programu Silverlight jest dokumentowany oddzielnie.</span><span class="sxs-lookup"><span data-stu-id="7b62d-156">`x:Name` for Silverlight is documented separately.</span></span> <span data-ttu-id="7b62d-157">Aby uzyskać więcej informacji, zobacz [obszar nazw XAML (x:) Funkcje językowe (Silverlight)](https://docs.microsoft.com/previous-versions/windows/silverlight/dotnet-windows-silverlight/cc188995(v=vs.95)).</span><span class="sxs-lookup"><span data-stu-id="7b62d-157">For more information, see [XAML Namespace (x:) Language Features (Silverlight)](https://docs.microsoft.com/previous-versions/windows/silverlight/dotnet-windows-silverlight/cc188995(v=vs.95)).</span></span>

## <a name="see-also"></a><span data-ttu-id="7b62d-158">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="7b62d-158">See also</span></span>

- <xref:System.Windows.FrameworkElement.Name%2A?displayProperty=nameWithType>
- <xref:System.Windows.FrameworkContentElement.Name%2A?displayProperty=nameWithType>
- [<span data-ttu-id="7b62d-159">Drzewa w WPF</span><span class="sxs-lookup"><span data-stu-id="7b62d-159">Trees in WPF</span></span>](../../framework/wpf/advanced/trees-in-wpf.md)
