---
title: Połączenie z serwerem SQL buforowanie (ADO.NET)
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: 7e51d44e-7c4e-4040-9332-f0190fe36f07
ms.openlocfilehash: f416ae8252d9991905da7eeaf4ce6398ff0e7461
ms.sourcegitcommit: 2eceb05f1a5bb261291a1f6a91c5153727ac1c19
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/04/2018
ms.locfileid: "43514966"
---
# <a name="sql-server-connection-pooling-adonet"></a><span data-ttu-id="6a8ea-102">Połączenie z serwerem SQL buforowanie (ADO.NET)</span><span class="sxs-lookup"><span data-stu-id="6a8ea-102">SQL Server Connection Pooling (ADO.NET)</span></span>
<span data-ttu-id="6a8ea-103">Nawiązywanie połączenia z serwerem bazy danych zazwyczaj składa się z kilku kroków czasochłonne.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-103">Connecting to a database server typically consists of several time-consuming steps.</span></span> <span data-ttu-id="6a8ea-104">Kanał fizycznych, takich jak gniazda lub nazwany potok należy ustalić, musi nastąpić uzgadnianie początkową z serwerem, informacje o parametrach połączenia musi zostać przeanalizowany, połączenie musi zostać uwierzytelniony przez serwer, należy uruchomić testy dla rejestrowanie w Bieżąca transakcja i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-104">A physical channel such as a socket or a named pipe must be established, the initial handshake with the server must occur, the connection string information must be parsed, the connection must be authenticated by the server, checks must be run for enlisting in the current transaction, and so on.</span></span>  
  
 <span data-ttu-id="6a8ea-105">W praktyce większość aplikacji używa tylko jednej lub kilku różnych konfiguracji dla połączenia.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-105">In practice, most applications use only one or a few different configurations for connections.</span></span> <span data-ttu-id="6a8ea-106">Oznacza to, że podczas wykonywania aplikacji wiele połączeń identyczne będzie wielokrotnie otwierania i zamykania.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-106">This means that during application execution, many identical connections will be repeatedly opened and closed.</span></span> <span data-ttu-id="6a8ea-107">Aby zminimalizować koszty otwarcia połączeń, [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)] wykorzystuje technikę optymalizacji o nazwie *buforowanie połączeń*.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-107">To minimize the cost of opening connections, [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)] uses an optimization technique called *connection pooling*.</span></span>  
  
 <span data-ttu-id="6a8ea-108">Pula połączeń zmniejsza liczbę razy, które muszą być otwarte nowe połączenia.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-108">Connection pooling reduces the number of times that new connections must be opened.</span></span> <span data-ttu-id="6a8ea-109">*Pulę* zachowuje własność połączenie fizyczne.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-109">The *pooler* maintains ownership of the physical connection.</span></span> <span data-ttu-id="6a8ea-110">Zarządza połączeń przechowując aktywny zestaw aktywnych połączeń dla każdej konfiguracji danego połączenia.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-110">It manages connections by keeping alive a set of active connections for each given connection configuration.</span></span> <span data-ttu-id="6a8ea-111">Zawsze, gdy użytkownik wywołuje `Open` połączenia, pulę szuka dostępnych połączeń w puli.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-111">Whenever a user calls `Open` on a connection, the pooler looks for an available connection in the pool.</span></span> <span data-ttu-id="6a8ea-112">Jeśli w puli połączeń jest dostępny, zwraca go do obiektu wywołującego, zamiast otwierać nowe połączenie.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-112">If a pooled connection is available, it returns it to the caller instead of opening a new connection.</span></span> <span data-ttu-id="6a8ea-113">Gdy aplikacja wywołuje `Close` w ramach połączenia pulę zwraca go do puli zestawów aktywnych połączeń, a nie jej zamknięciem.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-113">When the application calls `Close` on the connection, the pooler returns it to the pooled set of active connections instead of closing it.</span></span> <span data-ttu-id="6a8ea-114">Gdy połączenie jest zwracany do puli, jest gotowy do można użyć ponownie w następnym `Open` wywołania.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-114">Once the connection is returned to the pool, it is ready to be reused on the next `Open` call.</span></span>  
  
 <span data-ttu-id="6a8ea-115">Tylko na połączenia z taką samą konfigurację można puli.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-115">Only connections with the same configuration can be pooled.</span></span> [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)]<span data-ttu-id="6a8ea-116"> zapewnia kilka pul w tym samym czasie, jedną dla każdej konfiguracji.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-116"> keeps several pools at the same time, one for each configuration.</span></span> <span data-ttu-id="6a8ea-117">Połączenia są podzielone na pule przy użyciu parametrów połączenia, a także przez tożsamość w Windows stosowania zintegrowanych zabezpieczeń.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-117">Connections are separated into pools by connection string, and by Windows identity when integrated security is used.</span></span> <span data-ttu-id="6a8ea-118">Połączenia są również grupowane w pulach w oparciu czy biorących udział w transakcji.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-118">Connections are also pooled based on whether they are enlisted in a transaction.</span></span> <span data-ttu-id="6a8ea-119">Korzystając z <xref:System.Data.SqlClient.SqlConnection.ChangePassword%2A>, <xref:System.Data.SqlClient.SqlCredential> wystąpienia ma wpływ na puli połączeń.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-119">When using <xref:System.Data.SqlClient.SqlConnection.ChangePassword%2A>, the <xref:System.Data.SqlClient.SqlCredential> instance affects the connection pool.</span></span> <span data-ttu-id="6a8ea-120">Różne wystąpienia <xref:System.Data.SqlClient.SqlCredential> użyje pule inne połączenie, nawet jeśli identyfikator użytkownika i hasło są takie same.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-120">Different instances of <xref:System.Data.SqlClient.SqlCredential> will use different connection pools, even if the user ID and password are the same.</span></span>  
  
 <span data-ttu-id="6a8ea-121">Tworzenie puli połączeń mogą znacznie poprawić wydajność i skalowalność aplikacji.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-121">Pooling connections can significantly enhance the performance and scalability of your application.</span></span> <span data-ttu-id="6a8ea-122">Domyślnie, połączenie jest włączone buforowanie w [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)].</span><span class="sxs-lookup"><span data-stu-id="6a8ea-122">By default, connection pooling is enabled in [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)].</span></span> <span data-ttu-id="6a8ea-123">Chyba że jawnie wyłączyć ją, pulę optymalizuje połączenia, ponieważ są otwarte i zamknięte w aplikacji.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-123">Unless you explicitly disable it, the pooler optimizes the connections as they are opened and closed in your application.</span></span> <span data-ttu-id="6a8ea-124">Można też podać kilka modyfikatorów parametrów połączenia do sterowania zachowaniem buforowania połączeń.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-124">You can also supply several connection string modifiers to control connection pooling behavior.</span></span> <span data-ttu-id="6a8ea-125">Aby uzyskać więcej informacji zobacz "Kontrolowanie połączenia puli za pomocą połączenia ciągu słowa kluczowe" w dalszej części tego tematu.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-125">For more information, see "Controlling Connection Pooling with Connection String Keywords" later in this topic.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="6a8ea-126">Gdy jest włączone buforowanie połączeń, a jeśli wystąpi błąd upływu limitu czasu lub inny błąd logowania, zostanie zgłoszony wyjątek i kolejnych próbach połączenia zakończy się niepowodzeniem dla następnego pięć sekund, "okres blokowanie".</span><span class="sxs-lookup"><span data-stu-id="6a8ea-126">When connection pooling is enabled, and if a timeout error or other login error occurs, an exception will be thrown and subsequent connection attempts will fail for the next five seconds, the "blocking period".</span></span> <span data-ttu-id="6a8ea-127">Jeśli aplikacja próbuje nawiązać połączenie przed upływem blokowania, pierwszy wyjątek zostanie zgłoszony ponownie.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-127">If the application attempts to connect within the blocking period, the first exception will be thrown again.</span></span> <span data-ttu-id="6a8ea-128">Kolejne błędy po upływie okresu blokowania spowoduje nowych okresów blokujące, które jest dwa razy tak długo, jak w poprzednim okresie blokowania, maksymalnie do jednej minuty.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-128">Subsequent failures after a blocking period ends will result in a new blocking periods that is twice as long as the previous blocking period, up to a maximum of one minute.</span></span>  
  
## <a name="pool-creation-and-assignment"></a><span data-ttu-id="6a8ea-129">Tworzenie puli i przypisanie</span><span class="sxs-lookup"><span data-stu-id="6a8ea-129">Pool Creation and Assignment</span></span>  
 <span data-ttu-id="6a8ea-130">Przy pierwszym otwarciu połączenia, puli połączeń jest oparta na dokładne algorytm dopasowania, które kojarzy puli przy użyciu parametrów połączenia w połączeniu.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-130">When a connection is first opened, a connection pool is created based on an exact matching algorithm that associates the pool with the connection string in the connection.</span></span> <span data-ttu-id="6a8ea-131">Każdej puli połączeń jest skojarzony z parametrami połączenia distinct.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-131">Each connection pool is associated with a distinct connection string.</span></span> <span data-ttu-id="6a8ea-132">Po otwarciu nowego połączenia, jeśli parametry połączenia nie jest dokładnie dopasowany do istniejącej puli, tworzona jest nowa pula.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-132">When a new connection is opened, if the connection string is not an exact match to an existing pool, a new pool is created.</span></span> <span data-ttu-id="6a8ea-133">Połączenia są grupowane w pulach na proces w każdej domenie aplikacji, na parametry połączenia i używane są zabezpieczenia zintegrowane na Windows identity.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-133">Connections are pooled per process, per application domain, per connection string and when integrated security is used, per Windows identity.</span></span> <span data-ttu-id="6a8ea-134">Parametry połączenia muszą również być dokładnym dopasowaniem; słowa kluczowe w innej kolejności dla tego samego połączenia będą puli oddzielnie.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-134">Connection strings must also be an exact match; keywords supplied in a different order for the same connection will be pooled separately.</span></span>  
  
 <span data-ttu-id="6a8ea-135">W języku C# przykładzie trzy nowe <xref:System.Data.SqlClient.SqlConnection> obiekty są tworzone, ale tylko dwóch pul połączeń są wymagane do zarządzania nimi.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-135">In the following C# example, three new <xref:System.Data.SqlClient.SqlConnection> objects are created, but only two connection pools are required to manage them.</span></span> <span data-ttu-id="6a8ea-136">Należy pamiętać, że parametry połączenia pierwszego i drugiego różnią się wartość przypisana do `Initial Catalog`.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-136">Note that the first and second connection strings differ by the value assigned for `Initial Catalog`.</span></span>  
  
```  
using (SqlConnection connection = new SqlConnection(  
  "Integrated Security=SSPI;Initial Catalog=Northwind"))  
    {  
        connection.Open();        
        // Pool A is created.  
    }  
  
using (SqlConnection connection = new SqlConnection(  
  "Integrated Security=SSPI;Initial Catalog=pubs"))  
    {  
        connection.Open();        
        // Pool B is created because the connection strings differ.  
    }  
  
using (SqlConnection connection = new SqlConnection(  
  "Integrated Security=SSPI;Initial Catalog=Northwind"))  
    {  
        connection.Open();        
        // The connection string matches pool A.  
    }  
```  
  
 <span data-ttu-id="6a8ea-137">Jeśli `MinPoolSize` nie została określona w parametrach połączenia lub określone jako zero, połączeń w puli zostaną zamknięte po okresie braku aktywności.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-137">If `MinPoolSize` is either not specified in the connection string or is specified as zero, the connections in the pool will be closed after a period of inactivity.</span></span> <span data-ttu-id="6a8ea-138">Jednakże jeśli określony `MinPoolSize` jest większa od zera, puli połączeń nie jest niszczony, aż do `AppDomain` jest zwalniana i kończy proces.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-138">However, if the specified `MinPoolSize` is greater than zero, the connection pool is not destroyed until the `AppDomain` is unloaded and the process ends.</span></span> <span data-ttu-id="6a8ea-139">Konserwacja pule nieaktywne lub jest pusty obejmuje koszty minimalny system.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-139">Maintenance of inactive or empty pools involves minimal system overhead.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="6a8ea-140">Puli są automatycznie usuwane, gdy wystąpi błąd krytyczny, takich jak przejścia w tryb failover.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-140">The pool is automatically cleared when a fatal error occurs, such as a failover.</span></span>  
  
## <a name="adding-connections"></a><span data-ttu-id="6a8ea-141">Dodawanie połączeń</span><span class="sxs-lookup"><span data-stu-id="6a8ea-141">Adding Connections</span></span>  
 <span data-ttu-id="6a8ea-142">Puli połączeń jest tworzony dla każdego ciągu unikatowego połączenia.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-142">A connection pool is created for each unique connection string.</span></span> <span data-ttu-id="6a8ea-143">Po utworzeniu puli wielu obiektów połączeń są tworzone i dodawane do puli, dzięki czemu puli minimalny wymagany rozmiar jest spełniony.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-143">When a pool is created, multiple connection objects are created and added to the pool so that the minimum pool size requirement is satisfied.</span></span> <span data-ttu-id="6a8ea-144">Połączenia są dodawane do puli zgodnie z potrzebami, maksymalnie maksymalny rozmiar puli określony (wartość domyślna to 100).</span><span class="sxs-lookup"><span data-stu-id="6a8ea-144">Connections are added to the pool as needed, up to the maximum pool size specified (100 is the default).</span></span> <span data-ttu-id="6a8ea-145">Połączenia są wydawane do puli, gdy są one zamknięte lub usunięty.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-145">Connections are released back into the pool when they are closed or disposed.</span></span>  
  
 <span data-ttu-id="6a8ea-146">Gdy <xref:System.Data.SqlClient.SqlConnection> Zażądano obiektu, jest uzyskiwana z puli, jeśli można używać połączenia jest dostępny.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-146">When a <xref:System.Data.SqlClient.SqlConnection> object is requested, it is obtained from the pool if a usable connection is available.</span></span> <span data-ttu-id="6a8ea-147">Może być używany, połączenie musi być nieużywane, mają pasujących kontekstu transakcji lub być nieskojarzone z żadnym z kontekstem transakcji i mają prawidłowe łącze do serwera.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-147">To be usable, a connection must be unused, have a matching transaction context or be unassociated with any transaction context, and have a valid link to the server.</span></span>  
  
 <span data-ttu-id="6a8ea-148">Pulę połączeń spełnia żądań dla połączeń przez ponowne przydzielanie połączenia po ich wydaniu do puli.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-148">The connection pooler satisfies requests for connections by reallocating connections as they are released back into the pool.</span></span> <span data-ttu-id="6a8ea-149">Jeśli osiągnięty maksymalny rozmiar puli, a nie można używać połączenia jest dostępna, żądanie znajduje się w kolejce.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-149">If the maximum pool size has been reached and no usable connection is available, the request is queued.</span></span> <span data-ttu-id="6a8ea-150">Pulę następnie próbuje odzyskać wszystkie połączenia, dopóki nie zostanie osiągnięty limit czasu (wartość domyślna wynosi 15 sekund).</span><span class="sxs-lookup"><span data-stu-id="6a8ea-150">The pooler then tries to reclaim any connections until the time-out is reached (the default is 15 seconds).</span></span> <span data-ttu-id="6a8ea-151">Jeśli pulę nie może zrealizować żądanie, zanim upłynie limit czasu połączenia, zwracany jest wyjątek.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-151">If the pooler cannot satisfy the request before the connection times out, an exception is thrown.</span></span>  
  
> [!CAUTION]
>  <span data-ttu-id="6a8ea-152">Zdecydowanie zaleca się zawsze zamykaj połączenie po zakończeniu korzystania z niego, tak aby połączenie zostanie zwrócony do puli.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-152">We strongly recommend that you always close the connection when you are finished using it so that the connection will be returned to the pool.</span></span> <span data-ttu-id="6a8ea-153">Można to zrobić za pomocą `Close` lub `Dispose` metody `Connection` obiektu lub otwierając wszystkich połączeń wewnątrz `using` instrukcji w języku C# lub `Using` instrukcji w języku Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-153">You can do this using either the `Close` or `Dispose` methods of the `Connection` object, or by opening all connections inside a `using` statement in C#, or a `Using` statement in Visual Basic.</span></span> <span data-ttu-id="6a8ea-154">Połączenia, które nie są jawnie zamknięty, nie może dodać lub zwrócone do puli.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-154">Connections that are not explicitly closed might not be added or returned to the pool.</span></span> <span data-ttu-id="6a8ea-155">Aby uzyskać więcej informacji, zobacz [za pomocą instrukcji](~/docs/csharp/language-reference/keywords/using-statement.md) lub [porady: usuwanie zasobu systemu](~/docs/visual-basic/programming-guide/language-features/control-flow/how-to-dispose-of-a-system-resource.md) dla języka Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-155">For more information, see [using Statement](~/docs/csharp/language-reference/keywords/using-statement.md) or [How to: Dispose of a System Resource](~/docs/visual-basic/programming-guide/language-features/control-flow/how-to-dispose-of-a-system-resource.md) for Visual Basic.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="6a8ea-156">Nie wywołuj `Close` lub `Dispose` na `Connection`, `DataReader`, lub inne obiekt zarządzany w `Finalize` metody klasy.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-156">Do not call `Close` or `Dispose` on a `Connection`, a `DataReader`, or any other managed object in the `Finalize` method of your class.</span></span> <span data-ttu-id="6a8ea-157">W finalizatora zwolnić tylko niezarządzane zasoby, które należą bezpośrednio do swojej klasy.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-157">In a finalizer, only release unmanaged resources that your class owns directly.</span></span> <span data-ttu-id="6a8ea-158">Jeśli klasa nie ma żadnych niezarządzanych zasobów, nie dołączaj `Finalize` metody w swojej definicji klasy.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-158">If your class does not own any unmanaged resources, do not include a `Finalize` method in your class definition.</span></span> <span data-ttu-id="6a8ea-159">Aby uzyskać więcej informacji, zobacz [wyrzucania elementów bezużytecznych](../../../../docs/standard/garbage-collection/index.md).</span><span class="sxs-lookup"><span data-stu-id="6a8ea-159">For more information, see [Garbage Collection](../../../../docs/standard/garbage-collection/index.md).</span></span>  
  
<span data-ttu-id="6a8ea-160">Aby uzyskać więcej informacji na temat zdarzeń powiązanych ze otwierające i zamykające połączeń, zobacz [klasa zdarzenia logowania inspekcji](/sql/relational-databases/event-classes/audit-login-event-class) i [klasa zdarzenia wylogowania inspekcji](/sql/relational-databases/event-classes/audit-logout-event-class) w dokumentacji programu SQL Server.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-160">For more info about the events associated with opening and closing connections, see [Audit Login Event Class](/sql/relational-databases/event-classes/audit-login-event-class) and [Audit Logout Event Class](/sql/relational-databases/event-classes/audit-logout-event-class) in the SQL Server documentation.</span></span>  
  
## <a name="removing-connections"></a><span data-ttu-id="6a8ea-161">Usuwanie połączeń</span><span class="sxs-lookup"><span data-stu-id="6a8ea-161">Removing Connections</span></span>  
 <span data-ttu-id="6a8ea-162">Pulę połączeń usuwa połączenie z puli, po była bezczynna przez około 4 – 8 minut lub pulę wykryje, że zostały oddzielone połączenia z serwerem.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-162">The connection pooler removes a connection from the pool after it has been idle for approximately 4-8 minutes, or if the pooler detects that the connection with the server has been severed.</span></span> <span data-ttu-id="6a8ea-163">Należy pamiętać, że ODCIĘTA połączenia można wykryć tylko po próby komunikacji z serwerem.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-163">Note that a severed connection can be detected only after attempting to communicate with the server.</span></span> <span data-ttu-id="6a8ea-164">Jeśli połączenie zostanie znaleziony, który nie jest już połączony z serwerem, jest oznaczona jako niepoprawna.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-164">If a connection is found that is no longer connected to the server, it is marked as invalid.</span></span> <span data-ttu-id="6a8ea-165">Nieprawidłowy połączeń są usuwane z puli połączeń, tylko wtedy, gdy są one zamknięte lub odzyskać.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-165">Invalid connections are removed from the connection pool only when they are closed or reclaimed.</span></span>  
  
 <span data-ttu-id="6a8ea-166">Jeśli połączenie istnieje na serwerze, na którym nie zniknie, to połączenie może być pobierana z puli, nawet jeśli nie wykryto połączenia ODCIĘTA i ona oznaczona jako niepoprawna pulę połączeń.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-166">If a connection exists to a server that has disappeared, this connection can be drawn from the pool even if the connection pooler has not detected the severed connection and marked it as invalid.</span></span> <span data-ttu-id="6a8ea-167">Jest to wymagane, ponieważ obciążenie sprawdzania, czy połączenie jest nadal ważny wyeliminować korzyści pulę, powodując innej komunikacji dwustronnej z serwerem występuje.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-167">This is the case because the overhead of checking that the connection is still valid would eliminate the benefits of having a pooler by causing another round trip to the server to occur.</span></span> <span data-ttu-id="6a8ea-168">W takiej sytuacji pierwsza próba połączenia z wykryje, że połączenia zostały oddzielone, i zgłaszany jest wyjątek.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-168">When this occurs, the first attempt to use the connection will detect that the connection has been severed, and an exception is thrown.</span></span>  
  
## <a name="clearing-the-pool"></a><span data-ttu-id="6a8ea-169">Czyszczenie puli</span><span class="sxs-lookup"><span data-stu-id="6a8ea-169">Clearing the Pool</span></span>  
 [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)]<span data-ttu-id="6a8ea-170"> w wersji 2.0 wprowadzono dwie nowe metody, aby wyczyścić puli: <xref:System.Data.SqlClient.SqlConnection.ClearAllPools%2A> i <xref:System.Data.SqlClient.SqlConnection.ClearPool%2A>.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-170"> 2.0 introduced two new methods to clear the pool: <xref:System.Data.SqlClient.SqlConnection.ClearAllPools%2A> and <xref:System.Data.SqlClient.SqlConnection.ClearPool%2A>.</span></span> <span data-ttu-id="6a8ea-171">`ClearAllPools` Czyści pul połączeń dla danego dostawcy i `ClearPool` czyści puli połączeń, który jest skojarzony z określonego połączenia.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-171">`ClearAllPools` clears the connection pools for a given provider, and `ClearPool` clears the connection pool that is associated with a specific connection.</span></span> <span data-ttu-id="6a8ea-172">W przypadku połączenia używane w momencie zgłoszenia wywołania zostały odpowiednio oznaczone.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-172">If there are connections being used at the time of the call, they are marked appropriately.</span></span> <span data-ttu-id="6a8ea-173">Po zamknięciu, są ignorowane zamiast zwracane do puli.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-173">When they are closed, they are discarded instead of being returned to the pool.</span></span>  
  
## <a name="transaction-support"></a><span data-ttu-id="6a8ea-174">Obsługa transakcji</span><span class="sxs-lookup"><span data-stu-id="6a8ea-174">Transaction Support</span></span>  
 <span data-ttu-id="6a8ea-175">Połączenia są pobierane z puli i przypisane transakcji na podstawie kontekstu.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-175">Connections are drawn from the pool and assigned based on transaction context.</span></span> <span data-ttu-id="6a8ea-176">Chyba że `Enlist=false` określono w parametrach połączenia puli połączeń upewnia się, że połączenie jest zarejestrowany w <xref:System.Transactions.Transaction.Current%2A> kontekstu.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-176">Unless `Enlist=false` is specified in the connection string, the connection pool makes sure that the connection is enlisted in the <xref:System.Transactions.Transaction.Current%2A> context.</span></span> <span data-ttu-id="6a8ea-177">Po zamknięciu i zwrócone do puli za pomocą zobowiązaniom połączenie `System.Transactions` transakcji, jego została odłożona w taki sposób, że następne żądania dla tej puli połączeń o takiej samej `System.Transactions` transakcji zwróci tego samego połączenia, jeśli jest ona dostępna.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-177">When a connection is closed and returned to the pool with an enlisted `System.Transactions` transaction, it is set aside in such a way that the next request for that connection pool with the same `System.Transactions` transaction will return the same connection if it is available.</span></span> <span data-ttu-id="6a8ea-178">Jeśli takie żądanie zostało wydane, a brak puli połączeń, połączenie jest rysowana od składnika nietransakcyjnego puli i zarejestrowany.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-178">If such a request is issued, and there are no pooled connections available, a connection is drawn from the non-transacted part of the pool and enlisted.</span></span> <span data-ttu-id="6a8ea-179">Jeśli w dowolnym z tych obszarów puli dostępnych żadnych połączeń, nowe połączenie jest utworzony i zarejestrowany.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-179">If no connections are available in either area of the pool, a new connection is created and enlisted.</span></span>  
  
 <span data-ttu-id="6a8ea-180">Gdy połączenie jest zamknięte, jest on zwalniany powrót do puli i odpowiednie części pola na podstawie jego kontekstu transakcji.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-180">When a connection is closed, it is released back into the pool and into the appropriate subdivision based on its transaction context.</span></span> <span data-ttu-id="6a8ea-181">W związku z tym, można zamknąć połączenia nie generuje błąd, mimo że transakcja rozproszona jest nadal oczekujące.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-181">Therefore, you can close the connection without generating an error, even though a distributed transaction is still pending.</span></span> <span data-ttu-id="6a8ea-182">Dzięki temu można zatwierdzić lub przerwać transakcję rozproszoną później.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-182">This allows you to commit or abort the distributed transaction later.</span></span>  
  
## <a name="controlling-connection-pooling-with-connection-string-keywords"></a><span data-ttu-id="6a8ea-183">Kontrolowanie buforowania połączeń ze słowami kluczowymi ciąg połączenia</span><span class="sxs-lookup"><span data-stu-id="6a8ea-183">Controlling Connection Pooling with Connection String Keywords</span></span>  
 <span data-ttu-id="6a8ea-184">`ConnectionString` Właściwość <xref:System.Data.SqlClient.SqlConnection> obiekt obsługuje pary klucz/wartość ciągu połączenia, których można użyć, aby dostosować zachowanie buforowania logiki połączeń.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-184">The `ConnectionString` property of the <xref:System.Data.SqlClient.SqlConnection> object supports connection string key/value pairs that can be used to adjust the behavior of the connection pooling logic.</span></span> <span data-ttu-id="6a8ea-185">Aby uzyskać więcej informacji, zobacz <xref:System.Data.SqlClient.SqlConnection.ConnectionString%2A>.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-185">For more information, see <xref:System.Data.SqlClient.SqlConnection.ConnectionString%2A>.</span></span>  
  
## <a name="pool-fragmentation"></a><span data-ttu-id="6a8ea-186">Fragmentacja puli</span><span class="sxs-lookup"><span data-stu-id="6a8ea-186">Pool Fragmentation</span></span>  
 <span data-ttu-id="6a8ea-187">Fragmentacja puli jest to powszechny problem w wielu aplikacjach sieci Web, w której aplikacja może tworzyć dużej liczby pul, które nie są zwalniane, dopóki nie kończy procesu.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-187">Pool fragmentation is a common problem in many Web applications where the application can create a large number of pools that are not freed until the process exits.</span></span> <span data-ttu-id="6a8ea-188">Spowoduje to pozostawienie dużą liczbę połączeń otwartych i korzystanie z nich pamięci, co powoduje spadek wydajności.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-188">This leaves a large number of connections open and consuming memory, which results in poor performance.</span></span>  
  
### <a name="pool-fragmentation-due-to-integrated-security"></a><span data-ttu-id="6a8ea-189">Fragmentacja puli ze względu na zabezpieczenia zintegrowane</span><span class="sxs-lookup"><span data-stu-id="6a8ea-189">Pool Fragmentation Due to Integrated Security</span></span>  
 <span data-ttu-id="6a8ea-190">Połączenia są grupowane w pulach zgodnie z parametrów połączenia, a także tożsamości użytkownika.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-190">Connections are pooled according to the connection string plus the user identity.</span></span> <span data-ttu-id="6a8ea-191">W związku z tym Jeśli używasz uwierzytelniania podstawowego lub uwierzytelniania Windows w witrynie sieci Web i identyfikator logowania zintegrowanych zabezpieczeń, otrzymasz jedną pulę dla każdego użytkownika.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-191">Therefore, if you use Basic authentication or Windows Authentication on the Web site and an integrated security login, you get one pool per user.</span></span> <span data-ttu-id="6a8ea-192">Mimo że poprawia to wydajność bazy danych w kolejnych żądań dla pojedynczego użytkownika, ten użytkownik nie mogą korzystać z połączenia nawiązywane przez innych użytkowników.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-192">Although this improves the performance of subsequent database requests for a single user, that user cannot take advantage of connections made by other users.</span></span> <span data-ttu-id="6a8ea-193">Powoduje ona również co najmniej jedno połączenie na użytkownika na serwerze bazy danych.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-193">It also results in at least one connection per user to the database server.</span></span> <span data-ttu-id="6a8ea-194">To jest efektem szczególna Architektura aplikacji sieci Web, który deweloperzy należy porównać względem zabezpieczeń i wymogów dotyczących audytu.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-194">This is a side effect of a particular Web application architecture that developers must weigh against security and auditing requirements.</span></span>  
  
### <a name="pool-fragmentation-due-to-many-databases"></a><span data-ttu-id="6a8ea-195">Fragmentacja puli z powodu wielu baz danych</span><span class="sxs-lookup"><span data-stu-id="6a8ea-195">Pool Fragmentation Due to Many Databases</span></span>  
 <span data-ttu-id="6a8ea-196">Usługodawcy internetowi hostować wiele witryn sieci Web na jednym serwerze.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-196">Many Internet service providers host several Web sites on a single server.</span></span> <span data-ttu-id="6a8ea-197">Aby potwierdzić logowania uwierzytelniania formularzy, a następnie otworzyć połączenia z określoną bazą danych dla tego użytkownika lub grupy użytkowników mogą używać jednej bazy danych.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-197">They may use a single database to confirm a Forms authentication login and then open a connection to a specific database for that user or group of users.</span></span> <span data-ttu-id="6a8ea-198">Połączenie z bazą danych uwierzytelniania jest puli i używana przez wszystkich użytkowników.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-198">The connection to the authentication database is pooled and used by everyone.</span></span> <span data-ttu-id="6a8ea-199">Jednak jest oddzielną pulę połączeń dla każdej bazy danych, które zwiększają liczbę połączeń serwera.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-199">However, there is a separate pool of connections to each database, which increase the number of connections to the server.</span></span>  
  
 <span data-ttu-id="6a8ea-200">Dotyczy to również efekt uboczny projektowania aplikacji.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-200">This is also a side-effect of the application design.</span></span> <span data-ttu-id="6a8ea-201">Istnieje stosunkowo prosty sposób, aby uniknąć tego efektu bez uszczerbku dla zabezpieczeń podczas nawiązywania połączenia programu SQL Server.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-201">There is a relatively simple way to avoid this side effect without compromising security when you connect to SQL Server.</span></span> <span data-ttu-id="6a8ea-202">Zamiast nawiązywania połączenia z oddzielnej bazy danych dla każdego użytkownika lub grupy, należy nawiązać połączenie z tej samej bazy danych na serwerze, a następnie wykonaj [!INCLUDE[tsql](../../../../includes/tsql-md.md)] użycie instrukcji, aby przejść do żądanej bazy danych.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-202">Instead of connecting to a separate database for each user or group, connect to the same database on the server and then execute the [!INCLUDE[tsql](../../../../includes/tsql-md.md)] USE statement to change to the desired database.</span></span> <span data-ttu-id="6a8ea-203">Poniższy fragment kodu przedstawia tworzenie początkowego połączenia `master` bazy danych, a następnie przechodząc do żądanego bazą danych określoną w `databaseName` zmiennej ciągu.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-203">The following code fragment demonstrates creating an initial connection to the `master` database and then switching to the desired database specified in the `databaseName` string variable.</span></span>  
  
```vb  
' Assumes that command is a valid SqlCommand object and that  
' connectionString connects to master.  
    command.Text = "USE DatabaseName"  
Using connection As New SqlConnection(connectionString)  
    connection.Open()  
    command.ExecuteNonQuery()  
End Using  
```  
  
```csharp  
// Assumes that command is a SqlCommand object and that  
// connectionString connects to master.  
command.Text = "USE DatabaseName";  
using (SqlConnection connection = new SqlConnection(  
  connectionString))  
  {  
    connection.Open();  
    command.ExecuteNonQuery();  
  }  
```  
  
## <a name="application-roles-and-connection-pooling"></a><span data-ttu-id="6a8ea-204">Role aplikacji i pule połączeń</span><span class="sxs-lookup"><span data-stu-id="6a8ea-204">Application Roles and Connection Pooling</span></span>  
 <span data-ttu-id="6a8ea-205">Po programu SQL Server został aktywowany roli aplikacji, wywołując `sp_setapprole` systemowej procedury składowanej, nie można zresetować w kontekście zabezpieczeń tego połączenia.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-205">After a SQL Server application role has been activated by calling the `sp_setapprole` system stored procedure, the security context of that connection cannot be reset.</span></span> <span data-ttu-id="6a8ea-206">Jednak jeśli włączone jest buforowanie, połączenie jest zwracany do puli i występuje błąd podczas połączenia z puli zostanie ponownie użyty.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-206">However, if pooling is enabled, the connection is returned to the pool, and an error occurs when the pooled connection is reused.</span></span> <span data-ttu-id="6a8ea-207">Aby uzyskać więcej informacji, zobacz artykuł bazy wiedzy, "[błędów roli aplikacji SQL z puli zasobów OLE DB](https://support.microsoft.com/default.aspx?scid=KB;EN-US;Q229564)."</span><span class="sxs-lookup"><span data-stu-id="6a8ea-207">For more information, see the Knowledge Base article, "[SQL application role errors with OLE DB resource pooling](https://support.microsoft.com/default.aspx?scid=KB;EN-US;Q229564)."</span></span>  
  
### <a name="application-role-alternatives"></a><span data-ttu-id="6a8ea-208">Alternatywy ról aplikacji</span><span class="sxs-lookup"><span data-stu-id="6a8ea-208">Application Role Alternatives</span></span>  
 <span data-ttu-id="6a8ea-209">Zaleca się, że możesz korzystać z mechanizmami zabezpieczeń, które można użyć zamiast ról aplikacji.</span><span class="sxs-lookup"><span data-stu-id="6a8ea-209">We recommend that you take advantage of security mechanisms that you can use instead of application roles.</span></span> <span data-ttu-id="6a8ea-210">Aby uzyskać więcej informacji, zobacz [tworzenie ról aplikacji w programie SQL Server](../../../../docs/framework/data/adonet/sql/creating-application-roles-in-sql-server.md).</span><span class="sxs-lookup"><span data-stu-id="6a8ea-210">For more information, see [Creating Application Roles in SQL Server](../../../../docs/framework/data/adonet/sql/creating-application-roles-in-sql-server.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="6a8ea-211">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="6a8ea-211">See Also</span></span>  
 [<span data-ttu-id="6a8ea-212">Pula połączeń</span><span class="sxs-lookup"><span data-stu-id="6a8ea-212">Connection Pooling</span></span>](../../../../docs/framework/data/adonet/connection-pooling.md)  
 [<span data-ttu-id="6a8ea-213">SQL Server i ADO.NET</span><span class="sxs-lookup"><span data-stu-id="6a8ea-213">SQL Server and ADO.NET</span></span>](../../../../docs/framework/data/adonet/sql/index.md)  
 [<span data-ttu-id="6a8ea-214">Liczniki wydajności</span><span class="sxs-lookup"><span data-stu-id="6a8ea-214">Performance Counters</span></span>](../../../../docs/framework/data/adonet/performance-counters.md)  
 [<span data-ttu-id="6a8ea-215">ADO.NET zarządzanego dostawcy i Centrum deweloperów zestawu danych</span><span class="sxs-lookup"><span data-stu-id="6a8ea-215">ADO.NET Managed Providers and DataSet Developer Center</span></span>](https://go.microsoft.com/fwlink/?LinkId=217917)
