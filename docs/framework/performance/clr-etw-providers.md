---
title: Dostawcy CLR ETW
ms.date: 03/30/2017
helpviewer_keywords:
- ETW, CLR providers
- CLR ETW providers
ms.assetid: 0beafad4-b2c8-47f4-b342-83411d57a51f
author: mairaw
ms.author: mairaw
ms.openlocfilehash: 93a0271c521de6e390e323d92e93a5e7bf94444f
ms.sourcegitcommit: 289e06e904b72f34ac717dbcc5074239b977e707
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/17/2019
ms.locfileid: "71046733"
---
# <a name="clr-etw-providers"></a><span data-ttu-id="81047-102">Dostawcy CLR ETW</span><span class="sxs-lookup"><span data-stu-id="81047-102">CLR ETW Providers</span></span>
<span data-ttu-id="81047-103">Środowisko uruchomieniowe języka wspólnego (CLR) ma dwóch dostawców: dostawcę środowiska uruchomieniowego i dostawcę.</span><span class="sxs-lookup"><span data-stu-id="81047-103">The common language runtime (CLR) has two providers: the runtime provider and the rundown provider.</span></span>  
  
 <span data-ttu-id="81047-104">Dostawca środowiska uruchomieniowego zgłasza zdarzenia, w zależności od tego, które słowa kluczowe (kategorie zdarzeń) są włączone.</span><span class="sxs-lookup"><span data-stu-id="81047-104">The runtime provider raises events, depending on which keywords (categories of events) are enabled.</span></span> <span data-ttu-id="81047-105">Na przykład można zbierać zdarzenia modułu ładującego przez włączenie `LoaderKeyword` słowa kluczowego.</span><span class="sxs-lookup"><span data-stu-id="81047-105">For example, you can collect loader events by enabling the `LoaderKeyword` keyword.</span></span>  
  
 <span data-ttu-id="81047-106">Zdarzenia śledzenia zdarzeń systemu Windows (ETW) są rejestrowane w pliku z rozszerzeniem. etl, które może później być przetworzone w plikach z wartościami rozdzielanymi przecinkami (CSV), zgodnie z wymaganiami.</span><span class="sxs-lookup"><span data-stu-id="81047-106">Event Tracing for Windows (ETW) events are logged into a file that has an .etl extension, which can later be post-processed in comma-separated value (.csv) files as needed.</span></span> <span data-ttu-id="81047-107">Informacje o sposobach konwertowania pliku. etl do pliku CSV znajdują się w temacie [sterowanie rejestrowaniem .NET Framework](controlling-logging.md).</span><span class="sxs-lookup"><span data-stu-id="81047-107">For information about how to convert the .etl file to a .csv file, see [Controlling .NET Framework Logging](controlling-logging.md).</span></span>  
  
## <a name="the-runtime-provider"></a><span data-ttu-id="81047-108">Dostawca środowiska uruchomieniowego</span><span class="sxs-lookup"><span data-stu-id="81047-108">The Runtime Provider</span></span>  
 <span data-ttu-id="81047-109">Dostawca środowiska uruchomieniowego jest głównym dostawcą funkcji ETW CLR.</span><span class="sxs-lookup"><span data-stu-id="81047-109">The runtime provider is the main CLR ETW provider.</span></span>  
  
 <span data-ttu-id="81047-110">Identyfikator GUID dostawcy środowiska uruchomieniowego CLR to e13c0d23-ccbc-4e12-931b-d9cc2eee27e4.</span><span class="sxs-lookup"><span data-stu-id="81047-110">The CLR runtime provider GUID is e13c0d23-ccbc-4e12-931b-d9cc2eee27e4.</span></span>  
  
 <span data-ttu-id="81047-111">Aby zapoznać się z przykładami rejestrowania i wyświetlania zdarzeń ETW CLR przy użyciu powszechnie dostępnych narzędzi, zobacz [kontrolowanie rejestrowania .NET Framework](controlling-logging.md).</span><span class="sxs-lookup"><span data-stu-id="81047-111">For examples of how to log and view CLR ETW events by using commonly available tools, see [Controlling .NET Framework Logging](controlling-logging.md).</span></span>  
  
 <span data-ttu-id="81047-112">Oprócz używania słów kluczowych, takich jak `LoaderKeyword`, może być konieczne włączenie słów kluczowych do rejestrowania zdarzeń, które mogą być zgłaszane zbyt często.</span><span class="sxs-lookup"><span data-stu-id="81047-112">In addition to using keywords such as `LoaderKeyword`, you may have to enable keywords for logging events that may be raised too frequently.</span></span> <span data-ttu-id="81047-113">Słowakluczowe`EndEnumerationKeyword` i umożliwiają włączenie tych zdarzeń i są podsumowywane w [słowach kluczowych i poziomach funkcji ETW CLR.](clr-etw-keywords-and-levels.md) `StartEnumerationKeyword`</span><span class="sxs-lookup"><span data-stu-id="81047-113">The `StartEnumerationKeyword` and the `EndEnumerationKeyword` keywords enable these events and are summarized in [CLR ETW Keywords and Levels](clr-etw-keywords-and-levels.md).</span></span>  
  
## <a name="the-rundown-provider"></a><span data-ttu-id="81047-114">Dostawca uwalniania</span><span class="sxs-lookup"><span data-stu-id="81047-114">The Rundown Provider</span></span>  
 <span data-ttu-id="81047-115">Dostawca uwalniania musi być włączony do określonych zastosowań specjalnych.</span><span class="sxs-lookup"><span data-stu-id="81047-115">The rundown provider must be turned on for certain special-purpose uses.</span></span> <span data-ttu-id="81047-116">Jednak w przypadku większości użytkowników należy mieć wystarczające dla nich dostawca środowiska uruchomieniowego.</span><span class="sxs-lookup"><span data-stu-id="81047-116">However, for a majority of users, the runtime provider should suffice.</span></span>  
  
 <span data-ttu-id="81047-117">Identyfikator GUID dostawcy uwalniania CLR to A669021C-C450-4609-A035-5AF59AF4DF18.</span><span class="sxs-lookup"><span data-stu-id="81047-117">The CLR rundown provider GUID is A669021C-C450-4609-A035-5AF59AF4DF18.</span></span>  
  
 <span data-ttu-id="81047-118">Zwykle rejestrowanie ETW jest włączane przed uruchomieniem procesu, a rejestrowanie jest wyłączone po zakończeniu procesu.</span><span class="sxs-lookup"><span data-stu-id="81047-118">Normally, ETW logging is enabled before a process launches, and the logging is turned off after the process exits.</span></span> <span data-ttu-id="81047-119">Jeśli jednak rejestrowanie funkcji ETW jest włączone podczas wykonywania procesu, należy uzyskać dodatkowe informacje o tym procesie.</span><span class="sxs-lookup"><span data-stu-id="81047-119">However, if ETW logging is turned on while the process is executing, additional information is needed about the process.</span></span> <span data-ttu-id="81047-120">Na przykład w przypadku rozpoznawania symboli trzeba rejestrować zdarzenia metody dla metod, które zostały już załadowane przed włączeniem rejestrowania.</span><span class="sxs-lookup"><span data-stu-id="81047-120">For example, for symbol resolution you have to log method events for methods that were already loaded before logging was turned on.</span></span>  
  
 <span data-ttu-id="81047-121">Zdarzenia `DCStart` i`DCEnd` przechwytują stan procesu, gdy zbieranie danych zostało rozpoczęte i zatrzymane.</span><span class="sxs-lookup"><span data-stu-id="81047-121">The `DCStart` and `DCEnd` events capture the state of the process when data collection was started and stopped.</span></span> <span data-ttu-id="81047-122">(Stan dotyczy informacji na wysokim poziomie, w tym metod, które zostały skompilowane wcześniej (JIT) i zestawów, które zostały załadowane.) Te dwa zdarzenia mogą dostarczyć informacji na temat tego, co się już stało w procesie; na przykład, które metody zostały skompilowane JIT i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="81047-122">(State refers to information at a high level, including the methods that were already just-in-time (JIT) compiled and assemblies that were loaded.) These two events can provide information about what has already happened in the process; for example, which methods were JIT- compiled, and so on.</span></span>  
  
 <span data-ttu-id="81047-123">Tylko `DC`zdarzenia z `DCStart` ,`DCEnd`, lub`DCInit` w ich nazwach są zgłaszane w ramach dostawcy uwalniania.</span><span class="sxs-lookup"><span data-stu-id="81047-123">Only the events with `DC`, `DCStart`, `DCEnd`, or `DCInit` in their names are raised under the rundown provider.</span></span> <span data-ttu-id="81047-124">Ponadto te zdarzenia są wywoływane tylko w ramach dostawcy uwalniania.</span><span class="sxs-lookup"><span data-stu-id="81047-124">Additionally, these events are raised only under the rundown provider.</span></span>  
  
 <span data-ttu-id="81047-125">Oprócz filtrów słów kluczowych zdarzenia dostawca uwalniania obsługuje `StartRundownKeyword` również słowa kluczowe i `EndRundownKeyword` , aby zapewnić filtrowanie.</span><span class="sxs-lookup"><span data-stu-id="81047-125">In addition to the event keyword filters, the rundown provider also supports the `StartRundownKeyword` and `EndRundownKeyword` keywords to provide targeted filtering.</span></span>  
  
### <a name="start-rundown"></a><span data-ttu-id="81047-126">Rozpocznij uwalnianie</span><span class="sxs-lookup"><span data-stu-id="81047-126">Start Rundown</span></span>  
 <span data-ttu-id="81047-127">Rozpocznij uwalnianie jest wyzwalane, `StartRundownKeyword` gdy rejestrowanie w ramach dostawcy uwalniania jest włączone ze słowem kluczowym.</span><span class="sxs-lookup"><span data-stu-id="81047-127">A start rundown is triggered when logging under the rundown provider is enabled with the `StartRundownKeyword` keyword.</span></span> <span data-ttu-id="81047-128">Powoduje to wygenerowanie `DCStart` zdarzenia i przechwycenie stanu systemu.</span><span class="sxs-lookup"><span data-stu-id="81047-128">This causes the `DCStart` event to be raised, and captures the state of the system.</span></span> <span data-ttu-id="81047-129">Przed rozpoczęciem wyliczania `DCStartInit` zdarzenie jest zgłaszane.</span><span class="sxs-lookup"><span data-stu-id="81047-129">Before the start of the enumeration, the `DCStartInit` event is raised.</span></span> <span data-ttu-id="81047-130">Na końcu wyliczenia `DCStartComplete` zdarzenie jest zgłaszane w celu powiadomienia kontrolera, że zbieranie danych zostało zakończone normalnie.</span><span class="sxs-lookup"><span data-stu-id="81047-130">At the end of the enumeration, the `DCStartComplete` event is raised to notify the controller that data collection terminated normally.</span></span>  
  
### <a name="end-rundown"></a><span data-ttu-id="81047-131">Zakończ uwalnianie</span><span class="sxs-lookup"><span data-stu-id="81047-131">End Rundown</span></span>  
 <span data-ttu-id="81047-132">Zakończenie uwalniania jest wyzwalane, `EndRundownKeyword` gdy rejestrowanie w ramach dostawcy uwalniania jest włączone ze słowem kluczowym.</span><span class="sxs-lookup"><span data-stu-id="81047-132">An end rundown is triggered when logging under the rundown provider is enabled with the `EndRundownKeyword` keyword.</span></span> <span data-ttu-id="81047-133">Zakończenie końca spowoduje zatrzymanie profilowania dla procesu, który jest nadal wykonywany.</span><span class="sxs-lookup"><span data-stu-id="81047-133">End rundown stops profiling on a process that continues to execute.</span></span> <span data-ttu-id="81047-134">`DCEnd` Zdarzenia przechwytują stan systemu, gdy profilowanie zostało zatrzymane.</span><span class="sxs-lookup"><span data-stu-id="81047-134">The `DCEnd` events capture the state of the system when profiling is stopped.</span></span>  
  
 <span data-ttu-id="81047-135">Przed rozpoczęciem wyliczania `DCEndInit` zdarzenie jest zgłaszane.</span><span class="sxs-lookup"><span data-stu-id="81047-135">Before the start of the enumeration, the `DCEndInit` event is raised.</span></span> <span data-ttu-id="81047-136">Na końcu wyliczenia `DCEndComplete` zdarzenie jest zgłaszane w celu powiadomienia konsumenta, że zbieranie danych zostało zakończone normalnie.</span><span class="sxs-lookup"><span data-stu-id="81047-136">At the end of the enumeration, the `DCEndComplete` event is raised to notify the consumer that data collection terminated normally.</span></span> <span data-ttu-id="81047-137">Początkowy i końcowy uwalnianie są używane głównie do rozpoznawania symboli zarządzanych.</span><span class="sxs-lookup"><span data-stu-id="81047-137">Start rundown and end rundown are primarily used for managed symbol resolution.</span></span> <span data-ttu-id="81047-138">Rozpocznij uwalnianie może dostarczyć informacji o zakresie adresów dla metod, które zostały już skompilowane JIT przed uruchomieniem sesji profilowania.</span><span class="sxs-lookup"><span data-stu-id="81047-138">Start rundown can provide address range information for methods that were already JIT-compiled before the profiling session was started.</span></span> <span data-ttu-id="81047-139">Końcowa kompilacja może dostarczyć informacji o zakresie adresów dla wszystkich metod, które zostały skompilowane JIT, gdy profilowanie ma zostać wyłączone.</span><span class="sxs-lookup"><span data-stu-id="81047-139">End rundown can provide address range information for all methods that have been JIT-compiled when profiling is about to be turned off.</span></span>  
  
 <span data-ttu-id="81047-140">Zakończenie uwalniania nie odbywa się automatycznie, gdy sesja profilowania zostanie zatrzymana.</span><span class="sxs-lookup"><span data-stu-id="81047-140">End rundown does not happen automatically when a profiling session is stopped.</span></span> <span data-ttu-id="81047-141">Zamiast tego narzędzie, które poszukuje funkcji rozpoznawania symboli zarządzanych, musi jawnie wywołać sesję dostawcy uwalniania środowiska CLR z `EndRundownKeyword` włączonym słowem kluczowym tuż przed zatrzymaniem profilowania.</span><span class="sxs-lookup"><span data-stu-id="81047-141">Instead, a tool that is seeking to perform managed symbol resolution has to explicitly invoke a CLR rundown provider session with the `EndRundownKeyword` keyword enabled, just before profiling is stopped.</span></span>  
  
 <span data-ttu-id="81047-142">Chociaż parametr początkowy lub końcowy uwalniania może dostarczyć informacji o zakresie adresów metody dla rozdzielczości symboli zarządzanych, zalecamy użycie `EndRundownKeyword` słowa kluczowego (które dostarcza `DCEnd` `StartRundownKeyword` zdarzenia) zamiast słowa kluczowego (które zdarzenia `DCStart` dotyczące dostaw).</span><span class="sxs-lookup"><span data-stu-id="81047-142">Although either start rundown or end rundown can provide method address range information for managed symbol resolution, we recommend that you use the `EndRundownKeyword` keyword (which supplies `DCEnd` events) instead of the `StartRundownKeyword` keyword (which supplies `DCStart` events).</span></span> <span data-ttu-id="81047-143">Użycie `StartRundownKeyword` powoduje, że podczas sesji profilowania zostanie wykonane uwalnianie, co może zakłócać profilowany scenariusz.</span><span class="sxs-lookup"><span data-stu-id="81047-143">Using `StartRundownKeyword` causes the rundown to happen during the profiling session, which may disturb the profiled scenario.</span></span>  
  
## <a name="etw-data-collection-using-runtime-and-rundown-providers"></a><span data-ttu-id="81047-144">Zbieranie danych funkcji ETW przy użyciu dostawców środowiska uruchomieniowego i uwalniania</span><span class="sxs-lookup"><span data-stu-id="81047-144">ETW Data Collection Using Runtime and Rundown Providers</span></span>  
 <span data-ttu-id="81047-145">W poniższym przykładzie pokazano, jak używać dostawcy uwalniania środowiska CLR w taki sposób, aby umożliwiać Rozpoznawanie symboli zarządzanych procesów z minimalnym wpływem, niezależnie od tego, czy procesy są uruchamiane, czy kończyć się w oknie profilowanym.</span><span class="sxs-lookup"><span data-stu-id="81047-145">The following example demonstrates how to use the CLR rundown provider in a way that allows symbol resolution of managed processes with minimal impact, regardless of whether the processes start or end inside or outside the profiled window.</span></span>  
  
1. <span data-ttu-id="81047-146">Włącz rejestrowanie funkcji ETW przy użyciu dostawcy środowiska uruchomieniowego CLR:</span><span class="sxs-lookup"><span data-stu-id="81047-146">Turn on ETW logging by using the CLR runtime provider:</span></span>  
  
    ```console
    xperf -start clr -on e13c0d23-ccbc-4e12-931b-d9cc2eee27e4:0x1CCBD:0x5 -f clr1.etl      
    ```  
  
     <span data-ttu-id="81047-147">Dziennik zostanie zapisany w pliku pliku clr1. etl.</span><span class="sxs-lookup"><span data-stu-id="81047-147">The log will be saved to the clr1.etl file.</span></span>  
  
2. <span data-ttu-id="81047-148">Aby zatrzymać profilowanie, gdy proces jest kontynuowany, uruchom dostawcę uwalniania, aby `DCEnd` przechwycić zdarzenia:</span><span class="sxs-lookup"><span data-stu-id="81047-148">To stop profiling while the process continues to execute, start the rundown provider to capture the `DCEnd` events:</span></span>  
  
    ```console
    xperf -start clrRundown -on A669021C-C450-4609-A035-5AF59AF4DF18:0xB8:0x5 -f clr2.etl      
    ```  
  
     <span data-ttu-id="81047-149">Umożliwia to zbieranie `DCEnd` zdarzeń w celu uruchomienia sesji uwalniania.</span><span class="sxs-lookup"><span data-stu-id="81047-149">This enables the collection of `DCEnd` events to start a rundown session.</span></span> <span data-ttu-id="81047-150">W przypadku wszystkich zdarzeń, które mają być zbierane, może być konieczne odczekanie od 30 do 60 sekund.</span><span class="sxs-lookup"><span data-stu-id="81047-150">You may need to wait 30 to 60 seconds for all events to be collected.</span></span> <span data-ttu-id="81047-151">Dziennik zostanie zapisany w pliku pliku clr1. ET2.</span><span class="sxs-lookup"><span data-stu-id="81047-151">The log will be saved to the clr1.et2 file.</span></span>  
  
3. <span data-ttu-id="81047-152">Wyłącz wszystkie profilowania ETW:</span><span class="sxs-lookup"><span data-stu-id="81047-152">Turn off all ETW profiling:</span></span>  
  
    ```console
    xperf -stop clrRundown   
    xperf -stop clr  
    ```  
  
4. <span data-ttu-id="81047-153">Scal profile, aby utworzyć jeden plik dziennika:</span><span class="sxs-lookup"><span data-stu-id="81047-153">Merge the profiles to create one log file:</span></span>  
  
    ```console
    xperf -merge clr1.etl clr2.etl merged.etl  
    ```  
  
     <span data-ttu-id="81047-154">Scalony plik. etl będzie zawierać zdarzenia z sesji dostawcy środowiska uruchomieniowego i.</span><span class="sxs-lookup"><span data-stu-id="81047-154">The merged.etl file will contain the events from the runtime and the rundown provider sessions.</span></span>  
  
 <span data-ttu-id="81047-155">Za pomocą narzędzia można wykonać kroki 2 i 3 (rozpoczęcie sesji uwalniania, a następnie zakończenie profilowania) zamiast natychmiastowego wyłączenia profilowania, gdy użytkownik zażąda zatrzymania profilowania.</span><span class="sxs-lookup"><span data-stu-id="81047-155">A tool can execute steps 2 and 3 (starting a rundown session and then terminating profiling) instead of immediately turning off profiling when a user requests profiling to be stopped.</span></span> <span data-ttu-id="81047-156">Narzędzie może również wykonać krok 4.</span><span class="sxs-lookup"><span data-stu-id="81047-156">A tool can also execute step 4.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="81047-157">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="81047-157">See also</span></span>

- [<span data-ttu-id="81047-158">Zdarzenia ETW w środowisku CLR</span><span class="sxs-lookup"><span data-stu-id="81047-158">ETW Events in the Common Language Runtime</span></span>](etw-events-in-the-common-language-runtime.md)
