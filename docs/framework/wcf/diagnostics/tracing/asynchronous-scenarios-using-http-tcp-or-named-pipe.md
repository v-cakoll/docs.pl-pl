---
title: Scenariusze asynchroniczne z zastosowaniem protokołu HTTP lub TCP albo potoku nazwanego
ms.date: 03/30/2017
ms.assetid: a4d62402-43a4-48a4-9ced-220633ebc4ce
ms.openlocfilehash: d08f70186a59b8717c4441167ee720ba1c20b9dc
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/23/2019
ms.locfileid: "61998293"
---
# <a name="asynchronous-scenarios-using-http-tcp-or-named-pipe"></a><span data-ttu-id="7ae7c-102">Scenariusze asynchroniczne z zastosowaniem protokołu HTTP lub TCP albo potoku nazwanego</span><span class="sxs-lookup"><span data-stu-id="7ae7c-102">Asynchronous Scenarios using HTTP, TCP, or Named-Pipe</span></span>
<span data-ttu-id="7ae7c-103">W tym temacie opisano działania i transferów w scenariuszach różnych asynchroniczne żądanie/nietypizowana odpowiedź z żądaniami wielowątkowe przy użyciu protokołu HTTP, TCP, lub nazwany potok.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-103">This topic describes the activities and transfers for different asynchronous request/reply scenarios, with multithreaded requests using HTTP, TCP, or named pipe.</span></span>  
  
## <a name="asynchronous-requestreply-without-errors"></a><span data-ttu-id="7ae7c-104">Asynchroniczne żądanie/nietypizowana odpowiedź bez błędów</span><span class="sxs-lookup"><span data-stu-id="7ae7c-104">Asynchronous Request/Reply without Errors</span></span>  
 <span data-ttu-id="7ae7c-105">W tej sekcji opisano działania i transferów w scenariuszu asynchroniczne żądanie/nietypizowana odpowiedź z klientami wielowątkowych.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-105">This section describes the activities and transfers for an asynchronous request/reply scenario, with multithreaded clients.</span></span>  
  
 <span data-ttu-id="7ae7c-106">Działanie obiekt wywołujący skończy się, gdy `beginCall` zwróci wartość, a `endCall` zwraca.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-106">The caller activity terminates when `beginCall` returns, and `endCall` returns.</span></span> <span data-ttu-id="7ae7c-107">Jeśli wywołanie zwrotne jest wywoływana, zwraca wartość wywołania zwrotnego.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-107">If a callback is called, the callback returns.</span></span>  
  
 <span data-ttu-id="7ae7c-108">Działania o nazwie skończy się, gdy `beginCall` zwraca, `endCall` zwróci wartość, lub gdy wywołania zwrotnego zwraca, jeśli została wywołana z danego działania.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-108">The called activity terminates when `beginCall` returns, `endCall` returns, or when the callback returns if it was called from that activity.</span></span>  
  
### <a name="asynchronous-client-without-callback"></a><span data-ttu-id="7ae7c-109">Asynchroniczne klienta bez wywołania zwrotnego</span><span class="sxs-lookup"><span data-stu-id="7ae7c-109">Asynchronous Client without Callback</span></span>  
  
#### <a name="propagation-is-enabled-on-both-sides-using-http"></a><span data-ttu-id="7ae7c-110">Propagacja jest włączona po obu stronach, przy użyciu protokołu HTTP</span><span class="sxs-lookup"><span data-stu-id="7ae7c-110">Propagation is Enabled on Both Sides, using HTTP</span></span>  
 <span data-ttu-id="7ae7c-111">![Scenariusze asynchroniczne](../../../../../docs/framework/wcf/diagnostics/tracing/media/asyn1.gif "Asyn1")</span><span class="sxs-lookup"><span data-stu-id="7ae7c-111">![Asynchronous scenarios](../../../../../docs/framework/wcf/diagnostics/tracing/media/asyn1.gif "Asyn1")</span></span>  
  
 <span data-ttu-id="7ae7c-112">Rysunek 1.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-112">Figure 1.</span></span> <span data-ttu-id="7ae7c-113">Asynchroniczne klienta, bez wywołania zwrotnego `propagateActivity` = `true` po obu stronach, HTTP</span><span class="sxs-lookup"><span data-stu-id="7ae7c-113">Asynchronous client, no callback, `propagateActivity`=`true` on both sides, HTTP</span></span>  
  
 <span data-ttu-id="7ae7c-114">Jeśli `propagateActivity` = `true`, ProcessMessage wskazuje, które działania ProcessAction przesyłać do.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-114">If `propagateActivity`=`true`, ProcessMessage indicates which ProcessAction activity to transfer to.</span></span>  
  
 <span data-ttu-id="7ae7c-115">W przypadku scenariuszy opartych na protokole HTTP ReceiveBytes jest wywoływane na pierwszy komunikat do wysłania i istnieje okres istnienia żądania.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-115">For HTTP-based scenarios, ReceiveBytes is invoked on the first message to send, and exists for the lifetime of the request.</span></span>  
  
#### <a name="propagation-is-disabled-on-either-sides-using-http"></a><span data-ttu-id="7ae7c-116">Propagacja jest wyłączona na stronach albo przy użyciu protokołu HTTP</span><span class="sxs-lookup"><span data-stu-id="7ae7c-116">Propagation is Disabled on Either Sides, using HTTP</span></span>  
 <span data-ttu-id="7ae7c-117">Jeśli `propagateActivity` = `false` po obu stronach ProcessMessage nie wskazuje, które działania ProcessAction przesyłać do.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-117">If `propagateActivity`=`false` on either side, ProcessMessage does not indicate which ProcessAction activity to transfer to.</span></span> <span data-ttu-id="7ae7c-118">W związku z tym nowe działanie ProcessAction tymczasowe z nowym Identyfikatorem jest wywoływana.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-118">Therefore, a new temporary ProcessAction activity with a new ID is invoked.</span></span> <span data-ttu-id="7ae7c-119">Po dopasowaniu asynchronicznych odpowiedzi na żądanie w modelu ServiceModel kodu, identyfikator działania można pobrać z kontekstu lokalnego.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-119">When the asynchronous response is matched to the request in ServiceModel code, the Activity ID can be retrieved from the local context.</span></span> <span data-ttu-id="7ae7c-120">Rzeczywiste działanie ProcessAction mogą zostać przeniesione do za pomocą tego identyfikatora.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-120">The actual ProcessAction activity can be transferred to with that ID.</span></span>  
  
 <span data-ttu-id="7ae7c-121">![Scenariusze asynchroniczne z zastosowaniem protokołu HTTP&#47;TCP&#47;nazwanego potoku](../../../../../docs/framework/wcf/diagnostics/tracing/media/async2.gif "Async2")</span><span class="sxs-lookup"><span data-stu-id="7ae7c-121">![Asynchronous scenarios using HTTP&#47;TCP&#47;Named Pipe](../../../../../docs/framework/wcf/diagnostics/tracing/media/async2.gif "Async2")</span></span>  
  
 <span data-ttu-id="7ae7c-122">Rysunek 2.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-122">Figure 2.</span></span> <span data-ttu-id="7ae7c-123">Asynchroniczne klienta, bez wywołania zwrotnego `propagateActivity` = `false` po obu stronach HTTP</span><span class="sxs-lookup"><span data-stu-id="7ae7c-123">Asynchronous client, no callback, `propagateActivity`=`false` on either side, HTTP</span></span>  
  
 <span data-ttu-id="7ae7c-124">W przypadku scenariuszy opartych na protokole HTTP ReceiveBytes jest wywoływane na pierwszy komunikat do wysłania i istnieje okres istnienia żądania.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-124">For HTTP-based scenarios, ReceiveBytes is invoked on the first message to send, and exists for the lifetime of the request.</span></span>  
  
 <span data-ttu-id="7ae7c-125">Działanie procesu akcji jest tworzony na komputerze klienckim asynchronicznego podczas `propagateActivity` = `false` wywołujący lub wywoływany i gdy komunikat odpowiedzi zawiera nagłówek akcji.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-125">A Process Action activity is created on an asynchronous client when `propagateActivity`=`false` at the caller or callee, and when the response message does not include an Action header.</span></span>  
  
#### <a name="propagation-is-enabled-on-both-sides-using-tcp-or-named-pipe"></a><span data-ttu-id="7ae7c-126">Propagacja jest włączona po obu stronach, przy użyciu protokołu TCP lub nazwanego potoku</span><span class="sxs-lookup"><span data-stu-id="7ae7c-126">Propagation is Enabled on Both Sides, using TCP or Named Pipe</span></span>  
 <span data-ttu-id="7ae7c-127">![Scenariusze asynchroniczne z zastosowaniem protokołu HTTP&#47;TCP&#47;nazwanego potoku](../../../../../docs/framework/wcf/diagnostics/tracing/media/async3.gif "Async3")</span><span class="sxs-lookup"><span data-stu-id="7ae7c-127">![Asynchronous scenarios using HTTP&#47;TCP&#47;Named Pipe](../../../../../docs/framework/wcf/diagnostics/tracing/media/async3.gif "Async3")</span></span>  
  
 <span data-ttu-id="7ae7c-128">Rysunek 3.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-128">Figure 3.</span></span> <span data-ttu-id="7ae7c-129">Asynchroniczne klienta, bez wywołania zwrotnego `propagateActivity` = `true` po obu stronach nazwanych-potoku/TCP</span><span class="sxs-lookup"><span data-stu-id="7ae7c-129">Asynchronous client, no callback, `propagateActivity`=`true` on both sides, Named-Pipe/TCP</span></span>  
  
 <span data-ttu-id="7ae7c-130">W scenariuszu nazwanego lub oparte na protokole TCP ReceiveBytes jest wywoływane, gdy klient jest otwarty i istnieje okres istnienia połączenia.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-130">For a Named-Pipe or TCP-based scenario, ReceiveBytes is invoked when the client is opened, and exists for the lifetime of the connection.</span></span>  
  
 <span data-ttu-id="7ae7c-131">Podobnie jak rysunek 1, jeśli `propagateActivity` = `true`, ProcessMessage wskazuje, które działania ProcessAction przesyłać do.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-131">Similar to Figure 1, if `propagateActivity`=`true`, ProcessMessage indicates which ProcessAction activity to transfer to.</span></span>  
  
#### <a name="propagation-is-disabled-on-either-sides-using-tcp-or-named-pipe"></a><span data-ttu-id="7ae7c-132">Propagacja jest wyłączona na stronach albo przy użyciu protokołu TCP lub nazwanego potoku</span><span class="sxs-lookup"><span data-stu-id="7ae7c-132">Propagation is Disabled on Either Sides, using TCP or Named Pipe</span></span>  
 <span data-ttu-id="7ae7c-133">W scenariuszu nazwanego lub oparte na protokole TCP ReceiveBytes jest wywoływane, gdy klient jest otwarty i istnieje okres istnienia połączenia.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-133">For a Named-Pipe or TCP-based scenario, ReceiveBytes is invoked when the client is opened, and exists for the lifetime of the connection.</span></span>  
  
 <span data-ttu-id="7ae7c-134">Podobnie jak Fig.2, jeśli `propagateActivity` = `false` po obu stronach ProcessMessage nie wskazuje, które działania ProcessAction przesyłać do.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-134">Similar to Fig.2, If `propagateActivity`=`false` on either side, ProcessMessage does not indicate which ProcessAction activity to transfer to.</span></span> <span data-ttu-id="7ae7c-135">W związku z tym nowe działanie ProcessAction tymczasowe z nowym Identyfikatorem jest wywoływana.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-135">Therefore, a new temporary ProcessAction activity with a new ID is invoked.</span></span> <span data-ttu-id="7ae7c-136">Po dopasowaniu asynchronicznych odpowiedzi na żądanie w modelu ServiceModel kodu, identyfikator działania można pobrać z kontekstu lokalnego.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-136">When the asynchronous response is matched to the request in ServiceModel code, the Activity ID can be retrieved from the local context.</span></span> <span data-ttu-id="7ae7c-137">Rzeczywiste działanie ProcessAction mogą zostać przeniesione do za pomocą tego identyfikatora.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-137">The actual ProcessAction activity can be transferred to with that ID.</span></span>  
  
 <span data-ttu-id="7ae7c-138">![Scenariusze asynchroniczne z zastosowaniem protokołu HTTP&#47;TCP&#47; potoków nazwanych](../../../../../docs/framework/wcf/diagnostics/tracing/media/async4.gif "Async4")</span><span class="sxs-lookup"><span data-stu-id="7ae7c-138">![Asynchronous scenarios using HTTP&#47;TCP&#47; Named Pipes](../../../../../docs/framework/wcf/diagnostics/tracing/media/async4.gif "Async4")</span></span>  
  
 <span data-ttu-id="7ae7c-139">Rysunek 4.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-139">Figure 4.</span></span> <span data-ttu-id="7ae7c-140">Asynchroniczne klienta, bez wywołania zwrotnego `propagateActivity` = `false` po obu stronach nazwanych-potoku/TCP</span><span class="sxs-lookup"><span data-stu-id="7ae7c-140">Asynchronous client, no callback, `propagateActivity`=`false` on either side, Named-Pipe/TCP</span></span>  
  
### <a name="asynchronous-client-with-callback"></a><span data-ttu-id="7ae7c-141">Asynchroniczne klienta przy użyciu wywołania zwrotnego</span><span class="sxs-lookup"><span data-stu-id="7ae7c-141">Asynchronous client with Callback</span></span>  
 <span data-ttu-id="7ae7c-142">W tym scenariuszu dodaje działań, G i A ", wywołania zwrotnego i `endCall`i ich transferu we/wy.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-142">This scenario adds activities G and A’, for the callback and `endCall`, and their transfers in/out.</span></span>  
  
 <span data-ttu-id="7ae7c-143">W tej sekcji przedstawiono tylko przy użyciu protokołu HTTP z `propragateActivity` = `true`.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-143">This section only demonstrates using HTTP with `propragateActivity`=`true`.</span></span> <span data-ttu-id="7ae7c-144">Jednak dodatkowe działania i transfery dotyczą również innych przypadkach (czyli `propagateActivity` = `false`, przy użyciu lub TCP albo potoku nazwanego).</span><span class="sxs-lookup"><span data-stu-id="7ae7c-144">However, the additional activities and transfers also apply to the other cases (that is, `propagateActivity`=`false`, using TCP or Named-Pipe).</span></span>  
  
 <span data-ttu-id="7ae7c-145">Wywołanie zwrotne tworzy nowe działanie (G), gdy klient wywołuje kod użytkownika, aby powiadomić, że wyniki są gotowe.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-145">The callback creates a new activity (G) when the client calls user code to notify that results are ready.</span></span> <span data-ttu-id="7ae7c-146">Kod użytkownika wywołuje `endCall` w ramach wywołania zwrotnego (jak pokazano na rysunku 5) lub poza wywołanie zwrotne (rysunek 6).</span><span class="sxs-lookup"><span data-stu-id="7ae7c-146">User code then calls `endCall` within the callback (as shown in Figure 5) or outside the callback (Figure 6).</span></span> <span data-ttu-id="7ae7c-147">Ponieważ nie jest znany aktywności użytkownika, które `endCall` jest wywoływana, to działanie jest oznaczona etykietą `A’`.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-147">Because it is not known which user activity `endCall` is being called from, this activity is labeled `A’`.</span></span> <span data-ttu-id="7ae7c-148">Istnieje możliwość, A "może być taka sama jak lub różni się od A.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-148">It is possible that A’ can be identical to or different from A.</span></span>  
  
 <span data-ttu-id="7ae7c-149">![Scenariusze asynchroniczne](../../../../../docs/framework/wcf/diagnostics/tracing/media/asynccallback1.gif "AsyncCallback1")</span><span class="sxs-lookup"><span data-stu-id="7ae7c-149">![Asynchronous scenarios](../../../../../docs/framework/wcf/diagnostics/tracing/media/asynccallback1.gif "AsyncCallback1")</span></span>  
  
 <span data-ttu-id="7ae7c-150">Rysunek 5.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-150">Figure 5.</span></span> <span data-ttu-id="7ae7c-151">Asynchroniczne klienta z wywołaniem zwrotnym, `endCall` w wywołania zwrotnego</span><span class="sxs-lookup"><span data-stu-id="7ae7c-151">Asynchronous client with callback, `endCall` in Callback</span></span>  
  
 <span data-ttu-id="7ae7c-152">![Scenariusze asynchroniczne](../../../../../docs/framework/wcf/diagnostics/tracing/media/asynccallback2.gif "AsyncCallback2")</span><span class="sxs-lookup"><span data-stu-id="7ae7c-152">![Asynchronous Scenarios](../../../../../docs/framework/wcf/diagnostics/tracing/media/asynccallback2.gif "AsyncCallback2")</span></span>  
  
 <span data-ttu-id="7ae7c-153">Rysunek 6.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-153">Figure 6.</span></span> <span data-ttu-id="7ae7c-154">Asynchroniczne klienta z wywołaniem zwrotnym, `endCall` poza wywołania zwrotnego</span><span class="sxs-lookup"><span data-stu-id="7ae7c-154">Asynchronous client with callback, `endCall` outside of Callback</span></span>  
  
### <a name="asynchronous-server-with-callback"></a><span data-ttu-id="7ae7c-155">Asynchroniczne serwera przy użyciu wywołania zwrotnego</span><span class="sxs-lookup"><span data-stu-id="7ae7c-155">Asynchronous Server with Callback</span></span>  
 <span data-ttu-id="7ae7c-156">![Scenariusze asynchroniczne z zastosowaniem protokołu HTTP&#47;TCP&#47; nazwanych&#45;potoku](../../../../../docs/framework/wcf/diagnostics/tracing/media/aynchserver.gif "AynchServer")</span><span class="sxs-lookup"><span data-stu-id="7ae7c-156">![Asynchronous scenarios using HTTP&#47;TCP&#47; Named&#45;Pipe](../../../../../docs/framework/wcf/diagnostics/tracing/media/aynchserver.gif "AynchServer")</span></span>  
  
 <span data-ttu-id="7ae7c-157">Rysunek 7.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-157">Figure 7.</span></span> <span data-ttu-id="7ae7c-158">Asynchroniczne serwera, za pomocą wywołania zwrotnego</span><span class="sxs-lookup"><span data-stu-id="7ae7c-158">Asynchronous server, with callback</span></span>  
  
 <span data-ttu-id="7ae7c-159">Stos kanału wywołuje klienta na Message Receive: danych śledzenia dla tego przetwarzania są emitowane w ProcessRequest działania.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-159">The channel stack calls back the client on Message Receive: traces for this processing are emitted in the ProcessRequest activity itself.</span></span>  
  
## <a name="asynchronous-requestreply-with-errors"></a><span data-ttu-id="7ae7c-160">Asynchroniczne żądanie/nietypizowana odpowiedź z błędami</span><span class="sxs-lookup"><span data-stu-id="7ae7c-160">Asynchronous Request/Reply with Errors</span></span>  
 <span data-ttu-id="7ae7c-161">Błędy wiadomości są odbierane podczas `endCall`.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-161">Fault message errors are received during `endCall`.</span></span> <span data-ttu-id="7ae7c-162">W przeciwnym razie działań i transfery są podobne do poprzednich scenariuszach.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-162">Otherwise, activities and transfers are similar to previous scenarios.</span></span>  
  
## <a name="asynchronous-one-way-with-or-without-errors"></a><span data-ttu-id="7ae7c-163">Asynchroniczne jednokierunkowe, z lub bez błędów</span><span class="sxs-lookup"><span data-stu-id="7ae7c-163">Asynchronous One-Way with or without Errors</span></span>  
 <span data-ttu-id="7ae7c-164">Nie odpowiedzi lub błąd jest zwracany do klienta.</span><span class="sxs-lookup"><span data-stu-id="7ae7c-164">No response or fault is returned to the client.</span></span>
