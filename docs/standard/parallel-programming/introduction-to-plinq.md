---
title: Wprowadzenie do PLINQ
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- PLINQ queries, introduction to
ms.assetid: eaa720d8-8999-4eb7-8df5-3c19ca61cad0
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 39ca7ca02c2bb1050653daf1b53450533cc950dd
ms.sourcegitcommit: 155012a8a826ee8ab6aa49b1b3a3b532e7b7d9bd
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/04/2019
ms.locfileid: "66490972"
---
# <a name="introduction-to-plinq"></a><span data-ttu-id="02d8d-102">Wprowadzenie do PLINQ</span><span class="sxs-lookup"><span data-stu-id="02d8d-102">Introduction to PLINQ</span></span>

## <a name="what-is-a-parallel-query"></a><span data-ttu-id="02d8d-103">Co to jest zapytanie równoległe?</span><span class="sxs-lookup"><span data-stu-id="02d8d-103">What is a Parallel Query?</span></span>

<span data-ttu-id="02d8d-104">Language-Integrated Query (LINQ) została wprowadzona w programie .NET Framework 3.5.</span><span class="sxs-lookup"><span data-stu-id="02d8d-104">Language-Integrated Query (LINQ) was introduced in the .NET Framework 3.5.</span></span> <span data-ttu-id="02d8d-105">Zawiera funkcje zunifikowany model przeznaczony do wykonywania zapytań o dowolne <xref:System.Collections.IEnumerable?displayProperty=nameWithType> lub <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType> źródła danych w sposób bezpieczny dla typu.</span><span class="sxs-lookup"><span data-stu-id="02d8d-105">It features a unified model for querying any <xref:System.Collections.IEnumerable?displayProperty=nameWithType> or <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType> data source in a type-safe manner.</span></span> <span data-ttu-id="02d8d-106">LINQ to Objects do nazwa zapytania LINQ, które są uruchamiane na kolekcjach w pamięci, takich jak <xref:System.Collections.Generic.List%601> i tablice.</span><span class="sxs-lookup"><span data-stu-id="02d8d-106">LINQ to Objects is the name for LINQ queries that are run against in-memory collections such as <xref:System.Collections.Generic.List%601> and arrays.</span></span> <span data-ttu-id="02d8d-107">W tym artykule założono, że masz podstawową wiedzę na temat LINQ.</span><span class="sxs-lookup"><span data-stu-id="02d8d-107">This article assumes that you have a basic understanding of LINQ.</span></span> <span data-ttu-id="02d8d-108">Aby uzyskać więcej informacji, zobacz [Language-Integrated Query (LINQ) - C# ](../../csharp/programming-guide/concepts/linq/index.md) lub [Language-Integrated Query (LINQ) - Visual Basic](../../visual-basic/programming-guide/concepts/linq/index.md).</span><span class="sxs-lookup"><span data-stu-id="02d8d-108">For more information, see [Language-Integrated Query (LINQ) - C#](../../csharp/programming-guide/concepts/linq/index.md) or [Language-Integrated Query (LINQ) - Visual Basic](../../visual-basic/programming-guide/concepts/linq/index.md).</span></span>

<span data-ttu-id="02d8d-109">Równoległe LINQ (PLINQ) to implementacja przetwarzania równoległego wzorzec LINQ.</span><span class="sxs-lookup"><span data-stu-id="02d8d-109">Parallel LINQ (PLINQ) is a parallel implementation of the LINQ pattern.</span></span> <span data-ttu-id="02d8d-110">Zapytanie PLINQ pod wieloma względami przypomina nierównoległy LINQ do kwerendy obiekty.</span><span class="sxs-lookup"><span data-stu-id="02d8d-110">A PLINQ query in many ways resembles a non-parallel LINQ to Objects query.</span></span> <span data-ttu-id="02d8d-111">Zapytania PLINQ, podobnie jak w przypadku sekwencyjnych [!INCLUDE[vbteclinq](../../../includes/vbteclinq-md.md)] zapytań, działają na wszystkie w pamięci <xref:System.Collections.IEnumerable> lub <xref:System.Collections.Generic.IEnumerable%601> danych źródła i mają wstrzymane wykonanie, co oznacza, że nie rozpocząć wykonywanie aż do wyliczenia zapytania.</span><span class="sxs-lookup"><span data-stu-id="02d8d-111">PLINQ queries, just like sequential [!INCLUDE[vbteclinq](../../../includes/vbteclinq-md.md)] queries, operate on any in-memory <xref:System.Collections.IEnumerable> or <xref:System.Collections.Generic.IEnumerable%601> data source, and have deferred execution, which means they do not begin executing until the query is enumerated.</span></span> <span data-ttu-id="02d8d-112">Podstawowa różnica polega na tym, że PLINQ próbuje w pełni wykorzystać wszystkie procesory w systemie.</span><span class="sxs-lookup"><span data-stu-id="02d8d-112">The primary difference is that PLINQ attempts to make full use of all the processors on the system.</span></span> <span data-ttu-id="02d8d-113">Odbywa się to poprzez podział źródła danych na segmenty i następnie wykonywanie zapytań w każdym segmencie na oddzielnych wątkach roboczych równolegle na wielu procesorach.</span><span class="sxs-lookup"><span data-stu-id="02d8d-113">It does this by partitioning the data source into segments, and then executing the query on each segment on separate worker threads in parallel on multiple processors.</span></span> <span data-ttu-id="02d8d-114">W wielu przypadkach wykonywanie równoległe oznacza, że zapytanie działa znacznie szybciej.</span><span class="sxs-lookup"><span data-stu-id="02d8d-114">In many cases, parallel execution means that the query runs significantly faster.</span></span>

<span data-ttu-id="02d8d-115">Za pomocą przetwarzania równoległego, PLINQ może osiągnąć znaczne ulepszenia wydajności w porównaniu ze starszym kodem dla niektórych rodzajów zapytań, często po prostu przez dodanie <xref:System.Linq.ParallelEnumerable.AsParallel%2A> operację do źródła danych zapytania.</span><span class="sxs-lookup"><span data-stu-id="02d8d-115">Through parallel execution, PLINQ can achieve significant performance improvements over legacy code for certain kinds of queries, often just by adding the <xref:System.Linq.ParallelEnumerable.AsParallel%2A> query operation to the data source.</span></span> <span data-ttu-id="02d8d-116">Jednakże równoległość może wprowadzać własne złożoności, a nie wszystkie operacje na zapytaniach działają szybciej w programie PLINQ.</span><span class="sxs-lookup"><span data-stu-id="02d8d-116">However, parallelism can introduce its own complexities, and not all query operations run faster in PLINQ.</span></span> <span data-ttu-id="02d8d-117">W rzeczywistości przetwarzanie równoległe faktycznie spowalnia niektóre zapytania.</span><span class="sxs-lookup"><span data-stu-id="02d8d-117">In fact, parallelization actually slows down certain queries.</span></span> <span data-ttu-id="02d8d-118">W związku z tym należy zrozumieć, jak problemy, takie jak zamawianie, wpływają na zapytania równolegle.</span><span class="sxs-lookup"><span data-stu-id="02d8d-118">Therefore, you should understand how issues such as ordering affect parallel queries.</span></span> <span data-ttu-id="02d8d-119">Aby uzyskać więcej informacji, zobacz [ogólne informacje o przyspieszeniach w PLINQ](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="02d8d-119">For more information, see [Understanding Speedup in PLINQ](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md).</span></span>

> [!NOTE]
> <span data-ttu-id="02d8d-120">Ta dokumentacja używa wyrażeń lambda do definiowania delegatów w PLINQ.</span><span class="sxs-lookup"><span data-stu-id="02d8d-120">This documentation uses lambda expressions to define delegates in PLINQ.</span></span> <span data-ttu-id="02d8d-121">Jeśli nie znasz wyrażeń lambda w języku C# lub Visual Basic, zobacz [wyrażeń Lambda w PLINQ i TPL](../../../docs/standard/parallel-programming/lambda-expressions-in-plinq-and-tpl.md).</span><span class="sxs-lookup"><span data-stu-id="02d8d-121">If you are not familiar with lambda expressions in C# or Visual Basic, see [Lambda Expressions in PLINQ and TPL](../../../docs/standard/parallel-programming/lambda-expressions-in-plinq-and-tpl.md).</span></span>

<span data-ttu-id="02d8d-122">W dalszej części tego artykułu zawiera przegląd głównych klas PLINQ i w tym artykule omówiono sposób tworzenia zapytań PLINQ.</span><span class="sxs-lookup"><span data-stu-id="02d8d-122">The remainder of this article gives an overview of the main PLINQ classes, and discusses how to create PLINQ queries.</span></span> <span data-ttu-id="02d8d-123">Każda sekcja zawiera łącza do bardziej szczegółowych informacji i przykładów kodu.</span><span class="sxs-lookup"><span data-stu-id="02d8d-123">Each section contains links to more detailed information and code examples.</span></span>

## <a name="the-parallelenumerable-class"></a><span data-ttu-id="02d8d-124">Klasa ParallelEnumerable</span><span class="sxs-lookup"><span data-stu-id="02d8d-124">The ParallelEnumerable Class</span></span>

<span data-ttu-id="02d8d-125"><xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> Klasy ujawnia prawie wszystkie funkcje w PLINQ.</span><span class="sxs-lookup"><span data-stu-id="02d8d-125">The <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> class exposes almost all of PLINQ's functionality.</span></span> <span data-ttu-id="02d8d-126">Ten i pozostałe z <xref:System.Linq?displayProperty=nameWithType> typy przestrzenie nazw są kompilowane do zestawu System.Core.dll.</span><span class="sxs-lookup"><span data-stu-id="02d8d-126">It and the rest of the <xref:System.Linq?displayProperty=nameWithType> namespace types are compiled into the System.Core.dll assembly.</span></span> <span data-ttu-id="02d8d-127">Domyślne projekty C# i Visual Basic w programie Visual Studio odwołują się do zestawu i importują przestrzeń nazw.</span><span class="sxs-lookup"><span data-stu-id="02d8d-127">The default C# and Visual Basic projects in Visual Studio both reference the assembly and import the namespace.</span></span>

<span data-ttu-id="02d8d-128"><xref:System.Linq.ParallelEnumerable> zawiera implementacje wszystkich standardowych operatorów zapytań, które obsługuje LINQ to Objects, ale go nie podejmuje próby zrównoleglenia.</span><span class="sxs-lookup"><span data-stu-id="02d8d-128"><xref:System.Linq.ParallelEnumerable> includes implementations of all the standard query operators that LINQ to Objects supports, although it does not attempt to parallelize each one.</span></span> <span data-ttu-id="02d8d-129">Jeśli nie jesteś zaznajomiony z [!INCLUDE[vbteclinq](../../../includes/vbteclinq-md.md)], zobacz [wprowadzenie do LINQ (C#)](../../csharp/programming-guide/concepts/linq/index.md) i [wprowadzenie do LINQ (Visual Basic)](../../visual-basic/programming-guide/concepts/linq/introduction-to-linq.md).</span><span class="sxs-lookup"><span data-stu-id="02d8d-129">If you are not familiar with [!INCLUDE[vbteclinq](../../../includes/vbteclinq-md.md)], see [Introduction to LINQ (C#)](../../csharp/programming-guide/concepts/linq/index.md) and [Introduction to LINQ (Visual Basic)](../../visual-basic/programming-guide/concepts/linq/introduction-to-linq.md).</span></span>

<span data-ttu-id="02d8d-130">Oprócz standardowych operatorów zapytań <xref:System.Linq.ParallelEnumerable> klasa zawiera zestaw metod, które umożliwiają zachowania specyficzne dla wykonywania równoległego.</span><span class="sxs-lookup"><span data-stu-id="02d8d-130">In addition to the standard query operators, the <xref:System.Linq.ParallelEnumerable> class contains a set of methods that enable behaviors specific to parallel execution.</span></span> <span data-ttu-id="02d8d-131">Te metody specyficzne dla PLINQ są wymienione w poniższej tabeli.</span><span class="sxs-lookup"><span data-stu-id="02d8d-131">These PLINQ-specific methods are listed in the following table.</span></span>

|<span data-ttu-id="02d8d-132">ParallelEnumerable Operator</span><span class="sxs-lookup"><span data-stu-id="02d8d-132">ParallelEnumerable Operator</span></span>|<span data-ttu-id="02d8d-133">Opis</span><span class="sxs-lookup"><span data-stu-id="02d8d-133">Description</span></span>|
|---------------------------------|-----------------|
|<xref:System.Linq.ParallelEnumerable.AsParallel%2A>|<span data-ttu-id="02d8d-134">Punkt wejścia dla PLINQ.</span><span class="sxs-lookup"><span data-stu-id="02d8d-134">The entry point for PLINQ.</span></span> <span data-ttu-id="02d8d-135">Określa, że pozostała część zapytania powinna być przeprowadzana równolegle, jeśli jest to możliwe.</span><span class="sxs-lookup"><span data-stu-id="02d8d-135">Specifies that the rest of the query should be parallelized, if it is possible.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsSequential%2A>|<span data-ttu-id="02d8d-136">Określa, że pozostała część zapytania powinna być uruchamiana kolejno, jako nierównoległe zapytanie LINQ.</span><span class="sxs-lookup"><span data-stu-id="02d8d-136">Specifies that the rest of the query should be run sequentially, as a non-parallel LINQ query.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsOrdered%2A>|<span data-ttu-id="02d8d-137">Określa, że PLINQ powinno zachować kolejność sekwencji źródłowej rest zapytania lub do momentu kolejność ulegnie zmianie, na przykład przy użyciu klauzuli orderby (Order By w języku Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="02d8d-137">Specifies that PLINQ should preserve the ordering of the source sequence for the rest of the query, or until the ordering is changed, for example by the use of an orderby (Order By in Visual Basic) clause.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsUnordered%2A>|<span data-ttu-id="02d8d-138">Określa, że PLINQ dla pozostałej części zapytania nie jest wymagane dla zachowania kolejności sekwencji źródłowej.</span><span class="sxs-lookup"><span data-stu-id="02d8d-138">Specifies that PLINQ for the rest of the query is not required to preserve the ordering of the source sequence.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithCancellation%2A>|<span data-ttu-id="02d8d-139">Określa PLINQ powinno okresowo monitorować stan podanego tokenu anulowania i anulować wykonanie, jeśli żądanie.</span><span class="sxs-lookup"><span data-stu-id="02d8d-139">Specifies that PLINQ should periodically monitor the state of the provided cancellation token and cancel execution if it is requested.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A>|<span data-ttu-id="02d8d-140">Określa maksymalną liczbę procesorów, których PLINQ powinien używać do zrównoleglenia zapytania.</span><span class="sxs-lookup"><span data-stu-id="02d8d-140">Specifies the maximum number of processors that PLINQ should use to parallelize the query.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A>|<span data-ttu-id="02d8d-141">Udostępnia wskazówkę o tym, jak program PLINQ powinien, jeśli jest to możliwe, scalać wyniki przetwarzania równoległego do tylko jednej sekwencji w zużywającym wątku.</span><span class="sxs-lookup"><span data-stu-id="02d8d-141">Provides a hint about how PLINQ should, if it is possible, merge parallel results back into just one sequence on the consuming thread.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A>|<span data-ttu-id="02d8d-142">Określa, czy PLINQ powinien zrównoleglić zapytanie, nawet wtedy, gdy zachowanie domyślne będzie uruchamiać jej sekwencyjnie.</span><span class="sxs-lookup"><span data-stu-id="02d8d-142">Specifies whether PLINQ should parallelize the query even when the default behavior would be to run it sequentially.</span></span>|
|<xref:System.Linq.ParallelEnumerable.ForAll%2A>|<span data-ttu-id="02d8d-143">Metoda wyliczania wielowątkowego, w której w przeciwieństwie do iteracji wyników kwerendy, umożliwia przetwarzanie równoległe bez scalania powrotnego do wątku konsumenta.</span><span class="sxs-lookup"><span data-stu-id="02d8d-143">A multithreaded enumeration method that, unlike iterating over the results of the query, enables results to be processed in parallel without first merging back to the consumer thread.</span></span>|
|<span data-ttu-id="02d8d-144"><xref:System.Linq.ParallelEnumerable.Aggregate%2A> przeciążenie</span><span class="sxs-lookup"><span data-stu-id="02d8d-144"><xref:System.Linq.ParallelEnumerable.Aggregate%2A> overload</span></span>|<span data-ttu-id="02d8d-145">Przeciążenia, które są unikatowe dla PLINQ i umożliwia agregacje pośrednie w partycji lokalnej wątku, a także funkcję agregacji końcowej w celu połączenia wyników wszystkich partycji.</span><span class="sxs-lookup"><span data-stu-id="02d8d-145">An overload that is unique to PLINQ and enables intermediate aggregation over thread-local partitions, plus a final aggregation function to combine the results of all partitions.</span></span>|

## <a name="the-opt-in-model"></a><span data-ttu-id="02d8d-146">Model Opt-in</span><span class="sxs-lookup"><span data-stu-id="02d8d-146">The Opt-in Model</span></span>

<span data-ttu-id="02d8d-147">Podczas pisywania zapytania zgadzaj się na PLINQ przez wywołanie <xref:System.Linq.ParallelEnumerable.AsParallel%2A?displayProperty=nameWithType> metody rozszerzenia dla źródła danych, jak pokazano w poniższym przykładzie.</span><span class="sxs-lookup"><span data-stu-id="02d8d-147">When you write a query, opt in to PLINQ by invoking the <xref:System.Linq.ParallelEnumerable.AsParallel%2A?displayProperty=nameWithType> extension method on the data source, as shown in the following example.</span></span>

[!code-csharp[PLINQ#1](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#1)]
[!code-vb[PLINQ#1](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#1)]

<span data-ttu-id="02d8d-148"><xref:System.Linq.ParallelEnumerable.AsParallel%2A> — Metoda rozszerzenia wiąże operatory kolejnych zapytań, w tym przypadku `where` i `select`, <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> implementacji.</span><span class="sxs-lookup"><span data-stu-id="02d8d-148">The <xref:System.Linq.ParallelEnumerable.AsParallel%2A> extension method binds the subsequent query operators, in this case, `where` and `select`, to the <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> implementations.</span></span>

## <a name="execution-modes"></a><span data-ttu-id="02d8d-149">Tryby wykonywania</span><span class="sxs-lookup"><span data-stu-id="02d8d-149">Execution Modes</span></span>

<span data-ttu-id="02d8d-150">Domyślnie PLINQ jest Konserwatywny.</span><span class="sxs-lookup"><span data-stu-id="02d8d-150">By default, PLINQ is conservative.</span></span> <span data-ttu-id="02d8d-151">W czasie wykonywania infrastruktura PLINQ analizuje ogólną strukturę kwerendy.</span><span class="sxs-lookup"><span data-stu-id="02d8d-151">At run time, the PLINQ infrastructure analyzes the overall structure of the query.</span></span> <span data-ttu-id="02d8d-152">Jeśli zapytanie jest prawdopodobne szybsze przez przetwarzanie równoległe, program PLINQ dzieli sekwencję źródłową na zadania, które mogą być uruchamiane równolegle.</span><span class="sxs-lookup"><span data-stu-id="02d8d-152">If the query is likely to yield speedups by parallelization, PLINQ partitions the source sequence into tasks that can be run concurrently.</span></span> <span data-ttu-id="02d8d-153">Jeśli nie jest bezpieczne równoległe przetwarzanie zapytania, program PLINQ po prostu wykonuje zapytanie sekwencyjnie.</span><span class="sxs-lookup"><span data-stu-id="02d8d-153">If it is not safe to parallelize a query, PLINQ just runs the query sequentially.</span></span> <span data-ttu-id="02d8d-154">Jeśli narzędzie PLINQ ma wybór między potencjalnie kosztownym algorytmem równoległym lub niedrogim algorytmem sekwencyjnym, wybiera algorytm sekwencyjny domyślnie.</span><span class="sxs-lookup"><span data-stu-id="02d8d-154">If PLINQ has a choice between a potentially expensive parallel algorithm or an inexpensive sequential algorithm, it chooses the sequential algorithm by default.</span></span> <span data-ttu-id="02d8d-155">Możesz użyć <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> metody i <xref:System.Linq.ParallelExecutionMode?displayProperty=nameWithType> wyliczeniu, aby nakazać PLINQ wybrać algorytm równoległy.</span><span class="sxs-lookup"><span data-stu-id="02d8d-155">You can use the <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> method and the <xref:System.Linq.ParallelExecutionMode?displayProperty=nameWithType> enumeration to instruct PLINQ to select the parallel algorithm.</span></span> <span data-ttu-id="02d8d-156">Jest to przydatne, gdy wiadomo, polegająca na przetestowaniu i miary, która określone zapytanie jest równolegle wykonywane szybciej.</span><span class="sxs-lookup"><span data-stu-id="02d8d-156">This is useful when you know by testing and measurement that a particular query executes faster in parallel.</span></span> <span data-ttu-id="02d8d-157">Aby uzyskać więcej informacji, zobacz [jak: Określanie trybu wykonywania w PLINQ](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="02d8d-157">For more information, see [How to: Specify the Execution Mode in PLINQ](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md).</span></span>

## <a name="degree-of-parallelism"></a><span data-ttu-id="02d8d-158">Stopień równoległości</span><span class="sxs-lookup"><span data-stu-id="02d8d-158">Degree of Parallelism</span></span>

<span data-ttu-id="02d8d-159">Domyślnie PLINQ używa wszystkich procesorów na komputerze-hoście.</span><span class="sxs-lookup"><span data-stu-id="02d8d-159">By default, PLINQ uses all of the processors on the host computer.</span></span> <span data-ttu-id="02d8d-160">Możesz nakazać PLINQ używać nie więcej niż określoną liczbę procesorów za pomocą <xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="02d8d-160">You can instruct PLINQ to use no more than a specified number of processors by using the <xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A> method.</span></span> <span data-ttu-id="02d8d-161">Jest to przydatne, jeśli chcesz upewnić się, że inne procesy uruchomione na komputerze otrzymują określoną ilość czasu procesora CPU.</span><span class="sxs-lookup"><span data-stu-id="02d8d-161">This is useful when you want to make sure that other processes running on the computer receive a certain amount of CPU time.</span></span> <span data-ttu-id="02d8d-162">Poniższy urywek ogranicza zapytanie do wykorzystywania maksymalnie dwóch procesorów.</span><span class="sxs-lookup"><span data-stu-id="02d8d-162">The following snippet limits the query to utilizing a maximum of two processors.</span></span>

[!code-csharp[PLINQ#5](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinqsamples.cs#5)]
[!code-vb[PLINQ#5](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#5)]

<span data-ttu-id="02d8d-163">W przypadkach, gdzie zapytanie wykonuje znaczną ilość pracy bez powiązany obliczeniowych, takich jak We/Wy może być korzystne, aby określić stopień równoległości większy niż liczba rdzeni na maszynie.</span><span class="sxs-lookup"><span data-stu-id="02d8d-163">In cases where a query is performing a significant amount of non-compute-bound work such as File I/O, it might be beneficial to specify a degree of parallelism greater than the number of cores on the machine.</span></span>

## <a name="ordered-versus-unordered-parallel-queries"></a><span data-ttu-id="02d8d-164">Uporządkowane a nieuporządkowane zapytania równoległe</span><span class="sxs-lookup"><span data-stu-id="02d8d-164">Ordered Versus Unordered Parallel Queries</span></span>

<span data-ttu-id="02d8d-165">W niektórych zapytaniach operator zapytań musi generować wyniki, które zachowują kolejność sekwencji źródłowej.</span><span class="sxs-lookup"><span data-stu-id="02d8d-165">In some queries, a query operator must produce results that preserve the ordering of the source sequence.</span></span> <span data-ttu-id="02d8d-166">Program PLINQ zawiera <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> operatora w tym celu.</span><span class="sxs-lookup"><span data-stu-id="02d8d-166">PLINQ provides the <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> operator for this purpose.</span></span> <span data-ttu-id="02d8d-167"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A> różni się od <xref:System.Linq.ParallelEnumerable.AsSequential%2A>.</span><span class="sxs-lookup"><span data-stu-id="02d8d-167"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A> is distinct from <xref:System.Linq.ParallelEnumerable.AsSequential%2A>.</span></span> <span data-ttu-id="02d8d-168"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A> Sekwencji jest nadal przetwarzana równolegle, ale jego wyniki są buforowane i posortowane.</span><span class="sxs-lookup"><span data-stu-id="02d8d-168">An <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sequence is still processed in parallel, but its results are buffered and sorted.</span></span> <span data-ttu-id="02d8d-169">Ponieważ zachowanie kolejności zazwyczaj wymaga dodatkowej pracy <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sekwencji może być przetwarzana wolniej niż domyślna <xref:System.Linq.ParallelEnumerable.AsUnordered%2A> sekwencji.</span><span class="sxs-lookup"><span data-stu-id="02d8d-169">Because order preservation typically involves extra work, an <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sequence might be processed more slowly than the default <xref:System.Linq.ParallelEnumerable.AsUnordered%2A> sequence.</span></span> <span data-ttu-id="02d8d-170">Czy zamówiona operacja równoległa jest szybsza niż kolejna wersja operacji zależy od wielu czynników.</span><span class="sxs-lookup"><span data-stu-id="02d8d-170">Whether a particular ordered parallel operation is faster than a sequential version of the operation depends on many factors.</span></span>

<span data-ttu-id="02d8d-171">Poniższy przykład kodu pokazuje sposób korzystania z opcji dla zamówienia zachowania.</span><span class="sxs-lookup"><span data-stu-id="02d8d-171">The following code example shows how to opt in to order preservation.</span></span>

[!code-csharp[PLINQ#3](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#3)]
[!code-vb[PLINQ#3](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#3)]

<span data-ttu-id="02d8d-172">Aby uzyskać więcej informacji, zobacz [zamawianie zachowywania w PLINQ](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="02d8d-172">For more information, see [Order Preservation in PLINQ](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span></span>

## <a name="parallel-vs-sequential-queries"></a><span data-ttu-id="02d8d-173">Równoległe programu vs. Zapytania sekwencyjne</span><span class="sxs-lookup"><span data-stu-id="02d8d-173">Parallel vs. Sequential Queries</span></span>

<span data-ttu-id="02d8d-174">Niektóre operacje wymagają, że dane źródłowe były dostarczane w sposób sekwencyjny.</span><span class="sxs-lookup"><span data-stu-id="02d8d-174">Some operations require that the source data be delivered in a sequential manner.</span></span> <span data-ttu-id="02d8d-175"><xref:System.Linq.ParallelEnumerable> Zapytania, operatory powracają do trybu sekwencyjnego automatycznie, gdy jest to wymagane.</span><span class="sxs-lookup"><span data-stu-id="02d8d-175">The <xref:System.Linq.ParallelEnumerable> query operators revert to sequential mode automatically when it is required.</span></span> <span data-ttu-id="02d8d-176">Operatory zapytań zdefiniowanych przez użytkownika i delegatów użytkownika, które wymagają wykonywania sekwencyjnego, mechanizm PLINQ zawiera <xref:System.Linq.ParallelEnumerable.AsSequential%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="02d8d-176">For user-defined query operators and user delegates that require sequential execution, PLINQ provides the <xref:System.Linq.ParallelEnumerable.AsSequential%2A> method.</span></span> <span data-ttu-id="02d8d-177">Kiedy używasz <xref:System.Linq.ParallelEnumerable.AsSequential%2A>, wszystkie kolejne operatory w zapytaniu są wykonywane sekwencyjnie aż do <xref:System.Linq.ParallelEnumerable.AsParallel%2A> wywoływana jest ponownie.</span><span class="sxs-lookup"><span data-stu-id="02d8d-177">When you use <xref:System.Linq.ParallelEnumerable.AsSequential%2A>, all subsequent operators in the query are executed sequentially until <xref:System.Linq.ParallelEnumerable.AsParallel%2A> is called again.</span></span> <span data-ttu-id="02d8d-178">Aby uzyskać więcej informacji, zobacz [jak: Łączenie równoległych i sekwencyjnych zapytań LINQ](../../../docs/standard/parallel-programming/how-to-combine-parallel-and-sequential-linq-queries.md).</span><span class="sxs-lookup"><span data-stu-id="02d8d-178">For more information, see [How to: Combine Parallel and Sequential LINQ Queries](../../../docs/standard/parallel-programming/how-to-combine-parallel-and-sequential-linq-queries.md).</span></span>

## <a name="options-for-merging-query-results"></a><span data-ttu-id="02d8d-179">Opcje scalania wyników zapytań</span><span class="sxs-lookup"><span data-stu-id="02d8d-179">Options for Merging Query Results</span></span>

<span data-ttu-id="02d8d-180">Gdy zapytanie PLINQ jest wykonywane równolegle, jego wyniki z każdego wątku roboczego musi zostać połączony ponownie do głównego wątku, do spożycia przez `foreach` pętli (`For Each` w języku Visual Basic), lub wstawiania do listy lub tablicy.</span><span class="sxs-lookup"><span data-stu-id="02d8d-180">When a PLINQ query executes in parallel, its results from each worker thread must be merged back onto the main thread for consumption by a `foreach` loop (`For Each` in Visual Basic), or insertion into a list or array.</span></span> <span data-ttu-id="02d8d-181">W niektórych przypadkach może być korzystne, aby określić określony rodzaj operacji scalania, na przykład, w celu generowania wyników znacznie szybciej.</span><span class="sxs-lookup"><span data-stu-id="02d8d-181">In some cases, it might be beneficial to specify a particular kind of merge operation, for example, to begin producing results more quickly.</span></span> <span data-ttu-id="02d8d-182">W tym celu narzędzie PLINQ obsługuje <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> metody i <xref:System.Linq.ParallelMergeOptions> wyliczenia.</span><span class="sxs-lookup"><span data-stu-id="02d8d-182">For this purpose, PLINQ supports the <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> method, and the <xref:System.Linq.ParallelMergeOptions> enumeration.</span></span> <span data-ttu-id="02d8d-183">Aby uzyskać więcej informacji, zobacz [opcje scalania w PLINQ](../../../docs/standard/parallel-programming/merge-options-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="02d8d-183">For more information, see [Merge Options in PLINQ](../../../docs/standard/parallel-programming/merge-options-in-plinq.md).</span></span>

## <a name="the-forall-operator"></a><span data-ttu-id="02d8d-184">ForAll Operator</span><span class="sxs-lookup"><span data-stu-id="02d8d-184">The ForAll Operator</span></span>

<span data-ttu-id="02d8d-185">W kolejnych [!INCLUDE[vbteclinq](../../../includes/vbteclinq-md.md)] zapytań, wykonanie jest odroczone do czasu wyliczenia zapytania albo w `foreach` (`For Each` w języku Visual Basic) w pętli lub poprzez wywoływanie metody, takie jak <xref:System.Linq.ParallelEnumerable.ToList%2A> , <xref:System.Linq.ParallelEnumerable.ToArray%2A> , lub <xref:System.Linq.ParallelEnumerable.ToDictionary%2A>.</span><span class="sxs-lookup"><span data-stu-id="02d8d-185">In sequential [!INCLUDE[vbteclinq](../../../includes/vbteclinq-md.md)] queries, execution is deferred until the query is enumerated either in a `foreach` (`For Each` in Visual Basic) loop or by invoking a method such as <xref:System.Linq.ParallelEnumerable.ToList%2A> , <xref:System.Linq.ParallelEnumerable.ToArray%2A> , or <xref:System.Linq.ParallelEnumerable.ToDictionary%2A>.</span></span> <span data-ttu-id="02d8d-186">W programie PLINQ można również użyć `foreach` do wykonywania zapytania i iteracyjnego przeglądania wyników.</span><span class="sxs-lookup"><span data-stu-id="02d8d-186">In PLINQ, you can also use `foreach` to execute the query and iterate through the results.</span></span> <span data-ttu-id="02d8d-187">Jednak `foreach` sam nie działa w sposób równoległy, a w związku z tym, wymaga on, że dane wyjściowe z wszystkich zadań równoległych jest scalana z powrotem do wątku, na którym działa pętla.</span><span class="sxs-lookup"><span data-stu-id="02d8d-187">However, `foreach` itself does not run in parallel, and therefore, it requires that the output from all parallel tasks be merged back into the thread on which the loop is running.</span></span> <span data-ttu-id="02d8d-188">W programie PLINQ można użyć `foreach` gdy należy zachować ostateczną kolejność wyników zapytania, a także gdy są przetwarzania wyniki w sposób szeregowy, na przykład gdy wywołujesz `Console.WriteLine` dla każdego elementu.</span><span class="sxs-lookup"><span data-stu-id="02d8d-188">In PLINQ, you can use `foreach` when you must preserve the final ordering of the query results, and also whenever you are processing the results in a serial manner, for example when you are calling `Console.WriteLine` for each element.</span></span> <span data-ttu-id="02d8d-189">Dla szybszego wykonywania zapytań, gdy porządku nie jest wymagane i podczas przetwarzania wyników może się odbywać się równolegle, użyj <xref:System.Linq.ParallelEnumerable.ForAll%2A> metodę, aby wykonać zapytanie PLINQ.</span><span class="sxs-lookup"><span data-stu-id="02d8d-189">For faster query execution when order preservation is not required and when the processing of the results can itself be parallelized, use the <xref:System.Linq.ParallelEnumerable.ForAll%2A> method to execute a PLINQ query.</span></span> <span data-ttu-id="02d8d-190"><xref:System.Linq.ParallelEnumerable.ForAll%2A> nie wykonuje tego ostatniego kroku scalania.</span><span class="sxs-lookup"><span data-stu-id="02d8d-190"><xref:System.Linq.ParallelEnumerable.ForAll%2A> does not perform this final merge step.</span></span> <span data-ttu-id="02d8d-191">Poniższy przykład kodu pokazuje sposób użycia <xref:System.Linq.ParallelEnumerable.ForAll%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="02d8d-191">The following code example shows how to use the <xref:System.Linq.ParallelEnumerable.ForAll%2A> method.</span></span> <span data-ttu-id="02d8d-192"><xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType> jest używana w tym miejscu, ponieważ jest zoptymalizowany dla wielu wątków dodawanych jednocześnie bez próby usuwania jakichkolwiek elementów.</span><span class="sxs-lookup"><span data-stu-id="02d8d-192"><xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType> is used here because it is optimized for multiple threads adding concurrently without attempting to remove any items.</span></span>

[!code-csharp[PLINQ#4](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#4)]
[!code-vb[PLINQ#4](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#4)]

<span data-ttu-id="02d8d-193">Poniższa ilustracja przedstawia różnicę między `foreach` i <xref:System.Linq.ParallelEnumerable.ForAll%2A> w odniesieniu do wykonywania zapytań.</span><span class="sxs-lookup"><span data-stu-id="02d8d-193">The following illustration shows the difference between `foreach` and <xref:System.Linq.ParallelEnumerable.ForAll%2A> with regard to query execution.</span></span>

<span data-ttu-id="02d8d-194">![ForAll programu vs. ForEach](../../../docs/standard/parallel-programming/media/vs-isvnt-allvseach.png "VS_ISVNT_ALLvsEACH")</span><span class="sxs-lookup"><span data-stu-id="02d8d-194">![ForAll vs. ForEach](../../../docs/standard/parallel-programming/media/vs-isvnt-allvseach.png "VS_ISVNT_ALLvsEACH")</span></span>

## <a name="cancellation"></a><span data-ttu-id="02d8d-195">Anulowanie</span><span class="sxs-lookup"><span data-stu-id="02d8d-195">Cancellation</span></span>

<span data-ttu-id="02d8d-196">Program PLINQ jest zintegrowany z typami anulowania w .NET Framework 4.</span><span class="sxs-lookup"><span data-stu-id="02d8d-196">PLINQ is integrated with the cancellation types in .NET Framework 4.</span></span> <span data-ttu-id="02d8d-197">(Aby uzyskać więcej informacji, zobacz [anulowanie w zarządzanych wątkach](../../../docs/standard/threading/cancellation-in-managed-threads.md).) W związku z tym w przeciwieństwie do sekwencyjnego LINQ do zapytań obiekt, zapytania PLINQ może być anulowane.</span><span class="sxs-lookup"><span data-stu-id="02d8d-197">(For more information, see [Cancellation in Managed Threads](../../../docs/standard/threading/cancellation-in-managed-threads.md).) Therefore, unlike sequential LINQ to Objects queries, PLINQ queries can be canceled.</span></span> <span data-ttu-id="02d8d-198">Aby utworzyć zapytanie PLINQ można anulować, użyj <xref:System.Linq.ParallelEnumerable.WithCancellation%2A> operatora zapytania i podaj <xref:System.Threading.CancellationToken> wystąpienie jako argument.</span><span class="sxs-lookup"><span data-stu-id="02d8d-198">To create a cancelable PLINQ query, use the <xref:System.Linq.ParallelEnumerable.WithCancellation%2A> operator on the query and provide a <xref:System.Threading.CancellationToken> instance as the argument.</span></span> <span data-ttu-id="02d8d-199">Gdy <xref:System.Threading.CancellationToken.IsCancellationRequested%2A> właściwość token jest ustawiona na wartość true, PLINQ będzie zauważy, zatrzyma przetwarzanie na wszystkich wątkach i throw <xref:System.OperationCanceledException>.</span><span class="sxs-lookup"><span data-stu-id="02d8d-199">When the <xref:System.Threading.CancellationToken.IsCancellationRequested%2A> property on the token is set to true, PLINQ will notice it, stop processing on all threads, and throw an <xref:System.OperationCanceledException>.</span></span>

<span data-ttu-id="02d8d-200">Istnieje możliwość, że zapytanie PLINQ może kontynuować przetwarzanie niektóre elementy po ustawieniu tokenu odwołania.</span><span class="sxs-lookup"><span data-stu-id="02d8d-200">It is possible that a PLINQ query might continue to process some elements after the cancellation token is set.</span></span>

<span data-ttu-id="02d8d-201">Aby uzyskać szybsze odpowiedzi może również odpowiadać na żądania anulowania w długo wykonywanych delegatach użytkownika.</span><span class="sxs-lookup"><span data-stu-id="02d8d-201">For greater responsiveness, you can also respond to cancellation requests in long-running user delegates.</span></span> <span data-ttu-id="02d8d-202">Aby uzyskać więcej informacji, zobacz [jak: Anulowanie zapytania PLINQ](../../../docs/standard/parallel-programming/how-to-cancel-a-plinq-query.md).</span><span class="sxs-lookup"><span data-stu-id="02d8d-202">For more information, see [How to: Cancel a PLINQ Query](../../../docs/standard/parallel-programming/how-to-cancel-a-plinq-query.md).</span></span>

## <a name="exceptions"></a><span data-ttu-id="02d8d-203">Wyjątki</span><span class="sxs-lookup"><span data-stu-id="02d8d-203">Exceptions</span></span>

<span data-ttu-id="02d8d-204">Po wykonaniu zapytania PLINQ, wiele wyjątków może być wyrzucanych z różnych wątków jednocześnie.</span><span class="sxs-lookup"><span data-stu-id="02d8d-204">When a PLINQ query executes, multiple exceptions might be thrown from different threads simultaneously.</span></span> <span data-ttu-id="02d8d-205">Ponadto kod do obsługi wyjątków może być w innym wątku niż kod, który wygenerował wyjątek.</span><span class="sxs-lookup"><span data-stu-id="02d8d-205">Also, the code to handle the exception might be on a different thread than the code that threw the exception.</span></span> <span data-ttu-id="02d8d-206">Program PLINQ korzysta <xref:System.AggregateException> typ do hermetyzacji wszystkich wyjątków, które zostały zgłoszone przez zapytanie i kierowania tych wyjątków z powrotem do wątku wywołującego.</span><span class="sxs-lookup"><span data-stu-id="02d8d-206">PLINQ uses the <xref:System.AggregateException> type to encapsulate all the exceptions that were thrown by a query, and marshal those exceptions back to the calling thread.</span></span> <span data-ttu-id="02d8d-207">Na wątku wywołującym wymagane jest tylko jedna blokada try-catch.</span><span class="sxs-lookup"><span data-stu-id="02d8d-207">On the calling thread, only one try-catch block is required.</span></span> <span data-ttu-id="02d8d-208">Jednakże, można wykonać iterację przez wszystkie wyjątki, które są hermetyzowane w <xref:System.AggregateException> i przechwytywać tych, które możesz można bezpiecznie dokonać przywrócenia.</span><span class="sxs-lookup"><span data-stu-id="02d8d-208">However, you can iterate through all of the exceptions that are encapsulated in the <xref:System.AggregateException> and catch any that you can safely recover from.</span></span> <span data-ttu-id="02d8d-209">W rzadkich przypadkach niektóre wyjątki mogą być generowane, które nie zostały zapakowane w <xref:System.AggregateException>, i <xref:System.Threading.ThreadAbortException>s nie również zostały zapakowane.</span><span class="sxs-lookup"><span data-stu-id="02d8d-209">In rare cases, some exceptions may be thrown that are not wrapped in an <xref:System.AggregateException>, and <xref:System.Threading.ThreadAbortException>s  are also not wrapped.</span></span>

<span data-ttu-id="02d8d-210">Kiedy wyjątki mogą się pojawiać z powrotem w sąsiednim wątku, jest możliwe, że zapytanie może w dalszym ciągu przetwarzać niektóre elementy po wyjątku jest zgłaszany.</span><span class="sxs-lookup"><span data-stu-id="02d8d-210">When exceptions are allowed to bubble up back to the joining thread, then it is possible that a query may continue to process some items after the exception is raised.</span></span>

<span data-ttu-id="02d8d-211">Aby uzyskać więcej informacji, zobacz [jak: Obsługa wyjątków w zapytaniu PLINQ](../../../docs/standard/parallel-programming/how-to-handle-exceptions-in-a-plinq-query.md).</span><span class="sxs-lookup"><span data-stu-id="02d8d-211">For more information, see [How to: Handle Exceptions in a PLINQ Query](../../../docs/standard/parallel-programming/how-to-handle-exceptions-in-a-plinq-query.md).</span></span>

## <a name="custom-partitioners"></a><span data-ttu-id="02d8d-212">Niestandardowe Partycjonery</span><span class="sxs-lookup"><span data-stu-id="02d8d-212">Custom Partitioners</span></span>

<span data-ttu-id="02d8d-213">W niektórych przypadkach może poprawić wydajność zapytań przez napisanie niestandardowego partycjonera, który wykorzystuje pewne cechy danych źródłowych.</span><span class="sxs-lookup"><span data-stu-id="02d8d-213">In some cases, you can improve query performance by writing a custom partitioner that takes advantage of some characteristic of the source data.</span></span> <span data-ttu-id="02d8d-214">W zapytaniu sam niestandardowy partycjoner jest wyliczanym obiektem, którego dotyczy kwerenda.</span><span class="sxs-lookup"><span data-stu-id="02d8d-214">In the query, the custom partitioner itself is the enumerable object that is queried.</span></span>

[!code-csharp[PLINQ#2](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#2)]
[!code-vb[PLINQ#2](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq3.vb#2)]

<span data-ttu-id="02d8d-215">Program PLINQ obsługuje stałą liczbę partycji (chociaż dane mogą być dynamicznie przypisane do tych partycji w czasie wykonywania dla równoważenia obciążenia.).</span><span class="sxs-lookup"><span data-stu-id="02d8d-215">PLINQ supports a fixed number of partitions (although data may be dynamically reassigned to those partitions during run time for load balancing.).</span></span> <span data-ttu-id="02d8d-216"><xref:System.Threading.Tasks.Parallel.For%2A> i <xref:System.Threading.Tasks.Parallel.ForEach%2A> obsługują tylko dynamiczne partycjonowanie, co oznacza, że liczba partycji zmienia w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="02d8d-216"><xref:System.Threading.Tasks.Parallel.For%2A> and <xref:System.Threading.Tasks.Parallel.ForEach%2A> support only dynamic partitioning, which means that the number of partitions changes at run time.</span></span> <span data-ttu-id="02d8d-217">Aby uzyskać więcej informacji, zobacz [niestandardowe Partycjonery dla PLINQ i TPL](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md).</span><span class="sxs-lookup"><span data-stu-id="02d8d-217">For more information, see [Custom Partitioners for PLINQ and TPL](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md).</span></span>

## <a name="measuring-plinq-performance"></a><span data-ttu-id="02d8d-218">Mierzenie wydajności programu PLINQ</span><span class="sxs-lookup"><span data-stu-id="02d8d-218">Measuring PLINQ Performance</span></span>

<span data-ttu-id="02d8d-219">W wielu przypadkach zapytanie może odbywać się równolegle, ale obciążenie konfigurowania zapytania równoległego przewyższa uzyskane korzyści wydajności.</span><span class="sxs-lookup"><span data-stu-id="02d8d-219">In many cases, a query can be parallelized, but the overhead of setting up the parallel query outweighs the performance benefit gained.</span></span> <span data-ttu-id="02d8d-220">Jeśli zapytanie nie wykonuje wielu obliczeń lub jeśli źródło danych jest małe, zapytanie PLINQ może być wolniejsze niż sekwencyjne zapytanie LINQ to Objects.</span><span class="sxs-lookup"><span data-stu-id="02d8d-220">If a query does not perform much computation or if the data source is small, a PLINQ query may be slower than a sequential LINQ to Objects query.</span></span> <span data-ttu-id="02d8d-221">Analizatora wydajności równoległej w Visual Studio Team Server umożliwia porównać wydajność różnych zapytań, zlokalizować wąskie gardła przetwarzania i aby określić, czy Twoje zapytanie działa równolegle czy w sekwencji.</span><span class="sxs-lookup"><span data-stu-id="02d8d-221">You can use the Parallel Performance Analyzer in Visual Studio Team Server to compare the performance of various queries, to locate processing bottlenecks, and to determine whether your query is running in parallel or sequentially.</span></span> <span data-ttu-id="02d8d-222">Aby uzyskać więcej informacji, zobacz [Concurrency Visualizer](/visualstudio/profiling/concurrency-visualizer) i [jak: Mierzenie wydajności zapytań PLINQ](../../../docs/standard/parallel-programming/how-to-measure-plinq-query-performance.md).</span><span class="sxs-lookup"><span data-stu-id="02d8d-222">For more information, see [Concurrency Visualizer](/visualstudio/profiling/concurrency-visualizer) and [How to: Measure PLINQ Query Performance](../../../docs/standard/parallel-programming/how-to-measure-plinq-query-performance.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="02d8d-223">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="02d8d-223">See also</span></span>

- [<span data-ttu-id="02d8d-224">Równoległe LINQ (PLINQ)</span><span class="sxs-lookup"><span data-stu-id="02d8d-224">Parallel LINQ (PLINQ)</span></span>](../../../docs/standard/parallel-programming/parallel-linq-plinq.md)
- [<span data-ttu-id="02d8d-225">Ogólne informacje o przyspieszeniach w PLINQ</span><span class="sxs-lookup"><span data-stu-id="02d8d-225">Understanding Speedup in PLINQ</span></span>](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md)
