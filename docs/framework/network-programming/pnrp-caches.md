---
title: Pamięci podręczne PNRP
ms.date: 03/30/2017
ms.assetid: 270068d9-1b6b-4eb9-9e14-e02326bb88df
ms.openlocfilehash: 3ed3e11e702c8933b500421de5654b212cdd80d8
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/15/2020
ms.locfileid: "64622991"
---
# <a name="pnrp-caches"></a><span data-ttu-id="517ad-102">Pamięci podręczne PNRP</span><span class="sxs-lookup"><span data-stu-id="517ad-102">PNRP Caches</span></span>
<span data-ttu-id="517ad-103">Bufory protokołu PNRP (190) są lokalnymi kolekcjami algorytmicznie wybranych punktów końcowych elementów równorzędnych obsługiwanych w elementów równorzędnych.</span><span class="sxs-lookup"><span data-stu-id="517ad-103">Peer Name Resolution Protocol (PNRP) caches are local collections of algorithmically selected peer endpoints maintained on the peer.</span></span>  
  
## <a name="pnrp-cache-initialization"></a><span data-ttu-id="517ad-104">Inicjowanie pamięci podręcznej PNRP</span><span class="sxs-lookup"><span data-stu-id="517ad-104">PNRP Cache Initialization</span></span>  
 <span data-ttu-id="517ad-105">Aby zainicjować pamięć podręczną PNRP lub kolekcję rekordów nazw elementów równorzędnych, po uruchomieniu węzła równorzędnego węzeł może użyć następujących metod:</span><span class="sxs-lookup"><span data-stu-id="517ad-105">To initialize the PNRP cache, or Peer Name Record Collection, when a peer node starts up, a node can use the following methods:</span></span>  
  
- <span data-ttu-id="517ad-106">Trwałe wpisy pamięci podręcznej, które były obecne po zamknięciu węzła są ładowane z magazynu dysku twardego.</span><span class="sxs-lookup"><span data-stu-id="517ad-106">Persistent cache entries that were present when the node was shut down are loaded from hard disk storage.</span></span>  
  
- <span data-ttu-id="517ad-107">Jeśli aplikacja korzysta z infrastruktury współpracy P2P, informacje o współpracy są dostępne w Menedżerze kontaktów dla tego węzła.</span><span class="sxs-lookup"><span data-stu-id="517ad-107">If an application uses the P2P collaboration infrastructure, collaboration information is available in the Contact Manager for that node.</span></span>  
  
## <a name="scaling-peer-name-resolution-with-a-multi-level-cache"></a><span data-ttu-id="517ad-108">Skalowanie rozpoznawania nazw elementów równorzędnych za pomocą pamięci podręcznej wielopoziomowej</span><span class="sxs-lookup"><span data-stu-id="517ad-108">Scaling Peer Name Resolution with a Multi-Level Cache</span></span>  
 <span data-ttu-id="517ad-109">Aby zachować rozmiary pamięci podręcznych PNRP małe, węzły równorzędne użyć pamięci podręcznej wielopoziomowej, w którym każdy poziom zawiera maksymalną liczbę wpisów.</span><span class="sxs-lookup"><span data-stu-id="517ad-109">To keep the sizes of the PNRP caches small, peer nodes use a multi-level cache, in which each level contains a maximum number of entries.</span></span> <span data-ttu-id="517ad-110">Każdy poziom w pamięci podręcznej reprezentuje jedną dziesiątą mniejszą część miejsca numeru identyfikatora PNRP (2<sup>256</sup>).</span><span class="sxs-lookup"><span data-stu-id="517ad-110">Each level in the cache represents a one tenth smaller portion of the PNRP ID number space (2<sup>256</sup>).</span></span> <span data-ttu-id="517ad-111">Najniższy poziom w pamięci podręcznej zawiera lokalnie zarejestrowany identyfikator PNRP i inne identyfikatory PNRP, które są numerycznie blisko niego.</span><span class="sxs-lookup"><span data-stu-id="517ad-111">The lowest level in the cache contains a locally registered PNRP ID and other PNRP IDs that are numerically close to it.</span></span> <span data-ttu-id="517ad-112">Ponieważ poziom pamięci podręcznej jest wypełniony maksymalnie 20 wpisami, tworzony jest nowy niższy poziom.</span><span class="sxs-lookup"><span data-stu-id="517ad-112">As a level of the cache is filled with a maximum of 20 entries, a new lower level is created.</span></span> <span data-ttu-id="517ad-113">Maksymalna liczba poziomów w pamięci podręcznej jest w kolejności log10(Całkowita liczba identyfikatorów PNRP w chmurze).</span><span class="sxs-lookup"><span data-stu-id="517ad-113">The maximum number of levels in the cache is on the order of log10(Total number of PNRP IDs in the cloud).</span></span> <span data-ttu-id="517ad-114">Na przykład dla chmury globalnej z 100 milionów identyfikatorów PNRP, nie ma więcej niż 8 (=log10(100,000,000)) poziomy w pamięci podręcznej i podobną liczbę przeskoków, aby rozwiązać identyfikator PNRP podczas rozpoznawania nazw.</span><span class="sxs-lookup"><span data-stu-id="517ad-114">For example, for a global cloud with 100 million PNRP IDs, there are no more than 8 (=log10(100,000,000)) levels in the cache and a similar number of hops to resolve a PNRP ID during name resolution.</span></span> <span data-ttu-id="517ad-115">Mechanizm ten umożliwia tabelę mieszania rozproszonego, dla której można rozwiązać dowolny identyfikator PNRP, przesyłając dalej komunikaty żądania PNRP do najbliższego elementu równorzędnego, dopóki nie zostanie znaleziony element równorzędny z odpowiednim CPA.</span><span class="sxs-lookup"><span data-stu-id="517ad-115">This mechanism allows for a distributed hash table for which an arbitrary PNRP ID can be resolved by forwarding PNRP Request messages to the next-closest peer until the peer with the corresponding CPA is found.</span></span>  
  
 <span data-ttu-id="517ad-116">Aby upewnić się, że rozdzielczość może zakończyć, za każdym razem, gdy węzeł dodaje wpis do najniższego poziomu pamięci podręcznej, zalewa kopię wpisu do wszystkich węzłów w obrębie ostatniego poziomu pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="517ad-116">To ensure that resolution can complete, each time a node adds an entry to the lowest level of its cache, it floods a copy of the entry to all the nodes within the last level of the cache.</span></span>  
  
 <span data-ttu-id="517ad-117">Wpisy pamięci podręcznej są odświeżane w czasie.</span><span class="sxs-lookup"><span data-stu-id="517ad-117">The cache entries are refreshed over time.</span></span> <span data-ttu-id="517ad-118">Wpisy pamięci podręcznej, które są przestarzałe są usuwane z pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="517ad-118">Cache entries that are stale are removed from the cache.</span></span> <span data-ttu-id="517ad-119">W rezultacie tabela mieszania rozproszonych identyfikatorów PNRP jest oparta na aktywnych punktach końcowych, w przeciwieństwie do dns, w którym rekordy adresów i protokół DNS nie dają żadnej gwarancji, że węzeł skojarzony z adresem jest aktywnie w sieci.</span><span class="sxs-lookup"><span data-stu-id="517ad-119">The result is that the distributed hash table of PNRP IDs is based on active endpoints, unlike DNS in which address records and the DNS protocol provide no guarantee that the node associated with the address is actively on the network.</span></span>  
  
## <a name="other-pnrp-caches"></a><span data-ttu-id="517ad-120">Inne pamięci podręczne PNRP</span><span class="sxs-lookup"><span data-stu-id="517ad-120">Other PNRP Caches</span></span>  
 <span data-ttu-id="517ad-121">Innym trwałym magazynem danych jest lokalna pamięć podręczna.</span><span class="sxs-lookup"><span data-stu-id="517ad-121">Another persistent data store is the local cache.</span></span>  <span data-ttu-id="517ad-122">Oprócz innych obiektów potrzebnych do działania PNRP może zawierać rekordy skojarzone z chmurą PNRP lub sesją współpracy, która jest bezpiecznie publikowana i synchronizowana między wszystkimi członkami chmury.</span><span class="sxs-lookup"><span data-stu-id="517ad-122">In addition to the other objects needed for PNRP activity, it may include the records associated with a PNRP cloud or collaboration session that is securely published and synchronized between all the members of the cloud.</span></span> <span data-ttu-id="517ad-123">Ten magazyn replikowany reprezentuje widok danych grupy, który powinien być taki sam dla wszystkich członków grupy.</span><span class="sxs-lookup"><span data-stu-id="517ad-123">This replicated store represents the view of the group data, which should be the same for all group members.</span></span> <span data-ttu-id="517ad-124">Technicznie te obiekty nie są rekordy per se, ale raczej aplikacji, obecności i danych obiektów przeznaczonych dla lokalnej pamięci podręcznej.</span><span class="sxs-lookup"><span data-stu-id="517ad-124">Technically, these objects are not records per se, but rather application, presence, and object data destined for a local cache.</span></span> <span data-ttu-id="517ad-125">Użycie chmury PNRP zapewnia, że obiekty są propagowane do wszystkich węzłów w sesji współpracy lub chmurze PNRP.</span><span class="sxs-lookup"><span data-stu-id="517ad-125">Use of the PNRP cloud ensures that objects are propagated to all nodes in the collaboration session or PNRP cloud.</span></span>  <span data-ttu-id="517ad-126">Replikacja rekordów między członkami chmury używa SSL w celu zapewnienia szyfrowania i integralności danych.</span><span class="sxs-lookup"><span data-stu-id="517ad-126">Record replication between cloud members uses SSL to provide encryption and data integrity.</span></span>  
  
 <span data-ttu-id="517ad-127">Gdy element równorzędny łączy się z chmurą, nie odbiera automatycznie danych lokalnej pamięci podręcznej od elementu równorzędnego hosta, do którego się dołączają; muszą subskrybować element równorzędny hosta, aby otrzymywać aktualizacje w danych aplikacji, obecności i obiektu.</span><span class="sxs-lookup"><span data-stu-id="517ad-127">When a peer joins a cloud, they do not automatically receive local cache data from the host peer to which they attach; they have to subscribe to the host peer to receive updates in application, presence, and object data.</span></span> <span data-ttu-id="517ad-128">Po początkowej synchronizacji elementy równorzędne okresowo ponownie synchronizować swoje replikowane magazyny, aby upewnić się, że wszyscy członkowie grupy konsekwentnie mają ten sam widok.</span><span class="sxs-lookup"><span data-stu-id="517ad-128">After the initial synchronization, peers periodically resynchronize their replicated stores to ensure that all group members consistently have the same view.</span></span>  <span data-ttu-id="517ad-129">Sesja współpracy lub aplikacje w ramach sesji współpracy mogą również pełnić tę samą funkcję.</span><span class="sxs-lookup"><span data-stu-id="517ad-129">The collaboration session or applications within the collaboration session may also perform the same function.</span></span>  
  
 <span data-ttu-id="517ad-130">Po rozpoczęciu sesji współpracy dla chmury aplikacje mogą rejestrować elementy równorzędne i rozpocząć publikowanie swoich informacji przy użyciu zabezpieczeń zdefiniowanych przez zakres chmury.</span><span class="sxs-lookup"><span data-stu-id="517ad-130">After a collaboration session has begun for a cloud, applications can register peers and begin publishing their information using the security defined by the cloud scope.</span></span> <span data-ttu-id="517ad-131">Gdy element równorzędny łączy się z chmurą, mechanizmy zabezpieczeń dla chmury są stosowane do elementu równorzędnego, nadając mu zakres, w którym można uczestniczyć.</span><span class="sxs-lookup"><span data-stu-id="517ad-131">When a peer joins a cloud, the security mechanisms for the cloud are applied to the peer, giving it a scope in which to participate.</span></span>  <span data-ttu-id="517ad-132">Jego rekordy mogą być następnie publikowane bezpiecznie w zakresie chmury.</span><span class="sxs-lookup"><span data-stu-id="517ad-132">Its records can then be published securely within the scope of the cloud.</span></span> <span data-ttu-id="517ad-133">Należy zauważyć, że zakres chmury może nie być taki sam jak zakres aplikacji współpracy.</span><span class="sxs-lookup"><span data-stu-id="517ad-133">Note that cloud scope may not be the same as collaboration application scope.</span></span>  
  
 <span data-ttu-id="517ad-134">Elementy równorzędne mogą rejestrować zainteresowanie odbieraniem obiektów od innych rysów.</span><span class="sxs-lookup"><span data-stu-id="517ad-134">Peers can register interest in receiving objects from other peers.</span></span> <span data-ttu-id="517ad-135">Po zaktualizowaniu obiektu aplikacja współpracy jest powiadamiany i nowy obiekt jest przekazywany do wszystkich subskrybentów aplikacji.</span><span class="sxs-lookup"><span data-stu-id="517ad-135">When an object is updated, the collaboration application is notified and the new object is passed to all subscribers of the application.</span></span> <span data-ttu-id="517ad-136">Na przykład peer w aplikacji czatu grupowego może zarejestrować zainteresowanie otrzymywaniem informacji o aplikacji, która wyśle wszystkie rekordy czatu jako dane aplikacji.</span><span class="sxs-lookup"><span data-stu-id="517ad-136">For example, a peer in a group chat application can register interest in receiving application information, which will send it all chat records as application data.</span></span>  <span data-ttu-id="517ad-137">Dzięki temu może monitorować aktywność czatu w chmurze.</span><span class="sxs-lookup"><span data-stu-id="517ad-137">This allows it to monitor chat activity within the cloud.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="517ad-138">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="517ad-138">See also</span></span>

- <xref:System.Net.PeerToPeer>
