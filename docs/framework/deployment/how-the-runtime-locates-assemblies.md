---
title: Sposoby lokalizowania zestawów przez środowisko uruchomieniowe
ms.date: 03/30/2017
helpviewer_keywords:
- app.config files, assembly locations
- deploying applications [.NET Framework], assembly locations
- application configuration files, locating assemblies
- .NET Framework application deployment, locating assemblies
- locating assemblies
- assemblies [.NET Framework], location
ms.assetid: 772ac6f4-64d2-4cfb-92fd-58096dcd6c34
ms.openlocfilehash: 13e2661b67ba3b717b8917e80118175acb09e756
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/15/2020
ms.locfileid: "79181675"
---
# <a name="how-the-runtime-locates-assemblies"></a><span data-ttu-id="e5cf5-102">Sposoby lokalizowania zestawów przez środowisko uruchomieniowe</span><span class="sxs-lookup"><span data-stu-id="e5cf5-102">How the Runtime Locates Assemblies</span></span>

<span data-ttu-id="e5cf5-103">Aby pomyślnie wdrożyć aplikację .NET Framework, należy zrozumieć, jak środowisko wykonawcze języka wspólnego lokalizuje i wiąże się z zestawami, które tworzą aplikację.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-103">To successfully deploy your .NET Framework application, you must understand how the common language runtime locates and binds to the assemblies that make up your application.</span></span> <span data-ttu-id="e5cf5-104">Domyślnie środowisko uruchomieniowe próbuje powiązać z dokładną wersją zestawu, z pomocą których została zbudowana aplikacja.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-104">By default, the runtime attempts to bind with the exact version of an assembly that the application was built with.</span></span> <span data-ttu-id="e5cf5-105">To domyślne zachowanie można zastąpić ustawieniami pliku konfiguracyjnego.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-105">This default behavior can be overridden by configuration file settings.</span></span>

<span data-ttu-id="e5cf5-106">Środowisko wykonawcze języka wspólnego wykonuje szereg kroków podczas próby zlokalizowania zestawu i rozwiązania odwołania do zestawu.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-106">The common language runtime performs a number of steps when attempting to locate an assembly and resolve an assembly reference.</span></span> <span data-ttu-id="e5cf5-107">Każdy krok jest wyjaśniony w poniższych sekcjach.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-107">Each step is explained in the following sections.</span></span> <span data-ttu-id="e5cf5-108">Termin sondowanie jest często używany podczas opisywania sposobu, w jaki środowisko wykonawcze lokalizuje zestawy; odnosi się do zestawu heurystyki używanej do lokalizowania zestawu na podstawie jego nazwy i kultury.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-108">The term probing is often used when describing how the runtime locates assemblies; it refers to the set of heuristics used to locate the assembly based on its name and culture.</span></span>

> [!NOTE]
> <span data-ttu-id="e5cf5-109">Informacje o powiązaniach można wyświetlać w pliku dziennika za pomocą [przeglądarki dziennika wiązania zestawu (Fuslogvw.exe),](../tools/fuslogvw-exe-assembly-binding-log-viewer.md)która znajduje się w zestawie Windows SDK.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-109">You can view binding information in the log file using the [Assembly Binding Log Viewer (Fuslogvw.exe)](../tools/fuslogvw-exe-assembly-binding-log-viewer.md), which is included in the Windows SDK.</span></span>

## <a name="initiating-the-bind"></a><span data-ttu-id="e5cf5-110">Inicjowanie powiązania</span><span class="sxs-lookup"><span data-stu-id="e5cf5-110">Initiating the Bind</span></span>

<span data-ttu-id="e5cf5-111">Proces lokalizowania i wiązania z zestawem rozpoczyna się, gdy środowisko uruchomieniowe próbuje rozwiązać odwołanie do innego zestawu.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-111">The process of locating and binding to an assembly begins when the runtime attempts to resolve a reference to another assembly.</span></span> <span data-ttu-id="e5cf5-112">To odwołanie może być statyczne lub dynamiczne.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-112">This reference can be either static or dynamic.</span></span> <span data-ttu-id="e5cf5-113">Kompilator rejestruje odwołania statyczne w metadanych manifestu zestawu w czasie kompilacji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-113">The compiler records static references in the assembly manifest's metadata at build time.</span></span> <span data-ttu-id="e5cf5-114">Odniesienia dynamiczne są konstruowane w locie w wyniku <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>wywoływania różnych metod, takich jak .</span><span class="sxs-lookup"><span data-stu-id="e5cf5-114">Dynamic references are constructed on the fly as a result of calling various methods, such as <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>.</span></span>

<span data-ttu-id="e5cf5-115">Preferowanym sposobem odwoływania się do zestawu jest użycie pełnego odwołania, w tym nazwy zestawu, wersji, kultury i tokenu klucza publicznego (jeśli istnieje).</span><span class="sxs-lookup"><span data-stu-id="e5cf5-115">The preferred way to reference an assembly is to use a full reference, including the assembly name, version, culture, and public key token (if one exists).</span></span> <span data-ttu-id="e5cf5-116">Środowisko wykonawcze używa tych informacji do zlokalizowania zestawu, wykonując kroki opisane w dalszej części tej sekcji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-116">The runtime uses this information to locate the assembly, following the steps described later in this section.</span></span> <span data-ttu-id="e5cf5-117">Środowisko wykonawcze używa tego samego procesu rozpoznawania niezależnie od tego, czy odwołanie jest dla zestawu statycznego lub dynamicznego.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-117">The runtime uses the same resolution process regardless of whether the reference is for a static or dynamic assembly.</span></span>

<span data-ttu-id="e5cf5-118">Można również dokonać dynamicznego odwołania do zestawu, podając metodę wywołującą tylko częściowe informacje o zestawie, takie jak określanie tylko nazwy zestawu.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-118">You can also make a dynamic reference to an assembly by providing the calling method with only partial information about the assembly, such as specifying only the assembly name.</span></span> <span data-ttu-id="e5cf5-119">W takim przypadku tylko katalog aplikacji jest wyszukiwany dla zestawu i nie występuje inne sprawdzanie.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-119">In this case, only the application directory is searched for the assembly, and no other checking occurs.</span></span> <span data-ttu-id="e5cf5-120">Należy dokonać częściowego odwołania przy użyciu dowolnej z <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> różnych <xref:System.AppDomain.Load%2A?displayProperty=nameWithType>metod ładowania zestawów, takich jak lub .</span><span class="sxs-lookup"><span data-stu-id="e5cf5-120">You make a partial reference using any of the various methods for loading assemblies such as <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> or <xref:System.AppDomain.Load%2A?displayProperty=nameWithType>.</span></span>

<span data-ttu-id="e5cf5-121">Na koniec można dokonać odwołania dynamicznego przy <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> użyciu metody, takich jak i podać tylko częściowe informacje; następnie zakwalifikujesz odwołanie przy użyciu [ \<elementu qualifyAssembly>](../configure-apps/file-schema/runtime/qualifyassembly-element.md) w pliku konfiguracji aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-121">Finally, you can make a dynamic reference using a method such as <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> and provide only partial information; you then qualify the reference using the [\<qualifyAssembly>](../configure-apps/file-schema/runtime/qualifyassembly-element.md) element in the application configuration file.</span></span> <span data-ttu-id="e5cf5-122">Ten element umożliwia podanie pełnych informacji referencyjnych (nazwa, wersja, kultura i, jeśli dotyczy, token klucza publicznego) w pliku konfiguracji aplikacji, a nie w kodzie.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-122">This element allows you to provide the full reference information (name, version, culture and, if applicable, the public key token) in your application configuration file instead of in your code.</span></span> <span data-ttu-id="e5cf5-123">Tej techniki należy użyć, jeśli chcesz w pełni zakwalifikować odwołanie do zestawu poza katalogiem aplikacji lub jeśli chcesz odwołać się do zestawu w globalnej pamięci podręcznej zestawów, ale chcesz wygodę określania pełnego odwołania w pliku konfiguracyjnego, a nie w kodzie.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-123">You would use this technique if you wanted to fully qualify a reference to an assembly outside the application directory, or if you wanted to reference an assembly in the global assembly cache but you wanted the convenience of specifying the full reference in the configuration file instead of in your code.</span></span>

> [!NOTE]
> <span data-ttu-id="e5cf5-124">Tego typu częściowego odwołania nie należy używać z zestawami, które są współużytkowane przez kilka aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-124">This type of partial reference should not be used with assemblies that are shared among several applications.</span></span> <span data-ttu-id="e5cf5-125">Ponieważ ustawienia konfiguracji są stosowane dla aplikacji, a nie na zestaw, zestaw udostępniony przy użyciu tego typu częściowego odwołania wymagałoby każdej aplikacji przy użyciu zestawu udostępnionego mieć informacje kwalifikacyjne w pliku konfiguracji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-125">Because configuration settings are applied per application and not per assembly, a shared assembly using this type of partial reference would require each application using the shared assembly to have the qualifying information in its configuration file.</span></span>

<span data-ttu-id="e5cf5-126">Środowisko wykonawcze używa następujących kroków, aby rozwiązać odwołanie do zestawu:</span><span class="sxs-lookup"><span data-stu-id="e5cf5-126">The runtime uses the following steps to resolve an assembly reference:</span></span>

1. <span data-ttu-id="e5cf5-127">[Określa poprawną wersję zestawu,](#step1) sprawdzając odpowiednie pliki konfiguracyjne, w tym plik konfiguracji aplikacji, plik zasad wydawcy i plik konfiguracji komputera.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-127">[Determines the correct assembly version](#step1) by examining applicable configuration files, including the application configuration file, publisher policy file, and machine configuration file.</span></span> <span data-ttu-id="e5cf5-128">Jeśli plik konfiguracyjny znajduje się na komputerze zdalnym, środowisko wykonawcze musi najpierw zlokalizować i pobrać plik konfiguracji aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-128">If the configuration file is located on a remote machine, the runtime must locate and download the application configuration file first.</span></span>

2. <span data-ttu-id="e5cf5-129">[Sprawdza, czy nazwa zestawu została powiązana z przed,](#step2) a jeśli tak, używa wcześniej załadowanego zestawu.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-129">[Checks whether the assembly name has been bound to before](#step2) and, if so, uses the previously loaded assembly.</span></span> <span data-ttu-id="e5cf5-130">Jeśli poprzednie żądanie załadowania zestawu nie powiodło się, żądanie nie powiedzie się natychmiast bez próby załadowania zestawu.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-130">If a previous request to load the assembly failed, the request is failed immediately without attempting to load the assembly.</span></span>

    > [!NOTE]
    > <span data-ttu-id="e5cf5-131">Buforowanie błędów wiązania zestawu jest nowy w .NET Framework w wersji 2.0.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-131">The caching of assembly binding failures is new in the .NET Framework version 2.0.</span></span>

3. <span data-ttu-id="e5cf5-132">[Sprawdza globalną pamięć podręczną zestawów](#step3).</span><span class="sxs-lookup"><span data-stu-id="e5cf5-132">[Checks the global assembly cache](#step3).</span></span> <span data-ttu-id="e5cf5-133">Jeśli zestaw zostanie znaleziony tam, środowisko wykonawcze używa tego zestawu.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-133">If the assembly is found there, the runtime uses this assembly.</span></span>

4. <span data-ttu-id="e5cf5-134">[Sondy dla zespołu](#step4) przy użyciu następujących kroków:</span><span class="sxs-lookup"><span data-stu-id="e5cf5-134">[Probes for the assembly](#step4) using the following steps:</span></span>

    1. <span data-ttu-id="e5cf5-135">Jeśli zasady konfiguracji i wydawcy nie mają wpływu na oryginalne <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> odwołanie i jeśli żądanie powiązania zostało utworzone przy użyciu metody, środowisko uruchomieniowe sprawdza wskazówki dotyczące lokalizacji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-135">If configuration and publisher policy do not affect the original reference and if the bind request was created using the <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> method, the runtime checks for location hints.</span></span>

    2. <span data-ttu-id="e5cf5-136">Jeśli baza kodu zostanie znaleziona w plikach konfiguracyjnych, środowisko wykonawcze sprawdza tylko tę lokalizację.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-136">If a codebase is found in the configuration files, the runtime checks only this location.</span></span> <span data-ttu-id="e5cf5-137">Jeśli ta sonda nie powiedzie się, środowisko wykonawcze określa, że żądanie powiązania nie powiodło się i nie występuje inne sondowanie.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-137">If this probe fails, the runtime determines that the binding request failed and no other probing occurs.</span></span>

    3. <span data-ttu-id="e5cf5-138">Sondy do montażu przy użyciu heurystyki opisanej w [sekcji sondowania](#step4).</span><span class="sxs-lookup"><span data-stu-id="e5cf5-138">Probes for the assembly using the heuristics described in the [probing section](#step4).</span></span> <span data-ttu-id="e5cf5-139">Jeśli zestaw nie zostanie znaleziony po sondowaniu, środowisko wykonawcze żąda Instalatora Windows, aby zapewnić zestaw.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-139">If the assembly is not found after probing, the runtime requests the Windows Installer to provide the assembly.</span></span> <span data-ttu-id="e5cf5-140">Działa to jako funkcja instalacji na żądanie.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-140">This acts as an install-on-demand feature.</span></span>

        > [!NOTE]
        > <span data-ttu-id="e5cf5-141">Nie ma sprawdzania wersji dla zestawów bez silnych nazw, ani nie sprawdza środowiska wykonawczego w globalnej pamięci podręcznej zestawów dla zestawów bez silnych nazw.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-141">There is no version checking for assemblies without strong names, nor does the runtime check in the global assembly cache for assemblies without strong names.</span></span>

<a name="step1"></a>

## <a name="step-1-examining-the-configuration-files"></a><span data-ttu-id="e5cf5-142">Krok 1. Badanie plików konfiguracji</span><span class="sxs-lookup"><span data-stu-id="e5cf5-142">Step 1: Examining the Configuration Files</span></span>

<span data-ttu-id="e5cf5-143">Zachowanie wiązania zestawu można skonfigurować na różnych poziomach na podstawie trzech plików XML:</span><span class="sxs-lookup"><span data-stu-id="e5cf5-143">Assembly binding behavior can be configured at different levels based on three XML files:</span></span>

- <span data-ttu-id="e5cf5-144">Plik konfiguracyjny aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-144">Application configuration file.</span></span>

- <span data-ttu-id="e5cf5-145">Plik zasad wydawcy.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-145">Publisher policy file.</span></span>

- <span data-ttu-id="e5cf5-146">Plik konfiguracyjny komputera.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-146">Machine configuration file.</span></span>

<span data-ttu-id="e5cf5-147">Pliki te są zgodne z tą samą składnią i zawierają informacje, takie jak przekierowania powiązania, lokalizacja kodu i tryby wiązania dla poszczególnych zestawów.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-147">These files follow the same syntax and provide information such as binding redirects, the location of code, and binding modes for particular assemblies.</span></span> <span data-ttu-id="e5cf5-148">Każdy plik konfiguracji może zawierać [ \<element zestawuBinowanie>,](../configure-apps/file-schema/runtime/assemblybinding-element-for-runtime.md) który przekierowuje proces wiązania.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-148">Each configuration file can contain an [\<assemblyBinding> element](../configure-apps/file-schema/runtime/assemblybinding-element-for-runtime.md) that redirects the binding process.</span></span> <span data-ttu-id="e5cf5-149">Elementy podrzędne [ \<elementu zestawuBinowanie>](../configure-apps/file-schema/runtime/assemblybinding-element-for-runtime.md) obejmują [ \<zależnyAsześcij> element](../configure-apps/file-schema/runtime/dependentassembly-element.md).</span><span class="sxs-lookup"><span data-stu-id="e5cf5-149">The child elements of the [\<assemblyBinding> element](../configure-apps/file-schema/runtime/assemblybinding-element-for-runtime.md) include the [\<dependentAssembly> element](../configure-apps/file-schema/runtime/dependentassembly-element.md).</span></span> <span data-ttu-id="e5cf5-150">Elementy podrzędne [ \<dependentAssembly> element](../configure-apps/file-schema/runtime/dependentassembly-element.md) obejmują [ \<assemblyIdentity> element,](/visualstudio/deployment/assemblyidentity-element-clickonce-deployment) [ \<bindingRedirect> element](../configure-apps/file-schema/runtime/bindingredirect-element.md)i [ \<codeBase> element](../configure-apps/file-schema/runtime/codebase-element.md).</span><span class="sxs-lookup"><span data-stu-id="e5cf5-150">The children of [\<dependentAssembly> element](../configure-apps/file-schema/runtime/dependentassembly-element.md) include the [\<assemblyIdentity> element](/visualstudio/deployment/assemblyidentity-element-clickonce-deployment), the [\<bindingRedirect> element](../configure-apps/file-schema/runtime/bindingredirect-element.md), and the [\<codeBase> element](../configure-apps/file-schema/runtime/codebase-element.md).</span></span>

> [!NOTE]
> <span data-ttu-id="e5cf5-151">Informacje o konfiguracji można znaleźć w trzech plikach konfiguracyjnych; nie wszystkie elementy są prawidłowe we wszystkich plikach konfiguracyjnych.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-151">Configuration information can be found in the three configuration files; not all elements are valid in all configuration files.</span></span> <span data-ttu-id="e5cf5-152">Na przykład informacje o trybie powiązania i ścieżce prywatnej mogą znajdować się tylko w pliku konfiguracji aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-152">For example, binding mode and private path information can only be in the application configuration file.</span></span> <span data-ttu-id="e5cf5-153">Aby uzyskać pełną listę informacji zawartych w każdym pliku, zobacz [Konfigurowanie aplikacji przy użyciu plików konfiguracyjnych](../configure-apps/index.md).</span><span class="sxs-lookup"><span data-stu-id="e5cf5-153">For a complete list of the information that is contained in each file, see [Configuring Apps by Using Configuration Files](../configure-apps/index.md).</span></span>

### <a name="application-configuration-file"></a><span data-ttu-id="e5cf5-154">Plik konfiguracji aplikacji</span><span class="sxs-lookup"><span data-stu-id="e5cf5-154">Application Configuration File</span></span>

<span data-ttu-id="e5cf5-155">Po pierwsze środowisko uruchomieniowe języka wspólnego sprawdza plik konfiguracji aplikacji pod kątem informacji, które zastępuje informacje o wersji przechowywane w manifeście zestawu wywołującego.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-155">First, the common language runtime checks the application configuration file for information that overrides the version information stored in the calling assembly's manifest.</span></span> <span data-ttu-id="e5cf5-156">Plik konfiguracji aplikacji można wdrożyć z aplikacją, ale nie jest wymagane do wykonania aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-156">The application configuration file can be deployed with an application, but is not required for application execution.</span></span> <span data-ttu-id="e5cf5-157">Zazwyczaj pobieranie tego pliku jest niemal natychmiastowe, ale w sytuacjach, gdy baza aplikacji znajduje się na komputerze zdalnym, na przykład w scenariuszu opartym na sieci Web programu Internet Explorer, plik konfiguracyjny musi zostać pobrany.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-157">Usually the retrieval of this file is almost instantaneous, but in situations where the application base is on a remote computer, such as in an Internet Explorer Web-based scenario, the configuration file must be downloaded.</span></span>

<span data-ttu-id="e5cf5-158">W przypadku plików wykonywalnych klienta plik konfiguracji aplikacji znajduje się w tym samym katalogu co plik wykonywalny aplikacji i ma taką samą nazwę podstawową jak plik wykonywalny z rozszerzeniem .config.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-158">For client executables, the application configuration file resides in the same directory as the application's executable and has the same base name as the executable with a .config extension.</span></span> <span data-ttu-id="e5cf5-159">Na przykład plik konfiguracyjny dla C:\Program Files\Myapp\Myapp.exe to C:\Program Files\Myapp\Myapp.exe.config. W scenariuszu opartym na przeglądarce plik HTML musi używać \*\* \<elementu>łącza,\*\* aby jawnie wskazać plik konfiguracyjny.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-159">For example, the configuration file for C:\Program Files\Myapp\Myapp.exe is C:\Program Files\Myapp\Myapp.exe.config. In a browser-based scenario, the HTML file must use the **\<link>** element to explicitly point to the configuration file.</span></span>

<span data-ttu-id="e5cf5-160">Poniższy kod zawiera prosty przykład pliku konfiguracji aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-160">The following code provides a simple example of an application configuration file.</span></span> <span data-ttu-id="e5cf5-161">W tym <xref:System.Diagnostics.TextWriterTraceListener> przykładzie <xref:System.Diagnostics.Debug.Listeners%2A> dodaje do kolekcji, aby włączyć rejestrowanie informacji debugowania do pliku.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-161">This example adds a <xref:System.Diagnostics.TextWriterTraceListener> to the <xref:System.Diagnostics.Debug.Listeners%2A> collection to enable recording debug information to a file.</span></span>

```xml
<configuration>
   <system.diagnostics>
      <trace useGlobalLock="false" autoflush="true" indentsize="0">
         <listeners>
            <add name="myListener" type="System.Diagnostics.TextWriterTraceListener, system version=1.0.3300.0, Culture=neutral, PublicKeyToken=b77a5c561934e089" initializeData="c:\myListener.log" />
         </listeners>
      </trace>
   </system.diagnostics>
</configuration>
```

### <a name="publisher-policy-file"></a><span data-ttu-id="e5cf5-162">Plik zasad wydawcy</span><span class="sxs-lookup"><span data-stu-id="e5cf5-162">Publisher Policy File</span></span>

<span data-ttu-id="e5cf5-163">Po drugie środowisko wykonawcze sprawdza plik zasad wydawcy, jeśli istnieje.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-163">Second, the runtime examines the publisher policy file, if one exists.</span></span> <span data-ttu-id="e5cf5-164">Pliki zasad programu Publisher są rozpowszechniane przez wydawcę komponentu jako poprawka lub aktualizacja składnika udostępnionego.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-164">Publisher policy files are distributed by a component publisher as a fix or update to a shared component.</span></span> <span data-ttu-id="e5cf5-165">Pliki te zawierają informacje o zgodności wydane przez wydawcę udostępnionego składnika, który kieruje odwołanie do zestawu do nowej wersji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-165">These files contain compatibility information issued by the publisher of the shared component that directs an assembly reference to a new version.</span></span> <span data-ttu-id="e5cf5-166">W przeciwieństwie do plików konfiguracyjnych aplikacji i maszyn pliki zasad wydawcy są zawarte we własnym zestawie, które muszą być zainstalowane w globalnej pamięci podręcznej zestawów.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-166">Unlike application and machine configuration files, publisher policy files are contained in their own assembly that must be installed in the global assembly cache.</span></span>

<span data-ttu-id="e5cf5-167">Oto przykład pliku konfiguracji zasad programu Publisher:</span><span class="sxs-lookup"><span data-stu-id="e5cf5-167">The following is an example of a Publisher Policy configuration file:</span></span>

```xml
<configuration>
    <runtime>
        <assemblyBinding xmlns="urn:schemas-microsoft-com:asm.v1">

            <dependentAssembly>
                <assemblyIdentity name="asm6" publicKeyToken="c0305c36380ba429" />
                <bindingRedirect oldVersion="3.0.0.0" newVersion="2.0.0.0"/>
            </dependentAssembly>

        </assemblyBinding>
    </runtime>
</configuration>
```

<span data-ttu-id="e5cf5-168">Aby utworzyć złożenie, można użyć narzędzia [Al.exe (Assembly Linker)](../tools/al-exe-assembly-linker.md) z następującym poleceniem:</span><span class="sxs-lookup"><span data-stu-id="e5cf5-168">To create an assembly, you can use the [Al.exe (Assembly Linker)](../tools/al-exe-assembly-linker.md) tool with a command such as the following:</span></span>

```console
Al.exe /link:asm6.exe.config /out:policy.3.0.asm6.dll /keyfile: compatkey.dat /v:3.0.0.0
```

<span data-ttu-id="e5cf5-169">`compatkey.dat`jest plikiem klucza o silnej nazwie.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-169">`compatkey.dat` is a strong-name key file.</span></span> <span data-ttu-id="e5cf5-170">To polecenie tworzy zestaw o silnej nazwie, który można umieścić w globalnej pamięci podręcznej zestawów.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-170">This command creates a strong-named assembly you can place in the global assembly cache.</span></span>

> [!NOTE]
> <span data-ttu-id="e5cf5-171">Zasady programu Publisher mają wpływ na wszystkie aplikacje korzystające ze składnika udostępnionego.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-171">Publisher policy affects all applications that use a shared component.</span></span>

<span data-ttu-id="e5cf5-172">Plik konfiguracji zasad wydawcy zastępuje informacje o wersji pochodzące z aplikacji (czyli z manifestu zestawu lub z pliku konfiguracji aplikacji).</span><span class="sxs-lookup"><span data-stu-id="e5cf5-172">The publisher policy configuration file overrides version information that comes from the application (that is, from the assembly manifest or from the application configuration file).</span></span> <span data-ttu-id="e5cf5-173">Jeśli w pliku konfiguracji aplikacji nie ma instrukcji, aby przekierować wersję określoną w manifeście zestawu, plik zasad wydawcy zastępuje wersję określoną w manifeście zestawu.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-173">If there is no statement in the application configuration file to redirect the version specified in the assembly manifest, the publisher policy file overrides the version specified in the assembly manifest.</span></span> <span data-ttu-id="e5cf5-174">Jednak jeśli istnieje instrukcja przekierowywania w pliku konfiguracji aplikacji, zasady wydawcy zastępuje tę wersję, a nie określoną w manifeście.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-174">However, if there is a redirecting statement in the application configuration file, publisher policy overrides that version rather than the one specified in the manifest.</span></span>

<span data-ttu-id="e5cf5-175">Plik zasad wydawcy jest używany, gdy składnik udostępniony jest aktualizowany, a nowa wersja składnika udostępnionego powinna zostać pobrana przez wszystkie aplikacje używające tego składnika.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-175">A publisher policy file is used when a shared component is updated and the new version of the shared component should be picked up by all applications using that component.</span></span> <span data-ttu-id="e5cf5-176">Ustawienia w pliku zasad wydawcy zastępują ustawienia w pliku konfiguracji aplikacji, chyba że plik konfiguracji aplikacji wymusza tryb awaryjny.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-176">The settings in the publisher policy file override settings in the application configuration file, unless the application configuration file enforces safe mode.</span></span>

#### <a name="safe-mode"></a><span data-ttu-id="e5cf5-177">Tryb awaryjny</span><span class="sxs-lookup"><span data-stu-id="e5cf5-177">Safe Mode</span></span>
<span data-ttu-id="e5cf5-178">Pliki zasad programu Publisher są zwykle jawnie instalowane jako część dodatku Service Pack lub aktualizacji programu.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-178">Publisher policy files are usually explicitly installed as part of a service pack or program update.</span></span> <span data-ttu-id="e5cf5-179">Jeśli występuje problem z uaktualnionym składnikiem udostępnionym, można zignorować zastąpienia w pliku zasad wydawcy w trybie awaryjnym.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-179">If there is any problem with the upgraded shared component, you can ignore the overrides in the publisher policy file using safe mode.</span></span> <span data-ttu-id="e5cf5-180">Tryb awaryjny jest określany przez \*\* \<wydawcęPolicy apply="yes**&#124;**no"/>\*\* element, znajdujący się tylko w pliku konfiguracji aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-180">Safe mode is determined by the **\<publisherPolicy apply="yes**&#124;**no"/>** element, located only in the application configuration file.</span></span> <span data-ttu-id="e5cf5-181">Określa, czy informacje o konfiguracji zasad wydawcy powinny zostać usunięte z procesu wiązania.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-181">It specifies whether the publisher policy configuration information should be removed from the binding process.</span></span>

<span data-ttu-id="e5cf5-182">Tryb awaryjny można ustawić dla całej aplikacji lub dla wybranych zestawów.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-182">Safe mode can be set for the entire application or for selected assemblies.</span></span> <span data-ttu-id="e5cf5-183">Oznacza to, że można wyłączyć zasady dla wszystkich zestawów, które tworzą aplikację lub włączyć ją dla niektórych zestawów, ale nie dla innych.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-183">That is, you can turn off the policy for all assemblies that make up the application, or turn it on for some assemblies but not others.</span></span> <span data-ttu-id="e5cf5-184">Aby selektywnie zastosować zasady wydawcy do zestawów, które tworzą aplikację, ustaw \< \*\* \<wydawcęPolicja zastosuj\=nie/>\*\* i określ, których zestawów ma być naruszone przy użyciu elementu> **zależności.**</span><span class="sxs-lookup"><span data-stu-id="e5cf5-184">To selectively apply publisher policy to assemblies that make up an application, set **\<publisherPolicy apply\=no/>** and specify which assemblies you want to be affected using the \<**dependentAssembly**> element.</span></span> <span data-ttu-id="e5cf5-185">Aby zastosować zasady wydawcy do wszystkich zestawów, które tworzą aplikację, ustaw \*\* \<publisherPolicy apply\=no/>\*\* bez elementów zestawu zależnego.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-185">To apply publisher policy to all assemblies that make up the application, set **\<publisherPolicy apply\=no/>** with no dependent assembly elements.</span></span> <span data-ttu-id="e5cf5-186">Aby uzyskać więcej informacji o konfiguracji, zobacz [Konfigurowanie aplikacji przy użyciu plików konfiguracyjnych](../configure-apps/index.md).</span><span class="sxs-lookup"><span data-stu-id="e5cf5-186">For more about configuration, see [Configuring Apps by using Configuration Files](../configure-apps/index.md).</span></span>

### <a name="machine-configuration-file"></a><span data-ttu-id="e5cf5-187">Plik konfiguracji komputera</span><span class="sxs-lookup"><span data-stu-id="e5cf5-187">Machine Configuration File</span></span>
<span data-ttu-id="e5cf5-188">Po trzecie, środowisko wykonawcze sprawdza plik konfiguracji komputera.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-188">Third, the runtime examines the machine configuration file.</span></span> <span data-ttu-id="e5cf5-189">Ten plik o nazwie Machine.config znajduje się na komputerze lokalnym w podkatalogu konfiguracyjnym katalogu głównego, w którym jest zainstalowane środowisko wykonawcze.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-189">This file, called Machine.config, resides on the local computer in the Config subdirectory of the root directory where the runtime is installed.</span></span> <span data-ttu-id="e5cf5-190">Ten plik może służyć przez administratorów do określenia ograniczeń powiązania zestawu, które są lokalne dla tego komputera.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-190">This file can be used by administrators to specify assembly binding restrictions that are local to that computer.</span></span> <span data-ttu-id="e5cf5-191">Ustawienia w pliku konfiguracyjnym komputera mają pierwszeństwo przed wszystkimi innymi ustawieniami konfiguracji; nie oznacza to jednak, że wszystkie ustawienia konfiguracji powinny być umieszczone w tym pliku.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-191">The settings in the machine configuration file take precedence over all other configuration settings; however, this does not mean that all configuration settings should be put in this file.</span></span> <span data-ttu-id="e5cf5-192">Wersja określona przez plik zasad administratora jest ostateczna i nie można jej zastąpić.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-192">The version determined by the administrator policy file is final, and cannot be overridden.</span></span> <span data-ttu-id="e5cf5-193">Zastąpienia określone w pliku Machine.config mają wpływ na wszystkie aplikacje.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-193">Overrides specified in the Machine.config file affect all applications.</span></span> <span data-ttu-id="e5cf5-194">Aby uzyskać więcej informacji na temat plików konfiguracyjnych, zobacz [Konfigurowanie aplikacji przy użyciu plików konfiguracyjnych](../configure-apps/index.md).</span><span class="sxs-lookup"><span data-stu-id="e5cf5-194">For more information about configuration files, see [Configuring Apps by using Configuration Files](../configure-apps/index.md).</span></span>

<a name="step2"></a>

## <a name="step-2-checking-for-previously-referenced-assemblies"></a><span data-ttu-id="e5cf5-195">Krok 2. Sprawdzanie zestawów poprzednio występujących w odwołaniu</span><span class="sxs-lookup"><span data-stu-id="e5cf5-195">Step 2: Checking for Previously Referenced Assemblies</span></span>

<span data-ttu-id="e5cf5-196">Jeśli żądany zestaw został również żądany w poprzednich wywołaniach, środowisko uruchomieniowe języka wspólnego używa zestawu, który jest już załadowany.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-196">If the requested assembly has also been requested in previous calls, the common language runtime uses the assembly that is already loaded.</span></span> <span data-ttu-id="e5cf5-197">Może to mieć konsekwencje podczas nazewnictwa zestawów, które tworzą aplikację.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-197">This can have ramifications when naming assemblies that make up an application.</span></span> <span data-ttu-id="e5cf5-198">Aby uzyskać więcej informacji na temat nazewnictwa zestawów, zobacz [Nazwy zestawów](../../standard/assembly/names.md).</span><span class="sxs-lookup"><span data-stu-id="e5cf5-198">For more information about naming assemblies, see [Assembly Names](../../standard/assembly/names.md).</span></span>

<span data-ttu-id="e5cf5-199">Jeśli poprzednie żądanie dla zestawu nie powiodło się, kolejne żądania dla zestawu nie powiedzie się natychmiast bez próby załadowania zestawu.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-199">If a previous request for the assembly failed, subsequent requests for the assembly are failed immediately without attempting to load the assembly.</span></span> <span data-ttu-id="e5cf5-200">Począwszy od programu .NET Framework w wersji 2.0, błędy wiązania zestawu są buforowane, a buforowane informacje są używane do określenia, czy ma być podejmowana próba załadowania zestawu.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-200">Starting with the .NET Framework version 2.0, assembly binding failures are cached, and the cached information is used to determine whether to attempt to load the assembly.</span></span>

> [!NOTE]
> <span data-ttu-id="e5cf5-201">Aby przywrócić zachowanie .NET Framework w wersjach 1.0 i 1.1, które nie buforują błędów wiązania, należy [ \<uwzględnić disablecingBindingFailures> Element](../configure-apps/file-schema/runtime/disablecachingbindingfailures-element.md) w pliku konfiguracji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-201">To revert to the behavior of the .NET Framework versions 1.0 and 1.1, which did not cache binding failures, include the [\<disableCachingBindingFailures> Element](../configure-apps/file-schema/runtime/disablecachingbindingfailures-element.md) in your configuration file.</span></span>

<a name="step3"></a>

## <a name="step-3-checking-the-global-assembly-cache"></a><span data-ttu-id="e5cf5-202">Krok 3. Sprawdzanie globalnej pamięci podręcznej zestawów</span><span class="sxs-lookup"><span data-stu-id="e5cf5-202">Step 3: Checking the Global Assembly Cache</span></span>

<span data-ttu-id="e5cf5-203">W przypadku zestawów o silnej nazwie proces wiązania jest kontynuowany przez wyszukiwanie w globalnej pamięci podręcznej zestawów.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-203">For strong-named assemblies, the binding process continues by looking in the global assembly cache.</span></span> <span data-ttu-id="e5cf5-204">Globalna pamięć podręczna zestawów przechowuje zestawy, które mogą być używane przez kilka aplikacji na komputerze.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-204">The global assembly cache stores assemblies that can be used by several applications on a computer.</span></span> <span data-ttu-id="e5cf5-205">Wszystkie zestawy w globalnej pamięci podręcznej zestawów muszą mieć silne nazwy.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-205">All assemblies in the global assembly cache must have strong names.</span></span>

<a name="step4"></a>

## <a name="step-4-locating-the-assembly-through-codebases-or-probing"></a><span data-ttu-id="e5cf5-206">Krok 4. Lokalizowanie zestawu za pośrednictwem ścieżek bazowych kodu lub sondowania</span><span class="sxs-lookup"><span data-stu-id="e5cf5-206">Step 4: Locating the Assembly through Codebases or Probing</span></span>

<span data-ttu-id="e5cf5-207">Po określeniu poprawnej wersji zestawu przy użyciu informacji w odwołaniu do zestawu wywołującego i w plikach konfiguracyjnych oraz po zaewidencjonowaniu w globalnej pamięci podręcznej zestawów (tylko dla zestawów o silnych nazwach), wspólny język runtime próbuje znaleźć zestaw.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-207">After the correct assembly version has been determined by using the information in the calling assembly's reference and in the configuration files, and after it has checked in the global assembly cache (only for strong-named assemblies), the common language runtime attempts to find the assembly.</span></span> <span data-ttu-id="e5cf5-208">Proces lokalizowania zestawu obejmuje następujące kroki:</span><span class="sxs-lookup"><span data-stu-id="e5cf5-208">The process of locating an assembly involves the following steps:</span></span>

1. <span data-ttu-id="e5cf5-209">Jeśli [ \<w](../configure-apps/file-schema/runtime/codebase-element.md) pliku konfiguracji aplikacji zostanie znaleziony element codeBase>, środowisko wykonawcze sprawdza określoną lokalizację.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-209">If a [\<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element is found in the application configuration file, the runtime checks the specified location.</span></span> <span data-ttu-id="e5cf5-210">Jeśli zostanie znaleziony dopasowania, ten zestaw jest używany i nie sondowania występuje.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-210">If a match is found, that assembly is used and no probing occurs.</span></span> <span data-ttu-id="e5cf5-211">Jeśli zestaw nie zostanie znaleziony tam, żądanie powiązania kończy się niepowodzeniem.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-211">If the assembly is not found there, the binding request fails.</span></span>

2. <span data-ttu-id="e5cf5-212">Środowisko wykonawcze następnie sonduje dla zestawu, do którego istnieje odwołanie, przy użyciu reguł określonych w dalszej części tej sekcji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-212">The runtime then probes for the referenced assembly using the rules specified later in this section.</span></span>

> [!NOTE]
> <span data-ttu-id="e5cf5-213">Jeśli masz wiele wersji zestawu w katalogu i chcesz odwołać się do określonej wersji tego zestawu, należy użyć `privatePath` [ \<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element zamiast atrybutu [ \<sondowania>](../configure-apps/file-schema/runtime/probing-element.md) elementu.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-213">If you have multiple versions of an assembly in a directory and you want to reference a particular version of that assembly, you must use the [\<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element instead of the `privatePath` attribute of the [\<probing>](../configure-apps/file-schema/runtime/probing-element.md) element.</span></span> <span data-ttu-id="e5cf5-214">Jeśli używasz [ \<sondowania>](../configure-apps/file-schema/runtime/probing-element.md) element, środowisko uruchomieniowe zatrzymuje sondowanie po raz pierwszy znajdzie zestaw, który pasuje do prostej nazwy zestawu, do którego odwołuje się, czy jest to poprawne dopasowanie, czy nie.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-214">If you use the [\<probing>](../configure-apps/file-schema/runtime/probing-element.md) element, the runtime stops probing the first time it finds an assembly that matches the simple assembly name referenced, whether it is a correct match or not.</span></span> <span data-ttu-id="e5cf5-215">Jeśli jest to poprawne dopasowanie, używany jest ten zestaw.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-215">If it is a correct match, that assembly is used.</span></span> <span data-ttu-id="e5cf5-216">Jeśli nie jest poprawne dopasowanie, sondowanie zatrzymuje i powiązania nie powiedzie się.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-216">If it is not a correct match, probing stops and binding fails.</span></span>

### <a name="locating-the-assembly-through-codebases"></a><span data-ttu-id="e5cf5-217">Lokalizowanie zestawu za pomocą baz kodu</span><span class="sxs-lookup"><span data-stu-id="e5cf5-217">Locating the Assembly through Codebases</span></span>

<span data-ttu-id="e5cf5-218">Informacje codebase mogą być dostarczane przy użyciu [ \<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element w pliku konfiguracji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-218">Codebase information can be provided by using a [\<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element in a configuration file.</span></span> <span data-ttu-id="e5cf5-219">Ta baza kodu jest zawsze sprawdzana przed próbą sondowania środowiska wykonawczego dla zestawu, do którego istnieje odwołanie.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-219">This codebase is always checked before the runtime attempts to probe for the referenced assembly.</span></span> <span data-ttu-id="e5cf5-220">Jeśli plik zasad wydawcy zawierający przekierowanie ostatecznej wersji zawiera również [ \<element codeBase>,](../configure-apps/file-schema/runtime/codebase-element.md) ten [ \<element codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) jest używany.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-220">If a publisher policy file containing the final version redirect also contains a [\<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element, that [\<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element is the one that is used.</span></span> <span data-ttu-id="e5cf5-221">Na przykład jeśli plik konfiguracji aplikacji określa [ \<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element, a plik zasad wydawcy, który zastępuje informacje o aplikacji określa również [ \<element codeBase>,](../configure-apps/file-schema/runtime/codebase-element.md) jest używany element [ \<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) w pliku zasad wydawcy.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-221">For example, if your application configuration file specifies a [\<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element, and a publisher policy file that is overriding the application information also specifies a [\<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element, the [\<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element in the publisher policy file is used.</span></span>

<span data-ttu-id="e5cf5-222">Jeśli nie zostanie znalezione żadne dopasowanie w lokalizacji określonej przez [ \<element codeBase>,](../configure-apps/file-schema/runtime/codebase-element.md) żądanie powiązania kończy się niepowodzeniem i nie zostaną podjęte żadne dalsze kroki.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-222">If no match is found at the location specified by the [\<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element, the bind request fails and no further steps are taken.</span></span> <span data-ttu-id="e5cf5-223">Jeśli środowisko wykonawcze określa, że zestaw spełnia kryteria zestawu wywołującego, używa tego zestawu.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-223">If the runtime determines that an assembly matches the calling assembly's criteria, it uses that assembly.</span></span> <span data-ttu-id="e5cf5-224">Po załadowaniu pliku określonego przez danego [ \<koduBase>](../configure-apps/file-schema/runtime/codebase-element.md) element jest ładowany, środowisko wykonawcze sprawdza, czy nazwa, wersja, kultura i klucz publiczny są zgodne z odwołaniem zestawu wywołującego.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-224">When the file specified by the given [\<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element is loaded, the runtime checks to make sure that the name, version, culture, and public key match the calling assembly's reference.</span></span>

> [!NOTE]
> <span data-ttu-id="e5cf5-225">Zestawy, do których istnieje odwołanie poza katalogiem głównym aplikacji, muszą mieć silne nazwy i muszą być zainstalowane w globalnej pamięci podręcznej zestawów lub określone przy użyciu [ \<elementu codeBase>.](../configure-apps/file-schema/runtime/codebase-element.md)</span><span class="sxs-lookup"><span data-stu-id="e5cf5-225">Referenced assemblies outside the application's root directory must have strong names and must either be installed in the global assembly cache or specified using the [\<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element.</span></span>

### <a name="locating-the-assembly-through-probing"></a><span data-ttu-id="e5cf5-226">Lokalizowanie zespołu za pomocą sondowania</span><span class="sxs-lookup"><span data-stu-id="e5cf5-226">Locating the Assembly through Probing</span></span>

<span data-ttu-id="e5cf5-227">Jeśli w pliku konfiguracji aplikacji nie ma [ \<elementu codeBase>,](../configure-apps/file-schema/runtime/codebase-element.md) sondy środowiska wykonawczego dla zestawu przy użyciu czterech kryteriów:</span><span class="sxs-lookup"><span data-stu-id="e5cf5-227">If there is no [\<codeBase>](../configure-apps/file-schema/runtime/codebase-element.md) element in the application configuration file, the runtime probes for the assembly using four criteria:</span></span>

- <span data-ttu-id="e5cf5-228">Baza aplikacji, która jest lokalizacją główną, w której aplikacja jest wykonywana.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-228">Application base, which is the root location where the application is being executed.</span></span>

- <span data-ttu-id="e5cf5-229">Kultura, która jest atrybutem kultury zestawu, do którego się odwołuje.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-229">Culture, which is the culture attribute of the assembly being referenced.</span></span>

- <span data-ttu-id="e5cf5-230">Nazwa, która jest nazwą zestawu, do którego istnieje odwołanie.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-230">Name, which is the name of the referenced assembly.</span></span>

- <span data-ttu-id="e5cf5-231">`privatePath` [Atrybut \<sondowania>](../configure-apps/file-schema/runtime/probing-element.md) element, który jest zdefiniowaną przez użytkownika listą podkatalogów w lokalizacji głównej.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-231">The `privatePath` attribute of the [\<probing>](../configure-apps/file-schema/runtime/probing-element.md) element, which is the user-defined list of subdirectories under the root location.</span></span> <span data-ttu-id="e5cf5-232">Tę lokalizację można określić w pliku konfiguracji aplikacji <xref:System.AppDomainSetup.PrivateBinPath?displayProperty=nameWithType> i w kodzie zarządzanym przy użyciu właściwości dla domeny aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-232">This location can be specified in the application configuration file and in managed code using the <xref:System.AppDomainSetup.PrivateBinPath?displayProperty=nameWithType> property for an application domain.</span></span> <span data-ttu-id="e5cf5-233">Po określeniu w kodzie zarządzanym kod `privatePath` zarządzany jest badany jako pierwszy, a następnie ścieżka określona w pliku konfiguracji aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-233">When specified in managed code, the managed code `privatePath` is probed first, followed by the path specified in the application configuration file.</span></span>

#### <a name="probing-the-application-base-and-culture-directories"></a><span data-ttu-id="e5cf5-234">Sondowanie katalogów bazy aplikacji i kultury</span><span class="sxs-lookup"><span data-stu-id="e5cf5-234">Probing the Application Base and Culture Directories</span></span>

<span data-ttu-id="e5cf5-235">Środowisko wykonawcze zawsze rozpoczyna sondowanie w bazie aplikacji, która może być adresem URL lub katalogiem głównym aplikacji na komputerze.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-235">The runtime always begins probing in the application's base, which can be either a URL or the application's root directory on a computer.</span></span> <span data-ttu-id="e5cf5-236">Jeśli zestaw, do którego istnieje odwołanie, nie zostanie znaleziony w bazie aplikacji i nie podano żadnych informacji o kulturze, środowisko wykonawcze przeszukuje wszystkie podkatalogi o nazwie zestawu.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-236">If the referenced assembly is not found in the application base and no culture information is provided, the runtime searches any subdirectories with the assembly name.</span></span> <span data-ttu-id="e5cf5-237">Badane katalogi obejmują:</span><span class="sxs-lookup"><span data-stu-id="e5cf5-237">The directories probed include:</span></span>

- <span data-ttu-id="e5cf5-238">[baza aplikacji] / [nazwa zestawu].dll</span><span class="sxs-lookup"><span data-stu-id="e5cf5-238">[application base] / [assembly name].dll</span></span>

- <span data-ttu-id="e5cf5-239">[baza aplikacji] / [nazwa zestawu] / [nazwa zestawu].dll</span><span class="sxs-lookup"><span data-stu-id="e5cf5-239">[application base] / [assembly name] / [assembly name].dll</span></span>

<span data-ttu-id="e5cf5-240">Jeśli informacje o kulturze są określone dla zestawu, do którego istnieje odwołanie, badane są tylko następujące katalogi:</span><span class="sxs-lookup"><span data-stu-id="e5cf5-240">If culture information is specified for the referenced assembly, only the following directories are probed:</span></span>

- <span data-ttu-id="e5cf5-241">[baza aplikacji] / [kultura] / [nazwa zestawu].dll</span><span class="sxs-lookup"><span data-stu-id="e5cf5-241">[application base] / [culture] / [assembly name].dll</span></span>

- <span data-ttu-id="e5cf5-242">[baza aplikacji] / [kultura] / [nazwa zestawu] / [nazwa zestawu].dll</span><span class="sxs-lookup"><span data-stu-id="e5cf5-242">[application base] / [culture] / [assembly name] / [assembly name].dll</span></span>

#### <a name="probing-with-the-privatepath-attribute"></a><span data-ttu-id="e5cf5-243">Sondowanie za pomocą atrybutu privatePath</span><span class="sxs-lookup"><span data-stu-id="e5cf5-243">Probing with the privatePath Attribute</span></span>

<span data-ttu-id="e5cf5-244">Oprócz podkatalogów kultury i podkatalogów nazwanych dla zestawu, do którego istnieje odwołanie, środowisko wykonawcze sonduje również katalogi określone przy użyciu `privatePath` atrybutu elementu [ \<sondowania>.](../configure-apps/file-schema/runtime/probing-element.md)</span><span class="sxs-lookup"><span data-stu-id="e5cf5-244">In addition to the culture subdirectories and the subdirectories named for the referenced assembly, the runtime also probes directories specified using the `privatePath` attribute of the [\<probing>](../configure-apps/file-schema/runtime/probing-element.md) element.</span></span> <span data-ttu-id="e5cf5-245">Katalogi określone przy `privatePath` użyciu atrybutu muszą być podkatalogami katalogu głównego aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-245">The directories specified using the `privatePath` attribute must be subdirectories of the application's root directory.</span></span> <span data-ttu-id="e5cf5-246">Badane katalogi różnią się w zależności od tego, czy informacje o kulturze są zawarte w żądaniu zestawu, do którego istnieje odwołanie.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-246">The directories probed vary depending on whether culture information is included in the referenced assembly request.</span></span>

<span data-ttu-id="e5cf5-247">Środowisko wykonawcze zatrzymuje sondowanie po raz pierwszy znajdzie zestaw, który pasuje do prostej nazwy zestawu, do którego odwołuje się, czy jest to poprawne dopasowanie, czy nie.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-247">The runtime stops probing the first time it finds an assembly that matches the simple assembly name referenced, whether it is a correct match or not.</span></span> <span data-ttu-id="e5cf5-248">Jeśli jest to poprawne dopasowanie, używany jest ten zestaw.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-248">If it is a correct match, that assembly is used.</span></span> <span data-ttu-id="e5cf5-249">Jeśli nie jest poprawne dopasowanie, sondowanie zatrzymuje i powiązania nie powiedzie się.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-249">If it is not a correct match, probing stops and binding fails.</span></span>

<span data-ttu-id="e5cf5-250">Jeśli kultura jest uwzględniona, badane są następujące katalogi:</span><span class="sxs-lookup"><span data-stu-id="e5cf5-250">If culture is included, the following directories are probed:</span></span>

- <span data-ttu-id="e5cf5-251">[baza aplikacji] / [binpath] / [culture] / [nazwa zestawu].dll</span><span class="sxs-lookup"><span data-stu-id="e5cf5-251">[application base] / [binpath] / [culture] / [assembly name].dll</span></span>

- <span data-ttu-id="e5cf5-252">[baza aplikacji] / [binpath] / [culture] / [nazwa zestawu] / [nazwa zestawu].dll</span><span class="sxs-lookup"><span data-stu-id="e5cf5-252">[application base] / [binpath] / [culture] / [assembly name] / [assembly name].dll</span></span>

<span data-ttu-id="e5cf5-253">Jeśli informacje o kulturze nie są uwzględniane, badane są następujące katalogi:</span><span class="sxs-lookup"><span data-stu-id="e5cf5-253">If culture information is not included, the following directories are probed:</span></span>

- <span data-ttu-id="e5cf5-254">[baza aplikacji] / [binpath] / [nazwa zestawu].dll</span><span class="sxs-lookup"><span data-stu-id="e5cf5-254">[application base] / [binpath] / [assembly name].dll</span></span>

- <span data-ttu-id="e5cf5-255">[baza aplikacji] / [binpath] / [nazwa zestawu] / [nazwa zestawu].dll</span><span class="sxs-lookup"><span data-stu-id="e5cf5-255">[application base] / [binpath] / [assembly name] / [assembly name].dll</span></span>

#### <a name="probing-examples"></a><span data-ttu-id="e5cf5-256">Sondowanie przykładów</span><span class="sxs-lookup"><span data-stu-id="e5cf5-256">Probing Examples</span></span>

<span data-ttu-id="e5cf5-257">Biorąc pod uwagę następujące informacje:</span><span class="sxs-lookup"><span data-stu-id="e5cf5-257">Given the following information:</span></span>

- <span data-ttu-id="e5cf5-258">Nazwa zestawu, do którego istnieje odwołanie: myAssembly</span><span class="sxs-lookup"><span data-stu-id="e5cf5-258">Referenced assembly name: myAssembly</span></span>

- <span data-ttu-id="e5cf5-259">Katalog główny aplikacji:`http://www.code.microsoft.com`</span><span class="sxs-lookup"><span data-stu-id="e5cf5-259">Application root directory: `http://www.code.microsoft.com`</span></span>

- <span data-ttu-id="e5cf5-260">sondowanie>element w pliku konfiguracyjnym określa: bin [ \<](../configure-apps/file-schema/runtime/probing-element.md)</span><span class="sxs-lookup"><span data-stu-id="e5cf5-260">[\<probing>](../configure-apps/file-schema/runtime/probing-element.md) element in configuration file specifies: bin</span></span>

- <span data-ttu-id="e5cf5-261">Kultura: de</span><span class="sxs-lookup"><span data-stu-id="e5cf5-261">Culture: de</span></span>

<span data-ttu-id="e5cf5-262">Środowisko wykonawcze sonduje następujące adresy URL:</span><span class="sxs-lookup"><span data-stu-id="e5cf5-262">The runtime probes the following URLs:</span></span>

- `http://www.code.microsoft.com/de/myAssembly.dll`

- `http://www.code.microsoft.com/de/myAssembly/myAssembly.dll`

- `http://www.code.microsoft.com/bin/de/myAssembly.dll`

- `http://www.code.microsoft.com/bin/de/myAssembly/myAssembly.dll`

##### <a name="multiple-assemblies-with-the-same-name"></a><span data-ttu-id="e5cf5-263">Wiele zestawów o tej samej nazwie</span><span class="sxs-lookup"><span data-stu-id="e5cf5-263">Multiple Assemblies with the Same Name</span></span>

<span data-ttu-id="e5cf5-264">W poniższym przykładzie pokazano, jak skonfigurować wiele zestawów o tej samej nazwie.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-264">The following example shows how to configure multiple assemblies with the same name.</span></span>

```xml
<dependentAssembly>
   <assemblyIdentity name="Server" publicKeyToken="c0305c36380ba429" />
   <codeBase version="1.0.0.0" href="v1/Server.dll" />
   <codeBase version="2.0.0.0" href="v2/Server.dll" />
</dependentAssembly>
```

#### <a name="other-locations-probed"></a><span data-ttu-id="e5cf5-265">Inne lokalizacje badane</span><span class="sxs-lookup"><span data-stu-id="e5cf5-265">Other Locations Probed</span></span>

<span data-ttu-id="e5cf5-266">Lokalizacja zestawu można również określić przy użyciu bieżącego kontekstu wiązania.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-266">Assembly location can also be determined using the current binding context.</span></span> <span data-ttu-id="e5cf5-267">To najczęściej występuje, <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> gdy metoda jest używana i w scenariuszach współdziałać COM.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-267">This most often occurs when the <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> method is used and in COM interop scenarios.</span></span> <span data-ttu-id="e5cf5-268">Jeśli zestaw używa <xref:System.Reflection.Assembly.LoadFrom%2A> metody do odwoływania się do innego zestawu, lokalizacja zestawu wywołującego jest uważana za wskazówkę o tym, gdzie można znaleźć zestaw, do którego istnieje odwołanie.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-268">If an assembly uses the <xref:System.Reflection.Assembly.LoadFrom%2A> method to reference another assembly, the calling assembly's location is considered to be a hint about where to find the referenced assembly.</span></span> <span data-ttu-id="e5cf5-269">Jeśli zostanie znaleziony dopasowanie, ten zestaw jest ładowany.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-269">If a match is found, that assembly is loaded.</span></span> <span data-ttu-id="e5cf5-270">Jeśli nie zostanie znaleziony żaden mecz, środowisko wykonawcze kontynuuje semantyce wyszukiwania, a następnie wysyła zapytanie do Instalatora Windows, aby zapewnić zestaw.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-270">If no match is found, the runtime continues with its search semantics and then queries the Windows Installer to provide the assembly.</span></span> <span data-ttu-id="e5cf5-271">Jeśli nie zestaw jest pod warunkiem, że pasuje do żądania powiązania, wyjątek.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-271">If no assembly is provided that matches the binding request, an exception is thrown.</span></span> <span data-ttu-id="e5cf5-272">Ten wyjątek <xref:System.TypeLoadException> jest w kodzie zarządzanym, <xref:System.IO.FileNotFoundException> jeśli odwołuje się do typu lub jeśli nie znaleziono zestawu ładowanego.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-272">This exception is a <xref:System.TypeLoadException> in managed code if a type was referenced, or a <xref:System.IO.FileNotFoundException> if an assembly being loaded was not found.</span></span>

<span data-ttu-id="e5cf5-273">Na przykład jeśli Assembly1 odwołuje Assembly2 i Assembly1 został pobrany z `http://www.code.microsoft.com/utils`, że lokalizacja jest uważany za wskazówkę o tym, gdzie można znaleźć Assembly2.dll.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-273">For example, if Assembly1 references Assembly2 and Assembly1 was downloaded from `http://www.code.microsoft.com/utils`, that location is considered to be a hint about where to find Assembly2.dll.</span></span> <span data-ttu-id="e5cf5-274">Środowisko wykonawcze następnie sonduje `http://www.code.microsoft.com/utils/Assembly2.dll` dla `http://www.code.microsoft.com/utils/Assembly2/Assembly2.dll`zestawu w i .</span><span class="sxs-lookup"><span data-stu-id="e5cf5-274">The runtime then probes for the assembly in `http://www.code.microsoft.com/utils/Assembly2.dll` and `http://www.code.microsoft.com/utils/Assembly2/Assembly2.dll`.</span></span> <span data-ttu-id="e5cf5-275">Jeśli Assembly2 nie zostanie znaleziony w żadnej z tych lokalizacji, środowisko wykonawcze wysyła zapytanie do Instalatora Windows.</span><span class="sxs-lookup"><span data-stu-id="e5cf5-275">If Assembly2 is not found at either of those locations, the runtime queries the Windows Installer.</span></span>

## <a name="see-also"></a><span data-ttu-id="e5cf5-276">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="e5cf5-276">See also</span></span>

- [<span data-ttu-id="e5cf5-277">Najlepsze praktyki dotyczące ładowania zestawu</span><span class="sxs-lookup"><span data-stu-id="e5cf5-277">Best Practices for Assembly Loading</span></span>](best-practices-for-assembly-loading.md)
- [<span data-ttu-id="e5cf5-278">Wdrożenie</span><span class="sxs-lookup"><span data-stu-id="e5cf5-278">Deployment</span></span>](index.md)
