---
title: Konfigurowanie protokołów HTTP i HTTPS
description: Dowiedz się, jak skonfigurować protokół HTTP/HTTPS, aby umożliwić komunikację z usługami i klientami WCF. Skonfiguruj rejestrację adresu URL i wyjątek zapory przy użyciu Netsh.exe.
ms.date: 04/08/2019
helpviewer_keywords:
- configuring HTTP [WCF]
ms.assetid: b0c29a86-bc0c-41b3-bc1e-4eb5bb5714d4
ms.openlocfilehash: fbff78ff8e2c5c4fa73a56a3fdc15163596aa985
ms.sourcegitcommit: 358a28048f36a8dca39a9fe6e6ac1f1913acadd5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/23/2020
ms.locfileid: "85245144"
---
# <a name="configuring-http-and-https"></a><span data-ttu-id="97a62-104">Konfigurowanie protokołów HTTP i HTTPS</span><span class="sxs-lookup"><span data-stu-id="97a62-104">Configuring HTTP and HTTPS</span></span>

<span data-ttu-id="97a62-105">Usługi i klienci WCF mogą komunikować się za pośrednictwem protokołów HTTP i HTTPS.</span><span class="sxs-lookup"><span data-stu-id="97a62-105">WCF services and clients can communicate over HTTP and HTTPS.</span></span> <span data-ttu-id="97a62-106">Ustawienia protokołu HTTP/HTTPS są konfigurowane przy użyciu Internet Information Services (IIS) lub przy użyciu narzędzia wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="97a62-106">The HTTP/HTTPS settings are configured by using Internet Information Services (IIS) or through the use of a command-line tool.</span></span> <span data-ttu-id="97a62-107">Gdy usługa WCF jest hostowana w obszarze Ustawienia HTTP lub HTTPS usługi IIS, można skonfigurować w ramach usług IIS (za pomocą narzędzia inetmgr.exe).</span><span class="sxs-lookup"><span data-stu-id="97a62-107">When a WCF service is hosted under IIS HTTP or HTTPS settings can be configured within IIS (using the inetmgr.exe tool).</span></span> <span data-ttu-id="97a62-108">Jeśli usługa WCF jest samodzielna, ustawienia protokołu HTTP lub HTTPS są konfigurowane przy użyciu narzędzia wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="97a62-108">If a WCF service is self-hosted, HTTP or HTTPS settings are configured by using a command-line tool.</span></span>

<span data-ttu-id="97a62-109">Aby skonfigurować rejestrację adresów URL i dodać wyjątek zapory dla adresu URL, który będzie używany przez usługę, należy określić co najmniej.</span><span class="sxs-lookup"><span data-stu-id="97a62-109">At a minimum, you want to configure a URL registration and add a Firewall exception for the URL your service will be using.</span></span> <span data-ttu-id="97a62-110">Te ustawienia można skonfigurować za pomocą narzędzia Netsh.exe.</span><span class="sxs-lookup"><span data-stu-id="97a62-110">You can configure these settings with the Netsh.exe tool.</span></span>

## <a name="configuring-namespace-reservations"></a><span data-ttu-id="97a62-111">Konfigurowanie rezerwacji przestrzeni nazw</span><span class="sxs-lookup"><span data-stu-id="97a62-111">Configuring namespace reservations</span></span>

<span data-ttu-id="97a62-112">Rezerwacja przestrzeni nazw przypisuje prawa dla części przestrzeni nazw HTTP URL do określonej grupy użytkowników.</span><span class="sxs-lookup"><span data-stu-id="97a62-112">Namespace reservation assigns the rights for a portion of the HTTP URL namespace to a particular group of users.</span></span> <span data-ttu-id="97a62-113">Rezerwacja daje tym użytkownikom prawo do tworzenia usług, które nasłuchują na tej części przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="97a62-113">A reservation gives those users the right to create services that listen on that portion of the namespace.</span></span> <span data-ttu-id="97a62-114">Rezerwacje są prefiksami adresów URL, co oznacza, że rezerwacja obejmuje wszystkie ścieżki podrzędne ścieżki rezerwacji.</span><span class="sxs-lookup"><span data-stu-id="97a62-114">Reservations are URL prefixes, meaning that the reservation covers all subpaths of the reservation path.</span></span> <span data-ttu-id="97a62-115">Rezerwacje przestrzeni nazw zezwalają na dwa sposoby używania symboli wieloznacznych.</span><span class="sxs-lookup"><span data-stu-id="97a62-115">Namespace reservations permit two ways to use wildcards.</span></span> <span data-ttu-id="97a62-116">Dokumentacja interfejsu API serwera HTTP opisuje [kolejność rozpoznawania oświadczeń między przestrzeniami nazw, które obejmują symbole wieloznaczne](/windows/desktop/Http/routing-incoming-requests).</span><span class="sxs-lookup"><span data-stu-id="97a62-116">The HTTP Server API documentation describes the [order of resolution between namespace claims that involve wildcards](/windows/desktop/Http/routing-incoming-requests).</span></span>

<span data-ttu-id="97a62-117">Działająca aplikacja może utworzyć podobne żądanie, aby dodać rejestracje przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="97a62-117">A running application can create a similar request to add namespace registrations.</span></span> <span data-ttu-id="97a62-118">Rejestracje i rezerwacje konkurują w przypadku części przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="97a62-118">Registrations and reservations compete for portions of the namespace.</span></span> <span data-ttu-id="97a62-119">Rezerwacja może mieć pierwszeństwo przed rejestracją zgodnie z kolejnością rozwiązywania [między oświadczeniami przestrzeni nazw, które obejmują symbole wieloznaczne](/windows/desktop/Http/routing-incoming-requests).</span><span class="sxs-lookup"><span data-stu-id="97a62-119">A reservation may have precedence over a registration according to the order of resolution given in the [order of resolution between namespace claims that involve wildcards](/windows/desktop/Http/routing-incoming-requests).</span></span> <span data-ttu-id="97a62-120">W takim przypadku rezerwacja blokuje uruchomioną aplikację z otrzymywania żądań.</span><span class="sxs-lookup"><span data-stu-id="97a62-120">In this case, the reservation blocks the running application from receiving requests.</span></span>

<span data-ttu-id="97a62-121">Poniższy przykład używa narzędzia Netsh.exe:</span><span class="sxs-lookup"><span data-stu-id="97a62-121">The following example uses the Netsh.exe tool:</span></span>

```console
netsh http add urlacl url=http://+:80/MyUri user=DOMAIN\user
```

<span data-ttu-id="97a62-122">To polecenie dodaje rezerwację adresu URL dla określonego obszaru nazw URL dla konta domena\nazwa użytkownika.</span><span class="sxs-lookup"><span data-stu-id="97a62-122">This command adds a URL reservation for the specified URL namespace for the DOMAIN\user account.</span></span> <span data-ttu-id="97a62-123">Aby uzyskać więcej informacji na temat korzystania z polecenia netsh, wpisz `netsh http add urlacl /?` w wierszu polecenia i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="97a62-123">For more information on using the netsh command, type `netsh http add urlacl /?` in a command-prompt and press Enter.</span></span>

## <a name="configuring-a-firewall-exception"></a><span data-ttu-id="97a62-124">Konfigurowanie wyjątku zapory</span><span class="sxs-lookup"><span data-stu-id="97a62-124">Configuring a firewall exception</span></span>

<span data-ttu-id="97a62-125">Podczas samodzielnego udostępniania usługi WCF, która komunikuje się za pośrednictwem protokołu HTTP, należy dodać do konfiguracji zapory wyjątek, aby zezwolić na połączenia przychodzące przy użyciu określonego adresu URL.</span><span class="sxs-lookup"><span data-stu-id="97a62-125">When self-hosting a WCF service that communicates over HTTP, an exception must be added to the firewall configuration to allow inbound connections using a particular URL.</span></span>

## <a name="configuring-ssl-certificates"></a><span data-ttu-id="97a62-126">Konfigurowanie certyfikatów SSL</span><span class="sxs-lookup"><span data-stu-id="97a62-126">Configuring SSL certificates</span></span>

<span data-ttu-id="97a62-127">Protokół SSL (SSL) używa certyfikatów na kliencie i serwerze do przechowywania kluczy szyfrowania.</span><span class="sxs-lookup"><span data-stu-id="97a62-127">The Secure Sockets Layer (SSL) protocol uses certificates on the client and server to store encryption keys.</span></span> <span data-ttu-id="97a62-128">Serwer udostępnia swój certyfikat SSL podczas ustanawiania połączenia, aby klient mógł zweryfikować tożsamość serwera.</span><span class="sxs-lookup"><span data-stu-id="97a62-128">The server provides its SSL certificate when a connection is made so that the client can verify the server identity.</span></span> <span data-ttu-id="97a62-129">Serwer może również zażądać certyfikatu od klienta, aby zapewnić wzajemne uwierzytelnienie obu stron połączenia.</span><span class="sxs-lookup"><span data-stu-id="97a62-129">The server can also request a certificate from the client to provide mutual authentication of both sides of the connection.</span></span>

<span data-ttu-id="97a62-130">Certyfikaty są przechowywane w scentralizowanym magazynie zgodnie z adresem IP i numerem portu połączenia.</span><span class="sxs-lookup"><span data-stu-id="97a62-130">Certificates are stored in a centralized store according to the IP address and port number of the connection.</span></span> <span data-ttu-id="97a62-131">Specjalny adres IP 0.0.0.0 jest zgodny z dowolnym adresem IP komputera lokalnego.</span><span class="sxs-lookup"><span data-stu-id="97a62-131">The special IP address 0.0.0.0 matches any IP address for the local machine.</span></span> <span data-ttu-id="97a62-132">Należy pamiętać, że magazyn certyfikatów nie rozróżnia adresów URL na podstawie ścieżki.</span><span class="sxs-lookup"><span data-stu-id="97a62-132">Note that the certificate store doesn't distinguish URLs based on the path.</span></span> <span data-ttu-id="97a62-133">Usługi o tej samej kombinacji adresów IP i portów muszą udostępniać certyfikaty nawet wtedy, gdy ścieżka w adresie URL usług jest inna.</span><span class="sxs-lookup"><span data-stu-id="97a62-133">Services with the same IP address and port combination must share certificates even if the path in the URL for the services is different.</span></span>

<span data-ttu-id="97a62-134">Instrukcje krok po kroku znajdują się w temacie [How to: Configure a port with a SSL Certificate](how-to-configure-a-port-with-an-ssl-certificate.md).</span><span class="sxs-lookup"><span data-stu-id="97a62-134">For step-by-step instructions, see [How to: Configure a Port with an SSL Certificate](how-to-configure-a-port-with-an-ssl-certificate.md).</span></span>

## <a name="configuring-the-ip-listen-list"></a><span data-ttu-id="97a62-135">Konfigurowanie listy nasłuchiwania adresów IP</span><span class="sxs-lookup"><span data-stu-id="97a62-135">Configuring the IP Listen List</span></span>

<span data-ttu-id="97a62-136">Interfejs API serwera HTTP wiąże się tylko z adresem IP i portem, gdy użytkownik zarejestruje adres URL.</span><span class="sxs-lookup"><span data-stu-id="97a62-136">The HTTP Server API only binds to an IP address and port once a user registers a URL.</span></span> <span data-ttu-id="97a62-137">Domyślnie interfejs API serwera HTTP wiąże się z portem w adresie URL dla wszystkich adresów IP maszyny.</span><span class="sxs-lookup"><span data-stu-id="97a62-137">By default, the HTTP Server API binds to the port in the URL for all of the IP addresses of the machine.</span></span> <span data-ttu-id="97a62-138">Występuje konflikt, jeśli aplikacja, która nie korzysta z interfejsu API serwera HTTP, została wcześniej powiązana z tą kombinacją adresu IP i portu.</span><span class="sxs-lookup"><span data-stu-id="97a62-138">A conflict arises if an application that doesn't use the HTTP Server API has previously bound to that combination of IP address and port.</span></span> <span data-ttu-id="97a62-139">Lista nasłuchiwania adresów IP pozwala usługom WCF współistnieć z aplikacjami, które używają portu dla niektórych adresów IP maszyny.</span><span class="sxs-lookup"><span data-stu-id="97a62-139">The IP Listen List allows WCF services to coexist with applications that use a port for some of the IP addresses of the machine.</span></span> <span data-ttu-id="97a62-140">Jeśli lista nasłuchu IP zawiera jakieś wpisy, interfejs API serwera HTTP tworzy powiązanie tylko z tymi adresami IP, które określa lista.</span><span class="sxs-lookup"><span data-stu-id="97a62-140">If the IP Listen List contains any entries, the HTTP Server API only binds to those IP addresses that the list specifies.</span></span> <span data-ttu-id="97a62-141">Modyfikowanie listy nasłuchiwania adresów IP wymaga uprawnień administracyjnych.</span><span class="sxs-lookup"><span data-stu-id="97a62-141">Modifying the IP Listen List requires administrative privileges.</span></span>

<span data-ttu-id="97a62-142">Użyj narzędzia Netsh, aby zmodyfikować listę nasłuchiwania adresów IP, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="97a62-142">Use the netsh tool to modify the IP Listen List, as shown in the following example:</span></span>

```console
netsh http add iplisten ipaddress=0.0.0.0:8000
```

## <a name="other-configuration-settings"></a><span data-ttu-id="97a62-143">Inne ustawienia konfiguracji</span><span class="sxs-lookup"><span data-stu-id="97a62-143">Other configuration settings</span></span>

<span data-ttu-id="97a62-144">W przypadku korzystania <xref:System.ServiceModel.WSDualHttpBinding> z programu połączenie z klientem wykorzystuje wartości domyślne, które są zgodne z rezerwacjami przestrzeni nazw i Zaporą systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="97a62-144">When using <xref:System.ServiceModel.WSDualHttpBinding>, the client connection uses defaults that are compatible with namespace reservations and the Windows firewall.</span></span> <span data-ttu-id="97a62-145">W przypadku wybrania opcji dostosowania adresu podstawowego klienta podwójnego połączenia należy również skonfigurować te ustawienia protokołu HTTP na kliencie, aby odpowiadały nowemu adresowi.</span><span class="sxs-lookup"><span data-stu-id="97a62-145">If you choose to customize the client base address of a dual connection, then you also must configure these HTTP settings on the client to match the new address.</span></span>

<span data-ttu-id="97a62-146">Interfejs API serwera HTTP zawiera niektóre zaawansowane ustawienia konfiguracji, które nie są dostępne za pośrednictwem usługi HttpCfg.</span><span class="sxs-lookup"><span data-stu-id="97a62-146">The HTTP Server API has some advanced configuration settings that aren't available through HttpCfg.</span></span> <span data-ttu-id="97a62-147">Te ustawienia są przechowywane w rejestrze i stosowane do wszystkich aplikacji działających w systemach, które używają interfejsów API serwera HTTP.</span><span class="sxs-lookup"><span data-stu-id="97a62-147">These settings are maintained in the registry and apply to all applications running on the systems that use the HTTP Server APIs.</span></span> <span data-ttu-id="97a62-148">Aby uzyskać informacje na temat tych ustawień, zobacz [Http.sys ustawienia rejestru dla usług IIS](https://support.microsoft.com/help/820129/http-sys-registry-settings-for-windows).</span><span class="sxs-lookup"><span data-stu-id="97a62-148">For information about these settings, see [Http.sys registry settings for IIS](https://support.microsoft.com/help/820129/http-sys-registry-settings-for-windows).</span></span> <span data-ttu-id="97a62-149">Większość użytkowników nie musi zmieniać tych ustawień.</span><span class="sxs-lookup"><span data-stu-id="97a62-149">Most users don't need to change these settings.</span></span>

## <a name="see-also"></a><span data-ttu-id="97a62-150">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="97a62-150">See also</span></span>

- <xref:System.ServiceModel.WSDualHttpBinding>
- [<span data-ttu-id="97a62-151">Instrukcje: konfigurowanie portu z certyfikatem SSL</span><span class="sxs-lookup"><span data-stu-id="97a62-151">How to: Configure a Port with an SSL Certificate</span></span>](how-to-configure-a-port-with-an-ssl-certificate.md)
