---
title: "\"#Else\" musi być poprzedzona odpowiadającą \"#If\" lub \"#ElseIf\""
ms.date: 07/20/2015
f1_keywords:
- vbc30028
- bc30028
helpviewer_keywords:
- BC30028
ms.assetid: c6ed34de-d6ed-4227-9880-538055aff20a
ms.openlocfilehash: 198ca3377dffbece3ea6334f967cdfce666eeb13
ms.sourcegitcommit: 6b308cf6d627d78ee36dbbae8972a310ac7fd6c8
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 01/23/2019
ms.locfileid: "54494343"
---
# <a name="else-must-be-preceded-by-a-matching-if-or-elseif"></a><span data-ttu-id="b9d18-102">"#Else" musi być poprzedzona odpowiadającą "#If" lub "#ElseIf"</span><span class="sxs-lookup"><span data-stu-id="b9d18-102">'#Else' must be preceded by a matching '#If' or '#ElseIf'</span></span>
<span data-ttu-id="b9d18-103">`#Else` jest dyrektywą warunkowa.</span><span class="sxs-lookup"><span data-stu-id="b9d18-103">`#Else` is a conditional compilation directive.</span></span> <span data-ttu-id="b9d18-104">`#Else` Dyrektywy nie jest poprzedzony przez odpowiednią `#If` lub `#ElseIf` dyrektywy.</span><span class="sxs-lookup"><span data-stu-id="b9d18-104">An `#Else` directive is not preceded by a corresponding `#If` or `#ElseIf` directive.</span></span>  
  
 <span data-ttu-id="b9d18-105">**Identyfikator błędu:** BC30028</span><span class="sxs-lookup"><span data-stu-id="b9d18-105">**Error ID:** BC30028</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="b9d18-106">Aby poprawić ten błąd</span><span class="sxs-lookup"><span data-stu-id="b9d18-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="b9d18-107">Sprawdź, czy występuje przed `#If` lub `#ElseIf` nie jest oddzielony od to `#Else` przez pośredniczące bloku kompilacja warunkowa lub nieprawidłowo umieszczony `#End If`.</span><span class="sxs-lookup"><span data-stu-id="b9d18-107">Check that a preceding `#If` or `#ElseIf` is not separated from this `#Else` by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
2.  <span data-ttu-id="b9d18-108">Sprawdź, czy `#Else` jest poprzedzony przez inny `#Else` dyrektywy.</span><span class="sxs-lookup"><span data-stu-id="b9d18-108">Check that `#Else` is preceded by another `#Else` directive.</span></span> <span data-ttu-id="b9d18-109">Jeśli tak jest, albo usuń `#Else` lub zmień ją na `#ElseIf`.</span><span class="sxs-lookup"><span data-stu-id="b9d18-109">If it is, either remove `#Else` or change it to an `#ElseIf`.</span></span>  
  
3.  <span data-ttu-id="b9d18-110">Jeśli cała reszta będzie w kolejności, należy dodać `#If` dyrektywę na początku bloku kompilacji warunkowej.</span><span class="sxs-lookup"><span data-stu-id="b9d18-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="b9d18-111">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="b9d18-111">See also</span></span>
- [<span data-ttu-id="b9d18-112">#If...Then...#Else, dyrektywy</span><span class="sxs-lookup"><span data-stu-id="b9d18-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
