---
title: Zapis w bezpieczny i skuteczny C# kodu
description: Najnowsze ulepszenia C# języka umożliwiają pisanie weryfikowalny kod bezpieczny, że wydajność była poprzednio skojarzona z niebezpieczny kod.
ms.date: 10/23/2018
ms.custom: mvc
ms.openlocfilehash: 35d9cf89d8ba2ddb673554a76eb33ae59b178b42
ms.sourcegitcommit: bdd930b5df20a45c29483d905526a2a3e4d17c5b
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 12/11/2018
ms.locfileid: "53245686"
---
# <a name="write-safe-and-efficient-c-code"></a><span data-ttu-id="d4701-103">Zapis w bezpieczny i skuteczny C# kodu</span><span class="sxs-lookup"><span data-stu-id="d4701-103">Write safe and efficient C# code</span></span>

<span data-ttu-id="d4701-104">Nowe funkcje w C# umożliwiają pisanie weryfikowalny kod bezpieczny lepszą wydajność.</span><span class="sxs-lookup"><span data-stu-id="d4701-104">New features in C# enable you to write verifiable safe code with better performance.</span></span> <span data-ttu-id="d4701-105">Jeśli zastosujesz dokładnie te techniki, mniej scenariusze wymagają niebezpieczny kod.</span><span class="sxs-lookup"><span data-stu-id="d4701-105">If you carefully apply these techniques, fewer scenarios require unsafe code.</span></span> <span data-ttu-id="d4701-106">Te funkcje ułatwiają użyć odwołania do typów wartości jako metoda argumenty i zwraca metoda.</span><span class="sxs-lookup"><span data-stu-id="d4701-106">These features make it easier to use references to value types as method arguments and method returns.</span></span> <span data-ttu-id="d4701-107">Po zakończeniu bezpiecznie tych technik zminimalizować kopiowania typów wartości.</span><span class="sxs-lookup"><span data-stu-id="d4701-107">When done safely, these techniques minimize copying value types.</span></span> <span data-ttu-id="d4701-108">Korzystając z typów wartości, można zminimalizować liczbę alokacji i przekazuje kolekcji wyrzucania elementów.</span><span class="sxs-lookup"><span data-stu-id="d4701-108">By using value types, you can minimize the number of allocations and garbage collection passes.</span></span>

<span data-ttu-id="d4701-109">Duża część przykładowego kodu w tym artykule używa funkcji dodanych w C# 7.2.</span><span class="sxs-lookup"><span data-stu-id="d4701-109">Much of the sample code in this article uses features added in C# 7.2.</span></span> <span data-ttu-id="d4701-110">Aby korzystać z tych funkcji, należy skonfigurować projekt do C# 7.2 lub nowsza.</span><span class="sxs-lookup"><span data-stu-id="d4701-110">To use those features, you must configure your project to use C# 7.2 or later.</span></span> <span data-ttu-id="d4701-111">Aby uzyskać więcej informacji na temat ustawiania wersji języka, zobacz [skonfigurować wersję językową](language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="d4701-111">For more information on setting the language version, see [configure the language version](language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="d4701-112">Ten artykuł koncentruje się na temat metod do zarządzania zasobami wydajne.</span><span class="sxs-lookup"><span data-stu-id="d4701-112">This article focuses on techniques for efficient resource management.</span></span> <span data-ttu-id="d4701-113">Jedną z zalet przy użyciu typów wartości jest, aby uniknąć często alokacji sterty.</span><span class="sxs-lookup"><span data-stu-id="d4701-113">One advantage to using value types is that they often avoid heap allocations.</span></span> <span data-ttu-id="d4701-114">Wadą jest to zostaną skopiowane przez wartość.</span><span class="sxs-lookup"><span data-stu-id="d4701-114">The disadvantage is that they're copied by value.</span></span> <span data-ttu-id="d4701-115">To kosztem utrudnienie Optymalizowanie algorytmów, które działają na dużych ilości danych.</span><span class="sxs-lookup"><span data-stu-id="d4701-115">This tradeoff makes it harder to optimize algorithms that operate on large amounts of data.</span></span> <span data-ttu-id="d4701-116">Nowy język funkcje w C# 7.2 zapewniają mechanizmy, które umożliwiają bezpieczne wydajność kodu przy użyciu odwołań do typów wartości.</span><span class="sxs-lookup"><span data-stu-id="d4701-116">New language features in C# 7.2 provide mechanisms that enable safe efficient code using references to value types.</span></span> <span data-ttu-id="d4701-117">Należy uważnie używać tych funkcji do minimum zarówno alokacji i operacje kopiowania.</span><span class="sxs-lookup"><span data-stu-id="d4701-117">Use these features wisely to minimize both allocations and copy operations.</span></span> <span data-ttu-id="d4701-118">W tym artykule przeanalizowano tych nowych funkcji.</span><span class="sxs-lookup"><span data-stu-id="d4701-118">This article explores those new features.</span></span>

<span data-ttu-id="d4701-119">Ten artykuł koncentruje się na następujących technik zarządzania zasobów:</span><span class="sxs-lookup"><span data-stu-id="d4701-119">This article focuses on the following resource management techniques:</span></span>

- <span data-ttu-id="d4701-120">Zadeklaruj [ `readonly struct` ](language-reference/keywords/readonly.md#readonly-struct-example) programu express, że typ jest **niezmienialnych** i umożliwia kompilatorowi Zapisuj kopie w przypadku korzystania z [ `in` ](language-reference/keywords/in-parameter-modifier.md) parametrów.</span><span class="sxs-lookup"><span data-stu-id="d4701-120">Declare a [`readonly struct`](language-reference/keywords/readonly.md#readonly-struct-example) to express that a type is **immutable** and enables the compiler to save copies when using [`in`](language-reference/keywords/in-parameter-modifier.md) parameters.</span></span>
- <span data-ttu-id="d4701-121">Użyj [ `ref readonly` ](language-reference/keywords/ref.md#reference-return-values) zwrócenia, jeśli wartość zwracana to `struct` większy niż <xref:System.IntPtr.Size?displayProperty=nameWithType> i okresu istnienia pamięci masowej jest większa niż metoda zwracania wartości.</span><span class="sxs-lookup"><span data-stu-id="d4701-121">Use a [`ref readonly`](language-reference/keywords/ref.md#reference-return-values) return when the return value is a `struct` larger than <xref:System.IntPtr.Size?displayProperty=nameWithType> and the storage lifetime is greater than the method returning the value.</span></span>
- <span data-ttu-id="d4701-122">Gdy rozmiar `readonly struct` jest większy niż <xref:System.IntPtr.Size?displayProperty=nameWithType>, należy przekazać go jako `in` parametr ze względu na wydajność.</span><span class="sxs-lookup"><span data-stu-id="d4701-122">When the size of a `readonly struct` is bigger than <xref:System.IntPtr.Size?displayProperty=nameWithType>, you should pass it as an `in` parameter for performance reasons.</span></span>
- <span data-ttu-id="d4701-123">Nigdy nie przekazać `struct` jako `in` parametr o ile nie jest zadeklarowana za pomocą `readonly` modyfikator ponieważ może negatywnie wpłynąć na wydajność i może prowadzić do zasłoniętej zachowania.</span><span class="sxs-lookup"><span data-stu-id="d4701-123">Never pass a `struct` as an `in` parameter unless it's declared with the `readonly` modifier because it may negatively affect performance and could lead to an obscure behavior.</span></span>
- <span data-ttu-id="d4701-124">Użyj [ `ref struct` ](language-reference/keywords/ref.md#ref-struct-types), lub `readonly ref struct` takich jak <xref:System.Span%601> lub <xref:System.ReadOnlySpan%601> do pracy z pamięcią za pomocą sekwencji bajtów.</span><span class="sxs-lookup"><span data-stu-id="d4701-124">Use a [`ref struct`](language-reference/keywords/ref.md#ref-struct-types), or a `readonly ref struct` such as <xref:System.Span%601> or <xref:System.ReadOnlySpan%601> to work with memory as a sequence of bytes.</span></span>

<span data-ttu-id="d4701-125">Te techniki wymusić równoważyć dwóch celów konkurencyjnych w odniesieniu do **odwołania** i **wartości**.</span><span class="sxs-lookup"><span data-stu-id="d4701-125">These techniques force you to balance two competing goals with regard to **references** and **values**.</span></span> <span data-ttu-id="d4701-126">Zmienne, które są [typy odwołań](programming-guide/types/index.md#reference-types) utrzymywanie odwołania do lokalizacji w pamięci.</span><span class="sxs-lookup"><span data-stu-id="d4701-126">Variables that are [reference types](programming-guide/types/index.md#reference-types) hold a reference to the location in memory.</span></span> <span data-ttu-id="d4701-127">Zmienne, które są [typy wartości](programming-guide/types/index.md#value-types) bezpośrednio zawierać ich wartości.</span><span class="sxs-lookup"><span data-stu-id="d4701-127">Variables that are [value types](programming-guide/types/index.md#value-types) directly contain their value.</span></span> <span data-ttu-id="d4701-128">Te różnice wyróżnienie podstawowe różnice, które są ważne na potrzeby zarządzania zasobami pamięci.</span><span class="sxs-lookup"><span data-stu-id="d4701-128">These differences highlight the key differences that are important for managing memory resources.</span></span> <span data-ttu-id="d4701-129">**Typy wartości** zwykle są kopiowane, gdy przekazywane do metody lub zwrócone z metody.</span><span class="sxs-lookup"><span data-stu-id="d4701-129">**Value types** are typically copied when passed to a method or returned from a method.</span></span> <span data-ttu-id="d4701-130">To zachowanie obejmuje kopiowanie wartości `this` podczas wywoływania elementów członkowskich typu wartości.</span><span class="sxs-lookup"><span data-stu-id="d4701-130">This behavior includes copying the value of `this` when calling members of a value type.</span></span> <span data-ttu-id="d4701-131">Koszt kopii jest powiązana z rozmiaru typu.</span><span class="sxs-lookup"><span data-stu-id="d4701-131">The cost of the copy is related to the size of the type.</span></span> <span data-ttu-id="d4701-132">**Typy odwołań** są przydzielane na zarządzanym stosie.</span><span class="sxs-lookup"><span data-stu-id="d4701-132">**Reference types** are allocated on the managed heap.</span></span> <span data-ttu-id="d4701-133">Każdy nowy obiekt wymaga nowej alokacji, a następnie należy odzyskać.</span><span class="sxs-lookup"><span data-stu-id="d4701-133">Each new object requires a new allocation, and subsequently must be reclaimed.</span></span> <span data-ttu-id="d4701-134">Oba te operacje trwają czasu.</span><span class="sxs-lookup"><span data-stu-id="d4701-134">Both these operations take time.</span></span> <span data-ttu-id="d4701-135">Odwołanie jest kopiowane, gdy typ odwołania jest przekazywany jako argument do metody lub zwrócone z metody.</span><span class="sxs-lookup"><span data-stu-id="d4701-135">The reference is copied when a reference type is passed as an argument to a method or returned from a method.</span></span>

<span data-ttu-id="d4701-136">W tym artykule używa następujących koncepcji przykład struktury punktu 3W, aby wyjaśnić te zalecenia:</span><span class="sxs-lookup"><span data-stu-id="d4701-136">This article uses the following example concept of the 3D-point structure to explain these recommendations:</span></span>

```csharp
public struct Point3D
{
    public double X;
    public double Y;
    public double Z;
}
```

<span data-ttu-id="d4701-137">Różne przykłady używają różne implementacje tę koncepcję.</span><span class="sxs-lookup"><span data-stu-id="d4701-137">Different examples use different implementations of this concept.</span></span>

## <a name="declare-readonly-structs-for-immutable-value-types"></a><span data-ttu-id="d4701-138">Zadeklaruj struktur tylko do odczytu dla typów wartości niezmienialnych</span><span class="sxs-lookup"><span data-stu-id="d4701-138">Declare readonly structs for immutable value types</span></span>

<span data-ttu-id="d4701-139">Deklarowanie `struct` przy użyciu `readonly` modyfikator informuje kompilator, zgodne z zamiarami użytkownika jest utworzenie typu niezmienne.</span><span class="sxs-lookup"><span data-stu-id="d4701-139">Declaring a `struct` using the `readonly` modifier informs the compiler that your intent is to create an immutable type.</span></span> <span data-ttu-id="d4701-140">Kompilator wymusza tej decyzji projektowych, z następującymi regułami:</span><span class="sxs-lookup"><span data-stu-id="d4701-140">The compiler enforces that design decision with the following rules:</span></span>

- <span data-ttu-id="d4701-141">Wszystkie elementy członkowskie pola musi być `readonly`</span><span class="sxs-lookup"><span data-stu-id="d4701-141">All field members must be `readonly`</span></span>
- <span data-ttu-id="d4701-142">Wszystkie właściwości muszą być tylko do odczytu, w tym właściwości zaimplementowane automatycznie.</span><span class="sxs-lookup"><span data-stu-id="d4701-142">All properties must be read-only, including auto-implemented properties.</span></span>

<span data-ttu-id="d4701-143">Te dwie reguły są wystarczające, aby upewnić się, że żaden członek `readonly struct` modyfikuje stan tej struktury.</span><span class="sxs-lookup"><span data-stu-id="d4701-143">These two rules are sufficient to ensure that no member of a `readonly struct` modifies the state of that struct.</span></span> <span data-ttu-id="d4701-144">`struct` Można modyfikować.</span><span class="sxs-lookup"><span data-stu-id="d4701-144">The `struct` is immutable.</span></span> <span data-ttu-id="d4701-145">`Point3D` Strukturę można zdefiniować jako niezmienialny struktury, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="d4701-145">The `Point3D` structure could be defined as an immutable struct as shown in the following example:</span></span>

```csharp
readonly public struct ReadonlyPoint3D
{
    public ReadonlyPoint3D(double x, double y, double z)
    {
        this.X = x;
        this.Y = y;
        this.Z = z;
    }

    public double X { get; }
    public double Y { get; }
    public double Z { get; }
}
```

<span data-ttu-id="d4701-146">Zawsze wtedy, gdy zgodną z planem projektu w celu utworzenia typu wartości niezmienne, postępuj zgodnie z tego zalecenia.</span><span class="sxs-lookup"><span data-stu-id="d4701-146">Follow this recommendation whenever your design intent is to create an immutable value type.</span></span> <span data-ttu-id="d4701-147">Jakie ulepszenia wydajności są dodatkowe korzyści.</span><span class="sxs-lookup"><span data-stu-id="d4701-147">Any performance improvements are an added benefit.</span></span> <span data-ttu-id="d4701-148">`readonly struct` Wyraźnie wyraża zgodną z planem projektu.</span><span class="sxs-lookup"><span data-stu-id="d4701-148">The `readonly struct` clearly expresses your design intent.</span></span>

## <a name="use-ref-readonly-return-statements-for-large-structures-when-possible"></a><span data-ttu-id="d4701-149">Użyj `ref readonly return` instrukcji dla dużych struktur, gdy jest to możliwe</span><span class="sxs-lookup"><span data-stu-id="d4701-149">Use `ref readonly return` statements for large structures when possible</span></span>

<span data-ttu-id="d4701-150">Może zwracać wartości przez odwołanie, kiedy wartość zwracana nie jest lokalnym zwracanych metody.</span><span class="sxs-lookup"><span data-stu-id="d4701-150">You can return values by reference when the value being returned isn't local to the returning method.</span></span> <span data-ttu-id="d4701-151">Zwracanie przez odwołanie oznacza, że kopiowane jest tylko odwołanie, nie struktury.</span><span class="sxs-lookup"><span data-stu-id="d4701-151">Returning by reference means that only the reference is copied, not the structure.</span></span> <span data-ttu-id="d4701-152">W poniższym przykładzie `Origin` właściwości nie można użyć `ref` zwrócić, ponieważ wartość zwracana jest zmienna lokalna:</span><span class="sxs-lookup"><span data-stu-id="d4701-152">In the following example, the `Origin` property can't use a `ref` return because the value being returned is a local variable:</span></span>

```csharp
public Point3D Origin => new Point3D(0,0,0);
```

<span data-ttu-id="d4701-153">Jednak następującą definicję właściwości mogą być zwrócone przez odwołanie, ponieważ zwrócona wartość jest składową statyczną:</span><span class="sxs-lookup"><span data-stu-id="d4701-153">However, the following property definition can be returned by reference because the returned value is a static member:</span></span>

```csharp
public struct Point3D
{
    private static Point3D origin = new Point3D(0,0,0);

    // Dangerous! returning a mutable reference to internal storage
    public ref Point3D Origin => ref origin;

    // other members removed for space
}
```

<span data-ttu-id="d4701-154">Nie chcesz, aby modyfikowanie pochodzenia, dzięki czemu powinna zwrócić wartość przez obiekty wywołujące `readonly ref`:</span><span class="sxs-lookup"><span data-stu-id="d4701-154">You don't want callers modifying the origin, so you should return the value by `readonly ref`:</span></span>

```csharp
public struct Point3D
{
    private static Point3D origin = new Point3D(0,0,0);

    public static ref readonly Point3D Origin => ref origin;

    // other members removed for space
}
```

<span data-ttu-id="d4701-155">Zwracanie `ref readonly` umożliwia zapisywanie, kopiowanie większych struktur i zachować niezmienności członkowie danych wewnętrznych.</span><span class="sxs-lookup"><span data-stu-id="d4701-155">Returning `ref readonly` enables you to save copying larger structures and preserve the immutability of your internal data members.</span></span>

<span data-ttu-id="d4701-156">W witrynie wywołania obiektów wywołujących wprowadzić decyzja o korzystaniu z `Origin` właściwość jako `readonly ref` lub jako wartość:</span><span class="sxs-lookup"><span data-stu-id="d4701-156">At the call site, callers make the choice to use the `Origin` property as a `readonly ref` or as a value:</span></span>

[!code-csharp[AssignRefReadonly](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#AssignRefReadonly "Assigning a ref readonly")]

<span data-ttu-id="d4701-157">Pierwsze przypisanie w poprzednim kodzie tworzy kopię `Origin` stałych i przypisuje kopiujących.</span><span class="sxs-lookup"><span data-stu-id="d4701-157">The first assignment in the preceding code makes a copy of the `Origin` constant and assigns that copy.</span></span> <span data-ttu-id="d4701-158">Drugi przypisuje odwołania.</span><span class="sxs-lookup"><span data-stu-id="d4701-158">The second assigns a reference.</span></span> <span data-ttu-id="d4701-159">Należy zauważyć, że `readonly` modyfikator musi być częścią deklaracja zmiennej.</span><span class="sxs-lookup"><span data-stu-id="d4701-159">Notice that the `readonly` modifier must be part of the declaration of the variable.</span></span> <span data-ttu-id="d4701-160">Nie można zmodyfikować odwołania, do którego się odwołuje.</span><span class="sxs-lookup"><span data-stu-id="d4701-160">The reference to which it refers can't be modified.</span></span> <span data-ttu-id="d4701-161">Próba wykonania tej czynności powoduje błąd kompilacji.</span><span class="sxs-lookup"><span data-stu-id="d4701-161">Attempts to do so result in a compile-time error.</span></span>

<span data-ttu-id="d4701-162">`readonly` Modyfikator jest wymagany w deklaracji `originReference`.</span><span class="sxs-lookup"><span data-stu-id="d4701-162">The `readonly` modifier is required on the declaration of `originReference`.</span></span>

<span data-ttu-id="d4701-163">Kompilator wymusza to, że obiekt wywołujący nie można zmodyfikować odwołania.</span><span class="sxs-lookup"><span data-stu-id="d4701-163">The compiler enforces that the caller can't modify the reference.</span></span> <span data-ttu-id="d4701-164">Podejmowana jest próba przypisania wartości bezpośrednio wygenerować błąd kompilacji.</span><span class="sxs-lookup"><span data-stu-id="d4701-164">Attempts to assign the value directly generate a compile-time error.</span></span> <span data-ttu-id="d4701-165">Jednak kompilator nie wiadomo, jeśli metoda dowolnego elementu członkowskiego modyfikuje stan struktury.</span><span class="sxs-lookup"><span data-stu-id="d4701-165">However, the compiler can't know if any member method modifies the state of the struct.</span></span>
<span data-ttu-id="d4701-166">Aby upewnić się, że obiekt nie jest modyfikowana, kompilator tworzy kopię i wywołuje element członkowski odwołań za pomocą tej kopii.</span><span class="sxs-lookup"><span data-stu-id="d4701-166">To ensure that the object isn't modified, the compiler creates a copy and calls member references using that copy.</span></span> <span data-ttu-id="d4701-167">Wszystkie modyfikacje są tym kopia obronna.</span><span class="sxs-lookup"><span data-stu-id="d4701-167">Any modifications are to that defensive copy.</span></span>

## <a name="apply-the-in-modifier-to-readonly-struct-parameters-larger-than-systemintptrsize"></a><span data-ttu-id="d4701-168">Zastosuj `in` modyfikatora `readonly struct` parametry większe niż `System.IntPtr.Size`</span><span class="sxs-lookup"><span data-stu-id="d4701-168">Apply the `in` modifier to `readonly struct` parameters larger than `System.IntPtr.Size`</span></span>

<span data-ttu-id="d4701-169">`in` — Słowo kluczowe uzupełniają istniejące `ref` i `out` słów kluczowych, aby przekazywać argumentów przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="d4701-169">The `in` keyword complements the existing `ref` and `out` keywords to pass arguments by reference.</span></span> <span data-ttu-id="d4701-170">`in` — Słowo kluczowe Określa, przekazywanie argumentu przez odwołanie, ale wywoływanej metody nie modyfikuje wartości.</span><span class="sxs-lookup"><span data-stu-id="d4701-170">The `in` keyword specifies passing the argument by reference, but the called method doesn't modify the value.</span></span>

<span data-ttu-id="d4701-171">To dodawanie zapewnia pełną słownictwa wyrażenia zgodną z planem projektu.</span><span class="sxs-lookup"><span data-stu-id="d4701-171">This addition provides a full vocabulary to express your design intent.</span></span>
<span data-ttu-id="d4701-172">Typy wartości są kopiowane, gdy przekazywane do metody o nazwie, jeśli nie określisz dowolną z następujących modyfikatorów w podpisie metody.</span><span class="sxs-lookup"><span data-stu-id="d4701-172">Value types are copied when passed to a called method when you don't specify any of the following modifiers in the method signature.</span></span> <span data-ttu-id="d4701-173">Każda z tych modyfikatorów Określa, czy zmienna jest przekazywana przez odwołanie, unikając kopiowania.</span><span class="sxs-lookup"><span data-stu-id="d4701-173">Each of these modifiers specifies that a variable is passed by reference, avoiding the copy.</span></span> <span data-ttu-id="d4701-174">Każdy modyfikator wyraża innego zamiaru:</span><span class="sxs-lookup"><span data-stu-id="d4701-174">Each modifier expresses a different intent:</span></span>

- <span data-ttu-id="d4701-175">`out`: Ta metoda ustawia wartość argumentu jako parametr.</span><span class="sxs-lookup"><span data-stu-id="d4701-175">`out`: This method sets the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="d4701-176">`ref`: Ta metoda może ustawić wartość argumentu jako parametr.</span><span class="sxs-lookup"><span data-stu-id="d4701-176">`ref`: This method may set the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="d4701-177">`in`: Ta metoda nie modyfikuje wartość argumentu jako parametr.</span><span class="sxs-lookup"><span data-stu-id="d4701-177">`in`: This method doesn't modify the value of the argument used as this parameter.</span></span>

<span data-ttu-id="d4701-178">Dodaj `in` modyfikator do przekazywania argumentu przez odwołanie i Zadeklaruj swoje założenia projektowe, aby przekazywać argumentów przez odwołanie, aby uniknąć niepotrzebnego kopiowania.</span><span class="sxs-lookup"><span data-stu-id="d4701-178">Add the `in` modifier to pass an argument by reference and declare your design intent to pass arguments by reference to avoid unnecessary copying.</span></span> <span data-ttu-id="d4701-179">Nie zamierzasz zmodyfikować obiekt używany w roli tego argumentu.</span><span class="sxs-lookup"><span data-stu-id="d4701-179">You don't intend to modify the object used as that argument.</span></span>

<span data-ttu-id="d4701-180">Praktyka ta często zwiększa wydajność w przypadku typów wartości tylko do odczytu, które są większe niż <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="d4701-180">This practice often improves performance for readonly value types that are larger than <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span></span> <span data-ttu-id="d4701-181">Dla typów prostych (`sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`, `char`, `float`, `double`, `decimal` i `bool`, i `enum` typów), wszelkie potencjalne zwiększenie wydajności są minimial.</span><span class="sxs-lookup"><span data-stu-id="d4701-181">For simple types (`sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`, `char`, `float`, `double`, `decimal` and `bool`, and `enum` types), any potential performance gains are minimial.</span></span> <span data-ttu-id="d4701-182">W rzeczywistości limitu może obniżyć wydajność przy użyciu przekazywany przez odwołanie dla typów mniejszych niż <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="d4701-182">In fact, performance may degrade by using pass-by-reference for types smaller than <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span></span>

<span data-ttu-id="d4701-183">Poniższy kod przedstawia przykład metody, które oblicza odległość między dwoma punktami w przestrzeni 3D.</span><span class="sxs-lookup"><span data-stu-id="d4701-183">The following code shows an example of a method that calculates the distance between two points in 3D space.</span></span>

[!code-csharp[InArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgument "Specifying an in argument")]

<span data-ttu-id="d4701-184">Argumenty są dwie struktury, że każdy zawiera trzech liczb typu Double.</span><span class="sxs-lookup"><span data-stu-id="d4701-184">The arguments are two structures that each contain three doubles.</span></span> <span data-ttu-id="d4701-185">Wartość o podwójnej precyzji jest 8 bajtów, dzięki czemu każdy argument jest 24 bajty.</span><span class="sxs-lookup"><span data-stu-id="d4701-185">A double is 8 bytes, so each argument is 24 bytes.</span></span> <span data-ttu-id="d4701-186">Określając `in` modyfikatora, należy przekazać 4-bajtowych lub 8-bajtowych odwołania do tych argumentów, w zależności od architektury komputera.</span><span class="sxs-lookup"><span data-stu-id="d4701-186">By specifying the `in` modifier, you pass a 4 byte or 8-byte reference to those arguments, depending on the architecture of the machine.</span></span> <span data-ttu-id="d4701-187">Różnica w rozmiarze jest mała, ale dodaje, gdy Twoja aplikacja wywołuje tę metodę w pętli za pomocą wielu różnych wartości.</span><span class="sxs-lookup"><span data-stu-id="d4701-187">The difference in size is small, but it adds up when your application calls this method in a tight loop using many different values.</span></span>

<span data-ttu-id="d4701-188">`in` Uzupełniające modyfikator `out` i `ref` w inny sposób, jak również.</span><span class="sxs-lookup"><span data-stu-id="d4701-188">The `in` modifier complements `out` and `ref` in other ways as well.</span></span> <span data-ttu-id="d4701-189">Nie można utworzyć przeciążenia metody, które różnią się tylko w obecności właściwości `in`, `out`, lub `ref`.</span><span class="sxs-lookup"><span data-stu-id="d4701-189">You can't create overloads of a method that differ only in the presence of `in`, `out`, or `ref`.</span></span> <span data-ttu-id="d4701-190">Te nowe reguły rozszerzyć takie samo zachowanie, które zawsze miały zostały zdefiniowane dla `out` i `ref` parametrów.</span><span class="sxs-lookup"><span data-stu-id="d4701-190">These new rules extend the same behavior that had always been defined for `out` and `ref` parameters.</span></span> <span data-ttu-id="d4701-191">Podobnie jak `out` i `ref` modyfikatorów, typy wartości nie są zapakowany, ponieważ `in` modyfikator jest stosowany.</span><span class="sxs-lookup"><span data-stu-id="d4701-191">Like the `out` and `ref` modifiers, value types aren't boxed because the `in` modifier is applied.</span></span>

<span data-ttu-id="d4701-192">`in` Modyfikator mogą być stosowane do wszystkich elementów członkowskich, która przyjmuje parametry: metod delegatów, wyrażeń lambda, funkcji lokalnych, indeksatory, operatorów.</span><span class="sxs-lookup"><span data-stu-id="d4701-192">The `in` modifier may be applied to any member that takes parameters: methods, delegates, lambdas, local functions, indexers, operators.</span></span>

<span data-ttu-id="d4701-193">Kolejną funkcją `in` parametry są może używać wartości literałów lub stałe w jako argumentu do `in` parametru.</span><span class="sxs-lookup"><span data-stu-id="d4701-193">Another feature of `in` parameters is that you may use literal values or constants for the argument to an `in` parameter.</span></span> <span data-ttu-id="d4701-194">Ponadto, w odróżnieniu od `ref` lub `out` parametru, nie trzeba zastosować `in` modyfikator w witrynie wywołania.</span><span class="sxs-lookup"><span data-stu-id="d4701-194">Also, unlike a `ref` or `out` parameter, you don't need to apply the `in` modifier at the call site.</span></span> <span data-ttu-id="d4701-195">Poniższy kod pokazuje dwa przykłady wywoływania `CalculateDistance` metody.</span><span class="sxs-lookup"><span data-stu-id="d4701-195">The following code shows you two examples of calling the `CalculateDistance` method.</span></span> <span data-ttu-id="d4701-196">Pierwszy używa dwóch zmiennych lokalnych, przekazywany przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="d4701-196">The first uses two local variables passed by reference.</span></span> <span data-ttu-id="d4701-197">Drugi zawiera zmienną tymczasową utworzonych jako część wywołania metody.</span><span class="sxs-lookup"><span data-stu-id="d4701-197">The second includes a temporary variable created as part of the method call.</span></span>

[!code-csharp[UseInArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#UseInArgument "Specifying an In argument")]

<span data-ttu-id="d4701-198">Istnieje kilka sposobów, w których kompilator wymusza tylko do odczytu rodzaj `in` argumentu.</span><span class="sxs-lookup"><span data-stu-id="d4701-198">There are several ways in which the compiler enforces the read-only nature of an `in` argument.</span></span>  <span data-ttu-id="d4701-199">Po pierwsze, wywoływanej metody nie można przypisać bezpośrednio do `in` parametru.</span><span class="sxs-lookup"><span data-stu-id="d4701-199">First of all, the called method can't directly assign to an `in` parameter.</span></span> <span data-ttu-id="d4701-200">Bezpośrednio nie można przypisać do dowolnego pola `in` parametr, gdy ta wartość jest `struct` typu.</span><span class="sxs-lookup"><span data-stu-id="d4701-200">It can't directly assign to any field of an `in` parameter when that value is a `struct` type.</span></span> <span data-ttu-id="d4701-201">Ponadto nie można przekazać `in` parametr przy użyciu dowolnej metody `ref` lub `out` modyfikator.</span><span class="sxs-lookup"><span data-stu-id="d4701-201">In addition, you can't pass an `in` parameter to any method using the `ref` or `out` modifier.</span></span>
<span data-ttu-id="d4701-202">Te reguły mają zastosowanie do dowolnego pola `in` parametru podane, pole jest `struct` typu, a parametr jest również `struct` typu.</span><span class="sxs-lookup"><span data-stu-id="d4701-202">These rules apply to any field of an `in` parameter, provided the field is a `struct` type and the parameter is also a `struct` type.</span></span> <span data-ttu-id="d4701-203">W rzeczywistości te reguły stosuje się na wiele warstw dostępu do elementu członkowskiego, pod warunkiem typy na wszystkich poziomach dostępu do elementu członkowskiego `structs`.</span><span class="sxs-lookup"><span data-stu-id="d4701-203">In fact, these rules apply for multiple layers of member access provided the types at all levels of member access are `structs`.</span></span>
<span data-ttu-id="d4701-204">Kompilator wymusza, który `struct` typy przekazane jako `in` argumentów i ich `struct` elementy członkowskie są zmienne tylko do odczytu, gdy jest używana jako argumenty do innych metod.</span><span class="sxs-lookup"><span data-stu-id="d4701-204">The compiler enforces that `struct` types passed as  `in` arguments and their `struct` members are read-only variables when used as arguments to other methods.</span></span>

<span data-ttu-id="d4701-205">Korzystanie z `in` parametrów można uniknąć potencjalnych kosztów wydajności tworzenia kopii.</span><span class="sxs-lookup"><span data-stu-id="d4701-205">The use of `in` parameters can avoid the potential performance costs of making copies.</span></span> <span data-ttu-id="d4701-206">Go nie zmienia semantykę każde wywołanie metody.</span><span class="sxs-lookup"><span data-stu-id="d4701-206">It doesn't change the semantics of any method call.</span></span> <span data-ttu-id="d4701-207">W związku z tym, nie trzeba określić `in` modyfikator w witrynie wywołania.</span><span class="sxs-lookup"><span data-stu-id="d4701-207">Therefore, you don't need to specify the `in` modifier at the call site.</span></span> <span data-ttu-id="d4701-208">Pominięcie `in` modyfikator w witrynie wywołania informuje kompilator, mogą być kopię argumentu z następujących powodów:</span><span class="sxs-lookup"><span data-stu-id="d4701-208">Omitting the `in` modifier at the call site informs the compiler that it's allowed to make a copy of the argument for the following reasons:</span></span>

- <span data-ttu-id="d4701-209">Istnieje niejawna konwersja, ale nie tożsamość konwersję z typu argumentu z typem parametru.</span><span class="sxs-lookup"><span data-stu-id="d4701-209">There exists an implicit conversion but not an identity conversion from the argument type to the parameter type.</span></span>
- <span data-ttu-id="d4701-210">Argument jest wyrażeniem, ale nie ma zmienną znanych magazynu.</span><span class="sxs-lookup"><span data-stu-id="d4701-210">The argument is an expression but doesn't have a known storage variable.</span></span>
- <span data-ttu-id="d4701-211">Istnieje przeciążenie różni się obecności lub braku `in`.</span><span class="sxs-lookup"><span data-stu-id="d4701-211">An overload exists that differs by the presence or absence of `in`.</span></span> <span data-ttu-id="d4701-212">W takim przypadku według wartości przeciążenia ma lepsze dopasowanie.</span><span class="sxs-lookup"><span data-stu-id="d4701-212">In that case, the by value overload is a better match.</span></span>

<span data-ttu-id="d4701-213">Reguły te są przydatne w przypadku aktualizowania istniejącego kodu do argumenty odwołania tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="d4701-213">These rules are useful as you update existing code to use read-only reference arguments.</span></span> <span data-ttu-id="d4701-214">Wewnątrz metody o nazwie możesz wywołać dowolnej metody wystąpienia, która używa parametrów wielowartościowych.</span><span class="sxs-lookup"><span data-stu-id="d4701-214">Inside the called method, you can call any instance method that uses by value parameters.</span></span> <span data-ttu-id="d4701-215">W tych przypadkach kopię `in` utworzeniu parametru.</span><span class="sxs-lookup"><span data-stu-id="d4701-215">In those instances, a copy of the `in` parameter is created.</span></span> <span data-ttu-id="d4701-216">Ponieważ kompilator może utworzyć zmienną tymczasową dla każdego `in` parametru, można również określić wartości domyślnych dla każdej `in` parametru.</span><span class="sxs-lookup"><span data-stu-id="d4701-216">Because the compiler may create a temporary variable for any `in` parameter, you can also specify default values for any `in` parameter.</span></span> <span data-ttu-id="d4701-217">Poniższy kod określa pochodzenia (punkt 0,0) jako wartość domyślna dla drugi punkt:</span><span class="sxs-lookup"><span data-stu-id="d4701-217">The following code specifies the origin (point 0,0) as the default value for the second point:</span></span>

[!code-csharp[InArgumentDefault](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgumentDefault "Specifying defaults for an in parameter")]

<span data-ttu-id="d4701-218">Aby wymusić na kompilatorze argument tylko do odczytu jest przekazywany przez odwołanie, należy określić `in` modyfikator dla argumentów w witrynie wywołania, jak pokazano w poniższym kodzie:</span><span class="sxs-lookup"><span data-stu-id="d4701-218">To force the compiler to pass read-only arguments by reference, specify the `in` modifier on the arguments at the call site, as shown in the following code:</span></span>

[!code-csharp[UseInArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#ExplicitInArgument "Specifying an In argument")]

<span data-ttu-id="d4701-219">To zachowanie ułatwia przyjęcie `in` parametrów wraz z upływem czasu w dużych bazach kodu, gdzie możliwe są wzrost wydajności.</span><span class="sxs-lookup"><span data-stu-id="d4701-219">This behavior makes it easier to adopt `in` parameters over time in large codebases where performance gains are possible.</span></span> <span data-ttu-id="d4701-220">Możesz dodać `in` modyfikatora podpisy metod pierwszy.</span><span class="sxs-lookup"><span data-stu-id="d4701-220">You add the `in` modifier to method signatures first.</span></span> <span data-ttu-id="d4701-221">Następnie należy dodać `in` modyfikator na wywołania i utworzyć `readonly struct` typy umożliwiające kompilatorowi unikanie tworzenia kopii obrony `in` parametrów w większej liczby lokalizacji.</span><span class="sxs-lookup"><span data-stu-id="d4701-221">Then, you can add the `in` modifier at call sites and create `readonly struct` types to enable the compiler to avoid creating defensive copies of `in` parameters in more locations.</span></span>

<span data-ttu-id="d4701-222">`in` Nazwy parametru może również służyć za pomocą typów referencyjnych lub wartości liczbowych.</span><span class="sxs-lookup"><span data-stu-id="d4701-222">The `in` parameter designation can also be used with reference types or numeric values.</span></span> <span data-ttu-id="d4701-223">Jednak korzyści w obu przypadkach są minimalne, jeśli istnieje.</span><span class="sxs-lookup"><span data-stu-id="d4701-223">However, the benefits in both cases are minimal, if any.</span></span>

## <a name="never-use-mutable-structs-as-in-in-argument"></a><span data-ttu-id="d4701-224">Nigdy nie używaj mutable struktur, podobnie jak w `in` argumentu</span><span class="sxs-lookup"><span data-stu-id="d4701-224">Never use mutable structs as in `in` argument</span></span>

<span data-ttu-id="d4701-225">Opisane powyżej metody wyjaśniają, jak uniknąć kopii, zwracając odwołań i przekazanie wartości przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="d4701-225">The techniques described above explain how to avoid copies by returning references and passing values by reference.</span></span> <span data-ttu-id="d4701-226">Techniki te działają najlepiej, jeśli typy argumentów są deklarowane jako `readonly struct` typów.</span><span class="sxs-lookup"><span data-stu-id="d4701-226">These techniques work best when the argument types are declared as `readonly struct` types.</span></span> <span data-ttu-id="d4701-227">W przeciwnym razie kompilator musi utworzyć **obrony kopie** w wielu sytuacjach, aby wymusić tylko do odczytu ness żadnych argumentów.</span><span class="sxs-lookup"><span data-stu-id="d4701-227">Otherwise, the compiler must create **defensive copies** in many situations to enforce the readonly-ness of any arguments.</span></span> <span data-ttu-id="d4701-228">Rozważmy następujący przykład, który oblicza odległość punkt 3D ze źródła:</span><span class="sxs-lookup"><span data-stu-id="d4701-228">Consider the following example that calculates the distance of a 3D point from the origin:</span></span>

[!code-csharp[InArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgument "Specifying an in argument")]

<span data-ttu-id="d4701-229">`Point3D` Struktura jest *nie* struktury tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="d4701-229">The `Point3D` structure is *not* a readonly struct.</span></span> <span data-ttu-id="d4701-230">Istnieje sześć wywołań dostęp do różnych właściwości w treści tej metody.</span><span class="sxs-lookup"><span data-stu-id="d4701-230">There are six different property access calls in the body of this method.</span></span> <span data-ttu-id="d4701-231">Na pierwszego badania może mieć umieszczenie tych dostępach są bezpieczne.</span><span class="sxs-lookup"><span data-stu-id="d4701-231">On first examination, you may have thought these accesses were safe.</span></span> <span data-ttu-id="d4701-232">Gdy wszystkie `get` akcesor nie należy modyfikować stan obiektu.</span><span class="sxs-lookup"><span data-stu-id="d4701-232">After all, a `get` accessor shouldn't modify the state of the object.</span></span> <span data-ttu-id="d4701-233">Ale nie ma żadnej reguły języka, który wymusza.</span><span class="sxs-lookup"><span data-stu-id="d4701-233">But there's no language rule that enforces that.</span></span> <span data-ttu-id="d4701-234">Jest typową Konwencją.</span><span class="sxs-lookup"><span data-stu-id="d4701-234">It's only a common convention.</span></span> <span data-ttu-id="d4701-235">Można wdrożyć dowolny typ `get` dostępu, który zmodyfikował stan wewnętrzny.</span><span class="sxs-lookup"><span data-stu-id="d4701-235">Any type could implement a `get` accessor that modified the internal state.</span></span> <span data-ttu-id="d4701-236">Bez gwarancji niektóre języka kompilatora należy utworzyć tymczasowej kopii argument przed wywołaniem dowolnego elementu członkowskiego.</span><span class="sxs-lookup"><span data-stu-id="d4701-236">Without some language guarantee, the compiler must create a temporary copy of the argument before calling any member.</span></span> <span data-ttu-id="d4701-237">Magazyn tymczasowy jest tworzone na stosie, wartości argumentu są kopiowane do tymczasowego przechowywania i wartość jest kopiowany do stosu dla każdego dostępu do elementu członkowskiego jako `this` argumentu.</span><span class="sxs-lookup"><span data-stu-id="d4701-237">The temporary storage is created on the stack, the values of the argument are copied to the temporary storage, and the value is copied to the stack for each member access as the `this` argument.</span></span> <span data-ttu-id="d4701-238">W wielu sytuacjach te kopie negatywnie wpłynąć na wydajność, tyle że przekazać przez wartość jest szybsze niż — dostęp próbny, tylko do odczytu odwołanie gdy typ argumentu jest `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="d4701-238">In many situations, these copies harm performance enough that pass-by-value is faster than pass-by-readonly-reference when the argument type isn't a `readonly struct`.</span></span>

<span data-ttu-id="d4701-239">Zamiast tego, jeśli obliczeń odległość używa struktury niezmienne `ReadonlyPoint3D`, obiekty tymczasowe nie są potrzebne:</span><span class="sxs-lookup"><span data-stu-id="d4701-239">Instead, if the distance calculation uses the immutable struct, `ReadonlyPoint3D`, temporary objects are not needed:</span></span>

[!code-csharp[readonlyInArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#ReadOnlyInArgument "Specifying a readonly in argument")]

<span data-ttu-id="d4701-240">Kompilator generuje kod bardziej efektywne, gdy wywołujesz członkowie `readonly struct`: `this` Odwołanie, zamiast kopii receiver, jest zawsze `in` parametr przekazywany przez odwołanie do elementu członkowskiego.</span><span class="sxs-lookup"><span data-stu-id="d4701-240">The compiler generates more efficient code when you call members of a `readonly struct`: The `this` reference, instead of a copy of the receiver, is always an `in` parameter passed by reference to the member method.</span></span> <span data-ttu-id="d4701-241">Tego rodzaju optymalizacji zapisywanie, kopiowanie, gdy używasz `readonly struct` jako `in` argumentu.</span><span class="sxs-lookup"><span data-stu-id="d4701-241">This optimization saves copying when you use a `readonly struct` as an `in` argument.</span></span>

<span data-ttu-id="d4701-242">Możesz zobaczyć program przykładu, który ilustruje różnice wydajności przy użyciu [Benchmark.net](https://www.nuget.org/packages/BenchmarkDotNet/) w naszym [repozytorium przykładów](https://github.com/dotnet/samples/tree/master/csharp/safe-efficient-code/benchmark) w witrynie GitHub.</span><span class="sxs-lookup"><span data-stu-id="d4701-242">You can see an example program that demonstrates the performance differences using [Benchmark.net](https://www.nuget.org/packages/BenchmarkDotNet/) in our [samples repository](https://github.com/dotnet/samples/tree/master/csharp/safe-efficient-code/benchmark) on GitHub.</span></span> <span data-ttu-id="d4701-243">Porównuje się sukcesem, struktura mutable według wartości i według odwołania przekazywanie struktury niezmienne według wartości i według odwołania.</span><span class="sxs-lookup"><span data-stu-id="d4701-243">It compares passing a mutable struct by value and by reference with passing an immutable struct by value and by reference.</span></span> <span data-ttu-id="d4701-244">Korzystanie z niezmiennego struktury i — dostęp próbny przez odwołanie jest najszybszy.</span><span class="sxs-lookup"><span data-stu-id="d4701-244">The use of the immutable struct and pass by reference is fastest.</span></span>

## <a name="use-ref-struct-types-to-work-with-blocks-or-memory-on-a-single-stack-frame"></a><span data-ttu-id="d4701-245">Użyj `ref struct` typy do pracy z bloków lub pamięci na ramce stosu</span><span class="sxs-lookup"><span data-stu-id="d4701-245">Use `ref struct` types to work with blocks or memory on a single stack frame</span></span>

<span data-ttu-id="d4701-246">Funkcja języka powiązane jest zdolność do deklarowania typu wartości, które muszą być ograniczone do ramki stosu.</span><span class="sxs-lookup"><span data-stu-id="d4701-246">A related language feature is the ability to declare a value type that must be constrained to a single stack frame.</span></span> <span data-ttu-id="d4701-247">To ograniczenie włącza kompilator udostępnia kilka optymalizacji.</span><span class="sxs-lookup"><span data-stu-id="d4701-247">This restriction enables the compiler to make several optimizations.</span></span> <span data-ttu-id="d4701-248">Główną motywacją do tej funkcji został <xref:System.Span%601> i pokrewne struktury.</span><span class="sxs-lookup"><span data-stu-id="d4701-248">The primary motivation for this feature was <xref:System.Span%601> and related structures.</span></span> <span data-ttu-id="d4701-249">Osiągniesz poprawa wydajności wynikająca ze te rozszerzenia funkcjonalności za pomocą nowych i zaktualizowanych interfejsów API platformy .NET, które używanie <xref:System.Span%601> typu.</span><span class="sxs-lookup"><span data-stu-id="d4701-249">You'll achieve performance improvements from these enhancements by using new and updated .NET APIs that make use of the <xref:System.Span%601> type.</span></span>

<span data-ttu-id="d4701-250">Masz podobnych wymaganiach dotyczących pracy z pamięcią utworzone za pomocą [ `stackalloc` ](language-reference/keywords/stackalloc.md) lub w przypadku używania pamięci za pomocą międzyoperacyjnych interfejsów API.</span><span class="sxs-lookup"><span data-stu-id="d4701-250">You may have similar requirements working with memory created using [`stackalloc`](language-reference/keywords/stackalloc.md) or when using memory from interop APIs.</span></span> <span data-ttu-id="d4701-251">Definiowanie swoich własnych `ref struct` typy dla tych wymagań.</span><span class="sxs-lookup"><span data-stu-id="d4701-251">You can define your own `ref struct` types for those needs.</span></span>

## <a name="readonly-ref-struct-type"></a><span data-ttu-id="d4701-252">`readonly ref struct` Typ</span><span class="sxs-lookup"><span data-stu-id="d4701-252">`readonly ref struct` type</span></span>

<span data-ttu-id="d4701-253">Deklarowanie struktury jako `readonly ref` łączy korzyści i ograniczeń dotyczących `ref struct` i `readonly struct` deklaracji.</span><span class="sxs-lookup"><span data-stu-id="d4701-253">Declaring a struct as `readonly ref` combines the benefits and restrictions of `ref struct` and `readonly struct` declarations.</span></span> <span data-ttu-id="d4701-254">Pamięć używana przez zakres tylko do odczytu jest ograniczony do ramki stosu i pamięci używanej przez zakres tylko do odczytu nie mogą być modyfikowane.</span><span class="sxs-lookup"><span data-stu-id="d4701-254">The memory used by the readonly span is restricted to a single stack frame, and the memory used by the readonly span can't be modified.</span></span>

## <a name="conclusions"></a><span data-ttu-id="d4701-255">Wnioski</span><span class="sxs-lookup"><span data-stu-id="d4701-255">Conclusions</span></span>

<span data-ttu-id="d4701-256">Używanie typów wartości minimalizuje liczbę operacji alokacji:</span><span class="sxs-lookup"><span data-stu-id="d4701-256">Using value types minimizes the number of allocation operations:</span></span>

- <span data-ttu-id="d4701-257">Magazyn dla typów wartości jest na stosie dla zmiennych lokalnych i argumenty metody.</span><span class="sxs-lookup"><span data-stu-id="d4701-257">Storage for value types is stack allocated for local variables and method arguments.</span></span>
- <span data-ttu-id="d4701-258">Magazyn dla typów wartości, które należą do innych obiektów jest przydzielany w ramach tego obiektu, nie jako osobne alokacji.</span><span class="sxs-lookup"><span data-stu-id="d4701-258">Storage for value types that are members of other objects is allocated as part of that object, not as a separate allocation.</span></span>
- <span data-ttu-id="d4701-259">Magazyn dla typu wartości zwracać wartości jest na stosie.</span><span class="sxs-lookup"><span data-stu-id="d4701-259">Storage for value type return values is stack allocated.</span></span>

<span data-ttu-id="d4701-260">Kontrast wpisywany z odwołaniem w tej samej sytuacji:</span><span class="sxs-lookup"><span data-stu-id="d4701-260">Contrast that with reference types in those same situations:</span></span>

- <span data-ttu-id="d4701-261">Magazyn dla typów referencyjnych są sterty przydzielonej dla zmiennych lokalnych i argumenty metody.</span><span class="sxs-lookup"><span data-stu-id="d4701-261">Storage for reference types are heap allocated for local variables and method arguments.</span></span> <span data-ttu-id="d4701-262">Odwołania są przechowywane w stosie.</span><span class="sxs-lookup"><span data-stu-id="d4701-262">The reference is stored on the stack.</span></span>
- <span data-ttu-id="d4701-263">Magazyn dla typów odwołań, które należą do innych obiektów oddzielnie są przydzielane na stosie.</span><span class="sxs-lookup"><span data-stu-id="d4701-263">Storage for reference types that are members of other objects are separately allocated on the heap.</span></span> <span data-ttu-id="d4701-264">Obiekt zawierający przechowuje odwołania.</span><span class="sxs-lookup"><span data-stu-id="d4701-264">The containing object stores the reference.</span></span>
- <span data-ttu-id="d4701-265">Magazyn dla typu referencyjnego zwracają wartości jest sterty przydzielonej.</span><span class="sxs-lookup"><span data-stu-id="d4701-265">Storage for reference type return values is heap allocated.</span></span> <span data-ttu-id="d4701-266">Odwołanie do tego magazynu są przechowywane w stosie.</span><span class="sxs-lookup"><span data-stu-id="d4701-266">The reference to that storage is stored on the stack.</span></span>

<span data-ttu-id="d4701-267">Minimalizacja alokacji jest powiązana z stosowania kompromisów.</span><span class="sxs-lookup"><span data-stu-id="d4701-267">Minimizing allocations comes with tradeoffs.</span></span> <span data-ttu-id="d4701-268">Skopiuj większej ilości pamięci podczas rozmiar `struct` jest większy niż rozmiar odwołania.</span><span class="sxs-lookup"><span data-stu-id="d4701-268">You copy more memory when the size of the `struct` is larger than the size of a reference.</span></span> <span data-ttu-id="d4701-269">Odwołanie jest zwykle 64-bitowy lub 32-bitowy i zależy od Procesora komputera docelowego.</span><span class="sxs-lookup"><span data-stu-id="d4701-269">A reference is typically 64 bits or 32 bits, and depends on the target machine CPU.</span></span>

<span data-ttu-id="d4701-270">Tych kompromisów ogólnie ma wpływ na wydajność minimalny.</span><span class="sxs-lookup"><span data-stu-id="d4701-270">These tradeoffs generally have minimal performance impact.</span></span> <span data-ttu-id="d4701-271">Jednak dla dużych struktur lub większych kolekcji, zwiększa negatywny wpływ na wydajność.</span><span class="sxs-lookup"><span data-stu-id="d4701-271">However, for large structs or larger collections, the performance impact increases.</span></span> <span data-ttu-id="d4701-272">Wpływ mogą być duże w ścisłej pętli i ścieżek krytycznych dla programów.</span><span class="sxs-lookup"><span data-stu-id="d4701-272">The impact can be large in tight loops and hot paths for programs.</span></span>

<span data-ttu-id="d4701-273">Te ulepszenia C# języka są przeznaczone dla algorytmów krytyczne wydajności w przypadku, gdy minimalizując alokacji pamięci jest czynnikiem w osiąganiu niezbędne wydajności.</span><span class="sxs-lookup"><span data-stu-id="d4701-273">These enhancements to the C# language are designed for performance critical algorithms where minimizing memory allocations is a major factor in achieving the necessary performance.</span></span> <span data-ttu-id="d4701-274">Może się okazać, że nie są często używane funkcje te w kodzie, którą piszesz.</span><span class="sxs-lookup"><span data-stu-id="d4701-274">You may find that you don't often use these features in the code you write.</span></span> <span data-ttu-id="d4701-275">Jednak te ulepszenia zostały przyjęte w całej platformy .NET.</span><span class="sxs-lookup"><span data-stu-id="d4701-275">However, these enhancements have been adopted throughout .NET.</span></span> <span data-ttu-id="d4701-276">Ponieważ coraz więcej interfejsy API korzystać z tych funkcji, zobaczysz wydajności aplikacji, zwiększyć.</span><span class="sxs-lookup"><span data-stu-id="d4701-276">As more and more APIs make use of these features, you'll see the performance of your applications improve.</span></span>

## <a name="see-also"></a><span data-ttu-id="d4701-277">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="d4701-277">See also</span></span>

- [<span data-ttu-id="d4701-278">ref keyword</span><span class="sxs-lookup"><span data-stu-id="d4701-278">ref keyword</span></span>](language-reference/keywords/ref.md)
- [<span data-ttu-id="d4701-279">Wartości zwracane ref i zmienne lokalne ref</span><span class="sxs-lookup"><span data-stu-id="d4701-279">Ref returns and ref locals</span></span>](programming-guide/classes-and-structs/ref-returns.md)
