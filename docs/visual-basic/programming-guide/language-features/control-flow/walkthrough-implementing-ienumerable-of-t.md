---
title: Implementowanie interfejsu IEnumerable w języku Visual Basic
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: be2eefdc52d38df3071d457b7a71dbac6eaa2657
ms.sourcegitcommit: 3ab9254890a52a50762995fa6d7d77a00348db7e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/20/2018
ms.locfileid: "46490507"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="d1f45-102">Wskazówki: wdrażanie IEnumerable(Of T) w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="d1f45-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="d1f45-103"><xref:System.Collections.Generic.IEnumerable%601> Interfejs jest implementowany przez klasy, które mogą zwracać sekwencję wartości jeden element w danym momencie.</span><span class="sxs-lookup"><span data-stu-id="d1f45-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="d1f45-104">Zaletą zwracający dane, jeden element w danym momencie to, że nie masz załadować kompletny zestaw danych do pamięci z nią pracować.</span><span class="sxs-lookup"><span data-stu-id="d1f45-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="d1f45-105">Musisz użyć wystarczającą ilość pamięci do załadowania pojedynczy element z danych.</span><span class="sxs-lookup"><span data-stu-id="d1f45-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="d1f45-106">Klasy, które implementują `IEnumerable(T)` interfejsu może być używany z `For Each` pętli lub zapytań LINQ.</span><span class="sxs-lookup"><span data-stu-id="d1f45-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="d1f45-107">Na przykład rozważmy aplikację, która musi odczytywanie pliku tekstowego duże i zwracać każdy wiersz z pliku, który pasuje do kryteriów wyszukiwania określonego.</span><span class="sxs-lookup"><span data-stu-id="d1f45-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="d1f45-108">Aplikacja używa zapytania LINQ do zwrócenia wierszy z pliku, który spełniających określone kryteria.</span><span class="sxs-lookup"><span data-stu-id="d1f45-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="d1f45-109">Aby zbadać zawartość pliku za pomocą zapytań LINQ, aplikacja może ładować zawartość pliku do tablicy lub kolekcji.</span><span class="sxs-lookup"><span data-stu-id="d1f45-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="d1f45-110">Jednak ładowanie całego pliku do tablicy lub kolekcji będzie zużywać znacznie więcej pamięci niż jest to wymagane.</span><span class="sxs-lookup"><span data-stu-id="d1f45-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="d1f45-111">Zapytania LINQ zamiast tego można zbadać zawartość pliku przy użyciu klasy wyliczalny, zwracając tylko te wartości, które spełniają kryteria wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="d1f45-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="d1f45-112">Zapytania, które zwracają tylko kilka pasujących wartości będą zużywać znacznie mniej pamięci.</span><span class="sxs-lookup"><span data-stu-id="d1f45-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="d1f45-113">Można utworzyć klasę, która implementuje <xref:System.Collections.Generic.IEnumerable%601> interfejsu, aby uwidocznić źródło danych, jako wyliczalny dane.</span><span class="sxs-lookup"><span data-stu-id="d1f45-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="d1f45-114">Swojej klasie, która implementuje `IEnumerable(T)` interfejsu będzie wymagać innej klasy, która implementuje <xref:System.Collections.Generic.IEnumerator%601> interfejsu do iteracji przez źródło danych.</span><span class="sxs-lookup"><span data-stu-id="d1f45-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="d1f45-115">Te dwie klasy umożliwiają zwracają elementy danych po kolei jako określonego typu.</span><span class="sxs-lookup"><span data-stu-id="d1f45-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="d1f45-116">W tym instruktażu utworzysz klasę, która implementuje `IEnumerable(Of String)` interfejsu i klasę, która implementuje `IEnumerator(Of String)` interfejsu można odczytać tekstu jeden wiersz pliku naraz.</span><span class="sxs-lookup"><span data-stu-id="d1f45-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="d1f45-117">Tworzenie klasy Wyliczalny</span><span class="sxs-lookup"><span data-stu-id="d1f45-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="d1f45-118">**Utwórz projekt wyliczalny klas**</span><span class="sxs-lookup"><span data-stu-id="d1f45-118">**Create the enumerable class project**</span></span>

1.  <span data-ttu-id="d1f45-119">W języku Visual Basic na **pliku** menu wskaż **New** a następnie kliknij przycisk **projektu**.</span><span class="sxs-lookup"><span data-stu-id="d1f45-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1.  <span data-ttu-id="d1f45-120">W **nowy projekt** dialogowym **typów projektów** okienka, upewnij się, że **Windows** jest zaznaczone.</span><span class="sxs-lookup"><span data-stu-id="d1f45-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="d1f45-121">Wybierz **biblioteki klas** w **szablony** okienka.</span><span class="sxs-lookup"><span data-stu-id="d1f45-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="d1f45-122">W **nazwa** wpisz `StreamReaderEnumerable`, a następnie kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="d1f45-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="d1f45-123">Zostanie wyświetlony nowy projekt.</span><span class="sxs-lookup"><span data-stu-id="d1f45-123">The new project is displayed.</span></span>

1.  <span data-ttu-id="d1f45-124">W **Eksploratora rozwiązań**, kliknij prawym przyciskiem myszy plik Class1.vb i kliknij przycisk **Zmień nazwę**.</span><span class="sxs-lookup"><span data-stu-id="d1f45-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="d1f45-125">Zmień nazwę pliku do `StreamReaderEnumerable.vb` i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="d1f45-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="d1f45-126">Zmiana nazwy pliku spowoduje również zmianę nazwy klasy, która ma `StreamReaderEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="d1f45-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="d1f45-127">Ta klasa wdroży `IEnumerable(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="d1f45-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1.  <span data-ttu-id="d1f45-128">Kliknij prawym przyciskiem myszy projekt StreamReaderEnumerable, wskaż opcję **Dodaj**, a następnie kliknij przycisk **nowy element**.</span><span class="sxs-lookup"><span data-stu-id="d1f45-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="d1f45-129">Wybierz **klasy** szablonu.</span><span class="sxs-lookup"><span data-stu-id="d1f45-129">Select the **Class** template.</span></span> <span data-ttu-id="d1f45-130">W **nazwa** wpisz `StreamReaderEnumerator.vb` i kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="d1f45-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="d1f45-131">Pierwszą klasą w tym projekcie jest klasą wyliczalny i wdroży `IEnumerable(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="d1f45-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="d1f45-132">Implementuje ten interfejs ogólny <xref:System.Collections.IEnumerable> interfejsu i gwarantuje, że osoby korzystające z tej klasy mają dostęp do wartości wpisanych jako `String`.</span><span class="sxs-lookup"><span data-stu-id="d1f45-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="d1f45-133">**Dodaj kod do implementacji interfejsu IEnumerable**</span><span class="sxs-lookup"><span data-stu-id="d1f45-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="d1f45-134">Otwórz plik StreamReaderEnumerable.vb.</span><span class="sxs-lookup"><span data-stu-id="d1f45-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="d1f45-135">W wierszu po `Public Class StreamReaderEnumerable`, wpisz następujące polecenie i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="d1f45-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#1](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_1.vb)]

   <span data-ttu-id="d1f45-136">Visual Basic automatycznie wypełnia klasę za pomocą elementów członkowskich, które są wymagane przez `IEnumerable(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="d1f45-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="d1f45-137">Ta klasa wyliczalny odczyta wiersze z jeden wiersz pliku tekstowego w danym momencie.</span><span class="sxs-lookup"><span data-stu-id="d1f45-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="d1f45-138">Dodaj następujący kod do klasy w celu udostępnienia publicznego konstruktora, która przyjmuje ścieżkę do pliku jako parametr wejściowy.</span><span class="sxs-lookup"><span data-stu-id="d1f45-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#2](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_2.vb)]

4. <span data-ttu-id="d1f45-139">Implementacja <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> metody `IEnumerable(Of String)` interfejsu spowoduje zwrócenie nowego wystąpienia `StreamReaderEnumerator` klasy.</span><span class="sxs-lookup"><span data-stu-id="d1f45-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="d1f45-140">Implementacja `GetEnumerator` metody `IEnumerable` może pracować z interfejsem `Private`, ponieważ musisz udostępnić tylko członkowie `IEnumerable(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="d1f45-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="d1f45-141">Zastąp kod, który języka Visual Basic wygenerowany dla `GetEnumerator` metody z następującym kodem.</span><span class="sxs-lookup"><span data-stu-id="d1f45-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#3](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_3.vb)]  
  
<span data-ttu-id="d1f45-142">**Dodaj kod implementacji IEnumerator**</span><span class="sxs-lookup"><span data-stu-id="d1f45-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="d1f45-143">Otwórz plik StreamReaderEnumerator.vb.</span><span class="sxs-lookup"><span data-stu-id="d1f45-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="d1f45-144">W wierszu po `Public Class StreamReaderEnumerator`, wpisz następujące polecenie i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="d1f45-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#4](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_4.vb)]

   <span data-ttu-id="d1f45-145">Visual Basic automatycznie wypełnia klasę za pomocą elementów członkowskich, które są wymagane przez `IEnumerator(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="d1f45-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="d1f45-146">Klasa modułu wyliczającego otwiera plik tekstowy i wykonuje plik We/Wy odczytu wiersze z pliku.</span><span class="sxs-lookup"><span data-stu-id="d1f45-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="d1f45-147">Dodaj następujący kod do klasy w celu udostępnienia publicznego konstruktora, która przyjmuje ścieżkę do pliku jako parametr wejściowy, a następnie otwórz plik tekstowy do odczytu.</span><span class="sxs-lookup"><span data-stu-id="d1f45-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#5](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_5.vb)]

4. <span data-ttu-id="d1f45-148">`Current` Właściwości dla obu `IEnumerator(Of String)` i `IEnumerator` interfejsów zwracanie bieżącego elementu z pliku tekstowego jako `String`.</span><span class="sxs-lookup"><span data-stu-id="d1f45-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="d1f45-149">Implementacja `Current` właściwość `IEnumerator` może pracować z interfejsem `Private`, ponieważ musisz udostępnić tylko członkowie `IEnumerator(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="d1f45-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="d1f45-150">Zastąp kod, który języka Visual Basic wygenerowany dla `Current` właściwości z następującym kodem.</span><span class="sxs-lookup"><span data-stu-id="d1f45-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#6](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_6.vb)]

5. <span data-ttu-id="d1f45-151">`MoveNext` Metody `IEnumerator` interfejsu przechodzi do następnego elementu w pliku tekstowym i aktualizuje wartość, która jest zwracana przez `Current` właściwości.</span><span class="sxs-lookup"><span data-stu-id="d1f45-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="d1f45-152">Jeśli istnieje więcej elementów do odczytu, `MoveNext` metoda zwraca `False`; w przeciwnym razie `MoveNext` metoda zwraca `True`.</span><span class="sxs-lookup"><span data-stu-id="d1f45-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="d1f45-153">Dodaj następujący kod do `MoveNext` metody.</span><span class="sxs-lookup"><span data-stu-id="d1f45-153">Add the following code to the `MoveNext` method.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#7](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_7.vb)]

6. <span data-ttu-id="d1f45-154">`Reset` Metody `IEnumerator` interfejsu kieruje iteratora, aby wskazywała na początku pliku tekstowego i wyczyści bieżącą wartość elementu.</span><span class="sxs-lookup"><span data-stu-id="d1f45-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="d1f45-155">Dodaj następujący kod do `Reset` metody.</span><span class="sxs-lookup"><span data-stu-id="d1f45-155">Add the following code to the `Reset` method.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#8](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_8.vb)]

7. <span data-ttu-id="d1f45-156">`Dispose` Metody `IEnumerator` interfejsu gwarantuje, że wszystkie zasoby niezarządzane są zwalniane, przed iteratora jest niszczony.</span><span class="sxs-lookup"><span data-stu-id="d1f45-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="d1f45-157">Dojście do pliku, który jest używany przez `StreamReader` obiekt został niezarządzany zasób i muszą zostać zamknięte przed wystąpieniem iteratora.</span><span class="sxs-lookup"><span data-stu-id="d1f45-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="d1f45-158">Zastąp kod, który języka Visual Basic wygenerowany dla `Dispose` metoda następującym kodem.</span><span class="sxs-lookup"><span data-stu-id="d1f45-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#9](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_9.vb)] 
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="d1f45-159">Używając iteratora próbki</span><span class="sxs-lookup"><span data-stu-id="d1f45-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="d1f45-160">Można użyć wyliczalny klasy w kodzie oraz struktur sterujących, które wymagają obiekt, który implementuje `IEnumerable`, takich jak `For Next` pętli lub kwerendę LINQ.</span><span class="sxs-lookup"><span data-stu-id="d1f45-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="d1f45-161">W poniższym przykładzie przedstawiono `StreamReaderEnumerable` w zapytaniu programu LINQ.</span><span class="sxs-lookup"><span data-stu-id="d1f45-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_10.vb)]  
  
## <a name="see-also"></a><span data-ttu-id="d1f45-162">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="d1f45-162">See Also</span></span>  
 [<span data-ttu-id="d1f45-163">Wprowadzenie do LINQ w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="d1f45-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)  
 [<span data-ttu-id="d1f45-164">Przepływ sterowania</span><span class="sxs-lookup"><span data-stu-id="d1f45-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)  
 [<span data-ttu-id="d1f45-165">Struktury pętli</span><span class="sxs-lookup"><span data-stu-id="d1f45-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)  
 [<span data-ttu-id="d1f45-166">For Each...Next, instrukcja</span><span class="sxs-lookup"><span data-stu-id="d1f45-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
