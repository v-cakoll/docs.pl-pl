---
title: Jak bezpiecznie oddać za pomocą dopasowania wzorców i jest i jako operatorzy
description: Naucz się używać technik dopasowywania wzorców, aby bezpiecznie rzutować zmienne na inny typ. Można użyć dopasowania wzorca, jak również jest i jako operatory do bezpiecznej konwersji typów.
ms.date: 09/05/2018
helpviewer_keywords:
- cast operators [C#], as and is operators
- as operator [C#]
- is operator [C#]
ms.openlocfilehash: 762f8135063f7256ce7a167c65013703d9249039
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/14/2020
ms.locfileid: "73973092"
---
# <a name="how-to-safely-cast-by-using-pattern-matching-and-the-is-and-as-operators"></a><span data-ttu-id="be027-104">Jak bezpiecznie oddać za pomocą dopasowania wzorców i jest i jako operatorzy</span><span class="sxs-lookup"><span data-stu-id="be027-104">How to safely cast by using pattern matching and the is and as operators</span></span>

<span data-ttu-id="be027-105">Ponieważ obiekty są polimorficzne, zmienna typu klasy podstawowej może posiadać [typ](../programming-guide/types/index.md)pochodny .</span><span class="sxs-lookup"><span data-stu-id="be027-105">Because objects are polymorphic, it's possible for a variable of a base class type to hold a derived [type](../programming-guide/types/index.md).</span></span> <span data-ttu-id="be027-106">Aby uzyskać dostęp do elementów członkowskich instancji typu pochodnego, należy [rzutować](../programming-guide/types/casting-and-type-conversions.md) wartość z powrotem do typu pochodnego.</span><span class="sxs-lookup"><span data-stu-id="be027-106">To access the derived type's instance members, it's necessary to [cast](../programming-guide/types/casting-and-type-conversions.md) the value back to the derived type.</span></span> <span data-ttu-id="be027-107">Jednak oddanych stwarza ryzyko rzucania <xref:System.InvalidCastException>.</span><span class="sxs-lookup"><span data-stu-id="be027-107">However, a cast creates the risk of throwing an <xref:System.InvalidCastException>.</span></span> <span data-ttu-id="be027-108">C# zawiera instrukcje [dopasowania wzorca,](../pattern-matching.md) które wykonują rzutowanie warunkowo tylko wtedy, gdy zakończy się pomyślnie.</span><span class="sxs-lookup"><span data-stu-id="be027-108">C# provides [pattern matching](../pattern-matching.md) statements that perform a cast conditionally only when it will succeed.</span></span> <span data-ttu-id="be027-109">C# zapewnia również [is](../language-reference/operators/type-testing-and-cast.md#is-operator) i [jako](../language-reference/operators/type-testing-and-cast.md#as-operator) operatory do testowania, czy wartość jest określonego typu.</span><span class="sxs-lookup"><span data-stu-id="be027-109">C# also provides the [is](../language-reference/operators/type-testing-and-cast.md#is-operator) and [as](../language-reference/operators/type-testing-and-cast.md#as-operator) operators to test if a value is of a certain type.</span></span>

<span data-ttu-id="be027-110">Poniższy kod demonstruje `is` instrukcję dopasowywania wzorców.</span><span class="sxs-lookup"><span data-stu-id="be027-110">The following code demonstrates the pattern matching `is` statement.</span></span> <span data-ttu-id="be027-111">Zawiera metody, które testują argument metody, aby ustalić, czy jest to jeden z możliwych zestaw typów pochodnych:</span><span class="sxs-lookup"><span data-stu-id="be027-111">It contains methods that test a method argument to determine if it is one of a possible set of derived types:</span></span>

[!code-csharp[Pattern matching is statement](../../../samples/snippets/csharp/how-to/safelycast/patternmatching/Program.cs#PatternMatchingIs)]

<span data-ttu-id="be027-112">W poprzednim przykładzie przedstawiono kilka funkcji składni dopasowania wzorca.</span><span class="sxs-lookup"><span data-stu-id="be027-112">The preceding sample demonstrates a few features of pattern matching syntax.</span></span> <span data-ttu-id="be027-113">I `if (a is Mammal m)` `if (o is Mammal m)` instrukcje łączą test z przypisaniem inicjowania.</span><span class="sxs-lookup"><span data-stu-id="be027-113">The `if (a is Mammal m)` and `if (o is Mammal m)` statements combine the test with an initialization assignment.</span></span> <span data-ttu-id="be027-114">Przypisanie występuje tylko wtedy, gdy test zakończy się pomyślnie.</span><span class="sxs-lookup"><span data-stu-id="be027-114">The assignment occurs only when the test succeeds.</span></span> <span data-ttu-id="be027-115">Zmienna `m` jest tylko w `if` zakresie osadzonej instrukcji, gdzie został przypisany.</span><span class="sxs-lookup"><span data-stu-id="be027-115">The variable `m` is only in scope in the embedded `if` statement where it has been assigned.</span></span> <span data-ttu-id="be027-116">Nie można `m` uzyskać dostępu później w tej samej metodzie.</span><span class="sxs-lookup"><span data-stu-id="be027-116">You cannot access `m` later in the same method.</span></span> <span data-ttu-id="be027-117">Wypróbuj w interaktywnym oknie.</span><span class="sxs-lookup"><span data-stu-id="be027-117">Try it in the interactive window.</span></span>

<span data-ttu-id="be027-118">Tej samej składni można również użyć do testowania, jeśli [typ wartości nullable](../language-reference/builtin-types/nullable-value-types.md) ma wartość, jak pokazano w poniższym przykładowym kodzie:</span><span class="sxs-lookup"><span data-stu-id="be027-118">You can also use the same syntax for testing if a [nullable value type](../language-reference/builtin-types/nullable-value-types.md) has a value, as shown in the following sample code:</span></span>

[!code-csharp[Pattern matching with nullable types](../../../samples/snippets/csharp/how-to/safelycast/nullablepatternmatching/Program.cs#PatternMatchingNullable)]

<span data-ttu-id="be027-119">W poprzednim przykładzie przedstawiono inne funkcje dopasowania wzorca do użycia z konwersjami.</span><span class="sxs-lookup"><span data-stu-id="be027-119">The preceding sample demonstrates other features of pattern matching to use with conversions.</span></span> <span data-ttu-id="be027-120">Zmienną dla wzorca zerowego można przetestować, sprawdzając specjalnie `null` wartość.</span><span class="sxs-lookup"><span data-stu-id="be027-120">You can test a variable for the null pattern by checking specifically for the `null` value.</span></span> <span data-ttu-id="be027-121">Gdy wartość czasu wykonywania `null`zmiennej `is` jest , instrukcja `false`sprawdzanie typu zawsze zwraca .</span><span class="sxs-lookup"><span data-stu-id="be027-121">When the runtime value of the variable is `null`, an `is` statement checking for a type always returns `false`.</span></span> <span data-ttu-id="be027-122">Instrukcja `is` dopasowania wzorca nie zezwala na typ `int?` wartości `Nullable<int>`z wartością null, na przykład lub , ale można przetestować dla dowolnego innego typu wartości.</span><span class="sxs-lookup"><span data-stu-id="be027-122">The pattern matching `is` statement doesn't allow a nullable value type, such as `int?` or `Nullable<int>`, but you can test for any other value type.</span></span> <span data-ttu-id="be027-123">Wzorce `is` z poprzedniego przykładu nie są ograniczone do typów wartości nullable.</span><span class="sxs-lookup"><span data-stu-id="be027-123">The `is` patterns from the preceding example are not limited to the nullable value types.</span></span> <span data-ttu-id="be027-124">Można również użyć tych wzorców, aby sprawdzić, czy zmienna `null`typu odwołania ma wartość lub jest .</span><span class="sxs-lookup"><span data-stu-id="be027-124">You also can use those patterns to test if a variable of a reference type has a value or it's `null`.</span></span>

<span data-ttu-id="be027-125">W poprzednim przykładzie pokazano również, `is` jak używać `switch` wyrażenia dopasowania wzorca w instrukcji, gdzie zmienna może być jednym z wielu różnych typów.</span><span class="sxs-lookup"><span data-stu-id="be027-125">The preceding sample also shows how you use the pattern matching `is` expression in a `switch` statement where the variable may be one of many different types.</span></span>

<span data-ttu-id="be027-126">Jeśli chcesz sprawdzić, czy zmienna jest danym typem, ale nie przypisać go `is` `as` do nowej zmiennej, można użyć i operatorów dla typów odwołań i typów nullable.</span><span class="sxs-lookup"><span data-stu-id="be027-126">If you want to test if a variable is a given type, but not assign it to a new variable, you can use the `is` and `as` operators for reference types and nullable types.</span></span> <span data-ttu-id="be027-127">Poniższy kod pokazuje, jak `is` `as` używać i instrukcje, które były częścią języka C# przed dopasowywanie wzorca został wprowadzony do testowania, jeśli zmienna jest danego typu:</span><span class="sxs-lookup"><span data-stu-id="be027-127">The following code shows how to use the `is` and `as` statements that were part of the C# language before pattern matching was introduced to test if a variable is of a given type:</span></span>

[!code-csharp[testing variable types with the is and as statements](../../../samples/snippets/csharp/how-to/safelycast/asandis/Program.cs#IsAndAs)]

<span data-ttu-id="be027-128">Jak widać, porównując ten kod z kodem dopasowywania wzorców, składnia dopasowania wzorca zapewnia bardziej niezawodne funkcje, łącząc test i przypisanie w jednej instrukcji.</span><span class="sxs-lookup"><span data-stu-id="be027-128">As you can see by comparing this code with the pattern matching code, the pattern matching syntax provides more robust features by combining the test and the assignment in a single statement.</span></span> <span data-ttu-id="be027-129">W miarę możliwości należy używać składni dopasowywania wzorców.</span><span class="sxs-lookup"><span data-stu-id="be027-129">Use the pattern matching syntax whenever possible.</span></span>

<span data-ttu-id="be027-130">Możesz wypróbować te przykłady, patrząc na kod w naszym [repozytorium GitHub](https://github.com/dotnet/samples/tree/master/snippets/csharp/how-to/safelycast).</span><span class="sxs-lookup"><span data-stu-id="be027-130">You can try these samples by looking at the code in our [GitHub repository](https://github.com/dotnet/samples/tree/master/snippets/csharp/how-to/safelycast).</span></span> <span data-ttu-id="be027-131">Możesz też pobrać próbki [jako plik zip](https://github.com/dotnet/samples/raw/master/snippets/csharp/how-to/safelycast.zip).</span><span class="sxs-lookup"><span data-stu-id="be027-131">Or you can download the samples [as a zip file](https://github.com/dotnet/samples/raw/master/snippets/csharp/how-to/safelycast.zip).</span></span>
