---
title: Błąd kompilatora CS0304
ms.date: 07/20/2015
f1_keywords:
- CS0304
helpviewer_keywords:
- CS0304
ms.assetid: 22dc7211-57a8-4c92-96f6-fc1cf0263b68
ms.openlocfilehash: 25ffcf594befe115a400205bae795aefcecbb796
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: HT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/22/2019
ms.locfileid: "59973736"
---
# <a name="compiler-error-cs0304"></a><span data-ttu-id="c4d48-102">Błąd kompilatora CS0304</span><span class="sxs-lookup"><span data-stu-id="c4d48-102">Compiler Error CS0304</span></span>

<span data-ttu-id="c4d48-103">Nie można utworzyć wystąpienia typu zmiennej "type", ponieważ nie ma ograniczenia new()</span><span class="sxs-lookup"><span data-stu-id="c4d48-103">Cannot create an instance of the variable type 'type' because it does not have the new() constraint</span></span>  
  
 <span data-ttu-id="c4d48-104">Podczas implementacji klasy ogólnej, i chcesz użyć `new` — słowo kluczowe, aby utworzyć nowe wystąpienie klasy dowolny typ, który jest dostarczany dla parametru typu `T`, należy najpierw zastosować [ograniczenia new()](../../../csharp/language-reference/keywords/new.md) do `T` w Deklaracja klasy, jak pokazano w poniższym przykładzie.</span><span class="sxs-lookup"><span data-stu-id="c4d48-104">When you implement a generic class, and you want to use the `new` keyword to create a new instance of any type that is supplied for a type parameter `T`, you must apply the [new() constraint](../../../csharp/language-reference/keywords/new.md) to `T` in the class declaration, as shown in the following example.</span></span>  
  
```csharp  
class C<T> where T : new()  
```  
  
 <span data-ttu-id="c4d48-105">`new()` Ograniczenie wymusza typ bezpieczeństwa przez zagwarantowanie, że konkretne dowolnego typu, która jest dostarczana dla `T` ma domyślny, konstruktor bez parametrów.</span><span class="sxs-lookup"><span data-stu-id="c4d48-105">The `new()` constraint enforces type safety by guaranteeing that any concrete type that is supplied for `T` has a default, parameterless constructor.</span></span> <span data-ttu-id="c4d48-106">CS0304 występuje wtedy, gdy próbują użyć `new` operatora w treści klasy, aby utworzyć wystąpienie parametru typu `T` podczas `T` nie określa `new()` ograniczenia.</span><span class="sxs-lookup"><span data-stu-id="c4d48-106">CS0304 occurs if you attempt to use the `new` operator in the body of the class to create an instance of type parameter `T` when `T` does not specify the `new()` constraint.</span></span> <span data-ttu-id="c4d48-107">Po stronie klienta, jeśli kod próbuje utworzyć wystąpienie klasy ogólnej z typem, który ma nie konstruktora bez parametrów, który zostanie wygenerowany kod [błąd kompilatora CS0310](../../../csharp/language-reference/compiler-messages/cs0310.md).</span><span class="sxs-lookup"><span data-stu-id="c4d48-107">On the client side, if code attempts to instantiate the generic class with a type that has no parameterless constructor, that code will generate [Compiler Error CS0310](../../../csharp/language-reference/compiler-messages/cs0310.md).</span></span>  
  
 <span data-ttu-id="c4d48-108">Poniższy przykład generuje CS0304.</span><span class="sxs-lookup"><span data-stu-id="c4d48-108">The following example generates CS0304.</span></span>  
  
```csharp  
// CS0304.cs  
// Compile with: /target:library.  
class C<T>  
{  
    // The following line generates CS0304.  
    T t = new T();  
}  
```  
  
 <span data-ttu-id="c4d48-109">`new` Operator również nie jest dozwolona w metody klasy.</span><span class="sxs-lookup"><span data-stu-id="c4d48-109">The `new` operator also is not allowed in methods of the class.</span></span>  
  
```csharp  
// Compile with: /target:library.  
class C<T>  
{  
    public void ExampleMethod()  
    {  
        // The following line generates CS0304.  
        T t = new T();  
    }  
}  
```  
  
 <span data-ttu-id="c4d48-110">Aby uniknąć tego błędu, należy zadeklarować klasy przy użyciu `new()` ograniczenia, jak pokazano w poniższym przykładzie.</span><span class="sxs-lookup"><span data-stu-id="c4d48-110">To avoid the error, declare the class by using the `new()` constraint, as shown in the following example.</span></span>  
  
```csharp  
// Compile with: /target:library.  
class C<T> where T : new()  
{  
    T t = new T();  
  
    public void ExampleMethod()  
    {  
        T t = new T();  
    }  
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="c4d48-111">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="c4d48-111">See also</span></span>

- [<span data-ttu-id="c4d48-112">Błędy kompilatora C#</span><span class="sxs-lookup"><span data-stu-id="c4d48-112">C# Compiler Errors</span></span>](../../../csharp/language-reference/compiler-messages/index.md)
