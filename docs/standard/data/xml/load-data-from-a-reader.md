---
title: Ładowanie danych z czytnika
ms.date: 03/30/2017
ms.technology: dotnet-standard
ms.assetid: 7e74918c-bc72-4977-a49b-e1520a6d8f60
ms.openlocfilehash: 90a66e04bda4fb2ee4216e8aabd631afb2f28dd0
ms.sourcegitcommit: 5f236cd78cf09593c8945a7d753e0850e96a0b80
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 01/07/2020
ms.locfileid: "75710716"
---
# <a name="load-data-from-a-reader"></a><span data-ttu-id="67756-102">Ładowanie danych z czytnika</span><span class="sxs-lookup"><span data-stu-id="67756-102">Load Data from a Reader</span></span>
<span data-ttu-id="67756-103">Jeśli dokument XML zostanie załadowany przy użyciu metody <xref:System.Xml.XmlDocument.Load%2A> i parametru <xref:System.Xml.XmlReader>, występują różnice w zachowaniu, które występuje w porównaniu z zachowaniem ładowania danych z innych formatów.</span><span class="sxs-lookup"><span data-stu-id="67756-103">If an XML document is loaded using the <xref:System.Xml.XmlDocument.Load%2A> method and a parameter of an <xref:System.Xml.XmlReader>, there are differences in the behavior that occurs when compared to the behavior of loading data from the other formats.</span></span> <span data-ttu-id="67756-104">Jeśli czytnik jest w stanie początkowym, <xref:System.Xml.XmlDocument.Load%2A> zużywa całą zawartość z czytnika i kompiluje Document Object Model XML (DOM) ze wszystkich danych w czytniku.</span><span class="sxs-lookup"><span data-stu-id="67756-104">If the reader is in its initial state, <xref:System.Xml.XmlDocument.Load%2A> consumes the entire contents from the reader and builds the XML Document Object Model (DOM) from all the data in the reader.</span></span>  
  
 <span data-ttu-id="67756-105">Jeśli czytnik jest już umieszczony w węźle w dokumencie, a czytnik jest następnie przenoszona do metody <xref:System.Xml.XmlDocument.Load%2A>, <xref:System.Xml.XmlDocument.Load%2A> próbuje odczytać bieżący węzeł i wszystkie jego elementy równorzędne, aż do tagu końcowego, który zamknie bieżącą głębokość do pamięci.</span><span class="sxs-lookup"><span data-stu-id="67756-105">If the reader is already positioned on a node somewhere in the document, and the reader is then passed to the <xref:System.Xml.XmlDocument.Load%2A> method, <xref:System.Xml.XmlDocument.Load%2A> attempts to read the current node and all of its siblings, up to the end tag that closes the current depth into memory.</span></span> <span data-ttu-id="67756-106">Pomyślnie podjęto próbę <xref:System.Xml.XmlDocument.Load%2A> zależy od węzła, na którym jest on odczytywany podczas próby obciążenia, ponieważ <xref:System.Xml.XmlDocument.Load%2A> sprawdza, czy plik XML z czytnika jest poprawnie sformułowany.</span><span class="sxs-lookup"><span data-stu-id="67756-106">The success of the attempted <xref:System.Xml.XmlDocument.Load%2A> depends on the node that the reader is on when the load is attempted, as <xref:System.Xml.XmlDocument.Load%2A> verifies that the XML from the reader is well-formed.</span></span> <span data-ttu-id="67756-107">Jeśli plik XML nie jest poprawnie sformułowany, <xref:System.Xml.XmlDocument.Load%2A> zgłasza wyjątek.</span><span class="sxs-lookup"><span data-stu-id="67756-107">If the XML is not well-formed, the <xref:System.Xml.XmlDocument.Load%2A> throws an exception.</span></span> <span data-ttu-id="67756-108">Na przykład poniższy zestaw węzłów zawiera dwa elementy poziomu głównego, kod XML nie jest poprawnie sformułowany, a <xref:System.Xml.XmlDocument.Load%2A> zgłasza wyjątek.</span><span class="sxs-lookup"><span data-stu-id="67756-108">For example, the following set of nodes contain two root-level elements, the XML is not well-formed, and <xref:System.Xml.XmlDocument.Load%2A> throws an exception.</span></span>  
  
- <span data-ttu-id="67756-109">Węzeł komentarza, po którym następuje węzeł elementu, po którym następuje węzeł elementu, po którym następuje węzeł EndElement.</span><span class="sxs-lookup"><span data-stu-id="67756-109">Comment node, followed by an Element node, followed by an Element node, followed by an EndElement node.</span></span>  
  
 <span data-ttu-id="67756-110">Poniższy zestaw węzłów tworzy niekompletny DOM, ponieważ nie ma elementu głównego poziomu.</span><span class="sxs-lookup"><span data-stu-id="67756-110">The following set of nodes creates an incomplete DOM, because there is no root-level element.</span></span>  
  
- <span data-ttu-id="67756-111">Węzeł komentarza, po którym następuje węzeł ProcessingInstruction, po którym następuje węzeł komentarza, po którym następuje węzeł EndElement.</span><span class="sxs-lookup"><span data-stu-id="67756-111">Comment node followed by a ProcessingInstruction node followed by a Comment node followed by an EndElement node.</span></span>  
  
 <span data-ttu-id="67756-112">Nie zgłasza to wyjątku, a dane są ładowane.</span><span class="sxs-lookup"><span data-stu-id="67756-112">This does not throw an exception, and the data is loaded.</span></span> <span data-ttu-id="67756-113">Można dodać element główny na początku tych węzłów i utworzyć poprawnie sformułowany kod XML, który można zapisać bez błędu.</span><span class="sxs-lookup"><span data-stu-id="67756-113">You can add a root element to the top of these nodes and create well-formed XML that can be saved without error.</span></span>  
  
 <span data-ttu-id="67756-114">Jeśli czytnik znajduje się w węźle liścia, który jest nieprawidłowy dla poziomu głównego dokumentu (na przykład odstępu lub węzła atrybutu), czytnik jest nadal odczytywany do momentu, aż zostanie umieszczony w węźle, który może być używany jako element główny.</span><span class="sxs-lookup"><span data-stu-id="67756-114">If the reader is positioned on a leaf node that is invalid for the root level of a document (for example, a white space or attribute node), the reader continues to read until it is positioned on a node that can be used for the root.</span></span> <span data-ttu-id="67756-115">Dokument rozpoczyna ładowanie w tym momencie.</span><span class="sxs-lookup"><span data-stu-id="67756-115">The document begins loading at this point.</span></span>  
  
 <span data-ttu-id="67756-116">Domyślnie <xref:System.Xml.XmlDocument.Load%2A> nie sprawdza, czy kod XML jest prawidłowy przy użyciu definicji typu dokumentu (DTD) lub walidacji schematu.</span><span class="sxs-lookup"><span data-stu-id="67756-116">By default, <xref:System.Xml.XmlDocument.Load%2A> does not verify whether the XML is valid using document type definition (DTD) or schema validation.</span></span> <span data-ttu-id="67756-117">Sprawdza tylko, czy plik XML jest poprawnie sformułowany.</span><span class="sxs-lookup"><span data-stu-id="67756-117">It only verifies whether the XML is well-formed.</span></span> <span data-ttu-id="67756-118">Aby można było sprawdzić poprawność, należy utworzyć <xref:System.Xml.XmlReader> przy użyciu klasy <xref:System.Xml.XmlReaderSettings>.</span><span class="sxs-lookup"><span data-stu-id="67756-118">In order for validation to occur, you need to create an <xref:System.Xml.XmlReader> using the <xref:System.Xml.XmlReaderSettings> class.</span></span> <span data-ttu-id="67756-119">Klasa <xref:System.Xml.XmlReader> może wymusić walidację przy użyciu schematu DTD lub języka definicji schematu (XSD).</span><span class="sxs-lookup"><span data-stu-id="67756-119">The <xref:System.Xml.XmlReader> class can enforce validation using a DTD or Schema definition language (XSD) schema.</span></span> <span data-ttu-id="67756-120">Właściwość <xref:System.Xml.ValidationType> klasy <xref:System.Xml.XmlReaderSettings> określa, czy wystąpienie <xref:System.Xml.XmlReader> wymusza weryfikację.</span><span class="sxs-lookup"><span data-stu-id="67756-120">The <xref:System.Xml.ValidationType> property on the <xref:System.Xml.XmlReaderSettings> class determines whether the <xref:System.Xml.XmlReader> instance enforces validation.</span></span> <span data-ttu-id="67756-121">Aby uzyskać więcej informacji na temat walidacji danych XML, zobacz sekcję uwagi na stronie informacje referencyjne <xref:System.Xml.XmlReader>.</span><span class="sxs-lookup"><span data-stu-id="67756-121">For more information about validating XML data, see the Remarks section of the <xref:System.Xml.XmlReader> reference page.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="67756-122">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="67756-122">See also</span></span>

- [<span data-ttu-id="67756-123">Model DOM (XML Document Object Model)</span><span class="sxs-lookup"><span data-stu-id="67756-123">XML Document Object Model (DOM)</span></span>](../../../../docs/standard/data/xml/xml-document-object-model-dom.md)
