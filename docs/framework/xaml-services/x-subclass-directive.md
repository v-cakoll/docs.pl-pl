---
title: x:Subclass — dyrektywa
ms.date: 03/30/2017
f1_keywords:
- Subclass
- xSubclass
- x:Subclass
helpviewer_keywords:
- x:Subclass attribute [XAML Services]
- XAML [XAML Services], x:Subclass attribute
- Subclass attribute in XAML [XAML Services]
ms.assetid: 99f66072-8107-4362-ab99-8171dc83b469
ms.openlocfilehash: c348d8fa2bd66a9abbb64c9363bb4dae0933ba34
ms.sourcegitcommit: 5c1abeec15fbddcc7dbaa729fabc1f1f29f12045
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/15/2019
ms.locfileid: "58047999"
---
# <a name="xsubclass-directive"></a><span data-ttu-id="cd6a9-102">x:Subclass — dyrektywa</span><span class="sxs-lookup"><span data-stu-id="cd6a9-102">x:Subclass Directive</span></span>
<span data-ttu-id="cd6a9-103">Modyfikuje zachowanie kompilacji znaczników XAML podczas `x:Class` jest również udostępniany.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-103">Modifies XAML markup compile behavior when `x:Class` is also provided.</span></span> <span data-ttu-id="cd6a9-104">Zamiast tworzyć częściową klasą, która opiera się na `x:Class`, podane `x:Class` jest tworzona jako klasa pośrednicząca, i następnie powinien opierać się na podanej klasy pochodnej `x:Class`.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-104">Instead of creating a partial class that is based on `x:Class`, the provided `x:Class` is created as an intermediate class, and then your provided derived class is expected to be based on `x:Class`.</span></span>  
  
## <a name="xaml-attribute-usage"></a><span data-ttu-id="cd6a9-105">Użycie atrybutu języka XAML</span><span class="sxs-lookup"><span data-stu-id="cd6a9-105">XAML Attribute Usage</span></span>  
  
```  
<object x:Class="namespace.classname" x:Subclass="subclassNamespace.subclassName">  
   ...  
</object>  
```  
  
## <a name="xaml-values"></a><span data-ttu-id="cd6a9-106">Wartości XAML</span><span class="sxs-lookup"><span data-stu-id="cd6a9-106">XAML Values</span></span>  
  
|||  
|-|-|  
|`namespace`|<span data-ttu-id="cd6a9-107">Opcjonalna.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-107">Optional.</span></span> <span data-ttu-id="cd6a9-108">Określa przestrzeń nazw środowiska CLR, który zawiera `classname`.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-108">Specifies a CLR namespace that contains `classname`.</span></span> <span data-ttu-id="cd6a9-109">Jeśli `namespace` określono pojedynczego znaku kropki (.) oddziela `namespace` i `classname`.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-109">If `namespace` is specified, a dot (.) separates `namespace` and `classname`.</span></span>|  
|`classname`|<span data-ttu-id="cd6a9-110">Wymagana.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-110">Required.</span></span> <span data-ttu-id="cd6a9-111">Określa nazwę CLR częściową klasą, która łączy XAML załadowane i usługi związane z kodem dla tego XAML.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-111">Specifies the CLR name of the partial class that connects the loaded XAML and your code-behind for that XAML.</span></span> <span data-ttu-id="cd6a9-112">Zobacz uwagi.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-112">See Remarks.</span></span>|  
|`subclassNamespace`|<span data-ttu-id="cd6a9-113">Opcjonalna.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-113">Optional.</span></span> <span data-ttu-id="cd6a9-114">Może być inny niż `namespace` Jeśli każdej przestrzeni nazw można rozwiązać drugi.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-114">Can be different from `namespace` if each namespace can resolve the other.</span></span> <span data-ttu-id="cd6a9-115">Określa przestrzeń nazw środowiska CLR, który zawiera `subclassName`.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-115">Specifies a CLR namespace that contains `subclassName`.</span></span> <span data-ttu-id="cd6a9-116">Jeśli `subclassName` określono pojedynczego znaku kropki (.) oddziela `subclassNamespace` i `subclassName`.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-116">If `subclassName` is specified, a dot (.) separates `subclassNamespace` and `subclassName`.</span></span>|  
|`subclassName`|<span data-ttu-id="cd6a9-117">Wymagana.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-117">Required.</span></span> <span data-ttu-id="cd6a9-118">Określa nazwę CLR podklasy.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-118">Specifies the CLR name of the subclass.</span></span>|  
  
## <a name="dependencies"></a><span data-ttu-id="cd6a9-119">Zależności</span><span class="sxs-lookup"><span data-stu-id="cd6a9-119">Dependencies</span></span>  
 <span data-ttu-id="cd6a9-120">[x: Class — dyrektywa](x-class-directive.md) również musi być podana dla tego samego obiektu, a ten obiekt musi być elementem głównym produkcji XAML.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-120">[x:Class Directive](x-class-directive.md) must also be provided on the same object, and that object must be the root element of the XAML production.</span></span>  
  
## <a name="remarks"></a><span data-ttu-id="cd6a9-121">Uwagi</span><span class="sxs-lookup"><span data-stu-id="cd6a9-121">Remarks</span></span>  
 <span data-ttu-id="cd6a9-122">`x:Subclass` użycie jest przeznaczone głównie dla języków, które nie obsługują deklaracji klasy częściowej.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-122">`x:Subclass` usage is primarily intended for languages that do not support partial class declarations.</span></span>  
  
 <span data-ttu-id="cd6a9-123">Klasa służy jako `x:Subclass` nie może być klasą zagnieżdżoną i `x:Subclass` musi odwoływać się do obiektu głównego, zgodnie z opisem w sekcji "Dependencies".</span><span class="sxs-lookup"><span data-stu-id="cd6a9-123">The class used as the `x:Subclass` cannot be a nested class, and `x:Subclass` must refer to the root object as explained in the "Dependencies" section.</span></span>  
  
 <span data-ttu-id="cd6a9-124">W przeciwnym razie koncepcyjny znaczenie `x:Subclass` jest niezdefiniowana przy wdrażaniu usług programu .NET Framework XAML.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-124">Otherwise, the conceptual meaning of `x:Subclass` is undefined by a .NET Framework XAML Services implementation.</span></span> <span data-ttu-id="cd6a9-125">Jest to spowodowane zachowanie usług programu .NET Framework XAML nie określa ogólnym modelu programowania, XAML, które są połączone znaczników i kopii kodu.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-125">This is because .NET Framework XAML Services behavior does not specify the overall programming model by which XAML markup and backing code are connected.</span></span> <span data-ttu-id="cd6a9-126">Implementacje dalsze pojęcia związane z `x:Class` i `x:Subclass` są wykonywane przez określonych platform, korzystające z modeli programowania lub modeli aplikacji do definiowania sposobu łączenia znaczników XAML, skompilowany kod znaczników i na podstawie CLR związane z kodem.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-126">Implementations of further concepts related to `x:Class` and `x:Subclass` are performed by specific frameworks that use programming models or application models to define how to connect XAML markup, compiled markup, and CLR-based code-behind.</span></span> <span data-ttu-id="cd6a9-127">Każdej struktury, może mieć własne działania kompilacji, umożliwiające niektóre zachowania lub określonych składników, które muszą być zawarte w środowisku kompilacji.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-127">Each framework might have its own build actions that enable some of the behavior, or specific components that must be included in the build environment.</span></span> <span data-ttu-id="cd6a9-128">W ramach akcji kompilacji można też różnią się zależnie od określonego języka środowiska CLR, używany do związanym z kodem.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-128">Within a framework, build actions can also vary based on the specific CLR language that is used for the code-behind.</span></span>  
  
## <a name="wpf-usage-notes"></a><span data-ttu-id="cd6a9-129">Uwagi dotyczące użytkowania WPF</span><span class="sxs-lookup"><span data-stu-id="cd6a9-129">WPF Usage Notes</span></span>  
 <span data-ttu-id="cd6a9-130">`x:Subclass` można na głównej stronie lub <xref:System.Windows.Application> główny urząd certyfikacji w definicji aplikacji, która ma już `x:Class`.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-130">`x:Subclass` can be on a page root or on the <xref:System.Windows.Application> root in the application definition, which already has `x:Class`.</span></span> <span data-ttu-id="cd6a9-131">Deklarowanie `x:Subclass` dowolnego elementu innego niż katalog główny strony lub aplikacji lub określając jej w przypadku, gdy nie `x:Class` istnieje, powoduje błąd kompilacji.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-131">Declaring `x:Subclass` on any element other than a page or application root, or specifying it where no `x:Class` exists, causes a compile-time error.</span></span>  
  
 <span data-ttu-id="cd6a9-132">Tworzenie pochodnej klasy pracę poprawnie na potrzeby `x:Subclass` scenariusz jest dosyć złożona.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-132">Creating derived classes that work correctly for the `x:Subclass` scenario is fairly complex.</span></span> <span data-ttu-id="cd6a9-133">Może być konieczne zbadanie pośrednie pliki (pliki .g utworzony w folderze obj. projektu przez kompilacji znaczników, z nazwami, które zawierają nazwy plików .xaml).</span><span class="sxs-lookup"><span data-stu-id="cd6a9-133">You might need to examine the intermediate files (the .g files produced in the obj folder of your project by markup compile, with names that incorporate the .xaml file names).</span></span> <span data-ttu-id="cd6a9-134">Te pliki pośrednie mogą pomóc w określeniu pochodzenia pewnych narzędzi programistycznych w dołączonym do klas częściowych w skompilowanej aplikacji.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-134">These intermediate files can help you determine the origin of certain programming constructs in the joined partial classes in the compiled application.</span></span>  
  
 <span data-ttu-id="cd6a9-135">Programy obsługi zdarzeń w klasie pochodnej musi być `internal override` (`Friend Overrides` programu Microsoft Visual Basic) w celu zastąpienia klas zastępczych dla programów obsługi, podczas tworzenia klasy pośrednie podczas kompilacji.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-135">Event handlers in the derived class must be `internal override` (`Friend Overrides` in Microsoft Visual Basic) in order to override the stubs for the handlers as created in the intermediate class during compilation.</span></span> <span data-ttu-id="cd6a9-136">W przeciwnym razie implementacji klasy pochodnej ukryć (w tle) Implementacja klasy pośrednich i nie są wywoływane programy obsługi klasa pośrednicząca.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-136">Otherwise, the derived class implementations hide (shadow) the intermediate class implementation and the intermediate class handlers are not invoked.</span></span>  
  
 <span data-ttu-id="cd6a9-137">Podczas definiowania obu `x:Class` i `x:Subclass`, trzeba podawać żadnych implementację klasy, która odwołuje się do niej `x:Class`.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-137">When you define both `x:Class` and `x:Subclass`, you do not need to provide any implementation for the class that is referenced by `x:Class`.</span></span> <span data-ttu-id="cd6a9-138">Musisz nadać mu nazwę, za pośrednictwem `x:Class` atrybutu, aby kompilator zawiera wskazówki dotyczące klasy, która tworzy w plikach pośredni (kompilator nie wybierać nazwę domyślną w tym przypadku).</span><span class="sxs-lookup"><span data-stu-id="cd6a9-138">You only need to give it a name via the `x:Class` attribute so that the compiler has some guidance for the class that it creates in the intermediate files (the compiler does not select a default name in this case).</span></span> <span data-ttu-id="cd6a9-139">Możesz nadać `x:Class` Implementacja klasy; jednak nie jest to typowy scenariusz przy użyciu zarówno `x:Class` i `x:Subclass`.</span><span class="sxs-lookup"><span data-stu-id="cd6a9-139">You can give the `x:Class` class an implementation; however, this is not the typical scenario for using both `x:Class` and `x:Subclass`.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="cd6a9-140">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="cd6a9-140">See also</span></span>
- [<span data-ttu-id="cd6a9-141">x:Class, dyrektywa</span><span class="sxs-lookup"><span data-stu-id="cd6a9-141">x:Class Directive</span></span>](x-class-directive.md)
- [<span data-ttu-id="cd6a9-142">Klasy XAML i niestandardowe dla WPF</span><span class="sxs-lookup"><span data-stu-id="cd6a9-142">XAML and Custom Classes for WPF</span></span>](../wpf/advanced/xaml-and-custom-classes-for-wpf.md)
