---
title: Ograniczenia dotyczące parametrów typu — C# Przewodnik programowania
ms.custom: seodec18
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 5c36639d76a6fbd4e36f39486369a55a56a6e3ea
ms.sourcegitcommit: da2dd2772fcf32b44eb18b1cbe8affd17b1753c9
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/27/2019
ms.locfileid: "71396284"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="dbdf0-102">Ograniczenia dotyczące parametrów typu (C# Przewodnik programowania)</span><span class="sxs-lookup"><span data-stu-id="dbdf0-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="dbdf0-103">Ograniczenia informują kompilator o możliwościach, które argument typu musi zawierać.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="dbdf0-104">Bez żadnych ograniczeń argument typu może być dowolnym typem.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="dbdf0-105">Kompilator może przyjmować tylko elementy członkowskie <xref:System.Object?displayProperty=nameWithType>, która jest ostateczną klasą bazową dla dowolnego typu .NET.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="dbdf0-106">Aby uzyskać więcej informacji, zobacz [Dlaczego należy używać ograniczeń](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="dbdf0-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="dbdf0-107">Jeśli kod klienta próbuje utworzyć wystąpienie klasy przy użyciu typu, który nie jest dozwolony przez ograniczenie, wynikiem jest błąd czasu kompilacji.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="dbdf0-108">Ograniczenia są określone za pomocą kontekstowego słowa kluczowego `where`.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="dbdf0-109">W poniższej tabeli wymieniono siedem typów ograniczeń:</span><span class="sxs-lookup"><span data-stu-id="dbdf0-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="dbdf0-110">Typu</span><span class="sxs-lookup"><span data-stu-id="dbdf0-110">Constraint</span></span>|<span data-ttu-id="dbdf0-111">Opis</span><span class="sxs-lookup"><span data-stu-id="dbdf0-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="dbdf0-112">Argument typu musi być typem wartości.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-112">The type argument must be a value type.</span></span> <span data-ttu-id="dbdf0-113">Wszystkie wartości typu z wyjątkiem <xref:System.Nullable%601> można określić.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-113">Any value type except <xref:System.Nullable%601> can be specified.</span></span> <span data-ttu-id="dbdf0-114">Aby uzyskać więcej informacji na temat typów wartości null, zobacz [dopuszczanie typów wartości null](../nullable-types/index.md).</span><span class="sxs-lookup"><span data-stu-id="dbdf0-114">For more information about nullable value types, see [Nullable value types](../nullable-types/index.md).</span></span>|
|`where T : class`|<span data-ttu-id="dbdf0-115">Argument typu musi być typem referencyjnym.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-115">The type argument must be a reference type.</span></span> <span data-ttu-id="dbdf0-116">To ograniczenie dotyczy również dowolnego typu klasy, interfejsu, delegata lub tablicy.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-116">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="dbdf0-117">Argument typu musi być typem niedopuszczający wartości null.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-117">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="dbdf0-118">Argument może być typem referencyjnym niedopuszczający wartości null C# w 8,0 lub późniejszym lub nie DOPUSZCZANYM typem wartości.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-118">The argument can be a non-nullable reference type in C# 8.0 or later, or a not nullable value type.</span></span> <span data-ttu-id="dbdf0-119">To ograniczenie dotyczy również dowolnego typu klasy, interfejsu, delegata lub tablicy.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-119">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="dbdf0-120">Argument typu musi być [typem niezarządzanym](../../language-reference/builtin-types/unmanaged-types.md).</span><span class="sxs-lookup"><span data-stu-id="dbdf0-120">The type argument must be an [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span>|
|`where T : new()`|<span data-ttu-id="dbdf0-121">Typ argumentu musi mieć publicznego konstruktora bez parametrów.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-121">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="dbdf0-122">Gdy jest używany z innymi ograniczeniami `new()` ograniczenie musi być określony jako ostatni.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-122">When used together with other constraints, the `new()` constraint must be specified last.</span></span>|
|<span data-ttu-id="dbdf0-123">`where T :` *\<base nazwę klasy >*</span><span class="sxs-lookup"><span data-stu-id="dbdf0-123">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="dbdf0-124">Argument typu musi być lub pochodzić od określonej klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-124">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="dbdf0-125">`where T :` *\<interface nazwa >*</span><span class="sxs-lookup"><span data-stu-id="dbdf0-125">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="dbdf0-126">Argument typu muszą być lub implementować określonego interfejsu.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-126">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="dbdf0-127">Można określić wiele ograniczeń interfejsu.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-127">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="dbdf0-128">Można też ogólnego ograniczający interfejsu.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-128">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="dbdf0-129">Argumentu typu dostarczonego T musi być lub pochodzić od argument dostarczony dla U.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-129">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

<span data-ttu-id="dbdf0-130">Niektóre ograniczenia wykluczają się wzajemnie.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-130">Some of the constraints are mutually exclusive.</span></span> <span data-ttu-id="dbdf0-131">Wszystkie typy wartości muszą mieć dostępny Konstruktor bez parametrów.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-131">All value types must have an accessible parameterless constructor.</span></span> <span data-ttu-id="dbdf0-132">Ograniczenie `struct` implikuje ograniczenie `new()` i nie można łączyć ograniczenia `new()` z ograniczeniem `struct`.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-132">The `struct` constraint implies the `new()` constraint and the `new()` constraint can't be combined with the `struct` constraint.</span></span> <span data-ttu-id="dbdf0-133">Ograniczenie `unmanaged` implikuje ograniczenie `struct`.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-133">The `unmanaged` constraint implies the `struct` constraint.</span></span> <span data-ttu-id="dbdf0-134">Nie można łączyć ograniczenia `unmanaged` z ograniczeniami `struct` lub `new()`.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-134">The `unmanaged` constraint can't be combined with either the `struct` or `new()` constraints.</span></span>

## <a name="why-use-constraints"></a><span data-ttu-id="dbdf0-135">Dlaczego warto używać ograniczeń</span><span class="sxs-lookup"><span data-stu-id="dbdf0-135">Why use constraints</span></span>

<span data-ttu-id="dbdf0-136">Ograniczając parametr typu, zwiększa liczbę dozwolonych operacji i wywołań metod do tych, które są obsługiwane przez typ ograniczenia i wszystkie typy w hierarchii dziedziczenia.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-136">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="dbdf0-137">Podczas projektowania klas ogólnych lub metod w przypadku wykonywania operacji na ogólnych elementach członkowskich wykraczających poza proste przypisanie lub wywoływanie metod nieobsługiwanych przez <xref:System.Object?displayProperty=nameWithType> należy zastosować ograniczenia do parametru typu.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-137">When you design generic classes or methods, if you'll be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="dbdf0-138">Na przykład ograniczenie klasy bazowej instruuje kompilator, że tylko obiekty tego typu lub pochodne z tego typu będą używane jako argumenty typu.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-138">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="dbdf0-139">Gdy kompilator ma tę gwarancję, może zezwolić na wywoływanie metod tego typu w klasie generycznej.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-139">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="dbdf0-140">Poniższy przykład kodu demonstruje funkcjonalność, którą można dodać do klasy `GenericList<T>` (w artykule [wprowadzenie do typów ogólnych](introduction-to-generics.md)) przez zastosowanie ograniczenia klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-140">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](introduction-to-generics.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="dbdf0-141">Ograniczenie pozwala klasie generycznej używać właściwości `Employee.Name`.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-141">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="dbdf0-142">Ograniczenie określa, że wszystkie elementy typu `T` mają być obiektem `Employee` lub obiektem, który dziedziczy po `Employee`.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-142">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="dbdf0-143">Do tego samego parametru typu można zastosować wiele ograniczeń, a same ograniczenia mogą być typami ogólnymi w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="dbdf0-143">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="dbdf0-144">Podczas stosowania ograniczenia `where T : class` należy unikać operatorów `==` i `!=` w parametrze typu, ponieważ te operatory przetestują tylko tożsamość referencyjną, a nie dla równości wartości.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-144">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="dbdf0-145">To zachowanie występuje nawet wtedy, gdy te operatory są przeciążone w typie, który jest używany jako argument.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-145">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="dbdf0-146">Poniższy kod ilustruje ten punkt; Wyjście ma wartość false, mimo że Klasa <xref:System.String> przeciąża operator `==`.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-146">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="dbdf0-147">Kompilator wie, że `T` jest typem referencyjnym w czasie kompilacji i musi używać domyślnych operatorów, które są prawidłowe dla wszystkich typów referencyjnych.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-147">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="dbdf0-148">Jeśli konieczne jest przetestowanie pod kątem równości wartości, zalecanym sposobem jest również zastosowanie ograniczenia `where T : IEquatable<T>` lub `where T : IComparable<T>` i zaimplementowanie interfejsu w dowolnej klasie, która będzie używana do konstruowania klasy generycznej.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-148">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="dbdf0-149">Ograniczanie wielu parametrów</span><span class="sxs-lookup"><span data-stu-id="dbdf0-149">Constraining multiple parameters</span></span>

<span data-ttu-id="dbdf0-150">Można zastosować ograniczenia do wielu parametrów i wiele ograniczeń do jednego parametru, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="dbdf0-150">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="dbdf0-151">Niepowiązane parametry typu</span><span class="sxs-lookup"><span data-stu-id="dbdf0-151">Unbounded type parameters</span></span>

 <span data-ttu-id="dbdf0-152">Parametry typu, które nie mają żadnych ograniczeń, takich jak T w klasie publicznej `SampleClass<T>{}`, są nazywane niezwiązanymi parametrami typu.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-152">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="dbdf0-153">Parametry typu niepowiązanego mają następujące reguły:</span><span class="sxs-lookup"><span data-stu-id="dbdf0-153">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="dbdf0-154">Nie można używać operatorów `!=` i `==`, ponieważ nie ma gwarancji, że konkretny argument typu będzie obsługiwał te operatory.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-154">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="dbdf0-155">Mogą być konwertowane do i z `System.Object` lub jawnie konwertowane na dowolny typ interfejsu.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-155">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="dbdf0-156">Można je porównać z wartością [null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="dbdf0-156">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="dbdf0-157">Jeśli niezwiązany parametr jest porównywany z `null`, porównanie zwróci wartość false, jeśli argument typu jest typem wartości.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-157">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="dbdf0-158">Parametry typu jako ograniczenia</span><span class="sxs-lookup"><span data-stu-id="dbdf0-158">Type parameters as constraints</span></span>

<span data-ttu-id="dbdf0-159">Użycie parametru typu ogólnego jako ograniczenia jest przydatne, gdy funkcja członkowska z własnym parametrem typu musi ograniczyć ten parametr do parametru typu zawierającego typ, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="dbdf0-159">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="dbdf0-160">W poprzednim przykładzie `T` jest ograniczeniem typu w kontekście metody `Add` i niezwiązanym parametrem typu w kontekście klasy `List`.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-160">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="dbdf0-161">Parametry typu mogą być również używane jako ograniczenia w definicjach klasy generycznej.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-161">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="dbdf0-162">Parametr type musi być zadeklarowany w nawiasach kątowych wraz z innymi parametrami typu:</span><span class="sxs-lookup"><span data-stu-id="dbdf0-162">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="dbdf0-163">Użyteczność parametrów typu jako ograniczenia z klasami generycznymi jest ograniczona, ponieważ kompilator może nie zajmować niczego informacji o parametrze typu, z wyjątkiem tego, że pochodzi on z `System.Object`.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-163">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="dbdf0-164">Użyj parametrów typu jako ograniczeń dotyczących klas ogólnych w scenariuszach, w których chcesz wymusić relację dziedziczenia między dwoma parametrami typu.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-164">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="dbdf0-165">Ograniczenie NotNull</span><span class="sxs-lookup"><span data-stu-id="dbdf0-165">NotNull constraint</span></span>

<span data-ttu-id="dbdf0-166">Począwszy od C# 8,0, można użyć ograniczenia `notnull`, aby określić, że argument typu musi być typem wartości niedopuszczających wartości null lub typem referencyjnym, który nie dopuszcza wartości null.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-166">Beginning with C# 8.0, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="dbdf0-167">Ograniczenia `notnull` można używać tylko w kontekście `nullable enable`.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-167">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="dbdf0-168">Kompilator generuje ostrzeżenie w przypadku dodania ograniczenia `notnull` w kontekście dopuszczającym wartość null.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-168">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span> 

<span data-ttu-id="dbdf0-169">W przeciwieństwie do innych ograniczeń, gdy argument typu narusza ograniczenie `notnull`, kompilator generuje ostrzeżenie, gdy ten kod jest kompilowany w kontekście `nullable enable`.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-169">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="dbdf0-170">Jeśli kod jest kompilowany w kontekście Oblivious dopuszczający wartość null, kompilator nie generuje żadnych ostrzeżeń ani błędów.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-170">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="dbdf0-171">Niezarządzany warunek ograniczający</span><span class="sxs-lookup"><span data-stu-id="dbdf0-171">Unmanaged constraint</span></span>

<span data-ttu-id="dbdf0-172">Począwszy od C# 7,3, można użyć `unmanaged` ograniczenia, aby określić, że parametr typu musi być typem niezarządzanym. [typ niezarządzany](../../language-reference/builtin-types/unmanaged-types.md)</span><span class="sxs-lookup"><span data-stu-id="dbdf0-172">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be an [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="dbdf0-173">Ograniczenie `unmanaged` umożliwia zapisanie procedur wielokrotnego użytku w celu pracy z typami, które mogą być przetwarzane jako bloki pamięci, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="dbdf0-173">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="dbdf0-174">Poprzednia metoda musi być skompilowana w kontekście `unsafe`, ponieważ używa operatora `sizeof` dla typu nieznanego jako typ wbudowany.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-174">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="dbdf0-175">Bez ograniczenia `unmanaged` operator `sizeof` jest niedostępny.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-175">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="dbdf0-176">Delegowanie ograniczeń</span><span class="sxs-lookup"><span data-stu-id="dbdf0-176">Delegate constraints</span></span>

<span data-ttu-id="dbdf0-177">Począwszy od C# 7,3, można użyć <xref:System.Delegate?displayProperty=nameWithType> lub <xref:System.MulticastDelegate?displayProperty=nameWithType> jako ograniczenia klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-177">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="dbdf0-178">Środowisko CLR zawsze zezwala na to ograniczenie, ale C# język nie jest dozwolony.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-178">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="dbdf0-179">Ograniczenie `System.Delegate` umożliwia pisanie kodu, który współpracuje z delegatami w sposób bezpieczny dla typów.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-179">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="dbdf0-180">Poniższy kod definiuje metodę rozszerzenia, która łączy dwa Delegaty pod warunkiem, że są tego samego typu:</span><span class="sxs-lookup"><span data-stu-id="dbdf0-180">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="dbdf0-181">Możesz użyć powyższej metody do łączenia delegatów, które są tego samego typu:</span><span class="sxs-lookup"><span data-stu-id="dbdf0-181">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="dbdf0-182">Usunięcie komentarza do ostatniego wiersza nie spowoduje skompilowania.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-182">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="dbdf0-183">Elementy `first` i `test` są typami delegatów, ale są różnymi typami delegatów.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-183">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="dbdf0-184">Ograniczenia wyliczeniowe</span><span class="sxs-lookup"><span data-stu-id="dbdf0-184">Enum constraints</span></span>

<span data-ttu-id="dbdf0-185">Począwszy od C# 7,3, można również określić typ <xref:System.Enum?displayProperty=nameWithType> jako ograniczenie klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-185">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="dbdf0-186">Środowisko CLR zawsze zezwala na to ograniczenie, ale C# język nie jest dozwolony.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-186">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="dbdf0-187">Typy ogólne wykorzystujące `System.Enum` zapewniają programowanie bezpiecznego typu w celu buforowania wyników z używania metod statycznych w `System.Enum`.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-187">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="dbdf0-188">Poniższy przykład odnajduje wszystkie prawidłowe wartości dla typu wyliczeniowego, a następnie tworzy słownik, który mapuje te wartości na jego reprezentację w postaci ciągu.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-188">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="dbdf0-189">Używane metody wykorzystują odbicie, które mają wpływ na wydajność.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-189">The methods used make use of reflection, which has performance implications.</span></span> <span data-ttu-id="dbdf0-190">Można wywołać tę metodę, aby utworzyć kolekcję, która jest buforowana i ponownie używana zamiast powtarzających się wywołań, które wymagają odbicia.</span><span class="sxs-lookup"><span data-stu-id="dbdf0-190">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="dbdf0-191">Można go użyć, jak pokazano w poniższym przykładzie, aby utworzyć Wyliczenie i skompilować słownik jego wartości i nazw:</span><span class="sxs-lookup"><span data-stu-id="dbdf0-191">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="dbdf0-192">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="dbdf0-192">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="dbdf0-193">Przewodnik programowania w języku C#</span><span class="sxs-lookup"><span data-stu-id="dbdf0-193">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="dbdf0-194">Wprowadzenie do typów ogólnych</span><span class="sxs-lookup"><span data-stu-id="dbdf0-194">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="dbdf0-195">Klasy ogólne</span><span class="sxs-lookup"><span data-stu-id="dbdf0-195">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="dbdf0-196">new, ograniczenie</span><span class="sxs-lookup"><span data-stu-id="dbdf0-196">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
