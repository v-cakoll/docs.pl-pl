---
title: Zestawy kolekcjonowane dla dynamicznego generowania typów
description: ''
ms.date: 08/29/2017
helpviewer_keywords:
- reflection, dynamic assembly
- assemblies, collectible
- collectible assemblies, retrieving
ms.openlocfilehash: 02c7048e0321282463aa3558287d1d13c5e4f8d2
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/12/2020
ms.locfileid: "79180542"
---
# <a name="collectible-assemblies-for-dynamic-type-generation"></a><span data-ttu-id="58e20-102">Zestawy kolekcjonowane dla dynamicznego generowania typów</span><span class="sxs-lookup"><span data-stu-id="58e20-102">Collectible assemblies for dynamic type generation</span></span>

<span data-ttu-id="58e20-103">*Zestawy kolekcjonerskie* to zestawy dynamiczne, które można zwolnić bez zwalniania domeny aplikacji, w której zostały utworzone.</span><span class="sxs-lookup"><span data-stu-id="58e20-103">*Collectible assemblies* are dynamic assemblies that can be unloaded without unloading the application domain in which they were created.</span></span> <span data-ttu-id="58e20-104">Wszystkie zarządzane i niezarządzane pamięci używane przez kolekcjonowane zestawu i typów, które zawiera mogą być odzyskane.</span><span class="sxs-lookup"><span data-stu-id="58e20-104">All managed and unmanaged memory used by a collectible assembly and the types it contains can be reclaimed.</span></span> <span data-ttu-id="58e20-105">Informacje, takie jak nazwa zestawu, są usuwane z tabel wewnętrznych.</span><span class="sxs-lookup"><span data-stu-id="58e20-105">Information such as the assembly name is removed from internal tables.</span></span>

<span data-ttu-id="58e20-106">Aby włączyć zwalnianie, <xref:System.Reflection.Emit.AssemblyBuilderAccess.RunAndCollect?displayProperty=nameWithType> należy użyć flagi podczas tworzenia zespołu dynamicznego.</span><span class="sxs-lookup"><span data-stu-id="58e20-106">To enable unloading, use the <xref:System.Reflection.Emit.AssemblyBuilderAccess.RunAndCollect?displayProperty=nameWithType> flag when you create a dynamic assembly.</span></span> <span data-ttu-id="58e20-107">Zestaw jest przejściowy (to znaczy, że nie można go zapisać) i podlega ograniczeniom opisanym w sekcji [Ograniczenia dotyczące zestawów kolekcjonerskich.](#restrictions-on-collectible-assemblies)</span><span class="sxs-lookup"><span data-stu-id="58e20-107">The assembly is transient (that is, it cannot be saved) and is subject to limitations described in the [Restrictions on Collectible Assemblies](#restrictions-on-collectible-assemblies) section.</span></span> <span data-ttu-id="58e20-108">Środowisko uruchomieniowe języka wspólnego (CLR) zwalnia kolekcjonowania zestawu automatycznie po zwolnieniu wszystkich obiektów skojarzonych z zestawu.</span><span class="sxs-lookup"><span data-stu-id="58e20-108">The common language runtime (CLR) unloads a collectible assembly automatically when you release all objects associated with the assembly.</span></span> <span data-ttu-id="58e20-109">Pod każdym względem zestawy kolekcjonerskie są tworzone i używane w taki sam sposób jak inne zestawy dynamiczne.</span><span class="sxs-lookup"><span data-stu-id="58e20-109">In all other respects, collectible assemblies are created and used in the same way as other dynamic assemblies.</span></span>

## <a name="lifetime-of-collectible-assemblies"></a><span data-ttu-id="58e20-110">Okres istnienia zespołów kolekcjonerskich</span><span class="sxs-lookup"><span data-stu-id="58e20-110">Lifetime of collectible assemblies</span></span>

<span data-ttu-id="58e20-111">Okres istnienia kolekcjonowania zestawu jest kontrolowana przez istnienie odwołań do typów, które zawiera i obiektów, które są tworzone z tych typów.</span><span class="sxs-lookup"><span data-stu-id="58e20-111">The lifetime of a collectible assembly is controlled by the existence of references to the types it contains and the objects that are created from those types.</span></span> <span data-ttu-id="58e20-112">Środowisko uruchomieniowe języka wspólnego nie zwalnia zestawu, o ile`T` istnieje jeden lub więcej z poniższych ( jest dowolnym typem zdefiniowanym w zestawie):</span><span class="sxs-lookup"><span data-stu-id="58e20-112">The common language runtime does not unload an assembly as long as one or more of the following exist (`T` is any type that is defined in the assembly):</span></span>

- <span data-ttu-id="58e20-113">Wystąpienie elementu `T`.</span><span class="sxs-lookup"><span data-stu-id="58e20-113">An instance of `T`.</span></span>

- <span data-ttu-id="58e20-114">Wystąpienie tablicy `T`. .</span><span class="sxs-lookup"><span data-stu-id="58e20-114">An instance of an array of `T`.</span></span>

- <span data-ttu-id="58e20-115">Wystąpienie typu ogólnego, `T` który ma jako jeden z jego argumentów typu.</span><span class="sxs-lookup"><span data-stu-id="58e20-115">An instance of a generic type that has `T` as one of its type arguments.</span></span> <span data-ttu-id="58e20-116">Obejmuje to ogólne `T`kolekcje , nawet jeśli ta kolekcja jest pusta.</span><span class="sxs-lookup"><span data-stu-id="58e20-116">This includes generic collections of `T`, even if that collection is empty.</span></span>

- <span data-ttu-id="58e20-117">Wystąpienie <xref:System.Type> lub <xref:System.Reflection.Emit.TypeBuilder> które `T`reprezentuje .</span><span class="sxs-lookup"><span data-stu-id="58e20-117">An instance of <xref:System.Type> or <xref:System.Reflection.Emit.TypeBuilder> that represents `T`.</span></span>

   > [!IMPORTANT]
   > <span data-ttu-id="58e20-118">Należy zwolnić wszystkie obiekty, które reprezentują części złożenia.</span><span class="sxs-lookup"><span data-stu-id="58e20-118">You must release all objects that represent parts of the assembly.</span></span> <span data-ttu-id="58e20-119">`T` Definiuje, <xref:System.Reflection.Emit.ModuleBuilder> że zachowuje odwołanie <xref:System.Reflection.Emit.TypeBuilder>do , <xref:System.Reflection.Emit.AssemblyBuilder> i obiekt zachowuje <xref:System.Reflection.Emit.ModuleBuilder>odwołanie do , więc odwołania do tych obiektów musi zostać zwolniony.</span><span class="sxs-lookup"><span data-stu-id="58e20-119">The <xref:System.Reflection.Emit.ModuleBuilder> that defines `T` keeps a reference to the <xref:System.Reflection.Emit.TypeBuilder>, and the <xref:System.Reflection.Emit.AssemblyBuilder> object keeps a reference to the <xref:System.Reflection.Emit.ModuleBuilder>, so references to these objects must be released.</span></span> <span data-ttu-id="58e20-120">Nawet istnienie <xref:System.Reflection.Emit.LocalBuilder> lub <xref:System.Reflection.Emit.ILGenerator> wykorzystywane w konstrukcji `T` zapobiega rozładunku.</span><span class="sxs-lookup"><span data-stu-id="58e20-120">Even the existence of a <xref:System.Reflection.Emit.LocalBuilder> or an <xref:System.Reflection.Emit.ILGenerator> used in the construction of `T` prevents unloading.</span></span>

- <span data-ttu-id="58e20-121">Odwołanie statyczne `T` przez inny dynamicznie `T1` zdefiniowany typ, który jest nadal osiągalny przez wykonanie kodu.</span><span class="sxs-lookup"><span data-stu-id="58e20-121">A static reference to `T` by another dynamically defined type `T1` that is still reachable by executing code.</span></span> <span data-ttu-id="58e20-122">Na przykład `T1` może pochodzić `T` `T` od lub może być typem `T1`parametru w metodzie .</span><span class="sxs-lookup"><span data-stu-id="58e20-122">For example, `T1` might derive from `T`, or `T` might be the type of a parameter in a method of `T1`.</span></span>

- <span data-ttu-id="58e20-123">A **ByRef** do pola statycznego, `T`które należy do .</span><span class="sxs-lookup"><span data-stu-id="58e20-123">A **ByRef** to a static field that belongs to `T`.</span></span>

- <span data-ttu-id="58e20-124">A <xref:System.RuntimeTypeHandle> <xref:System.RuntimeFieldHandle>, <xref:System.RuntimeMethodHandle> lub który `T` odnosi się do `T`lub do składnika .</span><span class="sxs-lookup"><span data-stu-id="58e20-124">A <xref:System.RuntimeTypeHandle>, <xref:System.RuntimeFieldHandle>, or <xref:System.RuntimeMethodHandle> that refers to `T` or to a component of `T`.</span></span>

- <span data-ttu-id="58e20-125">Wystąpienie dowolnego obiektu odbicia, który może być używany <xref:System.Type> pośrednio `T`lub bezpośrednio w celu uzyskania dostępu do obiektu, który reprezentuje .</span><span class="sxs-lookup"><span data-stu-id="58e20-125">An instance of any reflection object that could be used indirectly or directly to access the <xref:System.Type> object that represents `T`.</span></span> <span data-ttu-id="58e20-126">Na przykład <xref:System.Type> obiekt `T` dla można uzyskać z typu tablicy, którego typ elementu jest `T`, lub od typu ogólnego, który ma `T` jako argument typu.</span><span class="sxs-lookup"><span data-stu-id="58e20-126">For example, the <xref:System.Type> object for `T` can be obtained from an array type whose element type is `T`, or from a generic type that has `T` as a type argument.</span></span>

- <span data-ttu-id="58e20-127">Metoda `M` na stosie wywołań dowolnego `M` wątku, `T` gdzie jest metodą lub metodą na poziomie modułu, która jest zdefiniowana w zestawie.</span><span class="sxs-lookup"><span data-stu-id="58e20-127">A method `M` on the call stack of any thread, where `M` is a method of `T` or a module-level method that is defined in the assembly.</span></span>

- <span data-ttu-id="58e20-128">Delegat do metody statycznej, która jest zdefiniowana w module zestawu.</span><span class="sxs-lookup"><span data-stu-id="58e20-128">A delegate to a static method that is defined in a module of the assembly.</span></span>

<span data-ttu-id="58e20-129">Jeśli istnieje tylko jeden element z tej listy tylko dla jednego typu lub jednej metody w zestawie, środowisko wykonawcze nie może zwolnić zestawu.</span><span class="sxs-lookup"><span data-stu-id="58e20-129">If only one item from this list exists for only one type or one method in the assembly, the runtime cannot unload the assembly.</span></span>

> [!NOTE]
> <span data-ttu-id="58e20-130">Środowisko wykonawcze faktycznie nie zwalnia zestawu, dopóki finalizatory nie uruchomią dla wszystkich elementów na liście.</span><span class="sxs-lookup"><span data-stu-id="58e20-130">The runtime does not actually unload the assembly until finalizers have run for all items in the list.</span></span>

<span data-ttu-id="58e20-131">Na potrzeby śledzenia okresu istnienia, skonstruowany `List<int>` typ ogólny, takich `List(Of Integer)` jak (w języku C#) lub (w języku Visual Basic), który jest tworzony i używany w generowaniu zestawu kolekcjonowania jest uważany za zdefiniowany w zestawie, który zawiera definicję typu ogólnego lub w zestawie, który zawiera definicję jednego z jego argumentów typu.</span><span class="sxs-lookup"><span data-stu-id="58e20-131">For purposes of tracking lifetime, a constructed generic type such as `List<int>` (in C#) or `List(Of Integer)` (in Visual Basic) that is created and used in the generation of a collectible assembly is considered to have been defined either in the assembly that contains the generic type definition or in an assembly that contains the definition of one of its type arguments.</span></span> <span data-ttu-id="58e20-132">Dokładny zestaw, który jest używany jest szczegół implementacji i może ulec zmianie.</span><span class="sxs-lookup"><span data-stu-id="58e20-132">The exact assembly that is used is an implementation detail and subject to change.</span></span>

## <a name="restrictions-on-collectible-assemblies"></a><span data-ttu-id="58e20-133">Ograniczenia dotyczące zespołów kolekcjonerskich</span><span class="sxs-lookup"><span data-stu-id="58e20-133">Restrictions on collectible assemblies</span></span>

<span data-ttu-id="58e20-134">Do zestawów kolekcjonerskich obowiązują następujące ograniczenia:</span><span class="sxs-lookup"><span data-stu-id="58e20-134">The following restrictions apply to collectible assemblies:</span></span>

- <span data-ttu-id="58e20-135">**Odniesienia statyczne** Typy w zwykłym złożeniu dynamicznym nie mogą mieć statycznych odwołań do typów, które są zdefiniowane w kolekcjonuje.</span><span class="sxs-lookup"><span data-stu-id="58e20-135">**Static references** Types in an ordinary dynamic assembly cannot have static references to types that are defined in a collectible assembly.</span></span> <span data-ttu-id="58e20-136">Na przykład jeśli zdefiniujesz zwykły typ, który dziedziczy z <xref:System.NotSupportedException> typu w kolekcjonowania zestawu, wyjątek.</span><span class="sxs-lookup"><span data-stu-id="58e20-136">For example, if you define an ordinary type that inherits from a type in a collectible assembly, a <xref:System.NotSupportedException> exception is thrown.</span></span> <span data-ttu-id="58e20-137">Typ w kolekcjonowania zestawu może mieć statyczne odwołania do typu w innym zestawie kolekcjonerskich, ale to wydłuża okres istnienia zestawu, do którego istnieje odwołanie do okresu istnienia zestawu odwołującego się.</span><span class="sxs-lookup"><span data-stu-id="58e20-137">A type in a collectible assembly can have static references to a type in another collectible assembly, but this extends the lifetime of the referenced assembly to the lifetime of the referencing assembly.</span></span>

- <span data-ttu-id="58e20-138">**Współdziałacze COM** Żadne interfejsy COM nie mogą być zdefiniowane w kolekcjonowania zestawu i żadnych wystąpień typów w kolekcjonowania zestawu mogą być konwertowane na obiekty COM.</span><span class="sxs-lookup"><span data-stu-id="58e20-138">**COM interop** No COM interfaces can be defined within a collectible assembly, and no instances of types within a collectible assembly can be converted into COM objects.</span></span> <span data-ttu-id="58e20-139">Typ w kolekcjonowania zestawu nie może służyć jako com wywoływane otoki (CCW) lub wywoływane otoki (RCW).</span><span class="sxs-lookup"><span data-stu-id="58e20-139">A type in a collectible assembly cannot serve as a COM callable wrapper (CCW) or runtime callable wrapper (RCW).</span></span> <span data-ttu-id="58e20-140">Jednak typy w zestawach kolekcjonerskich można używać obiektów, które implementują interfejsy COM.</span><span class="sxs-lookup"><span data-stu-id="58e20-140">However, types in collectible assemblies can use objects that implement COM interfaces.</span></span>

- <span data-ttu-id="58e20-141">**Wywołanie platformy** Metody, które <xref:System.Runtime.InteropServices.DllImportAttribute> mają atrybut nie będzie kompilować, gdy są one zadeklarowane w kolekcjonowania zestawu.</span><span class="sxs-lookup"><span data-stu-id="58e20-141">**Platform invoke** Methods that have the <xref:System.Runtime.InteropServices.DllImportAttribute> attribute will not compile when they are declared in a collectible assembly.</span></span> <span data-ttu-id="58e20-142">Instrukcja <xref:System.Reflection.Emit.OpCodes.Calli?displayProperty=nameWithType> nie może służyć do implementacji typu w kolekcjonerów zestawu i takich typów nie można zorganizować do kodu niezarządzanego.</span><span class="sxs-lookup"><span data-stu-id="58e20-142">The <xref:System.Reflection.Emit.OpCodes.Calli?displayProperty=nameWithType> instruction cannot be used in the implementation of a type in a collectible assembly, and such types cannot be marshaled to unmanaged code.</span></span> <span data-ttu-id="58e20-143">Jednak można wywołać do kodu macierzystego przy użyciu punktu wejścia, który jest zadeklarowany w zestawie niezbieralne.</span><span class="sxs-lookup"><span data-stu-id="58e20-143">However, you can call into native code by using an entry point that is declared in a non-collectible assembly.</span></span>

- <span data-ttu-id="58e20-144">**Kierowanie** Nie można marshal obiektami (w szczególności delegatów), które są zdefiniowane w zestawach kolekcjonerskich.</span><span class="sxs-lookup"><span data-stu-id="58e20-144">**Marshaling** Objects (in particular, delegates) that are defined in collectible assemblies cannot be marshaled.</span></span> <span data-ttu-id="58e20-145">Jest to ograniczenie dla wszystkich typów emitowanych przejściowych.</span><span class="sxs-lookup"><span data-stu-id="58e20-145">This is a restriction on all transient emitted types.</span></span>

- <span data-ttu-id="58e20-146">**Obciążenie zespołu** Odbicie emitować jest jedynym mechanizmem, który jest obsługiwany do ładowania zestawów kolekcjonerskich.</span><span class="sxs-lookup"><span data-stu-id="58e20-146">**Assembly loading** Reflection emit is the only mechanism that is supported for loading collectible assemblies.</span></span> <span data-ttu-id="58e20-147">Nie można zwolnić zestawów, które są ładowane przy użyciu innych form załadunku złożenia.</span><span class="sxs-lookup"><span data-stu-id="58e20-147">Assemblies that are loaded by using any other form of assembly loading cannot be unloaded.</span></span>

- <span data-ttu-id="58e20-148">**Obiekty powiązane kontekstem** Zmienne statyczne kontekstu nie są obsługiwane.</span><span class="sxs-lookup"><span data-stu-id="58e20-148">**Context-bound objects** Context-static variables are not supported.</span></span> <span data-ttu-id="58e20-149">Typy w kolekcjonuje <xref:System.ContextBoundObject>zestaw nie można rozszerzyć .</span><span class="sxs-lookup"><span data-stu-id="58e20-149">Types in a collectible assembly cannot extend <xref:System.ContextBoundObject>.</span></span> <span data-ttu-id="58e20-150">Jednak kod w zestawach kolekcjonerskich można użyć obiektów związanych z kontekstem, które są zdefiniowane w innym miejscu.</span><span class="sxs-lookup"><span data-stu-id="58e20-150">However, code in collectible assemblies can use context-bound objects that are defined elsewhere.</span></span>

- <span data-ttu-id="58e20-151">**Dane statyczne wątku** Zmienne statyczne wątku nie są obsługiwane.</span><span class="sxs-lookup"><span data-stu-id="58e20-151">**Thread-static data** Thread-static variables are not supported.</span></span>

## <a name="see-also"></a><span data-ttu-id="58e20-152">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="58e20-152">See also</span></span>

- [<span data-ttu-id="58e20-153">Emitowanie dynamicznych metod i zestawów</span><span class="sxs-lookup"><span data-stu-id="58e20-153">Emitting Dynamic Methods and Assemblies</span></span>](emitting-dynamic-methods-and-assemblies.md)
