---
title: Ograniczenia dotyczące parametrów typu — Przewodnik programowania w języku C#
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 4c4554c808ab15776f3217c257e0a60119ea2338
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/04/2020
ms.locfileid: "84368364"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="dd6b0-102">Ograniczenia dotyczące parametrów typu (Przewodnik programowania w języku C#)</span><span class="sxs-lookup"><span data-stu-id="dd6b0-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="dd6b0-103">Ograniczenia informują kompilator o możliwościach, które argument typu musi zawierać.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="dd6b0-104">Bez żadnych ograniczeń argument typu może być dowolnym typem.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="dd6b0-105">Kompilator może przyjąć tylko elementy członkowskie <xref:System.Object?displayProperty=nameWithType> , które jest ostateczną klasą bazową dla dowolnego typu .NET.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="dd6b0-106">Aby uzyskać więcej informacji, zobacz [Dlaczego należy używać ograniczeń](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="dd6b0-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="dd6b0-107">Jeśli kod klienta używa typu, który nie spełnia warunków ograniczenia, kompilator wygeneruje błąd.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-107">If client code uses a type that doesn't satisfy a constraint, the compiler issues an error.</span></span> <span data-ttu-id="dd6b0-108">Ograniczenia są określone za pomocą `where` kontekstowego słowa kluczowego.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="dd6b0-109">W poniższej tabeli wymieniono siedem typów ograniczeń:</span><span class="sxs-lookup"><span data-stu-id="dd6b0-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="dd6b0-110">Typu</span><span class="sxs-lookup"><span data-stu-id="dd6b0-110">Constraint</span></span>|<span data-ttu-id="dd6b0-111">Opis</span><span class="sxs-lookup"><span data-stu-id="dd6b0-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="dd6b0-112">Argument typu musi być typem wartości niedopuszczający wartości null.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-112">The type argument must be a non-nullable value type.</span></span> <span data-ttu-id="dd6b0-113">Aby uzyskać informacje o typach wartości null, zobacz [dopuszczanie typów wartości null](../../language-reference/builtin-types/nullable-value-types.md).</span><span class="sxs-lookup"><span data-stu-id="dd6b0-113">For information about nullable value types, see [Nullable value types](../../language-reference/builtin-types/nullable-value-types.md).</span></span> <span data-ttu-id="dd6b0-114">Ponieważ wszystkie typy wartości mają dostępny Konstruktor bez parametrów, `struct` ograniczenie implikuje `new()` ograniczenie i nie można go połączyć z `new()` ograniczeniem.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-114">Because all value types have an accessible parameterless constructor, the `struct` constraint implies the `new()` constraint and can't be combined with the `new()` constraint.</span></span> <span data-ttu-id="dd6b0-115">Nie można połączyć `struct` ograniczenia z `unmanaged` ograniczeniem.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-115">You can't combine the `struct` constraint with the `unmanaged` constraint.</span></span>|
|`where T : class`|<span data-ttu-id="dd6b0-116">Argument typu musi być typem referencyjnym.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-116">The type argument must be a reference type.</span></span> <span data-ttu-id="dd6b0-117">To ograniczenie dotyczy również dowolnego typu klasy, interfejsu, delegata lub tablicy.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-117">This constraint applies also to any class, interface, delegate, or array type.</span></span> <span data-ttu-id="dd6b0-118">W kontekście dopuszczającym wartość null w języku C# 8,0 lub nowszym, `T` musi to być typ referencyjny, który nie dopuszcza wartości null.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-118">In a nullable context in C# 8.0 or later, `T` must be a non-nullable reference type.</span></span> |
|`where T : class?`|<span data-ttu-id="dd6b0-119">Argument typu musi być typem referencyjnym, DOPUSZCZANYM jako Nullable lub nie dopuszczający wartości null.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-119">The type argument must be a reference type, either nullable or non-nullable.</span></span> <span data-ttu-id="dd6b0-120">To ograniczenie dotyczy również dowolnego typu klasy, interfejsu, delegata lub tablicy.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-120">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="dd6b0-121">Argument typu musi być typem niedopuszczający wartości null.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-121">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="dd6b0-122">Argument może być typem referencyjnym niedopuszczający wartości null w języku C# 8,0 lub nowszym albo typem wartości innym niż null.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-122">The argument can be a non-nullable reference type in C# 8.0 or later, or a non-nullable value type.</span></span> |
|`where T : unmanaged`|<span data-ttu-id="dd6b0-123">Argument typu musi być [typem niezarządzanym](../../language-reference/builtin-types/unmanaged-types.md)niedopuszczający wartości null.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-123">The type argument must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="dd6b0-124">`unmanaged`Ograniczenie implikuje `struct` ograniczenie i nie można go połączyć z `struct` `new()` ograniczeniami lub.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-124">The `unmanaged` constraint implies the `struct` constraint and can't be combined with either the `struct` or `new()` constraints.</span></span>|
|`where T : new()`|<span data-ttu-id="dd6b0-125">Argument typu musi mieć publiczny Konstruktor bez parametrów.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-125">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="dd6b0-126">W przypadku użycia razem z innymi ograniczeniami, `new()` ograniczenie musi być określone jako ostatnie.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-126">When used together with other constraints, the `new()` constraint must be specified last.</span></span> <span data-ttu-id="dd6b0-127">`new()`Nie można połączyć ograniczenia z `struct` `unmanaged` ograniczeniami i.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-127">The `new()` constraint can't be combined with the `struct` and `unmanaged` constraints.</span></span>|
|<span data-ttu-id="dd6b0-128">`where T :` *\<base class name>*</span><span class="sxs-lookup"><span data-stu-id="dd6b0-128">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="dd6b0-129">Argument typu musi być lub pochodzić od określonej klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-129">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="dd6b0-130">W kontekście dopuszczającym wartość null w języku C# 8,0 i nowszych `T` musi to być typ referencyjny niedopuszczający wartości null pochodzący od określonej klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-130">In a nullable context in C# 8.0 and later, `T` must be a non-nullable reference type derived from the specified base class.</span></span> |
|<span data-ttu-id="dd6b0-131">`where T :` *\<base class name>?*</span><span class="sxs-lookup"><span data-stu-id="dd6b0-131">`where T :` *\<base class name>?*</span></span>|<span data-ttu-id="dd6b0-132">Argument typu musi być lub pochodzić od określonej klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-132">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="dd6b0-133">W kontekście dopuszczającym wartość null w języku C# 8,0 i nowszych `T` może to być typ dopuszczający wartości null lub niedopuszczający wartości null pochodzący z określonej klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-133">In a nullable context in C# 8.0 and later, `T` may be either a nullable or non-nullable type derived from the specified base class.</span></span> |
|<span data-ttu-id="dd6b0-134">`where T :` *\<interface name>*</span><span class="sxs-lookup"><span data-stu-id="dd6b0-134">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="dd6b0-135">Argument typu musi być lub zaimplementować określony interfejs.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-135">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="dd6b0-136">Można określić wiele ograniczeń interfejsu.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-136">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="dd6b0-137">Interfejs ograniczający może być również ogólny.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-137">The constraining interface can also be generic.</span></span> <span data-ttu-id="dd6b0-138">W kontekście dopuszczającym wartość null w języku C# 8,0 i nowszych `T` musi być typem niedopuszczającym wartości null, który implementuje określony interfejs.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-138">In a nullable context in C# 8.0 and later, `T` must be a non-nullable type that implements the specified interface.</span></span>|
|<span data-ttu-id="dd6b0-139">`where T :` *\<interface name>?*</span><span class="sxs-lookup"><span data-stu-id="dd6b0-139">`where T :` *\<interface name>?*</span></span>|<span data-ttu-id="dd6b0-140">Argument typu musi być lub zaimplementować określony interfejs.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-140">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="dd6b0-141">Można określić wiele ograniczeń interfejsu.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-141">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="dd6b0-142">Interfejs ograniczający może być również ogólny.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-142">The constraining interface can also be generic.</span></span> <span data-ttu-id="dd6b0-143">W kontekście dopuszczającym wartość null w języku C# 8,0 `T` może to być typ referencyjny dopuszczający wartość null, typ referencyjny niedopuszczający wartości null lub typ wartości.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-143">In a nullable context in C# 8.0, `T` may be a nullable reference type, a non-nullable reference type, or a value type.</span></span> <span data-ttu-id="dd6b0-144">`T`nie może być typem wartości null.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-144">`T` may not be a nullable value type.</span></span>|
|`where T : U`|<span data-ttu-id="dd6b0-145">Argument typu dostarczony dla elementu `T` musi być lub dziedziczyć z argumentu dostarczonego dla `U` .</span><span class="sxs-lookup"><span data-stu-id="dd6b0-145">The type argument supplied for `T` must be or derive from the argument supplied for `U`.</span></span> <span data-ttu-id="dd6b0-146">W kontekście dopuszczającym wartość null, jeśli `U` jest typem referencyjnym niedopuszczający wartości null, `T` musi być typem referencyjnym, który nie dopuszcza wartości null.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-146">In a nullable context, if `U` is a non-nullable reference type, `T` must be non-nullable reference type.</span></span> <span data-ttu-id="dd6b0-147">Jeśli `U` jest typem referencyjnym dopuszczającym wartość null, `T` może mieć wartość null lub nie dopuszczający wartości null.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-147">If `U` is a nullable reference type, `T` may be either nullable or non-nullable.</span></span> |

## <a name="why-use-constraints"></a><span data-ttu-id="dd6b0-148">Dlaczego warto używać ograniczeń</span><span class="sxs-lookup"><span data-stu-id="dd6b0-148">Why use constraints</span></span>

<span data-ttu-id="dd6b0-149">Ograniczenia określają możliwości i oczekiwania parametru typu.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-149">Constraints specify the capabilities and expectations of a type parameter.</span></span> <span data-ttu-id="dd6b0-150">Deklarowanie tych ograniczeń oznacza, że można użyć operacji i wywołań metody typu ograniczenia.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-150">Declaring those constraints means you can use the operations and method calls of the constraining type.</span></span> <span data-ttu-id="dd6b0-151">Jeśli Klasa ogólna lub metoda korzysta z dowolnej operacji na elementach ogólnych poza prostym przypisaniem lub wywoływanie wszelkich metod nieobsługiwanych przez <xref:System.Object?displayProperty=nameWithType> , należy zastosować ograniczenia do parametru typu.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-151">If your generic class or method uses any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="dd6b0-152">Na przykład ograniczenie klasy bazowej instruuje kompilator, że tylko obiekty tego typu lub pochodne z tego typu będą używane jako argumenty typu.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-152">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="dd6b0-153">Gdy kompilator ma tę gwarancję, może zezwolić na wywoływanie metod tego typu w klasie generycznej.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-153">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="dd6b0-154">Poniższy przykład kodu demonstruje funkcjonalność, którą można dodać do `GenericList<T>` klasy (w temacie [wprowadzenie do typów ogólnych](../../../standard/generics/index.md)) przez zastosowanie ograniczenia klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-154">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](../../../standard/generics/index.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#9)]

<span data-ttu-id="dd6b0-155">Ograniczenie pozwala klasie generycznej używać `Employee.Name` właściwości.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-155">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="dd6b0-156">Ograniczenie określa, że wszystkie elementy typu `T` mają gwarantowany `Employee` obiekt lub obiekt, który dziedziczy z `Employee` .</span><span class="sxs-lookup"><span data-stu-id="dd6b0-156">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="dd6b0-157">Do tego samego parametru typu można zastosować wiele ograniczeń, a same ograniczenia mogą być typami ogólnymi w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="dd6b0-157">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#10)]

<span data-ttu-id="dd6b0-158">Podczas stosowania `where T : class` ograniczenia, należy unikać `==` operatorów i `!=` w parametrze typu, ponieważ te operatory przetestuje tylko tożsamość referencyjną, a nie dla równości wartości.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-158">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="dd6b0-159">To zachowanie występuje nawet wtedy, gdy te operatory są przeciążone w typie, który jest używany jako argument.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-159">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="dd6b0-160">Poniższy kod ilustruje ten punkt; wynik ma wartość false, mimo że <xref:System.String> Klasa przeciążuje `==` operator.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-160">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#11)]

<span data-ttu-id="dd6b0-161">Kompilator wie, że `T` jest typem referencyjnym w czasie kompilacji i musi używać domyślnych operatorów, które są prawidłowe dla wszystkich typów referencyjnych.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-161">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="dd6b0-162">Jeśli konieczne jest przetestowanie pod kątem równości wartości, zalecanym sposobem jest również `where T : IEquatable<T>` zastosowanie `where T : IComparable<T>` ograniczenia or i zaimplementowanie interfejsu w dowolnej klasie, która będzie używana do konstruowania klasy generycznej.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-162">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="dd6b0-163">Ograniczanie wielu parametrów</span><span class="sxs-lookup"><span data-stu-id="dd6b0-163">Constraining multiple parameters</span></span>

<span data-ttu-id="dd6b0-164">Można zastosować ograniczenia do wielu parametrów i wiele ograniczeń do jednego parametru, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="dd6b0-164">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="dd6b0-165">Niepowiązane parametry typu</span><span class="sxs-lookup"><span data-stu-id="dd6b0-165">Unbounded type parameters</span></span>

 <span data-ttu-id="dd6b0-166">Parametry typu, które nie mają żadnych ograniczeń, takich jak T w klasie publicznej `SampleClass<T>{}` , są nazywane niezwiązanymi parametrami typu.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-166">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="dd6b0-167">Parametry typu niepowiązanego mają następujące reguły:</span><span class="sxs-lookup"><span data-stu-id="dd6b0-167">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="dd6b0-168">`!=`Operatory i `==` nie mogą być używane, ponieważ nie ma gwarancji, że konkretny argument typu będzie obsługiwał te operatory.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-168">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="dd6b0-169">Mogą być konwertowane do i z `System.Object` lub jawnie konwertowane na dowolny typ interfejsu.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-169">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="dd6b0-170">Można je porównać z [wartością null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="dd6b0-170">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="dd6b0-171">Jeśli parametr niezwiązany jest porównywany z `null` , porównywanie zwróci wartość false, jeśli argument typu jest typem wartości.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-171">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="dd6b0-172">Parametry typu jako ograniczenia</span><span class="sxs-lookup"><span data-stu-id="dd6b0-172">Type parameters as constraints</span></span>

<span data-ttu-id="dd6b0-173">Użycie parametru typu ogólnego jako ograniczenia jest przydatne, gdy funkcja członkowska z własnym parametrem typu musi ograniczyć ten parametr do parametru typu zawierającego typ, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="dd6b0-173">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#13)]

<span data-ttu-id="dd6b0-174">W poprzednim przykładzie, `T` jest ograniczeniem typu w kontekście `Add` metody i niezwiązanym parametrem typu w kontekście `List` klasy.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-174">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="dd6b0-175">Parametry typu mogą być również używane jako ograniczenia w definicjach klasy generycznej.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-175">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="dd6b0-176">Parametr type musi być zadeklarowany w nawiasach kątowych wraz z innymi parametrami typu:</span><span class="sxs-lookup"><span data-stu-id="dd6b0-176">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#14)]

<span data-ttu-id="dd6b0-177">Użyteczność parametrów typu jako ograniczenia z klasami generycznymi jest ograniczona, ponieważ kompilator może nie zajmować niczego informacji o parametrze typu, z wyjątkiem tego, że pochodzi od `System.Object` .</span><span class="sxs-lookup"><span data-stu-id="dd6b0-177">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="dd6b0-178">Użyj parametrów typu jako ograniczeń dotyczących klas ogólnych w scenariuszach, w których chcesz wymusić relację dziedziczenia między dwoma parametrami typu.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-178">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="dd6b0-179">Ograniczenie NotNull</span><span class="sxs-lookup"><span data-stu-id="dd6b0-179">NotNull constraint</span></span>

<span data-ttu-id="dd6b0-180">Począwszy od języka C# 8,0 w kontekście dopuszczającym wartość null, można użyć `notnull` ograniczenia, aby określić, że argument typu musi być typem wartości niedopuszczających wartości null lub typem referencyjnym, który nie dopuszcza wartości null.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-180">Beginning with C# 8.0 in a nullable context, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="dd6b0-181">`notnull`Ograniczenie może być używane tylko w `nullable enable` kontekście.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-181">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="dd6b0-182">Kompilator generuje ostrzeżenie w przypadku dodania `notnull` ograniczenia w kontekście Oblivious dopuszczający wartość null.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-182">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span>

<span data-ttu-id="dd6b0-183">W przeciwieństwie do innych ograniczeń, gdy argument typu narusza `notnull` ograniczenie, kompilator generuje ostrzeżenie, gdy ten kod jest kompilowany w `nullable enable` kontekście.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-183">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="dd6b0-184">Jeśli kod jest kompilowany w kontekście Oblivious dopuszczający wartość null, kompilator nie generuje żadnych ostrzeżeń ani błędów.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-184">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

<span data-ttu-id="dd6b0-185">Począwszy od języka C# 8,0 w kontekście dopuszczającym wartość null, `class` ograniczenie określa, że argument typu musi być typem referencyjnym, który nie dopuszcza wartości null.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-185">Beginning with C# 8.0 in a nullable context, the `class` constraint specifies that the type argument must be a non-nullable reference type.</span></span> <span data-ttu-id="dd6b0-186">W kontekście dopuszczającym wartość null, gdy parametr typu jest typem referencyjnym dopuszczającym wartość null, kompilator generuje ostrzeżenie.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-186">In a nullable context, when a type parameter is a nullable reference type, the compiler generates a warning.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="dd6b0-187">Niezarządzany warunek ograniczający</span><span class="sxs-lookup"><span data-stu-id="dd6b0-187">Unmanaged constraint</span></span>

<span data-ttu-id="dd6b0-188">Począwszy od języka C# 7,3, można użyć `unmanaged` ograniczenia, aby określić, że parametr typu musi być [typem niezarządzanym](../../language-reference/builtin-types/unmanaged-types.md)niedopuszczający wartości null.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-188">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="dd6b0-189">`unmanaged`Ograniczenie umożliwia zapisanie procedur wielokrotnego użytku w celu pracy z typami, które mogą być manipulowane jako bloki pamięci, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="dd6b0-189">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](snippets/GenericWhereConstraints.cs#15)]

<span data-ttu-id="dd6b0-190">Poprzednia metoda musi być skompilowana w `unsafe` kontekście, ponieważ używa `sizeof` operatora na typie, który nie jest znany jako typ wbudowany.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-190">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="dd6b0-191">Bez `unmanaged` ograniczenia `sizeof` operator jest niedostępny.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-191">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

<span data-ttu-id="dd6b0-192">`unmanaged`Ograniczenie implikuje `struct` ograniczenie i nie może zostać połączone z nim.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-192">The `unmanaged` constraint implies the `struct` constraint and can't be combined with it.</span></span> <span data-ttu-id="dd6b0-193">Ponieważ `struct` ograniczenie implikuje `new()` ograniczenie, `unmanaged` ograniczenie nie może być również połączone z `new()` ograniczeniem.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-193">Because the `struct` constraint implies the `new()` constraint, the `unmanaged` constraint can't be combined with the `new()` constraint as well.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="dd6b0-194">Delegowanie ograniczeń</span><span class="sxs-lookup"><span data-stu-id="dd6b0-194">Delegate constraints</span></span>

<span data-ttu-id="dd6b0-195">Począwszy od języka C# 7,3, można użyć <xref:System.Delegate?displayProperty=nameWithType> lub <xref:System.MulticastDelegate?displayProperty=nameWithType> jako ograniczenie klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-195">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="dd6b0-196">Środowisko CLR zawsze zezwala na to ograniczenie, ale język C# niedozwolony.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-196">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="dd6b0-197">`System.Delegate`Ograniczenie pozwala napisać kod, który współpracuje z delegatami w sposób bezpieczny dla typu.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-197">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="dd6b0-198">Poniższy kod definiuje metodę rozszerzenia, która łączy dwa Delegaty pod warunkiem, że są tego samego typu:</span><span class="sxs-lookup"><span data-stu-id="dd6b0-198">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](snippets/GenericWhereConstraints.cs#16)]

<span data-ttu-id="dd6b0-199">Możesz użyć powyższej metody do łączenia delegatów, które są tego samego typu:</span><span class="sxs-lookup"><span data-stu-id="dd6b0-199">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](snippets/GenericWhereConstraints.cs#17)]

<span data-ttu-id="dd6b0-200">Usunięcie komentarza do ostatniego wiersza nie spowoduje skompilowania.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-200">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="dd6b0-201">Oba typy delegatów `first` `test` , ale są różnymi typami delegatów.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-201">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="dd6b0-202">Ograniczenia wyliczeniowe</span><span class="sxs-lookup"><span data-stu-id="dd6b0-202">Enum constraints</span></span>

<span data-ttu-id="dd6b0-203">Począwszy od języka C# 7,3, można również określić <xref:System.Enum?displayProperty=nameWithType> Typ jako ograniczenie klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-203">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="dd6b0-204">Środowisko CLR zawsze zezwala na to ograniczenie, ale język C# niedozwolony.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-204">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="dd6b0-205">Typy ogólne przy użyciu `System.Enum` zapewniają programowanie bezpiecznego typu w celu buforowania wyników z używania metod statycznych w `System.Enum` .</span><span class="sxs-lookup"><span data-stu-id="dd6b0-205">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="dd6b0-206">Poniższy przykład odnajduje wszystkie prawidłowe wartości dla typu wyliczeniowego, a następnie tworzy słownik, który mapuje te wartości na jego reprezentację w postaci ciągu.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-206">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the enum constraint](snippets/GenericWhereConstraints.cs#18)]

<span data-ttu-id="dd6b0-207">`Enum.GetValues`i `Enum.GetName` Użyj odbicia, które ma wpływ na wydajność.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-207">`Enum.GetValues` and `Enum.GetName` use reflection, which has performance implications.</span></span> <span data-ttu-id="dd6b0-208">Można wywołać, `EnumNamedValues` Aby utworzyć kolekcję, która jest buforowana i ponownie używana zamiast powtarzania wywołań, które wymagają odbicia.</span><span class="sxs-lookup"><span data-stu-id="dd6b0-208">You can call `EnumNamedValues` to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="dd6b0-209">Można go użyć, jak pokazano w poniższym przykładzie, aby utworzyć Wyliczenie i skompilować słownik jego wartości i nazw:</span><span class="sxs-lookup"><span data-stu-id="dd6b0-209">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[enum definition](snippets/GenericWhereConstraints.cs#19)]

[!code-csharp[using the enum constrained method](snippets/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="dd6b0-210">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="dd6b0-210">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="dd6b0-211">Przewodnik programowania w języku C#</span><span class="sxs-lookup"><span data-stu-id="dd6b0-211">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="dd6b0-212">Wprowadzenie do typów ogólnych</span><span class="sxs-lookup"><span data-stu-id="dd6b0-212">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="dd6b0-213">Klasy ogólne</span><span class="sxs-lookup"><span data-stu-id="dd6b0-213">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="dd6b0-214">nowe ograniczenie</span><span class="sxs-lookup"><span data-stu-id="dd6b0-214">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
