---
title: Wyprowadzanie z elementu WebRequest
ms.date: 03/30/2017
helpviewer_keywords:
- WebRequest class, pluggable protocols
- protocol-specific request handler
- sending data, pluggable protocols
- pluggable protocols, class criteria
- Internet, pluggable protocols
- receiving data, pluggable protocols
- protocols, pluggable
ms.assetid: 9810c177-973e-43d7-823c-14960bd625ea
ms.openlocfilehash: 6bee864f8d24076d16f226c29d61801e856739d9
ms.sourcegitcommit: 289e06e904b72f34ac717dbcc5074239b977e707
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/17/2019
ms.locfileid: "71048599"
---
# <a name="deriving-from-webrequest"></a><span data-ttu-id="39819-102">Wyprowadzanie z elementu WebRequest</span><span class="sxs-lookup"><span data-stu-id="39819-102">Deriving from WebRequest</span></span>
<span data-ttu-id="39819-103"><xref:System.Net.WebRequest> Klasa jest abstrakcyjną klasą bazową, która udostępnia podstawowe metody i właściwości służące do tworzenia obsługi żądań specyficznych dla protokołu, które pasują do .NET Framework modelu protokołów podłączanych.</span><span class="sxs-lookup"><span data-stu-id="39819-103">The <xref:System.Net.WebRequest> class is an abstract base class that provides the basic methods and properties for creating a protocol-specific request handler that fits the .NET Framework pluggable protocol model.</span></span> <span data-ttu-id="39819-104">Aplikacje korzystające z klasy **WebRequest** mogą żądać danych przy użyciu dowolnego obsługiwanego protokołu bez konieczności określania używanego protokołu.</span><span class="sxs-lookup"><span data-stu-id="39819-104">Applications that use the **WebRequest** class can request data using any supported protocol without needing to specify the protocol used.</span></span>  
  
 <span data-ttu-id="39819-105">Aby klasa specyficzna dla protokołu mogła być używana jako podłączany protokół, muszą być spełnione dwa kryteria: Klasa musi implementować <xref:System.Net.IWebRequestCreate> interfejs i musi być zarejestrowana <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> przy użyciu metody.</span><span class="sxs-lookup"><span data-stu-id="39819-105">Two criteria must be met in order for a protocol-specific class to be used as a pluggable protocol: The class must implement the <xref:System.Net.IWebRequestCreate> interface, and it must register with the <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="39819-106">Klasa musi zastąpić wszystkie metody abstrakcyjne i właściwości elementu **WebRequest** w celu zapewnienia podłączanego interfejsu.</span><span class="sxs-lookup"><span data-stu-id="39819-106">The class must override all the abstract methods and properties of **WebRequest** to provide the pluggable interface.</span></span>  
  
 <span data-ttu-id="39819-107">Wystąpienia **WebRequest** są przeznaczone do jednorazowego użytku; Jeśli chcesz wykonać kolejne żądanie, Utwórz nowe **żądanie**sieci Web.</span><span class="sxs-lookup"><span data-stu-id="39819-107">**WebRequest** instances are intended for one-time use; if you want to make another request, create a new **WebRequest**.</span></span> <span data-ttu-id="39819-108">Usługa **WebRequest** obsługuje <xref:System.Runtime.Serialization.ISerializable> interfejs, aby umożliwić deweloperom serializację serializacji **żądania WebRequest** , a następnie odtworzyć szablon pod kątem dodatkowych żądań.</span><span class="sxs-lookup"><span data-stu-id="39819-108">**WebRequest** supports the <xref:System.Runtime.Serialization.ISerializable> interface to enable developers to serialize a template **WebRequest** and then reconstruct the template for additional requests.</span></span>  
  
## <a name="iwebrequest-create-method"></a><span data-ttu-id="39819-109">IWebRequest — metoda tworzenia</span><span class="sxs-lookup"><span data-stu-id="39819-109">IWebRequest Create Method</span></span>  
 <span data-ttu-id="39819-110"><xref:System.Net.IWebRequestCreate.Create%2A> Metoda jest odpowiedzialna za Inicjowanie nowego wystąpienia klasy specyficznej dla protokołu.</span><span class="sxs-lookup"><span data-stu-id="39819-110">The <xref:System.Net.IWebRequestCreate.Create%2A> method is responsible for initializing a new instance of the protocol-specific class.</span></span> <span data-ttu-id="39819-111">Po utworzeniu <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> nowego **żądania WebRequest** Metoda dopasowuje żądany identyfikator URI przy użyciu prefiksów URI zarejestrowanych przy użyciu metody **RegisterPrefix** .</span><span class="sxs-lookup"><span data-stu-id="39819-111">When a new **WebRequest** is created, the <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> method matches the requested URI with the URI prefixes registered with the **RegisterPrefix** method.</span></span> <span data-ttu-id="39819-112">Metoda **Create** odpowiedniego elementu zależnego od protokołu musi zwracać zainicjowane wystąpienie elementu podrzędnego, które może przetworzyć standardową transakcję żądania/odpowiedzi dla protokołu bez modyfikowania żadnych pól specyficznych dla protokołu .</span><span class="sxs-lookup"><span data-stu-id="39819-112">The **Create** method of the proper protocol-specific descendant must return an initialized instance of the descendant capable of performing a standard request/response transaction for the protocol without needing any protocol-specific fields modified.</span></span>  
  
## <a name="connectiongroupname-property"></a><span data-ttu-id="39819-113">ConnectionGroupName Property</span><span class="sxs-lookup"><span data-stu-id="39819-113">ConnectionGroupName Property</span></span>  
 <span data-ttu-id="39819-114"><xref:System.Net.WebRequest.ConnectionGroupName%2A> Właściwość służy do nadania nazwy grupie połączeń z zasobem, dzięki czemu można wykonać wiele żądań za pośrednictwem jednego połączenia.</span><span class="sxs-lookup"><span data-stu-id="39819-114">The <xref:System.Net.WebRequest.ConnectionGroupName%2A> property is used to name a group of connections to a resource so that multiple requests can be made over a single connection.</span></span> <span data-ttu-id="39819-115">Aby zaimplementować udostępnianie połączenia, należy zastosować specyficzną dla protokołu metodę buforowania i przypisywania połączeń.</span><span class="sxs-lookup"><span data-stu-id="39819-115">To implement connection-sharing, you must use a protocol-specific method of pooling and assigning connections.</span></span> <span data-ttu-id="39819-116">Na przykład podaną <xref:System.Net.ServicePointManager> klasę implementuje Udostępnianie połączenia <xref:System.Net.HttpWebRequest> dla klasy.</span><span class="sxs-lookup"><span data-stu-id="39819-116">For example, the provided <xref:System.Net.ServicePointManager> class implements connection sharing for the <xref:System.Net.HttpWebRequest> class.</span></span> <span data-ttu-id="39819-117">Klasa **ServicePointManager** tworzy <xref:System.Net.ServicePoint> , która zapewnia połączenie z określonym serwerem dla każdej grupy połączeń.</span><span class="sxs-lookup"><span data-stu-id="39819-117">The **ServicePointManager** class creates a <xref:System.Net.ServicePoint> that provides a connection to a specific server for each connection group.</span></span>  
  
## <a name="contentlength-property"></a><span data-ttu-id="39819-118">Właściwość ContentLength</span><span class="sxs-lookup"><span data-stu-id="39819-118">ContentLength Property</span></span>  
 <span data-ttu-id="39819-119"><xref:System.Net.WebRequest.ContentLength%2A> Właściwość określa liczbę bajtów danych, które zostaną wysłane do serwera podczas przekazywania danych.</span><span class="sxs-lookup"><span data-stu-id="39819-119">The <xref:System.Net.WebRequest.ContentLength%2A> property specifies the number of bytes of data that will be sent to the server when uploading data.</span></span>  
  
 <span data-ttu-id="39819-120">Zazwyczaj właściwość musi być ustawiona, aby wskazać, że odbywa się przekazywanie, gdy właściwość ContentLength jest ustawiona na wartość większą od zera. <xref:System.Net.WebRequest.Method%2A></span><span class="sxs-lookup"><span data-stu-id="39819-120">Typically the <xref:System.Net.WebRequest.Method%2A> property must be set to indicate that an upload is taking place when the **ContentLength** property is set to a value greater than zero.</span></span>  
  
## <a name="contenttype-property"></a><span data-ttu-id="39819-121">ContentType — Właściwość</span><span class="sxs-lookup"><span data-stu-id="39819-121">ContentType Property</span></span>  
 <span data-ttu-id="39819-122"><xref:System.Net.WebRequest.ContentType%2A> Właściwość zawiera wszelkie specjalne informacje, które są wymagane do wysłania przez protokół do serwera w celu zidentyfikowania typu wysyłanej zawartości.</span><span class="sxs-lookup"><span data-stu-id="39819-122">The <xref:System.Net.WebRequest.ContentType%2A> property provides any special information that your protocol requires you to send to the server to identify the type of content that you are sending.</span></span> <span data-ttu-id="39819-123">Zwykle jest to typ zawartości MIME dla przekazanych danych.</span><span class="sxs-lookup"><span data-stu-id="39819-123">Typically this is the MIME content type of any data uploaded.</span></span>  
  
## <a name="credentials-property"></a><span data-ttu-id="39819-124">Właściwość poświadczeń</span><span class="sxs-lookup"><span data-stu-id="39819-124">Credentials Property</span></span>  
 <span data-ttu-id="39819-125"><xref:System.Net.WebRequest.Credentials%2A> Właściwość zawiera informacje, które są konieczne do uwierzytelnienia żądania na serwerze.</span><span class="sxs-lookup"><span data-stu-id="39819-125">The <xref:System.Net.WebRequest.Credentials%2A> property contains information needed to authenticate the request with the server.</span></span> <span data-ttu-id="39819-126">Należy zaimplementować szczegóły procesu uwierzytelniania dla używanego protokołu.</span><span class="sxs-lookup"><span data-stu-id="39819-126">You must implement the details of the authentication process for your protocol.</span></span> <span data-ttu-id="39819-127"><xref:System.Net.AuthenticationManager> Klasa jest odpowiedzialna za uwierzytelnianie żądań i dostarczanie tokenu uwierzytelniania.</span><span class="sxs-lookup"><span data-stu-id="39819-127">The <xref:System.Net.AuthenticationManager> class is responsible for authenticating requests and providing an authentication token.</span></span> <span data-ttu-id="39819-128">Klasa, która dostarcza poświadczenia używane przez protokół, musi implementować <xref:System.Net.ICredentials> interfejs.</span><span class="sxs-lookup"><span data-stu-id="39819-128">The class that provides the credentials used by your protocol must implement the <xref:System.Net.ICredentials> interface.</span></span>  
  
## <a name="headers-property"></a><span data-ttu-id="39819-129">Heads — Właściwość</span><span class="sxs-lookup"><span data-stu-id="39819-129">Headers Property</span></span>  
 <span data-ttu-id="39819-130"><xref:System.Net.WebRequest.Headers%2A> Właściwość zawiera arbitralną kolekcję par nazwa/wartość metadanych skojarzonych z żądaniem.</span><span class="sxs-lookup"><span data-stu-id="39819-130">The <xref:System.Net.WebRequest.Headers%2A> property contains an arbitrary collection of name/value pairs of metadata associated with the request.</span></span> <span data-ttu-id="39819-131">Wszystkie metadane potrzebne przez protokół, które mogą być wyrażone jako para nazwa/wartość, mogą być zawarte we właściwości **Heads** .</span><span class="sxs-lookup"><span data-stu-id="39819-131">Any metadata needed by the protocol that can be expressed as a name/value pair can be included in the **Headers** property.</span></span> <span data-ttu-id="39819-132">Zazwyczaj te informacje muszą być ustawione przed wywołaniem <xref:System.Net.WebRequest.GetRequestStream%2A> lub <xref:System.Net.WebRequest.GetResponse%2A> metodami; po wykonaniu żądania metadane są uznawane za tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="39819-132">Typically this information must be set before calling the <xref:System.Net.WebRequest.GetRequestStream%2A> or <xref:System.Net.WebRequest.GetResponse%2A> methods; once the request has been made, the metadata is considered read-only.</span></span>  
  
 <span data-ttu-id="39819-133">Nie jest wymagane używanie właściwości **Headers** do używania metadanych nagłówka.</span><span class="sxs-lookup"><span data-stu-id="39819-133">You are not required to use the **Headers** property to use header metadata.</span></span> <span data-ttu-id="39819-134">Metadane specyficzne dla protokołu mogą być uwidocznione jako właściwości; na przykład <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> Właściwość uwidacznia nagłówek HTTP **User-Agent** .</span><span class="sxs-lookup"><span data-stu-id="39819-134">Protocol-specific metadata can be exposed as properties; for example, the <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> property exposes the **User-Agent** HTTP header.</span></span> <span data-ttu-id="39819-135">Po udostępnieniu metadanych nagłówka jako właściwości nie należy zezwalać na ustawienie tej samej właściwości przy użyciu właściwości **Heads** .</span><span class="sxs-lookup"><span data-stu-id="39819-135">When you expose header metadata as a property, you should not allow the same property to be set using the **Headers** property.</span></span>  
  
## <a name="method-property"></a><span data-ttu-id="39819-136">Właściwość metody</span><span class="sxs-lookup"><span data-stu-id="39819-136">Method Property</span></span>  
 <span data-ttu-id="39819-137"><xref:System.Net.WebRequest.Method%2A> Właściwość zawiera zlecenie lub akcję, którą żądanie żąda serwera do wykonania.</span><span class="sxs-lookup"><span data-stu-id="39819-137">The <xref:System.Net.WebRequest.Method%2A> property contains the verb or action that the request is asking the server to perform.</span></span> <span data-ttu-id="39819-138">Wartość domyślna właściwości **Metoda** musi umożliwiać standardowe działanie żądania/odpowiedzi bez konieczności ustawiania właściwości specyficznych dla protokołu.</span><span class="sxs-lookup"><span data-stu-id="39819-138">The default for the **Method** property must enable a standard request/response action without requiring any protocol-specific properties to be set.</span></span> <span data-ttu-id="39819-139">Na przykład <xref:System.Net.HttpWebResponse.Method%2A> Metoda przyjmuje wartość domyślną Get, która żąda zasobu z serwera sieci Web i zwraca odpowiedź.</span><span class="sxs-lookup"><span data-stu-id="39819-139">For example, the <xref:System.Net.HttpWebResponse.Method%2A> method defaults to GET, which requests a resource from a Web server and returns the response.</span></span>  
  
 <span data-ttu-id="39819-140">Zazwyczaj Właściwość **ContentLength** musi być ustawiona na wartość większą od zera, gdy właściwość **metody** jest ustawiona na czasownik lub akcję, która wskazuje, że odbywa się przekazywanie.</span><span class="sxs-lookup"><span data-stu-id="39819-140">Typically the **ContentLength** property must be set to a value greater than zero when the **Method** property is set to a verb or action that indicates that an upload is taking place.</span></span>  
  
## <a name="preauthenticate-property"></a><span data-ttu-id="39819-141">Właściwość preuwierzytelniana</span><span class="sxs-lookup"><span data-stu-id="39819-141">PreAuthenticate Property</span></span>  
 <span data-ttu-id="39819-142">Aplikacje ustawiają <xref:System.Net.WebRequest.PreAuthenticate%2A> właściwość, aby wskazać, że informacje o uwierzytelnianiu powinny być wysyłane z żądaniem początkowym, a nie w oczekiwaniu na wyzwanie uwierzytelniania.</span><span class="sxs-lookup"><span data-stu-id="39819-142">Applications set the <xref:System.Net.WebRequest.PreAuthenticate%2A> property to indicate that authentication information should be sent with the initial request rather than waiting for an authentication challenge.</span></span> <span data-ttu-id="39819-143">Właściwość preauthentication ma znaczenie tylko w **przypadku, gdy** protokół obsługuje poświadczenia uwierzytelniania wysyłane z żądaniem początkowym.</span><span class="sxs-lookup"><span data-stu-id="39819-143">The **PreAuthenticate** property is only meaningful if the protocol supports authentication credentials sent with the initial request.</span></span>  
  
## <a name="proxy-property"></a><span data-ttu-id="39819-144">Właściwość serwera proxy</span><span class="sxs-lookup"><span data-stu-id="39819-144">Proxy Property</span></span>  
 <span data-ttu-id="39819-145"><xref:System.Net.WebRequest.Proxy%2A> Właściwość<xref:System.Net.IWebProxy> zawiera interfejs, który jest używany w celu uzyskania dostępu do żądanego zasobu.</span><span class="sxs-lookup"><span data-stu-id="39819-145">The <xref:System.Net.WebRequest.Proxy%2A> property contains an <xref:System.Net.IWebProxy> interface that is used to access the requested resource.</span></span> <span data-ttu-id="39819-146">Właściwość **proxy** ma znaczenie tylko wtedy, gdy protokół obsługuje żądania proxy.</span><span class="sxs-lookup"><span data-stu-id="39819-146">The **Proxy** property is meaningful only if your protocol supports proxied requests.</span></span> <span data-ttu-id="39819-147">Należy ustawić domyślny serwer proxy, jeśli jest wymagany przez protokół.</span><span class="sxs-lookup"><span data-stu-id="39819-147">You must set the default proxy if one is required by your protocol.</span></span>  
  
 <span data-ttu-id="39819-148">W niektórych środowiskach, takich jak za zaporą firmową, do korzystania z serwera proxy może być wymagany protokół.</span><span class="sxs-lookup"><span data-stu-id="39819-148">In some environments, such as behind a corporate firewall, your protocol might be required to use a proxy.</span></span> <span data-ttu-id="39819-149">W takim przypadku należy zaimplementować interfejs **IWebProxy** , aby utworzyć klasę proxy, która będzie działała na potrzeby protokołu.</span><span class="sxs-lookup"><span data-stu-id="39819-149">In that case, you must implement the **IWebProxy** interface to create a proxy class that will work for your protocol.</span></span>  
  
## <a name="requesturi-property"></a><span data-ttu-id="39819-150">Właściwość RequestUri</span><span class="sxs-lookup"><span data-stu-id="39819-150">RequestUri Property</span></span>  
 <span data-ttu-id="39819-151">Właściwość zawiera identyfikator URI, który został przesłany do metody **WebRequest. Create.** <xref:System.Net.WebRequest.RequestUri%2A></span><span class="sxs-lookup"><span data-stu-id="39819-151">The <xref:System.Net.WebRequest.RequestUri%2A> property contains the URI that was passed to the **WebRequest.Create** method.</span></span> <span data-ttu-id="39819-152">Jest on tylko do odczytu i nie można go zmienić po utworzeniu **żądania WebRequest** .</span><span class="sxs-lookup"><span data-stu-id="39819-152">It is read-only and cannot be changed once the **WebRequest** has been created.</span></span> <span data-ttu-id="39819-153">Jeśli protokół obsługuje przekierowywanie, odpowiedź może pochodzić z zasobu identyfikowanego przy użyciu innego identyfikatora URI.</span><span class="sxs-lookup"><span data-stu-id="39819-153">If your protocol supports redirection, the response can come from a resource identified by a different URI.</span></span> <span data-ttu-id="39819-154">Jeśli musisz zapewnić dostęp do identyfikatora URI, który odpowiedział, musisz podać dodatkową właściwość zawierającą ten identyfikator URI.</span><span class="sxs-lookup"><span data-stu-id="39819-154">If you need to provide access to the URI that responded, you must provide an additional property containing that URI.</span></span>  
  
## <a name="timeout-property"></a><span data-ttu-id="39819-155">Właściwość Timeout</span><span class="sxs-lookup"><span data-stu-id="39819-155">Timeout Property</span></span>  
 <span data-ttu-id="39819-156"><xref:System.Net.WebRequest.Timeout%2A> Właściwość zawiera czas oczekiwania (w milisekundach), po upływie którego żądanie przekroczy limit czasu i zgłosi wyjątek.</span><span class="sxs-lookup"><span data-stu-id="39819-156">The <xref:System.Net.WebRequest.Timeout%2A> property contains the length of time, in milliseconds, to wait before the request times out and throws an exception.</span></span> <span data-ttu-id="39819-157">**Limit czasu** ma zastosowanie tylko do żądań synchronicznych <xref:System.Net.WebRequest.GetResponse%2A> wykonanych przy użyciu metody; <xref:System.Net.WebRequest.Abort%2A> żądania asynchroniczne muszą używać metody, aby anulować oczekujące żądanie.</span><span class="sxs-lookup"><span data-stu-id="39819-157">**Timeout** applies only to synchronous requests made with the <xref:System.Net.WebRequest.GetResponse%2A> method; asynchronous requests must use the <xref:System.Net.WebRequest.Abort%2A> method to cancel a pending request.</span></span>  
  
 <span data-ttu-id="39819-158">Ustawienie właściwości **timeout** ma znaczenie tylko wtedy, gdy klasa specyficzna dla protokołu implementuje proces przekroczenia limitu czasu.</span><span class="sxs-lookup"><span data-stu-id="39819-158">Setting the **Timeout** property is meaningful only if the protocol-specific class implements a time-out process.</span></span>  
  
## <a name="abort-method"></a><span data-ttu-id="39819-159">Abort — Metoda</span><span class="sxs-lookup"><span data-stu-id="39819-159">Abort Method</span></span>  
 <span data-ttu-id="39819-160"><xref:System.Net.WebRequest.Abort%2A> Metoda anuluje oczekujące asynchroniczne żądanie do serwera.</span><span class="sxs-lookup"><span data-stu-id="39819-160">The <xref:System.Net.WebRequest.Abort%2A> method cancels a pending asynchronous request to a server.</span></span> <span data-ttu-id="39819-161">Po anulowaniu żądania wywołanie metody **GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**, **BeginGetRequestStream**lub **EndGetRequestStream** spowoduje zgłoszenie elementu <xref:System.Net.WebException> Właściwość ustawiona na <xref:System.Net.WebExceptionStatus>. <xref:System.Net.WebException.Status%2A></span><span class="sxs-lookup"><span data-stu-id="39819-161">After the request has been canceled, calling **GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**, **BeginGetRequestStream**, or **EndGetRequestStream** will throw a <xref:System.Net.WebException> with the <xref:System.Net.WebException.Status%2A> property set to <xref:System.Net.WebExceptionStatus>.</span></span>  
  
## <a name="begingetrequeststream-and-endgetrequeststream-methods"></a><span data-ttu-id="39819-162">Metody BeginGetRequestStream i EndGetRequestStream</span><span class="sxs-lookup"><span data-stu-id="39819-162">BeginGetRequestStream and EndGetRequestStream Methods</span></span>  
 <span data-ttu-id="39819-163"><xref:System.Net.WebRequest.BeginGetRequestStream%2A> Metoda uruchamia asynchroniczne żądanie dla strumienia, który jest używany do przekazywania danych na serwer.</span><span class="sxs-lookup"><span data-stu-id="39819-163">The <xref:System.Net.WebRequest.BeginGetRequestStream%2A> method starts an asynchronous request for the stream that is used to upload data to the server.</span></span> <span data-ttu-id="39819-164"><xref:System.Net.WebRequest.EndGetRequestStream%2A> Metoda kończy asynchroniczne żądanie i zwraca żądany strumień.</span><span class="sxs-lookup"><span data-stu-id="39819-164">The <xref:System.Net.WebRequest.EndGetRequestStream%2A> method completes the asynchronous request and returns the requested stream.</span></span> <span data-ttu-id="39819-165">Te metody implementują metodę **GetRequestStream** przy użyciu standardowego wzorca asynchronicznego .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="39819-165">These methods implement the **GetRequestStream** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="begingetresponse-and-endgetresponse-methods"></a><span data-ttu-id="39819-166">Metody BeginGetResponse i EndGetResponse</span><span class="sxs-lookup"><span data-stu-id="39819-166">BeginGetResponse and EndGetResponse Methods</span></span>  
 <span data-ttu-id="39819-167"><xref:System.Net.WebRequest.BeginGetResponse%2A> Metoda uruchamia asynchroniczne żądanie do serwera.</span><span class="sxs-lookup"><span data-stu-id="39819-167">The <xref:System.Net.WebRequest.BeginGetResponse%2A> method starts an asynchronous request to a server.</span></span> <span data-ttu-id="39819-168"><xref:System.Net.WebRequest.EndGetResponse%2A> Metoda kończy asynchroniczne żądanie i zwraca żądaną odpowiedź.</span><span class="sxs-lookup"><span data-stu-id="39819-168">The <xref:System.Net.WebRequest.EndGetResponse%2A> method completes the asynchronous request and returns the requested response.</span></span> <span data-ttu-id="39819-169">Te metody implementują metodę **GetResponse** przy użyciu wzorca asynchronicznego .NET Framework standardowego.</span><span class="sxs-lookup"><span data-stu-id="39819-169">These methods implement the **GetResponse** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="getrequeststream-method"></a><span data-ttu-id="39819-170">GetRequestStream, Metoda</span><span class="sxs-lookup"><span data-stu-id="39819-170">GetRequestStream Method</span></span>  
 <span data-ttu-id="39819-171"><xref:System.Net.WebRequest.GetRequestStream%2A> Metoda zwraca strumień, który jest używany do zapisywania danych na żądanym serwerze.</span><span class="sxs-lookup"><span data-stu-id="39819-171">The <xref:System.Net.WebRequest.GetRequestStream%2A> method returns a stream that is used to write data to the requested server.</span></span> <span data-ttu-id="39819-172">Zwrócony strumień powinien być strumieniem tylko do zapisu, który nie poszukuje; jest ona przeznaczona do jednokierunkowego strumienia danych, które są zapisywane na serwerze.</span><span class="sxs-lookup"><span data-stu-id="39819-172">The stream returned should be a write-only stream that does not seek; it is intended as a one-way stream of data that is written to the server.</span></span> <span data-ttu-id="39819-173">Strumień zwraca wartość false dla <xref:System.IO.Stream.CanRead%2A> <xref:System.IO.Stream.CanWrite%2A> właściwości i <xref:System.IO.Stream.CanSeek%2A> i ma wartość true dla właściwości.</span><span class="sxs-lookup"><span data-stu-id="39819-173">The stream returns false for the <xref:System.IO.Stream.CanRead%2A> and <xref:System.IO.Stream.CanSeek%2A> properties and true for the <xref:System.IO.Stream.CanWrite%2A> property.</span></span>  
  
 <span data-ttu-id="39819-174">Metoda **GetRequestStream** zazwyczaj otwiera połączenie z serwerem i, przed zwróceniem strumienia, wysyła informacje nagłówka, które wskazują, że dane są wysyłane do serwera.</span><span class="sxs-lookup"><span data-stu-id="39819-174">The **GetRequestStream** method typically opens a connection to the server and, before returning the stream, sends header information that indicates that data is being sent to the server.</span></span> <span data-ttu-id="39819-175">Ponieważ **GetRequestStream** rozpoczyna żądanie, ustawienie właściwości **nagłówka** lub właściwości **ContentLength** zazwyczaj nie jest dozwolone po wywołaniu **GetRequestStream**.</span><span class="sxs-lookup"><span data-stu-id="39819-175">Because **GetRequestStream** begins the request, setting any **Header** properties or the **ContentLength** property is typically not allowed after calling **GetRequestStream**.</span></span>  
  
## <a name="getresponse-method"></a><span data-ttu-id="39819-176">GetResponse — metoda</span><span class="sxs-lookup"><span data-stu-id="39819-176">GetResponse Method</span></span>  
 <span data-ttu-id="39819-177">Metoda zwraca specyficzny dla protokołu element podrzędny <xref:System.Net.WebResponse> klasy, która reprezentuje odpowiedź z serwera. <xref:System.Net.WebRequest.GetResponse%2A></span><span class="sxs-lookup"><span data-stu-id="39819-177">The <xref:System.Net.WebRequest.GetResponse%2A> method returns a protocol-specific descendant of the <xref:System.Net.WebResponse> class that represents the response from the server.</span></span> <span data-ttu-id="39819-178">O ile żądanie zostało już zainicjowane przez metodę **GetRequestStream** , Metoda **GetResponse** tworzy połączenie z zasobem identyfikowanym przez **RequestUri**, wysyła informacje nagłówka wskazujące typ tworzonego żądania , a następnie otrzymuje odpowiedź z zasobu.</span><span class="sxs-lookup"><span data-stu-id="39819-178">Unless the request has already been initiated by the **GetRequestStream** method, the **GetResponse** method creates a connection to the resource identified by **RequestUri**, sends header information indicating the type of request being made, and then receives the response from the resource.</span></span>  
  
 <span data-ttu-id="39819-179">Po wywołaniu metody **GetResponse** wszystkie właściwości należy traktować jako tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="39819-179">Once the **GetResponse** method is called, all properties should be considered read-only.</span></span> <span data-ttu-id="39819-180">Wystąpienia **WebRequest** są przeznaczone do jednorazowego użytku; Jeśli chcesz wykonać kolejne żądanie, należy utworzyć nowe **żądanie WebRequest**.</span><span class="sxs-lookup"><span data-stu-id="39819-180">**WebRequest** instances are intended for one-time use; if you want to make another request, you should create a new **WebRequest**.</span></span>  
  
 <span data-ttu-id="39819-181">Metoda **GetResponse** jest odpowiedzialna za utworzenie odpowiedniego elementu podrzędnego **WebResponse** w celu przechowywania odpowiedzi przychodzącej.</span><span class="sxs-lookup"><span data-stu-id="39819-181">The **GetResponse** method is responsible for creating an appropriate **WebResponse** descendant to contain the incoming response.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="39819-182">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="39819-182">See also</span></span>

- <xref:System.Net.WebRequest>
- <xref:System.Net.HttpWebRequest>
- <xref:System.Net.FileWebRequest>
- [<span data-ttu-id="39819-183">Programowanie protokołów podłączanych</span><span class="sxs-lookup"><span data-stu-id="39819-183">Programming Pluggable Protocols</span></span>](programming-pluggable-protocols.md)
- [<span data-ttu-id="39819-184">Wyprowadzanie z elementu WebResponse</span><span class="sxs-lookup"><span data-stu-id="39819-184">Deriving from WebResponse</span></span>](deriving-from-webresponse.md)
