---
title: Implementowanie interfejsu IEnumerable w języku Visual Basic
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: 1bc467854e0c1f082a986c1216e971c86d85c994
ms.sourcegitcommit: 40364ded04fa6cdcb2b6beca7f68412e2e12f633
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 02/28/2019
ms.locfileid: "56970080"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="a8b3f-102">Przewodnik: Implementowanie IEnumerable(Of T) w języku Visual Basic</span><span class="sxs-lookup"><span data-stu-id="a8b3f-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="a8b3f-103"><xref:System.Collections.Generic.IEnumerable%601> Interfejs jest implementowany przez klasy, które mogą zwracać sekwencję wartości jeden element w danym momencie.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="a8b3f-104">Zaletą zwracający dane, jeden element w danym momencie to, że nie masz załadować kompletny zestaw danych do pamięci z nią pracować.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="a8b3f-105">Musisz użyć wystarczającą ilość pamięci do załadowania pojedynczy element z danych.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="a8b3f-106">Klasy, które implementują `IEnumerable(T)` interfejsu może być używany z `For Each` pętli lub zapytań LINQ.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="a8b3f-107">Na przykład rozważmy aplikację, która musi odczytywanie pliku tekstowego duże i zwracać każdy wiersz z pliku, który pasuje do kryteriów wyszukiwania określonego.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="a8b3f-108">Aplikacja używa zapytania LINQ do zwrócenia wierszy z pliku, który spełniających określone kryteria.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="a8b3f-109">Aby zbadać zawartość pliku za pomocą zapytań LINQ, aplikacja może ładować zawartość pliku do tablicy lub kolekcji.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="a8b3f-110">Jednak ładowanie całego pliku do tablicy lub kolekcji będzie zużywać znacznie więcej pamięci niż jest to wymagane.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="a8b3f-111">Zapytania LINQ zamiast tego można zbadać zawartość pliku przy użyciu klasy wyliczalny, zwracając tylko te wartości, które spełniają kryteria wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="a8b3f-112">Zapytania, które zwracają tylko kilka pasujących wartości będą zużywać znacznie mniej pamięci.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="a8b3f-113">Można utworzyć klasę, która implementuje <xref:System.Collections.Generic.IEnumerable%601> interfejsu, aby uwidocznić źródło danych, jako wyliczalny dane.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="a8b3f-114">Swojej klasie, która implementuje `IEnumerable(T)` interfejsu będzie wymagać innej klasy, która implementuje <xref:System.Collections.Generic.IEnumerator%601> interfejsu do iteracji przez źródło danych.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="a8b3f-115">Te dwie klasy umożliwiają zwracają elementy danych po kolei jako określonego typu.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="a8b3f-116">W tym instruktażu utworzysz klasę, która implementuje `IEnumerable(Of String)` interfejsu i klasę, która implementuje `IEnumerator(Of String)` interfejsu można odczytać tekstu jeden wiersz pliku naraz.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="a8b3f-117">Tworzenie klasy Wyliczalny</span><span class="sxs-lookup"><span data-stu-id="a8b3f-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="a8b3f-118">**Utwórz projekt wyliczalny klas**</span><span class="sxs-lookup"><span data-stu-id="a8b3f-118">**Create the enumerable class project**</span></span>

1.  <span data-ttu-id="a8b3f-119">W języku Visual Basic na **pliku** menu wskaż **New** a następnie kliknij przycisk **projektu**.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1.  <span data-ttu-id="a8b3f-120">W **nowy projekt** dialogowym **typów projektów** okienka, upewnij się, że **Windows** jest zaznaczone.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="a8b3f-121">Wybierz **biblioteki klas** w **szablony** okienka.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="a8b3f-122">W **nazwa** wpisz `StreamReaderEnumerable`, a następnie kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="a8b3f-123">Zostanie wyświetlony nowy projekt.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-123">The new project is displayed.</span></span>

1.  <span data-ttu-id="a8b3f-124">W **Eksploratora rozwiązań**, kliknij prawym przyciskiem myszy plik Class1.vb i kliknij przycisk **Zmień nazwę**.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="a8b3f-125">Zmień nazwę pliku do `StreamReaderEnumerable.vb` i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="a8b3f-126">Zmiana nazwy pliku spowoduje również zmianę nazwy klasy, która ma `StreamReaderEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="a8b3f-127">Ta klasa wdroży `IEnumerable(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1.  <span data-ttu-id="a8b3f-128">Kliknij prawym przyciskiem myszy projekt StreamReaderEnumerable, wskaż opcję **Dodaj**, a następnie kliknij przycisk **nowy element**.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="a8b3f-129">Wybierz **klasy** szablonu.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-129">Select the **Class** template.</span></span> <span data-ttu-id="a8b3f-130">W **nazwa** wpisz `StreamReaderEnumerator.vb` i kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="a8b3f-131">Pierwszą klasą w tym projekcie jest klasą wyliczalny i wdroży `IEnumerable(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="a8b3f-132">Implementuje ten interfejs ogólny <xref:System.Collections.IEnumerable> interfejsu i gwarantuje, że osoby korzystające z tej klasy mają dostęp do wartości wpisanych jako `String`.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="a8b3f-133">**Dodaj kod do implementacji interfejsu IEnumerable**</span><span class="sxs-lookup"><span data-stu-id="a8b3f-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="a8b3f-134">Otwórz plik StreamReaderEnumerable.vb.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="a8b3f-135">W wierszu po `Public Class StreamReaderEnumerable`, wpisz następujące polecenie i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#1)]

   <span data-ttu-id="a8b3f-136">Visual Basic automatycznie wypełnia klasę za pomocą elementów członkowskich, które są wymagane przez `IEnumerable(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="a8b3f-137">Ta klasa wyliczalny odczyta wiersze z jeden wiersz pliku tekstowego w danym momencie.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="a8b3f-138">Dodaj następujący kod do klasy w celu udostępnienia publicznego konstruktora, która przyjmuje ścieżkę do pliku jako parametr wejściowy.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#2)]

4. <span data-ttu-id="a8b3f-139">Implementacja <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> metody `IEnumerable(Of String)` interfejsu spowoduje zwrócenie nowego wystąpienia `StreamReaderEnumerator` klasy.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="a8b3f-140">Implementacja `GetEnumerator` metody `IEnumerable` może pracować z interfejsem `Private`, ponieważ musisz udostępnić tylko członkowie `IEnumerable(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="a8b3f-141">Zastąp kod, który języka Visual Basic wygenerowany dla `GetEnumerator` metody z następującym kodem.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#3)]  
  
<span data-ttu-id="a8b3f-142">**Dodaj kod implementacji IEnumerator**</span><span class="sxs-lookup"><span data-stu-id="a8b3f-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="a8b3f-143">Otwórz plik StreamReaderEnumerator.vb.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="a8b3f-144">W wierszu po `Public Class StreamReaderEnumerator`, wpisz następujące polecenie i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#4)]

   <span data-ttu-id="a8b3f-145">Visual Basic automatycznie wypełnia klasę za pomocą elementów członkowskich, które są wymagane przez `IEnumerator(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="a8b3f-146">Klasa modułu wyliczającego otwiera plik tekstowy i wykonuje plik We/Wy odczytu wiersze z pliku.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="a8b3f-147">Dodaj następujący kod do klasy w celu udostępnienia publicznego konstruktora, która przyjmuje ścieżkę do pliku jako parametr wejściowy, a następnie otwórz plik tekstowy do odczytu.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#5)]

4. <span data-ttu-id="a8b3f-148">`Current` Właściwości dla obu `IEnumerator(Of String)` i `IEnumerator` interfejsów zwracanie bieżącego elementu z pliku tekstowego jako `String`.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="a8b3f-149">Implementacja `Current` właściwość `IEnumerator` może pracować z interfejsem `Private`, ponieważ musisz udostępnić tylko członkowie `IEnumerator(Of String)` interfejsu.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="a8b3f-150">Zastąp kod, który języka Visual Basic wygenerowany dla `Current` właściwości z następującym kodem.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#6)]

5. <span data-ttu-id="a8b3f-151">`MoveNext` Metody `IEnumerator` interfejsu przechodzi do następnego elementu w pliku tekstowym i aktualizuje wartość, która jest zwracana przez `Current` właściwości.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="a8b3f-152">Jeśli istnieje więcej elementów do odczytu, `MoveNext` metoda zwraca `False`; w przeciwnym razie `MoveNext` metoda zwraca `True`.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="a8b3f-153">Dodaj następujący kod do `MoveNext` metody.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-153">Add the following code to the `MoveNext` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#7)]

6. <span data-ttu-id="a8b3f-154">`Reset` Metody `IEnumerator` interfejsu kieruje iteratora, aby wskazywała na początku pliku tekstowego i wyczyści bieżącą wartość elementu.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="a8b3f-155">Dodaj następujący kod do `Reset` metody.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-155">Add the following code to the `Reset` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#8)]

7. <span data-ttu-id="a8b3f-156">`Dispose` Metody `IEnumerator` interfejsu gwarantuje, że wszystkie zasoby niezarządzane są zwalniane, przed iteratora jest niszczony.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="a8b3f-157">Dojście do pliku, który jest używany przez `StreamReader` obiekt został niezarządzany zasób i muszą zostać zamknięte przed wystąpieniem iteratora.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="a8b3f-158">Zastąp kod, który języka Visual Basic wygenerowany dla `Dispose` metoda następującym kodem.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#9)] 
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="a8b3f-159">Używając iteratora próbki</span><span class="sxs-lookup"><span data-stu-id="a8b3f-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="a8b3f-160">Można użyć wyliczalny klasy w kodzie oraz struktur sterujących, które wymagają obiekt, który implementuje `IEnumerable`, takich jak `For Next` pętli lub kwerendę LINQ.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="a8b3f-161">W poniższym przykładzie przedstawiono `StreamReaderEnumerable` w zapytaniu programu LINQ.</span><span class="sxs-lookup"><span data-stu-id="a8b3f-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/Module1.vb#10)]  
  
## <a name="see-also"></a><span data-ttu-id="a8b3f-162">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="a8b3f-162">See also</span></span>
- [<span data-ttu-id="a8b3f-163">Wprowadzenie do LINQ w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="a8b3f-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)
- [<span data-ttu-id="a8b3f-164">Przepływ sterowania</span><span class="sxs-lookup"><span data-stu-id="a8b3f-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)
- [<span data-ttu-id="a8b3f-165">Struktury pętli</span><span class="sxs-lookup"><span data-stu-id="a8b3f-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)
- [<span data-ttu-id="a8b3f-166">For Each...Next, instrukcja</span><span class="sxs-lookup"><span data-stu-id="a8b3f-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
