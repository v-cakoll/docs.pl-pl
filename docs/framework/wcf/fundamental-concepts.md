---
title: Podstawowe pojęcia programu Windows Communication Foundation
ms.date: 03/30/2017
helpviewer_keywords:
- WCF [WCF], concepts
- concepts [WCF]
- fundamentals [WCF]
- Windows Communication Foundation [WCF], concepts
ms.assetid: 3e7e0afd-7913-499d-bafb-eac7caacbc7a
ms.openlocfilehash: 9dcaa5f73dd8a4ec1943cb7fc840feee889563b8
ms.sourcegitcommit: 628e8147ca10187488e6407dab4c4e6ebe0cac47
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 10/15/2019
ms.locfileid: "72319848"
---
# <a name="fundamental-windows-communication-foundation-concepts"></a><span data-ttu-id="a2a64-102">Podstawowe pojęcia programu Windows Communication Foundation</span><span class="sxs-lookup"><span data-stu-id="a2a64-102">Fundamental Windows Communication Foundation Concepts</span></span>

<span data-ttu-id="a2a64-103">Ten dokument zawiera ogólny widok architektury Windows Communication Foundation (WCF).</span><span class="sxs-lookup"><span data-stu-id="a2a64-103">This document provides a high-level view of the Windows Communication Foundation (WCF) architecture.</span></span> <span data-ttu-id="a2a64-104">Ma ona na celu wyjaśnienie najważniejszych pojęć i ich dopasowania.</span><span class="sxs-lookup"><span data-stu-id="a2a64-104">It is intended to explain key concepts and how they fit together.</span></span> <span data-ttu-id="a2a64-105">Aby zapoznać się z samouczkiem dotyczącym tworzenia najprostszej wersji usługi i klienta WCF, zobacz [samouczek wprowadzenie](getting-started-tutorial.md).</span><span class="sxs-lookup"><span data-stu-id="a2a64-105">For a tutorial on creating the simplest version of a WCF service and client, see [Getting Started Tutorial](getting-started-tutorial.md).</span></span> <span data-ttu-id="a2a64-106">Aby poznać programowanie WCF, zobacz [podstawowe programowanie WCF](basic-wcf-programming.md).</span><span class="sxs-lookup"><span data-stu-id="a2a64-106">To learn WCF programming, see [Basic WCF Programming](basic-wcf-programming.md).</span></span>

## <a name="wcf-fundamentals"></a><span data-ttu-id="a2a64-107">Podstawy WCF</span><span class="sxs-lookup"><span data-stu-id="a2a64-107">WCF Fundamentals</span></span>

<span data-ttu-id="a2a64-108">WCF to środowisko uruchomieniowe i zestaw interfejsów API służących do tworzenia systemów wysyłających komunikaty między usługami i klientami.</span><span class="sxs-lookup"><span data-stu-id="a2a64-108">WCF is a runtime and a set of APIs for creating systems that send messages between services and clients.</span></span> <span data-ttu-id="a2a64-109">Ta sama infrastruktura i interfejsy API są używane do tworzenia aplikacji, które komunikują się z innymi aplikacjami w tym samym systemie komputerowym lub w systemie, który znajduje się w innej firmie i jest dostępny za pośrednictwem Internetu.</span><span class="sxs-lookup"><span data-stu-id="a2a64-109">The same infrastructure and APIs are used to create applications that communicate with other applications on the same computer system or on a system that resides in another company and is accessed over the Internet.</span></span>

### <a name="messaging-and-endpoints"></a><span data-ttu-id="a2a64-110">Wiadomości i punkty końcowe</span><span class="sxs-lookup"><span data-stu-id="a2a64-110">Messaging and Endpoints</span></span>

<span data-ttu-id="a2a64-111">Program WCF jest oparty na koncepcji komunikacji opartej na komunikatach, a wszystkie elementy, które mogą być modelowane jako wiadomości (na przykład żądanie HTTP lub komunikat usługi kolejkowania komunikatów (znany również jako MSMQ), mogą być reprezentowane w jednolity sposób w modelu programowania.</span><span class="sxs-lookup"><span data-stu-id="a2a64-111">WCF is based on the notion of message-based communication, and anything that can be modeled as a message (for example, an HTTP request or a Message Queuing (also known as MSMQ) message) can be represented in a uniform way in the programming model.</span></span> <span data-ttu-id="a2a64-112">Umożliwia to ujednolicony interfejs API w różnych mechanizmach transportu.</span><span class="sxs-lookup"><span data-stu-id="a2a64-112">This enables a unified API across different transport mechanisms.</span></span>

<span data-ttu-id="a2a64-113">Model odróżnia _klientów_, czyli aplikacje inicjujące komunikację i _usługi_, które są aplikacjami, które oczekują na komunikowanie się z nimi i odpowiadają na tę komunikację.</span><span class="sxs-lookup"><span data-stu-id="a2a64-113">The model distinguishes between _clients_, which are applications that initiate communication, and _services_, which are applications that wait for clients to communicate with them and respond to that communication.</span></span> <span data-ttu-id="a2a64-114">Pojedyncza aplikacja może działać zarówno jako klient, jak i usługa.</span><span class="sxs-lookup"><span data-stu-id="a2a64-114">A single application can act as both a client and a service.</span></span> <span data-ttu-id="a2a64-115">Aby zapoznać się z przykładami, zobacz [usługi dupleks](./feature-details/duplex-services.md) i [sieci peer-to-peer](./feature-details/peer-to-peer-networking.md).</span><span class="sxs-lookup"><span data-stu-id="a2a64-115">For examples, see [Duplex Services](./feature-details/duplex-services.md) and [Peer-to-Peer Networking](./feature-details/peer-to-peer-networking.md).</span></span>

<span data-ttu-id="a2a64-116">Komunikaty są wysyłane między punktami końcowymi.</span><span class="sxs-lookup"><span data-stu-id="a2a64-116">Messages are sent between endpoints.</span></span> <span data-ttu-id="a2a64-117">_Punkty końcowe_ to miejsca, w których komunikaty są wysyłane lub odbierane (lub oba), a także definiują wszystkie informacje wymagane do wymiany komunikatów.</span><span class="sxs-lookup"><span data-stu-id="a2a64-117">_Endpoints_ are places where messages are sent or received (or both), and they define all the information required for the message exchange.</span></span> <span data-ttu-id="a2a64-118">Usługa udostępnia jeden lub więcej punktów końcowych aplikacji (a także zero lub więcej punktów końcowych infrastruktury), a klient generuje punkt końcowy, który jest zgodny z jednym z punktów końcowych usługi.</span><span class="sxs-lookup"><span data-stu-id="a2a64-118">A service exposes one or more application endpoints (as well as zero or more infrastructure endpoints), and the client generates an endpoint that is compatible with one of the service's endpoints.</span></span>

<span data-ttu-id="a2a64-119">_Punkt końcowy_ opisuje w standardowym sposobie, w jaki wiadomości powinny być wysyłane, jak powinny być wysyłane i jak powinny wyglądać komunikaty.</span><span class="sxs-lookup"><span data-stu-id="a2a64-119">An _endpoint_ describes in a standard-based way where messages should be sent, how they should be sent, and what the messages should look like.</span></span> <span data-ttu-id="a2a64-120">Usługa może uwidaczniać te informacje jako metadane, które klienci mogą przetwarzać w celu generowania odpowiednich _stosów_komunikacji i klientów programu WCF.</span><span class="sxs-lookup"><span data-stu-id="a2a64-120">A service can expose this information as metadata that clients can process to generate appropriate WCF clients and communication _stacks_.</span></span>

### <a name="communication-protocols"></a><span data-ttu-id="a2a64-121">Protokoły komunikacyjne</span><span class="sxs-lookup"><span data-stu-id="a2a64-121">Communication Protocols</span></span>

<span data-ttu-id="a2a64-122">Jednym z wymaganych elementów stosu komunikacji jest _Protokół transportowy_.</span><span class="sxs-lookup"><span data-stu-id="a2a64-122">One required element of the communication stack is the _transport protocol_.</span></span> <span data-ttu-id="a2a64-123">Komunikaty mogą być wysyłane za pośrednictwem sieci intranet i Internetu przy użyciu wspólnych transportów, takich jak HTTP i TCP.</span><span class="sxs-lookup"><span data-stu-id="a2a64-123">Messages can be sent over intranets and the Internet using common transports, such as HTTP and TCP.</span></span> <span data-ttu-id="a2a64-124">Dołączono inne transporty obsługujące komunikację z aplikacjami i węzłami usługi kolejkowania komunikatów w sieci równorzędnej.</span><span class="sxs-lookup"><span data-stu-id="a2a64-124">Other transports are included that support communication with Message Queuing applications and nodes on a Peer Networking mesh.</span></span> <span data-ttu-id="a2a64-125">Więcej mechanizmów transportu można dodać przy użyciu wbudowanych punktów rozszerzenia programu WCF.</span><span class="sxs-lookup"><span data-stu-id="a2a64-125">More transport mechanisms can be added using the built-in extension points of WCF.</span></span>

<span data-ttu-id="a2a64-126">Innym wymaganym elementem w stosie komunikacyjnym jest kodowanie, które określa sposób formatowania danego komunikatu.</span><span class="sxs-lookup"><span data-stu-id="a2a64-126">Another required element in the communication stack is the encoding that specifies how any given message is formatted.</span></span> <span data-ttu-id="a2a64-127">Funkcja WCF udostępnia następujące kodowania:</span><span class="sxs-lookup"><span data-stu-id="a2a64-127">WCF provides the following encodings:</span></span>

- <span data-ttu-id="a2a64-128">Kodowanie tekstu, kodowanie międzyoperacyjnego.</span><span class="sxs-lookup"><span data-stu-id="a2a64-128">Text encoding, an interoperable encoding.</span></span>

- <span data-ttu-id="a2a64-129">Kodowanie mechanizmu optymalizacji transmisji wiadomości (MTOM), który jest międzyoperacyjnym sposobem wydajnego wysyłania danych binarnych bez struktury do i z usługi.</span><span class="sxs-lookup"><span data-stu-id="a2a64-129">Message Transmission Optimization Mechanism (MTOM) encoding, which is an interoperable way for efficiently sending unstructured binary data to and from a service.</span></span>

- <span data-ttu-id="a2a64-130">Kodowanie binarne w celu wydajnego transferu.</span><span class="sxs-lookup"><span data-stu-id="a2a64-130">Binary encoding for efficient transfer.</span></span>

<span data-ttu-id="a2a64-131">Więcej mechanizmów kodowania (na przykład kodowania kompresji) można dodać przy użyciu wbudowanych punktów rozszerzenia programu WCF.</span><span class="sxs-lookup"><span data-stu-id="a2a64-131">More encoding mechanisms (for example, a compression encoding) can be added using the built-in extension points of WCF.</span></span>

### <a name="message-patterns"></a><span data-ttu-id="a2a64-132">Wzorce komunikatów</span><span class="sxs-lookup"><span data-stu-id="a2a64-132">Message Patterns</span></span>

<span data-ttu-id="a2a64-133">Usługa WCF obsługuje kilka wzorców obsługi komunikatów, takich jak żądanie-odpowiedź, jednokierunkowe i dupleksowe.</span><span class="sxs-lookup"><span data-stu-id="a2a64-133">WCF supports several messaging patterns, including request-reply, one-way, and duplex communication.</span></span> <span data-ttu-id="a2a64-134">Różne transporty obsługują różne wzorce obsługi komunikatów i w ten sposób wpływają na typy interakcji, które są przez nie obsługiwane.</span><span class="sxs-lookup"><span data-stu-id="a2a64-134">Different transports support different messaging patterns, and thus affect the types of interactions that they support.</span></span> <span data-ttu-id="a2a64-135">Interfejsy API i środowisko uruchomieniowe WCF ułatwiają również bezpieczne i niezawodne wysyłanie komunikatów.</span><span class="sxs-lookup"><span data-stu-id="a2a64-135">The WCF APIs and runtime also help you to send messages securely and reliably.</span></span>

## <a name="wcf-terms"></a><span data-ttu-id="a2a64-136">Warunki WCF</span><span class="sxs-lookup"><span data-stu-id="a2a64-136">WCF Terms</span></span>

<span data-ttu-id="a2a64-137">Inne pojęcia i terminy używane w dokumentacji programu WCF obejmują następujące zagadnienia:</span><span class="sxs-lookup"><span data-stu-id="a2a64-137">Other concepts and terms used in the WCF documentation include the following:</span></span>

<span data-ttu-id="a2a64-138">**Komunikat**</span><span class="sxs-lookup"><span data-stu-id="a2a64-138">**Message**</span></span>  
 <span data-ttu-id="a2a64-139">Samodzielna jednostka danych, która może składać się z kilku części, w tym treści i nagłówków.</span><span class="sxs-lookup"><span data-stu-id="a2a64-139">A self-contained unit of data that can consist of several parts, including a body and headers.</span></span>

<span data-ttu-id="a2a64-140">**Usługa**</span><span class="sxs-lookup"><span data-stu-id="a2a64-140">**Service**</span></span>  
 <span data-ttu-id="a2a64-141">Konstrukcja, która uwidacznia jeden lub więcej punktów końcowych, przy czym każdy punkt końcowy uwidacznia jedną lub więcej operacji usługi.</span><span class="sxs-lookup"><span data-stu-id="a2a64-141">A construct that exposes one or more endpoints, with each endpoint exposing one or more service operations.</span></span>

<span data-ttu-id="a2a64-142">**Punkt końcowy**</span><span class="sxs-lookup"><span data-stu-id="a2a64-142">**Endpoint**</span></span>  
 <span data-ttu-id="a2a64-143">Konstrukcja, w której wysyłane lub odbierane są komunikaty (lub oba te elementy).</span><span class="sxs-lookup"><span data-stu-id="a2a64-143">A construct at which messages are sent or received (or both).</span></span> <span data-ttu-id="a2a64-144">Składa się z lokalizacji (adresu), która określa, gdzie mogą być wysyłane komunikaty, specyfikację mechanizmu komunikacji (powiązanie) opisującą sposób wysyłania komunikatów oraz definicję zestawu komunikatów, które mogą być wysyłane lub odbierane (lub oba te elementy). Lokalizacja (kontrakt usługi) opisująca, jaki komunikat można wysłać.</span><span class="sxs-lookup"><span data-stu-id="a2a64-144">It comprises a location (an address) that defines where messages can be sent, a specification of the communication mechanism (a binding) that describes how messages should be sent, and a definition for a set of messages that can be sent or received (or both) at that location (a service contract) that describes what message can be sent.</span></span>

<span data-ttu-id="a2a64-145">Usługa WCF jest narażona na świecie jako zbiór punktów końcowych.</span><span class="sxs-lookup"><span data-stu-id="a2a64-145">A WCF service is exposed to the world as a collection of endpoints.</span></span>

<span data-ttu-id="a2a64-146">**Punkt końcowy aplikacji**</span><span class="sxs-lookup"><span data-stu-id="a2a64-146">**Application endpoint**</span></span>  
 <span data-ttu-id="a2a64-147">Punkt końcowy uwidoczniony przez aplikację i odpowiadający kontraktowi usługi zaimplementowanemu przez aplikację.</span><span class="sxs-lookup"><span data-stu-id="a2a64-147">An endpoint exposed by the application and that corresponds to a service contract implemented by the application.</span></span>

<span data-ttu-id="a2a64-148">**Punkt końcowy infrastruktury**</span><span class="sxs-lookup"><span data-stu-id="a2a64-148">**Infrastructure endpoint**</span></span>  
 <span data-ttu-id="a2a64-149">Punkt końcowy, który jest udostępniany przez infrastrukturę w celu ułatwienia funkcjonalności, która jest wymagana lub oferowana przez usługę, która nie jest powiązana z umową usługi.</span><span class="sxs-lookup"><span data-stu-id="a2a64-149">An endpoint that is exposed by the infrastructure to facilitate functionality that is needed or provided by the service that does not relate to a service contract.</span></span> <span data-ttu-id="a2a64-150">Na przykład usługa może mieć punkt końcowy infrastruktury, który zawiera informacje o metadanych.</span><span class="sxs-lookup"><span data-stu-id="a2a64-150">For example, a service might have an infrastructure endpoint that provides metadata information.</span></span>

<span data-ttu-id="a2a64-151">**Ulica**</span><span class="sxs-lookup"><span data-stu-id="a2a64-151">**Address**</span></span>  
 <span data-ttu-id="a2a64-152">Określa lokalizację, w której odbierane są komunikaty.</span><span class="sxs-lookup"><span data-stu-id="a2a64-152">Specifies the location where messages are received.</span></span> <span data-ttu-id="a2a64-153">Jest on określony jako Uniform Resource Identifier (URI).</span><span class="sxs-lookup"><span data-stu-id="a2a64-153">It is specified as a Uniform Resource Identifier (URI).</span></span> <span data-ttu-id="a2a64-154">Część schematu identyfikatora URI nazywa mechanizm transportu, który ma być używany do uzyskiwania dostępu do adresu, takiego jak HTTP i TCP.</span><span class="sxs-lookup"><span data-stu-id="a2a64-154">The URI schema part names the transport mechanism to use to reach the address, such as HTTP and TCP.</span></span> <span data-ttu-id="a2a64-155">Hierarchiczna część identyfikatora URI zawiera unikatową lokalizację, której format zależy od mechanizmu transportowego.</span><span class="sxs-lookup"><span data-stu-id="a2a64-155">The hierarchical part of the URI contains a unique location whose format is dependent on the transport mechanism.</span></span>

<span data-ttu-id="a2a64-156">Adres punktu końcowego umożliwia tworzenie unikatowych adresów punktów końcowych dla każdego punktu końcowego w usłudze lub w określonych warunkach, aby udostępnić adres między punktami końcowymi.</span><span class="sxs-lookup"><span data-stu-id="a2a64-156">The endpoint address enables you to create unique endpoint addresses for each endpoint in a service or, under certain conditions, to share an address across endpoints.</span></span> <span data-ttu-id="a2a64-157">Poniższy przykład przedstawia adres przy użyciu protokołu HTTPS z portem innym niż domyślny:</span><span class="sxs-lookup"><span data-stu-id="a2a64-157">The following example shows an address using the HTTPS protocol with a non-default port:</span></span>

```http
HTTPS://cohowinery:8005/ServiceModelSamples/CalculatorService
```

<span data-ttu-id="a2a64-158">**Powiązanie**</span><span class="sxs-lookup"><span data-stu-id="a2a64-158">**Binding**</span></span>  
 <span data-ttu-id="a2a64-159">Definiuje, w jaki sposób punkt końcowy komunikuje się ze światem.</span><span class="sxs-lookup"><span data-stu-id="a2a64-159">Defines how an endpoint communicates to the world.</span></span> <span data-ttu-id="a2a64-160">Jest zbudowana z zestawu składników nazywanych elementami powiązania, które "stosują" jeden na drugim, aby utworzyć infrastrukturę komunikacji.</span><span class="sxs-lookup"><span data-stu-id="a2a64-160">It is constructed of a set of components called binding elements that "stack" one on top of the other to create the communication infrastructure.</span></span> <span data-ttu-id="a2a64-161">Co najmniej, powiązanie definiuje transport (na przykład HTTP lub TCP) i używane kodowanie (na przykład tekst lub binarny).</span><span class="sxs-lookup"><span data-stu-id="a2a64-161">At the very least, a binding defines the transport (such as HTTP or TCP) and the encoding being used (such as text or binary).</span></span> <span data-ttu-id="a2a64-162">Powiązanie może zawierać elementy wiążące, które określają szczegóły, takie jak mechanizmy zabezpieczeń używane do zabezpieczania komunikatów lub wzorzec wiadomości używany przez punkt końcowy.</span><span class="sxs-lookup"><span data-stu-id="a2a64-162">A binding can contain binding elements that specify details like the security mechanisms used to secure messages, or the message pattern used by an endpoint.</span></span> <span data-ttu-id="a2a64-163">Aby uzyskać więcej informacji, zobacz [Konfigurowanie usług](configuring-services.md).</span><span class="sxs-lookup"><span data-stu-id="a2a64-163">For more information, see [Configuring Services](configuring-services.md).</span></span>

<span data-ttu-id="a2a64-164">**Element powiązania**</span><span class="sxs-lookup"><span data-stu-id="a2a64-164">**Binding element**</span></span>  
 <span data-ttu-id="a2a64-165">Reprezentuje szczególny element powiązania, na przykład transport, kodowanie, implementację protokołu poziomu infrastruktury (na przykład WS-ReliableMessaging) lub dowolny inny składnik stosu komunikacji.</span><span class="sxs-lookup"><span data-stu-id="a2a64-165">Represents a particular piece of the binding, such as a transport, an encoding, an implementation of an infrastructure-level protocol (such as WS-ReliableMessaging), or any other component of the communication stack.</span></span>

<span data-ttu-id="a2a64-166">**Zachowania**</span><span class="sxs-lookup"><span data-stu-id="a2a64-166">**Behaviors**</span></span>  
 <span data-ttu-id="a2a64-167">Składnik kontrolujący różne aspekty czasu wykonywania usługi, punkt końcowy, konkretną operację lub klienta.</span><span class="sxs-lookup"><span data-stu-id="a2a64-167">A component that controls various run-time aspects of a service, an endpoint, a particular operation, or a client.</span></span> <span data-ttu-id="a2a64-168">Zachowania są pogrupowane według zakresu: typowe zachowania mają na celu globalne wpływ na wszystkie punkty końcowe, zachowania usługi dotyczą tylko aspektów związanych z usługą, zachowania punktów końcowych wpływają wyłącznie na właściwości powiązane z punktem końcowym, a zachowania poziomu operacji mają wpływ na konkretną składowa.</span><span class="sxs-lookup"><span data-stu-id="a2a64-168">Behaviors are grouped according to scope: common behaviors affect all endpoints globally, service behaviors affect only service-related aspects, endpoint behaviors affect only endpoint-related properties, and operation-level behaviors affect particular operations.</span></span> <span data-ttu-id="a2a64-169">Na przykład, jedno zachowanie usługi ma ograniczenia, które określa, jak działa usługa, gdy nadmiarowe komunikaty grożą zaprzeciążeniem możliwości obsługi.</span><span class="sxs-lookup"><span data-stu-id="a2a64-169">For example, one service behavior is throttling, which specifies how a service reacts when an excess of messages threaten to overwhelm its handling capabilities.</span></span> <span data-ttu-id="a2a64-170">Zachowanie punktu końcowego, z drugiej strony, kontroluje tylko aspekty, które są istotne dla punktów końcowych, takich jak i gdzie można znaleźć poświadczenia zabezpieczeń.</span><span class="sxs-lookup"><span data-stu-id="a2a64-170">An endpoint behavior, on the other hand, controls only aspects that are relevant to endpoints, such as how and where to find a security credential.</span></span>

<span data-ttu-id="a2a64-171">**Powiązania dostarczone przez system**</span><span class="sxs-lookup"><span data-stu-id="a2a64-171">**System-provided bindings**</span></span>  
 <span data-ttu-id="a2a64-172">Program WCF zawiera wiele powiązań dostarczonych przez system.</span><span class="sxs-lookup"><span data-stu-id="a2a64-172">WCF includes a number of system-provided bindings.</span></span> <span data-ttu-id="a2a64-173">Są to kolekcje elementów powiązania, które są zoptymalizowane pod kątem określonych scenariuszy.</span><span class="sxs-lookup"><span data-stu-id="a2a64-173">These are collections of binding elements that are optimized for specific scenarios.</span></span> <span data-ttu-id="a2a64-174">Na przykład <xref:System.ServiceModel.WSHttpBinding> jest zaprojektowana pod kątem współdziałania z usługami, które implementują różne specyfikacje WS-\*.</span><span class="sxs-lookup"><span data-stu-id="a2a64-174">For example, the <xref:System.ServiceModel.WSHttpBinding> is designed for interoperability with services that implement various WS-\* specifications.</span></span> <span data-ttu-id="a2a64-175">Te wstępnie zdefiniowane powiązania oszczędzają czas, wyświetlając tylko te opcje, które można prawidłowo zastosować do konkretnego scenariusza.</span><span class="sxs-lookup"><span data-stu-id="a2a64-175">These predefined bindings save time by presenting only those options that can be correctly applied to the specific scenario.</span></span> <span data-ttu-id="a2a64-176">Jeśli wstępnie zdefiniowane powiązanie nie spełnia wymagań użytkownika, można utworzyć własne niestandardowe powiązania.</span><span class="sxs-lookup"><span data-stu-id="a2a64-176">If a predefined binding does not meet your requirements, you can create your own custom binding.</span></span>

<span data-ttu-id="a2a64-177">**Konfiguracja a kodowanie**</span><span class="sxs-lookup"><span data-stu-id="a2a64-177">**Configuration versus coding**</span></span>  
 <span data-ttu-id="a2a64-178">Kontrolę nad aplikacją można wykonać za pomocą kodowania, za pomocą konfiguracji lub kombinacji obu tych elementów.</span><span class="sxs-lookup"><span data-stu-id="a2a64-178">Control of an application can be done either through coding, through configuration, or through a combination of both.</span></span> <span data-ttu-id="a2a64-179">W konfiguracji można umożliwić innym osobom niż deweloper (na przykład administratora sieci) Ustawianie parametrów klienta i usługi po napisaniu kodu i bez konieczności ponownego kompilowania.</span><span class="sxs-lookup"><span data-stu-id="a2a64-179">Configuration has the advantage of allowing someone other than the developer (for example, a network administrator) to set client and service parameters after the code is written and without having to recompile.</span></span> <span data-ttu-id="a2a64-180">Konfiguracja umożliwia nie tylko Ustawianie wartości takich jak adresy punktów końcowych, ale również umożliwia dalsze kontrolowanie, umożliwiając dodawanie punktów końcowych, powiązań i zachowań.</span><span class="sxs-lookup"><span data-stu-id="a2a64-180">Configuration not only enables you to set values like endpoint addresses, but also allows further control by enabling you to add endpoints, bindings, and behaviors.</span></span> <span data-ttu-id="a2a64-181">Kodowanie umożliwia deweloperowi zachowanie ścisłej kontroli nad wszystkimi składnikami usługi lub klienta, a także wszelkie ustawienia wykonywane przez konfigurację, a w razie potrzeby przesłonięte przez kod.</span><span class="sxs-lookup"><span data-stu-id="a2a64-181">Coding allows the developer to retain strict control over all components of the service or client, and any settings done through the configuration can be inspected and if needed overridden by the code.</span></span>

<span data-ttu-id="a2a64-182">**Operacja usługi**</span><span class="sxs-lookup"><span data-stu-id="a2a64-182">**Service operation**</span></span>  
 <span data-ttu-id="a2a64-183">Procedura zdefiniowana w kodzie usługi, która implementuje funkcje dla operacji.</span><span class="sxs-lookup"><span data-stu-id="a2a64-183">A procedure defined in a service's code that implements the functionality for an operation.</span></span> <span data-ttu-id="a2a64-184">Ta operacja jest udostępniana klientom jako metody na kliencie WCF.</span><span class="sxs-lookup"><span data-stu-id="a2a64-184">This operation is exposed to clients as methods on a WCF client.</span></span> <span data-ttu-id="a2a64-185">Metoda może zwracać wartość i może przyjmować opcjonalną liczbę argumentów lub nie przyjmować argumentów i nie zwraca odpowiedzi.</span><span class="sxs-lookup"><span data-stu-id="a2a64-185">The method can return a value, and can take an optional number of arguments, or take no arguments, and return no response.</span></span> <span data-ttu-id="a2a64-186">Na przykład operacja, która działa jako prosta "Hello", może służyć jako powiadomienie o obecności klienta i rozpocząć serię operacji.</span><span class="sxs-lookup"><span data-stu-id="a2a64-186">For example, an operation that functions as a simple "Hello" can be used as a notification of a client's presence and to begin a series of operations.</span></span>

<span data-ttu-id="a2a64-187">**Kontrakt usługi**</span><span class="sxs-lookup"><span data-stu-id="a2a64-187">**Service contract**</span></span>  
 <span data-ttu-id="a2a64-188">Łączy wiele operacji związanych z pojedynczą jednostką funkcjonalną.</span><span class="sxs-lookup"><span data-stu-id="a2a64-188">Ties together multiple related operations into a single functional unit.</span></span> <span data-ttu-id="a2a64-189">Kontrakt może definiować ustawienia poziomu usługi, takie jak przestrzeń nazw usługi, odpowiedni kontrakt wywołania zwrotnego i inne ustawienia.</span><span class="sxs-lookup"><span data-stu-id="a2a64-189">The contract can define service-level settings, such as the namespace of the service, a corresponding callback contract, and other such settings.</span></span> <span data-ttu-id="a2a64-190">W większości przypadków umowa jest definiowana przez utworzenie interfejsu w wybranym języku programowania i zastosowanie atrybutu <xref:System.ServiceModel.ServiceContractAttribute> do interfejsu.</span><span class="sxs-lookup"><span data-stu-id="a2a64-190">In most cases, the contract is defined by creating an interface in the programming language of your choice and applying the <xref:System.ServiceModel.ServiceContractAttribute> attribute to the interface.</span></span> <span data-ttu-id="a2a64-191">Rzeczywisty kod usługi powstaje przez implementację interfejsu.</span><span class="sxs-lookup"><span data-stu-id="a2a64-191">The actual service code results by implementing the interface.</span></span>

<span data-ttu-id="a2a64-192">**Kontrakt operacji**</span><span class="sxs-lookup"><span data-stu-id="a2a64-192">**Operation contract**</span></span>  
 <span data-ttu-id="a2a64-193">Kontrakt operacji definiuje parametry i zwracany typ operacji.</span><span class="sxs-lookup"><span data-stu-id="a2a64-193">An operation contract defines the parameters and return type of an operation.</span></span> <span data-ttu-id="a2a64-194">Podczas tworzenia interfejsu, który definiuje kontrakt usługi, należy wykonać kontrakt operacji, stosując atrybut <xref:System.ServiceModel.OperationContractAttribute> do każdej definicji metody, która jest częścią kontraktu.</span><span class="sxs-lookup"><span data-stu-id="a2a64-194">When creating an interface that defines the service contract, you signify an operation contract by applying the <xref:System.ServiceModel.OperationContractAttribute> attribute to each method definition that is part of the contract.</span></span> <span data-ttu-id="a2a64-195">Operacje mogą być modelowane jako przejmowanie pojedynczej wiadomości i zwracanie pojedynczej wiadomości lub jako zestawu typów i zwracanie typu.</span><span class="sxs-lookup"><span data-stu-id="a2a64-195">The operations can be modeled as taking a single message and returning a single message, or as taking a set of types and returning a type.</span></span> <span data-ttu-id="a2a64-196">W tym drugim przypadku system określi Format komunikatów, które muszą być wymieniane dla tej operacji.</span><span class="sxs-lookup"><span data-stu-id="a2a64-196">In the latter case, the system will determine the format for the messages that need to be exchanged for that operation.</span></span>

<span data-ttu-id="a2a64-197">**Kontrakt komunikatu**</span><span class="sxs-lookup"><span data-stu-id="a2a64-197">**Message contract**</span></span>  
 <span data-ttu-id="a2a64-198">Opisuje format wiadomości.</span><span class="sxs-lookup"><span data-stu-id="a2a64-198">Describes the format of a message.</span></span> <span data-ttu-id="a2a64-199">Na przykład deklaruje, czy elementy wiadomości powinny znajdować się w nagłówkach, a treści, jakiego poziomu zabezpieczeń należy zastosować do elementów tego komunikatu itd.</span><span class="sxs-lookup"><span data-stu-id="a2a64-199">For example, it declares whether message elements should go in headers versus the body, what level of security should be applied to what elements of the message, and so on.</span></span>

<span data-ttu-id="a2a64-200">**Umowa dotycząca błędów**</span><span class="sxs-lookup"><span data-stu-id="a2a64-200">**Fault contract**</span></span>  
 <span data-ttu-id="a2a64-201">Można skojarzyć z operacją usługi, aby zauważyć błędy, które mogą zostać zwrócone do obiektu wywołującego.</span><span class="sxs-lookup"><span data-stu-id="a2a64-201">Can be associated with a service operation to denote errors that can be returned to the caller.</span></span> <span data-ttu-id="a2a64-202">Z operacją może być skojarzonych zero lub więcej błędów.</span><span class="sxs-lookup"><span data-stu-id="a2a64-202">An operation can have zero or more faults associated with it.</span></span> <span data-ttu-id="a2a64-203">Te błędy są błędami protokołu SOAP, które są modelowane jako wyjątki w modelu programowania.</span><span class="sxs-lookup"><span data-stu-id="a2a64-203">These errors are SOAP faults that are modeled as exceptions in the programming model.</span></span>

<span data-ttu-id="a2a64-204">**Kontrakt danych**</span><span class="sxs-lookup"><span data-stu-id="a2a64-204">**Data contract**</span></span>  
 <span data-ttu-id="a2a64-205">Opisy w metadanych typów danych używanych przez usługę.</span><span class="sxs-lookup"><span data-stu-id="a2a64-205">The descriptions in metadata of the data types that a service uses.</span></span> <span data-ttu-id="a2a64-206">Dzięki temu inne osoby mogą współdziałać z usługą.</span><span class="sxs-lookup"><span data-stu-id="a2a64-206">This enables others to interoperate with the service.</span></span> <span data-ttu-id="a2a64-207">Typy danych mogą być używane w dowolnej części komunikatu, na przykład jako parametry lub typy zwracane.</span><span class="sxs-lookup"><span data-stu-id="a2a64-207">The data types can be used in any part of a message, for example, as parameters or return types.</span></span> <span data-ttu-id="a2a64-208">Jeśli usługa używa tylko typów prostych, nie ma potrzeby jawnego używania kontraktów danych.</span><span class="sxs-lookup"><span data-stu-id="a2a64-208">If the service is using only simple types, there is no need to explicitly use data contracts.</span></span>

<span data-ttu-id="a2a64-209">**Hosting**</span><span class="sxs-lookup"><span data-stu-id="a2a64-209">**Hosting**</span></span>  
 <span data-ttu-id="a2a64-210">Usługa musi być hostowana w pewnym procesie.</span><span class="sxs-lookup"><span data-stu-id="a2a64-210">A service must be hosted in some process.</span></span> <span data-ttu-id="a2a64-211">_Host_ to aplikacja, która kontroluje okres istnienia usługi.</span><span class="sxs-lookup"><span data-stu-id="a2a64-211">A _host_ is an application that controls the lifetime of the service.</span></span> <span data-ttu-id="a2a64-212">Usługi mogą być samoobsługowe lub zarządzane przez istniejący proces hostingu.</span><span class="sxs-lookup"><span data-stu-id="a2a64-212">Services can be self-hosted or managed by an existing hosting process.</span></span>

<span data-ttu-id="a2a64-213">**Usługa samodzielna**</span><span class="sxs-lookup"><span data-stu-id="a2a64-213">**Self-hosted service**</span></span>  
 <span data-ttu-id="a2a64-214">Usługa działająca w ramach aplikacji przetwarzanej utworzonej przez dewelopera.</span><span class="sxs-lookup"><span data-stu-id="a2a64-214">A service that runs within a process application that the developer created.</span></span> <span data-ttu-id="a2a64-215">Deweloper kontroluje swój okres istnienia, ustawia właściwości usługi, otwiera usługę (która ustawia ją w tryb nasłuchiwania) i zamyka usługę.</span><span class="sxs-lookup"><span data-stu-id="a2a64-215">The developer controls its lifetime, sets the properties of the service, opens the service (which sets it into a listening mode), and closes the service.</span></span>

<span data-ttu-id="a2a64-216">**Proces hostingu**</span><span class="sxs-lookup"><span data-stu-id="a2a64-216">**Hosting process**</span></span>  
 <span data-ttu-id="a2a64-217">Aplikacja, która jest przeznaczona do hostowania usług.</span><span class="sxs-lookup"><span data-stu-id="a2a64-217">An application that is designed to host services.</span></span> <span data-ttu-id="a2a64-218">Obejmują one Internet Information Services (IIS), usługi aktywacji systemu Windows (WAS) i usługi systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="a2a64-218">These include Internet Information Services (IIS), Windows Activation Services (WAS), and Windows Services.</span></span> <span data-ttu-id="a2a64-219">W tych scenariuszach hostowanych Host kontroluje okres istnienia usługi.</span><span class="sxs-lookup"><span data-stu-id="a2a64-219">In these hosted scenarios, the host controls the lifetime of the service.</span></span> <span data-ttu-id="a2a64-220">Na przykład przy użyciu usług IIS można skonfigurować katalog wirtualny, który zawiera zestaw usługi i plik konfiguracji.</span><span class="sxs-lookup"><span data-stu-id="a2a64-220">For example, using IIS you can set up a virtual directory that contains the service assembly and configuration file.</span></span> <span data-ttu-id="a2a64-221">Po odebraniu komunikatu Usługa IIS uruchamia usługę i kontroluje jej okres istnienia.</span><span class="sxs-lookup"><span data-stu-id="a2a64-221">When a message is received, IIS starts the service and controls its lifetime.</span></span>

<span data-ttu-id="a2a64-222">**Tworzenie wystąpienia**</span><span class="sxs-lookup"><span data-stu-id="a2a64-222">**Instancing**</span></span>  
 <span data-ttu-id="a2a64-223">Usługa ma model wystąpień.</span><span class="sxs-lookup"><span data-stu-id="a2a64-223">A service has an instancing model.</span></span> <span data-ttu-id="a2a64-224">Istnieją trzy modele wystąpienia: "Single", w których pojedynczej usłudze obiektów CLR wszyscy klienci; " na wywołanie "w którym tworzony jest nowy obiekt CLR w celu obsługi każdego wywołania klienta; i "na sesję", w której tworzony jest zestaw obiektów CLR, po jednej dla każdej oddzielnej sesji.</span><span class="sxs-lookup"><span data-stu-id="a2a64-224">There are three instancing models: "single," in which a single CLR object services all the clients; "per call," in which a new CLR object is created to handle each client call; and "per session," in which a set of CLR objects is created, one for each separate session.</span></span> <span data-ttu-id="a2a64-225">Wybór modelu wystąpienia zależy od wymagań aplikacji i oczekiwanego wzorca użycia usługi.</span><span class="sxs-lookup"><span data-stu-id="a2a64-225">The choice of an instancing model depends on the application requirements and the expected usage pattern of the service.</span></span>

<span data-ttu-id="a2a64-226">**Aplikacja kliencka**</span><span class="sxs-lookup"><span data-stu-id="a2a64-226">**Client application**</span></span>  
 <span data-ttu-id="a2a64-227">Program, który wymienia komunikaty z co najmniej jednym elementem końcowym.</span><span class="sxs-lookup"><span data-stu-id="a2a64-227">A program that exchanges messages with one or more endpoints.</span></span> <span data-ttu-id="a2a64-228">Aplikacja kliencka rozpoczyna się od utworzenia wystąpienia klienta WCF i wywoływania metod klienta programu WCF.</span><span class="sxs-lookup"><span data-stu-id="a2a64-228">The client application begins by creating an instance of a WCF client and calling methods of the WCF client.</span></span> <span data-ttu-id="a2a64-229">Należy pamiętać, że pojedyncza aplikacja może być zarówno klientem, jak i usługą.</span><span class="sxs-lookup"><span data-stu-id="a2a64-229">It is important to note that a single application can be both a client and a service.</span></span>

<span data-ttu-id="a2a64-230">**Channel**</span><span class="sxs-lookup"><span data-stu-id="a2a64-230">**Channel**</span></span>  
 <span data-ttu-id="a2a64-231">Konkretna implementacja elementu powiązania.</span><span class="sxs-lookup"><span data-stu-id="a2a64-231">A concrete implementation of a binding element.</span></span> <span data-ttu-id="a2a64-232">Powiązanie reprezentuje konfigurację, a kanał jest implementacją skojarzoną z tą konfiguracją.</span><span class="sxs-lookup"><span data-stu-id="a2a64-232">The binding represents the configuration, and the channel is the implementation associated with that configuration.</span></span> <span data-ttu-id="a2a64-233">W związku z tym istnieje kanał skojarzony z każdym elementem powiązania.</span><span class="sxs-lookup"><span data-stu-id="a2a64-233">Therefore, there is a channel associated with each binding element.</span></span> <span data-ttu-id="a2a64-234">Stos kanałów na siebie, aby utworzyć konkretną implementację powiązania: stos kanałów.</span><span class="sxs-lookup"><span data-stu-id="a2a64-234">Channels stack on top of each other to create the concrete implementation of the binding: the channel stack.</span></span>

<span data-ttu-id="a2a64-235">**Klient WCF**</span><span class="sxs-lookup"><span data-stu-id="a2a64-235">**WCF client**</span></span>  
 <span data-ttu-id="a2a64-236">Konstrukcja klient-aplikacja, która uwidacznia operacje usługi jako metody (w wybranym języku programowania .NET Framework, takich jak Visual Basic lub Wizualizacja C#).</span><span class="sxs-lookup"><span data-stu-id="a2a64-236">A client-application construct that exposes the service operations as methods (in the .NET Framework programming language of your choice, such as Visual Basic or Visual C#).</span></span> <span data-ttu-id="a2a64-237">Każda aplikacja może obsługiwać klienta WCF, w tym aplikację, która hostuje usługę.</span><span class="sxs-lookup"><span data-stu-id="a2a64-237">Any application can host a WCF client, including an application that hosts a service.</span></span> <span data-ttu-id="a2a64-238">W związku z tym można utworzyć usługę, która obejmuje klientów programu WCF innych usług.</span><span class="sxs-lookup"><span data-stu-id="a2a64-238">Therefore, it is possible to create a service that includes WCF clients of other services.</span></span>

<span data-ttu-id="a2a64-239">Klienta programu WCF można automatycznie wygenerować przy użyciu narzędzia do obsługi [metadanych ServiceModel (Svcutil. exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) i wskazać w uruchomionej usłudze, która publikuje metadane.</span><span class="sxs-lookup"><span data-stu-id="a2a64-239">A WCF client can be automatically generated by using the [ServiceModel Metadata Utility Tool (Svcutil.exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) and pointing it at a running service that publishes metadata.</span></span>

<span data-ttu-id="a2a64-240">**Metadane**</span><span class="sxs-lookup"><span data-stu-id="a2a64-240">**Metadata**</span></span>  
 <span data-ttu-id="a2a64-241">W usłudze program opisuje charakterystykę usługi, którą zewnętrzna jednostka musi zrozumieć, aby komunikować się z usługą.</span><span class="sxs-lookup"><span data-stu-id="a2a64-241">In a service, describes the characteristics of the service that an external entity needs to understand to communicate with the service.</span></span> <span data-ttu-id="a2a64-242">Metadane mogą być używane przez narzędzie do obsługi [metadanych ServiceModel (Svcutil. exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) w celu wygenerowania klienta programu WCF i dołączonej konfiguracji, która może być używana przez aplikację kliencką do współpracy z usługą.</span><span class="sxs-lookup"><span data-stu-id="a2a64-242">Metadata can be consumed by the [ServiceModel Metadata Utility Tool (Svcutil.exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) to generate a WCF client and accompanying configuration that a client application can use to interact with the service.</span></span>

<span data-ttu-id="a2a64-243">Metadane udostępniane przez usługę obejmują dokumenty schematu XML, które definiują kontrakt danych usługi i dokumenty WSDL opisujące metody usługi.</span><span class="sxs-lookup"><span data-stu-id="a2a64-243">The metadata exposed by the service includes XML schema documents, which define the data contract of the service, and WSDL documents, which describe the methods of the service.</span></span>

<span data-ttu-id="a2a64-244">Po włączeniu tej funkcji metadane usługi są generowane automatycznie przez program WCF przez sprawdzenie usługi i jej punktów końcowych.</span><span class="sxs-lookup"><span data-stu-id="a2a64-244">When enabled, metadata for the service is automatically generated by WCF by inspecting the service and its endpoints.</span></span> <span data-ttu-id="a2a64-245">Aby opublikować metadane z usługi, należy jawnie włączyć zachowanie metadanych.</span><span class="sxs-lookup"><span data-stu-id="a2a64-245">To publish metadata from a service, you must explicitly enable the metadata behavior.</span></span>

<span data-ttu-id="a2a64-246">**Security**</span><span class="sxs-lookup"><span data-stu-id="a2a64-246">**Security**</span></span>  
 <span data-ttu-id="a2a64-247">W programie WCF obejmuje poufność (szyfrowanie komunikatów, aby zapobiec podsłuchiwaniu), integralność (środki na potrzeby wykrywania manipulowania komunikatem), uwierzytelnianie (sposób sprawdzania poprawności serwerów i klientów) oraz autoryzację (kontrolę dostępu do zasoby).</span><span class="sxs-lookup"><span data-stu-id="a2a64-247">In WCF, includes confidentiality (encryption of messages to prevent eavesdropping), integrity (the means for detection of tampering with the message), authentication (the means for validation of servers and clients), and authorization (the control of access to resources).</span></span> <span data-ttu-id="a2a64-248">Te funkcje są udostępniane przy użyciu istniejących mechanizmów zabezpieczeń, takich jak TLS przez HTTP (znany również jako HTTPS) lub przez implementację co najmniej jednej z różnych specyfikacji zabezpieczeń WS-\*.</span><span class="sxs-lookup"><span data-stu-id="a2a64-248">These functions are provided by either leveraging existing security mechanisms, such as TLS over HTTP (also known as HTTPS), or by implementing one or more of the various WS-\* security specifications.</span></span>

<span data-ttu-id="a2a64-249">**Tryb zabezpieczeń transportu**</span><span class="sxs-lookup"><span data-stu-id="a2a64-249">**Transport security mode**</span></span>  
 <span data-ttu-id="a2a64-250">Określa, że poufność, integralność i uwierzytelnianie są udostępniane przez mechanizmy warstwy transportowej (takie jak HTTPS).</span><span class="sxs-lookup"><span data-stu-id="a2a64-250">Specifies that confidentiality, integrity, and authentication are provided by the transport layer mechanisms (such as HTTPS).</span></span> <span data-ttu-id="a2a64-251">W przypadku korzystania z transportu, takiego jak HTTPS, ten tryb ma zaletę wydajnej wydajności i jest zrozumiały ze względu na jego powszechność w Internecie.</span><span class="sxs-lookup"><span data-stu-id="a2a64-251">When using a transport like HTTPS, this mode has the advantage of being efficient in its performance, and well understood because of its prevalence on the Internet.</span></span> <span data-ttu-id="a2a64-252">Wadą jest to, że ten rodzaj zabezpieczeń jest stosowany osobno dla każdego przeskoku w ścieżce komunikacji, co sprawia, że komunikacja jest podatna na ataki "man in the middle".</span><span class="sxs-lookup"><span data-stu-id="a2a64-252">The disadvantage is that this kind of security is applied separately on each hop in the communication path, making the communication susceptible to a "man in the middle" attack.</span></span>

<span data-ttu-id="a2a64-253">**Tryb zabezpieczeń wiadomości**</span><span class="sxs-lookup"><span data-stu-id="a2a64-253">**Message security mode**</span></span>  
 <span data-ttu-id="a2a64-254">Określa, że zabezpieczenia są zapewniane przez implementację co najmniej jednej specyfikacji zabezpieczeń, takiej jak specyfikacja o nazwie [zabezpieczenia usług w sieci Web: zabezpieczenia komunikatów protokołu SOAP](https://go.microsoft.com/fwlink/?LinkId=94684).</span><span class="sxs-lookup"><span data-stu-id="a2a64-254">Specifies that security is provided by implementing one or more of the security specifications, such as the specification named [Web Services Security: SOAP Message Security](https://go.microsoft.com/fwlink/?LinkId=94684).</span></span> <span data-ttu-id="a2a64-255">Każdy komunikat zawiera niezbędne mechanizmy zapewnienia bezpieczeństwa w trakcie jego przesyłania oraz umożliwienie odbiornikom wykrywania naruszenia i odszyfrowywania wiadomości.</span><span class="sxs-lookup"><span data-stu-id="a2a64-255">Each message contains the necessary mechanisms to provide security during its transit, and to enable the receivers to detect tampering and to decrypt the messages.</span></span> <span data-ttu-id="a2a64-256">W tym sensie zabezpieczenia są hermetyzowane w każdym komunikacie, zapewniając kompleksowe zabezpieczenia dla wielu przeskoków.</span><span class="sxs-lookup"><span data-stu-id="a2a64-256">In this sense, the security is encapsulated within every message, providing end-to-end security across multiple hops.</span></span> <span data-ttu-id="a2a64-257">Ze względu na to, że informacje o zabezpieczeniach staną się częścią komunikatu, można również uwzględnić wiele rodzajów poświadczeń z komunikatem (są one określane jako _oświadczenia_).</span><span class="sxs-lookup"><span data-stu-id="a2a64-257">Because security information becomes part of the message, it is also possible to include multiple kinds of credentials with the message (these are referred to as _claims_).</span></span> <span data-ttu-id="a2a64-258">Takie podejście umożliwia również umożliwienie bezpiecznego przesyłania komunikatów w ramach dowolnego transportu, w tym wielu transportów między jej źródłem i miejscem docelowym.</span><span class="sxs-lookup"><span data-stu-id="a2a64-258">This approach also has the advantage of enabling the message to travel securely over any transport, including multiple transports between its origin and destination.</span></span> <span data-ttu-id="a2a64-259">Wadą tej metody jest złożoność stosowanych mechanizmów kryptograficznych, co skutkuje wpływem na wydajność.</span><span class="sxs-lookup"><span data-stu-id="a2a64-259">The disadvantage of this approach is the complexity of the cryptographic mechanisms employed, resulting in performance implications.</span></span>

<span data-ttu-id="a2a64-260">**Transport z użyciem poświadczeń komunikatów tryb zabezpieczeń**</span><span class="sxs-lookup"><span data-stu-id="a2a64-260">**Transport with message credential security mode**</span></span>  
 <span data-ttu-id="a2a64-261">Określa użycie warstwy transportowej w celu zapewnienia poufności, uwierzytelniania i integralności komunikatów, natomiast każdy komunikat może zawierać wiele poświadczeń (oświadczeń) wymaganych przez odbiorniki wiadomości.</span><span class="sxs-lookup"><span data-stu-id="a2a64-261">Specifies the use of the transport layer to provide confidentiality, authentication, and integrity of the messages, while each of the messages can contain multiple credentials (claims) required by the receivers of the message.</span></span>

<span data-ttu-id="a2a64-262">**WS-\***</span><span class="sxs-lookup"><span data-stu-id="a2a64-262">**WS-\***</span></span>  
 <span data-ttu-id="a2a64-263">Skrót dla rosnącego zestawu specyfikacji usługi sieci Web (WS), takich jak WS-Security, WS-ReliableMessaging i tak dalej, które są implementowane w programie WCF.</span><span class="sxs-lookup"><span data-stu-id="a2a64-263">Shorthand for the growing set of Web Service (WS) specifications, such as WS-Security, WS-ReliableMessaging, and so on, that are implemented in WCF.</span></span>

## <a name="see-also"></a><span data-ttu-id="a2a64-264">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="a2a64-264">See also</span></span>

- [<span data-ttu-id="a2a64-265">Co to jest program Windows Communication Foundation</span><span class="sxs-lookup"><span data-stu-id="a2a64-265">What Is Windows Communication Foundation</span></span>](whats-wcf.md)
- [<span data-ttu-id="a2a64-266">Architektura WCF (Windows Communication Foundation)</span><span class="sxs-lookup"><span data-stu-id="a2a64-266">Windows Communication Foundation Architecture</span></span>](architecture.md)
