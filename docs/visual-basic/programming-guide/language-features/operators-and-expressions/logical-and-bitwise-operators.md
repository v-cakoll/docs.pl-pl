---
title: Operatory logiczne i bitowe
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: 19d3511363f19319c2bd8ce872b62c1462b1370f
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/04/2020
ms.locfileid: "84403411"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="4cc34-102">Operatory logiczne i bitowe w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="4cc34-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="4cc34-103">Operatory logiczne porównują `Boolean` wyrażenia i zwracają `Boolean` wynik.</span><span class="sxs-lookup"><span data-stu-id="4cc34-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="4cc34-104">`And`Operatory, `Or` , `AndAlso` , `OrElse` i `Xor` są *binarne* , ponieważ przyjmują dwa operandy, natomiast `Not` operator jest *jednoargumentowy* , ponieważ przyjmuje jeden operand.</span><span class="sxs-lookup"><span data-stu-id="4cc34-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="4cc34-105">Niektóre z tych operatorów mogą również wykonywać bitowe operacje logiczne na wartościach całkowitych.</span><span class="sxs-lookup"><span data-stu-id="4cc34-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="4cc34-106">Jednoargumentowy operator logiczny</span><span class="sxs-lookup"><span data-stu-id="4cc34-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="4cc34-107">[Operator not](../../../language-reference/operators/not-operator.md) wykonuje logiczne *Negacja* dla `Boolean` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="4cc34-107">The [Not Operator](../../../language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="4cc34-108">Zwraca logiczne przeciwieństwo operandu.</span><span class="sxs-lookup"><span data-stu-id="4cc34-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="4cc34-109">Jeśli wyrażenie zwróci `True` wartość, a następnie `Not` zwraca `False` ; Jeśli wyrażenie zwróci wartość `False` , a następnie `Not` zwraca `True` .</span><span class="sxs-lookup"><span data-stu-id="4cc34-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="4cc34-110">Ilustruje to poniższy przykład.</span><span class="sxs-lookup"><span data-stu-id="4cc34-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="4cc34-111">Binarne operatory logiczne</span><span class="sxs-lookup"><span data-stu-id="4cc34-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="4cc34-112">[Operator and](../../../language-reference/operators/and-operator.md) *wykonuje koniunkcję logiczną dla* dwóch `Boolean` wyrażeń.</span><span class="sxs-lookup"><span data-stu-id="4cc34-112">The [And Operator](../../../language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="4cc34-113">Jeśli oba wyrażenia są szacowane do `True` , następnie `And` zwraca wartość `True` .</span><span class="sxs-lookup"><span data-stu-id="4cc34-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="4cc34-114">Jeśli co najmniej jedno wyrażenie oblicza wartość `False` , a następnie `And` zwraca `False` .</span><span class="sxs-lookup"><span data-stu-id="4cc34-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="4cc34-115">[Operator or](../../../language-reference/operators/or-operator.md) *wykonuje logiczne* *rozłączenie lub włączenie* dwóch `Boolean` wyrażeń.</span><span class="sxs-lookup"><span data-stu-id="4cc34-115">The [Or Operator](../../../language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="4cc34-116">Jeśli wyrażenie zwróci wartość `True` , lub obie wartości oblicza do `True` , następnie `Or` zwraca wartość `True` .</span><span class="sxs-lookup"><span data-stu-id="4cc34-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="4cc34-117">Jeśli wyrażenie nie zostanie oszacowane `True` , `Or` zwraca `False` .</span><span class="sxs-lookup"><span data-stu-id="4cc34-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="4cc34-118">[Operator XOR](../../../language-reference/operators/xor-operator.md) wykonuje logiczne *wykluczenia* dla dwóch `Boolean` wyrażeń.</span><span class="sxs-lookup"><span data-stu-id="4cc34-118">The [Xor Operator](../../../language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="4cc34-119">Jeśli dokładnie jedno wyrażenie ma wartość `True` , ale nie oba, `Xor` zwraca `True` .</span><span class="sxs-lookup"><span data-stu-id="4cc34-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="4cc34-120">Jeśli oba wyrażenia obliczają wartość `True` lub obie wartości obliczają do `False` , `Xor` zwraca `False` .</span><span class="sxs-lookup"><span data-stu-id="4cc34-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="4cc34-121">Poniższy przykład ilustruje `And` `Or` operatory, i `Xor` .</span><span class="sxs-lookup"><span data-stu-id="4cc34-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="4cc34-122">Operacje logiczne o krótkim obwodzie</span><span class="sxs-lookup"><span data-stu-id="4cc34-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="4cc34-123">[Operator AndAlso](../../../language-reference/operators/andalso-operator.md) jest bardzo podobny do `And` operatora, dzięki czemu wykonuje także koniunkcję logiczną dla dwóch `Boolean` wyrażeń.</span><span class="sxs-lookup"><span data-stu-id="4cc34-123">The [AndAlso Operator](../../../language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="4cc34-124">Kluczową różnicą między tymi dwoma są te, które `AndAlso` wykazuje zachowanie podczas *krótkiego obwodu* .</span><span class="sxs-lookup"><span data-stu-id="4cc34-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="4cc34-125">Jeśli pierwsze wyrażenie w `AndAlso` wyrażeniu zwraca wartość `False` , drugie wyrażenie nie jest oceniane, ponieważ nie może zmienić wyniku końcowego i `AndAlso` zwraca wartość `False` .</span><span class="sxs-lookup"><span data-stu-id="4cc34-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="4cc34-126">Podobnie [operator OrElse](../../../language-reference/operators/orelse-operator.md) wykonuje krótkie, logiczne rozłączenie dwóch `Boolean` wyrażeń.</span><span class="sxs-lookup"><span data-stu-id="4cc34-126">Similarly, the [OrElse Operator](../../../language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="4cc34-127">Jeśli pierwsze wyrażenie w `OrElse` wyrażeniu zwraca wartość `True` , drugie wyrażenie nie jest oceniane, ponieważ nie może zmienić wyniku końcowego i `OrElse` zwraca wartość `True` .</span><span class="sxs-lookup"><span data-stu-id="4cc34-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="4cc34-128">Krótkoterminowe kompromisy</span><span class="sxs-lookup"><span data-stu-id="4cc34-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="4cc34-129">Krótkie obwody mogą zwiększyć wydajność, nie obliczając wyrażenia, które nie może zmienić wyniku operacji logicznej.</span><span class="sxs-lookup"><span data-stu-id="4cc34-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="4cc34-130">Jeśli jednak to wyrażenie wykonuje dodatkowe akcje, krótkie obwody pomija te akcje.</span><span class="sxs-lookup"><span data-stu-id="4cc34-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="4cc34-131">Na przykład jeśli wyrażenie zawiera wywołanie `Function` procedury, ta procedura nie jest wywoływana, jeśli wyrażenie jest krótkie, a jakikolwiek dodatkowy kod zawarty w nie zostanie `Function` uruchomiony.</span><span class="sxs-lookup"><span data-stu-id="4cc34-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="4cc34-132">W związku z tym funkcja może działać tylko sporadycznie i może nie być testowana prawidłowo.</span><span class="sxs-lookup"><span data-stu-id="4cc34-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="4cc34-133">Lub logika programu może zależeć od kodu w `Function` .</span><span class="sxs-lookup"><span data-stu-id="4cc34-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="4cc34-134">Poniższy przykład ilustruje różnicę między elementami `And` , `Or` i ich krótkimi odpowiednikami.</span><span class="sxs-lookup"><span data-stu-id="4cc34-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="4cc34-135">W poprzednim przykładzie należy zauważyć, że jakiś istotny kod wewnątrz `checkIfValid()` nie jest uruchamiany, gdy wywołanie jest krótkie.</span><span class="sxs-lookup"><span data-stu-id="4cc34-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="4cc34-136">Pierwsza `If` instrukcja wywołuje `checkIfValid()` choć `12 > 45` Returns `False` , ponieważ nie `And` jest krótka obwód.</span><span class="sxs-lookup"><span data-stu-id="4cc34-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="4cc34-137">Druga `If` instrukcja nie wywołuje `checkIfValid()` , ponieważ gdy `12 > 45` zwraca `False` , `AndAlso` krótkie obwody drugie wyrażenie.</span><span class="sxs-lookup"><span data-stu-id="4cc34-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="4cc34-138">Trzecia `If` instrukcja wywołuje `checkIfValid()` choć `12 < 45` Returns `True` , ponieważ nie `Or` jest krótkim obwodem.</span><span class="sxs-lookup"><span data-stu-id="4cc34-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="4cc34-139">Czwarta `If` instrukcja nie wywołuje metody `checkIfValid()` , ponieważ gdy `12 < 45` zwraca `True` , `OrElse` krótkie obwody drugie wyrażenie.</span><span class="sxs-lookup"><span data-stu-id="4cc34-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="4cc34-140">Operacje bitowe</span><span class="sxs-lookup"><span data-stu-id="4cc34-140">Bitwise Operations</span></span>  
 <span data-ttu-id="4cc34-141">Operacje bitowe obliczają dwie wartości całkowite w postaci binarnej (podstawowej 2) formularza.</span><span class="sxs-lookup"><span data-stu-id="4cc34-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="4cc34-142">Porównują bity w odpowiednich pozycjach, a następnie przypisują wartości na podstawie porównania.</span><span class="sxs-lookup"><span data-stu-id="4cc34-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="4cc34-143">Poniższy przykład ilustruje `And` operatora.</span><span class="sxs-lookup"><span data-stu-id="4cc34-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="4cc34-144">Poprzedni przykład ustawia wartość `x` na 1.</span><span class="sxs-lookup"><span data-stu-id="4cc34-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="4cc34-145">Dzieje się tak z następujących powodów:</span><span class="sxs-lookup"><span data-stu-id="4cc34-145">This happens for the following reasons:</span></span>  
  
- <span data-ttu-id="4cc34-146">Wartości są traktowane jako dane binarne:</span><span class="sxs-lookup"><span data-stu-id="4cc34-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="4cc34-147">3 w postaci binarnej = 011</span><span class="sxs-lookup"><span data-stu-id="4cc34-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="4cc34-148">5 w postaci binarnej = 101</span><span class="sxs-lookup"><span data-stu-id="4cc34-148">5 in binary form = 101</span></span>  
  
- <span data-ttu-id="4cc34-149">`And`Operator porównuje reprezentacje binarne, jedną pozycję binarną (bit) jednocześnie.</span><span class="sxs-lookup"><span data-stu-id="4cc34-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="4cc34-150">Jeśli obie bity w danym położeniu są 1, to 1 zostanie umieszczony w tym położeniu w wyniku.</span><span class="sxs-lookup"><span data-stu-id="4cc34-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="4cc34-151">Jeśli jeden bit ma wartość 0, wówczas wartość 0 jest umieszczana w tym położeniu w wyniku.</span><span class="sxs-lookup"><span data-stu-id="4cc34-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="4cc34-152">W poprzednim przykładzie działa to w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="4cc34-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="4cc34-153">011 (3 w postaci binarnej)</span><span class="sxs-lookup"><span data-stu-id="4cc34-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="4cc34-154">101 (5 w postaci binarnej)</span><span class="sxs-lookup"><span data-stu-id="4cc34-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="4cc34-155">001 (wynik, w postaci binarnej)</span><span class="sxs-lookup"><span data-stu-id="4cc34-155">001 (The result, in binary form)</span></span>  
  
- <span data-ttu-id="4cc34-156">Wynik jest traktowany jako decimal.</span><span class="sxs-lookup"><span data-stu-id="4cc34-156">The result is treated as decimal.</span></span> <span data-ttu-id="4cc34-157">Wartość 001 jest reprezentacją binarną 1, więc `x` = 1.</span><span class="sxs-lookup"><span data-stu-id="4cc34-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="4cc34-158">Operacja bitowa `Or` jest podobna, z tą różnicą, że wartość 1 jest przypisana do bitu wynik, jeśli jedna lub obie wartości w porównaniu z porównaniem są 1.</span><span class="sxs-lookup"><span data-stu-id="4cc34-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="4cc34-159">`Xor`przypisuje 1 do bitu wynikowego, jeśli dokładnie jeden z porównywanych bitów (nie oba) ma wartość 1.</span><span class="sxs-lookup"><span data-stu-id="4cc34-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="4cc34-160">`Not`przyjmuje jeden operand i odwraca wszystkie bity, w tym bit znaku, i przypisuje tę wartość do wyniku.</span><span class="sxs-lookup"><span data-stu-id="4cc34-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="4cc34-161">Oznacza to, że w przypadku podpisanych liczb dodatnich `Not` zawsze zwraca wartość ujemną, a dla liczb ujemnych `Not` zawsze zwraca wartość dodatnią lub zero.</span><span class="sxs-lookup"><span data-stu-id="4cc34-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="4cc34-162">`AndAlso`Operatory i `OrElse` nie obsługują operacji bitowych.</span><span class="sxs-lookup"><span data-stu-id="4cc34-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="4cc34-163">Operacje bitowe można wykonywać tylko w przypadku typów całkowitych.</span><span class="sxs-lookup"><span data-stu-id="4cc34-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="4cc34-164">Aby można było wykonać operację bitową, wartości zmiennoprzecinkowe muszą być konwertowane na typy całkowite.</span><span class="sxs-lookup"><span data-stu-id="4cc34-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="4cc34-165">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="4cc34-165">See also</span></span>

- [<span data-ttu-id="4cc34-166">Operatory logiczne/bitowe (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="4cc34-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="4cc34-167">Wyrażenia logiczne</span><span class="sxs-lookup"><span data-stu-id="4cc34-167">Boolean Expressions</span></span>](boolean-expressions.md)
- [<span data-ttu-id="4cc34-168">Operatory arytmetyczne w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="4cc34-168">Arithmetic Operators in Visual Basic</span></span>](arithmetic-operators.md)
- [<span data-ttu-id="4cc34-169">Operatory porównania w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="4cc34-169">Comparison Operators in Visual Basic</span></span>](comparison-operators.md)
- [<span data-ttu-id="4cc34-170">Operatory łączenia w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="4cc34-170">Concatenation Operators in Visual Basic</span></span>](concatenation-operators.md)
- [<span data-ttu-id="4cc34-171">Skuteczna kombinacja operatorów</span><span class="sxs-lookup"><span data-stu-id="4cc34-171">Efficient Combination of Operators</span></span>](efficient-combination-of-operators.md)
