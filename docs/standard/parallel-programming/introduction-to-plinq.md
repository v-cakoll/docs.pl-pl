---
title: Wprowadzenie do PLINQ
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- PLINQ queries, introduction to
ms.assetid: eaa720d8-8999-4eb7-8df5-3c19ca61cad0
ms.openlocfilehash: e9ef72c2691a4dbb9c68202b29e0f5c77dcdaa74
ms.sourcegitcommit: 961ec21c22d2f1d55c9cc8a7edf2ade1d1fd92e3
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/02/2020
ms.locfileid: "80588183"
---
# <a name="introduction-to-plinq"></a><span data-ttu-id="7275b-102">Wprowadzenie do PLINQ</span><span class="sxs-lookup"><span data-stu-id="7275b-102">Introduction to PLINQ</span></span>

<span data-ttu-id="7275b-103">Parallel LINQ (PLINQ) jest równoległą implementacją wzorca [zapytania zintegrowanego z językiem (LINQ).](../../csharp/programming-guide/concepts/linq/index.md)</span><span class="sxs-lookup"><span data-stu-id="7275b-103">Parallel LINQ (PLINQ) is a parallel implementation of the [Language-Integrated Query (LINQ)](../../csharp/programming-guide/concepts/linq/index.md) pattern.</span></span> <span data-ttu-id="7275b-104">PLINQ implementuje pełny zestaw operatorów zapytań standardowych <xref:System.Linq> LINQ jako metody rozszerzenia dla obszaru nazw i ma dodatkowe operatory dla operacji równoległych.</span><span class="sxs-lookup"><span data-stu-id="7275b-104">PLINQ implements the full set of LINQ standard query operators as extension methods for the <xref:System.Linq> namespace and has additional operators for parallel operations.</span></span> <span data-ttu-id="7275b-105">PLINQ łączy prostotę i czytelność składni LINQ z mocą programowania równoległego.</span><span class="sxs-lookup"><span data-stu-id="7275b-105">PLINQ combines the simplicity and readability of LINQ syntax with the power of parallel programming.</span></span>

> [!TIP]
> <span data-ttu-id="7275b-106">Jeśli nie jesteś zaznajomiony z LINQ, posiada ujednolicony model do wykonywania zapytań dowolnego źródła danych wyliczalne w sposób bezpieczny dla typu.</span><span class="sxs-lookup"><span data-stu-id="7275b-106">If you're not familiar with LINQ, it features a unified model for querying any enumerable data source in a type-safe manner.</span></span> <span data-ttu-id="7275b-107">LINQ do obiektów jest nazwą dla zapytań LINQ, które są <xref:System.Collections.Generic.List%601> uruchamiane względem kolekcji w pamięci, takich jak i tablice.</span><span class="sxs-lookup"><span data-stu-id="7275b-107">LINQ to Objects is the name for LINQ queries that are run against in-memory collections such as <xref:System.Collections.Generic.List%601> and arrays.</span></span> <span data-ttu-id="7275b-108">W tym artykule przyjęto założenie, że masz podstawową wiedzę linq.</span><span class="sxs-lookup"><span data-stu-id="7275b-108">This article assumes that you have a basic understanding of LINQ.</span></span> <span data-ttu-id="7275b-109">Aby uzyskać więcej informacji, zobacz [Zapytanie zintegrowane z językiem (LINQ)](../../csharp/programming-guide/concepts/linq/index.md).</span><span class="sxs-lookup"><span data-stu-id="7275b-109">For more information, see [Language-Integrated Query (LINQ)](../../csharp/programming-guide/concepts/linq/index.md).</span></span>

## <a name="what-is-a-parallel-query"></a><span data-ttu-id="7275b-110">Co to jest kwerenda równoległa?</span><span class="sxs-lookup"><span data-stu-id="7275b-110">What is a Parallel query?</span></span>

<span data-ttu-id="7275b-111">Kwerenda PLINQ pod wieloma względami przypomina niedlaka linq do kwerendy obiektów.</span><span class="sxs-lookup"><span data-stu-id="7275b-111">A PLINQ query in many ways resembles a non-parallel LINQ to Objects query.</span></span> <span data-ttu-id="7275b-112">Zapytania PLINQ, podobnie jak sekwencyjne zapytania LINQ, działają <xref:System.Collections.IEnumerable> <xref:System.Collections.Generic.IEnumerable%601> na dowolnym źródle w pamięci lub źródła danych i mają odroczone wykonanie, co oznacza, że nie rozpoczynają wykonywania, dopóki kwerenda nie zostanie wyliczona.</span><span class="sxs-lookup"><span data-stu-id="7275b-112">PLINQ queries, just like sequential LINQ queries, operate on any in-memory <xref:System.Collections.IEnumerable> or <xref:System.Collections.Generic.IEnumerable%601> data source, and have deferred execution, which means they do not begin executing until the query is enumerated.</span></span> <span data-ttu-id="7275b-113">Podstawową różnicą jest to, że PLINQ próbuje w pełni wykorzystać wszystkie procesory w systemie.</span><span class="sxs-lookup"><span data-stu-id="7275b-113">The primary difference is that PLINQ attempts to make full use of all the processors on the system.</span></span> <span data-ttu-id="7275b-114">Robi to przez partycjonowanie źródła danych na segmenty, a następnie wykonywanie kwerendy w każdym segmencie na oddzielnych wątków roboczych równolegle na wielu procesorach.</span><span class="sxs-lookup"><span data-stu-id="7275b-114">It does this by partitioning the data source into segments, and then executing the query on each segment on separate worker threads in parallel on multiple processors.</span></span> <span data-ttu-id="7275b-115">W wielu przypadkach wykonanie równoległe oznacza, że kwerenda działa znacznie szybciej.</span><span class="sxs-lookup"><span data-stu-id="7275b-115">In many cases, parallel execution means that the query runs significantly faster.</span></span>

<span data-ttu-id="7275b-116">Dzięki wykonaniu równoległemu PLINQ można osiągnąć znaczną poprawę wydajności w stosunku do <xref:System.Linq.ParallelEnumerable.AsParallel%2A> starszego kodu dla niektórych rodzajów zapytań, często po prostu dodając operację kwerendy do źródła danych.</span><span class="sxs-lookup"><span data-stu-id="7275b-116">Through parallel execution, PLINQ can achieve significant performance improvements over legacy code for certain kinds of queries, often just by adding the <xref:System.Linq.ParallelEnumerable.AsParallel%2A> query operation to the data source.</span></span> <span data-ttu-id="7275b-117">Jednak równoległość może wprowadzić własne złożoności i nie wszystkie operacje kwerendy działają szybciej w PLINQ.</span><span class="sxs-lookup"><span data-stu-id="7275b-117">However, parallelism can introduce its own complexities, and not all query operations run faster in PLINQ.</span></span> <span data-ttu-id="7275b-118">W rzeczywistości równoległości faktycznie spowalnia niektóre zapytania.</span><span class="sxs-lookup"><span data-stu-id="7275b-118">In fact, parallelization actually slows down certain queries.</span></span> <span data-ttu-id="7275b-119">W związku z tym należy zrozumieć, jak problemy, takie jak kolejność wpływają na zapytania równoległe.</span><span class="sxs-lookup"><span data-stu-id="7275b-119">Therefore, you should understand how issues such as ordering affect parallel queries.</span></span> <span data-ttu-id="7275b-120">Aby uzyskać więcej informacji, zobacz [Opis przyspieszenia w PLINQ](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="7275b-120">For more information, see [Understanding Speedup in PLINQ](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md).</span></span>

> [!NOTE]
> <span data-ttu-id="7275b-121">Ta dokumentacja używa wyrażeń lambda do definiowania delegatów w PLINQ.</span><span class="sxs-lookup"><span data-stu-id="7275b-121">This documentation uses lambda expressions to define delegates in PLINQ.</span></span> <span data-ttu-id="7275b-122">Jeśli nie znasz wyrażeń lambda w języku C# lub Visual Basic, zobacz [Wyrażenia Lambda w PLINQ i TPL](../../../docs/standard/parallel-programming/lambda-expressions-in-plinq-and-tpl.md).</span><span class="sxs-lookup"><span data-stu-id="7275b-122">If you are not familiar with lambda expressions in C# or Visual Basic, see [Lambda Expressions in PLINQ and TPL](../../../docs/standard/parallel-programming/lambda-expressions-in-plinq-and-tpl.md).</span></span>

<span data-ttu-id="7275b-123">Poniżej dalsza część artykułu Zawiera omówienie głównych klas PLINQ i omówiono sposób tworzenia zapytań PLINQ.</span><span class="sxs-lookup"><span data-stu-id="7275b-123">The remainder of this article gives an overview of the main PLINQ classes and discusses how to create PLINQ queries.</span></span> <span data-ttu-id="7275b-124">Każda sekcja zawiera łącza do bardziej szczegółowych informacji i przykładów kodu.</span><span class="sxs-lookup"><span data-stu-id="7275b-124">Each section contains links to more detailed information and code examples.</span></span>

## <a name="the-parallelenumerable-class"></a><span data-ttu-id="7275b-125">Klasa ParallelEnumerable</span><span class="sxs-lookup"><span data-stu-id="7275b-125">The ParallelEnumerable Class</span></span>

<span data-ttu-id="7275b-126">Klasa <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> eksponuje prawie wszystkie funkcje PLINQ.</span><span class="sxs-lookup"><span data-stu-id="7275b-126">The <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> class exposes almost all of PLINQ's functionality.</span></span> <span data-ttu-id="7275b-127">On i pozostałe <xref:System.Linq?displayProperty=nameWithType> typy obszaru nazw są kompilowane do zestawu System.Core.dll.</span><span class="sxs-lookup"><span data-stu-id="7275b-127">It and the rest of the <xref:System.Linq?displayProperty=nameWithType> namespace types are compiled into the System.Core.dll assembly.</span></span> <span data-ttu-id="7275b-128">Domyślne projekty języka C# i Visual Basic w programie Visual Studio odwołują się do zestawu i importują obszar nazw.</span><span class="sxs-lookup"><span data-stu-id="7275b-128">The default C# and Visual Basic projects in Visual Studio both reference the assembly and import the namespace.</span></span>

<span data-ttu-id="7275b-129"><xref:System.Linq.ParallelEnumerable>zawiera implementacje wszystkich standardowych operatorów zapytań, które linq do obiektów obsługuje, chociaż nie próbuje zrównoleglić każdego z nich.</span><span class="sxs-lookup"><span data-stu-id="7275b-129"><xref:System.Linq.ParallelEnumerable> includes implementations of all the standard query operators that LINQ to Objects supports, although it does not attempt to parallelize each one.</span></span> <span data-ttu-id="7275b-130">Jeśli nie znasz linq, zobacz [Wprowadzenie do LINQ (C#)](../../csharp/programming-guide/concepts/linq/index.md) i [Wprowadzenie do LINQ (Visual Basic)](../../visual-basic/programming-guide/concepts/linq/introduction-to-linq.md).</span><span class="sxs-lookup"><span data-stu-id="7275b-130">If you are not familiar with LINQ, see [Introduction to LINQ (C#)](../../csharp/programming-guide/concepts/linq/index.md) and [Introduction to LINQ (Visual Basic)](../../visual-basic/programming-guide/concepts/linq/introduction-to-linq.md).</span></span>

<span data-ttu-id="7275b-131">Oprócz standardowych operatorów zapytań <xref:System.Linq.ParallelEnumerable> klasa zawiera zestaw metod, które umożliwiają zachowania specyficzne dla wykonywania równoległego.</span><span class="sxs-lookup"><span data-stu-id="7275b-131">In addition to the standard query operators, the <xref:System.Linq.ParallelEnumerable> class contains a set of methods that enable behaviors specific to parallel execution.</span></span> <span data-ttu-id="7275b-132">Te metody specyficzne dla PLINQ są wymienione w poniższej tabeli.</span><span class="sxs-lookup"><span data-stu-id="7275b-132">These PLINQ-specific methods are listed in the following table.</span></span>

|<span data-ttu-id="7275b-133">Operator równoległy</span><span class="sxs-lookup"><span data-stu-id="7275b-133">ParallelEnumerable Operator</span></span>|<span data-ttu-id="7275b-134">Opis</span><span class="sxs-lookup"><span data-stu-id="7275b-134">Description</span></span>|
|---------------------------------|-----------------|
|<xref:System.Linq.ParallelEnumerable.AsParallel%2A>|<span data-ttu-id="7275b-135">Punkt wejścia dla PLINQ.</span><span class="sxs-lookup"><span data-stu-id="7275b-135">The entry point for PLINQ.</span></span> <span data-ttu-id="7275b-136">Określa, że reszta kwerendy powinna być równoległa, jeśli jest to możliwe.</span><span class="sxs-lookup"><span data-stu-id="7275b-136">Specifies that the rest of the query should be parallelized, if it is possible.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsSequential%2A>|<span data-ttu-id="7275b-137">Określa, że pozostała część kwerendy powinna być uruchamiana sekwencyjnie jako kwerenda linq niedościskuteczna.</span><span class="sxs-lookup"><span data-stu-id="7275b-137">Specifies that the rest of the query should be run sequentially, as a non-parallel LINQ query.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsOrdered%2A>|<span data-ttu-id="7275b-138">Określa, że PLINQ należy zachować kolejność sekwencji źródłowej dla pozostałej części kwerendy lub do czasu zmiany kolejności, na przykład przez użycie orderby (Order By in Visual Basic) klauzuli.</span><span class="sxs-lookup"><span data-stu-id="7275b-138">Specifies that PLINQ should preserve the ordering of the source sequence for the rest of the query, or until the ordering is changed, for example by the use of an orderby (Order By in Visual Basic) clause.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsUnordered%2A>|<span data-ttu-id="7275b-139">Określa, że PLINQ dla pozostałej części kwerendy nie jest wymagane do zachowania kolejności sekwencji źródłowej.</span><span class="sxs-lookup"><span data-stu-id="7275b-139">Specifies that PLINQ for the rest of the query is not required to preserve the ordering of the source sequence.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithCancellation%2A>|<span data-ttu-id="7275b-140">Określa, że PLINQ należy okresowo monitorować stan tokenu anulowania pod warunkiem i anulować wykonanie, jeśli jest to wymagane.</span><span class="sxs-lookup"><span data-stu-id="7275b-140">Specifies that PLINQ should periodically monitor the state of the provided cancellation token and cancel execution if it is requested.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A>|<span data-ttu-id="7275b-141">Określa maksymalną liczbę procesorów, których plinq powinien używać do równoległości kwerendy.</span><span class="sxs-lookup"><span data-stu-id="7275b-141">Specifies the maximum number of processors that PLINQ should use to parallelize the query.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A>|<span data-ttu-id="7275b-142">Zawiera wskazówkę na temat tego, jak PLINQ należy, jeśli jest to możliwe, scalić równoległe wyniki z powrotem do tylko jednej sekwencji na wątku zużywającym.</span><span class="sxs-lookup"><span data-stu-id="7275b-142">Provides a hint about how PLINQ should, if it is possible, merge parallel results back into just one sequence on the consuming thread.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A>|<span data-ttu-id="7275b-143">Określa, czy PLINQ należy zrównać kwerendę nawet wtedy, gdy domyślne zachowanie byłoby uruchomić go sekwencyjnie.</span><span class="sxs-lookup"><span data-stu-id="7275b-143">Specifies whether PLINQ should parallelize the query even when the default behavior would be to run it sequentially.</span></span>|
|<xref:System.Linq.ParallelEnumerable.ForAll%2A>|<span data-ttu-id="7275b-144">Metoda wyliczenia wielowątkowego, która w przeciwieństwie do iteracji nad wynikami kwerendy umożliwia równoległe przetwarzanie wyników bez uprzedniego scalania z powrotem do wątku konsumenta.</span><span class="sxs-lookup"><span data-stu-id="7275b-144">A multithreaded enumeration method that, unlike iterating over the results of the query, enables results to be processed in parallel without first merging back to the consumer thread.</span></span>|
|<span data-ttu-id="7275b-145"><xref:System.Linq.ParallelEnumerable.Aggregate%2A>Przeciążenie</span><span class="sxs-lookup"><span data-stu-id="7275b-145"><xref:System.Linq.ParallelEnumerable.Aggregate%2A> overload</span></span>|<span data-ttu-id="7275b-146">Przeciążenie, które jest unikatowe dla PLINQ i umożliwia agregację pośrednią za pomocą partycji lokalnych wątków oraz końcową funkcję agregacji w celu łączenia wyników wszystkich partycji.</span><span class="sxs-lookup"><span data-stu-id="7275b-146">An overload that is unique to PLINQ and enables intermediate aggregation over thread-local partitions, plus a final aggregation function to combine the results of all partitions.</span></span>|

## <a name="the-opt-in-model"></a><span data-ttu-id="7275b-147">The Opt-in Model</span><span class="sxs-lookup"><span data-stu-id="7275b-147">The Opt-in Model</span></span>

<span data-ttu-id="7275b-148">Podczas pisania kwerendy, wybierz plinq, wywołując <xref:System.Linq.ParallelEnumerable.AsParallel%2A?displayProperty=nameWithType> metodę rozszerzenia w źródle danych, jak pokazano w poniższym przykładzie.</span><span class="sxs-lookup"><span data-stu-id="7275b-148">When you write a query, opt in to PLINQ by invoking the <xref:System.Linq.ParallelEnumerable.AsParallel%2A?displayProperty=nameWithType> extension method on the data source, as shown in the following example.</span></span>

[!code-csharp[PLINQ#1](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#1)]
[!code-vb[PLINQ#1](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#1)]

<span data-ttu-id="7275b-149">Metoda <xref:System.Linq.ParallelEnumerable.AsParallel%2A> rozszerzenia wiąże kolejne operatory kwerend, `where` w `select`tym <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> przypadku i , do implementacji.</span><span class="sxs-lookup"><span data-stu-id="7275b-149">The <xref:System.Linq.ParallelEnumerable.AsParallel%2A> extension method binds the subsequent query operators, in this case, `where` and `select`, to the <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> implementations.</span></span>

## <a name="execution-modes"></a><span data-ttu-id="7275b-150">Tryby wykonywania</span><span class="sxs-lookup"><span data-stu-id="7275b-150">Execution Modes</span></span>

<span data-ttu-id="7275b-151">Domyślnie PLINQ jest konserwatywny.</span><span class="sxs-lookup"><span data-stu-id="7275b-151">By default, PLINQ is conservative.</span></span> <span data-ttu-id="7275b-152">W czasie wykonywania infrastruktury PLINQ analizuje ogólną strukturę kwerendy.</span><span class="sxs-lookup"><span data-stu-id="7275b-152">At run time, the PLINQ infrastructure analyzes the overall structure of the query.</span></span> <span data-ttu-id="7275b-153">Jeśli kwerenda może przynieść przyspieszenia przez równoległość, PLINQ partycje sekwencji źródłowej do zadań, które mogą być uruchamiane jednocześnie.</span><span class="sxs-lookup"><span data-stu-id="7275b-153">If the query is likely to yield speedups by parallelization, PLINQ partitions the source sequence into tasks that can be run concurrently.</span></span> <span data-ttu-id="7275b-154">Jeśli nie jest bezpieczne do równoległości kwerendy, PLINQ po prostu uruchamia kwerendę sekwencyjnie.</span><span class="sxs-lookup"><span data-stu-id="7275b-154">If it is not safe to parallelize a query, PLINQ just runs the query sequentially.</span></span> <span data-ttu-id="7275b-155">Jeśli PLINQ ma wybór między algorytmem równoległym potencjalnie drogie lub niedrogi algorytm sekwencyjny, wybiera algorytm sekwencyjny domyślnie.</span><span class="sxs-lookup"><span data-stu-id="7275b-155">If PLINQ has a choice between a potentially expensive parallel algorithm or an inexpensive sequential algorithm, it chooses the sequential algorithm by default.</span></span> <span data-ttu-id="7275b-156">Można użyć <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> metody i <xref:System.Linq.ParallelExecutionMode?displayProperty=nameWithType> wyliczenia, aby poinstruować PLINQ, aby wybrać algorytm równoległy.</span><span class="sxs-lookup"><span data-stu-id="7275b-156">You can use the <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> method and the <xref:System.Linq.ParallelExecutionMode?displayProperty=nameWithType> enumeration to instruct PLINQ to select the parallel algorithm.</span></span> <span data-ttu-id="7275b-157">Jest to przydatne, gdy wiesz, testując i miara, że określone zapytanie wykonuje szybciej równolegle.</span><span class="sxs-lookup"><span data-stu-id="7275b-157">This is useful when you know by testing and measurement that a particular query executes faster in parallel.</span></span> <span data-ttu-id="7275b-158">Aby uzyskać więcej informacji, zobacz [Jak: Określanie trybu wykonywania w PLINQ](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="7275b-158">For more information, see [How to: Specify the Execution Mode in PLINQ](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md).</span></span>

## <a name="degree-of-parallelism"></a><span data-ttu-id="7275b-159">Stopień równoległości</span><span class="sxs-lookup"><span data-stu-id="7275b-159">Degree of Parallelism</span></span>

<span data-ttu-id="7275b-160">Domyślnie PLINQ używa wszystkich procesorów na komputerze-hoście.</span><span class="sxs-lookup"><span data-stu-id="7275b-160">By default, PLINQ uses all of the processors on the host computer.</span></span> <span data-ttu-id="7275b-161">Za pomocą <xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A> tej metody można poinstruować PLINQ, aby używała nie więcej niż określonej liczby procesorów.</span><span class="sxs-lookup"><span data-stu-id="7275b-161">You can instruct PLINQ to use no more than a specified number of processors by using the <xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A> method.</span></span> <span data-ttu-id="7275b-162">Jest to przydatne, gdy chcesz upewnić się, że inne procesy uruchomione na komputerze otrzymują określoną ilość czasu procesora CPU.</span><span class="sxs-lookup"><span data-stu-id="7275b-162">This is useful when you want to make sure that other processes running on the computer receive a certain amount of CPU time.</span></span> <span data-ttu-id="7275b-163">Poniższy fragment kodu ogranicza kwerendę do wykorzystania maksymalnie dwóch procesorów.</span><span class="sxs-lookup"><span data-stu-id="7275b-163">The following snippet limits the query to utilizing a maximum of two processors.</span></span>

[!code-csharp[PLINQ#5](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinqsamples.cs#5)]
[!code-vb[PLINQ#5](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#5)]

<span data-ttu-id="7275b-164">W przypadkach, gdy kwerenda wykonuje znaczną ilość pracy związanej z obliczeniami, takich jak we/wy pliku, może być korzystne określenie stopnia równoległości większej niż liczba rdzeni na komputerze.</span><span class="sxs-lookup"><span data-stu-id="7275b-164">In cases where a query is performing a significant amount of non-compute-bound work such as File I/O, it might be beneficial to specify a degree of parallelism greater than the number of cores on the machine.</span></span>

## <a name="ordered-versus-unordered-parallel-queries"></a><span data-ttu-id="7275b-165">Uporządkowane kwerendy równoległe w porównaniu z nieuiarszowanymi zapytaniami równoległymi</span><span class="sxs-lookup"><span data-stu-id="7275b-165">Ordered Versus Unordered Parallel Queries</span></span>

<span data-ttu-id="7275b-166">W niektórych kwerendach operator kwerendy musi uzyskać wyniki, które zachowują kolejność sekwencji źródłowej.</span><span class="sxs-lookup"><span data-stu-id="7275b-166">In some queries, a query operator must produce results that preserve the ordering of the source sequence.</span></span> <span data-ttu-id="7275b-167">PLINQ zapewnia <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> operatorowi w tym celu.</span><span class="sxs-lookup"><span data-stu-id="7275b-167">PLINQ provides the <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> operator for this purpose.</span></span> <span data-ttu-id="7275b-168"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A>różni się <xref:System.Linq.ParallelEnumerable.AsSequential%2A>od .</span><span class="sxs-lookup"><span data-stu-id="7275b-168"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A> is distinct from <xref:System.Linq.ParallelEnumerable.AsSequential%2A>.</span></span> <span data-ttu-id="7275b-169">Sekwencja <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> jest nadal przetwarzane równolegle, ale jej wyniki są buforowane i sortowane.</span><span class="sxs-lookup"><span data-stu-id="7275b-169">An <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sequence is still processed in parallel, but its results are buffered and sorted.</span></span> <span data-ttu-id="7275b-170">Ponieważ zachowanie zamówienia zazwyczaj obejmuje dodatkową <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> pracę, sekwencja może być <xref:System.Linq.ParallelEnumerable.AsUnordered%2A> przetwarzana wolniej niż domyślna sekwencja.</span><span class="sxs-lookup"><span data-stu-id="7275b-170">Because order preservation typically involves extra work, an <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sequence might be processed more slowly than the default <xref:System.Linq.ParallelEnumerable.AsUnordered%2A> sequence.</span></span> <span data-ttu-id="7275b-171">To, czy określona operacja równoległa uporządkowana jest szybsza niż sekwencyjna wersja operacji, zależy od wielu czynników.</span><span class="sxs-lookup"><span data-stu-id="7275b-171">Whether a particular ordered parallel operation is faster than a sequential version of the operation depends on many factors.</span></span>

<span data-ttu-id="7275b-172">Poniższy przykład kodu pokazuje, jak zdecydować się na zachowanie kolejności.</span><span class="sxs-lookup"><span data-stu-id="7275b-172">The following code example shows how to opt in to order preservation.</span></span>

[!code-csharp[PLINQ#3](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#3)]
[!code-vb[PLINQ#3](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#3)]

<span data-ttu-id="7275b-173">Aby uzyskać więcej informacji, zobacz [Zachowanie porządku w PLINQ](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="7275b-173">For more information, see [Order Preservation in PLINQ](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span></span>

## <a name="parallel-vs-sequential-queries"></a><span data-ttu-id="7275b-174">Zapytania równoległe a sekwencyjne</span><span class="sxs-lookup"><span data-stu-id="7275b-174">Parallel vs. Sequential Queries</span></span>

<span data-ttu-id="7275b-175">Niektóre operacje wymagają, aby dane źródłowe były dostarczane w sposób sekwencyjny.</span><span class="sxs-lookup"><span data-stu-id="7275b-175">Some operations require that the source data be delivered in a sequential manner.</span></span> <span data-ttu-id="7275b-176">Operatory <xref:System.Linq.ParallelEnumerable> kwerend automatycznie powracają do trybu sekwencyjnego, gdy jest to wymagane.</span><span class="sxs-lookup"><span data-stu-id="7275b-176">The <xref:System.Linq.ParallelEnumerable> query operators revert to sequential mode automatically when it is required.</span></span> <span data-ttu-id="7275b-177">Dla operatorów zapytań zdefiniowanych przez użytkownika i delegatów użytkownika, <xref:System.Linq.ParallelEnumerable.AsSequential%2A> które wymagają wykonywania sekwencyjnego, PLINQ udostępnia metodę.</span><span class="sxs-lookup"><span data-stu-id="7275b-177">For user-defined query operators and user delegates that require sequential execution, PLINQ provides the <xref:System.Linq.ParallelEnumerable.AsSequential%2A> method.</span></span> <span data-ttu-id="7275b-178">Podczas korzystania <xref:System.Linq.ParallelEnumerable.AsSequential%2A>wszystkie kolejne operatory w kwerendzie są <xref:System.Linq.ParallelEnumerable.AsParallel%2A> wykonywane sekwencyjnie, dopóki nie zostanie wywołana ponownie.</span><span class="sxs-lookup"><span data-stu-id="7275b-178">When you use <xref:System.Linq.ParallelEnumerable.AsSequential%2A>, all subsequent operators in the query are executed sequentially until <xref:System.Linq.ParallelEnumerable.AsParallel%2A> is called again.</span></span> <span data-ttu-id="7275b-179">Aby uzyskać więcej informacji, zobacz [Jak: Łączenie równoległych i sekwencyjnych zapytań LINQ](../../../docs/standard/parallel-programming/how-to-combine-parallel-and-sequential-linq-queries.md).</span><span class="sxs-lookup"><span data-stu-id="7275b-179">For more information, see [How to: Combine Parallel and Sequential LINQ Queries](../../../docs/standard/parallel-programming/how-to-combine-parallel-and-sequential-linq-queries.md).</span></span>

## <a name="options-for-merging-query-results"></a><span data-ttu-id="7275b-180">Opcje scalania wyników kwerendy</span><span class="sxs-lookup"><span data-stu-id="7275b-180">Options for Merging Query Results</span></span>

<span data-ttu-id="7275b-181">Gdy kwerenda PLINQ wykonuje równolegle, jej wyniki z każdego wątku roboczego muszą `foreach` być`For Each` scalone z powrotem do wątku głównego do użycia przez pętlę (w języku Visual Basic) lub wstawiania do listy lub tablicy.</span><span class="sxs-lookup"><span data-stu-id="7275b-181">When a PLINQ query executes in parallel, its results from each worker thread must be merged back onto the main thread for consumption by a `foreach` loop (`For Each` in Visual Basic), or insertion into a list or array.</span></span> <span data-ttu-id="7275b-182">W niektórych przypadkach może być korzystne określenie określonego rodzaju operacji scalania, na przykład, aby szybciej rozpocząć tworzenie wyników.</span><span class="sxs-lookup"><span data-stu-id="7275b-182">In some cases, it might be beneficial to specify a particular kind of merge operation, for example, to begin producing results more quickly.</span></span> <span data-ttu-id="7275b-183">W tym celu PLINQ <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> obsługuje metodę <xref:System.Linq.ParallelMergeOptions> i wyliczenie.</span><span class="sxs-lookup"><span data-stu-id="7275b-183">For this purpose, PLINQ supports the <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> method, and the <xref:System.Linq.ParallelMergeOptions> enumeration.</span></span> <span data-ttu-id="7275b-184">Aby uzyskać więcej informacji, zobacz [Opcje scalania w PLINQ](../../../docs/standard/parallel-programming/merge-options-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="7275b-184">For more information, see [Merge Options in PLINQ](../../../docs/standard/parallel-programming/merge-options-in-plinq.md).</span></span>

## <a name="the-forall-operator"></a><span data-ttu-id="7275b-185">The ForAll Operator</span><span class="sxs-lookup"><span data-stu-id="7275b-185">The ForAll Operator</span></span>

<span data-ttu-id="7275b-186">W kolejnych kwerendach LINQ wykonanie jest odroczone, dopóki kwerenda nie `foreach` zostanie`For Each` wyliczona w pętli (w <xref:System.Linq.ParallelEnumerable.ToList%2A> języku <xref:System.Linq.ParallelEnumerable.ToArray%2A> Visual <xref:System.Linq.ParallelEnumerable.ToDictionary%2A>Basic) lub wywołanie metody, takiej jak , lub .</span><span class="sxs-lookup"><span data-stu-id="7275b-186">In sequential LINQ queries, execution is deferred until the query is enumerated either in a `foreach` (`For Each` in Visual Basic) loop or by invoking a method such as <xref:System.Linq.ParallelEnumerable.ToList%2A> , <xref:System.Linq.ParallelEnumerable.ToArray%2A> , or <xref:System.Linq.ParallelEnumerable.ToDictionary%2A>.</span></span> <span data-ttu-id="7275b-187">W PLINQ, można `foreach` również użyć do wykonania kwerendy i iteracji za pośrednictwem wyników.</span><span class="sxs-lookup"><span data-stu-id="7275b-187">In PLINQ, you can also use `foreach` to execute the query and iterate through the results.</span></span> <span data-ttu-id="7275b-188">Jednak `foreach` sama nie działa równolegle i dlatego wymaga, aby dane wyjściowe ze wszystkich równoległych zadań zostały scalone z powrotem do wątku, na którym jest uruchomiona pętla.</span><span class="sxs-lookup"><span data-stu-id="7275b-188">However, `foreach` itself does not run in parallel, and therefore, it requires that the output from all parallel tasks be merged back into the thread on which the loop is running.</span></span> <span data-ttu-id="7275b-189">W PLINQ, można `foreach` użyć, gdy należy zachować ostateczną kolejność wyników kwerendy, a także za każdym razem, `Console.WriteLine` gdy są przetwarzane wyniki w sposób szeregowy, na przykład, gdy wywołujesz dla każdego elementu.</span><span class="sxs-lookup"><span data-stu-id="7275b-189">In PLINQ, you can use `foreach` when you must preserve the final ordering of the query results, and also whenever you are processing the results in a serial manner, for example when you are calling `Console.WriteLine` for each element.</span></span> <span data-ttu-id="7275b-190">Aby przyspieszyć wykonywanie zapytań, gdy zachowanie zleceń nie jest wymagane i gdy <xref:System.Linq.ParallelEnumerable.ForAll%2A> przetwarzanie wyników może być równoległe, użyj metody do wykonania kwerendy PLINQ.</span><span class="sxs-lookup"><span data-stu-id="7275b-190">For faster query execution when order preservation is not required and when the processing of the results can itself be parallelized, use the <xref:System.Linq.ParallelEnumerable.ForAll%2A> method to execute a PLINQ query.</span></span> <span data-ttu-id="7275b-191"><xref:System.Linq.ParallelEnumerable.ForAll%2A>nie wykonuje tego ostatniego kroku scalania.</span><span class="sxs-lookup"><span data-stu-id="7275b-191"><xref:System.Linq.ParallelEnumerable.ForAll%2A> does not perform this final merge step.</span></span> <span data-ttu-id="7275b-192">Poniższy przykład kodu pokazuje, <xref:System.Linq.ParallelEnumerable.ForAll%2A> jak używać metody.</span><span class="sxs-lookup"><span data-stu-id="7275b-192">The following code example shows how to use the <xref:System.Linq.ParallelEnumerable.ForAll%2A> method.</span></span> <span data-ttu-id="7275b-193"><xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType>jest w tym miejscu używany, ponieważ jest zoptymalizowany pod kątem wielu wątków dodawanie jednocześnie bez próby usunięcia żadnych elementów.</span><span class="sxs-lookup"><span data-stu-id="7275b-193"><xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType> is used here because it is optimized for multiple threads adding concurrently without attempting to remove any items.</span></span>

[!code-csharp[PLINQ#4](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#4)]
[!code-vb[PLINQ#4](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#4)]

<span data-ttu-id="7275b-194">Na poniższej ilustracji `foreach` <xref:System.Linq.ParallelEnumerable.ForAll%2A> przedstawiono różnicę między i w odniesieniu do wykonywania kwerend.</span><span class="sxs-lookup"><span data-stu-id="7275b-194">The following illustration shows the difference between `foreach` and <xref:System.Linq.ParallelEnumerable.ForAll%2A> with regard to query execution.</span></span>

<span data-ttu-id="7275b-195">![ForAll kontra ForEach](../../../docs/standard/parallel-programming/media/vs-isvnt-allvseach.png "VS_ISVNT_ALLvsEACH")</span><span class="sxs-lookup"><span data-stu-id="7275b-195">![ForAll vs. ForEach](../../../docs/standard/parallel-programming/media/vs-isvnt-allvseach.png "VS_ISVNT_ALLvsEACH")</span></span>

## <a name="cancellation"></a><span data-ttu-id="7275b-196">Anulowanie</span><span class="sxs-lookup"><span data-stu-id="7275b-196">Cancellation</span></span>

<span data-ttu-id="7275b-197">PLINQ jest zintegrowany z typami anulowania w .NET Framework 4.</span><span class="sxs-lookup"><span data-stu-id="7275b-197">PLINQ is integrated with the cancellation types in .NET Framework 4.</span></span> <span data-ttu-id="7275b-198">(Aby uzyskać więcej informacji, zobacz [Anulowanie w wątkach zarządzanych](../../../docs/standard/threading/cancellation-in-managed-threads.md).) W związku z tym, w przeciwieństwie do sekwencyjneGO LINQ do kwerend obiektów, zapytania PLINQ można anulować.</span><span class="sxs-lookup"><span data-stu-id="7275b-198">(For more information, see [Cancellation in Managed Threads](../../../docs/standard/threading/cancellation-in-managed-threads.md).) Therefore, unlike sequential LINQ to Objects queries, PLINQ queries can be canceled.</span></span> <span data-ttu-id="7275b-199">Aby utworzyć kwerendę PLINQ, <xref:System.Linq.ParallelEnumerable.WithCancellation%2A> której można anulować, <xref:System.Threading.CancellationToken> użyj operatora w kwerendzie i podaj wystąpienie jako argument.</span><span class="sxs-lookup"><span data-stu-id="7275b-199">To create a cancelable PLINQ query, use the <xref:System.Linq.ParallelEnumerable.WithCancellation%2A> operator on the query and provide a <xref:System.Threading.CancellationToken> instance as the argument.</span></span> <span data-ttu-id="7275b-200">Gdy <xref:System.Threading.CancellationToken.IsCancellationRequested%2A> właściwość na tokenie jest ustawiona na true, PLINQ zauważy to, zatrzyma przetwarzanie na wszystkich wątkach i wrzuć <xref:System.OperationCanceledException>.</span><span class="sxs-lookup"><span data-stu-id="7275b-200">When the <xref:System.Threading.CancellationToken.IsCancellationRequested%2A> property on the token is set to true, PLINQ will notice it, stop processing on all threads, and throw an <xref:System.OperationCanceledException>.</span></span>

<span data-ttu-id="7275b-201">Jest możliwe, że zapytanie PLINQ może nadal przetwarzać niektóre elementy po ustawieniu tokenu anulowania.</span><span class="sxs-lookup"><span data-stu-id="7275b-201">It is possible that a PLINQ query might continue to process some elements after the cancellation token is set.</span></span>

<span data-ttu-id="7275b-202">Aby uzyskać większą szybkość reakcji, można również odpowiadać na żądania anulowania w długotrwałych delegatów użytkowników.</span><span class="sxs-lookup"><span data-stu-id="7275b-202">For greater responsiveness, you can also respond to cancellation requests in long-running user delegates.</span></span> <span data-ttu-id="7275b-203">Aby uzyskać więcej informacji, zobacz [Jak: Anulowanie zapytania PLINQ](../../../docs/standard/parallel-programming/how-to-cancel-a-plinq-query.md).</span><span class="sxs-lookup"><span data-stu-id="7275b-203">For more information, see [How to: Cancel a PLINQ Query](../../../docs/standard/parallel-programming/how-to-cancel-a-plinq-query.md).</span></span>

## <a name="exceptions"></a><span data-ttu-id="7275b-204">Wyjątki</span><span class="sxs-lookup"><span data-stu-id="7275b-204">Exceptions</span></span>

<span data-ttu-id="7275b-205">Podczas wykonywania kwerendy PLINQ, wiele wyjątków może być zgłaszanych z różnych wątków jednocześnie.</span><span class="sxs-lookup"><span data-stu-id="7275b-205">When a PLINQ query executes, multiple exceptions might be thrown from different threads simultaneously.</span></span> <span data-ttu-id="7275b-206">Ponadto kod do obsługi wyjątku może być w innym wątku niż kod, który zgłosił wyjątek.</span><span class="sxs-lookup"><span data-stu-id="7275b-206">Also, the code to handle the exception might be on a different thread than the code that threw the exception.</span></span> <span data-ttu-id="7275b-207">PLINQ używa <xref:System.AggregateException> tego typu do hermetyzacji wszystkich wyjątków, które zostały odrzucone przez kwerendę i marshal te wyjątki z powrotem do wątku wywołującego.</span><span class="sxs-lookup"><span data-stu-id="7275b-207">PLINQ uses the <xref:System.AggregateException> type to encapsulate all the exceptions that were thrown by a query, and marshal those exceptions back to the calling thread.</span></span> <span data-ttu-id="7275b-208">W wątku wywołującym wymagany jest tylko jeden blok try-catch.</span><span class="sxs-lookup"><span data-stu-id="7275b-208">On the calling thread, only one try-catch block is required.</span></span> <span data-ttu-id="7275b-209">Można jednak iterować przez wszystkie wyjątki, które są hermetyzowane w <xref:System.AggregateException> i złapać dowolne, które można bezpiecznie odzyskać z.</span><span class="sxs-lookup"><span data-stu-id="7275b-209">However, you can iterate through all of the exceptions that are encapsulated in the <xref:System.AggregateException> and catch any that you can safely recover from.</span></span> <span data-ttu-id="7275b-210">W rzadkich przypadkach niektóre wyjątki mogą być <xref:System.AggregateException>generowane, <xref:System.Threading.ThreadAbortException>które nie są zawinięte w , i s również nie są zawijane.</span><span class="sxs-lookup"><span data-stu-id="7275b-210">In rare cases, some exceptions may be thrown that are not wrapped in an <xref:System.AggregateException>, and <xref:System.Threading.ThreadAbortException>s  are also not wrapped.</span></span>

<span data-ttu-id="7275b-211">Gdy wyjątki są dozwolone do bańki z powrotem do wątku łączącego, a następnie jest możliwe, że kwerenda może kontynuować przetwarzanie niektórych elementów po wyjątek jest wywoływany.</span><span class="sxs-lookup"><span data-stu-id="7275b-211">When exceptions are allowed to bubble up back to the joining thread, then it is possible that a query may continue to process some items after the exception is raised.</span></span>

<span data-ttu-id="7275b-212">Aby uzyskać więcej informacji, zobacz [Jak: Obsługa wyjątków w kwerendzie PLINQ](../../../docs/standard/parallel-programming/how-to-handle-exceptions-in-a-plinq-query.md).</span><span class="sxs-lookup"><span data-stu-id="7275b-212">For more information, see [How to: Handle Exceptions in a PLINQ Query](../../../docs/standard/parallel-programming/how-to-handle-exceptions-in-a-plinq-query.md).</span></span>

## <a name="custom-partitioners"></a><span data-ttu-id="7275b-213">Niestandardowe partycjonowania</span><span class="sxs-lookup"><span data-stu-id="7275b-213">Custom Partitioners</span></span>

<span data-ttu-id="7275b-214">W niektórych przypadkach można zwiększyć wydajność kwerendy, pisząc niestandardowy partycjoner, który korzysta z niektórych cech danych źródłowych.</span><span class="sxs-lookup"><span data-stu-id="7275b-214">In some cases, you can improve query performance by writing a custom partitioner that takes advantage of some characteristic of the source data.</span></span> <span data-ttu-id="7275b-215">W kwerendzie sam partycjonowanie niestandardowe jest obiektem wyliczalnym, który jest wyszukiwany.</span><span class="sxs-lookup"><span data-stu-id="7275b-215">In the query, the custom partitioner itself is the enumerable object that is queried.</span></span>

[!code-csharp[PLINQ#2](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#2)]
[!code-vb[PLINQ#2](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq3.vb#2)]

<span data-ttu-id="7275b-216">PLINQ obsługuje stałą liczbę partycji (chociaż dane mogą być dynamicznie ponownie przypisane do tych partycji w czasie wykonywania dla równoważenia obciążenia.).</span><span class="sxs-lookup"><span data-stu-id="7275b-216">PLINQ supports a fixed number of partitions (although data may be dynamically reassigned to those partitions during run time for load balancing.).</span></span> <span data-ttu-id="7275b-217"><xref:System.Threading.Tasks.Parallel.For%2A>i <xref:System.Threading.Tasks.Parallel.ForEach%2A> obsługuje tylko dynamiczne partycjonowanie, co oznacza, że liczba partycji zmienia się w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="7275b-217"><xref:System.Threading.Tasks.Parallel.For%2A> and <xref:System.Threading.Tasks.Parallel.ForEach%2A> support only dynamic partitioning, which means that the number of partitions changes at run time.</span></span> <span data-ttu-id="7275b-218">Aby uzyskać więcej informacji, zobacz [Niestandardowe partitionery dla PLINQ i TPL](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md).</span><span class="sxs-lookup"><span data-stu-id="7275b-218">For more information, see [Custom Partitioners for PLINQ and TPL](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md).</span></span>

## <a name="measuring-plinq-performance"></a><span data-ttu-id="7275b-219">Pomiar wydajności PLINQ</span><span class="sxs-lookup"><span data-stu-id="7275b-219">Measuring PLINQ Performance</span></span>

<span data-ttu-id="7275b-220">W wielu przypadkach kwerendy można zrównoleglić, ale obciążenie związane z konfigurowaniem zapytania równoległego przeważa nad uzyskanymi korzyściami z wydajności.</span><span class="sxs-lookup"><span data-stu-id="7275b-220">In many cases, a query can be parallelized, but the overhead of setting up the parallel query outweighs the performance benefit gained.</span></span> <span data-ttu-id="7275b-221">Jeśli kwerenda nie wykonuje wiele obliczeń lub jeśli źródło danych jest małe, kwerenda PLINQ może być wolniejsza niż sekwencyjne zapytanie LINQ do obiektów.</span><span class="sxs-lookup"><span data-stu-id="7275b-221">If a query does not perform much computation or if the data source is small, a PLINQ query may be slower than a sequential LINQ to Objects query.</span></span> <span data-ttu-id="7275b-222">Analizator wydajności równoległej w programie Visual Studio Team Server służy do porównywania wydajności różnych zapytań, lokalizowania wąskich gardeł przetwarzania i określania, czy kwerenda jest uruchomiona równolegle, czy sekwencyjnie.</span><span class="sxs-lookup"><span data-stu-id="7275b-222">You can use the Parallel Performance Analyzer in Visual Studio Team Server to compare the performance of various queries, to locate processing bottlenecks, and to determine whether your query is running in parallel or sequentially.</span></span> <span data-ttu-id="7275b-223">Aby uzyskać więcej informacji, zobacz [Wizualizator współbieżności](/visualstudio/profiling/concurrency-visualizer) i [jak: Pomiar wydajności kwerendy PLINQ](../../../docs/standard/parallel-programming/how-to-measure-plinq-query-performance.md).</span><span class="sxs-lookup"><span data-stu-id="7275b-223">For more information, see [Concurrency Visualizer](/visualstudio/profiling/concurrency-visualizer) and [How to: Measure PLINQ Query Performance](../../../docs/standard/parallel-programming/how-to-measure-plinq-query-performance.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="7275b-224">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="7275b-224">See also</span></span>

- [<span data-ttu-id="7275b-225">Równoległe LINQ (PLINQ)</span><span class="sxs-lookup"><span data-stu-id="7275b-225">Parallel LINQ (PLINQ)</span></span>](../../../docs/standard/parallel-programming/introduction-to-plinq.md)
- [<span data-ttu-id="7275b-226">Ogólne informacje o przyspieszeniach w PLINQ</span><span class="sxs-lookup"><span data-stu-id="7275b-226">Understanding Speedup in PLINQ</span></span>](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md)
