---
title: Typ "<variablename>" jest niejednoznaczny, ponieważ granice pętli i zmienna Step nie mogą zostać poszerzone do tego samego typu
ms.date: 07/20/2015
f1_keywords:
- vbc30983
- bc30983
helpviewer_keywords:
- BC30983
ms.assetid: 6b97153c-dee3-4429-b92a-2e5a212c864b
ms.openlocfilehash: 9e68d0a4edc735884e925d0823c34df4648adfe2
ms.sourcegitcommit: 14355b4b2fe5bcf874cac96d0a9e6376b567e4c7
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 01/30/2019
ms.locfileid: "55262812"
---
# <a name="type-of-variablename-is-ambiguous-because-the-loop-bounds-and-the-step-variable-do-not-widen-to-the-same-type"></a><span data-ttu-id="532eb-102">Typ "\<nazwa_zmiennej >" jest niejednoznaczny, ponieważ granice pętli i zmienna Step nie mogą zostać poszerzone do tego samego typu</span><span class="sxs-lookup"><span data-stu-id="532eb-102">Type of '\<variablename>' is ambiguous because the loop bounds and the step variable do not widen to the same type</span></span>
<span data-ttu-id="532eb-103">W kodzie `For...Next` pętli, w którym kompilator nie można wywnioskować typu danych dla zmienna sterująca pętli, ponieważ dotyczy następujących warunków:</span><span class="sxs-lookup"><span data-stu-id="532eb-103">Your code contains a `For...Next` loop in which the compiler cannot infer a data type for the loop control variable because the following conditions is true:</span></span>  
  
-   <span data-ttu-id="532eb-104">Typ danych zmienna sterująca pętli nie zostanie określony z `As` klauzuli.</span><span class="sxs-lookup"><span data-stu-id="532eb-104">The data type of the loop control variable is not specified with an `As` clause.</span></span>  
  
-   <span data-ttu-id="532eb-105">Granice pętli i zmienna zawiera co najmniej dwa typy danych.</span><span class="sxs-lookup"><span data-stu-id="532eb-105">The loop bounds and step variable contain at least two data types.</span></span>  
  
-   <span data-ttu-id="532eb-106">Istnieje kilka możliwych konwersji między typami danych.</span><span class="sxs-lookup"><span data-stu-id="532eb-106">More than one possible conversion exists between the data types.</span></span>  
  
-   <span data-ttu-id="532eb-107">Istnieje nie najlepszy typ między obiektami, tak, aby wybór typu zmienna sterująca pętli jest niejednoznaczna.</span><span class="sxs-lookup"><span data-stu-id="532eb-107">There is no best type among the candidates, so that the choice of type for the loop control variable is ambiguous.</span></span>  
  
 <span data-ttu-id="532eb-108">Na przykład następującą pętlę ma jedną granicę pętli typu `Integer` pętli one później powiązane typu `UInteger`:</span><span class="sxs-lookup"><span data-stu-id="532eb-108">For example, the following loop has one loop bound of type `Integer` and one loop bound of type `UInteger`:</span></span>  
  
```vb  
Dim m As Integer = 1  
Dim n As UInteger = 10  
' Not valid.  
' For i = m To n  
    ' Loop processing.  
' Next  
```  
  
 <span data-ttu-id="532eb-109">Brak możliwości konwersji między `Integer` i `UInteger`i możliwe konwersji między `UInteger` i `Integer`, oba są zawężających, ale tak nie jest najlepszym wyborem.</span><span class="sxs-lookup"><span data-stu-id="532eb-109">There is a possible conversion between `Integer` and `UInteger`, and a possible conversion between `UInteger` and `Integer`, but both are narrowing conversions so neither is the best choice.</span></span>  
  
 <span data-ttu-id="532eb-110">W następnym przykładzie trzeci zmienną typu `Double` zostanie dodany.</span><span class="sxs-lookup"><span data-stu-id="532eb-110">In the next example, a third variable of type `Double` is added.</span></span> <span data-ttu-id="532eb-111">Zarówno `Integer` i `UInteger` ma standardowych konwersji rozszerzającej na `Double`, co sprawia, że konwersja `Double` najlepszym wyborem.</span><span class="sxs-lookup"><span data-stu-id="532eb-111">Both `Integer` and `UInteger` have standard widening conversions to `Double`, which makes conversion to `Double` the best choice.</span></span> <span data-ttu-id="532eb-112">Wnioskowanie o typie przypisuje zmienna sterująca pętli `i` typu danych `Double`.</span><span class="sxs-lookup"><span data-stu-id="532eb-112">Type inference assigns to loop control variable `i` the data type `Double`.</span></span>  
  
```vb  
Dim stepVar As Double = 1  
' Valid.  
For i = m To n Step stepVar  
    ' Loop processing.  
Next  
```  
  
 <span data-ttu-id="532eb-113">**Identyfikator błędu:** BC30983</span><span class="sxs-lookup"><span data-stu-id="532eb-113">**Error ID:** BC30983</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="532eb-114">Aby poprawić ten błąd</span><span class="sxs-lookup"><span data-stu-id="532eb-114">To correct this error</span></span>  
  
-   <span data-ttu-id="532eb-115">Jawnie przekonwertować na typ, czy inne na przykład umożliwiać konwersję rozszerzającą, jedna ze zmiennych:</span><span class="sxs-lookup"><span data-stu-id="532eb-115">Explicitly convert one of the variables to a type that the others have a widening conversion to, for example:</span></span>  
  
    ```  
    For i = m To CLng(n)  
    ```  
  
-   <span data-ttu-id="532eb-116">Użyj `As` klauzulę, aby określić typ zmiennej formantu:</span><span class="sxs-lookup"><span data-stu-id="532eb-116">Use an `As` clause to specify the type of the control variable:</span></span>  
  
    ```  
    For i As Double = m To n   
    ```  
  
## <a name="see-also"></a><span data-ttu-id="532eb-117">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="532eb-117">See also</span></span>
- [<span data-ttu-id="532eb-118">For...Next, instrukcja</span><span class="sxs-lookup"><span data-stu-id="532eb-118">For...Next Statement</span></span>](../../visual-basic/language-reference/statements/for-next-statement.md)
- [<span data-ttu-id="532eb-119">Konwersje jawne i niejawne</span><span class="sxs-lookup"><span data-stu-id="532eb-119">Implicit and Explicit Conversions</span></span>](../../visual-basic/programming-guide/language-features/data-types/implicit-and-explicit-conversions.md)
- [<span data-ttu-id="532eb-120">Wnioskowanie o typie lokalnym</span><span class="sxs-lookup"><span data-stu-id="532eb-120">Local Type Inference</span></span>](../../visual-basic/programming-guide/language-features/variables/local-type-inference.md)
- [<span data-ttu-id="532eb-121">Option Infer, instrukcja</span><span class="sxs-lookup"><span data-stu-id="532eb-121">Option Infer Statement</span></span>](../../visual-basic/language-reference/statements/option-infer-statement.md)
- [<span data-ttu-id="532eb-122">Rozszerzanie i zwężanie konwersji</span><span class="sxs-lookup"><span data-stu-id="532eb-122">Widening and Narrowing Conversions</span></span>](../../visual-basic/programming-guide/language-features/data-types/widening-and-narrowing-conversions.md)
