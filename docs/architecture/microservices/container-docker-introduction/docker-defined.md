---
title: Co to jest Docker?
description: Architektura mikrousług .NET dla konteneryzowanych aplikacji .NET | Co to jest docker?
ms.date: 08/31/2018
ms.openlocfilehash: a53845d3bbcf24f3eaeb98b9e08b6f35a023c30e
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/14/2020
ms.locfileid: "75337704"
---
# <a name="what-is-docker"></a><span data-ttu-id="e3f2f-103">Co to jest Docker?</span><span class="sxs-lookup"><span data-stu-id="e3f2f-103">What is Docker?</span></span>

<span data-ttu-id="e3f2f-104">[Platforma Docker](https://www.docker.com/) jest [projektem typu open source](https://github.com/docker/docker) do automatyzacji wdrażania aplikacji jako przenośnych, samowystarczalnych kontenerów, które można uruchamiać w chmurze lub lokalnie.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-104">[Docker](https://www.docker.com/) is an [open-source project](https://github.com/docker/docker) for automating the deployment of applications as portable, self-sufficient containers that can run on the cloud or on-premises.</span></span> <span data-ttu-id="e3f2f-105">Docker jest również [firmą,](https://www.docker.com/) która promuje i rozwija tę technologię, współpracując z dostawcami chmury, systemu Linux i Windows, w tym microsoft.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-105">Docker is also a [company](https://www.docker.com/) that promotes and evolves this technology, working in collaboration with cloud, Linux, and Windows vendors, including Microsoft.</span></span>

![Diagram przedstawiający miejsca, w których można uruchomić kontenery platformy Docker.](./media/docker-defined/docker-containers-run-anywhere.png)

<span data-ttu-id="e3f2f-107">**Rysunek 2-2**.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-107">**Figure 2-2**.</span></span> <span data-ttu-id="e3f2f-108">Platforma Docker wdraża kontenery na wszystkich warstwach chmury hybrydowej.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-108">Docker deploys containers at all layers of the hybrid cloud.</span></span>

<span data-ttu-id="e3f2f-109">Kontenery platformy Docker można uruchamiać w dowolnym miejscu, lokalnie w centrum danych klienta, w zewnętrznym dostawcy usług lub w chmurze, na platformie Azure.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-109">Docker containers can run anywhere, on-premises in the customer datacenter, in an external service provider or in the cloud, on Azure.</span></span> <span data-ttu-id="e3f2f-110">Kontenery obrazów platformy Docker można uruchamiać natywnie w systemach Linux i Windows.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-110">Docker image containers can run natively on Linux and Windows.</span></span> <span data-ttu-id="e3f2f-111">Jednak obrazy systemu Windows można uruchamiać tylko na hostach systemu Windows, a obrazy systemu Linux mogą być uruchamiane na hostach systemu Linux i hostach systemu Windows (do tej pory przy użyciu maszyny Wirtualnej systemu Linux hyper-V), gdzie host oznacza serwer lub maszynę wirtualną.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-111">However, Windows images can run only on Windows hosts and Linux images can run on Linux hosts and Windows hosts (using a Hyper-V Linux VM, so far), where host means a server or a VM.</span></span>

<span data-ttu-id="e3f2f-112">Deweloperzy mogą korzystać ze środowisk programistycznych w systemach Windows, Linux lub macOS.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-112">Developers can use development environments on Windows, Linux, or macOS.</span></span> <span data-ttu-id="e3f2f-113">Na komputerze deweloperskim deweloper uruchamia hosta platformy Docker, w którym są wdrażane obrazy platformy Docker, w tym aplikacji i jej zależności.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-113">On the development computer, the developer runs a Docker host where Docker images are deployed, including the app and its dependencies.</span></span> <span data-ttu-id="e3f2f-114">Deweloperzy, którzy pracują w systemie Linux lub na macOS używać hosta Platformy Docker, który jest oparty na systemie Linux i mogą tworzyć obrazy tylko dla kontenerów systemu Linux.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-114">Developers who work on Linux or on macOS use a Docker host that is Linux based, and they can create images only for Linux containers.</span></span> <span data-ttu-id="e3f2f-115">(Deweloperzy pracujący nad systemem macOS mogą edytować kod lub uruchamiać kod CLI platformy Docker z systemu macOS, ale od czasu pisania kontenery nie są uruchamiane bezpośrednio w systemie macOS). Deweloperzy, którzy pracują w systemie Windows, mogą tworzyć obrazy dla kontenerów systemu Linux lub Windows.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-115">(Developers working on macOS can edit code or run the Docker CLI from macOS, but as of the time of this writing, containers don't run directly on macOS.) Developers who work on Windows can create images for either Linux or Windows Containers.</span></span>

<span data-ttu-id="e3f2f-116">Aby hostować kontenery w środowiskach programistycznych i udostępniać dodatkowe narzędzia deweloperskie, platforma Docker wysyła [platformę Docker Community Edition (CE)](https://www.docker.com/community-edition) dla systemu Windows lub systemu macOS.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-116">To host containers in development environments and provide additional developer tools, Docker ships [Docker Community Edition (CE)](https://www.docker.com/community-edition) for Windows or for macOS.</span></span> <span data-ttu-id="e3f2f-117">Te produkty instalują niezbędną maszynę wirtualną (host docker) do obsługi kontenerów.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-117">These products install the necessary VM (the Docker host) to host the containers.</span></span> <span data-ttu-id="e3f2f-118">Platforma Docker udostępnia również [platformę Docker Enterprise Edition (EE),](https://www.docker.com/enterprise-edition)która jest przeznaczona do rozwoju przedsiębiorstw i jest używana przez zespoły IT, które tworzą, akcesorią i uruchamianiem dużych aplikacji o znaczeniu krytycznym dla firmy w środowisku produkcyjnym.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-118">Docker also makes available [Docker Enterprise Edition (EE)](https://www.docker.com/enterprise-edition), which is designed for enterprise development and is used by IT teams who build, ship, and run large business-critical applications in production.</span></span>

<span data-ttu-id="e3f2f-119">Aby uruchomić [kontenery systemu Windows,](/virtualization/windowscontainers/about/)istnieją dwa typy środowiska uruchomieniowego:</span><span class="sxs-lookup"><span data-stu-id="e3f2f-119">To run [Windows Containers](/virtualization/windowscontainers/about/), there are two types of runtimes:</span></span>

- <span data-ttu-id="e3f2f-120">Kontenery systemu Windows Server zapewniają izolację aplikacji za pomocą technologii izolacji procesów i przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-120">Windows Server Containers provide application isolation through process and namespace isolation technology.</span></span> <span data-ttu-id="e3f2f-121">Kontener systemu Windows Server udostępnia jądro z hostem kontenera i ze wszystkimi kontenerami uruchomionymi na hoście.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-121">A Windows Server Container shares a kernel with the container host and with all containers running on the host.</span></span>

- <span data-ttu-id="e3f2f-122">Kontenery funkcji Hyper-V rozwiń izolacji dostarczonych przez kontenery systemu Windows Server, uruchamiając każdy kontener w wysoce zoptymalizowanej maszynie wirtualnej.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-122">Hyper-V Containers expand on the isolation provided by Windows Server Containers by running each container in a highly optimized virtual machine.</span></span> <span data-ttu-id="e3f2f-123">W tej konfiguracji jądro hosta kontenera nie jest współużytkowane z kontenerami funkcji Hyper-V, zapewniając lepszą izolację.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-123">In this configuration, the kernel of the container host isn't shared with the Hyper-V Containers, providing better isolation.</span></span>

<span data-ttu-id="e3f2f-124">Obrazy dla tych kontenerów są tworzone w ten sam sposób i działają tak samo.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-124">The images for these containers are created the same way and function the same.</span></span> <span data-ttu-id="e3f2f-125">Różnica polega na tym, jak kontener jest tworzony z obrazu z uruchomionym kontenerem funkcji Hyper-V wymaga dodatkowego parametru.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-125">The difference is in how the container is created from the image running a Hyper-V Container requires an extra parameter.</span></span> <span data-ttu-id="e3f2f-126">Aby uzyskać szczegółowe informacje, zobacz [Kontenery funkcji Hyper-V](https://docs.microsoft.com/virtualization/windowscontainers/manage-containers/hyperv-container).</span><span class="sxs-lookup"><span data-stu-id="e3f2f-126">For details, see [Hyper-V Containers](https://docs.microsoft.com/virtualization/windowscontainers/manage-containers/hyperv-container).</span></span>

## <a name="comparing-docker-containers-with-virtual-machines"></a><span data-ttu-id="e3f2f-127">Porównywanie kontenerów platformy Docker z maszynami wirtualnymi</span><span class="sxs-lookup"><span data-stu-id="e3f2f-127">Comparing Docker containers with virtual machines</span></span>

<span data-ttu-id="e3f2f-128">Rysunek 2-3 przedstawia porównanie maszyn wirtualnych i kontenerów platformy Docker.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-128">Figure 2-3 shows a comparison between VMs and Docker containers.</span></span>

| <span data-ttu-id="e3f2f-129">Maszyny wirtualne</span><span class="sxs-lookup"><span data-stu-id="e3f2f-129">Virtual Machines</span></span> | <span data-ttu-id="e3f2f-130">Kontenery platformy Docker</span><span class="sxs-lookup"><span data-stu-id="e3f2f-130">Docker Containers</span></span> |
| -----------------| ------------------|
|![Diagram przedstawiający stos sprzętu/oprogramowania tradycyjnej maszyny Wirtualnej.](./media/docker-defined/virtual-machine-hardware-software.png)|![Diagram przedstawiający stos sprzętu/oprogramowania dla kontenerów platformy Docker.](./media/docker-defined/docker-container-hardware-software.png)|
|<span data-ttu-id="e3f2f-133">Maszyny wirtualne obejmują aplikację, wymagane biblioteki lub pliki binarne oraz pełny system operacyjny gościa.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-133">Virtual machines include the application, the required libraries or binaries, and a full guest operating system.</span></span> <span data-ttu-id="e3f2f-134">Pełna wirtualizacja wymaga więcej zasobów niż konteneryzacji.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-134">Full virtualization requires more resources than containerization.</span></span> | <span data-ttu-id="e3f2f-135">Kontenery obejmują aplikację i wszystkie jej zależności.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-135">Containers include the application and all its dependencies.</span></span> <span data-ttu-id="e3f2f-136">Jednak współużytkują jądro systemu operacyjnego z innymi kontenerami, działa jako pojedyncze procesy w przestrzeni użytkownika w systemie operacyjnym hosta.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-136">However, they share the OS kernel with other containers, running as isolated processes in user space on the host operating system.</span></span> <span data-ttu-id="e3f2f-137">(Z wyjątkiem kontenerów funkcji Hyper-V, gdzie każdy kontener jest uruchamiany wewnątrz specjalnej maszyny wirtualnej na kontener).</span><span class="sxs-lookup"><span data-stu-id="e3f2f-137">(Except in Hyper-V containers, where each container runs inside of a special virtual machine per container.)</span></span> |

<span data-ttu-id="e3f2f-138">**Rysunek 2-3**.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-138">**Figure 2-3**.</span></span> <span data-ttu-id="e3f2f-139">Porównanie tradycyjnych maszyn wirtualnych z kontenerami platformy Docker</span><span class="sxs-lookup"><span data-stu-id="e3f2f-139">Comparison of traditional virtual machines to Docker containers</span></span>

<span data-ttu-id="e3f2f-140">W przypadku maszyn wirtualnych istnieją trzy warstwy podstawowe na serwerze hosta, od dołu do góry: infrastruktura, system operacyjny hosta i hypervisor, a na dodatek każda maszyna wirtualna ma własny system operacyjny i wszystkie niezbędne biblioteki.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-140">For VMs, there are three base layers in the host server, from the bottom-up: infrastructure, Host Operating System and a Hypervisor and on top of all that each VM has its own OS and all necessary libraries.</span></span> <span data-ttu-id="e3f2f-141">Dla platformy Docker serwer hosta ma tylko infrastrukturę i system operacyjny, a na dodatek aparat kontenera, który utrzymuje kontenera izolowane, ale udostępnianie podstawowych usług systemu operacyjnego.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-141">For Docker, the host server only has the infrastructure and the OS and on top of that, the container engine, that keeps container isolated but sharing the base OS services.</span></span>

<span data-ttu-id="e3f2f-142">Ponieważ kontenery wymagają znacznie mniej zasobów (na przykład nie potrzebują pełnego systemu operacyjnego), są łatwe do wdrożenia i uruchamiają się szybko.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-142">Because containers require far fewer resources (for example, they don't need a full OS), they're easy to deploy and they start fast.</span></span> <span data-ttu-id="e3f2f-143">Pozwala to na większą gęstość, co oznacza, że pozwala na uruchamianie większej liczby usług na tej samej jednostce sprzętowej, zmniejszając w ten sposób koszty.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-143">This allows you to have higher density, meaning that it allows you to run more services on the same hardware unit, thereby reducing costs.</span></span>

<span data-ttu-id="e3f2f-144">Jako efekt uboczny uruchamiania na tym samym jądrze, otrzymujesz mniejszą izolację niż maszyny wirtualne.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-144">As a side effect of running on the same kernel, you get less isolation than VMs.</span></span>

<span data-ttu-id="e3f2f-145">Głównym celem obrazu jest, że sprawia, że środowisko (zależności) takie same w różnych wdrożeniach.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-145">The main goal of an image is that it makes the environment (dependencies) the same across different deployments.</span></span> <span data-ttu-id="e3f2f-146">Oznacza to, że można go debugować na komputerze, a następnie wdrożyć go na innym komputerze z tego samego środowiska gwarantowane.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-146">This means that you can debug it on your machine and then deploy it to another machine with the same environment guaranteed.</span></span>

<span data-ttu-id="e3f2f-147">Obraz kontenera to sposób na spakowanie aplikacji lub usługi i wdrożenie jej w niezawodny i powtarzalny sposób.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-147">A container image is a way to package an app or service and deploy it in a reliable and reproducible way.</span></span> <span data-ttu-id="e3f2f-148">Można powiedzieć, że Docker to nie tylko technologia, ale także filozofia i proces.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-148">You could say that Docker isn't only a technology but also a philosophy and a process.</span></span>

<span data-ttu-id="e3f2f-149">Podczas korzystania z platformy Docker, nie usłyszysz deweloperzy mówią: "To działa na moim komputerze, dlaczego nie w środowisku produkcyjnym?"</span><span class="sxs-lookup"><span data-stu-id="e3f2f-149">When using Docker, you won't hear developers say, "It works on my machine, why not in production?"</span></span> <span data-ttu-id="e3f2f-150">Mogą po prostu powiedzieć" Działa na platformie Docker", ponieważ spakowana aplikacja platformy Docker może być wykonywana w dowolnym obsługiwanym środowisku platformy Docker i działa tak, jak była przeznaczona dla wszystkich celów wdrażania (takich jak Dev, QA, staging i production).</span><span class="sxs-lookup"><span data-stu-id="e3f2f-150">They can simply say, "It runs on Docker", because the packaged Docker application can be executed on any supported Docker environment, and it runs the way it was intended to on all deployment targets (such as Dev, QA, staging, and production).</span></span>

## <a name="a-simple-analogy"></a><span data-ttu-id="e3f2f-151">Prosta analogia</span><span class="sxs-lookup"><span data-stu-id="e3f2f-151">A simple analogy</span></span>

<span data-ttu-id="e3f2f-152">Być może prosta analogia może pomóc zrozumieć podstawową koncepcję Docker.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-152">Perhaps a simple analogy can help getting the grasp of the core concept of Docker.</span></span>

<span data-ttu-id="e3f2f-153">Cofniemy się na chwilę do lat 50.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-153">Let's go back in time to the 1950s for a moment.</span></span> <span data-ttu-id="e3f2f-154">Nie było edytorów tekstu, a kserokopiarki były używane wszędzie (rodzaj).</span><span class="sxs-lookup"><span data-stu-id="e3f2f-154">There were no word processors, and the photocopiers were used everywhere (kind of).</span></span>

<span data-ttu-id="e3f2f-155">Wyobraź sobie, że jesteś odpowiedzialny za szybkie wydawanie partii listów zgodnie z wymaganiami, aby wysłać je do klientów, przy użyciu prawdziwego papieru i kopert, które mają być dostarczone fizycznie na adres każdego klienta (nie było wtedy wiadomości e-mail).</span><span class="sxs-lookup"><span data-stu-id="e3f2f-155">Imagine you're responsible for quickly issuing batches of letters as required, to mail them to customers, using real paper and envelopes, to be delivered physically to each customer's address (there was no email back then).</span></span>

<span data-ttu-id="e3f2f-156">W pewnym momencie zdajesz sobie sprawę, że litery są tylko kompozycją dużego zestawu akapitów, które są zbierane i ułożone zgodnie z potrzebami, zgodnie z celem listu, więc opracujesz system szybkiego wydawania listów, spodziewając się potężnego podniesienia.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-156">At some point, you realize the letters are just a composition of a large set of paragraphs, which are picked and arranged as needed, according to the purpose of the letter, so you devise a system to issue letters quickly, expecting to get a hefty raise.</span></span>

<span data-ttu-id="e3f2f-157">System jest prosty:</span><span class="sxs-lookup"><span data-stu-id="e3f2f-157">The system is simple:</span></span>

1. <span data-ttu-id="e3f2f-158">Zaczynasz od talii przezroczystych arkuszy zawierających po jednym akapicie.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-158">You begin with a deck of transparent sheets containing one paragraph each.</span></span>

2. <span data-ttu-id="e3f2f-159">Aby wydać zestaw liter, należy wybrać arkusze z akapitami, których potrzebujesz, a następnie ułożyć i wyrównać je tak, aby wyglądały i dobrze czytały.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-159">To issue a set of letters, you pick the sheets with the paragraphs you need, then you stack and align them so they look and read fine.</span></span>

3. <span data-ttu-id="e3f2f-160">Na koniec umieszczasz zestaw w kserokopiarkai i naciśnij przycisk start, aby wyprodukować tyle liter, ile jest wymagane.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-160">Finally, you place the set in the photocopier and press start to produce as many letters as required.</span></span>

<span data-ttu-id="e3f2f-161">Tak więc, upraszczając, to jest podstawowa idea Docker.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-161">So, simplifying, that's the core idea of Docker.</span></span>

<span data-ttu-id="e3f2f-162">W platformie Docker każda warstwa jest wynikowym zestawem zmian, które zachodzą w systemie plików po wykonaniu polecenia, na przykład o zainstalowaniu programu.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-162">In Docker, each layer is the resulting set of changes that happen to the filesystem after executing a command, such as, installing a program.</span></span>

<span data-ttu-id="e3f2f-163">Tak więc, gdy "patrzysz" na system plików po skopiowaniu warstwy, zobaczysz wszystkie pliki, w tym warstwę, gdy program został zainstalowany.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-163">So, when you "look" at the filesystem after the layer has been copied, you see all the files, included the layer when the program was installed.</span></span>

<span data-ttu-id="e3f2f-164">Obraz można pomyśleć jako pomocniczy dysk twardy tylko do odczytu gotowy do zainstalowania na "komputerze", na którym system operacyjny jest już zainstalowany.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-164">You can think of an image as an auxiliary read-only hard disk ready to be installed in a "computer" where the operating system is already installed.</span></span>

<span data-ttu-id="e3f2f-165">Podobnie można myśleć o kontenerze jako "komputerze" z zainstalowanym dyskiem twardym obrazu.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-165">Similarly, you can think of a container as the "computer" with the image hard disk installed.</span></span> <span data-ttu-id="e3f2f-166">Kontener, podobnie jak komputer, może być włączony lub wyłączony.</span><span class="sxs-lookup"><span data-stu-id="e3f2f-166">The container, just like a computer, can be powered on or off.</span></span>

>[!div class="step-by-step"]
><span data-ttu-id="e3f2f-167">[Poprzedni](index.md)
>[następny](docker-terminology.md)</span><span class="sxs-lookup"><span data-stu-id="e3f2f-167">[Previous](index.md)
[Next](docker-terminology.md)</span></span>
