---
title: Dostawcy CLR ETW
description: 'Przejrzyj szczegółowe informacje o dwóch dostawcach śledzenia zdarzeń środowiska uruchomieniowego języka wspólnego (CLR): dostawcy runtimne i dostawcy.'
ms.date: 03/30/2017
helpviewer_keywords:
- ETW, CLR providers
- CLR ETW providers
ms.assetid: 0beafad4-b2c8-47f4-b342-83411d57a51f
ms.openlocfilehash: 9f86e8334482880c4f7cb23ec93a3c826c083389
ms.sourcegitcommit: 0fa2b7b658bf137e813a7f4d09589d64c148ebf5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 07/14/2020
ms.locfileid: "86309654"
---
# <a name="clr-etw-providers"></a><span data-ttu-id="99275-103">Dostawcy CLR ETW</span><span class="sxs-lookup"><span data-stu-id="99275-103">CLR ETW Providers</span></span>
<span data-ttu-id="99275-104">Środowisko uruchomieniowe języka wspólnego (CLR) ma dwóch dostawców: dostawcę środowiska uruchomieniowego i dostawcę.</span><span class="sxs-lookup"><span data-stu-id="99275-104">The common language runtime (CLR) has two providers: the runtime provider and the rundown provider.</span></span>  
  
 <span data-ttu-id="99275-105">Dostawca środowiska uruchomieniowego zgłasza zdarzenia, w zależności od tego, które słowa kluczowe (kategorie zdarzeń) są włączone.</span><span class="sxs-lookup"><span data-stu-id="99275-105">The runtime provider raises events, depending on which keywords (categories of events) are enabled.</span></span> <span data-ttu-id="99275-106">Na przykład można zbierać zdarzenia modułu ładującego przez włączenie `LoaderKeyword` słowa kluczowego.</span><span class="sxs-lookup"><span data-stu-id="99275-106">For example, you can collect loader events by enabling the `LoaderKeyword` keyword.</span></span>  
  
 <span data-ttu-id="99275-107">Zdarzenia śledzenia zdarzeń systemu Windows (ETW) są rejestrowane w pliku z rozszerzeniem. etl, które może później być przetworzone w plikach z wartościami rozdzielanymi przecinkami (CSV), zgodnie z wymaganiami.</span><span class="sxs-lookup"><span data-stu-id="99275-107">Event Tracing for Windows (ETW) events are logged into a file that has an .etl extension, which can later be post-processed in comma-separated value (.csv) files as needed.</span></span> <span data-ttu-id="99275-108">Informacje o sposobach konwertowania pliku. etl do pliku CSV znajdują się w temacie [sterowanie rejestrowaniem .NET Framework](controlling-logging.md).</span><span class="sxs-lookup"><span data-stu-id="99275-108">For information about how to convert the .etl file to a .csv file, see [Controlling .NET Framework Logging](controlling-logging.md).</span></span>  
  
## <a name="the-runtime-provider"></a><span data-ttu-id="99275-109">Dostawca środowiska uruchomieniowego</span><span class="sxs-lookup"><span data-stu-id="99275-109">The Runtime Provider</span></span>  
 <span data-ttu-id="99275-110">Dostawca środowiska uruchomieniowego jest głównym dostawcą funkcji ETW CLR.</span><span class="sxs-lookup"><span data-stu-id="99275-110">The runtime provider is the main CLR ETW provider.</span></span>  
  
 <span data-ttu-id="99275-111">Identyfikator GUID dostawcy środowiska uruchomieniowego CLR to e13c0d23-ccbc-4e12-931b-d9cc2eee27e4.</span><span class="sxs-lookup"><span data-stu-id="99275-111">The CLR runtime provider GUID is e13c0d23-ccbc-4e12-931b-d9cc2eee27e4.</span></span>  
  
 <span data-ttu-id="99275-112">Aby zapoznać się z przykładami rejestrowania i wyświetlania zdarzeń ETW CLR przy użyciu powszechnie dostępnych narzędzi, zobacz [kontrolowanie rejestrowania .NET Framework](controlling-logging.md).</span><span class="sxs-lookup"><span data-stu-id="99275-112">For examples of how to log and view CLR ETW events by using commonly available tools, see [Controlling .NET Framework Logging](controlling-logging.md).</span></span>  
  
 <span data-ttu-id="99275-113">Oprócz używania słów kluczowych, takich jak `LoaderKeyword` , może być konieczne włączenie słów kluczowych do rejestrowania zdarzeń, które mogą być zgłaszane zbyt często.</span><span class="sxs-lookup"><span data-stu-id="99275-113">In addition to using keywords such as `LoaderKeyword`, you may have to enable keywords for logging events that may be raised too frequently.</span></span> <span data-ttu-id="99275-114">`StartEnumerationKeyword` `EndEnumerationKeyword` Słowa kluczowe i umożliwiają włączenie tych zdarzeń i są podsumowywane w [słowach kluczowych i poziomach funkcji ETW CLR](clr-etw-keywords-and-levels.md).</span><span class="sxs-lookup"><span data-stu-id="99275-114">The `StartEnumerationKeyword` and the `EndEnumerationKeyword` keywords enable these events and are summarized in [CLR ETW Keywords and Levels](clr-etw-keywords-and-levels.md).</span></span>  
  
## <a name="the-rundown-provider"></a><span data-ttu-id="99275-115">Dostawca uwalniania</span><span class="sxs-lookup"><span data-stu-id="99275-115">The Rundown Provider</span></span>  
 <span data-ttu-id="99275-116">Dostawca uwalniania musi być włączony do określonych zastosowań specjalnych.</span><span class="sxs-lookup"><span data-stu-id="99275-116">The rundown provider must be turned on for certain special-purpose uses.</span></span> <span data-ttu-id="99275-117">Jednak w przypadku większości użytkowników należy mieć wystarczające dla nich dostawca środowiska uruchomieniowego.</span><span class="sxs-lookup"><span data-stu-id="99275-117">However, for a majority of users, the runtime provider should suffice.</span></span>  
  
 <span data-ttu-id="99275-118">Identyfikator GUID dostawcy uwalniania CLR to A669021C-C450-4609-A035-5AF59AF4DF18.</span><span class="sxs-lookup"><span data-stu-id="99275-118">The CLR rundown provider GUID is A669021C-C450-4609-A035-5AF59AF4DF18.</span></span>  
  
 <span data-ttu-id="99275-119">Zwykle rejestrowanie ETW jest włączane przed uruchomieniem procesu, a rejestrowanie jest wyłączone po zakończeniu procesu.</span><span class="sxs-lookup"><span data-stu-id="99275-119">Normally, ETW logging is enabled before a process launches, and the logging is turned off after the process exits.</span></span> <span data-ttu-id="99275-120">Jeśli jednak rejestrowanie funkcji ETW jest włączone podczas wykonywania procesu, należy uzyskać dodatkowe informacje o tym procesie.</span><span class="sxs-lookup"><span data-stu-id="99275-120">However, if ETW logging is turned on while the process is executing, additional information is needed about the process.</span></span> <span data-ttu-id="99275-121">Na przykład w przypadku rozpoznawania symboli trzeba rejestrować zdarzenia metody dla metod, które zostały już załadowane przed włączeniem rejestrowania.</span><span class="sxs-lookup"><span data-stu-id="99275-121">For example, for symbol resolution you have to log method events for methods that were already loaded before logging was turned on.</span></span>  
  
 <span data-ttu-id="99275-122">`DCStart`Zdarzenia i `DCEnd` przechwytują stan procesu, gdy zbieranie danych zostało rozpoczęte i zatrzymane.</span><span class="sxs-lookup"><span data-stu-id="99275-122">The `DCStart` and `DCEnd` events capture the state of the process when data collection was started and stopped.</span></span> <span data-ttu-id="99275-123">(Stan dotyczy informacji na wysokim poziomie, w tym metod, które zostały skompilowane wcześniej (JIT) i zestawów, które zostały załadowane.) Te dwa zdarzenia mogą dostarczyć informacji na temat tego, co się już stało w procesie; na przykład, które metody zostały skompilowane JIT i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="99275-123">(State refers to information at a high level, including the methods that were already just-in-time (JIT) compiled and assemblies that were loaded.) These two events can provide information about what has already happened in the process; for example, which methods were JIT- compiled, and so on.</span></span>  
  
 <span data-ttu-id="99275-124">Tylko zdarzenia z `DC` ,, `DCStart` `DCEnd` lub `DCInit` w ich nazwach są zgłaszane w ramach dostawcy uwalniania.</span><span class="sxs-lookup"><span data-stu-id="99275-124">Only the events with `DC`, `DCStart`, `DCEnd`, or `DCInit` in their names are raised under the rundown provider.</span></span> <span data-ttu-id="99275-125">Ponadto te zdarzenia są wywoływane tylko w ramach dostawcy uwalniania.</span><span class="sxs-lookup"><span data-stu-id="99275-125">Additionally, these events are raised only under the rundown provider.</span></span>  
  
 <span data-ttu-id="99275-126">Oprócz filtrów słów kluczowych zdarzenia dostawca uwalniania obsługuje również `StartRundownKeyword` `EndRundownKeyword` słowa kluczowe i, aby zapewnić filtrowanie.</span><span class="sxs-lookup"><span data-stu-id="99275-126">In addition to the event keyword filters, the rundown provider also supports the `StartRundownKeyword` and `EndRundownKeyword` keywords to provide targeted filtering.</span></span>  
  
### <a name="start-rundown"></a><span data-ttu-id="99275-127">Rozpocznij uwalnianie</span><span class="sxs-lookup"><span data-stu-id="99275-127">Start Rundown</span></span>  
 <span data-ttu-id="99275-128">Rozpocznij uwalnianie jest wyzwalane, gdy rejestrowanie w ramach dostawcy uwalniania jest włączone ze `StartRundownKeyword` słowem kluczowym.</span><span class="sxs-lookup"><span data-stu-id="99275-128">A start rundown is triggered when logging under the rundown provider is enabled with the `StartRundownKeyword` keyword.</span></span> <span data-ttu-id="99275-129">Powoduje to wygenerowanie `DCStart` zdarzenia i przechwycenie stanu systemu.</span><span class="sxs-lookup"><span data-stu-id="99275-129">This causes the `DCStart` event to be raised, and captures the state of the system.</span></span> <span data-ttu-id="99275-130">Przed rozpoczęciem wyliczania `DCStartInit` zdarzenie jest zgłaszane.</span><span class="sxs-lookup"><span data-stu-id="99275-130">Before the start of the enumeration, the `DCStartInit` event is raised.</span></span> <span data-ttu-id="99275-131">Na końcu wyliczenia `DCStartComplete` zdarzenie jest zgłaszane w celu powiadomienia kontrolera, że zbieranie danych zostało zakończone normalnie.</span><span class="sxs-lookup"><span data-stu-id="99275-131">At the end of the enumeration, the `DCStartComplete` event is raised to notify the controller that data collection terminated normally.</span></span>  
  
### <a name="end-rundown"></a><span data-ttu-id="99275-132">Zakończ uwalnianie</span><span class="sxs-lookup"><span data-stu-id="99275-132">End Rundown</span></span>  
 <span data-ttu-id="99275-133">Zakończenie uwalniania jest wyzwalane, gdy rejestrowanie w ramach dostawcy uwalniania jest włączone ze `EndRundownKeyword` słowem kluczowym.</span><span class="sxs-lookup"><span data-stu-id="99275-133">An end rundown is triggered when logging under the rundown provider is enabled with the `EndRundownKeyword` keyword.</span></span> <span data-ttu-id="99275-134">Zakończenie końca spowoduje zatrzymanie profilowania dla procesu, który jest nadal wykonywany.</span><span class="sxs-lookup"><span data-stu-id="99275-134">End rundown stops profiling on a process that continues to execute.</span></span> <span data-ttu-id="99275-135">`DCEnd`Zdarzenia przechwytują stan systemu, gdy profilowanie zostało zatrzymane.</span><span class="sxs-lookup"><span data-stu-id="99275-135">The `DCEnd` events capture the state of the system when profiling is stopped.</span></span>  
  
 <span data-ttu-id="99275-136">Przed rozpoczęciem wyliczania `DCEndInit` zdarzenie jest zgłaszane.</span><span class="sxs-lookup"><span data-stu-id="99275-136">Before the start of the enumeration, the `DCEndInit` event is raised.</span></span> <span data-ttu-id="99275-137">Na końcu wyliczenia `DCEndComplete` zdarzenie jest zgłaszane w celu powiadomienia konsumenta, że zbieranie danych zostało zakończone normalnie.</span><span class="sxs-lookup"><span data-stu-id="99275-137">At the end of the enumeration, the `DCEndComplete` event is raised to notify the consumer that data collection terminated normally.</span></span> <span data-ttu-id="99275-138">Początkowy i końcowy uwalnianie są używane głównie do rozpoznawania symboli zarządzanych.</span><span class="sxs-lookup"><span data-stu-id="99275-138">Start rundown and end rundown are primarily used for managed symbol resolution.</span></span> <span data-ttu-id="99275-139">Rozpocznij uwalnianie może dostarczyć informacji o zakresie adresów dla metod, które zostały już skompilowane JIT przed uruchomieniem sesji profilowania.</span><span class="sxs-lookup"><span data-stu-id="99275-139">Start rundown can provide address range information for methods that were already JIT-compiled before the profiling session was started.</span></span> <span data-ttu-id="99275-140">Końcowa kompilacja może dostarczyć informacji o zakresie adresów dla wszystkich metod, które zostały skompilowane JIT, gdy profilowanie ma zostać wyłączone.</span><span class="sxs-lookup"><span data-stu-id="99275-140">End rundown can provide address range information for all methods that have been JIT-compiled when profiling is about to be turned off.</span></span>  
  
 <span data-ttu-id="99275-141">Zakończenie uwalniania nie odbywa się automatycznie, gdy sesja profilowania zostanie zatrzymana.</span><span class="sxs-lookup"><span data-stu-id="99275-141">End rundown does not happen automatically when a profiling session is stopped.</span></span> <span data-ttu-id="99275-142">Zamiast tego narzędzie, które poszukuje funkcji rozpoznawania symboli zarządzanych, musi jawnie wywołać sesję dostawcy uwalniania środowiska CLR z `EndRundownKeyword` włączonym słowem kluczowym tuż przed zatrzymaniem profilowania.</span><span class="sxs-lookup"><span data-stu-id="99275-142">Instead, a tool that is seeking to perform managed symbol resolution has to explicitly invoke a CLR rundown provider session with the `EndRundownKeyword` keyword enabled, just before profiling is stopped.</span></span>  
  
 <span data-ttu-id="99275-143">Chociaż parametr początkowy lub końcowy uwalniania może dostarczyć informacji o zakresie adresów metody dla rozdzielczości symboli zarządzanych, zalecamy użycie `EndRundownKeyword` słowa kluczowego (które dostarcza `DCEnd` zdarzenia) zamiast `StartRundownKeyword` słowa kluczowego (które dostarcza `DCStart` zdarzenia).</span><span class="sxs-lookup"><span data-stu-id="99275-143">Although either start rundown or end rundown can provide method address range information for managed symbol resolution, we recommend that you use the `EndRundownKeyword` keyword (which supplies `DCEnd` events) instead of the `StartRundownKeyword` keyword (which supplies `DCStart` events).</span></span> <span data-ttu-id="99275-144">Użycie `StartRundownKeyword` powoduje, że podczas sesji profilowania zostanie wykonane uwalnianie, co może zakłócać profilowany scenariusz.</span><span class="sxs-lookup"><span data-stu-id="99275-144">Using `StartRundownKeyword` causes the rundown to happen during the profiling session, which may disturb the profiled scenario.</span></span>  
  
## <a name="etw-data-collection-using-runtime-and-rundown-providers"></a><span data-ttu-id="99275-145">Zbieranie danych funkcji ETW przy użyciu dostawców środowiska uruchomieniowego i uwalniania</span><span class="sxs-lookup"><span data-stu-id="99275-145">ETW Data Collection Using Runtime and Rundown Providers</span></span>  
 <span data-ttu-id="99275-146">W poniższym przykładzie pokazano, jak używać dostawcy uwalniania środowiska CLR w taki sposób, aby umożliwiać Rozpoznawanie symboli zarządzanych procesów z minimalnym wpływem, niezależnie od tego, czy procesy są uruchamiane, czy kończyć się w oknie profilowanym.</span><span class="sxs-lookup"><span data-stu-id="99275-146">The following example demonstrates how to use the CLR rundown provider in a way that allows symbol resolution of managed processes with minimal impact, regardless of whether the processes start or end inside or outside the profiled window.</span></span>  
  
1. <span data-ttu-id="99275-147">Włącz rejestrowanie funkcji ETW przy użyciu dostawcy środowiska uruchomieniowego CLR:</span><span class="sxs-lookup"><span data-stu-id="99275-147">Turn on ETW logging by using the CLR runtime provider:</span></span>  
  
    ```console
    xperf -start clr -on e13c0d23-ccbc-4e12-931b-d9cc2eee27e4:0x1CCBD:0x5 -f clr1.etl
    ```  
  
     <span data-ttu-id="99275-148">Dziennik zostanie zapisany w pliku pliku clr1. etl.</span><span class="sxs-lookup"><span data-stu-id="99275-148">The log will be saved to the clr1.etl file.</span></span>  
  
2. <span data-ttu-id="99275-149">Aby zatrzymać profilowanie, gdy proces jest kontynuowany, uruchom dostawcę uwalniania, aby przechwycić `DCEnd` zdarzenia:</span><span class="sxs-lookup"><span data-stu-id="99275-149">To stop profiling while the process continues to execute, start the rundown provider to capture the `DCEnd` events:</span></span>  
  
    ```console
    xperf -start clrRundown -on A669021C-C450-4609-A035-5AF59AF4DF18:0xB8:0x5 -f clr2.etl
    ```  
  
     <span data-ttu-id="99275-150">Umożliwia to zbieranie `DCEnd` zdarzeń w celu uruchomienia sesji uwalniania.</span><span class="sxs-lookup"><span data-stu-id="99275-150">This enables the collection of `DCEnd` events to start a rundown session.</span></span> <span data-ttu-id="99275-151">W przypadku wszystkich zdarzeń, które mają być zbierane, może być konieczne odczekanie od 30 do 60 sekund.</span><span class="sxs-lookup"><span data-stu-id="99275-151">You may need to wait 30 to 60 seconds for all events to be collected.</span></span> <span data-ttu-id="99275-152">Dziennik zostanie zapisany w pliku pliku clr1. ET2.</span><span class="sxs-lookup"><span data-stu-id="99275-152">The log will be saved to the clr1.et2 file.</span></span>  
  
3. <span data-ttu-id="99275-153">Wyłącz wszystkie profilowania ETW:</span><span class="sxs-lookup"><span data-stu-id="99275-153">Turn off all ETW profiling:</span></span>  
  
    ```console
    xperf -stop clrRundown
    xperf -stop clr  
    ```  
  
4. <span data-ttu-id="99275-154">Scal profile, aby utworzyć jeden plik dziennika:</span><span class="sxs-lookup"><span data-stu-id="99275-154">Merge the profiles to create one log file:</span></span>  
  
    ```console
    xperf -merge clr1.etl clr2.etl merged.etl  
    ```  
  
     <span data-ttu-id="99275-155">Scalony plik. etl będzie zawierać zdarzenia z sesji dostawcy środowiska uruchomieniowego i.</span><span class="sxs-lookup"><span data-stu-id="99275-155">The merged.etl file will contain the events from the runtime and the rundown provider sessions.</span></span>  
  
 <span data-ttu-id="99275-156">Za pomocą narzędzia można wykonać kroki 2 i 3 (rozpoczęcie sesji uwalniania, a następnie zakończenie profilowania) zamiast natychmiastowego wyłączenia profilowania, gdy użytkownik zażąda zatrzymania profilowania.</span><span class="sxs-lookup"><span data-stu-id="99275-156">A tool can execute steps 2 and 3 (starting a rundown session and then terminating profiling) instead of immediately turning off profiling when a user requests profiling to be stopped.</span></span> <span data-ttu-id="99275-157">Narzędzie może również wykonać krok 4.</span><span class="sxs-lookup"><span data-stu-id="99275-157">A tool can also execute step 4.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="99275-158">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="99275-158">See also</span></span>

- [<span data-ttu-id="99275-159">Zdarzenia ETW w środowisku CLR</span><span class="sxs-lookup"><span data-stu-id="99275-159">ETW Events in the Common Language Runtime</span></span>](etw-events-in-the-common-language-runtime.md)
