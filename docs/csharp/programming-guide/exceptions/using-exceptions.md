---
title: "Używanie wyjątków (Przewodnik programowania w języku C#)"
ms.date: 07/20/2015
ms.prod: .net
ms.technology: devlang-csharp
ms.topic: article
helpviewer_keywords:
- exception handling [C#], about exception handling
- exceptions [C#], about exceptions
ms.assetid: 71472c62-320a-470a-97d2-67995180389d
caps.latest.revision: "15"
author: BillWagner
ms.author: wiwagn
ms.openlocfilehash: 55c2cc0c6a1f852bd286b98927cc69f81119aeee
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 11/21/2017
---
# <a name="using-exceptions-c-programming-guide"></a><span data-ttu-id="2b5b4-102">Używanie wyjątków (Przewodnik programowania w języku C#)</span><span class="sxs-lookup"><span data-stu-id="2b5b4-102">Using Exceptions (C# Programming Guide)</span></span>
<span data-ttu-id="2b5b4-103">W języku C# błędów w programie w czasie wykonywania są propagowane za pośrednictwem programu przy użyciu mechanizmu o nazwie wyjątków.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-103">In C#, errors in the program at run time are propagated through the program by using a mechanism called exceptions.</span></span> <span data-ttu-id="2b5b4-104">Wyjątki są zgłaszane przez kod, który wystąpi błąd i przechwycony przez kod, który może poprawić ten błąd.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-104">Exceptions are thrown by code that encounters an error and caught by code that can correct the error.</span></span> <span data-ttu-id="2b5b4-105">Wyjątki może zostać wygenerowany przez .NET Framework środowisko uruchomieniowe języka wspólnego (CLR) lub kodu w programie.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-105">Exceptions can be thrown by the .NET Framework common language runtime (CLR) or by code in a program.</span></span> <span data-ttu-id="2b5b4-106">Po wyjątku rozprzestrzenia się górę stosu wywołań do `catch` instrukcji dla wyjątku został znaleziony.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-106">Once an exception is thrown, it propagates up the call stack until a `catch` statement for the exception is found.</span></span> <span data-ttu-id="2b5b4-107">Nieprzechwyconych wyjątków są obsługiwane przez program obsługi wyjątków ogólnych obsługiwanych przez system, który wyświetla okno dialogowe.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-107">Uncaught exceptions are handled by a generic exception handler provided by the system that displays a dialog box.</span></span>  
  
 <span data-ttu-id="2b5b4-108">Wyjątki są reprezentowane przez klasy pochodzące od <xref:System.Exception>.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-108">Exceptions are represented by classes derived from <xref:System.Exception>.</span></span> <span data-ttu-id="2b5b4-109">Ta klasa zawiera typ wyjątku i zawiera właściwości, które zawierały wszystkie szczegółowe informacje o wyjątku.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-109">This class identifies the type of exception and contains properties that have details about the exception.</span></span> <span data-ttu-id="2b5b4-110">Zgłaszanie wyjątku obejmuje tworzenie wystąpienia klasy pochodnej wyjątek, opcjonalnie konfigurowania właściwości wyjątku i następnie zgłaszanie obiektu przy użyciu `throw` — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-110">Throwing an exception involves creating an instance of an exception-derived class, optionally configuring properties of the exception, and then throwing the object by using the `throw` keyword.</span></span> <span data-ttu-id="2b5b4-111">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="2b5b4-111">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#1](../../../csharp/programming-guide/exceptions/codesnippet/CSharp/using-exceptions_1.cs)]  
  
 <span data-ttu-id="2b5b4-112">Po wyjątek środowiska uruchomieniowego sprawdza bieżącej instrukcji, aby zobaczyć, czy znajduje się w `try` bloku.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-112">After an exception is thrown, the runtime checks the current statement to see whether it is within a `try` block.</span></span> <span data-ttu-id="2b5b4-113">Jeśli tak jest, wszelkie `catch` bloki skojarzone z `try` bloku jest sprawdzenie, czy ich catch wyjątku.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-113">If it is, any `catch` blocks associated with the `try` block are checked to see whether they can catch the exception.</span></span> <span data-ttu-id="2b5b4-114">`Catch`bloki zazwyczaj określić typy wyjątków; Jeśli typ `catch` blok jest taki sam typ jak wyjątek lub klasę podstawową wyjątku, `catch` bloku może obsługiwać metodę.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-114">`Catch` blocks typically specify exception types; if the type of the `catch` block is the same type as the exception, or a base class of the exception, the `catch` block can handle the method.</span></span> <span data-ttu-id="2b5b4-115">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="2b5b4-115">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#2](../../../csharp/programming-guide/exceptions/codesnippet/CSharp/using-exceptions_2.cs)]  
  
 <span data-ttu-id="2b5b4-116">Jeśli instrukcja, która zgłasza wyjątek, nie jest w `try` bloku lub, jeśli `try` bloku ograniczający go nie posiada odpowiadającego `catch` bloku, środowisko uruchomieniowe sprawdza wywołania metody `try` instrukcji i `catch` bloków.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-116">If the statement that throws an exception is not within a `try` block or if the `try` block that encloses it has no matching `catch` block, the runtime checks the calling method for a `try` statement and `catch` blocks.</span></span> <span data-ttu-id="2b5b4-117">Środowisko uruchomieniowe nadal górę stosu wywołania, wyszukiwanie zgodnego `catch` bloku.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-117">The runtime continues up the calling stack, searching for a compatible `catch` block.</span></span> <span data-ttu-id="2b5b4-118">Po `catch` blok jest znaleziono i wykonać, kontrola jest przekazywana do następnej instrukcji, po którym `catch` bloku.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-118">After the `catch` block is found and executed, control is passed to the next statement after that `catch` block.</span></span>  
  
 <span data-ttu-id="2b5b4-119">A `try` instrukcji może zawierać więcej niż jedną `catch` bloku.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-119">A `try` statement can contain more than one `catch` block.</span></span> <span data-ttu-id="2b5b4-120">Pierwszy `catch` instrukcji, która może obsłużyć wyjątek jest wykonywane; wszystkie następujące `catch` instrukcje, nawet jeśli są one zgodne, są ignorowane.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-120">The first `catch` statement that can handle the exception is executed; any following `catch` statements, even if they are compatible, are ignored.</span></span> <span data-ttu-id="2b5b4-121">W związku z tym catch bloki zawsze powinna być wyświetlana z najbardziej określonego (lub pochodnego większość) do najmniej specyficznych.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-121">Therefore, catch blocks should always be ordered from most specific (or most-derived) to least specific.</span></span> <span data-ttu-id="2b5b4-122">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="2b5b4-122">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#3](../../../csharp/programming-guide/exceptions/codesnippet/CSharp/using-exceptions_3.cs)]  
  
 <span data-ttu-id="2b5b4-123">Przed `catch` wykonaniu bloku środowiska uruchomieniowego sprawdza, czy `finally` bloków.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-123">Before the `catch` block is executed, the runtime checks for `finally` blocks.</span></span> <span data-ttu-id="2b5b4-124">`Finally`bloki włączyć programisty wyczyścić niejednoznaczne stan może być pozostałe z przerwane `try` bloku, lub aby zwolnić dowolnych zasobów zewnętrznych (na przykład grafiki dojść, połączenia z bazą danych lub plików strumieni) bez oczekiwania na odzyskiwanie Moduł zbierający w środowisku uruchomieniowym finalize obiektów.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-124">`Finally` blocks enable the programmer to clean up any ambiguous state that could be left over from an aborted `try` block, or to release any external resources (such as graphics handles, database connections or file streams) without waiting for the garbage collector in the runtime to finalize the objects.</span></span> <span data-ttu-id="2b5b4-125">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="2b5b4-125">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#4](../../../csharp/programming-guide/exceptions/codesnippet/CSharp/using-exceptions_4.cs)]  
  
 <span data-ttu-id="2b5b4-126">Jeśli `WriteByte()` zwrócił wyjątek, kod w ciągu sekundy `try` bloku, który próbuje ponownie otworzyć plik będą się kończyć niepowodzeniem, jeśli `file.Close()` nie jest wywoływany, a plik pozostanie zablokowane.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-126">If `WriteByte()` threw an exception, the code in the second `try` block that tries to reopen the file would fail if `file.Close()` is not called, and the file would remain locked.</span></span> <span data-ttu-id="2b5b4-127">Ponieważ `finally` bloki są wykonywane, nawet jeśli wyjątek `finally` umożliwia bloku w poprzednim przykładzie plik zostanie zamknięty poprawnie i pozwala uniknąć wystąpił błąd.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-127">Because `finally` blocks are executed even if an exception is thrown, the `finally` block in the previous example allows for the file to be closed correctly and helps avoid an error.</span></span>  
  
 <span data-ttu-id="2b5b4-128">Jeśli zgodnego `catch` bloku znajduje się w stosie wywołań po wyjątek wystąpi jedno z trzech zdarzeń:</span><span class="sxs-lookup"><span data-stu-id="2b5b4-128">If no compatible `catch` block is found on the call stack after an exception is thrown, one of three things occurs:</span></span>  
  
-   <span data-ttu-id="2b5b4-129">Jeśli wyjątek jest w finalizator, finalizator zostało przerwane i podstawowej finalizator, jest wywoływana.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-129">If the exception is within a finalizer, the finalizer is aborted and the base finalizer, if any, is called.</span></span>  
  
-   <span data-ttu-id="2b5b4-130">Jeśli stos wywołań zawiera w konstruktorze statycznym lub inicjatorze pola statycznego <xref:System.TypeInitializationException> jest zgłaszany, z wyjątkiem oryginalnego przypisane do <xref:System.Exception.InnerException%2A> właściwości nowej wyjątek.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-130">If the call stack contains a static constructor, or a static field initializer, a <xref:System.TypeInitializationException> is thrown, with the original exception assigned to the <xref:System.Exception.InnerException%2A> property of the new exception.</span></span>  
  
-   <span data-ttu-id="2b5b4-131">Osiągnięto początek wątku zakończenia wątku.</span><span class="sxs-lookup"><span data-stu-id="2b5b4-131">If the start of the thread is reached, the thread is terminated.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="2b5b4-132">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="2b5b4-132">See Also</span></span>  
 [<span data-ttu-id="2b5b4-133">Przewodnik programowania w języku C#</span><span class="sxs-lookup"><span data-stu-id="2b5b4-133">C# Programming Guide</span></span>](../../../csharp/programming-guide/index.md)  
 [<span data-ttu-id="2b5b4-134">Wyjątki i obsługa wyjątków</span><span class="sxs-lookup"><span data-stu-id="2b5b4-134">Exceptions and Exception Handling</span></span>](../../../csharp/programming-guide/exceptions/index.md)
