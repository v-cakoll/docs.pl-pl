---
title: Korzystanie z wyjątków — Przewodnik programowania w języku C#
ms.date: 07/20/2015
helpviewer_keywords:
- exception handling [C#], about exception handling
- exceptions [C#], about exceptions
ms.assetid: 71472c62-320a-470a-97d2-67995180389d
ms.openlocfilehash: a00259dfd5634ad9b9c951c3cd76da97afe5077d
ms.sourcegitcommit: a241301495a84cc8c64fe972330d16edd619868b
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/01/2020
ms.locfileid: "84241698"
---
# <a name="use-exceptions-c-programming-guide"></a><span data-ttu-id="5d3ce-102">Używanie wyjątków (Przewodnik programowania w języku C#)</span><span class="sxs-lookup"><span data-stu-id="5d3ce-102">Use exceptions (C# programming guide)</span></span>

<span data-ttu-id="5d3ce-103">W języku C# błędy w programie w czasie wykonywania są propagowane za pośrednictwem programu przy użyciu mechanizmu nazywanego wyjątkami.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-103">In C#, errors in the program at run time are propagated through the program by using a mechanism called exceptions.</span></span> <span data-ttu-id="5d3ce-104">Wyjątki są zgłaszane przez kod, który napotka błąd i przechwycony przez kod, który może poprawić błąd.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-104">Exceptions are thrown by code that encounters an error and caught by code that can correct the error.</span></span> <span data-ttu-id="5d3ce-105">Wyjątki mogą być zgłaszane przez środowisko uruchomieniowe .NET lub kod w programie.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-105">Exceptions can be thrown by the .NET runtime or by code in a program.</span></span> <span data-ttu-id="5d3ce-106">Gdy wyjątek jest zgłaszany, propaguje stos wywołań do momentu `catch` znalezienia instrukcji dla wyjątku.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-106">Once an exception is thrown, it propagates up the call stack until a `catch` statement for the exception is found.</span></span> <span data-ttu-id="5d3ce-107">Nieprzechwycone wyjątki są obsługiwane przez ogólną procedurę obsługi wyjątków dostarczoną przez system, który wyświetla okno dialogowe.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-107">Uncaught exceptions are handled by a generic exception handler provided by the system that displays a dialog box.</span></span>  
  
 <span data-ttu-id="5d3ce-108">Wyjątki są reprezentowane przez klasy pochodne od <xref:System.Exception> .</span><span class="sxs-lookup"><span data-stu-id="5d3ce-108">Exceptions are represented by classes derived from <xref:System.Exception>.</span></span> <span data-ttu-id="5d3ce-109">Ta klasa identyfikuje typ wyjątku i zawiera właściwości, które zawierają szczegółowe informacje o wyjątku.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-109">This class identifies the type of exception and contains properties that have details about the exception.</span></span> <span data-ttu-id="5d3ce-110">Zgłaszanie wyjątku obejmuje utworzenie wystąpienia klasy pochodnej wyjątku, opcjonalnie skonfigurowanie właściwości wyjątku, a następnie wygenerowanie obiektu za pomocą `throw` słowa kluczowego.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-110">Throwing an exception involves creating an instance of an exception-derived class, optionally configuring properties of the exception, and then throwing the object by using the `throw` keyword.</span></span> <span data-ttu-id="5d3ce-111">Przykład:</span><span class="sxs-lookup"><span data-stu-id="5d3ce-111">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#1](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#1)]  
  
 <span data-ttu-id="5d3ce-112">Po zgłoszeniu wyjątku środowisko uruchomieniowe sprawdza bieżącą instrukcję, aby sprawdzić, czy znajduje się w `try` bloku.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-112">After an exception is thrown, the runtime checks the current statement to see whether it is within a `try` block.</span></span> <span data-ttu-id="5d3ce-113">Jeśli tak jest, wszystkie `catch` bloki skojarzone z `try` blokiem są sprawdzane w celu sprawdzenia, czy mogą przechwytywać wyjątek.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-113">If it is, any `catch` blocks associated with the `try` block are checked to see whether they can catch the exception.</span></span> <span data-ttu-id="5d3ce-114">`Catch`bloki zwykle określają typy wyjątków; Jeśli typ `catch` bloku jest tego samego typu co wyjątek lub Klasa bazowa wyjątku, `catch` blok może obsłużyć metodę.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-114">`Catch` blocks typically specify exception types; if the type of the `catch` block is the same type as the exception, or a base class of the exception, the `catch` block can handle the method.</span></span> <span data-ttu-id="5d3ce-115">Przykład:</span><span class="sxs-lookup"><span data-stu-id="5d3ce-115">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#2](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#2)]  
  
 <span data-ttu-id="5d3ce-116">Jeśli instrukcja zwracająca wyjątek nie znajduje się w `try` bloku lub jeśli `try` blok, który go zawiera, nie ma pasującego `catch` bloku, środowisko uruchomieniowe sprawdza metodę wywołującą dla `try` instrukcji i `catch` bloków.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-116">If the statement that throws an exception is not within a `try` block or if the `try` block that encloses it has no matching `catch` block, the runtime checks the calling method for a `try` statement and `catch` blocks.</span></span> <span data-ttu-id="5d3ce-117">Środowisko uruchomieniowe kontynuuje wywoływanie stosu, wyszukując zgodny `catch` blok.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-117">The runtime continues up the calling stack, searching for a compatible `catch` block.</span></span> <span data-ttu-id="5d3ce-118">Po `catch` znalezieniu i wykonaniu bloku, kontrola jest przenoszona do następnej instrukcji po tym `catch` bloku.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-118">After the `catch` block is found and executed, control is passed to the next statement after that `catch` block.</span></span>  
  
 <span data-ttu-id="5d3ce-119">`try`Instrukcja może zawierać więcej niż jeden `catch` blok.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-119">A `try` statement can contain more than one `catch` block.</span></span> <span data-ttu-id="5d3ce-120">Pierwsza `catch` instrukcja, która może obsłużyć wyjątek, jest wykonywana; wszelkie poniższe `catch` instrukcje, nawet jeśli są zgodne, są ignorowane.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-120">The first `catch` statement that can handle the exception is executed; any following `catch` statements, even if they are compatible, are ignored.</span></span> <span data-ttu-id="5d3ce-121">W związku z tym bloki catch powinny zawsze być uporządkowane z najbardziej specyficznych (lub najbardziej pochodnych) do najmniej określonych.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-121">Therefore, catch blocks should always be ordered from most specific (or most-derived) to least specific.</span></span> <span data-ttu-id="5d3ce-122">Przykład:</span><span class="sxs-lookup"><span data-stu-id="5d3ce-122">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#3)]  
  
 <span data-ttu-id="5d3ce-123">Przed `catch` wykonaniem bloku program sprawdza bloki w czasie wykonywania `finally` .</span><span class="sxs-lookup"><span data-stu-id="5d3ce-123">Before the `catch` block is executed, the runtime checks for `finally` blocks.</span></span> <span data-ttu-id="5d3ce-124">`Finally`bloki umożliwiają programistom czyszczenie dowolnego niejednoznacznego stanu, który może być pozostawiony przez przerwany `try` blok, lub do zwolnienia zasobów zewnętrznych (takich jak uchwyty grafiki, połączenia bazy danych lub strumienie plików) bez oczekiwania na zakończenie działania modułu zbierającego elementy bezużyteczne w środowisku uruchomieniowym.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-124">`Finally` blocks enable the programmer to clean up any ambiguous state that could be left over from an aborted `try` block, or to release any external resources (such as graphics handles, database connections or file streams) without waiting for the garbage collector in the runtime to finalize the objects.</span></span> <span data-ttu-id="5d3ce-125">Przykład:</span><span class="sxs-lookup"><span data-stu-id="5d3ce-125">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#4)]  
  
 <span data-ttu-id="5d3ce-126">W przypadku wypełniania `WriteByte()` wyjątku kod w drugim `try` bloku próbujący ponownie otworzyć plik mógłby się nie powieść `file.Close()` , jeśli nie zostanie wywołany, a plik pozostanie zablokowany.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-126">If `WriteByte()` threw an exception, the code in the second `try` block that tries to reopen the file would fail if `file.Close()` is not called, and the file would remain locked.</span></span> <span data-ttu-id="5d3ce-127">Ponieważ `finally` bloki są wykonywane nawet w przypadku zgłoszenia wyjątku, `finally` blok w poprzednim przykładzie umożliwia poprawne zamknięcie pliku i pomaga uniknąć błędu.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-127">Because `finally` blocks are executed even if an exception is thrown, the `finally` block in the previous example allows for the file to be closed correctly and helps avoid an error.</span></span>  
  
 <span data-ttu-id="5d3ce-128">Jeśli nie `catch` odnaleziono zgodnego bloku w stosie wywołań po zgłoszeniu wyjątku, wystąpi jedno z trzech rzeczy:</span><span class="sxs-lookup"><span data-stu-id="5d3ce-128">If no compatible `catch` block is found on the call stack after an exception is thrown, one of three things occurs:</span></span>  
  
- <span data-ttu-id="5d3ce-129">Jeśli wyjątek znajduje się w obrębie finalizatora, finalizator zostanie przerwany i zostanie wywołany podstawowy finalizator, jeśli istnieje.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-129">If the exception is within a finalizer, the finalizer is aborted and the base finalizer, if any, is called.</span></span>  
  
- <span data-ttu-id="5d3ce-130">Jeśli stos wywołań zawiera statyczny Konstruktor lub zostaje zgłoszony inicjator pola statycznego, <xref:System.TypeInitializationException> z pierwotnym wyjątkiem przypisanym do <xref:System.Exception.InnerException%2A> właściwości nowego wyjątku.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-130">If the call stack contains a static constructor, or a static field initializer, a <xref:System.TypeInitializationException> is thrown, with the original exception assigned to the <xref:System.Exception.InnerException%2A> property of the new exception.</span></span>  
  
- <span data-ttu-id="5d3ce-131">Jeśli początek wątku zostanie osiągnięty, wątek zostanie zakończony.</span><span class="sxs-lookup"><span data-stu-id="5d3ce-131">If the start of the thread is reached, the thread is terminated.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="5d3ce-132">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="5d3ce-132">See also</span></span>

- [<span data-ttu-id="5d3ce-133">Przewodnik programowania w języku C#</span><span class="sxs-lookup"><span data-stu-id="5d3ce-133">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="5d3ce-134">Wyjątki i obsługa wyjątków</span><span class="sxs-lookup"><span data-stu-id="5d3ce-134">Exceptions and Exception Handling</span></span>](./index.md)
