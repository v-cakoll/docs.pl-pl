---
title: Najlepsze praktyki dotyczące ładowania zestawu
ms.date: 03/30/2017
helpviewer_keywords:
- assemblies,binding
- LoadFrom method
- default load context
- TypeLoadException class,causes
- assembly loading errors
- load contexts
- assemblies,loading
- LoadWithPartialName method
- load-from context
ms.assetid: 68d1c539-6a47-4614-ab59-4b071c9d4b4c
ms.openlocfilehash: d1b6c2cd9f96a4acf48cbced48a86bc3e3409562
ms.sourcegitcommit: 5f236cd78cf09593c8945a7d753e0850e96a0b80
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 01/07/2020
ms.locfileid: "75716587"
---
# <a name="best-practices-for-assembly-loading"></a><span data-ttu-id="db438-102">Najlepsze praktyki dotyczące ładowania zestawu</span><span class="sxs-lookup"><span data-stu-id="db438-102">Best Practices for Assembly Loading</span></span>
<span data-ttu-id="db438-103">W tym artykule omówiono sposoby uniknięcia problemów z tożsamością typu, które mogą prowadzić do <xref:System.InvalidCastException>, <xref:System.MissingMethodException>i innych błędów.</span><span class="sxs-lookup"><span data-stu-id="db438-103">This article discusses ways to avoid problems of type identity that can lead to <xref:System.InvalidCastException>, <xref:System.MissingMethodException>, and other errors.</span></span> <span data-ttu-id="db438-104">W tym artykule omówiono następujące zalecenia:</span><span class="sxs-lookup"><span data-stu-id="db438-104">The article discusses the following recommendations:</span></span>  
  
- [<span data-ttu-id="db438-105">Zapoznaj się z zaletami i wadami kontekstów ładowania</span><span class="sxs-lookup"><span data-stu-id="db438-105">Understand the advantages and disadvantages of load contexts</span></span>](#load_contexts)  
  
- [<span data-ttu-id="db438-106">Unikaj tworzenia powiązań dla częściowych nazw zestawów</span><span class="sxs-lookup"><span data-stu-id="db438-106">Avoid binding on partial assembly names</span></span>](#avoid_partial_names)  
  
- [<span data-ttu-id="db438-107">Unikanie ładowania zestawu do wielu kontekstów</span><span class="sxs-lookup"><span data-stu-id="db438-107">Avoid loading an assembly into multiple contexts</span></span>](#avoid_loading_into_multiple_contexts)  
  
- [<span data-ttu-id="db438-108">Unikaj ładowania wielu wersji zestawu do tego samego kontekstu</span><span class="sxs-lookup"><span data-stu-id="db438-108">Avoid loading multiple versions of an assembly into the same context</span></span>](#avoid_loading_multiple_versions)  
  
- [<span data-ttu-id="db438-109">Rozważ przełączenie do domyślnego kontekstu ładowania</span><span class="sxs-lookup"><span data-stu-id="db438-109">Consider switching to the default load context</span></span>](#switch_to_default)  
  
 <span data-ttu-id="db438-110">Pierwsze zalecenie, [zrozumiałe zalety i wady kontekstów ładowania](#load_contexts), zawiera ogólne informacje na temat innych zaleceń, ponieważ wszystkie zależą od znajomości kontekstów ładowania.</span><span class="sxs-lookup"><span data-stu-id="db438-110">The first recommendation, [understand the advantages and disadvantages of load contexts](#load_contexts), provides background information for the other recommendations, because they all depend on a knowledge of load contexts.</span></span>  
  
<a name="load_contexts"></a>   
## <a name="understand-the-advantages-and-disadvantages-of-load-contexts"></a><span data-ttu-id="db438-111">Zapoznaj się z zaletami i wadami kontekstów ładowania</span><span class="sxs-lookup"><span data-stu-id="db438-111">Understand the Advantages and Disadvantages of Load Contexts</span></span>  
 <span data-ttu-id="db438-112">W domenie aplikacji zestawy mogą być ładowane do jednego z trzech kontekstów lub mogą być ładowane bez kontekstu:</span><span class="sxs-lookup"><span data-stu-id="db438-112">Within an application domain, assemblies can be loaded into one of three contexts, or they can be loaded without context:</span></span>  
  
- <span data-ttu-id="db438-113">Domyślny kontekst ładowania zawiera zestawy znalezione przez sondowanie globalnej pamięci podręcznej zestawów, magazyn zestawów hosta, jeśli środowisko uruchomieniowe jest hostowane (na przykład w SQL Server) i <xref:System.AppDomainSetup.ApplicationBase%2A> i <xref:System.AppDomainSetup.PrivateBinPath%2A> domeny aplikacji.</span><span class="sxs-lookup"><span data-stu-id="db438-113">The default load context contains assemblies found by probing the global assembly cache, the host assembly store if the runtime is hosted (for example, in SQL Server), and the <xref:System.AppDomainSetup.ApplicationBase%2A> and <xref:System.AppDomainSetup.PrivateBinPath%2A> of the application domain.</span></span> <span data-ttu-id="db438-114">Większość przeciążenia metody <xref:System.Reflection.Assembly.Load%2A> ładowania zestawów do tego kontekstu.</span><span class="sxs-lookup"><span data-stu-id="db438-114">Most overloads of the <xref:System.Reflection.Assembly.Load%2A> method load assemblies into this context.</span></span>  
  
- <span data-ttu-id="db438-115">Kontekst ładowania z zawiera zestawy, które są ładowane z lokalizacji, które nie są przeszukiwane przez moduł ładujący.</span><span class="sxs-lookup"><span data-stu-id="db438-115">The load-from context contains assemblies that are loaded from locations that are not searched by the loader.</span></span> <span data-ttu-id="db438-116">Na przykład, dodatki mogą być zainstalowane w katalogu, który nie znajduje się w ścieżce aplikacji.</span><span class="sxs-lookup"><span data-stu-id="db438-116">For example, add-ins might be installed in a directory that is not under the application path.</span></span> <span data-ttu-id="db438-117"><xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>, <xref:System.AppDomain.CreateInstanceFrom%2A?displayProperty=nameWithType>i <xref:System.AppDomain.ExecuteAssembly%2A?displayProperty=nameWithType> są przykładami metod, które ładują się według ścieżki.</span><span class="sxs-lookup"><span data-stu-id="db438-117"><xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>, <xref:System.AppDomain.CreateInstanceFrom%2A?displayProperty=nameWithType>, and <xref:System.AppDomain.ExecuteAssembly%2A?displayProperty=nameWithType> are examples of methods that load by path.</span></span>  
  
- <span data-ttu-id="db438-118">Kontekst tylko odbicia zawiera zestawy ładowane przy użyciu metod <xref:System.Reflection.Assembly.ReflectionOnlyLoad%2A> i <xref:System.Reflection.Assembly.ReflectionOnlyLoadFrom%2A>.</span><span class="sxs-lookup"><span data-stu-id="db438-118">The reflection-only context contains assemblies loaded with the <xref:System.Reflection.Assembly.ReflectionOnlyLoad%2A> and <xref:System.Reflection.Assembly.ReflectionOnlyLoadFrom%2A> methods.</span></span> <span data-ttu-id="db438-119">Nie można wykonać kodu w tym kontekście, dlatego nie jest on omawiany w tym miejscu.</span><span class="sxs-lookup"><span data-stu-id="db438-119">Code in this context cannot be executed, so it is not discussed here.</span></span> <span data-ttu-id="db438-120">Aby uzyskać więcej informacji, zobacz [instrukcje: ładowanie zestawów do kontekstu przeznaczonego tylko do odbicia](../reflection-and-codedom/how-to-load-assemblies-into-the-reflection-only-context.md).</span><span class="sxs-lookup"><span data-stu-id="db438-120">For more information, see [How to: Load Assemblies into the Reflection-Only Context](../reflection-and-codedom/how-to-load-assemblies-into-the-reflection-only-context.md).</span></span>  
  
- <span data-ttu-id="db438-121">Jeśli wygenerowałeś tymczasowy zestaw dynamiczny przy użyciu emisji odbicia, zestaw nie jest w żadnym kontekście.</span><span class="sxs-lookup"><span data-stu-id="db438-121">If you generated a transient dynamic assembly by using reflection emit, the assembly is not in any context.</span></span> <span data-ttu-id="db438-122">Ponadto większość zestawów, które są ładowane przy użyciu metody <xref:System.Reflection.Assembly.LoadFile%2A> są ładowane bez kontekstu, a zestawy, które są ładowane z tablic bajtowych są ładowane bez kontekstu, chyba że ich tożsamość (po zastosowaniu zasad) ustali, że znajdują się w globalnej pamięci podręcznej zestawów.</span><span class="sxs-lookup"><span data-stu-id="db438-122">In addition, most assemblies that are loaded by using the <xref:System.Reflection.Assembly.LoadFile%2A> method are loaded without context, and assemblies that are loaded from byte arrays are loaded without context unless their identity (after policy is applied) establishes that they are in the global assembly cache.</span></span>  
  
 <span data-ttu-id="db438-123">Konteksty wykonywania mają zalety i wady, zgodnie z opisem w poniższych sekcjach.</span><span class="sxs-lookup"><span data-stu-id="db438-123">The execution contexts have advantages and disadvantages, as discussed in the following sections.</span></span>  
  
### <a name="default-load-context"></a><span data-ttu-id="db438-124">Domyślny kontekst ładowania</span><span class="sxs-lookup"><span data-stu-id="db438-124">Default Load Context</span></span>  
 <span data-ttu-id="db438-125">Gdy zestawy są ładowane do domyślnego kontekstu ładowania, ich zależności są ładowane automatycznie.</span><span class="sxs-lookup"><span data-stu-id="db438-125">When assemblies are loaded into the default load context, their dependencies are loaded automatically.</span></span> <span data-ttu-id="db438-126">Zależności, które są ładowane do domyślnego kontekstu ładowania, są automatycznie dostępne dla zestawów w domyślnym kontekście ładowania lub w kontekście ładowania z.</span><span class="sxs-lookup"><span data-stu-id="db438-126">Dependencies that are loaded into the default load context are found automatically for assemblies in the default load context or the load-from context.</span></span> <span data-ttu-id="db438-127">Ładowanie przez tożsamość zestawu zwiększa stabilność aplikacji przez zapewnienie, że nieznane wersje zestawów nie są używane (zobacz sekcję [Unikaj tworzenia powiązań dla częściowych nazw zestawów](#avoid_partial_names) ).</span><span class="sxs-lookup"><span data-stu-id="db438-127">Loading by assembly identity increases the stability of applications by ensuring that unknown versions of assemblies are not used (see the [Avoid Binding on Partial Assembly Names](#avoid_partial_names) section).</span></span>  
  
 <span data-ttu-id="db438-128">Użycie domyślnego kontekstu ładowania ma następujące wady:</span><span class="sxs-lookup"><span data-stu-id="db438-128">Using the default load context has the following disadvantages:</span></span>  
  
- <span data-ttu-id="db438-129">Zależności, które są ładowane do innych kontekstów, są niedostępne.</span><span class="sxs-lookup"><span data-stu-id="db438-129">Dependencies that are loaded into other contexts are not available.</span></span>  
  
- <span data-ttu-id="db438-130">Nie można załadować zestawów z lokalizacji poza ścieżką sondy do domyślnego kontekstu ładowania.</span><span class="sxs-lookup"><span data-stu-id="db438-130">You cannot load assemblies from locations outside the probing path into the default load context.</span></span>  
  
### <a name="load-from-context"></a><span data-ttu-id="db438-131">Kontekst ładowania z</span><span class="sxs-lookup"><span data-stu-id="db438-131">Load-From Context</span></span>  
 <span data-ttu-id="db438-132">Kontekst załadowania umożliwia załadowanie zestawu ze ścieżki, która nie znajduje się pod ścieżką aplikacji i w związku z tym nie jest uwzględniona w trakcie sondowania.</span><span class="sxs-lookup"><span data-stu-id="db438-132">The load-from context lets you load an assembly from a path that is not under the application path, and therefore is not included in probing.</span></span> <span data-ttu-id="db438-133">Pozwala na zlokalizowanie i załadowanie zależności od tej ścieżki, ponieważ informacje o ścieżce są obsługiwane przez kontekst.</span><span class="sxs-lookup"><span data-stu-id="db438-133">It enables dependencies to be located and loaded from that path, because the path information is maintained by the context.</span></span> <span data-ttu-id="db438-134">Ponadto zestawy w tym kontekście mogą korzystać z zależności, które są ładowane do domyślnego kontekstu ładowania.</span><span class="sxs-lookup"><span data-stu-id="db438-134">In addition, assemblies in this context can use dependencies that are loaded into the default load context.</span></span>  
  
 <span data-ttu-id="db438-135">Ładowanie zestawów przy użyciu metody <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> lub jednej z innych metod, które ładują według ścieżki, ma następujące wady:</span><span class="sxs-lookup"><span data-stu-id="db438-135">Loading assemblies by using the <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> method, or one of the other methods that load by path, has the following disadvantages:</span></span>  
  
- <span data-ttu-id="db438-136">Jeśli zestaw o tej samej tożsamości jest już załadowany, <xref:System.Reflection.Assembly.LoadFrom%2A> zwraca załadowany zestaw, nawet jeśli określono inną ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="db438-136">If an assembly with the same identity is already loaded, <xref:System.Reflection.Assembly.LoadFrom%2A> returns the loaded assembly even if a different path was specified.</span></span>  
  
- <span data-ttu-id="db438-137">Jeśli zestaw jest ładowany z <xref:System.Reflection.Assembly.LoadFrom%2A>, a później zestaw w domyślnym kontekście ładowania podejmie próbę załadowania tego samego zestawu według nazwy wyświetlanej, próba załadowania zakończy się niepowodzeniem.</span><span class="sxs-lookup"><span data-stu-id="db438-137">If an assembly is loaded with <xref:System.Reflection.Assembly.LoadFrom%2A>, and later an assembly in the default load context tries to load the same assembly by display name, the load attempt fails.</span></span> <span data-ttu-id="db438-138">Taka sytuacja może wystąpić w przypadku deserializacji zestawu.</span><span class="sxs-lookup"><span data-stu-id="db438-138">This can occur when an assembly is deserialized.</span></span>  
  
- <span data-ttu-id="db438-139">Jeśli zestaw jest ładowany z <xref:System.Reflection.Assembly.LoadFrom%2A>, a ścieżka sondowania zawiera zestaw o tej samej tożsamości, ale w innej lokalizacji, może wystąpić <xref:System.InvalidCastException>, <xref:System.MissingMethodException>lub inne nieoczekiwane zachowanie.</span><span class="sxs-lookup"><span data-stu-id="db438-139">If an assembly is loaded with <xref:System.Reflection.Assembly.LoadFrom%2A>, and the probing path includes an assembly with the same identity but in a different location, an <xref:System.InvalidCastException>, <xref:System.MissingMethodException>, or other unexpected behavior can occur.</span></span>  
  
- <span data-ttu-id="db438-140"><xref:System.Reflection.Assembly.LoadFrom%2A> wymaga <xref:System.Security.Permissions.FileIOPermissionAccess.Read?displayProperty=nameWithType> i <xref:System.Security.Permissions.FileIOPermissionAccess.PathDiscovery?displayProperty=nameWithType>lub <xref:System.Net.WebPermission>, w określonej ścieżce.</span><span class="sxs-lookup"><span data-stu-id="db438-140"><xref:System.Reflection.Assembly.LoadFrom%2A> demands <xref:System.Security.Permissions.FileIOPermissionAccess.Read?displayProperty=nameWithType> and <xref:System.Security.Permissions.FileIOPermissionAccess.PathDiscovery?displayProperty=nameWithType>, or <xref:System.Net.WebPermission>, on the specified path.</span></span>  
  
- <span data-ttu-id="db438-141">Jeśli istnieje obraz natywny dla zestawu, nie jest on używany.</span><span class="sxs-lookup"><span data-stu-id="db438-141">If a native image exists for the assembly, it is not used.</span></span>  
  
- <span data-ttu-id="db438-142">Zestawu nie można załadować jako neutralnego dla domeny.</span><span class="sxs-lookup"><span data-stu-id="db438-142">The assembly cannot be loaded as domain-neutral.</span></span>  
  
- <span data-ttu-id="db438-143">W .NET Framework wersje 1,0 i 1,1 zasady nie są stosowane.</span><span class="sxs-lookup"><span data-stu-id="db438-143">In the .NET Framework versions 1.0 and 1.1, policy is not applied.</span></span>  
  
### <a name="no-context"></a><span data-ttu-id="db438-144">Brak kontekstu</span><span class="sxs-lookup"><span data-stu-id="db438-144">No Context</span></span>  
 <span data-ttu-id="db438-145">Ładowanie bez kontekstu jest jedyną opcją dla zestawów przejściowych, które są generowane przy użyciu emisji odbicia.</span><span class="sxs-lookup"><span data-stu-id="db438-145">Loading without context is the only option for transient assemblies that are generated with reflection emit.</span></span> <span data-ttu-id="db438-146">Ładowanie bez kontekstu jest jedynym sposobem ładowania wielu zestawów, które mają taką samą tożsamość w jednej domenie aplikacji.</span><span class="sxs-lookup"><span data-stu-id="db438-146">Loading without context is the only way to load multiple assemblies that have the same identity into one application domain.</span></span> <span data-ttu-id="db438-147">Należy unikać kosztu sondowania.</span><span class="sxs-lookup"><span data-stu-id="db438-147">The cost of probing is avoided.</span></span>  
  
 <span data-ttu-id="db438-148">Zestawy, które są ładowane z tablic bajtowych, są ładowane bez kontekstu, chyba że tożsamość zestawu, który jest ustanowiony, gdy zasady są stosowane, dopasowuje tożsamość zestawu w globalnej pamięci podręcznej zestawów; w takim przypadku zestaw jest ładowany z globalnej pamięci podręcznej zestawów.</span><span class="sxs-lookup"><span data-stu-id="db438-148">Assemblies that are loaded from byte arrays are loaded without context unless the identity of the assembly, which is established when policy is applied, matches the identity of an assembly in the global assembly cache; in that case, the assembly is loaded from the global assembly cache.</span></span>  
  
 <span data-ttu-id="db438-149">Ładowanie zestawów bez kontekstu ma następujące wady:</span><span class="sxs-lookup"><span data-stu-id="db438-149">Loading assemblies without context has the following disadvantages:</span></span>  
  
- <span data-ttu-id="db438-150">Inne zestawy nie mogą być powiązane z zestawami, które są ładowane bez kontekstu, chyba że obsłużysz zdarzenie <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="db438-150">Other assemblies cannot bind to assemblies that are loaded without context, unless you handle the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event.</span></span>  
  
- <span data-ttu-id="db438-151">Zależności nie są ładowane automatycznie.</span><span class="sxs-lookup"><span data-stu-id="db438-151">Dependencies are not loaded automatically.</span></span> <span data-ttu-id="db438-152">Można wstępnie załadować je bez kontekstu, załadować je do domyślnego kontekstu ładowania lub załadować je przez obsługę zdarzenia <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="db438-152">You can preload them without context, preload them into the default load context, or load them by handling the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event.</span></span>  
  
- <span data-ttu-id="db438-153">Ładowanie wielu zestawów o tej samej tożsamości bez kontekstu może powodować problemy z tożsamościami typu podobne do tych spowodowanych przez ładowanie zestawów o tej samej tożsamości w wielu kontekstach.</span><span class="sxs-lookup"><span data-stu-id="db438-153">Loading multiple assemblies with the same identity without context can cause type identity problems similar to those caused by loading assemblies with the same identity into multiple contexts.</span></span> <span data-ttu-id="db438-154">Zobacz [unikanie ładowania zestawu do wielu kontekstów](#avoid_loading_into_multiple_contexts).</span><span class="sxs-lookup"><span data-stu-id="db438-154">See [Avoid Loading an Assembly into Multiple Contexts](#avoid_loading_into_multiple_contexts).</span></span>  
  
- <span data-ttu-id="db438-155">Jeśli istnieje obraz natywny dla zestawu, nie jest on używany.</span><span class="sxs-lookup"><span data-stu-id="db438-155">If a native image exists for the assembly, it is not used.</span></span>  
  
- <span data-ttu-id="db438-156">Zestawu nie można załadować jako neutralnego dla domeny.</span><span class="sxs-lookup"><span data-stu-id="db438-156">The assembly cannot be loaded as domain-neutral.</span></span>  
  
- <span data-ttu-id="db438-157">W .NET Framework wersje 1,0 i 1,1 zasady nie są stosowane.</span><span class="sxs-lookup"><span data-stu-id="db438-157">In the .NET Framework versions 1.0 and 1.1, policy is not applied.</span></span>  
  
<a name="avoid_partial_names"></a>   
## <a name="avoid-binding-on-partial-assembly-names"></a><span data-ttu-id="db438-158">Unikaj tworzenia powiązań dla częściowych nazw zestawów</span><span class="sxs-lookup"><span data-stu-id="db438-158">Avoid Binding on Partial Assembly Names</span></span>  
 <span data-ttu-id="db438-159">Powiązanie części nazwy występuje, gdy podczas ładowania zestawu zostanie określona tylko część nazwy wyświetlanej zestawu (<xref:System.Reflection.Assembly.FullName%2A>).</span><span class="sxs-lookup"><span data-stu-id="db438-159">Partial name binding occurs when you specify only part of the assembly display name (<xref:System.Reflection.Assembly.FullName%2A>) when you load an assembly.</span></span> <span data-ttu-id="db438-160">Można na przykład wywołać metodę <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> tylko z prostą nazwą zestawu, pomijając wersję, kulturę i token klucza publicznego.</span><span class="sxs-lookup"><span data-stu-id="db438-160">For example, you might call the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method with only the simple name of the assembly, omitting the version, culture, and public key token.</span></span> <span data-ttu-id="db438-161">Lub można wywołać metodę <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType>, która najpierw wywołuje metodę <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> i, jeśli to nie powiodło się odnalezienie zestawu, przeszukuje globalną pamięć podręczną zestawów i ładuje najnowszą dostępną wersję zestawu.</span><span class="sxs-lookup"><span data-stu-id="db438-161">Or you might call the <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> method, which first calls the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and, if that fails to locate the assembly, searches the global assembly cache and loads the latest available version of the assembly.</span></span>  
  
 <span data-ttu-id="db438-162">Częściowe powiązanie nazw może spowodować wiele problemów, w tym następujące:</span><span class="sxs-lookup"><span data-stu-id="db438-162">Partial name binding can cause many problems, including the following:</span></span>  
  
- <span data-ttu-id="db438-163">Metoda <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> może załadować inny zestaw o tej samej prostej nazwie.</span><span class="sxs-lookup"><span data-stu-id="db438-163">The <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> method might load a different assembly with the same simple name.</span></span> <span data-ttu-id="db438-164">Na przykład dwie aplikacje mogą instalować dwa całkowicie różne zestawy, które mają prostą nazwę `GraphicsLibrary` w globalnej pamięci podręcznej zestawów.</span><span class="sxs-lookup"><span data-stu-id="db438-164">For example, two applications might install two completely different assemblies that both have the simple name `GraphicsLibrary` into the global assembly cache.</span></span>  
  
- <span data-ttu-id="db438-165">Zestaw, który jest faktycznie załadowany, może nie być zgodny z poprzednimi wersjami.</span><span class="sxs-lookup"><span data-stu-id="db438-165">The assembly that is actually loaded might not be backward-compatible.</span></span> <span data-ttu-id="db438-166">Na przykład nieokreślanie wersji może spowodować załadowanie znacznie późniejszej wersji niż wersja pierwotnie zapisywana przez program do użycia.</span><span class="sxs-lookup"><span data-stu-id="db438-166">For example, not specifying the version might result in the loading of a much later version than the version your program was originally written to use.</span></span> <span data-ttu-id="db438-167">Zmiany w nowszej wersji mogą powodować błędy w aplikacji.</span><span class="sxs-lookup"><span data-stu-id="db438-167">Changes in the later version might cause errors in your application.</span></span>  
  
- <span data-ttu-id="db438-168">Zestaw, który jest faktycznie załadowany, może nie być zgodny z przesyłaniem dalej.</span><span class="sxs-lookup"><span data-stu-id="db438-168">The assembly that is actually loaded might not be forward-compatible.</span></span> <span data-ttu-id="db438-169">Na przykład aplikacja została skompilowana i przetestowana przy użyciu najnowszej wersji zestawu, ale częściowe powiązanie może ładować wiele wcześniejszych wersji, które nie są używane przez aplikację.</span><span class="sxs-lookup"><span data-stu-id="db438-169">For example, you might have built and tested your application with the latest version of an assembly, but partial binding might load a much earlier version that lacks features your application uses.</span></span>  
  
- <span data-ttu-id="db438-170">Zainstalowanie nowych aplikacji może spowodować przerwanie istniejących aplikacji.</span><span class="sxs-lookup"><span data-stu-id="db438-170">Installing new applications can break existing applications.</span></span> <span data-ttu-id="db438-171">Aplikacja, która używa metody <xref:System.Reflection.Assembly.LoadWithPartialName%2A>, może być uszkodzona przez zainstalowanie nowszej, niezgodnej wersji zestawu udostępnionego.</span><span class="sxs-lookup"><span data-stu-id="db438-171">An application that uses the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method can be broken by installing a newer, incompatible version of a shared assembly.</span></span>  
  
- <span data-ttu-id="db438-172">Może wystąpić nieoczekiwane ładowanie zależności.</span><span class="sxs-lookup"><span data-stu-id="db438-172">Unexpected dependency loading can occur.</span></span> <span data-ttu-id="db438-173">Ładujesz dwa zestawy, które współużytkują zależność, ładując je za pomocą częściowego powiązania, może spowodować, że jeden zestaw będzie używany jako składnik, który nie został skompilowany lub przetestowany.</span><span class="sxs-lookup"><span data-stu-id="db438-173">It you load two assemblies that share a dependency, loading them with partial binding might result in one assembly using a component that it was not built or tested with.</span></span>  
  
 <span data-ttu-id="db438-174">Ze względu na problemy, które może to spowodować, Metoda <xref:System.Reflection.Assembly.LoadWithPartialName%2A> została oznaczona jako przestarzała.</span><span class="sxs-lookup"><span data-stu-id="db438-174">Because of the problems it can cause, the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method has been marked obsolete.</span></span> <span data-ttu-id="db438-175">Zaleca się, aby zamiast tego używać metody <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> i określić pełną nazwę wyświetlaną zestawu.</span><span class="sxs-lookup"><span data-stu-id="db438-175">We recommend that you use the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method instead, and specify full assembly display names.</span></span> <span data-ttu-id="db438-176">Zapoznaj [się z tematem zalety i wady kontekstów ładowania](#load_contexts) i [Rozważ zmianę domyślnego kontekstu ładowania](#switch_to_default).</span><span class="sxs-lookup"><span data-stu-id="db438-176">See [Understand the Advantages and Disadvantages of Load Contexts](#load_contexts) and [Consider Switching to the Default Load Context](#switch_to_default).</span></span>  
  
 <span data-ttu-id="db438-177">Jeśli chcesz użyć metody <xref:System.Reflection.Assembly.LoadWithPartialName%2A>, ponieważ sprawia to, że ładowanie zestawu jest łatwe, weź pod uwagę, że aplikacja nie powiedzie się z komunikatem o błędzie, który identyfikuje brakujący zestaw, może zapewnić lepszy interfejs użytkownika, niż automatycznie przy użyciu nieznanej wersji zestawu, co może spowodować nieprzewidywalne zachowanie i luki w zabezpieczeniach.</span><span class="sxs-lookup"><span data-stu-id="db438-177">If you want to use the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method because it makes assembly loading easy, consider that having your application fail with an error message that identifies the missing assembly is likely to provide a better user experience than automatically using an unknown version of the assembly, which might cause unpredictable behavior and security holes.</span></span>  
  
<a name="avoid_loading_into_multiple_contexts"></a>   
## <a name="avoid-loading-an-assembly-into-multiple-contexts"></a><span data-ttu-id="db438-178">Unikanie ładowania zestawu do wielu kontekstów</span><span class="sxs-lookup"><span data-stu-id="db438-178">Avoid Loading an Assembly into Multiple Contexts</span></span>  
 <span data-ttu-id="db438-179">Załadowanie zestawu do wielu kontekstów może spowodować problemy z tożsamościami typu.</span><span class="sxs-lookup"><span data-stu-id="db438-179">Loading an assembly into multiple contexts can cause type identity problems.</span></span> <span data-ttu-id="db438-180">Jeśli ten sam typ jest ładowany z tego samego zestawu do dwóch różnych kontekstów, jest tak, jakby dwa różne typy o tej samej nazwie zostały załadowane.</span><span class="sxs-lookup"><span data-stu-id="db438-180">If the same type is loaded from the same assembly into two different contexts, it is as if two different types with the same name had been loaded.</span></span> <span data-ttu-id="db438-181">Zostanie wygenerowany <xref:System.InvalidCastException>, jeśli spróbujesz rzutować jeden typ na drugi, z komunikatem o mylącym typie `MyType` nie można rzutować na `MyType`typu.</span><span class="sxs-lookup"><span data-stu-id="db438-181">An <xref:System.InvalidCastException> is thrown if you try to cast one type to the other, with the confusing message that type `MyType` cannot be cast to type `MyType`.</span></span>  
  
 <span data-ttu-id="db438-182">Załóżmy na przykład, że interfejs `ICommunicate` jest zadeklarowany w zestawie o nazwie `Utility`, do którego odwołuje się program, a także inne zestawy, które program załaduje.</span><span class="sxs-lookup"><span data-stu-id="db438-182">For example, suppose that the `ICommunicate` interface is declared in an assembly named `Utility`, which is referenced by your program and also by other assemblies that your program loads.</span></span> <span data-ttu-id="db438-183">Te inne zestawy zawierają typy implementujące interfejs `ICommunicate`, co pozwala programowi na ich używanie.</span><span class="sxs-lookup"><span data-stu-id="db438-183">These other assemblies contain types that implement the `ICommunicate` interface, allowing your program to use them.</span></span>  
  
 <span data-ttu-id="db438-184">Teraz Rozważmy, co się dzieje, gdy program jest uruchomiony.</span><span class="sxs-lookup"><span data-stu-id="db438-184">Now consider what happens when your program is run.</span></span> <span data-ttu-id="db438-185">Zestawy, do których odwołuje się program, są ładowane do domyślnego kontekstu ładowania.</span><span class="sxs-lookup"><span data-stu-id="db438-185">Assemblies that are referenced by your program are loaded into the default load context.</span></span> <span data-ttu-id="db438-186">Jeśli załadujesz zestaw docelowy za pomocą jego tożsamości, przy użyciu metody <xref:System.Reflection.Assembly.Load%2A>, będzie on znajdować się w domyślnym kontekście ładowania i dlatego będzie jego zależnościami.</span><span class="sxs-lookup"><span data-stu-id="db438-186">If you load a target assembly by its identity, using the <xref:System.Reflection.Assembly.Load%2A> method, it will be in the default load context, and so will its dependencies.</span></span> <span data-ttu-id="db438-187">Zarówno program, jak i zestaw docelowy będą używać tego samego zestawu `Utility`.</span><span class="sxs-lookup"><span data-stu-id="db438-187">Both your program and the target assembly will use the same `Utility` assembly.</span></span>  
  
 <span data-ttu-id="db438-188">Zaleca się jednak, aby załadować zestaw docelowy według ścieżki pliku przy użyciu metody <xref:System.Reflection.Assembly.LoadFile%2A>.</span><span class="sxs-lookup"><span data-stu-id="db438-188">However, suppose you load the target assembly by its file path, using the <xref:System.Reflection.Assembly.LoadFile%2A> method.</span></span> <span data-ttu-id="db438-189">Zestaw jest ładowany bez żadnego kontekstu, więc jego zależności nie są ładowane automatycznie.</span><span class="sxs-lookup"><span data-stu-id="db438-189">The assembly is loaded without any context, so its dependencies are not automatically loaded.</span></span> <span data-ttu-id="db438-190">Możesz mieć procedurę obsługi dla zdarzenia <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType>, aby dostarczyć zależność, i może załadować zestaw `Utility` bez kontekstu przy użyciu metody <xref:System.Reflection.Assembly.LoadFile%2A>.</span><span class="sxs-lookup"><span data-stu-id="db438-190">You might have a handler for the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event to supply the dependency, and it might load the `Utility` assembly with no context by using the <xref:System.Reflection.Assembly.LoadFile%2A> method.</span></span> <span data-ttu-id="db438-191">Teraz podczas tworzenia wystąpienia typu, który jest zawarty w zestawie docelowym i próba przypisania go do zmiennej typu `ICommunicate`, zostanie zgłoszony <xref:System.InvalidCastException>, ponieważ środowisko uruchomieniowe traktuje interfejsy `ICommunicate` w dwóch kopiach zestawu `Utility` jako różne typy.</span><span class="sxs-lookup"><span data-stu-id="db438-191">Now when you create an instance of a type that is contained in the target assembly and try to assign it to a variable of type `ICommunicate`, an <xref:System.InvalidCastException> is thrown because the runtime considers the `ICommunicate` interfaces in the two copies of the `Utility` assembly to be different types.</span></span>  
  
 <span data-ttu-id="db438-192">Istnieje wiele innych scenariuszy, w których zestaw może być ładowany do wielu kontekstów.</span><span class="sxs-lookup"><span data-stu-id="db438-192">There are many other scenarios in which an assembly can be loaded into multiple contexts.</span></span> <span data-ttu-id="db438-193">Najlepszym rozwiązaniem jest uniknięcie konfliktów przez przelokalizowanie zestawu docelowego w ścieżce aplikacji i użycie metody <xref:System.Reflection.Assembly.Load%2A> z pełną nazwą wyświetlaną.</span><span class="sxs-lookup"><span data-stu-id="db438-193">The best approach is to avoid conflicts by relocating the target assembly in your application path and using the <xref:System.Reflection.Assembly.Load%2A> method with the full display name.</span></span> <span data-ttu-id="db438-194">Zestaw jest następnie ładowany do domyślnego kontekstu ładowania, a oba zestawy używają tego samego zestawu `Utility`.</span><span class="sxs-lookup"><span data-stu-id="db438-194">The assembly is then loaded into the default load context, and both assemblies use the same `Utility` assembly.</span></span>  
  
 <span data-ttu-id="db438-195">Jeśli zestaw docelowy musi pozostać poza ścieżką aplikacji, można użyć metody <xref:System.Reflection.Assembly.LoadFrom%2A>, aby załadować go do kontekstu ładowania z.</span><span class="sxs-lookup"><span data-stu-id="db438-195">If the target assembly must remain outside your application path, you can use the <xref:System.Reflection.Assembly.LoadFrom%2A> method to load it into the load-from context.</span></span> <span data-ttu-id="db438-196">Jeśli zestaw docelowy został skompilowany za pomocą odwołania do zestawu `Utility` aplikacji, użyje zestawu `Utility`, który aplikacja została załadowana do domyślnego kontekstu ładowania.</span><span class="sxs-lookup"><span data-stu-id="db438-196">If the target assembly was compiled with a reference to your application's `Utility` assembly, it will use the `Utility` assembly that your application has loaded into the default load context.</span></span> <span data-ttu-id="db438-197">Należy pamiętać, że problemy mogą wystąpić, jeśli zestaw docelowy ma zależność od kopii zestawu `Utility` znajdującego się poza ścieżką aplikacji.</span><span class="sxs-lookup"><span data-stu-id="db438-197">Note that problems can occur if the target assembly has a dependency on a copy of the `Utility` assembly located outside your application path.</span></span> <span data-ttu-id="db438-198">Jeśli ten zestaw jest ładowany do kontekstu obciążenia przed załadowaniem zestawu `Utility` przez aplikację, ładowanie aplikacji zakończy się niepowodzeniem.</span><span class="sxs-lookup"><span data-stu-id="db438-198">If that assembly is loaded into the load-from context before your application loads the `Utility` assembly, your application's load will fail.</span></span>  
  
 <span data-ttu-id="db438-199">[Rozważ przełączenie się do domyślnej sekcji kontekstu ładowania](#switch_to_default) omawia alternatywy do używania ładowania ścieżki plików, takich jak <xref:System.Reflection.Assembly.LoadFile%2A> i <xref:System.Reflection.Assembly.LoadFrom%2A>.</span><span class="sxs-lookup"><span data-stu-id="db438-199">The [Consider Switching to the Default Load Context](#switch_to_default) section discusses alternatives to using file path loads such as <xref:System.Reflection.Assembly.LoadFile%2A> and <xref:System.Reflection.Assembly.LoadFrom%2A>.</span></span>  
  
<a name="avoid_loading_multiple_versions"></a>   
## <a name="avoid-loading-multiple-versions-of-an-assembly-into-the-same-context"></a><span data-ttu-id="db438-200">Unikaj ładowania wielu wersji zestawu do tego samego kontekstu</span><span class="sxs-lookup"><span data-stu-id="db438-200">Avoid Loading Multiple Versions of an Assembly into the Same Context</span></span>  
 <span data-ttu-id="db438-201">Ładowanie wielu wersji zestawu do jednego kontekstu ładowania może spowodować problemy z tożsamościami typu.</span><span class="sxs-lookup"><span data-stu-id="db438-201">Loading multiple versions of an assembly into one load context can cause type identity problems.</span></span> <span data-ttu-id="db438-202">Jeśli ten sam typ jest ładowany z dwóch wersji tego samego zestawu, jest tak, jakby dwa różne typy o tej samej nazwie zostały załadowane.</span><span class="sxs-lookup"><span data-stu-id="db438-202">If the same type is loaded from two versions of the same assembly, it is as if two different types with the same name had been loaded.</span></span> <span data-ttu-id="db438-203">Zostanie wygenerowany <xref:System.InvalidCastException>, jeśli spróbujesz rzutować jeden typ na drugi, z komunikatem o mylącym typie `MyType` nie można rzutować na `MyType`typu.</span><span class="sxs-lookup"><span data-stu-id="db438-203">An <xref:System.InvalidCastException> is thrown if you try to cast one type to the other, with the confusing message that type `MyType` cannot be cast to type `MyType`.</span></span>  
  
 <span data-ttu-id="db438-204">Na przykład program może załadować jedną wersję zestawu `Utility` bezpośrednio, a później może załadować kolejny zestaw, który ładuje inną wersję zestawu `Utility`.</span><span class="sxs-lookup"><span data-stu-id="db438-204">For example, your program might load one version of the `Utility` assembly directly, and later it might load another assembly that loads a different version of the `Utility` assembly.</span></span> <span data-ttu-id="db438-205">Lub błąd kodowania może spowodować dwie różne ścieżki kodu w aplikacji, aby załadować różne wersje zestawu.</span><span class="sxs-lookup"><span data-stu-id="db438-205">Or a coding error might cause two different code paths in your application to load different versions of an assembly.</span></span>  
  
 <span data-ttu-id="db438-206">W domyślnym kontekście ładowania ten problem może wystąpić w przypadku użycia metody <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> i określenia kompletnych nazw wyświetlanych zestawu zawierających różne numery wersji.</span><span class="sxs-lookup"><span data-stu-id="db438-206">In the default load context, this problem can occur when you use the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and specify complete assembly display names that include different version numbers.</span></span> <span data-ttu-id="db438-207">W przypadku zestawów, które są ładowane bez kontekstu, problem może być spowodowany użyciem metody <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType> do załadowania tego samego zestawu z różnych ścieżek.</span><span class="sxs-lookup"><span data-stu-id="db438-207">For assemblies that are loaded without context, the problem can be caused by using the <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType> method to load the same assembly from different paths.</span></span> <span data-ttu-id="db438-208">Środowisko uruchomieniowe traktuje dwa zestawy, które są ładowane z różnych ścieżek, aby były różnymi zestawami, nawet jeśli ich tożsamości są takie same.</span><span class="sxs-lookup"><span data-stu-id="db438-208">The runtime considers two assemblies that are loaded from different paths to be different assemblies, even if their identities are the same.</span></span>  
  
 <span data-ttu-id="db438-209">Oprócz problemów z tożsamościami, wiele wersji zestawu może spowodować <xref:System.MissingMethodException>, jeśli typ, który jest ładowany z jednej wersji zestawu, jest przenoszona do kodu, który oczekuje tego typu z innej wersji.</span><span class="sxs-lookup"><span data-stu-id="db438-209">In addition to type identity problems, multiple versions of an assembly can cause a <xref:System.MissingMethodException> if a type that is loaded from one version of the assembly is passed to code that expects that type from a different version.</span></span> <span data-ttu-id="db438-210">Na przykład kod może oczekiwać metody, która została dodana do nowszej wersji.</span><span class="sxs-lookup"><span data-stu-id="db438-210">For example, the code might expect a method that was added to the later version.</span></span>  
  
 <span data-ttu-id="db438-211">Jeśli zachowanie typu uległo zmianie między wersjami, mogą wystąpić bardziej subtelne błędy.</span><span class="sxs-lookup"><span data-stu-id="db438-211">More subtle errors can occur if the behavior of the type changed between versions.</span></span> <span data-ttu-id="db438-212">Na przykład metoda może zgłosić nieoczekiwany wyjątek lub zwrócić nieoczekiwaną wartość.</span><span class="sxs-lookup"><span data-stu-id="db438-212">For example, a method might throw an unexpected exception or return an unexpected value.</span></span>  
  
 <span data-ttu-id="db438-213">Uważnie Przejrzyj swój kod, aby upewnić się, że załadowana została tylko jedna wersja zestawu.</span><span class="sxs-lookup"><span data-stu-id="db438-213">Carefully review your code to ensure that only one version of an assembly is loaded.</span></span> <span data-ttu-id="db438-214">Za pomocą metody <xref:System.AppDomain.GetAssemblies%2A?displayProperty=nameWithType> można określić, które zestawy są ładowane w danym momencie.</span><span class="sxs-lookup"><span data-stu-id="db438-214">You can use the <xref:System.AppDomain.GetAssemblies%2A?displayProperty=nameWithType> method to determine which assemblies are loaded at any given time.</span></span>  
  
<a name="switch_to_default"></a>   
## <a name="consider-switching-to-the-default-load-context"></a><span data-ttu-id="db438-215">Rozważ przełączenie do domyślnego kontekstu ładowania</span><span class="sxs-lookup"><span data-stu-id="db438-215">Consider Switching to the Default Load Context</span></span>  
 <span data-ttu-id="db438-216">Sprawdź wzorce ładowania i wdrażania aplikacji.</span><span class="sxs-lookup"><span data-stu-id="db438-216">Examine your application's assembly loading and deployment patterns.</span></span> <span data-ttu-id="db438-217">Czy można wyeliminować zestawy, które są ładowane z tablic bajtowych?</span><span class="sxs-lookup"><span data-stu-id="db438-217">Can you eliminate assemblies that are loaded from byte arrays?</span></span> <span data-ttu-id="db438-218">Czy można przenieść zestawy do ścieżki Bing?</span><span class="sxs-lookup"><span data-stu-id="db438-218">Can you move assemblies into the probing path?</span></span> <span data-ttu-id="db438-219">Jeśli zestawy znajdują się w globalnej pamięci podręcznej zestawów lub w ścieżce do sondowania domeny aplikacji (czyli jej <xref:System.AppDomainSetup.ApplicationBase%2A> i <xref:System.AppDomainSetup.PrivateBinPath%2A>), można załadować zestaw według jego tożsamości.</span><span class="sxs-lookup"><span data-stu-id="db438-219">If assemblies are located in the global assembly cache or in the application domain's probing path (that is, its <xref:System.AppDomainSetup.ApplicationBase%2A> and <xref:System.AppDomainSetup.PrivateBinPath%2A>), you can load the assembly by its identity.</span></span>  
  
 <span data-ttu-id="db438-220">Jeśli nie można umieścić wszystkich zestawów w ścieżce sondowania, należy rozważyć alternatywy, takie jak użycie modelu dodatku .NET Framework, umieszczenie zestawów w globalnej pamięci podręcznej zestawów lub utworzenie domen aplikacji.</span><span class="sxs-lookup"><span data-stu-id="db438-220">If it is not possible to put all your assemblies in the probing path, consider alternatives such as using the .NET Framework add-in model, placing assemblies into the global assembly cache, or creating application domains.</span></span>  
  
### <a name="consider-using-the-net-framework-add-in-model"></a><span data-ttu-id="db438-221">Rozważ użycie modelu dodatku .NET Framework</span><span class="sxs-lookup"><span data-stu-id="db438-221">Consider Using the .NET Framework Add-In Model</span></span>  
 <span data-ttu-id="db438-222">Jeśli używasz kontekstu Załaduj-from do implementowania dodatków, które zwykle nie są zainstalowane w bazie aplikacji, użyj modelu dodatku .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="db438-222">If you are using the load-from context to implement add-ins, which typically are not installed in the application base, use the .NET Framework add-in model.</span></span> <span data-ttu-id="db438-223">Ten model zapewnia izolację na poziomie domeny aplikacji lub procesu, bez konieczności samodzielnego zarządzania domenami aplikacji.</span><span class="sxs-lookup"><span data-stu-id="db438-223">This model provides isolation at the application domain or process level, without requiring you to manage application domains yourself.</span></span> <span data-ttu-id="db438-224">Aby uzyskać informacje o modelu dodatków, zobacz [Dodatki i rozszerzalność](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/bb384200(v%3dvs.100)).</span><span class="sxs-lookup"><span data-stu-id="db438-224">For information about the add-in model, see [Add-ins and Extensibility](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/bb384200(v%3dvs.100)).</span></span>  
  
### <a name="consider-using-the-global-assembly-cache"></a><span data-ttu-id="db438-225">Rozważ użycie globalnej pamięci podręcznej zestawów</span><span class="sxs-lookup"><span data-stu-id="db438-225">Consider Using the Global Assembly Cache</span></span>  
 <span data-ttu-id="db438-226">Umieść zestawy w globalnej pamięci podręcznej zestawów, aby uzyskać korzyści wynikające ze wspólnej ścieżki zestawu, która znajduje się poza bazą aplikacji, bez utraty korzyści wynikającej z domyślnego kontekstu ładowania lub wyłączania wad innych kontekstów.</span><span class="sxs-lookup"><span data-stu-id="db438-226">Place assemblies in the global assembly cache to get the benefit of a shared assembly path that is outside the application base, without losing the advantages of the default load context or taking on the disadvantages of the other contexts.</span></span>  
  
### <a name="consider-using-application-domains"></a><span data-ttu-id="db438-227">Rozważ użycie domen aplikacji</span><span class="sxs-lookup"><span data-stu-id="db438-227">Consider Using Application Domains</span></span>  
 <span data-ttu-id="db438-228">Jeśli okaże się, że niektóre zestawy nie mogą być wdrożone w ścieżce sondowania aplikacji, należy rozważyć utworzenie nowej domeny aplikacji dla tych zestawów.</span><span class="sxs-lookup"><span data-stu-id="db438-228">If you determine that some of your assemblies cannot be deployed in the application's probing path, consider creating a new application domain for those assemblies.</span></span> <span data-ttu-id="db438-229">Użyj <xref:System.AppDomainSetup>, aby utworzyć nową domenę aplikacji, a następnie użyj właściwości <xref:System.AppDomainSetup.ApplicationBase%2A?displayProperty=nameWithType>, aby określić ścieżkę zawierającą zestawy, które mają zostać załadowane.</span><span class="sxs-lookup"><span data-stu-id="db438-229">Use an <xref:System.AppDomainSetup> to create the new application domain, and use the <xref:System.AppDomainSetup.ApplicationBase%2A?displayProperty=nameWithType> property to specify the path that contains the assemblies you want to load.</span></span> <span data-ttu-id="db438-230">Jeśli masz wiele katalogów do sondowania, możesz ustawić <xref:System.AppDomainSetup.ApplicationBase%2A> na katalog główny i użyć właściwości <xref:System.AppDomainSetup.PrivateBinPath%2A?displayProperty=nameWithType>, aby zidentyfikować podkatalogi do sondowania.</span><span class="sxs-lookup"><span data-stu-id="db438-230">If you have multiple directories to probe, you can set the <xref:System.AppDomainSetup.ApplicationBase%2A> to a root directory and use the <xref:System.AppDomainSetup.PrivateBinPath%2A?displayProperty=nameWithType> property to identify the subdirectories to probe.</span></span> <span data-ttu-id="db438-231">Alternatywnie można utworzyć wiele domen aplikacji i ustawić <xref:System.AppDomainSetup.ApplicationBase%2A> poszczególnych domen aplikacji do odpowiedniej ścieżki dla zestawów.</span><span class="sxs-lookup"><span data-stu-id="db438-231">Alternatively, you can create multiple application domains and set the <xref:System.AppDomainSetup.ApplicationBase%2A> of each application domain to the appropriate path for its assemblies.</span></span>  
  
 <span data-ttu-id="db438-232">Należy pamiętać, że można użyć metody <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> do załadowania tych zestawów.</span><span class="sxs-lookup"><span data-stu-id="db438-232">Note that you can use the <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> method to load these assemblies.</span></span> <span data-ttu-id="db438-233">Ponieważ znajdują się one teraz w ścieżce do sondowania, zostaną one załadowane do domyślnego kontekstu ładowania zamiast kontekstu ładowania z.</span><span class="sxs-lookup"><span data-stu-id="db438-233">Because they are now in the probing path, they will be loaded into the default load context instead of the load-from context.</span></span> <span data-ttu-id="db438-234">Zaleca się jednak przełączenie na metodę <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> i dostarczenie pełnych nazw wyświetlanych zestawu, aby upewnić się, że zawsze są używane poprawne wersje.</span><span class="sxs-lookup"><span data-stu-id="db438-234">However, we recommend that you switch to the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and supply full assembly display names to ensure that correct versions are always used.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="db438-235">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="db438-235">See also</span></span>

- <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>
- <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>
- <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType>
- <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType>
