---
title: "Wskazówki: uzyskiwanie dostępu do usługi OData za pomocą dostawców typów (F#)"
description: "Informacje o sposobie użycia dostawcy typów F # ODataService w F # 3.0 do generowania typów klienta do usługi OData i zapewnia zapytania danych źródeł usługi."
keywords: "Visual f #, f #, funkcjonalności programowania"
author: cartermp
ms.author: phcart
ms.date: 05/16/2016
ms.topic: language-reference
ms.prod: .net
ms.technology: devlang-fsharp
ms.devlang: fsharp
ms.assetid: 0adae84c-b0fa-455f-994b-274ecdc6df30
ms.openlocfilehash: 28c2e9a405670f4e5f9512e99e0e6c3e3082856c
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 10/18/2017
---
# <a name="walkthrough-accessing-an-odata-service-by-using-type-providers"></a><span data-ttu-id="b2f3c-104">Wskazówki: uzyskiwanie dostępu do usługi OData za pomocą dostawców typów</span><span class="sxs-lookup"><span data-stu-id="b2f3c-104">Walkthrough: Accessing an OData Service by Using Type Providers</span></span>

> [!NOTE]
<span data-ttu-id="b2f3c-105">Ten przewodnik został napisany w języku F # 3.0 i zostanie zaktualizowany.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-105">This guide was written for F# 3.0 and will be updated.</span></span>  <span data-ttu-id="b2f3c-106">Zobacz [FSharp.Data](http://fsharp.github.io/FSharp.Data/) dla dostawców typów aktualne, między platformami.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-106">See [FSharp.Data](http://fsharp.github.io/FSharp.Data/) for up-to-date, cross-platform type providers.</span></span>

> [!NOTE]
<span data-ttu-id="b2f3c-107">Linki odwołań interfejsu API spowoduje przejście do MSDN.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-107">The API reference links will take you to MSDN.</span></span>  <span data-ttu-id="b2f3c-108">Dokumentacja interfejsu API docs.microsoft.com nie została ukończona.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-108">The docs.microsoft.com API reference is not complete.</span></span>

<span data-ttu-id="b2f3c-109">OData, co oznacza Open Data Protocol, jest protokół transferu danych za pośrednictwem Internetu.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-109">OData, meaning Open Data Protocol, is a protocol for transferring data over the Internet.</span></span> <span data-ttu-id="b2f3c-110">Wielu dostawców danych udostępnić dostęp do swoich danych publikowania usługi sieci web OData.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-110">Many data providers expose access to their data by publishing an OData web service.</span></span> <span data-ttu-id="b2f3c-111">Dostęp do danych z dowolnego źródła OData w F # 3.0 używanie typów danych, które są automatycznie generowane przez `ODataService` typ dostawcy.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-111">You can access data from any OData source in F# 3.0 using data types that are automatically generated by the `ODataService` type provider.</span></span> <span data-ttu-id="b2f3c-112">Aby uzyskać więcej informacji na temat OData Zobacz https://msdn.microsoft.com/library/da3380cc-f6da-4c6c-bdb2-bb86afa59d62.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-112">For more information about OData, see https://msdn.microsoft.com/library/da3380cc-f6da-4c6c-bdb2-bb86afa59d62.</span></span>

<span data-ttu-id="b2f3c-113">Ten przewodnik zawiera wygenerować typy klienta dla usługi OData przy użyciu dostawcy typów ODataService F # oraz zapytania danych źródeł usługi.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-113">This walkthrough shows you how to use the F# ODataService type provider to generate client types for an OData service and query data feeds that the service provides.</span></span>

<span data-ttu-id="b2f3c-114">W tym przewodniku opisano następujące zadania, które trzeba wykonać, aby pomyślnie przerobić przewodnik:</span><span class="sxs-lookup"><span data-stu-id="b2f3c-114">This walkthrough illustrates the following tasks, which you should perform in this order for the walkthrough to succeed:</span></span>


- <span data-ttu-id="b2f3c-115">Konfigurowanie projekt klienta do usługi OData</span><span class="sxs-lookup"><span data-stu-id="b2f3c-115">Configuring a client project for an OData service</span></span>
<br />

- <span data-ttu-id="b2f3c-116">Uzyskiwanie dostępu do typów OData</span><span class="sxs-lookup"><span data-stu-id="b2f3c-116">Accessing OData types</span></span>
<br />

- <span data-ttu-id="b2f3c-117">Zapytanie usługi OData</span><span class="sxs-lookup"><span data-stu-id="b2f3c-117">Querying an OData service</span></span>
<br />

- <span data-ttu-id="b2f3c-118">Sprawdzanie żądań OData</span><span class="sxs-lookup"><span data-stu-id="b2f3c-118">Verifying the OData requests</span></span>
<br />


## <a name="configuring-a-client-project-for-an-odata-service"></a><span data-ttu-id="b2f3c-119">Konfigurowanie projekt klienta do usługi OData</span><span class="sxs-lookup"><span data-stu-id="b2f3c-119">Configuring a client project for an OData service</span></span>
<span data-ttu-id="b2f3c-120">W tym kroku należy skonfigurować projekt do używania dostawcy typów OData.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-120">In this step, you set up a project to use an OData type provider.</span></span>


#### <a name="to-configure-a-client-project-for-an-odata-service"></a><span data-ttu-id="b2f3c-121">Aby skonfigurować projekt klienta do usługi OData</span><span class="sxs-lookup"><span data-stu-id="b2f3c-121">To configure a client project for an OData service</span></span>

- <span data-ttu-id="b2f3c-122">Otwórz projekt aplikacji konsoli F #, a następnie dodaj odwołanie do `System.Data.Services.Client` zestawu struktury.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-122">Open an F# Console Application project, and then add a reference to the `System.Data.Services.Client` Framework assembly.</span></span>
<br />

- <span data-ttu-id="b2f3c-123">W obszarze `Extensions`, Dodaj odwołanie do `FSharp.Data.TypeProviders` zestawu.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-123">Under `Extensions`, add a reference to the `FSharp.Data.TypeProviders` assembly.</span></span>
<br />

## <a name="accessing-odata-types"></a><span data-ttu-id="b2f3c-124">Uzyskiwanie dostępu do typów OData</span><span class="sxs-lookup"><span data-stu-id="b2f3c-124">Accessing OData types</span></span>
<span data-ttu-id="b2f3c-125">W tym kroku utworzysz typ dostawcy, która zapewnia dostęp do danych i typów dla usługi OData.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-125">In this step, you create a type provider that provides access to the types and data for an OData service.</span></span>


#### <a name="to-access-odata-types"></a><span data-ttu-id="b2f3c-126">Aby dostęp do tych typów OData</span><span class="sxs-lookup"><span data-stu-id="b2f3c-126">To access OData types</span></span>

- <span data-ttu-id="b2f3c-127">W edytorze kodu Otwórz plik źródłowy języka F #, a następnie wprowadź poniższy kod.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-127">In the Code Editor, open an F# source file, and enter the following code.</span></span>
<br />

```fsharp
open Microsoft.FSharp.Data.TypeProviders


type Northwind = ODataService<"http://services.odata.org/Northwind/Northwind.svc/">

let db = Northwind.GetDataContext()
let fullContext = Northwind.ServiceTypes.NorthwindEntities()
```

<span data-ttu-id="b2f3c-128">W tym przykładzie ma wywołać dostawcy typów F # i nakazał go w celu utworzenia zestawu typów, które są oparte na określonej identyfikatora URI OData.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-128">In this example, you have invoked the F# type provider and instructed it to create a set of types that are based on the OData URI that you specified.</span></span> <span data-ttu-id="b2f3c-129">Dostępne są dwa obiekty zawierających informacje o danych; jeden z nich jest kontekst danych uproszczony, `db` w przykładzie.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-129">Two objects are available that contain information about the data; one is a simplified data context, `db` in the example.</span></span> <span data-ttu-id="b2f3c-130">Ten obiekt zawiera tylko typy danych, które są skojarzone z bazy danych, które obejmują typy tabel i źródeł danych.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-130">This object contains only the data types that are associated with the database, which include types for tables or feeds.</span></span> <span data-ttu-id="b2f3c-131">Drugi obiekt `fullContext` w tym przykładzie jest wystąpieniem `System.Data.Linq.DataContext` i zawiera wiele dodatkowych właściwości, metod i zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-131">The other object, `fullContext` in this example, is an instance of `System.Data.Linq.DataContext` and contains many additional properties, methods, and events.</span></span>
<br />


## <a name="querying-an-odata-service"></a><span data-ttu-id="b2f3c-132">Zapytanie usługi OData</span><span class="sxs-lookup"><span data-stu-id="b2f3c-132">Querying an OData service</span></span>
<span data-ttu-id="b2f3c-133">W tym kroku wyrażenia zapytania F # służy do wysłać zapytania do usługi OData.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-133">In this step, you use F# query expressions to query the OData service.</span></span>


#### <a name="to-query-an-odata-service"></a><span data-ttu-id="b2f3c-134">Kwerenda usługi OData</span><span class="sxs-lookup"><span data-stu-id="b2f3c-134">To query an OData service</span></span>

1. <span data-ttu-id="b2f3c-135">Teraz, gdy ustawiono typ dostawcy mogą wysyłać zapytania usługi OData.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-135">Now that you've set up the type provider, you can query an OData service.</span></span>
<br />  <span data-ttu-id="b2f3c-136">OData obsługuje tylko operacje zapytań dostępne.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-136">OData supports only a subset of the available query operations.</span></span> <span data-ttu-id="b2f3c-137">Obsługiwane są następujące operacje oraz ich odpowiednich słów kluczowych:</span><span class="sxs-lookup"><span data-stu-id="b2f3c-137">The following operations and their corresponding keywords are supported:</span></span>
<br />
  - <span data-ttu-id="b2f3c-138">Projekcja (`select`)</span><span class="sxs-lookup"><span data-stu-id="b2f3c-138">projection (`select`)</span></span>
<br />

  - <span data-ttu-id="b2f3c-139">Filtrowanie (`where`, przez użycie ciągu i Data operations)</span><span class="sxs-lookup"><span data-stu-id="b2f3c-139">filtering (`where`, by using string and date operations)</span></span>
<br />

  - <span data-ttu-id="b2f3c-140">stronicowanie (`skip`, `take`)</span><span class="sxs-lookup"><span data-stu-id="b2f3c-140">paging (`skip`, `take`)</span></span>
<br />

  - <span data-ttu-id="b2f3c-141">kolejność (`orderBy`, `thenBy`)</span><span class="sxs-lookup"><span data-stu-id="b2f3c-141">ordering (`orderBy`, `thenBy`)</span></span>
<br />

  - <span data-ttu-id="b2f3c-142">`AddQueryOption`i `Expand`, które są zależne od OData operacji</span><span class="sxs-lookup"><span data-stu-id="b2f3c-142">`AddQueryOption` and `Expand`, which are OData-specific operations</span></span>
<br />

  <span data-ttu-id="b2f3c-143">Aby uzyskać więcej informacji, zobacz [zagadnienia dotyczące LINQ &#40; Usługi danych WCF &#41; ](https://msdn.microsoft.com/library/ee622463.aspx).</span><span class="sxs-lookup"><span data-stu-id="b2f3c-143">For more information, see [LINQ Considerations &#40;WCF Data Services&#41;](https://msdn.microsoft.com/library/ee622463.aspx).</span></span>
<br />  <span data-ttu-id="b2f3c-144">Wszystkie pozycje źródła danych lub tabeli, użyć najprostszej formie wyrażenia zapytania, zgodnie z poniższym kodem:</span><span class="sxs-lookup"><span data-stu-id="b2f3c-144">If you want all of the entries in a feed or table, use the simplest form of the query expression, as in the following code:</span></span>
<br />

```fsharp
query {
  for customer in db.Customers do
  select customer
} |> Seq.iter (fun customer ->
                  printfn "ID: %s\nCompany: %s" customer.CustomerID customer.CompanyName
                  printfn "Contact: %s\nAddress: %s" customer.ContactName customer.Address
                  printfn "         %s, %s %s" customer.City customer.Region customer.PostalCode
                  printfn "%s\n" customer.Phone)
```

2. <span data-ttu-id="b2f3c-145">Określ pola lub kolumny, które mają przy użyciu spójnych kolekcji po słowie kluczowym select.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-145">Specify the fields or columns that you want by using a tuple after the select keyword.</span></span>
<br />

```fsharp
  query { 
    for cat in db.Categories do
    select (cat.CategoryID, cat.CategoryName, cat.Description)
  } |> Seq.iter (fun (id, name, description) ->
                    printfn "ID: %d\nCategory: %s\nDescription: %s\n" id name description)
```

3. <span data-ttu-id="b2f3c-146">Określ warunki, używając `where` klauzuli.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-146">Specify conditions by using a `where` clause.</span></span>
<br />

```fsharp
query {
  for employee in db.Employees do
  where (employee.EmployeeID = 9)
  select employee
} |> Seq.iter (fun employee ->
                  printfn "Name: %s ID: %d" (employee.FirstName + " " + employee.LastName) (employee.EmployeeID))
```

4. <span data-ttu-id="b2f3c-147">Określ warunek podciąg do zapytania przy użyciu `System.String.Contains(System.String)` metody.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-147">Specify a substring condition to the query by using the `System.String.Contains(System.String)` method.</span></span> <span data-ttu-id="b2f3c-148">Następujące zapytanie zwraca wszystkie produkty, które mają w nazwach "Chef".</span><span class="sxs-lookup"><span data-stu-id="b2f3c-148">The following query returns all products that have "Chef" in their names.</span></span> <span data-ttu-id="b2f3c-149">Ponadto użycie `System.Nullable<'T>.GetValueOrDefault()`.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-149">Also notice the use of `System.Nullable<'T>.GetValueOrDefault()`.</span></span> <span data-ttu-id="b2f3c-150">`UnitPrice` Jest wartość null, dzięki czemu możesz musi uzyskać wartość przy użyciu `Value` właściwości, lub należy wywołać `System.Nullable<'T>.GetValueOrDefault()`.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-150">The `UnitPrice` is a nullable value, so you must either get the value by using the `Value` property, or you must call `System.Nullable<'T>.GetValueOrDefault()`.</span></span>
<br />

```fsharp
query { 
  for product in db.Products do
  where (product.ProductName.Contains("Chef"))
  select product
} |> Seq.iter (fun product ->
                  printfn "ID: %d Product: %s" product.ProductID product.ProductName
                  printfn "Price: %M\n" (product.UnitPrice.GetValueOrDefault()))
```

5. <span data-ttu-id="b2f3c-151">Użyj `System.String.EndsWith(System.String)` metodę, aby określić, że ciąg kończy się niektórych podciąg.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-151">Use the `System.String.EndsWith(System.String)` method to specify that a string ends with a certain substring.</span></span>
<br />

```fsharp
query {
  for product in db.Products do
  where (product.ProductName.EndsWith("u"))
  select product
} |> Seq.iter (fun product ->
                  printfn "ID: %d Product: %s" product.ProductID product.ProductName
                  printfn "Price: %M\n" (product.UnitPrice.GetValueOrDefault()))
```

6. <span data-ttu-id="b2f3c-152">Łączenie warunków w klauzuli where klauzuli przy użyciu `&&` operatora.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-152">Combine conditions in a where clause by using the `&&` operator.</span></span>
<br />

```fsharp
// Open this module to use the nullable operators ?> and ?<.
open Microsoft.FSharp.Linq.NullableOperators

let salesIn1997 = query { 
  for sales in db.Category_Sales_for_1997 do
  where (sales.CategorySales ?> 50000.00M && sales.CategorySales ?< 60000.0M)
  select sales }

salesIn1997
|> Seq.iter (fun sales ->
                printfn "Category: %s Sales: %M" sales.CategoryName (sales.CategorySales.GetValueOrDefault()))
```

<span data-ttu-id="b2f3c-153">Operatory `?>` i `?<` są operatory dopuszczające wartość null.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-153">The operators `?>` and `?<` are nullable operators.</span></span> <span data-ttu-id="b2f3c-154">Można użyć pełny zestaw Operatory równości i porównanie dopuszczające wartość null.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-154">You can use a full set of nullable equality and comparison operators.</span></span> <span data-ttu-id="b2f3c-155">Aby uzyskać więcej informacji, zobacz [LINQ.nullableoperators — moduł](https://msdn.microsoft.com/visualfsharpdocs/conceptual/linq.nullableoperators-module-%5bfsharp%5d).</span><span class="sxs-lookup"><span data-stu-id="b2f3c-155">For more information, see [Linq.NullableOperators Module](https://msdn.microsoft.com/visualfsharpdocs/conceptual/linq.nullableoperators-module-%5bfsharp%5d).</span></span>
<br />

7. <span data-ttu-id="b2f3c-156">Użyj `sortBy` zapytań operatora, aby określić kolejność, użyj `thenBy` Aby określić inny poziom kolejności.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-156">Use the `sortBy` query operator to specify ordering, and use `thenBy` to specify another level of ordering.</span></span> <span data-ttu-id="b2f3c-157">Ponadto użycie spójnych kolekcji w części zapytania select.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-157">Notice also the use of a tuple in the select part of the query.</span></span> <span data-ttu-id="b2f3c-158">W związku z tym zapytanie ma krotka jako typ elementu.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-158">Therefore, the query has a tuple as an element type.</span></span>
<br />

```fsharp
printfn "Freight for some orders: "

query { 
  for order in db.Orders do
  sortBy (order.OrderDate.Value)
  thenBy (order.OrderID)
  select (order.OrderDate, order.OrderID, order.Customer.CompanyName)
} |> Seq.iter (fun (orderDate, orderID, company) ->
                  printfn "OrderDate: %s" (orderDate.GetValueOrDefault().ToString())
                  printfn "OrderID: %d Company: %s\n" orderID company)
```

8. <span data-ttu-id="b2f3c-159">Ignoruj określoną liczbę rekordów za pomocą operatora skip i użyj operatora podjęcia, aby określić liczbę zwracanych rekordów.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-159">Ignore a specified number of records by using the skip operator, and use the take operator to specify a number of records to return.</span></span> <span data-ttu-id="b2f3c-160">W ten sposób można zaimplementować stronicowania na strumieniowych źródeł danych.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-160">In this way, you can implement paging on data feeds.</span></span>
<br />

```fsharp
printfn "Get the first page of 2 employees."

query { 
  for employee in db.Employees do
  take 2
  select employee
} |> Seq.iter (fun employee ->
                  printfn "Name: %s ID: %d" (employee.FirstName + " " + employee.LastName) (employee.EmployeeID)) 

printfn "Get the next 2 employees."

query { 
  for employee in db.Employees do
  skip 2
  take 2
  select employee
} |> Seq.iter (fun employee ->
                  printfn "Name: %s ID: %d" (employee.FirstName + " " + employee.LastName) (employee.EmployeeID))
```

## <a name="verifying-the-odata-request"></a><span data-ttu-id="b2f3c-161">Weryfikowanie żądania OData</span><span class="sxs-lookup"><span data-stu-id="b2f3c-161">Verifying the OData request</span></span>
<span data-ttu-id="b2f3c-162">Zapytania OData, co jest przetłumaczyć określonego identyfikatora URI żądania OData.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-162">Every OData query is translated into a specific OData request URI.</span></span> <span data-ttu-id="b2f3c-163">Możesz sprawdzić, czy identyfikator URI, być może do debugowania w celach przez dodanie obsługi zdarzeń do `SendingRequest` zdarzenia dla obiekt kontekstu danych.</span><span class="sxs-lookup"><span data-stu-id="b2f3c-163">You can verify that URI, perhaps for debugging purposes, by adding an event handler to the `SendingRequest` event on the full data context object.</span></span>


#### <a name="to-verify-the-odata-request"></a><span data-ttu-id="b2f3c-164">Aby sprawdzić żądanie OData</span><span class="sxs-lookup"><span data-stu-id="b2f3c-164">To verify the OData request</span></span>

- <span data-ttu-id="b2f3c-165">Aby sprawdzić identyfikator URI żądania OData, użyj następującego kodu:</span><span class="sxs-lookup"><span data-stu-id="b2f3c-165">To verify the OData request URI, use the following code:</span></span>
<br />

```fsharp
// The DataContext property returns the full data context.
db.DataContext.SendingRequest.Add (fun eventArgs -> printfn "Requesting %A" eventArgs.Request.RequestUri)
```

<span data-ttu-id="b2f3c-166">Dane wyjściowe poprzedniego kodu to:</span><span class="sxs-lookup"><span data-stu-id="b2f3c-166">The output of the previous code is:</span></span>
<br />`requesting http://services.odata.org/Northwind/Northwind.svc/Orders()?$orderby=ShippedDate&amp;$select=OrderID,ShippedDate`


## <a name="see-also"></a><span data-ttu-id="b2f3c-167">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="b2f3c-167">See Also</span></span>
[<span data-ttu-id="b2f3c-168">Wyrażenia zapytań</span><span class="sxs-lookup"><span data-stu-id="b2f3c-168">Query Expressions</span></span>](../../language-reference/query-expressions.md)

[<span data-ttu-id="b2f3c-169">Zagadnienia dotyczące LINQ (usługi danych WCF)</span><span class="sxs-lookup"><span data-stu-id="b2f3c-169">LINQ Considerations (WCF Data Services)</span></span>](https://msdn.microsoft.com/library/ee622463.aspx)

[<span data-ttu-id="b2f3c-170">Odataservice — dostawca typów (F #)</span><span class="sxs-lookup"><span data-stu-id="b2f3c-170">ODataService Type Provider (F#)</span></span>](https://msdn.microsoft.com/visualfsharpdocs/conceptual/odataservice-type-provider-%5bfsharp%5d)
