---
title: Błąd kompilatora CS0459
ms.date: 07/20/2015
f1_keywords:
- CS0459
helpviewer_keywords:
- CS0459
ms.assetid: 01b058dd-8d65-4e9d-9de1-d47f9488d22a
ms.openlocfilehash: 4f6ae378f90f19060a5adcd4f2491fe81a299882
ms.sourcegitcommit: 559259da2738a7b33a46c0130e51d336091c2097
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 10/22/2019
ms.locfileid: "72773840"
---
# <a name="compiler-error-cs0459"></a><span data-ttu-id="e057a-102">Błąd kompilatora CS0459</span><span class="sxs-lookup"><span data-stu-id="e057a-102">Compiler Error CS0459</span></span>

<span data-ttu-id="e057a-103">Nie można pobrać adresu zmiennej lokalnej tylko do odczytu</span><span class="sxs-lookup"><span data-stu-id="e057a-103">Cannot take the address of a read-only local variable</span></span>

 <span data-ttu-id="e057a-104">W C# języku, który generuje zmienne lokalne tylko do odczytu, istnieją trzy typowe scenariusze: `foreach`, `using` i `fixed`.</span><span class="sxs-lookup"><span data-stu-id="e057a-104">There are three common scenarios in the C# language that generate read-only local variables: `foreach`, `using`, and `fixed`.</span></span> <span data-ttu-id="e057a-105">W każdym z tych przypadków nie wolno pisać do zmiennej lokalnej tylko do odczytu ani do jej adresu.</span><span class="sxs-lookup"><span data-stu-id="e057a-105">In each of these cases, you are not allowed to write to the read-only local variable, or to take its address.</span></span> <span data-ttu-id="e057a-106">Ten błąd jest generowany, gdy kompilator realizuje próbę uzyskania adresu zmiennej lokalnej tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="e057a-106">This error is generated when the compiler realizes you are trying to take the address of a read-only local variable.</span></span>

## <a name="example"></a><span data-ttu-id="e057a-107">Przykład</span><span class="sxs-lookup"><span data-stu-id="e057a-107">Example</span></span>

 <span data-ttu-id="e057a-108">Poniższy przykład generuje CS0459, gdy podejmowana jest próba podjęcia adresu zmiennej lokalnej tylko do odczytu w pętli `foreach` i w bloku instrukcji `fixed`:</span><span class="sxs-lookup"><span data-stu-id="e057a-108">The following example generates CS0459 when an attempt is made to take the address of a read-only local variable in a `foreach` loop and in a `fixed` statement block:</span></span>

```csharp
// CS0459.cs
// compile with: /unsafe

class Program
{
    public unsafe void M1()
    {
        int[] ints = new int[] { 1, 2, 3 };
        foreach (int i in ints)
        {
            int *j = &i;  // CS0459
        }

        fixed (int *i = &_i)
        {
            int **j = &i;  // CS0459
        }
    }

    private int _i = 0;
}
```

> [!NOTE]
> <span data-ttu-id="e057a-109">Kompilator Roslyn został zaktualizowany i ten błąd kompilatora został usunięty z programu Visual Studio 2017 w wersji 15,5, dlatego poprzedni kod zostanie pomyślnie skompilowany z tą wersją i nowszą.</span><span class="sxs-lookup"><span data-stu-id="e057a-109">Roslyn compiler was updated and this compiler error was removed starting with Visual Studio 2017 version 15.5, so the previous code would compile successfully with this version and later.</span></span>
