---
title: Kontrakty kodu
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-clr
ms.tgt_pltfrm: 
ms.topic: article
dev_langs:
- csharp
- vb
helpviewer_keywords: Code contracts
ms.assetid: 84526045-496f-489d-8517-a258cf76f040
caps.latest.revision: "15"
author: mairaw
ms.author: mairaw
manager: wpickett
ms.workload: dotnet
ms.openlocfilehash: 4a7f6dd2f97f7d57cdaa59d1420a34409804f9dd
ms.sourcegitcommit: 16186c34a957fdd52e5db7294f291f7530ac9d24
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 12/22/2017
---
# <a name="code-contracts"></a><span data-ttu-id="789ed-102">Kontrakty kodu</span><span class="sxs-lookup"><span data-stu-id="789ed-102">Code Contracts</span></span>
<span data-ttu-id="789ed-103">Kontrakty kodu zapewniają możliwość określenia warunków wstępnych, postconditions i invariants obiektu w kodzie.</span><span class="sxs-lookup"><span data-stu-id="789ed-103">Code contracts provide a way to specify preconditions, postconditions, and object invariants in your code.</span></span> <span data-ttu-id="789ed-104">Warunki wstępne są wymagania, które muszą zostać spełnione, wprowadzając metody lub właściwości.</span><span class="sxs-lookup"><span data-stu-id="789ed-104">Preconditions are requirements that must be met when entering a method or property.</span></span> <span data-ttu-id="789ed-105">Postconditions opisano oczekiwań w czasie, który zamyka kodu metody lub właściwości.</span><span class="sxs-lookup"><span data-stu-id="789ed-105">Postconditions describe expectations at the time the method or property code exits.</span></span> <span data-ttu-id="789ed-106">Obiekt invariants opisano oczekiwanym stanem dla klasy, która jest w dobrym stanie.</span><span class="sxs-lookup"><span data-stu-id="789ed-106">Object invariants describe the expected state for a class that is in a good state.</span></span>  
  
 <span data-ttu-id="789ed-107">Kontrakty kodu obejmują klasy znakowania kodu, statyczne Analizator do analizy w czasie kompilacji i analizatora czasu wykonywania.</span><span class="sxs-lookup"><span data-stu-id="789ed-107">Code contracts include classes for marking your code, a static analyzer for compile-time analysis, and a runtime analyzer.</span></span> <span data-ttu-id="789ed-108">Klasy dla kontraktów kodu można znaleźć w <xref:System.Diagnostics.Contracts> przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="789ed-108">The classes for code contracts can be found in the <xref:System.Diagnostics.Contracts> namespace.</span></span>  
  
 <span data-ttu-id="789ed-109">Korzyści wynikające z kontraktów kodu są następujące:</span><span class="sxs-lookup"><span data-stu-id="789ed-109">The benefits of code contracts include the following:</span></span>  
  
-   <span data-ttu-id="789ed-110">Ulepszone testowanie: kontraktów kodu zapewniają weryfikacji statycznych kontraktu, sprawdzanie środowiska uruchomieniowego i generowania dokumentacji.</span><span class="sxs-lookup"><span data-stu-id="789ed-110">Improved testing: Code contracts provide static contract verification, runtime checking, and documentation generation.</span></span>  
  
-   <span data-ttu-id="789ed-111">Automatyczne narzędzia do testowania: kontraktów kodu można użyć, aby wygenerować bardziej zrozumiałej testów jednostkowych przez filtrowanie znaczenia testu argumenty, które nie spełniają warunków wstępnych.</span><span class="sxs-lookup"><span data-stu-id="789ed-111">Automatic testing tools: You can use code contracts to generate more meaningful unit tests by filtering out meaningless test arguments that do not satisfy preconditions.</span></span>  
  
-   <span data-ttu-id="789ed-112">Weryfikacja statycznych: statyczne sprawdzania można zdecydować, czy istnieją wszelkie naruszenia Umowy, bez konieczności uruchamiania programu.</span><span class="sxs-lookup"><span data-stu-id="789ed-112">Static verification: The static checker can decide whether there are any contract violations without running the program.</span></span> <span data-ttu-id="789ed-113">Sprawdza niejawne umów, takich jak null wyłuskań i Tablica granice i kontrakty jawnego.</span><span class="sxs-lookup"><span data-stu-id="789ed-113">It checks for implicit contracts, such as null dereferences and array bounds, and explicit contracts.</span></span>  
  
-   <span data-ttu-id="789ed-114">Odwołanie dokumentacji: generator dokumentacji wspomaga istniejące pliki dokumentacji XML z informacjami o kontraktu.</span><span class="sxs-lookup"><span data-stu-id="789ed-114">Reference documentation: The documentation generator augments existing XML documentation files with contract information.</span></span> <span data-ttu-id="789ed-115">Dostępne są także arkusze stylów, które mogą być używane z [Sandcastle](https://github.com/EWSoftware/SHFB) tak, aby stron wygenerowanych dokumentacji zawierać sekcji kontraktu.</span><span class="sxs-lookup"><span data-stu-id="789ed-115">There are also style sheets that can be used with [Sandcastle](https://github.com/EWSoftware/SHFB) so that the generated documentation pages have contract sections.</span></span>  
  
 <span data-ttu-id="789ed-116">We wszystkich językach .NET Framework można od razu korzystać z umów. nie trzeba napisać specjalny analizator lub kompilatora.</span><span class="sxs-lookup"><span data-stu-id="789ed-116">All .NET Framework languages can immediately take advantage of contracts; you do not have to write a special parser or compiler.</span></span> <span data-ttu-id="789ed-117">Dodatek Visual Studio umożliwia określenie poziomu analizy kontraktu kodu do wykonania.</span><span class="sxs-lookup"><span data-stu-id="789ed-117">A Visual Studio add-in lets you specify the level of code contract analysis to be performed.</span></span> <span data-ttu-id="789ed-118">Analizatory można potwierdzić, że kontrakty te są poprawnie sformułowanym (kontrola typów i rozpoznawanie nazw) i może wywoływać skompilowanej postaci kontraktów w formacie język pośredni (MSIL) firmy Microsoft.</span><span class="sxs-lookup"><span data-stu-id="789ed-118">The analyzers can confirm that the contracts are well-formed (type checking and name resolution) and can produce a compiled form of the contracts in Microsoft intermediate language (MSIL) format.</span></span> <span data-ttu-id="789ed-119">Tworzenie umów w programie Visual Studio umożliwia wykorzystanie standardu IntelliSense dostarczanych przez narzędzie.</span><span class="sxs-lookup"><span data-stu-id="789ed-119">Authoring contracts in Visual Studio lets you take advantage of the standard IntelliSense provided by the tool.</span></span>  
  
 <span data-ttu-id="789ed-120">Większości metod w klasie kontraktu są warunkowo skompilowany; oznacza to, że kompilator emituje wywołania tych metod, tylko wtedy, gdy zdefiniować szczególne symbol CONTRACTS_FULL, przy użyciu `#define` dyrektywy.</span><span class="sxs-lookup"><span data-stu-id="789ed-120">Most methods in the contract class are conditionally compiled; that is, the compiler emits calls to these methods only when  you define a special symbol, CONTRACTS_FULL, by using the `#define` directive.</span></span> <span data-ttu-id="789ed-121">CONTRACTS_FULL umożliwia zapisanie kontraktów w kodzie bez użycia `#ifdef` dyrektywy; może utworzyć różne kompilacje, niektóre z kontraktami i niektóre bez.</span><span class="sxs-lookup"><span data-stu-id="789ed-121">CONTRACTS_FULL lets you write contracts in your code without using `#ifdef` directives; you can produce different builds, some with contracts, and some without.</span></span>  
  
 <span data-ttu-id="789ed-122">Narzędzia i szczegółowe instrukcje dotyczące używania kontraktów kodu, zobacz [kontraktów kodu](http://go.microsoft.com/fwlink/?LinkId=152461) w witrynie MSDN DevLabs.</span><span class="sxs-lookup"><span data-stu-id="789ed-122">For tools and detailed instructions for using code contracts, see [Code Contracts](http://go.microsoft.com/fwlink/?LinkId=152461) on the MSDN DevLabs Web site.</span></span>  
  
## <a name="preconditions"></a><span data-ttu-id="789ed-123">Warunki wstępne</span><span class="sxs-lookup"><span data-stu-id="789ed-123">Preconditions</span></span>  
 <span data-ttu-id="789ed-124">Warunki wstępne można wyrazić przy użyciu <xref:System.Diagnostics.Contracts.Contract.Requires%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-124">You can express preconditions by using the <xref:System.Diagnostics.Contracts.Contract.Requires%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="789ed-125">Warunki wstępne Określ stan po wywołaniu metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-125">Preconditions specify state when a method is invoked.</span></span> <span data-ttu-id="789ed-126">Zwykle są one używane do określenia prawidłowymi wartościami parametrów.</span><span class="sxs-lookup"><span data-stu-id="789ed-126">They are generally used to specify valid parameter values.</span></span> <span data-ttu-id="789ed-127">Wszystkie elementy członkowskie, które są wymienione w warunkach wstępnych muszą być co najmniej jako dostępne jako metoda. w przeciwnym razie warunki wstępne nie może być rozpoznawany przez wszystkich wywołań metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-127">All members that are mentioned in preconditions must be at least as accessible as the method itself; otherwise, the precondition might not be understood by all callers of a method.</span></span> <span data-ttu-id="789ed-128">Warunek musi mieć żadnych efektów ubocznych.</span><span class="sxs-lookup"><span data-stu-id="789ed-128">The condition must have no side-effects.</span></span> <span data-ttu-id="789ed-129">Zachowanie środowiska wykonawczego nie powiodło się warunki wstępne jest określana przez analizator środowiska wykonawczego.</span><span class="sxs-lookup"><span data-stu-id="789ed-129">The run-time behavior of failed preconditions is determined by the runtime analyzer.</span></span>  
  
 <span data-ttu-id="789ed-130">Na przykład następujący warunek wstępny wyraża parametru `x` musi mieć wartości null.</span><span class="sxs-lookup"><span data-stu-id="789ed-130">For example, the following precondition expresses that parameter `x` must be non-null.</span></span>  
  
 `Contract.Requires( x != null );`  
  
 <span data-ttu-id="789ed-131">Jeśli kodu musi zgłosić wyjątek określonego w przypadku niepowodzenia warunku wstępnego, można użyć ogólnego przeciążenia <xref:System.Diagnostics.Contracts.Contract.Requires%2A> w następujący sposób.</span><span class="sxs-lookup"><span data-stu-id="789ed-131">If your code must throw a particular exception on failure of a precondition, you can use the generic overload of <xref:System.Diagnostics.Contracts.Contract.Requires%2A> as follows.</span></span>  
  
 `Contract.Requires<ArgumentNullException>( x != null, "x" );`  
  
### <a name="legacy-requires-statements"></a><span data-ttu-id="789ed-132">Starsza wersja wymaga deklaracji</span><span class="sxs-lookup"><span data-stu-id="789ed-132">Legacy Requires Statements</span></span>  
 <span data-ttu-id="789ed-133">Większość kodu zawiera niektóre sprawdzanie poprawności parametru w postaci `if` - `then` - `throw` kodu.</span><span class="sxs-lookup"><span data-stu-id="789ed-133">Most code contains some parameter validation in the form of `if`-`then`-`throw` code.</span></span> <span data-ttu-id="789ed-134">Narzędzia kontraktu rozpoznać te instrukcje jako warunki wstępne w następujących przypadkach:</span><span class="sxs-lookup"><span data-stu-id="789ed-134">The contract tools recognize these statements as preconditions in the following cases:</span></span>  
  
-   <span data-ttu-id="789ed-135">Instrukcje występować przed wszystkimi innymi instrukcjami w metodzie.</span><span class="sxs-lookup"><span data-stu-id="789ed-135">The statements appear before any other statements in a method.</span></span>  
  
-   <span data-ttu-id="789ed-136">Cały zestaw takich instrukcji następuje jawnego <xref:System.Diagnostics.Contracts.Contract> wywołania metody, takie jak wywołanie <xref:System.Diagnostics.Contracts.Contract.Requires%2A>, <xref:System.Diagnostics.Contracts.Contract.Ensures%2A>, <xref:System.Diagnostics.Contracts.Contract.EnsuresOnThrow%2A>, lub <xref:System.Diagnostics.Contracts.Contract.EndContractBlock%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-136">The entire set of such statements is followed by an explicit <xref:System.Diagnostics.Contracts.Contract> method call, such as a call to the <xref:System.Diagnostics.Contracts.Contract.Requires%2A>, <xref:System.Diagnostics.Contracts.Contract.Ensures%2A>, <xref:System.Diagnostics.Contracts.Contract.EnsuresOnThrow%2A>, or <xref:System.Diagnostics.Contracts.Contract.EndContractBlock%2A> method.</span></span>  
  
 <span data-ttu-id="789ed-137">Gdy `if` - `then` - `throw` instrukcje pojawiają się w tym formularzu narzędzia rozpoznaje je jako starszych `requires` instrukcje.</span><span class="sxs-lookup"><span data-stu-id="789ed-137">When `if`-`then`-`throw` statements appear in this form, the tools recognize them as legacy `requires` statements.</span></span> <span data-ttu-id="789ed-138">Jeśli nie inne umowy wykonaj `if` - `then` - `throw` sekwencji, kod zakończenia <xref:System.Diagnostics.Contracts.Contract.EndContractBlock%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-138">If no other contracts follow the `if`-`then`-`throw` sequence, end the code with the <xref:System.Diagnostics.Contracts.Contract.EndContractBlock%2A?displayProperty=nameWithType> method.</span></span>  
  
```  
if ( x == null ) throw new ...  
Contract.EndContractBlock(); // All previous "if" checks are preconditions  
```  
  
 <span data-ttu-id="789ed-139">Należy pamiętać, że warunek w poprzednim teście zanegowaną warunek wstępny.</span><span class="sxs-lookup"><span data-stu-id="789ed-139">Note that the condition in the preceding test is a negated precondition.</span></span> <span data-ttu-id="789ed-140">(Będzie rzeczywiste warunek wstępny `x != null`.) Zanegowaną warunku wstępnego jest bardzo ograniczoną: musi być napisana, jak pokazano w poprzednim przykładzie; oznacza to, że powinien on zawierać nie `else` klauzule i treści `then` klauzuli musi być pojedynczym `throw` instrukcji.</span><span class="sxs-lookup"><span data-stu-id="789ed-140">(The actual precondition would be `x != null`.) A negated precondition is highly restricted: It must be written as shown in the previous example; that is, it should contain no `else` clauses, and the body of the `then` clause must be a single `throw` statement.</span></span> <span data-ttu-id="789ed-141">`if` Testu podlega reguły zarówno czystości i widoczności (zobacz [wskazówki dotyczące użycia](#usage_guidelines)), ale `throw` wyrażenie podlega tylko czystości reguły.</span><span class="sxs-lookup"><span data-stu-id="789ed-141">The `if` test is subject to both purity and visibility rules (see [Usage Guidelines](#usage_guidelines)), but the `throw` expression is subject only to purity rules.</span></span> <span data-ttu-id="789ed-142">Jednak należy jako widoczna jako metody, w którym występuje kontrakt typ zgłoszono wyjątek.</span><span class="sxs-lookup"><span data-stu-id="789ed-142">However, the type of the exception thrown must be as visible as the method in which the contract occurs.</span></span>  
  
## <a name="postconditions"></a><span data-ttu-id="789ed-143">Postconditions</span><span class="sxs-lookup"><span data-stu-id="789ed-143">Postconditions</span></span>  
 <span data-ttu-id="789ed-144">Postconditions są kontrakty stanu metody, gdy zostaje zakończone.</span><span class="sxs-lookup"><span data-stu-id="789ed-144">Postconditions are contracts for the state of a method when it terminates.</span></span> <span data-ttu-id="789ed-145">Warunku końcowego zaznaczono tylko zakończeniem metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-145">The postcondition is checked just before exiting a method.</span></span> <span data-ttu-id="789ed-146">Zachowanie środowiska wykonawczego nie powiodło się postconditions jest określana przez analizator środowiska wykonawczego.</span><span class="sxs-lookup"><span data-stu-id="789ed-146">The run-time behavior of failed postconditions is determined by the runtime analyzer.</span></span>  
  
 <span data-ttu-id="789ed-147">W odróżnieniu od warunki wstępne postconditions może odwoływać się do elementów członkowskich z mniejszą widoczność.</span><span class="sxs-lookup"><span data-stu-id="789ed-147">Unlike preconditions, postconditions may reference members with less visibility.</span></span> <span data-ttu-id="789ed-148">Klient nie może być można zrozumieć lub wprowadzić używać niektórych informacji wyrażone w warunku końcowego przy użyciu prywatnych stanu, ale nie ma to wpływu na możliwość klienta przy użyciu metody poprawnie.</span><span class="sxs-lookup"><span data-stu-id="789ed-148">A client may not be able to understand or make use of some of the information expressed by a postcondition using private state, but this does not affect the client's ability to use the method correctly.</span></span>  
  
### <a name="standard-postconditions"></a><span data-ttu-id="789ed-149">Standardowe Postconditions</span><span class="sxs-lookup"><span data-stu-id="789ed-149">Standard Postconditions</span></span>  
 <span data-ttu-id="789ed-150">Standardowe postconditions można wyrazić przy użyciu <xref:System.Diagnostics.Contracts.Contract.Ensures%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-150">You can express standard postconditions by using the <xref:System.Diagnostics.Contracts.Contract.Ensures%2A> method.</span></span> <span data-ttu-id="789ed-151">Postconditions express warunek, który musi być `true` po zakończeniu normalne metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-151">Postconditions express a condition that must be `true` upon normal termination of the method.</span></span>  
  
 `Contract.Ensures( this.F > 0 );`  
  
### <a name="exceptional-postconditions"></a><span data-ttu-id="789ed-152">Wyjątkowych Postconditions</span><span class="sxs-lookup"><span data-stu-id="789ed-152">Exceptional Postconditions</span></span>  
 <span data-ttu-id="789ed-153">Wyjątkowych postconditions są postconditions, które powinny być `true` podczas określonego wyjątku przez metodę.</span><span class="sxs-lookup"><span data-stu-id="789ed-153">Exceptional postconditions are postconditions that should be `true` when a particular exception is thrown by a method.</span></span> <span data-ttu-id="789ed-154">Można określić te postconditions przy użyciu <xref:System.Diagnostics.Contracts.Contract.EnsuresOnThrow%2A?displayProperty=nameWithType> metody, jak przedstawiono na poniższym przykładzie.</span><span class="sxs-lookup"><span data-stu-id="789ed-154">You can specify these postconditions by using the <xref:System.Diagnostics.Contracts.Contract.EnsuresOnThrow%2A?displayProperty=nameWithType> method, as the following example shows.</span></span>  
  
 `Contract.EnsuresOnThrow<T>( this.F > 0 );`  
  
 <span data-ttu-id="789ed-155">Argument jest warunek, który musi być `true` zawsze, gdy wyjątek, który jest podtypem `T` jest generowany.</span><span class="sxs-lookup"><span data-stu-id="789ed-155">The argument is the condition that must be `true` whenever an exception that is a subtype of `T` is thrown.</span></span>  
  
 <span data-ttu-id="789ed-156">Brak niektórych typów wyjątków, które są trudne do użycia w wyjątkowych warunku końcowego.</span><span class="sxs-lookup"><span data-stu-id="789ed-156">There are some exception types that are difficult to use in an exceptional postcondition.</span></span> <span data-ttu-id="789ed-157">Na przykład przy użyciu typu <xref:System.Exception> dla `T` wymaga metody zagwarantowanie warunku bez względu na typ wyjątku, który jest zgłaszany, nawet jeśli jest przepełnienie stosu lub innych wyjątku niemożliwe do formantu.</span><span class="sxs-lookup"><span data-stu-id="789ed-157">For example, using the type <xref:System.Exception> for `T` requires the method to guarantee the condition regardless of the type of exception that is thrown, even if it is a stack overflow or other impossible-to-control exception.</span></span> <span data-ttu-id="789ed-158">Tylko dla określonych wyjątków, które może zostać zgłoszony, gdy element członkowski jest wywoływana, na przykład kiedy należy używać wyjątkowych postconditions <xref:System.InvalidTimeZoneException> jest generowany dla <xref:System.TimeZoneInfo> wywołania metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-158">You should use exceptional postconditions only for specific exceptions that might be thrown when a member is called, for example, when an <xref:System.InvalidTimeZoneException> is thrown for a <xref:System.TimeZoneInfo> method call.</span></span>  
  
### <a name="special-postconditions"></a><span data-ttu-id="789ed-159">Specjalne Postconditions</span><span class="sxs-lookup"><span data-stu-id="789ed-159">Special Postconditions</span></span>  
 <span data-ttu-id="789ed-160">Następujących metod można stosować tylko w obrębie postconditions:</span><span class="sxs-lookup"><span data-stu-id="789ed-160">The following methods may be used only within postconditions:</span></span>  
  
-   <span data-ttu-id="789ed-161">Może się odwoływać do wartości zwracanych metody w postconditions przy użyciu wyrażenia `Contract.Result<T>()`, gdzie `T` zastępuje zwracany typ metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-161">You can refer to method return values in postconditions by using the expression `Contract.Result<T>()`, where `T` is replaced by the return type of the method.</span></span> <span data-ttu-id="789ed-162">Kompilator jest nie można wywnioskować typu, należy jawnie określić go.</span><span class="sxs-lookup"><span data-stu-id="789ed-162">When the compiler is unable to infer the type, you must explicitly provide it.</span></span> <span data-ttu-id="789ed-163">Na przykład nie można wywnioskować typów dla metod, które nie przyjmują żadnych argumentów, dzięki czemu wymaga następujących warunku końcowego jest kompilatora C#: `Contract.Ensures(0 <Contract.Result<int>())` metod z typem zwracanym `void` nie może odwoływać się do `Contract.Result<T>()` w ich postconditions.</span><span class="sxs-lookup"><span data-stu-id="789ed-163">For example, the C# compiler is unable to infer types for methods that do not take any arguments, so it requires the following postcondition: `Contract.Ensures(0 <Contract.Result<int>())` Methods with a return type of `void` cannot refer to `Contract.Result<T>()` in their postconditions.</span></span>  
  
-   <span data-ttu-id="789ed-164">Wartość prestate w warunku końcowego odwołuje się do wartości wyrażenia na początku metody lub właściwości.</span><span class="sxs-lookup"><span data-stu-id="789ed-164">A prestate value in a postcondition refers to the value of an expression at the start of a method or property.</span></span> <span data-ttu-id="789ed-165">Używa wyrażenia `Contract.OldValue<T>(e)`, gdzie `T` jest typem `e`.</span><span class="sxs-lookup"><span data-stu-id="789ed-165">It uses the expression `Contract.OldValue<T>(e)`, where `T` is the type of `e`.</span></span> <span data-ttu-id="789ed-166">W przypadku pominięcia argumentu typu ogólnego, zawsze, gdy kompilator jest w stanie wywnioskować jej typu.</span><span class="sxs-lookup"><span data-stu-id="789ed-166">You can omit the generic type argument whenever the compiler is able to infer its type.</span></span> <span data-ttu-id="789ed-167">(Na przykład kompilatora C# zawsze wnioskuje typ z powodu argumentu.) Istnieje kilka ograniczeń na to, co może wystąpić w `e` i konteksty, w których mogą występować wyrażenie stary.</span><span class="sxs-lookup"><span data-stu-id="789ed-167">(For example, the C# compiler always infers the type because it takes an argument.) There are several restrictions on what can occur in `e` and the contexts in which an old expression may appear.</span></span> <span data-ttu-id="789ed-168">Stare wyrażenie nie może zawierać innego wyrażenia stary.</span><span class="sxs-lookup"><span data-stu-id="789ed-168">An old expression cannot contain another old expression.</span></span> <span data-ttu-id="789ed-169">Przede wszystkim stare wyrażenie musi odwoływać się do wartości, który istniał w stanie warunek wstępny — metoda.</span><span class="sxs-lookup"><span data-stu-id="789ed-169">Most importantly, an old expression must refer to a value that existed in the method's precondition state.</span></span> <span data-ttu-id="789ed-170">Innymi słowy, musi być wyrażenie, które może przyjąć tak długo, jak jest warunek wstępny metody `true`.</span><span class="sxs-lookup"><span data-stu-id="789ed-170">In other words, it must be an expression that can be evaluated as long as the method's precondition is `true`.</span></span> <span data-ttu-id="789ed-171">Poniżej przedstawiono kilka wystąpień tej reguły.</span><span class="sxs-lookup"><span data-stu-id="789ed-171">Here are several instances of that rule.</span></span>  
  
    -   <span data-ttu-id="789ed-172">Wartość musi istnieć w stanie warunek wstępny metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-172">The value must exist in the method's precondition state.</span></span> <span data-ttu-id="789ed-173">W celu odwoływania się do pola w obiekcie, warunki wstępne musi gwarancji, że dany obiekt jest zawsze inną niż null.</span><span class="sxs-lookup"><span data-stu-id="789ed-173">In order to reference a field on an object, the preconditions must guarantee that that object is always non-null.</span></span>  
  
    -   <span data-ttu-id="789ed-174">Nie można odwołać się do wartości zwracanej metody w wyrażeniu starego:</span><span class="sxs-lookup"><span data-stu-id="789ed-174">You cannot refer to the method's return value in an old expression:</span></span>  
  
        ```  
        Contract.OldValue(Contract.Result<int>() + x) // ERROR  
        ```  
  
    -   <span data-ttu-id="789ed-175">Nie można odwołać się do `out` parametrów w wyrażeniu stary.</span><span class="sxs-lookup"><span data-stu-id="789ed-175">You cannot refer to `out` parameters in an old expression.</span></span>  
  
    -   <span data-ttu-id="789ed-176">Stare wyrażenie nie może zależeć od zmiennej powiązanej kwantyfikatora, jeśli zakres kwantyfikatora zależy od wartość zwracaną przez metodę:</span><span class="sxs-lookup"><span data-stu-id="789ed-176">An old expression cannot depend on the bound variable of a quantifier if the range of the quantifier depends on the return value of the method:</span></span>  
  
        ```  
        Contract. ForAll (0,Contract. Result<int>(),  
        i => Contract.OldValue(xs[i]) > 3); // ERROR  
        ```  
  
    -   <span data-ttu-id="789ed-177">Stare wyrażenie nie może odwoływać się do parametru anonimowe delegata <xref:System.Diagnostics.Contracts.Contract.ForAll%2A> lub <xref:System.Diagnostics.Contracts.Contract.Exists%2A> wywołania, chyba że jest on używany jako indeksatora lub argument wywołania metody:</span><span class="sxs-lookup"><span data-stu-id="789ed-177">An old expression cannot refer to the parameter of the anonymous delegate in a <xref:System.Diagnostics.Contracts.Contract.ForAll%2A> or <xref:System.Diagnostics.Contracts.Contract.Exists%2A> call unless it is used as an indexer or argument to a method call:</span></span>  
  
        ```  
        Contract. ForAll (0, xs .Length, i => Contract.OldValue(xs[i]) > 3); // OK  
        Contract. ForAll (0, xs .Length, i => Contract.OldValue(i) > 3); // ERROR  
        ```  
  
    -   <span data-ttu-id="789ed-178">Stare wyrażenie nie może wystąpić w treści anonimowe delegata Jeśli wartość wyrażenia starego zależy od żadnego parametrów anonimowej delegata, chyba że anonimowe delegat jest argument <xref:System.Diagnostics.Contracts.Contract.ForAll%2A> lub <xref:System.Diagnostics.Contracts.Contract.Exists%2A> metody:</span><span class="sxs-lookup"><span data-stu-id="789ed-178">An old expression cannot occur in the body of an anonymous delegate if the value of the old expression depends on any of the parameters of the anonymous delegate, unless the anonymous delegate is an argument to the <xref:System.Diagnostics.Contracts.Contract.ForAll%2A> or <xref:System.Diagnostics.Contracts.Contract.Exists%2A> method:</span></span>  
  
        ```  
        Method( ... (T t) => Contract.OldValue(... t ...) ... ); // ERROR  
        ```  
  
    -   <span data-ttu-id="789ed-179">`Out`Parametry powodują problemu, ponieważ kontrakty występować przed treści metody, a większość kompilatory nie zezwalaj na odwołania do `out` parametrów w postconditions.</span><span class="sxs-lookup"><span data-stu-id="789ed-179">`Out` parameters present a problem because contracts appear before the body of the method, and most compilers do not allow references to `out` parameters in postconditions.</span></span> <span data-ttu-id="789ed-180">Aby rozwiązać ten problem, <xref:System.Diagnostics.Contracts.Contract> klasa udostępnia <xref:System.Diagnostics.Contracts.Contract.ValueAtReturn%2A> metodę, która umożliwia warunku końcowego, na podstawie `out` parametru.</span><span class="sxs-lookup"><span data-stu-id="789ed-180">To solve this problem, the <xref:System.Diagnostics.Contracts.Contract> class provides the <xref:System.Diagnostics.Contracts.Contract.ValueAtReturn%2A> method, which allows a postcondition based on an `out` parameter.</span></span>  
  
        ```  
        public void OutParam(out int x) f  
        Contract.Ensures(Contract.ValueAtReturn(out x) == 3);  
        x = 3;  
        ```  
  
         <span data-ttu-id="789ed-181">Jak <xref:System.Diagnostics.Contracts.Contract.OldValue%2A> metody, można pominąć parametr typu ogólnego zawsze, gdy kompilator jest w stanie wywnioskować jej typu.</span><span class="sxs-lookup"><span data-stu-id="789ed-181">As with the <xref:System.Diagnostics.Contracts.Contract.OldValue%2A> method, you can omit the generic type parameter whenever the compiler is able to infer its type.</span></span> <span data-ttu-id="789ed-182">Kontrakt funkcji ponownego zapisu zastępuje wywołanie metody wartość `out` parametru.</span><span class="sxs-lookup"><span data-stu-id="789ed-182">The contract rewriter replaces the method call with the value of the `out` parameter.</span></span> <span data-ttu-id="789ed-183"><xref:System.Diagnostics.Contracts.Contract.ValueAtReturn%2A> Metody może występować tylko w postconditions.</span><span class="sxs-lookup"><span data-stu-id="789ed-183">The <xref:System.Diagnostics.Contracts.Contract.ValueAtReturn%2A> method may appear only in postconditions.</span></span> <span data-ttu-id="789ed-184">Argument do metody musi być `out` pola struktury lub parametr `out` parametru.</span><span class="sxs-lookup"><span data-stu-id="789ed-184">The argument to the method must be an `out` parameter or a field of a structure `out` parameter.</span></span> <span data-ttu-id="789ed-185">Drugie polecenie jest również przydatne podczas odwoływania się do pól w warunku końcowego konstruktora struktury.</span><span class="sxs-lookup"><span data-stu-id="789ed-185">The latter is also useful when referring to fields in the postcondition of a structure constructor.</span></span>  
  
        > [!NOTE]
        >  <span data-ttu-id="789ed-186">Obecnie narzędzi analizy kodu kontraktu nie sprawdzaj czy `out` parametry są poprawnie zainicjowane i pominąć ich wymienionej w warunku końcowego.</span><span class="sxs-lookup"><span data-stu-id="789ed-186">Currently, the code contract analysis tools do not check whether `out` parameters are initialized properly and disregard their mention in the postcondition.</span></span> <span data-ttu-id="789ed-187">W związku z tym w poprzednim przykładzie, jeśli wiersz po kontraktu ma wartość `x` zamiast przypisywać całkowitą go, kompilator nie będzie wystawiać Napraw błąd.</span><span class="sxs-lookup"><span data-stu-id="789ed-187">Therefore, in the previous example, if the line after the contract had used the value of `x` instead of assigning an integer to it, a compiler would not issue the correct error.</span></span> <span data-ttu-id="789ed-188">Jednakże w przypadku kompilacji, w którym symbol preprocesora CONTRACTS_FULL jest niezdefiniowana (takie kompilacji wydania asa), kompilator zgłosi błąd.</span><span class="sxs-lookup"><span data-stu-id="789ed-188">However, on a build where the CONTRACTS_FULL preprocessor symbol is not defined (such asa release build), the compiler will issue an error.</span></span>  
  
## <a name="invariants"></a><span data-ttu-id="789ed-189">Invariants</span><span class="sxs-lookup"><span data-stu-id="789ed-189">Invariants</span></span>  
 <span data-ttu-id="789ed-190">Obiekt invariants są warunki, które powinien mieć wartość PRAWDA dla każdego wystąpienia klasy, gdy ten obiekt jest widoczny dla klienta.</span><span class="sxs-lookup"><span data-stu-id="789ed-190">Object invariants are conditions that should be true for each instance of a class whenever that object is visible to a client.</span></span> <span data-ttu-id="789ed-191">One express warunków, w których obiekt jest uznawane za prawidłowe.</span><span class="sxs-lookup"><span data-stu-id="789ed-191">They express the conditions under which the object is considered to be correct.</span></span>  
  
 <span data-ttu-id="789ed-192">Niezmienna metody są identyfikowane za pomocą oznaczana z <xref:System.Diagnostics.Contracts.ContractInvariantMethodAttribute> atrybutu.</span><span class="sxs-lookup"><span data-stu-id="789ed-192">The invariant methods are identified by being marked with the <xref:System.Diagnostics.Contracts.ContractInvariantMethodAttribute> attribute.</span></span> <span data-ttu-id="789ed-193">Niezmienna metody muszą zawierać żadnego kodu z wyjątkiem sekwencję wywołań <xref:System.Diagnostics.Contracts.Contract.Invariant%2A> metody, z których każdy określa niezmiennej pojedynczych, jak pokazano w poniższym przykładzie.</span><span class="sxs-lookup"><span data-stu-id="789ed-193">The invariant methods must contain no code except for a sequence of calls to the <xref:System.Diagnostics.Contracts.Contract.Invariant%2A> method, each of which specifies an individual invariant, as shown in the following example.</span></span>  
  
```  
[ContractInvariantMethod]  
protected void ObjectInvariant ()   
{  
Contract.Invariant(this.y >= 0);  
Contract.Invariant(this.x > this.y);  
...  
}  
```  
  
 <span data-ttu-id="789ed-194">Invariants warunkowo są definiowane przez preprocesora symbol CONTRACTS_FULL.</span><span class="sxs-lookup"><span data-stu-id="789ed-194">Invariants are conditionally defined by the CONTRACTS_FULL preprocessor symbol.</span></span> <span data-ttu-id="789ed-195">Podczas sprawdzania czasu wykonywania, invariants są sprawdzane na końcu każdej metody publicznej.</span><span class="sxs-lookup"><span data-stu-id="789ed-195">During run-time checking, invariants are checked at the end of each public method.</span></span> <span data-ttu-id="789ed-196">Jeśli niezmiennej nazwa publiczną metodę w tej samej klasie, niezmiennej Sprawdź, czy czy zwykle odbywa się na końcu metody publicznej jest wyłączona.</span><span class="sxs-lookup"><span data-stu-id="789ed-196">If an invariant mentions a public method in the same class, the invariant check that would normally happen at the end of that public method is disabled.</span></span> <span data-ttu-id="789ed-197">Zamiast tego sprawdzanie jest wykonywane tylko na końcu wywołania metody zewnętrznych do tej klasy.</span><span class="sxs-lookup"><span data-stu-id="789ed-197">Instead, the check occurs only at the end of the outermost method call to that class.</span></span> <span data-ttu-id="789ed-198">Dzieje się również, jeśli klasa jest wprowadzono ponownie z powodu wywołanie do metody w klasie innej.</span><span class="sxs-lookup"><span data-stu-id="789ed-198">This also happens if the class is re-entered because of a call to a method on another class.</span></span> <span data-ttu-id="789ed-199">Invariants nie są sprawdzane dla obiekt finalizatory lub żadnych metod, które implementują <xref:System.IDisposable.Dispose%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-199">Invariants are not checked for object finalizers or for any methods that implement the <xref:System.IDisposable.Dispose%2A> method.</span></span>  
  
<a name="usage_guidelines"></a>   
## <a name="usage-guidelines"></a><span data-ttu-id="789ed-200">Wskazówki dotyczące użycia</span><span class="sxs-lookup"><span data-stu-id="789ed-200">Usage Guidelines</span></span>  
  
### <a name="contract-ordering"></a><span data-ttu-id="789ed-201">Umowy zamawiania</span><span class="sxs-lookup"><span data-stu-id="789ed-201">Contract Ordering</span></span>  
 <span data-ttu-id="789ed-202">W poniższej tabeli przedstawiono kolejność elementów, które powinny być używane podczas zapisywania kontraktów — metoda.</span><span class="sxs-lookup"><span data-stu-id="789ed-202">The following table shows the order of elements you should use when you write method contracts.</span></span>  
  
|`If-then-throw statements`|<span data-ttu-id="789ed-203">Warunki wstępne publicznego starszymi wersjami</span><span class="sxs-lookup"><span data-stu-id="789ed-203">Backward-compatible public preconditions</span></span>|  
|-|-|  
|<xref:System.Diagnostics.Contracts.Contract.Requires%2A>|<span data-ttu-id="789ed-204">Wszystkie warunki wstępne publicznego.</span><span class="sxs-lookup"><span data-stu-id="789ed-204">All public preconditions.</span></span>|  
|<xref:System.Diagnostics.Contracts.Contract.Ensures%2A>|<span data-ttu-id="789ed-205">Wszystkie publiczne postconditions (normalne).</span><span class="sxs-lookup"><span data-stu-id="789ed-205">All public (normal) postconditions.</span></span>|  
|<xref:System.Diagnostics.Contracts.Contract.EnsuresOnThrow%2A>|<span data-ttu-id="789ed-206">Wszystkie publiczne postconditions wyjątkowych.</span><span class="sxs-lookup"><span data-stu-id="789ed-206">All public exceptional postconditions.</span></span>|  
|<xref:System.Diagnostics.Contracts.Contract.Ensures%2A>|<span data-ttu-id="789ed-207">Wszystkie prywatne/wewnętrznego postconditions (normalne).</span><span class="sxs-lookup"><span data-stu-id="789ed-207">All private/internal (normal) postconditions.</span></span>|  
|<xref:System.Diagnostics.Contracts.Contract.EnsuresOnThrow%2A>|<span data-ttu-id="789ed-208">Wszystkie prywatne/wewnętrznego postconditions wyjątkowych.</span><span class="sxs-lookup"><span data-stu-id="789ed-208">All private/internal exceptional postconditions.</span></span>|  
|<xref:System.Diagnostics.Contracts.Contract.EndContractBlock%2A>|<span data-ttu-id="789ed-209">Jeśli przy użyciu `if` - `then` - `throw` styl warunki wstępne bez innych umów, Umieść wywołanie <xref:System.Diagnostics.Contracts.Contract.EndContractBlock%2A> z informacją, że wszystkie poprzednie, jeśli sprawdzanie warunków wstępnych.</span><span class="sxs-lookup"><span data-stu-id="789ed-209">If using `if`-`then`-`throw` style preconditions without any other contracts, place a call to <xref:System.Diagnostics.Contracts.Contract.EndContractBlock%2A> to indicate that all previous if checks are preconditions.</span></span>|  
  
<a name="purity"></a>   
### <a name="purity"></a><span data-ttu-id="789ed-210">Czystości</span><span class="sxs-lookup"><span data-stu-id="789ed-210">Purity</span></span>  
 <span data-ttu-id="789ed-211">Wszystkie metody, które są wywoływane w ramach kontraktu musi być czysty; oznacza to nie należy ich zaktualizować żadnych istniejących stanu.</span><span class="sxs-lookup"><span data-stu-id="789ed-211">All methods that are called within a contract must be pure; that is, they must not update any preexisting state.</span></span> <span data-ttu-id="789ed-212">Czystej metody może modyfikować obiekty, które zostały utworzone po wejściu w czystej metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-212">A pure method is allowed to modify objects that have been created after entry into the pure method.</span></span>  
  
 <span data-ttu-id="789ed-213">Obecnie narzędzia kontraktu kodu założono, że czysty są następujące elementy kodu:</span><span class="sxs-lookup"><span data-stu-id="789ed-213">Code contract tools currently assume that the following code elements are pure:</span></span>  
  
-   <span data-ttu-id="789ed-214">Metody, które są oznaczone ikoną z <xref:System.Diagnostics.Contracts.PureAttribute>.</span><span class="sxs-lookup"><span data-stu-id="789ed-214">Methods that are marked with the <xref:System.Diagnostics.Contracts.PureAttribute>.</span></span>  
  
-   <span data-ttu-id="789ed-215">Typy, które są oznaczone ikoną z <xref:System.Diagnostics.Contracts.PureAttribute> (atrybut dotyczy wszystkich typów metod).</span><span class="sxs-lookup"><span data-stu-id="789ed-215">Types that are marked with the <xref:System.Diagnostics.Contracts.PureAttribute> (the attribute applies to all the type's methods).</span></span>  
  
-   <span data-ttu-id="789ed-216">Metody dostępu get właściwości.</span><span class="sxs-lookup"><span data-stu-id="789ed-216">Property get accessors.</span></span>  
  
-   <span data-ttu-id="789ed-217">Operatory (metod statycznych, których nazwy rozpoczynają się od "op" oraz że mają jeden lub dwa parametry oraz zwracany typ inny niż void).</span><span class="sxs-lookup"><span data-stu-id="789ed-217">Operators (static methods whose names start with "op", and that have one or two parameters and a non-void return type).</span></span>  
  
-   <span data-ttu-id="789ed-218">Wszystkie metody, której w pełni kwalifikowana nazwa zaczyna się od "System.Diagnostics.Contracts.Contract", "System.String", "System.IO.Path" lub "System.Type".</span><span class="sxs-lookup"><span data-stu-id="789ed-218">Any method whose fully qualified name begins with "System.Diagnostics.Contracts.Contract", "System.String", "System.IO.Path", or "System.Type".</span></span>  
  
-   <span data-ttu-id="789ed-219">Wszelkie wywoływane delegata, pod warunkiem, że ma atrybut samego typu delegowanego <xref:System.Diagnostics.Contracts.PureAttribute>.</span><span class="sxs-lookup"><span data-stu-id="789ed-219">Any invoked delegate, provided that the delegate type itself is attributed with the <xref:System.Diagnostics.Contracts.PureAttribute>.</span></span> <span data-ttu-id="789ed-220">Typy delegatów <xref:System.Predicate%601?displayProperty=nameWithType> i <xref:System.Comparison%601?displayProperty=nameWithType> są traktowane jako czysty.</span><span class="sxs-lookup"><span data-stu-id="789ed-220">The delegate types <xref:System.Predicate%601?displayProperty=nameWithType> and <xref:System.Comparison%601?displayProperty=nameWithType> are considered pure.</span></span>  
  
<a name="visibility"></a>   
### <a name="visibility"></a><span data-ttu-id="789ed-221">Widoczność</span><span class="sxs-lookup"><span data-stu-id="789ed-221">Visibility</span></span>  
 <span data-ttu-id="789ed-222">Wszystkie elementy członkowskie wspomnianego kontraktu musi być co najmniej jako widoczny jako metody, w jakiej widnieją.</span><span class="sxs-lookup"><span data-stu-id="789ed-222">All members mentioned in a contract must be at least as visible as the method in which they appear.</span></span> <span data-ttu-id="789ed-223">Na przykład pole prywatne nie może być wspomnianego warunkiem wstępnym publiczną metodę; Klienci nie można sprawdzić poprawności taką umowę przed ich wywołania metody.</span><span class="sxs-lookup"><span data-stu-id="789ed-223">For example, a private field cannot be mentioned in a precondition for a public method; clients cannot validate such a contract before they call the method.</span></span> <span data-ttu-id="789ed-224">Jednak jeśli pole jest oznaczony atrybutem <xref:System.Diagnostics.Contracts.ContractPublicPropertyNameAttribute>, jest wykluczone z tych reguł.</span><span class="sxs-lookup"><span data-stu-id="789ed-224">However, if the field is marked with the <xref:System.Diagnostics.Contracts.ContractPublicPropertyNameAttribute>, it is exempt from these rules.</span></span>  
  
## <a name="example"></a><span data-ttu-id="789ed-225">Przykład</span><span class="sxs-lookup"><span data-stu-id="789ed-225">Example</span></span>  
 <span data-ttu-id="789ed-226">Poniższy przykład przedstawia użycie kontraktów kodu.</span><span class="sxs-lookup"><span data-stu-id="789ed-226">The following example shows the use of code contracts.</span></span>  
  
 [!code-csharp[ContractExample#1](../../../samples/snippets/csharp/VS_Snippets_CLR/contractexample/cs/program.cs#1)]
 [!code-vb[ContractExample#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/contractexample/vb/program.vb#1)]
