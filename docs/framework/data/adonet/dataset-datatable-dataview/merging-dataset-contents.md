---
title: Scalanie zawartości elementu DataSet
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: e5e9309a-3ebb-4a9c-9d78-21c4e2bafc5b
ms.openlocfilehash: abc9183666602a7ef369e690e3ae499f8c7b8b11
ms.sourcegitcommit: d2e1dfa7ef2d4e9ffae3d431cf6a4ffd9c8d378f
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/07/2019
ms.locfileid: "70784396"
---
# <a name="merging-dataset-contents"></a><span data-ttu-id="6fcd2-102">Scalanie zawartości elementu DataSet</span><span class="sxs-lookup"><span data-stu-id="6fcd2-102">Merging DataSet Contents</span></span>

<span data-ttu-id="6fcd2-103"><xref:System.Data.DataSet.Merge%2A> Możesz użyć `DataSet`metody <xref:System.Data.DataSet>, aby scalić zawartość tablicy, <xref:System.Data.DataTable>lub <xref:System.Data.DataRow> do istniejącej.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-103">You can use the <xref:System.Data.DataSet.Merge%2A> method to merge the contents of a <xref:System.Data.DataSet>, <xref:System.Data.DataTable>, or <xref:System.Data.DataRow> array into an existing `DataSet`.</span></span> <span data-ttu-id="6fcd2-104">Kilka czynników i opcji mają wpływ na sposób scalania nowych danych w istniejący `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-104">Several factors and options affect how new data is merged into an existing `DataSet`.</span></span>

## <a name="primary-keys"></a><span data-ttu-id="6fcd2-105">Klucze podstawowe</span><span class="sxs-lookup"><span data-stu-id="6fcd2-105">Primary Keys</span></span>

<span data-ttu-id="6fcd2-106">Jeśli tabela otrzymująca nowe dane i schemat z scalania ma klucz podstawowy, nowe wiersze z danych przychodzących są dopasowywane do istniejących wierszy, które mają te same <xref:System.Data.DataRowVersion.Original> wartości klucza podstawowego co te w danych przychodzących.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-106">If the table receiving new data and schema from a merge has a primary key, new rows from the incoming data are matched with existing rows that have the same <xref:System.Data.DataRowVersion.Original> primary key values as those in the incoming data.</span></span> <span data-ttu-id="6fcd2-107">Jeśli kolumny ze schematu przychodzącego są zgodne z istniejącymi schematami, dane w istniejących wierszach są modyfikowane.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-107">If the columns from the incoming schema match those of the existing schema, the data in the existing rows is modified.</span></span> <span data-ttu-id="6fcd2-108">Kolumny, które nie pasują do istniejącego schematu, są ignorowane lub dodawane na podstawie <xref:System.Data.Common.DataAdapter.MissingSchemaAction%2A> parametru.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-108">Columns that do not match the existing schema are either ignored or added based on the <xref:System.Data.Common.DataAdapter.MissingSchemaAction%2A> parameter.</span></span> <span data-ttu-id="6fcd2-109">Nowe wiersze z wartościami klucza podstawowego, które nie są zgodne z żadnymi istniejącymi wierszami, są dołączane do istniejącej tabeli.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-109">New rows with primary key values that do not match any existing rows are appended to the existing table.</span></span>

<span data-ttu-id="6fcd2-110">Jeśli przychodzące lub istniejące <xref:System.Data.DataRowState.Added>wiersze mają stan wiersza, ich wartości klucza podstawowego są dopasowywane <xref:System.Data.DataRowVersion.Current> przy użyciu wartości klucza `Added` podstawowego wiersza, ponieważ nie `Original` istnieje wersja wiersza.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-110">If incoming or existing rows have a row state of <xref:System.Data.DataRowState.Added>, their primary key values are matched using the <xref:System.Data.DataRowVersion.Current> primary key value of the `Added` row because no `Original` row version exists.</span></span>

<span data-ttu-id="6fcd2-111">Jeśli tabela przychodząca i istniejąca tabela zawierają kolumnę o tej samej nazwie, ale różne typy danych, zgłaszany jest wyjątek, <xref:System.Data.DataSet.MergeFailed> a zdarzenie `DataSet` jest wywoływane.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-111">If an incoming table and an existing table contain a column with the same name but different data types, an exception is thrown and the <xref:System.Data.DataSet.MergeFailed> event of the `DataSet` is raised.</span></span> <span data-ttu-id="6fcd2-112">Jeśli tabela przychodząca i istniejąca tabela mają zdefiniowane klucze, ale klucze podstawowe są dla różnych kolumn, zgłaszany jest wyjątek, a `MergeFailed` zdarzenie `DataSet` jest wywoływane.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-112">If an incoming table and an existing table both have defined keys, but the primary keys are for different columns, an exception is thrown and the `MergeFailed` event of the `DataSet` is raised.</span></span>

<span data-ttu-id="6fcd2-113">Jeśli tabela otrzymująca nowe dane z scalania nie ma klucza podstawowego, nie można dopasować nowych wierszy z danych przychodzących do istniejących wierszy w tabeli i są one dołączane do istniejącej tabeli.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-113">If the table receiving new data from a merge does not have a primary key, new rows from the incoming data cannot be matched to existing rows in the table and are instead appended to the existing table.</span></span>

## <a name="table-names-and-namespaces"></a><span data-ttu-id="6fcd2-114">Nazwy tabel i przestrzenie nazw</span><span class="sxs-lookup"><span data-stu-id="6fcd2-114">Table Names and Namespaces</span></span>

<span data-ttu-id="6fcd2-115"><xref:System.Data.DataTable>do <xref:System.Data.DataTable.Namespace%2A> obiektów można opcjonalnie przypisać wartość właściwości.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-115"><xref:System.Data.DataTable> objects can optionally be assigned a <xref:System.Data.DataTable.Namespace%2A> property value.</span></span> <span data-ttu-id="6fcd2-116">Gdy <xref:System.Data.DataTable.Namespace%2A> wartości są przypisane <xref:System.Data.DataSet> , może zawierać wiele <xref:System.Data.DataTable> obiektów o tej samej <xref:System.Data.DataTable.TableName%2A> wartości.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-116">When <xref:System.Data.DataTable.Namespace%2A> values are assigned, a <xref:System.Data.DataSet> can contain multiple <xref:System.Data.DataTable> objects with the same <xref:System.Data.DataTable.TableName%2A> value.</span></span> <span data-ttu-id="6fcd2-117">Podczas operacji scalania zarówno <xref:System.Data.DataTable.TableName%2A> i <xref:System.Data.DataTable.Namespace%2A> są używane do identyfikowania celu scalania.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-117">During merge operations, both <xref:System.Data.DataTable.TableName%2A> and <xref:System.Data.DataTable.Namespace%2A> are used to identify the target of a merge.</span></span> <span data-ttu-id="6fcd2-118">Jeśli nie <xref:System.Data.DataTable.Namespace%2A> została przypisana, <xref:System.Data.DataTable.TableName%2A> tylko służy do identyfikowania celu scalania.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-118">If no <xref:System.Data.DataTable.Namespace%2A> has been assigned, only the <xref:System.Data.DataTable.TableName%2A> is used to identify the target of a merge.</span></span>

> [!NOTE]
> <span data-ttu-id="6fcd2-119">To zachowanie zostało zmienione w wersji 2,0 .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-119">This behavior changed in version 2.0 of the .NET Framework.</span></span> <span data-ttu-id="6fcd2-120">W wersji 1,1, przestrzenie nazw były obsługiwane, ale zostały zignorowane podczas operacji scalania.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-120">In version 1.1, namespaces were supported but were ignored during merge operations.</span></span> <span data-ttu-id="6fcd2-121">Z tego powodu, <xref:System.Data.DataSet> który używa <xref:System.Data.DataTable.Namespace%2A> wartości właściwości, będzie mieć różne zachowania, w zależności od używanej wersji .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-121">For this reason, a <xref:System.Data.DataSet> that uses <xref:System.Data.DataTable.Namespace%2A> property values will have different behaviors depending on which version of the .NET Framework you are running.</span></span> <span data-ttu-id="6fcd2-122">Załóżmy na przykład, że masz `DataSets` dwie zawierające `DataTables` te same <xref:System.Data.DataTable.TableName%2A> wartości właściwości, ale różne <xref:System.Data.DataTable.Namespace%2A> wartości właściwości.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-122">For example, suppose you have two `DataSets` containing `DataTables` with the same <xref:System.Data.DataTable.TableName%2A> property values but different <xref:System.Data.DataTable.Namespace%2A> property values.</span></span> <span data-ttu-id="6fcd2-123">W wersji 1,1 .NET Framework różne <xref:System.Data.DataTable.Namespace%2A> nazwy zostaną zignorowane podczas scalania dwóch <xref:System.Data.DataSet> obiektów.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-123">In version 1.1 of the .NET Framework, the different <xref:System.Data.DataTable.Namespace%2A> names will be ignored when merging the two <xref:System.Data.DataSet> objects.</span></span> <span data-ttu-id="6fcd2-124">Jednak począwszy od wersji 2,0, scalenia powoduje utworzenie dwóch `DataTables` nowych obiektów w celu. <xref:System.Data.DataSet></span><span class="sxs-lookup"><span data-stu-id="6fcd2-124">However, starting with version 2.0, merging causes two new `DataTables` to be created in the target <xref:System.Data.DataSet>.</span></span> <span data-ttu-id="6fcd2-125">Scalanie `DataTables` nie wpłynie na oryginalne.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-125">The original `DataTables` will be unaffected by the merge.</span></span>

## <a name="preservechanges"></a><span data-ttu-id="6fcd2-126">PreserveChanges</span><span class="sxs-lookup"><span data-stu-id="6fcd2-126">PreserveChanges</span></span>

<span data-ttu-id="6fcd2-127">`DataSet`Gdy przekazujesz tablicę `DataTable`, `DataRow` `DataSet`, lub do metody, możesz dołączyć parametry opcjonalne, które określają, czy zachować zmiany w istniejącym i jak obsługiwać nowe elementy schematu `Merge` w danych przychodzących.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-127">When you pass a `DataSet`, `DataTable`, or `DataRow` array to the `Merge` method, you can include optional parameters that specify whether or not to preserve changes in the existing `DataSet`, and how to handle new schema elements found in the incoming data.</span></span> <span data-ttu-id="6fcd2-128">Pierwszy z tych parametrów po danych przychodzących jest flagą logiczną, <xref:System.Data.LoadOption.PreserveChanges>która określa, czy zachować zmiany w istniejącym. `DataSet`</span><span class="sxs-lookup"><span data-stu-id="6fcd2-128">The first of these parameters after the incoming data is a Boolean flag, <xref:System.Data.LoadOption.PreserveChanges>, which specifies whether or not to preserve the changes in the existing `DataSet`.</span></span> <span data-ttu-id="6fcd2-129">Jeśli flaga jest ustawiona na `true`, wartości przychodzące nie `Current` zastępują istniejących wartości w wierszu wersji istniejącego wiersza. `PreserveChanges`</span><span class="sxs-lookup"><span data-stu-id="6fcd2-129">If the `PreserveChanges` flag is set to `true`, incoming values do not overwrite existing values in the `Current` row version of the existing row.</span></span> <span data-ttu-id="6fcd2-130">Jeśli flaga jest ustawiona na `false`, wartości przychodzące zastępuje istniejące wartości w `Current` wierszu wersji istniejącego wiersza. `PreserveChanges`</span><span class="sxs-lookup"><span data-stu-id="6fcd2-130">If the `PreserveChanges` flag is set to `false`, incoming values do overwrite the existing values in the `Current` row version of the existing row.</span></span> <span data-ttu-id="6fcd2-131">Jeśli flaga nie jest określona, jest domyślnie ustawiona na `false` wartość. `PreserveChanges`</span><span class="sxs-lookup"><span data-stu-id="6fcd2-131">If the `PreserveChanges` flag is not specified, it is set to `false` by default.</span></span> <span data-ttu-id="6fcd2-132">Aby uzyskać więcej informacji o wersjach wierszy, zobacz [Stany wiersza i wersje wierszy](row-states-and-row-versions.md).</span><span class="sxs-lookup"><span data-stu-id="6fcd2-132">For more information about row versions, see [Row States and Row Versions](row-states-and-row-versions.md).</span></span>

<span data-ttu-id="6fcd2-133">Gdy `PreserveChanges` tak `true`jest, dane z istniejącego <xref:System.Data.DataRowVersion.Current> wiersza są zachowywane w wersji wiersza <xref:System.Data.DataRowVersion.Original> istniejącego wiersza, podczas gdy dane z wiersza wersji istniejącego wiersza są zastępowane danymi z `Original` wiersza wersja wiersza przychodzącego.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-133">When `PreserveChanges` is `true`, the data from the existing row is maintained in the <xref:System.Data.DataRowVersion.Current> row version of the existing row, while the data from the <xref:System.Data.DataRowVersion.Original> row version of the existing row is overwritten with the data from the `Original` row version of the incoming row.</span></span> <span data-ttu-id="6fcd2-134">Istniejący wiersz jest ustawiony na <xref:System.Data.DataRowState.Modified>. <xref:System.Data.DataRow.RowState%2A></span><span class="sxs-lookup"><span data-stu-id="6fcd2-134">The <xref:System.Data.DataRow.RowState%2A> of the existing row is set to <xref:System.Data.DataRowState.Modified>.</span></span> <span data-ttu-id="6fcd2-135">Obowiązują następujące wyjątki:</span><span class="sxs-lookup"><span data-stu-id="6fcd2-135">The following exceptions apply:</span></span>

- <span data-ttu-id="6fcd2-136">Jeśli istniejący wiersz `RowState` ma wartość `Deleted`, `RowState` pozostaje `Deleted` i nie jest ustawiony na `Modified`.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-136">If the existing row has a `RowState` of `Deleted`, this `RowState` remains `Deleted` and is not set to `Modified`.</span></span> <span data-ttu-id="6fcd2-137">W takim przypadku dane `Original` z przychodzącego wiersza będą nadal przechowywane w wersji wiersza istniejącego wiersza, `Original` zastępując wersję wiersza istniejącego wiersza (chyba że `Added`przychodzący wiersz ma wartość `RowState` ).</span><span class="sxs-lookup"><span data-stu-id="6fcd2-137">In this case, the data from the incoming row will still be stored in the `Original` row version of the existing row, overwriting the `Original` row version of the existing row (unless the incoming row has a `RowState` of `Added`).</span></span>

- <span data-ttu-id="6fcd2-138">Jeśli przychodzący wiersz zawiera `RowState` z `Added` `Original` , dane z wersji wiersza istniejącego wiersza nie zostaną zastąpione danymi z wiersza przychodzącego, `Original` ponieważ w wierszu przychodzącym nie ma wersji wiersza.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-138">If the incoming row has a `RowState` of `Added`, the data from the `Original` row version of the existing row will not be overwritten with data from the incoming row, because the incoming row does not have an `Original` row version.</span></span>

<span data-ttu-id="6fcd2-139">Gdy `PreserveChanges` ma `Original` `Current` `RowState` `RowState` wartość, zarówno wersje wierszy, jak i w istniejącym wierszu są zastępowane danymi z wiersza przychodzącego, a istniejący wiersz jest ustawiony na wiersz przychodzący. `false`</span><span class="sxs-lookup"><span data-stu-id="6fcd2-139">When `PreserveChanges` is `false`, both the `Current` and `Original` row versions in the existing row are overwritten with the data from the incoming row, and the `RowState` of the existing row is set to the `RowState` of the incoming row.</span></span> <span data-ttu-id="6fcd2-140">Obowiązują następujące wyjątki:</span><span class="sxs-lookup"><span data-stu-id="6fcd2-140">The following exceptions apply:</span></span>

- <span data-ttu-id="6fcd2-141">Jeśli przychodzący wiersz ma `RowState` `Unchanged` a i `Modified`istniejący wiersz ma `RowState` , `Deleted`, lub `Added`, w `RowState` istniejącym wierszu ma ustawioną wartość `Modified`.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-141">If the incoming row has a `RowState` of `Unchanged` and the existing row has a `RowState` of `Modified`, `Deleted`, or `Added`, the `RowState` of the existing row is set to `Modified`.</span></span>

- <span data-ttu-id="6fcd2-142">Jeśli wiersz przychodzący ma `RowState` wartość `Added`, a `Unchanged`istniejący wiersz ma `RowState` , `Modified`, lub `Deleted`, w `RowState` istniejącym wierszu jest ustawiony na `Modified`.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-142">If the incoming row has a `RowState` of `Added`, and the existing row has a `RowState` of `Unchanged`, `Modified`, or `Deleted`, the `RowState` of the existing row is set to `Modified`.</span></span> <span data-ttu-id="6fcd2-143">Ponadto dane z `Original` wersji wiersza istniejącego wiersza nie są zastępowane danymi z wiersza przychodzącego, ponieważ w wierszu przychodzącym nie `Original` ma wersji wiersza.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-143">Also, the data from the `Original` row version of the existing row is not overwritten with data from the incoming row, because the incoming row does not have an `Original` row version.</span></span>

## <a name="missingschemaaction"></a><span data-ttu-id="6fcd2-144">MissingSchemaAction</span><span class="sxs-lookup"><span data-stu-id="6fcd2-144">MissingSchemaAction</span></span>

<span data-ttu-id="6fcd2-145">Można użyć opcjonalnego <xref:System.Data.MissingSchemaAction> parametru metody, `Merge` aby określić, jak `Merge` program będzie obsługiwać elementy schematu w danych przychodzących, które nie są częścią istniejącej `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-145">You can use the optional <xref:System.Data.MissingSchemaAction> parameter of the `Merge` method to specify how `Merge` will handle schema elements in the incoming data that are not part of the existing `DataSet`.</span></span>

<span data-ttu-id="6fcd2-146">W poniższej tabeli opisano opcje dla programu `MissingSchemaAction`.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-146">The following table describes the options for `MissingSchemaAction`.</span></span>

|<span data-ttu-id="6fcd2-147">MissingSchemaAction — opcja</span><span class="sxs-lookup"><span data-stu-id="6fcd2-147">MissingSchemaAction option</span></span>|<span data-ttu-id="6fcd2-148">Opis</span><span class="sxs-lookup"><span data-stu-id="6fcd2-148">Description</span></span>|
|--------------------------------|-----------------|
|<xref:System.Data.MissingSchemaAction.Add>|<span data-ttu-id="6fcd2-149">Dodaj nowe informacje o schemacie do `DataSet` i Wypełnij nowe kolumny wartościami przychodzącymi.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-149">Add the new schema information to the `DataSet` and populate the new columns with the incoming values.</span></span> <span data-ttu-id="6fcd2-150">Domyślnie włączone.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-150">This is the default.</span></span>|
|<xref:System.Data.MissingSchemaAction.AddWithKey>|<span data-ttu-id="6fcd2-151">Dodaj nowy schemat i informacje o kluczu podstawowym do `DataSet` i Wypełnij nowe kolumny wartościami przychodzącymi.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-151">Add the new schema and primary key information to the `DataSet` and populate the new columns with the incoming values.</span></span>|
|<xref:System.Data.MissingSchemaAction.Error>|<span data-ttu-id="6fcd2-152">Zgłoś wyjątek, Jeśli napotkano niezgodne informacje o schemacie.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-152">Throw an exception if mismatched schema information is encountered.</span></span>|
|<xref:System.Data.MissingSchemaAction.Ignore>|<span data-ttu-id="6fcd2-153">Ignoruj nowe informacje o schemacie.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-153">Ignore the new schema information.</span></span>|

## <a name="constraints"></a><span data-ttu-id="6fcd2-154">Ograniczenia</span><span class="sxs-lookup"><span data-stu-id="6fcd2-154">Constraints</span></span>

<span data-ttu-id="6fcd2-155">Przy użyciu `DataSet`metody ograniczenia nie są sprawdzane, dopóki wszystkie nowe dane nie zostaną dodane do istniejącej. `Merge`</span><span class="sxs-lookup"><span data-stu-id="6fcd2-155">With the `Merge` method, constraints are not checked until all new data has been added to the existing `DataSet`.</span></span> <span data-ttu-id="6fcd2-156">Po dodaniu danych ograniczenia są wymuszane na bieżących wartościach w `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-156">Once the data has been added, constraints are enforced on the current values in the `DataSet`.</span></span> <span data-ttu-id="6fcd2-157">Musisz się upewnić, że kod obsługuje wszystkie wyjątki, które mogą być zgłaszane z powodu naruszeń ograniczenia.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-157">You must ensure that your code handles any exceptions that might be thrown due to constraint violations.</span></span>

<span data-ttu-id="6fcd2-158">Rozważ przypadek, w którym istniejący wiersz w a `DataSet` `Unchanged` jest wiersz z wartością klucza podstawowego 1.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-158">Consider a case where an existing row in a `DataSet` is an `Unchanged` row with a primary key value of 1.</span></span> <span data-ttu-id="6fcd2-159">Podczas `Modified` operacji scalania z wierszem przychodzącym `Original` z `Current` wartością klucza podstawowego 2 i wartością klucza podstawowego 1, istniejący wiersz i wiersz `Original` przychodzący nie są uważane za zgodne, ponieważ wartości klucza podstawowego różniącego.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-159">During a merge operation with a `Modified` incoming row with an `Original` primary key value of 2 and a `Current` primary key value of 1, the existing row and the incoming row are not considered matching because the `Original` primary key values differ.</span></span> <span data-ttu-id="6fcd2-160">Jednak po zakończeniu scalania i sprawdzeniu ograniczeń zostanie zgłoszony wyjątek, ponieważ `Current` wartości klucza podstawowego naruszają unikatowe ograniczenie dla kolumny klucza podstawowego.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-160">However, when the merge is completed and constraints are checked, an exception will be thrown because the `Current` primary key values violate the unique constraint for the primary key column.</span></span>

> [!NOTE]
> <span data-ttu-id="6fcd2-161">Gdy wiersze są wstawiane do tabeli bazy danych zawierającej kolumnę z autoprzyrostem, taką jak kolumna tożsamości, wartość kolumny tożsamości zwracana przez wstawianie może być niezgodna z wartością w `DataSet`, powodując dołączenie zwracanych wierszy zamiast scalonych.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-161">When rows are inserted into a database table containing an auto incrementing column such as an identity column, the identity column value returned by the insert may not match the value in the `DataSet`, causing the returned rows to be appended instead of merged.</span></span> <span data-ttu-id="6fcd2-162">Aby uzyskać więcej informacji, zobacz [pobieranie tożsamości lub wartości AutoNumber](../retrieving-identity-or-autonumber-values.md).</span><span class="sxs-lookup"><span data-stu-id="6fcd2-162">For more information, see [Retrieving Identity or Autonumber Values](../retrieving-identity-or-autonumber-values.md).</span></span>

<span data-ttu-id="6fcd2-163">Poniższy przykład kodu Scala dwa `DataSet` obiekty z różnymi schematami w jeden `DataSet` z połączonymi schematami dwóch obiektów przychodzących `DataSet` .</span><span class="sxs-lookup"><span data-stu-id="6fcd2-163">The following code example merges two `DataSet` objects with different schemas into one `DataSet` with the combined schemas of the two incoming `DataSet` objects.</span></span>

[!code-csharp[DataWorks DataSet.Merge#1](../../../../../samples/snippets/csharp/VS_Snippets_ADO.NET/DataWorks DataSet.Merge/CS/source.cs#1)]
[!code-vb[DataWorks DataSet.Merge#1](../../../../../samples/snippets/visualbasic/VS_Snippets_ADO.NET/DataWorks DataSet.Merge/VB/source.vb#1)]

<span data-ttu-id="6fcd2-164">Poniższy przykład kodu przyjmuje istniejące `DataSet` aktualizacje i przekazuje te aktualizacje do programu `DataAdapter` w celu ich przetworzenia w źródle danych.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-164">The following code example takes an existing `DataSet` with updates and passes those updates to a `DataAdapter` to be processed at the data source.</span></span> <span data-ttu-id="6fcd2-165">Następnie wyniki zostaną scalone z oryginałem `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-165">The results are then merged into the original `DataSet`.</span></span> <span data-ttu-id="6fcd2-166">Po odrzuceniu zmian, które spowodowały błąd, scalone zmiany są zatwierdzane za `AcceptChanges`pomocą.</span><span class="sxs-lookup"><span data-stu-id="6fcd2-166">After rejecting changes that resulted in an error, the merged changes are committed with `AcceptChanges`.</span></span>

[!code-csharp[DataWorks DataSet.MergeAcceptChanges#1](../../../../../samples/snippets/csharp/VS_Snippets_ADO.NET/DataWorks DataSet.MergeAcceptChanges/CS/source.cs#1)]
[!code-vb[DataWorks DataSet.MergeAcceptChanges#1](../../../../../samples/snippets/visualbasic/VS_Snippets_ADO.NET/DataWorks DataSet.MergeAcceptChanges/VB/source.vb#1)]

[!code-csharp[DataWorks DataSet.MergeAcceptChanges#2](../../../../../samples/snippets/csharp/VS_Snippets_ADO.NET/DataWorks DataSet.MergeAcceptChanges/CS/source.cs#2)]
[!code-vb[DataWorks DataSet.MergeAcceptChanges#2](../../../../../samples/snippets/visualbasic/VS_Snippets_ADO.NET/DataWorks DataSet.MergeAcceptChanges/VB/source.vb#2)]

## <a name="see-also"></a><span data-ttu-id="6fcd2-167">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="6fcd2-167">See also</span></span>

- [<span data-ttu-id="6fcd2-168">Elementy DataSet, DataTable i DataView</span><span class="sxs-lookup"><span data-stu-id="6fcd2-168">DataSets, DataTables, and DataViews</span></span>](index.md)
- [<span data-ttu-id="6fcd2-169">Stany wiersza i wersje wiersza</span><span class="sxs-lookup"><span data-stu-id="6fcd2-169">Row States and Row Versions</span></span>](row-states-and-row-versions.md)
- [<span data-ttu-id="6fcd2-170">Elementy DataAdapter i DataReaders</span><span class="sxs-lookup"><span data-stu-id="6fcd2-170">DataAdapters and DataReaders</span></span>](../dataadapters-and-datareaders.md)
- [<span data-ttu-id="6fcd2-171">Pobieranie i modyfikowanie danych ADO.NET</span><span class="sxs-lookup"><span data-stu-id="6fcd2-171">Retrieving and Modifying Data in ADO.NET</span></span>](../retrieving-and-modifying-data.md)
- [<span data-ttu-id="6fcd2-172">Pobieranie tożsamości lub wartości automatycznych numerów</span><span class="sxs-lookup"><span data-stu-id="6fcd2-172">Retrieving Identity or Autonumber Values</span></span>](../retrieving-identity-or-autonumber-values.md)
- [<span data-ttu-id="6fcd2-173">Omówienie ADO.NET</span><span class="sxs-lookup"><span data-stu-id="6fcd2-173">ADO.NET Overview</span></span>](../ado-net-overview.md)
