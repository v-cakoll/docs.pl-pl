---
title: Przekazywanie parametrów typu wartości — C# Przewodnik programowania
ms.date: 07/20/2015
helpviewer_keywords:
- method parameters [C#], value types
- parameters [C#], value
ms.assetid: 193ab86f-5f9b-4359-ac29-7cdf8afad3a6
ms.openlocfilehash: 670af18d4b2b356aa33a0a03a29c05f5ba9bf78f
ms.sourcegitcommit: de17a7a0a37042f0d4406f5ae5393531caeb25ba
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 01/24/2020
ms.locfileid: "76744495"
---
# <a name="passing-value-type-parameters-c-programming-guide"></a><span data-ttu-id="ee9cd-102">Przekazywanie parametrów typu wartość (Przewodnik programowania w języku C#)</span><span class="sxs-lookup"><span data-stu-id="ee9cd-102">Passing Value-Type Parameters (C# Programming Guide)</span></span>
<span data-ttu-id="ee9cd-103">Zmienna [typu wartości](../../language-reference/builtin-types/value-types.md) zawiera dane bezpośrednio w przeciwieństwie do zmiennej [typu odwołania](../../language-reference/keywords/reference-types.md) , która zawiera odwołanie do danych.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-103">A [value-type](../../language-reference/builtin-types/value-types.md) variable contains its data directly as opposed to a [reference-type](../../language-reference/keywords/reference-types.md) variable, which contains a reference to its data.</span></span> <span data-ttu-id="ee9cd-104">Przekazanie zmiennej typu wartości do metody przez wartość oznacza przekazanie kopii zmiennej do metody.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-104">Passing a value-type variable to a method by value means passing a copy of the variable to the method.</span></span> <span data-ttu-id="ee9cd-105">Wszelkie zmiany parametru, który ma miejsce wewnątrz metody, nie mają wpływu na oryginalne dane przechowywane w zmiennej argumentu.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-105">Any changes to the parameter that take place inside the method have no affect on the original data stored in the argument variable.</span></span> <span data-ttu-id="ee9cd-106">Jeśli metoda wywoływana ma zmienić wartość argumentu, należy przekazać ją przez odwołanie przy użyciu słowa kluczowego [ref](../../language-reference/keywords/ref.md) lub [out](../../language-reference/keywords/out-parameter-modifier.md) .</span><span class="sxs-lookup"><span data-stu-id="ee9cd-106">If you want the called method to change the value of the argument, you must pass it by reference, using the [ref](../../language-reference/keywords/ref.md) or [out](../../language-reference/keywords/out-parameter-modifier.md) keyword.</span></span> <span data-ttu-id="ee9cd-107">Możesz również użyć słowa kluczowego [in](../../language-reference/keywords/in-parameter-modifier.md) , aby przekazać parametr wartości przez odwołanie, aby uniknąć kopiowania, bez zagwarantowania, że wartość nie zostanie zmieniona.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-107">You may also use the [in](../../language-reference/keywords/in-parameter-modifier.md) keyword to pass a value parameter by reference to avoid the copy while guaranteeing that the value will not be changed.</span></span> <span data-ttu-id="ee9cd-108">Dla uproszczenia poniższe przykłady używają `ref`.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-108">For simplicity, the following examples use `ref`.</span></span>  
  
## <a name="passing-value-types-by-value"></a><span data-ttu-id="ee9cd-109">Przekazywanie typów wartości według wartości</span><span class="sxs-lookup"><span data-stu-id="ee9cd-109">Passing Value Types by Value</span></span>  
 <span data-ttu-id="ee9cd-110">Poniższy przykład ilustruje przekazywanie parametrów typu wartości według wartości.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-110">The following example demonstrates passing value-type parameters by value.</span></span> <span data-ttu-id="ee9cd-111">Zmienna `n` jest przenoszona przez wartość do `SquareIt`metody.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-111">The variable `n` is passed by value to the method `SquareIt`.</span></span> <span data-ttu-id="ee9cd-112">Wszelkie zmiany, które mają miejsce wewnątrz metody, nie mają wpływu na oryginalną wartość zmiennej.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-112">Any changes that take place inside the method have no affect on the original value of the variable.</span></span>  
  
 [!code-csharp[csProgGuideParameters#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#3)]  
  
 <span data-ttu-id="ee9cd-113">Zmienna `n` jest typem wartości.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-113">The variable `n` is a value type.</span></span> <span data-ttu-id="ee9cd-114">Zawiera ona dane, wartość `5`.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-114">It contains its data, the value `5`.</span></span> <span data-ttu-id="ee9cd-115">Gdy `SquareIt` jest wywoływana, zawartość `n` jest kopiowana do `x`parametru, który jest kwadratowy wewnątrz metody.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-115">When `SquareIt` is invoked, the contents of `n` are copied into the parameter `x`, which is squared inside the method.</span></span> <span data-ttu-id="ee9cd-116">Jednak w `Main`wartość `n` jest taka sama po wywołaniu metody `SquareIt`, tak jak wcześniej.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-116">In `Main`, however, the value of `n` is the same after calling the `SquareIt` method as it was before.</span></span> <span data-ttu-id="ee9cd-117">Zmiana, która ma miejsce wewnątrz metody, ma wpływ tylko na zmienną lokalną `x`.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-117">The change that takes place inside the method only affects the local variable `x`.</span></span>  
  
## <a name="passing-value-types-by-reference"></a><span data-ttu-id="ee9cd-118">Przekazywanie typów wartości według odwołania</span><span class="sxs-lookup"><span data-stu-id="ee9cd-118">Passing Value Types by Reference</span></span>  
 <span data-ttu-id="ee9cd-119">Poniższy przykład jest taki sam jak w poprzednim przykładzie, z tą różnicą, że argument jest przenoszona jako parametr `ref`.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-119">The following example is the same as the previous example, except that the argument is passed as a `ref` parameter.</span></span> <span data-ttu-id="ee9cd-120">Wartość argumentu podstawowego, `n`, jest zmieniana, gdy `x` zostanie zmieniona w metodzie.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-120">The value of the underlying argument, `n`, is changed when `x` is changed in the method.</span></span>  
  
 [!code-csharp[csProgGuideParameters#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#4)]  
  
 <span data-ttu-id="ee9cd-121">W tym przykładzie nie jest to wartość `n`, która jest przenoszona; Zamiast tego jest przesyłane odwołanie do `n`.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-121">In this example, it is not the value of `n` that is passed; rather, a reference to `n` is passed.</span></span> <span data-ttu-id="ee9cd-122">Parametr `x` nie [jest liczbą całkowitą](../../language-reference/builtin-types/integral-numeric-types.md); jest to odwołanie do `int`, w tym przypadku odwołania do `n`.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-122">The parameter `x` is not an [int](../../language-reference/builtin-types/integral-numeric-types.md); it is a reference to an `int`, in this case, a reference to `n`.</span></span> <span data-ttu-id="ee9cd-123">W związku z tym, gdy `x` jest kwadrat wewnątrz metody, to w rzeczywistości jest kwadratowa wartość, do której odnosi się `x`, `n`.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-123">Therefore, when `x` is squared inside the method, what actually is squared is what `x` refers to, `n`.</span></span>  
  
## <a name="swapping-value-types"></a><span data-ttu-id="ee9cd-124">Zamienianie typów wartości</span><span class="sxs-lookup"><span data-stu-id="ee9cd-124">Swapping Value Types</span></span>  
 <span data-ttu-id="ee9cd-125">Typowym przykładem zmiany wartości argumentów jest metoda wymiany, w której przekazywane są dwie zmienne do metody, a metoda zamienia ich zawartość.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-125">A common example of changing the values of arguments is a swap method, where you pass two variables to the method, and the method swaps their contents.</span></span> <span data-ttu-id="ee9cd-126">Należy przekazać argumenty do metody zamiany przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-126">You must pass the arguments to the swap method by reference.</span></span> <span data-ttu-id="ee9cd-127">W przeciwnym razie zastąpią lokalne kopie parametrów wewnątrz metody, a metoda wywołująca nie ma żadnych zmian.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-127">Otherwise, you swap local copies of the parameters inside the method, and no change occurs in the calling method.</span></span> <span data-ttu-id="ee9cd-128">Poniższy przykład zamienia wartości całkowite.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-128">The following example swaps integer values.</span></span>  
  
 [!code-csharp[csProgGuideParameters#5](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#5)]  
  
 <span data-ttu-id="ee9cd-129">Po wywołaniu metody `SwapByRef` należy użyć słowa kluczowego `ref` w wywołaniu, jak pokazano w poniższym przykładzie.</span><span class="sxs-lookup"><span data-stu-id="ee9cd-129">When you call the `SwapByRef` method, use the `ref` keyword in the call, as shown in the following example.</span></span>  
  
 [!code-csharp[csProgGuideParameters#6](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#6)]  
  
## <a name="see-also"></a><span data-ttu-id="ee9cd-130">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="ee9cd-130">See also</span></span>

- [<span data-ttu-id="ee9cd-131">Przewodnik programowania w języku C#</span><span class="sxs-lookup"><span data-stu-id="ee9cd-131">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="ee9cd-132">Przekazywanie parametrów</span><span class="sxs-lookup"><span data-stu-id="ee9cd-132">Passing Parameters</span></span>](./passing-parameters.md)
- [<span data-ttu-id="ee9cd-133">Przekazywanie parametrów typu odwołanie</span><span class="sxs-lookup"><span data-stu-id="ee9cd-133">Passing Reference-Type Parameters</span></span>](./passing-reference-type-parameters.md)
