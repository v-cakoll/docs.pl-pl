---
title: Praca z macOS Catalina Notarization
description: Jak obsłużyć problemy z notarization i certyfikatami w programie macOS po zainstalowaniu środowiska uruchomieniowego platformy .NET Core, zestawu SDK i aplikacji utworzonych za pomocą platformy .NET Core.
author: thraka
ms.author: adegeo
ms.date: 02/14/2020
ms.openlocfilehash: b16ef4074f829246df0aedebf7ffe4df75faed51
ms.sourcegitcommit: 00aa62e2f469c2272a457b04e66b4cc3c97a800b
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 02/28/2020
ms.locfileid: "78165366"
---
# <a name="macos-catalina-notarization-and-the-impact-on-net-core-downloads-and-projects"></a><span data-ttu-id="af34d-103">macOS Catalina Notarization i wpływ na pobieranie i projekty platformy .NET Core</span><span class="sxs-lookup"><span data-stu-id="af34d-103">macOS Catalina Notarization and the impact on .NET Core downloads and projects</span></span>

<span data-ttu-id="af34d-104">Począwszy od programu macOS Catalina (wersja 10,15), całe oprogramowanie skompilowane po 1 czerwca 2019 i dystrybuowane z IDENTYFIKATORem dewelopera musi być notarialne.</span><span class="sxs-lookup"><span data-stu-id="af34d-104">Beginning with macOS Catalina (version 10.15), all software built after June 1, 2019, and distributed with Developer ID, must be notarized.</span></span> <span data-ttu-id="af34d-105">To wymaganie dotyczy środowiska uruchomieniowego .NET Core, zestaw .NET Core SDK i oprogramowania utworzonego za pomocą platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="af34d-105">This requirement applies to the .NET Core runtime, .NET Core SDK, and software created with .NET Core.</span></span> <span data-ttu-id="af34d-106">W tym artykule opisano typowe scenariusze, które mogą być używane przez platformę .NET Core i macOS notarization.</span><span class="sxs-lookup"><span data-stu-id="af34d-106">This article describes the common scenarios you may face with .NET Core and macOS notarization.</span></span>

## <a name="installing-net-core"></a><span data-ttu-id="af34d-107">Instalowanie programu .NET Core</span><span class="sxs-lookup"><span data-stu-id="af34d-107">Installing .NET Core</span></span>

<span data-ttu-id="af34d-108">Instalatory dla programu .NET Core (zarówno środowisko uruchomieniowe, jak i zestaw SDK) w wersji 3,1, 3,0 i 2,1 zostały zgłoszone od 18 lutego 2020.</span><span class="sxs-lookup"><span data-stu-id="af34d-108">The installers for .NET Core (both runtime and SDK) versions 3.1, 3.0, and 2.1, have been notarized since February 18, 2020.</span></span> <span data-ttu-id="af34d-109">Wcześniejsze wersje nie zostały ujawnione.</span><span class="sxs-lookup"><span data-stu-id="af34d-109">Prior released versions aren't notarized.</span></span> <span data-ttu-id="af34d-110">Możesz ręcznie zainstalować nieświadomą wersję programu .NET Core, pobierając najpierw Instalatora, a następnie używając `sudo installer` polecenia.</span><span class="sxs-lookup"><span data-stu-id="af34d-110">You can manually install a non-notarized version of .NET Core by first downloading the installer, and then using the `sudo installer` command.</span></span> <span data-ttu-id="af34d-111">Aby uzyskać więcej informacji, zobacz [Pobierz i ręcznie zainstaluj program macOS](sdk.md?pivots=os-macos#download-and-manually-install).</span><span class="sxs-lookup"><span data-stu-id="af34d-111">For more information, see [Download and manually install for macOS](sdk.md?pivots=os-macos#download-and-manually-install).</span></span>

<span data-ttu-id="af34d-112">Począwszy od następujących wersji, instalatorzy programu .NET Core są notariusz:</span><span class="sxs-lookup"><span data-stu-id="af34d-112">Beginning with the following versions, the .NET Core installers are notarized:</span></span>

- <span data-ttu-id="af34d-113">Środowisko uruchomieniowe platformy .NET Core</span><span class="sxs-lookup"><span data-stu-id="af34d-113">.NET Core Runtime</span></span>
  - <span data-ttu-id="af34d-114">2.1.16</span><span class="sxs-lookup"><span data-stu-id="af34d-114">2.1.16</span></span>
  - <span data-ttu-id="af34d-115">3.0.3</span><span class="sxs-lookup"><span data-stu-id="af34d-115">3.0.3</span></span>
  - <span data-ttu-id="af34d-116">3.1.2</span><span class="sxs-lookup"><span data-stu-id="af34d-116">3.1.2</span></span>

- <span data-ttu-id="af34d-117">Zestaw .NET Core SDK</span><span class="sxs-lookup"><span data-stu-id="af34d-117">.NET Core SDK</span></span>
  - <span data-ttu-id="af34d-118">2.1.512</span><span class="sxs-lookup"><span data-stu-id="af34d-118">2.1.512</span></span>
  - <span data-ttu-id="af34d-119">3.0.103</span><span class="sxs-lookup"><span data-stu-id="af34d-119">3.0.103</span></span>
  - <span data-ttu-id="af34d-120">3.1.102</span><span class="sxs-lookup"><span data-stu-id="af34d-120">3.1.102</span></span>

## <a name="apphost-is-disabled-by-default"></a><span data-ttu-id="af34d-121">appHost jest domyślnie wyłączona</span><span class="sxs-lookup"><span data-stu-id="af34d-121">appHost is disabled by default</span></span>

<span data-ttu-id="af34d-122">Domyślnie nienotarialne wersje zestaw .NET Core SDK 3,0 i powyżej tworzą natywny plik wykonywalny (znany jako **appHost**), gdy projekt kompiluje, publikuje lub jest uruchomiony.</span><span class="sxs-lookup"><span data-stu-id="af34d-122">By default, non-notarized versions of the .NET Core SDK 3.0 and above produce a native Mach-O executable (known as the **appHost**) when your project compiles, publishes, or is run.</span></span> <span data-ttu-id="af34d-123">Ten plik wykonywalny jest wygodnym sposobem uruchomienia aplikacji.</span><span class="sxs-lookup"><span data-stu-id="af34d-123">This executable is a convenient way to run your app.</span></span> <span data-ttu-id="af34d-124">W przeciwnym razie aplikacja musi być uruchomiona, uruchamiając `dotnet <filename.dll>`.</span><span class="sxs-lookup"><span data-stu-id="af34d-124">Otherwise, your app must be started by running `dotnet <filename.dll>`.</span></span> <span data-ttu-id="af34d-125">Po włączeniu appHost polecenie `dotnet run` jest wywoływane w kontekście appHost.</span><span class="sxs-lookup"><span data-stu-id="af34d-125">When the appHost is enabled, the `dotnet run` command is invoked in the context of the appHost.</span></span> <span data-ttu-id="af34d-126">Aby uzyskać więcej informacji, zobacz [kontekst appHost](#context-of-the-apphost).</span><span class="sxs-lookup"><span data-stu-id="af34d-126">For more information, see [Context of the appHost](#context-of-the-apphost).</span></span>

<span data-ttu-id="af34d-127">Począwszy od wersji zestaw .NET Core SDK 3,0 i nowszych, plik wykonywalny appHost nie jest generowany domyślnie.</span><span class="sxs-lookup"><span data-stu-id="af34d-127">Starting with the notarized versions of the .NET Core SDK 3.0 and above, the appHost executable isn't generated by default.</span></span> <span data-ttu-id="af34d-128">Generowanie appHost można włączyć przy użyciu ustawienia logicznego [`UseAppHost`](../project-sdk/msbuild-props.md#useapphost) w pliku projektu.</span><span class="sxs-lookup"><span data-stu-id="af34d-128">You can turn on appHost generation with the [`UseAppHost`](../project-sdk/msbuild-props.md#useapphost) boolean setting in the project file.</span></span> <span data-ttu-id="af34d-129">Można również przełączać appHost za pomocą parametru `-p:UseAppHost` w wierszu polecenia dla określonego polecenia `dotnet` uruchamianego:</span><span class="sxs-lookup"><span data-stu-id="af34d-129">You can also toggle the appHost with the `-p:UseAppHost` parameter on the command line for the specific `dotnet` command you run:</span></span>

- <span data-ttu-id="af34d-130">Plik projektu</span><span class="sxs-lookup"><span data-stu-id="af34d-130">Project file</span></span>

  ```xml
  <PropertyGroup>
    <UseAppHost>true</UseAppHost>
  </PropertyGroup>
  ```

- <span data-ttu-id="af34d-131">Parametr wiersza polecenia</span><span class="sxs-lookup"><span data-stu-id="af34d-131">Command-line parameter</span></span>

  ```dotnetcli
  dotnet run -p:UseAppHost=true
  ```

<span data-ttu-id="af34d-132">AppHost jest zawsze tworzona przy publikowaniu [własnej aplikacji.](../deploying/index.md#publish-self-contained)</span><span class="sxs-lookup"><span data-stu-id="af34d-132">An appHost is always created when you publish your app [self-contained](../deploying/index.md#publish-self-contained).</span></span>

<span data-ttu-id="af34d-133">Aby uzyskać więcej informacji na temat ustawienia `UseAppHost`, zobacz [Właściwości programu MSBuild dla Microsoft. NET. SDK](../project-sdk/msbuild-props.md#useapphost).</span><span class="sxs-lookup"><span data-stu-id="af34d-133">For more information about the `UseAppHost` setting, see [MSBuild properties for Microsoft.NET.Sdk](../project-sdk/msbuild-props.md#useapphost).</span></span>

### <a name="context-of-the-apphost"></a><span data-ttu-id="af34d-134">Kontekst appHost</span><span class="sxs-lookup"><span data-stu-id="af34d-134">Context of the appHost</span></span>

<span data-ttu-id="af34d-135">Gdy appHost jest włączona w projekcie i używasz polecenia `dotnet run`, aby uruchomić aplikację, aplikacja jest wywoływana w kontekście appHost, a nie domyślnym hostem (domyślnym hostem jest polecenie `dotnet`).</span><span class="sxs-lookup"><span data-stu-id="af34d-135">When the appHost is enabled in your project, and you use the `dotnet run` command to run your app, the app is invoked in the context of the appHost and not the default host (the default host is the `dotnet` command).</span></span> <span data-ttu-id="af34d-136">Jeśli appHost jest wyłączona w projekcie, polecenie `dotnet run` uruchamia aplikację w kontekście domyślnego hosta.</span><span class="sxs-lookup"><span data-stu-id="af34d-136">If the appHost is disabled in your project, the `dotnet run` command runs your app in the context of the default host.</span></span> <span data-ttu-id="af34d-137">Nawet jeśli appHost jest wyłączona, opublikowanie aplikacji jako samodzielnego spowoduje wygenerowanie pliku wykonywalnego appHost, a użytkownicy używają tego pliku wykonywalnego do uruchomienia aplikacji.</span><span class="sxs-lookup"><span data-stu-id="af34d-137">Even if the appHost is disabled, publishing your app as self-contained generates an appHost executable, and users use that executable to run your app.</span></span> <span data-ttu-id="af34d-138">Uruchomienie aplikacji przy użyciu `dotnet <filename.dll>` wywołuje aplikację przy użyciu domyślnego hosta, udostępnionego środowiska uruchomieniowego.</span><span class="sxs-lookup"><span data-stu-id="af34d-138">Running your app with `dotnet <filename.dll>` invokes the app with the default host, the shared runtime.</span></span>

<span data-ttu-id="af34d-139">Po wywołaniu aplikacji używającej appHost partycja certyfikatu, do której uzyskuje dostęp aplikacja, różni się od notarialnego hosta domyślnego.</span><span class="sxs-lookup"><span data-stu-id="af34d-139">When an app using the appHost is invoked, the certificate partition accessed by the app is different from the notarized default host.</span></span> <span data-ttu-id="af34d-140">Jeśli aplikacja musi uzyskać dostęp do certyfikatów zainstalowanych za pośrednictwem domyślnego hosta, użyj polecenia `dotnet run`, aby uruchomić aplikację z pliku projektu, lub użyj polecenia `dotnet <filename.dll>`, aby uruchomić aplikację bezpośrednio.</span><span class="sxs-lookup"><span data-stu-id="af34d-140">If your app must access the certificates installed through the default host, use the `dotnet run` command to run your app from its project file, or use the `dotnet <filename.dll>` command to start the app directly.</span></span>

<span data-ttu-id="af34d-141">Więcej informacji na temat tego scenariusza znajduje się w sekcji [ASP.NET Core i macOS oraz certyfikaty](#aspnet-core-and-macos-and-certificates) .</span><span class="sxs-lookup"><span data-stu-id="af34d-141">More information about this scenario is provided in the [ASP.NET Core and macOS and certificates](#aspnet-core-and-macos-and-certificates) section.</span></span>

## <a name="aspnet-core-and-macos-and-certificates"></a><span data-ttu-id="af34d-142">ASP.NET Core i macOS oraz certyfikaty</span><span class="sxs-lookup"><span data-stu-id="af34d-142">ASP.NET Core and macOS and certificates</span></span>

<span data-ttu-id="af34d-143">Platforma .NET Core zapewnia możliwość zarządzania certyfikatami w łańcuchu macOS z klasą <xref:System.Security.Cryptography.X509Certificates>.</span><span class="sxs-lookup"><span data-stu-id="af34d-143">.NET Core provides the ability to manage certificates in the macOS Keychain with the <xref:System.Security.Cryptography.X509Certificates> class.</span></span> <span data-ttu-id="af34d-144">Dostęp do pęku kluczy macOS używa tożsamości aplikacji jako klucza podstawowego podczas wybierania partycji do uwzględnienia.</span><span class="sxs-lookup"><span data-stu-id="af34d-144">Access to the macOS Keychain uses the applications identity as the primary key when deciding which partition to consider.</span></span> <span data-ttu-id="af34d-145">Na przykład niepodpisane aplikacje przechowują wpisy tajne w partycji bez znaku, ale podpisane aplikacje przechowują wpisy tajne w partycjach, które mają dostęp do nich.</span><span class="sxs-lookup"><span data-stu-id="af34d-145">For example, unsigned applications store secrets in the unsigned partition, but signed applications store their secrets in partitions only they can access.</span></span> <span data-ttu-id="af34d-146">Źródło wykonania, które wywołuje aplikację, decyduje o tym, której partycji użyć.</span><span class="sxs-lookup"><span data-stu-id="af34d-146">The source of execution that invokes your app decides which partition to use.</span></span>

<span data-ttu-id="af34d-147">Platforma .NET Core udostępnia trzy źródła wykonywania: [appHost](#apphost-is-disabled-by-default), host domyślny (polecenie `dotnet`) i hosta niestandardowego.</span><span class="sxs-lookup"><span data-stu-id="af34d-147">.NET Core provides three sources of execution: [appHost](#apphost-is-disabled-by-default), default host (the `dotnet` command), and a custom host.</span></span> <span data-ttu-id="af34d-148">Każdy model wykonywania może mieć różne tożsamości, podpisane lub niepodpisane i ma dostęp do różnych partycji w łańcuchu kluczy.</span><span class="sxs-lookup"><span data-stu-id="af34d-148">Each execution model may have different identities, either signed or unsigned, and has access to different partitions within the Keychain.</span></span> <span data-ttu-id="af34d-149">Certyfikaty zaimportowane przez jeden tryb mogą nie być dostępne z innego.</span><span class="sxs-lookup"><span data-stu-id="af34d-149">Certificates imported by one mode may not be accessible from another.</span></span> <span data-ttu-id="af34d-150">Na przykład, notarialne wersje platformy .NET Core mają domyślnie podpisanym hostem.</span><span class="sxs-lookup"><span data-stu-id="af34d-150">For example, the notarized versions of .NET Core have a default host that is signed.</span></span> <span data-ttu-id="af34d-151">Certyfikaty są importowane do bezpiecznej partycji na podstawie jej tożsamości.</span><span class="sxs-lookup"><span data-stu-id="af34d-151">Certificates are imported into a secure partition based on its identity.</span></span> <span data-ttu-id="af34d-152">Te certyfikaty nie są dostępne z wygenerowanego appHost, ponieważ appHost jest niepodpisany.</span><span class="sxs-lookup"><span data-stu-id="af34d-152">These certificates aren't accessible from a generated appHost, as the appHost is unsigned.</span></span>

<span data-ttu-id="af34d-153">Inny przykład domyślnie ASP.NET Core importuje domyślny certyfikat SSL za pośrednictwem domyślnego hosta.</span><span class="sxs-lookup"><span data-stu-id="af34d-153">Another example, by default, ASP.NET Core imports a default SSL certificate through the default host.</span></span> <span data-ttu-id="af34d-154">ASP.NET Core aplikacji korzystających z appHost nie będzie mieć dostępu do tego certyfikatu i wystąpi błąd podczas wykrywania przez program .NET Core certyfikatu jest niedostępny.</span><span class="sxs-lookup"><span data-stu-id="af34d-154">ASP.NET Core applications that use an appHost won't have access to this certificate and will receive an error when .NET Core detects the certificate isn't accessible.</span></span> <span data-ttu-id="af34d-155">Komunikat o błędzie zawiera instrukcje dotyczące sposobu rozwiązania tego problemu.</span><span class="sxs-lookup"><span data-stu-id="af34d-155">The error message provides instructions on how to fix this problem.</span></span>

<span data-ttu-id="af34d-156">Jeśli jest wymagane Udostępnianie certyfikatów, macOS udostępnia opcje konfiguracji za pomocą narzędzia `security`.</span><span class="sxs-lookup"><span data-stu-id="af34d-156">If certificate sharing is required, macOS provides configuration options with the `security` utility.</span></span>

<span data-ttu-id="af34d-157">Aby uzyskać więcej informacji na temat rozwiązywania problemów z certyfikatem ASP.NET Core, zobacz [Wymuszanie protokołu HTTPS w programie ASP.NET Core](/aspnet/core/security/enforcing-ssl?view=aspnetcore-3.1&tabs=visual-studio#troubleshoot-certificate-problems).</span><span class="sxs-lookup"><span data-stu-id="af34d-157">For more information on how to troubleshoot ASP.NET Core certificate issues, see [Enforce HTTPS in ASP.NET Core](/aspnet/core/security/enforcing-ssl?view=aspnetcore-3.1&tabs=visual-studio#troubleshoot-certificate-problems).</span></span>

## <a name="default-entitlements"></a><span data-ttu-id="af34d-158">Uprawnienia domyślne</span><span class="sxs-lookup"><span data-stu-id="af34d-158">Default entitlements</span></span>

<span data-ttu-id="af34d-159">Domyślny Host platformy .NET Core (polecenie `dotnet`) ma zestaw domyślnych uprawnień.</span><span class="sxs-lookup"><span data-stu-id="af34d-159">.NET Core’s default host (the `dotnet` command) has a set of default entitlements.</span></span> <span data-ttu-id="af34d-160">Te uprawnienia są wymagane do prawidłowego działania platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="af34d-160">These entitlements are required for proper operation of .NET Core.</span></span> <span data-ttu-id="af34d-161">Istnieje możliwość, że aplikacja może potrzebować dodatkowych uprawnień. w takim przypadku należy wygenerować i użyć [appHost](#apphost-is-disabled-by-default) , a następnie dodać wymagane uprawnienia lokalnie.</span><span class="sxs-lookup"><span data-stu-id="af34d-161">It's possible that your application may need additional entitlements, in which case you'll need to generate and use an [appHost](#apphost-is-disabled-by-default) and then add the necessary entitlements locally.</span></span>
 
<span data-ttu-id="af34d-162">Domyślny zestaw uprawnień dla platformy .NET Core:</span><span class="sxs-lookup"><span data-stu-id="af34d-162">Default set of entitlements for .NET Core:</span></span>

- `com.apple.security.cs.allow-jit`
- `com.apple.security.cs.allow-unsigned-executable-memory`
- `com.apple.security.cs.allow-dyld-environment-variables`
- `com.apple.security.cs.disable-library-validation`

## <a name="notarize-a-net-core-app"></a><span data-ttu-id="af34d-163">Notarize aplikacji .NET Core</span><span class="sxs-lookup"><span data-stu-id="af34d-163">Notarize a .NET Core app</span></span>

<span data-ttu-id="af34d-164">Jeśli aplikacja ma być uruchamiana w witrynie macOS Catalina (wersja 10,15) lub nowsza, należy notarize aplikację.</span><span class="sxs-lookup"><span data-stu-id="af34d-164">If you want your application to run on macOS Catalina (version 10.15) or higher, you'll want to notarize your app.</span></span> <span data-ttu-id="af34d-165">AppHost przesyłana z aplikacją dla notarization powinna być używana z co najmniej tymi samymi [domyślnymi uprawnieniami](#default-entitlements) dla platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="af34d-165">The appHost you submit with your application for notarization should be used with at least the same [default entitlements](#default-entitlements) for .NET Core.</span></span>

## <a name="next-steps"></a><span data-ttu-id="af34d-166">Następne kroki</span><span class="sxs-lookup"><span data-stu-id="af34d-166">Next steps</span></span>

- <span data-ttu-id="af34d-167">[Zależności i wymagania dotyczące platformy .NET Core](dependencies.md).</span><span class="sxs-lookup"><span data-stu-id="af34d-167">[.NET Core dependencies and requirements](dependencies.md).</span></span>
- <span data-ttu-id="af34d-168">[Zainstaluj zestaw .NET Core SDK](sdk.md).</span><span class="sxs-lookup"><span data-stu-id="af34d-168">[Install the .NET Core SDK](sdk.md).</span></span>
- [<span data-ttu-id="af34d-169">Instalowanie środowiska uruchomieniowego platformy .NET Core</span><span class="sxs-lookup"><span data-stu-id="af34d-169">Install the .NET Core Runtime</span></span>](runtime.md)
