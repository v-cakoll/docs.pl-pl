---
title: Migrowanie C++projektów/CLI do platformy .NET Core
description: Dowiedz się więcej C++na temat przenoszenia projektów/CLI do platformy .NET Core.
author: mjrousos
ms.date: 01/10/2020
ms.openlocfilehash: eb03f2a5ff42e8279fd3ebd6ee6fb6d955f6798d
ms.sourcegitcommit: c01c18755bb7b0f82c7232314ccf7955ea7834db
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 01/15/2020
ms.locfileid: "75964931"
---
# <a name="how-to-port-a-ccli-project-to-net-core"></a><span data-ttu-id="a7961-103">Jak przenieść projekt C++/CLI do platformy .NET Core</span><span class="sxs-lookup"><span data-stu-id="a7961-103">How to port a C++/CLI project to .NET Core</span></span>

<span data-ttu-id="a7961-104">Począwszy od platformy .NET Core 3,1 i programu Visual Studio 2019 w wersji 16,4, [ C++/CLI projekty](/cpp/dotnet/dotnet-programming-with-cpp-cli-visual-cpp) mogą kierować platformą .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a7961-104">Beginning with .NET Core 3.1 and Visual Studio 2019 version 16.4, [C++/CLI projects](/cpp/dotnet/dotnet-programming-with-cpp-cli-visual-cpp) can target .NET Core.</span></span> <span data-ttu-id="a7961-105">Ta obsługa umożliwia przenoszenie aplikacji klasycznych systemu Windows z C++warstwami międzyoperacyjnymi/CLI do platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a7961-105">This support makes it possible to port Windows desktop applications with C++/CLI interop layers to .NET Core.</span></span> <span data-ttu-id="a7961-106">W tym artykule opisano sposób przenoszenia C++projektów/CLI z .NET Framework do programu .net Core 3,1.</span><span class="sxs-lookup"><span data-stu-id="a7961-106">This article describes how to port C++/CLI projects from .NET Framework to .NET Core 3.1.</span></span>

## <a name="ccli-net-core-limitations"></a><span data-ttu-id="a7961-107">C++Ograniczenia dotyczące/CLI .NET Core</span><span class="sxs-lookup"><span data-stu-id="a7961-107">C++/CLI .NET Core limitations</span></span>

<span data-ttu-id="a7961-108">Istnieją pewne ważne ograniczenia dotyczące przenoszenia projektów C++/CLI do platformy .NET Core w porównaniu z innymi językami:</span><span class="sxs-lookup"><span data-stu-id="a7961-108">There are some important limitations to porting C++/CLI projects to .NET Core compared to other languages:</span></span>

* <span data-ttu-id="a7961-109">C++Obsługa/CLI dla platformy .NET Core jest tylko w systemie Windows.</span><span class="sxs-lookup"><span data-stu-id="a7961-109">C++/CLI support for .NET Core is Windows only.</span></span>
* <span data-ttu-id="a7961-110">C++Projekty/CLI nie mogą wskazywać .NET Standard, tylko .NET Core (lub .NET Framework).</span><span class="sxs-lookup"><span data-stu-id="a7961-110">C++/CLI projects can't target .NET Standard, only .NET Core (or .NET Framework).</span></span>
* <span data-ttu-id="a7961-111">C++Projekty/CLI nie obsługują nowego formatu pliku projektu w stylu zestawu SDK.</span><span class="sxs-lookup"><span data-stu-id="a7961-111">C++/CLI projects don't support the new SDK-style project file format.</span></span> <span data-ttu-id="a7961-112">Zamiast tego, nawet w przypadku określania programu C++.NET Core, projekty/CLI używają istniejącego formatu pliku VCXPROJ.</span><span class="sxs-lookup"><span data-stu-id="a7961-112">Instead, even when targeting .NET Core, C++/CLI projects use the existing vcxproj file format.</span></span>
* <span data-ttu-id="a7961-113">C++Projekty/CLI nie mogą wiele obiektów docelowych w wielu platformach .NET.</span><span class="sxs-lookup"><span data-stu-id="a7961-113">C++/CLI projects can't multitarget multiple .NET platforms.</span></span> <span data-ttu-id="a7961-114">Jeśli konieczne jest skompilowanie projektu C++/CLI dla obu .NET Framework i .NET Core, należy użyć oddzielnych plików projektu.</span><span class="sxs-lookup"><span data-stu-id="a7961-114">If you need to build a C++/CLI project for both .NET Framework and .NET Core, use separate project files.</span></span>
* <span data-ttu-id="a7961-115">Platforma .NET Core nie obsługuje kompilacji `-clr:pure` ani `-clr:safe`, tylko nowej opcji `-clr:netcore` (która jest równoważna z `-clr` dla .NET Framework).</span><span class="sxs-lookup"><span data-stu-id="a7961-115">.NET Core doesn't support `-clr:pure` or `-clr:safe` compilation, only the new `-clr:netcore` option (which is equivalent to `-clr` for .NET Framework).</span></span>

## <a name="port-a-ccli-project"></a><span data-ttu-id="a7961-116">Port projektu C++/CLI</span><span class="sxs-lookup"><span data-stu-id="a7961-116">Port a C++/CLI project</span></span>

<span data-ttu-id="a7961-117">Aby przenieść projekt C++/CLI do platformy .NET Core, wprowadź następujące zmiany w pliku VCXPROJ.</span><span class="sxs-lookup"><span data-stu-id="a7961-117">To port a C++/CLI project to .NET Core, make the following changes to the vcxproj file.</span></span> <span data-ttu-id="a7961-118">Te kroki migracji różnią się od czynności wymaganych dla innych typów projektów C++, ponieważ projekty/CLI nie używają plików projektu w stylu zestawu SDK.</span><span class="sxs-lookup"><span data-stu-id="a7961-118">These migration steps differ from the steps needed for other project types because C++/CLI projects don't use SDK-style project files.</span></span>

1. <span data-ttu-id="a7961-119">Zastąp właściwości `<CLRSupport>true</CLRSupport>` `<CLRSupport>NetCore</CLRSupport>`.</span><span class="sxs-lookup"><span data-stu-id="a7961-119">Replace `<CLRSupport>true</CLRSupport>` properties with `<CLRSupport>NetCore</CLRSupport>`.</span></span> <span data-ttu-id="a7961-120">Ta właściwość często występuje w grupach właściwości specyficznych dla konfiguracji, więc może być konieczne zamienienie jej w wielu miejscach.</span><span class="sxs-lookup"><span data-stu-id="a7961-120">This property is often in configuration-specific property groups, so you may need to replace it in multiple places.</span></span>
2. <span data-ttu-id="a7961-121">Zastąp właściwości `<TargetFrameworkVersion>` `<TargetFramework>netcoreapp3.1</TargetFramework>`.</span><span class="sxs-lookup"><span data-stu-id="a7961-121">Replace `<TargetFrameworkVersion>` properties with `<TargetFramework>netcoreapp3.1</TargetFramework>`.</span></span>
3. <span data-ttu-id="a7961-122">Usuń wszystkie odnośniki .NET Framework (takie jak `<Reference Include="System" />`).</span><span class="sxs-lookup"><span data-stu-id="a7961-122">Remove any .NET Framework references (like `<Reference Include="System" />`).</span></span> <span data-ttu-id="a7961-123">Zestawy zestaw .NET Core SDK są automatycznie przywoływane podczas korzystania z `<CLRSupport>NetCore</CLRSupport>`.</span><span class="sxs-lookup"><span data-stu-id="a7961-123">.NET Core SDK assemblies are automatically referenced when using `<CLRSupport>NetCore</CLRSupport>`.</span></span>
4. <span data-ttu-id="a7961-124">W razie potrzeby zaktualizuj użycie interfejsu API w plikach CPP, aby usunąć interfejsy API niedostępne dla platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a7961-124">Update API usage in cpp files, as necessary, to remove APIs unavailable to .NET Core.</span></span> <span data-ttu-id="a7961-125">Ponieważ C++projekty/CLI mają dość wąskie warstwy międzyoperacyjności, często nie trzeba zmieniać wielu zmian.</span><span class="sxs-lookup"><span data-stu-id="a7961-125">Because C++/CLI projects tend to be fairly thin interop layers, there are often not many changes needed.</span></span> <span data-ttu-id="a7961-126">[Analizator przenośności platformy .NET](../../standard/analyzers/portability-analyzer.md) może służyć do identyfikowania nieobsługiwanych interfejsów API C++platformy .NET używanych przez pliki binarne/CLI tak samo jak w przypadku czystych, zarządzanych plików binarnych.</span><span class="sxs-lookup"><span data-stu-id="a7961-126">The [.NET Portability Analyzer](../../standard/analyzers/portability-analyzer.md) can be used to identify unsupported .NET APIs used by C++/CLI binaries just as with purely managed binaries.</span></span> <span data-ttu-id="a7961-127">Wskazówki dotyczące określania przenośności kodu i aktualizowania projektów do pracy z interfejsami API programu .NET Core są dostępne w obszarze [wskazówki dotyczące przenoszenia biblioteki](./libraries.md#determine-portability).</span><span class="sxs-lookup"><span data-stu-id="a7961-127">Guidelines for determining code portability and updating projects to work with .NET Core APIs are available in the [library porting guidance](./libraries.md#determine-portability).</span></span>

### <a name="wpf-and-windows-forms-usage"></a><span data-ttu-id="a7961-128">Użycie WPF i Windows Forms</span><span class="sxs-lookup"><span data-stu-id="a7961-128">WPF and Windows Forms usage</span></span>

<span data-ttu-id="a7961-129">Projekty platformy C++.NET Core/CLI mogą używać interfejsów API Windows Forms i WPF.</span><span class="sxs-lookup"><span data-stu-id="a7961-129">.NET Core C++/CLI projects can use Windows Forms and WPF APIs.</span></span> <span data-ttu-id="a7961-130">Aby użyć tych interfejsów API pulpitu systemu Windows, należy dodać jawne odwołania do bibliotek interfejsu użytkownika.</span><span class="sxs-lookup"><span data-stu-id="a7961-130">To use these Windows desktop APIs, you need to add explicit framework references to the UI libraries.</span></span> <span data-ttu-id="a7961-131">Projekty w stylu zestawu SDK, które używają interfejsów API systemu Windows, są automatycznie odwołujące się do niezbędnych bibliotek struktury przy użyciu zestawu SDK `Microsoft.NET.Sdk.WindowsDesktop`.</span><span class="sxs-lookup"><span data-stu-id="a7961-131">SDK-style projects that use Windows desktop APIs reference the necessary framework libraries automatically by using the `Microsoft.NET.Sdk.WindowsDesktop` SDK.</span></span> <span data-ttu-id="a7961-132">Ponieważ C++projekty/CLI nie używają formatu projektu w stylu zestawu SDK, muszą dodać jawne odwołania do struktury w przypadku określania platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a7961-132">Because C++/CLI projects don't use the SDK-style project format, they need to add explicit framework references when targeting .NET Core.</span></span>

<span data-ttu-id="a7961-133">Aby użyć Windows Forms interfejsów API, należy dodać to odwołanie do pliku VCXPROJ:</span><span class="sxs-lookup"><span data-stu-id="a7961-133">To use Windows Forms APIs, add this reference to the vcxproj file:</span></span>

```xml
<!-- Reference all of Windows Forms -->
<FrameworkReference Include="Microsoft.WindowsDesktop.App.WindowsForms" />
```

<span data-ttu-id="a7961-134">Aby używać interfejsów API WPF, należy dodać to odwołanie do pliku VCXPROJ:</span><span class="sxs-lookup"><span data-stu-id="a7961-134">To use WPF APIs, add this reference to the vcxproj file:</span></span>

```xml
<!-- Reference all of WPF -->
<FrameworkReference Include="Microsoft.WindowsDesktop.App.WPF" />
```

<span data-ttu-id="a7961-135">Aby użyć zarówno Windows Forms, jak i interfejsów API WPF, należy dodać to odwołanie do pliku VCXPROJ:</span><span class="sxs-lookup"><span data-stu-id="a7961-135">To use both Windows Forms and WPF APIs, add this reference to the vcxproj file:</span></span>

```xml
<!-- Reference the entirety of the Windows desktop framework:
     Windows Forms, WPF, and the types that provide integration between them -->
<FrameworkReference Include="Microsoft.WindowsDesktop.App" />
```

<span data-ttu-id="a7961-136">Obecnie nie jest możliwe dodawanie tych odwołań za pomocą Menedżera odwołań programu Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="a7961-136">Currently, it's not possible to add these references using Visual Studio's reference manager.</span></span> <span data-ttu-id="a7961-137">Zamiast tego należy ręcznie zaktualizować plik projektu.</span><span class="sxs-lookup"><span data-stu-id="a7961-137">Instead, update the project file manually.</span></span> <span data-ttu-id="a7961-138">Tę aktualizację można wykonać w programie Visual Studio, zwalniając projekt, a następnie edytując plik projektu.</span><span class="sxs-lookup"><span data-stu-id="a7961-138">This update can be done in Visual Studio by unloading the project and then editing the project file.</span></span> <span data-ttu-id="a7961-139">Można również użyć innego edytora, takiego jak VS Code.</span><span class="sxs-lookup"><span data-stu-id="a7961-139">You can also use another editor like VS Code.</span></span>

## <a name="build-without-msbuild"></a><span data-ttu-id="a7961-140">Kompiluj bez programu MSBuild</span><span class="sxs-lookup"><span data-stu-id="a7961-140">Build without MSBuild</span></span>

<span data-ttu-id="a7961-141">Możliwe jest również tworzenie C++projektów/CLI bez użycia programu MSBuild.</span><span class="sxs-lookup"><span data-stu-id="a7961-141">It's also possible to build C++/CLI projects without using MSBuild.</span></span> <span data-ttu-id="a7961-142">Wykonaj następujące kroki, aby utworzyć C++projekt/CLI dla platformy .NET Core bezpośrednio przy użyciu *CL. exe* i *link. exe*:</span><span class="sxs-lookup"><span data-stu-id="a7961-142">Follow these steps to build a C++/CLI project for .NET Core directly with *cl.exe* and *link.exe*:</span></span>

1. <span data-ttu-id="a7961-143">Podczas kompilowania Przekaż `-clr:netcore` do *CL. exe*.</span><span class="sxs-lookup"><span data-stu-id="a7961-143">When compiling, pass `-clr:netcore` to *cl.exe*.</span></span>
2. <span data-ttu-id="a7961-144">Odwołuje się do niezbędnych zestawów referencyjnych platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a7961-144">Reference necessary .NET Core reference assemblies.</span></span>
3. <span data-ttu-id="a7961-145">Podczas łączenia należy udostępnić katalog hosta aplikacji .NET Core jako `LibPath` (aby można było znaleźć *ijwhost. lib* ).</span><span class="sxs-lookup"><span data-stu-id="a7961-145">When linking, provide the .NET Core app host directory as a `LibPath` (so that *ijwhost.lib* can be found).</span></span>
4. <span data-ttu-id="a7961-146">Skopiuj *ijwhost. dll* (z katalogu hosta aplikacji .NET Core) do katalogu wyjściowego projektu.</span><span class="sxs-lookup"><span data-stu-id="a7961-146">Copy *ijwhost.dll* (from the .NET Core app host directory) to the project's output directory.</span></span>
5. <span data-ttu-id="a7961-147">Upewnij się, że plik [runtimeconfig. JSON](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md) istnieje dla pierwszego składnika aplikacji, w którym będzie uruchamiany kod zarządzany.</span><span class="sxs-lookup"><span data-stu-id="a7961-147">Make sure a [runtimeconfig.json](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md) file exists for the first component of the application that will run managed code.</span></span> <span data-ttu-id="a7961-148">Jeśli aplikacja ma zarządzany punkt wejścia, plik `runtime.config` zostanie utworzony i skopiowany automatycznie.</span><span class="sxs-lookup"><span data-stu-id="a7961-148">If the application has a managed entry point, a `runtime.config` file will be created and copied automatically.</span></span> <span data-ttu-id="a7961-149">Jeśli aplikacja ma natywny punkt wejścia, należy utworzyć plik `runtimeconfig.json` dla pierwszej C++biblioteki/CLI, aby użyć środowiska uruchomieniowego platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a7961-149">If the application has a native entry point, though, you need to create a `runtimeconfig.json` file for the first C++/CLI library to use the .NET Core runtime.</span></span>

## <a name="known-issues"></a><span data-ttu-id="a7961-150">Znane problemy</span><span class="sxs-lookup"><span data-stu-id="a7961-150">Known issues</span></span>

<span data-ttu-id="a7961-151">Istnieje kilka znanych problemów, które należy wyszukać podczas pracy z C++projektami/CLI przeznaczonymi dla platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a7961-151">There are a couple known issues to look out for when working with C++/CLI projects targeting .NET Core.</span></span>

* <span data-ttu-id="a7961-152">Odwołanie do struktury WPF w projektach platformy C++.NET Core/CLI obecnie powoduje, że niektóre nadmiarowe ostrzeżenia o nie mogą zaimportować symboli.</span><span class="sxs-lookup"><span data-stu-id="a7961-152">A WPF framework reference in .NET Core C++/CLI projects currently causes some extraneous warnings about being unable to import symbols.</span></span> <span data-ttu-id="a7961-153">Te ostrzeżenia można bezpiecznie zignorować i powinny zostać wkrótce naprawione.</span><span class="sxs-lookup"><span data-stu-id="a7961-153">These warnings can be safely ignored and should be fixed soon.</span></span>
* <span data-ttu-id="a7961-154">Jeśli aplikacja ma natywny punkt wejścia, biblioteka C++/CLI, która najpierw wykonuje kod zarządzany, wymaga pliku [runtimeconfig. JSON](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md) .</span><span class="sxs-lookup"><span data-stu-id="a7961-154">If the application has a native entry point, the C++/CLI library that first executes managed code needs a [runtimeconfig.json](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md) file.</span></span> <span data-ttu-id="a7961-155">Ten plik konfiguracji jest używany podczas uruchamiania środowiska uruchomieniowego .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a7961-155">This config file is used when the .NET Core runtime starts.</span></span> <span data-ttu-id="a7961-156">C++Projekty/CLI nie tworzą jeszcze automatycznie plików `runtimeconfig.json` w czasie kompilacji, więc plik musi być wygenerowany ręcznie.</span><span class="sxs-lookup"><span data-stu-id="a7961-156">C++/CLI projects don't create `runtimeconfig.json` files automatically at build time yet, so the file must be generated manually.</span></span> <span data-ttu-id="a7961-157">Jeśli biblioteka C++/CLI jest wywoływana z zarządzanego punktu wejścia, biblioteka C++/CLI nie wymaga pliku `runtimeconfig.json` (ponieważ zestaw punktów wejścia będzie używany podczas uruchamiania środowiska uruchomieniowego).</span><span class="sxs-lookup"><span data-stu-id="a7961-157">If a C++/CLI library is called from a managed entry point, then the C++/CLI library doesn't need a `runtimeconfig.json` file (since the entry point assembly will have one that is used when starting the runtime).</span></span> <span data-ttu-id="a7961-158">Poniżej przedstawiono prosty przykładowy plik `runtimeconfig.json`.</span><span class="sxs-lookup"><span data-stu-id="a7961-158">A simple sample `runtimeconfig.json` file is shown below.</span></span> <span data-ttu-id="a7961-159">Aby uzyskać więcej informacji, zobacz [specyfikację w witrynie GitHub](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md).</span><span class="sxs-lookup"><span data-stu-id="a7961-159">For more information, see the [spec on GitHub](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md).</span></span>

    ```json
    {
          "runtimeOptions": {
             "tfm": "netcoreapp3.1",
             "framework": {
                "name": "Microsoft.NETCore.App",
                "version": "3.1.0"
             }
          }
    }
    ```
