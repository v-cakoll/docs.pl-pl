---
title: Ograniczenia dotyczące parametrów typu (Przewodnik programowania w języku C#)
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: df5a509296f3fb9e8e77a273a0636c74a6f86da3
ms.sourcegitcommit: 5bbfe34a9a14e4ccb22367e57b57585c208cf757
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/18/2018
ms.locfileid: "45999261"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="01ddc-102">Ograniczenia dotyczące parametrów typu (C# Programming Guide)</span><span class="sxs-lookup"><span data-stu-id="01ddc-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="01ddc-103">Ograniczenia informuje kompilator o możliwościach, którego argument typu musi mieć.</span><span class="sxs-lookup"><span data-stu-id="01ddc-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="01ddc-104">Bez żadnych ograniczeń argument typu może być dowolnego typu.</span><span class="sxs-lookup"><span data-stu-id="01ddc-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="01ddc-105">Kompilator, można założyć tylko członkowie <xref:System.Object?displayPropety=nameWithType>, czyli ultimate klasę bazową dla wszystkich typów .NET.</span><span class="sxs-lookup"><span data-stu-id="01ddc-105">The compiler can only assume the members of <xref:System.Object?displayPropety=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="01ddc-106">Aby uzyskać więcej informacji, zobacz [Dlaczego warto korzystać z ograniczeniami](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="01ddc-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="01ddc-107">Jeśli kod klienta próbuje utworzyć wystąpienie klasy przy użyciu typu, który nie jest dozwolona przez ograniczenie, wynikiem jest błąd w czasie kompilacji.</span><span class="sxs-lookup"><span data-stu-id="01ddc-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="01ddc-108">Ograniczenia są określane za pomocą `where` kontekstowego słowa kluczowego.</span><span class="sxs-lookup"><span data-stu-id="01ddc-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="01ddc-109">W poniższej tabeli wymieniono siedem typy ograniczeń:</span><span class="sxs-lookup"><span data-stu-id="01ddc-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="01ddc-110">Ograniczenia</span><span class="sxs-lookup"><span data-stu-id="01ddc-110">Constraint</span></span>|<span data-ttu-id="01ddc-111">Opis</span><span class="sxs-lookup"><span data-stu-id="01ddc-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="01ddc-112">Argument typu musi być typem wartości.</span><span class="sxs-lookup"><span data-stu-id="01ddc-112">The type argument must be a value type.</span></span> <span data-ttu-id="01ddc-113">Wszystkie wartości typu z wyjątkiem <xref:System.Nullable%601> można określić.</span><span class="sxs-lookup"><span data-stu-id="01ddc-113">Any value type except <xref:System.Nullable%601> can be specified.</span></span> <span data-ttu-id="01ddc-114">Aby uzyskać więcej informacji na temat typów dopuszczających wartości zerowe zobacz [typów dopuszczających wartości zerowe](../nullable-types/index.md).</span><span class="sxs-lookup"><span data-stu-id="01ddc-114">For more information about nullable types, see [Nullable types](../nullable-types/index.md).</span></span>|
|`where T : class`|<span data-ttu-id="01ddc-115">Argument typu musi być typem referencyjnym.</span><span class="sxs-lookup"><span data-stu-id="01ddc-115">The type argument must be a reference type.</span></span> <span data-ttu-id="01ddc-116">To ograniczenie ma zastosowanie także do klasy, interfejsu, delegata lub typu tablicowego.</span><span class="sxs-lookup"><span data-stu-id="01ddc-116">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="01ddc-117">Argument typu nie może być typem referencyjnym i nie może zawierać żadnych składowych typu odwołania na każdym poziomie zagnieżdżania.</span><span class="sxs-lookup"><span data-stu-id="01ddc-117">The type argument must not be a reference type and must not contain any reference type members at any level of nesting.</span></span>|
|`where T : new()`|<span data-ttu-id="01ddc-118">Typ argumentu musi mieć publicznego konstruktora bez parametrów.</span><span class="sxs-lookup"><span data-stu-id="01ddc-118">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="01ddc-119">Gdy jest używany z innymi ograniczeniami `new()` ograniczenie musi być określony jako ostatni.</span><span class="sxs-lookup"><span data-stu-id="01ddc-119">When used together with other constraints, the `new()` constraint must be specified last.</span></span>|
|<span data-ttu-id="01ddc-120">`where T :` *\<Klasa bazowa nazwa >*</span><span class="sxs-lookup"><span data-stu-id="01ddc-120">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="01ddc-121">Argument typu musi być lub pochodzić od określonej klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="01ddc-121">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="01ddc-122">`where T :` *\<Nazwa interfejsu >*</span><span class="sxs-lookup"><span data-stu-id="01ddc-122">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="01ddc-123">Argument typu muszą być lub implementować określonego interfejsu.</span><span class="sxs-lookup"><span data-stu-id="01ddc-123">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="01ddc-124">Można określić wiele ograniczeń interfejsu.</span><span class="sxs-lookup"><span data-stu-id="01ddc-124">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="01ddc-125">Można też ogólnego ograniczający interfejsu.</span><span class="sxs-lookup"><span data-stu-id="01ddc-125">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="01ddc-126">Argumentu typu dostarczonego T musi być lub pochodzić od argument dostarczony dla U.</span><span class="sxs-lookup"><span data-stu-id="01ddc-126">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

<span data-ttu-id="01ddc-127">Niektóre ograniczenia wzajemnie się wykluczają.</span><span class="sxs-lookup"><span data-stu-id="01ddc-127">Some of the constraints are mutually exclusive.</span></span> <span data-ttu-id="01ddc-128">Wszystkie typy wartości, musi mieć konstruktora bez parametrów dostępny.</span><span class="sxs-lookup"><span data-stu-id="01ddc-128">All value types must have an accessible parameterless constructor.</span></span> <span data-ttu-id="01ddc-129">`struct` Oznacza ograniczenie `new()` ograniczenia i `new()` ograniczenia nie można łączyć z `struct` ograniczenia.</span><span class="sxs-lookup"><span data-stu-id="01ddc-129">The `struct` constraint implies the `new()` constraint and the `new()` constraint cannot be combined with the `struct` constraint.</span></span> <span data-ttu-id="01ddc-130">`unmanaged` Oznacza ograniczenie `struct` ograniczenia.</span><span class="sxs-lookup"><span data-stu-id="01ddc-130">The `unmanaged` constraint implies the `struct` constraint.</span></span> <span data-ttu-id="01ddc-131">`unmanaged` Ograniczenia nie można łączyć z jedną `struct` lub `new()` ograniczenia.</span><span class="sxs-lookup"><span data-stu-id="01ddc-131">The `unmanaged` constraint cannot be combined with either the `struct` or `new()` constraints.</span></span>

## <a name="why-use-constraints"></a><span data-ttu-id="01ddc-132">Dlaczego warto korzystać z ograniczeniami</span><span class="sxs-lookup"><span data-stu-id="01ddc-132">Why use constraints</span></span>

<span data-ttu-id="01ddc-133">Ograniczając parametr typu, zwiększenie liczby dopuszczalnych operacji i wywołania metody do obsługiwanych przez ograniczający typu oraz wszystkich typów w hierarchii dziedziczenia.</span><span class="sxs-lookup"><span data-stu-id="01ddc-133">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="01ddc-134">Podczas projektowania klas ogólnych lub metody, jeśli wykonywanie żadnych operacji na ogólnych składowych poza przypisanie proste lub wszystkie metody, które nie są obsługiwane przez wywołanie <xref:System.Object?displayProperty=nameWithType>, należy zastosować ograniczenia do parametru typu.</span><span class="sxs-lookup"><span data-stu-id="01ddc-134">When you design generic classes or methods, if you will be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you will have to apply constraints to the type parameter.</span></span> <span data-ttu-id="01ddc-135">Na przykład ograniczenia klasy bazowej informuje kompilator, że tylko obiekty tego typu lub pochodzić z tego typu będzie służyć jako argumentów typu.</span><span class="sxs-lookup"><span data-stu-id="01ddc-135">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="01ddc-136">Gdy kompilator ma gwarancji, umożliwia metody tego typu można wywołać w klasy ogólnej.</span><span class="sxs-lookup"><span data-stu-id="01ddc-136">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="01ddc-137">Poniższy przykład kodu pokazuje funkcje, możesz dodać do `GenericList<T>` klasy (w [wprowadzenie do typów ogólnych](introduction-to-generics.md)), stosując ograniczenia klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="01ddc-137">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](introduction-to-generics.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="01ddc-138">Ograniczenie umożliwia klasy generycznej użyć `Employee.Name` właściwości.</span><span class="sxs-lookup"><span data-stu-id="01ddc-138">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="01ddc-139">Ograniczenie Określa, że wszystkie elementy typu `T` zapewniona jest albo `Employee` obiekt lub obiekt, który dziedziczy z `Employee`.</span><span class="sxs-lookup"><span data-stu-id="01ddc-139">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="01ddc-140">Wiele ograniczeń mogą być stosowane do tego samego parametru typu, a same ograniczenia mogą być typów ogólnych w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="01ddc-140">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="01ddc-141">Podczas stosowania `where T : class` ograniczenia, należy unikać `==` i `!=` operatory dla parametru typu, ponieważ te operatory przetestuje tożsamości odwołanie, nie na równoważność wartości.</span><span class="sxs-lookup"><span data-stu-id="01ddc-141">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="01ddc-142">Dzieje się tak nawet wtedy, gdy te operatory są przeciążone w typie, który jest używany jako argument.</span><span class="sxs-lookup"><span data-stu-id="01ddc-142">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="01ddc-143">Poniższy kod ilustruje ten punkt; nawet jeśli dane wyjściowe są fałszywe <xref:System.String> klasy przeciążenia `==` operatora.</span><span class="sxs-lookup"><span data-stu-id="01ddc-143">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="01ddc-144">Kompilator wie jedynie, że T jest typem odwołania w czasie kompilacji i musi być domyślne operatory, które są prawidłowe dla wszystkich typów odniesienia.</span><span class="sxs-lookup"><span data-stu-id="01ddc-144">The compiler only knows that T is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="01ddc-145">Jeśli należy przetestować na równoważność wartości, zalecaną metodą jest również dotyczyć `where T : IEquatable<T>` lub `where T : IComparable<T>` ograniczenia i zaimplementuj interfejs w każdej klasy, która będzie służyć do konstruowania klasy ogólnej.</span><span class="sxs-lookup"><span data-stu-id="01ddc-145">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="01ddc-146">Ograniczając wiele parametrów</span><span class="sxs-lookup"><span data-stu-id="01ddc-146">Constraining multiple parameters</span></span>

<span data-ttu-id="01ddc-147">Ograniczenia można zastosować do wielu parametrów, a wiele ograniczeń na jeden parametr, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="01ddc-147">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="01ddc-148">Parametry typu niepowiązanego</span><span class="sxs-lookup"><span data-stu-id="01ddc-148">Unbounded type parameters</span></span>

 <span data-ttu-id="01ddc-149">Parametry, które mają nie ograniczeń, takich jak T w klasę publiczną typu `SampleClass<T>{}`, są nazywane parametrami typu niepowiązanego.</span><span class="sxs-lookup"><span data-stu-id="01ddc-149">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="01ddc-150">Parametry typu niepowiązanego mają następujące reguły:</span><span class="sxs-lookup"><span data-stu-id="01ddc-150">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="01ddc-151">`!=` i `==` nie można używać operatorów, ponieważ nie ma żadnej gwarancji, że argument typu konkretnego obejmie te operatory.</span><span class="sxs-lookup"><span data-stu-id="01ddc-151">The `!=` and `==` operators cannot be used because there is no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="01ddc-152">Mogą być konwertowane do i z `System.Object` lub jawnie konwertowane na dowolny typ interfejsu.</span><span class="sxs-lookup"><span data-stu-id="01ddc-152">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="01ddc-153">Możesz porównać ich [null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="01ddc-153">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="01ddc-154">Jeśli podano parametr niepowiązanego jest porównywany z `null`, porównanie zawsze zwróci wartość false, jeśli argument typu jest typem wartości.</span><span class="sxs-lookup"><span data-stu-id="01ddc-154">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="01ddc-155">Parametry typu jako ograniczenia</span><span class="sxs-lookup"><span data-stu-id="01ddc-155">Type parameters as constraints</span></span>

<span data-ttu-id="01ddc-156">Użyj parametru typu ogólnego jako ograniczenie jest przydatne, gdy funkcja elementu członkowskiego z parametrem typu ma ograniczenie tego parametru, aby parametr typu zawierającego, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="01ddc-156">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="01ddc-157">W poprzednim przykładzie `T` ograniczenie typu w kontekście `Add` metody i parametrów niepowiązanego typu w kontekście `List` klasy.</span><span class="sxs-lookup"><span data-stu-id="01ddc-157">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="01ddc-158">Parametrów typu można również jako warunki ograniczające w definicji klasy ogólnej.</span><span class="sxs-lookup"><span data-stu-id="01ddc-158">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="01ddc-159">Parametr typu musi być zadeklarowany w nawiasach kątowych wraz z innymi parametrami typu:</span><span class="sxs-lookup"><span data-stu-id="01ddc-159">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="01ddc-160">Użyteczność parametrów typu jako warunki ograniczające z klasami ogólnego jest ograniczona, ponieważ kompilator może przyjąć, nie ma parametru typu z tą różnicą, że pochodzi od klasy `System.Object`.</span><span class="sxs-lookup"><span data-stu-id="01ddc-160">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="01ddc-161">Używać parametrów typu jako ograniczenia dotyczące klas ogólnych w scenariuszach, w których chcesz wymusić relacje dziedziczenia między dwa parametry typu.</span><span class="sxs-lookup"><span data-stu-id="01ddc-161">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="01ddc-162">Ograniczenie Unmanaged</span><span class="sxs-lookup"><span data-stu-id="01ddc-162">Unmanaged constraint</span></span>

<span data-ttu-id="01ddc-163">Począwszy od języka C# 7.3, można użyć `unmanaged` ograniczenie, aby określić, że parametr typu musi być **niezarządzany typ**.</span><span class="sxs-lookup"><span data-stu-id="01ddc-163">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be an **unmanaged type**.</span></span> <span data-ttu-id="01ddc-164">**Niezarządzany typ** to typ, który nie jest typem odwołania i nie zawiera pola typu odwołania na każdym poziomie zagnieżdżania.</span><span class="sxs-lookup"><span data-stu-id="01ddc-164">An **unmanaged type** is a type that is not a reference type and doesn't contain reference type fields at any level of nesting.</span></span> <span data-ttu-id="01ddc-165">`unmanaged` Ograniczenie pozwala na zapis wielokrotnego użytku procedur do pracy z typami, które mogą być zmieniane jako bloki pamięci, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="01ddc-165">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="01ddc-166">Powyższa metoda musi zostać skompilowany w `unsafe` kontekstu, ponieważ używa ona `sizeof` operator dla typu, nie jest znany jako typ wbudowany.</span><span class="sxs-lookup"><span data-stu-id="01ddc-166">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="01ddc-167">Bez `unmanaged` ograniczenie `sizeof` operatora jest niedostępny.</span><span class="sxs-lookup"><span data-stu-id="01ddc-167">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="01ddc-168">Delegowanie ograniczeń</span><span class="sxs-lookup"><span data-stu-id="01ddc-168">Delegate constraints</span></span>

<span data-ttu-id="01ddc-169">Ponadto począwszy od języka C# 7.3, można użyć <xref:System.Delegate?displayProperty=nameWithType> lub <xref:System.MulticastDelegate?displayProperty=nameWithType> jako ograniczenie klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="01ddc-169">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="01ddc-170">Środowisko CLR jest zawsze dozwolona to ograniczenie, ale niedozwolone w języku C#, go.</span><span class="sxs-lookup"><span data-stu-id="01ddc-170">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="01ddc-171">`System.Delegate` Ograniczenie pozwala napisać kod, który współdziała z delegatów w sposób bezpieczny.</span><span class="sxs-lookup"><span data-stu-id="01ddc-171">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="01ddc-172">Poniższy kod definiuje metodę rozszerzenia, która łączy dwa delegaty warunkiem, że są tego samego typu:</span><span class="sxs-lookup"><span data-stu-id="01ddc-172">The following code defines an extension method that combines two delegates provided they are the same type:</span></span>

[!code-csharp[using the delegate constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="01ddc-173">Metoda powyżej umożliwia łączenie obiektów delegowanych, które są tego samego typu:</span><span class="sxs-lookup"><span data-stu-id="01ddc-173">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="01ddc-174">Jeśli usuniesz komentarz ostatni wiersz, nie będzie kompilacji.</span><span class="sxs-lookup"><span data-stu-id="01ddc-174">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="01ddc-175">Zarówno `first` i `test` są typami delegatów, ale są one delegata różnych typów.</span><span class="sxs-lookup"><span data-stu-id="01ddc-175">Both `first` and `test` are delegate types, but they are different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="01ddc-176">Ograniczenia typu wyliczeniowego</span><span class="sxs-lookup"><span data-stu-id="01ddc-176">Enum constraints</span></span>

<span data-ttu-id="01ddc-177">Począwszy od języka C# 7.3, można również określić <xref:System.Enum?displayProperty=nameWithType> typu jako ograniczenia klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="01ddc-177">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="01ddc-178">Środowisko CLR jest zawsze dozwolona to ograniczenie, ale niedozwolone w języku C#, go.</span><span class="sxs-lookup"><span data-stu-id="01ddc-178">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="01ddc-179">Za pomocą typów ogólnych `System.Enum` umożliwiające bezpieczny programowania do wyników z pamięci podręcznej z poziomu przy użyciu metod statycznych w `System.Enum`.</span><span class="sxs-lookup"><span data-stu-id="01ddc-179">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="01ddc-180">Poniższy przykład umożliwia znalezienie wszystkich prawidłowych wartości dla typu wyliczeniowego, a następnie tworzy słownik mapujący te wartości na jego reprezentację ciągu.</span><span class="sxs-lookup"><span data-stu-id="01ddc-180">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="01ddc-181">Metody stosowane używać odbicia, która ma wpływ na wydajność.</span><span class="sxs-lookup"><span data-stu-id="01ddc-181">The methods used make use of reflection, which has performance implications.</span></span> <span data-ttu-id="01ddc-182">Możesz wywołać tę metodę, aby utworzyć kolekcję pamięci podręcznej i ponowne użycie zamiast powtarzania wywołania, które wymagają odbicia.</span><span class="sxs-lookup"><span data-stu-id="01ddc-182">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="01ddc-183">Można użyć tego jak pokazano w następującym przykładzie do wyliczenia i tworzenia słownik nazw i wartości:</span><span class="sxs-lookup"><span data-stu-id="01ddc-183">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="01ddc-184">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="01ddc-184">See Also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="01ddc-185">Przewodnik programowania w języku C#</span><span class="sxs-lookup"><span data-stu-id="01ddc-185">C# Programming Guide</span></span>](../../../csharp/programming-guide/index.md)  
- [<span data-ttu-id="01ddc-186">Wprowadzenie do typów ogólnych</span><span class="sxs-lookup"><span data-stu-id="01ddc-186">Introduction to Generics</span></span>](../../../csharp/programming-guide/generics/introduction-to-generics.md)  
- [<span data-ttu-id="01ddc-187">Klasy ogólne</span><span class="sxs-lookup"><span data-stu-id="01ddc-187">Generic Classes</span></span>](../../../csharp/programming-guide/generics/generic-classes.md)  
- [<span data-ttu-id="01ddc-188">new, ograniczenie</span><span class="sxs-lookup"><span data-stu-id="01ddc-188">new Constraint</span></span>](../../../csharp/language-reference/keywords/new-constraint.md)  
