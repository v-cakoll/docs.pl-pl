---
title: Wdrażanie IEnumerable
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: 4151a680050f234d450d8de5e67a767c54e8df68
ms.sourcegitcommit: 43d10ef65f0f1fd6c3b515e363bde11a3fcd8d6d
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/04/2020
ms.locfileid: "78266914"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="a3bd1-102">Wskazówki: wdrażanie IEnumerable(Of T) w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="a3bd1-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="a3bd1-103">Interfejs <xref:System.Collections.Generic.IEnumerable%601> jest implementowany przez klasy, które mogą zwracać sekwencję wartości po jednym elemencie naraz.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="a3bd1-104">Zaletą zwracania danych jeden element naraz jest, że nie trzeba załadować pełny zestaw danych do pamięci, aby pracować z nim.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="a3bd1-105">Wystarczy użyć wystarczającej ilości pamięci, aby załadować pojedynczy element z danych.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="a3bd1-106">Klasy, które `IEnumerable(T)` implementują interfejs `For Each` może służyć z pętli lub zapytań LINQ.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="a3bd1-107">Rozważmy na przykład aplikację, która musi odczytać duży plik tekstowy i zwrócić każdy wiersz z pliku, który spełnia określone kryteria wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="a3bd1-108">Aplikacja używa kwerendy LINQ do zwracania wierszy z pliku, które odpowiadają określonym kryteriom.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="a3bd1-109">Aby zbadać zawartość pliku przy użyciu kwerendy LINQ, aplikacja może załadować zawartość pliku do tablicy lub kolekcji.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="a3bd1-110">Jednak ładowanie całego pliku do tablicy lub kolekcji zużywa znacznie więcej pamięci niż jest to wymagane.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="a3bd1-111">Kwerenda LINQ zamiast tego kwerendy zawartości pliku przy użyciu klasy wyliczalne, zwracając tylko wartości, które odpowiadają kryteriom wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="a3bd1-112">Kwerendy, które zwracają tylko kilka pasujących wartości zużywa znacznie mniej pamięci.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="a3bd1-113">Można utworzyć klasę, która <xref:System.Collections.Generic.IEnumerable%601> implementuje interfejs, aby udostępnić dane źródłowe jako wyliczalne dane.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="a3bd1-114">Klasa, która implementuje `IEnumerable(T)` interfejs będzie wymagać <xref:System.Collections.Generic.IEnumerator%601> innej klasy, która implementuje interfejs do iteracji za pośrednictwem danych źródłowych.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="a3bd1-115">Te dwie klasy umożliwiają zwracanie elementów danych sekwencyjnie jako określonego typu.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="a3bd1-116">W tym instruktażu utworzysz klasę, `IEnumerable(Of String)` która implementuje interfejs `IEnumerator(Of String)` i klasę, która implementuje interfejs do odczytu pliku tekstowego po jednym wierszu naraz.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="a3bd1-117">Tworzenie klasy wyliczalnej</span><span class="sxs-lookup"><span data-stu-id="a3bd1-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="a3bd1-118">**Tworzenie projektu klasy wyliczalnej**</span><span class="sxs-lookup"><span data-stu-id="a3bd1-118">**Create the enumerable class project**</span></span>

1. <span data-ttu-id="a3bd1-119">W języku Visual Basic w menu **Plik** wskaż polecenie **Nowy,** a następnie kliknij polecenie **Projekt**.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1. <span data-ttu-id="a3bd1-120">W oknie dialogowym **Nowy projekt** w okienku **Typy projektów** upewnij się, że system **Windows** jest zaznaczony.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="a3bd1-121">Wybierz **bibliotekę klas** w okienku **Szablony.**</span><span class="sxs-lookup"><span data-stu-id="a3bd1-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="a3bd1-122">W polu **Nazwa** `StreamReaderEnumerable`wpisz , a następnie kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="a3bd1-123">Zostanie wyświetlony nowy projekt.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-123">The new project is displayed.</span></span>

1. <span data-ttu-id="a3bd1-124">W **Eksploratorze rozwiązań**kliknij prawym przyciskiem myszy plik Class1.vb i kliknij polecenie **Zmień nazwę**.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="a3bd1-125">Zmień nazwę pliku `StreamReaderEnumerable.vb` na i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="a3bd1-126">Zmiana nazwy pliku spowoduje również zmianę `StreamReaderEnumerable`nazwy klasy na .</span><span class="sxs-lookup"><span data-stu-id="a3bd1-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="a3bd1-127">Ta klasa zaimplementuje `IEnumerable(Of String)` interfejs.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1. <span data-ttu-id="a3bd1-128">Kliknij prawym przyciskiem myszy projekt StreamReaderEnumerable, wskaż polecenie **Dodaj**, a następnie kliknij polecenie **Nowy element**.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="a3bd1-129">Wybierz szablon **Klasa.**</span><span class="sxs-lookup"><span data-stu-id="a3bd1-129">Select the **Class** template.</span></span> <span data-ttu-id="a3bd1-130">W polu **Nazwa** `StreamReaderEnumerator.vb` wpisz i kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="a3bd1-131">Pierwsza klasa w tym projekcie jest klasą wyliczalną i zaimplementuje `IEnumerable(Of String)` interfejs.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="a3bd1-132">Ten ogólny interfejs <xref:System.Collections.IEnumerable> implementuje interfejs i gwarantuje, że konsumenci tej `String`klasy mogą uzyskiwać dostęp do wartości wpisanych jako .</span><span class="sxs-lookup"><span data-stu-id="a3bd1-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="a3bd1-133">**Dodaj kod do zaimplementowania IEnumerable**</span><span class="sxs-lookup"><span data-stu-id="a3bd1-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="a3bd1-134">Otwórz plik StreamReaderEnumerable.vb.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="a3bd1-135">W wierszu `Public Class StreamReaderEnumerable`po wpisz następujące polecenie i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#1)]

   <span data-ttu-id="a3bd1-136">Visual Basic automatycznie wypełnia klasę członkami, które są wymagane `IEnumerable(Of String)` przez interfejs.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="a3bd1-137">Ta klasa wyliczalna będzie odczytywać wiersze z pliku tekstowego po jednym wierszu naraz.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="a3bd1-138">Dodaj następujący kod do klasy, aby udostępnić konstruktora publicznego, który przyjmuje ścieżkę pliku jako parametr wejściowy.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#2)]

4. <span data-ttu-id="a3bd1-139">Implementacja <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> metody `IEnumerable(Of String)` interfejsu zwróci nowe wystąpienie `StreamReaderEnumerator` klasy.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="a3bd1-140">Implementacja `GetEnumerator` metody `IEnumerable` interfejsu może być `Private`wykonana , ponieważ trzeba udostępnić `IEnumerable(Of String)` tylko elementy członkowskie interfejsu.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="a3bd1-141">Zastąp kod wygenerowany `GetEnumerator` przez program Visual Basic dla metod następującym kodem.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#3)]  
  
<span data-ttu-id="a3bd1-142">**Dodaj kod do zaimplementowania IEnumerator**</span><span class="sxs-lookup"><span data-stu-id="a3bd1-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="a3bd1-143">Otwórz plik StreamReaderEnumerator.vb.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="a3bd1-144">W wierszu `Public Class StreamReaderEnumerator`po wpisz następujące polecenie i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#4)]

   <span data-ttu-id="a3bd1-145">Visual Basic automatycznie wypełnia klasę członkami, które są wymagane `IEnumerator(Of String)` przez interfejs.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="a3bd1-146">Klasa wyliczania otwiera plik tekstowy i wykonuje we/wy pliku, aby odczytać wiersze z pliku.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="a3bd1-147">Dodaj następujący kod do klasy, aby udostępnić konstruktora publicznego, który przyjmuje ścieżkę pliku jako parametr wejściowy i otwórz plik tekstowy do odczytu.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#5)]

4. <span data-ttu-id="a3bd1-148">`Current` Właściwości zarówno dla `IEnumerator(Of String)` i `IEnumerator` interfejsów zwracają bieżący element z `String`pliku tekstowego jako .</span><span class="sxs-lookup"><span data-stu-id="a3bd1-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="a3bd1-149">Implementacja `Current` właściwości `IEnumerator` interfejsu mogą być `Private`wykonane, ponieważ trzeba udostępnić `IEnumerator(Of String)` tylko elementy członkowskie interfejsu.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="a3bd1-150">Zastąp kod wygenerowany `Current` przez program Visual Basic dla właściwości następującym kodem.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#6)]

5. <span data-ttu-id="a3bd1-151">Metoda `MoveNext` `IEnumerator` interfejsu przechodzi do następnego elementu w pliku tekstowym i aktualizuje `Current` wartość zwracaną przez właściwość.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="a3bd1-152">Jeśli nie ma więcej elementów `MoveNext` do `False`odczytu, metoda zwraca ; w `MoveNext` przeciwnym `True`razie metoda zwraca .</span><span class="sxs-lookup"><span data-stu-id="a3bd1-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="a3bd1-153">Dodaj następujący kod do metody `MoveNext`:</span><span class="sxs-lookup"><span data-stu-id="a3bd1-153">Add the following code to the `MoveNext` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#7)]

6. <span data-ttu-id="a3bd1-154">Metoda `Reset` `IEnumerator` interfejsu kieruje iteratora do punktu początkowego pliku tekstowego i czyści bieżącą wartość elementu.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="a3bd1-155">Dodaj następujący kod do metody `Reset`:</span><span class="sxs-lookup"><span data-stu-id="a3bd1-155">Add the following code to the `Reset` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#8)]

7. <span data-ttu-id="a3bd1-156">Metoda `Dispose` `IEnumerator` interfejsu gwarantuje, że wszystkie zasoby niezarządzane są zwalniane przed zniszczeniem iteratora.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="a3bd1-157">Dojście do pliku, `StreamReader` który jest używany przez obiekt jest zasobem niezarządzanym i musi zostać zamknięty przed zniszczeniem wystąpienia iteratora.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="a3bd1-158">Zastąp kod wygenerowany `Dispose` przez program Visual Basic dla metody następującym kodem.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#9)]
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="a3bd1-159">Korzystanie z przykładowego iteratora</span><span class="sxs-lookup"><span data-stu-id="a3bd1-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="a3bd1-160">Można użyć klasy wyliczalnej w kodzie wraz ze strukturami `IEnumerable`kontroli, które `For Next` wymagają obiektu, który implementuje , takich jak pętla lub kwerenda LINQ.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="a3bd1-161">Poniższy przykład `StreamReaderEnumerable` pokazuje w kwerendzie LINQ.</span><span class="sxs-lookup"><span data-stu-id="a3bd1-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/Module1.vb#10)]  
  
## <a name="see-also"></a><span data-ttu-id="a3bd1-162">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="a3bd1-162">See also</span></span>

- [<span data-ttu-id="a3bd1-163">Wprowadzenie do LINQ w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="a3bd1-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)
- [<span data-ttu-id="a3bd1-164">Przepływ sterowania</span><span class="sxs-lookup"><span data-stu-id="a3bd1-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)
- [<span data-ttu-id="a3bd1-165">Struktury pętli</span><span class="sxs-lookup"><span data-stu-id="a3bd1-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)
- [<span data-ttu-id="a3bd1-166">For Each...Next, instrukcja</span><span class="sxs-lookup"><span data-stu-id="a3bd1-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
