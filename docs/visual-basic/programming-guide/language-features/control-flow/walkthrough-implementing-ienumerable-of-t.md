---
title: Implementowanie interfejsu IEnumerable
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: f40fcf7e0724addc478b261dcd36d09e1d8a751a
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 11/22/2019
ms.locfileid: "74333686"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="5dd5a-102">Wskazówki: wdrażanie IEnumerable(Of T) w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="5dd5a-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="5dd5a-103">Interfejs <xref:System.Collections.Generic.IEnumerable%601> jest implementowany przez klasy, które mogą zwracać sekwencję wartości po jednym elemencie naraz.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="5dd5a-104">Zaletą zwracania danych z jednego elementu jednocześnie jest to, że nie trzeba ładować kompletnego zestawu danych do pamięci, aby z niego korzystać.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="5dd5a-105">Wystarczy użyć wystarczającej ilości pamięci do załadowania pojedynczego elementu z danych.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="5dd5a-106">Klasy implementujące interfejs `IEnumerable(T)` mogą być używane z pętlą `For Each` lub kwerend LINQ.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="5dd5a-107">Rozważmy na przykład aplikację, która musi odczytać duży plik tekstowy i zwrócić każdy wiersz z pliku zgodnego z określonymi kryteriami wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="5dd5a-108">Aplikacja używa zapytania LINQ do zwracania wierszy z pliku, które pasują do określonych kryteriów.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="5dd5a-109">Aby zbadać zawartość pliku przy użyciu zapytania LINQ, aplikacja może załadować zawartość pliku do tablicy lub kolekcji.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="5dd5a-110">Jednak załadowanie całego pliku do tablicy lub kolekcji spowodowałoby dużo więcej pamięci niż jest to wymagane.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="5dd5a-111">Zapytanie LINQ mogło zamiast tego zbadać zawartość pliku przy użyciu klasy wyliczalnej, zwracając tylko wartości pasujące do kryteriów wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="5dd5a-112">Zapytania, które zwracają tylko kilka pasujących wartości, zużywają znacznie mniej pamięci.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="5dd5a-113">Można utworzyć klasę, która implementuje interfejs <xref:System.Collections.Generic.IEnumerable%601>, aby uwidocznić dane źródłowe jako wyliczalne dane.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="5dd5a-114">Klasa implementująca interfejs `IEnumerable(T)` będzie wymagała innej klasy implementującej interfejs <xref:System.Collections.Generic.IEnumerator%601> do iteracji przez dane źródłowe.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="5dd5a-115">Te dwie klasy umożliwiają zwracanie elementów danych sekwencyjnie jako określonego typu.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="5dd5a-116">W tym instruktażu utworzysz klasę, która implementuje interfejs `IEnumerable(Of String)` i klasę implementującą interfejs `IEnumerator(Of String)` do odczytywania pliku tekstowego po jednym wierszu.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="5dd5a-117">Tworzenie klasy wyliczalnej</span><span class="sxs-lookup"><span data-stu-id="5dd5a-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="5dd5a-118">**Utwórz wyliczalny projekt klasy**</span><span class="sxs-lookup"><span data-stu-id="5dd5a-118">**Create the enumerable class project**</span></span>

1. <span data-ttu-id="5dd5a-119">W Visual Basic w menu **plik** wskaż polecenie **Nowy** , a następnie kliknij pozycję **projekt**.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1. <span data-ttu-id="5dd5a-120">W oknie dialogowym **Nowy projekt** w okienku **typy projektów** upewnij się, że wybrano opcję **Windows** .</span><span class="sxs-lookup"><span data-stu-id="5dd5a-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="5dd5a-121">W okienku **Szablony** wybierz pozycję **Biblioteka klas** .</span><span class="sxs-lookup"><span data-stu-id="5dd5a-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="5dd5a-122">W polu **Nazwa** wpisz `StreamReaderEnumerable`, a następnie kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="5dd5a-123">Zostanie wyświetlony nowy projekt.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-123">The new project is displayed.</span></span>

1. <span data-ttu-id="5dd5a-124">W **Eksplorator rozwiązań**kliknij prawym przyciskiem myszy plik Class1. vb i kliknij polecenie **Zmień nazwę**.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="5dd5a-125">Zmień nazwę pliku na `StreamReaderEnumerable.vb` i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="5dd5a-126">Zmiana nazwy pliku spowoduje również zmianę nazwy klasy na `StreamReaderEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="5dd5a-127">Ta klasa spowoduje zaimplementowanie interfejsu `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1. <span data-ttu-id="5dd5a-128">Kliknij prawym przyciskiem myszy projekt StreamReaderEnumerable, wskaż polecenie **Dodaj**, a następnie kliknij pozycję **nowy element**.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="5dd5a-129">Wybierz szablon **klasy** .</span><span class="sxs-lookup"><span data-stu-id="5dd5a-129">Select the **Class** template.</span></span> <span data-ttu-id="5dd5a-130">W polu **Nazwa** wpisz `StreamReaderEnumerator.vb` i kliknij przycisk **OK**.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="5dd5a-131">Pierwsza klasa w tym projekcie jest klasą wyliczalną i implementuje interfejs `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="5dd5a-132">Ten interfejs generyczny implementuje interfejs <xref:System.Collections.IEnumerable> i gwarantuje, że konsumenci tej klasy mogą uzyskać dostęp do wartości wpisanych jako `String`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="5dd5a-133">**Dodaj kod w celu zaimplementowania interfejsu IEnumerable**</span><span class="sxs-lookup"><span data-stu-id="5dd5a-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="5dd5a-134">Otwórz plik StreamReaderEnumerable. vb.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="5dd5a-135">W wierszu po `Public Class StreamReaderEnumerable`wpisz następujące polecenie i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#1)]

   <span data-ttu-id="5dd5a-136">Visual Basic automatycznie wypełnia klasę elementami członkowskimi, które są wymagane przez interfejs `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="5dd5a-137">Ta klasa wyliczalna odczytaje wiersze z pliku tekstowego po jednym wierszu.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="5dd5a-138">Dodaj następujący kod do klasy, aby uwidocznić konstruktora publicznego, który Pobiera ścieżkę pliku jako parametr wejściowy.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#2)]

4. <span data-ttu-id="5dd5a-139">Implementacja metody <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> interfejsu `IEnumerable(Of String)` zwróci nowe wystąpienie klasy `StreamReaderEnumerator`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="5dd5a-140">Implementację metody `GetEnumerator` interfejsu `IEnumerable` można wykonać `Private`, ponieważ należy uwidocznić tylko elementy członkowskie interfejsu `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="5dd5a-141">Zastąp kod, który Visual Basic wygenerowany dla metod `GetEnumerator`, przy użyciu następującego kodu.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#3)]  
  
<span data-ttu-id="5dd5a-142">**Dodawanie kodu w celu zaimplementowania IEnumerator**</span><span class="sxs-lookup"><span data-stu-id="5dd5a-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="5dd5a-143">Otwórz plik StreamReaderEnumerator. vb.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="5dd5a-144">W wierszu po `Public Class StreamReaderEnumerator`wpisz następujące polecenie i naciśnij klawisz ENTER.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#4)]

   <span data-ttu-id="5dd5a-145">Visual Basic automatycznie wypełnia klasę elementami członkowskimi, które są wymagane przez interfejs `IEnumerator(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="5dd5a-146">Klasa Enumerator otwiera plik tekstowy i wykonuje operacje we/wy pliku w celu odczytania wierszy z pliku.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="5dd5a-147">Dodaj następujący kod do klasy, aby uwidocznić konstruktora publicznego, który Pobiera ścieżkę pliku jako parametr wejściowy, i Otwórz plik tekstowy do odczytu.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#5)]

4. <span data-ttu-id="5dd5a-148">Właściwości `Current` dla interfejsów `IEnumerator(Of String)` i `IEnumerator` zwracają bieżący element z pliku tekstowego jako `String`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="5dd5a-149">Implementacja właściwości `Current` interfejsu `IEnumerator` można wykonać `Private`, ponieważ należy uwidocznić tylko elementy członkowskie interfejsu `IEnumerator(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="5dd5a-150">Zastąp kod, który Visual Basic wygenerowany dla właściwości `Current` przy użyciu następującego kodu.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#6)]

5. <span data-ttu-id="5dd5a-151">Metoda `MoveNext` interfejsu `IEnumerator` przechodzi do następnego elementu w pliku tekstowym i aktualizuje wartość zwracaną przez właściwość `Current`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="5dd5a-152">Jeśli nie ma więcej elementów do odczytania, Metoda `MoveNext` zwraca `False`; w przeciwnym razie metoda `MoveNext` zwraca `True`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="5dd5a-153">Dodaj następujący kod do metody `MoveNext`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-153">Add the following code to the `MoveNext` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#7)]

6. <span data-ttu-id="5dd5a-154">Metoda `Reset` interfejsu `IEnumerator` kieruje iterator, aby wskazywał na początek pliku tekstowego i czyści bieżącą wartość elementu.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="5dd5a-155">Dodaj następujący kod do metody `Reset`.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-155">Add the following code to the `Reset` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#8)]

7. <span data-ttu-id="5dd5a-156">Metoda `Dispose` interfejsu `IEnumerator` gwarantuje, że wszystkie niezarządzane zasoby są wydawane przed zniszczeniem iteratora.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="5dd5a-157">Dojście do pliku, które jest używane przez obiekt `StreamReader`, jest zasobem niezarządzanym i musi zostać zamknięte przed zniszczeniem wystąpienia iteratora.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="5dd5a-158">Zastąp kod, który Visual Basic wygenerowany dla metody `Dispose`, przy użyciu następującego kodu.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#9)] 
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="5dd5a-159">Użycie iteratora przykładowego</span><span class="sxs-lookup"><span data-stu-id="5dd5a-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="5dd5a-160">Można użyć klasy wyliczalnej w kodzie wraz ze strukturami formantów wymagającymi obiektu, który implementuje `IEnumerable`, takich jak pętla `For Next` lub zapytanie LINQ.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="5dd5a-161">Poniższy przykład pokazuje `StreamReaderEnumerable` w kwerendzie LINQ.</span><span class="sxs-lookup"><span data-stu-id="5dd5a-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/Module1.vb#10)]  
  
## <a name="see-also"></a><span data-ttu-id="5dd5a-162">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="5dd5a-162">See also</span></span>

- [<span data-ttu-id="5dd5a-163">Wprowadzenie do LINQ w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="5dd5a-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)
- [<span data-ttu-id="5dd5a-164">Przepływ sterowania</span><span class="sxs-lookup"><span data-stu-id="5dd5a-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)
- [<span data-ttu-id="5dd5a-165">Struktury pętli</span><span class="sxs-lookup"><span data-stu-id="5dd5a-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)
- [<span data-ttu-id="5dd5a-166">For Each...Next, instrukcja</span><span class="sxs-lookup"><span data-stu-id="5dd5a-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
