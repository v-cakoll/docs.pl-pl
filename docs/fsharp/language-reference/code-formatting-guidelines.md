---
title: Wskazówki dotyczące formatowania kodu (F#)
description: 'Dowiedz się wskazówki dotyczące formatowania wcięcia kodu dla języka czytelności, estetykę, normalizacji i kompilacji programowania w języku F #.'
ms.date: 05/16/2016
ms.openlocfilehash: 5bb1f9958a21beb795f9174e44f24c7194453fc3
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 05/04/2018
---
# <a name="code-formatting-guidelines"></a><span data-ttu-id="a846f-103">Wskazówki dotyczące formatowania kodu</span><span class="sxs-lookup"><span data-stu-id="a846f-103">Code Formatting Guidelines</span></span>

<span data-ttu-id="a846f-104">W tym temacie przedstawiono wskazówki wcięcia kodu języka F #.</span><span class="sxs-lookup"><span data-stu-id="a846f-104">This topic summarizes code indentation guidelines for F#.</span></span> <span data-ttu-id="a846f-105">Ponieważ języka F # jest wrażliwe na podziały wiersza i wcięcia, go nie jest tylko problem czytelności, estetycznych problem lub kodowania problem normalizacji poprawnie formatowania kodu.</span><span class="sxs-lookup"><span data-stu-id="a846f-105">Because the F# language is sensitive to line breaks and indentation, it is not just a readability issue, aesthetic issue, or coding standardization issue to format your code correctly.</span></span> <span data-ttu-id="a846f-106">Należy sformatować kodu poprawnie na jej poprawnej kompilacji.</span><span class="sxs-lookup"><span data-stu-id="a846f-106">You must format your code correctly for it to compile correctly.</span></span>


## <a name="general-rules-for-indentation"></a><span data-ttu-id="a846f-107">Zasady ogólne dotyczące wcięcie</span><span class="sxs-lookup"><span data-stu-id="a846f-107">General Rules for Indentation</span></span>
<span data-ttu-id="a846f-108">Gdy wymagana jest wcięcie, należy użyć spacje, tabulatory nie.</span><span class="sxs-lookup"><span data-stu-id="a846f-108">When indentation is required, you must use spaces, not tabs.</span></span> <span data-ttu-id="a846f-109">Wymagana jest co najmniej jedną przestrzeń.</span><span class="sxs-lookup"><span data-stu-id="a846f-109">At least one space is required.</span></span> <span data-ttu-id="a846f-110">Organizacja może utworzyć standardów kodowania, aby określić liczbę spacji dla wcięć; Typowy jest trzech lub czterech spacji wcięcia na każdym poziomie, gdzie występuje wcięcia.</span><span class="sxs-lookup"><span data-stu-id="a846f-110">Your organization can create coding standards to specify the number of spaces to use for indentation; three or four spaces of indentation at each level where indentation occurs is typical.</span></span> <span data-ttu-id="a846f-111">Można skonfigurować programu Visual Studio, aby dopasować standardów wcięcia w organizacji, zmieniając opcje w `Options` okno dialogowe, które jest dostępne po `Tools` menu.</span><span class="sxs-lookup"><span data-stu-id="a846f-111">You can configure Visual Studio to match your organization's indentation standards by changing the options in the `Options` dialog box, which is available from the `Tools` menu.</span></span> <span data-ttu-id="a846f-112">W `Text Editor` węzła, rozwiń węzeł `F#` , a następnie kliknij przycisk `Tabs`.</span><span class="sxs-lookup"><span data-stu-id="a846f-112">In the `Text Editor` node, expand `F#` and then click `Tabs`.</span></span> <span data-ttu-id="a846f-113">Opis dostępnych opcji, zobacz [opcje, Edytor tekstu, wszystkie języki, karty](https://msdn.microsoft.com/library/7sffa753.aspx).</span><span class="sxs-lookup"><span data-stu-id="a846f-113">For a description of the available options, see [Options, Text Editor, All Languages, Tabs](https://msdn.microsoft.com/library/7sffa753.aspx).</span></span>

<span data-ttu-id="a846f-114">Ogólnie rzecz biorąc gdy kompilator kodu, przechowuje wewnętrznego stosu, który wskazuje bieżący poziom zagnieżdżenia.</span><span class="sxs-lookup"><span data-stu-id="a846f-114">In general, when the compiler parses your code, it maintains an internal stack that indicates the current level of nesting.</span></span> <span data-ttu-id="a846f-115">Gdy tworzone jest wcięcie kodu, nowy poziom zagnieżdżenia jest utworzone lub wypychana na tym stosu wewnętrznego.</span><span class="sxs-lookup"><span data-stu-id="a846f-115">When code is indented, a new level of nesting is created, or pushed onto this internal stack.</span></span> <span data-ttu-id="a846f-116">Po zakończeniu konstrukcję poziom jest zdjęte ze stosu.</span><span class="sxs-lookup"><span data-stu-id="a846f-116">When a construct ends, the level is popped.</span></span> <span data-ttu-id="a846f-117">Wcięcie jest jednym ze sposobów sygnalizuje koniec poziomu i pop stosu wewnętrznego, ale niektórych tokenów również zostać zdjęte ze stosu, takich jak poziom `end` — słowo kluczowe, lub zamykający nawias klamrowy lub nawias.</span><span class="sxs-lookup"><span data-stu-id="a846f-117">Indentation is one way to signal the end of a level and pop the internal stack, but certain tokens also cause the level to be popped, such as the `end` keyword, or a closing brace or parenthesis.</span></span>

<span data-ttu-id="a846f-118">Kod w wielowierszowym konstrukcji, takich jak definicją typu definicji funkcji `try...with` konstrukcję i zapętlenia konstrukcje, musi wcięty względem wiersza otwierania konstrukcji.</span><span class="sxs-lookup"><span data-stu-id="a846f-118">Code in a multiline construct, such as a type definition, function definition, `try...with` construct, and looping constructs, must be indented relative to the opening line of the construct.</span></span> <span data-ttu-id="a846f-119">Pierwszy wiersz z wcięciami ustanawia pozycję w kolumnie dla kolejnych kodu w tej samej konstrukcji.</span><span class="sxs-lookup"><span data-stu-id="a846f-119">The first indented line establishes a column position for subsequent code in the same construct.</span></span> <span data-ttu-id="a846f-120">Poziom wcięcia jest nazywany *kontekstu*.</span><span class="sxs-lookup"><span data-stu-id="a846f-120">The indentation level is called a *context*.</span></span> <span data-ttu-id="a846f-121">Pozycja kolumny Ustawia minimalną kolumny, określany jako *wiersza poza*, dla kolejnych wierszy kodu, które znajdują się w tym samym kontekście.</span><span class="sxs-lookup"><span data-stu-id="a846f-121">The column position sets a minimum column, referred to as an *offside line*, for subsequent lines of code that are in the same context.</span></span> <span data-ttu-id="a846f-122">W przypadku napotkania wiersz kodu wcięta mniej niż ta pozycja kolumny ustalonych, kompilator zakłada zakończył kontekstu i czy możesz są teraz generowanie kodu na następny poziom, poprzedni kontekst.</span><span class="sxs-lookup"><span data-stu-id="a846f-122">When a line of code is encountered that is indented less than this established column position, the compiler assumes that the context has ended and that you are now coding at the next level up, in the previous context.</span></span> <span data-ttu-id="a846f-123">Termin *poza* jest używany do opisu stanu, w którym wiersz kodu wyzwala koniec konstrukcję, ponieważ nie jest wystarczająco daleko wcięcia.</span><span class="sxs-lookup"><span data-stu-id="a846f-123">The term *offside* is used to describe the condition in which a line of code triggers the end of a construct because it is not indented far enough.</span></span> <span data-ttu-id="a846f-124">Innymi słowy kod z lewej strony poza linii jest poza.</span><span class="sxs-lookup"><span data-stu-id="a846f-124">In other words, code to the left of an offside line is offside.</span></span> <span data-ttu-id="a846f-125">W kodzie poprawnie wcięta możesz korzystać z reguły poza aby odróżniać koniec konstrukcji.</span><span class="sxs-lookup"><span data-stu-id="a846f-125">In correctly indented code, you take advantage of the offside rule in order to delineate the end of constructs.</span></span> <span data-ttu-id="a846f-126">Jeśli używasz wcięcia nieprawidłowo, poza warunek może spowodować, że kompilator ostrzeżenie lub może doprowadzić do nieprawidłowej interpretacji kodu.</span><span class="sxs-lookup"><span data-stu-id="a846f-126">If you use indentation improperly, an offside condition can cause the compiler to issue a warning or can lead to the incorrect interpretation of your code.</span></span>

<span data-ttu-id="a846f-127">Poza wiersze są określane w następujący sposób.</span><span class="sxs-lookup"><span data-stu-id="a846f-127">Offside lines are determined as follows.</span></span>


- <span data-ttu-id="a846f-128">`=` Token skojarzony z `let` wprowadza poza wiersza w kolumnie pierwszego tokena po `=` logowania.</span><span class="sxs-lookup"><span data-stu-id="a846f-128">An `=` token associated with a `let` introduces an offside line at the column of the first token after the `=` sign.</span></span>


- <span data-ttu-id="a846f-129">W `if...then...else` wyrażenia, pozycja kolumny pierwszy token po `then` — słowo kluczowe lub `else` — słowo kluczowe wprowadza poza wiersza.</span><span class="sxs-lookup"><span data-stu-id="a846f-129">In an `if...then...else` expression, the column position of the first token after the `then` keyword or the `else` keyword introduces an offside line.</span></span>


- <span data-ttu-id="a846f-130">W `try...with` wyrażenia, pierwszy token po `try` wprowadza poza wiersza.</span><span class="sxs-lookup"><span data-stu-id="a846f-130">In a `try...with` expression, the first token after `try` introduces an offside line.</span></span>


- <span data-ttu-id="a846f-131">W `match` wyrażenia, pierwszy token po `with` i pierwszy token po każdym poleceniu `->` wprowadzenie poza wierszy.</span><span class="sxs-lookup"><span data-stu-id="a846f-131">In a `match` expression, the first token after `with` and the first token after each `->` introduce offside lines.</span></span>


- <span data-ttu-id="a846f-132">Pierwszy token po `with` jako typ rozszerzenia wprowadza poza wiersza.</span><span class="sxs-lookup"><span data-stu-id="a846f-132">The first token after `with` in a type extension introduces an offside line.</span></span>


- <span data-ttu-id="a846f-133">Pierwszy token po otwierający nawias klamrowy lub nawias lub po `begin` — słowo kluczowe, wprowadza poza wiersza.</span><span class="sxs-lookup"><span data-stu-id="a846f-133">The first token after an opening brace or parenthesis, or after the `begin` keyword, introduces an offside line.</span></span>


- <span data-ttu-id="a846f-134">Pierwszy znak w słowa kluczowe `let`, `if`, i `module` wprowadzenie poza wierszy.</span><span class="sxs-lookup"><span data-stu-id="a846f-134">The first character in the keywords `let`, `if`, and `module` introduce offside lines.</span></span>


<span data-ttu-id="a846f-135">Poniższe przykłady kodu przedstawiają reguły wcięcia.</span><span class="sxs-lookup"><span data-stu-id="a846f-135">The following code examples illustrate the indentation rules.</span></span> <span data-ttu-id="a846f-136">W tym miejscu wydruku instrukcje korzystają z wcięcie Aby skojarzyć je z odpowiedniego kontekstu.</span><span class="sxs-lookup"><span data-stu-id="a846f-136">Here, the print statements rely on indentation to associate them with the appropriate context.</span></span> <span data-ttu-id="a846f-137">Za każdym razem, gdy wcięcie przewiduje, kontekst jest zdjęte ze stosu, jak i przywraca poprzedni kontekst.</span><span class="sxs-lookup"><span data-stu-id="a846f-137">Every time the indentation shifts, the context is popped and returns to the previous context.</span></span> <span data-ttu-id="a846f-138">W związku z tym miejscem jest wydrukowany na końcu każdej iteracji; "Gotowe"!</span><span class="sxs-lookup"><span data-stu-id="a846f-138">Therefore, a space is printed at the end of each iteration; "Done!"</span></span> <span data-ttu-id="a846f-139">tylko jest drukowany jeden raz, ponieważ poza Wcięcie określa, że nie jest w pętli.</span><span class="sxs-lookup"><span data-stu-id="a846f-139">is only printed one time because the offside indentation establishes that it is not part of the loop.</span></span> <span data-ttu-id="a846f-140">Drukowanie ciągu "Context najwyższego poziomu" nie jest częścią funkcji.</span><span class="sxs-lookup"><span data-stu-id="a846f-140">The printing of the string "Top-level context" is not part of the function.</span></span> <span data-ttu-id="a846f-141">W związku z tym wydrukowaniem najpierw podczas statycznego inicjowania przed wywołaniem funkcji.</span><span class="sxs-lookup"><span data-stu-id="a846f-141">Therefore, it is printed first, during the static initialization, before the function is called.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet1.fs)]

<span data-ttu-id="a846f-142">Dane wyjściowe są następujące:</span><span class="sxs-lookup"><span data-stu-id="a846f-142">The output is as follows.</span></span>

```
Top-level context

(Negative number) Zero 1 2 3 Done!
```

<span data-ttu-id="a846f-143">Po przerwaniu długie wiersze kontynuacji wiersza musi wcięty między konstrukcja otaczającej.</span><span class="sxs-lookup"><span data-stu-id="a846f-143">When you break long lines, the continuation of the line must be indented farther than the enclosing construct.</span></span> <span data-ttu-id="a846f-144">Na przykład argumenty funkcji muszą wcięty między pierwszym znakiem nazwy funkcji, jak pokazano w poniższym kodzie.</span><span class="sxs-lookup"><span data-stu-id="a846f-144">For example, function arguments must be indented farther than the first character of the function name, as shown in the following code.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet2.fs)]

<span data-ttu-id="a846f-145">Istnieją wyjątki od tych reguł, zgodnie z opisem w następnej sekcji.</span><span class="sxs-lookup"><span data-stu-id="a846f-145">There are exceptions to these rules, as described in the next section.</span></span>


## <a name="indentation-in-modules"></a><span data-ttu-id="a846f-146">Wcięcia w modułach</span><span class="sxs-lookup"><span data-stu-id="a846f-146">Indentation in Modules</span></span>
<span data-ttu-id="a846f-147">Kod w lokalnym module musi być wcięty względem modułu, ale kod w module najwyższego poziomu nie musi być wcięta.</span><span class="sxs-lookup"><span data-stu-id="a846f-147">Code in a local module must be indented relative to the module, but code in a top-level module does not have to be indented.</span></span> <span data-ttu-id="a846f-148">Nie masz elementy Namespace wcięcia.</span><span class="sxs-lookup"><span data-stu-id="a846f-148">Namespace elements do not have to be indented.</span></span>

<span data-ttu-id="a846f-149">W poniższych przykładach kodu ilustrację.</span><span class="sxs-lookup"><span data-stu-id="a846f-149">The following code examples illustrate this.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet3.fs)]
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet4.fs)]

<span data-ttu-id="a846f-150">Aby uzyskać więcej informacji, zobacz [modułów](modules.md).</span><span class="sxs-lookup"><span data-stu-id="a846f-150">For more information, see [Modules](modules.md).</span></span>


## <a name="exceptions-to-the-basic-indentation-rules"></a><span data-ttu-id="a846f-151">Wyjątki reguł wcięcia podstawowe</span><span class="sxs-lookup"><span data-stu-id="a846f-151">Exceptions to the Basic Indentation Rules</span></span>
<span data-ttu-id="a846f-152">Zasady, zgodnie z opisem w poprzedniej sekcji, to czy kod w wielowierszowym konstrukcje musi być wcięty względem wcięcie pierwszego wiersza konstrukcji i koniec konstrukcja jest określany przez podczas pierwszego wiersza poza.</span><span class="sxs-lookup"><span data-stu-id="a846f-152">The general rule, as described in the previous section, is that code in multiline constructs must be indented relative to the indentation of the first line of the construct, and that the end of the construct is determined by when the first offside line occurs.</span></span> <span data-ttu-id="a846f-153">Wyjątek od reguł dotyczących gdy end sytuacjach jest, że niektóre konstrukcje, takich jak `try...with` wyrażenia `if...then...else` wyrażenie i stosowania `and` Składnia deklaracji wzajemnie funkcje rekursywne lub typów, ma wiele części.</span><span class="sxs-lookup"><span data-stu-id="a846f-153">An exception to the rule about when contexts end is that some constructs, such as the `try...with` expression, the `if...then...else` expression, and the use of `and` syntax for declaring mutually recursive functions or types, have multiple parts.</span></span> <span data-ttu-id="a846f-154">Wcięcie nowsze składniki, takie jak `then` i `else` w `if...then...else` wyrażenie w tym samym poziomie jako token, który rozpoczyna się wyrażenie, ale zamiast wskazujący punktu końcowego do kontekstu, reprezentuje następnej części tego samego kontekstu.</span><span class="sxs-lookup"><span data-stu-id="a846f-154">You indent the later parts, such as `then` and `else` in an `if...then...else` expression, at the same level as the token that starts the expression, but instead of indicating an end to the context, it represents the next part of the same context.</span></span> <span data-ttu-id="a846f-155">W związku z tym `if...then...else` wyrażenie można napisać tak jak w poniższym przykładzie kodu.</span><span class="sxs-lookup"><span data-stu-id="a846f-155">Therefore, an `if...then...else` expression can be written as in the following code example.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet5.fs)]

<span data-ttu-id="a846f-156">Wyjątek poza reguły dotyczą tylko `then` i `else` słów kluczowych.</span><span class="sxs-lookup"><span data-stu-id="a846f-156">The exception to the offside rule applies only to the `then` and `else` keywords.</span></span> <span data-ttu-id="a846f-157">W związku z tym mimo że nie jest błąd wcięcia `then` i `else` Ponadto nieudanym wcięcie wierszy kodu w `then` bloku generuje ostrzeżenie.</span><span class="sxs-lookup"><span data-stu-id="a846f-157">Therefore, although it is not an error to indent the `then` and `else` further, failing to indent the lines of code in a `then` block produces a warning.</span></span> <span data-ttu-id="a846f-158">Jest to zilustrowane w następujących wierszach kodu.</span><span class="sxs-lookup"><span data-stu-id="a846f-158">This is illustrated in the following lines of code.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet6.fs)]
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet7.fs)]

<span data-ttu-id="a846f-159">Dla kodu `else` dotyczy blok, dodatkowe reguły specjalne.</span><span class="sxs-lookup"><span data-stu-id="a846f-159">For code in an `else` block, an additional special rule applies.</span></span> <span data-ttu-id="a846f-160">Ostrzeżenie w poprzednim przykładzie występuje tylko w kodzie w `then` bloku, a nie na kod w `else` bloku.</span><span class="sxs-lookup"><span data-stu-id="a846f-160">The warning in the previous example occurs only on the code in the `then` block, not on the code in the `else` block.</span></span> <span data-ttu-id="a846f-161">Dzięki temu można napisać kod, który sprawdza bez wymuszania reszty kod funkcję, która może znajdować się w różnych warunków na początku funkcji `else` bloku wcięcia.</span><span class="sxs-lookup"><span data-stu-id="a846f-161">This allows you to write code that checks for various conditions at the beginning of a function without forcing the rest of the code for the function, which might be in an `else` block, to be indented.</span></span> <span data-ttu-id="a846f-162">W związku z tym można napisać następujące bez tworzenia ostrzeżenie.</span><span class="sxs-lookup"><span data-stu-id="a846f-162">Thus, you can write the following without producing a warning.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet8.fs)]

<span data-ttu-id="a846f-163">Kolejny wyjątek do reguły, który zakończy się kontekst wiersza nie jest wcięty, ile poprzedniego wiersza jest infiksu operatorów, takich jak `+` i `|>`.</span><span class="sxs-lookup"><span data-stu-id="a846f-163">Another exception to the rule that contexts end when a line is not indented as far as a previous line is for infix operators, such as `+` and `|>`.</span></span> <span data-ttu-id="a846f-164">Wiersze rozpoczynające się infiksu operatory są dozwolone, aby rozpocząć `(1 + oplength)` kolumn przed normalnej pozycji bez wyzwalania punktu końcowego do kontekstu, gdzie `oplength` jest liczba znaków, które tworzą operatora.</span><span class="sxs-lookup"><span data-stu-id="a846f-164">Lines that start with infix operators are permitted to begin `(1 + oplength)` columns before the normal position without triggering an end to the context, where `oplength` is the number of characters that make up the operator.</span></span> <span data-ttu-id="a846f-165">Powoduje to pierwszy token po operatorze, aby były wyrównane z poprzedniego wiersza.</span><span class="sxs-lookup"><span data-stu-id="a846f-165">This causes the first token after the operator to align with the previous line.</span></span>

<span data-ttu-id="a846f-166">Na przykład w poniższym kodzie `+` symboli może być wcięta dwie kolumny krótszym niż poprzedniego wiersza.</span><span class="sxs-lookup"><span data-stu-id="a846f-166">For example, in the following code, the `+` symbol is permitted to be indented two columns less than the previous line.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet9.fs)]

<span data-ttu-id="a846f-167">Chociaż wcięcia zwykle zwiększa się wraz ze wzrostem wyższy poziom zagnieżdżenia, istnieje kilka konstrukcji, w których kompilator pozwala na zresetowanie wcięcie do dolnej położenie kolumny.</span><span class="sxs-lookup"><span data-stu-id="a846f-167">Although indentation usually increases as the level of nesting becomes higher, there are several constructs in which the compiler allows you to reset the indentation to a lower column position.</span></span>

<span data-ttu-id="a846f-168">Konstrukcje, które zezwala na resetowanie położenia kolumny są następujące:</span><span class="sxs-lookup"><span data-stu-id="a846f-168">The constructs that permit a reset of column position are as follows:</span></span>


- <span data-ttu-id="a846f-169">Treść funkcji anonimowych.</span><span class="sxs-lookup"><span data-stu-id="a846f-169">Bodies of anonymous functions.</span></span> <span data-ttu-id="a846f-170">W poniższym kodzie wydruku wyrażenie rozpoczyna się od pozycji w kolumnie, która jest dostępne na lewo od `fun` — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="a846f-170">In the following code, the print expression starts at a column position that is farther to the left than the `fun` keyword.</span></span> <span data-ttu-id="a846f-171">Jednak wiersz nie może rozpoczynać się w kolumnie z lewej strony początek poprzedniego poziomu wcięcia (to znaczy po lewej `L` w `List`).</span><span class="sxs-lookup"><span data-stu-id="a846f-171">However, the line must not start at a column to the left of the start of the previous indentation level (that is, to the left of the `L` in `List`).</span></span>
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet10.fs)]

- <span data-ttu-id="a846f-172">Konstrukcje ujęta w nawiasy lub przez `begin` i `end` w `then` lub `else` zablokować z `if...then...else` wyrażenia, pod warunkiem wcięcie nie jest nie mniejszy niż pozycja kolumny `if` — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="a846f-172">Constructs enclosed by parentheses or by `begin` and `end` in a `then` or `else` block of an `if...then...else` expression, provided the indentation is no less than the column position of the `if` keyword.</span></span> <span data-ttu-id="a846f-173">Ten wyjątek umożliwia stylu kodowania, w którym nawias otwierający lub `begin` jest używany na końcu wiersza po `then` lub `else`.</span><span class="sxs-lookup"><span data-stu-id="a846f-173">This exception allows for a coding style in which an opening parenthesis or `begin` is used at the end of a line after `then` or `else`.</span></span>


- <span data-ttu-id="a846f-174">Treści modułów, klasy, interfejsy i struktur rozdzielone `begin...end`, `{...}`, `class...end`, lub `interface...end`.</span><span class="sxs-lookup"><span data-stu-id="a846f-174">Bodies of modules, classes, interfaces, and structures delimited by `begin...end`, `{...}`, `class...end`, or `interface...end`.</span></span> <span data-ttu-id="a846f-175">Dzięki temu stylu, w którym kluczowego otwarcia definicji typu może być na tym samym wierszu co nazwa typu bez wymuszania całej treści być wcięta między otwierania — słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="a846f-175">This allows for a style in which the opening keyword of a type definition can be on the same line as the type name without forcing the whole body to be indented farther than the opening keyword.</span></span>
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet13.fs)]


## <a name="see-also"></a><span data-ttu-id="a846f-176">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="a846f-176">See Also</span></span>
[<span data-ttu-id="a846f-177">Dokumentacja języka F#</span><span class="sxs-lookup"><span data-stu-id="a846f-177">F# Language Reference</span></span>](index.md)
