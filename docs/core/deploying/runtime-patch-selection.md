---
title: Środowisko uruchomieniowe przenoszenia do przodu dla platformy .NET Core aplikację samodzielną wdrożeń.
description: Dowiedz się więcej o dotnet publikowania zmian w przypadku wdrożeń niezależna.
author: KathleenDollard
ms.date: 05/31/2018
ms.custom: seodec18
ms.openlocfilehash: 9af1454ede03b277f9b1a10e1d99a997e38809ea
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/23/2019
ms.locfileid: "61613563"
---
# <a name="self-contained-deployment-runtime-roll-forward"></a><span data-ttu-id="e9e4c-103">Niezależne wdrożenia środowiska uruchomieniowego przenoszenia do przodu</span><span class="sxs-lookup"><span data-stu-id="e9e4c-103">Self-contained deployment runtime roll forward</span></span>

<span data-ttu-id="e9e4c-104">.NET core [niezależna acji](index.md) biblioteki .NET Core i środowisko uruchomieniowe platformy .NET Core.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-104">.NET Core [self-contained application deployments](index.md) include both the .NET Core libraries and the .NET Core runtime.</span></span> <span data-ttu-id="e9e4c-105">Począwszy od .NET Core 2.1 SDK (wersja 2.1.300), wdrożenie aplikacji niezależna [publikuje najwyższy runtime poprawki na swojej maszynie](https://github.com/dotnet/designs/pull/36).</span><span class="sxs-lookup"><span data-stu-id="e9e4c-105">Starting in .NET Core 2.1 SDK (version 2.1.300), a self-contained application deployment [publishes the highest patch runtime on your machine](https://github.com/dotnet/designs/pull/36).</span></span> <span data-ttu-id="e9e4c-106">Domyślnie [ `dotnet publish` ](../tools/dotnet-publish.md) dla wdrożenia niezależna wybiera najnowszej wersji, instalowany jako część zestawu SDK na maszynie publikowania.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-106">By default, [`dotnet publish`](../tools/dotnet-publish.md) for a self-contained deployment selects the latest version installed as part of the SDK on the publishing machine.</span></span> <span data-ttu-id="e9e4c-107">Dzięki temu Twojej wdrożonej aplikacji do uruchamiania przy użyciu poprawek zabezpieczeń (i inne poprawki) dostępnych w trakcie `publish`.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-107">This enables your deployed application to run with security fixes (and other fixes) available during `publish`.</span></span> <span data-ttu-id="e9e4c-108">Aplikacja musi być opublikowany ponownie w celu uzyskania nowych poprawek.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-108">The application must be re-published to obtain a new patch.</span></span> <span data-ttu-id="e9e4c-109">Aplikacje samodzielne są tworzone przez określenie `-r <RID>` na `dotnet publish` polecenia lub określając [identyfikator środowiska uruchomieniowego (RID)](../rid-catalog.md) w pliku projektu (csproj / vbproj) lub w wierszu polecenia.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-109">Self-contained applications are created by specifying `-r <RID>` on the `dotnet publish` command or by specifying the [runtime identifier (RID)](../rid-catalog.md) in the project file (csproj / vbproj) or on the command line.</span></span>

## <a name="patch-version-roll-forward-overview"></a><span data-ttu-id="e9e4c-110">Omówienie do przodu roll wersja poprawki</span><span class="sxs-lookup"><span data-stu-id="e9e4c-110">Patch version roll forward overview</span></span>

<span data-ttu-id="e9e4c-111">[`restore`](../tools/dotnet-restore.md), [ `build` ](../tools/dotnet-build.md) i [ `publish` ](../tools/dotnet-publish.md) są `dotnet` poleceń, które można uruchomić osobno.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-111">[`restore`](../tools/dotnet-restore.md), [`build`](../tools/dotnet-build.md) and [`publish`](../tools/dotnet-publish.md) are `dotnet` commands that can run separately.</span></span> <span data-ttu-id="e9e4c-112">Wybór środowiska uruchomieniowego jest częścią `restore` operacji nie `publish` lub `build`.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-112">The runtime choice is part of the `restore` operation, not `publish` or `build`.</span></span> <span data-ttu-id="e9e4c-113">Jeśli wywołasz `publish`, najnowsza wersja poprawki zostanie wybrany.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-113">If you call `publish`, the latest patch version will be chosen.</span></span> <span data-ttu-id="e9e4c-114">Jeśli wywołasz `publish` z `--no-restore` argumentu, możesz nie zawierają wersji poprawki żądaną ponieważ wcześniej `restore` nie mogło zostać wykonane za pomocą nowej aplikacji niezależna publikowania zasad.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-114">If you call `publish` with the `--no-restore` argument, then you may not get the desired patch version because a prior `restore` may not have been executed with the new self-contained application publishing policy.</span></span> <span data-ttu-id="e9e4c-115">W tym przypadku błąd kompilacji jest generowany z tekstem podobny do następującego:</span><span class="sxs-lookup"><span data-stu-id="e9e4c-115">In this case, a build error is generated with text similar to the following:</span></span>

  <span data-ttu-id="e9e4c-116">"Projektu zostało przywrócone, za pomocą wersji 2.0.0 pakietów Microsoft.NETCore.App, ale z bieżącymi ustawieniami wersji 2.0.6 będzie używana zamiast tego.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-116">"The project was restored using Microsoft.NETCore.App version 2.0.0, but with current settings, version 2.0.6 would be used instead.</span></span> <span data-ttu-id="e9e4c-117">Aby rozwiązać ten problem, upewnij się, że te same ustawienia są stosowane do przywracania i kolejne operacje, takie jak kompilacja lub opublikować.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-117">To resolve this issue, make sure the same settings are used for restore and for subsequent operations such as build or publish.</span></span> <span data-ttu-id="e9e4c-118">Zazwyczaj ten problem może wystąpić, jeśli RuntimeIdentifier zostaje ustalona podczas tworzenia lub opublikować, ale nie podczas przywracania."</span><span class="sxs-lookup"><span data-stu-id="e9e4c-118">Typically this issue can occur if the RuntimeIdentifier property is set during build or publish but not during restore."</span></span>

> [!NOTE]
> <span data-ttu-id="e9e4c-119">`restore` i `build` mogą być uruchamiane niejawnie jako część innego polecenia, takie jak `publish`.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-119">`restore` and `build` can be run implicitly as part of another command, like `publish`.</span></span> <span data-ttu-id="e9e4c-120">Po uruchomieniu niejawnie jako część innego polecenia, są one wyposażone dodatkowy kontekst, aby odpowiednie artefakty są tworzone.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-120">When run implicitly as part of another command, they are provided with additional context so that the right artifacts are produced.</span></span> <span data-ttu-id="e9e4c-121">Gdy możesz `publish` aparatu plików wykonywalnych (na przykład `dotnet publish -r linux-x64`), niejawny `restore` przywraca pakietów dla środowiska uruchomieniowego x64 systemu linux.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-121">When you `publish` with a runtime (for example, `dotnet publish -r linux-x64`), the implicit `restore` restores packages for the linux-x64 runtime.</span></span> <span data-ttu-id="e9e4c-122">Jeśli wywołasz `restore` jawnie, nie są przywracane pakiety środowiska uruchomieniowego domyślnie, ponieważ nie ma tego kontekstu.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-122">If you call `restore` explicitly, it does not restore runtime packages by default, because it doesn't have that context.</span></span>

## <a name="how-to-avoid-restore-during-publish"></a><span data-ttu-id="e9e4c-123">Jak unikać przywracania podczas publikowania</span><span class="sxs-lookup"><span data-stu-id="e9e4c-123">How to avoid restore during publish</span></span>

<span data-ttu-id="e9e4c-124">Uruchamianie `restore` jako część `publish` operacji może być niepożądane dla danego scenariusza.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-124">Running `restore` as part of the `publish` operation may be undesirable for your scenario.</span></span> <span data-ttu-id="e9e4c-125">Aby uniknąć `restore` podczas `publish` podczas tworzenia aplikacji niezależna, wykonaj następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="e9e4c-125">To avoid `restore` during `publish` while creating self-contained applications, do the following:</span></span>

* <span data-ttu-id="e9e4c-126">Ustaw `RuntimeIdentifiers` właściwość rozdzieloną średnikami listę wszystkich [RID](../rid-catalog.md) do opublikowania.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-126">Set the `RuntimeIdentifiers` property to a semicolon-separated list of all the [RIDs](../rid-catalog.md) to be published.</span></span>
* <span data-ttu-id="e9e4c-127">Ustaw `TargetLatestRuntimePatch` właściwość `true`.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-127">Set the `TargetLatestRuntimePatch` property to `true`.</span></span>

## <a name="no-restore-argument-with-dotnet-publish-options"></a><span data-ttu-id="e9e4c-128">Opcje publikowania argument bez przywracania za pomocą polecenia dotnet</span><span class="sxs-lookup"><span data-stu-id="e9e4c-128">No-restore argument with dotnet publish options</span></span>

<span data-ttu-id="e9e4c-129">Jeśli chcesz tworzyć aplikacje zarówno niezależna i [zależny od struktury aplikacji](index.md) przy użyciu tego samego pliku projektu, i chcesz używać `--no-restore` argumentu `dotnet publish`, a następnie wybierz jedno z następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="e9e4c-129">If you want to create both self-contained applications and [framework-dependent applications](index.md) with the same project file, and you want to use the `--no-restore` argument with `dotnet publish`, then choose one of the following:</span></span>

1. <span data-ttu-id="e9e4c-130">Preferuj zachowań zależnych od framework.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-130">Prefer the framework-dependent behavior.</span></span> <span data-ttu-id="e9e4c-131">Jeśli aplikacja jest zależny od struktury, jest to zachowanie domyślne.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-131">If the application is framework-dependent, this is the default behavior.</span></span> <span data-ttu-id="e9e4c-132">Jeśli aplikacja jest niezależna i można używać bez 2.1.0 lokalne środowisko uruchomieniowe, ustaw `TargetLatestRuntimePatch` do `false` w pliku projektu.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-132">If the application is self-contained, and can use an unpatched 2.1.0 local runtime, set the `TargetLatestRuntimePatch` to `false` in the project file.</span></span>

2. <span data-ttu-id="e9e4c-133">Preferuj niezależna zachowanie.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-133">Prefer the self-contained behavior.</span></span> <span data-ttu-id="e9e4c-134">Jeśli aplikacja jest niezależny, jest to zachowanie domyślne.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-134">If the application is self-contained, this is the default behavior.</span></span> <span data-ttu-id="e9e4c-135">Jeśli aplikacja jest zależny od struktury i wymaga Najnowsza zainstalowana poprawka, ustaw `TargetLatestRuntimePatch` do `true` w pliku projektu.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-135">If the application is framework-dependent, and requires the latest patch installed, set `TargetLatestRuntimePatch` to `true` in the project file.</span></span>

3. <span data-ttu-id="e9e4c-136">Wykonaj jawną kontrolę framework w wersji środowiska uruchomieniowego, ustawiając `RuntimeFrameworkVersion` do wersji poprawki określonych w pliku projektu.</span><span class="sxs-lookup"><span data-stu-id="e9e4c-136">Take explicit control of the runtime framework version by setting `RuntimeFrameworkVersion` to the specific patch version in the project file.</span></span>
