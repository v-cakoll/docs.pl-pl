---
title: Ścieżka formatów plików w systemie Windows
ms.date: 06/28/2018
ms.technology: dotnet-standard
ms.topic: article
helpviewer_keywords:
- I/O, long paths
- long paths
- path formats, Windows
author: rpetrusha
ms.author: ronpet
ms.workload:
- dotnet
- dotnetcore
ms.openlocfilehash: a5fccf5ea86469f14963fad8e7d2af0f7c68d2df
ms.sourcegitcommit: 979597cd8055534b63d2c6ee8322938a27d0c87b
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/29/2018
ms.locfileid: "37106940"
---
# <a name="file-path-formats-on-windows-systems"></a><span data-ttu-id="fdc06-102">Ścieżka formatów plików w systemie Windows</span><span class="sxs-lookup"><span data-stu-id="fdc06-102">File path formats on Windows systems</span></span>

<span data-ttu-id="fdc06-103">Elementy członkowskie wielu typów w <xref:System.IO> obejmują przestrzeni nazw `path` parametr, który pozwala określić ścieżkę bezwzględną ani względną do zasobu systemu plików.</span><span class="sxs-lookup"><span data-stu-id="fdc06-103">Members of many of the types in the <xref:System.IO> namespace include a `path` parameter that lets you specify an absolute or relative path to a file system resource.</span></span> <span data-ttu-id="fdc06-104">Ta ścieżka są następnie przekazywane do [Windows plik systemowy interfejsów API](https://msdn.microsoft.com/library/windows/desktop/aa364407(v=vs.85).aspx).</span><span class="sxs-lookup"><span data-stu-id="fdc06-104">This path is then passed to [Windows file sysem APIs](https://msdn.microsoft.com/library/windows/desktop/aa364407(v=vs.85).aspx).</span></span> <span data-ttu-id="fdc06-105">W tym temacie opisano formaty ścieżki do plików używanych w systemach Windows.</span><span class="sxs-lookup"><span data-stu-id="fdc06-105">This topic discusses the formats for file paths that you can use on Windows systems.</span></span>

## <a name="traditional-dos-paths"></a><span data-ttu-id="fdc06-106">Tradycyjny ścieżki DOS</span><span class="sxs-lookup"><span data-stu-id="fdc06-106">Traditional DOS paths</span></span>

<span data-ttu-id="fdc06-107">Standardowa ścieżka DOS może składać się z trzech składników:</span><span class="sxs-lookup"><span data-stu-id="fdc06-107">A standard DOS path can consist of three components:</span></span>

- <span data-ttu-id="fdc06-108">Wolumin lub litery dysku, a następnie separatora woluminu (`:`).</span><span class="sxs-lookup"><span data-stu-id="fdc06-108">A volume or drive letter followed by the volume separator (`:`).</span></span>
- <span data-ttu-id="fdc06-109">Nazwa katalogu.</span><span class="sxs-lookup"><span data-stu-id="fdc06-109">A directory name.</span></span> <span data-ttu-id="fdc06-110">[Znakiem separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>) oddziela podkatalogów w ramach hierarchii katalogów zagnieżdżonych.</span><span class="sxs-lookup"><span data-stu-id="fdc06-110">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates subdirectories within the nested directory hierarchy.</span></span>
- <span data-ttu-id="fdc06-111">Opcjonalna nazwa pliku.</span><span class="sxs-lookup"><span data-stu-id="fdc06-111">An optional filename.</span></span> <span data-ttu-id="fdc06-112">[Znakiem separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>) oddziela ścieżkę pliku i nazwa pliku.</span><span class="sxs-lookup"><span data-stu-id="fdc06-112">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates the file path and the filename.</span></span>

<span data-ttu-id="fdc06-113">W przypadku wszystkich trzech komponentów ścieżka jest bezwzględna.</span><span class="sxs-lookup"><span data-stu-id="fdc06-113">If all three components are present, the path is absolute.</span></span> <span data-ttu-id="fdc06-114">Jeśli określono nie woluminu lub litery dysku i rozpoczyna się od nazwy katalogów [znakiem separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>), ścieżka jest względna z katalogu głównego bieżącego dysku.</span><span class="sxs-lookup"><span data-stu-id="fdc06-114">If no volume or drive letter is specified and the directory names begins with the [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>), the path is relative from the root of the current drive.</span></span> <span data-ttu-id="fdc06-115">W przeciwnym razie ścieżka jest powiązane z bieżącym katalogiem.</span><span class="sxs-lookup"><span data-stu-id="fdc06-115">Otherwise, the path is relative to the current directory.</span></span> <span data-ttu-id="fdc06-116">W poniższej tabeli przedstawiono niektóre możliwe katalogu i ścieżki plików.</span><span class="sxs-lookup"><span data-stu-id="fdc06-116">The following table shows some possible directory and file paths.</span></span>

|<span data-ttu-id="fdc06-117">Ścieżka</span><span class="sxs-lookup"><span data-stu-id="fdc06-117">Path</span></span>  |<span data-ttu-id="fdc06-118">Opis</span><span class="sxs-lookup"><span data-stu-id="fdc06-118">Description</span></span>  |
| -- | -- |
| `C:\Documents\Newsletters\Summer2018.pdf` | <span data-ttu-id="fdc06-119">Ścieżka bezwzględna do pliku w folderze głównym dysku C:</span><span class="sxs-lookup"><span data-stu-id="fdc06-119">An absolute file path from the root of drive C:</span></span> |
| `\Program Files\Custom Utilities\StringFinder.exe` | <span data-ttu-id="fdc06-120">Ścieżka bezwzględna od głównego bieżącego dysku.</span><span class="sxs-lookup"><span data-stu-id="fdc06-120">An absolute path from the root of the current drive.</span></span> |
| `2018\January.xlsx` | <span data-ttu-id="fdc06-121">Ścieżka względna do pliku w podkatalogu bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="fdc06-121">A relative path to a file in a subdirectory of the current directory.</span></span> |
| `..\Publications\TravelBrochure.pdf` | <span data-ttu-id="fdc06-122">Ścieżka względna do pliku w katalogu, w którym jest element równorzędny bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="fdc06-122">A relative path to file in a directory that is a peer of the current directory.</span></span> |
| `C:\Projects\apilibrary\apilibrary.sln` | <span data-ttu-id="fdc06-123">Ścieżka bezwzględna do pliku w folderze głównym dysku C:</span><span class="sxs-lookup"><span data-stu-id="fdc06-123">An absolute path to a file from the root of drive C:</span></span> |
| `C:Projects\apilibrary\apilibrary.sln` | <span data-ttu-id="fdc06-124">Ścieżka względna od katalogu bieżącego dysku C:.</span><span class="sxs-lookup"><span data-stu-id="fdc06-124">A relative path from the current directory of the C: drive.</span></span> |

> [!IMPORTANT]
> <span data-ttu-id="fdc06-125">Należy pamiętać, to różnica między ostatnich dwóch ścieżek.</span><span class="sxs-lookup"><span data-stu-id="fdc06-125">Note the difference between the last two paths.</span></span> <span data-ttu-id="fdc06-126">Zarówno określić opcjonalne woluminu specyfikator (C: w obu przypadkach), ale pierwszy rozpoczyna się od katalogu głównego określonego woluminu, podczas gdy druga nie.</span><span class="sxs-lookup"><span data-stu-id="fdc06-126">Both specify the optional volume specifier (C: in both cases), but the first begins with the root of the specified volume, whereas the second does not.</span></span> <span data-ttu-id="fdc06-127">W wyniku pierwszy jest ścieżką bezwzględną z katalogu głównego dysku C:, drugi jest ścieżką względną z bieżącego katalogu dysku C:.</span><span class="sxs-lookup"><span data-stu-id="fdc06-127">As result, the first is an absolute path from the root directory of drive C:, whereas the second is a relative path from the current directory of drive C:.</span></span> <span data-ttu-id="fdc06-128">Użycie drugiego formularza po pierwszym jest przeznaczona jest wspólne źródło usterki, które obejmują ścieżki plików systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="fdc06-128">Use of the second form when the first is intended is a common source of bugs that involve Windows file paths.</span></span>

<span data-ttu-id="fdc06-129">Można określić, czy ścieżka pliku jest w pełni kwalifikowana (oznacza to, że ścieżka jest niezależne od bieżącego katalogu i nie ulega zmianie, gdy zmienia się bieżący katalog) przez wywołanie metody <xref:System.IO.Path.IsPathFullyQualified%2A?displayProperty=nameWthType> metody.</span><span class="sxs-lookup"><span data-stu-id="fdc06-129">You can determine whether a file path is fully qualified (that is, it the path is independent of the current directory and does not change when the current directory changes) by calling the <xref:System.IO.Path.IsPathFullyQualified%2A?displayProperty=nameWthType> method.</span></span> <span data-ttu-id="fdc06-130">Należy pamiętać, że takie ścieżki może zawierać segmentów względna katalogu (`.` i `..`) i nadal być w pełni kwalifikowana Jeśli rozpoznana ścieżka wskazuje zawsze tej samej lokalizacji.</span><span class="sxs-lookup"><span data-stu-id="fdc06-130">Note that such a path can include relative directory segments (`.` and `..`) and still be fully qualified if the resolved path always points to the same location.</span></span>

<span data-ttu-id="fdc06-131">Poniższy przykład przedstawia różnicę między ścieżki względne i bezwzględne.</span><span class="sxs-lookup"><span data-stu-id="fdc06-131">The following example illustrates the difference between absolute and relative paths.</span></span> <span data-ttu-id="fdc06-132">Przyjęto założenie, że istnieje katalog D:\FY2018\ i nie ustawienie bieżącej wykazu dla D:\ w wierszu polecenia przed uruchomieniem w przykładzie.</span><span class="sxs-lookup"><span data-stu-id="fdc06-132">It assumes that the directory D:\FY2018\ exists, and that you haven't set any curent directory for D:\ from the command prompt before running the example.</span></span>

[!code-csharp[absolute-and-relative-paths](~/samples/snippets/standard/io/file-names/cs/paths.cs)]
[!code-vb[absolute-and-relative-paths](~/samples/snippets/standard/io/file-names/vb/paths.vb)]

## <a name="unc-paths"></a><span data-ttu-id="fdc06-133">Ścieżki UNC</span><span class="sxs-lookup"><span data-stu-id="fdc06-133">UNC paths</span></span>

<span data-ttu-id="fdc06-134">Universal naming convention (UNC) ścieżki, które są używane do dostępu do zasobów sieciowych, mają następujący format:</span><span class="sxs-lookup"><span data-stu-id="fdc06-134">Universal naming convention (UNC) paths, which are used to access network resources, have the following format:</span></span>

- <span data-ttu-id="fdc06-135">Serwera lub nazwę hosta, który jest poprzedzone \\ \\.</span><span class="sxs-lookup"><span data-stu-id="fdc06-135">A server or host name, which is prefaced by \\\\.</span></span> <span data-ttu-id="fdc06-136">Nazwa serwera może być nazwą NetBIOS komputera lub adres IP/nazwa FQDN (IPv4 i IPv6 są obsługiwane).</span><span class="sxs-lookup"><span data-stu-id="fdc06-136">The server name can be a NetBIOS machine name or an IP/FQDN address (IPv4 as well as v6 are supported).</span></span>
- <span data-ttu-id="fdc06-137">Nazwa udziału jest oddzielona od nazwy hosta przez \\.</span><span class="sxs-lookup"><span data-stu-id="fdc06-137">A share name, which is separated from the host name by \\.</span></span> <span data-ttu-id="fdc06-138">Serwer i nazwę udziału składają się woluminu.</span><span class="sxs-lookup"><span data-stu-id="fdc06-138">Together, the server and share name make up the volume.</span></span>
- <span data-ttu-id="fdc06-139">Nazwa katalogu.</span><span class="sxs-lookup"><span data-stu-id="fdc06-139">A directory name.</span></span> <span data-ttu-id="fdc06-140">[Znakiem separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>) oddziela podkatalogów w ramach hierarchii katalogów zagnieżdżonych.</span><span class="sxs-lookup"><span data-stu-id="fdc06-140">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates subdirectories within the nested directory hierarchy.</span></span>
- <span data-ttu-id="fdc06-141">Opcjonalna nazwa pliku.</span><span class="sxs-lookup"><span data-stu-id="fdc06-141">An optional filename.</span></span> <span data-ttu-id="fdc06-142">[Znakiem separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>) oddziela ścieżkę pliku i nazwa pliku.</span><span class="sxs-lookup"><span data-stu-id="fdc06-142">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates the file path and the filename.</span></span>

<span data-ttu-id="fdc06-143">Oto kilka przykładów ścieżki UNC:</span><span class="sxs-lookup"><span data-stu-id="fdc06-143">The following are some examples of UNC paths:</span></span>

|<span data-ttu-id="fdc06-144">Ścieżka</span><span class="sxs-lookup"><span data-stu-id="fdc06-144">Path</span></span>  |<span data-ttu-id="fdc06-145">Opis</span><span class="sxs-lookup"><span data-stu-id="fdc06-145">Description</span></span>  |
| -- | -- |
| `\\system07\C$\` | <span data-ttu-id="fdc06-146">Katalog główny dysku c: dysków na `system07`.</span><span class="sxs-lookup"><span data-stu-id="fdc06-146">The root directory of the C: drive on `system07`.</span></span> |
| `\\Server2\Share\Test\Foo.txt` | <span data-ttu-id="fdc06-147">Plik Foo.txt w katalogu testu \\ \\Serwer2\\udział woluminu.</span><span class="sxs-lookup"><span data-stu-id="fdc06-147">The Foo.txt file in the Test directory of the \\\\Server2\\Share volume.</span></span>|

<span data-ttu-id="fdc06-148">Ścieżki UNC zawsze musi być w pełni kwalifikowana.</span><span class="sxs-lookup"><span data-stu-id="fdc06-148">UNC paths must always be fully qualified.</span></span> <span data-ttu-id="fdc06-149">Obejmują one segmentów względna katalogu (`.` i `..`), ale musi to być część w pełni kwalifikowaną ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="fdc06-149">They can include relative directory segments (`.` and `..`), but these must be part of a fully qualified path.</span></span> <span data-ttu-id="fdc06-150">Używając ścieżek względnych tylko przez mapowanie ścieżką UNC do litery dysku.</span><span class="sxs-lookup"><span data-stu-id="fdc06-150">You can use relative paths only by mapping a UNC path to a drive letter.</span></span>

## <a name="dos-device-paths"></a><span data-ttu-id="fdc06-151">Ścieżki urządzenia systemu DOS</span><span class="sxs-lookup"><span data-stu-id="fdc06-151">DOS device paths</span></span>

<span data-ttu-id="fdc06-152">System operacyjny Windows ma ujednoliconego obiektu modelu, który wskazuje wszystkie zasoby, w tym pliki.</span><span class="sxs-lookup"><span data-stu-id="fdc06-152">The Windows operating system has a unified object model that points to all resources, including files.</span></span> <span data-ttu-id="fdc06-153">Te ścieżki obiektu są dostępne z okna konsoli i są widoczne dla warstwy Win32 za pomocą specjalnego folderu linki symboliczne zmapowane do starszej wersji ścieżki DOS i UNC.</span><span class="sxs-lookup"><span data-stu-id="fdc06-153">These object paths are accessible from the console window and are exposed to the Win32 layer through a special folder of symbolic links that legacy DOS and UNC paths are mapped to.</span></span> <span data-ttu-id="fdc06-154">Ten folder specjalny jest dostępny za pośrednictwem składnia ścieżki DOS urządzenia, które jest jednym z:</span><span class="sxs-lookup"><span data-stu-id="fdc06-154">This special folder is accessed via the DOS device path syntax, which is one of:</span></span>

`\\.\C:\Test\Foo.txt`  
`\\?\C:\Test\Foo.txt`

> [!NOTE]
> <span data-ttu-id="fdc06-155">Składnia ścieżki urządzenia systemu DOS jest obsługiwana w implementacji .NET działających w systemie Windows począwszy od programu .NET Core 1.1 i .NET Framework 4.6.2.</span><span class="sxs-lookup"><span data-stu-id="fdc06-155">DOS device path syntax is supported on .NET implementations running on Windows starting with .NET Core 1.1 and .NET Framework 4.6.2.</span></span>

<span data-ttu-id="fdc06-156">Ścieżka urządzenia systemu DOS składa się z następujących składników:</span><span class="sxs-lookup"><span data-stu-id="fdc06-156">The DOS device path consists of the following components:</span></span>

- <span data-ttu-id="fdc06-157">Specyfikator ścieżka urządzenia (`\\.\` lub `\\?\`), który identyfikuje ścieżkę jako ścieżka urządzenia systemu DOS.</span><span class="sxs-lookup"><span data-stu-id="fdc06-157">The device path specifier (`\\.\` or `\\?\`), which identifies the path as a DOS device path.</span></span>

   > [!NOTE]
   > <span data-ttu-id="fdc06-158">`\\?\` Jest obsługiwane we wszystkich wersjach programu .NET Core i w programie .NET Framework, począwszy od wersji 4.6.2.</span><span class="sxs-lookup"><span data-stu-id="fdc06-158">The `\\?\` is supported in all versions of .NET Core and in the .NET Framework starting with version 4.6.2.</span></span>
   
- <span data-ttu-id="fdc06-159">Łącze symboliczne do obiektu urządzenia "rzeczywiste" (C: w tym przypadku).</span><span class="sxs-lookup"><span data-stu-id="fdc06-159">A symbolic link to the "real" device object (C: in this case).</span></span>

   <span data-ttu-id="fdc06-160">Pierwszy segment ścieżki urządzenia systemu DOS po specyfikatorze ścieżka urządzenia identyfikuje woluminu lub dysku.</span><span class="sxs-lookup"><span data-stu-id="fdc06-160">The first segment of the DOS device path after the device path specifier identifies the volume or drive.</span></span> <span data-ttu-id="fdc06-161">(Na przykład `\\?\C:\` i `\\.\BootPartition\`.)</span><span class="sxs-lookup"><span data-stu-id="fdc06-161">(For example, `\\?\C:\` and `\\.\BootPartition\`.)</span></span>

   <span data-ttu-id="fdc06-162">Ma określonego łącza UNC, które jest wywołane nie zaskakująco `UNC`.</span><span class="sxs-lookup"><span data-stu-id="fdc06-162">There is a specific link for UNCs that is called, not surprisingly, `UNC`.</span></span> <span data-ttu-id="fdc06-163">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="fdc06-163">For example:</span></span>

      `\\.\UNC\Server\Share\Test\Foo.txt`
      `\\?\UNC\Server\Share\Test\Foo.txt`

    <span data-ttu-id="fdc06-164">Dla urządzenia UNC część serwera i udostępniania jest formularze woluminu.</span><span class="sxs-lookup"><span data-stu-id="fdc06-164">For device UNCs, the server/share portion is forms the volume.</span></span> <span data-ttu-id="fdc06-165">Na przykład w `\\?\server1\e:\utilities\\filecomparer\`, część serwera i udostępniania jest server1\utilities.</span><span class="sxs-lookup"><span data-stu-id="fdc06-165">For example, in `\\?\server1\e:\utilities\\filecomparer\`, the server/share portion is server1\utilities.</span></span> <span data-ttu-id="fdc06-166">Jest to istotne w wywołaniu metody, takie jak <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> segmentów względna katalogu; nie ma możliwości Przejdź poza woluminu.</span><span class="sxs-lookup"><span data-stu-id="fdc06-166">This is significant when calling a method such as <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> with relative directory segments; it is never possible to navigate past the volume.</span></span> 

<span data-ttu-id="fdc06-167">Definicja w pełni kwalifikowana ścieżki urządzenia systemu DOS.</span><span class="sxs-lookup"><span data-stu-id="fdc06-167">DOS device paths are fully qualified by definition.</span></span> <span data-ttu-id="fdc06-168">Segmenty względna katalogu (`.` i `..`) są niedozwolone.</span><span class="sxs-lookup"><span data-stu-id="fdc06-168">Relative directory segments (`.` and `..`) are not allowed.</span></span> <span data-ttu-id="fdc06-169">Bieżący katalogów nigdy nie wprowadzają do ich użycia.</span><span class="sxs-lookup"><span data-stu-id="fdc06-169">Current directories never enter into their usage.</span></span>

## <a name="example-ways-to-refer-to-the-same-file"></a><span data-ttu-id="fdc06-170">Przykład: Sposoby odwoływać się do tego samego pliku</span><span class="sxs-lookup"><span data-stu-id="fdc06-170">Example: Ways to refer to the same file</span></span>

<span data-ttu-id="fdc06-171">Poniższy przykład przedstawia niektóre sposoby, w którym użytkownik może odwoływać się do pliku przy użyciu interfejsów API w <xref:System.IO> przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="fdc06-171">The following example illustrates some of the ways in which you can refer to a file when using the APIs in the <xref:System.IO> namespace.</span></span> <span data-ttu-id="fdc06-172">Przykład tworzy <xref:System.IO.FileInfo> obiektu i używa jej <xref:System.IO.FileInfo.Name> i <xref:System.IO.FileInfo.Length> właściwości, aby wyświetlić nazwę pliku i długość pliku.</span><span class="sxs-lookup"><span data-stu-id="fdc06-172">The example instantiates a <xref:System.IO.FileInfo> object and uses its <xref:System.IO.FileInfo.Name> and <xref:System.IO.FileInfo.Length> properties to display the filename and the length of the file.</span></span>

[!code-csharp[referring-to-the-same-file](~/samples/snippets/standard/io/file-names/cs/file-refs.cs)]
[!code-vb[referring-to-the-same-file](~/samples/snippets/standard/io/file-names/vb/file-refs.vb)]

## <a name="path-normalization"></a><span data-ttu-id="fdc06-173">Ścieżka normalizacji</span><span class="sxs-lookup"><span data-stu-id="fdc06-173">Path normalization</span></span>

<span data-ttu-id="fdc06-174">Znormalizowany są prawie wszystkie ścieżki przekazany do interfejsów API systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="fdc06-174">Almost all paths passed to Windows APIs are normalized.</span></span> <span data-ttu-id="fdc06-175">Podczas normalizacji Windows wykonuje następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="fdc06-175">During normalization, Windows performs the following steps:</span></span>

- <span data-ttu-id="fdc06-176">Określa ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="fdc06-176">Identifies the path.</span></span>
- <span data-ttu-id="fdc06-177">Dotyczy bieżącego katalogu częściowo kwalifikowanej ścieżki (względnej).</span><span class="sxs-lookup"><span data-stu-id="fdc06-177">Applies the current directory to partially qualified (relative) paths.</span></span>
- <span data-ttu-id="fdc06-178">Canonicalizes separatory składnika i katalogu.</span><span class="sxs-lookup"><span data-stu-id="fdc06-178">Canonicalizes component and directory separators.</span></span>
- <span data-ttu-id="fdc06-179">Oblicza składniki względna katalogu (`.` dla bieżącego katalogu i `..` dla katalogu nadrzędnego).</span><span class="sxs-lookup"><span data-stu-id="fdc06-179">Evaluates relative directory components (`.` for the current directory and `..` for the parent directory).</span></span>
- <span data-ttu-id="fdc06-180">Przycina niektórych znaków.</span><span class="sxs-lookup"><span data-stu-id="fdc06-180">Trims certain characters.</span></span>

<span data-ttu-id="fdc06-181">Ten normalizacji niejawnie się stanie, ale należy go jawnie przez wywołanie metody <xref:System.IO.Path.GetFullPath%2A?displayProperty=nameWithType> metodę, która opakowuje wywołanie [funkcja GetFullPathName()](https://msdn.microsoft.com/library/windows/desktop/aa364963(v=vs.85).aspx).</span><span class="sxs-lookup"><span data-stu-id="fdc06-181">This normalization happens implicitly, but you can do it explicitly by calling the <xref:System.IO.Path.GetFullPath%2A?displayProperty=nameWithType> method, which wraps a call to the  [GetFullPathName() function](https://msdn.microsoft.com/library/windows/desktop/aa364963(v=vs.85).aspx).</span></span> <span data-ttu-id="fdc06-182">Możesz także wywołać systemu Windows [funkcja GetFullPathName()](https://msdn.microsoft.com/library/windows/desktop/aa364963(v=vs.85).aspx) bezpośrednio przy użyciu metody P/Invoke.</span><span class="sxs-lookup"><span data-stu-id="fdc06-182">You can also call the Windows [GetFullPathName() function](https://msdn.microsoft.com/library/windows/desktop/aa364963(v=vs.85).aspx) directly using P/Invoke.</span></span> <span data-ttu-id="fdc06-183">Możesz także wywołać</span><span class="sxs-lookup"><span data-stu-id="fdc06-183">You can also call the</span></span> 

### <a name="identifying-the-path"></a><span data-ttu-id="fdc06-184">Identyfikowanie ścieżki</span><span class="sxs-lookup"><span data-stu-id="fdc06-184">Identifying the path</span></span>

<span data-ttu-id="fdc06-185">Pierwszym etapem normalizacji ścieżki identyfikuje typu ścieżki.</span><span class="sxs-lookup"><span data-stu-id="fdc06-185">The first step in path normalization is identifying the type of path.</span></span> <span data-ttu-id="fdc06-186">Ścieżki dzielą się na kilka kategorii:</span><span class="sxs-lookup"><span data-stu-id="fdc06-186">Paths fall into one of a few categories:</span></span>

- <span data-ttu-id="fdc06-187">Są one ścieżki urządzenia. oznacza to, Rozpocznij dwa separatory i znak zapytania lub okres (`\\?` lub `\\.`).</span><span class="sxs-lookup"><span data-stu-id="fdc06-187">They are device paths; that is, they begin with two separators and a question mark or period (`\\?` or `\\.`).</span></span>
- <span data-ttu-id="fdc06-188">Są one ścieżki UNC. oznacza to, że ich rozpoczynać się od dwóch separatorów bez znaku zapytania lub okres.</span><span class="sxs-lookup"><span data-stu-id="fdc06-188">They are UNC paths; that is, they begin with two separators without a question mark or period.</span></span> 
- <span data-ttu-id="fdc06-189">Są one pełni kwalifikowanej ścieżki DOS. oznacza to, ich rozpoczynać się od litery dysku, separatora woluminu i separatora składnika (`C:\`).</span><span class="sxs-lookup"><span data-stu-id="fdc06-189">They are fully qualified DOS paths; that is, they begin with a drive letter, a volume separator, and a component separator (`C:\`).</span></span>
- <span data-ttu-id="fdc06-190">Wyznaczają starszych urządzeń (`CON`, `LPT1`).</span><span class="sxs-lookup"><span data-stu-id="fdc06-190">They designate a legacy device (`CON`, `LPT1`).</span></span>
- <span data-ttu-id="fdc06-191">Są one względem katalogu głównego dysku bieżącego; oznacza to, rozpocząć się separatorem pojedynczego składnika (`\`).</span><span class="sxs-lookup"><span data-stu-id="fdc06-191">They are relative to the root of the current drive; that is, they begin with a single component separator (`\`).</span></span>
- <span data-ttu-id="fdc06-192">Są one powiązane z bieżącym katalogiem określony dysk; oznacza to, że ich zaczynać się od litery dysku, separatora woluminu i separatora składnika (`C:`).</span><span class="sxs-lookup"><span data-stu-id="fdc06-192">They are relative to the current directory of a specified drive; that is, they begin with a drive letter, a volume separator, and no component separator (`C:`).</span></span>
- <span data-ttu-id="fdc06-193">Są one powiązane z bieżącym katalogiem; oznacza to, rozpocząć się inaczej (`temp\testfile.txt`).</span><span class="sxs-lookup"><span data-stu-id="fdc06-193">They are relative to the current directory; that is, they begin with anything else (`temp\testfile.txt`).</span></span>

<span data-ttu-id="fdc06-194">Typ ścieżki Określa, czy bieżący katalog jest stosowane w określony sposób.</span><span class="sxs-lookup"><span data-stu-id="fdc06-194">The type of the path determines whether or not a current directory is applied in some way.</span></span> <span data-ttu-id="fdc06-195">Określa również, co to jest "root" ścieżki.</span><span class="sxs-lookup"><span data-stu-id="fdc06-195">It also determines what the "root" of the path is.</span></span>

### <a name="handling-legacy-devices"></a><span data-ttu-id="fdc06-196">Obsługa starszych urządzeń</span><span class="sxs-lookup"><span data-stu-id="fdc06-196">Handling legacy devices</span></span>

<span data-ttu-id="fdc06-197">Jeśli ścieżka nie jest starszego urządzenia systemu DOS takich jak `CON`, `COM1`, lub `LPT1`, jest konwertowana na ścieżkę do urządzenia, dołączając `\\.\` i zwrócone.</span><span class="sxs-lookup"><span data-stu-id="fdc06-197">If the path is a legacy DOS device such as `CON`, `COM1`, or `LPT1`, it is converted into a device path by prepending `\\.\` and returned.</span></span> 

<span data-ttu-id="fdc06-198">Ścieżki, która zaczyna się od nazwy starszych urządzeń zawsze jest interpretowana jako starszego urządzenia przez <xref:System.IO.Path.GetFullPath(System.String)?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="fdc06-198">A path that begins with a legacy device name is always interpreted as a legacy device by the <xref:System.IO.Path.GetFullPath(System.String)?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="fdc06-199">Na przykład ścieżka urządzenia systemu DOS dla `CON.TXT` jest `\\.\CON`i ścieżkę urządzenia systemu DOS `COM1.TXT\file1.txt` jest `\\.\COM1`.</span><span class="sxs-lookup"><span data-stu-id="fdc06-199">For example, the DOS device path for `CON.TXT` is `\\.\CON`, and the DOS device path for `COM1.TXT\file1.txt` is `\\.\COM1`.</span></span>

### <a name="applying-the-current-directory"></a><span data-ttu-id="fdc06-200">Stosowanie bieżącego katalogu</span><span class="sxs-lookup"><span data-stu-id="fdc06-200">Applying the current directory</span></span>

<span data-ttu-id="fdc06-201">Jeśli ścieżki nie jest w pełni kwalifikowana, system Windows stosuje bieżącego katalogu do niego.</span><span class="sxs-lookup"><span data-stu-id="fdc06-201">If a path isn't fully qualified, Windows applies the current directory to it.</span></span> <span data-ttu-id="fdc06-202">UNC i ścieżki urządzenia nie mają bieżącego katalogu, które są stosowane.</span><span class="sxs-lookup"><span data-stu-id="fdc06-202">UNCs and device paths do not have the current directory applied.</span></span> <span data-ttu-id="fdc06-203">Nie jest pełną dysku z separatorem C:\\.</span><span class="sxs-lookup"><span data-stu-id="fdc06-203">Neither does a full drive with separator C:\\.</span></span>

<span data-ttu-id="fdc06-204">Jeśli ścieżka rozpoczyna się od separatora pojedynczego składnika, zostanie zastosowane dysku z bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="fdc06-204">If the path starts with a single component separator, the drive from the current directory is applied.</span></span> <span data-ttu-id="fdc06-205">Na przykład, jeśli ścieżka pliku jest `\utilities` i bieżący katalog jest `C:\temp\`, i tworzy normalizacji `C:\utilities`.</span><span class="sxs-lookup"><span data-stu-id="fdc06-205">For example, if the file path is `\utilities` and the current directory is `C:\temp\`, normalization produces `C:\utilities`.</span></span>

<span data-ttu-id="fdc06-206">Jeśli ścieżka rozpoczyna się od litery dysku, separatora woluminu i separatora składnika, zostanie zastosowane ostatniego bieżącego katalogu, ustaw z powłoki poleceń określonego dysku.</span><span class="sxs-lookup"><span data-stu-id="fdc06-206">If the path starts with a drive letter, volume separator, and no component separator, the last current directory set from the command shell for the specified drive is applied.</span></span> <span data-ttu-id="fdc06-207">Jeśli ostatniego bieżącego katalogu nie została ustawiona, zostanie zastosowane samego dysku.</span><span class="sxs-lookup"><span data-stu-id="fdc06-207">If the last current directory was not set, the drive alone is applied.</span></span> <span data-ttu-id="fdc06-208">Na przykład, jeśli ścieżka pliku jest `D:sources`, bieżący katalog jest `C:\Documents\`, i został ostatniego bieżącego katalogu na dysku D: `D:\sources\`, wynikiem jest `D:\sources\sources`.</span><span class="sxs-lookup"><span data-stu-id="fdc06-208">For example, if the file path is `D:sources`, the current directory is `C:\Documents\`, and the last current directory on drive D: was `D:\sources\`, the result is `D:\sources\sources`.</span></span> <span data-ttu-id="fdc06-209">Te ścieżki "dysk względnej" są wspólne źródło błędów logiki programów i skryptów.</span><span class="sxs-lookup"><span data-stu-id="fdc06-209">These "drive relative" paths are a common source of program and script logic errors.</span></span> <span data-ttu-id="fdc06-210">Przy założeniu, że ścieżki rozpoczynającej się od litery i dwukropka nie jest względna oczywiście nie jest prawidłowa.</span><span class="sxs-lookup"><span data-stu-id="fdc06-210">Assuming that a path beginning with a letter and a colon isn't relative is obviously not correct.</span></span>

<span data-ttu-id="fdc06-211">Jeśli ścieżka rozpoczyna się od czegoś innego niż separatora, są stosowane bieżącego dysku i bieżący katalog.</span><span class="sxs-lookup"><span data-stu-id="fdc06-211">If the path starts with something other than a separator, the current drive and current directory are applied.</span></span> <span data-ttu-id="fdc06-212">Na przykład, jeśli ścieżka jest `filecompare` i bieżący katalog jest `C:\utilities\`, wynikiem jest `C:\utilities\filecompare\`.</span><span class="sxs-lookup"><span data-stu-id="fdc06-212">For example, if the path is `filecompare` and the current directory is `C:\utilities\`, the result is `C:\utilities\filecompare\`.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="fdc06-213">Ścieżki względne są niebezpieczne w aplikacjach wielowątkowych (to znaczy, że większość aplikacji), ponieważ bieżący katalog to ustawienie dla procesu.</span><span class="sxs-lookup"><span data-stu-id="fdc06-213">Relative paths are dangerous in multithreaded applications (that is, most applications) because the current directory is a per-process setting.</span></span> <span data-ttu-id="fdc06-214">Którymkolwiek wątku można zmienić bieżący katalog w dowolnym momencie.</span><span class="sxs-lookup"><span data-stu-id="fdc06-214">Any thread can change the current directory at any time.</span></span> <span data-ttu-id="fdc06-215">Począwszy od programu .NET Core 2.1, można wywołać <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> metody można odczytać ścieżki bezwzględnej ścieżki względnej i ścieżki podstawowej (bieżącego katalogu), który chcesz rozwiązać go przed.</span><span class="sxs-lookup"><span data-stu-id="fdc06-215">Starting with .NET Core 2.1, you can call the <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> method to get an absolute path from a relative path and the base path (the current directory) that you want to resolve it against.</span></span> 

### <a name="canonicalizing-separators"></a><span data-ttu-id="fdc06-216">Kanoniczną separatorów</span><span class="sxs-lookup"><span data-stu-id="fdc06-216">Canonicalizing separators</span></span>

<span data-ttu-id="fdc06-217">Przekazuj wszystkie ukośniki (`/`) są konwertowane na standardowe separatora systemu Windows, kreski ułamkowej odwróconej (`\`).</span><span class="sxs-lookup"><span data-stu-id="fdc06-217">All forward slashes (`/`) are converted into the standard Windows separator, the back slash (`\`).</span></span> <span data-ttu-id="fdc06-218">Jeśli są obecne, szereg ukośniki, które należy wykonać pierwsze dwa ukośniki są zwijane do pojedynczego ukośnika.</span><span class="sxs-lookup"><span data-stu-id="fdc06-218">If they are present, a series of slashes that follow the first two slashes are collapsed into a single slash.</span></span>

### <a name="evaluating-relative-components"></a><span data-ttu-id="fdc06-219">Ocena względną składników</span><span class="sxs-lookup"><span data-stu-id="fdc06-219">Evaluating relative components</span></span>

<span data-ttu-id="fdc06-220">W trakcie przetwarzania ścieżki wszystkich składników lub segmentów, które składają się z jedną lub okres double (`.` lub `..`) są oceniane:</span><span class="sxs-lookup"><span data-stu-id="fdc06-220">As the path is processed, any components or segments that are composed of a single or a double period (`.` or `..`) are evaluated:</span></span> 

- <span data-ttu-id="fdc06-221">Dla jednego okresu jak bieżący segment zostanie usunięty, ponieważ odwołuje się do bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="fdc06-221">For a single period, the current segment is removed, since it refers to the current directory.</span></span>

- <span data-ttu-id="fdc06-222">Okres dwa razy segment nadrzędnego, jak bieżący segment są usuwane, ponieważ podwójny okres odwołuje się do katalogu nadrzędnego.</span><span class="sxs-lookup"><span data-stu-id="fdc06-222">For a double period, the current segment and the parent segment are removed, since the double period refers to the parent directory.</span></span>

   <span data-ttu-id="fdc06-223">Katalogi nadrzędne są usuwane tylko, jeśli nie znajdują się one poza główny ścieżki.</span><span class="sxs-lookup"><span data-stu-id="fdc06-223">Parent directories are only removed if they aren't past the root of the path.</span></span> <span data-ttu-id="fdc06-224">Główny ścieżki zależy od typu ścieżki.</span><span class="sxs-lookup"><span data-stu-id="fdc06-224">The root of the path depends on the type of path.</span></span> <span data-ttu-id="fdc06-225">Jest dysku (`C:\`) dla systemu DOS ścieżek, serwera i udostępniania dla UNC (`\\Server\Share`) i prefiksu ścieżka urządzenia dla ścieżki urządzeń (`\\?\` lub `\\.\`).</span><span class="sxs-lookup"><span data-stu-id="fdc06-225">It is the drive (`C:\`) for DOS paths, the server/share for UNCs (`\\Server\Share`), and the device path prefix for device paths (`\\?\` or `\\.\`).</span></span>

### <a name="trimming-characters"></a><span data-ttu-id="fdc06-226">Przycinanie znaków</span><span class="sxs-lookup"><span data-stu-id="fdc06-226">Trimming characters</span></span>

<span data-ttu-id="fdc06-227">Wraz z przebiegów separatorów i segmentów względną usunięte wcześniej niektóre dodatkowe znaki zostaną usunięte podczas normalizacji:</span><span class="sxs-lookup"><span data-stu-id="fdc06-227">Along with the runs of separators and relative segments removed earlier, some additional characters are removed during normalization:</span></span>

- <span data-ttu-id="fdc06-228">Jeśli segment kończy się w zadanym przedziale czasu, tego okresu zostanie usunięta.</span><span class="sxs-lookup"><span data-stu-id="fdc06-228">If a segment ends in a single period, that period is removed.</span></span> <span data-ttu-id="fdc06-229">(Segment okresu pojedynczym lub podwójnym znormalizowanych w poprzednim kroku.</span><span class="sxs-lookup"><span data-stu-id="fdc06-229">(A segment of a single or double period is normalized in the previous step.</span></span> <span data-ttu-id="fdc06-230">Segment trzy lub więcej okresów nie jest znormalizowana i jest rzeczywistą nazwą prawidłowego pliku/katalogu.)</span><span class="sxs-lookup"><span data-stu-id="fdc06-230">A segment of three or more periods is not normalized and is actually a valid file/directory name.)</span></span>

- <span data-ttu-id="fdc06-231">Jeśli ścieżka nie kończyć się separatorem, wszystkie końcowe kropek i spacji (U + 0020) zostaną usunięte.</span><span class="sxs-lookup"><span data-stu-id="fdc06-231">If the path doesn't end in a separator, all trailing periods and spaces (U+0020) are removed.</span></span> <span data-ttu-id="fdc06-232">Ostatni segment w przypadku po prostu okres pojedynczym lub podwójnym, podlega zasadę względną składniki powyżej.</span><span class="sxs-lookup"><span data-stu-id="fdc06-232">If the last segment is simply a single or double period, it falls under the relative components rule above.</span></span> 

   <span data-ttu-id="fdc06-233">Ta reguła oznacza, że nazwę katalogu można utworzyć z spacji przez dodanie końcowe separatora po miejsce.</span><span class="sxs-lookup"><span data-stu-id="fdc06-233">This rule means that you can create a directory name with a trailing space by adding a trailing separator after the space.</span></span>  

   > [!IMPORTANT]
   > <span data-ttu-id="fdc06-234">Należy **nigdy nie** Utwórz nazwę pliku lub katalogu z spacje końcowe.</span><span class="sxs-lookup"><span data-stu-id="fdc06-234">You should **never** create a directory or filename with a trailing space.</span></span> <span data-ttu-id="fdc06-235">Spacje końcowe może utrudniać lub niemożliwe, aby uzyskać dostęp do katalogu i aplikacje często się niepowodzeniem podczas próby obsługi katalogów lub plików, których nazwy zawierają spacje końcowe.</span><span class="sxs-lookup"><span data-stu-id="fdc06-235">Trailing spaces can make it difficult or impossible to access a directory, and applications commonly fail when attempting to handle directories or files whose names include trailing spaces.</span></span>

## <a name="skipping-normalization"></a><span data-ttu-id="fdc06-236">Pomijanie normalizacji</span><span class="sxs-lookup"><span data-stu-id="fdc06-236">Skipping normalization</span></span>

<span data-ttu-id="fdc06-237">Zwykle dowolną ścieżkę przekazany do Windows API (skutecznie) jest przekazywana do [funkcja GetFullPathName](https://msdn.microsoft.com/library/windows/desktop/aa364963(v=vs.85).aspx) i znormalizowana.</span><span class="sxs-lookup"><span data-stu-id="fdc06-237">Normally, any path passed to a Windows API is (effectively) passed to the [GetFullPathName function](https://msdn.microsoft.com/library/windows/desktop/aa364963(v=vs.85).aspx) and normalized.</span></span> <span data-ttu-id="fdc06-238">Istnieje jeden wyjątek ważne: ścieżki urządzenia, która rozpoczyna się od znaku zapytania zamiast okres.</span><span class="sxs-lookup"><span data-stu-id="fdc06-238">There is one important exception: a device path that begins with a question mark instead of a period.</span></span> <span data-ttu-id="fdc06-239">Jeśli ścieżka rozpoczyna się dokładnie z `\\?\` (Uwaga Użycie canonical ukośnik odwrotny), jest ona znormalizowany.</span><span class="sxs-lookup"><span data-stu-id="fdc06-239">Unless the path starts exactly with `\\?\` (note the use of the canonical backslash), it is normalized.</span></span>

<span data-ttu-id="fdc06-240">Dlaczego czy chcesz pominąć normalizacji?</span><span class="sxs-lookup"><span data-stu-id="fdc06-240">Why would you want to skip normalization?</span></span> <span data-ttu-id="fdc06-241">Istnieją trzy główne przyczyny:</span><span class="sxs-lookup"><span data-stu-id="fdc06-241">There are three major reasons:</span></span>

1. <span data-ttu-id="fdc06-242">Aby uzyskać dostęp do ścieżki, które nie są zwykle dostępne, ale są prawnych.</span><span class="sxs-lookup"><span data-stu-id="fdc06-242">To get access to paths that are normally unavailable but are legal.</span></span> <span data-ttu-id="fdc06-243">Plik lub katalog o nazwie `hidden.`, na przykład istnieje możliwość dostępu w inny sposób.</span><span class="sxs-lookup"><span data-stu-id="fdc06-243">A file or directory called `hidden.`, for example, is impossible to access in any other way.</span></span> 

1. <span data-ttu-id="fdc06-244">Aby zwiększyć wydajność, pomijając normalizacji, jeśli zostały już znormalizowany.</span><span class="sxs-lookup"><span data-stu-id="fdc06-244">To improve performance by skipping normalization if you've already normalized.</span></span>

1. <span data-ttu-id="fdc06-245">W programie .NET Framework, aby pominąć `MAX_PATH` Sprawdź, czy długość ścieżki do obsługi ścieżek, które są większe niż 259 znaków.</span><span class="sxs-lookup"><span data-stu-id="fdc06-245">On the .NET Framework only, to skip the `MAX_PATH` check for path length to allow for paths that are greater than 259 characters.</span></span> <span data-ttu-id="fdc06-246">Większość interfejsów API Zezwól na to, z pewnymi wyjątkami.</span><span class="sxs-lookup"><span data-stu-id="fdc06-246">Most APIs allow this, with some exceptions.</span></span>

> [!NOTE]
> <span data-ttu-id="fdc06-247">Oprogramowanie .NET core obsługuje długich ścieżek niejawnie i nie wykonuje `MAX_PATH` Sprawdź.</span><span class="sxs-lookup"><span data-stu-id="fdc06-247">.NET Core handles long paths implicitly and does not perform a `MAX_PATH` check.</span></span> <span data-ttu-id="fdc06-248">`MAX_PATH` Sprawdzenie jest stosowane tylko dla programu .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="fdc06-248">The `MAX_PATH` check applies only to the .NET Framework.</span></span>

<span data-ttu-id="fdc06-249">Pomijanie normalizacji i max kontroli ścieżki jest jedyną różnicą między składni ścieżki dwóch urządzenia; w przeciwnym razie są one identyczne.</span><span class="sxs-lookup"><span data-stu-id="fdc06-249">Skipping normalization and max path checks is the only difference between the two device path syntaxes; they are otherwise identical.</span></span> <span data-ttu-id="fdc06-250">Należy zachować ostrożność przy normalizacji zostanie pominięta, ponieważ można łatwo utworzyć ścieżek, które są trudne do postępowania w przypadku aplikacji "normal".</span><span class="sxs-lookup"><span data-stu-id="fdc06-250">Be careful with skipping normalization, since you can easily create paths that are difficult for "normal" applications to deal with.</span></span>

<span data-ttu-id="fdc06-251">Ścieżki zaczynające się `\\?\` są nadal znormalizowany je, aby jawnie przekazania [funkcja GetFullPathName](https://msdn.microsoft.com/library/windows/desktop/aa364963(v=vs.85).aspx).</span><span class="sxs-lookup"><span data-stu-id="fdc06-251">Paths that start with `\\?\` are still normalized if you explicitly pass them to the [GetFullPathName function](https://msdn.microsoft.com/library/windows/desktop/aa364963(v=vs.85).aspx).</span></span>

<span data-ttu-id="fdc06-252">Należy pamiętać o możliwości ścieżki z więcej niż `MAX_PATH` znaków [GetFullPathName](https://msdn.microsoft.com/library/windows/desktop/aa364963(v=vs.85).aspx) bez `\\?\`.</span><span class="sxs-lookup"><span data-stu-id="fdc06-252">Note that you can paths of more than `MAX_PATH` characters to [GetFullPathName](https://msdn.microsoft.com/library/windows/desktop/aa364963(v=vs.85).aspx) without `\\?\`.</span></span> <span data-ttu-id="fdc06-253">Obsługuje ona dowolnego długość ścieżki do maksymalny rozmiar ciągu obsługujący systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="fdc06-253">It supports arbitrary length paths up to the maximum string size that Windows can handle.</span></span>

## <a name="case-and-the-windows-file-system"></a><span data-ttu-id="fdc06-254">Wielkość liter i systemu plików</span><span class="sxs-lookup"><span data-stu-id="fdc06-254">Case and the Windows file system</span></span>

<span data-ttu-id="fdc06-255">Cecha systemu plików z systemem innym niż Windows użytkowników i deweloperów znaleźć mylące jest tej ścieżki i nazwy katalogów jest rozróżniana wielkość liter.</span><span class="sxs-lookup"><span data-stu-id="fdc06-255">A peculiarity of the Windows file system that non-Windows users and developers find confusing is that path and directory names are case-insensitive.</span></span> <span data-ttu-id="fdc06-256">Oznacza to, że nazwy katalogów i plików odzwierciedlają wielkość liter w wyrazie ciągi używane podczas ich tworzenia.</span><span class="sxs-lookup"><span data-stu-id="fdc06-256">That is, directory and file names reflect the casing of the strings used when they are created.</span></span> <span data-ttu-id="fdc06-257">Na przykład wywołanie metody</span><span class="sxs-lookup"><span data-stu-id="fdc06-257">For example, the method call</span></span>

```csharp
Directory.Create(TeStDiReCtOrY);
```
<span data-ttu-id="fdc06-258">Tworzy katalog o nazwie TeStDiReCtOrY.</span><span class="sxs-lookup"><span data-stu-id="fdc06-258">creates a directory named TeStDiReCtOrY.</span></span> <span data-ttu-id="fdc06-259">Jeśli zmienisz katalog lub plik, aby zmienić jego case, nazwa pliku lub katalogu odzwierciedla przypadku ciąg używany po zmianie nazwy.</span><span class="sxs-lookup"><span data-stu-id="fdc06-259">If you rename a directory or file to change its case, the directory or file name reflects the case of the string used when you rename it.</span></span> <span data-ttu-id="fdc06-260">Na przykład poniższy kod zmienia nazwę pliku o nazwie test.txt do Test.txt:</span><span class="sxs-lookup"><span data-stu-id="fdc06-260">For example, the following code renames a file named test.txt to Test.txt:</span></span>

```csharp
using System;
using System.IO;

class Example
{
   public static void Main()
   {
      var fi = new FileInfo(@".\test.txt");
      fi.MoveTo(@".\Test.txt");
   }
}
``` 
```vb
Imports System.IO

Module Example
   Public Sub Main()
      Dim fi As New FileInfo(".\test.txt")
      fi.MoveTo(".\Test.txt")
   End Sub
End Module
```

<span data-ttu-id="fdc06-261">Jednak bez uwzględniania wielkości liter podczas porównywania nazw plików i katalogów.</span><span class="sxs-lookup"><span data-stu-id="fdc06-261">However, directory and file name comparisons are case-insensitive.</span></span> <span data-ttu-id="fdc06-262">Jeśli wyszukać plik o nazwie "test.txt", interfejsów API systemu plików .NET zignorować liter w porównaniu.</span><span class="sxs-lookup"><span data-stu-id="fdc06-262">If you search for a file named "test.txt", .NET file system APIs ignore case in the comparison.</span></span> <span data-ttu-id="fdc06-263">Test.txt, testu. TXT, testu. TXT i wszelkich innych kombinacji wielkich i małych liter będą zgodne "test.txt".</span><span class="sxs-lookup"><span data-stu-id="fdc06-263">Test.txt, TEST.TXT, test.TXT, and any other combination of upper- and lowercase letters will match "test.txt".</span></span>
