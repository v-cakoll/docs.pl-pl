---
title: Publikowanie aplikacji za pomocą wiersza wiersza wiersza wiersza wiersza i sprzedaży .NET Core
description: Dowiedz się, jak opublikować aplikację .NET Core przy użyciu poleceń wiersza polecenia .NET Core.
author: thraka
ms.author: adegeo
ms.date: 12/12/2019
dev_langs:
- csharp
- vb
ms.openlocfilehash: f4c2a4ccf551c53e4aa4e125cb5720d6f1cc9601
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/14/2020
ms.locfileid: "79399071"
---
# <a name="publish-net-core-apps-with-the-net-core-cli"></a><span data-ttu-id="9af8a-103">Publikowanie aplikacji .NET Core za pomocą wiersza wiersza wiersza wiersza wiersza .NET Core</span><span class="sxs-lookup"><span data-stu-id="9af8a-103">Publish .NET Core apps with the .NET Core CLI</span></span>

<span data-ttu-id="9af8a-104">W tym artykule pokazano, jak można opublikować aplikację .NET Core z wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="9af8a-104">This article demonstrates how you can publish your .NET Core application from the command line.</span></span> <span data-ttu-id="9af8a-105">.NET Core udostępnia trzy sposoby publikowania aplikacji.</span><span class="sxs-lookup"><span data-stu-id="9af8a-105">.NET Core provides three ways to publish your applications.</span></span> <span data-ttu-id="9af8a-106">Wdrożenie zależne od struktury tworzy wieloplatformowy plik dll, który używa lokalnie zainstalowanego programu .NET Core.</span><span class="sxs-lookup"><span data-stu-id="9af8a-106">Framework-dependent deployment produces a cross-platform .dll file that uses the locally installed .NET Core runtime.</span></span> <span data-ttu-id="9af8a-107">Plik wykonywalny zależny od struktury tworzy plik wykonywalny specyficzny dla platformy, który używa lokalnie zainstalowanego programu wykonawczego .NET Core.</span><span class="sxs-lookup"><span data-stu-id="9af8a-107">Framework-dependent executable produces a platform-specific executable that uses the locally installed .NET Core runtime.</span></span> <span data-ttu-id="9af8a-108">Samodzielny plik wykonywalny tworzy plik wykonywalny specyficzny dla platformy i zawiera lokalną kopię programu wykonawczego .NET Core.</span><span class="sxs-lookup"><span data-stu-id="9af8a-108">Self-contained executable produces a platform-specific executable and includes a local copy of the .NET Core runtime.</span></span>

<span data-ttu-id="9af8a-109">Aby zapoznać się z omówieniem tych trybów publikowania, zobacz [.NET Core Application Deployment](index.md).</span><span class="sxs-lookup"><span data-stu-id="9af8a-109">For an overview of these publishing modes, see [.NET Core Application Deployment](index.md).</span></span>

<span data-ttu-id="9af8a-110">Szukasz szybkiej pomocy na temat korzystania z cli?</span><span class="sxs-lookup"><span data-stu-id="9af8a-110">Looking for some quick help on using the CLI?</span></span> <span data-ttu-id="9af8a-111">W poniższej tabeli przedstawiono kilka przykładów publikowania aplikacji.</span><span class="sxs-lookup"><span data-stu-id="9af8a-111">The following table shows some examples of how to publish your app.</span></span> <span data-ttu-id="9af8a-112">Można określić platformę docelową za pomocą parametru `-f <TFM>` lub edytując plik projektu.</span><span class="sxs-lookup"><span data-stu-id="9af8a-112">You can specify the target framework with the `-f <TFM>` parameter or by editing the project file.</span></span> <span data-ttu-id="9af8a-113">Aby uzyskać więcej informacji, zobacz [Publikowanie podstaw](#publishing-basics).</span><span class="sxs-lookup"><span data-stu-id="9af8a-113">For more information, see [Publishing basics](#publishing-basics).</span></span>

| <span data-ttu-id="9af8a-114">Tryb publikowania</span><span class="sxs-lookup"><span data-stu-id="9af8a-114">Publish Mode</span></span> | <span data-ttu-id="9af8a-115">Wersja zestawu SDK</span><span class="sxs-lookup"><span data-stu-id="9af8a-115">SDK Version</span></span> | <span data-ttu-id="9af8a-116">Polecenie</span><span class="sxs-lookup"><span data-stu-id="9af8a-116">Command</span></span> |
| ------------ | ----------- | ------- |
| <span data-ttu-id="9af8a-117">Wdrożenie zależne od struktury</span><span class="sxs-lookup"><span data-stu-id="9af8a-117">Framework-dependent deployment</span></span> | <span data-ttu-id="9af8a-118">2.x</span><span class="sxs-lookup"><span data-stu-id="9af8a-118">2.x</span></span> | `dotnet publish -c Release` |
| <span data-ttu-id="9af8a-119">Plik wykonywalny zależny od struktury</span><span class="sxs-lookup"><span data-stu-id="9af8a-119">Framework-dependent executable</span></span> | <span data-ttu-id="9af8a-120">2.2</span><span class="sxs-lookup"><span data-stu-id="9af8a-120">2.2</span></span> | `dotnet publish -c Release -r <RID> --self-contained false` |
|                                | <span data-ttu-id="9af8a-121">3.0</span><span class="sxs-lookup"><span data-stu-id="9af8a-121">3.0</span></span> | `dotnet publish -c Release -r <RID> --self-contained false` |
|                                | <span data-ttu-id="9af8a-122">3.0\*</span><span class="sxs-lookup"><span data-stu-id="9af8a-122">3.0\*</span></span> | `dotnet publish -c Release` |
| <span data-ttu-id="9af8a-123">Samodzielne wdrażanie</span><span class="sxs-lookup"><span data-stu-id="9af8a-123">Self-contained deployment</span></span>      | <span data-ttu-id="9af8a-124">2.1</span><span class="sxs-lookup"><span data-stu-id="9af8a-124">2.1</span></span> | `dotnet publish -c Release -r <RID> --self-contained true` |
|                                | <span data-ttu-id="9af8a-125">2.2</span><span class="sxs-lookup"><span data-stu-id="9af8a-125">2.2</span></span> | `dotnet publish -c Release -r <RID> --self-contained true` |
|                                | <span data-ttu-id="9af8a-126">3.0</span><span class="sxs-lookup"><span data-stu-id="9af8a-126">3.0</span></span> | `dotnet publish -c Release -r <RID> --self-contained true` |

<span data-ttu-id="9af8a-127">\*W przypadku korzystania z pakietu SDK w wersji 3.0 plik `dotnet publish` wykonywalny zależny od struktury jest domyślnym trybem publikowania podczas uruchamiania polecenia podstawowego.</span><span class="sxs-lookup"><span data-stu-id="9af8a-127">\* When using SDK version 3.0, framework-dependent executable is the default publishing mode when running the basic `dotnet publish` command.</span></span> <span data-ttu-id="9af8a-128">Dotyczy to tylko wtedy, gdy projekt jest przeznaczony dla **.NET Core 2.1** lub **.NET Core 3.0**.</span><span class="sxs-lookup"><span data-stu-id="9af8a-128">This only applies when the project targets either **.NET Core 2.1** or **.NET Core 3.0**.</span></span>

## <a name="publishing-basics"></a><span data-ttu-id="9af8a-129">Podstawowe informacje o publikowaniu</span><span class="sxs-lookup"><span data-stu-id="9af8a-129">Publishing basics</span></span>

<span data-ttu-id="9af8a-130">Ustawienie `<TargetFramework>` pliku projektu określa domyślną strukturę docelową podczas publikowania aplikacji.</span><span class="sxs-lookup"><span data-stu-id="9af8a-130">The `<TargetFramework>` setting of the project file specifies the default target framework when you publish your app.</span></span> <span data-ttu-id="9af8a-131">Można zmienić platformę docelową na dowolną [prawidłową moniker framework target (TFM)](../../standard/frameworks.md).</span><span class="sxs-lookup"><span data-stu-id="9af8a-131">You can change the target framework to any valid [Target Framework Moniker (TFM)](../../standard/frameworks.md).</span></span> <span data-ttu-id="9af8a-132">Na przykład jeśli projekt `<TargetFramework>netcoreapp2.2</TargetFramework>`używa , plik binarny, który jest przeznaczony dla .NET Core 2.2 jest tworzony.</span><span class="sxs-lookup"><span data-stu-id="9af8a-132">For example, if your project uses `<TargetFramework>netcoreapp2.2</TargetFramework>`, a binary that targets .NET Core 2.2 is created.</span></span> <span data-ttu-id="9af8a-133">TFM określony w tym ustawieniu jest domyślnym obiektem docelowym używanym [`dotnet publish`](../tools/dotnet-publish.md) przez polecenie.</span><span class="sxs-lookup"><span data-stu-id="9af8a-133">The TFM specified in this setting is the default target used by the [`dotnet publish`](../tools/dotnet-publish.md) command.</span></span>

<span data-ttu-id="9af8a-134">Jeśli chcesz kierować więcej niż jedną strukturę, można ustawić `<TargetFrameworks>` ustawienie na więcej niż jedną wartość TFM oddzielone średnikiem.</span><span class="sxs-lookup"><span data-stu-id="9af8a-134">If you want to target more than one framework, you can set the `<TargetFrameworks>` setting to more than one TFM value separated by a semicolon.</span></span> <span data-ttu-id="9af8a-135">Można opublikować jedną z platform `dotnet publish -f <TFM>` za pomocą polecenia.</span><span class="sxs-lookup"><span data-stu-id="9af8a-135">You can publish one of the frameworks with the `dotnet publish -f <TFM>` command.</span></span> <span data-ttu-id="9af8a-136">Na przykład, jeśli `<TargetFrameworks>netcoreapp2.1;netcoreapp2.2</TargetFrameworks>` masz `dotnet publish -f netcoreapp2.1`i uruchomisz, tworzony jest plik binarny przeznaczony dla .NET Core 2.1.</span><span class="sxs-lookup"><span data-stu-id="9af8a-136">For example, if you have `<TargetFrameworks>netcoreapp2.1;netcoreapp2.2</TargetFrameworks>` and run `dotnet publish -f netcoreapp2.1`, a binary that targets .NET Core 2.1 is created.</span></span>

<span data-ttu-id="9af8a-137">O ile nie ustalono inaczej, [`dotnet publish`](../tools/dotnet-publish.md) katalog `./bin/<BUILD-CONFIGURATION>/<TFM>/publish/`wyjściowy polecenia to .</span><span class="sxs-lookup"><span data-stu-id="9af8a-137">Unless otherwise set, the output directory of the [`dotnet publish`](../tools/dotnet-publish.md) command is `./bin/<BUILD-CONFIGURATION>/<TFM>/publish/`.</span></span> <span data-ttu-id="9af8a-138">Domyślnym trybem **BUILD-CONFIGURATION** jest **Debug,** chyba że zmieniono go z parametrem. `-c`</span><span class="sxs-lookup"><span data-stu-id="9af8a-138">The default **BUILD-CONFIGURATION** mode is **Debug** unless changed with the `-c` parameter.</span></span> <span data-ttu-id="9af8a-139">Na przykład `dotnet publish -c Release -f netcoreapp2.1` publikuje `myfolder/bin/Release/netcoreapp2.1/publish/`do .</span><span class="sxs-lookup"><span data-stu-id="9af8a-139">For example, `dotnet publish -c Release -f netcoreapp2.1` publishes to `myfolder/bin/Release/netcoreapp2.1/publish/`.</span></span>

<span data-ttu-id="9af8a-140">Jeśli używasz .NET Core SDK 3.0 lub nowszego, domyślny tryb publikowania dla aplikacji docelowych .NET Core w wersjach 2.1, 2.2, 3.0 lub nowszej wersji jest zależny od struktury.</span><span class="sxs-lookup"><span data-stu-id="9af8a-140">If you use .NET Core SDK 3.0 or later, the default publish mode for apps that target .NET Core versions 2.1, 2.2, 3.0, or a later version, is framework-dependent executable.</span></span>

<span data-ttu-id="9af8a-141">Jeśli używasz .NET Core SDK 2.1, domyślny tryb publikowania dla aplikacji, które są przeznaczone dla .NET Core w wersjach 2.1 i 2.2 jest wdrożenie zależne od struktury.</span><span class="sxs-lookup"><span data-stu-id="9af8a-141">If you use .NET Core SDK 2.1, the default publish mode for apps that target .NET Core versions 2.1 and 2.2 is framework-dependent deployment.</span></span>

### <a name="native-dependencies"></a><span data-ttu-id="9af8a-142">Zależności macierzyste</span><span class="sxs-lookup"><span data-stu-id="9af8a-142">Native dependencies</span></span>

<span data-ttu-id="9af8a-143">Jeśli aplikacja ma zależności macierzyste, może nie działać w innym systemie operacyjnym.</span><span class="sxs-lookup"><span data-stu-id="9af8a-143">If your app has native dependencies, it may not run on a different operating system.</span></span> <span data-ttu-id="9af8a-144">Jeśli na przykład aplikacja korzysta z natywnego interfejsu API systemu Windows, nie będzie działać w systemie macOS ani Linux.</span><span class="sxs-lookup"><span data-stu-id="9af8a-144">For example, if your app uses the native Windows API, it won't run on macOS or Linux.</span></span> <span data-ttu-id="9af8a-145">Należy podać kod specyficzne dla platformy i skompilować plik wykonywalny dla każdej platformy.</span><span class="sxs-lookup"><span data-stu-id="9af8a-145">You would need to provide platform-specific code and compile an executable for each platform.</span></span>

<span data-ttu-id="9af8a-146">Należy również wziąć pod uwagę, jeśli biblioteka, do której odwołujesz się, ma zależność natywną, aplikacja może nie działać na każdej platformie.</span><span class="sxs-lookup"><span data-stu-id="9af8a-146">Consider also, if a library you referenced has a native dependency, your app may not run on every platform.</span></span> <span data-ttu-id="9af8a-147">Jednak jest możliwe, pakiet NuGet, do którego odwołujesz się zawiera wersje specyficzne dla platformy do obsługi wymaganych zależności macierzystych dla Ciebie.</span><span class="sxs-lookup"><span data-stu-id="9af8a-147">However, it's possible a NuGet package you're referencing has included platform-specific versions to handle the required native dependencies for you.</span></span>

<span data-ttu-id="9af8a-148">Podczas dystrybucji aplikacji z natywnych zależności, `dotnet publish -r <RID>` może być konieczne użycie przełącznika, aby określić platformę docelową, które mają zostać opublikowane dla.</span><span class="sxs-lookup"><span data-stu-id="9af8a-148">When distributing an app with native dependencies, you may need to use the `dotnet publish -r <RID>` switch to specify the target platform you want to publish for.</span></span> <span data-ttu-id="9af8a-149">Aby uzyskać listę identyfikatorów czasu wykonywania, zobacz [Katalog identyfikatorów ustawień wykonywania (RID).](../rid-catalog.md)</span><span class="sxs-lookup"><span data-stu-id="9af8a-149">For a list of runtime identifiers, see [Runtime Identifier (RID) catalog](../rid-catalog.md).</span></span>

<span data-ttu-id="9af8a-150">Więcej informacji na temat plików binarnych specyficznych dla platformy jest omówione w sekcjach [wykonawczych](#self-contained-deployment) i wdrażania [zależnych od struktury.](#framework-dependent-executable)</span><span class="sxs-lookup"><span data-stu-id="9af8a-150">More information about platform-specific binaries is covered in the [Framework-dependent executable](#framework-dependent-executable) and [Self-contained deployment](#self-contained-deployment) sections.</span></span>

## <a name="sample-app"></a><span data-ttu-id="9af8a-151">Przykładowa aplikacja</span><span class="sxs-lookup"><span data-stu-id="9af8a-151">Sample app</span></span>

<span data-ttu-id="9af8a-152">Za pomocą następującej aplikacji można eksplorować polecenia publikowania.</span><span class="sxs-lookup"><span data-stu-id="9af8a-152">You can use the following app to explore the publishing commands.</span></span> <span data-ttu-id="9af8a-153">Aplikacja jest tworzona przez uruchomienie następujących poleceń w terminalu:</span><span class="sxs-lookup"><span data-stu-id="9af8a-153">The app is created by running the following commands in your terminal:</span></span>

```dotnetcli
mkdir apptest1
cd apptest1
dotnet new console
dotnet add package Figgle
```

<span data-ttu-id="9af8a-154">Plik `Program.cs` `Program.vb` lub plik wygenerowany przez szablon konsoli musi zostać zmieniony na następujący:</span><span class="sxs-lookup"><span data-stu-id="9af8a-154">The `Program.cs` or `Program.vb` file that is generated by the console template needs to be changed to the following:</span></span>

```csharp
using System;

namespace apptest1
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine(Figgle.FiggleFonts.Standard.Render("Hello, World!"));
        }
    }
}
```

```vb
Module Program
    Sub Main(args As String())
        Console.WriteLine(Figgle.FiggleFonts.Standard.Render("Hello, World!"))
    End Sub
End Module
```

<span data-ttu-id="9af8a-155">Po uruchomieniu aplikacji[`dotnet run`](../tools/dotnet-run.md)( ) wyświetlane są następujące dane wyjściowe:</span><span class="sxs-lookup"><span data-stu-id="9af8a-155">When you run the app ([`dotnet run`](../tools/dotnet-run.md)), the following output is displayed:</span></span>

```terminal
  _   _      _ _         __        __         _     _ _
 | | | | ___| | | ___    \ \      / /__  _ __| | __| | |
 | |_| |/ _ \ | |/ _ \    \ \ /\ / / _ \| '__| |/ _` | |
 |  _  |  __/ | | (_) |    \ V  V / (_) | |  | | (_| |_|
 |_| |_|\___|_|_|\___( )    \_/\_/ \___/|_|  |_|\__,_(_)
                     |/
```

## <a name="framework-dependent-deployment"></a><span data-ttu-id="9af8a-156">Wdrożenie zależne od struktury</span><span class="sxs-lookup"><span data-stu-id="9af8a-156">Framework-dependent deployment</span></span>

<span data-ttu-id="9af8a-157">W przypadku polecenia CLI .NET Core 2.x wdrożenie zależne od struktury (FDD) jest domyślnym trybem dla polecenia podstawowego. `dotnet publish`</span><span class="sxs-lookup"><span data-stu-id="9af8a-157">For the .NET Core SDK 2.x CLI, framework-dependent deployment (FDD) is the default mode for the basic `dotnet publish` command.</span></span>

<span data-ttu-id="9af8a-158">Podczas publikowania aplikacji jako `<PROJECT-NAME>.dll` FDD, plik jest `./bin/<BUILD-CONFIGURATION>/<TFM>/publish/` tworzony w folderze.</span><span class="sxs-lookup"><span data-stu-id="9af8a-158">When you publish your app as an FDD, a `<PROJECT-NAME>.dll` file is created in the `./bin/<BUILD-CONFIGURATION>/<TFM>/publish/` folder.</span></span> <span data-ttu-id="9af8a-159">Aby uruchomić aplikację, przejdź do folderu `dotnet <PROJECT-NAME>.dll` wyjściowego i użyj polecenia.</span><span class="sxs-lookup"><span data-stu-id="9af8a-159">To run your app, navigate to the output folder and use the `dotnet <PROJECT-NAME>.dll` command.</span></span>

<span data-ttu-id="9af8a-160">Aplikacja jest skonfigurowana do kierowania określonej wersji programu .NET Core.</span><span class="sxs-lookup"><span data-stu-id="9af8a-160">Your app is configured to target a specific version of .NET Core.</span></span> <span data-ttu-id="9af8a-161">Ten docelowy program .NET Core runtime jest wymagane na dowolnym komputerze, na którym aplikacja jest uruchamiana.</span><span class="sxs-lookup"><span data-stu-id="9af8a-161">That targeted .NET Core runtime is required to be on any machine where your app runs.</span></span> <span data-ttu-id="9af8a-162">Na przykład jeśli aplikacja jest przeznaczona dla .NET Core 2.2, każdy komputer, na której działa aplikacja, musi mieć zainstalowany czas uruchomieniowy .NET Core 2.2.</span><span class="sxs-lookup"><span data-stu-id="9af8a-162">For example, if your app targets .NET Core 2.2, any machine that your app runs on must have the .NET Core 2.2 runtime installed.</span></span> <span data-ttu-id="9af8a-163">Jak podano w [sekcji Podstawy publikowania,](#publishing-basics) można edytować plik projektu, aby zmienić domyślną strukturę docelową lub kierować więcej niż jedną strukturę.</span><span class="sxs-lookup"><span data-stu-id="9af8a-163">As stated in the [Publishing basics](#publishing-basics) section, you can edit your project file to change the default target framework or to target more than one framework.</span></span>

<span data-ttu-id="9af8a-164">Publikowanie fdd tworzy aplikację, która automatycznie przerzuca dalej do najnowszej poprawki zabezpieczeń .NET Core dostępnej w systemie, w który działa aplikacja.</span><span class="sxs-lookup"><span data-stu-id="9af8a-164">Publishing an FDD creates an app that automatically rolls-forward to the latest .NET Core security patch available on the system that runs the app.</span></span> <span data-ttu-id="9af8a-165">Aby uzyskać więcej informacji na temat powiązania wersji w czasie kompilacji, zobacz [Wybierz wersję .NET Core do użycia](../versions/selection.md#framework-dependent-apps-roll-forward).</span><span class="sxs-lookup"><span data-stu-id="9af8a-165">For more information on version binding at compile time, see [Select the .NET Core version to use](../versions/selection.md#framework-dependent-apps-roll-forward).</span></span>

## <a name="framework-dependent-executable"></a><span data-ttu-id="9af8a-166">Plik wykonywalny zależny od struktury</span><span class="sxs-lookup"><span data-stu-id="9af8a-166">Framework-dependent executable</span></span>

<span data-ttu-id="9af8a-167">Dla polecenia CLI .NET Core SDK 3.x plik wykonywalny zależny `dotnet publish` od struktury (FDE) jest domyślnym trybem polecenia podstawowego.</span><span class="sxs-lookup"><span data-stu-id="9af8a-167">For the .NET Core SDK 3.x CLI, framework-dependent executable (FDE) is the default mode for the basic `dotnet publish` command.</span></span> <span data-ttu-id="9af8a-168">Nie trzeba określać żadnych innych parametrów, tak długo, jak chcesz kierować bieżącego systemu operacyjnego.</span><span class="sxs-lookup"><span data-stu-id="9af8a-168">You don't need to specify any other parameters, as long as you want to target the current operating system.</span></span>

<span data-ttu-id="9af8a-169">W tym trybie host wykonywalny specyficzny dla platformy jest tworzony w celu hosta aplikacji między platformami.</span><span class="sxs-lookup"><span data-stu-id="9af8a-169">In this mode, a platform-specific executable host is created to host your cross-platform app.</span></span> <span data-ttu-id="9af8a-170">Ten tryb jest podobny do FDD, ponieważ FDD `dotnet` wymaga hosta w postaci polecenia.</span><span class="sxs-lookup"><span data-stu-id="9af8a-170">This mode is similar to FDD, as FDD requires a host in the form of the `dotnet` command.</span></span> <span data-ttu-id="9af8a-171">Nazwa pliku wykonywalnego hosta różni się `<PROJECT-FILE>.exe`w zależności od platformy i nosi nazwę podobną do .</span><span class="sxs-lookup"><span data-stu-id="9af8a-171">The host executable filename varies per platform and is named something similar to `<PROJECT-FILE>.exe`.</span></span> <span data-ttu-id="9af8a-172">Ten plik wykonywalny `dotnet <PROJECT-FILE>.dll`można uruchomić bezpośrednio zamiast wywoływania , co nadal jest akceptowalnym sposobem uruchamiania aplikacji.</span><span class="sxs-lookup"><span data-stu-id="9af8a-172">You can run this executable directly instead of calling `dotnet <PROJECT-FILE>.dll`, which is still an acceptable way to run the app.</span></span>

<span data-ttu-id="9af8a-173">Aplikacja jest skonfigurowana do kierowania określonej wersji programu .NET Core.</span><span class="sxs-lookup"><span data-stu-id="9af8a-173">Your app is configured to target a specific version of .NET Core.</span></span> <span data-ttu-id="9af8a-174">Ten docelowy program .NET Core runtime jest wymagane na dowolnym komputerze, na którym aplikacja jest uruchamiana.</span><span class="sxs-lookup"><span data-stu-id="9af8a-174">That targeted .NET Core runtime is required to be on any machine where your app runs.</span></span> <span data-ttu-id="9af8a-175">Na przykład jeśli aplikacja jest przeznaczona dla .NET Core 2.2, każdy komputer, na której działa aplikacja, musi mieć zainstalowany czas uruchomieniowy .NET Core 2.2.</span><span class="sxs-lookup"><span data-stu-id="9af8a-175">For example, if your app targets .NET Core 2.2, any machine that your app runs on must have the .NET Core 2.2 runtime installed.</span></span> <span data-ttu-id="9af8a-176">Jak podano w [sekcji Podstawy publikowania,](#publishing-basics) można edytować plik projektu, aby zmienić domyślną strukturę docelową lub kierować więcej niż jedną strukturę.</span><span class="sxs-lookup"><span data-stu-id="9af8a-176">As stated in the [Publishing basics](#publishing-basics) section, you can edit your project file to change the default target framework or to target more than one framework.</span></span>

<span data-ttu-id="9af8a-177">Publikowanie FDE tworzy aplikację, która automatycznie przerzuca dalej do najnowszej poprawki zabezpieczeń .NET Core dostępnej w systemie uruchamiającym aplikację.</span><span class="sxs-lookup"><span data-stu-id="9af8a-177">Publishing an FDE creates an app that automatically rolls-forward to the latest .NET Core security patch available on the system that runs the app.</span></span> <span data-ttu-id="9af8a-178">Aby uzyskać więcej informacji na temat powiązania wersji w czasie kompilacji, zobacz [Wybierz wersję .NET Core do użycia](../versions/selection.md#framework-dependent-apps-roll-forward).</span><span class="sxs-lookup"><span data-stu-id="9af8a-178">For more information on version binding at compile time, see [Select the .NET Core version to use](../versions/selection.md#framework-dependent-apps-roll-forward).</span></span>

<span data-ttu-id="9af8a-179">W przypadku programu .NET Core 2.2 i wcześniejszych `dotnet publish` należy użyć następujących przełączników z poleceniem do opublikowania FDE:</span><span class="sxs-lookup"><span data-stu-id="9af8a-179">For .NET Core 2.2 and earlier, you must use the following switches with the `dotnet publish` command to publish an FDE:</span></span>

- <span data-ttu-id="9af8a-180">`-r <RID>`Ten przełącznik używa identyfikatora (RID) do określenia platformy docelowej.</span><span class="sxs-lookup"><span data-stu-id="9af8a-180">`-r <RID>` This switch uses an identifier (RID) to specify the target platform.</span></span> <span data-ttu-id="9af8a-181">Aby uzyskać listę identyfikatorów czasu wykonywania, zobacz [Katalog identyfikatorów ustawień wykonywania (RID).](../rid-catalog.md)</span><span class="sxs-lookup"><span data-stu-id="9af8a-181">For a list of runtime identifiers, see [Runtime Identifier (RID) catalog](../rid-catalog.md).</span></span>

- <span data-ttu-id="9af8a-182">`--self-contained false`Ten przełącznik informuje zestaw SDK .NET Core o utworzeniu pliku wykonywalnego jako FDE.</span><span class="sxs-lookup"><span data-stu-id="9af8a-182">`--self-contained false` This switch tells the .NET Core SDK to create an executable as an FDE.</span></span>

<span data-ttu-id="9af8a-183">Za każdym razem, gdy używasz `-r` przełącznika, ścieżka folderu wyjściowego zmienia się na:`./bin/<BUILD-CONFIGURATION>/<TFM>/<RID>/publish/`</span><span class="sxs-lookup"><span data-stu-id="9af8a-183">Whenever you use the `-r` switch, the output folder path changes to: `./bin/<BUILD-CONFIGURATION>/<TFM>/<RID>/publish/`</span></span>

<span data-ttu-id="9af8a-184">Jeśli używasz [przykładowej](#sample-app)aplikacji `dotnet publish -f netcoreapp2.2 -r win10-x64 --self-contained false`, uruchom .</span><span class="sxs-lookup"><span data-stu-id="9af8a-184">If you use the [example app](#sample-app), run `dotnet publish -f netcoreapp2.2 -r win10-x64 --self-contained false`.</span></span> <span data-ttu-id="9af8a-185">To polecenie tworzy następujący plik wykonywalny:`./bin/Debug/netcoreapp2.2/win10-x64/publish/apptest1.exe`</span><span class="sxs-lookup"><span data-stu-id="9af8a-185">This command creates the following executable: `./bin/Debug/netcoreapp2.2/win10-x64/publish/apptest1.exe`</span></span>

> [!NOTE]
> <span data-ttu-id="9af8a-186">Całkowity rozmiar wdrożenia można zmniejszyć, włączając **tryb niezmienny globalizacji.**</span><span class="sxs-lookup"><span data-stu-id="9af8a-186">You can reduce the total size of your deployment by enabling **globalization invariant mode**.</span></span> <span data-ttu-id="9af8a-187">Ten tryb jest przydatny w przypadku aplikacji, które nie są globalnie świadome i które mogą używać konwencji formatowania, konwencji wielkości liter oraz porównania i sortowania ciągów [kultury niezmiennej.](xref:System.Globalization.CultureInfo.InvariantCulture)</span><span class="sxs-lookup"><span data-stu-id="9af8a-187">This mode is useful for applications that are not globally aware and that can use the formatting conventions, casing conventions, and string comparison and sort order of the [invariant culture](xref:System.Globalization.CultureInfo.InvariantCulture).</span></span> <span data-ttu-id="9af8a-188">Aby uzyskać więcej informacji na temat **trybu niezmiennego globalizacji** i sposobu jego włączenia, zobacz [Tryb niezmienny globalizacji .NET .](https://github.com/dotnet/runtime/blob/master/docs/design/features/globalization-invariant-mode.md)</span><span class="sxs-lookup"><span data-stu-id="9af8a-188">For more information about **globalization invariant mode** and how to enable it, see [.NET Core Globalization Invariant Mode](https://github.com/dotnet/runtime/blob/master/docs/design/features/globalization-invariant-mode.md).</span></span>

## <a name="self-contained-deployment"></a><span data-ttu-id="9af8a-189">Samodzielne wdrażanie</span><span class="sxs-lookup"><span data-stu-id="9af8a-189">Self-contained deployment</span></span>

<span data-ttu-id="9af8a-190">Podczas publikowania niezależnego wdrożenia (SCD) zestaw SDK .NET Core tworzy plik wykonywalny specyficzny dla platformy.</span><span class="sxs-lookup"><span data-stu-id="9af8a-190">When you publish a self-contained deployment (SCD), the .NET Core SDK creates a platform-specific executable.</span></span> <span data-ttu-id="9af8a-191">Publikowanie dysku SCD zawiera wszystkie wymagane pliki .NET Core do uruchomienia aplikacji, ale nie zawiera [natywnych zależności .NET Core](https://github.com/dotnet/core/blob/master/Documentation/prereqs.md).</span><span class="sxs-lookup"><span data-stu-id="9af8a-191">Publishing an SCD includes all required .NET Core files to run your app but it doesn't include the [native dependencies of .NET Core](https://github.com/dotnet/core/blob/master/Documentation/prereqs.md).</span></span> <span data-ttu-id="9af8a-192">Te zależności muszą być obecne w systemie przed uruchomieniem aplikacji.</span><span class="sxs-lookup"><span data-stu-id="9af8a-192">These dependencies must be present on the system before the app runs.</span></span>

<span data-ttu-id="9af8a-193">Publikowanie dysku SCD tworzy aplikację, która nie jest przerzucana do najnowszej dostępnej poprawki zabezpieczeń .NET Core.</span><span class="sxs-lookup"><span data-stu-id="9af8a-193">Publishing an SCD creates an app that doesn't roll forward to the latest available .NET Core security patch.</span></span> <span data-ttu-id="9af8a-194">Aby uzyskać więcej informacji na temat powiązania wersji w czasie kompilacji, zobacz [Wybierz wersję .NET Core do użycia](../versions/selection.md#self-contained-deployments-include-the-selected-runtime).</span><span class="sxs-lookup"><span data-stu-id="9af8a-194">For more information on version binding at compile time, see [Select the .NET Core version to use](../versions/selection.md#self-contained-deployments-include-the-selected-runtime).</span></span>

<span data-ttu-id="9af8a-195">Aby opublikować dysk SCD, należy użyć następujących przełączników z `dotnet publish` poleceniem:</span><span class="sxs-lookup"><span data-stu-id="9af8a-195">You must use the following switches with the `dotnet publish` command to publish an SCD:</span></span>

- <span data-ttu-id="9af8a-196">`-r <RID>`Ten przełącznik używa identyfikatora (RID) do określenia platformy docelowej.</span><span class="sxs-lookup"><span data-stu-id="9af8a-196">`-r <RID>` This switch uses an identifier (RID) to specify the target platform.</span></span> <span data-ttu-id="9af8a-197">Aby uzyskać listę identyfikatorów czasu wykonywania, zobacz [Katalog identyfikatorów ustawień wykonywania (RID).](../rid-catalog.md)</span><span class="sxs-lookup"><span data-stu-id="9af8a-197">For a list of runtime identifiers, see [Runtime Identifier (RID) catalog](../rid-catalog.md).</span></span>

- <span data-ttu-id="9af8a-198">`--self-contained true`Ten przełącznik informuje zestaw SDK .NET Core o utworzeniu pliku wykonywalnego jako dysku SCD.</span><span class="sxs-lookup"><span data-stu-id="9af8a-198">`--self-contained true` This switch tells the .NET Core SDK to create an executable as an SCD.</span></span>

> [!NOTE]
> <span data-ttu-id="9af8a-199">Całkowity rozmiar wdrożenia można zmniejszyć, włączając **tryb niezmienny globalizacji.**</span><span class="sxs-lookup"><span data-stu-id="9af8a-199">You can reduce the total size of your deployment by enabling **globalization invariant mode**.</span></span> <span data-ttu-id="9af8a-200">Ten tryb jest przydatny w przypadku aplikacji, które nie są globalnie świadome i które mogą używać konwencji formatowania, konwencji wielkości liter oraz porównania i sortowania ciągów [kultury niezmiennej.](xref:System.Globalization.CultureInfo.InvariantCulture)</span><span class="sxs-lookup"><span data-stu-id="9af8a-200">This mode is useful for applications that are not globally aware and that can use the formatting conventions, casing conventions, and string comparison and sort order of the [invariant culture](xref:System.Globalization.CultureInfo.InvariantCulture).</span></span> <span data-ttu-id="9af8a-201">Aby uzyskać więcej informacji na temat **trybu niezmiennego globalizacji** i sposobu jego włączenia, zobacz [Tryb niezmienny globalizacji .NET .](https://github.com/dotnet/runtime/blob/master/docs/design/features/globalization-invariant-mode.md)</span><span class="sxs-lookup"><span data-stu-id="9af8a-201">For more information about **globalization invariant mode** and how to enable it, see [.NET Core Globalization Invariant Mode](https://github.com/dotnet/runtime/blob/master/docs/design/features/globalization-invariant-mode.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="9af8a-202">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="9af8a-202">See also</span></span>

- [<span data-ttu-id="9af8a-203">Omówienie wdrażania aplikacji podstawowej .NET</span><span class="sxs-lookup"><span data-stu-id="9af8a-203">.NET Core Application Deployment Overview</span></span>](index.md)
- [<span data-ttu-id="9af8a-204">Wykaz identyfikatora środowiska uruchomienionowego programu .NET Core (RID)</span><span class="sxs-lookup"><span data-stu-id="9af8a-204">.NET Core Runtime IDentifier (RID) catalog</span></span>](../rid-catalog.md)
