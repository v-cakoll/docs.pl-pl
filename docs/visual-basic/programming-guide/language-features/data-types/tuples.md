---
title: "Spójne kolekcje w języku Visual Basic"
ms.custom: 
ms.date: 04/23/2017
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: devlang-visual-basic
ms.topic: article
helpviewer_keywords: tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: be50b22e9acca9ff8cfbde798d78869ee1c72634
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 10/18/2017
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="6a0a0-102">Krotki (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="6a0a0-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="6a0a0-103">Począwszy od programu Visual Basic 2017 języka Visual Basic udostępnia wbudowaną obsługę krotek, która umożliwia tworzenie krotek i uzyskiwanie dostępu do elementów krotek łatwiejsze.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="6a0a0-104">Krotka jest strukturą lekki danych, która ma określoną liczbę i sekwencja wartości.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-104">A tuple is a light-weight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="6a0a0-105">W przypadku wystąpienia spójnej kolekcji, należy zdefiniować liczbę i typ danych w każdej wartości (lub elementu).</span><span class="sxs-lookup"><span data-stu-id="6a0a0-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="6a0a0-106">Na przykład krotki 2 (lub parę) zawiera dwa elementy.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="6a0a0-107">Może być pierwszym `Boolean` wartości, podczas gdy druga `String`.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="6a0a0-108">Ponieważ spójne kolekcje ułatwiają przechowywanie wielu wartości w jednym obiekcie, często są one używane jako lekkie sposób zwrócenia wiele wartości z metody.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="6a0a0-109">Obsługa krotki wymaga <xref:System.ValueTuple> typu.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="6a0a0-110">.NET Framework 4.7 nie jest zainstalowany, należy dodać pakiet NuGet `System.ValueTuple`, która jest dostępna w galerii NuGet.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="6a0a0-111">Bez tego pakietu może wystąpić błąd kompilacji podobne do "Wstępnie zdefiniowany typ"ValueTuple(Of,,,)"nie został zdefiniowany ani zaimportowany."</span><span class="sxs-lookup"><span data-stu-id="6a0a0-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="6a0a0-112">Tworzenie wystąpień i przy użyciu spójnych kolekcji</span><span class="sxs-lookup"><span data-stu-id="6a0a0-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="6a0a0-113">Umieszczając nawiasami wiadomości błyskawicznych wartości rozdzielanych przecinkami Utwórz wystąpienie spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="6a0a0-114">Każdy z tych wartości staje się pole spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="6a0a0-115">Na przykład poniższy kod definiuje triple (lub krotki 3) z `Date` jako wartość pierwszego `String` jako jego, a `Boolean` jako jego innych.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="6a0a0-116">Domyślnie nazwa każdego pola w krotce składa się z ciągu `Item` wraz z pozycji na podstawie jednego pola w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="6a0a0-117">Dla tego krotki 3 `Date` pole jest `Item1`, `String` pole jest `Item2`i `Boolean` pole jest `Item3`.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="6a0a0-118">W poniższym przykładzie przedstawiono wartości pól spójna kolekcja znajdująca się w poprzednim wierszu kodu</span><span class="sxs-lookup"><span data-stu-id="6a0a0-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="6a0a0-119">Pola spójnych kolekcji Visual Basic są odczytu i zapisu; Po krotka został uruchomiony, można modyfikować jej wartości.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="6a0a0-120">Poniższy przykład modyfikuje dwie z trzech pól krotki utworzony w poprzednim przykładzie i wyświetla wyniki.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="6a0a0-121">Tworzenie wystąpień i przy użyciu spójnej kolekcji o nazwie</span><span class="sxs-lookup"><span data-stu-id="6a0a0-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="6a0a0-122">Zamiast przy użyciu domyślnych nazw pól spójnej kolekcji, można utworzyć wystąpienia *o nazwie krotki* przez przypisanie własne nazwy elementów spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="6a0a0-123">Pola krotki mają dostęp przy użyciu nazw przypisane *lub* przez domyślne nazwy.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="6a0a0-124">Poniższy przykład tworzy wystąpienie tego samego krotki 3 jak poprzednio, z wyjątkiem tego, że jawnie nazwy pierwsze pole `EventDate`, drugi `Name`, a trzeci `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="6a0a0-125">Następnie wyświetla wartości pól, modyfikuje je i ponownie wyświetla wartości pól.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="tuples-versus-structures"></a><span data-ttu-id="6a0a0-126">Spójne kolekcje i struktury</span><span class="sxs-lookup"><span data-stu-id="6a0a0-126">Tuples versus structures</span></span>

<span data-ttu-id="6a0a0-127">Krotka Visual Basic jest typem wartości, który jest wystąpieniem jednej z **System.ValueTuple** typów ogólnych.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-127">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="6a0a0-128">Na przykład `holiday` krotki zdefiniowane w poprzednim przykładzie jest wystąpieniem <xref:System.ValueTuple%603> struktury.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-128">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="6a0a0-129">Zaprojektowano go jako lekkie kontener dla danych.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-129">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="6a0a0-130">Ponieważ spójnej kolekcji ma na celu ułatwić tworzenie obiektu z wieloma elementami danych, brakuje niektórych funkcji, które mogą mieć strukturze niestandardowej.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-130">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="6a0a0-131">Należą do nich następujące elementy:</span><span class="sxs-lookup"><span data-stu-id="6a0a0-131">These include:</span></span>

- <span data-ttu-id="6a0a0-132">Elementy członkowskie klienta.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-132">Customer members.</span></span> <span data-ttu-id="6a0a0-133">Nie można definiować własnych właściwości, metody lub zdarzenia dla spójnych kolekcji.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-133">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="6a0a0-134">Sprawdzanie poprawności.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-134">Validation.</span></span> <span data-ttu-id="6a0a0-135">Nie można sprawdzić poprawności danych przypisany do pola.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-135">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="6a0a0-136">Immutability.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-136">Immutability.</span></span> <span data-ttu-id="6a0a0-137">Modyfikowalne są krotek Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-137">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="6a0a0-138">Natomiast w strukturze niestandardowej umożliwia kontrolowanie, czy wystąpienie jest modyfikowalna lub modyfikować.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-138">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="6a0a0-139">Jeśli niestandardowe elementy członkowskie, właściwość i pole weryfikacji lub immutability są ważne, należy użyć programu Visual Basic [struktury](../../../language-reference/statements/structure-statement.md) instrukcji, aby zdefiniować typu wartości niestandardowych.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-139">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="6a0a0-140">Krotka Visual Basic dziedziczą członkowie jego **ValueTuple** typu.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-140">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="6a0a0-141">Oprócz pól należą do nich następujące metody:</span><span class="sxs-lookup"><span data-stu-id="6a0a0-141">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="6a0a0-142">Element członkowski</span><span class="sxs-lookup"><span data-stu-id="6a0a0-142">Member</span></span> | <span data-ttu-id="6a0a0-143">Opis</span><span class="sxs-lookup"><span data-stu-id="6a0a0-143">Description</span></span> |
| ---|---|
| <span data-ttu-id="6a0a0-144">Wykonanie funkcji CompareTo</span><span class="sxs-lookup"><span data-stu-id="6a0a0-144">CompareTo</span></span> | <span data-ttu-id="6a0a0-145">Porównuje bieżącą spójną kolekcję do innego krotki z taką samą liczbę elementów.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-145">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="6a0a0-146">równa się</span><span class="sxs-lookup"><span data-stu-id="6a0a0-146">Equals</span></span> | <span data-ttu-id="6a0a0-147">Określa, czy bieżącą spójną kolekcję jest równa innego spójnej kolekcji lub obiektu.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-147">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="6a0a0-148">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="6a0a0-148">GetHashCode</span></span> | <span data-ttu-id="6a0a0-149">Oblicza wartość skrótu dla bieżącego wystąpienia.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-149">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="6a0a0-150">ToString</span><span class="sxs-lookup"><span data-stu-id="6a0a0-150">ToString</span></span> | <span data-ttu-id="6a0a0-151">Zwraca reprezentację ciągu tego spójnej kolekcji, która ma postać `(Item1, Item2...)`, gdzie `Item1` i `Item2` reprezentują wartości pól spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-151">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="6a0a0-152">Ponadto **ValueTuple** typy implementować <xref:System.Collections.IStructuralComparable> i <xref:System.Collections.IStructuralEquatable> interfejsów, które umożliwiają definiowanie comparers klienta.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-152">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="6a0a0-153">Przypisanie i spójnych kolekcji</span><span class="sxs-lookup"><span data-stu-id="6a0a0-153">Assignment and tuples</span></span>

<span data-ttu-id="6a0a0-154">Visual Basic obsługuje przypisanie między typami spójnej kolekcji, które mają taką samą liczbę pól.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-154">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="6a0a0-155">Typy pól mogą być konwertowane, jeśli spełniony jest jeden z następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="6a0a0-155">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="6a0a0-156">W polu źródłowa i docelowa są tego samego typu.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-156">The source and target field are of the same type.</span></span>

- <span data-ttu-id="6a0a0-157">Zdefiniowano konwersji rozszerzającej (lub niejawne) typu źródłowego na typ docelowy.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-157">A widening (or implicit) conversion of the source type to the target type is defined.</span></span> 

- <span data-ttu-id="6a0a0-158">`Option Strict`jest `On`, a zdefiniowano konwersji zawężającej (bezpośredniego lub pośredniego) typu źródłowego na typ docelowy.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-158">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="6a0a0-159">Ta konwersja może zgłoszenia wyjątku, jeśli wartością źródłową jest poza zakresem typu docelowego.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-159">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="6a0a0-160">Inne konwersje nie są uznawane za przydziałów.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-160">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="6a0a0-161">Oto typy przydziałów, które mogą między typami spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-161">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="6a0a0-162">Należy wziąć pod uwagę następujące zmienne używane w poniższych przykładach:</span><span class="sxs-lookup"><span data-stu-id="6a0a0-162">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="6a0a0-163">Pierwsze dwie zmienne `unnamed` i `anonymous`, nie ma nazwy semantycznego podana dla pola.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-163">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="6a0a0-164">Ich nazwy pól są domyślnymi `Item1` i `Item2`.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-164">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="6a0a0-165">Ostatnie dwie zmienne `named` i `differentName` mają nazwy pól semantycznego.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-165">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="6a0a0-166">Należy pamiętać, że te dwie spójne kolekcje mają różne nazwy pól.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-166">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="6a0a0-167">Wszystkie cztery te krotki mają taką samą liczbę pól (określanych jako "Liczba ról") i typy pól są identyczne.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-167">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="6a0a0-168">W związku z tym wszystkie te przydziały działają:</span><span class="sxs-lookup"><span data-stu-id="6a0a0-168">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="6a0a0-169">Należy zauważyć, że nazwy krotki nie są przypisane.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-169">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="6a0a0-170">Wartości pól są przypisywane kolejności pól w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-170">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="6a0a0-171">Warto zauważyć, że firma Microsoft można przypisać `named` spójnej kolekcji do `conversion` spójnej kolekcji, nawet jeśli pierwsze pole `named` jest `Integer`, a pierwsze pole `conversion` jest `Long`.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-171">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="6a0a0-172">To przypisanie zakończy się pomyślnie, ponieważ konwersja `Integer` do `Long` jest konwersję rozszerzającą.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-172">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="6a0a0-173">Spójnych kolekcji zawierający różne liczby pól nie są można przypisać:</span><span class="sxs-lookup"><span data-stu-id="6a0a0-173">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="6a0a0-174">Wartości zwracane krotek jako — metoda</span><span class="sxs-lookup"><span data-stu-id="6a0a0-174">Tuples as method return values</span></span>

<span data-ttu-id="6a0a0-175">Metoda może zwracać tylko jedną wartość.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-175">A method can return only a single value.</span></span> <span data-ttu-id="6a0a0-176">Często jednak chcesz wywołania metody, aby zwrócić wielu wartości.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-176">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="6a0a0-177">Istnieje kilka sposobów obejścia tego ograniczenia:</span><span class="sxs-lookup"><span data-stu-id="6a0a0-177">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="6a0a0-178">Możesz tworzyć niestandardowe klasy lub struktury którego właściwości lub pola reprezentują wartości zwracanych przez metodę.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-178">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="6a0a0-179">W związku z tym jest to rozwiązanie ogromnych; wymaga zdefiniowania typu niestandardowego, którego jedynym celem jest można pobrać wartości z wywołania metody.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-179">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="6a0a0-180">Zwraca pojedynczą wartość z metody i pozostałe wartości zwracane przez przekazywanie ich poprzez odwołanie do metody.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-180">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="6a0a0-181">Obejmuje to obciążenie tworzenia wystąpienia w zmiennej i ryzyka nieodwracalne nadpisanie wartość zmiennej, który jest przekazywany przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-181">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="6a0a0-182">Można użyć spójnej kolekcji, która to lekkie rozwiązanie do pobierania wiele wartości zwrotnych.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-182">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="6a0a0-183">Na przykład **TryParse** metod .NET powrotu `Boolean` wartość, która wskazuje, czy podczas analizowania operacja zakończyła się pomyślnie.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-183">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="6a0a0-184">Wynik operacji przetwarzania jest zwracana w zmiennej przekazywana przez odwołanie do metody.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-184">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="6a0a0-185">Zazwyczaj wywołanie metodę analizowania, takich jak <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> wygląda podobnie do następującego:</span><span class="sxs-lookup"><span data-stu-id="6a0a0-185">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="6a0a0-186">Zostanie zwrócona krotka z przetwarzaniem operacji, jeśli firma Microsoft zawijać wywołanie <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> metody własnej metody.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-186">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="6a0a0-187">W poniższym przykładzie `NumericLibrary.ParseInteger` wywołania <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> metodę i zwraca spójną kolekcję o nazwie z dwoma elementami.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-187">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span> 

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="6a0a0-188">Następnie można wywołać metody z kodu podobne do poniższych:</span><span class="sxs-lookup"><span data-stu-id="6a0a0-188">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="6a0a0-189">Krotki Visual Basic i spójnych kolekcji w programie .NET Framework</span><span class="sxs-lookup"><span data-stu-id="6a0a0-189">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="6a0a0-190">Krotka Visual Basic jest wystąpienie jednego z **System.ValueTuple** typów ogólnych, które zostały wprowadzone w .NET Framework 4.7.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-190">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="6a0a0-191">.NET Framework obejmuje również zestaw ogólny **System.Tuple** klasy.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-191">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="6a0a0-192">Te klasy jednak różnić się od krotek Visual Basic i **System.ValueTuple** typów ogólnych na kilka sposobów:</span><span class="sxs-lookup"><span data-stu-id="6a0a0-192">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="6a0a0-193">Elementy **krotki** klasy są właściwości o nazwie `Item1`, `Item2`i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-193">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="6a0a0-194">W Visual Basic krotek i **ValueTuple** pola są typy elementów spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-194">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="6a0a0-195">Nie można przypisać łatwy do rozpoznania nazwy elementów **krotki** wystąpienia lub **ValueTuple** wystąpienia.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-195">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="6a0a0-196">Visual Basic umożliwia przypisanie nazw, które komunikują się znaczenie pola.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-196">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="6a0a0-197">Właściwości **krotki** wystąpienia są tylko do odczytu; spójne kolekcje są niezmienne.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-197">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="6a0a0-198">W Visual Basic krotek i **ValueTuple** typów, pól krotki są odczytu i zapisu; spójne kolekcje są modyfikowalne.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-198">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="6a0a0-199">Ogólnego **krotki** typy są typy referencyjne.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-199">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="6a0a0-200">Korzystanie z tych **krotki** typy oznacza Alokacja obiektów.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-200">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="6a0a0-201">Na gorąco ścieżki to ma zauważalnego wpływu na wydajność aplikacji.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-201">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="6a0a0-202">Krotki Visual Basic i **ValueTuple** typy są typami wartości.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-202">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="6a0a0-203">Metody rozszerzenia w <xref:System.TupleExtensions> klasy ułatwia konwersję między krotek Visual Basic .NET **krotki** obiektów.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-203">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="6a0a0-204">**ToTuple** metoda konwertuje spójnych kolekcji Visual Basic .NET **krotki** obiektu i **ToValueTuple** metoda konwertuje .NET **krotki** obiekt do spójnych kolekcji Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-204">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="6a0a0-205">Poniższy przykład tworzy spójną kolekcję, konwertuje ją na .NET **krotki** obiektu i konwertuje ją z powrotem do spójnych kolekcji Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-205">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="6a0a0-206">Przykład następnie porównuje to krotki z oryginału, aby upewnić się, że są one takie same.</span><span class="sxs-lookup"><span data-stu-id="6a0a0-206">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="6a0a0-207">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="6a0a0-207">See also</span></span>

[<span data-ttu-id="6a0a0-208">Dokumentacja języka Visual Basic</span><span class="sxs-lookup"><span data-stu-id="6a0a0-208">Visual Basic Language Reference</span></span>](index.md)  
