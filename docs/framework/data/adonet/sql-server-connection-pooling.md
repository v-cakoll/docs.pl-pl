---
title: Buforowanie połączeń programu SQL Server
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: 7e51d44e-7c4e-4040-9332-f0190fe36f07
ms.openlocfilehash: 149511bd4e84baabf11eca014257127b587830df
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/12/2020
ms.locfileid: "79149001"
---
# <a name="sql-server-connection-pooling-adonet"></a><span data-ttu-id="b784b-102">Buforowanie połączenia z programem SQL Server (ADO.NET)</span><span class="sxs-lookup"><span data-stu-id="b784b-102">SQL Server Connection Pooling (ADO.NET)</span></span>
<span data-ttu-id="b784b-103">Łączenie się z serwerem bazy danych zazwyczaj składa się z kilku czasochłonnych kroków.</span><span class="sxs-lookup"><span data-stu-id="b784b-103">Connecting to a database server typically consists of several time-consuming steps.</span></span> <span data-ttu-id="b784b-104">Kanał fizyczny, taki jak gniazdo lub nazwany potok, musi nastąpić początkowy uścisk dłoni z serwerem, informacje o ciągu połączenia muszą być analizowane, połączenie musi być uwierzytelnione przez serwer, kontrole muszą być uruchamiane w celu zarejestrowania w bieżącej transakcji itd.</span><span class="sxs-lookup"><span data-stu-id="b784b-104">A physical channel such as a socket or a named pipe must be established, the initial handshake with the server must occur, the connection string information must be parsed, the connection must be authenticated by the server, checks must be run for enlisting in the current transaction, and so on.</span></span>  
  
 <span data-ttu-id="b784b-105">W praktyce większość aplikacji używa tylko jednej lub kilku różnych konfiguracji dla połączeń.</span><span class="sxs-lookup"><span data-stu-id="b784b-105">In practice, most applications use only one or a few different configurations for connections.</span></span> <span data-ttu-id="b784b-106">Oznacza to, że podczas wykonywania aplikacji wiele identycznych połączeń będzie wielokrotnie otwieranych i zamykanych.</span><span class="sxs-lookup"><span data-stu-id="b784b-106">This means that during application execution, many identical connections will be repeatedly opened and closed.</span></span> <span data-ttu-id="b784b-107">Aby zminimalizować koszt otwierania połączeń, ADO.NET stosuje technikę optymalizacji zwaną *buforowaniem połączeń*.</span><span class="sxs-lookup"><span data-stu-id="b784b-107">To minimize the cost of opening connections, ADO.NET uses an optimization technique called *connection pooling*.</span></span>  
  
 <span data-ttu-id="b784b-108">Buforowanie połączeń zmniejsza liczbę nowych połączeń.</span><span class="sxs-lookup"><span data-stu-id="b784b-108">Connection pooling reduces the number of times that new connections must be opened.</span></span> <span data-ttu-id="b784b-109">*Pooler* zachowuje własność połączenia fizycznego.</span><span class="sxs-lookup"><span data-stu-id="b784b-109">The *pooler* maintains ownership of the physical connection.</span></span> <span data-ttu-id="b784b-110">Zarządza połączeniami, utrzymując przy życiu zestaw aktywnych połączeń dla każdej konfiguracji danego połączenia.</span><span class="sxs-lookup"><span data-stu-id="b784b-110">It manages connections by keeping alive a set of active connections for each given connection configuration.</span></span> <span data-ttu-id="b784b-111">Za każdym razem, gdy użytkownik wywołuje `Open` połączenie, pooler szuka dostępnego połączenia w puli.</span><span class="sxs-lookup"><span data-stu-id="b784b-111">Whenever a user calls `Open` on a connection, the pooler looks for an available connection in the pool.</span></span> <span data-ttu-id="b784b-112">Jeśli połączenie w puli jest dostępne, zwraca go do obiektu wywołującego zamiast otwierania nowego połączenia.</span><span class="sxs-lookup"><span data-stu-id="b784b-112">If a pooled connection is available, it returns it to the caller instead of opening a new connection.</span></span> <span data-ttu-id="b784b-113">Gdy aplikacja `Close` wywołuje połączenie, pooler zwraca go do puli zestaw aktywnych połączeń zamiast zamykania go.</span><span class="sxs-lookup"><span data-stu-id="b784b-113">When the application calls `Close` on the connection, the pooler returns it to the pooled set of active connections instead of closing it.</span></span> <span data-ttu-id="b784b-114">Po powrocie połączenia do puli, jest gotowy do ponownego `Open` użycia przy następnym wywołaniu.</span><span class="sxs-lookup"><span data-stu-id="b784b-114">Once the connection is returned to the pool, it is ready to be reused on the next `Open` call.</span></span>  
  
 <span data-ttu-id="b784b-115">Tylko połączenia z tą samą konfiguracją mogą być łączone.</span><span class="sxs-lookup"><span data-stu-id="b784b-115">Only connections with the same configuration can be pooled.</span></span> <span data-ttu-id="b784b-116">ADO.NET przechowuje kilka pul jednocześnie, po jednym dla każdej konfiguracji.</span><span class="sxs-lookup"><span data-stu-id="b784b-116">ADO.NET keeps several pools at the same time, one for each configuration.</span></span> <span data-ttu-id="b784b-117">Połączenia są podzielone na pule według ciągu połączenia i przez tożsamość systemu Windows, gdy używane są zintegrowane zabezpieczenia.</span><span class="sxs-lookup"><span data-stu-id="b784b-117">Connections are separated into pools by connection string, and by Windows identity when integrated security is used.</span></span> <span data-ttu-id="b784b-118">Połączenia są również łączone na podstawie tego, czy są one zarejestrowane w transakcji.</span><span class="sxs-lookup"><span data-stu-id="b784b-118">Connections are also pooled based on whether they are enlisted in a transaction.</span></span> <span data-ttu-id="b784b-119">Podczas <xref:System.Data.SqlClient.SqlConnection.ChangePassword%2A>korzystania <xref:System.Data.SqlClient.SqlCredential> wystąpienie wpływa na pulę połączeń.</span><span class="sxs-lookup"><span data-stu-id="b784b-119">When using <xref:System.Data.SqlClient.SqlConnection.ChangePassword%2A>, the <xref:System.Data.SqlClient.SqlCredential> instance affects the connection pool.</span></span> <span data-ttu-id="b784b-120">Różne wystąpienia <xref:System.Data.SqlClient.SqlCredential> będą używać różnych pul połączeń, nawet jeśli identyfikator użytkownika i hasło są takie same.</span><span class="sxs-lookup"><span data-stu-id="b784b-120">Different instances of <xref:System.Data.SqlClient.SqlCredential> will use different connection pools, even if the user ID and password are the same.</span></span>  
  
 <span data-ttu-id="b784b-121">Łączenie połączeń może znacznie zwiększyć wydajność i skalowalność aplikacji.</span><span class="sxs-lookup"><span data-stu-id="b784b-121">Pooling connections can significantly enhance the performance and scalability of your application.</span></span> <span data-ttu-id="b784b-122">Domyślnie buforowanie połączeń jest włączone w ADO.NET.</span><span class="sxs-lookup"><span data-stu-id="b784b-122">By default, connection pooling is enabled in ADO.NET.</span></span> <span data-ttu-id="b784b-123">Jeśli nie zostanie jawnie wyłączyć, pooler optymalizuje połączenia, ponieważ są one otwarte i zamknięte w aplikacji.</span><span class="sxs-lookup"><span data-stu-id="b784b-123">Unless you explicitly disable it, the pooler optimizes the connections as they are opened and closed in your application.</span></span> <span data-ttu-id="b784b-124">Można również podać kilka modyfikatorów ciągu połączenia, aby kontrolować zachowanie buforowania połączeń.</span><span class="sxs-lookup"><span data-stu-id="b784b-124">You can also supply several connection string modifiers to control connection pooling behavior.</span></span> <span data-ttu-id="b784b-125">Aby uzyskać więcej informacji, zobacz "Kontrolowanie buforowania połączeń za pomocą słów kluczowych ciągu połączenia" w dalszej części tego tematu.</span><span class="sxs-lookup"><span data-stu-id="b784b-125">For more information, see "Controlling Connection Pooling with Connection String Keywords" later in this topic.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="b784b-126">Gdy buforowanie połączeń jest włączone i jeśli wystąpi błąd limitu czasu lub inny błąd logowania, zostanie zgłoszony wyjątek, a kolejne próby połączenia nie powiodą się przez następne pięć sekund, "okres blokowania".</span><span class="sxs-lookup"><span data-stu-id="b784b-126">When connection pooling is enabled, and if a timeout error or other login error occurs, an exception will be thrown and subsequent connection attempts will fail for the next five seconds, the "blocking period".</span></span> <span data-ttu-id="b784b-127">Jeśli aplikacja próbuje połączyć się w okresie blokowania, pierwszy wyjątek zostanie zgłoszony ponownie.</span><span class="sxs-lookup"><span data-stu-id="b784b-127">If the application attempts to connect within the blocking period, the first exception will be thrown again.</span></span> <span data-ttu-id="b784b-128">Kolejne awarie po zakończeniu okresu blokowania spowoduje nowe okresy blokowania, który jest dwa razy dłuższy niż poprzedni okres blokowania, maksymalnie do jednej minuty.</span><span class="sxs-lookup"><span data-stu-id="b784b-128">Subsequent failures after a blocking period ends will result in a new blocking periods that is twice as long as the previous blocking period, up to a maximum of one minute.</span></span>  
  
## <a name="pool-creation-and-assignment"></a><span data-ttu-id="b784b-129">Tworzenie i przypisywanie puli</span><span class="sxs-lookup"><span data-stu-id="b784b-129">Pool Creation and Assignment</span></span>  
 <span data-ttu-id="b784b-130">Po pierwszym otwarciu połączenia pula połączeń jest tworzona na podstawie algorytmu dokładnego dopasowywania, który kojarzy pulę z ciągiem połączenia w połączeniu.</span><span class="sxs-lookup"><span data-stu-id="b784b-130">When a connection is first opened, a connection pool is created based on an exact matching algorithm that associates the pool with the connection string in the connection.</span></span> <span data-ttu-id="b784b-131">Każda pula połączeń jest skojarzona z odrębnym ciągiem połączenia.</span><span class="sxs-lookup"><span data-stu-id="b784b-131">Each connection pool is associated with a distinct connection string.</span></span> <span data-ttu-id="b784b-132">Po otwarciu nowego połączenia, jeśli parametry połączenia nie jest dokładne dopasowanie do istniejącej puli, tworzony jest nowy puli.</span><span class="sxs-lookup"><span data-stu-id="b784b-132">When a new connection is opened, if the connection string is not an exact match to an existing pool, a new pool is created.</span></span> <span data-ttu-id="b784b-133">Połączenia są łączone według procesu, domeny aplikacji, ciągu połączenia i gdy używane są zintegrowane zabezpieczenia, według tożsamości systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="b784b-133">Connections are pooled per process, per application domain, per connection string and when integrated security is used, per Windows identity.</span></span> <span data-ttu-id="b784b-134">Parametry połączenia muszą być również dokładne dopasowanie; słowa kluczowe podane w innej kolejności dla tego samego połączenia będą łączone oddzielnie.</span><span class="sxs-lookup"><span data-stu-id="b784b-134">Connection strings must also be an exact match; keywords supplied in a different order for the same connection will be pooled separately.</span></span>  
  
 <span data-ttu-id="b784b-135">W poniższym przykładzie języka <xref:System.Data.SqlClient.SqlConnection> C# tworzone są trzy nowe obiekty, ale tylko dwa pule połączeń są wymagane do zarządzania nimi.</span><span class="sxs-lookup"><span data-stu-id="b784b-135">In the following C# example, three new <xref:System.Data.SqlClient.SqlConnection> objects are created, but only two connection pools are required to manage them.</span></span> <span data-ttu-id="b784b-136">Należy zauważyć, że pierwszy i drugi ciąg połączeń `Initial Catalog`różnią się wartością przypisaną do .</span><span class="sxs-lookup"><span data-stu-id="b784b-136">Note that the first and second connection strings differ by the value assigned for `Initial Catalog`.</span></span>  
  
```csharp
using (SqlConnection connection = new SqlConnection(  
  "Integrated Security=SSPI;Initial Catalog=Northwind"))  
    {  
        connection.Open();
        // Pool A is created.  
    }  
  
using (SqlConnection connection = new SqlConnection(  
  "Integrated Security=SSPI;Initial Catalog=pubs"))  
    {  
        connection.Open();
        // Pool B is created because the connection strings differ.  
    }  
  
using (SqlConnection connection = new SqlConnection(  
  "Integrated Security=SSPI;Initial Catalog=Northwind"))  
    {  
        connection.Open();
        // The connection string matches pool A.  
    }  
```  
  
 <span data-ttu-id="b784b-137">Jeśli `MinPoolSize` nie jest określony w ciągu połączenia lub jest określony jako zero, połączenia w puli zostaną zamknięte po okresie braku aktywności.</span><span class="sxs-lookup"><span data-stu-id="b784b-137">If `MinPoolSize` is either not specified in the connection string or is specified as zero, the connections in the pool will be closed after a period of inactivity.</span></span> <span data-ttu-id="b784b-138">Jednak jeśli określony `MinPoolSize` jest większy niż zero, pula połączeń `AppDomain` nie jest niszczony, dopóki nie zostanie zwolniony i proces kończy.</span><span class="sxs-lookup"><span data-stu-id="b784b-138">However, if the specified `MinPoolSize` is greater than zero, the connection pool is not destroyed until the `AppDomain` is unloaded and the process ends.</span></span> <span data-ttu-id="b784b-139">Konserwacja nieaktywnych lub pustych pul wiąże się z minimalnym obciążeniem systemu.</span><span class="sxs-lookup"><span data-stu-id="b784b-139">Maintenance of inactive or empty pools involves minimal system overhead.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="b784b-140">Pula jest automatycznie czyszczona po wystąpieniu błędu krytycznego, takiego jak przełącznie w stan failover.</span><span class="sxs-lookup"><span data-stu-id="b784b-140">The pool is automatically cleared when a fatal error occurs, such as a failover.</span></span>  
  
## <a name="adding-connections"></a><span data-ttu-id="b784b-141">Dodawanie połączeń</span><span class="sxs-lookup"><span data-stu-id="b784b-141">Adding Connections</span></span>  
 <span data-ttu-id="b784b-142">Pula połączeń jest tworzona dla każdego unikatowego ciągu połączenia.</span><span class="sxs-lookup"><span data-stu-id="b784b-142">A connection pool is created for each unique connection string.</span></span> <span data-ttu-id="b784b-143">Podczas tworzenia puli, wiele obiektów połączenia są tworzone i dodawane do puli, tak aby spełnione jest minimalne wymagania dotyczące rozmiaru puli.</span><span class="sxs-lookup"><span data-stu-id="b784b-143">When a pool is created, multiple connection objects are created and added to the pool so that the minimum pool size requirement is satisfied.</span></span> <span data-ttu-id="b784b-144">Połączenia są dodawane do puli w razie potrzeby, do maksymalnego rozmiaru puli określony (100 jest ustawieniem domyślnym).</span><span class="sxs-lookup"><span data-stu-id="b784b-144">Connections are added to the pool as needed, up to the maximum pool size specified (100 is the default).</span></span> <span data-ttu-id="b784b-145">Połączenia są zwalniane z powrotem do puli, gdy są zamknięte lub usunięte.</span><span class="sxs-lookup"><span data-stu-id="b784b-145">Connections are released back into the pool when they are closed or disposed.</span></span>  
  
 <span data-ttu-id="b784b-146">Gdy <xref:System.Data.SqlClient.SqlConnection> obiekt jest wymagany, jest uzyskiwany z puli, jeśli dostępne jest połączenie użyteczne.</span><span class="sxs-lookup"><span data-stu-id="b784b-146">When a <xref:System.Data.SqlClient.SqlConnection> object is requested, it is obtained from the pool if a usable connection is available.</span></span> <span data-ttu-id="b784b-147">Aby można było konfigurować, połączenie musi być nieużywane, mieć pasujący kontekst transakcji lub być niezwiązane z żadnym kontekstem transakcji i mieć prawidłowe łącze do serwera.</span><span class="sxs-lookup"><span data-stu-id="b784b-147">To be usable, a connection must be unused, have a matching transaction context or be unassociated with any transaction context, and have a valid link to the server.</span></span>  
  
 <span data-ttu-id="b784b-148">Pooler połączenia spełnia żądania połączeń przez ponowne przydzielanie połączeń, ponieważ są one zwalniane z powrotem do puli.</span><span class="sxs-lookup"><span data-stu-id="b784b-148">The connection pooler satisfies requests for connections by reallocating connections as they are released back into the pool.</span></span> <span data-ttu-id="b784b-149">Jeśli osiągnięto maksymalny rozmiar puli i nie jest dostępne połączenie użytkowe, żądanie jest umieszczane w kolejce.</span><span class="sxs-lookup"><span data-stu-id="b784b-149">If the maximum pool size has been reached and no usable connection is available, the request is queued.</span></span> <span data-ttu-id="b784b-150">Pooler następnie próbuje odzyskać wszelkie połączenia, dopóki nie zostanie osiągnięty limit czasu (wartość domyślna to 15 sekund).</span><span class="sxs-lookup"><span data-stu-id="b784b-150">The pooler then tries to reclaim any connections until the time-out is reached (the default is 15 seconds).</span></span> <span data-ttu-id="b784b-151">Jeśli pooler nie może spełnić żądania przed upłyniem czasu połączenia, wyjątek.</span><span class="sxs-lookup"><span data-stu-id="b784b-151">If the pooler cannot satisfy the request before the connection times out, an exception is thrown.</span></span>  
  
> [!CAUTION]
> <span data-ttu-id="b784b-152">Zdecydowanie zaleca się, aby zawsze zamknąć połączenie po zakończeniu korzystania z niego, tak aby połączenie zostanie zwrócone do puli.</span><span class="sxs-lookup"><span data-stu-id="b784b-152">We strongly recommend that you always close the connection when you are finished using it so that the connection will be returned to the pool.</span></span> <span data-ttu-id="b784b-153">Można to zrobić za `Close` pomocą `Dispose` lub `Connection` metody obiektu lub otwierając wszystkie `using` połączenia wewnątrz instrukcji w `Using` języku C#lub instrukcji w języku Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="b784b-153">You can do this using either the `Close` or `Dispose` methods of the `Connection` object, or by opening all connections inside a `using` statement in C#, or a `Using` statement in Visual Basic.</span></span> <span data-ttu-id="b784b-154">Połączenia, które nie są jawnie zamknięte, mogą nie zostać dodane lub zwrócone do puli.</span><span class="sxs-lookup"><span data-stu-id="b784b-154">Connections that are not explicitly closed might not be added or returned to the pool.</span></span> <span data-ttu-id="b784b-155">Aby uzyskać więcej informacji, zobacz [korzystanie z instrukcji](../../../csharp/language-reference/keywords/using-statement.md) lub [jak: Usuwanie zasobu systemowego](../../../visual-basic/programming-guide/language-features/control-flow/how-to-dispose-of-a-system-resource.md) dla języka Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="b784b-155">For more information, see [using Statement](../../../csharp/language-reference/keywords/using-statement.md) or [How to: Dispose of a System Resource](../../../visual-basic/programming-guide/language-features/control-flow/how-to-dispose-of-a-system-resource.md) for Visual Basic.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="b784b-156">Nie należy `Close` `Dispose` wywoływać `Connection`ani `DataReader`na , a , `Finalize` lub innego obiektu zarządzanego w metodzie klasy.</span><span class="sxs-lookup"><span data-stu-id="b784b-156">Do not call `Close` or `Dispose` on a `Connection`, a `DataReader`, or any other managed object in the `Finalize` method of your class.</span></span> <span data-ttu-id="b784b-157">W finalizatorze tylko zwolnić niezarządzanych zasobów, które posiada bezpośrednio klasy.</span><span class="sxs-lookup"><span data-stu-id="b784b-157">In a finalizer, only release unmanaged resources that your class owns directly.</span></span> <span data-ttu-id="b784b-158">Jeśli klasa nie jest właścicielem żadnych zasobów niezarządzanych, nie należy uwzględniać `Finalize` metody w definicji klasy.</span><span class="sxs-lookup"><span data-stu-id="b784b-158">If your class does not own any unmanaged resources, do not include a `Finalize` method in your class definition.</span></span> <span data-ttu-id="b784b-159">Aby uzyskać więcej informacji, zobacz [Wyrzucanie elementów bezużytecznych](../../../standard/garbage-collection/index.md).</span><span class="sxs-lookup"><span data-stu-id="b784b-159">For more information, see [Garbage Collection](../../../standard/garbage-collection/index.md).</span></span>  
  
<span data-ttu-id="b784b-160">Aby uzyskać więcej informacji na temat zdarzeń skojarzonych z otwieraniem i zamykaniem połączeń, zobacz [Inspekcja klasy zdarzeń logowania](/sql/relational-databases/event-classes/audit-login-event-class) i [klasa zdarzenia wylogowania inspekcji](/sql/relational-databases/event-classes/audit-logout-event-class) w dokumentacji programu SQL Server.</span><span class="sxs-lookup"><span data-stu-id="b784b-160">For more info about the events associated with opening and closing connections, see [Audit Login Event Class](/sql/relational-databases/event-classes/audit-login-event-class) and [Audit Logout Event Class](/sql/relational-databases/event-classes/audit-logout-event-class) in the SQL Server documentation.</span></span>  
  
## <a name="removing-connections"></a><span data-ttu-id="b784b-161">Usuwanie połączeń</span><span class="sxs-lookup"><span data-stu-id="b784b-161">Removing Connections</span></span>  
 <span data-ttu-id="b784b-162">Pooler połączenia usuwa połączenie z puli po jego bezczynny przez około 4-8 minut lub jeśli pooler wykryje, że połączenie z serwerem zostało zerwane.</span><span class="sxs-lookup"><span data-stu-id="b784b-162">The connection pooler removes a connection from the pool after it has been idle for approximately 4-8 minutes, or if the pooler detects that the connection with the server has been severed.</span></span> <span data-ttu-id="b784b-163">Należy zauważyć, że zerwane połączenie można wykryć tylko po próbie komunikowania się z serwerem.</span><span class="sxs-lookup"><span data-stu-id="b784b-163">Note that a severed connection can be detected only after attempting to communicate with the server.</span></span> <span data-ttu-id="b784b-164">Jeśli zostanie znalezione połączenie, które nie jest już połączone z serwerem, jest oznaczone jako nieprawidłowe.</span><span class="sxs-lookup"><span data-stu-id="b784b-164">If a connection is found that is no longer connected to the server, it is marked as invalid.</span></span> <span data-ttu-id="b784b-165">Nieprawidłowe połączenia są usuwane z puli połączeń tylko wtedy, gdy są zamknięte lub odzyskane.</span><span class="sxs-lookup"><span data-stu-id="b784b-165">Invalid connections are removed from the connection pool only when they are closed or reclaimed.</span></span>  
  
 <span data-ttu-id="b784b-166">Jeśli istnieje połączenie z serwerem, który zniknął, to połączenie można wyciągnąć z puli, nawet jeśli pooler połączenia nie wykrył zerwane połączenie i oznaczone jako nieprawidłowe.</span><span class="sxs-lookup"><span data-stu-id="b784b-166">If a connection exists to a server that has disappeared, this connection can be drawn from the pool even if the connection pooler has not detected the severed connection and marked it as invalid.</span></span> <span data-ttu-id="b784b-167">Dzieje się tak, ponieważ obciążenie związane z sprawdzaniem, czy połączenie jest nadal prawidłowe, wyeliminowałoby korzyści wynikające z posiadania poolera, powodując wystąpienie innej podróży w obie strony do serwera.</span><span class="sxs-lookup"><span data-stu-id="b784b-167">This is the case because the overhead of checking that the connection is still valid would eliminate the benefits of having a pooler by causing another round trip to the server to occur.</span></span> <span data-ttu-id="b784b-168">W takim przypadku pierwsza próba użycia połączenia wykryje, że połączenie zostało zerwane i zostanie zgłoszony wyjątek.</span><span class="sxs-lookup"><span data-stu-id="b784b-168">When this occurs, the first attempt to use the connection will detect that the connection has been severed, and an exception is thrown.</span></span>  
  
## <a name="clearing-the-pool"></a><span data-ttu-id="b784b-169">Czyszczenie basenu</span><span class="sxs-lookup"><span data-stu-id="b784b-169">Clearing the Pool</span></span>  
 <span data-ttu-id="b784b-170">ADO.NET 2.0 wprowadzono dwie nowe metody oczyszczania <xref:System.Data.SqlClient.SqlConnection.ClearAllPools%2A> <xref:System.Data.SqlClient.SqlConnection.ClearPool%2A>puli: i .</span><span class="sxs-lookup"><span data-stu-id="b784b-170">ADO.NET 2.0 introduced two new methods to clear the pool: <xref:System.Data.SqlClient.SqlConnection.ClearAllPools%2A> and <xref:System.Data.SqlClient.SqlConnection.ClearPool%2A>.</span></span> <span data-ttu-id="b784b-171">`ClearAllPools`czyści pule połączeń dla danego `ClearPool` dostawcy i czyści pulę połączeń skojarzoną z określonym połączeniem.</span><span class="sxs-lookup"><span data-stu-id="b784b-171">`ClearAllPools` clears the connection pools for a given provider, and `ClearPool` clears the connection pool that is associated with a specific connection.</span></span> <span data-ttu-id="b784b-172">Jeśli istnieją połączenia używane w czasie połączenia, są one odpowiednio oznaczone.</span><span class="sxs-lookup"><span data-stu-id="b784b-172">If there are connections being used at the time of the call, they are marked appropriately.</span></span> <span data-ttu-id="b784b-173">Gdy są zamknięte, są odrzucane zamiast zwracać do puli.</span><span class="sxs-lookup"><span data-stu-id="b784b-173">When they are closed, they are discarded instead of being returned to the pool.</span></span>  
  
## <a name="transaction-support"></a><span data-ttu-id="b784b-174">Obsługa transakcji</span><span class="sxs-lookup"><span data-stu-id="b784b-174">Transaction Support</span></span>  
 <span data-ttu-id="b784b-175">Połączenia są pobierane z puli i przypisywane na podstawie kontekstu transakcji.</span><span class="sxs-lookup"><span data-stu-id="b784b-175">Connections are drawn from the pool and assigned based on transaction context.</span></span> <span data-ttu-id="b784b-176">O `Enlist=false` ile nie określono w ciągu połączenia, pula połączeń zapewnia, że połączenie jest zarejestrowane w <xref:System.Transactions.Transaction.Current%2A> kontekście.</span><span class="sxs-lookup"><span data-stu-id="b784b-176">Unless `Enlist=false` is specified in the connection string, the connection pool makes sure that the connection is enlisted in the <xref:System.Transactions.Transaction.Current%2A> context.</span></span> <span data-ttu-id="b784b-177">Gdy połączenie jest zamknięte i zwrócone do `System.Transactions` puli z zarejestrowaną transakcją, jest odłożone w `System.Transactions` taki sposób, że następne żądanie dla tej puli połączeń z tą samą transakcją zwróci to samo połączenie, jeśli jest dostępne.</span><span class="sxs-lookup"><span data-stu-id="b784b-177">When a connection is closed and returned to the pool with an enlisted `System.Transactions` transaction, it is set aside in such a way that the next request for that connection pool with the same `System.Transactions` transaction will return the same connection if it is available.</span></span> <span data-ttu-id="b784b-178">Jeśli takie żądanie zostanie wystawione i nie ma dostępnych połączeń w puli, połączenie jest pobierane z nieprzekonanej części puli i zarejestrowane.</span><span class="sxs-lookup"><span data-stu-id="b784b-178">If such a request is issued, and there are no pooled connections available, a connection is drawn from the non-transacted part of the pool and enlisted.</span></span> <span data-ttu-id="b784b-179">Jeśli w żadnym z obszarów puli nie są dostępne żadne połączenia, zostanie utworzone i zarejestrowane nowe połączenie.</span><span class="sxs-lookup"><span data-stu-id="b784b-179">If no connections are available in either area of the pool, a new connection is created and enlisted.</span></span>  
  
 <span data-ttu-id="b784b-180">Po zamknięciu połączenia jest zwalniany z powrotem do puli i do odpowiedniego podpodziału na podstawie kontekstu transakcji.</span><span class="sxs-lookup"><span data-stu-id="b784b-180">When a connection is closed, it is released back into the pool and into the appropriate subdivision based on its transaction context.</span></span> <span data-ttu-id="b784b-181">W związku z tym można zamknąć połączenie bez generowania błędu, nawet jeśli transakcja rozproszona jest nadal oczekująca.</span><span class="sxs-lookup"><span data-stu-id="b784b-181">Therefore, you can close the connection without generating an error, even though a distributed transaction is still pending.</span></span> <span data-ttu-id="b784b-182">Dzięki temu można zatwierdzić lub przerwać transakcję rozproszoną później.</span><span class="sxs-lookup"><span data-stu-id="b784b-182">This allows you to commit or abort the distributed transaction later.</span></span>  
  
## <a name="controlling-connection-pooling-with-connection-string-keywords"></a><span data-ttu-id="b784b-183">Kontrolowanie buforowania połączeń za pomocą słów kluczowych ciągu połączenia</span><span class="sxs-lookup"><span data-stu-id="b784b-183">Controlling Connection Pooling with Connection String Keywords</span></span>  
 <span data-ttu-id="b784b-184">Właściwość `ConnectionString` <xref:System.Data.SqlClient.SqlConnection> obiektu obsługuje pary klucza/wartości ciągu połączenia, które mogą służyć do dostosowywania zachowania logiki buforowania połączeń.</span><span class="sxs-lookup"><span data-stu-id="b784b-184">The `ConnectionString` property of the <xref:System.Data.SqlClient.SqlConnection> object supports connection string key/value pairs that can be used to adjust the behavior of the connection pooling logic.</span></span> <span data-ttu-id="b784b-185">Aby uzyskać więcej informacji, zobacz <xref:System.Data.SqlClient.SqlConnection.ConnectionString%2A>.</span><span class="sxs-lookup"><span data-stu-id="b784b-185">For more information, see <xref:System.Data.SqlClient.SqlConnection.ConnectionString%2A>.</span></span>  
  
## <a name="pool-fragmentation"></a><span data-ttu-id="b784b-186">Fragmentacja puli</span><span class="sxs-lookup"><span data-stu-id="b784b-186">Pool Fragmentation</span></span>  
 <span data-ttu-id="b784b-187">Fragmentacja puli jest częstym problemem w wielu aplikacjach sieci Web, gdzie aplikacja może utworzyć dużą liczbę pul, które nie są zwalniane, dopóki proces nie zostanie zakończy.</span><span class="sxs-lookup"><span data-stu-id="b784b-187">Pool fragmentation is a common problem in many Web applications where the application can create a large number of pools that are not freed until the process exits.</span></span> <span data-ttu-id="b784b-188">Pozostawia to dużą liczbę połączeń otwartych i zużywa pamięć, co powoduje niską wydajność.</span><span class="sxs-lookup"><span data-stu-id="b784b-188">This leaves a large number of connections open and consuming memory, which results in poor performance.</span></span>  
  
### <a name="pool-fragmentation-due-to-integrated-security"></a><span data-ttu-id="b784b-189">Fragmentacja puli ze względu na zintegrowane zabezpieczenia</span><span class="sxs-lookup"><span data-stu-id="b784b-189">Pool Fragmentation Due to Integrated Security</span></span>  
 <span data-ttu-id="b784b-190">Połączenia są łączone zgodnie z ciągiem połączenia oraz tożsamością użytkownika.</span><span class="sxs-lookup"><span data-stu-id="b784b-190">Connections are pooled according to the connection string plus the user identity.</span></span> <span data-ttu-id="b784b-191">W związku z tym jeśli używasz uwierzytelniania podstawowego lub uwierzytelniania systemu Windows w witrynie sieci Web i zintegrowanego logowania zabezpieczeń, otrzymasz jedną pulę na użytkownika.</span><span class="sxs-lookup"><span data-stu-id="b784b-191">Therefore, if you use Basic authentication or Windows Authentication on the Web site and an integrated security login, you get one pool per user.</span></span> <span data-ttu-id="b784b-192">Mimo że zwiększa to wydajność kolejnych żądań bazy danych dla pojedynczego użytkownika, ten użytkownik nie może korzystać z połączeń nawiązynych przez innych użytkowników.</span><span class="sxs-lookup"><span data-stu-id="b784b-192">Although this improves the performance of subsequent database requests for a single user, that user cannot take advantage of connections made by other users.</span></span> <span data-ttu-id="b784b-193">Powoduje to również co najmniej jedno połączenie na użytkownika z serwerem bazy danych.</span><span class="sxs-lookup"><span data-stu-id="b784b-193">It also results in at least one connection per user to the database server.</span></span> <span data-ttu-id="b784b-194">Jest to efekt uboczny określonej architektury aplikacji sieci Web, którą deweloperzy muszą ważyć z wymaganiami dotyczącymi zabezpieczeń i inspekcji.</span><span class="sxs-lookup"><span data-stu-id="b784b-194">This is a side effect of a particular Web application architecture that developers must weigh against security and auditing requirements.</span></span>  
  
### <a name="pool-fragmentation-due-to-many-databases"></a><span data-ttu-id="b784b-195">Fragmentacja puli z powodu wielu baz danych</span><span class="sxs-lookup"><span data-stu-id="b784b-195">Pool Fragmentation Due to Many Databases</span></span>  
 <span data-ttu-id="b784b-196">Wielu dostawców usług internetowych hostuje kilka witryn sieci Web na jednym serwerze.</span><span class="sxs-lookup"><span data-stu-id="b784b-196">Many Internet service providers host several Web sites on a single server.</span></span> <span data-ttu-id="b784b-197">Mogą użyć jednej bazy danych, aby potwierdzić identyfikator logowania do uwierzytelniania formularzy, a następnie otworzyć połączenie z określoną bazą danych dla tego użytkownika lub grupy użytkowników.</span><span class="sxs-lookup"><span data-stu-id="b784b-197">They may use a single database to confirm a Forms authentication login and then open a connection to a specific database for that user or group of users.</span></span> <span data-ttu-id="b784b-198">Połączenie z bazą danych uwierzytelniania jest łączone i używane przez wszystkich.</span><span class="sxs-lookup"><span data-stu-id="b784b-198">The connection to the authentication database is pooled and used by everyone.</span></span> <span data-ttu-id="b784b-199">Istnieje jednak oddzielna pula połączeń z każdą bazą danych, które zwiększają liczbę połączeń z serwerem.</span><span class="sxs-lookup"><span data-stu-id="b784b-199">However, there is a separate pool of connections to each database, which increase the number of connections to the server.</span></span>  
  
 <span data-ttu-id="b784b-200">Jest to również efekt uboczny projektu aplikacji.</span><span class="sxs-lookup"><span data-stu-id="b784b-200">This is also a side-effect of the application design.</span></span> <span data-ttu-id="b784b-201">Istnieje stosunkowo prosty sposób, aby uniknąć tego efektu ubocznego bez uszczerbku dla zabezpieczeń podczas łączenia się z programem SQL Server.</span><span class="sxs-lookup"><span data-stu-id="b784b-201">There is a relatively simple way to avoid this side effect without compromising security when you connect to SQL Server.</span></span> <span data-ttu-id="b784b-202">Zamiast łączyć się z oddzielną bazą danych dla każdego użytkownika lub grupy, połącz się z tą samą bazą danych na serwerze, a następnie wykonaj instrukcję Transact-SQL USE, aby zmienić żądaną bazę danych.</span><span class="sxs-lookup"><span data-stu-id="b784b-202">Instead of connecting to a separate database for each user or group, connect to the same database on the server and then execute the Transact-SQL USE statement to change to the desired database.</span></span> <span data-ttu-id="b784b-203">Poniższy fragment kodu demonstruje tworzenie `master` początkowego połączenia z bazą danych, `databaseName` a następnie przełączanie do żądanej bazy danych określonej w zmiennej ciągu.</span><span class="sxs-lookup"><span data-stu-id="b784b-203">The following code fragment demonstrates creating an initial connection to the `master` database and then switching to the desired database specified in the `databaseName` string variable.</span></span>  
  
```vb  
' Assumes that command is a valid SqlCommand object and that  
' connectionString connects to master.  
    command.Text = "USE DatabaseName"  
Using connection As New SqlConnection(connectionString)  
    connection.Open()  
    command.ExecuteNonQuery()  
End Using  
```  
  
```csharp  
// Assumes that command is a SqlCommand object and that  
// connectionString connects to master.  
command.Text = "USE DatabaseName";  
using (SqlConnection connection = new SqlConnection(  
  connectionString))  
  {  
    connection.Open();  
    command.ExecuteNonQuery();  
  }  
```  
  
## <a name="application-roles-and-connection-pooling"></a><span data-ttu-id="b784b-204">Role aplikacji i buforowanie połączeń</span><span class="sxs-lookup"><span data-stu-id="b784b-204">Application Roles and Connection Pooling</span></span>  
 <span data-ttu-id="b784b-205">Po aktywowaniu roli aplikacji programu SQL `sp_setapprole` Server przez wywołanie procedury składowanej systemu nie można zresetować kontekstu zabezpieczeń tego połączenia.</span><span class="sxs-lookup"><span data-stu-id="b784b-205">After a SQL Server application role has been activated by calling the `sp_setapprole` system stored procedure, the security context of that connection cannot be reset.</span></span> <span data-ttu-id="b784b-206">Jednak jeśli buforowanie jest włączone, połączenie jest zwracane do puli, a błąd występuje, gdy połączenie w puli jest ponownie.</span><span class="sxs-lookup"><span data-stu-id="b784b-206">However, if pooling is enabled, the connection is returned to the pool, and an error occurs when the pooled connection is reused.</span></span> <span data-ttu-id="b784b-207">Aby uzyskać więcej informacji, zobacz artykuł z bazy wiedzy Knowledge Base, "[Błędy roli aplikacji SQL z buforowania zasobów OLE DB](https://support.microsoft.com/default.aspx?scid=KB;EN-US;Q229564)."</span><span class="sxs-lookup"><span data-stu-id="b784b-207">For more information, see the Knowledge Base article, "[SQL application role errors with OLE DB resource pooling](https://support.microsoft.com/default.aspx?scid=KB;EN-US;Q229564)."</span></span>  
  
### <a name="application-role-alternatives"></a><span data-ttu-id="b784b-208">Alternatywy roli aplikacji</span><span class="sxs-lookup"><span data-stu-id="b784b-208">Application Role Alternatives</span></span>  
 <span data-ttu-id="b784b-209">Zaleca się korzystanie z mechanizmów zabezpieczeń, których można użyć zamiast ról aplikacji.</span><span class="sxs-lookup"><span data-stu-id="b784b-209">We recommend that you take advantage of security mechanisms that you can use instead of application roles.</span></span> <span data-ttu-id="b784b-210">Aby uzyskać więcej informacji, zobacz [Tworzenie ról aplikacji w programie SQL Server](./sql/creating-application-roles-in-sql-server.md).</span><span class="sxs-lookup"><span data-stu-id="b784b-210">For more information, see [Creating Application Roles in SQL Server](./sql/creating-application-roles-in-sql-server.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="b784b-211">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="b784b-211">See also</span></span>

- [<span data-ttu-id="b784b-212">Buforowanie połączeń</span><span class="sxs-lookup"><span data-stu-id="b784b-212">Connection Pooling</span></span>](connection-pooling.md)
- [<span data-ttu-id="b784b-213">SQL Server i ADO.NET</span><span class="sxs-lookup"><span data-stu-id="b784b-213">SQL Server and ADO.NET</span></span>](./sql/index.md)
- [<span data-ttu-id="b784b-214">Liczniki wydajności</span><span class="sxs-lookup"><span data-stu-id="b784b-214">Performance Counters</span></span>](performance-counters.md)
- [<span data-ttu-id="b784b-215">Omówienie ADO.NET</span><span class="sxs-lookup"><span data-stu-id="b784b-215">ADO.NET Overview</span></span>](ado-net-overview.md)
