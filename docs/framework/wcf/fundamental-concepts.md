---
title: Podstawowe pojęcia programu Windows Communication Foundation
description: Zapoznaj się z podstawowymi informacjami na temat architektury Windows Communication Foundation (WCF) z tym ogólnym objaśnieniem.
ms.date: 03/30/2017
helpviewer_keywords:
- WCF [WCF], concepts
- concepts [WCF]
- fundamentals [WCF]
- Windows Communication Foundation [WCF], concepts
ms.assetid: 3e7e0afd-7913-499d-bafb-eac7caacbc7a
ms.openlocfilehash: 93e75942487a1a81a8b0e8ecd8d9d666610152dc
ms.sourcegitcommit: 358a28048f36a8dca39a9fe6e6ac1f1913acadd5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 06/23/2020
ms.locfileid: "85244678"
---
# <a name="fundamental-windows-communication-foundation-concepts"></a><span data-ttu-id="c28f3-103">Podstawowe pojęcia programu Windows Communication Foundation</span><span class="sxs-lookup"><span data-stu-id="c28f3-103">Fundamental Windows Communication Foundation Concepts</span></span>

<span data-ttu-id="c28f3-104">Ten dokument zawiera ogólny widok architektury Windows Communication Foundation (WCF).</span><span class="sxs-lookup"><span data-stu-id="c28f3-104">This document provides a high-level view of the Windows Communication Foundation (WCF) architecture.</span></span> <span data-ttu-id="c28f3-105">Ma ona na celu wyjaśnienie najważniejszych pojęć i ich dopasowania.</span><span class="sxs-lookup"><span data-stu-id="c28f3-105">It is intended to explain key concepts and how they fit together.</span></span> <span data-ttu-id="c28f3-106">Aby zapoznać się z samouczkiem dotyczącym tworzenia najprostszej wersji usługi i klienta WCF, zobacz [samouczek wprowadzenie](getting-started-tutorial.md).</span><span class="sxs-lookup"><span data-stu-id="c28f3-106">For a tutorial on creating the simplest version of a WCF service and client, see [Getting Started Tutorial](getting-started-tutorial.md).</span></span> <span data-ttu-id="c28f3-107">Aby poznać programowanie WCF, zobacz [podstawowe programowanie WCF](basic-wcf-programming.md).</span><span class="sxs-lookup"><span data-stu-id="c28f3-107">To learn WCF programming, see [Basic WCF Programming](basic-wcf-programming.md).</span></span>

## <a name="wcf-fundamentals"></a><span data-ttu-id="c28f3-108">Podstawy WCF</span><span class="sxs-lookup"><span data-stu-id="c28f3-108">WCF Fundamentals</span></span>

<span data-ttu-id="c28f3-109">WCF to środowisko uruchomieniowe i zestaw interfejsów API służących do tworzenia systemów wysyłających komunikaty między usługami i klientami.</span><span class="sxs-lookup"><span data-stu-id="c28f3-109">WCF is a runtime and a set of APIs for creating systems that send messages between services and clients.</span></span> <span data-ttu-id="c28f3-110">Ta sama infrastruktura i interfejsy API są używane do tworzenia aplikacji, które komunikują się z innymi aplikacjami w tym samym systemie komputerowym lub w systemie, który znajduje się w innej firmie i jest dostępny za pośrednictwem Internetu.</span><span class="sxs-lookup"><span data-stu-id="c28f3-110">The same infrastructure and APIs are used to create applications that communicate with other applications on the same computer system or on a system that resides in another company and is accessed over the Internet.</span></span>

### <a name="messaging-and-endpoints"></a><span data-ttu-id="c28f3-111">Wiadomości i punkty końcowe</span><span class="sxs-lookup"><span data-stu-id="c28f3-111">Messaging and Endpoints</span></span>

<span data-ttu-id="c28f3-112">Program WCF jest oparty na koncepcji komunikacji opartej na komunikatach, a wszystkie elementy, które mogą być modelowane jako wiadomości (na przykład żądanie HTTP lub komunikat usługi kolejkowania komunikatów (znany również jako MSMQ), mogą być reprezentowane w jednolity sposób w modelu programowania.</span><span class="sxs-lookup"><span data-stu-id="c28f3-112">WCF is based on the notion of message-based communication, and anything that can be modeled as a message (for example, an HTTP request or a Message Queuing (also known as MSMQ) message) can be represented in a uniform way in the programming model.</span></span> <span data-ttu-id="c28f3-113">Umożliwia to ujednolicony interfejs API w różnych mechanizmach transportu.</span><span class="sxs-lookup"><span data-stu-id="c28f3-113">This enables a unified API across different transport mechanisms.</span></span>

<span data-ttu-id="c28f3-114">Model odróżnia _klientów_, czyli aplikacje inicjujące komunikację i _usługi_, które są aplikacjami, które oczekują na komunikowanie się z nimi i odpowiadają na tę komunikację.</span><span class="sxs-lookup"><span data-stu-id="c28f3-114">The model distinguishes between _clients_, which are applications that initiate communication, and _services_, which are applications that wait for clients to communicate with them and respond to that communication.</span></span> <span data-ttu-id="c28f3-115">Pojedyncza aplikacja może działać zarówno jako klient, jak i usługa.</span><span class="sxs-lookup"><span data-stu-id="c28f3-115">A single application can act as both a client and a service.</span></span> <span data-ttu-id="c28f3-116">Aby zapoznać się z przykładami, zobacz [usługi dupleks](./feature-details/duplex-services.md) i [sieci peer-to-peer](./feature-details/peer-to-peer-networking.md).</span><span class="sxs-lookup"><span data-stu-id="c28f3-116">For examples, see [Duplex Services](./feature-details/duplex-services.md) and [Peer-to-Peer Networking](./feature-details/peer-to-peer-networking.md).</span></span>

<span data-ttu-id="c28f3-117">Komunikaty są wysyłane między punktami końcowymi.</span><span class="sxs-lookup"><span data-stu-id="c28f3-117">Messages are sent between endpoints.</span></span> <span data-ttu-id="c28f3-118">_Punkty końcowe_ to miejsca, w których komunikaty są wysyłane lub odbierane (lub oba), a także definiują wszystkie informacje wymagane do wymiany komunikatów.</span><span class="sxs-lookup"><span data-stu-id="c28f3-118">_Endpoints_ are places where messages are sent or received (or both), and they define all the information required for the message exchange.</span></span> <span data-ttu-id="c28f3-119">Usługa udostępnia jeden lub więcej punktów końcowych aplikacji (a także zero lub więcej punktów końcowych infrastruktury), a klient generuje punkt końcowy, który jest zgodny z jednym z punktów końcowych usługi.</span><span class="sxs-lookup"><span data-stu-id="c28f3-119">A service exposes one or more application endpoints (as well as zero or more infrastructure endpoints), and the client generates an endpoint that is compatible with one of the service's endpoints.</span></span>

<span data-ttu-id="c28f3-120">_Punkt końcowy_ opisuje w standardowym sposobie, w jaki wiadomości powinny być wysyłane, jak powinny być wysyłane i jak powinny wyglądać komunikaty.</span><span class="sxs-lookup"><span data-stu-id="c28f3-120">An _endpoint_ describes in a standard-based way where messages should be sent, how they should be sent, and what the messages should look like.</span></span> <span data-ttu-id="c28f3-121">Usługa może uwidaczniać te informacje jako metadane, które klienci mogą przetwarzać w celu generowania odpowiednich _stosów_komunikacji i klientów programu WCF.</span><span class="sxs-lookup"><span data-stu-id="c28f3-121">A service can expose this information as metadata that clients can process to generate appropriate WCF clients and communication _stacks_.</span></span>

### <a name="communication-protocols"></a><span data-ttu-id="c28f3-122">Protokoły komunikacyjne</span><span class="sxs-lookup"><span data-stu-id="c28f3-122">Communication Protocols</span></span>

<span data-ttu-id="c28f3-123">Jednym z wymaganych elementów stosu komunikacji jest _Protokół transportowy_.</span><span class="sxs-lookup"><span data-stu-id="c28f3-123">One required element of the communication stack is the _transport protocol_.</span></span> <span data-ttu-id="c28f3-124">Komunikaty mogą być wysyłane za pośrednictwem sieci intranet i Internetu przy użyciu wspólnych transportów, takich jak HTTP i TCP.</span><span class="sxs-lookup"><span data-stu-id="c28f3-124">Messages can be sent over intranets and the Internet using common transports, such as HTTP and TCP.</span></span> <span data-ttu-id="c28f3-125">Dołączono inne transporty obsługujące komunikację z aplikacjami i węzłami usługi kolejkowania komunikatów w sieci równorzędnej.</span><span class="sxs-lookup"><span data-stu-id="c28f3-125">Other transports are included that support communication with Message Queuing applications and nodes on a Peer Networking mesh.</span></span> <span data-ttu-id="c28f3-126">Więcej mechanizmów transportu można dodać przy użyciu wbudowanych punktów rozszerzenia programu WCF.</span><span class="sxs-lookup"><span data-stu-id="c28f3-126">More transport mechanisms can be added using the built-in extension points of WCF.</span></span>

<span data-ttu-id="c28f3-127">Innym wymaganym elementem w stosie komunikacyjnym jest kodowanie, które określa sposób formatowania danego komunikatu.</span><span class="sxs-lookup"><span data-stu-id="c28f3-127">Another required element in the communication stack is the encoding that specifies how any given message is formatted.</span></span> <span data-ttu-id="c28f3-128">Funkcja WCF udostępnia następujące kodowania:</span><span class="sxs-lookup"><span data-stu-id="c28f3-128">WCF provides the following encodings:</span></span>

- <span data-ttu-id="c28f3-129">Kodowanie tekstu, kodowanie międzyoperacyjnego.</span><span class="sxs-lookup"><span data-stu-id="c28f3-129">Text encoding, an interoperable encoding.</span></span>

- <span data-ttu-id="c28f3-130">Kodowanie mechanizmu optymalizacji transmisji wiadomości (MTOM), który jest międzyoperacyjnym sposobem wydajnego wysyłania danych binarnych bez struktury do i z usługi.</span><span class="sxs-lookup"><span data-stu-id="c28f3-130">Message Transmission Optimization Mechanism (MTOM) encoding, which is an interoperable way for efficiently sending unstructured binary data to and from a service.</span></span>

- <span data-ttu-id="c28f3-131">Kodowanie binarne w celu wydajnego transferu.</span><span class="sxs-lookup"><span data-stu-id="c28f3-131">Binary encoding for efficient transfer.</span></span>

<span data-ttu-id="c28f3-132">Więcej mechanizmów kodowania (na przykład kodowania kompresji) można dodać przy użyciu wbudowanych punktów rozszerzenia programu WCF.</span><span class="sxs-lookup"><span data-stu-id="c28f3-132">More encoding mechanisms (for example, a compression encoding) can be added using the built-in extension points of WCF.</span></span>

### <a name="message-patterns"></a><span data-ttu-id="c28f3-133">Wzorce komunikatów</span><span class="sxs-lookup"><span data-stu-id="c28f3-133">Message Patterns</span></span>

<span data-ttu-id="c28f3-134">Usługa WCF obsługuje kilka wzorców obsługi komunikatów, takich jak żądanie-odpowiedź, jednokierunkowe i dupleksowe.</span><span class="sxs-lookup"><span data-stu-id="c28f3-134">WCF supports several messaging patterns, including request-reply, one-way, and duplex communication.</span></span> <span data-ttu-id="c28f3-135">Różne transporty obsługują różne wzorce obsługi komunikatów i w ten sposób wpływają na typy interakcji, które są przez nie obsługiwane.</span><span class="sxs-lookup"><span data-stu-id="c28f3-135">Different transports support different messaging patterns, and thus affect the types of interactions that they support.</span></span> <span data-ttu-id="c28f3-136">Interfejsy API i środowisko uruchomieniowe WCF ułatwiają również bezpieczne i niezawodne wysyłanie komunikatów.</span><span class="sxs-lookup"><span data-stu-id="c28f3-136">The WCF APIs and runtime also help you to send messages securely and reliably.</span></span>

## <a name="wcf-terms"></a><span data-ttu-id="c28f3-137">Warunki WCF</span><span class="sxs-lookup"><span data-stu-id="c28f3-137">WCF Terms</span></span>

<span data-ttu-id="c28f3-138">Inne pojęcia i terminy używane w dokumentacji programu WCF obejmują następujące zagadnienia:</span><span class="sxs-lookup"><span data-stu-id="c28f3-138">Other concepts and terms used in the WCF documentation include the following:</span></span>

<span data-ttu-id="c28f3-139">**Wiadomość**</span><span class="sxs-lookup"><span data-stu-id="c28f3-139">**Message**</span></span>  
 <span data-ttu-id="c28f3-140">Samodzielna jednostka danych, która może składać się z kilku części, w tym treści i nagłówków.</span><span class="sxs-lookup"><span data-stu-id="c28f3-140">A self-contained unit of data that can consist of several parts, including a body and headers.</span></span>

<span data-ttu-id="c28f3-141">**Usługa**</span><span class="sxs-lookup"><span data-stu-id="c28f3-141">**Service**</span></span>  
 <span data-ttu-id="c28f3-142">Konstrukcja, która uwidacznia jeden lub więcej punktów końcowych, przy czym każdy punkt końcowy uwidacznia jedną lub więcej operacji usługi.</span><span class="sxs-lookup"><span data-stu-id="c28f3-142">A construct that exposes one or more endpoints, with each endpoint exposing one or more service operations.</span></span>

<span data-ttu-id="c28f3-143">**Punkt końcowy**</span><span class="sxs-lookup"><span data-stu-id="c28f3-143">**Endpoint**</span></span>  
 <span data-ttu-id="c28f3-144">Konstrukcja, w której wysyłane lub odbierane są komunikaty (lub oba te elementy).</span><span class="sxs-lookup"><span data-stu-id="c28f3-144">A construct at which messages are sent or received (or both).</span></span> <span data-ttu-id="c28f3-145">Obejmuje ona lokalizację (adres), która określa, gdzie mogą być wysyłane komunikaty, specyfikację mechanizmu komunikacji (powiązanie) opisującą sposób wysyłania komunikatów oraz definicję zestawu komunikatów, które mogą być wysyłane lub odbierane (lub oba) w tej lokalizacji (kontraktu usługi) opisującej, jaki komunikat można wysłać.</span><span class="sxs-lookup"><span data-stu-id="c28f3-145">It comprises a location (an address) that defines where messages can be sent, a specification of the communication mechanism (a binding) that describes how messages should be sent, and a definition for a set of messages that can be sent or received (or both) at that location (a service contract) that describes what message can be sent.</span></span>

<span data-ttu-id="c28f3-146">Usługa WCF jest narażona na świecie jako zbiór punktów końcowych.</span><span class="sxs-lookup"><span data-stu-id="c28f3-146">A WCF service is exposed to the world as a collection of endpoints.</span></span>

<span data-ttu-id="c28f3-147">**Punkt końcowy aplikacji**</span><span class="sxs-lookup"><span data-stu-id="c28f3-147">**Application endpoint**</span></span>  
 <span data-ttu-id="c28f3-148">Punkt końcowy uwidoczniony przez aplikację i odpowiadający kontraktowi usługi zaimplementowanemu przez aplikację.</span><span class="sxs-lookup"><span data-stu-id="c28f3-148">An endpoint exposed by the application and that corresponds to a service contract implemented by the application.</span></span>

<span data-ttu-id="c28f3-149">**Punkt końcowy infrastruktury**</span><span class="sxs-lookup"><span data-stu-id="c28f3-149">**Infrastructure endpoint**</span></span>  
 <span data-ttu-id="c28f3-150">Punkt końcowy, który jest udostępniany przez infrastrukturę w celu ułatwienia funkcjonalności, która jest wymagana lub oferowana przez usługę, która nie jest powiązana z umową usługi.</span><span class="sxs-lookup"><span data-stu-id="c28f3-150">An endpoint that is exposed by the infrastructure to facilitate functionality that is needed or provided by the service that does not relate to a service contract.</span></span> <span data-ttu-id="c28f3-151">Na przykład usługa może mieć punkt końcowy infrastruktury, który zawiera informacje o metadanych.</span><span class="sxs-lookup"><span data-stu-id="c28f3-151">For example, a service might have an infrastructure endpoint that provides metadata information.</span></span>

<span data-ttu-id="c28f3-152">**Adres**</span><span class="sxs-lookup"><span data-stu-id="c28f3-152">**Address**</span></span>  
 <span data-ttu-id="c28f3-153">Określa lokalizację, w której odbierane są komunikaty.</span><span class="sxs-lookup"><span data-stu-id="c28f3-153">Specifies the location where messages are received.</span></span> <span data-ttu-id="c28f3-154">Jest on określony jako Uniform Resource Identifier (URI).</span><span class="sxs-lookup"><span data-stu-id="c28f3-154">It is specified as a Uniform Resource Identifier (URI).</span></span> <span data-ttu-id="c28f3-155">Część schematu identyfikatora URI nazywa mechanizm transportu, który ma być używany do uzyskiwania dostępu do adresu, takiego jak HTTP i TCP.</span><span class="sxs-lookup"><span data-stu-id="c28f3-155">The URI schema part names the transport mechanism to use to reach the address, such as HTTP and TCP.</span></span> <span data-ttu-id="c28f3-156">Hierarchiczna część identyfikatora URI zawiera unikatową lokalizację, której format zależy od mechanizmu transportowego.</span><span class="sxs-lookup"><span data-stu-id="c28f3-156">The hierarchical part of the URI contains a unique location whose format is dependent on the transport mechanism.</span></span>

<span data-ttu-id="c28f3-157">Adres punktu końcowego umożliwia tworzenie unikatowych adresów punktów końcowych dla każdego punktu końcowego w usłudze lub w określonych warunkach, aby udostępnić adres między punktami końcowymi.</span><span class="sxs-lookup"><span data-stu-id="c28f3-157">The endpoint address enables you to create unique endpoint addresses for each endpoint in a service or, under certain conditions, to share an address across endpoints.</span></span> <span data-ttu-id="c28f3-158">Poniższy przykład przedstawia adres przy użyciu protokołu HTTPS z portem innym niż domyślny:</span><span class="sxs-lookup"><span data-stu-id="c28f3-158">The following example shows an address using the HTTPS protocol with a non-default port:</span></span>

```http
HTTPS://cohowinery:8005/ServiceModelSamples/CalculatorService
```

<span data-ttu-id="c28f3-159">**Wiązanie**</span><span class="sxs-lookup"><span data-stu-id="c28f3-159">**Binding**</span></span>  
 <span data-ttu-id="c28f3-160">Definiuje, w jaki sposób punkt końcowy komunikuje się ze światem.</span><span class="sxs-lookup"><span data-stu-id="c28f3-160">Defines how an endpoint communicates to the world.</span></span> <span data-ttu-id="c28f3-161">Jest zbudowana z zestawu składników nazywanych elementami powiązania, które "stosują" jeden na drugim, aby utworzyć infrastrukturę komunikacji.</span><span class="sxs-lookup"><span data-stu-id="c28f3-161">It is constructed of a set of components called binding elements that "stack" one on top of the other to create the communication infrastructure.</span></span> <span data-ttu-id="c28f3-162">Co najmniej, powiązanie definiuje transport (na przykład HTTP lub TCP) i używane kodowanie (na przykład tekst lub binarny).</span><span class="sxs-lookup"><span data-stu-id="c28f3-162">At the very least, a binding defines the transport (such as HTTP or TCP) and the encoding being used (such as text or binary).</span></span> <span data-ttu-id="c28f3-163">Powiązanie może zawierać elementy wiążące, które określają szczegóły, takie jak mechanizmy zabezpieczeń używane do zabezpieczania komunikatów lub wzorzec wiadomości używany przez punkt końcowy.</span><span class="sxs-lookup"><span data-stu-id="c28f3-163">A binding can contain binding elements that specify details like the security mechanisms used to secure messages, or the message pattern used by an endpoint.</span></span> <span data-ttu-id="c28f3-164">Aby uzyskać więcej informacji, zobacz [Konfigurowanie usług](configuring-services.md).</span><span class="sxs-lookup"><span data-stu-id="c28f3-164">For more information, see [Configuring Services](configuring-services.md).</span></span>

<span data-ttu-id="c28f3-165">**Element powiązania**</span><span class="sxs-lookup"><span data-stu-id="c28f3-165">**Binding element**</span></span>  
 <span data-ttu-id="c28f3-166">Reprezentuje szczególny element powiązania, na przykład transport, kodowanie, implementację protokołu poziomu infrastruktury (na przykład WS-ReliableMessaging) lub dowolny inny składnik stosu komunikacji.</span><span class="sxs-lookup"><span data-stu-id="c28f3-166">Represents a particular piece of the binding, such as a transport, an encoding, an implementation of an infrastructure-level protocol (such as WS-ReliableMessaging), or any other component of the communication stack.</span></span>

<span data-ttu-id="c28f3-167">**Zachowania**</span><span class="sxs-lookup"><span data-stu-id="c28f3-167">**Behaviors**</span></span>  
 <span data-ttu-id="c28f3-168">Składnik kontrolujący różne aspekty czasu wykonywania usługi, punkt końcowy, konkretną operację lub klienta.</span><span class="sxs-lookup"><span data-stu-id="c28f3-168">A component that controls various run-time aspects of a service, an endpoint, a particular operation, or a client.</span></span> <span data-ttu-id="c28f3-169">Zachowania są pogrupowane według zakresu: typowe zachowania mają na celu globalne wpływ na wszystkie punkty końcowe, zachowania usługi dotyczą tylko aspektów związanych z usługą, zachowania punktów końcowych wpływają wyłącznie na właściwości powiązane z punktem końcowym, a zachowania poziomu operacji wpływają na określone operacje.</span><span class="sxs-lookup"><span data-stu-id="c28f3-169">Behaviors are grouped according to scope: common behaviors affect all endpoints globally, service behaviors affect only service-related aspects, endpoint behaviors affect only endpoint-related properties, and operation-level behaviors affect particular operations.</span></span> <span data-ttu-id="c28f3-170">Na przykład, jedno zachowanie usługi ma ograniczenia, które określa, jak działa usługa, gdy nadmiarowe komunikaty grożą zaprzeciążeniem możliwości obsługi.</span><span class="sxs-lookup"><span data-stu-id="c28f3-170">For example, one service behavior is throttling, which specifies how a service reacts when an excess of messages threaten to overwhelm its handling capabilities.</span></span> <span data-ttu-id="c28f3-171">Zachowanie punktu końcowego, z drugiej strony, kontroluje tylko aspekty, które są istotne dla punktów końcowych, takich jak i gdzie można znaleźć poświadczenia zabezpieczeń.</span><span class="sxs-lookup"><span data-stu-id="c28f3-171">An endpoint behavior, on the other hand, controls only aspects that are relevant to endpoints, such as how and where to find a security credential.</span></span>

<span data-ttu-id="c28f3-172">**Powiązania dostarczone przez system**</span><span class="sxs-lookup"><span data-stu-id="c28f3-172">**System-provided bindings**</span></span>  
 <span data-ttu-id="c28f3-173">Program WCF zawiera wiele powiązań dostarczonych przez system.</span><span class="sxs-lookup"><span data-stu-id="c28f3-173">WCF includes a number of system-provided bindings.</span></span> <span data-ttu-id="c28f3-174">Są to kolekcje elementów powiązania, które są zoptymalizowane pod kątem określonych scenariuszy.</span><span class="sxs-lookup"><span data-stu-id="c28f3-174">These are collections of binding elements that are optimized for specific scenarios.</span></span> <span data-ttu-id="c28f3-175">Na przykład program <xref:System.ServiceModel.WSHttpBinding> jest przeznaczony do współdziałania z usługami, które implementują różne specyfikacje WS-Specification \* .</span><span class="sxs-lookup"><span data-stu-id="c28f3-175">For example, the <xref:System.ServiceModel.WSHttpBinding> is designed for interoperability with services that implement various WS-\* specifications.</span></span> <span data-ttu-id="c28f3-176">Te wstępnie zdefiniowane powiązania oszczędzają czas, wyświetlając tylko te opcje, które można prawidłowo zastosować do konkretnego scenariusza.</span><span class="sxs-lookup"><span data-stu-id="c28f3-176">These predefined bindings save time by presenting only those options that can be correctly applied to the specific scenario.</span></span> <span data-ttu-id="c28f3-177">Jeśli wstępnie zdefiniowane powiązanie nie spełnia wymagań użytkownika, można utworzyć własne niestandardowe powiązania.</span><span class="sxs-lookup"><span data-stu-id="c28f3-177">If a predefined binding does not meet your requirements, you can create your own custom binding.</span></span>

<span data-ttu-id="c28f3-178">**Konfiguracja a kodowanie**</span><span class="sxs-lookup"><span data-stu-id="c28f3-178">**Configuration versus coding**</span></span>  
 <span data-ttu-id="c28f3-179">Kontrolę nad aplikacją można wykonać za pomocą kodowania, za pomocą konfiguracji lub kombinacji obu tych elementów.</span><span class="sxs-lookup"><span data-stu-id="c28f3-179">Control of an application can be done either through coding, through configuration, or through a combination of both.</span></span> <span data-ttu-id="c28f3-180">W konfiguracji można umożliwić innym osobom niż deweloper (na przykład administratora sieci) Ustawianie parametrów klienta i usługi po napisaniu kodu i bez konieczności ponownego kompilowania.</span><span class="sxs-lookup"><span data-stu-id="c28f3-180">Configuration has the advantage of allowing someone other than the developer (for example, a network administrator) to set client and service parameters after the code is written and without having to recompile.</span></span> <span data-ttu-id="c28f3-181">Konfiguracja umożliwia nie tylko Ustawianie wartości takich jak adresy punktów końcowych, ale również umożliwia dalsze kontrolowanie, umożliwiając dodawanie punktów końcowych, powiązań i zachowań.</span><span class="sxs-lookup"><span data-stu-id="c28f3-181">Configuration not only enables you to set values like endpoint addresses, but also allows further control by enabling you to add endpoints, bindings, and behaviors.</span></span> <span data-ttu-id="c28f3-182">Kodowanie umożliwia deweloperowi zachowanie ścisłej kontroli nad wszystkimi składnikami usługi lub klienta, a także wszelkie ustawienia wykonywane przez konfigurację, a w razie potrzeby przesłonięte przez kod.</span><span class="sxs-lookup"><span data-stu-id="c28f3-182">Coding allows the developer to retain strict control over all components of the service or client, and any settings done through the configuration can be inspected and if needed overridden by the code.</span></span>

<span data-ttu-id="c28f3-183">**Operacja usługi**</span><span class="sxs-lookup"><span data-stu-id="c28f3-183">**Service operation**</span></span>  
 <span data-ttu-id="c28f3-184">Procedura zdefiniowana w kodzie usługi, która implementuje funkcje dla operacji.</span><span class="sxs-lookup"><span data-stu-id="c28f3-184">A procedure defined in a service's code that implements the functionality for an operation.</span></span> <span data-ttu-id="c28f3-185">Ta operacja jest udostępniana klientom jako metody na kliencie WCF.</span><span class="sxs-lookup"><span data-stu-id="c28f3-185">This operation is exposed to clients as methods on a WCF client.</span></span> <span data-ttu-id="c28f3-186">Metoda może zwracać wartość i może przyjmować opcjonalną liczbę argumentów lub nie przyjmować argumentów i nie zwraca odpowiedzi.</span><span class="sxs-lookup"><span data-stu-id="c28f3-186">The method can return a value, and can take an optional number of arguments, or take no arguments, and return no response.</span></span> <span data-ttu-id="c28f3-187">Na przykład operacja, która działa jako prosta "Hello", może służyć jako powiadomienie o obecności klienta i rozpocząć serię operacji.</span><span class="sxs-lookup"><span data-stu-id="c28f3-187">For example, an operation that functions as a simple "Hello" can be used as a notification of a client's presence and to begin a series of operations.</span></span>

<span data-ttu-id="c28f3-188">**Kontrakt usługi**</span><span class="sxs-lookup"><span data-stu-id="c28f3-188">**Service contract**</span></span>  
 <span data-ttu-id="c28f3-189">Łączy wiele operacji związanych z pojedynczą jednostką funkcjonalną.</span><span class="sxs-lookup"><span data-stu-id="c28f3-189">Ties together multiple related operations into a single functional unit.</span></span> <span data-ttu-id="c28f3-190">Kontrakt może definiować ustawienia poziomu usługi, takie jak przestrzeń nazw usługi, odpowiedni kontrakt wywołania zwrotnego i inne ustawienia.</span><span class="sxs-lookup"><span data-stu-id="c28f3-190">The contract can define service-level settings, such as the namespace of the service, a corresponding callback contract, and other such settings.</span></span> <span data-ttu-id="c28f3-191">W większości przypadków umowa jest definiowana przez utworzenie interfejsu w wybranym języku programowania i zastosowanie <xref:System.ServiceModel.ServiceContractAttribute> atrybutu do interfejsu.</span><span class="sxs-lookup"><span data-stu-id="c28f3-191">In most cases, the contract is defined by creating an interface in the programming language of your choice and applying the <xref:System.ServiceModel.ServiceContractAttribute> attribute to the interface.</span></span> <span data-ttu-id="c28f3-192">Rzeczywisty kod usługi powstaje przez implementację interfejsu.</span><span class="sxs-lookup"><span data-stu-id="c28f3-192">The actual service code results by implementing the interface.</span></span>

<span data-ttu-id="c28f3-193">**Kontrakt operacji**</span><span class="sxs-lookup"><span data-stu-id="c28f3-193">**Operation contract**</span></span>  
 <span data-ttu-id="c28f3-194">Kontrakt operacji definiuje parametry i zwracany typ operacji.</span><span class="sxs-lookup"><span data-stu-id="c28f3-194">An operation contract defines the parameters and return type of an operation.</span></span> <span data-ttu-id="c28f3-195">Podczas tworzenia interfejsu, który definiuje kontrakt usługi, oznacza to, że kontrakt operacji jest stosowany przez zastosowanie <xref:System.ServiceModel.OperationContractAttribute> atrybutu do każdej definicji metody, która jest częścią kontraktu.</span><span class="sxs-lookup"><span data-stu-id="c28f3-195">When creating an interface that defines the service contract, you signify an operation contract by applying the <xref:System.ServiceModel.OperationContractAttribute> attribute to each method definition that is part of the contract.</span></span> <span data-ttu-id="c28f3-196">Operacje mogą być modelowane jako przejmowanie pojedynczej wiadomości i zwracanie pojedynczej wiadomości lub jako zestawu typów i zwracanie typu.</span><span class="sxs-lookup"><span data-stu-id="c28f3-196">The operations can be modeled as taking a single message and returning a single message, or as taking a set of types and returning a type.</span></span> <span data-ttu-id="c28f3-197">W tym drugim przypadku system określi Format komunikatów, które muszą być wymieniane dla tej operacji.</span><span class="sxs-lookup"><span data-stu-id="c28f3-197">In the latter case, the system will determine the format for the messages that need to be exchanged for that operation.</span></span>

<span data-ttu-id="c28f3-198">**Kontrakt komunikatu**</span><span class="sxs-lookup"><span data-stu-id="c28f3-198">**Message contract**</span></span>  
 <span data-ttu-id="c28f3-199">Opisuje format wiadomości.</span><span class="sxs-lookup"><span data-stu-id="c28f3-199">Describes the format of a message.</span></span> <span data-ttu-id="c28f3-200">Na przykład deklaruje, czy elementy wiadomości powinny znajdować się w nagłówkach, a treści, jakiego poziomu zabezpieczeń należy zastosować do elementów tego komunikatu itd.</span><span class="sxs-lookup"><span data-stu-id="c28f3-200">For example, it declares whether message elements should go in headers versus the body, what level of security should be applied to what elements of the message, and so on.</span></span>

<span data-ttu-id="c28f3-201">**Umowa dotycząca błędów**</span><span class="sxs-lookup"><span data-stu-id="c28f3-201">**Fault contract**</span></span>  
 <span data-ttu-id="c28f3-202">Można skojarzyć z operacją usługi, aby zauważyć błędy, które mogą zostać zwrócone do obiektu wywołującego.</span><span class="sxs-lookup"><span data-stu-id="c28f3-202">Can be associated with a service operation to denote errors that can be returned to the caller.</span></span> <span data-ttu-id="c28f3-203">Z operacją może być skojarzonych zero lub więcej błędów.</span><span class="sxs-lookup"><span data-stu-id="c28f3-203">An operation can have zero or more faults associated with it.</span></span> <span data-ttu-id="c28f3-204">Te błędy są błędami protokołu SOAP, które są modelowane jako wyjątki w modelu programowania.</span><span class="sxs-lookup"><span data-stu-id="c28f3-204">These errors are SOAP faults that are modeled as exceptions in the programming model.</span></span>

<span data-ttu-id="c28f3-205">**Kontrakt danych**</span><span class="sxs-lookup"><span data-stu-id="c28f3-205">**Data contract**</span></span>  
 <span data-ttu-id="c28f3-206">Opisy w metadanych typów danych używanych przez usługę.</span><span class="sxs-lookup"><span data-stu-id="c28f3-206">The descriptions in metadata of the data types that a service uses.</span></span> <span data-ttu-id="c28f3-207">Dzięki temu inne osoby mogą współdziałać z usługą.</span><span class="sxs-lookup"><span data-stu-id="c28f3-207">This enables others to interoperate with the service.</span></span> <span data-ttu-id="c28f3-208">Typy danych mogą być używane w dowolnej części komunikatu, na przykład jako parametry lub typy zwracane.</span><span class="sxs-lookup"><span data-stu-id="c28f3-208">The data types can be used in any part of a message, for example, as parameters or return types.</span></span> <span data-ttu-id="c28f3-209">Jeśli usługa używa tylko typów prostych, nie ma potrzeby jawnego używania kontraktów danych.</span><span class="sxs-lookup"><span data-stu-id="c28f3-209">If the service is using only simple types, there is no need to explicitly use data contracts.</span></span>

<span data-ttu-id="c28f3-210">**Hosting**</span><span class="sxs-lookup"><span data-stu-id="c28f3-210">**Hosting**</span></span>  
 <span data-ttu-id="c28f3-211">Usługa musi być hostowana w pewnym procesie.</span><span class="sxs-lookup"><span data-stu-id="c28f3-211">A service must be hosted in some process.</span></span> <span data-ttu-id="c28f3-212">_Host_ to aplikacja, która kontroluje okres istnienia usługi.</span><span class="sxs-lookup"><span data-stu-id="c28f3-212">A _host_ is an application that controls the lifetime of the service.</span></span> <span data-ttu-id="c28f3-213">Usługi mogą być samoobsługowe lub zarządzane przez istniejący proces hostingu.</span><span class="sxs-lookup"><span data-stu-id="c28f3-213">Services can be self-hosted or managed by an existing hosting process.</span></span>

<span data-ttu-id="c28f3-214">**Usługa samodzielna**</span><span class="sxs-lookup"><span data-stu-id="c28f3-214">**Self-hosted service**</span></span>  
 <span data-ttu-id="c28f3-215">Usługa działająca w ramach aplikacji przetwarzanej utworzonej przez dewelopera.</span><span class="sxs-lookup"><span data-stu-id="c28f3-215">A service that runs within a process application that the developer created.</span></span> <span data-ttu-id="c28f3-216">Deweloper kontroluje swój okres istnienia, ustawia właściwości usługi, otwiera usługę (która ustawia ją w tryb nasłuchiwania) i zamyka usługę.</span><span class="sxs-lookup"><span data-stu-id="c28f3-216">The developer controls its lifetime, sets the properties of the service, opens the service (which sets it into a listening mode), and closes the service.</span></span>

<span data-ttu-id="c28f3-217">**Proces hostingu**</span><span class="sxs-lookup"><span data-stu-id="c28f3-217">**Hosting process**</span></span>  
 <span data-ttu-id="c28f3-218">Aplikacja, która jest przeznaczona do hostowania usług.</span><span class="sxs-lookup"><span data-stu-id="c28f3-218">An application that is designed to host services.</span></span> <span data-ttu-id="c28f3-219">Obejmują one Internet Information Services (IIS), usługi aktywacji systemu Windows (WAS) i usługi systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="c28f3-219">These include Internet Information Services (IIS), Windows Activation Services (WAS), and Windows Services.</span></span> <span data-ttu-id="c28f3-220">W tych scenariuszach hostowanych Host kontroluje okres istnienia usługi.</span><span class="sxs-lookup"><span data-stu-id="c28f3-220">In these hosted scenarios, the host controls the lifetime of the service.</span></span> <span data-ttu-id="c28f3-221">Na przykład przy użyciu usług IIS można skonfigurować katalog wirtualny, który zawiera zestaw usługi i plik konfiguracji.</span><span class="sxs-lookup"><span data-stu-id="c28f3-221">For example, using IIS you can set up a virtual directory that contains the service assembly and configuration file.</span></span> <span data-ttu-id="c28f3-222">Po odebraniu komunikatu Usługa IIS uruchamia usługę i kontroluje jej okres istnienia.</span><span class="sxs-lookup"><span data-stu-id="c28f3-222">When a message is received, IIS starts the service and controls its lifetime.</span></span>

<span data-ttu-id="c28f3-223">**Tworzenie wystąpienia**</span><span class="sxs-lookup"><span data-stu-id="c28f3-223">**Instancing**</span></span>  
 <span data-ttu-id="c28f3-224">Usługa ma model wystąpień.</span><span class="sxs-lookup"><span data-stu-id="c28f3-224">A service has an instancing model.</span></span> <span data-ttu-id="c28f3-225">Istnieją trzy modele wystąpienia: "Single", w których pojedynczej usłudze obiektów CLR wszyscy klienci; " na wywołanie "w którym tworzony jest nowy obiekt CLR w celu obsługi każdego wywołania klienta; i "na sesję", w której tworzony jest zestaw obiektów CLR, po jednej dla każdej oddzielnej sesji.</span><span class="sxs-lookup"><span data-stu-id="c28f3-225">There are three instancing models: "single," in which a single CLR object services all the clients; "per call," in which a new CLR object is created to handle each client call; and "per session," in which a set of CLR objects is created, one for each separate session.</span></span> <span data-ttu-id="c28f3-226">Wybór modelu wystąpienia zależy od wymagań aplikacji i oczekiwanego wzorca użycia usługi.</span><span class="sxs-lookup"><span data-stu-id="c28f3-226">The choice of an instancing model depends on the application requirements and the expected usage pattern of the service.</span></span>

<span data-ttu-id="c28f3-227">**Aplikacja kliencka**</span><span class="sxs-lookup"><span data-stu-id="c28f3-227">**Client application**</span></span>  
 <span data-ttu-id="c28f3-228">Program, który wymienia komunikaty z co najmniej jednym elementem końcowym.</span><span class="sxs-lookup"><span data-stu-id="c28f3-228">A program that exchanges messages with one or more endpoints.</span></span> <span data-ttu-id="c28f3-229">Aplikacja kliencka rozpoczyna się od utworzenia wystąpienia klienta WCF i wywoływania metod klienta programu WCF.</span><span class="sxs-lookup"><span data-stu-id="c28f3-229">The client application begins by creating an instance of a WCF client and calling methods of the WCF client.</span></span> <span data-ttu-id="c28f3-230">Należy pamiętać, że pojedyncza aplikacja może być zarówno klientem, jak i usługą.</span><span class="sxs-lookup"><span data-stu-id="c28f3-230">It is important to note that a single application can be both a client and a service.</span></span>

<span data-ttu-id="c28f3-231">**Kanał**</span><span class="sxs-lookup"><span data-stu-id="c28f3-231">**Channel**</span></span>  
 <span data-ttu-id="c28f3-232">Konkretna implementacja elementu powiązania.</span><span class="sxs-lookup"><span data-stu-id="c28f3-232">A concrete implementation of a binding element.</span></span> <span data-ttu-id="c28f3-233">Powiązanie reprezentuje konfigurację, a kanał jest implementacją skojarzoną z tą konfiguracją.</span><span class="sxs-lookup"><span data-stu-id="c28f3-233">The binding represents the configuration, and the channel is the implementation associated with that configuration.</span></span> <span data-ttu-id="c28f3-234">W związku z tym istnieje kanał skojarzony z każdym elementem powiązania.</span><span class="sxs-lookup"><span data-stu-id="c28f3-234">Therefore, there is a channel associated with each binding element.</span></span> <span data-ttu-id="c28f3-235">Stos kanałów na siebie, aby utworzyć konkretną implementację powiązania: stos kanałów.</span><span class="sxs-lookup"><span data-stu-id="c28f3-235">Channels stack on top of each other to create the concrete implementation of the binding: the channel stack.</span></span>

<span data-ttu-id="c28f3-236">**Klient WCF**</span><span class="sxs-lookup"><span data-stu-id="c28f3-236">**WCF client**</span></span>  
 <span data-ttu-id="c28f3-237">Konstrukcja klient-aplikacja, która uwidacznia operacje usługi jako metody (w wybranym języku programowania .NET Framework, takich jak Visual Basic lub Visual C#).</span><span class="sxs-lookup"><span data-stu-id="c28f3-237">A client-application construct that exposes the service operations as methods (in the .NET Framework programming language of your choice, such as Visual Basic or Visual C#).</span></span> <span data-ttu-id="c28f3-238">Każda aplikacja może obsługiwać klienta WCF, w tym aplikację, która hostuje usługę.</span><span class="sxs-lookup"><span data-stu-id="c28f3-238">Any application can host a WCF client, including an application that hosts a service.</span></span> <span data-ttu-id="c28f3-239">W związku z tym można utworzyć usługę, która obejmuje klientów programu WCF innych usług.</span><span class="sxs-lookup"><span data-stu-id="c28f3-239">Therefore, it is possible to create a service that includes WCF clients of other services.</span></span>

<span data-ttu-id="c28f3-240">Klienta WCF można automatycznie wygenerować przy użyciu narzędzia do obsługi [metadanych ServiceModel (Svcutil.exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) i wskazać w uruchomionej usłudze, która publikuje metadane.</span><span class="sxs-lookup"><span data-stu-id="c28f3-240">A WCF client can be automatically generated by using the [ServiceModel Metadata Utility Tool (Svcutil.exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) and pointing it at a running service that publishes metadata.</span></span>

<span data-ttu-id="c28f3-241">**Metadane**</span><span class="sxs-lookup"><span data-stu-id="c28f3-241">**Metadata**</span></span>  
 <span data-ttu-id="c28f3-242">W usłudze program opisuje charakterystykę usługi, którą zewnętrzna jednostka musi zrozumieć, aby komunikować się z usługą.</span><span class="sxs-lookup"><span data-stu-id="c28f3-242">In a service, describes the characteristics of the service that an external entity needs to understand to communicate with the service.</span></span> <span data-ttu-id="c28f3-243">Metadane mogą być używane przez narzędzie do obsługi [metadanych modelu ServiceModel (Svcutil.exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) do generowania klienta programu WCF i dołączonej konfiguracji, która może być używana przez aplikację kliencką do współpracy z usługą.</span><span class="sxs-lookup"><span data-stu-id="c28f3-243">Metadata can be consumed by the [ServiceModel Metadata Utility Tool (Svcutil.exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) to generate a WCF client and accompanying configuration that a client application can use to interact with the service.</span></span>

<span data-ttu-id="c28f3-244">Metadane udostępniane przez usługę obejmują dokumenty schematu XML, które definiują kontrakt danych usługi i dokumenty WSDL opisujące metody usługi.</span><span class="sxs-lookup"><span data-stu-id="c28f3-244">The metadata exposed by the service includes XML schema documents, which define the data contract of the service, and WSDL documents, which describe the methods of the service.</span></span>

<span data-ttu-id="c28f3-245">Po włączeniu tej funkcji metadane usługi są generowane automatycznie przez program WCF przez sprawdzenie usługi i jej punktów końcowych.</span><span class="sxs-lookup"><span data-stu-id="c28f3-245">When enabled, metadata for the service is automatically generated by WCF by inspecting the service and its endpoints.</span></span> <span data-ttu-id="c28f3-246">Aby opublikować metadane z usługi, należy jawnie włączyć zachowanie metadanych.</span><span class="sxs-lookup"><span data-stu-id="c28f3-246">To publish metadata from a service, you must explicitly enable the metadata behavior.</span></span>

<span data-ttu-id="c28f3-247">**Bezpieczeństwo**</span><span class="sxs-lookup"><span data-stu-id="c28f3-247">**Security**</span></span>  
 <span data-ttu-id="c28f3-248">W programie WCF obejmuje poufność (szyfrowanie komunikatów, aby zapobiec podsłuchiwaniu), integralność (środki na potrzeby wykrywania manipulowania komunikatem), uwierzytelnianie (sposób sprawdzania poprawności serwerów i klientów) oraz autoryzację (kontrolę dostępu do zasobów).</span><span class="sxs-lookup"><span data-stu-id="c28f3-248">In WCF, includes confidentiality (encryption of messages to prevent eavesdropping), integrity (the means for detection of tampering with the message), authentication (the means for validation of servers and clients), and authorization (the control of access to resources).</span></span> <span data-ttu-id="c28f3-249">Te funkcje są udostępniane przy użyciu istniejących mechanizmów zabezpieczeń, takich jak TLS za pośrednictwem protokołu HTTP (znanego również jako HTTPS) lub przez implementację co najmniej jednej z różnych \* specyfikacji WS-Security.</span><span class="sxs-lookup"><span data-stu-id="c28f3-249">These functions are provided by either leveraging existing security mechanisms, such as TLS over HTTP (also known as HTTPS), or by implementing one or more of the various WS-\* security specifications.</span></span>

<span data-ttu-id="c28f3-250">**Tryb zabezpieczeń transportu**</span><span class="sxs-lookup"><span data-stu-id="c28f3-250">**Transport security mode**</span></span>  
 <span data-ttu-id="c28f3-251">Określa, że poufność, integralność i uwierzytelnianie są udostępniane przez mechanizmy warstwy transportowej (takie jak HTTPS).</span><span class="sxs-lookup"><span data-stu-id="c28f3-251">Specifies that confidentiality, integrity, and authentication are provided by the transport layer mechanisms (such as HTTPS).</span></span> <span data-ttu-id="c28f3-252">W przypadku korzystania z transportu, takiego jak HTTPS, ten tryb ma zaletę wydajnej wydajności i jest zrozumiały ze względu na jego powszechność w Internecie.</span><span class="sxs-lookup"><span data-stu-id="c28f3-252">When using a transport like HTTPS, this mode has the advantage of being efficient in its performance, and well understood because of its prevalence on the Internet.</span></span> <span data-ttu-id="c28f3-253">Wadą jest to, że ten rodzaj zabezpieczeń jest stosowany osobno dla każdego przeskoku w ścieżce komunikacji, co sprawia, że komunikacja jest podatna na ataki "man in the middle".</span><span class="sxs-lookup"><span data-stu-id="c28f3-253">The disadvantage is that this kind of security is applied separately on each hop in the communication path, making the communication susceptible to a "man in the middle" attack.</span></span>

<span data-ttu-id="c28f3-254">**Tryb zabezpieczeń wiadomości**</span><span class="sxs-lookup"><span data-stu-id="c28f3-254">**Message security mode**</span></span>  
 <span data-ttu-id="c28f3-255">Określa, że zabezpieczenia są zapewniane przez implementację co najmniej jednej specyfikacji zabezpieczeń, takiej jak specyfikacja o nazwie [zabezpieczenia usług w sieci Web: zabezpieczenia komunikatów protokołu SOAP](http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-soap-message-security-1.0.pdf).</span><span class="sxs-lookup"><span data-stu-id="c28f3-255">Specifies that security is provided by implementing one or more of the security specifications, such as the specification named [Web Services Security: SOAP Message Security](http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-soap-message-security-1.0.pdf).</span></span> <span data-ttu-id="c28f3-256">Każdy komunikat zawiera niezbędne mechanizmy zapewnienia bezpieczeństwa w trakcie jego przesyłania oraz umożliwienie odbiornikom wykrywania naruszenia i odszyfrowywania wiadomości.</span><span class="sxs-lookup"><span data-stu-id="c28f3-256">Each message contains the necessary mechanisms to provide security during its transit, and to enable the receivers to detect tampering and to decrypt the messages.</span></span> <span data-ttu-id="c28f3-257">W tym sensie zabezpieczenia są hermetyzowane w każdym komunikacie, zapewniając kompleksowe zabezpieczenia dla wielu przeskoków.</span><span class="sxs-lookup"><span data-stu-id="c28f3-257">In this sense, the security is encapsulated within every message, providing end-to-end security across multiple hops.</span></span> <span data-ttu-id="c28f3-258">Ze względu na to, że informacje o zabezpieczeniach staną się częścią komunikatu, można również uwzględnić wiele rodzajów poświadczeń z komunikatem (są one określane jako _oświadczenia_).</span><span class="sxs-lookup"><span data-stu-id="c28f3-258">Because security information becomes part of the message, it is also possible to include multiple kinds of credentials with the message (these are referred to as _claims_).</span></span> <span data-ttu-id="c28f3-259">Takie podejście umożliwia również umożliwienie bezpiecznego przesyłania komunikatów w ramach dowolnego transportu, w tym wielu transportów między jej źródłem i miejscem docelowym.</span><span class="sxs-lookup"><span data-stu-id="c28f3-259">This approach also has the advantage of enabling the message to travel securely over any transport, including multiple transports between its origin and destination.</span></span> <span data-ttu-id="c28f3-260">Wadą tej metody jest złożoność stosowanych mechanizmów kryptograficznych, co skutkuje wpływem na wydajność.</span><span class="sxs-lookup"><span data-stu-id="c28f3-260">The disadvantage of this approach is the complexity of the cryptographic mechanisms employed, resulting in performance implications.</span></span>

<span data-ttu-id="c28f3-261">**Transport z użyciem poświadczeń komunikatów tryb zabezpieczeń**</span><span class="sxs-lookup"><span data-stu-id="c28f3-261">**Transport with message credential security mode**</span></span>  
 <span data-ttu-id="c28f3-262">Określa użycie warstwy transportowej w celu zapewnienia poufności, uwierzytelniania i integralności komunikatów, natomiast każdy komunikat może zawierać wiele poświadczeń (oświadczeń) wymaganych przez odbiorniki wiadomości.</span><span class="sxs-lookup"><span data-stu-id="c28f3-262">Specifies the use of the transport layer to provide confidentiality, authentication, and integrity of the messages, while each of the messages can contain multiple credentials (claims) required by the receivers of the message.</span></span>

<span data-ttu-id="c28f3-263">**WS\***</span><span class="sxs-lookup"><span data-stu-id="c28f3-263">**WS-\***</span></span>  
 <span data-ttu-id="c28f3-264">Skrót dla rosnącego zestawu specyfikacji usługi sieci Web (WS), takich jak WS-Security, WS-ReliableMessaging i tak dalej, które są implementowane w programie WCF.</span><span class="sxs-lookup"><span data-stu-id="c28f3-264">Shorthand for the growing set of Web Service (WS) specifications, such as WS-Security, WS-ReliableMessaging, and so on, that are implemented in WCF.</span></span>

## <a name="see-also"></a><span data-ttu-id="c28f3-265">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="c28f3-265">See also</span></span>

- [<span data-ttu-id="c28f3-266">Co to jest program Windows Communication Foundation</span><span class="sxs-lookup"><span data-stu-id="c28f3-266">What Is Windows Communication Foundation</span></span>](whats-wcf.md)
- [<span data-ttu-id="c28f3-267">Architektura WCF (Windows Communication Foundation)</span><span class="sxs-lookup"><span data-stu-id="c28f3-267">Windows Communication Foundation Architecture</span></span>](architecture.md)
