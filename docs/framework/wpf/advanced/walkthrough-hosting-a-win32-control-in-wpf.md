---
title: 'Przewodnik: hostowanie kontrolki Win32 w WPF'
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- hosting Win32 control in WPF [WPF]
- Win32 code [WPF], WPF interoperation
ms.assetid: a676b1eb-fc55-4355-93ab-df840c41cea0
ms.openlocfilehash: 1ba060fcefb2d8be24d597c7b1ccb7a79d6d5ceb
ms.sourcegitcommit: 5b6d778ebb269ee6684fb57ad69a8c28b06235b9
ms.translationtype: HT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/08/2019
ms.locfileid: "59160696"
---
# <a name="walkthrough-hosting-a-win32-control-in-wpf"></a><span data-ttu-id="7c422-102">Przewodnik: hostowanie kontrolki Win32 w WPF</span><span class="sxs-lookup"><span data-stu-id="7c422-102">Walkthrough: Hosting a Win32 Control in WPF</span></span>
<span data-ttu-id="7c422-103">Windows Presentation Foundation (WPF) zapewnia rozbudowane środowisko do tworzenia aplikacji.</span><span class="sxs-lookup"><span data-stu-id="7c422-103">Windows Presentation Foundation (WPF) provides a rich environment for creating applications.</span></span> <span data-ttu-id="7c422-104">Jednak jeśli masz znaczne inwestycje w kodzie Win32, może być bardziej efektywne ponownie użyć przynajmniej część tego kodu w aplikacji WPF, a nie jego przepisania całkowicie.</span><span class="sxs-lookup"><span data-stu-id="7c422-104">However, when you have a substantial investment in Win32 code, it may be more effective to reuse at least some of that code in your WPF application rather than rewrite it completely.</span></span> <span data-ttu-id="7c422-105">WPF zapewnia prosty mechanizm do obsługi oknie Win32, na stronie programu WPF.</span><span class="sxs-lookup"><span data-stu-id="7c422-105">WPF provides a straightforward mechanism for hosting a Win32 window, on a WPF page.</span></span>  
  
 <span data-ttu-id="7c422-106">Ten temat przeprowadzi Cię przez aplikację, [Hosting kontrolki ListBox Win32 w przykładzie WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control), aby kontrolować Win32 pole listy hostów.</span><span class="sxs-lookup"><span data-stu-id="7c422-106">This topic walks you through an application, [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control), that hosts a Win32 list box control.</span></span> <span data-ttu-id="7c422-107">Ta procedura ogólne można rozszerzyć z hostingiem dowolne Win32 okno.</span><span class="sxs-lookup"><span data-stu-id="7c422-107">This general procedure can be extended to hosting any Win32 window.</span></span>  

<a name="requirements"></a>   
## <a name="requirements"></a><span data-ttu-id="7c422-108">Wymagania</span><span class="sxs-lookup"><span data-stu-id="7c422-108">Requirements</span></span>  
 <span data-ttu-id="7c422-109">W tym temacie założono podstawowe umiejętność programowania WPF i Windows API.</span><span class="sxs-lookup"><span data-stu-id="7c422-109">This topic assumes a basic familiarity with both WPF and Windows API programming.</span></span> <span data-ttu-id="7c422-110">Aby uzyskać podstawowe wprowadzenie do programowania WPF, zobacz [wprowadzenie](../getting-started/index.md).</span><span class="sxs-lookup"><span data-stu-id="7c422-110">For a basic introduction to WPF programming, see [Getting Started](../getting-started/index.md).</span></span> <span data-ttu-id="7c422-111">Wprowadzenie do programowania interfejsu API Windows, zobacz dowolny wiele książek na ten temat, w szczególności *programowania Windows* przez Charles Petzold.</span><span class="sxs-lookup"><span data-stu-id="7c422-111">For an introduction to Windows API programming, see any of the numerous books on the subject, in particular *Programming Windows* by Charles Petzold.</span></span>  
  
 <span data-ttu-id="7c422-112">Ponieważ przykładu, który towarzyszy w tym temacie jest zaimplementowana w C#, to sprawia, że użycie platformę wywołania usług (funkcja PInvoke) dostęp do interfejsu API Windows.</span><span class="sxs-lookup"><span data-stu-id="7c422-112">Because the sample that accompanies this topic is implemented in C#, it makes use of Platform Invocation Services (PInvoke) to access the Windows API.</span></span> <span data-ttu-id="7c422-113">Pewną znajomość PInvoke jest przydatna, ale nie są niezbędne.</span><span class="sxs-lookup"><span data-stu-id="7c422-113">Some familiarity with PInvoke is helpful but not essential.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="7c422-114">Ten temat zawiera szereg przykładów kodu z próbki skojarzone.</span><span class="sxs-lookup"><span data-stu-id="7c422-114">This topic includes a number of code examples from the associated sample.</span></span> <span data-ttu-id="7c422-115">Jednak aby zwiększyć czytelność, nie obejmuje pełny przykładowy kod.</span><span class="sxs-lookup"><span data-stu-id="7c422-115">However, for readability, it does not include the complete sample code.</span></span> <span data-ttu-id="7c422-116">Można uzyskać lub wyświetlić kompletny kod z [Hosting kontrolki ListBox Win32 w przykładzie WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control).</span><span class="sxs-lookup"><span data-stu-id="7c422-116">You can obtain or view complete code from [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control).</span></span>  
  
<a name="basic_procedure"></a>   
## <a name="the-basic-procedure"></a><span data-ttu-id="7c422-117">Podstawowa procedura</span><span class="sxs-lookup"><span data-stu-id="7c422-117">The Basic Procedure</span></span>  
 <span data-ttu-id="7c422-118">W tej sekcji opisano podstawowe procedury do obsługi oknie Win32 na stronie programu WPF.</span><span class="sxs-lookup"><span data-stu-id="7c422-118">This section outlines the basic procedure for hosting a Win32 window on a WPF page.</span></span> <span data-ttu-id="7c422-119">Pozostałe sekcje przechodzą przez szczegółowe informacje o każdym kroku.</span><span class="sxs-lookup"><span data-stu-id="7c422-119">The remaining sections go through the details of each step.</span></span>  
  
 <span data-ttu-id="7c422-120">Podstawowa procedura obsługi jest:</span><span class="sxs-lookup"><span data-stu-id="7c422-120">The basic hosting procedure is:</span></span>  
  
1.  <span data-ttu-id="7c422-121">Implementowanie strony WPF do hostowania okna.</span><span class="sxs-lookup"><span data-stu-id="7c422-121">Implement a WPF page to host the window.</span></span> <span data-ttu-id="7c422-122">Jedna z technik jest utworzenie <xref:System.Windows.Controls.Border> element, aby zarezerwować części strony okna hostowanej.</span><span class="sxs-lookup"><span data-stu-id="7c422-122">One technique is to create a <xref:System.Windows.Controls.Border> element to reserve a section of the page for the hosted window.</span></span>  
  
2.  <span data-ttu-id="7c422-123">Implementowanie klasy do hostowania kontrolki, która dziedziczy <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="7c422-123">Implement a class to host the control that inherits from <xref:System.Windows.Interop.HwndHost>.</span></span>  
  
3.  <span data-ttu-id="7c422-124">W tej klasie zastąpić <xref:System.Windows.Interop.HwndHost> składowej klasy <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>.</span><span class="sxs-lookup"><span data-stu-id="7c422-124">In that class, override the <xref:System.Windows.Interop.HwndHost> class member <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>.</span></span>  
  
4.  <span data-ttu-id="7c422-125">Utworzyć hostowanej okno jako element podrzędny okno, które zawiera stronę WPF.</span><span class="sxs-lookup"><span data-stu-id="7c422-125">Create the hosted window as a child of the window that contains the WPF page.</span></span> <span data-ttu-id="7c422-126">Mimo że konwencjonalne programowania WPF nie trzeba jawnie wprowadzać z niej korzystać, strona macierzysta jest oknem z dojściem (HWND).</span><span class="sxs-lookup"><span data-stu-id="7c422-126">Although conventional WPF programming does not need to explicitly make use of it, the hosting page is a window with a handle (HWND).</span></span> <span data-ttu-id="7c422-127">Pojawi się Strona HWND za pośrednictwem `hwndParent` parametru <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="7c422-127">You receive the page HWND through the `hwndParent` parameter of the <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A> method.</span></span> <span data-ttu-id="7c422-128">Okno hostowanej powinien zostać utworzony jako element podrzędny tego HWND.</span><span class="sxs-lookup"><span data-stu-id="7c422-128">The hosted window should be created as a child of this HWND.</span></span>  
  
5.  <span data-ttu-id="7c422-129">Po utworzeniu okno hosta Zwróć HWND hostowanej okna.</span><span class="sxs-lookup"><span data-stu-id="7c422-129">Once you have created the host window, return the HWND of the hosted window.</span></span> <span data-ttu-id="7c422-130">Jeśli chcesz udostępnić co najmniej jednej kontrolki Win32 są zazwyczaj tworzone okna hosta jako element podrzędny HWND i utworzyć formanty elementy podrzędne tego okna hosta.</span><span class="sxs-lookup"><span data-stu-id="7c422-130">If you want to host one or more Win32 controls, you typically create a host window as a child of the HWND and make the controls children of that host window.</span></span> <span data-ttu-id="7c422-131">OPAKOWYWANIE formantów w oknie hosta zapewnia prostą metodę dla strony WPF może otrzymywać powiadomienia z formantów, które dotyczy niektóre konkretnej kwestii Win32, dzięki powiadomieniom granicę HWND.</span><span class="sxs-lookup"><span data-stu-id="7c422-131">Wrapping the controls in a host window provides a simple way for your WPF page to receive notifications from the controls, which deals with some particular Win32 issues with notifications across the HWND boundary.</span></span>  
  
6.  <span data-ttu-id="7c422-132">Obsługuj wybrane komunikaty wysyłane do okna hosta, takich jak powiadomienia z formantów podrzędnych.</span><span class="sxs-lookup"><span data-stu-id="7c422-132">Handle selected messages sent to the host window, such as notifications from child controls.</span></span> <span data-ttu-id="7c422-133">Istnieją dwa sposoby, aby to zrobić.</span><span class="sxs-lookup"><span data-stu-id="7c422-133">There are two ways to do this.</span></span>  
  
    -   <span data-ttu-id="7c422-134">Jeśli wolisz do obsługi wiadomości w klasie hostingu, Zastąp <xref:System.Windows.Interop.HwndHost.WndProc%2A> metody <xref:System.Windows.Interop.HwndHost> klasy.</span><span class="sxs-lookup"><span data-stu-id="7c422-134">If you prefer to handle messages in your hosting class, override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> method of the <xref:System.Windows.Interop.HwndHost> class.</span></span>  
  
    -   <span data-ttu-id="7c422-135">Jeśli chcesz mieć WPF obsługi komunikatów, obsługa <xref:System.Windows.Interop.HwndHost> klasy <xref:System.Windows.Interop.HwndHost.MessageHook> zdarzenia w swojej związanym z kodem.</span><span class="sxs-lookup"><span data-stu-id="7c422-135">If you prefer to have the WPF handle the messages, handle the <xref:System.Windows.Interop.HwndHost> class <xref:System.Windows.Interop.HwndHost.MessageHook> event in your code-behind.</span></span> <span data-ttu-id="7c422-136">To zdarzenie występuje dla każdego komunikatu odebranego przez okno hostowanej.</span><span class="sxs-lookup"><span data-stu-id="7c422-136">This event occurs for every message that is received by the hosted window.</span></span> <span data-ttu-id="7c422-137">Jeśli ta opcja jest wybrana, nadal musisz przesłonić <xref:System.Windows.Interop.HwndHost.WndProc%2A>, ale wystarczy tylko minimalne implementacji.</span><span class="sxs-lookup"><span data-stu-id="7c422-137">If you choose this option, you must still override <xref:System.Windows.Interop.HwndHost.WndProc%2A>, but you only need a minimal implementation.</span></span>  
  
7.  <span data-ttu-id="7c422-138">Zastąp <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> i <xref:System.Windows.Interop.HwndHost.WndProc%2A> metody <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="7c422-138">Override the <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> and <xref:System.Windows.Interop.HwndHost.WndProc%2A> methods of <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="7c422-139">Konieczne jest przesłonięcie tych metod w celu zaspokojenia <xref:System.Windows.Interop.HwndHost> kontraktu, ale może być tylko konieczne podanie minimalny implementacji.</span><span class="sxs-lookup"><span data-stu-id="7c422-139">You must override these methods to satisfy the <xref:System.Windows.Interop.HwndHost> contract, but you may only need to provide a minimal implementation.</span></span>  
  
8.  <span data-ttu-id="7c422-140">W pliku związanym z kodem, Utwórz wystąpienie obiektu hostingu klasy formantu, co element podrzędny elementu <xref:System.Windows.Controls.Border> element, który jest przeznaczony do obsługi oknie.</span><span class="sxs-lookup"><span data-stu-id="7c422-140">In your code-behind file, create an instance of the control hosting class and make it a child of the <xref:System.Windows.Controls.Border> element that is intended to host the window.</span></span>  
  
9. <span data-ttu-id="7c422-141">Komunikować się z oknie hostowanej przez wysłanie go [!INCLUDE[TLA#tla_win](../../../../includes/tlasharptla-win-md.md)] komunikatów i obsługi komunikatów z jego okien podrzędnych, takich jak powiadomienia wysyłane przez formanty.</span><span class="sxs-lookup"><span data-stu-id="7c422-141">Communicate with the hosted window by sending it [!INCLUDE[TLA#tla_win](../../../../includes/tlasharptla-win-md.md)] messages and handling messages from its child windows, such as notifications sent by controls.</span></span>  
  
<a name="page_layout"></a>   
## <a name="implement-the-page-layout"></a><span data-ttu-id="7c422-142">Implementowanie układu strony</span><span class="sxs-lookup"><span data-stu-id="7c422-142">Implement the Page Layout</span></span>  
 <span data-ttu-id="7c422-143">Układ strony WPF, który obsługuje kontrolki ListBox składa się z dwóch regionach.</span><span class="sxs-lookup"><span data-stu-id="7c422-143">The layout for the WPF page that hosts the ListBox Control consists of two regions.</span></span> <span data-ttu-id="7c422-144">Po lewej stronie strony znajduje się kilka kontrolek WPF, które zapewniają [!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)] pozwala manipulować formantu Win32.</span><span class="sxs-lookup"><span data-stu-id="7c422-144">The left side of the page hosts several WPF controls that provide a [!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)] that allows you to manipulate the Win32 control.</span></span> <span data-ttu-id="7c422-145">Prawy górny róg strony ma kwadratowy region hostowanej kontrolki ListBox.</span><span class="sxs-lookup"><span data-stu-id="7c422-145">The upper right corner of the page has a square region for the hosted ListBox Control.</span></span>  
  
 <span data-ttu-id="7c422-146">Kod implementujący ten układ jest bardzo proste.</span><span class="sxs-lookup"><span data-stu-id="7c422-146">The code to implement this layout is quite simple.</span></span> <span data-ttu-id="7c422-147">Element główny jest <xref:System.Windows.Controls.DockPanel> zawierający dwa typy elementów podrzędnych.</span><span class="sxs-lookup"><span data-stu-id="7c422-147">The root element is a <xref:System.Windows.Controls.DockPanel> that has two child elements.</span></span> <span data-ttu-id="7c422-148">Pierwsza to <xref:System.Windows.Controls.Border> elementu, który jest hostem kontrolki ListBox.</span><span class="sxs-lookup"><span data-stu-id="7c422-148">The first is a <xref:System.Windows.Controls.Border> element that hosts the ListBox Control.</span></span> <span data-ttu-id="7c422-149">Zajmuje 200 x 200 cali kwadratowy prawy górny róg strony.</span><span class="sxs-lookup"><span data-stu-id="7c422-149">It occupies a 200x200 square in the upper right corner of the page.</span></span> <span data-ttu-id="7c422-150">Drugi to <xref:System.Windows.Controls.StackPanel> element, który zawiera zestaw kontrolek WPF, wyświetlane informacje, które umożliwiają manipulowanie kontrolki ListBox, ustawiając udostępniane współdziałanie właściwości.</span><span class="sxs-lookup"><span data-stu-id="7c422-150">The second is a <xref:System.Windows.Controls.StackPanel> element that contains a set of WPF controls that display information and allow you to manipulate the ListBox Control by setting exposed interoperation properties.</span></span> <span data-ttu-id="7c422-151">Dla każdego z elementów, które są elementami podrzędnymi <xref:System.Windows.Controls.StackPanel>, zobacz materiały referencyjne dla różnych elementów, które są używane szczegółowe informacje na temat tych elementów są lub co zrobić, te są wymienione w poniższym przykładowym kodzie, ale nie są opisane w tym miejscu (podstawowa współdziałanie modelu nie wymaga żadnej z nich, są one udostępniane niektóre interakcyjności do przykładu).</span><span class="sxs-lookup"><span data-stu-id="7c422-151">For each of the elements that are children of the <xref:System.Windows.Controls.StackPanel>, see the reference material for the various elements used for details on what these elements are or what they do, these are listed in the example code below but will not be explained here (the basic interoperation model does not require any of them, they are provided to add some interactivity to the sample).</span></span>  
  
 [!code-xaml[WPFHostingWin32Control#WPFUI](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml#wpfui)]  
  
<a name="host_class"></a>   
## <a name="implement-a-class-to-host-the-microsoft-win32-control"></a><span data-ttu-id="7c422-152">Implementowanie klasy do hostowania kontrolki Microsoft Win32</span><span class="sxs-lookup"><span data-stu-id="7c422-152">Implement a Class to Host the Microsoft Win32 Control</span></span>  
 <span data-ttu-id="7c422-153">Podstawowe w tym przykładzie jest to klasa, która faktycznie obsługuje formant ControlHost.cs.</span><span class="sxs-lookup"><span data-stu-id="7c422-153">The core of this sample is the class that actually hosts the control, ControlHost.cs.</span></span> <span data-ttu-id="7c422-154">Dziedziczy <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="7c422-154">It inherits from <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="7c422-155">Konstruktor przyjmuje dwa parametry, wysokości i szerokości, które odnoszą się do wysokości i szerokości <xref:System.Windows.Controls.Border> elementu, który jest hostem kontrolki ListBox.</span><span class="sxs-lookup"><span data-stu-id="7c422-155">The constructor takes two parameters, height and width, which correspond to the height and width of the <xref:System.Windows.Controls.Border> element that hosts the ListBox control.</span></span> <span data-ttu-id="7c422-156">Wartości te są używane, później, upewnij się, że rozmiar dopasowania kontroli <xref:System.Windows.Controls.Border> elementu.</span><span class="sxs-lookup"><span data-stu-id="7c422-156">These values are used later to ensure that the size of the control matches the <xref:System.Windows.Controls.Border> element.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostclass)]
 [!code-vb[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostclass)]  
  
 <span data-ttu-id="7c422-157">Istnieje również zestaw stałych.</span><span class="sxs-lookup"><span data-stu-id="7c422-157">There is also a set of constants.</span></span> <span data-ttu-id="7c422-158">Te stałe dużej mierze są pobierane z Winuser.h i umożliwiają używanie konwencjonalnej nazwy podczas wywoływania funkcji Win32.</span><span class="sxs-lookup"><span data-stu-id="7c422-158">These constants are largely taken from Winuser.h, and allow you to use conventional names when calling Win32 functions.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostconstants)]
 [!code-vb[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostconstants)]  
  
<a name="buildwindowcore"></a>   
### <a name="override-buildwindowcore-to-create-the-microsoft-win32-window"></a><span data-ttu-id="7c422-159">Zastąp BuildWindowCore można utworzyć okna Microsoft Win32</span><span class="sxs-lookup"><span data-stu-id="7c422-159">Override BuildWindowCore to Create the Microsoft Win32 Window</span></span>  
 <span data-ttu-id="7c422-160">Możesz zastąpić tę metodę, aby utworzyć w oknie Win32, w którym będzie hostowana przez stronę i nawiązywanie połączenia między oknem i strony.</span><span class="sxs-lookup"><span data-stu-id="7c422-160">You override this method to create the Win32 window that will be hosted by the page, and make the connection between the window and the page.</span></span> <span data-ttu-id="7c422-161">Ponieważ ten przykład obejmuje hosting kontrolki ListBox, są tworzone dwa okna.</span><span class="sxs-lookup"><span data-stu-id="7c422-161">Because this sample involves hosting a ListBox Control, two windows are created.</span></span> <span data-ttu-id="7c422-162">Pierwsza to okno, które faktycznie jest hostowana przez stronę WPF.</span><span class="sxs-lookup"><span data-stu-id="7c422-162">The first is the window that is actually hosted by the WPF page.</span></span> <span data-ttu-id="7c422-163">ListBox — formant jest tworzony jako element podrzędny tego okna.</span><span class="sxs-lookup"><span data-stu-id="7c422-163">The ListBox Control is created as a child of that window.</span></span>  
  
 <span data-ttu-id="7c422-164">Przyczyną tego podejścia jest w celu uproszczenia procesu odbierania powiadomień w formancie.</span><span class="sxs-lookup"><span data-stu-id="7c422-164">The reason for this approach is to simplify the process of receiving notifications from the control.</span></span> <span data-ttu-id="7c422-165"><xref:System.Windows.Interop.HwndHost> Klasa służy do przetwarzania komunikatów wysłanych do okna, które obsługuje on.</span><span class="sxs-lookup"><span data-stu-id="7c422-165">The <xref:System.Windows.Interop.HwndHost> class allows you to process messages sent to the window that it is hosting.</span></span> <span data-ttu-id="7c422-166">Jeśli host systemu Win32 kontrolować bezpośrednio, zostanie wyświetlony komunikaty wysyłane do pętli komunikatów wewnętrznej kontrolki.</span><span class="sxs-lookup"><span data-stu-id="7c422-166">If you host a Win32 control directly, you receive the messages sent to the internal message loop of the control.</span></span> <span data-ttu-id="7c422-167">Możesz wyświetlić kontroli i wyślij go komunikaty, ale nie otrzymasz powiadomienia, które kontrolki wysyła do okna nadrzędnego.</span><span class="sxs-lookup"><span data-stu-id="7c422-167">You can display the control and send it messages, but you do not receive the notifications that the control sends to its parent window.</span></span> <span data-ttu-id="7c422-168">Oznacza to, między innymi, że nie ma możliwości wykrywania, gdy użytkownik wchodzi w interakcję z kontrolką.</span><span class="sxs-lookup"><span data-stu-id="7c422-168">This means, among other things, that you have no way of detecting when the user interacts with the control.</span></span> <span data-ttu-id="7c422-169">Zamiast tego należy utworzyć okno hosta i sprawić, że formant z elementem podrzędnym tego okna.</span><span class="sxs-lookup"><span data-stu-id="7c422-169">Instead, create a host window and make the control a child of that window.</span></span> <span data-ttu-id="7c422-170">Umożliwia to przetwarzanie wiadomości dla okna hosta, w tym powiadomień wysyłanych przez kontrolkę.</span><span class="sxs-lookup"><span data-stu-id="7c422-170">This allows you to process the messages for the host window including the notifications sent to it by the control.</span></span> <span data-ttu-id="7c422-171">Dla wygody ponieważ host jest nieco więcej niż prosty otoki dla formantu, pakiet zostanie określane jako kontrolce ListBox.</span><span class="sxs-lookup"><span data-stu-id="7c422-171">For convenience, since the host window is little more than a simple wrapper for the control, the package will be referred to as a ListBox control.</span></span>  
  
<a name="create_the_window_and_listbox"></a>   
#### <a name="create-the-host-window-and-listbox-control"></a><span data-ttu-id="7c422-172">Utwórz okno hosta i ListBox, kontrolka</span><span class="sxs-lookup"><span data-stu-id="7c422-172">Create the Host Window and ListBox Control</span></span>  
 <span data-ttu-id="7c422-173">Użyj funkcji PInvoke, aby utworzyć okna dla kontrolki hosta przez tworzenie i rejestrowanie klasy okna i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="7c422-173">You can use PInvoke to create a host window for the control by creating and registering a window class, and so on.</span></span> <span data-ttu-id="7c422-174">Jednak znacznie prostsze jest można utworzyć okna przy użyciu klasy wstępnie zdefiniowanych okna "statyczny".</span><span class="sxs-lookup"><span data-stu-id="7c422-174">However, a much simpler approach is to create a window with the predefined "static" window class.</span></span> <span data-ttu-id="7c422-175">Zawiera procedurę okna niezbędna, aby otrzymywać powiadomienia z formantu i wymaga minimalnego kodowania.</span><span class="sxs-lookup"><span data-stu-id="7c422-175">This provides you with the window procedure you need in order to receive notifications from the control, and requires minimal coding.</span></span>  
  
 <span data-ttu-id="7c422-176">HWND formantu jest dostępna za pośrednictwem właściwości tylko do odczytu, tak, aby strony hosta służy do wysyłania komunikatów do formantu.</span><span class="sxs-lookup"><span data-stu-id="7c422-176">The HWND of the control is exposed through a read-only property, such that the host page can use it to send messages to the control.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#intptrproperty)]
 [!code-vb[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#intptrproperty)]  
  
 <span data-ttu-id="7c422-177">ListBox — formant jest tworzony jako element podrzędny okno hosta.</span><span class="sxs-lookup"><span data-stu-id="7c422-177">The ListBox control is created as a child of the host window.</span></span> <span data-ttu-id="7c422-178">Wysokość i szerokość oba okna są ustawione na wartości przekazane do konstruktora omówione powyżej.</span><span class="sxs-lookup"><span data-stu-id="7c422-178">The height and width of both windows are set to the values passed to the constructor, discussed above.</span></span> <span data-ttu-id="7c422-179">Dzięki temu, rozmiar okna hosta i kontroli są identyczne z zarezerwowanym obszarem, na stronie.</span><span class="sxs-lookup"><span data-stu-id="7c422-179">This ensures that the size of the host window and control is identical to the reserved area on the page.</span></span>  <span data-ttu-id="7c422-180">Po utworzeniu systemu windows, zwraca próbkę <xref:System.Runtime.InteropServices.HandleRef> obiekt, który zawiera HWND okna hosta.</span><span class="sxs-lookup"><span data-stu-id="7c422-180">After the windows are created, the sample returns a <xref:System.Runtime.InteropServices.HandleRef> object that contains the HWND of the host window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcore)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcore)]  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcorehelper)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcorehelper)]  
  
<a name="destroywindow_wndproc"></a>   
### <a name="implement-destroywindow-and-wndproc"></a><span data-ttu-id="7c422-181">Destroywindow — Implementowanie i WndProc</span><span class="sxs-lookup"><span data-stu-id="7c422-181">Implement DestroyWindow and WndProc</span></span>  
 <span data-ttu-id="7c422-182">Oprócz <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>, konieczne jest również przesłonięcie <xref:System.Windows.Interop.HwndHost.WndProc%2A> i <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> metody <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="7c422-182">In addition to <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>, you must also override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> methods of the <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="7c422-183">W tym przykładzie wiadomości dla formantu są obsługiwane przez <xref:System.Windows.Interop.HwndHost.MessageHook> obsługi, dlatego implementacja <xref:System.Windows.Interop.HwndHost.WndProc%2A> i <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> jest minimalny.</span><span class="sxs-lookup"><span data-stu-id="7c422-183">In this example, the messages for the control are handled by the <xref:System.Windows.Interop.HwndHost.MessageHook> handler, thus the implementation of <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> is minimal.</span></span> <span data-ttu-id="7c422-184">W przypadku właściwości <xref:System.Windows.Interop.HwndHost.WndProc%2A>ustaw `handled` do `false` wskazują, że komunikat nie został obsłużony i zwracają 0.</span><span class="sxs-lookup"><span data-stu-id="7c422-184">In the case of <xref:System.Windows.Interop.HwndHost.WndProc%2A>, set `handled` to `false` to indicate that the  message was not handled and return 0.</span></span> <span data-ttu-id="7c422-185">Aby uzyskać <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>, po prostu zniszczyć okna.</span><span class="sxs-lookup"><span data-stu-id="7c422-185">For <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>, simply destroy the window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroy)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroy)]  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroyhelper)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroyhelper)]  
  
<a name="host_the_control"></a>   
## <a name="host-the-control-on-the-page"></a><span data-ttu-id="7c422-186">Kontrolki na stronie hosta</span><span class="sxs-lookup"><span data-stu-id="7c422-186">Host the Control on the Page</span></span>  
 <span data-ttu-id="7c422-187">Aby obsługiwać formantu na stronie, należy najpierw utworzyć nowe wystąpienie klasy `ControlHost` klasy.</span><span class="sxs-lookup"><span data-stu-id="7c422-187">To host the control on the page, you first create a new instance of the `ControlHost` class.</span></span> <span data-ttu-id="7c422-188">Przekaż wysokość i szerokość elementu obramowania, który zawiera kontrolkę (`ControlHostElement`) do `ControlHost` konstruktora.</span><span class="sxs-lookup"><span data-stu-id="7c422-188">Pass the height and width of the border element that contains the control (`ControlHostElement`) to the `ControlHost` constructor.</span></span> <span data-ttu-id="7c422-189">Daje to gwarancję, że pole listy jest mały rozmiar.</span><span class="sxs-lookup"><span data-stu-id="7c422-189">This ensures that the ListBox is sized correctly.</span></span> <span data-ttu-id="7c422-190">Następnie Hostuj formantu na stronie, przypisując `ControlHost` obiekt <xref:System.Windows.Controls.Decorator.Child%2A> właściwości hosta <xref:System.Windows.Controls.Border>.</span><span class="sxs-lookup"><span data-stu-id="7c422-190">You then host the control on the page by assigning the `ControlHost` object to the <xref:System.Windows.Controls.Decorator.Child%2A> property of the host <xref:System.Windows.Controls.Border>.</span></span>  
  
 <span data-ttu-id="7c422-191">Przykład dołącza obsługę do <xref:System.Windows.Interop.HwndHost.MessageHook> zdarzenia `ControlHost` do odbierania komunikatów z formantu.</span><span class="sxs-lookup"><span data-stu-id="7c422-191">The sample attaches a handler to the <xref:System.Windows.Interop.HwndHost.MessageHook> event of the `ControlHost` to receive messages from the control.</span></span> <span data-ttu-id="7c422-192">To zdarzenie jest wywoływane dla każdej wiadomości wysyłane do okna hostowanej.</span><span class="sxs-lookup"><span data-stu-id="7c422-192">This event is raised for every message sent to the hosted window.</span></span> <span data-ttu-id="7c422-193">W tym przypadku są to komunikaty wysyłane do okna, który otacza rzeczywistego formantu ListBox, łącznie z powiadomień z poziomu kontroli.</span><span class="sxs-lookup"><span data-stu-id="7c422-193">In this case, these are the messages sent to window that wraps the actual ListBox control, including notifications from the control.</span></span> <span data-ttu-id="7c422-194">Przykład wywołuje SendMessage do uzyskania informacji z formantu i modyfikowania jego zawartości.</span><span class="sxs-lookup"><span data-stu-id="7c422-194">The sample calls SendMessage to obtain information from the control and modify its contents.</span></span> <span data-ttu-id="7c422-195">Szczegółowe informacje o jak strona komunikuje się za pomocą kontrolki są omówione w następnej sekcji.</span><span class="sxs-lookup"><span data-stu-id="7c422-195">The details of how the page communicates with the control are discussed in the next section.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="7c422-196">Należy zauważyć, że nie istnieją dwie deklaracje funkcji PInvoke dla SendMessage.</span><span class="sxs-lookup"><span data-stu-id="7c422-196">Notice that there are two PInvoke declarations for SendMessage.</span></span> <span data-ttu-id="7c422-197">Jest to konieczne, ponieważ korzysta z jednego `wParam` parametr do przekazania ciąg, a druga używa go w celu przekazania liczbą całkowitą.</span><span class="sxs-lookup"><span data-stu-id="7c422-197">This is necessary because one uses the `wParam` parameter to pass a string and the other uses it to pass an integer.</span></span> <span data-ttu-id="7c422-198">Potrzebujesz oddzielnych deklarację dla każdego podpisu upewnić się, że danych jest organizowana poprawnie.</span><span class="sxs-lookup"><span data-stu-id="7c422-198">You need a separate declaration for each signature to ensure that the data is marshaled correctly.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#hostwindowclass)]
 [!code-vb[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#hostwindowclass)]  
  
 [!code-csharp[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#controlmsgfiltersendmessage)]
 [!code-vb[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#controlmsgfiltersendmessage)]  
  
<a name="communication"></a>   
## <a name="implement-communication-between-the-control-and-the-page"></a><span data-ttu-id="7c422-199">Implementowanie komunikacji między formantem i strony</span><span class="sxs-lookup"><span data-stu-id="7c422-199">Implement Communication Between the Control and the Page</span></span>  
 <span data-ttu-id="7c422-200">Kontrolki można manipulować wysyłając komunikaty Windows.</span><span class="sxs-lookup"><span data-stu-id="7c422-200">You manipulate the control by sending it Windows messages.</span></span> <span data-ttu-id="7c422-201">Kontrolki powiadamia, gdy użytkownik wchodzi w interakcję z nią przez wysłanie powiadomienia do okna hosta.</span><span class="sxs-lookup"><span data-stu-id="7c422-201">The control notifies you when the user interacts with it by sending notifications to its host window.</span></span> <span data-ttu-id="7c422-202">[Hosting kontrolki ListBox Win32 w WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control) przykład obejmuje interfejs użytkownika, który zawiera kilka przykładów jak to działa:</span><span class="sxs-lookup"><span data-stu-id="7c422-202">The [Hosting a Win32 ListBox Control in WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control) sample includes a UI that provides several examples of how this works:</span></span>  
  
-   <span data-ttu-id="7c422-203">Dołącz element do listy.</span><span class="sxs-lookup"><span data-stu-id="7c422-203">Append an item to the list.</span></span>  
  
-   <span data-ttu-id="7c422-204">Usuń wybrany element z listy</span><span class="sxs-lookup"><span data-stu-id="7c422-204">Delete the selected item from the list</span></span>  
  
-   <span data-ttu-id="7c422-205">Wyświetl tekst aktualnie wybranego elementu.</span><span class="sxs-lookup"><span data-stu-id="7c422-205">Display the text of the currently selected item.</span></span>  
  
-   <span data-ttu-id="7c422-206">Wyświetl liczbę elementów na liście.</span><span class="sxs-lookup"><span data-stu-id="7c422-206">Display the number of items in the list.</span></span>  
  
 <span data-ttu-id="7c422-207">Użytkownik może również wybrać element w polu listy, klikając ją, tak samo, jak w przypadku konwencjonalnych aplikacji Win32.</span><span class="sxs-lookup"><span data-stu-id="7c422-207">The user can also select an item in the list box by clicking on it, just as they would for a conventional Win32 application.</span></span> <span data-ttu-id="7c422-208">Dane wyświetlane aktualizowane wówczas, gdy użytkownik zmieni stan pola listy, wybierając, dodając lub dołączanie elementu.</span><span class="sxs-lookup"><span data-stu-id="7c422-208">The displayed data is updated each time the user changes the state of the list box by selecting, adding, or appending an item.</span></span>  
  
 <span data-ttu-id="7c422-209">Aby dołączyć elementy, Wyślij pole listy [ `LB_ADDSTRING` komunikat](/windows/desktop/Controls/lb-addstring).</span><span class="sxs-lookup"><span data-stu-id="7c422-209">To append items, send the list box an [`LB_ADDSTRING` message](/windows/desktop/Controls/lb-addstring).</span></span> <span data-ttu-id="7c422-210">Aby usunąć elementy, Wyślij [ `LB_GETCURSEL` ](/windows/desktop/Controls/lb-getcursel) uzyskać indeks bieżącego zaznaczenia i następnie [ `LB_DELETESTRING` ](/windows/desktop/Controls/lb-deletestring) można usunąć elementu.</span><span class="sxs-lookup"><span data-stu-id="7c422-210">To delete items, send [`LB_GETCURSEL`](/windows/desktop/Controls/lb-getcursel) to get the index of the current selection and then [`LB_DELETESTRING`](/windows/desktop/Controls/lb-deletestring) to delete the item.</span></span> <span data-ttu-id="7c422-211">Przykład wysyła również [ `LB_GETCOUNT` ](/windows/desktop/Controls/lb-getcount)i używa zwracanej wartości, aby zaktualizować ekran, który pokazuje liczbę elementów.</span><span class="sxs-lookup"><span data-stu-id="7c422-211">The sample also sends [`LB_GETCOUNT`](/windows/desktop/Controls/lb-getcount), and uses the returned value to update the display that shows the number of items.</span></span> <span data-ttu-id="7c422-212">Oba te wystąpienia [ `SendMessage` ](/windows/desktop/api/winuser/nf-winuser-sendmessage) użyj jednej z deklaracjami funkcji PInvoke opisanych w poprzedniej sekcji.</span><span class="sxs-lookup"><span data-stu-id="7c422-212">Both these instances of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) use one of the PInvoke declarations discussed in the previous section.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#appenddeletetext)]
 [!code-vb[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#appenddeletetext)]  
  
 <span data-ttu-id="7c422-213">Gdy użytkownik wybierze element lub zmieni się ich wyboru, formantu powiadamia okno hosta przez wysłanie go [ `WM_COMMAND` komunikat](/windows/desktop/menurc/wm-command), co powoduje zdarzenie <xref:System.Windows.Interop.HwndHost.MessageHook> zdarzeń dla strony.</span><span class="sxs-lookup"><span data-stu-id="7c422-213">When the user selects an item or changes their selection, the control notifies the host window by sending it a [`WM_COMMAND` message](/windows/desktop/menurc/wm-command), which raises the <xref:System.Windows.Interop.HwndHost.MessageHook> event for the page.</span></span> <span data-ttu-id="7c422-214">Program obsługi otrzymuje te same informacje jak procedura okno główne okno hosta.</span><span class="sxs-lookup"><span data-stu-id="7c422-214">The handler receives the same information as the main window procedure of the host window.</span></span> <span data-ttu-id="7c422-215">Przekazuje odwołanie na wartość logiczną `handled`.</span><span class="sxs-lookup"><span data-stu-id="7c422-215">It also passes a reference to a Boolean value, `handled`.</span></span> <span data-ttu-id="7c422-216">Możesz ustawić `handled` do `true` oznacza, że zapewnienia obsługi wiadomości i żadne dalsze przetwarzanie nie jest wymagana.</span><span class="sxs-lookup"><span data-stu-id="7c422-216">You set `handled` to `true` to indicate that you have handled the message and no further processing is needed.</span></span>  
  
 <span data-ttu-id="7c422-217">[`WM_COMMAND`](/windows/desktop/menurc/wm-command) są wysyłane z różnych powodów, więc należy zbadać identyfikator powiadomień do określenia, czy to zdarzenie, którą chcesz obsługiwać.</span><span class="sxs-lookup"><span data-stu-id="7c422-217">[`WM_COMMAND`](/windows/desktop/menurc/wm-command) is sent for a variety of reasons, so you must examine the notification ID to determine whether it is an event that you wish to handle.</span></span> <span data-ttu-id="7c422-218">Identyfikator znajduje się w wyższe słowo `wParam` parametru.</span><span class="sxs-lookup"><span data-stu-id="7c422-218">The ID is contained in the high word of the `wParam` parameter.</span></span> <span data-ttu-id="7c422-219">W przykładzie użyto operatory bitowe można wyodrębnić identyfikatora.</span><span class="sxs-lookup"><span data-stu-id="7c422-219">The sample uses bitwise operators to extract the ID.</span></span> <span data-ttu-id="7c422-220">Jeśli wprowadzone przez użytkownika lub zmianie ich wyboru, będzie identyfikator [ `LBN_SELCHANGE` ](/windows/desktop/Controls/lbn-selchange).</span><span class="sxs-lookup"><span data-stu-id="7c422-220">If the user has made or changed their selection, the ID will be [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange).</span></span>  
  
 <span data-ttu-id="7c422-221">Gdy [ `LBN_SELCHANGE` ](/windows/desktop/Controls/lbn-selchange) jest odbierane, przykładowy kod pobiera indeks zaznaczonego elementu, wysyłając kontrolki [ `LB_GETCURSEL` komunikat](/windows/desktop/Controls/lb-getcursel).</span><span class="sxs-lookup"><span data-stu-id="7c422-221">When [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange) is received, the sample gets the index of the selected item by sending the control a [`LB_GETCURSEL` message](/windows/desktop/Controls/lb-getcursel).</span></span> <span data-ttu-id="7c422-222">Aby uzyskać tekst, należy najpierw utworzyć <xref:System.Text.StringBuilder>.</span><span class="sxs-lookup"><span data-stu-id="7c422-222">To get the text, you first create a <xref:System.Text.StringBuilder>.</span></span> <span data-ttu-id="7c422-223">Następnie wyślij kontrolki [ `LB_GETTEXT` komunikat](/windows/desktop/Controls/lb-gettext).</span><span class="sxs-lookup"><span data-stu-id="7c422-223">You then send the control an [`LB_GETTEXT` message](/windows/desktop/Controls/lb-gettext).</span></span> <span data-ttu-id="7c422-224">Przekaż pustą <xref:System.Text.StringBuilder> obiektu jako `wParam` parametru.</span><span class="sxs-lookup"><span data-stu-id="7c422-224">Pass the empty <xref:System.Text.StringBuilder> object as the `wParam` parameter.</span></span> <span data-ttu-id="7c422-225">Gdy [ `SendMessage` ](/windows/desktop/api/winuser/nf-winuser-sendmessage) zwraca, <xref:System.Text.StringBuilder> będzie zawierać tekst wybranego elementu.</span><span class="sxs-lookup"><span data-stu-id="7c422-225">When [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) returns, the <xref:System.Text.StringBuilder> will contain the text of the selected item.</span></span> <span data-ttu-id="7c422-226">Użycie tego [ `SendMessage` ](/windows/desktop/api/winuser/nf-winuser-sendmessage) wymaga kolejny deklaracji funkcji PInvoke.</span><span class="sxs-lookup"><span data-stu-id="7c422-226">This use of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) requires yet another PInvoke declaration.</span></span>  
  
 <span data-ttu-id="7c422-227">Wreszcie, ustaw `handled` do `true` do wskazania obsługiwania wiadomości.</span><span class="sxs-lookup"><span data-stu-id="7c422-227">Finally, set `handled` to `true` to indicate that the message has been handled.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="7c422-228">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="7c422-228">See also</span></span>

- <xref:System.Windows.Interop.HwndHost>
- [<span data-ttu-id="7c422-229">WPF i Win32 — Współdziałanie</span><span class="sxs-lookup"><span data-stu-id="7c422-229">WPF and Win32 Interoperation</span></span>](wpf-and-win32-interoperation.md)
- [<span data-ttu-id="7c422-230">Przewodnik: Moja pierwsza aplikacja klasyczna WPF</span><span class="sxs-lookup"><span data-stu-id="7c422-230">Walkthrough: My first WPF desktop application</span></span>](../getting-started/walkthrough-my-first-wpf-desktop-application.md)
