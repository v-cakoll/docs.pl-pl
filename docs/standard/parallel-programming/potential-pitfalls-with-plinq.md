---
title: Potencjalne pułapki związane z PLINQ
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- PLINQ queries, pitfalls
ms.assetid: 75a38b55-4bc4-488a-87d5-89dbdbdc76a2
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 9c4decd01938500fe6330c48caa33b845916aaff
ms.sourcegitcommit: a885cc8c3e444ca6471348893d5373c6e9e49a47
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/06/2018
ms.locfileid: "43863010"
---
# <a name="potential-pitfalls-with-plinq"></a><span data-ttu-id="c2d94-102">Potencjalne pułapki związane z PLINQ</span><span class="sxs-lookup"><span data-stu-id="c2d94-102">Potential Pitfalls with PLINQ</span></span>
<span data-ttu-id="c2d94-103">W wielu przypadkach PLINQ może zapewnić znaczne ulepszenia wydajności za pośrednictwem sekwencyjnego LINQ do kwerendy obiekty.</span><span class="sxs-lookup"><span data-stu-id="c2d94-103">In many cases, PLINQ can provide significant performance improvements over sequential LINQ to Objects queries.</span></span> <span data-ttu-id="c2d94-104">Jednak pracy przekształcają wykonywania zapytania wprowadza złożoności, który może prowadzić do problemów, które sekwencyjnego kodu nie są jako wspólne lub nie zostaną napotkane w ogóle.</span><span class="sxs-lookup"><span data-stu-id="c2d94-104">However, the work of parallelizing the query execution introduces complexity that can lead to problems that, in sequential code, are not as common or are not encountered at all.</span></span> <span data-ttu-id="c2d94-105">W tym temacie wymieniono niektóre rozwiązania, aby uniknąć podczas pisania zapytań PLINQ.</span><span class="sxs-lookup"><span data-stu-id="c2d94-105">This topic lists some practices to avoid when you write PLINQ queries.</span></span>  
  
## <a name="do-not-assume-that-parallel-is-always-faster"></a><span data-ttu-id="c2d94-106">Nie należy zakładać, że równoległa jest zawsze szybciej</span><span class="sxs-lookup"><span data-stu-id="c2d94-106">Do Not Assume That Parallel Is Always Faster</span></span>  
 <span data-ttu-id="c2d94-107">Czasami przetwarzanie równoległe powoduje, że zapytanie PLINQ działać wolniej niż jego LINQ do obiekty równoważne.</span><span class="sxs-lookup"><span data-stu-id="c2d94-107">Parallelization sometimes causes a PLINQ query to run slower than its LINQ to Objects equivalent.</span></span> <span data-ttu-id="c2d94-108">Podstawowa zasada mówi to kwerend, które mają kilka elementów źródła i delegatów użytkownika szybkie, prawdopodobnie nie będzie przyspieszenie znacznie.</span><span class="sxs-lookup"><span data-stu-id="c2d94-108">The basic rule of thumb is that queries that have few source elements and fast user delegates are unlikely to speedup much.</span></span> <span data-ttu-id="c2d94-109">Ponieważ wiele czynników są zaangażowane w wydajności, firma Microsoft zaleca jednak pomiaru rzeczywiste wyniki, przed podjęciem decyzji o tym, czy ma być używany program PLINQ.</span><span class="sxs-lookup"><span data-stu-id="c2d94-109">However, because many factors are involved in performance, we recommend that you measure actual results before you decide whether to use PLINQ.</span></span> <span data-ttu-id="c2d94-110">Aby uzyskać więcej informacji, zobacz [ogólne informacje o przyspieszeniach w PLINQ](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="c2d94-110">For more information, see [Understanding Speedup in PLINQ](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md).</span></span>  
  
## <a name="avoid-writing-to-shared-memory-locations"></a><span data-ttu-id="c2d94-111">Należy unikać pisania do lokalizacji udostępnionej pamięci</span><span class="sxs-lookup"><span data-stu-id="c2d94-111">Avoid Writing to Shared Memory Locations</span></span>  
 <span data-ttu-id="c2d94-112">W kodzie sekwencyjnych nie jest niczym niezwykłym, aby odczytać lub zapisać zmienne statyczne lub pola klasy.</span><span class="sxs-lookup"><span data-stu-id="c2d94-112">In sequential code, it is not uncommon to read from or write to static variables or class fields.</span></span> <span data-ttu-id="c2d94-113">Zawsze, gdy wiele wątków uzyskują dostęp do takich zmiennych jednocześnie, istnieje jednak duże prawdopodobieństwo jednoczesnego wyścigu.</span><span class="sxs-lookup"><span data-stu-id="c2d94-113">However, whenever multiple threads are accessing such variables concurrently, there is a big potential for race conditions.</span></span> <span data-ttu-id="c2d94-114">Mimo że blokad służy do synchronizowania dostępu do zmiennej, koszt synchronizacji może obniżyć wydajność.</span><span class="sxs-lookup"><span data-stu-id="c2d94-114">Even though you can use locks to synchronize access to the variable, the cost of synchronization can hurt performance.</span></span> <span data-ttu-id="c2d94-115">Dlatego zaleca się że uniknąć, lub co najmniej ograniczyć, dostęp do udostępnionego stanu w możliwie zapytanie PLINQ.</span><span class="sxs-lookup"><span data-stu-id="c2d94-115">Therefore, we recommend that you avoid, or at least limit, access to shared state in a PLINQ query as much as possible.</span></span>  
  
## <a name="avoid-over-parallelization"></a><span data-ttu-id="c2d94-116">Unikaj nadmiernego przetwarzania równoległego</span><span class="sxs-lookup"><span data-stu-id="c2d94-116">Avoid Over-Parallelization</span></span>  
 <span data-ttu-id="c2d94-117">Za pomocą `AsParallel` operatora ponosić kosztów ogólnych partycjonowania kolekcji źródłowej i synchronizacji wątków roboczych.</span><span class="sxs-lookup"><span data-stu-id="c2d94-117">By using the `AsParallel` operator, you incur the overhead costs of partitioning the source collection and synchronizing the worker threads.</span></span> <span data-ttu-id="c2d94-118">Korzyści wynikające z przetwarzaniem równoległym dalsze są ograniczone przez liczbę procesorów w komputerze.</span><span class="sxs-lookup"><span data-stu-id="c2d94-118">The benefits of parallelization are further limited by the number of processors on the computer.</span></span> <span data-ttu-id="c2d94-119">Nie ma żadnych przyspieszenie, które można uzyskać, uruchamiając wiele wątków powiązany obliczeniowych w tylko jednym procesorze.</span><span class="sxs-lookup"><span data-stu-id="c2d94-119">There is no speedup to be gained by running multiple compute-bound threads on just one processor.</span></span> <span data-ttu-id="c2d94-120">W związku z tym należy uważać, aby nie nadmiernie równoległe przetwarzanie zapytania.</span><span class="sxs-lookup"><span data-stu-id="c2d94-120">Therefore, you must be careful not to over-parallelize a query.</span></span>  
  
 <span data-ttu-id="c2d94-121">Najbardziej typowym scenariuszem, w których nadmiernego przetwarzania równoległego może wystąpić trwa zapytań zagnieżdżonej, jak pokazano w poniższym fragmencie kodu.</span><span class="sxs-lookup"><span data-stu-id="c2d94-121">The most common scenario in which over-parallelization can occur is in nested queries, as shown in the following snippet.</span></span>  
  
 [!code-csharp[PLINQ#20](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinqsamples.cs#20)]
 [!code-vb[PLINQ#20](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#20)]  
  
 <span data-ttu-id="c2d94-122">W takim przypadku najlepiej zrównoleglić tylko zewnętrznym źródle danych (klientów), chyba że zastosować co najmniej jeden z następujących warunków:</span><span class="sxs-lookup"><span data-stu-id="c2d94-122">In this case, it is best to parallelize only the outer data source (customers) unless one or more of the following conditions apply:</span></span>  
  
-   <span data-ttu-id="c2d94-123">Źródło danych wewnętrzny (odbiorcy. Zamówienia) jest uznany za długa.</span><span class="sxs-lookup"><span data-stu-id="c2d94-123">The inner data source (cust.Orders) is known to be very long.</span></span>  
  
-   <span data-ttu-id="c2d94-124">Kosztowne obliczenia są wykonywane na każde zamówienie.</span><span class="sxs-lookup"><span data-stu-id="c2d94-124">You are performing an expensive computation on each order.</span></span> <span data-ttu-id="c2d94-125">(Operacja pokazano w przykładzie nie jest kosztowna).</span><span class="sxs-lookup"><span data-stu-id="c2d94-125">(The operation shown in the example is not expensive.)</span></span>  
  
-   <span data-ttu-id="c2d94-126">System docelowy jest znana wystarczającej liczby procesorów, którą należy obsługiwać liczbę wątków, które zostaną wygenerowane przez zrównoleglić zapytanie na `cust.Orders`.</span><span class="sxs-lookup"><span data-stu-id="c2d94-126">The target system is known to have enough processors to handle the number of threads that will be produced by parallelizing the query on `cust.Orders`.</span></span>  
  
 <span data-ttu-id="c2d94-127">We wszystkich przypadkach najlepszym sposobem określenia kształtu optymalne zapytania jest do testowania i miar.</span><span class="sxs-lookup"><span data-stu-id="c2d94-127">In all cases, the best way to determine the optimum query shape is to test and measure.</span></span> <span data-ttu-id="c2d94-128">Aby uzyskać więcej informacji, zobacz [jak: wydajności zapytań PLINQ miary](../../../docs/standard/parallel-programming/how-to-measure-plinq-query-performance.md).</span><span class="sxs-lookup"><span data-stu-id="c2d94-128">For more information, see [How to: Measure PLINQ Query Performance](../../../docs/standard/parallel-programming/how-to-measure-plinq-query-performance.md).</span></span>  
  
## <a name="avoid-calls-to-non-thread-safe-methods"></a><span data-ttu-id="c2d94-129">Należy unikać wywołania metod innych niż wątkowo</span><span class="sxs-lookup"><span data-stu-id="c2d94-129">Avoid Calls to Non-Thread-Safe Methods</span></span>  
 <span data-ttu-id="c2d94-130">Zapisywanie metod wystąpienia innych wątkowo z PLINQ zapytania może prowadzić do uszkodzenia danych, które mogą lub nie może być wykryte w programach.</span><span class="sxs-lookup"><span data-stu-id="c2d94-130">Writing to non-thread-safe instance methods from a PLINQ query can lead to data corruption which may or may not go undetected in your program.</span></span> <span data-ttu-id="c2d94-131">Może to również prowadzić do wyjątków.</span><span class="sxs-lookup"><span data-stu-id="c2d94-131">It can also lead to exceptions.</span></span> <span data-ttu-id="c2d94-132">W poniższym przykładzie wielu wątków będzie próba wywołania `Filestream.Write` metoda równocześnie, które nie jest obsługiwane przez klasę.</span><span class="sxs-lookup"><span data-stu-id="c2d94-132">In the following example, multiple threads would be attempting to call the `Filestream.Write` method simultaneously, which is not supported by the class.</span></span>  
  
```vb  
Dim fs As FileStream = File.OpenWrite(…)  
a.Where(...).OrderBy(...).Select(...).ForAll(Sub(x) fs.Write(x))  
```  
  
```csharp  
FileStream fs = File.OpenWrite(...);  
a.Where(...).OrderBy(...).Select(...).ForAll(x => fs.Write(x));  
```  
  
## <a name="limit-calls-to-thread-safe-methods"></a><span data-ttu-id="c2d94-133">Ogranicz wywołania metod metodą o bezpiecznych wątkach</span><span class="sxs-lookup"><span data-stu-id="c2d94-133">Limit Calls to Thread-Safe Methods</span></span>  
 <span data-ttu-id="c2d94-134">Najbardziej statycznych metod .NET Framework są odporne na wątki i może być wywoływana z wielu wątków jednocześnie.</span><span class="sxs-lookup"><span data-stu-id="c2d94-134">Most static methods in the .NET Framework are thread-safe and can be called from multiple threads concurrently.</span></span> <span data-ttu-id="c2d94-135">Jednak nawet w takich przypadkach zaangażowane synchronizacji może prowadzić do znaczne spowolnienie w zapytaniu.</span><span class="sxs-lookup"><span data-stu-id="c2d94-135">However, even in these cases, the synchronization involved can lead to significant slowdown in the query.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="c2d94-136">Możesz sprawdzić to samodzielnie, wstawiając wywołania niektórych <xref:System.Console.WriteLine%2A> w zapytaniach.</span><span class="sxs-lookup"><span data-stu-id="c2d94-136">You can test for this yourself by inserting some calls to <xref:System.Console.WriteLine%2A> in your queries.</span></span> <span data-ttu-id="c2d94-137">Chociaż ta metoda jest używana w przykładach dokumentacji w celach demonstracyjnych, nie należy używać go w zapytania PLINQ.</span><span class="sxs-lookup"><span data-stu-id="c2d94-137">Although this method is used in the documentation examples for demonstration purposes, do not use it in PLINQ queries.</span></span>  
  
## <a name="avoid-unnecessary-ordering-operations"></a><span data-ttu-id="c2d94-138">Unikaj niepotrzebnych operacji porządkowanie</span><span class="sxs-lookup"><span data-stu-id="c2d94-138">Avoid Unnecessary Ordering Operations</span></span>  
 <span data-ttu-id="c2d94-139">Gdy PLINQ jest wykonywane zapytania równolegle, dzieli sekwencję źródłową na partycje, które mogą być stosowane jednocześnie w wielu wątkach.</span><span class="sxs-lookup"><span data-stu-id="c2d94-139">When PLINQ executes a query in parallel, it divides the source sequence into partitions that can be operated on concurrently on multiple threads.</span></span> <span data-ttu-id="c2d94-140">Domyślnie nie jest przewidywalne kolejności, w którym partycje są przetwarzane, a wyniki są dostarczane (z wyjątkiem operatorów, takich jak `OrderBy`).</span><span class="sxs-lookup"><span data-stu-id="c2d94-140">By default, the order in which the partitions are processed and the results are delivered is not predictable (except for operators such as `OrderBy`).</span></span> <span data-ttu-id="c2d94-141">Możesz nakazać PLINQ dla zachowania kolejności sekwencji dowolnego źródła, ale ma negatywny wpływ na wydajność.</span><span class="sxs-lookup"><span data-stu-id="c2d94-141">You can instruct PLINQ to preserve the ordering of any source sequence, but this has a negative impact on performance.</span></span> <span data-ttu-id="c2d94-142">Najlepszym rozwiązaniem jest zawsze, gdy jest to możliwe, jest struktura zapytania, aby nie należy polegać na porządku.</span><span class="sxs-lookup"><span data-stu-id="c2d94-142">The best practice, whenever possible, is to structure queries so that they do not rely on order preservation.</span></span> <span data-ttu-id="c2d94-143">Aby uzyskać więcej informacji, zobacz [zamawianie zachowywania w PLINQ](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="c2d94-143">For more information, see [Order Preservation in PLINQ](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span></span>  
  
## <a name="prefer-forall-to-foreach-when-it-is-possible"></a><span data-ttu-id="c2d94-144">Wolisz ForAll ForEach, gdy jest to możliwe</span><span class="sxs-lookup"><span data-stu-id="c2d94-144">Prefer ForAll to ForEach When It Is Possible</span></span>  
 <span data-ttu-id="c2d94-145">Mimo że PLINQ wykonuje zapytanie w wielu wątkach, jeśli wykorzystasz wyniki w `foreach` pętli (`For Each` w języku Visual Basic), a następnie wyniki zapytania musi być scalana z powrotem do jednego wątku i szeregowo uzyskiwał dostęp do modułu wyliczającego.</span><span class="sxs-lookup"><span data-stu-id="c2d94-145">Although PLINQ executes a query on multiple threads, if you consume the results in a `foreach` loop (`For Each` in Visual Basic), then the query results must be merged back into one thread and accessed serially by the enumerator.</span></span> <span data-ttu-id="c2d94-146">W niektórych przypadkach jest nieuniknione; Jednak w miarę możliwości należy użyć `ForAll` metodę umożliwiającą włączenie każdy wątek w danych wyjściowych swój własny wyników, na przykład, przez napisanie kolekcji metodą o bezpiecznych wątkach, takich jak <xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="c2d94-146">In some cases, this is unavoidable; however, whenever possible, use the `ForAll` method to enable each thread to output its own results, for example, by writing to a thread-safe collection such as <xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="c2d94-147">Ten sam problem dotyczy <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> innymi słowy, `source.AsParallel().Where().ForAll(...)` powinny być silnie preferowany dla wiadomości</span><span class="sxs-lookup"><span data-stu-id="c2d94-147">The same issue applies to <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> In other words, `source.AsParallel().Where().ForAll(...)` should be strongly preferred to</span></span>  
  
 <span data-ttu-id="c2d94-148">`Parallel.ForEach(source.AsParallel().Where(), ...)`.</span><span class="sxs-lookup"><span data-stu-id="c2d94-148">`Parallel.ForEach(source.AsParallel().Where(), ...)`.</span></span>  
  
## <a name="be-aware-of-thread-affinity-issues"></a><span data-ttu-id="c2d94-149">Należy pamiętać o problemów koligacji wątku</span><span class="sxs-lookup"><span data-stu-id="c2d94-149">Be Aware of Thread Affinity Issues</span></span>  
 <span data-ttu-id="c2d94-150">Niektóre technologie, na przykład współdziałanie COM dla składników Single-Threaded apartamentu (STA), Windows Forms i Windows Presentation Foundation (WPF), nakładają ograniczenia koligacji wątku, które wymagają kod wymagany do uruchomienia w określonym wątku.</span><span class="sxs-lookup"><span data-stu-id="c2d94-150">Some technologies, for example, COM interoperability for Single-Threaded Apartment (STA) components, Windows Forms, and Windows Presentation Foundation (WPF), impose thread affinity restrictions that require code to run on a specific thread.</span></span> <span data-ttu-id="c2d94-151">Na przykład w Windows Forms i WPF formant może zostać oceniony jedynie w wątku, na którym została utworzona.</span><span class="sxs-lookup"><span data-stu-id="c2d94-151">For example, in both Windows Forms and WPF, a control can only be accessed on the thread on which it was created.</span></span> <span data-ttu-id="c2d94-152">Jeśli zostanie podjęta próba dostępu do udostępnionego stanu formantu Windows Forms w zapytaniu PLINQ, zgłaszany jest wyjątek, jeśli są uruchomione w debugerze.</span><span class="sxs-lookup"><span data-stu-id="c2d94-152">If you try to access the shared state of a Windows Forms control in a PLINQ query, an exception is raised if you are running in the debugger.</span></span> <span data-ttu-id="c2d94-153">(To ustawienie może być wyłączone.) Jednak jeśli zapytanie jest używany przez wątek interfejsu użytkownika, tak powstały formant z `foreach` wyników pętli, która wylicza zapytania, ponieważ ten kod jest wykonywany tylko jednego wątku.</span><span class="sxs-lookup"><span data-stu-id="c2d94-153">(This setting can be turned off.) However, if your query is consumed on the UI thread, then you can access the control from the `foreach` loop that enumerates the query results because that code executes on just one thread.</span></span>  
  
## <a name="do-not-assume-that-iterations-of-foreach-for-and-forall-always-execute-in-parallel"></a><span data-ttu-id="c2d94-154">Nie należy zakładać, że iteracji ForEach, dla i równolegle uruchomić zawsze ForAll</span><span class="sxs-lookup"><span data-stu-id="c2d94-154">Do Not Assume that Iterations of ForEach, For and ForAll Always Execute in Parallel</span></span>  
 <span data-ttu-id="c2d94-155">Ważne jest, aby pamiętać, że poszczególnych iteracji w <xref:System.Threading.Tasks.Parallel.For%2A?displayProperty=nameWithType>, <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> lub <xref:System.Linq.ParallelEnumerable.ForAll%2A> pętli może, ale nie trzeba wykonywać równolegle.</span><span class="sxs-lookup"><span data-stu-id="c2d94-155">It is important to keep in mind that individual iterations in a <xref:System.Threading.Tasks.Parallel.For%2A?displayProperty=nameWithType>, <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> or <xref:System.Linq.ParallelEnumerable.ForAll%2A> loop may but do not have to execute in parallel.</span></span> <span data-ttu-id="c2d94-156">W związku z tym należy unikać pisania żadnego kodu, który zależy od pod kątem poprawności na równoległe wykonywanie iteracji lub wykonywania iteracji w określonej kolejności.</span><span class="sxs-lookup"><span data-stu-id="c2d94-156">Therefore, you should avoid writing any code that depends for correctness on parallel execution of iterations or on the execution of iterations in any particular order.</span></span>  
  
 <span data-ttu-id="c2d94-157">Na przykład ten kod jest prawdopodobnie zakleszczenie:</span><span class="sxs-lookup"><span data-stu-id="c2d94-157">For example, this code is likely to deadlock:</span></span>  
  
```vb  
Dim mre = New ManualResetEventSlim()  
    Enumerable.Range(0, ProcessorCount * 100).AsParallel().ForAll(Sub(j)   
  
                                                     If j = Environment.ProcessorCount Then  
  
                                                         Console.WriteLine("Set on {0} with value of {1}", Thread.CurrentThread.ManagedThreadId, j)  
                                                         mre.Set()  
  
                                                     Else  
  
                                                         Console.WriteLine("Waiting on {0} with value of {1}", Thread.CurrentThread.ManagedThreadId, j)  
                                                         mre.Wait()  
                                                     End If  
    End Sub) ' deadlocks  
```  
  
```csharp  
ManualResetEventSlim mre = new ManualResetEventSlim();  
            Enumerable.Range(0, ProcessorCount * 100).AsParallel().ForAll((j) =>  
            {  
                if (j == Environment.ProcessorCount)  
                {  
                    Console.WriteLine("Set on {0} with value of {1}", Thread.CurrentThread.ManagedThreadId, j);  
                    mre.Set();  
                }  
                else  
                {  
                    Console.WriteLine("Waiting on {0} with value of {1}", Thread.CurrentThread.ManagedThreadId, j);  
                    mre.Wait();  
                }  
            }); //deadlocks  
```  
  
 <span data-ttu-id="c2d94-158">W tym przykładzie jednej iteracji ustawia zdarzenie, a wszystkie inne iteracje oczekiwania na zdarzenie.</span><span class="sxs-lookup"><span data-stu-id="c2d94-158">In this example, one iteration sets an event, and all other iterations wait on the event.</span></span> <span data-ttu-id="c2d94-159">Brak iteracji oczekiwania można wykonać przed ukończeniem iteracji ustawienie zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="c2d94-159">None of the waiting iterations can complete until the event-setting iteration has completed.</span></span> <span data-ttu-id="c2d94-160">Jednak jest możliwe, że iteracji oczekujące blokuje wszystkie wątki, które są używane do wykonywania pętli równoległej, zanim iteracji ustawienie zdarzenia miała szansę, aby wykonać.</span><span class="sxs-lookup"><span data-stu-id="c2d94-160">However, it is possible that the waiting iterations block all threads that are used to execute the parallel loop, before the event-setting iteration has had a chance to execute.</span></span> <span data-ttu-id="c2d94-161">Skutkuje to zakleszczenie — iteracji ustawienie zdarzenia nigdy nie zostanie wykonana i iteracje oczekiwania nigdy nie będzie wznawiania.</span><span class="sxs-lookup"><span data-stu-id="c2d94-161">This results in a deadlock – the event-setting iteration will never execute, and the waiting iterations will never wake up.</span></span>  
  
 <span data-ttu-id="c2d94-162">W szczególności jednej iteracji pętli równoległej nigdy nie należy czekać na innej iteracji pętli postępu.</span><span class="sxs-lookup"><span data-stu-id="c2d94-162">In particular, one iteration of a parallel loop should never wait on another iteration of the loop to make progress.</span></span> <span data-ttu-id="c2d94-163">Jeśli pętli równoległej decyduje zaplanować iteracje kolejno, ale w kolejności przeciwnej, nastąpi zakleszczenia.</span><span class="sxs-lookup"><span data-stu-id="c2d94-163">If the parallel loop decides to schedule the iterations sequentially but in the opposite order, a deadlock will occur.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="c2d94-164">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="c2d94-164">See also</span></span>

- [<span data-ttu-id="c2d94-165">Równoległe LINQ (PLINQ)</span><span class="sxs-lookup"><span data-stu-id="c2d94-165">Parallel LINQ (PLINQ)</span></span>](../../../docs/standard/parallel-programming/parallel-linq-plinq.md)
