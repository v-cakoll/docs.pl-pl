---
title: Wprowadzenie do analizy semantycznej
description: Ten samouczek zawiera omówienie pracy z analizą semantyczną przy użyciu zestawu SDK kompilatora .NET.
ms.date: 02/06/2018
ms.custom: mvc
ms.openlocfilehash: a6dcaeeb86acb5c0e1602f01dc5952ffd9d5e3f5
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/14/2020
ms.locfileid: "78240513"
---
# <a name="get-started-with-semantic-analysis"></a><span data-ttu-id="a4057-103">Wprowadzenie do analizy semantycznej</span><span class="sxs-lookup"><span data-stu-id="a4057-103">Get started with semantic analysis</span></span>

<span data-ttu-id="a4057-104">W tym samouczku przyjęto założenie, że znasz interfejs API składni.</span><span class="sxs-lookup"><span data-stu-id="a4057-104">This tutorial assumes you're familiar with the Syntax API.</span></span> <span data-ttu-id="a4057-105">Wprowadzenie [do analizy składni](syntax-analysis.md) artykuł zawiera wystarczające wprowadzenie.</span><span class="sxs-lookup"><span data-stu-id="a4057-105">The [get started with syntax analysis](syntax-analysis.md) article provides sufficient introduction.</span></span>

<span data-ttu-id="a4057-106">W tym samouczku można zapoznać **się z symboli** **i powiązań interfejsów API**.</span><span class="sxs-lookup"><span data-stu-id="a4057-106">In this tutorial, you explore the **Symbol** and **Binding APIs**.</span></span> <span data-ttu-id="a4057-107">Te interfejsy API zawierają informacje o _znaczeniu semantycznym_ programu.</span><span class="sxs-lookup"><span data-stu-id="a4057-107">These APIs provide information about the _semantic meaning_ of a program.</span></span> <span data-ttu-id="a4057-108">Umożliwiają one zadawanie pytań i odpowiadanie na pytania dotyczące typów reprezentowanych przez dowolny symbol w programie.</span><span class="sxs-lookup"><span data-stu-id="a4057-108">They enable you to ask and answer questions about the types represented by any symbol in your program.</span></span>

<span data-ttu-id="a4057-109">Musisz zainstalować zestaw **SDK platformy kompilatora .NET:**</span><span class="sxs-lookup"><span data-stu-id="a4057-109">You'll need to install the **.NET Compiler Platform SDK**:</span></span>

[!INCLUDE[interactive-note](~/includes/roslyn-installation.md)]

## <a name="understanding-compilations-and-symbols"></a><span data-ttu-id="a4057-110">Opis kompilacji i symboli</span><span class="sxs-lookup"><span data-stu-id="a4057-110">Understanding Compilations and Symbols</span></span>

<span data-ttu-id="a4057-111">Podczas pracy z zestawem SDK kompilatora .NET zapoznaj się z rozróżnieniem między interfejsem API składni a interfejsem API semantycznego.</span><span class="sxs-lookup"><span data-stu-id="a4057-111">As you work more with the .NET Compiler SDK, you become familiar with the distinctions between Syntax API and the Semantic API.</span></span> <span data-ttu-id="a4057-112">**Interfejs API składni** umożliwia przyjrzenie się _strukturze_ programu.</span><span class="sxs-lookup"><span data-stu-id="a4057-112">The **Syntax API** allows you to look at the _structure_ of a program.</span></span> <span data-ttu-id="a4057-113">Jednak często chcesz bogatsze informacje o semantyce lub _znaczeniu_ programu.</span><span class="sxs-lookup"><span data-stu-id="a4057-113">However, often you want richer information about the semantics or _meaning_ of a program.</span></span> <span data-ttu-id="a4057-114">Podczas gdy plik kodu luźnego lub fragment kodu języka Visual Basic lub C# może być analizowany syntaktycznie w izolacji, nie ma sensu zadawać pytań, takich jak "jaki jest typ tej zmiennej" w próżni.</span><span class="sxs-lookup"><span data-stu-id="a4057-114">While a loose code file or snippet of Visual Basic or C# code can be syntactically analyzed in isolation, it's not meaningful to ask questions such as "what's the type of this variable" in a vacuum.</span></span> <span data-ttu-id="a4057-115">Znaczenie nazwy typu może być zależne od odwołań do zestawu, importu obszaru nazw lub innych plików kodu.</span><span class="sxs-lookup"><span data-stu-id="a4057-115">The meaning of a type name may be dependent on assembly references, namespace imports, or other code files.</span></span> <span data-ttu-id="a4057-116">Odpowiedzi na te pytania są udzielane przy użyciu <xref:Microsoft.CodeAnalysis.Compilation?displayProperty=nameWithType> **interfejsu API semantycznego,** w szczególności klasy.</span><span class="sxs-lookup"><span data-stu-id="a4057-116">Those questions are answered using the **Semantic API**, specifically the <xref:Microsoft.CodeAnalysis.Compilation?displayProperty=nameWithType> class.</span></span>

<span data-ttu-id="a4057-117">Wystąpienie <xref:Microsoft.CodeAnalysis.Compilation> jest analogiczne do pojedynczego projektu, jak widać przez kompilator i reprezentuje wszystko, co potrzebne do skompilowania programu Visual Basic lub C#.</span><span class="sxs-lookup"><span data-stu-id="a4057-117">An instance of <xref:Microsoft.CodeAnalysis.Compilation> is analogous to a single project as seen by the compiler and represents everything needed to compile a Visual Basic or C# program.</span></span> <span data-ttu-id="a4057-118">**Kompilacja** zawiera zestaw plików źródłowych do skompilowania, odwołania do zestawu i opcje kompilatora.</span><span class="sxs-lookup"><span data-stu-id="a4057-118">The **compilation** includes the set of source files to be compiled, assembly references, and compiler options.</span></span> <span data-ttu-id="a4057-119">Można powód o znaczeniu kodu przy użyciu wszystkich innych informacji w tym kontekście.</span><span class="sxs-lookup"><span data-stu-id="a4057-119">You can reason about the meaning of the code using all the other information in this context.</span></span> <span data-ttu-id="a4057-120">A <xref:Microsoft.CodeAnalysis.Compilation> umożliwia **znajdowanie symboli** — jednostek, takich jak typy, przestrzenie nazw, elementy członkowskie i zmienne, do których odnoszą się nazwy i inne wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="a4057-120">A <xref:Microsoft.CodeAnalysis.Compilation> allows you to find **Symbols** - entities such as types, namespaces, members, and variables which names and other expressions refer to.</span></span> <span data-ttu-id="a4057-121">Proces kojarzenia nazw i wyrażeń z **symbolami** nosi nazwę **Binding**.</span><span class="sxs-lookup"><span data-stu-id="a4057-121">The process of associating names and expressions with **Symbols** is called **Binding**.</span></span>

<span data-ttu-id="a4057-122">Like <xref:Microsoft.CodeAnalysis.SyntaxTree?displayProperty=nameWithType> <xref:Microsoft.CodeAnalysis.Compilation> , jest klasą abstrakcyjną z pochodnymi specyficznymi dla języka.</span><span class="sxs-lookup"><span data-stu-id="a4057-122">Like <xref:Microsoft.CodeAnalysis.SyntaxTree?displayProperty=nameWithType>, <xref:Microsoft.CodeAnalysis.Compilation> is an abstract class with language-specific derivatives.</span></span> <span data-ttu-id="a4057-123">Podczas tworzenia wystąpienia kompilacji, należy wywołać metodę <xref:Microsoft.CodeAnalysis.CSharp.CSharpCompilation?displayProperty=nameWithType> fabryki <xref:Microsoft.CodeAnalysis.VisualBasic.VisualBasicCompilation?displayProperty=nameWithType>na (lub ) klasy.</span><span class="sxs-lookup"><span data-stu-id="a4057-123">When creating an instance of Compilation, you must invoke a factory method on the <xref:Microsoft.CodeAnalysis.CSharp.CSharpCompilation?displayProperty=nameWithType> (or <xref:Microsoft.CodeAnalysis.VisualBasic.VisualBasicCompilation?displayProperty=nameWithType>) class.</span></span>

## <a name="querying-symbols"></a><span data-ttu-id="a4057-124">Wykonywanie zapytań dotyczących symboli</span><span class="sxs-lookup"><span data-stu-id="a4057-124">Querying symbols</span></span>

<span data-ttu-id="a4057-125">W tym samouczku ponownie spojrzysz na program "Hello World".</span><span class="sxs-lookup"><span data-stu-id="a4057-125">In this tutorial, you look at the "Hello World" program again.</span></span> <span data-ttu-id="a4057-126">Tym razem kwerendy symbole w programie, aby zrozumieć, jakie typy te symbole reprezentują.</span><span class="sxs-lookup"><span data-stu-id="a4057-126">This time, you query the symbols in the program to understand what types those symbols represent.</span></span> <span data-ttu-id="a4057-127">Kwerenda dla typów w przestrzeni nazw i dowiedzieć się, aby znaleźć metody dostępne dla typu.</span><span class="sxs-lookup"><span data-stu-id="a4057-127">You query for the types in a namespace, and learn to find the methods available on a type.</span></span>

<span data-ttu-id="a4057-128">Gotowy kod dla tego przykładu można zobaczyć w [naszym repozytorium GitHub](https://github.com/dotnet/samples/tree/master/csharp/roslyn-sdk/SemanticQuickStart).</span><span class="sxs-lookup"><span data-stu-id="a4057-128">You can see the finished code for this sample in [our GitHub repository](https://github.com/dotnet/samples/tree/master/csharp/roslyn-sdk/SemanticQuickStart).</span></span>

> [!NOTE]
> <span data-ttu-id="a4057-129">Typy Drzewa składni używają dziedziczenia do opisywania różnych elementów składni, które są prawidłowe w różnych lokalizacjach w programie.</span><span class="sxs-lookup"><span data-stu-id="a4057-129">The Syntax Tree types use inheritance to describe the different syntax elements that are valid at different locations in the program.</span></span> <span data-ttu-id="a4057-130">Korzystanie z tych interfejsów API często oznacza właściwości rzutowania lub członków kolekcji do określonych typów pochodnych.</span><span class="sxs-lookup"><span data-stu-id="a4057-130">Using these APIs often means casting properties or collection members to specific derived types.</span></span> <span data-ttu-id="a4057-131">W poniższych przykładach przypisania i rzutowania są oddzielne instrukcje, przy użyciu jawnie wpisane zmienne.</span><span class="sxs-lookup"><span data-stu-id="a4057-131">In the following examples, the assignment and the casts are separate statements, using explicitly typed variables.</span></span> <span data-ttu-id="a4057-132">Można odczytać kod, aby zobaczyć typy zwracane interfejsu API i typu wykonywania zwróconych obiektów.</span><span class="sxs-lookup"><span data-stu-id="a4057-132">You can read the code to see the return types of the API and the runtime type of the objects returned.</span></span> <span data-ttu-id="a4057-133">W praktyce jest bardziej powszechne do używania niejawnie wpisane zmienne i polegać na nazwy interfejsu API do opisania typu obiektów badane.</span><span class="sxs-lookup"><span data-stu-id="a4057-133">In practice, it's more common to use implicitly typed variables and rely on API names to describe the type of objects being examined.</span></span>

<span data-ttu-id="a4057-134">Utwórz nowy projekt narzędzia do **analizy kodu autonomicznego** języka C#:</span><span class="sxs-lookup"><span data-stu-id="a4057-134">Create a new C# **Stand-Alone Code Analysis Tool** project:</span></span>

* <span data-ttu-id="a4057-135">W programie Visual Studio wybierz pozycję **Plik** > **nowy** > **projekt,** aby wyświetlić okno dialogowe Nowy projekt.</span><span class="sxs-lookup"><span data-stu-id="a4057-135">In Visual Studio, choose **File** > **New** > **Project** to display the New Project dialog.</span></span>
* <span data-ttu-id="a4057-136">W obszarze**Rozszerzalność** **języka Visual C#** > wybierz pozycję **Autonomiczne narzędzie do analizy kodu**.</span><span class="sxs-lookup"><span data-stu-id="a4057-136">Under **Visual C#** > **Extensibility**, choose **Stand-Alone Code Analysis Tool**.</span></span>
* <span data-ttu-id="a4057-137">Nazwij swój projekt "**SemanticQuickStart**" i kliknij przycisk OK.</span><span class="sxs-lookup"><span data-stu-id="a4057-137">Name your project "**SemanticQuickStart**" and click OK.</span></span>

<span data-ttu-id="a4057-138">Będziesz analizować podstawowe "Hello World!"</span><span class="sxs-lookup"><span data-stu-id="a4057-138">You're going to analyze the basic "Hello World!"</span></span> <span data-ttu-id="a4057-139">program pokazany wcześniej.</span><span class="sxs-lookup"><span data-stu-id="a4057-139">program shown earlier.</span></span>
<span data-ttu-id="a4057-140">Dodaj tekst programu Hello World jako stałą w swojej `Program` klasie:</span><span class="sxs-lookup"><span data-stu-id="a4057-140">Add the text for the Hello World program as a constant in your `Program` class:</span></span>

[!code-csharp[Declare the program test](../../../../samples/snippets/csharp/roslyn-sdk/SemanticQuickStart/Program.cs#1 "Declare a constant string for the program text to analyze")]

<span data-ttu-id="a4057-141">Następnie dodaj następujący kod, aby utworzyć drzewo składni `programText` dla tekstu kodu w stałej.</span><span class="sxs-lookup"><span data-stu-id="a4057-141">Next, add the following code to build the syntax tree for the code text in the `programText` constant.</span></span>  <span data-ttu-id="a4057-142">Dodaj następujący wiersz `Main` do metody:</span><span class="sxs-lookup"><span data-stu-id="a4057-142">Add the following line to your `Main` method:</span></span>

[!code-csharp[Create the tree](../../../../samples/snippets/csharp/roslyn-sdk/SemanticQuickStart/Program.cs#2 "Create the syntax tree")]

<span data-ttu-id="a4057-143">Następnie zbuduj <xref:Microsoft.CodeAnalysis.CSharp.CSharpCompilation> z drzewa, które zostały już utworzone.</span><span class="sxs-lookup"><span data-stu-id="a4057-143">Next, build a <xref:Microsoft.CodeAnalysis.CSharp.CSharpCompilation> from the tree you already created.</span></span> <span data-ttu-id="a4057-144">"Hello World" próbki opiera <xref:System.String> <xref:System.Console> się na i typów.</span><span class="sxs-lookup"><span data-stu-id="a4057-144">The "Hello World" sample relies on the <xref:System.String> and <xref:System.Console> types.</span></span> <span data-ttu-id="a4057-145">Należy odwołać się do zestawu, który deklaruje te dwa typy w kompilacji.</span><span class="sxs-lookup"><span data-stu-id="a4057-145">You need to reference the assembly that declares those two types in your compilation.</span></span> <span data-ttu-id="a4057-146">Dodaj następujący wiersz `Main` do metody, aby utworzyć kompilację drzewa składni, w tym odwołanie do odpowiedniego zestawu:</span><span class="sxs-lookup"><span data-stu-id="a4057-146">Add the following line to your `Main` method to create a compilation of your syntax tree, including the reference to the appropriate assembly:</span></span>

[!code-csharp[Create the compilation](../../../../samples/snippets/csharp/roslyn-sdk/SemanticQuickStart/Program.cs#3 "Create the compilation for the semantic model")]

<span data-ttu-id="a4057-147">Metoda <xref:Microsoft.CodeAnalysis.CSharp.CSharpCompilation.AddReferences%2A?displayProperty=nameWithType> dodaje odwołania do kompilacji.</span><span class="sxs-lookup"><span data-stu-id="a4057-147">The <xref:Microsoft.CodeAnalysis.CSharp.CSharpCompilation.AddReferences%2A?displayProperty=nameWithType> method adds references to the compilation.</span></span> <span data-ttu-id="a4057-148">Metoda <xref:Microsoft.CodeAnalysis.MetadataReference.CreateFromFile%2A?displayProperty=nameWithType> ładuje zespół jako odniesienie.</span><span class="sxs-lookup"><span data-stu-id="a4057-148">The <xref:Microsoft.CodeAnalysis.MetadataReference.CreateFromFile%2A?displayProperty=nameWithType> method loads an assembly as a reference.</span></span>

## <a name="querying-the-semantic-model"></a><span data-ttu-id="a4057-149">Wykonywanie zapytań dotyczących modelu semantycznego</span><span class="sxs-lookup"><span data-stu-id="a4057-149">Querying the semantic model</span></span>

<span data-ttu-id="a4057-150">Gdy masz <xref:Microsoft.CodeAnalysis.Compilation> można poprosić o <xref:Microsoft.CodeAnalysis.SemanticModel> for <xref:Microsoft.CodeAnalysis.SyntaxTree> any zawarte <xref:Microsoft.CodeAnalysis.Compilation>w tym .</span><span class="sxs-lookup"><span data-stu-id="a4057-150">Once you have a <xref:Microsoft.CodeAnalysis.Compilation> you can ask it for a <xref:Microsoft.CodeAnalysis.SemanticModel> for any <xref:Microsoft.CodeAnalysis.SyntaxTree> contained in that <xref:Microsoft.CodeAnalysis.Compilation>.</span></span> <span data-ttu-id="a4057-151">Można myśleć o modelu semantycznym jako źródło wszystkich informacji, które normalnie można uzyskać z intellisense.</span><span class="sxs-lookup"><span data-stu-id="a4057-151">You can think of the semantic model as the source for all the information you would normally get from intellisense.</span></span> <span data-ttu-id="a4057-152">A <xref:Microsoft.CodeAnalysis.SemanticModel> może odpowiadać na pytania takie jak "Jakie nazwy mają miejsce w tej lokalizacji?", "Jakie elementy członkowskie są dostępne z tej metody?", "Jakie zmienne są używane w tym bloku tekstu?" i "Do czego odnosi się ta nazwa/wyrażenie?"</span><span class="sxs-lookup"><span data-stu-id="a4057-152">A <xref:Microsoft.CodeAnalysis.SemanticModel> can answer questions like "What names are in scope at this location?", "What members are accessible from this method?", "What variables are used in this block of text?", and "What does this name/expression refer to?"</span></span> <span data-ttu-id="a4057-153">Dodaj tę instrukcję, aby utworzyć model semantyczny:</span><span class="sxs-lookup"><span data-stu-id="a4057-153">Add this statement to create the semantic model:</span></span>

[!code-csharp[Create the semantic model](../../../../samples/snippets/csharp/roslyn-sdk/SemanticQuickStart/Program.cs#4 "Create the semantic model")]

## <a name="binding-a-name"></a><span data-ttu-id="a4057-154">Powiązanie nazwy</span><span class="sxs-lookup"><span data-stu-id="a4057-154">Binding a name</span></span>

<span data-ttu-id="a4057-155">Tworzy <xref:Microsoft.CodeAnalysis.Compilation> z <xref:Microsoft.CodeAnalysis.SemanticModel> . <xref:Microsoft.CodeAnalysis.SyntaxTree></span><span class="sxs-lookup"><span data-stu-id="a4057-155">The <xref:Microsoft.CodeAnalysis.Compilation> creates the  <xref:Microsoft.CodeAnalysis.SemanticModel> from the <xref:Microsoft.CodeAnalysis.SyntaxTree>.</span></span> <span data-ttu-id="a4057-156">Po utworzeniu modelu można zbadać go, aby znaleźć pierwszą `using` `System` dyrektywę i pobrać informacje o symbolu obszaru nazw.</span><span class="sxs-lookup"><span data-stu-id="a4057-156">After creating the model, you can query it to find the first `using` directive, and retrieve the symbol information for the `System` namespace.</span></span> <span data-ttu-id="a4057-157">Dodaj te dwa `Main` wiersze do metody, aby utworzyć model semantyczny i pobrać symbol dla pierwszego using instrukcji:</span><span class="sxs-lookup"><span data-stu-id="a4057-157">Add these two lines to your `Main` method to create the semantic model and retrieve the symbol for the first using statement:</span></span>

[!code-csharp[Find the namespace symbol for the first using](../../../../samples/snippets/csharp/roslyn-sdk/SemanticQuickStart/Program.cs#5 "Find the namespace symbol for the first using")]

<span data-ttu-id="a4057-158">Poprzedni kod pokazuje, jak powiązać nazwę `using` w pierwszej dyrektywie, aby pobrać <xref:Microsoft.CodeAnalysis.SymbolInfo?displayProperty=nameWithType> dla obszaru `System` nazw.</span><span class="sxs-lookup"><span data-stu-id="a4057-158">The preceding code shows how to bind the name in the first `using` directive to retrieve a <xref:Microsoft.CodeAnalysis.SymbolInfo?displayProperty=nameWithType> for the `System` namespace.</span></span> <span data-ttu-id="a4057-159">Poprzedni kod ilustruje również, że za pomocą **modelu składni,** aby znaleźć strukturę kodu; używasz **modelu semantycznego,** aby zrozumieć jego znaczenie.</span><span class="sxs-lookup"><span data-stu-id="a4057-159">The preceding code also illustrates that you use the **syntax model** to find the structure of the code; you use the **semantic model** to understand its meaning.</span></span> <span data-ttu-id="a4057-160">**Model składni** znajdzie `System` ciąg w using instrukcji.</span><span class="sxs-lookup"><span data-stu-id="a4057-160">The **syntax model** finds the string `System` in the using statement.</span></span> <span data-ttu-id="a4057-161">**Model semantyczny** ma wszystkie informacje o typach `System` zdefiniowanych w przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="a4057-161">The **semantic model** has all the information about the types defined in the `System` namespace.</span></span>

<span data-ttu-id="a4057-162">Z <xref:Microsoft.CodeAnalysis.SymbolInfo> obiektu można uzyskać <xref:Microsoft.CodeAnalysis.ISymbol?displayProperty=nameWithType> przy <xref:Microsoft.CodeAnalysis.SymbolInfo.Symbol?displayProperty=nameWithType> użyciu właściwości.</span><span class="sxs-lookup"><span data-stu-id="a4057-162">From the <xref:Microsoft.CodeAnalysis.SymbolInfo> object you can obtain the <xref:Microsoft.CodeAnalysis.ISymbol?displayProperty=nameWithType> using the <xref:Microsoft.CodeAnalysis.SymbolInfo.Symbol?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="a4057-163">Ta właściwość zwraca symbol, do który odwołuje się to wyrażenie.</span><span class="sxs-lookup"><span data-stu-id="a4057-163">This property returns the symbol this expression refers to.</span></span> <span data-ttu-id="a4057-164">W przypadku wyrażeń, które nie odwołują się `null`do niczego (np. literały liczbowe), ta właściwość jest .</span><span class="sxs-lookup"><span data-stu-id="a4057-164">For expressions that don't refer to anything (such as numeric literals) this property is `null`.</span></span> <span data-ttu-id="a4057-165">Gdy <xref:Microsoft.CodeAnalysis.SymbolInfo.Symbol?displayProperty=nameWithType> nie ma wartości <xref:Microsoft.CodeAnalysis.ISymbol.Kind?displayProperty=nameWithType> null, oznacza typ symbolu.</span><span class="sxs-lookup"><span data-stu-id="a4057-165">When the <xref:Microsoft.CodeAnalysis.SymbolInfo.Symbol?displayProperty=nameWithType> is not null, the <xref:Microsoft.CodeAnalysis.ISymbol.Kind?displayProperty=nameWithType> denotes the type of the symbol.</span></span> <span data-ttu-id="a4057-166">W tym przykładzie <xref:Microsoft.CodeAnalysis.ISymbol.Kind?displayProperty=nameWithType> właściwość <xref:Microsoft.CodeAnalysis.SymbolKind.Namespace?displayProperty=nameWithType>jest .</span><span class="sxs-lookup"><span data-stu-id="a4057-166">In this example, the <xref:Microsoft.CodeAnalysis.ISymbol.Kind?displayProperty=nameWithType> property is a <xref:Microsoft.CodeAnalysis.SymbolKind.Namespace?displayProperty=nameWithType>.</span></span> <span data-ttu-id="a4057-167">Dodaj następujący kod `Main` do metody.</span><span class="sxs-lookup"><span data-stu-id="a4057-167">Add the following code to your `Main` method.</span></span> <span data-ttu-id="a4057-168">Pobiera symbol obszaru nazw, `System` a następnie wyświetla wszystkie podrzędne `System` przestrzenie nazw zadeklarowane w obszarze nazw:</span><span class="sxs-lookup"><span data-stu-id="a4057-168">It retrieves the symbol for the `System` namespace and then displays all the child namespaces declared in the `System` namespace:</span></span>

[!code-csharp[Display all the child namespaces](../../../../samples/snippets/csharp/roslyn-sdk/SemanticQuickStart/Program.cs#6 "Display all the child namespaces from this compilation")]

<span data-ttu-id="a4057-169">Uruchom program i powinieneś zobaczyć następujące dane wyjściowe:</span><span class="sxs-lookup"><span data-stu-id="a4057-169">Run the program and you should see the following output:</span></span>

```output
System.Collections
System.Configuration
System.Deployment
System.Diagnostics
System.Globalization
System.IO
System.Numerics
System.Reflection
System.Resources
System.Runtime
System.Security
System.StubHelpers
System.Text
System.Threading
Press any key to continue . . .
```

> [!NOTE]
> <span data-ttu-id="a4057-170">Dane wyjściowe nie obejmują każdego obszaru nazw, który `System` jest podrzędnym obszarem nazw obszaru nazw.</span><span class="sxs-lookup"><span data-stu-id="a4057-170">The output does not include every namespace that is a child namespace of the `System` namespace.</span></span> <span data-ttu-id="a4057-171">Wyświetla każdy obszar nazw, który jest obecny w tej `System.String` kompilacji, która odwołuje się tylko do zestawu, gdzie jest zadeklarowany.</span><span class="sxs-lookup"><span data-stu-id="a4057-171">It displays every namespace that is present in this compilation, which only references the assembly where `System.String` is declared.</span></span> <span data-ttu-id="a4057-172">Wszystkie przestrzenie nazw zadeklarowane w innych zestawach nie są znane tej kompilacji</span><span class="sxs-lookup"><span data-stu-id="a4057-172">Any namespaces declared in other assemblies are not known to this compilation</span></span>

### <a name="binding-an-expression"></a><span data-ttu-id="a4057-173">Powiązanie wyrażenia</span><span class="sxs-lookup"><span data-stu-id="a4057-173">Binding an expression</span></span>

<span data-ttu-id="a4057-174">Poprzedni kod pokazuje, jak znaleźć symbol przez powiązanie z nazwą.</span><span class="sxs-lookup"><span data-stu-id="a4057-174">The preceding code shows how to find a symbol by binding to a name.</span></span> <span data-ttu-id="a4057-175">Istnieją inne wyrażenia w programie C#, które mogą być powiązane, które nie są nazwy.</span><span class="sxs-lookup"><span data-stu-id="a4057-175">There are other expressions in a C# program that can be bound that aren't names.</span></span> <span data-ttu-id="a4057-176">Aby zademonstrować tę możliwość, przejdźmy do powiązania z literałem prostego ciągu.</span><span class="sxs-lookup"><span data-stu-id="a4057-176">To demonstrate this capability, let's access the binding to a simple string literal.</span></span>

<span data-ttu-id="a4057-177">Program "Hello World" <xref:Microsoft.CodeAnalysis.CSharp.Syntax.LiteralExpressionSyntax?displayProperty=nameWithType>zawiera program "Hello, World!"</span><span class="sxs-lookup"><span data-stu-id="a4057-177">The "Hello World" program contains a <xref:Microsoft.CodeAnalysis.CSharp.Syntax.LiteralExpressionSyntax?displayProperty=nameWithType>, the "Hello, World!"</span></span> <span data-ttu-id="a4057-178">ciąg wyświetlany na konsoli.</span><span class="sxs-lookup"><span data-stu-id="a4057-178">string displayed to the console.</span></span>

<span data-ttu-id="a4057-179">Znajdziesz "Cześć, Świat!"</span><span class="sxs-lookup"><span data-stu-id="a4057-179">You find the "Hello, World!"</span></span> <span data-ttu-id="a4057-180">przez zlokalizowanie pojedynczego ciągu literał w programie.</span><span class="sxs-lookup"><span data-stu-id="a4057-180">string by locating the single string literal in the program.</span></span> <span data-ttu-id="a4057-181">Następnie po zlokalizowaniu węzła składni pobierz informacje o typie tego węzła z modelu semantycznego.</span><span class="sxs-lookup"><span data-stu-id="a4057-181">Then, once you've located the syntax node, get the type info for that node from the semantic model.</span></span> <span data-ttu-id="a4057-182">Dodaj następujący kod `Main` do metody:</span><span class="sxs-lookup"><span data-stu-id="a4057-182">Add the following code to your `Main` method:</span></span>

[!code-csharp[Find the namespace symbol for the only using](../../../../samples/snippets/csharp/roslyn-sdk/SemanticQuickStart/Program.cs#7 "Find the namespace symbol for the only using")]

<span data-ttu-id="a4057-183">Struktura <xref:Microsoft.CodeAnalysis.TypeInfo?displayProperty=nameWithType> zawiera <xref:Microsoft.CodeAnalysis.TypeInfo.Type?displayProperty=nameWithType> właściwość, która umożliwia dostęp do informacji semantycznych o typie literału.</span><span class="sxs-lookup"><span data-stu-id="a4057-183">The <xref:Microsoft.CodeAnalysis.TypeInfo?displayProperty=nameWithType> struct includes a <xref:Microsoft.CodeAnalysis.TypeInfo.Type?displayProperty=nameWithType> property that enables access to the semantic information about the type of the literal.</span></span> <span data-ttu-id="a4057-184">W tym przykładzie jest `string` to typ.</span><span class="sxs-lookup"><span data-stu-id="a4057-184">In this example, that's the `string` type.</span></span> <span data-ttu-id="a4057-185">Dodaj deklarację, która przypisuje tę właściwość do zmiennej lokalnej:</span><span class="sxs-lookup"><span data-stu-id="a4057-185">Add a declaration that assigns this property to a local variable:</span></span>

[!code-csharp[Find the semantic information about the string type](../../../../samples/snippets/csharp/roslyn-sdk/SemanticQuickStart/Program.cs#8 "Use the string literal to access the semantic information in the string type.")]

<span data-ttu-id="a4057-186">Aby zakończyć ten samouczek, skompilujmy kwerendę LINQ, która `string` tworzy sekwencję wszystkich metod publicznych zadeklarowanych na typie, który zwraca plik `string`.</span><span class="sxs-lookup"><span data-stu-id="a4057-186">To finish this tutorial, let's build a LINQ query that creates a sequence of all the public methods declared on the `string` type that return a `string`.</span></span> <span data-ttu-id="a4057-187">Ta kwerenda staje się złożona, więc skompilujmy je wiersz po wierszu, a następnie zrekonstruujmy jako pojedynczą kwerendę.</span><span class="sxs-lookup"><span data-stu-id="a4057-187">This query gets complex, so let's build it line by line, then reconstruct it as a single query.</span></span> <span data-ttu-id="a4057-188">Źródłem tej kwerendy jest sekwencja wszystkich `string` elementów członkowskich zadeklarowanych na typie:</span><span class="sxs-lookup"><span data-stu-id="a4057-188">The source for this query is the sequence of all members declared on the `string` type:</span></span>

[!code-csharp[Access the sequence of members on the string type](../../../../samples/snippets/csharp/roslyn-sdk/SemanticQuickStart/Program.cs#9 "Access the sequence of members on the string type.")]

<span data-ttu-id="a4057-189">Ta sekwencja źródłowa zawiera wszystkie elementy członkowskie, <xref:System.Collections.Immutable.ImmutableArray%601.OfType%2A?displayProperty=nameWithType> w tym właściwości <xref:Microsoft.CodeAnalysis.IMethodSymbol?displayProperty=nameWithType> i pola, więc przefiltruj ją za pomocą metody, aby znaleźć elementy, które są obiektami:</span><span class="sxs-lookup"><span data-stu-id="a4057-189">That source sequence contains all members, including properties and fields, so filter it using the <xref:System.Collections.Immutable.ImmutableArray%601.OfType%2A?displayProperty=nameWithType> method to find elements that are <xref:Microsoft.CodeAnalysis.IMethodSymbol?displayProperty=nameWithType> objects:</span></span>

[!code-csharp[Filter the sequence to only methods](../../../../samples/snippets/csharp/roslyn-sdk/SemanticQuickStart/Program.cs#10 "Find the subset of the collection that is the methods.")]

<span data-ttu-id="a4057-190">Następnie dodaj inny filtr, aby zwrócić tylko te `string`metody, które są publiczne i zwracają:</span><span class="sxs-lookup"><span data-stu-id="a4057-190">Next, add another filter to return only those methods that are public and return a `string`:</span></span>

[!code-csharp[Filter on return type and accessibility](../../../../samples/snippets/csharp/roslyn-sdk/SemanticQuickStart/Program.cs#11 "Find only the public methods that return a string.")]

<span data-ttu-id="a4057-191">Zaznacz tylko właściwość name i tylko różne nazwy, usuwając wszelkie przeciążenia:</span><span class="sxs-lookup"><span data-stu-id="a4057-191">Select only the name property, and only distinct names by removing any overloads:</span></span>

[!code-csharp[find the distinct names.](../../../../samples/snippets/csharp/roslyn-sdk/SemanticQuickStart/Program.cs#12 "Use the string literal to access the semantic information in the string type.")]

<span data-ttu-id="a4057-192">Można również utworzyć pełną kwerendę przy użyciu składni kwerendy LINQ, a następnie wyświetlić wszystkie nazwy metod w konsoli:</span><span class="sxs-lookup"><span data-stu-id="a4057-192">You can also build the full query using the LINQ query syntax, and then display all the method names in  the console:</span></span>

[!code-csharp[build and display the results of this query.](../../../../samples/snippets/csharp/roslyn-sdk/SemanticQuickStart/Program.cs#13 "Build and display the results of the query.")]

<span data-ttu-id="a4057-193">Skompiluj i uruchom program.</span><span class="sxs-lookup"><span data-stu-id="a4057-193">Build and run the program.</span></span> <span data-ttu-id="a4057-194">Powinny zostać wyświetlone następujące dane wyjściowe:</span><span class="sxs-lookup"><span data-stu-id="a4057-194">You should see the following output:</span></span>

```output
Join
Substring
Trim
TrimStart
TrimEnd
Normalize
PadLeft
PadRight
ToLower
ToLowerInvariant
ToUpper
ToUpperInvariant
ToString
Insert
Replace
Remove
Format
Copy
Concat
Intern
IsInterned
Press any key to continue . . .
```

<span data-ttu-id="a4057-195">Interfejs API semantycznego służy do znajdowania i wyświetlania informacji o symbolach, które są częścią tego programu.</span><span class="sxs-lookup"><span data-stu-id="a4057-195">You've used the Semantic API to find and display information about the symbols that are part of this program.</span></span>
