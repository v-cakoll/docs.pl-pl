---
title: Błąd kompilatora CS0029
ms.date: 07/20/2015
f1_keywords:
- CS0029
helpviewer_keywords:
- CS0029
ms.assetid: 63c3e574-1868-4a9e-923e-dcd9f38bce88
ms.openlocfilehash: 1c193d6fe6c3d380bfd84af57df34e6841b724a7
ms.sourcegitcommit: 986f836f72ef10876878bd6217174e41464c145a
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 08/19/2019
ms.locfileid: "69608251"
---
# <a name="compiler-error-cs0029"></a><span data-ttu-id="1057d-102">Błąd kompilatora CS0029</span><span class="sxs-lookup"><span data-stu-id="1057d-102">Compiler Error CS0029</span></span>

<span data-ttu-id="1057d-103">Nie można niejawnie przekonwertować typu "Type" na typ "Type".</span><span class="sxs-lookup"><span data-stu-id="1057d-103">Cannot implicitly convert type 'type' to 'type'</span></span>  
  
 <span data-ttu-id="1057d-104">Kompilator wymaga jawnej konwersji.</span><span class="sxs-lookup"><span data-stu-id="1057d-104">The compiler requires an explicit conversion.</span></span> <span data-ttu-id="1057d-105">Na przykład może być konieczne Rzutowanie wartości r na taki sam typ jak l-wartością.</span><span class="sxs-lookup"><span data-stu-id="1057d-105">For example, you may need to cast an r-value to be the same type as an l-value.</span></span> <span data-ttu-id="1057d-106">Lub należy zapewnić procedury konwersji do obsługi określonych przeciążeń operatora.</span><span class="sxs-lookup"><span data-stu-id="1057d-106">Or, you must provide conversion routines to support certain operator overloads.</span></span>  
  
 <span data-ttu-id="1057d-107">Konwersje muszą wystąpić podczas przypisywania zmiennej jednego typu do zmiennej innego typu.</span><span class="sxs-lookup"><span data-stu-id="1057d-107">Conversions must occur when assigning a variable of one type to a variable of a different type.</span></span> <span data-ttu-id="1057d-108">Podczas przypisywania między zmiennymi różnych typów kompilator musi przekonwertować typ po prawej stronie operatora przypisania na typ po lewej stronie operatora przypisania, a następnie.</span><span class="sxs-lookup"><span data-stu-id="1057d-108">When making an assignment between variables of different types, the compiler must convert the type on the right-hand side of the assignment operator to the type on the left-hand side of the assignment operator.</span></span> <span data-ttu-id="1057d-109">Wykonaj następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="1057d-109">Take the following the code:</span></span>  

```csharp
int i = 50;  
long lng = 100;  
i = lng;  
```

 <span data-ttu-id="1057d-110">`i = lng;`tworzy przypisanie, ale typy danych zmiennych po lewej i prawej stronie operatora przypisania nie są zgodne.</span><span class="sxs-lookup"><span data-stu-id="1057d-110">`i = lng;` makes an assignment, but the data types of the variables on the left and right-hand side of the assignment operator don't match.</span></span> <span data-ttu-id="1057d-111">Przed przypisaniem kompilator niejawnie konwertuje zmienną `lng`, która jest typu Long na int. Jest to niejawne, ponieważ żaden kod jawnie nakazuje kompilatorowi wykonanie tej konwersji.</span><span class="sxs-lookup"><span data-stu-id="1057d-111">Before making the assignment the compiler is implicitly converting the variable `lng`, which is of type long, to an int. This is implicit because no code explicitly instructed the compiler to perform this conversion.</span></span> <span data-ttu-id="1057d-112">Problem z tym kodem jest uznawany za zawężający, a kompilator nie zezwala na niejawne konwersje, ponieważ może to spowodować utratę danych.</span><span class="sxs-lookup"><span data-stu-id="1057d-112">The problem with this code is that this is considered a narrowing conversion, and the compiler does not allow implicit narrowing conversions because there could be a potential loss of data.</span></span>  
  
 <span data-ttu-id="1057d-113">Podczas konwertowania na typ danych, który zajmuje mniej miejsca w pamięci niż typ danych, z której jest przeprowadzana konwersja zawężania.</span><span class="sxs-lookup"><span data-stu-id="1057d-113">A narrowing conversion exists when converting to a data type that occupies less storage space in memory than the data type we are converting from.</span></span> <span data-ttu-id="1057d-114">Na przykład konwersja typu Long na typ int zostałaby uznana za konwersję zawężaną.</span><span class="sxs-lookup"><span data-stu-id="1057d-114">For example, converting a long to an int would be considered a narrowing conversion.</span></span> <span data-ttu-id="1057d-115">Długi czas zajmowany przez 8 bajtów pamięci, podczas gdy int zajmuje 4 bajty.</span><span class="sxs-lookup"><span data-stu-id="1057d-115">A long occupies 8 bytes of memory while an int occupies 4 bytes.</span></span> <span data-ttu-id="1057d-116">Aby dowiedzieć się, jak może dojść do utraty danych, weź pod uwagę następujący przykład:</span><span class="sxs-lookup"><span data-stu-id="1057d-116">To see how data loss can occur, consider the following sample:</span></span>  

```csharp
int i = 50;  
long lng = 3147483647;  
i = lng;  
```

 <span data-ttu-id="1057d-117">Zmienna `lng` zawiera teraz wartość, która nie może być przechowywana w zmiennej `i` , ponieważ jest zbyt duża.</span><span class="sxs-lookup"><span data-stu-id="1057d-117">The variable `lng` now contains a value that cannot be stored in the variable `i` because it is too large.</span></span> <span data-ttu-id="1057d-118">Jeśli przekonwertujemy tę wartość na typ int, będziemy tracić część naszych danych, a przekonwertowana wartość nie będzie taka sama jak wartość przed konwersją.</span><span class="sxs-lookup"><span data-stu-id="1057d-118">If we were to convert this value to an int type we would be losing some of our data and the converted value would not be the same as the value before the conversion.</span></span>  
  
 <span data-ttu-id="1057d-119">Konwersja rozszerzająca byłaby przeciwieństwem konwersji zawężanej.</span><span class="sxs-lookup"><span data-stu-id="1057d-119">A widening conversion would be the opposite of a narrowing conversion.</span></span> <span data-ttu-id="1057d-120">Podczas rozszerzania konwersji Konwertujemy na typ danych, który zajmuje więcej miejsca do magazynowania w pamięci niż typ danych, z którego jest przeprowadzana konwersja.</span><span class="sxs-lookup"><span data-stu-id="1057d-120">With widening conversions, we are converting to a data type that occupies more storage space in memory than the data type we are converting from.</span></span> <span data-ttu-id="1057d-121">Oto przykład konwersji rozszerzającej:</span><span class="sxs-lookup"><span data-stu-id="1057d-121">Here is an example of a widening conversion:</span></span>  

```csharp
int i = 50;  
long lng = 100;  
lng = i;  
```

 <span data-ttu-id="1057d-122">Zwróć uwagę na różnice między tym przykładem kodu a pierwszym.</span><span class="sxs-lookup"><span data-stu-id="1057d-122">Notice the difference between this code sample and the first.</span></span> <span data-ttu-id="1057d-123">Tym razem zmienna `lng` znajduje się po lewej stronie operatora przypisania, więc jest to element docelowy tego przydziału.</span><span class="sxs-lookup"><span data-stu-id="1057d-123">This time the variable `lng` is on the left-hand side of the assignment operator, so it is the target of our assignment.</span></span> <span data-ttu-id="1057d-124">Aby można było wykonać przypisanie, kompilator musi niejawnie skonwertować zmienną `i`, która jest typu int, na typ Long.</span><span class="sxs-lookup"><span data-stu-id="1057d-124">Before the assignment can be made, the compiler must implicitly convert the variable `i`, which is of type int, to type long.</span></span> <span data-ttu-id="1057d-125">Jest to konwersja rozszerzająca, ponieważ Konwertujemy z typu, który zajmuje 4 bajty pamięci (int) do typu, który zajmuje 8 bajtów pamięci (Long).</span><span class="sxs-lookup"><span data-stu-id="1057d-125">This is a widening conversion since we are converting from a type that occupies 4 bytes of memory (an int) to a type that occupies 8 bytes of memory (a long).</span></span> <span data-ttu-id="1057d-126">Niejawne konwersje rozszerzające są dozwolone, ponieważ nie ma potencjalnej utraty danych.</span><span class="sxs-lookup"><span data-stu-id="1057d-126">Implicit widening conversions are allowed because there is no potential loss of data.</span></span> <span data-ttu-id="1057d-127">Każda wartość, która może być przechowywana w int, może być również przechowywana w długim.</span><span class="sxs-lookup"><span data-stu-id="1057d-127">Any value that can be stored in an int can also be stored in a long.</span></span>  
  
 <span data-ttu-id="1057d-128">Wiemy, że niejawne konwersje nie są dozwolone, więc aby można było skompilować ten kod, musimy jawnie skonwertować typ danych.</span><span class="sxs-lookup"><span data-stu-id="1057d-128">We know that implicit narrowing conversions are not allowed, so to be able to compile this code we need to explicitly convert the data type.</span></span> <span data-ttu-id="1057d-129">Konwersje jawne są wykonywane przy użyciu rzutowania.</span><span class="sxs-lookup"><span data-stu-id="1057d-129">Explicit conversions are done using casting.</span></span> <span data-ttu-id="1057d-130">Rzutowanie jest terminem używanym w C# do opisywania konwersji jednego typu danych na inny.</span><span class="sxs-lookup"><span data-stu-id="1057d-130">Casting is the term used in C# to describe converting one data type to another.</span></span> <span data-ttu-id="1057d-131">Aby uzyskać kod do skompilowania, musimy użyć następującej składni:</span><span class="sxs-lookup"><span data-stu-id="1057d-131">To get the code to compile we would need to use the following syntax:</span></span>  

```csharp
int i = 50;  
long lng = 100;  
i = (int) lng;   // cast to int  
```

 <span data-ttu-id="1057d-132">Trzeci wiersz kodu nakazuje kompilatorowi jawne przekonwertowanie zmiennej `lng`, która jest typu Long na int przed przypisaniem.</span><span class="sxs-lookup"><span data-stu-id="1057d-132">The third line of code tells the compiler to explicitly convert the variable `lng`, which is of type long, to an int before making the assignment.</span></span> <span data-ttu-id="1057d-133">Należy pamiętać, że w przypadku konwersji z wąskim użyciem istnieje potencjalna utrata danych.</span><span class="sxs-lookup"><span data-stu-id="1057d-133">Remember that with a narrowing conversion, there is a potential loss of data.</span></span> <span data-ttu-id="1057d-134">Konwersje wąskie powinny być używane z zachowaniem ostrożności, a nawet w przypadku kompilowania kodu można uzyskać nieoczekiwane wyniki w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="1057d-134">Narrowing conversions should be used with caution and even though the code will compile you may get unexpected results at run-time.</span></span>  
  
 <span data-ttu-id="1057d-135">Ta dyskusja dotyczy tylko typów wartości.</span><span class="sxs-lookup"><span data-stu-id="1057d-135">This discussion has only been for value types.</span></span> <span data-ttu-id="1057d-136">Podczas pracy z typami wartości Pracujesz bezpośrednio z danymi przechowywanymi w zmiennej.</span><span class="sxs-lookup"><span data-stu-id="1057d-136">When working with value types you work directly with the data stored in the variable.</span></span> <span data-ttu-id="1057d-137">Jednak .NET Framework ma również typy referencyjne.</span><span class="sxs-lookup"><span data-stu-id="1057d-137">However, the .NET Framework also has reference types.</span></span> <span data-ttu-id="1057d-138">Podczas pracy z typami referencyjnymi pracujesz z odwołaniem do zmiennej, a nie rzeczywistymi danymi.</span><span class="sxs-lookup"><span data-stu-id="1057d-138">When working with reference types you are working with a reference to a variable, not the actual data.</span></span> <span data-ttu-id="1057d-139">Przykłady typów odwołań to klasy, interfejsy i tablice.</span><span class="sxs-lookup"><span data-stu-id="1057d-139">Examples of reference types would be classes, interfaces and arrays.</span></span> <span data-ttu-id="1057d-140">Nie można jawnie lub jawnie skonwertować jednego typu odwołania na inny, chyba że kompilator zezwala na określoną konwersję lub zaimplementowano odpowiednie operatory konwersji.</span><span class="sxs-lookup"><span data-stu-id="1057d-140">You cannot implicitly or explicitly convert one reference type to another unless the compiler allows the specific conversion or the appropriate conversion operators are implemented.</span></span>  
  
 <span data-ttu-id="1057d-141">Poniższy przykład generuje CS0029:</span><span class="sxs-lookup"><span data-stu-id="1057d-141">The following sample generates CS0029:</span></span>  

```csharp
// CS0029.cs  
public class MyInt  
{  
    private int x = 0;
  
    // Uncomment this conversion routine to resolve CS0029  
    /*  
    public static implicit operator int(MyInt i)  
    {  
       return i.x;  
    }  
    */  
  
    public static void Main()  
   {  
      MyInt myInt = new MyInt();  
      int i = myInt; // CS0029  
   }  
}  
```

## <a name="see-also"></a><span data-ttu-id="1057d-142">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="1057d-142">See also</span></span>

- [<span data-ttu-id="1057d-143">Zdefiniowane przez użytkownika operatory konwersji</span><span class="sxs-lookup"><span data-stu-id="1057d-143">User-defined conversion operators</span></span>](../operators/user-defined-conversion-operators.md)
