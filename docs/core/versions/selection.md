---
title: Wybierz wersję .NET Core, której chcesz użyć
description: Dowiedz się, jak program .NET Core automatycznie wyszukuje i wybiera wersje środowiska wykonawczego dla programu. Ponadto w tym artykule uczy się, jak wymusić określoną wersję.
author: thraka
ms.author: adegeo
ms.date: 03/24/2020
ms.openlocfilehash: 26aecdf2bf3ebd033e80eec26159eb9fa3cd54dd
ms.sourcegitcommit: 59e36e65ac81cdd094a5a84617625b2a0ff3506e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/27/2020
ms.locfileid: "80345158"
---
# <a name="select-the-net-core-version-to-use"></a><span data-ttu-id="34776-104">Wybierz wersję .NET Core, której chcesz użyć</span><span class="sxs-lookup"><span data-stu-id="34776-104">Select the .NET Core version to use</span></span>

<span data-ttu-id="34776-105">W tym artykule opisano zasady używane przez narzędzia .NET Core, zestaw SDK i środowisko uruchomieniowe do wybierania wersji.</span><span class="sxs-lookup"><span data-stu-id="34776-105">This article explains the policies used by the .NET Core tools, SDK, and runtime for selecting versions.</span></span> <span data-ttu-id="34776-106">Te zasady zapewniają równowagę między uruchamianiem aplikacji przy użyciu określonych wersji i ułatwienie uaktualniania maszyn deweloperów i użytkowników końcowych.</span><span class="sxs-lookup"><span data-stu-id="34776-106">These policies provide a balance between running applications using the specified versions and enabling ease of upgrading both developer and end-user machines.</span></span> <span data-ttu-id="34776-107">Te zasady wykonują następujące akcje:</span><span class="sxs-lookup"><span data-stu-id="34776-107">These policies perform the following actions:</span></span>

- <span data-ttu-id="34776-108">Łatwe i wydajne wdrażanie programu .NET Core, w tym aktualizacje zabezpieczeń i niezawodności.</span><span class="sxs-lookup"><span data-stu-id="34776-108">Easy and efficient deployment of .NET Core, including security and reliability updates.</span></span>
- <span data-ttu-id="34776-109">Użyj najnowszych narzędzi i poleceń niezależnie od docelowego środowiska uruchomieniowego.</span><span class="sxs-lookup"><span data-stu-id="34776-109">Use the latest tools and commands independent of target runtime.</span></span>

<span data-ttu-id="34776-110">Wybór wersji występuje:</span><span class="sxs-lookup"><span data-stu-id="34776-110">Version selection occurs:</span></span>

- <span data-ttu-id="34776-111">Po uruchomieniu polecenia SDK, [SDK używa najnowszej zainstalowanej wersji](#the-sdk-uses-the-latest-installed-version).</span><span class="sxs-lookup"><span data-stu-id="34776-111">When you run an SDK command, [the SDK uses the latest installed version](#the-sdk-uses-the-latest-installed-version).</span></span>
- <span data-ttu-id="34776-112">Podczas tworzenia zestawu, [monikerów struktury docelowej zdefiniować interfejsy API czasu kompilacji](#target-framework-monikers-define-build-time-apis).</span><span class="sxs-lookup"><span data-stu-id="34776-112">When you build an assembly, [target framework monikers define build time APIs](#target-framework-monikers-define-build-time-apis).</span></span>
- <span data-ttu-id="34776-113">Po uruchomieniu aplikacji .NET Core [aplikacje zależne od struktury docelowej są przesuwające się do przodu](#framework-dependent-apps-roll-forward).</span><span class="sxs-lookup"><span data-stu-id="34776-113">When you run a .NET Core application, [target framework dependent apps roll forward](#framework-dependent-apps-roll-forward).</span></span>
- <span data-ttu-id="34776-114">Podczas publikowania aplikacji niezależnej, [samodzielne wdrożenia obejmują wybrane środowisko uruchomieniowe](#self-contained-deployments-include-the-selected-runtime).</span><span class="sxs-lookup"><span data-stu-id="34776-114">When you publish a self-contained application, [self-contained deployments include the selected runtime](#self-contained-deployments-include-the-selected-runtime).</span></span>

<span data-ttu-id="34776-115">W pozostałej części tego dokumentu przeanalizowano te cztery scenariusze.</span><span class="sxs-lookup"><span data-stu-id="34776-115">The rest of this document examines those four scenarios.</span></span>

## <a name="the-sdk-uses-the-latest-installed-version"></a><span data-ttu-id="34776-116">SDK korzysta z najnowszej zainstalowanej wersji</span><span class="sxs-lookup"><span data-stu-id="34776-116">The SDK uses the latest installed version</span></span>

<span data-ttu-id="34776-117">Polecenia SDK `dotnet new` obejmują `dotnet run`i .</span><span class="sxs-lookup"><span data-stu-id="34776-117">SDK commands include `dotnet new` and `dotnet run`.</span></span> <span data-ttu-id="34776-118">Wierszka polecenia .NET Core musi wybrać `dotnet` wersję SDK dla każdego polecenia.</span><span class="sxs-lookup"><span data-stu-id="34776-118">The .NET Core CLI must choose an SDK version for every `dotnet` command.</span></span> <span data-ttu-id="34776-119">Domyślnie używa najnowszego SDK zainstalowanego na komputerze, nawet jeśli:</span><span class="sxs-lookup"><span data-stu-id="34776-119">It uses the latest SDK installed on the machine by default, even if:</span></span>

- <span data-ttu-id="34776-120">Projekt jest przeznaczony dla wcześniejszej wersji środowiska uruchomieniowego .NET Core.</span><span class="sxs-lookup"><span data-stu-id="34776-120">The project targets an earlier version of the .NET Core runtime.</span></span>
- <span data-ttu-id="34776-121">Najnowsza wersja .NET Core SDK jest wersją zapoznawczą.</span><span class="sxs-lookup"><span data-stu-id="34776-121">The latest version of the .NET Core SDK is a preview version.</span></span>

<span data-ttu-id="34776-122">Możesz korzystać z najnowszych funkcji i ulepszeń SDK podczas kierowania na wcześniejsze wersje środowiska uruchomieniowego .NET Core.</span><span class="sxs-lookup"><span data-stu-id="34776-122">You can take advantage of the latest SDK features and improvements while targeting earlier .NET Core runtime versions.</span></span> <span data-ttu-id="34776-123">Można kierować wiele wersji środowiska uruchomieniowego .NET Core na różnych projektach, przy użyciu tych samych narzędzi zestawu SDK dla wszystkich projektów.</span><span class="sxs-lookup"><span data-stu-id="34776-123">You can target multiple runtime versions of .NET Core on different projects, using the same SDK tools for all projects.</span></span>

<span data-ttu-id="34776-124">W rzadkich przypadkach może być konieczne użycie wcześniejszej wersji sdk.</span><span class="sxs-lookup"><span data-stu-id="34776-124">On rare occasions, you may need to use an earlier version of the SDK.</span></span> <span data-ttu-id="34776-125">Można określić tę wersję w pliku [ *global.json* ](../tools/global-json.md).</span><span class="sxs-lookup"><span data-stu-id="34776-125">You specify that version in a [*global.json* file](../tools/global-json.md).</span></span> <span data-ttu-id="34776-126">Zasady "użyj najnowszych" oznaczają, że używasz tylko *global.json,* aby określić wersję .NET Core SDK wcześniej niż najnowsza zainstalowana wersja.</span><span class="sxs-lookup"><span data-stu-id="34776-126">The "use latest" policy means you only use *global.json* to specify a .NET Core SDK version earlier than the latest installed version.</span></span>

<span data-ttu-id="34776-127">*global.json* można umieścić w dowolnym miejscu w hierarchii plików.</span><span class="sxs-lookup"><span data-stu-id="34776-127">*global.json* can be placed anywhere in the file hierarchy.</span></span> <span data-ttu-id="34776-128">Cli przeszukuje w górę z katalogu projektu dla pierwszego *global.json* znajdzie.</span><span class="sxs-lookup"><span data-stu-id="34776-128">The CLI searches upward from the project directory for the first *global.json* it finds.</span></span> <span data-ttu-id="34776-129">Użytkownik kontroluje, które projekty danego *global.json* ma zastosowanie do jego miejsca w systemie plików.</span><span class="sxs-lookup"><span data-stu-id="34776-129">You control which projects a given *global.json* applies to by its place in the file system.</span></span> <span data-ttu-id="34776-130">Plik .NET CLI wyszukuje plik *global.json* iterująco nawigując ścieżkę w górę od bieżącego katalogu roboczego.</span><span class="sxs-lookup"><span data-stu-id="34776-130">The .NET CLI searches for a *global.json* file iteratively navigating the path upward from the current working directory.</span></span> <span data-ttu-id="34776-131">Pierwszy znaleziony plik *global.json* określa używaną wersję.</span><span class="sxs-lookup"><span data-stu-id="34776-131">The first *global.json* file found specifies the version used.</span></span> <span data-ttu-id="34776-132">Jeśli ta wersja SDK jest zainstalowana, ta wersja jest używana.</span><span class="sxs-lookup"><span data-stu-id="34776-132">If that SDK version is installed, that version is used.</span></span> <span data-ttu-id="34776-133">Jeśli nie znaleziono pliku SDK określonego w *pliku global.json,* plik .NET CLI używa [reguł dopasowania](../tools/global-json.md#matching-rules) do wybierania zgodnego sdk lub nie powiedzie się, jeśli nie zostanie znaleziony.</span><span class="sxs-lookup"><span data-stu-id="34776-133">If the SDK specified in the *global.json* is not found, the .NET CLI uses [matching rules](../tools/global-json.md#matching-rules) to select a compatible SDK, or fails if none is found.</span></span>

<span data-ttu-id="34776-134">W poniższym przykładzie przedstawiono składnię *global.json:*</span><span class="sxs-lookup"><span data-stu-id="34776-134">The following example shows the *global.json* syntax:</span></span>

``` json
{
  "sdk": {
    "version": "3.0.0"
  }
}
```

<span data-ttu-id="34776-135">Proces wyboru wersji SDK jest:</span><span class="sxs-lookup"><span data-stu-id="34776-135">The process for selecting an SDK version is:</span></span>

1. <span data-ttu-id="34776-136">`dotnet`wyszukuje plik *global.json* iteracyjne odwrotnej nawigacji ścieżki w górę od bieżącego katalogu roboczego.</span><span class="sxs-lookup"><span data-stu-id="34776-136">`dotnet` searches for a *global.json* file iteratively reverse-navigating the path upward from the current working directory.</span></span>
1. <span data-ttu-id="34776-137">`dotnet`używa SDK określonego w pierwszym znalezionym *pliku global.json.*</span><span class="sxs-lookup"><span data-stu-id="34776-137">`dotnet` uses the SDK specified in the first *global.json* found.</span></span>
1. <span data-ttu-id="34776-138">`dotnet`używa najnowszego zainstalowanego SDK, jeśli nie zostanie znaleziony *plik global.json.*</span><span class="sxs-lookup"><span data-stu-id="34776-138">`dotnet` uses the latest installed SDK if no *global.json* is found.</span></span>

<span data-ttu-id="34776-139">Więcej informacji na temat wybierania wersji SDK można dowiedzieć się w sekcji [Reguły dopasowania](../tools/global-json.md#matching-rules) w artykule *global.json*.</span><span class="sxs-lookup"><span data-stu-id="34776-139">You can learn more about selecting an SDK version in the [Matching rules](../tools/global-json.md#matching-rules) section of the article on *global.json*.</span></span>

## <a name="target-framework-monikers-define-build-time-apis"></a><span data-ttu-id="34776-140">Monikery struktury docelowej definiują interfejsy API czasu kompilacji</span><span class="sxs-lookup"><span data-stu-id="34776-140">Target Framework Monikers define build time APIs</span></span>

<span data-ttu-id="34776-141">Tworzenie projektu względem interfejsów API zdefiniowanych w **monikeru platformy docelowej** (TFM).</span><span class="sxs-lookup"><span data-stu-id="34776-141">You build your project against APIs defined in a **Target Framework Moniker** (TFM).</span></span> <span data-ttu-id="34776-142">Określić [platformę docelową](../../standard/frameworks.md) w pliku projektu.</span><span class="sxs-lookup"><span data-stu-id="34776-142">You specify the [target framework](../../standard/frameworks.md) in the project file.</span></span> <span data-ttu-id="34776-143">Ustaw `TargetFramework` element w pliku projektu, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="34776-143">Set the `TargetFramework` element in your project file as shown in the following example:</span></span>

``` xml
<TargetFramework>netcoreapp3.0</TargetFramework>
```

<span data-ttu-id="34776-144">Możesz utworzyć projekt na wielu TFM.</span><span class="sxs-lookup"><span data-stu-id="34776-144">You may build your project against multiple TFMs.</span></span> <span data-ttu-id="34776-145">Ustawienie wielu struktur docelowych jest bardziej powszechne w przypadku bibliotek, ale można to zrobić również za pomocą aplikacji.</span><span class="sxs-lookup"><span data-stu-id="34776-145">Setting multiple target frameworks is more common for libraries but can be done with applications as well.</span></span> <span data-ttu-id="34776-146">Należy określić `TargetFrameworks` właściwość `TargetFramework`(liczba mnoga ).</span><span class="sxs-lookup"><span data-stu-id="34776-146">You specify a `TargetFrameworks` property (plural of `TargetFramework`).</span></span> <span data-ttu-id="34776-147">Struktury docelowe są rozdzielane średnikami, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="34776-147">The target frameworks are semicolon-delimited as shown in the following example:</span></span>

``` xml
<TargetFrameworks>netcoreapp3.0;net47</TargetFrameworks>
```

<span data-ttu-id="34776-148">Dany zestaw SDK obsługuje stały zestaw struktur, ograniczona do struktury docelowej środowiska wykonawczego, z jakim jest dostarczany.</span><span class="sxs-lookup"><span data-stu-id="34776-148">A given SDK supports a fixed set of frameworks, capped to the target framework of the runtime it ships with.</span></span> <span data-ttu-id="34776-149">Na przykład zestaw SDK .NET Core 3.0 zawiera środowisko uruchomieniowe .NET Core `netcoreapp3.0` 3.0, które jest implementacją struktury docelowej.</span><span class="sxs-lookup"><span data-stu-id="34776-149">For example, the .NET Core 3.0 SDK includes the .NET Core 3.0 runtime, which is an implementation of the `netcoreapp3.0` target framework.</span></span> <span data-ttu-id="34776-150">Zestaw SDK .NET Core 3.0 obsługuje `netcoreapp2.1`, `netcoreapp2.2`, `netcoreapp3.0`ale nie `netcoreapp3.1` (lub wyższy).</span><span class="sxs-lookup"><span data-stu-id="34776-150">The .NET Core 3.0 SDK supports `netcoreapp2.1`, `netcoreapp2.2`, `netcoreapp3.0`, but not `netcoreapp3.1` (or higher).</span></span> <span data-ttu-id="34776-151">Instalujesz sdk .NET Core 3.1, aby utworzyć dla `netcoreapp3.1`.</span><span class="sxs-lookup"><span data-stu-id="34776-151">You install the .NET Core 3.1 SDK to build for `netcoreapp3.1`.</span></span>

<span data-ttu-id="34776-152">.NET Standardowe struktury docelowe są również ograniczone do struktury docelowej środowiska wykonawczego sdk ships z.</span><span class="sxs-lookup"><span data-stu-id="34776-152">.NET Standard target frameworks are also capped to the target framework of the runtime the SDK ships with.</span></span> <span data-ttu-id="34776-153">SDK .NET Core 3.1 jest `netstandard2.1`ograniczony do .</span><span class="sxs-lookup"><span data-stu-id="34776-153">The .NET Core 3.1 SDK is capped to `netstandard2.1`.</span></span> <span data-ttu-id="34776-154">Aby uzyskać więcej informacji, zobacz [.NET Standard](../../standard/net-standard.md).</span><span class="sxs-lookup"><span data-stu-id="34776-154">For more information, see [.NET Standard](../../standard/net-standard.md).</span></span>

## <a name="framework-dependent-apps-roll-forward"></a><span data-ttu-id="34776-155">Aplikacje zależne od struktury są przewijane do przodu</span><span class="sxs-lookup"><span data-stu-id="34776-155">Framework-dependent apps roll forward</span></span>

<span data-ttu-id="34776-156">Po uruchomieniu aplikacji ze [`dotnet run`](../tools/dotnet-run.md)źródła z , z [**wdrożenia zależnego od struktury**](../deploying/index.md#publish-runtime-dependent) z `myapp.exe` `dotnet` [`dotnet myapp.dll`](../tools/dotnet.md#description), lub z pliku [**wykonywalnego zależnego od struktury**](../deploying/index.md#publish-runtime-dependent) z , plik wykonywalny jest **hostem** aplikacji.</span><span class="sxs-lookup"><span data-stu-id="34776-156">When you run an application from source with [`dotnet run`](../tools/dotnet-run.md), from a [**framework-dependent deployment**](../deploying/index.md#publish-runtime-dependent) with [`dotnet myapp.dll`](../tools/dotnet.md#description), or from a [**framework-dependent executable**](../deploying/index.md#publish-runtime-dependent) with `myapp.exe`, the `dotnet` executable is the **host** for the application.</span></span>

<span data-ttu-id="34776-157">Host wybiera najnowszą wersję poprawki zainstalowaną na komputerze.</span><span class="sxs-lookup"><span data-stu-id="34776-157">The host chooses the latest patch version installed on the machine.</span></span> <span data-ttu-id="34776-158">Na przykład jeśli `netcoreapp3.0` określono w pliku `3.0.4` projektu i jest zainstalowany najnowszy `3.0.4` czas wykonywania .NET, środowisko wykonawcze jest używany.</span><span class="sxs-lookup"><span data-stu-id="34776-158">For example, if you specified `netcoreapp3.0` in your project file, and `3.0.4` is the latest .NET runtime installed, the `3.0.4` runtime is used.</span></span>

<span data-ttu-id="34776-159">Jeśli nie `3.0.*` zostanie znaleziona `3.*` żadna dopuszczalna wersja, używana jest nowa wersja.</span><span class="sxs-lookup"><span data-stu-id="34776-159">If no acceptable `3.0.*` version is found, a new `3.*` version is used.</span></span> <span data-ttu-id="34776-160">Na przykład jeśli `netcoreapp3.0` określono `3.1.0` i tylko jest zainstalowany, `3.1.0` aplikacja jest uruchamiana przy użyciu środowiska wykonawczego.</span><span class="sxs-lookup"><span data-stu-id="34776-160">For example, if you specified `netcoreapp3.0` and only `3.1.0` is installed, the application runs using the `3.1.0` runtime.</span></span> <span data-ttu-id="34776-161">To zachowanie jest określane jako "wersja pomocnicza roll-forward."</span><span class="sxs-lookup"><span data-stu-id="34776-161">This behavior is referred to as "minor version roll-forward."</span></span> <span data-ttu-id="34776-162">Niższe wersje również nie będą brane pod uwagę.</span><span class="sxs-lookup"><span data-stu-id="34776-162">Lower versions also won't be considered.</span></span> <span data-ttu-id="34776-163">Gdy nie jest zainstalowany nie dopuszczalne środowisko uruchomieniowe, aplikacja nie zostanie uruchomiony.</span><span class="sxs-lookup"><span data-stu-id="34776-163">When no acceptable runtime is installed, the application won't run.</span></span>

<span data-ttu-id="34776-164">Kilka przykładów użycia demonstruje zachowanie, jeśli kierowane 3.0:</span><span class="sxs-lookup"><span data-stu-id="34776-164">A few usage examples demonstrate the behavior, if you target 3.0:</span></span>

- <span data-ttu-id="34776-165">✔️ określono 3.0.</span><span class="sxs-lookup"><span data-stu-id="34776-165">✔️ 3.0 is specified.</span></span> <span data-ttu-id="34776-166">3.0.5 jest najwyższą zainstalowaną wersją poprawki.</span><span class="sxs-lookup"><span data-stu-id="34776-166">3.0.5 is the highest patch version installed.</span></span> <span data-ttu-id="34776-167">3.0.5.</span><span class="sxs-lookup"><span data-stu-id="34776-167">3.0.5 is used.</span></span>
- <span data-ttu-id="34776-168">❌3.0.</span><span class="sxs-lookup"><span data-stu-id="34776-168">❌ 3.0 is specified.</span></span> <span data-ttu-id="34776-169">Nie są zainstalowane wersje 3.0.\*.</span><span class="sxs-lookup"><span data-stu-id="34776-169">No 3.0.\* versions are installed.</span></span> <span data-ttu-id="34776-170">2.1.1 jest najwyższym zainstalowanym czasem wykonywania.</span><span class="sxs-lookup"><span data-stu-id="34776-170">2.1.1 is the highest runtime installed.</span></span> <span data-ttu-id="34776-171">Zostanie wyświetlony komunikat o błędzie.</span><span class="sxs-lookup"><span data-stu-id="34776-171">An error message is displayed.</span></span>
- <span data-ttu-id="34776-172">✔️ określono 3.0.</span><span class="sxs-lookup"><span data-stu-id="34776-172">✔️ 3.0 is specified.</span></span> <span data-ttu-id="34776-173">Nie są zainstalowane wersje 3.0.\*.</span><span class="sxs-lookup"><span data-stu-id="34776-173">No 3.0.\* versions are installed.</span></span> <span data-ttu-id="34776-174">3.1.0 jest najwyższą zainstalowaną wersją środowiska wykonawczego.</span><span class="sxs-lookup"><span data-stu-id="34776-174">3.1.0 is the highest runtime version installed.</span></span> <span data-ttu-id="34776-175">3.1.0.</span><span class="sxs-lookup"><span data-stu-id="34776-175">3.1.0 is used.</span></span>
- <span data-ttu-id="34776-176">❌2.0.</span><span class="sxs-lookup"><span data-stu-id="34776-176">❌ 2.0 is specified.</span></span> <span data-ttu-id="34776-177">Nie są zainstalowane wersje 2.x.</span><span class="sxs-lookup"><span data-stu-id="34776-177">No 2.x versions are installed.</span></span> <span data-ttu-id="34776-178">3.0.0 jest najwyższym zainstalowanym czasem wykonywania.</span><span class="sxs-lookup"><span data-stu-id="34776-178">3.0.0 is the highest runtime installed.</span></span> <span data-ttu-id="34776-179">Zostanie wyświetlony komunikat o błędzie.</span><span class="sxs-lookup"><span data-stu-id="34776-179">An error message is displayed.</span></span>

<span data-ttu-id="34776-180">Wersja pomocnicza do przodu ma jeden efekt uboczny, który może mieć wpływ na użytkowników końcowych.</span><span class="sxs-lookup"><span data-stu-id="34776-180">Minor version roll-forward has one side-effect that may affect end users.</span></span> <span data-ttu-id="34776-181">Poniżej przedstawiono przykładowy scenariusz:</span><span class="sxs-lookup"><span data-stu-id="34776-181">Consider the following scenario:</span></span>

1. <span data-ttu-id="34776-182">Aplikacja określa, że wymagane jest 3.0.</span><span class="sxs-lookup"><span data-stu-id="34776-182">The application specifies that 3.0 is required.</span></span>
2. <span data-ttu-id="34776-183">Po uruchomieniu wersja 3.0.\* nie jest zainstalowana, jednak jest 3.1.0.</span><span class="sxs-lookup"><span data-stu-id="34776-183">When run, version 3.0.\* is not installed, however, 3.1.0 is.</span></span> <span data-ttu-id="34776-184">Zostanie użyta wersja 3.1.0.</span><span class="sxs-lookup"><span data-stu-id="34776-184">Version 3.1.0 will be used.</span></span>
3. <span data-ttu-id="34776-185">Później użytkownik instaluje 3.0.5 i uruchamia aplikację ponownie, 3.0.5 będzie teraz używany.</span><span class="sxs-lookup"><span data-stu-id="34776-185">Later, the user installs 3.0.5 and runs the application again, 3.0.5 will now be used.</span></span>

<span data-ttu-id="34776-186">Jest możliwe, że 3.0.5 i 3.1.0 zachowują się inaczej, szczególnie w scenariuszach, takich jak serializacji danych binarnych.</span><span class="sxs-lookup"><span data-stu-id="34776-186">It's possible that 3.0.5 and 3.1.0 behave differently, particularly for scenarios like serializing binary data.</span></span>

## <a name="self-contained-deployments-include-the-selected-runtime"></a><span data-ttu-id="34776-187">Samodzielne wdrożenia obejmują wybrany czas pracy</span><span class="sxs-lookup"><span data-stu-id="34776-187">Self-contained deployments include the selected runtime</span></span>

<span data-ttu-id="34776-188">Aplikację można opublikować jako [**samodzielną dystrybucję.**](../deploying/index.md#publish-self-contained)</span><span class="sxs-lookup"><span data-stu-id="34776-188">You can publish an application as a [**self-contained distribution**](../deploying/index.md#publish-self-contained).</span></span> <span data-ttu-id="34776-189">Takie podejście łączy środowisko uruchomieniowe .NET Core i biblioteki z aplikacją.</span><span class="sxs-lookup"><span data-stu-id="34776-189">This approach bundles the .NET Core runtime and libraries with your application.</span></span> <span data-ttu-id="34776-190">Samodzielne wdrożenia nie mają zależności od środowisk uruchomieniowych.</span><span class="sxs-lookup"><span data-stu-id="34776-190">Self-contained deployments don't have a dependency on runtime environments.</span></span> <span data-ttu-id="34776-191">Wybór wersji środowiska uruchomieniowego występuje w czasie publikowania, a nie w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="34776-191">Runtime version selection occurs at publishing time, not run time.</span></span>

<span data-ttu-id="34776-192">Proces publikowania wybiera najnowszą wersję poprawki danej rodziny wykonawczej.</span><span class="sxs-lookup"><span data-stu-id="34776-192">The publishing process selects the latest patch version of the given runtime family.</span></span> <span data-ttu-id="34776-193">Na przykład `dotnet publish` wybierze .NET Core 3.0.4, jeśli jest to najnowsza wersja poprawki w rodzinie środowiska uruchomieniowego .NET Core 3.0.</span><span class="sxs-lookup"><span data-stu-id="34776-193">For example, `dotnet publish` will select .NET Core 3.0.4 if it is the latest patch version in the .NET Core 3.0 runtime family.</span></span> <span data-ttu-id="34776-194">Struktura docelowa (w tym najnowsze zainstalowane poprawki zabezpieczeń) jest pakowana z aplikacją.</span><span class="sxs-lookup"><span data-stu-id="34776-194">The target framework (including the latest installed security patches) is packaged with the application.</span></span>

<span data-ttu-id="34776-195">Jest to błąd, jeśli minimalna wersja określona dla aplikacji nie jest spełniona.</span><span class="sxs-lookup"><span data-stu-id="34776-195">It's an error if the minimum version specified for an application isn't satisfied.</span></span> <span data-ttu-id="34776-196">`dotnet publish`wiąże się z najnowszą wersją poprawki środowiska uruchomieniowego (w ramach danej rodziny wersji major.minor).</span><span class="sxs-lookup"><span data-stu-id="34776-196">`dotnet publish` binds to the latest runtime patch version (within a given major.minor version family).</span></span> <span data-ttu-id="34776-197">`dotnet publish`nie obsługuje semantyki roll-forward `dotnet run`.</span><span class="sxs-lookup"><span data-stu-id="34776-197">`dotnet publish` doesn't support the roll-forward semantics of `dotnet run`.</span></span> <span data-ttu-id="34776-198">Aby uzyskać więcej informacji na temat poprawek i samodzielnych wdrożeń, zobacz artykuł dotyczący [wyboru poprawek środowiska wykonawczego](../deploying/runtime-patch-selection.md) podczas wdrażania aplikacji .NET Core.</span><span class="sxs-lookup"><span data-stu-id="34776-198">For more information about patches and self-contained deployments, see the article on [runtime patch selection](../deploying/runtime-patch-selection.md) in deploying .NET Core applications.</span></span>

<span data-ttu-id="34776-199">Samodzielne wdrożenia mogą wymagać określonej wersji poprawki.</span><span class="sxs-lookup"><span data-stu-id="34776-199">Self-contained deployments may require a specific patch version.</span></span> <span data-ttu-id="34776-200">Można zastąpić minimalną wersję poprawki środowiska uruchomieniowego (do wersji wyższej lub niższej) w pliku projektu, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="34776-200">You can override the minimum runtime patch version (to higher or lower versions) in the project file, as shown in the following example:</span></span>

``` xml
<RuntimeFrameworkVersion>3.0.4</RuntimeFrameworkVersion>
```

<span data-ttu-id="34776-201">Element `RuntimeFrameworkVersion` zastępuje domyślne zasady wersji.</span><span class="sxs-lookup"><span data-stu-id="34776-201">The `RuntimeFrameworkVersion` element  overrides the default version policy.</span></span> <span data-ttu-id="34776-202">W przypadku wdrożeń `RuntimeFrameworkVersion` samodzielnych określa *dokładną* wersję struktury środowiska wykonawczego.</span><span class="sxs-lookup"><span data-stu-id="34776-202">For self-contained deployments, the `RuntimeFrameworkVersion` specifies the *exact* runtime framework version.</span></span> <span data-ttu-id="34776-203">W przypadku aplikacji zależnych od struktury `RuntimeFrameworkVersion` określa *minimalną* wymaganą wersję struktury środowiska wykonawczego.</span><span class="sxs-lookup"><span data-stu-id="34776-203">For framework-dependent applications, the `RuntimeFrameworkVersion` specifies the *minimum* required runtime framework version.</span></span>

## <a name="see-also"></a><span data-ttu-id="34776-204">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="34776-204">See also</span></span>

- <span data-ttu-id="34776-205">[Pobierz i zainstaluj program .NET Core](../install/index.md).</span><span class="sxs-lookup"><span data-stu-id="34776-205">[Download and install .NET Core](../install/index.md).</span></span>
- <span data-ttu-id="34776-206">[Jak usunąć środowisko uruchomieniowe .NET Core i SDK](remove-runtime-sdk-versions.md).</span><span class="sxs-lookup"><span data-stu-id="34776-206">[How to remove the .NET Core Runtime and SDK](remove-runtime-sdk-versions.md).</span></span>
