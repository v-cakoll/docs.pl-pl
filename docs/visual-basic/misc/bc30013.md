---
title: "\"#ElseIf\", \"#Else\" lub \"#End If\" muszą być poprzedzone odpowiadającą jej instrukcją \"#If\""
ms.date: 07/20/2015
f1_keywords:
- vbc30013
- bc30013
helpviewer_keywords:
- BC30013
ms.assetid: 8fe2d23c-8b8f-46d8-90f2-7f8857ea43bb
ms.openlocfilehash: 88e4de9458ab2596486c6aeb7c0e61455dad73d1
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 08/22/2019
ms.locfileid: "69935239"
---
# <a name="elseif-else-or-end-if-must-be-preceded-by-a-matching-if"></a><span data-ttu-id="4de57-102">"#ElseIf", "#Else" lub "#End If" muszą być poprzedzone odpowiadającą jej instrukcją "#If"</span><span class="sxs-lookup"><span data-stu-id="4de57-102">'#ElseIf', '#Else', or '#End If' must be preceded by a matching '#If'</span></span>
<span data-ttu-id="4de57-103">`#ElseIf`, `#Else`, i `#End If` są dyrektywami kompilacji warunkowej.</span><span class="sxs-lookup"><span data-stu-id="4de57-103">`#ElseIf`, `#Else`, and `#End If` are conditional compilation directives.</span></span> <span data-ttu-id="4de57-104">,, Lub `#If` nie jest poprzedzony przez odpowiednią dyrektywę. `#End If` `#Else` `#ElseIf`</span><span class="sxs-lookup"><span data-stu-id="4de57-104">The `#ElseIf`, `#Else`, or `#End If` is not preceded by a corresponding `#If` directive.</span></span>  
  
 <span data-ttu-id="4de57-105">**Identyfikator błędu:** BC30013</span><span class="sxs-lookup"><span data-stu-id="4de57-105">**Error ID:** BC30013</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="4de57-106">Aby poprawić ten błąd</span><span class="sxs-lookup"><span data-stu-id="4de57-106">To correct this error</span></span>  
  
1. <span data-ttu-id="4de57-107">Upewnij się, że `#If` zamierzona nie jest oddzielona od klauzuli, w której występuje działający blok kompilacji warunkowej lub `#End If`nieprawidłowo umieszczony.</span><span class="sxs-lookup"><span data-stu-id="4de57-107">Check that the intended `#If` is not separated from the clause in question by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="4de57-108">W każdym `#Else` `#If` bloku dozwolony jest tylko jeden `#Else` , więc dwie kolejne dyrektywy powodują wystąpienie tego błędu.</span><span class="sxs-lookup"><span data-stu-id="4de57-108">Only one `#Else` is permitted in each `#If` block, so two successive `#Else` directives cause this error.</span></span>  
  
2. <span data-ttu-id="4de57-109">Sprawdź, czy w `#` poprzedniej `#If` dyrektywie nie brakuje wiodącego elementu.</span><span class="sxs-lookup"><span data-stu-id="4de57-109">Check that the leading `#` is not missing from an earlier `#If` directive.</span></span>  
  
3. <span data-ttu-id="4de57-110">Jeśli wszystko inne jest w porządku, Dodaj `#If` dyrektywę na początku bloku kompilacji warunkowej.</span><span class="sxs-lookup"><span data-stu-id="4de57-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="4de57-111">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="4de57-111">See also</span></span>

- [<span data-ttu-id="4de57-112">#If...Then...#Else, dyrektywy</span><span class="sxs-lookup"><span data-stu-id="4de57-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
