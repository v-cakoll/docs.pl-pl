---
title: Ograniczenia dotyczące parametrów typu — C# Przewodnik programowania
ms.custom: seodec18
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: f09f93f27aa4f50cfb7e09b9d6d4f98f22e1ac9a
ms.sourcegitcommit: 1e7ac70be1b4d89708c0d9552897515f2cbf52c4
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 07/24/2019
ms.locfileid: "68433556"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="297e1-102">Ograniczenia dotyczące parametrów typu (C# Przewodnik programowania)</span><span class="sxs-lookup"><span data-stu-id="297e1-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="297e1-103">Ograniczenia informują kompilator o możliwościach, które argument typu musi zawierać.</span><span class="sxs-lookup"><span data-stu-id="297e1-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="297e1-104">Bez żadnych ograniczeń argument typu może być dowolnym typem.</span><span class="sxs-lookup"><span data-stu-id="297e1-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="297e1-105">Kompilator może przyjąć tylko elementy członkowskie <xref:System.Object?displayProperty=nameWithType>, które jest ostateczną klasą bazową dla dowolnego typu .NET.</span><span class="sxs-lookup"><span data-stu-id="297e1-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="297e1-106">Aby uzyskać więcej informacji, zobacz [Dlaczego należy używać ograniczeń](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="297e1-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="297e1-107">Jeśli kod klienta próbuje utworzyć wystąpienie klasy przy użyciu typu, który nie jest dozwolony przez ograniczenie, wynikiem jest błąd czasu kompilacji.</span><span class="sxs-lookup"><span data-stu-id="297e1-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="297e1-108">Ograniczenia są określone za pomocą `where` kontekstowego słowa kluczowego.</span><span class="sxs-lookup"><span data-stu-id="297e1-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="297e1-109">W poniższej tabeli wymieniono siedem typów ograniczeń:</span><span class="sxs-lookup"><span data-stu-id="297e1-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="297e1-110">Typu</span><span class="sxs-lookup"><span data-stu-id="297e1-110">Constraint</span></span>|<span data-ttu-id="297e1-111">Opis</span><span class="sxs-lookup"><span data-stu-id="297e1-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="297e1-112">Argument typu musi być typem wartości.</span><span class="sxs-lookup"><span data-stu-id="297e1-112">The type argument must be a value type.</span></span> <span data-ttu-id="297e1-113">Wszystkie wartości typu z wyjątkiem <xref:System.Nullable%601> można określić.</span><span class="sxs-lookup"><span data-stu-id="297e1-113">Any value type except <xref:System.Nullable%601> can be specified.</span></span> <span data-ttu-id="297e1-114">Aby uzyskać więcej informacji na temat typów dopuszczających wartości null, zobacz [Typy dopuszczające wartości null](../nullable-types/index.md).</span><span class="sxs-lookup"><span data-stu-id="297e1-114">For more information about nullable types, see [Nullable types](../nullable-types/index.md).</span></span>|
|`where T : class`|<span data-ttu-id="297e1-115">Argument typu musi być typem referencyjnym.</span><span class="sxs-lookup"><span data-stu-id="297e1-115">The type argument must be a reference type.</span></span> <span data-ttu-id="297e1-116">To ograniczenie dotyczy również dowolnego typu klasy, interfejsu, delegata lub tablicy.</span><span class="sxs-lookup"><span data-stu-id="297e1-116">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="297e1-117">Argument typu musi być [typem](../../language-reference/builtin-types/unmanaged-types.md)niezarządzanym.</span><span class="sxs-lookup"><span data-stu-id="297e1-117">The type argument must be an [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span>|
|`where T : new()`|<span data-ttu-id="297e1-118">Typ argumentu musi mieć publicznego konstruktora bez parametrów.</span><span class="sxs-lookup"><span data-stu-id="297e1-118">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="297e1-119">Gdy jest używany z innymi ograniczeniami `new()` ograniczenie musi być określony jako ostatni.</span><span class="sxs-lookup"><span data-stu-id="297e1-119">When used together with other constraints, the `new()` constraint must be specified last.</span></span>|
|<span data-ttu-id="297e1-120">`where T :`Nazwa klasy bazowej >  *\<*</span><span class="sxs-lookup"><span data-stu-id="297e1-120">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="297e1-121">Argument typu musi być lub pochodzić od określonej klasy podstawowej.</span><span class="sxs-lookup"><span data-stu-id="297e1-121">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="297e1-122">`where T :` *nazwainterfejsu\<>*</span><span class="sxs-lookup"><span data-stu-id="297e1-122">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="297e1-123">Argument typu muszą być lub implementować określonego interfejsu.</span><span class="sxs-lookup"><span data-stu-id="297e1-123">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="297e1-124">Można określić wiele ograniczeń interfejsu.</span><span class="sxs-lookup"><span data-stu-id="297e1-124">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="297e1-125">Można też ogólnego ograniczający interfejsu.</span><span class="sxs-lookup"><span data-stu-id="297e1-125">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="297e1-126">Argumentu typu dostarczonego T musi być lub pochodzić od argument dostarczony dla U.</span><span class="sxs-lookup"><span data-stu-id="297e1-126">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

<span data-ttu-id="297e1-127">Niektóre ograniczenia wykluczają się wzajemnie.</span><span class="sxs-lookup"><span data-stu-id="297e1-127">Some of the constraints are mutually exclusive.</span></span> <span data-ttu-id="297e1-128">Wszystkie typy wartości muszą mieć dostępny Konstruktor bez parametrów.</span><span class="sxs-lookup"><span data-stu-id="297e1-128">All value types must have an accessible parameterless constructor.</span></span> <span data-ttu-id="297e1-129">Ograniczenie implikuje `new()` ograniczenie i `new()` ograniczenie nie może zostać połączone z `struct` ograniczeniem. `struct`</span><span class="sxs-lookup"><span data-stu-id="297e1-129">The `struct` constraint implies the `new()` constraint and the `new()` constraint cannot be combined with the `struct` constraint.</span></span> <span data-ttu-id="297e1-130">Ograniczenie implikuje `struct`ograniczenie. `unmanaged`</span><span class="sxs-lookup"><span data-stu-id="297e1-130">The `unmanaged` constraint implies the `struct` constraint.</span></span> <span data-ttu-id="297e1-131">Nie można łączyć `struct` `new()` ograniczenia z ograniczeniami ani. `unmanaged`</span><span class="sxs-lookup"><span data-stu-id="297e1-131">The `unmanaged` constraint cannot be combined with either the `struct` or `new()` constraints.</span></span>

## <a name="why-use-constraints"></a><span data-ttu-id="297e1-132">Dlaczego warto używać ograniczeń</span><span class="sxs-lookup"><span data-stu-id="297e1-132">Why use constraints</span></span>

<span data-ttu-id="297e1-133">Ograniczając parametr typu, zwiększa liczbę dozwolonych operacji i wywołań metod do tych, które są obsługiwane przez typ ograniczenia i wszystkie typy w hierarchii dziedziczenia.</span><span class="sxs-lookup"><span data-stu-id="297e1-133">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="297e1-134">Podczas projektowania klas ogólnych lub metod, jeśli będzie wykonywana jakakolwiek operacja na ogólnych składowych poza prostym przypisaniem lub wywołaniem jakichkolwiek metod nieobsługiwanych przez <xref:System.Object?displayProperty=nameWithType>program, należy zastosować ograniczenia do parametru typu.</span><span class="sxs-lookup"><span data-stu-id="297e1-134">When you design generic classes or methods, if you will be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you will have to apply constraints to the type parameter.</span></span> <span data-ttu-id="297e1-135">Na przykład ograniczenie klasy bazowej instruuje kompilator, że tylko obiekty tego typu lub pochodne z tego typu będą używane jako argumenty typu.</span><span class="sxs-lookup"><span data-stu-id="297e1-135">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="297e1-136">Gdy kompilator ma tę gwarancję, może zezwolić na wywoływanie metod tego typu w klasie generycznej.</span><span class="sxs-lookup"><span data-stu-id="297e1-136">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="297e1-137">Poniższy przykład kodu demonstruje funkcjonalność, którą można dodać do `GenericList<T>` klasy (w temacie [wprowadzenie do typów ogólnych](introduction-to-generics.md)) przez zastosowanie ograniczenia klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="297e1-137">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](introduction-to-generics.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="297e1-138">Ograniczenie pozwala klasie generycznej używać `Employee.Name` właściwości.</span><span class="sxs-lookup"><span data-stu-id="297e1-138">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="297e1-139">Ograniczenie określa, że wszystkie elementy typu `T` mają gwarantowany `Employee` obiekt lub obiekt, który dziedziczy z `Employee`.</span><span class="sxs-lookup"><span data-stu-id="297e1-139">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="297e1-140">Do tego samego parametru typu można zastosować wiele ograniczeń, a same ograniczenia mogą być typami ogólnymi w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="297e1-140">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="297e1-141">Podczas stosowania `where T : class` ograniczenia, `==` należy unikać operatorów i `!=` w parametrze typu, ponieważ te operatory przetestuje tylko tożsamość referencyjną, a nie dla równości wartości.</span><span class="sxs-lookup"><span data-stu-id="297e1-141">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="297e1-142">To zachowanie występuje nawet wtedy, gdy te operatory są przeciążone w typie, który jest używany jako argument.</span><span class="sxs-lookup"><span data-stu-id="297e1-142">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="297e1-143">Poniższy kod ilustruje ten punkt; wynik ma wartość false <xref:System.String> , mimo że Klasa `==` przeciążuje operator.</span><span class="sxs-lookup"><span data-stu-id="297e1-143">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="297e1-144">Kompilator wie, że T jest typem referencyjnym w czasie kompilacji i musi używać domyślnych operatorów, które są prawidłowe dla wszystkich typów referencyjnych.</span><span class="sxs-lookup"><span data-stu-id="297e1-144">The compiler only knows that T is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="297e1-145">Jeśli konieczne jest przetestowanie pod kątem równości wartości, zalecanym sposobem jest również `where T : IEquatable<T>` zastosowanie `where T : IComparable<T>` ograniczenia or i zaimplementowanie interfejsu w dowolnej klasie, która będzie używana do konstruowania klasy generycznej.</span><span class="sxs-lookup"><span data-stu-id="297e1-145">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="297e1-146">Ograniczanie wielu parametrów</span><span class="sxs-lookup"><span data-stu-id="297e1-146">Constraining multiple parameters</span></span>

<span data-ttu-id="297e1-147">Można zastosować ograniczenia do wielu parametrów i wiele ograniczeń do jednego parametru, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="297e1-147">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="297e1-148">Niepowiązane parametry typu</span><span class="sxs-lookup"><span data-stu-id="297e1-148">Unbounded type parameters</span></span>

 <span data-ttu-id="297e1-149">Parametry typu, które nie mają żadnych ograniczeń, takich jak T w `SampleClass<T>{}`klasie publicznej, są nazywane niezwiązanymi parametrami typu.</span><span class="sxs-lookup"><span data-stu-id="297e1-149">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="297e1-150">Parametry typu niepowiązanego mają następujące reguły:</span><span class="sxs-lookup"><span data-stu-id="297e1-150">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="297e1-151">Operatory `!=` i`==` nie mogą być używane, ponieważ nie ma gwarancji, że argument typu konkretnego będzie obsługiwał te operatory.</span><span class="sxs-lookup"><span data-stu-id="297e1-151">The `!=` and `==` operators cannot be used because there is no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="297e1-152">Mogą być konwertowane do i z `System.Object` lub jawnie konwertowane na dowolny typ interfejsu.</span><span class="sxs-lookup"><span data-stu-id="297e1-152">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="297e1-153">Można je porównać z wartością [null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="297e1-153">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="297e1-154">Jeśli parametr niezwiązany jest porównywany z `null`, porównywanie zwróci wartość false, jeśli argument typu jest typem wartości.</span><span class="sxs-lookup"><span data-stu-id="297e1-154">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="297e1-155">Parametry typu jako ograniczenia</span><span class="sxs-lookup"><span data-stu-id="297e1-155">Type parameters as constraints</span></span>

<span data-ttu-id="297e1-156">Użycie parametru typu ogólnego jako ograniczenia jest przydatne, gdy funkcja członkowska z własnym parametrem typu musi ograniczyć ten parametr do parametru typu zawierającego typ, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="297e1-156">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="297e1-157">W poprzednim przykładzie, `T` jest ograniczeniem typu w kontekście `Add` metody i niezwiązanym parametrem typu `List` w kontekście klasy.</span><span class="sxs-lookup"><span data-stu-id="297e1-157">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="297e1-158">Parametry typu mogą być również używane jako ograniczenia w definicjach klasy generycznej.</span><span class="sxs-lookup"><span data-stu-id="297e1-158">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="297e1-159">Parametr type musi być zadeklarowany w nawiasach kątowych wraz z innymi parametrami typu:</span><span class="sxs-lookup"><span data-stu-id="297e1-159">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="297e1-160">Użyteczność parametrów typu jako ograniczenia z klasami generycznymi jest ograniczona, ponieważ kompilator może nie zajmować niczego informacji o parametrze typu, z `System.Object`wyjątkiem tego, że pochodzi od.</span><span class="sxs-lookup"><span data-stu-id="297e1-160">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="297e1-161">Użyj parametrów typu jako ograniczeń dotyczących klas ogólnych w scenariuszach, w których chcesz wymusić relację dziedziczenia między dwoma parametrami typu.</span><span class="sxs-lookup"><span data-stu-id="297e1-161">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="297e1-162">Niezarządzany warunek ograniczający</span><span class="sxs-lookup"><span data-stu-id="297e1-162">Unmanaged constraint</span></span>

<span data-ttu-id="297e1-163">Począwszy od C# 7,3, można użyć `unmanaged` ograniczenia, aby określić, że parametr typu musi być typem niezarządzanym. [](../../language-reference/builtin-types/unmanaged-types.md)</span><span class="sxs-lookup"><span data-stu-id="297e1-163">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be an [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="297e1-164">`unmanaged` Ograniczenie umożliwia zapisanie procedur wielokrotnego użytku w celu pracy z typami, które mogą być manipulowane jako bloki pamięci, jak pokazano w następującym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="297e1-164">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="297e1-165">Poprzednia metoda musi być skompilowana w `unsafe` kontekście, ponieważ `sizeof` używa operatora na typie, który nie jest znany jako typ wbudowany.</span><span class="sxs-lookup"><span data-stu-id="297e1-165">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="297e1-166">`unmanaged` Bez ograniczenia `sizeof` operator jest niedostępny.</span><span class="sxs-lookup"><span data-stu-id="297e1-166">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="297e1-167">Delegowanie ograniczeń</span><span class="sxs-lookup"><span data-stu-id="297e1-167">Delegate constraints</span></span>

<span data-ttu-id="297e1-168">Począwszy od C# 7,3, można użyć <xref:System.Delegate?displayProperty=nameWithType> lub <xref:System.MulticastDelegate?displayProperty=nameWithType> jako ograniczenie klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="297e1-168">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="297e1-169">Środowisko CLR zawsze zezwala na to ograniczenie, ale C# język nie jest dozwolony.</span><span class="sxs-lookup"><span data-stu-id="297e1-169">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="297e1-170">`System.Delegate` Ograniczenie pozwala napisać kod, który współpracuje z delegatami w sposób bezpieczny dla typu.</span><span class="sxs-lookup"><span data-stu-id="297e1-170">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="297e1-171">Poniższy kod definiuje metodę rozszerzenia, która łączy dwa Delegaty, pod warunkiem, że są tego samego typu:</span><span class="sxs-lookup"><span data-stu-id="297e1-171">The following code defines an extension method that combines two delegates provided they are the same type:</span></span>

[!code-csharp[using the delegate constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="297e1-172">Możesz użyć powyższej metody do łączenia delegatów, które są tego samego typu:</span><span class="sxs-lookup"><span data-stu-id="297e1-172">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="297e1-173">Usunięcie komentarza do ostatniego wiersza nie spowoduje skompilowania.</span><span class="sxs-lookup"><span data-stu-id="297e1-173">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="297e1-174">Oba `first`typy delegatów, ale są różnymi typami delegatów. `test`</span><span class="sxs-lookup"><span data-stu-id="297e1-174">Both `first` and `test` are delegate types, but they are different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="297e1-175">Ograniczenia wyliczeniowe</span><span class="sxs-lookup"><span data-stu-id="297e1-175">Enum constraints</span></span>

<span data-ttu-id="297e1-176">Począwszy od C# 7,3, można również określić <xref:System.Enum?displayProperty=nameWithType> typ jako ograniczenie klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="297e1-176">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="297e1-177">Środowisko CLR zawsze zezwala na to ograniczenie, ale C# język nie jest dozwolony.</span><span class="sxs-lookup"><span data-stu-id="297e1-177">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="297e1-178">Typy ogólne przy `System.Enum` użyciu zapewniają programowanie bezpiecznego typu w celu buforowania wyników z używania metod statycznych w `System.Enum`.</span><span class="sxs-lookup"><span data-stu-id="297e1-178">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="297e1-179">Poniższy przykład odnajduje wszystkie prawidłowe wartości dla typu wyliczeniowego, a następnie tworzy słownik, który mapuje te wartości na jego reprezentację w postaci ciągu.</span><span class="sxs-lookup"><span data-stu-id="297e1-179">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="297e1-180">Używane metody wykorzystują odbicie, które mają wpływ na wydajność.</span><span class="sxs-lookup"><span data-stu-id="297e1-180">The methods used make use of reflection, which has performance implications.</span></span> <span data-ttu-id="297e1-181">Można wywołać tę metodę, aby utworzyć kolekcję, która jest buforowana i ponownie używana zamiast powtarzających się wywołań, które wymagają odbicia.</span><span class="sxs-lookup"><span data-stu-id="297e1-181">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="297e1-182">Można go użyć, jak pokazano w poniższym przykładzie, aby utworzyć Wyliczenie i skompilować słownik jego wartości i nazw:</span><span class="sxs-lookup"><span data-stu-id="297e1-182">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="297e1-183">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="297e1-183">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="297e1-184">Przewodnik programowania w języku C#</span><span class="sxs-lookup"><span data-stu-id="297e1-184">C# Programming Guide</span></span>](../../../csharp/programming-guide/index.md)
- [<span data-ttu-id="297e1-185">Wprowadzenie do typów ogólnych</span><span class="sxs-lookup"><span data-stu-id="297e1-185">Introduction to Generics</span></span>](../../../csharp/programming-guide/generics/index.md)
- [<span data-ttu-id="297e1-186">Klasy ogólne</span><span class="sxs-lookup"><span data-stu-id="297e1-186">Generic Classes</span></span>](../../../csharp/programming-guide/generics/generic-classes.md)
- [<span data-ttu-id="297e1-187">new, ograniczenie</span><span class="sxs-lookup"><span data-stu-id="297e1-187">new Constraint</span></span>](../../../csharp/language-reference/keywords/new-constraint.md)
