---
title: Co nowego w języku C# 7.0 - Przewodnik po językach C#
description: Zapoznaj się z omówieniem nowych funkcji w wersji 7.0 języka Języka C#.
ms.date: 02/20/2019
ms.assetid: fd41596d-d0c2-4816-b94d-c4d00a5d0243
ms.openlocfilehash: a6ac5c00ceb2ce8e5e56e2a86a8cde937d5108e2
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/14/2020
ms.locfileid: "79399694"
---
# <a name="whats-new-in-c-70"></a><span data-ttu-id="541bf-103">Co nowego w języku C# 7.0</span><span class="sxs-lookup"><span data-stu-id="541bf-103">What's new in C# 7.0</span></span>

<span data-ttu-id="541bf-104">C# 7.0 dodaje szereg nowych funkcji do języka Języka C#:</span><span class="sxs-lookup"><span data-stu-id="541bf-104">C# 7.0 adds a number of new features to the C# language:</span></span>

- [<span data-ttu-id="541bf-105">`out`Zmiennych</span><span class="sxs-lookup"><span data-stu-id="541bf-105">`out` variables</span></span>](#out-variables)
  - <span data-ttu-id="541bf-106">Można zadeklarować `out` wartości w wierszu jako argumenty do metody, w której są używane.</span><span class="sxs-lookup"><span data-stu-id="541bf-106">You can declare `out` values inline as arguments to the method where they're used.</span></span>
- [<span data-ttu-id="541bf-107">Krotek</span><span class="sxs-lookup"><span data-stu-id="541bf-107">Tuples</span></span>](#tuples)
  - <span data-ttu-id="541bf-108">Można tworzyć lekkie, nienazwane typy zawierające wiele pól publicznych.</span><span class="sxs-lookup"><span data-stu-id="541bf-108">You can create lightweight, unnamed types that contain multiple public fields.</span></span> <span data-ttu-id="541bf-109">Kompilatory i narzędzia IDE zrozumieć semantyki tych typów.</span><span class="sxs-lookup"><span data-stu-id="541bf-109">Compilers and IDE tools understand the semantics of these types.</span></span>
- [<span data-ttu-id="541bf-110">Odrzucenia</span><span class="sxs-lookup"><span data-stu-id="541bf-110">Discards</span></span>](#discards)
  - <span data-ttu-id="541bf-111">Odrzuty są tymczasowe, tylko do zapisu zmienne używane w przypisaniach, gdy nie dbasz o wartość przypisaną.</span><span class="sxs-lookup"><span data-stu-id="541bf-111">Discards are temporary, write-only variables used in assignments when you don't care about the value assigned.</span></span> <span data-ttu-id="541bf-112">Są one najbardziej przydatne podczas dekonstrukcji krotek i typów zdefiniowanych przez użytkownika, a także podczas wywoływania metod z `out` parametrami.</span><span class="sxs-lookup"><span data-stu-id="541bf-112">They're most useful when deconstructing tuples and user-defined types, as well as when calling methods with `out` parameters.</span></span>
- [<span data-ttu-id="541bf-113">Dopasowywanie wzorców</span><span class="sxs-lookup"><span data-stu-id="541bf-113">Pattern Matching</span></span>](#pattern-matching)
  - <span data-ttu-id="541bf-114">Logikę rozgałęzienia można utworzyć na podstawie dowolnych typów i wartości członków tych typów.</span><span class="sxs-lookup"><span data-stu-id="541bf-114">You can create branching logic based on arbitrary types and values of the members of those types.</span></span>
- [<span data-ttu-id="541bf-115">`ref`mieszkańców i zwraca</span><span class="sxs-lookup"><span data-stu-id="541bf-115">`ref` locals and returns</span></span>](#ref-locals-and-returns)
  - <span data-ttu-id="541bf-116">Metody zmiennych lokalnych i wartości zwracanych mogą być odwołaniami do innego magazynu.</span><span class="sxs-lookup"><span data-stu-id="541bf-116">Method local variables and return values can be references to other storage.</span></span>
- [<span data-ttu-id="541bf-117">Funkcje lokalne</span><span class="sxs-lookup"><span data-stu-id="541bf-117">Local Functions</span></span>](#local-functions)
  - <span data-ttu-id="541bf-118">Można zagnieździć funkcje wewnątrz innych funkcji, aby ograniczyć ich zakres i widoczność.</span><span class="sxs-lookup"><span data-stu-id="541bf-118">You can nest functions inside other functions to limit their scope and visibility.</span></span>
- [<span data-ttu-id="541bf-119">Więcej członków zabudowanych wyrazami</span><span class="sxs-lookup"><span data-stu-id="541bf-119">More expression-bodied members</span></span>](#more-expression-bodied-members)
  - <span data-ttu-id="541bf-120">Lista elementów członkowskich, które mogą być autorami przy użyciu wyrażeń wzrosła.</span><span class="sxs-lookup"><span data-stu-id="541bf-120">The list of members that can be authored using expressions has grown.</span></span>
- [<span data-ttu-id="541bf-121">`throw`Wyrażenia</span><span class="sxs-lookup"><span data-stu-id="541bf-121">`throw` Expressions</span></span>](#throw-expressions)
  - <span data-ttu-id="541bf-122">Można zgłaszać wyjątki w konstrukcjach kodu, `throw` które wcześniej nie były dozwolone, ponieważ była instrukcją.</span><span class="sxs-lookup"><span data-stu-id="541bf-122">You can throw exceptions in code constructs that previously weren't allowed because `throw` was a statement.</span></span>
- [<span data-ttu-id="541bf-123">Uogólnione typy zwrotów asynchronicznej</span><span class="sxs-lookup"><span data-stu-id="541bf-123">Generalized async return types</span></span>](#generalized-async-return-types)
  - <span data-ttu-id="541bf-124">Metody zadeklarowane `async` za pomocą modyfikatora `Task` `Task<T>`mogą zwracać inne typy oprócz i .</span><span class="sxs-lookup"><span data-stu-id="541bf-124">Methods declared with the `async` modifier can return other types in addition to `Task` and `Task<T>`.</span></span>
- [<span data-ttu-id="541bf-125">Ulepszenia składni literału liczbowego</span><span class="sxs-lookup"><span data-stu-id="541bf-125">Numeric literal syntax improvements</span></span>](#numeric-literal-syntax-improvements)
  - <span data-ttu-id="541bf-126">Nowe tokeny poprawiają czytelność dla stałych liczbowych.</span><span class="sxs-lookup"><span data-stu-id="541bf-126">New tokens improve readability for numeric constants.</span></span>

<span data-ttu-id="541bf-127">W dalszej części tego artykułu przedstawiono omówienie każdej funkcji.</span><span class="sxs-lookup"><span data-stu-id="541bf-127">The remainder of this article provides an overview of each feature.</span></span> <span data-ttu-id="541bf-128">Dla każdej funkcji dowiesz się, co za nią kryje.</span><span class="sxs-lookup"><span data-stu-id="541bf-128">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="541bf-129">Dowiesz się składni.</span><span class="sxs-lookup"><span data-stu-id="541bf-129">You'll learn the syntax.</span></span> <span data-ttu-id="541bf-130">Te funkcje można eksplorować w swoim środowisku za pomocą narzędzia globalnego: `dotnet try`</span><span class="sxs-lookup"><span data-stu-id="541bf-130">You can explore these features in your environment using the `dotnet try` global tool:</span></span>

1. <span data-ttu-id="541bf-131">Zainstaluj narzędzie globalne [dotnet-try.](https://github.com/dotnet/try/blob/master/README.md#setup)</span><span class="sxs-lookup"><span data-stu-id="541bf-131">Install the [dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) global tool.</span></span>
1. <span data-ttu-id="541bf-132">Klonuj repozytorium [dotnet/try-samples.](https://github.com/dotnet/try-samples)</span><span class="sxs-lookup"><span data-stu-id="541bf-132">Clone the [dotnet/try-samples](https://github.com/dotnet/try-samples) repository.</span></span>
1. <span data-ttu-id="541bf-133">Ustaw bieżący katalog na podkatalog *csharp7* dla repozytorium *próbek.*</span><span class="sxs-lookup"><span data-stu-id="541bf-133">Set the current directory to the *csharp7* subdirectory for the *try-samples* repository.</span></span>
1. <span data-ttu-id="541bf-134">Uruchom polecenie `dotnet try`.</span><span class="sxs-lookup"><span data-stu-id="541bf-134">Run `dotnet try`.</span></span>

## <a name="out-variables"></a><span data-ttu-id="541bf-135">`out`Zmiennych</span><span class="sxs-lookup"><span data-stu-id="541bf-135">`out` variables</span></span>

<span data-ttu-id="541bf-136">Istniejąca składnia `out` obsługująca parametry została ulepszona w tej wersji.</span><span class="sxs-lookup"><span data-stu-id="541bf-136">The existing syntax that supports `out` parameters has been improved in this version.</span></span> <span data-ttu-id="541bf-137">Można teraz `out` zadeklarować zmienne na liście argumentów wywołania metody, zamiast pisać oddzielną deklarację deklaracji:</span><span class="sxs-lookup"><span data-stu-id="541bf-137">You can now declare `out` variables in the argument list of a method call, rather than writing a separate declaration statement:</span></span>

[!code-csharp[OutVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVariableDeclarations "Out variable declarations")]

<span data-ttu-id="541bf-138">Można określić typ zmiennej `out` dla jasności, jak pokazano powyżej.</span><span class="sxs-lookup"><span data-stu-id="541bf-138">You may want to specify the type of the `out` variable for clarity, as shown above.</span></span> <span data-ttu-id="541bf-139">Jednak język obsługuje przy użyciu niejawnie wpisane zmiennej lokalnej:</span><span class="sxs-lookup"><span data-stu-id="541bf-139">However, the language does support using an implicitly typed local variable:</span></span>

[!code-csharp[OutVarVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVarVariableDeclarations "Implicitly typed Out variable")]

- <span data-ttu-id="541bf-140">Kod jest łatwiejszy do odczytania.</span><span class="sxs-lookup"><span data-stu-id="541bf-140">The code is easier to read.</span></span>
  - <span data-ttu-id="541bf-141">Deklarujesz zmienną out, w której jej używasz, a nie w innym wierszu powyżej.</span><span class="sxs-lookup"><span data-stu-id="541bf-141">You declare the out variable where you use it, not on another line above.</span></span>
- <span data-ttu-id="541bf-142">Nie ma potrzeby przypisywania wartości początkowej.</span><span class="sxs-lookup"><span data-stu-id="541bf-142">No need to assign an initial value.</span></span>
  - <span data-ttu-id="541bf-143">Deklarując zmienną, `out` w której jest używana w wywołaniu metody, nie można przypadkowo użyć go przed przypisanym.</span><span class="sxs-lookup"><span data-stu-id="541bf-143">By declaring the `out` variable where it's used in a method call, you can't accidentally use it before it is assigned.</span></span>

## <a name="tuples"></a><span data-ttu-id="541bf-144">Krotki</span><span class="sxs-lookup"><span data-stu-id="541bf-144">Tuples</span></span>

<span data-ttu-id="541bf-145">C# zapewnia bogatą składnię dla klas i struktur, który jest używany do wyjaśnienia intencji projektu.</span><span class="sxs-lookup"><span data-stu-id="541bf-145">C# provides a rich syntax for classes and structs that is used to explain your design intent.</span></span> <span data-ttu-id="541bf-146">Ale czasami ta bogata składnia wymaga dodatkowej pracy przy minimalnych korzyściach.</span><span class="sxs-lookup"><span data-stu-id="541bf-146">But sometimes that rich syntax requires extra work with minimal benefit.</span></span> <span data-ttu-id="541bf-147">Często można napisać metody, które wymagają prostej struktury zawierającej więcej niż jeden element danych.</span><span class="sxs-lookup"><span data-stu-id="541bf-147">You may often write methods that need a simple structure containing more than one data element.</span></span> <span data-ttu-id="541bf-148">Aby obsługiwać te scenariusze *krotek* zostały dodane do języka C#.</span><span class="sxs-lookup"><span data-stu-id="541bf-148">To support these scenarios *tuples* were added to C#.</span></span> <span data-ttu-id="541bf-149">Krotek są lekkie struktury danych, które zawierają wiele pól do reprezentowania elementów członkowskich danych.</span><span class="sxs-lookup"><span data-stu-id="541bf-149">Tuples are lightweight data structures that contain multiple fields to represent the data members.</span></span>
<span data-ttu-id="541bf-150">Pola nie są sprawdzane i nie można zdefiniować własnych metod</span><span class="sxs-lookup"><span data-stu-id="541bf-150">The fields aren't validated, and you can't define your own methods</span></span>

> [!NOTE]
> <span data-ttu-id="541bf-151">Tuples były dostępne przed C# 7.0, ale były nieefektywne i nie miał obsługi języka.</span><span class="sxs-lookup"><span data-stu-id="541bf-151">Tuples were available before C# 7.0, but they were inefficient and had no language support.</span></span>
> <span data-ttu-id="541bf-152">Oznaczało to, że elementy krotki `Item1` `Item2` można było odwoływać się tylko jako , i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="541bf-152">This meant that tuple elements could only be referenced as `Item1`, `Item2` and so on.</span></span> <span data-ttu-id="541bf-153">C# 7.0 wprowadza obsługę języka dla krotek, który umożliwia nazwy semantyczne dla pól krotki przy użyciu nowych, bardziej wydajnych typów krotki.</span><span class="sxs-lookup"><span data-stu-id="541bf-153">C# 7.0 introduces language support for tuples, which enables semantic names for the fields of a tuple using new, more efficient tuple types.</span></span>

<span data-ttu-id="541bf-154">Krotka można utworzyć, przypisując wartość do każdego elementu członkowskiego i opcjonalnie podając nazwy semantyczne każdemu członkowi krotki:</span><span class="sxs-lookup"><span data-stu-id="541bf-154">You can create a tuple by assigning a value to each member, and optionally providing semantic names to each of the members of the tuple:</span></span>

[!code-csharp[NamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#NamedTuple "Named tuple")]

<span data-ttu-id="541bf-155">Krotka `namedLetters` zawiera pola określane `Alpha` `Beta`jako i .</span><span class="sxs-lookup"><span data-stu-id="541bf-155">The `namedLetters` tuple contains fields referred to as `Alpha` and `Beta`.</span></span> <span data-ttu-id="541bf-156">Te nazwy istnieją tylko w czasie kompilacji i nie są zachowywane, na przykład podczas sprawdzania krotki przy użyciu odbicia w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="541bf-156">Those names exist only at compile time and aren't preserved, for example when inspecting the tuple using reflection at runtime.</span></span>

<span data-ttu-id="541bf-157">W przydziale krotki można również określić nazwy pól po prawej stronie przypisania:</span><span class="sxs-lookup"><span data-stu-id="541bf-157">In a tuple assignment, you can also specify the names of the fields on the right-hand side of the assignment:</span></span>

[!code-csharp[ImplicitNamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#ImplicitNamedTuple "Implicitly named tuple")]

<span data-ttu-id="541bf-158">Może się okazać, że chcesz rozpakować elementy członkowskie krotki, które zostały zwrócone z metody.</span><span class="sxs-lookup"><span data-stu-id="541bf-158">There may be times when you want to unpackage the members of a tuple that were returned from a method.</span></span>  <span data-ttu-id="541bf-159">Można to zrobić, deklarując oddzielne zmienne dla każdej z wartości w krotce.</span><span class="sxs-lookup"><span data-stu-id="541bf-159">You can do that by declaring separate variables for each of the values in the tuple.</span></span> <span data-ttu-id="541bf-160">To rozpakowanie nazywa *się dekonstrukcją* krotki:</span><span class="sxs-lookup"><span data-stu-id="541bf-160">This unpackaging is called *deconstructing* the tuple:</span></span>

[!code-csharp[CallingWithDeconstructor](~/samples/snippets/csharp/new-in-7/program.cs#CallingWithDeconstructor "Deconstructing a tuple")]

<span data-ttu-id="541bf-161">Można również podać podobną dekonstrukcję dla dowolnego typu w .NET.</span><span class="sxs-lookup"><span data-stu-id="541bf-161">You can also provide a similar deconstruction for any type in .NET.</span></span> <span data-ttu-id="541bf-162">Piszesz `Deconstruct` metodę jako element członkowski klasy.</span><span class="sxs-lookup"><span data-stu-id="541bf-162">You write a `Deconstruct` method as a member of the class.</span></span> <span data-ttu-id="541bf-163">Ta `Deconstruct` metoda zawiera `out` zestaw argumentów dla każdej z właściwości, które chcesz wyodrębnić.</span><span class="sxs-lookup"><span data-stu-id="541bf-163">That `Deconstruct` method provides a set of `out` arguments for each of the properties you want to extract.</span></span> <span data-ttu-id="541bf-164">Należy `Point` wziąć pod uwagę tę klasę, która `X` `Y` zapewnia metodę dekonstruktora, która wyodrębnia i współrzędne:</span><span class="sxs-lookup"><span data-stu-id="541bf-164">Consider this `Point` class that provides a deconstructor method that extracts the `X` and `Y` coordinates:</span></span>

[!code-csharp[PointWithDeconstruction](~/samples/snippets/csharp/new-in-7/point.cs#PointWithDeconstruction "Point with deconstruction method")]

<span data-ttu-id="541bf-165">Poszczególne pola można wyodrębnić, `Point` przypisując do krotki:</span><span class="sxs-lookup"><span data-stu-id="541bf-165">You can extract the individual fields by assigning a `Point` to a tuple:</span></span>

[!code-csharp[DeconstructPoint](~/samples/snippets/csharp/new-in-7/program.cs#DeconstructPoint "Deconstruct a point")]

<span data-ttu-id="541bf-166">Możesz dowiedzieć się więcej o krotek w [artykule krotek](../tuples.md).</span><span class="sxs-lookup"><span data-stu-id="541bf-166">You can learn more in depth about tuples in the [tuples article](../tuples.md).</span></span>

## <a name="discards"></a><span data-ttu-id="541bf-167">Odrzucenia</span><span class="sxs-lookup"><span data-stu-id="541bf-167">Discards</span></span>

<span data-ttu-id="541bf-168">Często podczas dekonstrukcji krotki lub `out` wywoływania metody z parametrami, jesteś zmuszony do zdefiniowania zmiennej, której wartość nie obchodzi i nie zamierzasz używać.</span><span class="sxs-lookup"><span data-stu-id="541bf-168">Often when deconstructing a tuple or calling a method with `out` parameters, you're forced to define a variable whose value you don't care about and don't intend to use.</span></span> <span data-ttu-id="541bf-169">C# dodaje obsługę *odrzutów* do obsługi tego scenariusza.</span><span class="sxs-lookup"><span data-stu-id="541bf-169">C# adds support for *discards* to handle this scenario.</span></span> <span data-ttu-id="541bf-170">Odrzut jest zmienną tylko do `_` zapisu, której nazwa jest (znak podkreślenia); można przypisać wszystkie wartości, które mają zostać odrzucone do pojedynczej zmiennej.</span><span class="sxs-lookup"><span data-stu-id="541bf-170">A discard is a write-only variable whose name is `_` (the underscore character); you can assign all of the values that you intend to discard to the single variable.</span></span> <span data-ttu-id="541bf-171">Odrzucenie jest jak zmienna bez przypisanych; oprócz instrukcji przypisania odrzutu nie można używać w kodzie.</span><span class="sxs-lookup"><span data-stu-id="541bf-171">A discard is like an unassigned variable; apart from the assignment statement, the discard can't be used in code.</span></span>

<span data-ttu-id="541bf-172">Odrzuty są obsługiwane w następujących scenariuszach:</span><span class="sxs-lookup"><span data-stu-id="541bf-172">Discards are supported in the following scenarios:</span></span>

- <span data-ttu-id="541bf-173">Podczas dekonstrukcji krotek lub typów zdefiniowanych przez użytkownika.</span><span class="sxs-lookup"><span data-stu-id="541bf-173">When deconstructing tuples or user-defined types.</span></span>
- <span data-ttu-id="541bf-174">Podczas wywoływania metod [z](../language-reference/keywords/out-parameter-modifier.md) out parametrów.</span><span class="sxs-lookup"><span data-stu-id="541bf-174">When calling methods with [out](../language-reference/keywords/out-parameter-modifier.md) parameters.</span></span>
- <span data-ttu-id="541bf-175">W operacji dopasowania wzorca z [is](../language-reference/keywords/is.md) i [switch](../language-reference/keywords/switch.md) instrukcji.</span><span class="sxs-lookup"><span data-stu-id="541bf-175">In a pattern matching operation with the [is](../language-reference/keywords/is.md) and [switch](../language-reference/keywords/switch.md) statements.</span></span>
- <span data-ttu-id="541bf-176">Jako identyfikator autonomiczny, gdy chcesz jawnie zidentyfikować wartość przypisania jako odrzucenie.</span><span class="sxs-lookup"><span data-stu-id="541bf-176">As a standalone identifier when you want to explicitly identify the value of an assignment as a discard.</span></span>

<span data-ttu-id="541bf-177">W poniższym `QueryCityDataForYears` przykładzie zdefiniowano metodę, która zwraca 6-krotki, która zawiera dane dla miasta przez dwa różne lata.</span><span class="sxs-lookup"><span data-stu-id="541bf-177">The following example defines a `QueryCityDataForYears` method that returns a 6-tuple that contains data for a city for two different years.</span></span> <span data-ttu-id="541bf-178">Wywołanie metody w przykładzie dotyczy tylko dwóch wartości populacji zwracanych przez metodę i w ten sposób traktuje pozostałe wartości w krotce jako odrzuty, gdy dekonstruuje krotkę.</span><span class="sxs-lookup"><span data-stu-id="541bf-178">The method call in the example is concerned only with the two population values returned by the method and so treats the remaining values in the tuple as discards when it deconstructs the tuple.</span></span>

[!code-csharp[Tuple-discard](~/samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="541bf-179">Aby uzyskać więcej informacji, zobacz [Odrzucanie](../discards.md).</span><span class="sxs-lookup"><span data-stu-id="541bf-179">For more information, see [Discards](../discards.md).</span></span>

## <a name="pattern-matching"></a><span data-ttu-id="541bf-180">Dopasowanie do wzorca</span><span class="sxs-lookup"><span data-stu-id="541bf-180">Pattern matching</span></span>

<span data-ttu-id="541bf-181">*Dopasowywanie wzorców* jest funkcją, która umożliwia implementanie wysyłania metody we właściwościach innych niż typ obiektu.</span><span class="sxs-lookup"><span data-stu-id="541bf-181">*Pattern matching* is a feature that allows you to implement method dispatch on properties other than the type of an object.</span></span> <span data-ttu-id="541bf-182">Prawdopodobnie znasz już wysyłanie metody na podstawie typu obiektu.</span><span class="sxs-lookup"><span data-stu-id="541bf-182">You're probably already familiar with method dispatch based on the type of an object.</span></span> <span data-ttu-id="541bf-183">W programowaniu obiektowym metody wirtualne i zastępowanie zapewniają składnię języka w celu zaimplementowania wysyłania metod na podstawie typu obiektu.</span><span class="sxs-lookup"><span data-stu-id="541bf-183">In object-oriented programming, virtual and override methods provide language syntax to implement method dispatching based on an object's type.</span></span> <span data-ttu-id="541bf-184">Klasy podstawowe i pochodne zapewniają różne implementacje.</span><span class="sxs-lookup"><span data-stu-id="541bf-184">Base and Derived classes provide different implementations.</span></span>
<span data-ttu-id="541bf-185">Wyrażenia dopasowywania wzorców rozszerzają tę koncepcję, dzięki czemu można łatwo zaimplementować podobne wzorce wysyłki dla typów i elementów danych, które nie są powiązane za pośrednictwem hierarchii dziedziczenia.</span><span class="sxs-lookup"><span data-stu-id="541bf-185">Pattern matching expressions extend this concept so that you can easily implement similar dispatch patterns for types and data elements that aren't related through an inheritance hierarchy.</span></span>

<span data-ttu-id="541bf-186">Dopasowywanie wzorców obsługuje `is` wyrażenia i `switch` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="541bf-186">Pattern matching supports `is` expressions and `switch` expressions.</span></span> <span data-ttu-id="541bf-187">Każdy umożliwia sprawdzanie obiektu i jego właściwości, aby ustalić, czy ten obiekt spełnia poszukiwany wzorzec.</span><span class="sxs-lookup"><span data-stu-id="541bf-187">Each enables inspecting an object and its properties to determine if that object satisfies the sought pattern.</span></span> <span data-ttu-id="541bf-188">Słowo kluczowe `when` służy do określania dodatkowych reguł do wzorca.</span><span class="sxs-lookup"><span data-stu-id="541bf-188">You use the `when` keyword to specify additional rules to the pattern.</span></span>

<span data-ttu-id="541bf-189">Wyrażenie `is` wzorca rozszerza znany [ `is` operator](../language-reference/keywords/is.md#pattern-matching-with-is) do kwerendy obiektu o jego typ ie i przypisać wynik w jednej instrukcji.</span><span class="sxs-lookup"><span data-stu-id="541bf-189">The `is` pattern expression extends the familiar [`is` operator](../language-reference/keywords/is.md#pattern-matching-with-is) to query an object about its type and assign the result in one instruction.</span></span> <span data-ttu-id="541bf-190">Poniższy kod sprawdza, czy `int`zmienna jest , a jeśli tak, dodaje ją do bieżącej sumy:</span><span class="sxs-lookup"><span data-stu-id="541bf-190">The following code checks if a variable is an `int`, and if so, adds it to the current sum:</span></span>

```csharp
if (input is int count)
    sum += count;
```

<span data-ttu-id="541bf-191">W poprzednim małym przykładzie przedstawiono ulepszenia `is` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="541bf-191">The preceding small example demonstrates the enhancements to the `is` expression.</span></span> <span data-ttu-id="541bf-192">Można przetestować typy wartości, a także typy odwołań i można przypisać pomyślny wynik do nowej zmiennej poprawnego typu.</span><span class="sxs-lookup"><span data-stu-id="541bf-192">You can test against value types as well as reference types, and you can assign the successful result to a new variable of the correct type.</span></span>

<span data-ttu-id="541bf-193">Wyrażenie dopasowania przełącznika ma znajomą składnię, `switch` na podstawie instrukcji już część języka C#.</span><span class="sxs-lookup"><span data-stu-id="541bf-193">The switch match expression has a familiar syntax, based on the `switch` statement already part of the C# language.</span></span> <span data-ttu-id="541bf-194">Zaktualizowana instrukcja switch ma kilka nowych konstrukcji:</span><span class="sxs-lookup"><span data-stu-id="541bf-194">The updated switch statement has several new constructs:</span></span>

- <span data-ttu-id="541bf-195">Typ zarządzania wyrażeniem `switch` nie jest już ograniczony `Enum` do `string`typów, typów, typów ani typu dającego wartość null odpowiadającego jednemu z tych typów.</span><span class="sxs-lookup"><span data-stu-id="541bf-195">The governing type of a `switch` expression is no longer restricted to integral types, `Enum` types, `string`, or a nullable type corresponding to one of those types.</span></span> <span data-ttu-id="541bf-196">Można użyć dowolnego typu.</span><span class="sxs-lookup"><span data-stu-id="541bf-196">Any type may be used.</span></span>
- <span data-ttu-id="541bf-197">Można przetestować typ `switch` wyrażenia w `case` każdej etykiecie.</span><span class="sxs-lookup"><span data-stu-id="541bf-197">You can test the type of the `switch` expression in each `case` label.</span></span> <span data-ttu-id="541bf-198">Podobnie jak `is` w przypadku wyrażenia, można przypisać nową zmienną do tego typu.</span><span class="sxs-lookup"><span data-stu-id="541bf-198">As with the `is` expression, you may assign a new variable to that type.</span></span>
- <span data-ttu-id="541bf-199">Można dodać `when` klauzulę do dalszych warunków testu na tej zmiennej.</span><span class="sxs-lookup"><span data-stu-id="541bf-199">You may add a `when` clause to further test conditions on that variable.</span></span>
- <span data-ttu-id="541bf-200">Kolejność `case` etykiet jest teraz ważna.</span><span class="sxs-lookup"><span data-stu-id="541bf-200">The order of `case` labels is now important.</span></span> <span data-ttu-id="541bf-201">Pierwsza gałąź do dopasowania jest wykonywana; inne są pomijane.</span><span class="sxs-lookup"><span data-stu-id="541bf-201">The first branch to match is executed; others are skipped.</span></span>

<span data-ttu-id="541bf-202">Poniższy kod pokazuje te nowe funkcje:</span><span class="sxs-lookup"><span data-stu-id="541bf-202">The following code demonstrates these new features:</span></span>

```csharp
public static int SumPositiveNumbers(IEnumerable<object> sequence)
{
    int sum = 0;
    foreach (var i in sequence)
    {
        switch (i)
        {
            case 0:
                break;
            case IEnumerable<int> childSequence:
            {
                foreach(var item in childSequence)
                    sum += (item > 0) ? item : 0;
                break;
            }
            case int n when n > 0:
                sum += n;
                break;
            case null:
                throw new NullReferenceException("Null found in sequence");
            default:
                throw new InvalidOperationException("Unrecognized type");
        }
    }
    return sum;
}
```

- <span data-ttu-id="541bf-203">`case 0:`jest znanym stałym wzorem.</span><span class="sxs-lookup"><span data-stu-id="541bf-203">`case 0:` is the familiar constant pattern.</span></span>
- <span data-ttu-id="541bf-204">`case IEnumerable<int> childSequence:`jest wzorem typu.</span><span class="sxs-lookup"><span data-stu-id="541bf-204">`case IEnumerable<int> childSequence:` is a type pattern.</span></span>
- <span data-ttu-id="541bf-205">`case int n when n > 0:`jest wzorem typu z `when` dodatkowym warunkiem.</span><span class="sxs-lookup"><span data-stu-id="541bf-205">`case int n when n > 0:` is a type pattern with an additional `when` condition.</span></span>
- <span data-ttu-id="541bf-206">`case null:`jest wzorcem zerowym.</span><span class="sxs-lookup"><span data-stu-id="541bf-206">`case null:` is the null pattern.</span></span>
- <span data-ttu-id="541bf-207">`default:`jest znanym przypadkiem domyślnym.</span><span class="sxs-lookup"><span data-stu-id="541bf-207">`default:` is the familiar default case.</span></span>

<span data-ttu-id="541bf-208">Możesz dowiedzieć się więcej o dopasowywaniu wzorców w [dopasowaniu wzorców w języku C#.](../pattern-matching.md)</span><span class="sxs-lookup"><span data-stu-id="541bf-208">You can learn more about pattern matching in [Pattern Matching in C#](../pattern-matching.md).</span></span>

## <a name="ref-locals-and-returns"></a><span data-ttu-id="541bf-209">Ref mieszkańców i zwraca</span><span class="sxs-lookup"><span data-stu-id="541bf-209">Ref locals and returns</span></span>

<span data-ttu-id="541bf-210">Ta funkcja umożliwia algorytmy, które używają i zwracają odwołania do zmiennych zdefiniowanych w innym miejscu.</span><span class="sxs-lookup"><span data-stu-id="541bf-210">This feature enables algorithms that use and return references to variables defined elsewhere.</span></span> <span data-ttu-id="541bf-211">Jednym z przykładów jest praca z dużymi macierzami i znalezienie pojedynczej lokalizacji o pewnych cechach.</span><span class="sxs-lookup"><span data-stu-id="541bf-211">One example is working with large matrices, and finding a single location with certain characteristics.</span></span> <span data-ttu-id="541bf-212">Następująca metoda zwraca **odwołanie** do tego magazynu w macierzy:</span><span class="sxs-lookup"><span data-stu-id="541bf-212">The following method returns a **reference** to that storage in the matrix:</span></span>

[!code-csharp[FindReturningRef](~/samples/snippets/csharp/new-in-7/MatrixSearch.cs#FindReturningRef "Find returning by reference")]

<span data-ttu-id="541bf-213">Można zadeklarować wartość zwracaną `ref` jako i zmodyfikować tę wartość w macierzy, jak pokazano w następującym kodzie:</span><span class="sxs-lookup"><span data-stu-id="541bf-213">You can declare the return value as a `ref` and modify that value in the matrix, as shown in the following code:</span></span>

[!code-csharp[AssignRefReturn](~/samples/snippets/csharp/new-in-7/Program.cs#AssignRefReturn "Assign ref return")]

<span data-ttu-id="541bf-214">Język C# ma kilka reguł, które chronią przed nadużywaniem `ref` miejscowych i zwraca:</span><span class="sxs-lookup"><span data-stu-id="541bf-214">The C# language has several rules that protect you from misusing the `ref` locals and returns:</span></span>

- <span data-ttu-id="541bf-215">Słowo `ref` kluczowe należy dodać do podpisu `return` metody i do wszystkich instrukcji w metodzie.</span><span class="sxs-lookup"><span data-stu-id="541bf-215">You must add the `ref` keyword to the method signature and to all `return` statements in a method.</span></span>
  - <span data-ttu-id="541bf-216">To sprawia, że jasne metoda zwraca przez odwołanie w całej metody.</span><span class="sxs-lookup"><span data-stu-id="541bf-216">That makes it clear the method returns by reference throughout the method.</span></span>
- <span data-ttu-id="541bf-217">A `ref return` może być przypisany do `ref` zmiennej wartości lub zmiennej.</span><span class="sxs-lookup"><span data-stu-id="541bf-217">A `ref return` may be assigned to a value variable, or a `ref` variable.</span></span>
  - <span data-ttu-id="541bf-218">Obiekt wywołujący określa, czy zwracana wartość jest kopiowana, czy nie.</span><span class="sxs-lookup"><span data-stu-id="541bf-218">The caller controls whether the return value is copied or not.</span></span> <span data-ttu-id="541bf-219">Pominięcie modyfikatora `ref` podczas przypisywania wartości zwracanej wskazuje, że obiekt wywołujący chce kopię wartości, a nie odwołanie do magazynu.</span><span class="sxs-lookup"><span data-stu-id="541bf-219">Omitting the `ref` modifier when assigning the return value indicates that the caller wants a copy of the value, not a reference to the storage.</span></span>
- <span data-ttu-id="541bf-220">Nie można przypisać wartości zwracanej standardowej `ref` metody do zmiennej lokalnej.</span><span class="sxs-lookup"><span data-stu-id="541bf-220">You can't assign a standard method return value to a `ref` local variable.</span></span>
  - <span data-ttu-id="541bf-221">To nie pozwala na oświadczenia, takie jak`ref int i = sequence.Count();`</span><span class="sxs-lookup"><span data-stu-id="541bf-221">That disallows statements like `ref int i = sequence.Count();`</span></span>
- <span data-ttu-id="541bf-222">Nie można zwrócić `ref` do zmiennej, której okres istnienia nie wykracza poza wykonanie metody.</span><span class="sxs-lookup"><span data-stu-id="541bf-222">You can't return a `ref` to a variable whose lifetime doesn't extend beyond the execution of the method.</span></span>
  - <span data-ttu-id="541bf-223">Oznacza to, że nie można zwrócić odwołanie do zmiennej lokalnej lub zmiennej o podobnym zakresie.</span><span class="sxs-lookup"><span data-stu-id="541bf-223">That means you can't return a reference to a local variable or a variable with a similar scope.</span></span>
- <span data-ttu-id="541bf-224">`ref`miejscowych i zwraca nie mogą być używane z metodami asynchronicznym.</span><span class="sxs-lookup"><span data-stu-id="541bf-224">`ref` locals and returns can't be used with async methods.</span></span>
  - <span data-ttu-id="541bf-225">Kompilator nie może wiedzieć, czy zmienna, do której istnieje odwołanie, została ustawiona na jej wartość końcową, gdy zwraca się metoda asynchroniczną.</span><span class="sxs-lookup"><span data-stu-id="541bf-225">The compiler can't know if the referenced variable has been set to its final value when the async method returns.</span></span>

<span data-ttu-id="541bf-226">Dodanie ref locals i ref zwraca umożliwia algorytmy, które są bardziej wydajne, unikając kopiowania wartości lub wykonywania operacji dereferencing wiele razy.</span><span class="sxs-lookup"><span data-stu-id="541bf-226">The addition of ref locals and ref returns enables algorithms that are more efficient by avoiding copying values, or performing dereferencing operations multiple times.</span></span>

<span data-ttu-id="541bf-227">Dodanie `ref` do wartości zwracanej jest [zmianą zgodną ze źródłem](version-update-considerations.md#source-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="541bf-227">Adding `ref` to the return value is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span> <span data-ttu-id="541bf-228">Istniejący kod kompiluje, ale wartość zwracana ref jest kopiowana po przypisaniu.</span><span class="sxs-lookup"><span data-stu-id="541bf-228">Existing code compiles, but the ref return value is copied when assigned.</span></span> <span data-ttu-id="541bf-229">Obiekty wywołujące muszą zaktualizować magazyn `ref` dla wartości zwracanej do zmiennej lokalnej do przechowywania zwrotu jako odwołanie.</span><span class="sxs-lookup"><span data-stu-id="541bf-229">Callers must update the storage for the return value to a `ref` local variable to store the return as a reference.</span></span>

<span data-ttu-id="541bf-230">Aby uzyskać więcej informacji, zobacz artykuł [ref słowa kluczowego.](../language-reference/keywords/ref.md)</span><span class="sxs-lookup"><span data-stu-id="541bf-230">For more information, see the [ref keyword](../language-reference/keywords/ref.md) article.</span></span>

## <a name="local-functions"></a><span data-ttu-id="541bf-231">Funkcje lokalne</span><span class="sxs-lookup"><span data-stu-id="541bf-231">Local functions</span></span>

<span data-ttu-id="541bf-232">Wiele projektów dla klas obejmują metody, które są wywoływane tylko z jednej lokalizacji.</span><span class="sxs-lookup"><span data-stu-id="541bf-232">Many designs for classes include methods that are called from only one location.</span></span> <span data-ttu-id="541bf-233">Te dodatkowe metody prywatne zachować każdą metodę małe i koncentruje.</span><span class="sxs-lookup"><span data-stu-id="541bf-233">These additional private methods keep each method small and focused.</span></span> <span data-ttu-id="541bf-234">*Funkcje lokalne* umożliwiają deklarowanie metod w kontekście innej metody.</span><span class="sxs-lookup"><span data-stu-id="541bf-234">*Local functions* enable you to declare methods inside the context of another method.</span></span> <span data-ttu-id="541bf-235">Funkcje lokalne ułatwiają czytelnikom klasy, aby zobaczyć, że metoda lokalna jest wywoływana tylko z kontekstu, w którym jest zadeklarowany.</span><span class="sxs-lookup"><span data-stu-id="541bf-235">Local functions make it easier for readers of the class to see that the local method is only called from the context in which it is declared.</span></span>

<span data-ttu-id="541bf-236">Istnieją dwa typowe przypadki użycia dla funkcji lokalnych: publiczne metody iteratorem i publiczne metody asynchroniczne.</span><span class="sxs-lookup"><span data-stu-id="541bf-236">There are two common use cases for local functions: public iterator methods and public async methods.</span></span> <span data-ttu-id="541bf-237">Oba typy metod generują kod, który zgłasza błędy później niż programiści mogą się spodziewać.</span><span class="sxs-lookup"><span data-stu-id="541bf-237">Both types of methods generate code that reports errors later than programmers might expect.</span></span> <span data-ttu-id="541bf-238">W metodach iteratora wszelkie wyjątki są przestrzegane tylko podczas wywoływania kodu, który wylicza zwróconą sekwencję.</span><span class="sxs-lookup"><span data-stu-id="541bf-238">In iterator methods, any exceptions are observed only when calling code that enumerates the returned sequence.</span></span> <span data-ttu-id="541bf-239">W metodach asynchronicznej wszelkie wyjątki są `Task` obserwowane tylko wtedy, gdy oczekuje się zwróconego.</span><span class="sxs-lookup"><span data-stu-id="541bf-239">In async methods, any exceptions are only observed when the returned `Task` is awaited.</span></span> <span data-ttu-id="541bf-240">W poniższym przykładzie przedstawiono oddzielenie sprawdzania poprawności parametrów od implementacji iteratorem przy użyciu funkcji lokalnej:</span><span class="sxs-lookup"><span data-stu-id="541bf-240">The following example demonstrates separating parameter validation from the iterator implementation using a local function:</span></span>

[!code-csharp[22_IteratorMethodLocal](~/samples/snippets/csharp/new-in-7/Iterator.cs#IteratorMethodLocal "Iterator method with local function")]

<span data-ttu-id="541bf-241">Tę samą technikę `async` można zastosować za pomocą metod, aby zapewnić, że wyjątki wynikające z sprawdzania poprawności argumentu są generowane przed rozpoczęciem pracy asynchronicznej:</span><span class="sxs-lookup"><span data-stu-id="541bf-241">The same technique can be employed with `async` methods to ensure that exceptions arising from argument validation are thrown before the asynchronous work begins:</span></span>

[!code-csharp[TaskExample](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#TaskExample "Task returning method with local function")]

> [!NOTE]
> <span data-ttu-id="541bf-242">Niektóre projekty, które są obsługiwane przez funkcje lokalne można również wykonać przy użyciu *wyrażeń lambda*.</span><span class="sxs-lookup"><span data-stu-id="541bf-242">Some of the designs that are supported by local functions could also be accomplished using *lambda expressions*.</span></span> <span data-ttu-id="541bf-243">Aby uzyskać więcej informacji, zobacz [Funkcje lokalne a wyrażenia lambda](../local-functions-vs-lambdas.md).</span><span class="sxs-lookup"><span data-stu-id="541bf-243">For more information, see [Local functions vs. lambda expressions](../local-functions-vs-lambdas.md).</span></span>

## <a name="more-expression-bodied-members"></a><span data-ttu-id="541bf-244">Więcej członków zabudowanych wyrazami</span><span class="sxs-lookup"><span data-stu-id="541bf-244">More expression-bodied members</span></span>

<span data-ttu-id="541bf-245">C# 6 wprowadzono [elementy członkowskie zabudowane wyrażeniem](csharp-6.md#expression-bodied-function-members) dla funkcji elementów członkowskich i właściwości tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="541bf-245">C# 6 introduced [expression-bodied members](csharp-6.md#expression-bodied-function-members) for member functions, and read-only properties.</span></span> <span data-ttu-id="541bf-246">C# 7.0 rozszerza dozwolonych elementów członkowskich, które mogą być implementowane jako wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="541bf-246">C# 7.0 expands the allowed members that can be implemented as expressions.</span></span> <span data-ttu-id="541bf-247">W języku C# 7.0 można zaimplementować *konstruktory*, *finalizatory*i `get` `set` akcesory na *właściwości* i *indeksatory*.</span><span class="sxs-lookup"><span data-stu-id="541bf-247">In C# 7.0, you can implement *constructors*, *finalizers*, and `get` and `set` accessors on *properties* and *indexers*.</span></span> <span data-ttu-id="541bf-248">Poniższy kod przedstawia przykłady każdego z nich:</span><span class="sxs-lookup"><span data-stu-id="541bf-248">The following code shows examples of each:</span></span>

[!code-csharp[ExpressionBodiedMembers](~/samples/snippets/csharp/new-in-7/expressionmembers.cs#ExpressionBodiedEverything "new expression-bodied members")]

> [!NOTE]
> <span data-ttu-id="541bf-249">W tym przykładzie nie wymaga finalizatora, ale jest wyświetlany do wykazania składni.</span><span class="sxs-lookup"><span data-stu-id="541bf-249">This example does not need a finalizer, but it is shown to demonstrate the syntax.</span></span> <span data-ttu-id="541bf-250">Nie należy implementować finalizatora w klasie, chyba że jest to konieczne do zwolnienia zasobów niezarządzanych.</span><span class="sxs-lookup"><span data-stu-id="541bf-250">You should not implement a finalizer in your class unless it is necessary to  release unmanaged resources.</span></span> <span data-ttu-id="541bf-251">Należy również rozważyć <xref:System.Runtime.InteropServices.SafeHandle> użycie klasy zamiast bezpośredniego zarządzania zasobami niezarządzanymi.</span><span class="sxs-lookup"><span data-stu-id="541bf-251">You should also consider using the <xref:System.Runtime.InteropServices.SafeHandle> class instead of managing unmanaged resources directly.</span></span>

<span data-ttu-id="541bf-252">Te nowe lokalizacje dla członków zabudowanych wyrażeniem stanowią ważny punkt kontrolny dla języka Języka C#: Te funkcje zostały wdrożone przez członków społeczności pracujących nad projektem [Roslyn](https://github.com/dotnet/Roslyn) typu open source.</span><span class="sxs-lookup"><span data-stu-id="541bf-252">These new locations for expression-bodied members represent an important milestone for the C# language: These features were implemented by community members working on the open-source [Roslyn](https://github.com/dotnet/Roslyn) project.</span></span>

<span data-ttu-id="541bf-253">Zmiana metody na element członkowski wyrażenia jest [zmianą zgodną z binarną](version-update-considerations.md#binary-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="541bf-253">Changing a method to an expression bodied member is a [binary compatible change](version-update-considerations.md#binary-compatible-changes).</span></span>

## <a name="throw-expressions"></a><span data-ttu-id="541bf-254">Wyrażenia throw</span><span class="sxs-lookup"><span data-stu-id="541bf-254">Throw expressions</span></span>

<span data-ttu-id="541bf-255">W języku `throw` C#, zawsze była instrukcja.</span><span class="sxs-lookup"><span data-stu-id="541bf-255">In C#, `throw` has always been a statement.</span></span> <span data-ttu-id="541bf-256">Ponieważ `throw` jest instrukcją, a nie wyrażenie, były konstrukcje C#, gdzie nie można go używać.</span><span class="sxs-lookup"><span data-stu-id="541bf-256">Because `throw` is a statement, not an expression, there were C# constructs where you couldn't use it.</span></span> <span data-ttu-id="541bf-257">Obejmowały one wyrażenia warunkowe, wyrażenia zerowe łączenia i niektóre wyrażenia lambda.</span><span class="sxs-lookup"><span data-stu-id="541bf-257">These included conditional expressions, null coalescing expressions, and some lambda expressions.</span></span> <span data-ttu-id="541bf-258">Dodanie elementów członkowskich zabudowanych wyrażeniedodaje `throw` więcej lokalizacji, gdzie wyrażenia będą przydatne.</span><span class="sxs-lookup"><span data-stu-id="541bf-258">The addition of expression-bodied members adds more locations where `throw` expressions would be useful.</span></span> <span data-ttu-id="541bf-259">Tak, aby można było napisać dowolną z tych konstrukcji, C# 7.0 wprowadza [*wyrażenia throw*](../language-reference/keywords/throw.md#the-throw-expression).</span><span class="sxs-lookup"><span data-stu-id="541bf-259">So that you can write any of these constructs, C# 7.0 introduces [*throw expressions*](../language-reference/keywords/throw.md#the-throw-expression).</span></span>

<span data-ttu-id="541bf-260">Ten dodatek ułatwia pisanie więcej kodu opartego na wyrażeniach.</span><span class="sxs-lookup"><span data-stu-id="541bf-260">This addition makes it easier to write more expression-based code.</span></span> <span data-ttu-id="541bf-261">Nie potrzebujesz dodatkowych instrukcji do sprawdzania błędów.</span><span class="sxs-lookup"><span data-stu-id="541bf-261">You don't need additional statements for error checking.</span></span>

## <a name="generalized-async-return-types"></a><span data-ttu-id="541bf-262">Uogólnione typy zwrotów asynchronicznej</span><span class="sxs-lookup"><span data-stu-id="541bf-262">Generalized async return types</span></span>

<span data-ttu-id="541bf-263">Zwracanie `Task` obiektu z metod asynchronicznej może wprowadzić wąskie gardła wydajności w niektórych ścieżkach.</span><span class="sxs-lookup"><span data-stu-id="541bf-263">Returning a `Task` object from async methods can introduce performance bottlenecks in certain paths.</span></span> <span data-ttu-id="541bf-264">`Task`jest typem odwołania, więc użycie go oznacza przydzielanie obiektu.</span><span class="sxs-lookup"><span data-stu-id="541bf-264">`Task` is a reference type, so using it means allocating an object.</span></span> <span data-ttu-id="541bf-265">W przypadkach, gdy metoda `async` zadeklarowana za pomocą modyfikatora zwraca wynik buforowany lub kończy synchronicznie, dodatkowe alokacje mogą stać się znacznym kosztem czasu w krytycznych sekcjach kodu dotyczących wydajności.</span><span class="sxs-lookup"><span data-stu-id="541bf-265">In cases where a method declared with the `async` modifier returns a cached result, or completes synchronously, the extra allocations can become a significant time cost in performance critical sections of code.</span></span> <span data-ttu-id="541bf-266">Może stać się kosztowne, jeśli te alokacje występują w ciasnych pętli.</span><span class="sxs-lookup"><span data-stu-id="541bf-266">It can become costly if those allocations occur in tight loops.</span></span>

<span data-ttu-id="541bf-267">Nowa funkcja języka oznacza, że typy zwracania `Task`metody `Task<T>`asynchronicznej nie są ograniczone do , i `void`.</span><span class="sxs-lookup"><span data-stu-id="541bf-267">The new language feature means that async method return types aren't limited to `Task`, `Task<T>`, and `void`.</span></span> <span data-ttu-id="541bf-268">Zwracany typ musi nadal spełniać wzorzec asynchronicznego, co oznacza, że `GetAwaiter` metoda musi być dostępna.</span><span class="sxs-lookup"><span data-stu-id="541bf-268">The returned type must still satisfy the async pattern, meaning a `GetAwaiter` method must be accessible.</span></span> <span data-ttu-id="541bf-269">Jako jeden konkretny `ValueTask` przykład typ został dodany do .NET, aby skorzystać z tej nowej funkcji języka:</span><span class="sxs-lookup"><span data-stu-id="541bf-269">As one concrete example, the `ValueTask` type has been added to .NET to make use of this new language feature:</span></span>

[!code-csharp[UsingValueTask](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#UsingValueTask "Using ValueTask")]

> [!NOTE]
> <span data-ttu-id="541bf-270">Należy dodać pakiet [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) NuGet w celu <xref:System.Threading.Tasks.ValueTask%601> użycia tego typu.</span><span class="sxs-lookup"><span data-stu-id="541bf-270">You need to add the NuGet package [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) in order to use the <xref:System.Threading.Tasks.ValueTask%601> type.</span></span>

<span data-ttu-id="541bf-271">To ulepszenie jest najbardziej przydatne dla `Task` autorów biblioteki, aby uniknąć przydzielania w stanie krytycznym kodu.</span><span class="sxs-lookup"><span data-stu-id="541bf-271">This enhancement is most useful for library authors to avoid allocating a `Task` in performance critical code.</span></span>

## <a name="numeric-literal-syntax-improvements"></a><span data-ttu-id="541bf-272">Ulepszenia składni literału liczbowego</span><span class="sxs-lookup"><span data-stu-id="541bf-272">Numeric literal syntax improvements</span></span>

<span data-ttu-id="541bf-273">Błędne odczytanie stałych liczbowych może utrudnić zrozumienie kodu podczas czytania go po raz pierwszy.</span><span class="sxs-lookup"><span data-stu-id="541bf-273">Misreading numeric constants can make it harder to understand code when reading it for the first time.</span></span> <span data-ttu-id="541bf-274">Maski bitowe lub inne wartości symboliczne są podatne na nieporozumienia.</span><span class="sxs-lookup"><span data-stu-id="541bf-274">Bit masks or other symbolic values are prone to misunderstanding.</span></span> <span data-ttu-id="541bf-275">C# 7.0 zawiera dwie nowe funkcje do zapisu liczb w najbardziej czytelny sposób dla zamierzonego zastosowania: *literały binarne*i *separatory cyfr*.</span><span class="sxs-lookup"><span data-stu-id="541bf-275">C# 7.0 includes two new features to write numbers in the most readable fashion for the intended use: *binary literals*, and *digit separators*.</span></span>

<span data-ttu-id="541bf-276">W tych przypadkach, gdy tworzysz maski bitowe lub gdy binarna reprezentacja liczby tworzy najbardziej czytelny kod, zapisz tę liczbę w formacie binarnym:</span><span class="sxs-lookup"><span data-stu-id="541bf-276">For those times when you're creating bit masks, or whenever a binary representation of a number makes the most readable code, write that number in binary:</span></span>

[!code-csharp[ThousandSeparators](~/samples/snippets/csharp/new-in-7/Program.cs#ThousandSeparators "Thousands separators")]

<span data-ttu-id="541bf-277">Na `0b` początku stałej wskazuje, że liczba jest zapisywana jako liczba binarna.</span><span class="sxs-lookup"><span data-stu-id="541bf-277">The `0b` at the beginning of the constant indicates that the number is written as a binary number.</span></span> <span data-ttu-id="541bf-278">Liczby binarne mogą się wydłużyć, więc często łatwiej jest `_` zobaczyć wzorce bitów, wprowadzając jako separator cyfr, jak pokazano powyżej w stałej binarnej.</span><span class="sxs-lookup"><span data-stu-id="541bf-278">Binary numbers can get long, so it's often easier to see the bit patterns by introducing the `_` as a digit separator, as shown above in the binary constant.</span></span> <span data-ttu-id="541bf-279">Separator cyfr może pojawić się w dowolnym miejscu w stałej.</span><span class="sxs-lookup"><span data-stu-id="541bf-279">The digit separator can appear anywhere in the constant.</span></span> <span data-ttu-id="541bf-280">W przypadku numerów 10 bazowych często używa się go jako separatora tysięcy:</span><span class="sxs-lookup"><span data-stu-id="541bf-280">For base 10 numbers, it is common to use it as a thousands separator:</span></span>

[!code-csharp[LargeIntegers](~/samples/snippets/csharp/new-in-7/Program.cs#LargeIntegers "Large integer")]

<span data-ttu-id="541bf-281">Separatora cyfr można `decimal`używać `float`z `double` , i typy, jak również:</span><span class="sxs-lookup"><span data-stu-id="541bf-281">The digit separator can be used with `decimal`, `float`, and `double` types as well:</span></span>

[!code-csharp[OtherConstants](~/samples/snippets/csharp/new-in-7/Program.cs#OtherConstants "non-integral constants")]

<span data-ttu-id="541bf-282">W sumie można zadeklarować stałe liczbowe o znacznie większej czytelności.</span><span class="sxs-lookup"><span data-stu-id="541bf-282">Taken together, you can declare numeric constants with much more readability.</span></span>
