---
title: Błąd kompilatora CS0121
ms.date: 08/14/2018
f1_keywords:
- CS0121
helpviewer_keywords:
- CS0121
ms.assetid: 316cb77e-a500-4129-ae1b-e68b9188fd3e
ms.openlocfilehash: b2a3a761056cb53b9afc746df0d5807a69c8b5ed
ms.sourcegitcommit: 7bc6887ab658550baa78f1520ea735838249345e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 01/03/2020
ms.locfileid: "75635980"
---
# <a name="compiler-error-cs0121"></a><span data-ttu-id="c056d-102">Błąd kompilatora CS0121</span><span class="sxs-lookup"><span data-stu-id="c056d-102">Compiler error CS0121</span></span>

<span data-ttu-id="c056d-103">Wywołanie jest niejednoznaczne między następującymi metodami lub właściwościami: "Metoda1" i "method2"</span><span class="sxs-lookup"><span data-stu-id="c056d-103">The call is ambiguous between the following methods or properties: 'method1' and 'method2'</span></span>

<span data-ttu-id="c056d-104">Ze względu na niejawną konwersję kompilator nie mógł wywołać jednej formy przeciążonej metody.</span><span class="sxs-lookup"><span data-stu-id="c056d-104">Due to implicit conversion, the compiler was not able to call one form of an overloaded method.</span></span> <span data-ttu-id="c056d-105">Ten błąd można rozwiązać w jeden z następujących sposobów:</span><span class="sxs-lookup"><span data-stu-id="c056d-105">You can resolve this error in one of the following ways:</span></span>

- <span data-ttu-id="c056d-106">Określ parametry metody w taki sposób, że niejawna konwersja nie jest wykonywana.</span><span class="sxs-lookup"><span data-stu-id="c056d-106">Specify the method parameters in such a way that implicit conversion does not take place.</span></span>
- <span data-ttu-id="c056d-107">Usuń wszystkie przeciążenia dla metody.</span><span class="sxs-lookup"><span data-stu-id="c056d-107">Remove all overloads for the method.</span></span>
- <span data-ttu-id="c056d-108">Rzutowanie na właściwy typ przed wywołaniem metody.</span><span class="sxs-lookup"><span data-stu-id="c056d-108">Cast to proper type before calling the method.</span></span>
- <span data-ttu-id="c056d-109">Użyj nazwanych argumentów.</span><span class="sxs-lookup"><span data-stu-id="c056d-109">Use named arguments.</span></span>

## <a name="example"></a><span data-ttu-id="c056d-110">Przykład</span><span class="sxs-lookup"><span data-stu-id="c056d-110">Example</span></span>

<span data-ttu-id="c056d-111">Poniższe przykłady generują błąd kompilatora CS0121:</span><span class="sxs-lookup"><span data-stu-id="c056d-111">The following examples generate compiler error CS0121:</span></span>

```csharp
public class Program
{
    static void f(int i, double d)
    {
    }

    static void f(double d, int i)
    {
    }

    public static void Main()
    {
        f(1, 1);   // CS0121

        // Try the following code instead:
        // f(1, 1.0);
        // or
        // f(1.0, 1);
        // or
        // f(1, (double)1);   // Cast and specify which method to call.
        // or
        // f(i: 1, 1);
        // or
        // f(d: 1, 1);
        
        // f(i: 1, d: 1); // This still gives CS0121
    }
}
```

## <a name="example"></a><span data-ttu-id="c056d-112">Przykład</span><span class="sxs-lookup"><span data-stu-id="c056d-112">Example</span></span>

```csharp
class Program2
{
    static int ol_invoked = 0;

    delegate int D1(int x);
    delegate T D1<T>(T x);
    delegate T D1<T, U>(U u);

    static void F(D1 d1) { ol_invoked = 1; }
    static void F<T>(D1<T> d1t) { ol_invoked = 2; }
    static void F<T, U>(D1<T, U> d1t) { ol_invoked = 3; }

    static int Test001()
    {
        F(delegate(int x) { return 1; }); // CS0121
        if (ol_invoked == 1)
            return 0;
        else
            return 1;
    }

    static int Main()
    {
        return Test001();
    }
}
```
