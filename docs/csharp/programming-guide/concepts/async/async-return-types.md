---
title: Asynchroniczne typy zwracane (C#)
ms.date: 05/29/2017
ms.assetid: ddb2539c-c898-48c1-ad92-245e4a996df8
ms.openlocfilehash: 3d3c7d610dd1287d2c7284a5edd9c92810a74dba
ms.sourcegitcommit: 5bbfe34a9a14e4ccb22367e57b57585c208cf757
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 09/17/2018
ms.locfileid: "45972495"
---
# <a name="async-return-types-c"></a><span data-ttu-id="41a1d-102">Asynchroniczne typy zwracane (C#)</span><span class="sxs-lookup"><span data-stu-id="41a1d-102">Async Return Types (C#)</span></span>
<span data-ttu-id="41a1d-103">Metody asynchroniczne mogą być zwracane typy:</span><span class="sxs-lookup"><span data-stu-id="41a1d-103">Async methods can have the following return types:</span></span>

- <span data-ttu-id="41a1d-104"><xref:System.Threading.Tasks.Task%601>, w metodzie asynchronicznej, która nie zwraca wartości.</span><span class="sxs-lookup"><span data-stu-id="41a1d-104"><xref:System.Threading.Tasks.Task%601>, for an async method that returns a value.</span></span> 
 
-  <span data-ttu-id="41a1d-105"><xref:System.Threading.Tasks.Task>, w metodzie asynchronicznej, która wykonuje operację, ale nie zwraca żadnej wartości.</span><span class="sxs-lookup"><span data-stu-id="41a1d-105"><xref:System.Threading.Tasks.Task>, for an async method that performs an operation but returns no value.</span></span>

- <span data-ttu-id="41a1d-106">`void`, procedury obsługi zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="41a1d-106">`void`, for an event handler.</span></span> 

- <span data-ttu-id="41a1d-107">Począwszy od C# 7.0, dowolny typ, który jest dostępny `GetAwaiter` metody.</span><span class="sxs-lookup"><span data-stu-id="41a1d-107">Starting with C# 7.0, any type that has an accessible `GetAwaiter` method.</span></span> <span data-ttu-id="41a1d-108">Obiekt zwrócony przez `GetAwaiter` musi implementować metody <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> interfejsu.</span><span class="sxs-lookup"><span data-stu-id="41a1d-108">The object returned by the `GetAwaiter` method must implement the <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> interface.</span></span>
  
<span data-ttu-id="41a1d-109">Aby uzyskać więcej informacji na temat metod asynchronicznych, zobacz [Asynchronous Programming with async i await (C#)](../../../../csharp/programming-guide/concepts/async/index.md).</span><span class="sxs-lookup"><span data-stu-id="41a1d-109">For more information about async methods, see [Asynchronous Programming with async and await (C#)](../../../../csharp/programming-guide/concepts/async/index.md).</span></span>  
  
<span data-ttu-id="41a1d-110">Każdy typ zwracany jest badany w jednej z następujących sekcji, a można znaleźć pełny przykład, który używa wszystkich trzech typów na końcu tego tematu.</span><span class="sxs-lookup"><span data-stu-id="41a1d-110">Each return type is examined in one of the following sections, and you can find a full example that uses all three types at the end of the topic.</span></span>  
  
##  <a name="BKMK_TaskTReturnType"></a> <span data-ttu-id="41a1d-111">Zadanie\<TResult\> zwracany typ</span><span class="sxs-lookup"><span data-stu-id="41a1d-111">Task\<TResult\> Return Type</span></span>  
<span data-ttu-id="41a1d-112"><xref:System.Threading.Tasks.Task%601> Typ zwracany jest używany w metodzie asynchronicznej, która zawiera [zwracają](../../../../csharp/language-reference/keywords/return.md) instrukcji (C#), w której operator ma typ `TResult`.</span><span class="sxs-lookup"><span data-stu-id="41a1d-112">The <xref:System.Threading.Tasks.Task%601> return type is used for an async method that contains a [return](../../../../csharp/language-reference/keywords/return.md) (C#) statement in which the operand has type `TResult`.</span></span>  
  
<span data-ttu-id="41a1d-113">W poniższym przykładzie `GetLeisureHours` metoda asynchroniczna `return` instrukcję, która zwraca liczbę całkowitą.</span><span class="sxs-lookup"><span data-stu-id="41a1d-113">In the following example, the `GetLeisureHours` async method contains a `return` statement that returns an integer.</span></span> <span data-ttu-id="41a1d-114">Dlatego deklaracja metody musi określać typ zwracany `Task<int>`.</span><span class="sxs-lookup"><span data-stu-id="41a1d-114">Therefore, the method declaration must specify a return type of `Task<int>`.</span></span>  <span data-ttu-id="41a1d-115"><xref:System.Threading.Tasks.Task.FromResult%2A> Metody asynchronicznej jest symbolem zastępczym dla operacji, która zwraca wartość typu ciąg.</span><span class="sxs-lookup"><span data-stu-id="41a1d-115">The <xref:System.Threading.Tasks.Task.FromResult%2A> async method is a placeholder for an operation that returns a string.</span></span>
  
[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns1.cs)]

<span data-ttu-id="41a1d-116">Podczas `GetLeisureHours` jest wywoływana z poziomu wyrażenia await w `ShowTodaysInfo` metodę, wyrażenie await pobiera wartość całkowitą (wartość `leisureHours`) jest przechowywana w zadaniu zwrócony przez `GetLeisureHours` metody.</span><span class="sxs-lookup"><span data-stu-id="41a1d-116">When `GetLeisureHours` is called from within an await expression in the `ShowTodaysInfo` method, the await expression retrieves the integer value (the value of `leisureHours`) that's stored in the task returned by the `GetLeisureHours` method.</span></span> <span data-ttu-id="41a1d-117">Aby uzyskać więcej informacji dotyczących wyrażeń oczekiwania, zobacz [await](../../../../csharp/language-reference/keywords/await.md).</span><span class="sxs-lookup"><span data-stu-id="41a1d-117">For more information about await expressions, see [await](../../../../csharp/language-reference/keywords/await.md).</span></span>  
  
<span data-ttu-id="41a1d-118">Można lepiej zrozumieć, jak to się dzieje oddzielając wywołanie `GetLeisureHours` ze stosowania `await`, jak pokazano w poniższym kodzie.</span><span class="sxs-lookup"><span data-stu-id="41a1d-118">You can better understand how this happens by separating the call to `GetLeisureHours` from the application of `await`, as the following code shows.</span></span> <span data-ttu-id="41a1d-119">Wywołanie metody `GetLeisureHours` , która nie jest oczekiwana natychmiast zwraca `Task<int>`, jak można oczekiwać od deklaracji metody.</span><span class="sxs-lookup"><span data-stu-id="41a1d-119">A call to method `GetLeisureHours` that isn't immediately awaited returns a `Task<int>`, as you would expect from the declaration of the method.</span></span> <span data-ttu-id="41a1d-120">Zadanie zostanie przypisane do `infoTask` zmiennej w przykładzie.</span><span class="sxs-lookup"><span data-stu-id="41a1d-120">The task is assigned to the `infoTask` variable in the example.</span></span> <span data-ttu-id="41a1d-121">Ponieważ `infoTask` jest <xref:System.Threading.Tasks.Task%601>, zawiera on <xref:System.Threading.Tasks.Task%601.Result> właściwości typu `TResult`.</span><span class="sxs-lookup"><span data-stu-id="41a1d-121">Because `infoTask` is a <xref:System.Threading.Tasks.Task%601>, it contains a <xref:System.Threading.Tasks.Task%601.Result> property of type `TResult`.</span></span> <span data-ttu-id="41a1d-122">W tym przypadku `TResult` reprezentuje typ liczby całkowitej.</span><span class="sxs-lookup"><span data-stu-id="41a1d-122">In this case, `TResult` represents an integer type.</span></span> <span data-ttu-id="41a1d-123">Gdy `await` jest stosowany do `infoTask`, wyrażenie czekania zwraca wynik zawartość <xref:System.Threading.Tasks.Task%601.Result%2A> właściwość `infoTask`.</span><span class="sxs-lookup"><span data-stu-id="41a1d-123">When `await` is applied to `infoTask`, the await expression evaluates to the contents of the <xref:System.Threading.Tasks.Task%601.Result%2A> property of `infoTask`.</span></span> <span data-ttu-id="41a1d-124">Wartość jest przypisywana do `ret` zmiennej.</span><span class="sxs-lookup"><span data-stu-id="41a1d-124">The value is assigned to the `ret` variable.</span></span>  
  
> [!IMPORTANT]
>  <span data-ttu-id="41a1d-125"><xref:System.Threading.Tasks.Task%601.Result%2A> Właściwość jest właściwością blokowania.</span><span class="sxs-lookup"><span data-stu-id="41a1d-125">The <xref:System.Threading.Tasks.Task%601.Result%2A> property is a blocking property.</span></span> <span data-ttu-id="41a1d-126">Jeśli spróbujesz się do niej dostęp przed zakończeniem jej zadania, wątek, który jest obecnie aktywny jest zablokowany do momentu ukończenia zadania i wartość jest dostępna.</span><span class="sxs-lookup"><span data-stu-id="41a1d-126">If you try to access it before its task is finished, the thread that's currently active is blocked until the task completes and the value is available.</span></span> <span data-ttu-id="41a1d-127">W większości przypadków należy dostęp do wartości przy użyciu `await` zamiast bezpośrednio dostęp do właściwości.</span><span class="sxs-lookup"><span data-stu-id="41a1d-127">In most cases, you should access the value by using `await` instead of accessing the property directly.</span></span> <br/> <span data-ttu-id="41a1d-128">Poprzedni przykład pobrać wartość <xref:System.Threading.Tasks.Task%601.Result%2A> właściwości do blokowania wątku głównego, aby `ShowTodaysInfo` metoda może zakończyć wykonywania przed aplikacji zakończył się.</span><span class="sxs-lookup"><span data-stu-id="41a1d-128">The previous example retrieved the value of the <xref:System.Threading.Tasks.Task%601.Result%2A> property to block the main thread so that the `ShowTodaysInfo` method could finish execution before the application ended.</span></span>  

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns1a.cs#1)]
  
##  <a name="BKMK_TaskReturnType"></a> <span data-ttu-id="41a1d-129">Zwracany typ Task</span><span class="sxs-lookup"><span data-stu-id="41a1d-129">Task Return Type</span></span>  
<span data-ttu-id="41a1d-130">Metody asynchroniczne, które nie zawierają `return` instrukcji lub które zawierają `return` instrukcję, która nie zwraca operandu są zazwyczaj ma typ zwracany <xref:System.Threading.Tasks.Task>.</span><span class="sxs-lookup"><span data-stu-id="41a1d-130">Async methods that don't contain a `return` statement or that contain a `return` statement that doesn't return an operand usually have a return type of <xref:System.Threading.Tasks.Task>.</span></span> <span data-ttu-id="41a1d-131">Takie metody zwracają `void` jeśli one były uruchamiane synchronicznie.</span><span class="sxs-lookup"><span data-stu-id="41a1d-131">Such methods return `void` if they run synchronously.</span></span> <span data-ttu-id="41a1d-132">Jeśli używasz <xref:System.Threading.Tasks.Task> zwracany typ metody asynchronicznej, wywoływania metody można użyć `await` operator wstrzymania uzupełniania wywołującego do momentu zakończenia wywołanej metody asynchronicznej.</span><span class="sxs-lookup"><span data-stu-id="41a1d-132">If you use a <xref:System.Threading.Tasks.Task> return type for an async method, a calling method can use an `await` operator to suspend the caller's completion until the called async method has finished.</span></span>  
  
<span data-ttu-id="41a1d-133">W poniższym przykładzie `WaitAndApologize` metoda asynchroniczna nie zawiera `return` instrukcji, więc metoda ta zwraca <xref:System.Threading.Tasks.Task> obiektu.</span><span class="sxs-lookup"><span data-stu-id="41a1d-133">In the following example, the `WaitAndApologize` async method doesn't contain a `return` statement, so the method returns a <xref:System.Threading.Tasks.Task> object.</span></span> <span data-ttu-id="41a1d-134">Dzięki temu `WaitAndApologize` oczekiwanie.</span><span class="sxs-lookup"><span data-stu-id="41a1d-134">This enables `WaitAndApologize` to be awaited.</span></span> <span data-ttu-id="41a1d-135">Należy pamiętać, że <xref:System.Threading.Tasks.Task> nie zawiera typu `Result` właściwość ponieważ nie zwraca żadnej wartości.</span><span class="sxs-lookup"><span data-stu-id="41a1d-135">Note that the <xref:System.Threading.Tasks.Task> type doesn't include a `Result` property because it has no return value.</span></span>  

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns2.cs)]  
  
<span data-ttu-id="41a1d-136">`WaitAndApologize` jest oczekiwane, za pomocą instrukcji await, a nie wyrażenia await, podobnie do instrukcji wywołujące na metodę asynchroniczną zwracającą typ void.</span><span class="sxs-lookup"><span data-stu-id="41a1d-136">`WaitAndApologize` is awaited by using an await statement instead of an await expression, similar to the calling statement for a synchronous void-returning method.</span></span> <span data-ttu-id="41a1d-137">W tym przypadku zastosowanie operatora await nie generuje wartości.</span><span class="sxs-lookup"><span data-stu-id="41a1d-137">The application of an await operator in this case doesn't produce a value.</span></span>  
  
<span data-ttu-id="41a1d-138">Jak w poprzednim <xref:System.Threading.Tasks.Task%601> przykład, można oddzielić wywołanie `WaitAndApologize` z aplikacji operatora await, jak poniższy kod przedstawia.</span><span class="sxs-lookup"><span data-stu-id="41a1d-138">As in the previous <xref:System.Threading.Tasks.Task%601> example, you can separate the call to `WaitAndApologize` from the application of an await operator, as the following code shows.</span></span> <span data-ttu-id="41a1d-139">Jednak należy pamiętać, że `Task` nie ma `Result` właściwość i że wartość nie jest generowany, gdy await operator jest stosowany do `Task`.</span><span class="sxs-lookup"><span data-stu-id="41a1d-139">However, remember that a `Task` doesn't have a `Result` property, and that no value is produced when an await operator is applied to a `Task`.</span></span>  
  
<span data-ttu-id="41a1d-140">Poniższy kod oddziela wywoływanie metody `WaitAndApologize` metody z Oczekujące na zadanie, które zwraca metoda.</span><span class="sxs-lookup"><span data-stu-id="41a1d-140">The following code separates calling the `WaitAndApologize` method from awaiting the task that the method returns.</span></span>  
 
[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns2a.cs#1)]  
 
##  <a name="BKMK_VoidReturnType"></a> <span data-ttu-id="41a1d-141">Typ zwracany void</span><span class="sxs-lookup"><span data-stu-id="41a1d-141">Void return type</span></span>

<span data-ttu-id="41a1d-142">Możesz użyć `void` zwracany typ w procedurze obsługi zdarzeń asynchronicznych, które wymagają `void` typ zwracany.</span><span class="sxs-lookup"><span data-stu-id="41a1d-142">You use the `void` return type in asynchronous event handlers, which require a `void` return type.</span></span> <span data-ttu-id="41a1d-143">W przypadku metod innych niż procedury obsługi zdarzeń, które nie zwrócą wartość, powinna zwrócić <xref:System.Threading.Tasks.Task> zamiast tego, ponieważ metoda asynchroniczna zwraca `void` nie może być oczekiwany.</span><span class="sxs-lookup"><span data-stu-id="41a1d-143">For methods other than event handlers that don't return a value, you should return a <xref:System.Threading.Tasks.Task> instead, because an async method that returns `void` can't be awaited.</span></span> <span data-ttu-id="41a1d-144">Dowolny obiekt wywołujący taką metodę musi być w stanie kontynuować do zakończenia bez oczekiwania na zakończenie metody asynchronicznej, a obiekt wywołujący musi być niezależny od wartości lub wyjątków, które generuje metoda asynchroniczna.</span><span class="sxs-lookup"><span data-stu-id="41a1d-144">Any caller of such a method must be able to continue to completion without waiting for the called async method to finish, and the caller must be independent of any values or exceptions that the async method generates.</span></span>  
  
<span data-ttu-id="41a1d-145">Obiekt wywołujący metodę async zwraca void nie może przechwytywać wyjątków, które są generowane przez tę metodę, i takie nieobsłużone wyjątki mogą może spowodować awarię aplikacji.</span><span class="sxs-lookup"><span data-stu-id="41a1d-145">The caller of a void-returning async method can't catch exceptions that are thrown from the method, and such unhandled exceptions are likely to cause your application to fail.</span></span> <span data-ttu-id="41a1d-146">Jeśli wystąpi wyjątek w metodzie asynchronicznej, która zwraca <xref:System.Threading.Tasks.Task> lub <xref:System.Threading.Tasks.Task%601>, wyjątek jest przechowywany w zwróconym zadaniu i jest ponownie zgłaszany, gdy zadanie jest oczekiwane.</span><span class="sxs-lookup"><span data-stu-id="41a1d-146">If an exception occurs in an async method that returns a <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601>, the exception is stored in the returned task and is rethrown when the task is awaited.</span></span> <span data-ttu-id="41a1d-147">Dlatego upewnij się, że każda metoda asynchroniczna, która może spowodować wyjątek ma typ zwracany <xref:System.Threading.Tasks.Task> lub <xref:System.Threading.Tasks.Task%601> i że trwa oczekiwanie na wywołania metody.</span><span class="sxs-lookup"><span data-stu-id="41a1d-147">Therefore, make sure that any async method that can produce an exception has a return type of <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> and that calls to the method are awaited.</span></span>  
  
<span data-ttu-id="41a1d-148">Aby uzyskać więcej informacji dotyczących wyjątków CATCH w metodach asynchronicznych, zobacz [wyjątki w metodach asynchronicznych](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) części [try-catch —](../../../language-reference/keywords/try-catch.md) tematu.</span><span class="sxs-lookup"><span data-stu-id="41a1d-148">For more information about how to catch exceptions in async methods, see the [Exceptions in Async Methods](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) section of the [try-catch](../../../language-reference/keywords/try-catch.md) topic.</span></span>  
  
<span data-ttu-id="41a1d-149">Poniższy przykład pokazuje zachowanie programu async obsługi zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="41a1d-149">The following example shows the behavior of an async event handler.</span></span> <span data-ttu-id="41a1d-150">Należy pamiętać, że w przykładowym kodzie, programu async obsługi zdarzeń musi umożliwić wiedzieć, po zakończeniu wątku głównego.</span><span class="sxs-lookup"><span data-stu-id="41a1d-150">Note that in the example code, an async event handler must let the main thread know when it finishes.</span></span> <span data-ttu-id="41a1d-151">Następnie wątek główny może oczekiwać programu async obsługi zdarzeń ukończyć przed zakończeniem pracy w programie.</span><span class="sxs-lookup"><span data-stu-id="41a1d-151">Then the main thread can wait for an async event handler to complete before exiting the program.</span></span>
 
[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns3.cs)]  
 
## <a name="generalized-async-return-types-and-valuetasktresult"></a><span data-ttu-id="41a1d-152">Uogólnionego asynchroniczne typy zwracane i ValueTask\<TResult\></span><span class="sxs-lookup"><span data-stu-id="41a1d-152">Generalized async return types and ValueTask\<TResult\></span></span>

<span data-ttu-id="41a1d-153">Począwszy od języka C# 7.0 metody asynchronicznej może zwrócić dowolnego typu, który ma dostępne `GetAwaiter` metody.</span><span class="sxs-lookup"><span data-stu-id="41a1d-153">Starting with C# 7.0, an async method can return any type that has an accessible `GetAwaiter` method.</span></span>
 
<span data-ttu-id="41a1d-154">Ponieważ <xref:System.Threading.Tasks.Task> i <xref:System.Threading.Tasks.Task%601> są typami odwołań, alokacji pamięci w ścieżkach newralgicznym dla wydajności, szczególnie w przypadku, gdy alokacje występują w ścisłej pętli, może niekorzystnie wpłynąć na wydajność.</span><span class="sxs-lookup"><span data-stu-id="41a1d-154">Because <xref:System.Threading.Tasks.Task> and <xref:System.Threading.Tasks.Task%601> are reference types, memory allocation in performance-critical paths, particularly when allocations occur in tight loops, can adversely affect performance.</span></span> <span data-ttu-id="41a1d-155">Pomoc techniczna dla ogólnych typów zwracanych oznacza, że może zwrócić typu lightweight wartości zamiast typem referencyjnym, aby uniknąć alokacji pamięci dodatkowe.</span><span class="sxs-lookup"><span data-stu-id="41a1d-155">Support for generalized return types means that you can return a lightweight value type instead of a reference type to avoid additional memory allocations.</span></span> 

<span data-ttu-id="41a1d-156">.NET zapewnia <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> struktury jako lekki stosowania uogólnionego wartość zwracania zadania.</span><span class="sxs-lookup"><span data-stu-id="41a1d-156">.NET provides the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> structure as a light-weight implementation of a generalized task-returning value.</span></span> <span data-ttu-id="41a1d-157">Aby użyć <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> typu, należy dodać `System.Threading.Tasks.Extensions` pakiet NuGet do projektu.</span><span class="sxs-lookup"><span data-stu-id="41a1d-157">To use the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> type, you must add the `System.Threading.Tasks.Extensions` NuGet package to your project.</span></span> <span data-ttu-id="41a1d-158">W poniższym przykładzie użyto <xref:System.Threading.Tasks.ValueTask%601> przedstawia strukturę, aby pobrać wartość dwóch dice.</span><span class="sxs-lookup"><span data-stu-id="41a1d-158">The following example uses the <xref:System.Threading.Tasks.ValueTask%601> structure to retrieve the value of two dice rolls.</span></span> 
  
[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-valuetask.cs)]

## <a name="see-also"></a><span data-ttu-id="41a1d-159">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="41a1d-159">See Also</span></span>

- <xref:System.Threading.Tasks.Task.FromResult%2A>   
- [<span data-ttu-id="41a1d-160">Wskazówki: Uzyskiwanie dostępu do sieci Web za pomocą async i await (C#)</span><span class="sxs-lookup"><span data-stu-id="41a1d-160">Walkthrough: Accessing the Web by Using async and await (C#)</span></span>](../../../../csharp/programming-guide/concepts/async/walkthrough-accessing-the-web-by-using-async-and-await.md)   
- [<span data-ttu-id="41a1d-161">Przepływ sterowania w programach Async (C#)</span><span class="sxs-lookup"><span data-stu-id="41a1d-161">Control Flow in Async Programs (C#)</span></span>](../../../../csharp/programming-guide/concepts/async/control-flow-in-async-programs.md)   
- [<span data-ttu-id="41a1d-162">async</span><span class="sxs-lookup"><span data-stu-id="41a1d-162">async</span></span>](../../../../csharp/language-reference/keywords/async.md)   
- [<span data-ttu-id="41a1d-163">await</span><span class="sxs-lookup"><span data-stu-id="41a1d-163">await</span></span>](../../../../csharp/language-reference/keywords/await.md)
