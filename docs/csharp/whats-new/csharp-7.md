---
title: Co nowego w C# 7,0 — C# Przewodnik
description: Zapoznaj się z omówieniem nowych funkcji w wersji 7,0 C# języka.
ms.date: 02/20/2019
ms.assetid: fd41596d-d0c2-4816-b94d-c4d00a5d0243
ms.openlocfilehash: 73563a04dea04c942a6326d6a04ddd54bb80b0ed
ms.sourcegitcommit: 5f236cd78cf09593c8945a7d753e0850e96a0b80
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 01/07/2020
ms.locfileid: "75694585"
---
# <a name="whats-new-in-c-70"></a><span data-ttu-id="823fb-103">Co nowego w C# 7,0</span><span class="sxs-lookup"><span data-stu-id="823fb-103">What's new in C# 7.0</span></span>

<span data-ttu-id="823fb-104">C#7,0 dodaje wiele nowych funkcji do C# języka:</span><span class="sxs-lookup"><span data-stu-id="823fb-104">C# 7.0 adds a number of new features to the C# language:</span></span>

- [<span data-ttu-id="823fb-105">zmienne `out`</span><span class="sxs-lookup"><span data-stu-id="823fb-105">`out` variables</span></span>](#out-variables)
  - <span data-ttu-id="823fb-106">Można zadeklarować wartości `out` w tekście jako argumenty metody, gdzie są używane.</span><span class="sxs-lookup"><span data-stu-id="823fb-106">You can declare `out` values inline as arguments to the method where they're used.</span></span>
- [<span data-ttu-id="823fb-107">Krotki</span><span class="sxs-lookup"><span data-stu-id="823fb-107">Tuples</span></span>](#tuples)
  - <span data-ttu-id="823fb-108">Można tworzyć lekkie, nienazwane typy, które zawierają wiele pól publicznych.</span><span class="sxs-lookup"><span data-stu-id="823fb-108">You can create lightweight, unnamed types that contain multiple public fields.</span></span> <span data-ttu-id="823fb-109">Narzędzia kompilatora i IDE rozumieją semantykę tych typów.</span><span class="sxs-lookup"><span data-stu-id="823fb-109">Compilers and IDE tools understand the semantics of these types.</span></span>
- [<span data-ttu-id="823fb-110">Odrzucenia</span><span class="sxs-lookup"><span data-stu-id="823fb-110">Discards</span></span>](#discards)
  - <span data-ttu-id="823fb-111">Odrzucenia są tymczasowymi zmiennymi tylko do zapisu, które są używane w przypisaniach, gdy nie ma opieki nad przypisaną wartością.</span><span class="sxs-lookup"><span data-stu-id="823fb-111">Discards are temporary, write-only variables used in assignments when you don't care about the value assigned.</span></span> <span data-ttu-id="823fb-112">Są one najbardziej przydatne podczas dekonstruowania krotek i typów zdefiniowanych przez użytkownika, a także podczas wywoływania metod przy użyciu parametrów `out`.</span><span class="sxs-lookup"><span data-stu-id="823fb-112">They're most useful when deconstructing tuples and user-defined types, as well as when calling methods with `out` parameters.</span></span>
- [<span data-ttu-id="823fb-113">Dopasowanie do wzorca</span><span class="sxs-lookup"><span data-stu-id="823fb-113">Pattern Matching</span></span>](#pattern-matching)
  - <span data-ttu-id="823fb-114">Można utworzyć logikę rozgałęziania na podstawie dowolnego typu i wartości elementów członkowskich tych typów.</span><span class="sxs-lookup"><span data-stu-id="823fb-114">You can create branching logic based on arbitrary types and values of the members of those types.</span></span>
- [<span data-ttu-id="823fb-115">`ref` lokalne i zwraca</span><span class="sxs-lookup"><span data-stu-id="823fb-115">`ref` locals and returns</span></span>](#ref-locals-and-returns)
  - <span data-ttu-id="823fb-116">Zmienne lokalne metody i zwracane wartości mogą być odwołaniami do innego magazynu.</span><span class="sxs-lookup"><span data-stu-id="823fb-116">Method local variables and return values can be references to other storage.</span></span>
- [<span data-ttu-id="823fb-117">Funkcje lokalne</span><span class="sxs-lookup"><span data-stu-id="823fb-117">Local Functions</span></span>](#local-functions)
  - <span data-ttu-id="823fb-118">Funkcje w innych funkcjach można zagnieżdżać w celu ograniczenia ich zakresu i widoczności.</span><span class="sxs-lookup"><span data-stu-id="823fb-118">You can nest functions inside other functions to limit their scope and visibility.</span></span>
- [<span data-ttu-id="823fb-119">Więcej składowych wyrażeń</span><span class="sxs-lookup"><span data-stu-id="823fb-119">More expression-bodied members</span></span>](#more-expression-bodied-members)
  - <span data-ttu-id="823fb-120">Lista elementów członkowskich, które mogą zostać utworzone przy użyciu wyrażeń.</span><span class="sxs-lookup"><span data-stu-id="823fb-120">The list of members that can be authored using expressions has grown.</span></span>
- [<span data-ttu-id="823fb-121">Wyrażenia `throw`</span><span class="sxs-lookup"><span data-stu-id="823fb-121">`throw` Expressions</span></span>](#throw-expressions)
  - <span data-ttu-id="823fb-122">Można zgłosić wyjątki w konstrukcjach kodu, które wcześniej nie były dozwolone, ponieważ `throw` była instrukcją.</span><span class="sxs-lookup"><span data-stu-id="823fb-122">You can throw exceptions in code constructs that previously weren't allowed because `throw` was a statement.</span></span>
- [<span data-ttu-id="823fb-123">Uogólnione asynchroniczne typy zwracane</span><span class="sxs-lookup"><span data-stu-id="823fb-123">Generalized async return types</span></span>](#generalized-async-return-types)
  - <span data-ttu-id="823fb-124">Metody zadeklarowane za pomocą modyfikatora `async` mogą zwracać inne typy oprócz `Task` i `Task<T>`.</span><span class="sxs-lookup"><span data-stu-id="823fb-124">Methods declared with the `async` modifier can return other types in addition to `Task` and `Task<T>`.</span></span>
- [<span data-ttu-id="823fb-125">Udoskonalenia składni literału liczbowego</span><span class="sxs-lookup"><span data-stu-id="823fb-125">Numeric literal syntax improvements</span></span>](#numeric-literal-syntax-improvements)
  - <span data-ttu-id="823fb-126">Nowe tokeny zwiększają czytelność dla stałych numerycznych.</span><span class="sxs-lookup"><span data-stu-id="823fb-126">New tokens improve readability for numeric constants.</span></span>

<span data-ttu-id="823fb-127">Pozostała część tego artykułu zawiera omówienie każdej funkcji.</span><span class="sxs-lookup"><span data-stu-id="823fb-127">The remainder of this article provides an overview of each feature.</span></span> <span data-ttu-id="823fb-128">Dla każdej funkcji znajdziesz jej uzasadnienie.</span><span class="sxs-lookup"><span data-stu-id="823fb-128">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="823fb-129">Poznasz składnię.</span><span class="sxs-lookup"><span data-stu-id="823fb-129">You'll learn the syntax.</span></span> <span data-ttu-id="823fb-130">Te funkcje można eksplorować w środowisku za pomocą narzędzia globalnego `dotnet try`:</span><span class="sxs-lookup"><span data-stu-id="823fb-130">You can explore these features in your environment using the `dotnet try` global tool:</span></span>

1. <span data-ttu-id="823fb-131">Zainstaluj narzędzie [dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) Global.</span><span class="sxs-lookup"><span data-stu-id="823fb-131">Install the [dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) global tool.</span></span>
1. <span data-ttu-id="823fb-132">Sklonuj repozytorium [dotnet/try-Samples](https://github.com/dotnet/try-samples) .</span><span class="sxs-lookup"><span data-stu-id="823fb-132">Clone the [dotnet/try-samples](https://github.com/dotnet/try-samples) repository.</span></span>
1. <span data-ttu-id="823fb-133">Ustaw bieżący katalog na podkatalog *csharp7* dla repozytorium *try-Samples* .</span><span class="sxs-lookup"><span data-stu-id="823fb-133">Set the current directory to the *csharp7* subdirectory for the *try-samples* repository.</span></span>
1. <span data-ttu-id="823fb-134">Uruchom polecenie `dotnet try`.</span><span class="sxs-lookup"><span data-stu-id="823fb-134">Run `dotnet try`.</span></span>

## <a name="out-variables"></a><span data-ttu-id="823fb-135">zmienne `out`</span><span class="sxs-lookup"><span data-stu-id="823fb-135">`out` variables</span></span>

<span data-ttu-id="823fb-136">Istniejąca składnia, która obsługuje `out` parametry, została ulepszona w tej wersji.</span><span class="sxs-lookup"><span data-stu-id="823fb-136">The existing syntax that supports `out` parameters has been improved in this version.</span></span> <span data-ttu-id="823fb-137">Teraz można zadeklarować zmienne `out` na liście argumentów wywołania metody zamiast napisać oddzielną instrukcję deklaracji:</span><span class="sxs-lookup"><span data-stu-id="823fb-137">You can now declare `out` variables in the argument list of a method call, rather than writing a separate declaration statement:</span></span>

[!code-csharp[OutVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVariableDeclarations "Out variable declarations")]

<span data-ttu-id="823fb-138">Możesz chcieć określić typ zmiennej `out` dla przejrzystości, jak pokazano powyżej.</span><span class="sxs-lookup"><span data-stu-id="823fb-138">You may want to specify the type of the `out` variable for clarity, as shown above.</span></span> <span data-ttu-id="823fb-139">Język obsługuje jednak użycie niejawnie wpisanej zmiennej lokalnej:</span><span class="sxs-lookup"><span data-stu-id="823fb-139">However, the language does support using an implicitly typed local variable:</span></span>

[!code-csharp[OutVarVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVarVariableDeclarations "Implicitly typed Out variable")]

- <span data-ttu-id="823fb-140">Kod jest łatwiejszy do odczytania.</span><span class="sxs-lookup"><span data-stu-id="823fb-140">The code is easier to read.</span></span>
  - <span data-ttu-id="823fb-141">Można zadeklarować zmienną out tam, gdzie ją używasz, a nie w innym wierszu powyżej.</span><span class="sxs-lookup"><span data-stu-id="823fb-141">You declare the out variable where you use it, not on another line above.</span></span>
- <span data-ttu-id="823fb-142">Nie trzeba przypisywać wartości początkowej.</span><span class="sxs-lookup"><span data-stu-id="823fb-142">No need to assign an initial value.</span></span>
  - <span data-ttu-id="823fb-143">Deklarując zmienną `out`, w której jest używana w wywołaniu metody, nie można jej przypadkowo użyć przed przypisaniem.</span><span class="sxs-lookup"><span data-stu-id="823fb-143">By declaring the `out` variable where it's used in a method call, you can't accidentally use it before it is assigned.</span></span>

## <a name="tuples"></a><span data-ttu-id="823fb-144">Krotki</span><span class="sxs-lookup"><span data-stu-id="823fb-144">Tuples</span></span>

<span data-ttu-id="823fb-145">C#zawiera rozbudowaną składnię dla klas i struktur, które są używane do wyjaśnienia zamiaru projektowania.</span><span class="sxs-lookup"><span data-stu-id="823fb-145">C# provides a rich syntax for classes and structs that is used to explain your design intent.</span></span> <span data-ttu-id="823fb-146">Jednak czasami rozbudowana Składnia wymaga dodatkowej pracy z minimalną korzyścią.</span><span class="sxs-lookup"><span data-stu-id="823fb-146">But sometimes that rich syntax requires extra work with minimal benefit.</span></span> <span data-ttu-id="823fb-147">Często możesz pisać metody, które potrzebują prostej struktury zawierającej więcej niż jeden element danych.</span><span class="sxs-lookup"><span data-stu-id="823fb-147">You may often write methods that need a simple structure containing more than one data element.</span></span> <span data-ttu-id="823fb-148">Aby obsługiwać te scenariusze, *krotki* zostały dodane do C#.</span><span class="sxs-lookup"><span data-stu-id="823fb-148">To support these scenarios *tuples* were added to C#.</span></span> <span data-ttu-id="823fb-149">Krotki są lekkimi strukturami danych, które zawierają wiele pól do reprezentowania elementów członkowskich danych.</span><span class="sxs-lookup"><span data-stu-id="823fb-149">Tuples are lightweight data structures that contain multiple fields to represent the data members.</span></span>
<span data-ttu-id="823fb-150">Pola nie są zweryfikowane i nie można definiować własnych metod</span><span class="sxs-lookup"><span data-stu-id="823fb-150">The fields aren't validated, and you can't define your own methods</span></span>

> [!NOTE]
> <span data-ttu-id="823fb-151">Krotki były dostępne przed C# 7,0, ale były niewydajne i nie obsługiwały języka.</span><span class="sxs-lookup"><span data-stu-id="823fb-151">Tuples were available before C# 7.0, but they were inefficient and had no language support.</span></span>
> <span data-ttu-id="823fb-152">Oznacza to, że elementy krotki mogą być przywoływane tylko jako `Item1`, `Item2` i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="823fb-152">This meant that tuple elements could only be referenced as `Item1`, `Item2` and so on.</span></span> <span data-ttu-id="823fb-153">C#7,0 wprowadza obsługę języków dla krotek, co umożliwia używanie nazw semantycznych dla pól krotki przy użyciu nowych, wydajniejszych typów krotek.</span><span class="sxs-lookup"><span data-stu-id="823fb-153">C# 7.0 introduces language support for tuples, which enables semantic names for the fields of a tuple using new, more efficient tuple types.</span></span>

<span data-ttu-id="823fb-154">Można utworzyć krotkę, przypisując wartość do każdego elementu członkowskiego, a opcjonalnie dostarczając nazwy semantyczne dla każdego z elementów członkowskich krotki:</span><span class="sxs-lookup"><span data-stu-id="823fb-154">You can create a tuple by assigning a value to each member, and optionally providing semantic names to each of the members of the tuple:</span></span>

[!code-csharp[NamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#NamedTuple "Named tuple")]

<span data-ttu-id="823fb-155">Spójna `namedLetters` krotka zawiera pola, które są nazywane `Alpha` i `Beta`.</span><span class="sxs-lookup"><span data-stu-id="823fb-155">The `namedLetters` tuple contains fields referred to as `Alpha` and `Beta`.</span></span> <span data-ttu-id="823fb-156">Te nazwy istnieją tylko w czasie kompilacji i nie są zachowywane, na przykład podczas sprawdzania krotki przy użyciu odbicia w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="823fb-156">Those names exist only at compile time and aren't preserved, for example when inspecting the tuple using reflection at runtime.</span></span>

<span data-ttu-id="823fb-157">W przypisaniu spójnej kolekcji można także określić nazwy pól po prawej stronie przypisania:</span><span class="sxs-lookup"><span data-stu-id="823fb-157">In a tuple assignment, you can also specify the names of the fields on the right-hand side of the assignment:</span></span>

[!code-csharp[ImplicitNamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#ImplicitNamedTuple "Implicitly named tuple")]

<span data-ttu-id="823fb-158">Mogą wystąpić sytuacje, w których chcesz rozpakować elementy członkowskie spójnej kolekcji, która została zwrócona z metody.</span><span class="sxs-lookup"><span data-stu-id="823fb-158">There may be times when you want to unpackage the members of a tuple that were returned from a method.</span></span>  <span data-ttu-id="823fb-159">Można to zrobić przez zadeklarowanie oddzielnych zmiennych dla każdej wartości w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="823fb-159">You can do that by declaring separate variables for each of the values in the tuple.</span></span> <span data-ttu-id="823fb-160">To rozpakowanie nosi nazwę *dekonstrukcja* krotki:</span><span class="sxs-lookup"><span data-stu-id="823fb-160">This unpackaging is called *deconstructing* the tuple:</span></span>

[!code-csharp[CallingWithDeconstructor](~/samples/snippets/csharp/new-in-7/program.cs#CallingWithDeconstructor "Deconstructing a tuple")]

<span data-ttu-id="823fb-161">Można także zapewnić podobną dekonstrukcję dla dowolnego typu w programie .NET.</span><span class="sxs-lookup"><span data-stu-id="823fb-161">You can also provide a similar deconstruction for any type in .NET.</span></span> <span data-ttu-id="823fb-162">Metodę `Deconstruct` należy napisać jako element członkowski klasy.</span><span class="sxs-lookup"><span data-stu-id="823fb-162">You write a `Deconstruct` method as a member of the class.</span></span> <span data-ttu-id="823fb-163">Ta metoda `Deconstruct` dostarcza zestaw `out` argumentów dla każdej właściwości, która ma zostać wyodrębniona.</span><span class="sxs-lookup"><span data-stu-id="823fb-163">That `Deconstruct` method provides a set of `out` arguments for each of the properties you want to extract.</span></span> <span data-ttu-id="823fb-164">Rozważmy tę `Point` klasę, która zapewnia metodę dekonstruktora, która wyodrębnia `X` i współrzędne `Y`:</span><span class="sxs-lookup"><span data-stu-id="823fb-164">Consider this `Point` class that provides a deconstructor method that extracts the `X` and `Y` coordinates:</span></span>

[!code-csharp[PointWithDeconstruction](~/samples/snippets/csharp/new-in-7/point.cs#PointWithDeconstruction "Point with deconstruction method")]

<span data-ttu-id="823fb-165">Poszczególne pola można wyodrębnić, przypisując `Point` do krotki:</span><span class="sxs-lookup"><span data-stu-id="823fb-165">You can extract the individual fields by assigning a `Point` to a tuple:</span></span>

[!code-csharp[DeconstructPoint](~/samples/snippets/csharp/new-in-7/program.cs#DeconstructPoint "Deconstruct a point")]

<span data-ttu-id="823fb-166">Więcej informacji na temat spójnych krotek można znaleźć w [artykule krotkis](../tuples.md).</span><span class="sxs-lookup"><span data-stu-id="823fb-166">You can learn more in depth about tuples in the [tuples article](../tuples.md).</span></span>

## <a name="discards"></a><span data-ttu-id="823fb-167">Odrzucenia</span><span class="sxs-lookup"><span data-stu-id="823fb-167">Discards</span></span>

<span data-ttu-id="823fb-168">Często podczas dekonstruowania krotki lub wywoływania metody za pomocą parametrów `out`, wymuszane jest zdefiniowanie zmiennej, której wartość nie jest zastosowana i nie zamierza się jej używać.</span><span class="sxs-lookup"><span data-stu-id="823fb-168">Often when deconstructing a tuple or calling a method with `out` parameters, you're forced to define a variable whose value you don't care about and don't intend to use.</span></span> <span data-ttu-id="823fb-169">C#dodaje obsługę *odrzutów* , aby obsłużyć ten scenariusz.</span><span class="sxs-lookup"><span data-stu-id="823fb-169">C# adds support for *discards* to handle this scenario.</span></span> <span data-ttu-id="823fb-170">Odrzucanie to zmienna tylko do zapisu, której nazwa jest `_` (znak podkreślenia); można przypisać wszystkie wartości, które mają zostać odrzucone do pojedynczej zmiennej.</span><span class="sxs-lookup"><span data-stu-id="823fb-170">A discard is a write-only variable whose name is `_` (the underscore character); you can assign all of the values that you intend to discard to the single variable.</span></span> <span data-ttu-id="823fb-171">Odrzucenie przypomina przypisaną zmienną; poza instrukcją przypisania nie można używać odrzucania w kodzie.</span><span class="sxs-lookup"><span data-stu-id="823fb-171">A discard is like an unassigned variable; apart from the assignment statement, the discard can't be used in code.</span></span>

<span data-ttu-id="823fb-172">Odrzucenia są obsługiwane w następujących scenariuszach:</span><span class="sxs-lookup"><span data-stu-id="823fb-172">Discards are supported in the following scenarios:</span></span>

- <span data-ttu-id="823fb-173">Podczas dekonstrukcji krotek lub typów zdefiniowanych przez użytkownika.</span><span class="sxs-lookup"><span data-stu-id="823fb-173">When deconstructing tuples or user-defined types.</span></span>
- <span data-ttu-id="823fb-174">Podczas wywoływania metod z parametrami [out](../language-reference/keywords/out-parameter-modifier.md) .</span><span class="sxs-lookup"><span data-stu-id="823fb-174">When calling methods with [out](../language-reference/keywords/out-parameter-modifier.md) parameters.</span></span>
- <span data-ttu-id="823fb-175">W operacji dopasowania wzorca z instrukcjami with [i](../language-reference/keywords/is.md) [Switch](../language-reference/keywords/switch.md) .</span><span class="sxs-lookup"><span data-stu-id="823fb-175">In a pattern matching operation with the [is](../language-reference/keywords/is.md) and [switch](../language-reference/keywords/switch.md) statements.</span></span>
- <span data-ttu-id="823fb-176">Jako identyfikator autonomiczny, gdy chcesz jawnie określić wartość przypisania jako odrzucenie.</span><span class="sxs-lookup"><span data-stu-id="823fb-176">As a standalone identifier when you want to explicitly identify the value of an assignment as a discard.</span></span>

<span data-ttu-id="823fb-177">W poniższym przykładzie zdefiniowano metodę `QueryCityDataForYears`, która zwraca 6-krotkę zawierającą dane dla miasta z dwóch różnych lat.</span><span class="sxs-lookup"><span data-stu-id="823fb-177">The following example defines a `QueryCityDataForYears` method that returns a 6-tuple that contains data for a city for two different years.</span></span> <span data-ttu-id="823fb-178">Wywołanie metody w przykładzie jest rozważane tylko z dwiema wartościami populacji zwracanymi przez metodę i dlatego traktują pozostałe wartości w spójnej kolekcji jako odrzucane podczas dekonstruowania krotki.</span><span class="sxs-lookup"><span data-stu-id="823fb-178">The method call in the example is concerned only with the two population values returned by the method and so treats the remaining values in the tuple as discards when it deconstructs the tuple.</span></span>

[!code-csharp[Tuple-discard](~/samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="823fb-179">Aby uzyskać więcej informacji, zobacz [odrzucanie](../discards.md).</span><span class="sxs-lookup"><span data-stu-id="823fb-179">For more information, see [Discards](../discards.md).</span></span>

## <a name="pattern-matching"></a><span data-ttu-id="823fb-180">Dopasowanie do wzorca</span><span class="sxs-lookup"><span data-stu-id="823fb-180">Pattern matching</span></span>

<span data-ttu-id="823fb-181">*Dopasowywanie do wzorców* to funkcja, która umożliwia implementowanie metody wysyłania we właściwościach innych niż typ obiektu.</span><span class="sxs-lookup"><span data-stu-id="823fb-181">*Pattern matching* is a feature that allows you to implement method dispatch on properties other than the type of an object.</span></span> <span data-ttu-id="823fb-182">Prawdopodobnie znasz już metodę wysyłania metody na podstawie typu obiektu.</span><span class="sxs-lookup"><span data-stu-id="823fb-182">You're probably already familiar with method dispatch based on the type of an object.</span></span> <span data-ttu-id="823fb-183">W programowaniu zorientowanym obiektowo metody wirtualne i przesłonięcia zapewniają składnię języka, aby zaimplementować metodę wysyłania na podstawie typu obiektu.</span><span class="sxs-lookup"><span data-stu-id="823fb-183">In object-oriented programming, virtual and override methods provide language syntax to implement method dispatching based on an object's type.</span></span> <span data-ttu-id="823fb-184">Klasy podstawowe i pochodne zapewniają różne implementacje.</span><span class="sxs-lookup"><span data-stu-id="823fb-184">Base and Derived classes provide different implementations.</span></span>
<span data-ttu-id="823fb-185">Wyrażenia dopasowania wzorców poszerzają tę koncepcję, aby można było łatwo zaimplementować podobne wzorce wysyłania dla typów i elementów danych, które nie są związane z hierarchią dziedziczenia.</span><span class="sxs-lookup"><span data-stu-id="823fb-185">Pattern matching expressions extend this concept so that you can easily implement similar dispatch patterns for types and data elements that aren't related through an inheritance hierarchy.</span></span>

<span data-ttu-id="823fb-186">Dopasowywanie wzorców obsługuje wyrażenia `is` i wyrażenia `switch`.</span><span class="sxs-lookup"><span data-stu-id="823fb-186">Pattern matching supports `is` expressions and `switch` expressions.</span></span> <span data-ttu-id="823fb-187">Każdy umożliwia inspekcję obiektu i jego właściwości w celu ustalenia, czy ten obiekt spełnia oczekiwany wzorzec.</span><span class="sxs-lookup"><span data-stu-id="823fb-187">Each enables inspecting an object and its properties to determine if that object satisfies the sought pattern.</span></span> <span data-ttu-id="823fb-188">Użyj słowa kluczowego `when`, aby określić dodatkowe reguły do wzorca.</span><span class="sxs-lookup"><span data-stu-id="823fb-188">You use the `when` keyword to specify additional rules to the pattern.</span></span>

<span data-ttu-id="823fb-189">Wyrażenie wzorca `is` rozszerza znanego [operatora`is`](../language-reference/keywords/is.md#pattern-matching-with-is) do wykonywania zapytań względem obiektu o jego typie i przypisywania wyniku w jednej instrukcji.</span><span class="sxs-lookup"><span data-stu-id="823fb-189">The `is` pattern expression extends the familiar [`is` operator](../language-reference/keywords/is.md#pattern-matching-with-is) to query an object about its type and assign the result in one instruction.</span></span> <span data-ttu-id="823fb-190">Poniższy kod sprawdza, czy zmienna jest `int`, a jeśli tak, dodaje ją do bieżącej sumy:</span><span class="sxs-lookup"><span data-stu-id="823fb-190">The following code checks if a variable is an `int`, and if so, adds it to the current sum:</span></span>

```csharp
if (input is int count)
    sum += count;
```

<span data-ttu-id="823fb-191">Powyższy mały przykład ilustruje ulepszenia wyrażenia `is`.</span><span class="sxs-lookup"><span data-stu-id="823fb-191">The preceding small example demonstrates the enhancements to the `is` expression.</span></span> <span data-ttu-id="823fb-192">Można testować względem typów wartości, a także typów referencyjnych, a następnie można przypisać prawidłowy wynik do nowej zmiennej poprawnego typu.</span><span class="sxs-lookup"><span data-stu-id="823fb-192">You can test against value types as well as reference types, and you can assign the successful result to a new variable of the correct type.</span></span>

<span data-ttu-id="823fb-193">Wyrażenie dopasowania przełącznika ma znaną składnię opartą na instrukcji `switch`, która jest już częścią C# języka.</span><span class="sxs-lookup"><span data-stu-id="823fb-193">The switch match expression has a familiar syntax, based on the `switch` statement already part of the C# language.</span></span> <span data-ttu-id="823fb-194">Zaktualizowana instrukcja SWITCH zawiera kilka nowych konstrukcji:</span><span class="sxs-lookup"><span data-stu-id="823fb-194">The updated switch statement has several new constructs:</span></span>

- <span data-ttu-id="823fb-195">Typ regulujący wyrażenie `switch` nie jest już ograniczony do typów całkowitych, typów `Enum`, `string`lub typu dopuszczającego wartość null odpowiadającego jednemu z tych typów.</span><span class="sxs-lookup"><span data-stu-id="823fb-195">The governing type of a `switch` expression is no longer restricted to integral types, `Enum` types, `string`, or a nullable type corresponding to one of those types.</span></span> <span data-ttu-id="823fb-196">Można użyć dowolnego typu.</span><span class="sxs-lookup"><span data-stu-id="823fb-196">Any type may be used.</span></span>
- <span data-ttu-id="823fb-197">Typ wyrażenia `switch` można testować w każdej `case` etykiecie.</span><span class="sxs-lookup"><span data-stu-id="823fb-197">You can test the type of the `switch` expression in each `case` label.</span></span> <span data-ttu-id="823fb-198">Podobnie jak w przypadku wyrażenia `is`, można przypisać nową zmienną do tego typu.</span><span class="sxs-lookup"><span data-stu-id="823fb-198">As with the `is` expression, you may assign a new variable to that type.</span></span>
- <span data-ttu-id="823fb-199">Można dodać klauzulę `when` do dalszych warunków testowania tej zmiennej.</span><span class="sxs-lookup"><span data-stu-id="823fb-199">You may add a `when` clause to further test conditions on that variable.</span></span>
- <span data-ttu-id="823fb-200">Kolejność etykiet `case` jest teraz ważna.</span><span class="sxs-lookup"><span data-stu-id="823fb-200">The order of `case` labels is now important.</span></span> <span data-ttu-id="823fb-201">Pierwsza gałąź do dopasowania jest wykonywana; pozostałe są pomijane.</span><span class="sxs-lookup"><span data-stu-id="823fb-201">The first branch to match is executed; others are skipped.</span></span>

<span data-ttu-id="823fb-202">Poniższy kod ilustruje następujące nowe funkcje:</span><span class="sxs-lookup"><span data-stu-id="823fb-202">The following code demonstrates these new features:</span></span>

```csharp
public static int SumPositiveNumbers(IEnumerable<object> sequence)
{
    int sum = 0;
    foreach (var i in sequence)
    {
        switch (i)
        {
            case 0:
                break;
            case IEnumerable<int> childSequence:
            {
                foreach(var item in childSequence)
                    sum += (item > 0) ? item : 0;
                break;
            }
            case int n when n > 0:
                sum += n;
                break;
            case null:
                throw new NullReferenceException("Null found in sequence");
            default:
                throw new InvalidOperationException("Unrecognized type");
        }
    }
    return sum;
}
```

- <span data-ttu-id="823fb-203">`case 0:` jest znanym wzorcem stałej.</span><span class="sxs-lookup"><span data-stu-id="823fb-203">`case 0:` is the familiar constant pattern.</span></span>
- <span data-ttu-id="823fb-204">`case IEnumerable<int> childSequence:` jest wzorcem typu.</span><span class="sxs-lookup"><span data-stu-id="823fb-204">`case IEnumerable<int> childSequence:` is a type pattern.</span></span>
- <span data-ttu-id="823fb-205">`case int n when n > 0:` jest wzorcem typu z dodatkowym warunkiem `when`.</span><span class="sxs-lookup"><span data-stu-id="823fb-205">`case int n when n > 0:` is a type pattern with an additional `when` condition.</span></span>
- <span data-ttu-id="823fb-206">`case null:` jest wzorcem o wartości null.</span><span class="sxs-lookup"><span data-stu-id="823fb-206">`case null:` is the null pattern.</span></span>
- <span data-ttu-id="823fb-207">`default:` jest znanym domyślnym przypadkiem.</span><span class="sxs-lookup"><span data-stu-id="823fb-207">`default:` is the familiar default case.</span></span>

<span data-ttu-id="823fb-208">Więcej informacji na temat dopasowywania wzorców w [programie można znaleźć C#w temacie. ](../pattern-matching.md)</span><span class="sxs-lookup"><span data-stu-id="823fb-208">You can learn more about pattern matching in [Pattern Matching in C#](../pattern-matching.md).</span></span>

## <a name="ref-locals-and-returns"></a><span data-ttu-id="823fb-209">Ref locales i Returns</span><span class="sxs-lookup"><span data-stu-id="823fb-209">Ref locals and returns</span></span>

<span data-ttu-id="823fb-210">Ta funkcja włącza algorytmy, które używają i zwracają odwołania do zmiennych zdefiniowanych w innym miejscu.</span><span class="sxs-lookup"><span data-stu-id="823fb-210">This feature enables algorithms that use and return references to variables defined elsewhere.</span></span> <span data-ttu-id="823fb-211">Jeden przykład pracuje z dużymi macierzami i odnajduje jedną lokalizację z określonymi cechami.</span><span class="sxs-lookup"><span data-stu-id="823fb-211">One example is working with large matrices, and finding a single location with certain characteristics.</span></span> <span data-ttu-id="823fb-212">Poniższa metoda zwraca **odwołanie** do tego magazynu w macierzy:</span><span class="sxs-lookup"><span data-stu-id="823fb-212">The following method returns a **reference** to that storage in the matrix:</span></span>

[!code-csharp[FindReturningRef](~/samples/snippets/csharp/new-in-7/MatrixSearch.cs#FindReturningRef "Find returning by reference")]

<span data-ttu-id="823fb-213">Można zadeklarować wartość zwracaną jako `ref` i zmodyfikować tę wartość w macierzy, jak pokazano w poniższym kodzie:</span><span class="sxs-lookup"><span data-stu-id="823fb-213">You can declare the return value as a `ref` and modify that value in the matrix, as shown in the following code:</span></span>

[!code-csharp[AssignRefReturn](~/samples/snippets/csharp/new-in-7/Program.cs#AssignRefReturn "Assign ref return")]

<span data-ttu-id="823fb-214">C# Język ma kilka reguł, które chronią przed niepożądanym użyciem `ref` lokalnych i zwraca:</span><span class="sxs-lookup"><span data-stu-id="823fb-214">The C# language has several rules that protect you from misusing the `ref` locals and returns:</span></span>

- <span data-ttu-id="823fb-215">Należy dodać słowo kluczowe `ref` do sygnatury metody oraz do wszystkich instrukcji `return` w metodzie.</span><span class="sxs-lookup"><span data-stu-id="823fb-215">You must add the `ref` keyword to the method signature and to all `return` statements in a method.</span></span>
  - <span data-ttu-id="823fb-216">Dzięki temu czyszczenie metody następuje przez odwołanie w całej metodzie.</span><span class="sxs-lookup"><span data-stu-id="823fb-216">That makes it clear the method returns by reference throughout the method.</span></span>
- <span data-ttu-id="823fb-217">`ref return` może być przypisana do zmiennej wartości lub zmiennej `ref`.</span><span class="sxs-lookup"><span data-stu-id="823fb-217">A `ref return` may be assigned to a value variable, or a `ref` variable.</span></span>
  - <span data-ttu-id="823fb-218">Obiekt wywołujący kontroluje, czy wartość zwracana jest kopiowana, czy nie.</span><span class="sxs-lookup"><span data-stu-id="823fb-218">The caller controls whether the return value is copied or not.</span></span> <span data-ttu-id="823fb-219">Pominięcie modyfikatora `ref` podczas przypisywania wartości zwracanej wskazuje, że obiekt wywołujący chce mieć kopię wartości, a nie odwołanie do magazynu.</span><span class="sxs-lookup"><span data-stu-id="823fb-219">Omitting the `ref` modifier when assigning the return value indicates that the caller wants a copy of the value, not a reference to the storage.</span></span>
- <span data-ttu-id="823fb-220">Nie można przypisać wartości zwracanej metody standardowej do `ref` zmiennej lokalnej.</span><span class="sxs-lookup"><span data-stu-id="823fb-220">You can't assign a standard method return value to a `ref` local variable.</span></span>
  - <span data-ttu-id="823fb-221">Nie zezwala na instrukcje, takie jak `ref int i = sequence.Count();`</span><span class="sxs-lookup"><span data-stu-id="823fb-221">That disallows statements like `ref int i = sequence.Count();`</span></span>
- <span data-ttu-id="823fb-222">Nie można zwrócić `ref` do zmiennej, której okres istnienia nie przekracza wykonywania metody.</span><span class="sxs-lookup"><span data-stu-id="823fb-222">You can't return a `ref` to a variable whose lifetime doesn't extend beyond the execution of the method.</span></span>
  - <span data-ttu-id="823fb-223">Oznacza to, że nie można zwrócić odwołania do zmiennej lokalnej lub zmiennej o podobnym zakresie.</span><span class="sxs-lookup"><span data-stu-id="823fb-223">That means you can't return a reference to a local variable or a variable with a similar scope.</span></span>
- <span data-ttu-id="823fb-224">`ref` locale i Returns nie mogą być używane z metodami asynchronicznymi.</span><span class="sxs-lookup"><span data-stu-id="823fb-224">`ref` locals and returns can't be used with async methods.</span></span>
  - <span data-ttu-id="823fb-225">Kompilator nie może wiedzieć, czy przywoływana zmienna została ustawiona na jej końcową wartość, gdy Metoda asynchroniczna zwraca.</span><span class="sxs-lookup"><span data-stu-id="823fb-225">The compiler can't know if the referenced variable has been set to its final value when the async method returns.</span></span>

<span data-ttu-id="823fb-226">Dodanie elementów lokalnych ref i Return ref umożliwia stosowanie algorytmów, które są bardziej wydajne przez uniknięcie kopiowania wartości lub wielokrotne wykonywanie operacji usuwania odwołań.</span><span class="sxs-lookup"><span data-stu-id="823fb-226">The addition of ref locals and ref returns enables algorithms that are more efficient by avoiding copying values, or performing dereferencing operations multiple times.</span></span>

<span data-ttu-id="823fb-227">Dodanie `ref` do wartości zwracanej jest [zmianą zgodną ze źródłem](version-update-considerations.md#source-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="823fb-227">Adding `ref` to the return value is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span> <span data-ttu-id="823fb-228">Istniejący kod kompiluje, ale zwracana wartość Ref jest kopiowana po przypisaniu.</span><span class="sxs-lookup"><span data-stu-id="823fb-228">Existing code compiles, but the ref return value is copied when assigned.</span></span> <span data-ttu-id="823fb-229">Obiekty wywołujące muszą zaktualizować magazyn dla wartości zwracanej do `ref` zmiennej lokalnej, aby zapisać zwrot jako odwołanie.</span><span class="sxs-lookup"><span data-stu-id="823fb-229">Callers must update the storage for the return value to a `ref` local variable to store the return as a reference.</span></span>

<span data-ttu-id="823fb-230">Aby uzyskać więcej informacji, zobacz artykuł [słowo kluczowe ref](../language-reference/keywords/ref.md) .</span><span class="sxs-lookup"><span data-stu-id="823fb-230">For more information, see the [ref keyword](../language-reference/keywords/ref.md) article.</span></span>

## <a name="local-functions"></a><span data-ttu-id="823fb-231">Funkcje lokalne</span><span class="sxs-lookup"><span data-stu-id="823fb-231">Local functions</span></span>

<span data-ttu-id="823fb-232">Wiele projektów klas obejmuje metody, które są wywoływane tylko z jednej lokalizacji.</span><span class="sxs-lookup"><span data-stu-id="823fb-232">Many designs for classes include methods that are called from only one location.</span></span> <span data-ttu-id="823fb-233">Te dodatkowe metody prywatne przechowują każdą metodę na małe i skoncentrowane.</span><span class="sxs-lookup"><span data-stu-id="823fb-233">These additional private methods keep each method small and focused.</span></span> <span data-ttu-id="823fb-234">*Funkcje lokalne* umożliwiają deklarowanie metod wewnątrz kontekstu innej metody.</span><span class="sxs-lookup"><span data-stu-id="823fb-234">*Local functions* enable you to declare methods inside the context of another method.</span></span> <span data-ttu-id="823fb-235">Funkcje lokalne ułatwiają czytelnikom klasy sprawdzenie, czy metoda lokalna jest wywoływana tylko z kontekstu, w którym jest zadeklarowana.</span><span class="sxs-lookup"><span data-stu-id="823fb-235">Local functions make it easier for readers of the class to see that the local method is only called from the context in which it is declared.</span></span>

<span data-ttu-id="823fb-236">Istnieją dwa typowe przypadki użycia funkcji lokalnych: metody iteratora publicznego i publiczne metody async.</span><span class="sxs-lookup"><span data-stu-id="823fb-236">There are two common use cases for local functions: public iterator methods and public async methods.</span></span> <span data-ttu-id="823fb-237">Oba typy metod generują kod, który zgłasza błędy później niż programiści mogą oczekiwać.</span><span class="sxs-lookup"><span data-stu-id="823fb-237">Both types of methods generate code that reports errors later than programmers might expect.</span></span> <span data-ttu-id="823fb-238">W metodach iteratora wszystkie wyjątki są obserwowane tylko podczas wywoływania kodu, który wylicza zwracaną sekwencję.</span><span class="sxs-lookup"><span data-stu-id="823fb-238">In iterator methods, any exceptions are observed only when calling code that enumerates the returned sequence.</span></span> <span data-ttu-id="823fb-239">W metodach asynchronicznych wszelkie wyjątki są przestrzegane tylko wtedy, gdy zwracane `Task` są oczekiwane.</span><span class="sxs-lookup"><span data-stu-id="823fb-239">In async methods, any exceptions are only observed when the returned `Task` is awaited.</span></span> <span data-ttu-id="823fb-240">Poniższy przykład ilustruje oddzielanie walidacji parametrów od implementacji iteratora przy użyciu funkcji lokalnej:</span><span class="sxs-lookup"><span data-stu-id="823fb-240">The following example demonstrates separating parameter validation from the iterator implementation using a local function:</span></span>

[!code-csharp[22_IteratorMethodLocal](~/samples/snippets/csharp/new-in-7/Iterator.cs#IteratorMethodLocal "Iterator method with local function")]

<span data-ttu-id="823fb-241">Ta sama technika może być stosowana z metodami `async`, aby upewnić się, że wyjątki wynikające z walidacji argumentów są zgłaszane przed rozpoczęciem pracy asynchronicznej:</span><span class="sxs-lookup"><span data-stu-id="823fb-241">The same technique can be employed with `async` methods to ensure that exceptions arising from argument validation are thrown before the asynchronous work begins:</span></span>

[!code-csharp[TaskExample](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#TaskExample "Task returning method with local function")]

> [!NOTE]
> <span data-ttu-id="823fb-242">Niektóre projekty, które są obsługiwane przez funkcje lokalne, można również zrealizować przy użyciu *wyrażeń lambda*.</span><span class="sxs-lookup"><span data-stu-id="823fb-242">Some of the designs that are supported by local functions could also be accomplished using *lambda expressions*.</span></span> <span data-ttu-id="823fb-243">Aby uzyskać więcej informacji, zobacz temat [funkcje lokalne a wyrażenia lambda](../local-functions-vs-lambdas.md).</span><span class="sxs-lookup"><span data-stu-id="823fb-243">For more information, see [Local functions vs. lambda expressions](../local-functions-vs-lambdas.md).</span></span>

## <a name="more-expression-bodied-members"></a><span data-ttu-id="823fb-244">Więcej składowych wyrażeń</span><span class="sxs-lookup"><span data-stu-id="823fb-244">More expression-bodied members</span></span>

<span data-ttu-id="823fb-245">C#6 wprowadzono [składowe z wyrażeniami](csharp-6.md#expression-bodied-function-members) dla funkcji Członkowskich i właściwości tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="823fb-245">C# 6 introduced [expression-bodied members](csharp-6.md#expression-bodied-function-members) for member functions, and read-only properties.</span></span> <span data-ttu-id="823fb-246">C#7,0 rozwija dozwolone elementy członkowskie, które mogą być zaimplementowane jako wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="823fb-246">C# 7.0 expands the allowed members that can be implemented as expressions.</span></span> <span data-ttu-id="823fb-247">W C# 7,0 można zaimplementować *konstruktory*, *finalizatory*i `get` i metody dostępu `set` do *Właściwości* i *indeksatorów*.</span><span class="sxs-lookup"><span data-stu-id="823fb-247">In C# 7.0, you can implement *constructors*, *finalizers*, and `get` and `set` accessors on *properties* and *indexers*.</span></span> <span data-ttu-id="823fb-248">Poniższy kod przedstawia przykłady każdego z nich:</span><span class="sxs-lookup"><span data-stu-id="823fb-248">The following code shows examples of each:</span></span>

[!code-csharp[ExpressionBodiedMembers](~/samples/snippets/csharp/new-in-7/expressionmembers.cs#ExpressionBodiedEverything "new expression-bodied members")]

> [!NOTE]
> <span data-ttu-id="823fb-249">Ten przykład nie wymaga finalizatora, ale jest pokazywany do zademonstrowania składni.</span><span class="sxs-lookup"><span data-stu-id="823fb-249">This example does not need a finalizer, but it is shown to demonstrate the syntax.</span></span> <span data-ttu-id="823fb-250">Nie należy implementować finalizatora w klasie, chyba że konieczne jest zwolnienie niezarządzanych zasobów.</span><span class="sxs-lookup"><span data-stu-id="823fb-250">You should not implement a finalizer in your class unless it is necessary to  release unmanaged resources.</span></span> <span data-ttu-id="823fb-251">Należy również rozważyć użycie klasy <xref:System.Runtime.InteropServices.SafeHandle> zamiast bezpośrednio zarządzać niezarządzanymi zasobami.</span><span class="sxs-lookup"><span data-stu-id="823fb-251">You should also consider using the <xref:System.Runtime.InteropServices.SafeHandle> class instead of managing unmanaged resources directly.</span></span>

<span data-ttu-id="823fb-252">Te nowe lokalizacje elementów członkowskich będących członkami wyrażeń reprezentują ważny punkt kontrolny dla C# języka: te funkcje zostały zaimplementowane przez członków społeczności pracujących w projekcie [Roslyn](https://github.com/dotnet/Roslyn) typu open source.</span><span class="sxs-lookup"><span data-stu-id="823fb-252">These new locations for expression-bodied members represent an important milestone for the C# language: These features were implemented by community members working on the open-source [Roslyn](https://github.com/dotnet/Roslyn) project.</span></span>

<span data-ttu-id="823fb-253">Zmiana metody na wyrażenie składowane składowej jest [zgodną z binarną zmianą](version-update-considerations.md#binary-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="823fb-253">Changing a method to an expression bodied member is a [binary compatible change](version-update-considerations.md#binary-compatible-changes).</span></span>

## <a name="throw-expressions"></a><span data-ttu-id="823fb-254">Wyrażenia throw</span><span class="sxs-lookup"><span data-stu-id="823fb-254">Throw expressions</span></span>

<span data-ttu-id="823fb-255">W C#programie `throw` zawsze była instrukcją.</span><span class="sxs-lookup"><span data-stu-id="823fb-255">In C#, `throw` has always been a statement.</span></span> <span data-ttu-id="823fb-256">Ponieważ `throw` jest instrukcją, a nie wyrażeniem, istnieją C# konstrukcje, w których nie można było ich użyć.</span><span class="sxs-lookup"><span data-stu-id="823fb-256">Because `throw` is a statement, not an expression, there were C# constructs where you couldn't use it.</span></span> <span data-ttu-id="823fb-257">Uwzględnione wyrażenia warunkowe, wyrażenia łączenia o wartości null i niektóre wyrażenia lambda.</span><span class="sxs-lookup"><span data-stu-id="823fb-257">These included conditional expressions, null coalescing expressions, and some lambda expressions.</span></span> <span data-ttu-id="823fb-258">Dodanie elementów członkowskich będących członkami wyrażeń dodaje więcej lokalizacji, w których wyrażenia `throw` byłyby przydatne.</span><span class="sxs-lookup"><span data-stu-id="823fb-258">The addition of expression-bodied members adds more locations where `throw` expressions would be useful.</span></span> <span data-ttu-id="823fb-259">Aby można było napisać dowolny z tych konstrukcji, C# 7,0 wprowadza [*wyrażenia throw*](../language-reference/keywords/throw.md#the-throw-expression).</span><span class="sxs-lookup"><span data-stu-id="823fb-259">So that you can write any of these constructs, C# 7.0 introduces [*throw expressions*](../language-reference/keywords/throw.md#the-throw-expression).</span></span>

<span data-ttu-id="823fb-260">To dodanie ułatwia zapisanie więcej kodu opartego na wyrażeniach.</span><span class="sxs-lookup"><span data-stu-id="823fb-260">This addition makes it easier to write more expression-based code.</span></span> <span data-ttu-id="823fb-261">Nie potrzebujesz dodatkowych instrukcji do sprawdzania błędów.</span><span class="sxs-lookup"><span data-stu-id="823fb-261">You don't need additional statements for error checking.</span></span>

## <a name="generalized-async-return-types"></a><span data-ttu-id="823fb-262">Uogólnione asynchroniczne typy zwracane</span><span class="sxs-lookup"><span data-stu-id="823fb-262">Generalized async return types</span></span>

<span data-ttu-id="823fb-263">Zwrócenie obiektu `Task` z metod asynchronicznych może spowodować wąskie gardła wydajności w określonych ścieżkach.</span><span class="sxs-lookup"><span data-stu-id="823fb-263">Returning a `Task` object from async methods can introduce performance bottlenecks in certain paths.</span></span> <span data-ttu-id="823fb-264">`Task` to typ referencyjny, dlatego użycie go oznacza przydzielenie obiektu.</span><span class="sxs-lookup"><span data-stu-id="823fb-264">`Task` is a reference type, so using it means allocating an object.</span></span> <span data-ttu-id="823fb-265">W przypadkach, gdy metoda zadeklarowana za pomocą modyfikatora `async` zwraca zbuforowany wynik lub zostanie zakończona synchronicznie, dodatkowe alokacje mogą stać się ważnym kosztem w przypadku krytycznych sekcji kodu.</span><span class="sxs-lookup"><span data-stu-id="823fb-265">In cases where a method declared with the `async` modifier returns a cached result, or completes synchronously, the extra allocations can become a significant time cost in performance critical sections of code.</span></span> <span data-ttu-id="823fb-266">Może być kosztowna, jeśli te przydziały występują w ścisłych pętlach.</span><span class="sxs-lookup"><span data-stu-id="823fb-266">It can become costly if those allocations occur in tight loops.</span></span>

<span data-ttu-id="823fb-267">Nowa funkcja języka oznacza, że typy zwracające metodę asynchroniczną nie są ograniczone do `Task`, `Task<T>`i `void`.</span><span class="sxs-lookup"><span data-stu-id="823fb-267">The new language feature means that async method return types aren't limited to `Task`, `Task<T>`, and `void`.</span></span> <span data-ttu-id="823fb-268">Zwracany typ musi nadal spełniać wzorzec asynchroniczny, co oznacza, że metoda `GetAwaiter` musi być dostępna.</span><span class="sxs-lookup"><span data-stu-id="823fb-268">The returned type must still satisfy the async pattern, meaning a `GetAwaiter` method must be accessible.</span></span> <span data-ttu-id="823fb-269">W jednym z konkretnych przykładów typ `ValueTask` został dodany do programu .NET Framework, aby można było korzystać z tej nowej funkcji języka:</span><span class="sxs-lookup"><span data-stu-id="823fb-269">As one concrete example, the `ValueTask` type has been added to the .NET framework to make use of this new language feature:</span></span>

[!code-csharp[UsingValueTask](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#UsingValueTask "Using ValueTask")]

> [!NOTE]
> <span data-ttu-id="823fb-270">Należy dodać pakiet NuGet [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) , aby można było użyć typu <xref:System.Threading.Tasks.ValueTask%601>.</span><span class="sxs-lookup"><span data-stu-id="823fb-270">You need to add the NuGet package [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) in order to use the <xref:System.Threading.Tasks.ValueTask%601> type.</span></span>

<span data-ttu-id="823fb-271">To ulepszenie jest najbardziej przydatne dla autorów bibliotek, aby uniknąć alokowania `Task` w kodzie krytycznym wydajności.</span><span class="sxs-lookup"><span data-stu-id="823fb-271">This enhancement is most useful for library authors to avoid allocating a `Task` in performance critical code.</span></span>

## <a name="numeric-literal-syntax-improvements"></a><span data-ttu-id="823fb-272">Udoskonalenia składni literału liczbowego</span><span class="sxs-lookup"><span data-stu-id="823fb-272">Numeric literal syntax improvements</span></span>

<span data-ttu-id="823fb-273">Nieodczytanie stałych numerycznych może utrudnić zrozumienie kodu podczas odczytywania go po raz pierwszy.</span><span class="sxs-lookup"><span data-stu-id="823fb-273">Misreading numeric constants can make it harder to understand code when reading it for the first time.</span></span> <span data-ttu-id="823fb-274">Maski bitowe lub inne wartości symboliczne są podatne na nieporozumienia.</span><span class="sxs-lookup"><span data-stu-id="823fb-274">Bit masks or other symbolic values are prone to misunderstanding.</span></span> <span data-ttu-id="823fb-275">C#7,0 obejmuje dwie nowe funkcje do zapisu liczb w najbardziej czytelny sposób dla zamierzonego użycia: *literały binarne*oraz *separatory cyfr*.</span><span class="sxs-lookup"><span data-stu-id="823fb-275">C# 7.0 includes two new features to write numbers in the most readable fashion for the intended use: *binary literals*, and *digit separators*.</span></span>

<span data-ttu-id="823fb-276">Dla tych czasów podczas tworzenia masek bitowych lub za każdym razem, gdy binarna reprezentacja liczby sprawia, że jest to najbardziej czytelny kod, Zapisz ten numer w postaci binarnej:</span><span class="sxs-lookup"><span data-stu-id="823fb-276">For those times when you're creating bit masks, or whenever a binary representation of a number makes the most readable code, write that number in binary:</span></span>

[!code-csharp[ThousandSeparators](~/samples/snippets/csharp/new-in-7/Program.cs#ThousandSeparators "Thousands separators")]

<span data-ttu-id="823fb-277">`0b` na początku stałej wskazuje, że liczba jest zapisywana jako liczba binarna.</span><span class="sxs-lookup"><span data-stu-id="823fb-277">The `0b` at the beginning of the constant indicates that the number is written as a binary number.</span></span> <span data-ttu-id="823fb-278">Liczby binarne mogą być długie, więc często łatwiej jest zobaczyć wzorce bitowe, wprowadzając `_` jako separator cyfr, jak pokazano powyżej w stałej liczbie binarnej.</span><span class="sxs-lookup"><span data-stu-id="823fb-278">Binary numbers can get long, so it's often easier to see the bit patterns by introducing the `_` as a digit separator, as shown above in the binary constant.</span></span> <span data-ttu-id="823fb-279">Separator cyfr może pojawić się w dowolnym miejscu w stałej.</span><span class="sxs-lookup"><span data-stu-id="823fb-279">The digit separator can appear anywhere in the constant.</span></span> <span data-ttu-id="823fb-280">W przypadku numerów podstawowych 10 często należy używać jej jako separatora tysięcy:</span><span class="sxs-lookup"><span data-stu-id="823fb-280">For base 10 numbers, it is common to use it as a thousands separator:</span></span>

[!code-csharp[LargeIntegers](~/samples/snippets/csharp/new-in-7/Program.cs#LargeIntegers "Large integer")]

<span data-ttu-id="823fb-281">Separator cyfr może być również używany z typami `decimal`, `float`i `double`:</span><span class="sxs-lookup"><span data-stu-id="823fb-281">The digit separator can be used with `decimal`, `float`, and `double` types as well:</span></span>

[!code-csharp[OtherConstants](~/samples/snippets/csharp/new-in-7/Program.cs#OtherConstants "non-integral constants")]

<span data-ttu-id="823fb-282">Ze sobą można zadeklarować stałe liczbowe o znacznie większej czytelności.</span><span class="sxs-lookup"><span data-stu-id="823fb-282">Taken together, you can declare numeric constants with much more readability.</span></span>
