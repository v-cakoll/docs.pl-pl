---
title: Formaty ścieżek plików w systemie Windows
ms.date: 06/06/2019
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- I/O, long paths
- long paths
- path formats, Windows
ms.openlocfilehash: 258cf59fb8383fe131f4a0e78dac6189e1d9c91e
ms.sourcegitcommit: 30a558d23e3ac5a52071121a52c305c85fe15726
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 12/25/2019
ms.locfileid: "75337670"
---
# <a name="file-path-formats-on-windows-systems"></a><span data-ttu-id="e161f-102">Formaty ścieżek plików w systemie Windows</span><span class="sxs-lookup"><span data-stu-id="e161f-102">File path formats on Windows systems</span></span>

<span data-ttu-id="e161f-103">Elementy członkowskie wielu typów w przestrzeni nazw <xref:System.IO> zawierają parametr `path`, który pozwala określić ścieżkę bezwzględną lub względną do zasobu systemu plików.</span><span class="sxs-lookup"><span data-stu-id="e161f-103">Members of many of the types in the <xref:System.IO> namespace include a `path` parameter that lets you specify an absolute or relative path to a file system resource.</span></span> <span data-ttu-id="e161f-104">Ta ścieżka jest następnie przesyłana do [interfejsów API systemu plików Windows](/windows/desktop/fileio/file-systems).</span><span class="sxs-lookup"><span data-stu-id="e161f-104">This path is then passed to [Windows file system APIs](/windows/desktop/fileio/file-systems).</span></span> <span data-ttu-id="e161f-105">W tym temacie omówiono formaty ścieżek plików, których można używać w systemach Windows.</span><span class="sxs-lookup"><span data-stu-id="e161f-105">This topic discusses the formats for file paths that you can use on Windows systems.</span></span>

## <a name="traditional-dos-paths"></a><span data-ttu-id="e161f-106">Tradycyjne ścieżki DOS</span><span class="sxs-lookup"><span data-stu-id="e161f-106">Traditional DOS paths</span></span>

<span data-ttu-id="e161f-107">Standardowa ścieżka DOS może składać się z trzech składników:</span><span class="sxs-lookup"><span data-stu-id="e161f-107">A standard DOS path can consist of three components:</span></span>

- <span data-ttu-id="e161f-108">Litera woluminu lub dysku, po którym następuje separator woluminu (`:`).</span><span class="sxs-lookup"><span data-stu-id="e161f-108">A volume or drive letter followed by the volume separator (`:`).</span></span>
- <span data-ttu-id="e161f-109">Nazwa katalogu.</span><span class="sxs-lookup"><span data-stu-id="e161f-109">A directory name.</span></span> <span data-ttu-id="e161f-110">[Znak separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>) oddziela podkatalogi w hierarchii katalogów zagnieżdżonych.</span><span class="sxs-lookup"><span data-stu-id="e161f-110">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates subdirectories within the nested directory hierarchy.</span></span>
- <span data-ttu-id="e161f-111">Opcjonalna nazwa pliku.</span><span class="sxs-lookup"><span data-stu-id="e161f-111">An optional filename.</span></span> <span data-ttu-id="e161f-112">[Znak separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>) oddziela ścieżkę pliku i nazwę pliku.</span><span class="sxs-lookup"><span data-stu-id="e161f-112">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates the file path and the filename.</span></span>

<span data-ttu-id="e161f-113">Jeśli wszystkie trzy składniki są obecne, ścieżka jest bezwzględna.</span><span class="sxs-lookup"><span data-stu-id="e161f-113">If all three components are present, the path is absolute.</span></span> <span data-ttu-id="e161f-114">Jeśli nie określono woluminu ani litery dysku, a nazwa katalogu rozpoczyna się od [znaku separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>), ścieżka jest określana względem katalogu głównego bieżącego dysku.</span><span class="sxs-lookup"><span data-stu-id="e161f-114">If no volume or drive letter is specified and the directory name begins with the [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>), the path is relative from the root of the current drive.</span></span> <span data-ttu-id="e161f-115">W przeciwnym razie ścieżka jest określana względem bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="e161f-115">Otherwise, the path is relative to the current directory.</span></span> <span data-ttu-id="e161f-116">W poniższej tabeli przedstawiono niektóre możliwe ścieżki katalogów i plików.</span><span class="sxs-lookup"><span data-stu-id="e161f-116">The following table shows some possible directory and file paths.</span></span>

|<span data-ttu-id="e161f-117">Ścieżka</span><span class="sxs-lookup"><span data-stu-id="e161f-117">Path</span></span>  |<span data-ttu-id="e161f-118">Opis</span><span class="sxs-lookup"><span data-stu-id="e161f-118">Description</span></span>  |
| -- | -- |
| `C:\Documents\Newsletters\Summer2018.pdf` | <span data-ttu-id="e161f-119">Bezwzględna ścieżka pliku z katalogu głównego dysku C:</span><span class="sxs-lookup"><span data-stu-id="e161f-119">An absolute file path from the root of drive C:</span></span> |
| `\Program Files\Custom Utilities\StringFinder.exe` | <span data-ttu-id="e161f-120">Ścieżka bezwzględna z katalogu głównego bieżącego dysku.</span><span class="sxs-lookup"><span data-stu-id="e161f-120">An absolute path from the root of the current drive.</span></span> |
| `2018\January.xlsx` | <span data-ttu-id="e161f-121">Ścieżka względna do pliku w podkatalogu bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="e161f-121">A relative path to a file in a subdirectory of the current directory.</span></span> |
| `..\Publications\TravelBrochure.pdf` | <span data-ttu-id="e161f-122">Ścieżka względna do pliku w katalogu, który jest elementem równorzędnym bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="e161f-122">A relative path to file in a directory that is a peer of the current directory.</span></span> |
| `C:\Projects\apilibrary\apilibrary.sln` | <span data-ttu-id="e161f-123">Ścieżka bezwzględna do pliku z katalogu głównego dysku C:</span><span class="sxs-lookup"><span data-stu-id="e161f-123">An absolute path to a file from the root of drive C:</span></span> |
| `C:Projects\apilibrary\apilibrary.sln` | <span data-ttu-id="e161f-124">Ścieżka względna z bieżącego katalogu dysku C:.</span><span class="sxs-lookup"><span data-stu-id="e161f-124">A relative path from the current directory of the C: drive.</span></span> |

> [!IMPORTANT]
> <span data-ttu-id="e161f-125">Zwróć uwagę na różnicę między ostatnimi dwiema ścieżkami.</span><span class="sxs-lookup"><span data-stu-id="e161f-125">Note the difference between the last two paths.</span></span> <span data-ttu-id="e161f-126">Oba określają opcjonalny specyfikator woluminu (C: w obu przypadkach), ale pierwszy zaczyna się od elementu głównego określonego woluminu, a drugi nie.</span><span class="sxs-lookup"><span data-stu-id="e161f-126">Both specify the optional volume specifier (C: in both cases), but the first begins with the root of the specified volume, whereas the second does not.</span></span> <span data-ttu-id="e161f-127">W związku z tym pierwsza jest ścieżką bezwzględną z katalogu głównego dysku C:, a druga jest ścieżką względną z bieżącego katalogu dysku C:.</span><span class="sxs-lookup"><span data-stu-id="e161f-127">As result, the first is an absolute path from the root directory of drive C:, whereas the second is a relative path from the current directory of drive C:.</span></span> <span data-ttu-id="e161f-128">Użycie drugiego formularza, gdy pierwszy jest zamierzony, jest wspólnym źródłem błędów, które obejmują ścieżki plików systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="e161f-128">Use of the second form when the first is intended is a common source of bugs that involve Windows file paths.</span></span>

<span data-ttu-id="e161f-129">Można określić, czy ścieżka pliku jest w pełni kwalifikowana (czyli czy ścieżka jest niezależna od bieżącego katalogu i nie ulega zmianie, gdy bieżący katalog ulegnie zmianie) przez wywołanie metody <xref:System.IO.Path.IsPathFullyQualified%2A?displayProperty=nameWthType>.</span><span class="sxs-lookup"><span data-stu-id="e161f-129">You can determine whether a file path is fully qualified (that is, it the path is independent of the current directory and does not change when the current directory changes) by calling the <xref:System.IO.Path.IsPathFullyQualified%2A?displayProperty=nameWthType> method.</span></span> <span data-ttu-id="e161f-130">Należy zauważyć, że taka ścieżka może zawierać względne segmenty katalogów (`.` i `..`) i nadal być w pełni kwalifikowane, jeśli rozpoznana ścieżka zawsze wskazuje tę samą lokalizację.</span><span class="sxs-lookup"><span data-stu-id="e161f-130">Note that such a path can include relative directory segments (`.` and `..`) and still be fully qualified if the resolved path always points to the same location.</span></span>

<span data-ttu-id="e161f-131">Poniższy przykład ilustruje różnicę między ścieżkami bezwzględnymi i względnymi.</span><span class="sxs-lookup"><span data-stu-id="e161f-131">The following example illustrates the difference between absolute and relative paths.</span></span> <span data-ttu-id="e161f-132">Przyjęto założenie, że katalog D:\FY2018\ istnieje i nie ustawił bieżącego katalogu dla D:\ w wierszu polecenia przed uruchomieniem przykładu.</span><span class="sxs-lookup"><span data-stu-id="e161f-132">It assumes that the directory D:\FY2018\ exists, and that you haven't set any current directory for D:\ from the command prompt before running the example.</span></span>

[!code-csharp[absolute-and-relative-paths](~/samples/snippets/standard/io/file-names/cs/paths.cs)]
[!code-vb[absolute-and-relative-paths](~/samples/snippets/standard/io/file-names/vb/paths.vb)]

## <a name="unc-paths"></a><span data-ttu-id="e161f-133">Ścieżki UNC</span><span class="sxs-lookup"><span data-stu-id="e161f-133">UNC paths</span></span>

<span data-ttu-id="e161f-134">Ścieżki Universal Naming Convention (UNC), które są używane do uzyskiwania dostępu do zasobów sieciowych, mają następujący format:</span><span class="sxs-lookup"><span data-stu-id="e161f-134">Universal naming convention (UNC) paths, which are used to access network resources, have the following format:</span></span>

- <span data-ttu-id="e161f-135">Nazwa serwera lub hosta, które jest poprzedzone \\\\.</span><span class="sxs-lookup"><span data-stu-id="e161f-135">A server or host name, which is prefaced by \\\\.</span></span> <span data-ttu-id="e161f-136">Nazwa serwera może być nazwą maszyny NetBIOS lub adresem IP/FQDN (obsługiwane są również adresy IPv4 i V6).</span><span class="sxs-lookup"><span data-stu-id="e161f-136">The server name can be a NetBIOS machine name or an IP/FQDN address (IPv4 as well as v6 are supported).</span></span>
- <span data-ttu-id="e161f-137">Nazwa udziału, która jest oddzielona od nazwy hosta przez \\.</span><span class="sxs-lookup"><span data-stu-id="e161f-137">A share name, which is separated from the host name by \\.</span></span> <span data-ttu-id="e161f-138">Razem serwer i nazwa udziału tworzą wolumin.</span><span class="sxs-lookup"><span data-stu-id="e161f-138">Together, the server and share name make up the volume.</span></span>
- <span data-ttu-id="e161f-139">Nazwa katalogu.</span><span class="sxs-lookup"><span data-stu-id="e161f-139">A directory name.</span></span> <span data-ttu-id="e161f-140">[Znak separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>) oddziela podkatalogi w hierarchii katalogów zagnieżdżonych.</span><span class="sxs-lookup"><span data-stu-id="e161f-140">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates subdirectories within the nested directory hierarchy.</span></span>
- <span data-ttu-id="e161f-141">Opcjonalna nazwa pliku.</span><span class="sxs-lookup"><span data-stu-id="e161f-141">An optional filename.</span></span> <span data-ttu-id="e161f-142">[Znak separatora katalogu](<xref:System.IO.Path.DirectorySeparatorChar>) oddziela ścieżkę pliku i nazwę pliku.</span><span class="sxs-lookup"><span data-stu-id="e161f-142">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates the file path and the filename.</span></span>

<span data-ttu-id="e161f-143">Poniżej przedstawiono kilka przykładów ścieżek UNC:</span><span class="sxs-lookup"><span data-stu-id="e161f-143">The following are some examples of UNC paths:</span></span>

|<span data-ttu-id="e161f-144">Ścieżka</span><span class="sxs-lookup"><span data-stu-id="e161f-144">Path</span></span>  |<span data-ttu-id="e161f-145">Opis</span><span class="sxs-lookup"><span data-stu-id="e161f-145">Description</span></span>  |
| -- | -- |
| `\\system07\C$\` | <span data-ttu-id="e161f-146">Katalog główny dysku C: na `system07`.</span><span class="sxs-lookup"><span data-stu-id="e161f-146">The root directory of the C: drive on `system07`.</span></span> |
| `\\Server2\Share\Test\Foo.txt` | <span data-ttu-id="e161f-147">Plik foo. txt w katalogu testowym \\\\Serwer2\\udziału woluminu.</span><span class="sxs-lookup"><span data-stu-id="e161f-147">The Foo.txt file in the Test directory of the \\\\Server2\\Share volume.</span></span>|

<span data-ttu-id="e161f-148">Ścieżki UNC muszą zawsze być w pełni kwalifikowane.</span><span class="sxs-lookup"><span data-stu-id="e161f-148">UNC paths must always be fully qualified.</span></span> <span data-ttu-id="e161f-149">Mogą one zawierać względne segmenty katalogów (`.` i `..`), ale muszą być częścią w pełni kwalifikowanej ścieżki.</span><span class="sxs-lookup"><span data-stu-id="e161f-149">They can include relative directory segments (`.` and `..`), but these must be part of a fully qualified path.</span></span> <span data-ttu-id="e161f-150">Ścieżek względnych można używać tylko przez mapowanie ścieżki UNC na literę dysku.</span><span class="sxs-lookup"><span data-stu-id="e161f-150">You can use relative paths only by mapping a UNC path to a drive letter.</span></span>

## <a name="dos-device-paths"></a><span data-ttu-id="e161f-151">Ścieżki urządzeń DOS</span><span class="sxs-lookup"><span data-stu-id="e161f-151">DOS device paths</span></span>

<span data-ttu-id="e161f-152">System operacyjny Windows ma ujednolicony model obiektów, który wskazuje na wszystkie zasoby, w tym pliki.</span><span class="sxs-lookup"><span data-stu-id="e161f-152">The Windows operating system has a unified object model that points to all resources, including files.</span></span> <span data-ttu-id="e161f-153">Te ścieżki obiektów są dostępne z okna konsoli i są widoczne dla warstwy Win32 za pośrednictwem specjalnego folderu linków symbolicznych, do których są mapowane starsze ścieżki DOS i UNC.</span><span class="sxs-lookup"><span data-stu-id="e161f-153">These object paths are accessible from the console window and are exposed to the Win32 layer through a special folder of symbolic links that legacy DOS and UNC paths are mapped to.</span></span> <span data-ttu-id="e161f-154">Dostęp do tego folderu specjalnego uzyskuje się za pośrednictwem składni ścieżki urządzenia DOS, która jest jedną z:</span><span class="sxs-lookup"><span data-stu-id="e161f-154">This special folder is accessed via the DOS device path syntax, which is one of:</span></span>

`\\.\C:\Test\Foo.txt`
`\\?\C:\Test\Foo.txt`

<span data-ttu-id="e161f-155">Oprócz identyfikowania dysku za pomocą jego litery dysku, można zidentyfikować wolumin przy użyciu identyfikatora GUID woluminu.</span><span class="sxs-lookup"><span data-stu-id="e161f-155">In addition to identifying a drive by its drive letter, you can identify a volume by using its volume GUID.</span></span> <span data-ttu-id="e161f-156">Ma to formę:</span><span class="sxs-lookup"><span data-stu-id="e161f-156">This takes the form:</span></span>

`\\.\Volume{b75e2c83-0000-0000-0000-602f00000000}\Test\Foo.txt`
`\\?\Volume{b75e2c83-0000-0000-0000-602f00000000}\Test\Foo.txt`

> [!NOTE]
> <span data-ttu-id="e161f-157">Składnia ścieżki urządzenia DOS jest obsługiwana w implementacjach platformy .NET działających w systemie Windows, począwszy od platformy .NET Core 1,1 i .NET Framework 4.6.2.</span><span class="sxs-lookup"><span data-stu-id="e161f-157">DOS device path syntax is supported on .NET implementations running on Windows starting with .NET Core 1.1 and .NET Framework 4.6.2.</span></span>

<span data-ttu-id="e161f-158">Ścieżka urządzenia DOS składa się z następujących składników:</span><span class="sxs-lookup"><span data-stu-id="e161f-158">The DOS device path consists of the following components:</span></span>

- <span data-ttu-id="e161f-159">Specyfikator ścieżki urządzenia (`\\.\` lub `\\?\`), który identyfikuje ścieżkę jako ścieżkę urządzenia DOS.</span><span class="sxs-lookup"><span data-stu-id="e161f-159">The device path specifier (`\\.\` or `\\?\`), which identifies the path as a DOS device path.</span></span>

   > [!NOTE]
   > <span data-ttu-id="e161f-160">`\\?\` jest obsługiwane we wszystkich wersjach programu .NET Core i w .NET Framework, począwszy od wersji 4.6.2.</span><span class="sxs-lookup"><span data-stu-id="e161f-160">The `\\?\` is supported in all versions of .NET Core and in the .NET Framework starting with version 4.6.2.</span></span>

- <span data-ttu-id="e161f-161">Symboliczny link do obiektu urządzenia "Real" (C: w przypadku nazwy dysku lub woluminu {b75e2c83-0000-0000-0000-602f00000000} w przypadku identyfikatora GUID woluminu).</span><span class="sxs-lookup"><span data-stu-id="e161f-161">A symbolic link to the "real" device object (C: in the case of a drive name, or Volume{b75e2c83-0000-0000-0000-602f00000000} in the case of a volume GUID).</span></span>

   <span data-ttu-id="e161f-162">Pierwszy segment ścieżki urządzenia DOS po specyfikator ścieżki urządzenia identyfikuje wolumin lub dysk.</span><span class="sxs-lookup"><span data-stu-id="e161f-162">The first segment of the DOS device path after the device path specifier identifies the volume or drive.</span></span> <span data-ttu-id="e161f-163">(Na przykład `\\?\C:\` i `\\.\BootPartition\`.)</span><span class="sxs-lookup"><span data-stu-id="e161f-163">(For example, `\\?\C:\` and `\\.\BootPartition\`.)</span></span>

   <span data-ttu-id="e161f-164">Istnieje konkretne łącze dla UNCs o nazwie, a nie Surprisingly, `UNC`.</span><span class="sxs-lookup"><span data-stu-id="e161f-164">There is a specific link for UNCs that is called, not surprisingly, `UNC`.</span></span> <span data-ttu-id="e161f-165">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="e161f-165">For example:</span></span>

  `\\.\UNC\Server\Share\Test\Foo.txt`
  `\\?\UNC\Server\Share\Test\Foo.txt`

    <span data-ttu-id="e161f-166">W przypadku urządzenia UNCs część serwer/udział tworzy wolumin.</span><span class="sxs-lookup"><span data-stu-id="e161f-166">For device UNCs, the server/share portion forms the volume.</span></span> <span data-ttu-id="e161f-167">Na przykład w `\\?\server1\e:\utilities\\filecomparer\`część serwer/udział jest server1\utilities.</span><span class="sxs-lookup"><span data-stu-id="e161f-167">For example, in `\\?\server1\e:\utilities\\filecomparer\`, the server/share portion is server1\utilities.</span></span> <span data-ttu-id="e161f-168">Jest to istotne w przypadku wywołania metody, takiej jak <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> z względnymi segmentami katalogu; nie jest możliwe przechodzenie poza wolumin.</span><span class="sxs-lookup"><span data-stu-id="e161f-168">This is significant when calling a method such as <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> with relative directory segments; it is never possible to navigate past the volume.</span></span>

<span data-ttu-id="e161f-169">Ścieżki urządzeń systemu DOS są w pełni kwalifikowane według definicji.</span><span class="sxs-lookup"><span data-stu-id="e161f-169">DOS device paths are fully qualified by definition.</span></span> <span data-ttu-id="e161f-170">Powiązane segmenty katalogu (`.` i `..`) są niedozwolone.</span><span class="sxs-lookup"><span data-stu-id="e161f-170">Relative directory segments (`.` and `..`) are not allowed.</span></span> <span data-ttu-id="e161f-171">Bieżące katalogi nigdy nie są wprowadzane do ich użycia.</span><span class="sxs-lookup"><span data-stu-id="e161f-171">Current directories never enter into their usage.</span></span>

## <a name="example-ways-to-refer-to-the-same-file"></a><span data-ttu-id="e161f-172">Przykład: sposoby odwoływania się do tego samego pliku</span><span class="sxs-lookup"><span data-stu-id="e161f-172">Example: Ways to refer to the same file</span></span>

<span data-ttu-id="e161f-173">Poniższy przykład ilustruje kilka sposobów, w których można odwoływać się do pliku w przypadku używania interfejsów API w przestrzeni nazw <xref:System.IO>.</span><span class="sxs-lookup"><span data-stu-id="e161f-173">The following example illustrates some of the ways in which you can refer to a file when using the APIs in the <xref:System.IO> namespace.</span></span> <span data-ttu-id="e161f-174">Przykład tworzy wystąpienie obiektu <xref:System.IO.FileInfo> i używa jego właściwości <xref:System.IO.FileInfo.Name> i <xref:System.IO.FileInfo.Length> do wyświetlania nazwy pliku i długości pliku.</span><span class="sxs-lookup"><span data-stu-id="e161f-174">The example instantiates a <xref:System.IO.FileInfo> object and uses its <xref:System.IO.FileInfo.Name> and <xref:System.IO.FileInfo.Length> properties to display the filename and the length of the file.</span></span>

[!code-csharp[referring-to-the-same-file](~/samples/snippets/standard/io/file-names/cs/file-refs.cs)]
[!code-vb[referring-to-the-same-file](~/samples/snippets/standard/io/file-names/vb/file-refs.vb)]

## <a name="path-normalization"></a><span data-ttu-id="e161f-175">Normalizacja ścieżki</span><span class="sxs-lookup"><span data-stu-id="e161f-175">Path normalization</span></span>

<span data-ttu-id="e161f-176">Prawie wszystkie ścieżki przesyłane do interfejsów API systemu Windows są znormalizowane.</span><span class="sxs-lookup"><span data-stu-id="e161f-176">Almost all paths passed to Windows APIs are normalized.</span></span> <span data-ttu-id="e161f-177">Podczas normalizacji system Windows wykonuje następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="e161f-177">During normalization, Windows performs the following steps:</span></span>

- <span data-ttu-id="e161f-178">Identyfikuje ścieżkę.</span><span class="sxs-lookup"><span data-stu-id="e161f-178">Identifies the path.</span></span>
- <span data-ttu-id="e161f-179">Stosuje bieżący katalog do częściowo kwalifikowanych (względnych) ścieżek.</span><span class="sxs-lookup"><span data-stu-id="e161f-179">Applies the current directory to partially qualified (relative) paths.</span></span>
- <span data-ttu-id="e161f-180">Separatory składników Canonicalizes i katalogów.</span><span class="sxs-lookup"><span data-stu-id="e161f-180">Canonicalizes component and directory separators.</span></span>
- <span data-ttu-id="e161f-181">Oblicza powiązane składniki katalogu (`.` dla bieżącego katalogu i `..` dla katalogu nadrzędnego).</span><span class="sxs-lookup"><span data-stu-id="e161f-181">Evaluates relative directory components (`.` for the current directory and `..` for the parent directory).</span></span>
- <span data-ttu-id="e161f-182">Przycina określone znaki.</span><span class="sxs-lookup"><span data-stu-id="e161f-182">Trims certain characters.</span></span>

<span data-ttu-id="e161f-183">Ta normalizacja występuje niejawnie, ale można ją jawnie, wywołując metodę <xref:System.IO.Path.GetFullPath%2A?displayProperty=nameWithType>, która otacza wywołanie [funkcji GetFullPathName ()](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).</span><span class="sxs-lookup"><span data-stu-id="e161f-183">This normalization happens implicitly, but you can do it explicitly by calling the <xref:System.IO.Path.GetFullPath%2A?displayProperty=nameWithType> method, which wraps a call to the  [GetFullPathName() function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).</span></span> <span data-ttu-id="e161f-184">Możesz również wywołać [funkcję GetFullPathName systemu Windows ()](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) bezpośrednio przy użyciu metody P/Invoke.</span><span class="sxs-lookup"><span data-stu-id="e161f-184">You can also call the Windows [GetFullPathName() function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) directly using P/Invoke.</span></span>

### <a name="identifying-the-path"></a><span data-ttu-id="e161f-185">Identyfikowanie ścieżki</span><span class="sxs-lookup"><span data-stu-id="e161f-185">Identifying the path</span></span>

<span data-ttu-id="e161f-186">Pierwszy krok w normalizacji ścieżki identyfikuje typ ścieżki.</span><span class="sxs-lookup"><span data-stu-id="e161f-186">The first step in path normalization is identifying the type of path.</span></span> <span data-ttu-id="e161f-187">Ścieżki mieszczą się w jednej z kilku kategorii:</span><span class="sxs-lookup"><span data-stu-id="e161f-187">Paths fall into one of a few categories:</span></span>

- <span data-ttu-id="e161f-188">Są to ścieżki urządzeń; oznacza to, że zaczynają się od dwóch separatorów i znaku zapytania lub kropki (`\\?` lub `\\.`).</span><span class="sxs-lookup"><span data-stu-id="e161f-188">They are device paths; that is, they begin with two separators and a question mark or period (`\\?` or `\\.`).</span></span>
- <span data-ttu-id="e161f-189">Są to ścieżki UNC; oznacza to, że zaczynają się od dwóch separatorów bez znaku zapytania lub kropki.</span><span class="sxs-lookup"><span data-stu-id="e161f-189">They are UNC paths; that is, they begin with two separators without a question mark or period.</span></span>
- <span data-ttu-id="e161f-190">Są to w pełni kwalifikowane ścieżki DOS; oznacza to, że zaczynają się literą dysku, separatorem woluminów i separatorem składników (`C:\`).</span><span class="sxs-lookup"><span data-stu-id="e161f-190">They are fully qualified DOS paths; that is, they begin with a drive letter, a volume separator, and a component separator (`C:\`).</span></span>
- <span data-ttu-id="e161f-191">Wyznaczy starsze urządzenie (`CON`, `LPT1`).</span><span class="sxs-lookup"><span data-stu-id="e161f-191">They designate a legacy device (`CON`, `LPT1`).</span></span>
- <span data-ttu-id="e161f-192">Są one względne dla katalogu głównego bieżącego dysku; oznacza to, że zaczynają się od pojedynczego separatora składnika (`\`).</span><span class="sxs-lookup"><span data-stu-id="e161f-192">They are relative to the root of the current drive; that is, they begin with a single component separator (`\`).</span></span>
- <span data-ttu-id="e161f-193">Są one względne wobec bieżącego katalogu określonego dysku; oznacza to, że zaczynają się literą dysku, separatorem woluminów i bez separatora składników (`C:`).</span><span class="sxs-lookup"><span data-stu-id="e161f-193">They are relative to the current directory of a specified drive; that is, they begin with a drive letter, a volume separator, and no component separator (`C:`).</span></span>
- <span data-ttu-id="e161f-194">Są one względne wobec bieżącego katalogu; oznacza to, że zaczynają się one jakimkolwiek innym (`temp\testfile.txt`).</span><span class="sxs-lookup"><span data-stu-id="e161f-194">They are relative to the current directory; that is, they begin with anything else (`temp\testfile.txt`).</span></span>

<span data-ttu-id="e161f-195">Typ ścieżki Określa, czy bieżący katalog jest stosowany w jakiś sposób.</span><span class="sxs-lookup"><span data-stu-id="e161f-195">The type of the path determines whether or not a current directory is applied in some way.</span></span> <span data-ttu-id="e161f-196">Określa również, jak "root" ścieżki.</span><span class="sxs-lookup"><span data-stu-id="e161f-196">It also determines what the "root" of the path is.</span></span>

### <a name="handling-legacy-devices"></a><span data-ttu-id="e161f-197">Obsługa starszych urządzeń</span><span class="sxs-lookup"><span data-stu-id="e161f-197">Handling legacy devices</span></span>

<span data-ttu-id="e161f-198">Jeśli ścieżka to starsze urządzenie systemu DOS, takie jak `CON`, `COM1`lub `LPT1`, jest konwertowane na ścieżkę urządzenia, w zależności od tego, `\\.\` i zwrócone.</span><span class="sxs-lookup"><span data-stu-id="e161f-198">If the path is a legacy DOS device such as `CON`, `COM1`, or `LPT1`, it is converted into a device path by prepending `\\.\` and returned.</span></span>

<span data-ttu-id="e161f-199">Ścieżka rozpoczynająca się od starszej nazwy urządzenia jest zawsze interpretowana jako starsze urządzenie przez metodę <xref:System.IO.Path.GetFullPath(System.String)?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="e161f-199">A path that begins with a legacy device name is always interpreted as a legacy device by the <xref:System.IO.Path.GetFullPath(System.String)?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="e161f-200">Na przykład ścieżka urządzenia DOS dla `CON.TXT` jest `\\.\CON`, a ścieżka urządzenia DOS dla `COM1.TXT\file1.txt` jest `\\.\COM1`.</span><span class="sxs-lookup"><span data-stu-id="e161f-200">For example, the DOS device path for `CON.TXT` is `\\.\CON`, and the DOS device path for `COM1.TXT\file1.txt` is `\\.\COM1`.</span></span>

### <a name="applying-the-current-directory"></a><span data-ttu-id="e161f-201">Stosowanie bieżącego katalogu</span><span class="sxs-lookup"><span data-stu-id="e161f-201">Applying the current directory</span></span>

<span data-ttu-id="e161f-202">Jeśli ścieżka nie jest w pełni kwalifikowana, system Windows stosuje bieżący katalog.</span><span class="sxs-lookup"><span data-stu-id="e161f-202">If a path isn't fully qualified, Windows applies the current directory to it.</span></span> <span data-ttu-id="e161f-203">UNCs i ścieżki urządzeń nie mają zastosowanego bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="e161f-203">UNCs and device paths do not have the current directory applied.</span></span> <span data-ttu-id="e161f-204">Ani nie pełni dysku z separatorem C:\\.</span><span class="sxs-lookup"><span data-stu-id="e161f-204">Neither does a full drive with separator C:\\.</span></span>

<span data-ttu-id="e161f-205">Jeśli ścieżka rozpoczyna się od pojedynczego separatora składnika, zostanie zastosowany dysk z bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="e161f-205">If the path starts with a single component separator, the drive from the current directory is applied.</span></span> <span data-ttu-id="e161f-206">Na przykład jeśli ścieżka pliku jest `\utilities` a bieżący katalog jest `C:\temp\`, normalizacja produkuje `C:\utilities`.</span><span class="sxs-lookup"><span data-stu-id="e161f-206">For example, if the file path is `\utilities` and the current directory is `C:\temp\`, normalization produces `C:\utilities`.</span></span>

<span data-ttu-id="e161f-207">Jeśli ścieżka rozpoczyna się od litery dysku, separatora woluminu i bez separatora składników, zostanie zastosowany ostatni bieżący zestaw katalogów z powłoki poleceń dla określonego dysku.</span><span class="sxs-lookup"><span data-stu-id="e161f-207">If the path starts with a drive letter, volume separator, and no component separator, the last current directory set from the command shell for the specified drive is applied.</span></span> <span data-ttu-id="e161f-208">Jeśli ostatni bieżący katalog nie został ustawiony, tylko dysk zostanie zastosowany.</span><span class="sxs-lookup"><span data-stu-id="e161f-208">If the last current directory was not set, the drive alone is applied.</span></span> <span data-ttu-id="e161f-209">Na przykład jeśli ścieżka pliku jest `D:sources`, bieżący katalog jest `C:\Documents\`i ostatni bieżący katalog na dysku D: był `D:\sources\`, wynik jest `D:\sources\sources`.</span><span class="sxs-lookup"><span data-stu-id="e161f-209">For example, if the file path is `D:sources`, the current directory is `C:\Documents\`, and the last current directory on drive D: was `D:\sources\`, the result is `D:\sources\sources`.</span></span> <span data-ttu-id="e161f-210">Te ścieżki "względne" dysku są typowym źródłem błędów logiki programu i skryptów.</span><span class="sxs-lookup"><span data-stu-id="e161f-210">These "drive relative" paths are a common source of program and script logic errors.</span></span> <span data-ttu-id="e161f-211">Przy założeniu, że ścieżka rozpoczynająca się od litery i dwukropka nie jest poprawna.</span><span class="sxs-lookup"><span data-stu-id="e161f-211">Assuming that a path beginning with a letter and a colon isn't relative is obviously not correct.</span></span>

<span data-ttu-id="e161f-212">Jeśli ścieżka zaczyna się od czegoś innego niż separator, zostanie zastosowany bieżący dysk i bieżący katalog.</span><span class="sxs-lookup"><span data-stu-id="e161f-212">If the path starts with something other than a separator, the current drive and current directory are applied.</span></span> <span data-ttu-id="e161f-213">Na przykład jeśli ścieżka jest `filecompare` a bieżący katalog jest `C:\utilities\`, wynik jest `C:\utilities\filecompare\`.</span><span class="sxs-lookup"><span data-stu-id="e161f-213">For example, if the path is `filecompare` and the current directory is `C:\utilities\`, the result is `C:\utilities\filecompare\`.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="e161f-214">Ścieżki względne są niebezpieczne w aplikacjach wielowątkowych (czyli większości aplikacji), ponieważ bieżący katalog jest ustawieniem dla procesu.</span><span class="sxs-lookup"><span data-stu-id="e161f-214">Relative paths are dangerous in multithreaded applications (that is, most applications) because the current directory is a per-process setting.</span></span> <span data-ttu-id="e161f-215">Dowolny wątek może zmienić bieżący katalog w dowolnym momencie.</span><span class="sxs-lookup"><span data-stu-id="e161f-215">Any thread can change the current directory at any time.</span></span> <span data-ttu-id="e161f-216">Począwszy od platformy .NET Core 2,1, można wywołać metodę <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType>, aby uzyskać ścieżkę bezwzględną ze ścieżki względnej i ścieżki podstawowej (bieżącego katalogu), dla której chcesz rozwiązać ten problem.</span><span class="sxs-lookup"><span data-stu-id="e161f-216">Starting with .NET Core 2.1, you can call the <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> method to get an absolute path from a relative path and the base path (the current directory) that you want to resolve it against.</span></span>

### <a name="canonicalizing-separators"></a><span data-ttu-id="e161f-217">Separatory formę kanoniczną działa</span><span class="sxs-lookup"><span data-stu-id="e161f-217">Canonicalizing separators</span></span>

<span data-ttu-id="e161f-218">Wszystkie ukośniki (`/`) są konwertowane na standardowy separator systemu Windows, ukośnik odwrotny (`\`).</span><span class="sxs-lookup"><span data-stu-id="e161f-218">All forward slashes (`/`) are converted into the standard Windows separator, the back slash (`\`).</span></span> <span data-ttu-id="e161f-219">Jeśli są obecne, Seria ukośników, które są zgodne z dwoma ukośnikami, jest zwiniętych na pojedynczy ukośnik.</span><span class="sxs-lookup"><span data-stu-id="e161f-219">If they are present, a series of slashes that follow the first two slashes are collapsed into a single slash.</span></span>

### <a name="evaluating-relative-components"></a><span data-ttu-id="e161f-220">Ocenianie składników względnych</span><span class="sxs-lookup"><span data-stu-id="e161f-220">Evaluating relative components</span></span>

<span data-ttu-id="e161f-221">W miarę przetwarzania ścieżki są oceniane wszystkie składniki lub segmenty składające się z pojedynczego lub podwójnego okresu (`.` lub `..`):</span><span class="sxs-lookup"><span data-stu-id="e161f-221">As the path is processed, any components or segments that are composed of a single or a double period (`.` or `..`) are evaluated:</span></span>

- <span data-ttu-id="e161f-222">W przypadku pojedynczego okresu bieżący segment jest usuwany, ponieważ odnosi się do bieżącego katalogu.</span><span class="sxs-lookup"><span data-stu-id="e161f-222">For a single period, the current segment is removed, since it refers to the current directory.</span></span>

- <span data-ttu-id="e161f-223">W przypadku podwójnego okresu bieżący segment i segment nadrzędny są usuwane, ponieważ podwójny okres odwołuje się do katalogu nadrzędnego.</span><span class="sxs-lookup"><span data-stu-id="e161f-223">For a double period, the current segment and the parent segment are removed, since the double period refers to the parent directory.</span></span>

   <span data-ttu-id="e161f-224">Katalogi nadrzędne są usuwane tylko wtedy, gdy nie znajdują się poza katalogiem głównym ścieżki.</span><span class="sxs-lookup"><span data-stu-id="e161f-224">Parent directories are only removed if they aren't past the root of the path.</span></span> <span data-ttu-id="e161f-225">Katalog główny ścieżki zależy od typu ścieżki.</span><span class="sxs-lookup"><span data-stu-id="e161f-225">The root of the path depends on the type of path.</span></span> <span data-ttu-id="e161f-226">Jest to dysk (`C:\`) dla ścieżek DOS, serwer/udział dla UNCs (`\\Server\Share`) oraz prefiks ścieżki urządzenia dla ścieżek urządzeń (`\\?\` lub `\\.\`).</span><span class="sxs-lookup"><span data-stu-id="e161f-226">It is the drive (`C:\`) for DOS paths, the server/share for UNCs (`\\Server\Share`), and the device path prefix for device paths (`\\?\` or `\\.\`).</span></span>

### <a name="trimming-characters"></a><span data-ttu-id="e161f-227">Przycinanie znaków</span><span class="sxs-lookup"><span data-stu-id="e161f-227">Trimming characters</span></span>

<span data-ttu-id="e161f-228">Wraz z uruchomieniami separatorów i segmentów względnych usuniętych wcześniej niektóre dodatkowe znaki są usuwane podczas normalizacji:</span><span class="sxs-lookup"><span data-stu-id="e161f-228">Along with the runs of separators and relative segments removed earlier, some additional characters are removed during normalization:</span></span>

- <span data-ttu-id="e161f-229">Jeśli segment zostanie zakończony w pojedynczym okresie, ten okres jest usuwany.</span><span class="sxs-lookup"><span data-stu-id="e161f-229">If a segment ends in a single period, that period is removed.</span></span> <span data-ttu-id="e161f-230">(Segment pojedynczego lub podwójnego okresu jest znormalizowany w poprzednim kroku.</span><span class="sxs-lookup"><span data-stu-id="e161f-230">(A segment of a single or double period is normalized in the previous step.</span></span> <span data-ttu-id="e161f-231">Segment trzech lub więcej okresów nie jest znormalizowany i jest w rzeczywistości prawidłową nazwą pliku/katalogu.</span><span class="sxs-lookup"><span data-stu-id="e161f-231">A segment of three or more periods is not normalized and is actually a valid file/directory name.)</span></span>

- <span data-ttu-id="e161f-232">Jeśli ścieżka nie kończy się separatorem, wszystkie końcowe kropki i spacje (U + 0020) są usuwane.</span><span class="sxs-lookup"><span data-stu-id="e161f-232">If the path doesn't end in a separator, all trailing periods and spaces (U+0020) are removed.</span></span> <span data-ttu-id="e161f-233">Jeśli ostatni segment jest po prostu pojedynczym lub podwójnym okresem, jest on objęty powyższą regułą składników względnych.</span><span class="sxs-lookup"><span data-stu-id="e161f-233">If the last segment is simply a single or double period, it falls under the relative components rule above.</span></span>

   <span data-ttu-id="e161f-234">Ta reguła oznacza, że można utworzyć nazwę katalogu z końcowym miejscem, dodając separator końcowy po odstępie.</span><span class="sxs-lookup"><span data-stu-id="e161f-234">This rule means that you can create a directory name with a trailing space by adding a trailing separator after the space.</span></span>

   > [!IMPORTANT]
   > <span data-ttu-id="e161f-235">**Nigdy nie** należy tworzyć katalogów ani nazw plików z końcowym miejscem.</span><span class="sxs-lookup"><span data-stu-id="e161f-235">You should **never** create a directory or filename with a trailing space.</span></span> <span data-ttu-id="e161f-236">Spacje końcowe mogą być trudne lub niemożliwe do uzyskania dostępu do katalogu, a aplikacje często kończą się niepowodzeniem podczas próby obsługi katalogów lub plików, których nazwy zawierają spacje końcowe.</span><span class="sxs-lookup"><span data-stu-id="e161f-236">Trailing spaces can make it difficult or impossible to access a directory, and applications commonly fail when attempting to handle directories or files whose names include trailing spaces.</span></span>

## <a name="skipping-normalization"></a><span data-ttu-id="e161f-237">Pomijanie normalizacji</span><span class="sxs-lookup"><span data-stu-id="e161f-237">Skipping normalization</span></span>

<span data-ttu-id="e161f-238">Zwykle wszystkie ścieżki przesłane do interfejsu API systemu Windows są (efektywnie) przesyłane do [funkcji GetFullPathName](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) i znormalizowane.</span><span class="sxs-lookup"><span data-stu-id="e161f-238">Normally, any path passed to a Windows API is (effectively) passed to the [GetFullPathName function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) and normalized.</span></span> <span data-ttu-id="e161f-239">Istnieje jeden ważny wyjątek: ścieżka urządzenia rozpoczynająca się od znaku zapytania zamiast kropki.</span><span class="sxs-lookup"><span data-stu-id="e161f-239">There is one important exception: a device path that begins with a question mark instead of a period.</span></span> <span data-ttu-id="e161f-240">Chyba że ścieżka zaczyna się dokładnie od `\\?\` (Zwróć uwagę na użycie kanonicznego ukośnika odwrotnego), jest znormalizowana.</span><span class="sxs-lookup"><span data-stu-id="e161f-240">Unless the path starts exactly with `\\?\` (note the use of the canonical backslash), it is normalized.</span></span>

<span data-ttu-id="e161f-241">Dlaczego chcesz pominąć normalizację?</span><span class="sxs-lookup"><span data-stu-id="e161f-241">Why would you want to skip normalization?</span></span> <span data-ttu-id="e161f-242">Istnieją trzy główne przyczyny:</span><span class="sxs-lookup"><span data-stu-id="e161f-242">There are three major reasons:</span></span>

1. <span data-ttu-id="e161f-243">Aby uzyskać dostęp do ścieżek, które są zwykle niedostępne, ale są dozwolone.</span><span class="sxs-lookup"><span data-stu-id="e161f-243">To get access to paths that are normally unavailable but are legal.</span></span> <span data-ttu-id="e161f-244">Plik lub katalog o nazwie `hidden.`, na przykład, jest niemożliwe do uzyskania dostępu w inny sposób.</span><span class="sxs-lookup"><span data-stu-id="e161f-244">A file or directory called `hidden.`, for example, is impossible to access in any other way.</span></span>

1. <span data-ttu-id="e161f-245">Aby poprawić wydajność dzięki pominięciu normalizacji, jeśli został już znormalizowany.</span><span class="sxs-lookup"><span data-stu-id="e161f-245">To improve performance by skipping normalization if you've already normalized.</span></span>

1. <span data-ttu-id="e161f-246">Tylko w .NET Framework, aby pominąć `MAX_PATH` sprawdzić długość ścieżki, aby umożliwić używanie ścieżek o długości większej niż 259 znaków.</span><span class="sxs-lookup"><span data-stu-id="e161f-246">On the .NET Framework only, to skip the `MAX_PATH` check for path length to allow for paths that are greater than 259 characters.</span></span> <span data-ttu-id="e161f-247">Większość interfejsów API zezwala na to, z pewnymi wyjątkami.</span><span class="sxs-lookup"><span data-stu-id="e161f-247">Most APIs allow this, with some exceptions.</span></span>

> [!NOTE]
> <span data-ttu-id="e161f-248">Program .NET Core obsługuje długie ścieżki niejawnie i nie sprawdza `MAX_PATH`.</span><span class="sxs-lookup"><span data-stu-id="e161f-248">.NET Core handles long paths implicitly and does not perform a `MAX_PATH` check.</span></span> <span data-ttu-id="e161f-249">Sprawdzanie `MAX_PATH` ma zastosowanie tylko do .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="e161f-249">The `MAX_PATH` check applies only to the .NET Framework.</span></span>

<span data-ttu-id="e161f-250">Pomijanie normalizacji i maksymalne sprawdzanie ścieżki jest jedyną różnicą między dwiema składnią ścieżki urządzenia; są one identyczne.</span><span class="sxs-lookup"><span data-stu-id="e161f-250">Skipping normalization and max path checks is the only difference between the two device path syntaxes; they are otherwise identical.</span></span> <span data-ttu-id="e161f-251">Należy zachować ostrożność w przypadku pominięcia normalizacji, ponieważ można łatwo tworzyć ścieżki, które są trudne do rozpatrzenia "normalnych" aplikacji.</span><span class="sxs-lookup"><span data-stu-id="e161f-251">Be careful with skipping normalization, since you can easily create paths that are difficult for "normal" applications to deal with.</span></span>

<span data-ttu-id="e161f-252">Ścieżki, które zaczynają się od `\\?\` są ciągle znormalizowane, jeśli zostaną jawnie przekazane do [funkcji GetFullPathName](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).</span><span class="sxs-lookup"><span data-stu-id="e161f-252">Paths that start with `\\?\` are still normalized if you explicitly pass them to the [GetFullPathName function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).</span></span>

<span data-ttu-id="e161f-253">Można przekazać ścieżki zawierające więcej niż `MAX_PATH` znaków do [GetFullPathName](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) bez `\\?\`.</span><span class="sxs-lookup"><span data-stu-id="e161f-253">You can pass paths of more than `MAX_PATH` characters to [GetFullPathName](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) without `\\?\`.</span></span> <span data-ttu-id="e161f-254">Obsługuje ona dowolne ścieżki długości do maksymalnego rozmiaru ciągu, który może obsłużyć system Windows.</span><span class="sxs-lookup"><span data-stu-id="e161f-254">It supports arbitrary length paths up to the maximum string size that Windows can handle.</span></span>

## <a name="case-and-the-windows-file-system"></a><span data-ttu-id="e161f-255">Wielkość liter i system plików Windows</span><span class="sxs-lookup"><span data-stu-id="e161f-255">Case and the Windows file system</span></span>

<span data-ttu-id="e161f-256">Specyficzna dla systemu Windows system plików, który nie jest przeznaczony dla użytkowników i deweloperów, jest mylący, ponieważ nazwy ścieżek i katalogów nie uwzględniają wielkości liter.</span><span class="sxs-lookup"><span data-stu-id="e161f-256">A peculiarity of the Windows file system that non-Windows users and developers find confusing is that path and directory names are case-insensitive.</span></span> <span data-ttu-id="e161f-257">Oznacza to, że nazwy katalogów i plików odzwierciedlają wielkość liter ciągów używanych podczas tworzenia.</span><span class="sxs-lookup"><span data-stu-id="e161f-257">That is, directory and file names reflect the casing of the strings used when they are created.</span></span> <span data-ttu-id="e161f-258">Na przykład wywołanie metody</span><span class="sxs-lookup"><span data-stu-id="e161f-258">For example, the method call</span></span>

```csharp
Directory.Create("TeStDiReCtOrY");
```

```vb
Directory.Create("TeStDiReCtOrY")
```

<span data-ttu-id="e161f-259">tworzy katalog o nazwie TeStDiReCtOrY.</span><span class="sxs-lookup"><span data-stu-id="e161f-259">creates a directory named TeStDiReCtOrY.</span></span> <span data-ttu-id="e161f-260">Jeśli zmienisz nazwę katalogu lub pliku, aby zmienić jego wielkość liter, nazwa katalogu lub pliku odzwierciedla wielkość liter w ciągu używanym podczas zmiany nazwy.</span><span class="sxs-lookup"><span data-stu-id="e161f-260">If you rename a directory or file to change its case, the directory or file name reflects the case of the string used when you rename it.</span></span> <span data-ttu-id="e161f-261">Na przykład poniższy kod zmienia nazwę pliku o nazwie test. txt na test. txt:</span><span class="sxs-lookup"><span data-stu-id="e161f-261">For example, the following code renames a file named test.txt to Test.txt:</span></span>

[!code-csharp[case-and-renaming](~/samples/snippets/standard/io/file-names/cs/rename.cs)]
[!code-vb[case-and-renaming](~/samples/snippets/standard/io/file-names/vb/rename.vb)]

<span data-ttu-id="e161f-262">Jednak porównania nazw katalogów i plików nie uwzględniają wielkości liter.</span><span class="sxs-lookup"><span data-stu-id="e161f-262">However, directory and file name comparisons are case-insensitive.</span></span> <span data-ttu-id="e161f-263">W przypadku wyszukiwania pliku o nazwie "test. txt" interfejsy API systemu plików platformy .NET ignorują wielkość liter w porównaniu.</span><span class="sxs-lookup"><span data-stu-id="e161f-263">If you search for a file named "test.txt", .NET file system APIs ignore case in the comparison.</span></span> <span data-ttu-id="e161f-264">Test. txt, TEST. TXT, test. TXT i wszystkie inne kombinacje wielkich i małych liter będą zgodne z "test. txt".</span><span class="sxs-lookup"><span data-stu-id="e161f-264">Test.txt, TEST.TXT, test.TXT, and any other combination of upper- and lowercase letters will match "test.txt".</span></span>
