---
title: Co nowego w C# 7,2
description: Omówienie nowych funkcji w C# 7,2.
ms.date: 08/16/2017
ms.openlocfilehash: a2010b2bda769a625deb545964a2cc127aaf2e06
ms.sourcegitcommit: 6f28b709592503d27077b16fff2e2eacca569992
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 08/28/2019
ms.locfileid: "70105745"
---
# <a name="whats-new-in-c-72"></a><span data-ttu-id="d1706-103">Co nowego w C# 7,2</span><span class="sxs-lookup"><span data-stu-id="d1706-103">What's new in C# 7.2</span></span>

<span data-ttu-id="d1706-104">C#7,2 to inna wersja punktu, która dodaje wiele przydatnych funkcji.</span><span class="sxs-lookup"><span data-stu-id="d1706-104">C# 7.2 is another point release that adds a number of useful features.</span></span>
<span data-ttu-id="d1706-105">Jeden motyw dla tej wersji działa wydajniej z typami wartości poprzez uniknięcie niepotrzebnych kopii lub przydziałów.</span><span class="sxs-lookup"><span data-stu-id="d1706-105">One theme for this release is working more efficiently with value types by avoiding unnecessary copies or allocations.</span></span>

<span data-ttu-id="d1706-106">Pozostałe funkcje są małe, całkiem-do-mają funkcje.</span><span class="sxs-lookup"><span data-stu-id="d1706-106">The remaining features are small, nice-to-have features.</span></span>

<span data-ttu-id="d1706-107">C#7,2 używa elementu konfiguracja [wyboru wersji języka](../language-reference/configure-language-version.md) , aby wybrać wersję języka kompilatora.</span><span class="sxs-lookup"><span data-stu-id="d1706-107">C# 7.2 uses the [language version selection](../language-reference/configure-language-version.md) configuration element to select the compiler language version.</span></span>

<span data-ttu-id="d1706-108">Nowe funkcje języka w tej wersji są następujące:</span><span class="sxs-lookup"><span data-stu-id="d1706-108">The new language features in this release are:</span></span>

- [<span data-ttu-id="d1706-109">Techniki pisania bezpiecznego wydajnego kodu</span><span class="sxs-lookup"><span data-stu-id="d1706-109">Techniques for writing safe efficient code</span></span>](#safe-efficient-code-enhancements)
  - <span data-ttu-id="d1706-110">Kombinacja ulepszeń składni, które umożliwiają pracę z typami wartości przy użyciu semantyki referencyjnej.</span><span class="sxs-lookup"><span data-stu-id="d1706-110">A combination of syntax improvements that enable working with value types using reference semantics.</span></span>
- [<span data-ttu-id="d1706-111">Argumenty nazwane inne niż końcowe</span><span class="sxs-lookup"><span data-stu-id="d1706-111">Non-trailing named arguments</span></span>](#non-trailing-named-arguments)
  - <span data-ttu-id="d1706-112">Po nazwanych argumentach mogą występować argumenty pozycyjne.</span><span class="sxs-lookup"><span data-stu-id="d1706-112">Named arguments can be followed by positional arguments.</span></span>
- [<span data-ttu-id="d1706-113">Wiodące znaki podkreślenia w literałach numerycznych</span><span class="sxs-lookup"><span data-stu-id="d1706-113">Leading underscores in numeric literals</span></span>](#leading-underscores-in-numeric-literals)
  - <span data-ttu-id="d1706-114">Literały numeryczne mogą teraz zawierać podkreślenia wiodące przed wszelkimi drukowanymi cyframi.</span><span class="sxs-lookup"><span data-stu-id="d1706-114">Numeric literals can now have leading underscores before any printed digits.</span></span>
- [<span data-ttu-id="d1706-115">`private protected`Modyfikator dostępu</span><span class="sxs-lookup"><span data-stu-id="d1706-115">`private protected` access modifier</span></span>](#private-protected-access-modifier)
  - <span data-ttu-id="d1706-116">Modyfikator `private protected` dostępu umożliwia dostęp do klas pochodnych w tym samym zestawie.</span><span class="sxs-lookup"><span data-stu-id="d1706-116">The `private protected` access modifier enables access for derived classes in the same assembly.</span></span>
- [<span data-ttu-id="d1706-117">Wyrażenia `ref` warunkowe</span><span class="sxs-lookup"><span data-stu-id="d1706-117">Conditional `ref` expressions</span></span>](#conditional-ref-expressions)
  - <span data-ttu-id="d1706-118">Wynik wyrażenia warunkowego (`?:`) może teraz być odwołaniem.</span><span class="sxs-lookup"><span data-stu-id="d1706-118">The result of a conditional expression (`?:`) can now be a reference.</span></span>

<span data-ttu-id="d1706-119">Pozostała część tego artykułu zawiera omówienie każdej funkcji.</span><span class="sxs-lookup"><span data-stu-id="d1706-119">The remainder of this article provides an overview of each feature.</span></span> <span data-ttu-id="d1706-120">Dla każdej funkcji znajdziesz jej uzasadnienie.</span><span class="sxs-lookup"><span data-stu-id="d1706-120">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="d1706-121">Poznasz składnię.</span><span class="sxs-lookup"><span data-stu-id="d1706-121">You'll learn the syntax.</span></span> <span data-ttu-id="d1706-122">Te funkcje można eksplorować w środowisku za pomocą `dotnet try` narzędzia globalnego:</span><span class="sxs-lookup"><span data-stu-id="d1706-122">You can explore these features in your environment using the `dotnet try` global tool:</span></span>

1. <span data-ttu-id="d1706-123">Zainstaluj narzędzie [dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) Global.</span><span class="sxs-lookup"><span data-stu-id="d1706-123">Install the [dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) global tool.</span></span>
1. <span data-ttu-id="d1706-124">Sklonuj repozytorium [dotnet/try-Samples](https://github.com/dotnet/try-samples) .</span><span class="sxs-lookup"><span data-stu-id="d1706-124">Clone the [dotnet/try-samples](https://github.com/dotnet/try-samples) repository.</span></span>
1. <span data-ttu-id="d1706-125">Ustaw bieżący katalog na podkatalog *csharp7* dla repozytorium *try-Samples* .</span><span class="sxs-lookup"><span data-stu-id="d1706-125">Set the current directory to the *csharp7* subdirectory for the *try-samples* repository.</span></span>
1. <span data-ttu-id="d1706-126">Uruchom `dotnet try`.</span><span class="sxs-lookup"><span data-stu-id="d1706-126">Run `dotnet try`.</span></span>

## <a name="safe-efficient-code-enhancements"></a><span data-ttu-id="d1706-127">Bezpieczne usprawnienia kodu</span><span class="sxs-lookup"><span data-stu-id="d1706-127">Safe efficient code enhancements</span></span>

<span data-ttu-id="d1706-128">Funkcje języka wprowadzone w 7,2 umożliwiają współpracę z typami wartości przy użyciu semantyki odwołań.</span><span class="sxs-lookup"><span data-stu-id="d1706-128">Language features introduced in 7.2 let you work with value types while using reference semantics.</span></span> <span data-ttu-id="d1706-129">Są one przeznaczone do zwiększania wydajności poprzez minimalizowanie kopiowania typów wartości bez ponoszenia alokacji pamięci skojarzonej z użyciem typów referencyjnych.</span><span class="sxs-lookup"><span data-stu-id="d1706-129">They are designed to increase performance by minimizing copying value types without incurring the memory allocations associated with using reference types.</span></span> <span data-ttu-id="d1706-130">Funkcje obejmują:</span><span class="sxs-lookup"><span data-stu-id="d1706-130">The features include:</span></span>

- <span data-ttu-id="d1706-131">`in` Modyfikator parametrów, aby określić, że argument jest przesyłany przez odwołanie, ale nie modyfikowane przez wywołaną metodę.</span><span class="sxs-lookup"><span data-stu-id="d1706-131">The `in` modifier on parameters, to specify that an argument is passed by reference but not modified by the called method.</span></span> <span data-ttu-id="d1706-132">Dodanie modyfikatora do argumentu jest zgodną [zmianą źródłową.](version-update-considerations.md#source-compatible-changes) `in`</span><span class="sxs-lookup"><span data-stu-id="d1706-132">Adding the `in` modifier to an argument is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span>
- <span data-ttu-id="d1706-133">`ref readonly` Modyfikator metody zwraca, aby wskazać, że metoda zwraca swoją wartość przez odwołanie, ale nie zezwala na zapis w tym obiekcie.</span><span class="sxs-lookup"><span data-stu-id="d1706-133">The `ref readonly` modifier on method returns, to indicate that a method returns its value by reference but doesn't allow writes to that object.</span></span> <span data-ttu-id="d1706-134">Dodanie modyfikatora jest zgodną ze [źródłem zmian](version-update-considerations.md#source-compatible-changes), jeśli powrót jest przypisany do wartości. `ref readonly`</span><span class="sxs-lookup"><span data-stu-id="d1706-134">Adding the `ref readonly` modifier is a [source compatible change](version-update-considerations.md#source-compatible-changes), if the return is assigned to a value.</span></span> <span data-ttu-id="d1706-135">Dodanie modyfikatora do istniejącej `ref` instrukcji return jest niezgodną zmianą. [](version-update-considerations.md#incompatible-changes) `readonly`</span><span class="sxs-lookup"><span data-stu-id="d1706-135">Adding the `readonly` modifier to an existing `ref` return statement is an [incompatible change](version-update-considerations.md#incompatible-changes).</span></span> <span data-ttu-id="d1706-136">Wymaga, `ref` `readonly` aby wywołujący zaktualizował deklarację zmiennych lokalnych w celu uwzględnienia modyfikatora.</span><span class="sxs-lookup"><span data-stu-id="d1706-136">It requires callers to update the declaration of `ref` local variables to include the `readonly` modifier.</span></span>
- <span data-ttu-id="d1706-137">Deklaracja wskazująca, że struktura jest niezmienna i powinna zostać przeniesiona `in` jako parametr do metod składowych. `readonly struct`</span><span class="sxs-lookup"><span data-stu-id="d1706-137">The `readonly struct` declaration, to indicate that a struct is immutable and should be passed as an `in` parameter to its member methods.</span></span> <span data-ttu-id="d1706-138">Dodanie modyfikatora do istniejącej deklaracji struktury jest zgodną [binarną zmianą.](version-update-considerations.md#binary-compatible-changes) `readonly`</span><span class="sxs-lookup"><span data-stu-id="d1706-138">Adding the `readonly` modifier to an existing struct declaration is a [binary compatible change](version-update-considerations.md#binary-compatible-changes).</span></span>
- <span data-ttu-id="d1706-139">`ref struct` Deklaracja wskazująca, że typ struktury bezpośrednio uzyskuje dostęp do pamięci zarządzanej i zawsze musi mieć przydzieloną stos.</span><span class="sxs-lookup"><span data-stu-id="d1706-139">The `ref struct` declaration, to indicate that a struct type accesses managed memory directly and must always be stack allocated.</span></span> <span data-ttu-id="d1706-140">Dodanie modyfikatora do istniejącej `struct` deklaracji jest niezgodną zmianą. [](version-update-considerations.md#incompatible-changes) `ref`</span><span class="sxs-lookup"><span data-stu-id="d1706-140">Adding the `ref` modifier to an existing `struct` declaration is an [incompatible change](version-update-considerations.md#incompatible-changes).</span></span> <span data-ttu-id="d1706-141">Element `ref struct` a nie może być składową klasy ani używać w innych lokalizacjach, w których może być przydzielony na stercie.</span><span class="sxs-lookup"><span data-stu-id="d1706-141">A `ref struct` cannot be a member of a class or used in other locations where it may be allocated on the heap.</span></span>

<span data-ttu-id="d1706-142">Więcej informacji na temat tych zmian można znaleźć w artykule [pisanie bezpiecznego wydajnego kodu](../write-safe-efficient-code.md).</span><span class="sxs-lookup"><span data-stu-id="d1706-142">You can read more about all these changes in [Write safe efficient code](../write-safe-efficient-code.md).</span></span>

## <a name="non-trailing-named-arguments"></a><span data-ttu-id="d1706-143">Argumenty nazwane inne niż końcowe</span><span class="sxs-lookup"><span data-stu-id="d1706-143">Non-trailing named arguments</span></span>

<span data-ttu-id="d1706-144">Wywołania metod mogą teraz używać nazwanych argumentów, które poprzedzają argumenty pozycyjne, gdy te nazwane argumenty znajdują się w poprawnych pozycjach.</span><span class="sxs-lookup"><span data-stu-id="d1706-144">Method calls may now use named arguments that precede positional arguments when those named arguments are in the correct positions.</span></span> <span data-ttu-id="d1706-145">Aby uzyskać więcej informacji [, zobacz Argumenty nazwane i opcjonalne](../programming-guide/classes-and-structs/named-and-optional-arguments.md).</span><span class="sxs-lookup"><span data-stu-id="d1706-145">For more information see [Named and optional arguments](../programming-guide/classes-and-structs/named-and-optional-arguments.md).</span></span>

## <a name="leading-underscores-in-numeric-literals"></a><span data-ttu-id="d1706-146">Wiodące znaki podkreślenia w literałach numerycznych</span><span class="sxs-lookup"><span data-stu-id="d1706-146">Leading underscores in numeric literals</span></span>

<span data-ttu-id="d1706-147">Implementacja obsługi separatorów cyfr w C# 7,0 nie zezwala `_` na pierwszy znak wartości literału.</span><span class="sxs-lookup"><span data-stu-id="d1706-147">The implementation of support for digit separators in C# 7.0 didn't allow the `_` to be the first character of the literal value.</span></span> <span data-ttu-id="d1706-148">Szesnastkowe i binarne literały numeryczne mogą teraz zaczynać `_`się od.</span><span class="sxs-lookup"><span data-stu-id="d1706-148">Hex and binary numeric literals may now begin with an `_`.</span></span>

<span data-ttu-id="d1706-149">Na przykład:</span><span class="sxs-lookup"><span data-stu-id="d1706-149">For example:</span></span>

```csharp
int binaryValue = 0b_0101_0101;
```

## <a name="_private-protected_-access-modifier"></a><span data-ttu-id="d1706-150">modyfikator _prywatnego_ dostępu chronionego</span><span class="sxs-lookup"><span data-stu-id="d1706-150">_private protected_ access modifier</span></span>

<span data-ttu-id="d1706-151">Nowy modyfikator dostępu złożonego: `private protected` wskazuje, że element członkowski może być dostępny przez zawiera klasy lub klasy pochodne, które są zadeklarowane w tym samym zestawie.</span><span class="sxs-lookup"><span data-stu-id="d1706-151">A new compound access modifier: `private protected` indicates that a member may be accessed by containing class or derived classes that are declared in the same assembly.</span></span> <span data-ttu-id="d1706-152">Zezwala na dostęp przez klasy pochodne lub klasy, które znajdują się w tym `private protected` samym zestawie, ogranicza dostęp do typów pochodnych zadeklarowanych w tym samym zestawie. `protected internal`</span><span class="sxs-lookup"><span data-stu-id="d1706-152">While `protected internal` allows access by derived classes or classes that are in the same assembly, `private protected` limits access to derived types declared in the same assembly.</span></span>

<span data-ttu-id="d1706-153">Aby uzyskać więcej informacji, zobacz [Modyfikatory dostępu](../language-reference/keywords/access-modifiers.md) w dokumentacji języka.</span><span class="sxs-lookup"><span data-stu-id="d1706-153">For more information see [access modifiers](../language-reference/keywords/access-modifiers.md) in the language reference.</span></span>

## <a name="conditional-ref-expressions"></a><span data-ttu-id="d1706-154">Wyrażenia `ref` warunkowe</span><span class="sxs-lookup"><span data-stu-id="d1706-154">Conditional `ref` expressions</span></span>

<span data-ttu-id="d1706-155">Na koniec wyrażenie warunkowe może generować wynik ref zamiast wyniku wartości.</span><span class="sxs-lookup"><span data-stu-id="d1706-155">Finally, the conditional expression may produce a ref result instead of a value result.</span></span> <span data-ttu-id="d1706-156">Na przykład Napisz następujące polecenie, aby pobrać odwołanie do pierwszego elementu w jednej z dwóch tablic:</span><span class="sxs-lookup"><span data-stu-id="d1706-156">For example, you would write the following to retrieve a reference to the first element in one of two arrays:</span></span>

```csharp
ref var r = ref (arr != null ? ref arr[0] : ref otherArr[0]);
```

<span data-ttu-id="d1706-157">Zmienna `r` jest odwołaniem do pierwszej wartości `arr` z lub `otherArr`.</span><span class="sxs-lookup"><span data-stu-id="d1706-157">The variable `r` is a reference to the first value in either `arr` or `otherArr`.</span></span>

<span data-ttu-id="d1706-158">Aby uzyskać więcej informacji, zobacz [operator warunkowy (?:)](../language-reference/operators/conditional-operator.md) w dokumentacji języka.</span><span class="sxs-lookup"><span data-stu-id="d1706-158">For more information, see [conditional operator (?:)](../language-reference/operators/conditional-operator.md) in the language reference.</span></span>
