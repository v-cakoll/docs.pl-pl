---
title: Wyprowadzanie z elementu WebResponse
ms.date: 03/30/2017
helpviewer_keywords:
- Deriving from WebResponse
ms.assetid: f11d4866-a199-4087-9306-a5a4c18b13db
ms.openlocfilehash: bd06928b08eb085ef13371687fb1e5b92c6c1d86
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/15/2020
ms.locfileid: "71048579"
---
# <a name="deriving-from-webresponse"></a><span data-ttu-id="2f0f4-102">Wyprowadzanie z elementu WebResponse</span><span class="sxs-lookup"><span data-stu-id="2f0f4-102">Deriving from WebResponse</span></span>
<span data-ttu-id="2f0f4-103">Klasa <xref:System.Net.WebResponse> jest abstrakcyjną klasą podstawową, która udostępnia podstawowe metody i właściwości tworzenia odpowiedzi specyficznej dla protokołu, która pasuje do modelu protokołu .NET Framework pluggable protocol.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-103">The <xref:System.Net.WebResponse> class is an abstract base class that provides the basic methods and properties for creating a protocol-specific response that fits the .NET Framework pluggable protocol model.</span></span> <span data-ttu-id="2f0f4-104">Aplikacje, <xref:System.Net.WebRequest> które używają tej klasy do żądania danych z zasobów, otrzymują odpowiedzi w **webresponse**.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-104">Applications that use the <xref:System.Net.WebRequest> class to request data from resources receive the responses in a **WebResponse**.</span></span> <span data-ttu-id="2f0f4-105">Elementy **podrzędne webresponse** specyficzne dla protokołu muszą implementować abstrakcyjne elementy członkowskie klasy **WebResponse.**</span><span class="sxs-lookup"><span data-stu-id="2f0f4-105">Protocol-specific **WebResponse** descendants must implement the abstract members of the **WebResponse** class.</span></span>  
  
 <span data-ttu-id="2f0f4-106">Skojarzona klasa **WebRequest** musi utworzyć elementy **podrzędne WebResponse.**</span><span class="sxs-lookup"><span data-stu-id="2f0f4-106">The associated **WebRequest** class must create **WebResponse** descendants.</span></span> <span data-ttu-id="2f0f4-107">Na przykład <xref:System.Net.HttpWebResponse> wystąpienia są tworzone tylko w <xref:System.Net.HttpWebRequest.GetResponse%2A?displayProperty=nameWithType> <xref:System.Net.HttpWebRequest.EndGetResponse%2A?displayProperty=nameWithType>wyniku wywołania lub .</span><span class="sxs-lookup"><span data-stu-id="2f0f4-107">For example, <xref:System.Net.HttpWebResponse> instances are created only as the result of calling <xref:System.Net.HttpWebRequest.GetResponse%2A?displayProperty=nameWithType> or <xref:System.Net.HttpWebRequest.EndGetResponse%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="2f0f4-108">Każda **webresponse** zawiera wynik żądania do zasobu i nie jest przeznaczony do ponownego użycia.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-108">Each **WebResponse** contains the result of a request to a resource and is not intended to be reused.</span></span>  
  
## <a name="contentlength-property"></a><span data-ttu-id="2f0f4-109">Właściwość ContentLength</span><span class="sxs-lookup"><span data-stu-id="2f0f4-109">ContentLength Property</span></span>  
 <span data-ttu-id="2f0f4-110">Właściwość <xref:System.Net.WebResponse.ContentLength%2A> wskazuje liczbę bajtów danych, które są dostępne <xref:System.Net.WebResponse.GetResponseStream%2A> ze strumienia zwróconego przez metodę.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-110">The <xref:System.Net.WebResponse.ContentLength%2A> property indicates the number of bytes of data that are available from the stream returned by the <xref:System.Net.WebResponse.GetResponseStream%2A> method.</span></span> <span data-ttu-id="2f0f4-111">**Właściwość ContentLength** nie wskazuje liczby bajtów informacji nagłówka lub metadanych zwracanych przez serwer; wskazuje tylko liczbę bajtów danych w samym żądanym zasobie.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-111">The **ContentLength** property does not indicate the number of bytes of header or metadata information returned by the server; it indicates only the number of bytes of data in the requested resource itself.</span></span>  
  
## <a name="contenttype-property"></a><span data-ttu-id="2f0f4-112">Właściwość ContentType</span><span class="sxs-lookup"><span data-stu-id="2f0f4-112">ContentType Property</span></span>  
 <span data-ttu-id="2f0f4-113">Właściwość <xref:System.Net.WebResponse.ContentType%2A> zawiera wszelkie specjalne informacje, które protokół wymaga wysłania do klienta w celu zidentyfikowania typu zawartości wysyłanej przez serwer.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-113">The <xref:System.Net.WebResponse.ContentType%2A> property provides any special information that your protocol requires you to send to the client to identify the type of content being sent by the server.</span></span> <span data-ttu-id="2f0f4-114">Zazwyczaj jest to typ zawartości MIME zwracanych danych.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-114">Typically this is the MIME content type of any data returned.</span></span>  
  
## <a name="headers-property"></a><span data-ttu-id="2f0f4-115">Właściwość nagłówków</span><span class="sxs-lookup"><span data-stu-id="2f0f4-115">Headers Property</span></span>  
 <span data-ttu-id="2f0f4-116">Właściwość <xref:System.Net.WebResponse.Headers%2A> zawiera dowolną kolekcję pary nazw/wartości metadanych skojarzonych z odpowiedzią.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-116">The <xref:System.Net.WebResponse.Headers%2A> property contains an arbitrary collection of name/value pairs of metadata associated with the response.</span></span> <span data-ttu-id="2f0f4-117">Wszystkie metadane wymagane przez protokół, które mogą być wyrażone jako para nazwa/wartość mogą być zawarte w **Headers** właściwości.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-117">Any metadata needed by the protocol that can be expressed as a name/value pair can be included in the **Headers** property.</span></span>  
  
 <span data-ttu-id="2f0f4-118">Nie musisz używać właściwości **Nagłówki** do używania metadanych nagłówka.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-118">You are not required to use the **Headers** property to use header metadata.</span></span> <span data-ttu-id="2f0f4-119">Metadane specyficzne dla protokołu mogą być udostępniane jako właściwości; na przykład <xref:System.Net.HttpWebResponse.LastModified%2A?displayProperty=nameWithType> właściwość udostępnia **nagłówek HTTP ostatniej modyfikacji.**</span><span class="sxs-lookup"><span data-stu-id="2f0f4-119">Protocol-specific metadata can be exposed as properties; for example, the <xref:System.Net.HttpWebResponse.LastModified%2A?displayProperty=nameWithType> property exposes the **Last-Modified** HTTP header.</span></span> <span data-ttu-id="2f0f4-120">Podczas uwidaczniania metadanych nagłówka jako właściwości, nie należy zezwalać na tę samą właściwość, aby ustawić przy użyciu **headers** właściwości.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-120">When you expose header metadata as a property, you should not allow the same property to be set using the **Headers** property.</span></span>  
  
## <a name="responseuri-property"></a><span data-ttu-id="2f0f4-121">Właściwość ResponseUri</span><span class="sxs-lookup"><span data-stu-id="2f0f4-121">ResponseUri Property</span></span>  
 <span data-ttu-id="2f0f4-122">Właściwość <xref:System.Net.WebResponse.ResponseUri%2A> zawiera identyfikator URI zasobu, który faktycznie pod warunkiem odpowiedzi.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-122">The <xref:System.Net.WebResponse.ResponseUri%2A> property contains the URI of the resource that actually provided the response.</span></span> <span data-ttu-id="2f0f4-123">Dla protokołów, które nie obsługują przekierowania **ResponseUri** <xref:System.Net.WebRequest.RequestUri%2A> będzie taka sama jak właściwość **WebRequest,** który utworzył odpowiedź.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-123">For protocols that do not support redirection, **ResponseUri** will be the same as the <xref:System.Net.WebRequest.RequestUri%2A> property of the **WebRequest** that created the response.</span></span> <span data-ttu-id="2f0f4-124">Jeśli protokół obsługuje przekierowywanie **żądania, ResponseUri** będzie zawierać identyfikator URI odpowiedzi.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-124">If the protocol supports redirecting the request, **ResponseUri** will contain the URI of the response.</span></span>  
  
## <a name="close-method"></a><span data-ttu-id="2f0f4-125">Close — Metoda</span><span class="sxs-lookup"><span data-stu-id="2f0f4-125">Close Method</span></span>  
 <span data-ttu-id="2f0f4-126">Metoda <xref:System.Net.WebResponse.Close%2A> zamyka wszystkie połączenia wykonane przez żądanie i odpowiedź i czyści zasoby używane przez odpowiedź.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-126">The <xref:System.Net.WebResponse.Close%2A> method closes any connections made by the request and response and cleans up resources used by the response.</span></span> <span data-ttu-id="2f0f4-127">**Close** Metoda zamyka wszelkie wystąpienia strumienia używane przez odpowiedź, ale nie zgłasza wyjątek, jeśli strumień <xref:System.IO.Stream.Close%2A?displayProperty=nameWithType> odpowiedzi został wcześniej zamknięty przez wywołanie metody.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-127">The **Close** method closes any stream instances used by the response, but it does not throw an exception if the response stream was previously closed by a call to the <xref:System.IO.Stream.Close%2A?displayProperty=nameWithType> method.</span></span>  
  
## <a name="getresponsestream-method"></a><span data-ttu-id="2f0f4-128">Metoda GetResponseStream</span><span class="sxs-lookup"><span data-stu-id="2f0f4-128">GetResponseStream Method</span></span>  
 <span data-ttu-id="2f0f4-129">Metoda <xref:System.Net.WebResponse.GetResponseStream%2A> zwraca strumień zawierający odpowiedź z żądanego zasobu.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-129">The <xref:System.Net.WebResponse.GetResponseStream%2A> method returns a stream containing the response from the requested resource.</span></span> <span data-ttu-id="2f0f4-130">Strumień odpowiedzi zawiera tylko dane zwracane przez zasób; każdy nagłówek lub metadane zawarte w odpowiedzi powinny zostać usunięte z odpowiedzi i narażone na aplikację za pośrednictwem właściwości specyficznych dla protokołu lub **headers** właściwości.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-130">The response stream contains only the data returned by the resource; any header or metadata included in the response should be stripped from the response and exposed to the application through protocol-specific properties or the **Headers** property.</span></span>  
  
 <span data-ttu-id="2f0f4-131">Wystąpienie strumienia **zwrócone przez GetResponseStream** metoda jest własnością aplikacji i może być zamknięty bez zamykania **WebResponse**.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-131">The stream instance returned by the **GetResponseStream** method is owned by the application and can be closed without closing the **WebResponse**.</span></span> <span data-ttu-id="2f0f4-132">Zgodnie z konwencją wywołanie **Metody WebResponse.Close** również zamyka strumień zwrócony przez **GetResponse**.</span><span class="sxs-lookup"><span data-stu-id="2f0f4-132">By convention, calling the **WebResponse.Close** method also closes the stream returned by **GetResponse**.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="2f0f4-133">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="2f0f4-133">See also</span></span>

- <xref:System.Net.WebResponse>
- <xref:System.Net.HttpWebResponse>
- <xref:System.Net.FileWebResponse>
- [<span data-ttu-id="2f0f4-134">Programowanie protokołów podłączanych</span><span class="sxs-lookup"><span data-stu-id="2f0f4-134">Programming Pluggable Protocols</span></span>](programming-pluggable-protocols.md)
- [<span data-ttu-id="2f0f4-135">Wyprowadzanie z elementu WebRequest</span><span class="sxs-lookup"><span data-stu-id="2f0f4-135">Deriving from WebRequest</span></span>](deriving-from-webrequest.md)
