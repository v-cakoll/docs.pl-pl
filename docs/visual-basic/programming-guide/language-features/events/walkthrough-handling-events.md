---
title: Obsługa zdarzeń
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: cb42f2e41e366cf8765cb9395d1a5641434bab40
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 11/22/2019
ms.locfileid: "74345083"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="b821c-102">Wskazówki: obsługa zdarzeń (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="b821c-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="b821c-103">Jest to drugi z dwóch tematów, które pokazują, jak korzystać z zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="b821c-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="b821c-104">Pierwszy temat, [Przewodnik: deklarowanie i](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)wywoływanie zdarzeń, pokazuje sposób deklarowania i zgłaszania zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="b821c-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="b821c-105">Ta sekcja używa formularza i klasy z tego przewodnika, aby pokazać, jak obsługiwać zdarzenia po ich przejściu.</span><span class="sxs-lookup"><span data-stu-id="b821c-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="b821c-106">Przykład klasy `Widget` używa tradycyjnych instrukcji obsługi zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="b821c-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="b821c-107">Visual Basic zapewnia inne techniki pracy ze zdarzeniami.</span><span class="sxs-lookup"><span data-stu-id="b821c-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="b821c-108">W ramach ćwiczenia można zmodyfikować ten przykład, aby użyć instrukcji `AddHandler` i `Handles`.</span><span class="sxs-lookup"><span data-stu-id="b821c-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="b821c-109">Aby obsłużyć zdarzenie PercentDone klasy widget</span><span class="sxs-lookup"><span data-stu-id="b821c-109">To handle the PercentDone event of the Widget class</span></span>  
  
1. <span data-ttu-id="b821c-110">Umieść następujący kod w `Form1`:</span><span class="sxs-lookup"><span data-stu-id="b821c-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#4)]  
  
     <span data-ttu-id="b821c-111">Słowo kluczowe `WithEvents` określa, że zmienna `mWidget` jest używana do obsługi zdarzeń obiektu.</span><span class="sxs-lookup"><span data-stu-id="b821c-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="b821c-112">Należy określić rodzaj obiektu, dostarczając nazwę klasy, z której zostanie utworzony obiekt.</span><span class="sxs-lookup"><span data-stu-id="b821c-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="b821c-113">Zmienna `mWidget` jest zadeklarowana w `Form1`, ponieważ zmienne `WithEvents` muszą być na poziomie klasy.</span><span class="sxs-lookup"><span data-stu-id="b821c-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="b821c-114">Ta wartość jest prawdziwa niezależnie od typu klasy, w której je umieszczasz.</span><span class="sxs-lookup"><span data-stu-id="b821c-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="b821c-115">Zmienna `mblnCancel` jest używana do anulowania metody `LongTask`.</span><span class="sxs-lookup"><span data-stu-id="b821c-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="b821c-116">Pisanie kodu do obsługi zdarzenia</span><span class="sxs-lookup"><span data-stu-id="b821c-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="b821c-117">Gdy tylko zadeklarujesz zmienną przy użyciu `WithEvents`, nazwa zmiennej jest wyświetlana na liście rozwijanej w **edytorze kodu**klasy.</span><span class="sxs-lookup"><span data-stu-id="b821c-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="b821c-118">Po wybraniu `mWidget`zdarzenia klasy `Widget` są wyświetlane na liście rozwijanej po prawej stronie.</span><span class="sxs-lookup"><span data-stu-id="b821c-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="b821c-119">Wybranie zdarzenia powoduje wyświetlenie odpowiedniej procedury zdarzenia z prefiksem `mWidget` i podkreśleniem.</span><span class="sxs-lookup"><span data-stu-id="b821c-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="b821c-120">Wszystkie procedury zdarzeń skojarzone z zmienną `WithEvents` są przydawane nazwie zmiennej jako prefiks.</span><span class="sxs-lookup"><span data-stu-id="b821c-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="b821c-121">Aby obsłużyć zdarzenie</span><span class="sxs-lookup"><span data-stu-id="b821c-121">To handle an event</span></span>  
  
1. <span data-ttu-id="b821c-122">Wybierz pozycję `mWidget` z listy rozwijanej po lewej stronie w **edytorze kodu**.</span><span class="sxs-lookup"><span data-stu-id="b821c-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="b821c-123">Wybierz zdarzenie `PercentDone` z listy rozwijanej po prawej stronie.</span><span class="sxs-lookup"><span data-stu-id="b821c-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="b821c-124">**Edytor kodu** otwiera procedurę zdarzenia `mWidget_PercentDone`.</span><span class="sxs-lookup"><span data-stu-id="b821c-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="b821c-125">**Edytor kodu** jest przydatny, ale nie jest wymagany, do wstawiania nowych programów obsługi zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="b821c-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="b821c-126">W tym instruktażu bardziej bezpośrednim jest tylko kopiowanie programów obsługi zdarzeń bezpośrednio do kodu.</span><span class="sxs-lookup"><span data-stu-id="b821c-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3. <span data-ttu-id="b821c-127">Dodaj następujący kod do programu obsługi zdarzeń `mWidget_PercentDone`:</span><span class="sxs-lookup"><span data-stu-id="b821c-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#5)]  
  
     <span data-ttu-id="b821c-128">Za każdym razem, gdy zostanie zgłoszone zdarzenie `PercentDone`, procedura zdarzenia wyświetla procent wykonania w kontrolce `Label`.</span><span class="sxs-lookup"><span data-stu-id="b821c-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="b821c-129">Metoda `DoEvents` pozwala na odświeżenie etykiety, a także daje użytkownikowi możliwość kliknięcia przycisku **Anuluj** .</span><span class="sxs-lookup"><span data-stu-id="b821c-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4. <span data-ttu-id="b821c-130">Dodaj następujący kod dla programu obsługi zdarzeń `Button2_Click`:</span><span class="sxs-lookup"><span data-stu-id="b821c-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#6)]  
  
 <span data-ttu-id="b821c-131">Jeśli użytkownik kliknie przycisk **Anuluj** podczas `LongTask` jest uruchomiony, zdarzenie `Button2_Click` zostanie wykonane natychmiast, gdy instrukcja `DoEvents` umożliwia przetwarzanie zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="b821c-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="b821c-132">Zmienna na poziomie klasy `mblnCancel` jest ustawiona na `True`, a zdarzenie `mWidget_PercentDone` przetestuje je i ustawia argument `ByRef Cancel` na `True`.</span><span class="sxs-lookup"><span data-stu-id="b821c-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="b821c-133">Łączenie zmiennej WithEvents z obiektem</span><span class="sxs-lookup"><span data-stu-id="b821c-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="b821c-134">`Form1` jest teraz skonfigurowany do obsługi zdarzeń `Widget` obiektu.</span><span class="sxs-lookup"><span data-stu-id="b821c-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="b821c-135">To wszystko, co pozostało, aby znaleźć `Widget` w miejscu.</span><span class="sxs-lookup"><span data-stu-id="b821c-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="b821c-136">Kiedy deklarujesz zmienną `WithEvents` w czasie projektowania, żaden obiekt nie jest skojarzony z nim.</span><span class="sxs-lookup"><span data-stu-id="b821c-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="b821c-137">Zmienna `WithEvents` jest taka sama jak jakakolwiek inna zmienna obiektu.</span><span class="sxs-lookup"><span data-stu-id="b821c-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="b821c-138">Należy utworzyć obiekt i przypisać do niego odwołanie za pomocą zmiennej `WithEvents`.</span><span class="sxs-lookup"><span data-stu-id="b821c-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="b821c-139">Aby utworzyć obiekt i przypisać do niego odwołanie</span><span class="sxs-lookup"><span data-stu-id="b821c-139">To create an object and assign a reference to it</span></span>  
  
1. <span data-ttu-id="b821c-140">Wybierz pozycję **(zdarzenia Form1)** z listy rozwijanej po lewej stronie w **edytorze kodu**.</span><span class="sxs-lookup"><span data-stu-id="b821c-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="b821c-141">Wybierz zdarzenie `Load` z listy rozwijanej po prawej stronie.</span><span class="sxs-lookup"><span data-stu-id="b821c-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="b821c-142">**Edytor kodu** otwiera procedurę zdarzenia `Form1_Load`.</span><span class="sxs-lookup"><span data-stu-id="b821c-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3. <span data-ttu-id="b821c-143">Dodaj następujący kod dla procedury zdarzenia `Form1_Load`, aby utworzyć `Widget`:</span><span class="sxs-lookup"><span data-stu-id="b821c-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#7)]  
  
 <span data-ttu-id="b821c-144">Gdy ten kod jest wykonywany, Visual Basic tworzy obiekt `Widget` i łączy jego zdarzenia z procedurami zdarzeń skojarzonymi z `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="b821c-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="b821c-145">Od tego momentu, gdy `Widget` zgłasza zdarzenie `PercentDone`, zostanie wykonana procedura zdarzenia `mWidget_PercentDone`.</span><span class="sxs-lookup"><span data-stu-id="b821c-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="b821c-146">Aby wywołać metodę LongTask</span><span class="sxs-lookup"><span data-stu-id="b821c-146">To call the LongTask method</span></span>  
  
- <span data-ttu-id="b821c-147">Dodaj następujący kod do programu obsługi zdarzeń `Button1_Click`:</span><span class="sxs-lookup"><span data-stu-id="b821c-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#8)]  
  
 <span data-ttu-id="b821c-148">Przed wywołaniem metody `LongTask`, etykieta, która wyświetla wartość procentową, musi zostać zainicjowana, a flaga `Boolean` na poziomie klasy dla anulowania metody musi być ustawiona na `False`.</span><span class="sxs-lookup"><span data-stu-id="b821c-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="b821c-149">`LongTask` jest wywoływana z czasem trwania zadania wynoszącym 12,2 sekund.</span><span class="sxs-lookup"><span data-stu-id="b821c-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="b821c-150">Zdarzenie `PercentDone` jest zgłaszane raz w każdej jednej trzeciej części sekundy.</span><span class="sxs-lookup"><span data-stu-id="b821c-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="b821c-151">Za każdym razem, gdy zdarzenie jest zgłaszane, wykonywana jest `mWidget_PercentDone` procedura zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="b821c-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="b821c-152">Gdy `LongTask` jest wykonywane, `mblnCancel` jest testowana, aby sprawdzić, czy `LongTask` zakończyła się normalnie, lub jeśli została zatrzymana, ponieważ `mblnCancel` została ustawiona na `True`.</span><span class="sxs-lookup"><span data-stu-id="b821c-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="b821c-153">Procent ukończenia jest aktualizowany tylko w poprzednim przypadku.</span><span class="sxs-lookup"><span data-stu-id="b821c-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="b821c-154">Aby uruchomić program</span><span class="sxs-lookup"><span data-stu-id="b821c-154">To run the program</span></span>  
  
1. <span data-ttu-id="b821c-155">Naciśnij klawisz F5, aby umieścić projekt w trybie uruchamiania.</span><span class="sxs-lookup"><span data-stu-id="b821c-155">Press F5 to put the project in run mode.</span></span>  
  
2. <span data-ttu-id="b821c-156">Kliknij przycisk **Rozpocznij zadanie** .</span><span class="sxs-lookup"><span data-stu-id="b821c-156">Click the **Start Task** button.</span></span> <span data-ttu-id="b821c-157">Za każdym razem, gdy zdarzenie `PercentDone` zostanie wywołane, etykieta zostaje zaktualizowana o wartość procentową wykonanego zadania.</span><span class="sxs-lookup"><span data-stu-id="b821c-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3. <span data-ttu-id="b821c-158">Kliknij przycisk **Anuluj** , aby zatrzymać zadanie.</span><span class="sxs-lookup"><span data-stu-id="b821c-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="b821c-159">Zauważ, że wygląd przycisku **Anuluj** nie zmienia się natychmiast po jego kliknięciu.</span><span class="sxs-lookup"><span data-stu-id="b821c-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="b821c-160">Nie można wykonać zdarzenia `Click`, dopóki instrukcja `My.Application.DoEvents` nie zezwala na przetwarzanie zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="b821c-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="b821c-161">Metoda `My.Application.DoEvents` nie przetwarza zdarzeń w taki sam sposób jak w przypadku formularza.</span><span class="sxs-lookup"><span data-stu-id="b821c-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="b821c-162">Na przykład w tym instruktażu należy kliknąć przycisk **Anuluj** dwa razy.</span><span class="sxs-lookup"><span data-stu-id="b821c-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="b821c-163">Aby umożliwić bezpośrednie obsługiwanie zdarzeń przez formularz, można użyć wielowątkowości.</span><span class="sxs-lookup"><span data-stu-id="b821c-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="b821c-164">Aby uzyskać więcej informacji, zobacz sekcję [zarządzane wątki](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="b821c-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="b821c-165">Może się pojawić monit o uruchomienie programu z użyciem klawisza F11 i przekroczenie kodu w wierszu.</span><span class="sxs-lookup"><span data-stu-id="b821c-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="b821c-166">Można jasno zobaczyć, jak wykonywanie wprowadza `LongTask`, a następnie krótko wprowadzić ponownie `Form1` przy każdym wyjściu zdarzenia `PercentDone`.</span><span class="sxs-lookup"><span data-stu-id="b821c-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="b821c-167">Co się stanie, jeśli podczas wykonywania w kodzie `Form1`następuje ponowne wywołanie metody `LongTask`?</span><span class="sxs-lookup"><span data-stu-id="b821c-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="b821c-168">W najgorszym przypadku może wystąpić przepełnienie stosu, jeśli `LongTask` były wywoływane za każdym razem, gdy zdarzenie zostało zgłoszone.</span><span class="sxs-lookup"><span data-stu-id="b821c-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="b821c-169">Można spowodować, że zmienna `mWidget` obsługiwać zdarzenia dla innego obiektu `Widget`, przypisując odwołanie do nowej `Widget` do `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="b821c-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="b821c-170">W rzeczywistości możesz wprowadzić kod w `Button1_Click` zrobić to za każdym razem, gdy klikniesz przycisk.</span><span class="sxs-lookup"><span data-stu-id="b821c-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="b821c-171">Aby obsłużyć zdarzenia dla innego widżetu</span><span class="sxs-lookup"><span data-stu-id="b821c-171">To handle events for a different widget</span></span>  
  
- <span data-ttu-id="b821c-172">Dodaj następujący wiersz kodu do procedury `Button1_Click`, bezpośrednio poprzedzając wiersz, który odczytuje `mWidget.LongTask(12.2, 0.33)`:</span><span class="sxs-lookup"><span data-stu-id="b821c-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#9)]  
  
 <span data-ttu-id="b821c-173">Powyższy kod tworzy nowy `Widget` przy każdym kliknięciu przycisku.</span><span class="sxs-lookup"><span data-stu-id="b821c-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="b821c-174">Gdy tylko Metoda `LongTask` zostanie zakończona, odwołanie do `Widget` zostanie wydane, a `Widget` zostanie zniszczona.</span><span class="sxs-lookup"><span data-stu-id="b821c-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="b821c-175">Zmienna `WithEvents` może zawierać tylko jedno odwołanie do obiektu naraz, więc Jeśli przypiszesz inny obiekt `Widget` do `mWidget`, poprzednie zdarzenia `Widget`go obiektu nie będą już obsługiwane.</span><span class="sxs-lookup"><span data-stu-id="b821c-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="b821c-176">Jeśli `mWidget` jest jedyną zmienną obiektu zawierającą odwołanie do starego `Widget`, obiekt zostanie zniszczony.</span><span class="sxs-lookup"><span data-stu-id="b821c-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="b821c-177">Jeśli chcesz obsługiwać zdarzenia z kilku `Widget` obiektów, użyj instrukcji `AddHandler`, aby przetwarzać zdarzenia z poszczególnych obiektów osobno.</span><span class="sxs-lookup"><span data-stu-id="b821c-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="b821c-178">W razie potrzeby można zadeklarować dowolną liczbę zmiennych `WithEvents`, ale tablice `WithEvents` zmiennych nie są obsługiwane.</span><span class="sxs-lookup"><span data-stu-id="b821c-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="b821c-179">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="b821c-179">See also</span></span>

- [<span data-ttu-id="b821c-180">Przewodnik: deklarowanie i wywoływanie zdarzeń</span><span class="sxs-lookup"><span data-stu-id="b821c-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)
- [<span data-ttu-id="b821c-181">Zdarzenia</span><span class="sxs-lookup"><span data-stu-id="b821c-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
