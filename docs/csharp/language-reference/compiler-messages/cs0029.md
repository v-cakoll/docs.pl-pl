---
title: Błąd kompilatora CS0029
ms.date: 07/20/2015
f1_keywords:
- CS0029
helpviewer_keywords:
- CS0029
ms.assetid: 63c3e574-1868-4a9e-923e-dcd9f38bce88
ms.openlocfilehash: f4dec41945ace6c6b0821b9870a88960efbe1e52
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/14/2020
ms.locfileid: "71332301"
---
# <a name="compiler-error-cs0029"></a><span data-ttu-id="27b5d-102">Błąd kompilatora CS0029</span><span class="sxs-lookup"><span data-stu-id="27b5d-102">Compiler Error CS0029</span></span>

<span data-ttu-id="27b5d-103">Nie można niejawnie przekonwertować typu "typ" na "typ"</span><span class="sxs-lookup"><span data-stu-id="27b5d-103">Cannot implicitly convert type 'type' to 'type'</span></span>

 <span data-ttu-id="27b5d-104">Kompilator wymaga jawnej konwersji.</span><span class="sxs-lookup"><span data-stu-id="27b5d-104">The compiler requires an explicit conversion.</span></span> <span data-ttu-id="27b5d-105">Na przykład może być konieczne rzutowania wartość r być tego samego typu jako wartość l.</span><span class="sxs-lookup"><span data-stu-id="27b5d-105">For example, you may need to cast an r-value to be the same type as an l-value.</span></span> <span data-ttu-id="27b5d-106">Lub należy podać procedury konwersji do obsługi niektórych przeciążeń operatora.</span><span class="sxs-lookup"><span data-stu-id="27b5d-106">Or, you must provide conversion routines to support certain operator overloads.</span></span>

 <span data-ttu-id="27b5d-107">Konwersje muszą wystąpić podczas przypisywania zmiennej jednego typu do zmiennej innego typu.</span><span class="sxs-lookup"><span data-stu-id="27b5d-107">Conversions must occur when assigning a variable of one type to a variable of a different type.</span></span> <span data-ttu-id="27b5d-108">Podczas przypisywania między zmiennymi różnych typów kompilator musi przekonwertować typ po prawej stronie operatora przypisania na typ po lewej stronie operatora przypisania.</span><span class="sxs-lookup"><span data-stu-id="27b5d-108">When making an assignment between variables of different types, the compiler must convert the type on the right-hand side of the assignment operator to the type on the left-hand side of the assignment operator.</span></span> <span data-ttu-id="27b5d-109">Weź następujący kod:</span><span class="sxs-lookup"><span data-stu-id="27b5d-109">Take the following the code:</span></span>

```csharp
int i = 50;
long lng = 100;
i = lng;
```

 <span data-ttu-id="27b5d-110">`i = lng;`sprawia, że przypisanie, ale typy danych zmiennych po lewej i prawej stronie operatora przypisania nie są zgodne.</span><span class="sxs-lookup"><span data-stu-id="27b5d-110">`i = lng;` makes an assignment, but the data types of the variables on the left and right-hand side of the assignment operator don't match.</span></span> <span data-ttu-id="27b5d-111">Przed wykonaniem przypisania kompilator jest `lng`niejawnie konwersji zmiennej , który jest typu long, do int. Jest to niejawne, ponieważ żaden kod jawnie poinstruował kompilatora do wykonania tej konwersji.</span><span class="sxs-lookup"><span data-stu-id="27b5d-111">Before making the assignment the compiler is implicitly converting the variable `lng`, which is of type long, to an int. This is implicit because no code explicitly instructed the compiler to perform this conversion.</span></span> <span data-ttu-id="27b5d-112">Problem z tym kodem polega na tym, że jest to uważane za konwersję zawężającą, a kompilator nie zezwala na niejawne konwersje zawężające, ponieważ może to oznaczać potencjalną utratę danych.</span><span class="sxs-lookup"><span data-stu-id="27b5d-112">The problem with this code is that this is considered a narrowing conversion, and the compiler does not allow implicit narrowing conversions because there could be a potential loss of data.</span></span>

 <span data-ttu-id="27b5d-113">Konwersja zawężająca istnieje podczas konwertowania na typ danych, który zajmuje mniej miejsca w pamięci niż typ danych, z których konwertujemy.</span><span class="sxs-lookup"><span data-stu-id="27b5d-113">A narrowing conversion exists when converting to a data type that occupies less storage space in memory than the data type we are converting from.</span></span> <span data-ttu-id="27b5d-114">Na przykład konwersja long do int będzie uważana za konwersję zwężającą.</span><span class="sxs-lookup"><span data-stu-id="27b5d-114">For example, converting a long to an int would be considered a narrowing conversion.</span></span> <span data-ttu-id="27b5d-115">Długi zajmuje 8 bajtów pamięci, podczas gdy int zajmuje 4 bajty.</span><span class="sxs-lookup"><span data-stu-id="27b5d-115">A long occupies 8 bytes of memory while an int occupies 4 bytes.</span></span> <span data-ttu-id="27b5d-116">Aby zobaczyć, jak może wystąpić utrata danych, należy wziąć pod uwagę następujący przykład:</span><span class="sxs-lookup"><span data-stu-id="27b5d-116">To see how data loss can occur, consider the following sample:</span></span>

```csharp
int i = 50;
long lng = 3147483647;
i = lng;
```

 <span data-ttu-id="27b5d-117">Zmienna `lng` zawiera teraz wartość, która nie `i` może być przechowywana w zmiennej, ponieważ jest zbyt duża.</span><span class="sxs-lookup"><span data-stu-id="27b5d-117">The variable `lng` now contains a value that cannot be stored in the variable `i` because it is too large.</span></span> <span data-ttu-id="27b5d-118">Gdybyśmy mieli przekonwertować tę wartość na typ int, tracilibyśmy niektóre z naszych danych, a przekonwertowana wartość nie byłaby taka sama jak wartość sprzed konwersji.</span><span class="sxs-lookup"><span data-stu-id="27b5d-118">If we were to convert this value to an int type we would be losing some of our data and the converted value would not be the same as the value before the conversion.</span></span>

 <span data-ttu-id="27b5d-119">Poszerzenie konwersji byłoby przeciwieństwem konwersji zwężenie.</span><span class="sxs-lookup"><span data-stu-id="27b5d-119">A widening conversion would be the opposite of a narrowing conversion.</span></span> <span data-ttu-id="27b5d-120">Wraz z rozszerzającymi się konwersjami konwertujemy na typ danych, który zajmuje więcej miejsca w pamięci niż typ danych, z których konwertujemy.</span><span class="sxs-lookup"><span data-stu-id="27b5d-120">With widening conversions, we are converting to a data type that occupies more storage space in memory than the data type we are converting from.</span></span> <span data-ttu-id="27b5d-121">Oto przykład poszerzenia konwersji:</span><span class="sxs-lookup"><span data-stu-id="27b5d-121">Here is an example of a widening conversion:</span></span>

```csharp
int i = 50;
long lng = 100;
lng = i;
```

 <span data-ttu-id="27b5d-122">Zwróć uwagę na różnicę między tym przykładzie kodu i pierwszy.</span><span class="sxs-lookup"><span data-stu-id="27b5d-122">Notice the difference between this code sample and the first.</span></span> <span data-ttu-id="27b5d-123">Tym razem `lng` zmienna znajduje się po lewej stronie operatora przypisania, więc jest celem naszego przypisania.</span><span class="sxs-lookup"><span data-stu-id="27b5d-123">This time the variable `lng` is on the left-hand side of the assignment operator, so it is the target of our assignment.</span></span> <span data-ttu-id="27b5d-124">Przed przypisaniem można dokonać kompilator musi `i`niejawnie przekonwertować zmienną , która jest typu int, aby wpisać long.</span><span class="sxs-lookup"><span data-stu-id="27b5d-124">Before the assignment can be made, the compiler must implicitly convert the variable `i`, which is of type int, to type long.</span></span> <span data-ttu-id="27b5d-125">Jest to konwersja poszerzenie, ponieważ jesteśmy konwersji z typu, który zajmuje 4 bajty pamięci (int) do typu, który zajmuje 8 bajtów pamięci (długi).</span><span class="sxs-lookup"><span data-stu-id="27b5d-125">This is a widening conversion since we are converting from a type that occupies 4 bytes of memory (an int) to a type that occupies 8 bytes of memory (a long).</span></span> <span data-ttu-id="27b5d-126">Niejawne konwersje rozszerzające są dozwolone, ponieważ nie ma potencjalnej utraty danych.</span><span class="sxs-lookup"><span data-stu-id="27b5d-126">Implicit widening conversions are allowed because there is no potential loss of data.</span></span> <span data-ttu-id="27b5d-127">Każda wartość, która może być przechowywana w int mogą być również przechowywane w długim.</span><span class="sxs-lookup"><span data-stu-id="27b5d-127">Any value that can be stored in an int can also be stored in a long.</span></span>

 <span data-ttu-id="27b5d-128">Wiemy, że niejawne konwersje zawężające nie są dozwolone, więc aby móc skompilować ten kod, musimy jawnie przekonwertować typ danych.</span><span class="sxs-lookup"><span data-stu-id="27b5d-128">We know that implicit narrowing conversions are not allowed, so to be able to compile this code we need to explicitly convert the data type.</span></span> <span data-ttu-id="27b5d-129">Jawne konwersje są wykonywane przy użyciu rzutowania.</span><span class="sxs-lookup"><span data-stu-id="27b5d-129">Explicit conversions are done using casting.</span></span> <span data-ttu-id="27b5d-130">Rzutowanie jest termin używany w języku C# do opisania konwersji jednego typu danych do innego.</span><span class="sxs-lookup"><span data-stu-id="27b5d-130">Casting is the term used in C# to describe converting one data type to another.</span></span> <span data-ttu-id="27b5d-131">Aby uzyskać kod do skompilowania, musimy użyć następującej składni:</span><span class="sxs-lookup"><span data-stu-id="27b5d-131">To get the code to compile we would need to use the following syntax:</span></span>

```csharp
int i = 50;
long lng = 100;
i = (int) lng;   // Cast to int.
```

 <span data-ttu-id="27b5d-132">Trzeci wiersz kodu informuje kompilator jawnie `lng`przekonwertować zmienną , która jest typu long, do int przed dokonaniem przypisania.</span><span class="sxs-lookup"><span data-stu-id="27b5d-132">The third line of code tells the compiler to explicitly convert the variable `lng`, which is of type long, to an int before making the assignment.</span></span> <span data-ttu-id="27b5d-133">Pamiętaj, że w przypadku konwersji zwężenia istnieje potencjalna utrata danych.</span><span class="sxs-lookup"><span data-stu-id="27b5d-133">Remember that with a narrowing conversion, there is a potential loss of data.</span></span> <span data-ttu-id="27b5d-134">Konwersje zawężające powinny być używane z ostrożnością, a nawet jeśli kod skompiluje może uzyskać nieoczekiwane wyniki w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="27b5d-134">Narrowing conversions should be used with caution and even though the code will compile you may get unexpected results at run-time.</span></span>

 <span data-ttu-id="27b5d-135">Ta dyskusja dotyczyła tylko typów wartości.</span><span class="sxs-lookup"><span data-stu-id="27b5d-135">This discussion has only been for value types.</span></span> <span data-ttu-id="27b5d-136">Podczas pracy z typami wartości pracujesz bezpośrednio z danymi przechowywanymi w zmiennej.</span><span class="sxs-lookup"><span data-stu-id="27b5d-136">When working with value types you work directly with the data stored in the variable.</span></span> <span data-ttu-id="27b5d-137">Jednak .NET Framework ma również typy odwołań.</span><span class="sxs-lookup"><span data-stu-id="27b5d-137">However, the .NET Framework also has reference types.</span></span> <span data-ttu-id="27b5d-138">Podczas pracy z typami odwołań pracujesz z odwołaniem do zmiennej, a nie rzeczywistych danych.</span><span class="sxs-lookup"><span data-stu-id="27b5d-138">When working with reference types you are working with a reference to a variable, not the actual data.</span></span> <span data-ttu-id="27b5d-139">Przykładami typów odwołań są klasy, interfejsy i tablice.</span><span class="sxs-lookup"><span data-stu-id="27b5d-139">Examples of reference types would be classes, interfaces and arrays.</span></span> <span data-ttu-id="27b5d-140">Nie można niejawnie lub jawnie przekonwertować jeden typ odwołania do innego, chyba że kompilator umożliwia określonej konwersji lub odpowiednie operatory konwersji są implementowane.</span><span class="sxs-lookup"><span data-stu-id="27b5d-140">You cannot implicitly or explicitly convert one reference type to another unless the compiler allows the specific conversion or the appropriate conversion operators are implemented.</span></span>

 <span data-ttu-id="27b5d-141">Poniższy przykład generuje CS0029:</span><span class="sxs-lookup"><span data-stu-id="27b5d-141">The following sample generates CS0029:</span></span>

```csharp
// CS0029.cs
public class MyInt
{
    private int x = 0;

    // Uncomment this conversion routine to resolve CS0029.
    /*
    public static implicit operator int(MyInt i)
    {
        return i.x;
    }
    */

    public static void Main()
    {
        var myInt = new MyInt();
        int i = myInt; // CS0029
    }
}
```

## <a name="see-also"></a><span data-ttu-id="27b5d-142">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="27b5d-142">See also</span></span>

- [<span data-ttu-id="27b5d-143">Operatory konwersji zdefiniowane przez użytkownika</span><span class="sxs-lookup"><span data-stu-id="27b5d-143">User-defined conversion operators</span></span>](../operators/user-defined-conversion-operators.md)
