---
title: Jak zdefiniować równość wartości dla typu - Przewodnik programowania C#
ms.date: 07/20/2015
helpviewer_keywords:
- overriding Equals method [C#]
- object equivalence [C#]
- Equals method [C#], overriding
- value equality [C#]
- equivalence [C#]
ms.assetid: 4084581e-b931-498b-9534-cf7ef5b68690
ms.openlocfilehash: 140be18698a40be8f394b31fcd42b97d6685cb98
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/14/2020
ms.locfileid: "79157094"
---
# <a name="how-to-define-value-equality-for-a-type-c-programming-guide"></a><span data-ttu-id="d65de-102">Jak zdefiniować równość wartości dla typu (Przewodnik programowania C#)</span><span class="sxs-lookup"><span data-stu-id="d65de-102">How to define value equality for a type (C# Programming Guide)</span></span>

<span data-ttu-id="d65de-103">Podczas definiowania klasy lub struktury, należy zdecydować, czy ma sens, aby utworzyć niestandardową definicję równości wartości (lub równoważności) dla typu.</span><span class="sxs-lookup"><span data-stu-id="d65de-103">When you define a class or struct, you decide whether it makes sense to create a custom definition of value equality (or equivalence) for the type.</span></span> <span data-ttu-id="d65de-104">Zazwyczaj należy zaimplementować równość wartości, gdy obiekty tego typu mają zostać dodane do kolekcji pewnego rodzaju lub gdy ich głównym celem jest przechowywanie zestawu pól lub właściwości.</span><span class="sxs-lookup"><span data-stu-id="d65de-104">Typically, you implement value equality when objects of the type are expected to be added to a collection of some sort, or when their primary purpose is to store a set of fields or properties.</span></span> <span data-ttu-id="d65de-105">Definicję równości wartości można oprzeć na porównaniu wszystkich pól i właściwości w typie lub oprzeć definicję na podzbiorze.</span><span class="sxs-lookup"><span data-stu-id="d65de-105">You can base your definition of value equality on a comparison of all the fields and properties in the type, or you can base the definition on a subset.</span></span>

<span data-ttu-id="d65de-106">W obu przypadkach i w obu klasach i strukturach implementacja powinna być zgodna z pięcioma `x` `y` gwarancjami równoważności (W przypadku poniższych reguł załóżmy, że , i `z` nie są null):</span><span class="sxs-lookup"><span data-stu-id="d65de-106">In either case, and in both classes and structs, your implementation should follow the five guarantees of equivalence (For the following rules, assume that `x`, `y` and `z` are not null):</span></span>  
  
1. <span data-ttu-id="d65de-107">`x.Equals(x)`zwraca `true`.</span><span class="sxs-lookup"><span data-stu-id="d65de-107">`x.Equals(x)` returns `true`.</span></span> <span data-ttu-id="d65de-108">Nazywa się to właściwością refleksyjny.</span><span class="sxs-lookup"><span data-stu-id="d65de-108">This is called the reflexive property.</span></span>  
  
2. <span data-ttu-id="d65de-109">`x.Equals(y)`zwraca tę samą wartość co `y.Equals(x)`.</span><span class="sxs-lookup"><span data-stu-id="d65de-109">`x.Equals(y)` returns the same value as `y.Equals(x)`.</span></span> <span data-ttu-id="d65de-110">Jest to nazywane właściwością symetryczną.</span><span class="sxs-lookup"><span data-stu-id="d65de-110">This is called the symmetric property.</span></span>  
  
3. <span data-ttu-id="d65de-111">jeśli `(x.Equals(y) && y.Equals(z))` `true`powróci `x.Equals(z)` , `true`a następnie zwraca .</span><span class="sxs-lookup"><span data-stu-id="d65de-111">if `(x.Equals(y) && y.Equals(z))` returns `true`, then `x.Equals(z)` returns `true`.</span></span> <span data-ttu-id="d65de-112">Jest to nazywane właściwością przechodnią.</span><span class="sxs-lookup"><span data-stu-id="d65de-112">This is called the transitive property.</span></span>  
  
4. <span data-ttu-id="d65de-113">Kolejne wywołania `x.Equals(y)` zwracania tej samej wartości, o ile obiekty, do których odwołują się x i y, nie są modyfikowane.</span><span class="sxs-lookup"><span data-stu-id="d65de-113">Successive invocations of `x.Equals(y)` return the same value as long as the objects referenced by x and y are not modified.</span></span>  
  
5. <span data-ttu-id="d65de-114">Każda wartość nienull nie jest równa null.</span><span class="sxs-lookup"><span data-stu-id="d65de-114">Any non-null value is not equal to null.</span></span> <span data-ttu-id="d65de-115">Jednak CLR sprawdza null na wszystkie wywołania `NullReferenceException` metody `this` i zgłasza, jeśli odwołanie będzie null.</span><span class="sxs-lookup"><span data-stu-id="d65de-115">However, the CLR checks for null on all method calls and throws a `NullReferenceException` if the `this` reference would be null.</span></span> <span data-ttu-id="d65de-116">W `x.Equals(y)` związku z tym `x` zgłasza wyjątek, gdy jest null.</span><span class="sxs-lookup"><span data-stu-id="d65de-116">Therefore, `x.Equals(y)` throws an exception when `x` is null.</span></span> <span data-ttu-id="d65de-117">To łamie zasady 1 lub 2, `Equals`w zależności od argumentu .</span><span class="sxs-lookup"><span data-stu-id="d65de-117">That breaks rules 1 or 2, depending on the argument to `Equals`.</span></span>

 <span data-ttu-id="d65de-118">Każda struktura, która zostanie zdefiniowana, ma już domyślną <xref:System.ValueType?displayProperty=nameWithType> implementację <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> równości wartości, która dziedziczy po zastąpieniu metody.</span><span class="sxs-lookup"><span data-stu-id="d65de-118">Any struct that you define already has a default implementation of value equality that it inherits from the <xref:System.ValueType?displayProperty=nameWithType> override of the <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="d65de-119">Ta implementacja używa odbicia, aby zbadać wszystkie pola i właściwości w typie.</span><span class="sxs-lookup"><span data-stu-id="d65de-119">This implementation uses reflection to examine all the fields and properties in the type.</span></span> <span data-ttu-id="d65de-120">Mimo że ta implementacja daje poprawne wyniki, jest stosunkowo powolny w porównaniu do implementacji niestandardowej, które można napisać specjalnie dla typu.</span><span class="sxs-lookup"><span data-stu-id="d65de-120">Although this implementation produces correct results, it is relatively slow compared to a custom implementation that you write specifically for the type.</span></span>  
  
 <span data-ttu-id="d65de-121">Szczegóły implementacji równości wartości są różne dla klas i struktur.</span><span class="sxs-lookup"><span data-stu-id="d65de-121">The implementation details for value equality are different for classes and structs.</span></span> <span data-ttu-id="d65de-122">Jednak zarówno klasy, jak i struktury wymagają tych samych podstawowych kroków w celu wdrożenia równości:</span><span class="sxs-lookup"><span data-stu-id="d65de-122">However, both classes and structs require the same basic steps for implementing equality:</span></span>  
  
1. <span data-ttu-id="d65de-123">Zastąp metodę [wirtualną.](../../language-reference/keywords/virtual.md) <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType></span><span class="sxs-lookup"><span data-stu-id="d65de-123">Override the [virtual](../../language-reference/keywords/virtual.md) <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="d65de-124">W większości przypadków implementacji `bool Equals( object obj )` należy po prostu `Equals` wywołać metodę specyficzne <xref:System.IEquatable%601?displayProperty=nameWithType> dla typu, która jest implementacja interfejsu.</span><span class="sxs-lookup"><span data-stu-id="d65de-124">In most cases, your implementation of `bool Equals( object obj )` should just call into the type-specific `Equals` method that is the implementation of the <xref:System.IEquatable%601?displayProperty=nameWithType> interface.</span></span> <span data-ttu-id="d65de-125">(Patrz krok 2.)</span><span class="sxs-lookup"><span data-stu-id="d65de-125">(See step 2.)</span></span>  
  
2. <span data-ttu-id="d65de-126">Zaimplementuj <xref:System.IEquatable%601?displayProperty=nameWithType> interfejs, udostępniając `Equals` metodę specyficzną dla typu.</span><span class="sxs-lookup"><span data-stu-id="d65de-126">Implement the <xref:System.IEquatable%601?displayProperty=nameWithType> interface by providing a type-specific `Equals` method.</span></span> <span data-ttu-id="d65de-127">To jest, gdy rzeczywiste porównanie równoważności jest wykonywana.</span><span class="sxs-lookup"><span data-stu-id="d65de-127">This is where the actual equivalence comparison is performed.</span></span> <span data-ttu-id="d65de-128">Na przykład można zdefiniować równość, porównując tylko jedno lub dwa pola w typie.</span><span class="sxs-lookup"><span data-stu-id="d65de-128">For example, you might decide to define equality by comparing only one or two fields in your type.</span></span> <span data-ttu-id="d65de-129">Nie zgłaszaj wyjątków od `Equals`.</span><span class="sxs-lookup"><span data-stu-id="d65de-129">Do not throw exceptions from `Equals`.</span></span> <span data-ttu-id="d65de-130">Tylko dla klas: Ta metoda powinna sprawdzać tylko pola, które są zadeklarowane w klasie.</span><span class="sxs-lookup"><span data-stu-id="d65de-130">For classes only: This method should examine only fields that are declared in the class.</span></span> <span data-ttu-id="d65de-131">Należy wywołać, `base.Equals` aby zbadać pola, które znajdują się w klasie podstawowej.</span><span class="sxs-lookup"><span data-stu-id="d65de-131">It should call `base.Equals` to examine fields that are in the base class.</span></span> <span data-ttu-id="d65de-132">(Nie należy tego robić, jeśli <xref:System.Object>typ dziedziczy bezpośrednio z , ponieważ <xref:System.Object> implementacja <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> wykonuje sprawdzanie równości odwołania.)</span><span class="sxs-lookup"><span data-stu-id="d65de-132">(Do not do this if the type inherits directly from <xref:System.Object>, because the <xref:System.Object> implementation of <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> performs a reference equality check.)</span></span>  
  
3. <span data-ttu-id="d65de-133">Opcjonalnie, ale zalecane: [==](../../language-reference/operators/equality-operators.md#equality-operator-) Przeciążenie i [!=](../../language-reference/operators/equality-operators.md#inequality-operator-) operatorów.</span><span class="sxs-lookup"><span data-stu-id="d65de-133">Optional but recommended: Overload the [==](../../language-reference/operators/equality-operators.md#equality-operator-) and [!=](../../language-reference/operators/equality-operators.md#inequality-operator-) operators.</span></span>  
  
4. <span data-ttu-id="d65de-134">Zastąp <xref:System.Object.GetHashCode%2A?displayProperty=nameWithType> tak, aby dwa obiekty, które mają równość wartości, wytwarzają ten sam kod skrótu.</span><span class="sxs-lookup"><span data-stu-id="d65de-134">Override <xref:System.Object.GetHashCode%2A?displayProperty=nameWithType> so that two objects that have value equality produce the same hash code.</span></span>  
  
5. <span data-ttu-id="d65de-135">Opcjonalnie: aby obsługiwać definicje "większe niż" lub <xref:System.IComparable%601> "mniej niż", zaimplementuj interfejs dla swojego typu, a także przeciążają [<=](../../language-reference/operators/comparison-operators.md#less-than-or-equal-operator-) i [>=](../../language-reference/operators/comparison-operators.md#greater-than-or-equal-operator-) operatorów.</span><span class="sxs-lookup"><span data-stu-id="d65de-135">Optional: To support definitions for "greater than" or "less than," implement the <xref:System.IComparable%601> interface for your type, and also overload the [<=](../../language-reference/operators/comparison-operators.md#less-than-or-equal-operator-) and [>=](../../language-reference/operators/comparison-operators.md#greater-than-or-equal-operator-) operators.</span></span>  
  
 <span data-ttu-id="d65de-136">Pierwszy przykład, który następuje pokazuje implementacji klasy.</span><span class="sxs-lookup"><span data-stu-id="d65de-136">The first example that follows shows a class implementation.</span></span> <span data-ttu-id="d65de-137">Drugi przykład pokazuje implementacji struktury.</span><span class="sxs-lookup"><span data-stu-id="d65de-137">The second example shows a struct implementation.</span></span>  

## <a name="example"></a><span data-ttu-id="d65de-138">Przykład</span><span class="sxs-lookup"><span data-stu-id="d65de-138">Example</span></span>

 <span data-ttu-id="d65de-139">W poniższym przykładzie pokazano, jak zaimplementować równość wartości w klasie (typ odwołania).</span><span class="sxs-lookup"><span data-stu-id="d65de-139">The following example shows how to implement value equality in a class (reference type).</span></span>  
  
 [!code-csharp[csProgGuideStatements#19](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideStatements/CS/Statements.cs#19)]  
  
 <span data-ttu-id="d65de-140">W klasach (typy odwołań) <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> domyślna implementacja obu metod wykonuje porównanie równości odwołania, a nie sprawdzanie równości wartości.</span><span class="sxs-lookup"><span data-stu-id="d65de-140">On classes (reference types), the default implementation of both <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> methods performs a reference equality comparison, not a value equality check.</span></span> <span data-ttu-id="d65de-141">Gdy realizator zastępuje metodę wirtualną, celem jest nadanie mu semantyki równości wartości.</span><span class="sxs-lookup"><span data-stu-id="d65de-141">When an implementer overrides the virtual method, the purpose is to give it value equality semantics.</span></span>  
  
 <span data-ttu-id="d65de-142">I `==` `!=` operatory mogą być używane z klas, nawet jeśli klasa nie przeciąża ć je.</span><span class="sxs-lookup"><span data-stu-id="d65de-142">The `==` and `!=` operators can be used with classes even if the class does not overload them.</span></span> <span data-ttu-id="d65de-143">Jednak zachowanie domyślne jest do wykonywania sprawdzania równości odwołania.</span><span class="sxs-lookup"><span data-stu-id="d65de-143">However, the default behavior is to perform a reference equality check.</span></span> <span data-ttu-id="d65de-144">W klasie, jeśli przeciążenia `Equals` metody, należy `==` przeciążać i `!=` operatorów, ale nie jest wymagane.</span><span class="sxs-lookup"><span data-stu-id="d65de-144">In a class, if you overload the `Equals` method, you should overload the `==` and `!=` operators, but it is not required.</span></span>  

## <a name="example"></a><span data-ttu-id="d65de-145">Przykład</span><span class="sxs-lookup"><span data-stu-id="d65de-145">Example</span></span>

 <span data-ttu-id="d65de-146">W poniższym przykładzie pokazano, jak zaimplementować równość wartości w strukturze (typ wartości):</span><span class="sxs-lookup"><span data-stu-id="d65de-146">The following example shows how to implement value equality in a struct (value type):</span></span>  
  
 [!code-csharp[csProgGuideStatements#20](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideStatements/CS/Statements.cs#20)]  
  
 <span data-ttu-id="d65de-147">Dla struktur domyślna implementacja <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> (która jest zastąpiona wersja w) <xref:System.ValueType?displayProperty=nameWithType>wykonuje sprawdzanie równości wartości przy użyciu odbicia do porównania wartości każdego pola w typie.</span><span class="sxs-lookup"><span data-stu-id="d65de-147">For structs, the default implementation of <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> (which is the overridden version in <xref:System.ValueType?displayProperty=nameWithType>) performs a value equality check by using reflection to compare the values of every field in the type.</span></span> <span data-ttu-id="d65de-148">Gdy realizator zastępuje `Equals` metodę wirtualną w strukturze, celem jest zapewnienie bardziej efektywnego sposobu wykonywania sprawdzania równości wartości i opcjonalnie oparcie porównania na niektórych podzbiorach pola lub właściwości struktury.</span><span class="sxs-lookup"><span data-stu-id="d65de-148">When an implementer overrides the virtual `Equals` method in a struct, the purpose is to provide a more efficient means of performing the value equality check and optionally to base the comparison on some subset of the struct's field or properties.</span></span>  
  
 <span data-ttu-id="d65de-149">Operatory [==](../../language-reference/operators/equality-operators.md#equality-operator-) i [!=](../../language-reference/operators/equality-operators.md#inequality-operator-) nie mogą działać na strukturze, chyba że struktura jawnie je przeciąża.</span><span class="sxs-lookup"><span data-stu-id="d65de-149">The [==](../../language-reference/operators/equality-operators.md#equality-operator-) and [!=](../../language-reference/operators/equality-operators.md#inequality-operator-) operators cannot operate on a struct unless the struct explicitly overloads them.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="d65de-150">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="d65de-150">See also</span></span>

- [<span data-ttu-id="d65de-151">Porównywanie równości</span><span class="sxs-lookup"><span data-stu-id="d65de-151">Equality comparisons</span></span>](equality-comparisons.md)
- [<span data-ttu-id="d65de-152">Przewodnik programowania w języku C#</span><span class="sxs-lookup"><span data-stu-id="d65de-152">C# programming guide</span></span>](../index.md)
