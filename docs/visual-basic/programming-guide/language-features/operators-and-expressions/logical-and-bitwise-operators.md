---
title: Operatory logiczne i bitowe w Visual Basic
ms.custom: 
ms.date: 07/20/2015
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: devlang-visual-basic
ms.topic: article
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
caps.latest.revision: "22"
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: ba48f722a11e93f82ae99aa407c3096a964e5ddd
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 11/21/2017
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="cb863-102">Operatory logiczne i bitowe w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="cb863-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="cb863-103">Operatory logiczne porównują `Boolean` wyrażeń i przywracać `Boolean` wynik.</span><span class="sxs-lookup"><span data-stu-id="cb863-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="cb863-104">`And`, `Or`, `AndAlso`, `OrElse`, I `Xor` operatory są *binary* ponieważ podejmują dwóch argumentów operacji, podczas gdy `Not` operator jest *jednoargumentowy* z powodu jednego argumentu.</span><span class="sxs-lookup"><span data-stu-id="cb863-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="cb863-105">Niektóre z tych operatorów można również wykonywać operacje logiczne bitowe wartości całkowite.</span><span class="sxs-lookup"><span data-stu-id="cb863-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="cb863-106">Jednoargumentowy Operator logiczny</span><span class="sxs-lookup"><span data-stu-id="cb863-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="cb863-107">[Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) wykonuje logiczną *negacji* na `Boolean` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="cb863-107">The [Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="cb863-108">Daje on logicznej przeciwieństwem jej argument.</span><span class="sxs-lookup"><span data-stu-id="cb863-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="cb863-109">Jeśli wyrażenie ma `True`, następnie `Not` zwraca `False`; Jeśli wyrażenie ma `False`, następnie `Not` zwraca `True`.</span><span class="sxs-lookup"><span data-stu-id="cb863-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="cb863-110">Ilustruje to poniższy przykład.</span><span class="sxs-lookup"><span data-stu-id="cb863-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](../../../../visual-basic/language-reference/operators/codesnippet/VisualBasic/logical-and-bitwise-operators_1.vb)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="cb863-111">Operatory logiczne binarne</span><span class="sxs-lookup"><span data-stu-id="cb863-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="cb863-112">[i operatora](../../../../visual-basic/language-reference/operators/and-operator.md) wykonuje logiczną *połączeniu* na dwóch `Boolean` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="cb863-112">The [And Operator](../../../../visual-basic/language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="cb863-113">Jeśli oba wyrażenia mają `True`, następnie `And` zwraca `True`.</span><span class="sxs-lookup"><span data-stu-id="cb863-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="cb863-114">Jeśli co najmniej jedno z wyrażeń daje w wyniku `False`, następnie `And` zwraca `False`.</span><span class="sxs-lookup"><span data-stu-id="cb863-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="cb863-115">[Lub Operator](../../../../visual-basic/language-reference/operators/or-operator.md) wykonuje logiczną *rozłączenia* lub *włączenia* na dwóch `Boolean` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="cb863-115">The [Or Operator](../../../../visual-basic/language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="cb863-116">Jeśli wyrażenie zwraca `True`, lub oba obliczać `True`, następnie `Or` zwraca `True`.</span><span class="sxs-lookup"><span data-stu-id="cb863-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="cb863-117">Jeśli żadna wyrażenie `True`, `Or` zwraca `False`.</span><span class="sxs-lookup"><span data-stu-id="cb863-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="cb863-118">[Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) wykonuje logiczną *wykluczeń* na dwóch `Boolean` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="cb863-118">The [Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="cb863-119">Jeśli dokładnie jedno wyrażenie daje w wyniku `True`, ale nie oba `Xor` zwraca `True`.</span><span class="sxs-lookup"><span data-stu-id="cb863-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="cb863-120">Jeśli oba wyrażenia mają `True` lub oba obliczać `False`, `Xor` zwraca `False`.</span><span class="sxs-lookup"><span data-stu-id="cb863-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="cb863-121">Poniższy przykład przedstawia `And`, `Or`, i `Xor` operatorów.</span><span class="sxs-lookup"><span data-stu-id="cb863-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](../../../../visual-basic/language-reference/operators/codesnippet/VisualBasic/logical-and-bitwise-operators_2.vb)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="cb863-122">Zwarcie logicznego operacji</span><span class="sxs-lookup"><span data-stu-id="cb863-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="cb863-123">[AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) jest bardzo podobny do `And` operatora, w tym również wykonuje logiczną na dwóch `Boolean` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="cb863-123">The [AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="cb863-124">Najważniejsza różnica między nimi jest to, że `AndAlso` wykazuje *zwarcie* zachowanie.</span><span class="sxs-lookup"><span data-stu-id="cb863-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="cb863-125">Jeśli pierwsze wyrażenie w `AndAlso` wyrażenie daje w wyniku `False`, a następnie drugie wyrażenie nie jest oceniany, ponieważ nie można zmienić wynik końcowy i `AndAlso` zwraca `False`.</span><span class="sxs-lookup"><span data-stu-id="cb863-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="cb863-126">Podobnie [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) wykonuje zwarcie logicznego rozłączenia dwóch `Boolean` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="cb863-126">Similarly, the [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="cb863-127">Jeśli pierwsze wyrażenie w `OrElse` wyrażenie daje w wyniku `True`, a następnie drugie wyrażenie nie jest oceniany, ponieważ nie można zmienić wynik końcowy i `OrElse` zwraca `True`.</span><span class="sxs-lookup"><span data-stu-id="cb863-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="cb863-128">Zwarcie kompromisy</span><span class="sxs-lookup"><span data-stu-id="cb863-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="cb863-129">Zwarcie może poprawić wydajność przez nie obliczenia wyrażenia, którego nie można zmienić wynik operacji logicznej.</span><span class="sxs-lookup"><span data-stu-id="cb863-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="cb863-130">Jednak jeśli tego wyrażenia przeprowadza dodatkowych akcji, zwarcie pomija te akcje.</span><span class="sxs-lookup"><span data-stu-id="cb863-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="cb863-131">Na przykład, jeśli wyrażenie zawiera wywołanie `Function` procedury, że procedura nie jest wywoływana, gdy wyrażenie jest zwartym i jakiegokolwiek dodatkowego kodu zawarte w `Function` nie działa.</span><span class="sxs-lookup"><span data-stu-id="cb863-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="cb863-132">W związku z tym funkcja może być uruchamiany tylko od czasu do czasu i może nie być poprawnie badane.</span><span class="sxs-lookup"><span data-stu-id="cb863-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="cb863-133">Lub logiki program może być zależna od kodu w `Function`.</span><span class="sxs-lookup"><span data-stu-id="cb863-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="cb863-134">Poniższy przykład przedstawia różnice między `And`, `Or`i ich odpowiedniki short-circuiting.</span><span class="sxs-lookup"><span data-stu-id="cb863-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](../../../../visual-basic/language-reference/operators/codesnippet/VisualBasic/logical-and-bitwise-operators_3.vb)]  
  
 [!code-vb[VbVbalrOperators#80](../../../../visual-basic/language-reference/operators/codesnippet/VisualBasic/logical-and-bitwise-operators_4.vb)]  
  
 [!code-vb[VbVbalrOperators#79](../../../../visual-basic/language-reference/operators/codesnippet/VisualBasic/logical-and-bitwise-operators_5.vb)]  
  
 <span data-ttu-id="cb863-135">W poprzednim przykładzie, należy pamiętać, że niektóre ważne kodu wewnątrz `checkIfValid()` nie działa, gdy wywołanie jest zwartym.</span><span class="sxs-lookup"><span data-stu-id="cb863-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="cb863-136">Pierwszy `If` wywołania instrukcji `checkIfValid()` mimo że `12 > 45` zwraca `False`, ponieważ `And` nie zwarcia.</span><span class="sxs-lookup"><span data-stu-id="cb863-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="cb863-137">Drugi `If` nie mogą wywoływać instrukcji `checkIfValid()`, ponieważ podczas `12 > 45` zwraca `False`, `AndAlso` short-circuits drugie wyrażenie.</span><span class="sxs-lookup"><span data-stu-id="cb863-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="cb863-138">Trzeci `If` wywołania instrukcji `checkIfValid()` mimo że `12 < 45` zwraca `True`, ponieważ `Or` nie zwarcia.</span><span class="sxs-lookup"><span data-stu-id="cb863-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="cb863-139">Czwarta `If` nie mogą wywoływać instrukcji `checkIfValid()`, ponieważ podczas `12 < 45` zwraca `True`, `OrElse` short-circuits drugie wyrażenie.</span><span class="sxs-lookup"><span data-stu-id="cb863-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="cb863-140">Operacje bitowe</span><span class="sxs-lookup"><span data-stu-id="cb863-140">Bitwise Operations</span></span>  
 <span data-ttu-id="cb863-141">Operacje bitowe ocenić dwóch wartości całkowitych na wartości w postaci pliku binarnego (podstawa 2).</span><span class="sxs-lookup"><span data-stu-id="cb863-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="cb863-142">Ich porównać bitów w odpowiednich miejscach, a następnie przypisz wartości na podstawie porównania.</span><span class="sxs-lookup"><span data-stu-id="cb863-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="cb863-143">Poniższy przykład przedstawia `And` operatora.</span><span class="sxs-lookup"><span data-stu-id="cb863-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/codesnippet/VisualBasic/logical-and-bitwise-operators_6.vb)]  
  
 <span data-ttu-id="cb863-144">Powyższy przykład ustawia wartość `x` do 1.</span><span class="sxs-lookup"><span data-stu-id="cb863-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="cb863-145">Dzieje się z następujących powodów:</span><span class="sxs-lookup"><span data-stu-id="cb863-145">This happens for the following reasons:</span></span>  
  
-   <span data-ttu-id="cb863-146">Wartości są traktowane jako dane binarne:</span><span class="sxs-lookup"><span data-stu-id="cb863-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="cb863-147">3 w formacie binarnym = 011</span><span class="sxs-lookup"><span data-stu-id="cb863-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="cb863-148">5 w postaci binarnej = 101</span><span class="sxs-lookup"><span data-stu-id="cb863-148">5 in binary form = 101</span></span>  
  
-   <span data-ttu-id="cb863-149">`And` Operator porównuje binarne reprezentacje jedną pozycję binarne (bit) w czasie.</span><span class="sxs-lookup"><span data-stu-id="cb863-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="cb863-150">Jeśli oba bity na określonej pozycji 1, 1 znajduje się w tym miejscu w wyniku.</span><span class="sxs-lookup"><span data-stu-id="cb863-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="cb863-151">Jeśli albo bit ma wartość 0, 0 znajduje się w tym miejscu w wyniku.</span><span class="sxs-lookup"><span data-stu-id="cb863-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="cb863-152">W powyższym przykładzie wynik działania jest prawidłowy w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="cb863-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="cb863-153">011 (3 w formacie binarnym)</span><span class="sxs-lookup"><span data-stu-id="cb863-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="cb863-154">101 (5 w formacie binarnym)</span><span class="sxs-lookup"><span data-stu-id="cb863-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="cb863-155">001 (wyniku w postaci binarnej)</span><span class="sxs-lookup"><span data-stu-id="cb863-155">001 (The result, in binary form)</span></span>  
  
-   <span data-ttu-id="cb863-156">Wynik jest traktowane jako dziesiętne.</span><span class="sxs-lookup"><span data-stu-id="cb863-156">The result is treated as decimal.</span></span> <span data-ttu-id="cb863-157">Wartość 001 to binarna reprezentacja 1, więc `x` = 1.</span><span class="sxs-lookup"><span data-stu-id="cb863-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="cb863-158">Bitowe `Or` operacja jest podobny, z tą różnicą, że 1 jest przypisany do bitu wyniku, jeśli jednego lub obu porównaniu bitów jest 1.</span><span class="sxs-lookup"><span data-stu-id="cb863-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="cb863-159">`Xor`przypisuje 1 bitu wyniku, jeśli jest dokładnie jeden z porównaniu usługi bits (nie obie) 1.</span><span class="sxs-lookup"><span data-stu-id="cb863-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="cb863-160">`Not`przyjmuje jeden operand i odwraca wszystkie bity, łącznie z bitowego logowania i przypisuje wartość tego wyniku.</span><span class="sxs-lookup"><span data-stu-id="cb863-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="cb863-161">Oznacza to, że dla podpisanej liczby dodatnie `Not` zawsze zwraca wartość ujemną, a liczby ujemne `Not` zawsze zwraca wartość dodatnią lub wartość zero.</span><span class="sxs-lookup"><span data-stu-id="cb863-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="cb863-162">`AndAlso` i `OrElse` operatory nie obsługują operacje bitowe.</span><span class="sxs-lookup"><span data-stu-id="cb863-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="cb863-163">Operacje bitowe mogą być wykonywane na tylko w przypadku typów całkowitych.</span><span class="sxs-lookup"><span data-stu-id="cb863-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="cb863-164">Wartości zmiennoprzecinkowych muszą zostać skonwertowane do typów całkowitych przed kontynuowaniem operacji.</span><span class="sxs-lookup"><span data-stu-id="cb863-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="cb863-165">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="cb863-165">See Also</span></span>  
 [<span data-ttu-id="cb863-166">Operatory logiczne/bitowe (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="cb863-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../../visual-basic/language-reference/operators/logical-bitwise-operators.md)  
 [<span data-ttu-id="cb863-167">Wyrażenia logiczne</span><span class="sxs-lookup"><span data-stu-id="cb863-167">Boolean Expressions</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md)  
 [<span data-ttu-id="cb863-168">Operatory arytmetyczne w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="cb863-168">Arithmetic Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/arithmetic-operators.md)  
 [<span data-ttu-id="cb863-169">Operatory porównania w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="cb863-169">Comparison Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/comparison-operators.md)  
 [<span data-ttu-id="cb863-170">Operatory łączenia w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="cb863-170">Concatenation Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/concatenation-operators.md)  
 [<span data-ttu-id="cb863-171">Skuteczna kombinacja operatorów</span><span class="sxs-lookup"><span data-stu-id="cb863-171">Efficient Combination of Operators</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/efficient-combination-of-operators.md)
