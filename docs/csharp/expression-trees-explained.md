---
title: Wyjaśniono drzew wyrażeń
description: Więcej informacji na temat drzew wyrażeń i jak są one przydatne w tłumaczenie algorytmów dla zewnętrznych wykonywania i kontroli kodu przed jej wykonanie.
ms.date: 06/20/2016
ms.assetid: bbcdd339-86eb-4ae5-9911-4c214a39a92d
ms.openlocfilehash: 97cba9e5ec388729d23fb2689dfc1842a42af9b6
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 05/04/2018
---
# <a name="expression-trees-explained"></a><span data-ttu-id="f6570-103">Wyjaśniono drzew wyrażeń</span><span class="sxs-lookup"><span data-stu-id="f6570-103">Expression Trees Explained</span></span>

[<span data-ttu-id="f6570-104">Poprzednie — omówienie</span><span class="sxs-lookup"><span data-stu-id="f6570-104">Previous -- Overview</span></span>](expression-trees.md)

<span data-ttu-id="f6570-105">Drzewo wyrażenia jest strukturą danych, który definiuje kodu.</span><span class="sxs-lookup"><span data-stu-id="f6570-105">An Expression Tree is a data structure that defines code.</span></span> <span data-ttu-id="f6570-106">Są one oparte na tej samej struktury, używane do analizowania kodu i generowanie skompilowanych danych wyjściowych kompilatora.</span><span class="sxs-lookup"><span data-stu-id="f6570-106">They are based on the same structures that a compiler uses to analyze code and generate the compiled output.</span></span> <span data-ttu-id="f6570-107">Omówione w tym samouczku, można zauważyć dość nieco podobieństwa między drzew wyrażeń i typy używane w interfejsach API Roslyn do budowania [analizatory i CodeFixes](https://github.com/dotnet/roslyn-analyzers).</span><span class="sxs-lookup"><span data-stu-id="f6570-107">As you read through this tutorial, you will notice quite a bit of similarity between Expression Trees and the types used in the Roslyn APIs to build [Analyzers and CodeFixes](https://github.com/dotnet/roslyn-analyzers).</span></span>
<span data-ttu-id="f6570-108">(Analizatory i CodeFixes są pakiety NuGet, które wykonują analizy statycznej na kod i może sugerować potencjalne rozwiązania dla dewelopera). Pojęcia są podobne, a w rezultacie jest strukturą danych, umożliwiający kontroli kodu źródłowego w znaczący sposób.</span><span class="sxs-lookup"><span data-stu-id="f6570-108">(Analyzers and CodeFixes are NuGet packages that perform static analysis on code and can suggest potential fixes for a developer.) The concepts are similar, and the end result is a data structure that allows examination of the source code in a meaningful way.</span></span> <span data-ttu-id="f6570-109">Jednak drzew wyrażeń są oparte na zbiór całkiem klasy i interfejsy API niż Roslyn interfejsów API.</span><span class="sxs-lookup"><span data-stu-id="f6570-109">However, Expression Trees are based on a totally different set of classes and APIs than the Roslyn APIs.</span></span>
    
<span data-ttu-id="f6570-110">Oto prosty przykład.</span><span class="sxs-lookup"><span data-stu-id="f6570-110">Let's look at a simple example.</span></span>
<span data-ttu-id="f6570-111">Oto wiersz kodu:</span><span class="sxs-lookup"><span data-stu-id="f6570-111">Here's a line of code:</span></span>
```csharp
var sum = 1 + 2;
```
<span data-ttu-id="f6570-112">Gdyby analizowanie to jako drzewo wyrażenia drzewa zawiera kilka węzłów.</span><span class="sxs-lookup"><span data-stu-id="f6570-112">If you were to analyze this as an expression tree, the tree contains several nodes.</span></span>
<span data-ttu-id="f6570-113">Najbardziej zewnętrznego węzeł jest deklaracja zmiennej instrukcji za pomocą przypisania (`var sum = 1 + 2;`) tego węzła peryferyjnych zawiera kilka węzłów podrzędnych: deklaracja zmiennej, operatora przypisania i reprezentujący po prawej stronie znaku równości.</span><span class="sxs-lookup"><span data-stu-id="f6570-113">The outermost node is a variable declaration statement with assignment (`var sum = 1 + 2;`) That outermost node contains several child nodes: a variable declaration, an assignment operator, and an expression representing the right hand side of the equals sign.</span></span> <span data-ttu-id="f6570-114">Czy wyrażenie jest podzielona na wyrażeń, które reprezentują operacja dodawania i lewy i prawy argumentów operacji dodawania.</span><span class="sxs-lookup"><span data-stu-id="f6570-114">That expression is further subdivided into expressions that represent the addition operation, and left and right operands of the addition.</span></span>

<span data-ttu-id="f6570-115">Umożliwia przechodzenie bardziej do wyrażeń, które składają się z prawej strony znaku równości.</span><span class="sxs-lookup"><span data-stu-id="f6570-115">Let's drill down a bit more into the expressions that make up the right side of the equals sign.</span></span>
<span data-ttu-id="f6570-116">Wyrażenie jest `1 + 2`.</span><span class="sxs-lookup"><span data-stu-id="f6570-116">The expression is `1 + 2`.</span></span> <span data-ttu-id="f6570-117">To wyrażenie binarne.</span><span class="sxs-lookup"><span data-stu-id="f6570-117">That's a binary expression.</span></span> <span data-ttu-id="f6570-118">W szczególności jest to wyrażenie binarne dodawania.</span><span class="sxs-lookup"><span data-stu-id="f6570-118">More specifically, it's a binary addition expression.</span></span> <span data-ttu-id="f6570-119">Wyrażenie binarne dodanie ma dwa elementy podrzędne, reprezentujący lewy i prawy węzły wyrażenia dodawania.</span><span class="sxs-lookup"><span data-stu-id="f6570-119">A binary addition expression has two children, representing the left and right nodes of the addition expression.</span></span> <span data-ttu-id="f6570-120">W tym miejscu oba węzły są wyrażenia stałe: lewy argument operacji jest wartość `1`, prawy operand jest to wartość `2`.</span><span class="sxs-lookup"><span data-stu-id="f6570-120">Here, both nodes are constant expressions: The left operand is the value `1`, and the right operand is the value `2`.</span></span>

<span data-ttu-id="f6570-121">Efekty wizualne, całą instrukcję jest drzewo: można uruchomić w węźle głównym i przesyłane do każdego węzła w drzewie, aby wyświetlić kod, który stanowi instrukcji:</span><span class="sxs-lookup"><span data-stu-id="f6570-121">Visually, the entire statement is a tree: You could start at the root node, and travel to each node in the tree to see the code that makes up the statement:</span></span>

- <span data-ttu-id="f6570-122">Deklaracja zmiennej instrukcji za pomocą przypisania (`var sum = 1 + 2;`)</span><span class="sxs-lookup"><span data-stu-id="f6570-122">Variable declaration statement with assignment (`var sum = 1 + 2;`)</span></span>
    * <span data-ttu-id="f6570-123">Deklaracja zmiennej typu niejawnego (`var sum`)</span><span class="sxs-lookup"><span data-stu-id="f6570-123">Implicit variable type declaration (`var sum`)</span></span>
        - <span data-ttu-id="f6570-124">Niejawne var — słowo kluczowe (`var`)</span><span class="sxs-lookup"><span data-stu-id="f6570-124">Implicit var keyword (`var`)</span></span>
        - <span data-ttu-id="f6570-125">Nazwa zmiennej deklaracji (`sum`)</span><span class="sxs-lookup"><span data-stu-id="f6570-125">Variable name declaration (`sum`)</span></span>
    * <span data-ttu-id="f6570-126">Operator przypisania (`=`)</span><span class="sxs-lookup"><span data-stu-id="f6570-126">Assignment operator (`=`)</span></span>
    * <span data-ttu-id="f6570-127">Wyrażenie binarne dodawania (`1 + 2`)</span><span class="sxs-lookup"><span data-stu-id="f6570-127">Binary addition expression (`1 + 2`)</span></span>
        - <span data-ttu-id="f6570-128">Lewej strony (`1`)</span><span class="sxs-lookup"><span data-stu-id="f6570-128">Left operand (`1`)</span></span>
        - <span data-ttu-id="f6570-129">Operator dodawania (`+`)</span><span class="sxs-lookup"><span data-stu-id="f6570-129">Addition operator (`+`)</span></span>
        - <span data-ttu-id="f6570-130">Prawy argument operacji (`2`)</span><span class="sxs-lookup"><span data-stu-id="f6570-130">Right operand (`2`)</span></span>

<span data-ttu-id="f6570-131">Może to wyglądać skomplikowane, ale jest bardzo zaawansowaną.</span><span class="sxs-lookup"><span data-stu-id="f6570-131">This may look complicated, but it is very powerful.</span></span> <span data-ttu-id="f6570-132">Po tym samym procesie rozkładają się znacznie bardziej złożonych.</span><span class="sxs-lookup"><span data-stu-id="f6570-132">Following the same process, you can decompose much more complicated expressions.</span></span> <span data-ttu-id="f6570-133">Należy wziąć pod uwagę tego wyrażenia:</span><span class="sxs-lookup"><span data-stu-id="f6570-133">Consider this expression:</span></span>
```csharp
var finalAnswer = this.SecretSauceFunction(
    currentState.createInterimResult(), currentState.createSecondValue(1, 2),
    decisionServer.considerFinalOptions("hello")) +
    MoreSecretSauce('A', DateTime.Now, true);
```

<span data-ttu-id="f6570-134">Powyższe wyrażenie jest również deklaracji zmiennej z przydziałem.</span><span class="sxs-lookup"><span data-stu-id="f6570-134">The expression above is also a variable declaration with an assignment.</span></span>
<span data-ttu-id="f6570-135">W tym wystąpieniu po prawej stronie przypisania jest znacznie bardziej skomplikowane drzewa.</span><span class="sxs-lookup"><span data-stu-id="f6570-135">In this instance, the right hand side of the assignment is a much more complicated tree.</span></span>
<span data-ttu-id="f6570-136">Nie użyjemy dekompozycji to wyrażenie, ale należy wziąć pod uwagę, co może być w różnych węzłach.</span><span class="sxs-lookup"><span data-stu-id="f6570-136">I'm not going to decompose this expression, but consider what the different nodes might be.</span></span> <span data-ttu-id="f6570-137">Brak przy użyciu bieżącego obiektu jako odbiornik, który ma jawnego wywołania metody `this` odbiornika, który nie obsługuje.</span><span class="sxs-lookup"><span data-stu-id="f6570-137">There are method calls using the current object as a receiver, one that has an explicit `this` receiver, one that does not.</span></span> <span data-ttu-id="f6570-138">Brak wywołania metody, przy użyciu innych obiektów odbiornik, stałych argumentów o różnych typach.</span><span class="sxs-lookup"><span data-stu-id="f6570-138">There are method calls using other receiver objects, there are constant arguments of different types.</span></span> <span data-ttu-id="f6570-139">A na koniec jest operator binarny dodawania.</span><span class="sxs-lookup"><span data-stu-id="f6570-139">And finally, there is a binary addition operator.</span></span> <span data-ttu-id="f6570-140">W zależności od typu zwracanego przez `SecretSauceFunction()` lub `MoreSecretSauce()`, że operator binarny dodawania może być wywołanie metody operator dodawania przesłonięte, rozpoznawania wywołanie metody statycznej operatora binarnego dodawania zdefiniowanej dla klasy.</span><span class="sxs-lookup"><span data-stu-id="f6570-140">Depending on the return type of `SecretSauceFunction()` or `MoreSecretSauce()`, that binary addition operator may be a method call to an overridden addition operator, resolving to a static method call to the binary addition operator defined for a class.</span></span>

<span data-ttu-id="f6570-141">Pomimo tego postrzegana złożoności powyższe wyrażenie tworzy strukturę drzewa, która może zostać przesłane bez pierwszej próbie.</span><span class="sxs-lookup"><span data-stu-id="f6570-141">Despite this perceived complexity, the expression above creates a tree structure that can be navigated as easily as the first sample.</span></span> <span data-ttu-id="f6570-142">Można zachować przechodzenie węzłów podrzędnych można znaleźć w wyrażeniu węzłów liści.</span><span class="sxs-lookup"><span data-stu-id="f6570-142">You can keep traversing child nodes to find leaf nodes in the expression.</span></span> <span data-ttu-id="f6570-143">Węzły nadrzędne będzie zawierać odwołań do ich elementy podrzędne, a każdy węzeł ma właściwość, która opisuje rodzaj węzła jest.</span><span class="sxs-lookup"><span data-stu-id="f6570-143">Parent nodes will have references to their children, and each node has a property that describes what kind of node it is.</span></span>

<span data-ttu-id="f6570-144">Struktura drzewa wyrażenia jest bardzo spójna.</span><span class="sxs-lookup"><span data-stu-id="f6570-144">The structure of an expression tree is very consistent.</span></span> <span data-ttu-id="f6570-145">Gdy znasz już podstawy, można zrozumieć nawet najbardziej złożoną kodu, kiedy jest reprezentowany jako drzewo wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="f6570-145">Once you've learned the basics, you can understand even the most complex code when it is represented as an expression tree.</span></span> <span data-ttu-id="f6570-146">Przejrzysty wygląd w strukturze danych wyjaśniono, jak analizować najbardziej złożonych programów C# i utworzyć odpowiednie dane wyjściowe z tego kodu źródłowego skomplikowane kompilatora C#.</span><span class="sxs-lookup"><span data-stu-id="f6570-146">The elegance in the data structure explains how the C# compiler can analyze the most complex C# programs and create proper output from that complicated source code.</span></span>

<span data-ttu-id="f6570-147">Po zapoznanie się ze struktury drzewa wyrażeń, zostanie ustalone, że wiedzy, które zostały uzyskane w szybko umożliwia pracę z wielu bardziej zaawansowanych scenariuszy.</span><span class="sxs-lookup"><span data-stu-id="f6570-147">Once you become familiar with the structure of expression trees, you will find that knowledge you've gained quickly enables you to work with many more and more advanced scenarios.</span></span> <span data-ttu-id="f6570-148">Brak wyjątkowo wydajnego na drzewa wyrażeń.</span><span class="sxs-lookup"><span data-stu-id="f6570-148">There is incredible power to expression trees.</span></span>

<span data-ttu-id="f6570-149">Oprócz tłumaczenia algorytmy do wykonania w innych środowiskach, drzew wyrażeń można ułatwić zapisu algorytmów inspekcji przed jego wykonaniem kodu.</span><span class="sxs-lookup"><span data-stu-id="f6570-149">In addition to translating algorithms to execute in other environments, expression trees can be used to make it easier to write algorithms that inspect code before executing it.</span></span> <span data-ttu-id="f6570-150">Można napisanie metody, którego argumenty wyrażenia, a następnie sprawdź te wyrażenia przed wykonaniem kodu.</span><span class="sxs-lookup"><span data-stu-id="f6570-150">You can write a method whose arguments are expressions and then examine those expressions before executing the code.</span></span> <span data-ttu-id="f6570-151">Drzewo wyrażenia jest reprezentację pełnego kodu: można zobaczyć wartości wszystkie wyrażenia podrzędnego.</span><span class="sxs-lookup"><span data-stu-id="f6570-151">The Expression Tree is a full representation of the code: you can see values of any sub-expression.</span></span>
<span data-ttu-id="f6570-152">Nazwy metod i właściwości jest widoczny.</span><span class="sxs-lookup"><span data-stu-id="f6570-152">You can see method and property names.</span></span> <span data-ttu-id="f6570-153">Można zobaczyć wartość wszystkie wyrażenia stałej.</span><span class="sxs-lookup"><span data-stu-id="f6570-153">You can see the value of any constant expressions.</span></span>
<span data-ttu-id="f6570-154">Można również przekonwertować drzewo wyrażenia wykonywalnego delegata i wykonywania kodu.</span><span class="sxs-lookup"><span data-stu-id="f6570-154">You can also convert an expression tree into an executable delegate, and execute the code.</span></span>

<span data-ttu-id="f6570-155">Interfejsy API do drzewa wyrażeń umożliwiają tworzenie drzewa reprezentujących prawie konstrukcji prawidłowy kod.</span><span class="sxs-lookup"><span data-stu-id="f6570-155">The APIs for Expression Trees enable you to create trees that represent almost any valid code construct.</span></span> <span data-ttu-id="f6570-156">Jednak aby rzeczy, wystarczy, niektóre idioms C# nie można utworzyć drzewa wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="f6570-156">However, to keep things as simple as possible, some C# idioms cannot be created in an expression tree.</span></span> <span data-ttu-id="f6570-157">Przykładem jest asynchronicznego wyrażenia (przy użyciu `async` i `await` słowa kluczowe).</span><span class="sxs-lookup"><span data-stu-id="f6570-157">One example is asynchronous expressions (using the `async` and `await` keywords).</span></span> <span data-ttu-id="f6570-158">Jeśli Twoje potrzeby wymagają asynchroniczne algorytmów, będzie potrzebny do manipulowania `Task` obiekty bezpośrednio, zamiast polegać na obsługa kompilatora.</span><span class="sxs-lookup"><span data-stu-id="f6570-158">If your needs require asynchronous algorithms, you would need to manipulate the `Task` objects directly, rather than rely on the compiler support.</span></span> <span data-ttu-id="f6570-159">Inny jest utworzenie pętli.</span><span class="sxs-lookup"><span data-stu-id="f6570-159">Another is in creating loops.</span></span> <span data-ttu-id="f6570-160">Zazwyczaj w celu utworzenia tych elementów przy użyciu `for`, `foreach`, `while` lub `do` pętli.</span><span class="sxs-lookup"><span data-stu-id="f6570-160">Typically, you create these by using `for`, `foreach`, `while` or `do` loops.</span></span> <span data-ttu-id="f6570-161">Jak można zauważyć [dalej w tej serii](expression-trees-building.md), interfejsy API do drzewa wyrażeń obsługuje wyrażenia jednej pętli z `break` i `continue` wyrażeń sterujących powtarzające się pętli.</span><span class="sxs-lookup"><span data-stu-id="f6570-161">As you'll see [later in this series](expression-trees-building.md), the APIs for expression trees support a single loop expression, with `break` and `continue` expressions that control repeating the loop.</span></span>

<span data-ttu-id="f6570-162">Jedyną operacją, której nie można wykonać, jest zmodyfikowanie drzewo wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="f6570-162">The one thing you can't do is modify an expression tree.</span></span>  <span data-ttu-id="f6570-163">Drzewa wyrażeń są niezmienne danych struktury.</span><span class="sxs-lookup"><span data-stu-id="f6570-163">Expression Trees are immutable data structures.</span></span> <span data-ttu-id="f6570-164">Jeśli chcesz zmodyfikować (Zmień) wyrażenie drzewa, należy utworzyć nowe drzewo będący kopię oryginału, ale odpowiednie zmiany.</span><span class="sxs-lookup"><span data-stu-id="f6570-164">If you want to mutate (change) an expression tree, you must create a new tree that is a copy of the original, but with your desired changes.</span></span> 

[<span data-ttu-id="f6570-165">Dalej--Framework typy obsługi drzewa wyrażeń</span><span class="sxs-lookup"><span data-stu-id="f6570-165">Next -- Framework Types Supporting Expression Trees</span></span>](expression-classes.md)
