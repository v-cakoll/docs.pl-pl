---
title: Przesłanianie tożsamości usługi na potrzeby uwierzytelniania
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: d613a22b-07d7-41a4-bada-1adc653b9b5d
ms.openlocfilehash: d1c0cc487d8deea7045ee9653d1eae9b73ec864f
ms.sourcegitcommit: 7e2128d4a4c45b4274bea3b8e5760d4694569ca1
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 01/14/2020
ms.locfileid: "75938015"
---
# <a name="overriding-the-identity-of-a-service-for-authentication"></a><span data-ttu-id="36a01-102">Przesłanianie tożsamości usługi na potrzeby uwierzytelniania</span><span class="sxs-lookup"><span data-stu-id="36a01-102">Overriding the Identity of a Service for Authentication</span></span>
<span data-ttu-id="36a01-103">Zazwyczaj nie trzeba ustawiać tożsamości w usłudze, ponieważ wybór typu poświadczeń klienta określa typ tożsamości uwidocznionej w metadanych usługi.</span><span class="sxs-lookup"><span data-stu-id="36a01-103">Typically, you do not have to set the identity on a service because the selection of a client credential type dictates the type of identity exposed in the service metadata.</span></span> <span data-ttu-id="36a01-104">Na przykład poniższy kod konfiguracyjny używa elementu [\<wsHttpBinding >](../../configure-apps/file-schema/wcf/wshttpbinding.md) i ustawia atrybut `clientCredentialType` dla systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="36a01-104">For example, the following configuration code uses the [\<wsHttpBinding>](../../configure-apps/file-schema/wcf/wshttpbinding.md) element and sets the `clientCredentialType` attribute to Windows.</span></span>  

 <span data-ttu-id="36a01-105">Poniższy fragment Web Services Description Language (WSDL) przedstawia tożsamość punktu końcowego zdefiniowanego wcześniej.</span><span class="sxs-lookup"><span data-stu-id="36a01-105">The following Web Services Description Language (WSDL) fragment shows the identity for the endpoint previously defined.</span></span> <span data-ttu-id="36a01-106">W tym przykładzie usługa działa jako usługa samodzielna w ramach określonego konta użytkownika (username@contoso.com), w związku z czym tożsamość głównej nazwy użytkownika (UPN) zawiera nazwę konta.</span><span class="sxs-lookup"><span data-stu-id="36a01-106">In this example, the service is running as a self-hosted service under a particular user account (username@contoso.com) and therefore the user principal name (UPN) identity contains the account name.</span></span> <span data-ttu-id="36a01-107">Nazwa UPN jest również nazywana nazwą logowania użytkownika w domenie systemu Windows.</span><span class="sxs-lookup"><span data-stu-id="36a01-107">The UPN is also known as the user logon name in a Windows domain.</span></span>  

 <span data-ttu-id="36a01-108">Aby zapoznać się z przykładową aplikacją, która demonstruje ustawienie tożsamości, zobacz [przykład Identity Service](../samples/service-identity-sample.md).</span><span class="sxs-lookup"><span data-stu-id="36a01-108">For a sample application that demonstrates identity setting, see [Service Identity Sample](../samples/service-identity-sample.md).</span></span> <span data-ttu-id="36a01-109">Aby uzyskać więcej informacji na temat tożsamości usługi, zobacz [tożsamość usługi i uwierzytelnianie](../feature-details/service-identity-and-authentication.md).</span><span class="sxs-lookup"><span data-stu-id="36a01-109">For more information about service identity, see [Service Identity and Authentication](../feature-details/service-identity-and-authentication.md).</span></span>  
  
## <a name="kerberos-authentication-and-identity"></a><span data-ttu-id="36a01-110">Uwierzytelnianie i tożsamość Kerberos</span><span class="sxs-lookup"><span data-stu-id="36a01-110">Kerberos Authentication and Identity</span></span>  
 <span data-ttu-id="36a01-111">Domyślnie, gdy usługa jest skonfigurowana do korzystania z poświadczeń systemu Windows, element [\<identity >](../../configure-apps/file-schema/wcf/identity.md) zawierający [\<userPrincipalName >](../../configure-apps/file-schema/wcf/userprincipalname.md) lub [\<elementu ServicePrincipalName >](../../configure-apps/file-schema/wcf/serviceprincipalname.md) jest generowany w języku WSDL.</span><span class="sxs-lookup"><span data-stu-id="36a01-111">By default, when a service is configured to use a Windows credential, an [\<identity>](../../configure-apps/file-schema/wcf/identity.md) element that contains a [\<userPrincipalName>](../../configure-apps/file-schema/wcf/userprincipalname.md) or [\<servicePrincipalName>](../../configure-apps/file-schema/wcf/serviceprincipalname.md) element is generated in the WSDL.</span></span> <span data-ttu-id="36a01-112">Jeśli usługa jest uruchomiona w ramach konta `LocalSystem`, `LocalService`lub `NetworkService`, nazwa główna usługi (SPN) jest generowana domyślnie w postaci `host/`\<*hostname*>, ponieważ te konta mają dostęp do danych SPN komputera.</span><span class="sxs-lookup"><span data-stu-id="36a01-112">If the service is running under the `LocalSystem`, `LocalService`, or `NetworkService` account, a service principal name (SPN) is generated by default in the form of `host/`\<*hostname*> because those accounts have access to the computer's SPN data.</span></span> <span data-ttu-id="36a01-113">Jeśli usługa jest uruchomiona przy użyciu innego konta, Windows Communication Foundation (WCF) generuje nazwę UPN w postaci \<*username*>@<*nazwa_domeny*`>`.</span><span class="sxs-lookup"><span data-stu-id="36a01-113">If the service is running under a different account, Windows Communication Foundation (WCF) generates a UPN in the form of \<*username*>@<*domainName*`>`.</span></span> <span data-ttu-id="36a01-114">Dzieje się tak, ponieważ uwierzytelnianie Kerberos wymaga, aby nazwa UPN lub nazwa SPN była dostarczana do klienta w celu uwierzytelnienia usługi.</span><span class="sxs-lookup"><span data-stu-id="36a01-114">This occurs because Kerberos authentication requires that a UPN or SPN be supplied to the client to authenticate the service.</span></span>  
  
 <span data-ttu-id="36a01-115">Można również użyć narzędzia Setspn. exe do zarejestrowania dodatkowej nazwy SPN z kontem usługi w domenie.</span><span class="sxs-lookup"><span data-stu-id="36a01-115">You can also use the Setspn.exe tool to register an additional SPN with a service's account in a domain.</span></span> <span data-ttu-id="36a01-116">Następnie można użyć nazwy SPN jako tożsamości usługi.</span><span class="sxs-lookup"><span data-stu-id="36a01-116">You can then use the SPN as the identity of the service.</span></span> <span data-ttu-id="36a01-117">Aby pobrać narzędzie, zobacz [Windows 2000 Resource Kit Tool: Setspn. exe](https://go.microsoft.com/fwlink/?LinkId=91752).</span><span class="sxs-lookup"><span data-stu-id="36a01-117">To download the tool, see [Windows 2000 Resource Kit Tool : Setspn.exe](https://go.microsoft.com/fwlink/?LinkId=91752).</span></span> <span data-ttu-id="36a01-118">Więcej informacji o narzędziu można znaleźć w temacie [Setspn Overview](https://docs.microsoft.com/previous-versions/windows/it-pro/windows-server-2003/cc773257(v=ws.10)).</span><span class="sxs-lookup"><span data-stu-id="36a01-118">For more information about the tool, see [Setspn Overview](https://docs.microsoft.com/previous-versions/windows/it-pro/windows-server-2003/cc773257(v=ws.10)).</span></span>  
  
> [!NOTE]
> <span data-ttu-id="36a01-119">Aby użyć typu poświadczeń systemu Windows bez negocjowania, konto użytkownika usługi musi mieć dostęp do nazwy SPN, która jest zarejestrowana w domenie Active Directory.</span><span class="sxs-lookup"><span data-stu-id="36a01-119">To use the Windows credential type without negotiation, the service's user account must have access to the SPN that is registered with the Active Directory domain.</span></span> <span data-ttu-id="36a01-120">Można to zrobić w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="36a01-120">You can do this in the following ways:</span></span>  
  
- <span data-ttu-id="36a01-121">Użyj konta NetworkService lub LocalSystem do uruchomienia usługi.</span><span class="sxs-lookup"><span data-stu-id="36a01-121">Use the NetworkService or LocalSystem account to run your service.</span></span> <span data-ttu-id="36a01-122">Ponieważ te konta mają dostęp do nazwy SPN maszyny, która została ustanowiona, gdy komputer jest przyłączony do domeny Active Directory, funkcja WCF automatycznie generuje prawidłowy element SPN w ramach punktu końcowego usługi w metadanych usługi (WSDL).</span><span class="sxs-lookup"><span data-stu-id="36a01-122">Because those accounts have access to the machine SPN that is established when the machine joins the Active Directory domain, WCF automatically generates the proper SPN element inside the service's endpoint in the service's metadata (WSDL).</span></span>  
  
- <span data-ttu-id="36a01-123">Użyj dowolnego konta domeny Active Directory, aby uruchomić usługę.</span><span class="sxs-lookup"><span data-stu-id="36a01-123">Use an arbitrary Active Directory domain account to run your service.</span></span> <span data-ttu-id="36a01-124">W takim przypadku należy ustanowić nazwę SPN dla tego konta domeny, którą można wykonać za pomocą narzędzia narzędzia Setspn. exe.</span><span class="sxs-lookup"><span data-stu-id="36a01-124">In this case, establish an SPN for that domain account, which you can do by using the Setspn.exe utility tool.</span></span> <span data-ttu-id="36a01-125">Po utworzeniu nazwy SPN dla konta usługi Skonfiguruj program WCF do publikowania tej nazwy SPN na klientach usługi za pomocą metadanych (WSDL).</span><span class="sxs-lookup"><span data-stu-id="36a01-125">Once you create the SPN for the service's account, configure WCF to publish that SPN to the service's clients through its metadata (WSDL).</span></span> <span data-ttu-id="36a01-126">W tym celu należy ustawić tożsamość punktu końcowego dla uwidocznionego punktu końcowego za pomocą pliku lub kodu konfiguracji aplikacji.</span><span class="sxs-lookup"><span data-stu-id="36a01-126">This is done by setting the endpoint identity for the exposed endpoint, either through an application configuration file or code.</span></span>  
  
 <span data-ttu-id="36a01-127">Aby uzyskać więcej informacji na temat nazw SPN, protokołu Kerberos i Active Directory, zobacz [dodatek dotyczący protokołu Kerberos dla systemu Windows](https://docs.microsoft.com/previous-versions/msp-n-p/ff649429(v=pandp.10)).</span><span class="sxs-lookup"><span data-stu-id="36a01-127">For more information about SPNs, the Kerberos protocol, and Active Directory, see [Kerberos Technical Supplement for Windows](https://docs.microsoft.com/previous-versions/msp-n-p/ff649429(v=pandp.10)).</span></span>  
  
### <a name="when-spn-or-upn-equals-the-empty-string"></a><span data-ttu-id="36a01-128">Gdy nazwa SPN lub nazwa UPN jest równa pustemu ciągowi</span><span class="sxs-lookup"><span data-stu-id="36a01-128">When SPN or UPN Equals the Empty String</span></span>  
 <span data-ttu-id="36a01-129">Jeśli ustawisz nazwę SPN lub nazwę UPN równą pustemu ciągowi, następuje wiele różnych rzeczy, w zależności od poziomu zabezpieczeń i używanego trybu uwierzytelniania:</span><span class="sxs-lookup"><span data-stu-id="36a01-129">If you set the SPN or UPN equal to an empty string, a number of different things happen, depending on the security level and authentication mode being used:</span></span>  
  
- <span data-ttu-id="36a01-130">W przypadku korzystania z zabezpieczeń na poziomie transportu jest wybierane uwierzytelnianie NT LanMan (NTLM).</span><span class="sxs-lookup"><span data-stu-id="36a01-130">If you are using transport level security, NT LanMan (NTLM) authentication is chosen.</span></span>  
  
- <span data-ttu-id="36a01-131">W przypadku korzystania z zabezpieczeń na poziomie komunikatów uwierzytelnianie może zakończyć się niepowodzeniem, w zależności od trybu uwierzytelniania:</span><span class="sxs-lookup"><span data-stu-id="36a01-131">If you are using message level security, authentication may fail, depending on the authentication mode:</span></span>  
  
- <span data-ttu-id="36a01-132">Jeśli używasz trybu `spnego`, a atrybut `AllowNtlm` jest ustawiony na `false`, uwierzytelnianie nie powiedzie się.</span><span class="sxs-lookup"><span data-stu-id="36a01-132">If you are using `spnego` mode and the `AllowNtlm` attribute is set to `false`, authentication fail.</span></span>  
  
- <span data-ttu-id="36a01-133">Jeśli używasz trybu `spnego`, a atrybut `AllowNtlm` jest ustawiony na `true`, uwierzytelnianie kończy się niepowodzeniem, jeśli nazwa UPN jest pusta, ale zostanie zakończona, gdy nazwa SPN jest pusta.</span><span class="sxs-lookup"><span data-stu-id="36a01-133">If you are using `spnego` mode and the `AllowNtlm` attribute is set to `true`, authentication fails if the UPN is empty, but succeeds if the SPN is empty.</span></span>  
  
- <span data-ttu-id="36a01-134">W przypadku korzystania z protokołu Kerberos Direct (znanego również jako "z pojedynczym zrzutem") uwierzytelnianie kończy się niepowodzeniem.</span><span class="sxs-lookup"><span data-stu-id="36a01-134">If you are using Kerberos direct (also known as "one-shot"), authentication fails.</span></span>  
  
### <a name="using-the-identity-element-in-configuration"></a><span data-ttu-id="36a01-135">Używanie elementu \<Identity > w konfiguracji</span><span class="sxs-lookup"><span data-stu-id="36a01-135">Using the \<identity> Element in Configuration</span></span>  
 <span data-ttu-id="36a01-136">Jeśli zmienisz typ poświadczeń klienta w powiązaniu wcześniej pokazanym na certyfikacie`,`, wygenerowany kod WSDL zawiera certyfikat X. 509 z serializowanym algorytmem Base64 dla wartości tożsamości, jak pokazano w poniższym kodzie.</span><span class="sxs-lookup"><span data-stu-id="36a01-136">If you change the client credential type in the binding previously shown to Certificate`,` then the generated WSDL contains a Base64 serialized X.509 certificate for the identity value as shown in the following code.</span></span> <span data-ttu-id="36a01-137">Jest to wartość domyślna dla wszystkich typów poświadczeń klienta innych niż Windows.</span><span class="sxs-lookup"><span data-stu-id="36a01-137">This is the default for all client credential types other than Windows.</span></span>  

 <span data-ttu-id="36a01-138">Można zmienić wartość domyślnej tożsamości usługi lub zmienić typ tożsamości za pomocą <`identity`> elementu w konfiguracji lub przez ustawienie tożsamości w kodzie.</span><span class="sxs-lookup"><span data-stu-id="36a01-138">You can change the value of the default service identity or change the type of the identity by using the <`identity`> element in configuration or by setting the identity in code.</span></span> <span data-ttu-id="36a01-139">Poniższy kod konfiguracji ustawia tożsamość systemu nazw domen (DNS) z wartością `contoso.com`.</span><span class="sxs-lookup"><span data-stu-id="36a01-139">The following configuration code sets a domain name system (DNS) identity with the value `contoso.com`.</span></span>  

### <a name="setting-identity-programmatically"></a><span data-ttu-id="36a01-140">Programowo Ustawianie tożsamości</span><span class="sxs-lookup"><span data-stu-id="36a01-140">Setting Identity Programmatically</span></span>  
 <span data-ttu-id="36a01-141">Usługa nie musi jawnie określać tożsamości, ponieważ funkcja WCF automatycznie ją określa.</span><span class="sxs-lookup"><span data-stu-id="36a01-141">Your service does not have to explicitly specify an identity, because WCF automatically determines it.</span></span> <span data-ttu-id="36a01-142">Jednak funkcja WCF umożliwia określenie tożsamości w punkcie końcowym, jeśli jest to wymagane.</span><span class="sxs-lookup"><span data-stu-id="36a01-142">However, WCF allows you to specify an identity on an endpoint, if required.</span></span> <span data-ttu-id="36a01-143">Poniższy kod dodaje nowy punkt końcowy usługi z określoną tożsamością DNS.</span><span class="sxs-lookup"><span data-stu-id="36a01-143">The following code adds a new service endpoint with a specific DNS identity.</span></span>  
  
 [!code-csharp[C_Identity#5](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_identity/cs/source.cs#5)]
 [!code-vb[C_Identity#5](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_identity/vb/source.vb#5)]  
  
## <a name="see-also"></a><span data-ttu-id="36a01-144">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="36a01-144">See also</span></span>

- [<span data-ttu-id="36a01-145">Instrukcje: tworzenie niestandardowego weryfikatora tożsamości klienta</span><span class="sxs-lookup"><span data-stu-id="36a01-145">How to: Create a Custom Client Identity Verifier</span></span>](how-to-create-a-custom-client-identity-verifier.md)
- [<span data-ttu-id="36a01-146">Uwierzytelnianie i tożsamość usług</span><span class="sxs-lookup"><span data-stu-id="36a01-146">Service Identity and Authentication</span></span>](../feature-details/service-identity-and-authentication.md)
