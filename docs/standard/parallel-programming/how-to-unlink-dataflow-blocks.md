---
title: 'Porady: Rozłączanie bloków przepływu danych'
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- dataflow blocks, unlinking in TPL
- Task Parallel Library, dataflows
- TPL dataflow library, unlinking dataflow blocks
ms.assetid: 40f0208d-4618-47f7-85cf-4913d07d2d7d
ms.openlocfilehash: b49cfc9730ba154202baf15093a54ba3ce0e2a8a
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/15/2020
ms.locfileid: "73139294"
---
# <a name="how-to-unlink-dataflow-blocks"></a><span data-ttu-id="d8d6a-102">Porady: Rozłączanie bloków przepływu danych</span><span class="sxs-lookup"><span data-stu-id="d8d6a-102">How to: Unlink Dataflow Blocks</span></span>
<span data-ttu-id="d8d6a-103">W tym dokumencie opisano sposób odłączenia docelowego bloku przepływu danych od źródła.</span><span class="sxs-lookup"><span data-stu-id="d8d6a-103">This document describes how to unlink a target dataflow block from its source.</span></span>

[!INCLUDE [tpl-install-instructions](../../../includes/tpl-install-instructions.md)]

## <a name="example"></a><span data-ttu-id="d8d6a-104">Przykład</span><span class="sxs-lookup"><span data-stu-id="d8d6a-104">Example</span></span>  
 <span data-ttu-id="d8d6a-105">Poniższy przykład tworzy <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> trzy obiekty, z `TrySolution` których każdy wywołuje metodę do obliczenia wartości.</span><span class="sxs-lookup"><span data-stu-id="d8d6a-105">The following example creates three <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> objects, each of which calls the `TrySolution` method to compute a value.</span></span> <span data-ttu-id="d8d6a-106">W tym przykładzie wymaga tylko wynik `TrySolution` z pierwszego wywołania do zakończenia.</span><span class="sxs-lookup"><span data-stu-id="d8d6a-106">This example requires only the result from the first call to `TrySolution` to finish.</span></span>  
  
 [!code-csharp[TPLDataflow_ReceiveAny#1](../../../samples/snippets/csharp/VS_Snippets_Misc/tpldataflow_receiveany/cs/dataflowreceiveany.cs#1)]
 [!code-vb[TPLDataflow_ReceiveAny#1](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpldataflow_receiveany/vb/dataflowreceiveany.vb#1)]  
  
 <span data-ttu-id="d8d6a-107">Aby otrzymać wartość z <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> pierwszego obiektu, który kończy, `ReceiveFromAny(T)` w tym przykładzie definiuje metodę.</span><span class="sxs-lookup"><span data-stu-id="d8d6a-107">To receive the value from the first <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> object that finishes, this example defines the `ReceiveFromAny(T)` method.</span></span> <span data-ttu-id="d8d6a-108">Metoda `ReceiveFromAny(T)` akceptuje tablicę <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601> obiektów i łączy każdy z <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> tych obiektów do obiektu.</span><span class="sxs-lookup"><span data-stu-id="d8d6a-108">The `ReceiveFromAny(T)` method accepts an array of <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601> objects and links each of these objects to a <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object.</span></span> <span data-ttu-id="d8d6a-109">Korzystając z <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> metody, aby połączyć blok przepływu danych źródłowych do bloku docelowego, źródło propaguje wiadomości do obiektu docelowego, gdy dane stają się dostępne.</span><span class="sxs-lookup"><span data-stu-id="d8d6a-109">When you use the <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> method to link a source dataflow block to a target block, the source propagates messages to the target as data becomes available.</span></span> <span data-ttu-id="d8d6a-110">Ponieważ <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> klasa akceptuje tylko pierwszą wiadomość, która `ReceiveFromAny(T)` jest oferowana, <xref:System.Threading.Tasks.Dataflow.DataflowBlock.Receive%2A> metoda daje jej wynik, wywołując metodę.</span><span class="sxs-lookup"><span data-stu-id="d8d6a-110">Because the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> class accepts only the first message that it is offered, the `ReceiveFromAny(T)` method produces its result by calling the <xref:System.Threading.Tasks.Dataflow.DataflowBlock.Receive%2A> method.</span></span> <span data-ttu-id="d8d6a-111">Spowoduje to wygenerowanie pierwszej wiadomości, która jest oferowana do <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> obiektu.</span><span class="sxs-lookup"><span data-stu-id="d8d6a-111">This produces the first message that is offered to the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object.</span></span> <span data-ttu-id="d8d6a-112">Metoda <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> ma przeciążoną wersję, <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions> która przyjmuje <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions.MaxMessages> obiekt z właściwością, `1`która, gdy jest ustawiona na , instruuje blok źródłowy, aby odłączyć od obiektu docelowego po obiekt docelowy odbiera jeden komunikat ze źródła.</span><span class="sxs-lookup"><span data-stu-id="d8d6a-112">The <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> method has an overloaded version that takes an <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions> object with a <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions.MaxMessages> property that, when it is set to `1`, instructs the source block to unlink from the target after the target receives one message from the source.</span></span> <span data-ttu-id="d8d6a-113">Ważne jest, <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> aby obiekt odłączał się od jego źródeł, <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> ponieważ relacja między tablicą <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> źródeł a obiektem nie jest już wymagana po odebraniu wiadomości przez obiekt.</span><span class="sxs-lookup"><span data-stu-id="d8d6a-113">It is important for the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object to unlink from its sources because the relationship between the array of sources and the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object is no longer required after the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object receives a message.</span></span>  
  
 <span data-ttu-id="d8d6a-114">Aby włączyć pozostałe `TrySolution` wywołania do końca po jednym z `TrySolution` nich <xref:System.Threading.CancellationToken> oblicza wartość, metoda przyjmuje `ReceiveFromAny(T)` obiekt, który jest anulowany po wywołaniu zwraca.</span><span class="sxs-lookup"><span data-stu-id="d8d6a-114">To enable the remaining calls to `TrySolution` to end after one of them computes a value, the `TrySolution` method takes a <xref:System.Threading.CancellationToken> object that is canceled after the call to `ReceiveFromAny(T)` returns.</span></span> <span data-ttu-id="d8d6a-115">Metoda <xref:System.Threading.SpinWait.SpinUntil%2A> zwraca, <xref:System.Threading.CancellationToken> gdy ten obiekt jest anulowany.</span><span class="sxs-lookup"><span data-stu-id="d8d6a-115">The <xref:System.Threading.SpinWait.SpinUntil%2A> method returns when this <xref:System.Threading.CancellationToken> object is canceled.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="d8d6a-116">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="d8d6a-116">See also</span></span>

- [<span data-ttu-id="d8d6a-117">Przepływ danych</span><span class="sxs-lookup"><span data-stu-id="d8d6a-117">Dataflow</span></span>](../../../docs/standard/parallel-programming/dataflow-task-parallel-library.md)
