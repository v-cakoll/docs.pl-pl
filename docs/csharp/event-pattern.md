---
title: Standardowe wzorce zdarzeń platformy .NET
description: Dowiedz się więcej o wzorcach zdarzeń .NET i sposobach tworzenia standardowych źródeł zdarzeń oraz subskrybowania i przetwarzania standardowych zdarzeń w kodzie.
ms.date: 06/20/2016
ms.technology: csharp-fundamentals
ms.assetid: 8a3133d6-4ef2-46f9-9c8d-a8ea8898e4c9
ms.openlocfilehash: 517e46ffec163a9bd49baa58fc0b37b54b2b2809
ms.sourcegitcommit: 43d10ef65f0f1fd6c3b515e363bde11a3fcd8d6d
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/03/2020
ms.locfileid: "78239862"
---
# <a name="standard-net-event-patterns"></a><span data-ttu-id="2534c-103">Standardowe wzorce zdarzeń platformy .NET</span><span class="sxs-lookup"><span data-stu-id="2534c-103">Standard .NET event patterns</span></span>

[<span data-ttu-id="2534c-104">Wstecz</span><span class="sxs-lookup"><span data-stu-id="2534c-104">Previous</span></span>](events-overview.md)

<span data-ttu-id="2534c-105">Zdarzenia platformy .NET zwykle są zgodne z kilkoma znanymi wzorcami.</span><span class="sxs-lookup"><span data-stu-id="2534c-105">.NET events generally follow a few known patterns.</span></span> <span data-ttu-id="2534c-106">Ujednolicenie tych wzorców oznacza, że deweloperzy mogą korzystać z wiedzy o standardowych wzorcach, które można zastosować do dowolnego programu zdarzeń platformy .NET.</span><span class="sxs-lookup"><span data-stu-id="2534c-106">Standardizing on these patterns means that developers can leverage knowledge of those standard patterns, which can be applied to any .NET event program.</span></span>

<span data-ttu-id="2534c-107">Przyjrzyjmy się tym standardowym wzorcem, dzięki czemu będziesz mieć wszystkie informacje potrzebne do tworzenia standardowych źródeł zdarzeń oraz subskrybować i przetwarzać standardowe zdarzenia w kodzie.</span><span class="sxs-lookup"><span data-stu-id="2534c-107">Let's go through these standard patterns so you will have all the knowledge you need to create standard event sources, and subscribe and process standard events in your code.</span></span>

## <a name="event-delegate-signatures"></a><span data-ttu-id="2534c-108">Sygnatury delegatów zdarzeń</span><span class="sxs-lookup"><span data-stu-id="2534c-108">Event Delegate Signatures</span></span>

<span data-ttu-id="2534c-109">Podpis standardowy dla delegata zdarzenia .NET to:</span><span class="sxs-lookup"><span data-stu-id="2534c-109">The standard signature for a .NET event delegate is:</span></span>

```csharp
void OnEventRaised(object sender, EventArgs args);
```

<span data-ttu-id="2534c-110">Zwracany typ to void.</span><span class="sxs-lookup"><span data-stu-id="2534c-110">The return type is void.</span></span> <span data-ttu-id="2534c-111">Zdarzenia są oparte na delegatach i są delegatami multiemisji.</span><span class="sxs-lookup"><span data-stu-id="2534c-111">Events are based on delegates and are multicast delegates.</span></span> <span data-ttu-id="2534c-112">Obsługuje wielu subskrybentów dla każdego źródła zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="2534c-112">That supports multiple subscribers for any event source.</span></span> <span data-ttu-id="2534c-113">Pojedyncza wartość zwracana z metody nie jest skalowana do wielu subskrybentów zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="2534c-113">The single return value from a method doesn't scale to multiple event subscribers.</span></span> <span data-ttu-id="2534c-114">Która wartość zwracana jest wyświetlana przez Źródło zdarzenia po podwyższeniu poziomu zdarzenia?</span><span class="sxs-lookup"><span data-stu-id="2534c-114">Which return value does the event source see after raising an event?</span></span> <span data-ttu-id="2534c-115">W dalszej części tego artykułu zobaczysz, jak utworzyć protokoły zdarzeń obsługujące Subskrybenci zdarzeń, które zgłaszają informacje do źródła zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="2534c-115">Later in this article you'll see how to create event protocols that support event subscribers that report information to the event source.</span></span>

<span data-ttu-id="2534c-116">Lista argumentów zawiera dwa argumenty: nadawca i argumenty zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="2534c-116">The argument list contains two arguments: the sender, and the event arguments.</span></span> <span data-ttu-id="2534c-117">Typ czasu kompilacji `sender` jest `System.Object`, nawet jeśli prawdopodobnie znasz bardziej pochodny typ, który zawsze będzie prawidłowy.</span><span class="sxs-lookup"><span data-stu-id="2534c-117">The compile time type of `sender` is `System.Object`, even though you likely know a more derived type that would always be correct.</span></span> <span data-ttu-id="2534c-118">Zgodnie z Konwencją Użyj `object`.</span><span class="sxs-lookup"><span data-stu-id="2534c-118">By convention, use `object`.</span></span>

<span data-ttu-id="2534c-119">Drugi argument jest zazwyczaj typem, który jest pochodną `System.EventArgs`.</span><span class="sxs-lookup"><span data-stu-id="2534c-119">The second argument has typically been a type that is derived from `System.EventArgs`.</span></span> <span data-ttu-id="2534c-120">(Zobaczysz w [następnej sekcji](modern-events.md) , że ta konwencja nie jest już wymuszana). Jeśli typ zdarzenia nie wymaga żadnych dodatkowych argumentów, nadal będą dostępne oba argumenty.</span><span class="sxs-lookup"><span data-stu-id="2534c-120">(You'll see in the [next section](modern-events.md) that this convention is no longer enforced.) If your event type does not need any additional arguments, you will still provide both arguments.</span></span>
<span data-ttu-id="2534c-121">Istnieje specjalna wartość `EventArgs.Empty`, która powinna być używana do określenia, że zdarzenie nie zawiera żadnych dodatkowych informacji.</span><span class="sxs-lookup"><span data-stu-id="2534c-121">There is a special value, `EventArgs.Empty` that you should use to denote that your event does not contain any additional information.</span></span>

<span data-ttu-id="2534c-122">Utwórzmy klasę, która wyświetla listę plików w katalogu lub dowolnego podkatalogów, które są zgodne ze wzorcem.</span><span class="sxs-lookup"><span data-stu-id="2534c-122">Let's build a class that lists files in a directory, or any of its subdirectories that follow a pattern.</span></span> <span data-ttu-id="2534c-123">Ten składnik zgłasza zdarzenie dla każdego znalezionego pliku, który pasuje do wzorca.</span><span class="sxs-lookup"><span data-stu-id="2534c-123">This component raises an event for each file found that matches the pattern.</span></span>

<span data-ttu-id="2534c-124">Korzystanie z modelu zdarzeń zapewnia pewne zalety projektowania.</span><span class="sxs-lookup"><span data-stu-id="2534c-124">Using an event model provides some design advantages.</span></span> <span data-ttu-id="2534c-125">Można utworzyć wiele detektorów zdarzeń, które wykonują różne akcje po znalezieniu szukanego pliku.</span><span class="sxs-lookup"><span data-stu-id="2534c-125">You can create multiple event listeners that perform different actions when a sought file is found.</span></span> <span data-ttu-id="2534c-126">Łącząc różne detektory, można tworzyć bardziej niezawodne algorytmy.</span><span class="sxs-lookup"><span data-stu-id="2534c-126">Combining the different listeners can create more robust algorithms.</span></span>

<span data-ttu-id="2534c-127">Oto początkowa deklaracja argumentu zdarzenia do znajdowania szukanego pliku:</span><span class="sxs-lookup"><span data-stu-id="2534c-127">Here is the initial event argument declaration for finding a sought file:</span></span> 

[!code-csharp[EventArgs](../../samples/snippets/csharp/events/Program.cs#EventArgsV1 "Define event arguments")]

<span data-ttu-id="2534c-128">Mimo że ten typ wygląda jak mały typ danych, należy przestrzegać Konwencji i wprowadzić jako typ odwołania (`class`).</span><span class="sxs-lookup"><span data-stu-id="2534c-128">Even though this type looks like a small, data-only type, you should follow the convention and make it a reference (`class`) type.</span></span> <span data-ttu-id="2534c-129">Oznacza to, że obiekt argumentu zostanie przesłany przez odwołanie, a wszystkie aktualizacje danych będą widoczne dla wszystkich subskrybentów.</span><span class="sxs-lookup"><span data-stu-id="2534c-129">That means the argument object will be passed by reference, and any updates to the data will be viewed by all subscribers.</span></span> <span data-ttu-id="2534c-130">Pierwsza wersja jest niezmiennym obiektem.</span><span class="sxs-lookup"><span data-stu-id="2534c-130">The first version is an immutable object.</span></span> <span data-ttu-id="2534c-131">Należy preferować, aby właściwości w typie argumentu zdarzenia były niezmienne.</span><span class="sxs-lookup"><span data-stu-id="2534c-131">You should prefer to make the properties in your event argument type immutable.</span></span> <span data-ttu-id="2534c-132">Dzięki temu jeden subskrybent nie może zmienić wartości przed ich wyświetleniem przez innego subskrybenta.</span><span class="sxs-lookup"><span data-stu-id="2534c-132">That way, one subscriber cannot change the values before another subscriber sees them.</span></span> <span data-ttu-id="2534c-133">(Istnieją wyjątki dotyczące tego, jak pokazano poniżej).</span><span class="sxs-lookup"><span data-stu-id="2534c-133">(There are exceptions to this, as you'll see below.)</span></span>  

<span data-ttu-id="2534c-134">Następnie musimy utworzyć deklarację zdarzenia w klasie FileSearcher.</span><span class="sxs-lookup"><span data-stu-id="2534c-134">Next, we need to create the event declaration in the FileSearcher class.</span></span> <span data-ttu-id="2534c-135">Korzystanie z typu `EventHandler<T>` oznacza, że nie trzeba tworzyć jeszcze innej definicji typu.</span><span class="sxs-lookup"><span data-stu-id="2534c-135">Leveraging the `EventHandler<T>` type means that you don't need to create yet another type definition.</span></span> <span data-ttu-id="2534c-136">Wystarczy użyć ogólnej specjalizacji.</span><span class="sxs-lookup"><span data-stu-id="2534c-136">You simply use a generic specialization.</span></span>

<span data-ttu-id="2534c-137">Wypełnijmy klasę FileSearcher, aby wyszukać pliki pasujące do wzorca, i podnieść poprawne zdarzenie, gdy zostanie odnalezione dopasowanie.</span><span class="sxs-lookup"><span data-stu-id="2534c-137">Let's fill out the FileSearcher class to search for files that match a pattern, and raise the correct event when a match is discovered.</span></span>

[!code-csharp[FileSearcher](../../samples/snippets/csharp/events/Program.cs#FileSearcherV1 "Create the initial file searcher")]

## <a name="defining-and-raising-field-like-events"></a><span data-ttu-id="2534c-138">Definiowanie i podnoszenie zdarzeń przypominających pola</span><span class="sxs-lookup"><span data-stu-id="2534c-138">Defining and Raising Field-Like Events</span></span>

<span data-ttu-id="2534c-139">Najprostszym sposobem dodania zdarzenia do klasy jest zadeklarowanie tego zdarzenia jako pola publicznego, jak w poprzednim przykładzie:</span><span class="sxs-lookup"><span data-stu-id="2534c-139">The simplest way to add an event to your class is to declare that event as a public field, as in the preceding example:</span></span>

[!code-csharp[DeclareEvent](../../samples/snippets/csharp/events/Program.cs#DeclareEvent "Declare the file found event")]

<span data-ttu-id="2534c-140">Wygląda na to, że deklaruje pole publiczne, które wydaje się być niewłaściwym sposobem postępowania zorientowanego obiektowo.</span><span class="sxs-lookup"><span data-stu-id="2534c-140">This looks like it's declaring a public field, which would appear to be bad object-oriented practice.</span></span> <span data-ttu-id="2534c-141">Chcesz chronić dostęp do danych za poorednictwem właściwości lub metod.</span><span class="sxs-lookup"><span data-stu-id="2534c-141">You want to protect data access through properties, or methods.</span></span> <span data-ttu-id="2534c-142">Chociaż może to wyglądać jak niewłaściwe rozwiązanie, kod wygenerowany przez kompilator tworzy otoki, tak aby obiekty zdarzeń były dostępne tylko w bezpieczny sposób.</span><span class="sxs-lookup"><span data-stu-id="2534c-142">While this may look like a bad practice, the code generated by the compiler does create wrappers so that the event objects can only be accessed in safe ways.</span></span> <span data-ttu-id="2534c-143">Jedyne operacje dostępne na zdarzeniu podobnym do pola to procedura obsługi dodawania:</span><span class="sxs-lookup"><span data-stu-id="2534c-143">The only operations available on a field-like event are add handler:</span></span>

[!code-csharp[DeclareEventHandler](../../samples/snippets/csharp/events/Program.cs#DeclareEventHandler "Declare the file found event handler")]

<span data-ttu-id="2534c-144">i Usuń procedurę obsługi:</span><span class="sxs-lookup"><span data-stu-id="2534c-144">and remove handler:</span></span>

[!code-csharp[RemoveEventHandler](../../samples/snippets/csharp/events/Program.cs#RemoveHandler "Remove the event handler")]

<span data-ttu-id="2534c-145">Należy zauważyć, że dla programu obsługi istnieje zmienna lokalna.</span><span class="sxs-lookup"><span data-stu-id="2534c-145">Note that there's a local variable for the handler.</span></span> <span data-ttu-id="2534c-146">Jeśli użyto treści wyrażenia lambda, usuwanie nie będzie działało poprawnie.</span><span class="sxs-lookup"><span data-stu-id="2534c-146">If you used the body of the lambda, the remove would not work correctly.</span></span> <span data-ttu-id="2534c-147">Będzie ono innym wystąpieniem delegata i w trybie dyskretnym nic nie rób.</span><span class="sxs-lookup"><span data-stu-id="2534c-147">It would be a different instance of the delegate, and silently do nothing.</span></span>

<span data-ttu-id="2534c-148">Kod poza klasą nie może zgłosić zdarzenia, ani nie może wykonać żadnych innych operacji.</span><span class="sxs-lookup"><span data-stu-id="2534c-148">Code outside the class cannot raise the event, nor can it perform any other operations.</span></span>

## <a name="returning-values-from-event-subscribers"></a><span data-ttu-id="2534c-149">Zwracanie wartości z subskrybentów zdarzeń</span><span class="sxs-lookup"><span data-stu-id="2534c-149">Returning Values from Event Subscribers</span></span>

<span data-ttu-id="2534c-150">Twoja wersja prosta działa prawidłowo.</span><span class="sxs-lookup"><span data-stu-id="2534c-150">Your simple version is working fine.</span></span> <span data-ttu-id="2534c-151">Dodajmy kolejną funkcję: anulowanie.</span><span class="sxs-lookup"><span data-stu-id="2534c-151">Let's add another feature: Cancellation.</span></span>

<span data-ttu-id="2534c-152">Po podniesieniu wykrytego zdarzenia odbiorniki powinny być w stanie przerwać dalsze przetwarzanie, jeśli ten plik jest ostatnim z nich.</span><span class="sxs-lookup"><span data-stu-id="2534c-152">When you raise the found event, listeners should be able to stop further processing, if this file is that last one sought.</span></span>

<span data-ttu-id="2534c-153">Programy obsługi zdarzeń nie zwracają wartości, dlatego należy komunikować się w inny sposób.</span><span class="sxs-lookup"><span data-stu-id="2534c-153">The event handlers do not return a value, so you need to communicate that in another way.</span></span> <span data-ttu-id="2534c-154">Standardowy wzorzec zdarzeń używa obiektu EventArgs do dołączania pól, których Subskrybenci mogą używać do komunikowania się z anulowaniem.</span><span class="sxs-lookup"><span data-stu-id="2534c-154">The standard event pattern uses the EventArgs object to include fields that event subscribers can use to communicate cancel.</span></span>

<span data-ttu-id="2534c-155">Istnieją dwa różne wzorce, których można użyć na podstawie semantyki kontraktu anulowania.</span><span class="sxs-lookup"><span data-stu-id="2534c-155">There are two different patterns that could be used, based on the semantics of the Cancel contract.</span></span> <span data-ttu-id="2534c-156">W obu przypadkach należy dodać pole Boolean do EventArguments dla znalezionego zdarzenia pliku.</span><span class="sxs-lookup"><span data-stu-id="2534c-156">In both cases, you'll add a boolean field to the EventArguments for the found file event.</span></span> 

<span data-ttu-id="2534c-157">Jeden wzorzec zezwoli jednemu subskrybentowi na anulowanie operacji.</span><span class="sxs-lookup"><span data-stu-id="2534c-157">One pattern would allow any one subscriber to cancel the operation.</span></span>
<span data-ttu-id="2534c-158">Dla tego wzorca nowe pole jest inicjowane w celu `false`.</span><span class="sxs-lookup"><span data-stu-id="2534c-158">For this pattern, the new field is initialized to `false`.</span></span> <span data-ttu-id="2534c-159">Każdy subskrybent może zmienić go na `true`.</span><span class="sxs-lookup"><span data-stu-id="2534c-159">Any subscriber can change it to `true`.</span></span> <span data-ttu-id="2534c-160">Gdy Wszyscy subskrybenci napotkają zgłoszone zdarzenie, składnik FileSearcher analizuje wartość logiczną i wykonuje akcję.</span><span class="sxs-lookup"><span data-stu-id="2534c-160">After all subscribers have seen the event raised, the FileSearcher component examines the boolean value and takes action.</span></span>

<span data-ttu-id="2534c-161">Drugi wzorzec spowoduje anulowanie operacji tylko wtedy, gdy Wszyscy subskrybenci chcieli operacja została anulowana.</span><span class="sxs-lookup"><span data-stu-id="2534c-161">The second pattern would only cancel the operation if all subscribers wanted the operation cancelled.</span></span> <span data-ttu-id="2534c-162">W tym wzorcu nowe pole jest inicjowane w celu wskazania, że operacja powinna zostać anulowana, a każdy subskrybent może ją zmienić, aby wskazać, że operacja powinna być kontynuowana.</span><span class="sxs-lookup"><span data-stu-id="2534c-162">In this pattern, the new field is initialized to indicate the operation should cancel, and any subscriber could change it to indicate the operation should continue.</span></span>
<span data-ttu-id="2534c-163">Gdy Wszyscy subskrybenci napotkają zgłoszone zdarzenie, składnik FileSearcher analizuje wartość logiczną i wykonuje akcję.</span><span class="sxs-lookup"><span data-stu-id="2534c-163">After all subscribers have seen the event raised, the FileSearcher component examines the boolean and takes action.</span></span> <span data-ttu-id="2534c-164">Ten wzorzec zawiera jeden dodatkowy krok: składnik musi wiedzieć, czy każdy subskrybent widział zdarzenie.</span><span class="sxs-lookup"><span data-stu-id="2534c-164">There is one extra step in this pattern: the component needs to know if any subscribers have seen the event.</span></span> <span data-ttu-id="2534c-165">Jeśli nie ma subskrybentów, pole wskazuje nieprawidłowe anulowanie.</span><span class="sxs-lookup"><span data-stu-id="2534c-165">If there are no subscribers, the field would indicate a cancel incorrectly.</span></span>

<span data-ttu-id="2534c-166">Zaimplementujmy pierwszą wersję dla tego przykładu.</span><span class="sxs-lookup"><span data-stu-id="2534c-166">Let's implement the first version for this sample.</span></span> <span data-ttu-id="2534c-167">Należy dodać pole Boolean o nazwie `CancelRequested` do typu `FileFoundArgs`:</span><span class="sxs-lookup"><span data-stu-id="2534c-167">You need to add a boolean field named `CancelRequested` to the `FileFoundArgs` type:</span></span>

[!code-csharp[EventArgs](../../samples/snippets/csharp/events/Program.cs#EventArgs "Update event arguments")]

<span data-ttu-id="2534c-168">To nowe pole jest automatycznie inicjowane w celu `false`, wartości domyślnej pola Boolean, więc nie można go anulować przypadkowo.</span><span class="sxs-lookup"><span data-stu-id="2534c-168">This new Field is automatically initialized to `false`, the default value for a Boolean field, so you don't cancel accidentally.</span></span> <span data-ttu-id="2534c-169">Jedyną inną zmianą w składniku jest sprawdzenie flagi po podniesieniu zdarzenia, aby sprawdzić, czy którykolwiek z subskrybentów zażądał anulowania:</span><span class="sxs-lookup"><span data-stu-id="2534c-169">The only other change to the component is to check the flag after raising the event to see if any of the subscribers have requested a cancellation:</span></span>

```csharp
public void List(string directory, string searchPattern)
{
    foreach (var file in Directory.EnumerateFiles(directory, searchPattern))
    {
        var args = new FileFoundArgs(file);
        FileFound?.Invoke(this, args);
        if (args.CancelRequested)
            break;
    }
}
```

<span data-ttu-id="2534c-170">Jedną z zalet tego wzorca jest to, że nie jest to istotna zmiana.</span><span class="sxs-lookup"><span data-stu-id="2534c-170">One advantage of this pattern is that it isn't a breaking change.</span></span>
<span data-ttu-id="2534c-171">Żaden z subskrybentów nie zażądał anulowania przed i nadal nie jest.</span><span class="sxs-lookup"><span data-stu-id="2534c-171">None of the subscribers requested a cancellation before, and they still are not.</span></span> <span data-ttu-id="2534c-172">Żaden kod subskrybenta nie wymaga aktualizacji, chyba że chcą obsługiwać nowy protokół anulowania.</span><span class="sxs-lookup"><span data-stu-id="2534c-172">None of the subscriber code needs updating unless they want to support the new cancel protocol.</span></span> <span data-ttu-id="2534c-173">Jest ona bardzo luźno powiązana.</span><span class="sxs-lookup"><span data-stu-id="2534c-173">It's very loosely coupled.</span></span>

<span data-ttu-id="2534c-174">Zaktualizujmy subskrybenta, aby zażądał anulowania po znalezieniu pierwszego pliku wykonywalnego:</span><span class="sxs-lookup"><span data-stu-id="2534c-174">Let's update the subscriber so that it requests a cancellation once it finds the first executable:</span></span>

```csharp
EventHandler<FileFoundArgs> onFileFound = (sender, eventArgs) =>
{
    Console.WriteLine(eventArgs.FoundFile);
    eventArgs.CancelRequested = true;
};
```

## <a name="adding-another-event-declaration"></a><span data-ttu-id="2534c-175">Dodawanie kolejnej deklaracji zdarzenia</span><span class="sxs-lookup"><span data-stu-id="2534c-175">Adding Another Event Declaration</span></span>

<span data-ttu-id="2534c-176">Dodajmy jeszcze jedną funkcję i zademonstrowasz inne idiomy języka dla zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="2534c-176">Let's add one more feature, and demonstrate other language idioms for events.</span></span> <span data-ttu-id="2534c-177">Dodajmy Przeciążenie metody `Search`, która przechodzi przez wszystkie podkatalogi w poszukiwaniu plików.</span><span class="sxs-lookup"><span data-stu-id="2534c-177">Let's add an overload of the `Search` method that traverses all subdirectories in search of files.</span></span>

<span data-ttu-id="2534c-178">Może to być długotrwała operacja w katalogu z wieloma katalogami podrzędnymi.</span><span class="sxs-lookup"><span data-stu-id="2534c-178">This could get to be a lengthy operation in a directory with many sub-directories.</span></span> <span data-ttu-id="2534c-179">Dodajmy zdarzenie, które zostanie wywołane po rozpoczęciu każdego nowego wyszukiwania w katalogu.</span><span class="sxs-lookup"><span data-stu-id="2534c-179">Let's add an event that gets raised when each new directory search begins.</span></span> <span data-ttu-id="2534c-180">Dzięki temu Subskrybenci mogą śledzić postęp i aktualizować użytkownika w miarę postępu.</span><span class="sxs-lookup"><span data-stu-id="2534c-180">This enables subscribers to track progress, and update the user as to progress.</span></span> <span data-ttu-id="2534c-181">Wszystkie utworzone dotąd przykłady są publiczne.</span><span class="sxs-lookup"><span data-stu-id="2534c-181">All the samples you've created so far are public.</span></span> <span data-ttu-id="2534c-182">Utwórzmy to zdarzenie wewnętrzne.</span><span class="sxs-lookup"><span data-stu-id="2534c-182">Let's make this one an internal event.</span></span> <span data-ttu-id="2534c-183">Oznacza to, że można również uczynić typy używane dla argumentów wewnętrznie.</span><span class="sxs-lookup"><span data-stu-id="2534c-183">That means you can also make the types used for the arguments internal as well.</span></span>

<span data-ttu-id="2534c-184">Zacznij od utworzenia nowej klasy pochodnej EventArgs na potrzeby raportowania nowego katalogu i postępu.</span><span class="sxs-lookup"><span data-stu-id="2534c-184">You'll start by creating the new EventArgs derived class for reporting the new directory and progress.</span></span> 

[!code-csharp[DirEventArgs](../../samples/snippets/csharp/events/Program.cs#SearchDirEventArgs "Define search directory event arguments")]

<span data-ttu-id="2534c-185">Ponownie można postępować zgodnie z zaleceniami, aby utworzyć niezmienny typ referencyjny dla argumentów zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="2534c-185">Again, you can follow the recommendations to make an immutable reference type for the event arguments.</span></span>

<span data-ttu-id="2534c-186">Następnie zdefiniuj zdarzenie.</span><span class="sxs-lookup"><span data-stu-id="2534c-186">Next, define the event.</span></span> <span data-ttu-id="2534c-187">Tym razem użyjesz innej składni.</span><span class="sxs-lookup"><span data-stu-id="2534c-187">This time, you'll use a different syntax.</span></span> <span data-ttu-id="2534c-188">Oprócz używania składni pola można jawnie utworzyć właściwość z obsługą dodawania i usuwania.</span><span class="sxs-lookup"><span data-stu-id="2534c-188">In addition to using the field syntax, you can explicitly create the property, with add and remove handlers.</span></span> <span data-ttu-id="2534c-189">W tym przykładzie nie będzie potrzebny dodatkowy kod w tych procedurach obsługi, ale pokazano, jak je utworzyć.</span><span class="sxs-lookup"><span data-stu-id="2534c-189">In this sample, you won't need extra code in those handlers, but this shows how you would create them.</span></span>

[!code-csharp[Declare event with add and remove handlers](../../samples/snippets/csharp/events/Program.cs#DeclareSearchEvent "Declare the event with add and remove handlers")]

<span data-ttu-id="2534c-190">Na wiele sposobów kod napisany w tym miejscu odzwierciedla kod wygenerowany przez kompilator dla definicji zdarzenia pól, które wcześniej były widoczne.</span><span class="sxs-lookup"><span data-stu-id="2534c-190">In many ways, the code you write here mirrors the code the compiler generates for the field event definitions you've seen earlier.</span></span> <span data-ttu-id="2534c-191">Tworzysz wydarzenie przy użyciu składni podobnej do tej, która jest używana do [Właściwości](properties.md).</span><span class="sxs-lookup"><span data-stu-id="2534c-191">You create the event using syntax very similar to that used for [properties](properties.md).</span></span> <span data-ttu-id="2534c-192">Należy zauważyć, że programy obsługi mają różne nazwy: `add` i `remove`.</span><span class="sxs-lookup"><span data-stu-id="2534c-192">Notice that the handlers have different names: `add` and `remove`.</span></span> <span data-ttu-id="2534c-193">Są one wywoływane, aby subskrybować zdarzenie lub anulować subskrypcję zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="2534c-193">These are called to subscribe to the event, or unsubscribe from the event.</span></span> <span data-ttu-id="2534c-194">Zwróć uwagę, że należy również zadeklarować prywatne pole zapasowe do przechowywania zmiennej zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="2534c-194">Notice that you also must declare a private backing field to store the event variable.</span></span> <span data-ttu-id="2534c-195">Jest ona zainicjowana na wartość null.</span><span class="sxs-lookup"><span data-stu-id="2534c-195">It is initialized to null.</span></span>

<span data-ttu-id="2534c-196">Następnie Dodajmy Przeciążenie metody `Search`, która przechodzi podkatalogi i wywołuje oba zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="2534c-196">Next, let's add the overload of the `Search` method that traverses subdirectories and raises both events.</span></span> <span data-ttu-id="2534c-197">Najprostszym sposobem osiągnięcia tego celu jest użycie domyślnego argumentu w celu określenia, czy chcesz przeszukać wszystkie katalogi:</span><span class="sxs-lookup"><span data-stu-id="2534c-197">The easiest way to accomplish this is to use a default argument to specify that you want to search all directories:</span></span>

[!code-csharp[SearchImplementation](../../samples/snippets/csharp/events/Program.cs#FinalImplementation "Implementation to search directories")]

<span data-ttu-id="2534c-198">W tym momencie można uruchomić aplikację wywołującą Przeciążenie do przeszukiwania wszystkich podkatalogów.</span><span class="sxs-lookup"><span data-stu-id="2534c-198">At this point, you can run the application calling the overload for searching all sub-directories.</span></span> <span data-ttu-id="2534c-199">Nie ma subskrybentów nowego zdarzenia `ChangeDirectory`, ale użycie `?.Invoke()` idiom gwarantuje, że działa poprawnie.</span><span class="sxs-lookup"><span data-stu-id="2534c-199">There are no subscribers on the new `ChangeDirectory` event, but using the `?.Invoke()` idiom ensures that this works correctly.</span></span>

 <span data-ttu-id="2534c-200">Dodajmy procedurę obsługi, aby napisać wiersz pokazujący postęp w oknie konsoli.</span><span class="sxs-lookup"><span data-stu-id="2534c-200">Let's add a handler to write a line that shows the progress in the console window.</span></span> 

[!code-csharp[Search](../../samples/snippets/csharp/events/Program.cs#Search "Declare event handler")]

<span data-ttu-id="2534c-201">Pojawiły się wzorce, które są obserwowane w całym ekosystemie platformy .NET.</span><span class="sxs-lookup"><span data-stu-id="2534c-201">You've seen patterns that are followed throughout the .NET ecosystem.</span></span>
<span data-ttu-id="2534c-202">Poznanie tych wzorców i Konwencji umożliwia szybkie pisanie idiomatyczne C# i platformy .NET.</span><span class="sxs-lookup"><span data-stu-id="2534c-202">By learning these patterns and conventions, you'll be writing idiomatic C# and .NET quickly.</span></span>

<span data-ttu-id="2534c-203">Następnie zobaczysz pewne zmiany w tych wzorcach w najnowszej wersji programu .NET.</span><span class="sxs-lookup"><span data-stu-id="2534c-203">Next, you'll see some changes in these patterns in the most recent release of .NET.</span></span>

[<span data-ttu-id="2534c-204">Dalej</span><span class="sxs-lookup"><span data-stu-id="2534c-204">Next</span></span>](modern-events.md)
