---
title: Typy zwrotów asynchronii (C#)
ms.date: 04/14/2020
ms.assetid: ddb2539c-c898-48c1-ad92-245e4a996df8
ms.openlocfilehash: 73a6e1924652c8635377547e2faddc864ac5540a
ms.sourcegitcommit: c91110ef6ee3fedb591f3d628dc17739c4a7071e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/15/2020
ms.locfileid: "81389132"
---
# <a name="async-return-types-c"></a><span data-ttu-id="accb6-102">Typy zwrotów asynchronii (C#)</span><span class="sxs-lookup"><span data-stu-id="accb6-102">Async Return Types (C#)</span></span>

<span data-ttu-id="accb6-103">Metody asynchronizowe mogą mieć następujące typy zwracane:</span><span class="sxs-lookup"><span data-stu-id="accb6-103">Async methods can have the following return types:</span></span>

- <span data-ttu-id="accb6-104"><xref:System.Threading.Tasks.Task%601>, dla metody asynchronii, która zwraca wartość.</span><span class="sxs-lookup"><span data-stu-id="accb6-104"><xref:System.Threading.Tasks.Task%601>, for an async method that returns a value.</span></span>
- <span data-ttu-id="accb6-105"><xref:System.Threading.Tasks.Task>, dla metody asynchronizowej, która wykonuje operację, ale zwraca żadną wartość.</span><span class="sxs-lookup"><span data-stu-id="accb6-105"><xref:System.Threading.Tasks.Task>, for an async method that performs an operation but returns no value.</span></span>
- <span data-ttu-id="accb6-106">`void`, dla programu obsługi zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="accb6-106">`void`, for an event handler.</span></span>
- <span data-ttu-id="accb6-107">Począwszy od języka C# 7.0, `GetAwaiter` dowolnego typu, który ma metodę dostępną.</span><span class="sxs-lookup"><span data-stu-id="accb6-107">Starting with C# 7.0, any type that has an accessible `GetAwaiter` method.</span></span> <span data-ttu-id="accb6-108">Obiekt zwracany `GetAwaiter` przez metodę <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> musi implementować interfejs.</span><span class="sxs-lookup"><span data-stu-id="accb6-108">The object returned by the `GetAwaiter` method must implement the <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> interface.</span></span>
- <span data-ttu-id="accb6-109">Począwszy od C# 8.0, <xref:System.Collections.Generic.IAsyncEnumerable%601>dla metody asynchronii, która zwraca strumień *asynchronii*.</span><span class="sxs-lookup"><span data-stu-id="accb6-109">Starting with C# 8.0, <xref:System.Collections.Generic.IAsyncEnumerable%601>, for an async method that returns an *async stream*.</span></span>

<span data-ttu-id="accb6-110">Aby uzyskać więcej informacji na temat metod [asynchronicznych, zobacz Programowanie asynchroniczne za pomocą asynchronii i await (C#)](./index.md).</span><span class="sxs-lookup"><span data-stu-id="accb6-110">For more information about async methods, see [Asynchronous Programming with async and await (C#)](./index.md).</span></span>  
  
## <a name="tasktresult-return-type"></a><span data-ttu-id="accb6-111">Typ\<powrotu\> zadania TResult</span><span class="sxs-lookup"><span data-stu-id="accb6-111">Task\<TResult\> Return Type</span></span>  
<span data-ttu-id="accb6-112">Typ <xref:System.Threading.Tasks.Task%601> zwracany jest używany dla metody asynchroniiowej, która zawiera instrukcję [return](../../../language-reference/keywords/return.md) (C#), w której jest `TResult`operand .</span><span class="sxs-lookup"><span data-stu-id="accb6-112">The <xref:System.Threading.Tasks.Task%601> return type is used for an async method that contains a [return](../../../language-reference/keywords/return.md) (C#) statement in which the operand is `TResult`.</span></span>  
  
<span data-ttu-id="accb6-113">W poniższym przykładzie `GetLeisureHours` metoda asynchroniowa zawiera instrukcję, `return` która zwraca liczbę całkowitą.</span><span class="sxs-lookup"><span data-stu-id="accb6-113">In the following example, the `GetLeisureHours` async method contains a `return` statement that returns an integer.</span></span> <span data-ttu-id="accb6-114">W związku z tym deklaracja metody `Task<int>`musi określać typ zwracany .</span><span class="sxs-lookup"><span data-stu-id="accb6-114">Therefore, the method declaration must specify a return type of `Task<int>`.</span></span>  <span data-ttu-id="accb6-115">Metoda <xref:System.Threading.Tasks.Task.FromResult%2A> asynchronikowa jest symbolem zastępczym operacji zwraca ciąg.</span><span class="sxs-lookup"><span data-stu-id="accb6-115">The <xref:System.Threading.Tasks.Task.FromResult%2A> async method is a placeholder for an operation that returns a string.</span></span>
  
:::code language="csharp" source="./snippets/async-returns1.cs" id="SnippetFirstExample":::

<span data-ttu-id="accb6-116">Gdy `GetLeisureHours` jest wywoływana z wewnątrz `ShowTodaysInfo` await wyrażenie w metodzie, wyrażenie await pobiera `leisureHours`wartość całkowitą (wartość ), który `GetLeisureHours` jest przechowywany w zadaniu zwróconym przez metodę.</span><span class="sxs-lookup"><span data-stu-id="accb6-116">When `GetLeisureHours` is called from within an await expression in the `ShowTodaysInfo` method, the await expression retrieves the integer value (the value of `leisureHours`) that's stored in the task returned by the `GetLeisureHours` method.</span></span> <span data-ttu-id="accb6-117">Aby uzyskać więcej informacji na temat oczekujących wyrażeń, zobacz [await](../../../language-reference/operators/await.md).</span><span class="sxs-lookup"><span data-stu-id="accb6-117">For more information about await expressions, see [await](../../../language-reference/operators/await.md).</span></span>  
  
<span data-ttu-id="accb6-118">Można lepiej zrozumieć, `await` jak pobiera `Task<T>` wynik z a `GetLeisureHours` przez oddzielenie `await`wywołania od aplikacji , jak pokazano w poniższym kodzie.</span><span class="sxs-lookup"><span data-stu-id="accb6-118">You can better understand how `await` retrieves the result from a `Task<T>` by separating the call to `GetLeisureHours` from the application of `await`, as the following code shows.</span></span> <span data-ttu-id="accb6-119">Wywołanie metody, `GetLeisureHours` która nie jest natychmiast `Task<int>`oczekiwana zwraca , zgodnie z oczekiwaniami po deklaracji metody.</span><span class="sxs-lookup"><span data-stu-id="accb6-119">A call to method `GetLeisureHours` that isn't immediately awaited returns a `Task<int>`, as you would expect from the declaration of the method.</span></span> <span data-ttu-id="accb6-120">Zadanie jest przypisane `integerTask` do zmiennej w przykładzie.</span><span class="sxs-lookup"><span data-stu-id="accb6-120">The task is assigned to the `integerTask` variable in the example.</span></span> <span data-ttu-id="accb6-121">Ponieważ `integerTask` jest <xref:System.Threading.Tasks.Task%601>, zawiera <xref:System.Threading.Tasks.Task%601.Result> właściwość `TResult`typu .</span><span class="sxs-lookup"><span data-stu-id="accb6-121">Because `integerTask` is a <xref:System.Threading.Tasks.Task%601>, it contains a <xref:System.Threading.Tasks.Task%601.Result> property of type `TResult`.</span></span> <span data-ttu-id="accb6-122">W takim `TResult` przypadku reprezentuje typ liczby całkowitej.</span><span class="sxs-lookup"><span data-stu-id="accb6-122">In this case, `TResult` represents an integer type.</span></span> <span data-ttu-id="accb6-123">Po `await` zastosowaniu `integerTask`do wyrażenia await oblicza zawartość <xref:System.Threading.Tasks.Task%601.Result%2A> właściwości `integerTask`.</span><span class="sxs-lookup"><span data-stu-id="accb6-123">When `await` is applied to `integerTask`, the await expression evaluates to the contents of the <xref:System.Threading.Tasks.Task%601.Result%2A> property of `integerTask`.</span></span> <span data-ttu-id="accb6-124">Wartość jest przypisana `ret` do zmiennej.</span><span class="sxs-lookup"><span data-stu-id="accb6-124">The value is assigned to the `ret` variable.</span></span>  
  
> [!IMPORTANT]
> <span data-ttu-id="accb6-125">Właściwość <xref:System.Threading.Tasks.Task%601.Result%2A> jest właściwością blokującą.</span><span class="sxs-lookup"><span data-stu-id="accb6-125">The <xref:System.Threading.Tasks.Task%601.Result%2A> property is a blocking property.</span></span> <span data-ttu-id="accb6-126">Jeśli spróbujesz uzyskać do niego dostęp przed zakończeniem zadania, wątek, który jest aktualnie aktywny, jest zablokowany do momentu ukończenia zadania i udostępnienia wartości.</span><span class="sxs-lookup"><span data-stu-id="accb6-126">If you try to access it before its task is finished, the thread that's currently active is blocked until the task completes and the value is available.</span></span> <span data-ttu-id="accb6-127">W większości przypadków należy uzyskać dostęp `await` do wartości przy użyciu zamiast dostępu do właściwości bezpośrednio.</span><span class="sxs-lookup"><span data-stu-id="accb6-127">In most cases, you should access the value by using `await` instead of accessing the property directly.</span></span> <br/> <span data-ttu-id="accb6-128">W poprzednim przykładzie pobrano <xref:System.Threading.Tasks.Task%601.Result%2A> wartość właściwości, aby zablokować wątek główny, dzięki czemu `ShowTodaysInfo` metoda może zakończyć wykonywanie przed zakończeniem aplikacji.</span><span class="sxs-lookup"><span data-stu-id="accb6-128">The previous example retrieved the value of the <xref:System.Threading.Tasks.Task%601.Result%2A> property to block the main thread so that the `ShowTodaysInfo` method could finish execution before the application ended.</span></span>  

:::code language="csharp" source="./snippets/async-returns1a.cs" id="SnippetSecondVersion":::

## <a name="task-return-type"></a><span data-ttu-id="accb6-129">Typ zwrotu zadania</span><span class="sxs-lookup"><span data-stu-id="accb6-129">Task Return Type</span></span>  
<span data-ttu-id="accb6-130">Metody asynchronizowe, `return` które nie zawierają `return` instrukcji lub które zawierają instrukcję, która <xref:System.Threading.Tasks.Task>nie zwraca operandu, zwykle mają typ zwracany .</span><span class="sxs-lookup"><span data-stu-id="accb6-130">Async methods that don't contain a `return` statement or that contain a `return` statement that doesn't return an operand usually have a return type of <xref:System.Threading.Tasks.Task>.</span></span> <span data-ttu-id="accb6-131">Takie metody `void` zwracają, jeśli działają synchronicznie.</span><span class="sxs-lookup"><span data-stu-id="accb6-131">Such methods return `void` if they run synchronously.</span></span> <span data-ttu-id="accb6-132">Jeśli używasz <xref:System.Threading.Tasks.Task> typu zwracanego dla metody asynchroniiowej, metoda wywołująca może użyć `await` operatora, aby zawiesić zakończenie wywołującego, dopóki wywołana metoda asynchronii nie zostanie zakończona.</span><span class="sxs-lookup"><span data-stu-id="accb6-132">If you use a <xref:System.Threading.Tasks.Task> return type for an async method, a calling method can use an `await` operator to suspend the caller's completion until the called async method has finished.</span></span>  
  
<span data-ttu-id="accb6-133">W poniższym przykładzie `WaitAndApologize` metoda asynchroniowa `return` nie zawiera instrukcji, <xref:System.Threading.Tasks.Task> więc metoda zwraca obiekt.</span><span class="sxs-lookup"><span data-stu-id="accb6-133">In the following example, the `WaitAndApologize` async method doesn't contain a `return` statement, so the method returns a <xref:System.Threading.Tasks.Task> object.</span></span> <span data-ttu-id="accb6-134">Powrót umożliwia `Task` `WaitAndApologize` oczekiwanie.</span><span class="sxs-lookup"><span data-stu-id="accb6-134">Returning a `Task` enables `WaitAndApologize` to be awaited.</span></span> <span data-ttu-id="accb6-135">Typ <xref:System.Threading.Tasks.Task> nie zawiera `Result` właściwości, ponieważ nie ma wartości zwracanej.</span><span class="sxs-lookup"><span data-stu-id="accb6-135">The <xref:System.Threading.Tasks.Task> type doesn't include a `Result` property because it has no return value.</span></span>  

:::code language="csharp" source="./snippets/async-returns2.cs" id="SnippetTaskReturn":::

<span data-ttu-id="accb6-136">`WaitAndApologize`oczekuje się za pomocą await instrukcji zamiast await wyrażenie, podobne do instrukcji wywołującej dla synchroniczne void-returning metody.</span><span class="sxs-lookup"><span data-stu-id="accb6-136">`WaitAndApologize` is awaited by using an await statement instead of an await expression, similar to the calling statement for a synchronous void-returning method.</span></span> <span data-ttu-id="accb6-137">Zastosowanie operatora await w tym przypadku nie daje wartości.</span><span class="sxs-lookup"><span data-stu-id="accb6-137">The application of an await operator in this case doesn't produce a value.</span></span>  
  
<span data-ttu-id="accb6-138">Podobnie jak <xref:System.Threading.Tasks.Task%601> w poprzednim przykładzie, `WaitAndApologize` można oddzielić wywołanie od zastosowania operatora await, jak pokazano w poniższym kodzie.</span><span class="sxs-lookup"><span data-stu-id="accb6-138">As in the previous <xref:System.Threading.Tasks.Task%601> example, you can separate the call to `WaitAndApologize` from the application of an await operator, as the following code shows.</span></span> <span data-ttu-id="accb6-139">Należy jednak pamiętać, że `Task` a `Result` nie ma właściwości i że żadna wartość nie `Task`jest wytwarzana, gdy operator await jest stosowany do .</span><span class="sxs-lookup"><span data-stu-id="accb6-139">However, remember that a `Task` doesn't have a `Result` property, and that no value is produced when an await operator is applied to a `Task`.</span></span>  
  
<span data-ttu-id="accb6-140">Poniższy kod oddziela `WaitAndApologize` wywołanie metody od oczekiwania na zadanie, które zwraca metoda.</span><span class="sxs-lookup"><span data-stu-id="accb6-140">The following code separates calling the `WaitAndApologize` method from awaiting the task that the method returns.</span></span>  

:::code language="csharp" source="./snippets/async-returns2a.cs" id="SnippetAwaitTask":::

## <a name="void-return-type"></a><span data-ttu-id="accb6-141">Unieważnij typ zwrotu</span><span class="sxs-lookup"><span data-stu-id="accb6-141">Void return type</span></span>

<span data-ttu-id="accb6-142">Typ zwracany `void` jest używany w programach obsługi zdarzeń `void` asynchronicznych, które wymagają typu zwracanego.</span><span class="sxs-lookup"><span data-stu-id="accb6-142">You use the `void` return type in asynchronous event handlers, which require a `void` return type.</span></span> <span data-ttu-id="accb6-143">Dla metod innych niż programy obsługi zdarzeń, które nie <xref:System.Threading.Tasks.Task> zwracają wartości, należy zwrócić zamiast `void` tego, ponieważ metoda asynchronii, która zwraca nie można oczekiwać.</span><span class="sxs-lookup"><span data-stu-id="accb6-143">For methods other than event handlers that don't return a value, you should return a <xref:System.Threading.Tasks.Task> instead, because an async method that returns `void` can't be awaited.</span></span> <span data-ttu-id="accb6-144">Każdy wywołujący takiej metody musi kontynuować dopełnienia bez oczekiwania na wywołaną metodę asynchronii, aby zakończyć.</span><span class="sxs-lookup"><span data-stu-id="accb6-144">Any caller of such a method must continue to completion without waiting for the called async method to finish.</span></span> <span data-ttu-id="accb6-145">Wywołujący musi być niezależny od wszelkich wartości lub wyjątków, które generuje metoda asynchroniza.</span><span class="sxs-lookup"><span data-stu-id="accb6-145">The caller must be independent of any values or exceptions that the async method generates.</span></span>  
  
<span data-ttu-id="accb6-146">Wywołujący metody asynchronii zwracającej void nie można złapać wyjątki generowane z metody i takie nieobsługiwanie wyjątki mogą spowodować, że aplikacja zakończy się niepowodzeniem.</span><span class="sxs-lookup"><span data-stu-id="accb6-146">The caller of a void-returning async method can't catch exceptions thrown from the method, and such unhandled exceptions are likely to cause your application to fail.</span></span> <span data-ttu-id="accb6-147">Jeśli metoda, która <xref:System.Threading.Tasks.Task> <xref:System.Threading.Tasks.Task%601> zwraca lub zgłasza wyjątek, wyjątek jest przechowywany w zwróconym zadaniu.</span><span class="sxs-lookup"><span data-stu-id="accb6-147">If a method that returns a <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> throws an exception, the exception is stored in the returned task.</span></span> <span data-ttu-id="accb6-148">Wyjątek jest rethrown, gdy zadanie jest oczekiwany.</span><span class="sxs-lookup"><span data-stu-id="accb6-148">The exception is rethrown when the task is awaited.</span></span> <span data-ttu-id="accb6-149">W związku z tym upewnij się, że każda metoda asynchronii, która może powodować wyjątek ma typ zwracany <xref:System.Threading.Tasks.Task> lub <xref:System.Threading.Tasks.Task%601> że wywołania metody są oczekiwane.</span><span class="sxs-lookup"><span data-stu-id="accb6-149">Therefore, make sure that any async method that can produce an exception has a return type of <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> and that calls to the method are awaited.</span></span>  
  
<span data-ttu-id="accb6-150">Aby uzyskać więcej informacji na temat sposobu wychwytuj wyjątki w metodach asynchronizacyjnych, zobacz [wyjątki w metody asynchronii](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) sekcji [try-catch](../../../language-reference/keywords/try-catch.md) artykułu.</span><span class="sxs-lookup"><span data-stu-id="accb6-150">For more information about how to catch exceptions in async methods, see the [Exceptions in Async Methods](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) section of the [try-catch](../../../language-reference/keywords/try-catch.md) article.</span></span>  
  
<span data-ttu-id="accb6-151">W poniższym przykładzie przedstawiono zachowanie programu obsługi zdarzeń asynchronicznych.</span><span class="sxs-lookup"><span data-stu-id="accb6-151">The following example shows the behavior of an async event handler.</span></span> <span data-ttu-id="accb6-152">W przykładowym kodzie program obsługi zdarzeń asynchroniiowych musi poinformować o zakończeniu wątku głównego.</span><span class="sxs-lookup"><span data-stu-id="accb6-152">In the example code, an async event handler must let the main thread know when it finishes.</span></span> <span data-ttu-id="accb6-153">Następnie główny wątek może czekać na program obsługi zdarzeń asynchronii, aby zakończyć przed zamknięciem programu.</span><span class="sxs-lookup"><span data-stu-id="accb6-153">Then the main thread can wait for an async event handler to complete before exiting the program.</span></span>

:::code language="csharp" source="./snippets/async-returns3.cs":::

## <a name="generalized-async-return-types-and-valuetasktresult"></a><span data-ttu-id="accb6-154">Uogólnione typy zwracania\<asynchronii i ValueTask TResult\></span><span class="sxs-lookup"><span data-stu-id="accb6-154">Generalized async return types and ValueTask\<TResult\></span></span>

<span data-ttu-id="accb6-155">Począwszy od języka C# 7.0, metoda asynchroniowa może zwracać dowolny typ, który ma dostępną `GetAwaiter` metodę.</span><span class="sxs-lookup"><span data-stu-id="accb6-155">Starting with C# 7.0, an async method can return any type that has an accessible `GetAwaiter` method.</span></span>

<span data-ttu-id="accb6-156">Ponieważ <xref:System.Threading.Tasks.Task> <xref:System.Threading.Tasks.Task%601> i są typy odwołań, alokacji pamięci w ścieżkach krytycznych dla wydajności, szczególnie gdy alokacje występują w ciasnych pętli, może niekorzystnie wpłynąć na wydajność.</span><span class="sxs-lookup"><span data-stu-id="accb6-156">Because <xref:System.Threading.Tasks.Task> and <xref:System.Threading.Tasks.Task%601> are reference types, memory allocation in performance-critical paths, particularly when allocations occur in tight loops, can adversely affect performance.</span></span> <span data-ttu-id="accb6-157">Obsługa uogólnionych typów zwracanych oznacza, że można zwrócić lekki typ wartości zamiast typu odwołania, aby uniknąć dodatkowych alokacji pamięci.</span><span class="sxs-lookup"><span data-stu-id="accb6-157">Support for generalized return types means that you can return a lightweight value type instead of a reference type to avoid additional memory allocations.</span></span>

<span data-ttu-id="accb6-158">.NET zapewnia <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> strukturę jako lekką implementację uogólnionej wartości zwracającej zadania.</span><span class="sxs-lookup"><span data-stu-id="accb6-158">.NET provides the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> structure as a lightweight implementation of a generalized task-returning value.</span></span> <span data-ttu-id="accb6-159">Aby użyć <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> tego typu, `System.Threading.Tasks.Extensions` należy dodać pakiet NuGet do projektu.</span><span class="sxs-lookup"><span data-stu-id="accb6-159">To use the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> type, you must add the `System.Threading.Tasks.Extensions` NuGet package to your project.</span></span> <span data-ttu-id="accb6-160">W poniższym przykładzie <xref:System.Threading.Tasks.ValueTask%601> użyto struktury do pobrania wartości dwóch kośćch rolkach.</span><span class="sxs-lookup"><span data-stu-id="accb6-160">The following example uses the <xref:System.Threading.Tasks.ValueTask%601> structure to retrieve the value of two dice rolls.</span></span>
  
:::code language="csharp" source="./snippets/async-valuetask.cs":::

## <a name="async-streams-with-iasyncenumerablet"></a><span data-ttu-id="accb6-161">Strumienie asynchroniczne z IAsyncEnumerable\<T\></span><span class="sxs-lookup"><span data-stu-id="accb6-161">Async streams with IAsyncEnumerable\<T\></span></span>

<span data-ttu-id="accb6-162">Począwszy od C# 8.0, metoda asynchroniowa może zwrócić <xref:System.Collections.Generic.IAsyncEnumerable%601> *strumień asynchroniowy,* reprezentowany przez .</span><span class="sxs-lookup"><span data-stu-id="accb6-162">Starting with C# 8.0, an async method may return an *async stream*, represented by <xref:System.Collections.Generic.IAsyncEnumerable%601>.</span></span> <span data-ttu-id="accb6-163">Strumień asynchronowy umożliwia wyliczenie elementów odczytanych ze strumienia, gdy elementy są generowane we fragmentach z powtarzającymi się wywołaniami asynchronizacyjnymi.</span><span class="sxs-lookup"><span data-stu-id="accb6-163">An async stream provides a way to enumerate items read from a stream when elements are generated in chunks with repeated asynchronous calls.</span></span> <span data-ttu-id="accb6-164">W poniższym przykładzie przedstawiono metodę asynchronizową, która generuje strumień asynchronii:</span><span class="sxs-lookup"><span data-stu-id="accb6-164">The following example shows an async method that generates an async stream:</span></span>

:::code language="csharp" source="./snippets/AsyncStreams.cs" id="SnippetGenerateAsyncStream":::

<span data-ttu-id="accb6-165">W poprzednim przykładzie odczytuje wiersze z ciągu asynchronicznie.</span><span class="sxs-lookup"><span data-stu-id="accb6-165">The preceding example reads lines from a string asynchronously.</span></span> <span data-ttu-id="accb6-166">Po odczytie każdego wiersza kod wylicza każde słowo w ciągu.</span><span class="sxs-lookup"><span data-stu-id="accb6-166">Once each line is read, the code enumerates each word in the string.</span></span> <span data-ttu-id="accb6-167">Wywołujący będzie wyliczyć każdego `await foreach` wyrazu za pomocą instrukcji.</span><span class="sxs-lookup"><span data-stu-id="accb6-167">Callers would enumerate each word using the `await foreach` statement.</span></span> <span data-ttu-id="accb6-168">Metoda oczekuje, gdy trzeba asynchronicznie odczytać następny wiersz z ciągu źródłowego.</span><span class="sxs-lookup"><span data-stu-id="accb6-168">The method awaits when it needs to asynchronously read the next line from the source string.</span></span>

## <a name="see-also"></a><span data-ttu-id="accb6-169">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="accb6-169">See also</span></span>

- <xref:System.Threading.Tasks.Task.FromResult%2A>
- [<span data-ttu-id="accb6-170">Przewodnik: Uzyskiwanie dostępu do sieci Web przy użyciu asynchronii i oczekiwania (C#)</span><span class="sxs-lookup"><span data-stu-id="accb6-170">Walkthrough: Accessing the Web by Using async and await (C#)</span></span>](./walkthrough-accessing-the-web-by-using-async-and-await.md)
- [<span data-ttu-id="accb6-171">Przepływ sterowania w programach Asynchronii (C#)</span><span class="sxs-lookup"><span data-stu-id="accb6-171">Control Flow in Async Programs (C#)</span></span>](./control-flow-in-async-programs.md)
- [<span data-ttu-id="accb6-172">async</span><span class="sxs-lookup"><span data-stu-id="accb6-172">async</span></span>](../../../language-reference/keywords/async.md)
- [<span data-ttu-id="accb6-173">await</span><span class="sxs-lookup"><span data-stu-id="accb6-173">await</span></span>](../../../language-reference/operators/await.md)
