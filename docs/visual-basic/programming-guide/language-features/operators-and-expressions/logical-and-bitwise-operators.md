---
title: Operatory logiczne i bitowe w Visual Basic
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: ac47b6d7fa4861d18646a23f442caccc4062852f
ms.sourcegitcommit: 0be8a279af6d8a43e03141e349d3efd5d35f8767
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/18/2019
ms.locfileid: "58819310"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="f955a-102">Operatory logiczne i bitowe w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="f955a-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="f955a-103">Operatory logiczne, porównanie `Boolean` wyrażeń i zwrócenia `Boolean` wynik.</span><span class="sxs-lookup"><span data-stu-id="f955a-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="f955a-104">`And`, `Or`, `AndAlso`, `OrElse`, I `Xor` operatory są *binarne* ponieważ przyjmują dwa argumenty operacji, podczas gdy `Not` operator jest *jednoargumentowe* z powodu jeden argument operacji.</span><span class="sxs-lookup"><span data-stu-id="f955a-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="f955a-105">Niektóre z tych operatorów można również wykonać operacje logiczne bitowe dla wartości całkowitych.</span><span class="sxs-lookup"><span data-stu-id="f955a-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="f955a-106">Jednoargumentowy Operator logiczny</span><span class="sxs-lookup"><span data-stu-id="f955a-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="f955a-107">[Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) wykonuje logiczne *negacji* na `Boolean` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="f955a-107">The [Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="f955a-108">Daje przeciwną wartość logiczną swojego operandu.</span><span class="sxs-lookup"><span data-stu-id="f955a-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="f955a-109">Jeśli wyrażenie ma `True`, następnie `Not` zwraca `False`; Jeśli wyrażenie ma `False`, następnie `Not` zwraca `True`.</span><span class="sxs-lookup"><span data-stu-id="f955a-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="f955a-110">Ilustruje to poniższy przykład.</span><span class="sxs-lookup"><span data-stu-id="f955a-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="f955a-111">Operatory dwuargumentowe logiczne</span><span class="sxs-lookup"><span data-stu-id="f955a-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="f955a-112">[Operatora](../../../../visual-basic/language-reference/operators/and-operator.md) wykonuje logiczne *połączeniu* na dwóch `Boolean` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="f955a-112">The [And Operator](../../../../visual-basic/language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="f955a-113">Jeśli oba wyrażenia mają `True`, następnie `And` zwraca `True`.</span><span class="sxs-lookup"><span data-stu-id="f955a-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="f955a-114">Jeśli co najmniej jedno z wyrażeń daje w wyniku `False`, następnie `And` zwraca `False`.</span><span class="sxs-lookup"><span data-stu-id="f955a-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="f955a-115">[Operatora Or](../../../../visual-basic/language-reference/operators/or-operator.md) wykonuje logiczne *rozłączenia* lub *włączenia* na dwóch `Boolean` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="f955a-115">The [Or Operator](../../../../visual-basic/language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="f955a-116">Jeśli wyrażenie zwraca `True`, lub oba zwrócić `True`, następnie `Or` zwraca `True`.</span><span class="sxs-lookup"><span data-stu-id="f955a-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="f955a-117">Jeśli żadna wyrażenie `True`, `Or` zwraca `False`.</span><span class="sxs-lookup"><span data-stu-id="f955a-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="f955a-118">[Xor — Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) wykonuje logiczne *wykluczeń* na dwóch `Boolean` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="f955a-118">The [Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="f955a-119">Jeśli dokładnie jedno wyrażenie, które daje w wyniku `True`, ale nie obu `Xor` zwraca `True`.</span><span class="sxs-lookup"><span data-stu-id="f955a-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="f955a-120">Jeśli oba wyrażenia mają `True` lub zwrócić oba `False`, `Xor` zwraca `False`.</span><span class="sxs-lookup"><span data-stu-id="f955a-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="f955a-121">W poniższym przykładzie pokazano `And`, `Or`, i `Xor` operatorów.</span><span class="sxs-lookup"><span data-stu-id="f955a-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="f955a-122">Zwarcie operacji logicznych</span><span class="sxs-lookup"><span data-stu-id="f955a-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="f955a-123">[AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) jest bardzo podobny do `And` operatora, w tym również wykonuje logiczną na dwóch `Boolean` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="f955a-123">The [AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="f955a-124">Główną różnicą między nimi jest to, że `AndAlso` wykazuje *zwarcie* zachowanie.</span><span class="sxs-lookup"><span data-stu-id="f955a-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="f955a-125">Jeśli pierwsze wyrażenie w `AndAlso` wyrażenie daje w wyniku `False`, a następnie drugie wyrażenie nie jest oceniany, ponieważ nie można zmienić, wynik końcowy i `AndAlso` zwraca `False`.</span><span class="sxs-lookup"><span data-stu-id="f955a-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="f955a-126">Podobnie [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) wykonuje zwarcie logicznego rozłączenia dwóch `Boolean` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="f955a-126">Similarly, the [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="f955a-127">Jeśli pierwsze wyrażenie w `OrElse` wyrażenie daje w wyniku `True`, a następnie drugie wyrażenie nie jest oceniany, ponieważ nie można zmienić, wynik końcowy i `OrElse` zwraca `True`.</span><span class="sxs-lookup"><span data-stu-id="f955a-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="f955a-128">Zwarcie wad</span><span class="sxs-lookup"><span data-stu-id="f955a-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="f955a-129">Zwarcie może zwiększyć wydajność przez nie obliczenia wyrażenia, którego nie można zmienić wynik operacji logicznej.</span><span class="sxs-lookup"><span data-stu-id="f955a-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="f955a-130">Jednak jeśli to wyrażenie wykonuje dodatkowe akcje, zwarcie pomija tych akcji.</span><span class="sxs-lookup"><span data-stu-id="f955a-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="f955a-131">Na przykład, jeśli wyrażenie zawiera wywołanie `Function` procedurę, że procedura nie jest wywoływana, jeśli wyrażenie jest zwartym i wszelkie dodatkowe kod zawarty w `Function` nie działa.</span><span class="sxs-lookup"><span data-stu-id="f955a-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="f955a-132">W związku z tym funkcja mogą być uruchamiane tylko od czasu do czasu i może nie być poprawnie badane.</span><span class="sxs-lookup"><span data-stu-id="f955a-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="f955a-133">Lub logiki programu może zależeć od kodu w `Function`.</span><span class="sxs-lookup"><span data-stu-id="f955a-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="f955a-134">Poniższy przykład ilustruje różnicę między `And`, `Or`i ich odpowiedniki short-circuiting.</span><span class="sxs-lookup"><span data-stu-id="f955a-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="f955a-135">W poprzednim przykładzie, należy pamiętać, że niektóre ważne kod wewnątrz `checkIfValid()` nie jest uruchamiane, gdy wywołanie jest zwartym.</span><span class="sxs-lookup"><span data-stu-id="f955a-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="f955a-136">Pierwszy `If` instrukcja wywołuje `checkIfValid()` mimo że `12 > 45` zwraca `False`, ponieważ `And` nie powodują pominięcie.</span><span class="sxs-lookup"><span data-stu-id="f955a-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="f955a-137">Drugi `If` instrukcja wywołuje `checkIfValid()`, ponieważ gdy `12 > 45` zwraca `False`, `AndAlso` short-circuits drugie wyrażenie.</span><span class="sxs-lookup"><span data-stu-id="f955a-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="f955a-138">Trzeci `If` instrukcja wywołuje `checkIfValid()` mimo że `12 < 45` zwraca `True`, ponieważ `Or` nie powodują pominięcie.</span><span class="sxs-lookup"><span data-stu-id="f955a-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="f955a-139">Czwarty `If` instrukcja wywołuje `checkIfValid()`, ponieważ gdy `12 < 45` zwraca `True`, `OrElse` short-circuits drugie wyrażenie.</span><span class="sxs-lookup"><span data-stu-id="f955a-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="f955a-140">Operacje bitowe</span><span class="sxs-lookup"><span data-stu-id="f955a-140">Bitwise Operations</span></span>  
 <span data-ttu-id="f955a-141">Operacje bitowe ocenić dwóch wartości całkowitych, w postaci pliku binarnego (podstawa 2).</span><span class="sxs-lookup"><span data-stu-id="f955a-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="f955a-142">Ich porównanie usługi bits w odpowiednich miejscach i przypisywanie na podstawie porównania wartości.</span><span class="sxs-lookup"><span data-stu-id="f955a-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="f955a-143">W poniższym przykładzie pokazano `And` operatora.</span><span class="sxs-lookup"><span data-stu-id="f955a-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="f955a-144">Poprzedni przykład ustawia wartość `x` 1.</span><span class="sxs-lookup"><span data-stu-id="f955a-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="f955a-145">Dzieje się z następujących powodów:</span><span class="sxs-lookup"><span data-stu-id="f955a-145">This happens for the following reasons:</span></span>  
  
-   <span data-ttu-id="f955a-146">Wartości są traktowane jako dane binarne:</span><span class="sxs-lookup"><span data-stu-id="f955a-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="f955a-147">3 w formacie binarnym = 011</span><span class="sxs-lookup"><span data-stu-id="f955a-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="f955a-148">5 w formacie binarnym = 101</span><span class="sxs-lookup"><span data-stu-id="f955a-148">5 in binary form = 101</span></span>  
  
-   <span data-ttu-id="f955a-149">`And` Operator porównuje reprezentacji binarnych jedną pozycję binarny (bity) w danym momencie.</span><span class="sxs-lookup"><span data-stu-id="f955a-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="f955a-150">Jeśli oba bity na określonej pozycji 1, 1 jest umieszczany w tym miejscu, w wyniku.</span><span class="sxs-lookup"><span data-stu-id="f955a-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="f955a-151">Jeśli bit albo ma wartość 0, 0 jest umieszczany w tym miejscu, w wyniku.</span><span class="sxs-lookup"><span data-stu-id="f955a-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="f955a-152">W powyższym przykładzie wynik działania jest prawidłowy w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="f955a-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="f955a-153">011 (3 w formacie binarnym)</span><span class="sxs-lookup"><span data-stu-id="f955a-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="f955a-154">101 (5 w formacie binarnym)</span><span class="sxs-lookup"><span data-stu-id="f955a-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="f955a-155">001 (wynik, w formacie binarnym)</span><span class="sxs-lookup"><span data-stu-id="f955a-155">001 (The result, in binary form)</span></span>  
  
-   <span data-ttu-id="f955a-156">Wynik jest traktowany jako dziesiętne.</span><span class="sxs-lookup"><span data-stu-id="f955a-156">The result is treated as decimal.</span></span> <span data-ttu-id="f955a-157">Wartość 001 jest binarna reprezentacja 1, więc `x` = 1.</span><span class="sxs-lookup"><span data-stu-id="f955a-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="f955a-158">Operatora testu koniunkcji `Or` operacji jest podobny, z tą różnicą, że 1 jest przypisana do bitu wyniku, jeśli jeden lub oba bitów w porównaniu jest 1.</span><span class="sxs-lookup"><span data-stu-id="f955a-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="f955a-159">`Xor` przypisuje do bitu wyniku 1, jeśli dokładnie jeden stosunku liczby bitów (nie obie) ma wartość 1.</span><span class="sxs-lookup"><span data-stu-id="f955a-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="f955a-160">`Not` przyjmuje jeden argument operacji i odwraca wszystkie bity, łącznie z bitem, przypisuje wartość do wyniku.</span><span class="sxs-lookup"><span data-stu-id="f955a-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="f955a-161">Oznacza to, że dla podpisanej liczby dodatnie `Not` zawsze zwraca wartość ujemną i liczb ujemnych `Not` zawsze zwraca wartość dodatnią lub wartość zero.</span><span class="sxs-lookup"><span data-stu-id="f955a-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="f955a-162">`AndAlso` i `OrElse` operatorów nie obsługują operacje bitowe.</span><span class="sxs-lookup"><span data-stu-id="f955a-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="f955a-163">Operacje bitowe można wykonać na tylko w przypadku typów całkowitych.</span><span class="sxs-lookup"><span data-stu-id="f955a-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="f955a-164">Wartości zmiennoprzecinkowe można przekonwertować do typów całkowitych, przed rozpoczęciem operacji na poziomie bitowym.</span><span class="sxs-lookup"><span data-stu-id="f955a-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="f955a-165">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="f955a-165">See also</span></span>

- [<span data-ttu-id="f955a-166">Operatory logiczne/bitowe (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="f955a-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../../visual-basic/language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="f955a-167">Wyrażenia logiczne</span><span class="sxs-lookup"><span data-stu-id="f955a-167">Boolean Expressions</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md)
- [<span data-ttu-id="f955a-168">Operatory arytmetyczne w Visual Basic</span><span class="sxs-lookup"><span data-stu-id="f955a-168">Arithmetic Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/arithmetic-operators.md)
- [<span data-ttu-id="f955a-169">Operatory porównania w języku Visual Basic</span><span class="sxs-lookup"><span data-stu-id="f955a-169">Comparison Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/comparison-operators.md)
- [<span data-ttu-id="f955a-170">Operatory łączenia w języku Visual Basic</span><span class="sxs-lookup"><span data-stu-id="f955a-170">Concatenation Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/concatenation-operators.md)
- [<span data-ttu-id="f955a-171">Skuteczna kombinacja operatorów</span><span class="sxs-lookup"><span data-stu-id="f955a-171">Efficient Combination of Operators</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/efficient-combination-of-operators.md)
