---
title: Co nowego w C# 7.0 - C# Przewodnik
description: Zapoznaj się z omówieniem nowych funkcji w wersji 7.0 języka C#.
ms.date: 02/20/2019
ms.assetid: fd41596d-d0c2-4816-b94d-c4d00a5d0243
ms.openlocfilehash: 1291de95b88b3de16fb94fb376fb4153dd4a5862
ms.sourcegitcommit: 73aa9653547a1cd70ee6586221f79cc29b588ebd
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/23/2020
ms.locfileid: "82102674"
---
# <a name="whats-new-in-c-70"></a><span data-ttu-id="96dc1-103">Co nowego w języku C# 7.0</span><span class="sxs-lookup"><span data-stu-id="96dc1-103">What's new in C# 7.0</span></span>

<span data-ttu-id="96dc1-104">C# 7.0 dodaje szereg nowych funkcji do języka C#:</span><span class="sxs-lookup"><span data-stu-id="96dc1-104">C# 7.0 adds a number of new features to the C# language:</span></span>

- [<span data-ttu-id="96dc1-105">`out`Zmiennych</span><span class="sxs-lookup"><span data-stu-id="96dc1-105">`out` variables</span></span>](#out-variables)
  - <span data-ttu-id="96dc1-106">Wartości można `out` zadeklarować jako argumenty do metody, w której są używane.</span><span class="sxs-lookup"><span data-stu-id="96dc1-106">You can declare `out` values inline as arguments to the method where they're used.</span></span>
- [<span data-ttu-id="96dc1-107">Krotki</span><span class="sxs-lookup"><span data-stu-id="96dc1-107">Tuples</span></span>](#tuples)
  - <span data-ttu-id="96dc1-108">Można utworzyć odciążone, nienazwane typy, które zawierają wiele pól publicznych.</span><span class="sxs-lookup"><span data-stu-id="96dc1-108">You can create lightweight, unnamed types that contain multiple public fields.</span></span> <span data-ttu-id="96dc1-109">Kompilatory i narzędzia IDE zrozumieć semantyki tych typów.</span><span class="sxs-lookup"><span data-stu-id="96dc1-109">Compilers and IDE tools understand the semantics of these types.</span></span>
- [<span data-ttu-id="96dc1-110">Odrzucenia</span><span class="sxs-lookup"><span data-stu-id="96dc1-110">Discards</span></span>](#discards)
  - <span data-ttu-id="96dc1-111">Odrzuty są zmiennymi tymczasowymi, tylko do zapisu używanymi w przydziałach, gdy nie zależy Ci na przypisanej wartości.</span><span class="sxs-lookup"><span data-stu-id="96dc1-111">Discards are temporary, write-only variables used in assignments when you don't care about the value assigned.</span></span> <span data-ttu-id="96dc1-112">Są one najbardziej przydatne podczas dekonstrukcji krotek i typów zdefiniowanych `out` przez użytkownika, a także podczas wywoływania metod z parametrami.</span><span class="sxs-lookup"><span data-stu-id="96dc1-112">They're most useful when deconstructing tuples and user-defined types, as well as when calling methods with `out` parameters.</span></span>
- [<span data-ttu-id="96dc1-113">Dopasowanie wzorca</span><span class="sxs-lookup"><span data-stu-id="96dc1-113">Pattern Matching</span></span>](#pattern-matching)
  - <span data-ttu-id="96dc1-114">Można utworzyć logikę rozgałęzienia na podstawie dowolnych typów i wartości elementów członkowskich tych typów.</span><span class="sxs-lookup"><span data-stu-id="96dc1-114">You can create branching logic based on arbitrary types and values of the members of those types.</span></span>
- [<span data-ttu-id="96dc1-115">`ref`mieszkańców i powrotów</span><span class="sxs-lookup"><span data-stu-id="96dc1-115">`ref` locals and returns</span></span>](#ref-locals-and-returns)
  - <span data-ttu-id="96dc1-116">Metody zmienne lokalne i zwracane wartości mogą być odwołania do innych magazynów.</span><span class="sxs-lookup"><span data-stu-id="96dc1-116">Method local variables and return values can be references to other storage.</span></span>
- [<span data-ttu-id="96dc1-117">Funkcje lokalne</span><span class="sxs-lookup"><span data-stu-id="96dc1-117">Local Functions</span></span>](#local-functions)
  - <span data-ttu-id="96dc1-118">Można zagnieżdżać funkcje wewnątrz innych funkcji, aby ograniczyć ich zakres i widoczność.</span><span class="sxs-lookup"><span data-stu-id="96dc1-118">You can nest functions inside other functions to limit their scope and visibility.</span></span>
- [<span data-ttu-id="96dc1-119">Więcej członków wyrazu</span><span class="sxs-lookup"><span data-stu-id="96dc1-119">More expression-bodied members</span></span>](#more-expression-bodied-members)
  - <span data-ttu-id="96dc1-120">Lista elementów członkowskich, które mogą być autorstwa przy użyciu wyrażeń wzrosła.</span><span class="sxs-lookup"><span data-stu-id="96dc1-120">The list of members that can be authored using expressions has grown.</span></span>
- [<span data-ttu-id="96dc1-121">`throw`Wyrażenia</span><span class="sxs-lookup"><span data-stu-id="96dc1-121">`throw` Expressions</span></span>](#throw-expressions)
  - <span data-ttu-id="96dc1-122">Można zgłaszać wyjątki w konstrukcjach kodu, które `throw` wcześniej nie były dozwolone, ponieważ była instrukcja.</span><span class="sxs-lookup"><span data-stu-id="96dc1-122">You can throw exceptions in code constructs that previously weren't allowed because `throw` was a statement.</span></span>
- [<span data-ttu-id="96dc1-123">Uogólnione typy zwrotów asynchronizowanych</span><span class="sxs-lookup"><span data-stu-id="96dc1-123">Generalized async return types</span></span>](#generalized-async-return-types)
  - <span data-ttu-id="96dc1-124">Metody zadeklarowane `async` za pomocą modyfikatora `Task` `Task<T>`mogą zwracać inne typy oprócz i .</span><span class="sxs-lookup"><span data-stu-id="96dc1-124">Methods declared with the `async` modifier can return other types in addition to `Task` and `Task<T>`.</span></span>
- [<span data-ttu-id="96dc1-125">Ulepszenia składni literału liczbowego</span><span class="sxs-lookup"><span data-stu-id="96dc1-125">Numeric literal syntax improvements</span></span>](#numeric-literal-syntax-improvements)
  - <span data-ttu-id="96dc1-126">Nowe tokeny zwiększają czytelność dla stałych liczbowych.</span><span class="sxs-lookup"><span data-stu-id="96dc1-126">New tokens improve readability for numeric constants.</span></span>

<span data-ttu-id="96dc1-127">Poniżej dalsza część artykułu Zawiera omówienie każdej funkcji.</span><span class="sxs-lookup"><span data-stu-id="96dc1-127">The remainder of this article provides an overview of each feature.</span></span> <span data-ttu-id="96dc1-128">Dla każdej funkcji, dowiesz się rozumowanie za nim.</span><span class="sxs-lookup"><span data-stu-id="96dc1-128">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="96dc1-129">Nauczysz się składni.</span><span class="sxs-lookup"><span data-stu-id="96dc1-129">You'll learn the syntax.</span></span> <span data-ttu-id="96dc1-130">Za pomocą narzędzia globalnego można `dotnet try` eksplorować te funkcje w swoim środowisku:</span><span class="sxs-lookup"><span data-stu-id="96dc1-130">You can explore these features in your environment using the `dotnet try` global tool:</span></span>

1. <span data-ttu-id="96dc1-131">Zainstaluj narzędzie globalne [dotnet-try.](https://github.com/dotnet/try/blob/master/README.md#setup)</span><span class="sxs-lookup"><span data-stu-id="96dc1-131">Install the [dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) global tool.</span></span>
1. <span data-ttu-id="96dc1-132">Sklonuj repozytorium [dotnet/try-samples.](https://github.com/dotnet/try-samples)</span><span class="sxs-lookup"><span data-stu-id="96dc1-132">Clone the [dotnet/try-samples](https://github.com/dotnet/try-samples) repository.</span></span>
1. <span data-ttu-id="96dc1-133">Ustaw bieżący katalog na podkatalog *csharp7* dla repozytorium *try-samples.*</span><span class="sxs-lookup"><span data-stu-id="96dc1-133">Set the current directory to the *csharp7* subdirectory for the *try-samples* repository.</span></span>
1. <span data-ttu-id="96dc1-134">Uruchom polecenie `dotnet try`.</span><span class="sxs-lookup"><span data-stu-id="96dc1-134">Run `dotnet try`.</span></span>

## <a name="out-variables"></a><span data-ttu-id="96dc1-135">`out`Zmiennych</span><span class="sxs-lookup"><span data-stu-id="96dc1-135">`out` variables</span></span>

<span data-ttu-id="96dc1-136">W tej wersji ulepszono istniejącą składnię obsługującą `out` parametry.</span><span class="sxs-lookup"><span data-stu-id="96dc1-136">The existing syntax that supports `out` parameters has been improved in this version.</span></span> <span data-ttu-id="96dc1-137">Teraz można `out` zadeklarować zmienne na liście argumentów wywołania metody, zamiast pisać oddzielną instrukcję deklaracji:</span><span class="sxs-lookup"><span data-stu-id="96dc1-137">You can now declare `out` variables in the argument list of a method call, rather than writing a separate declaration statement:</span></span>

[!code-csharp[OutVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVariableDeclarations "Out variable declarations")]

<span data-ttu-id="96dc1-138">Można określić typ zmiennej `out` dla jasności, jak pokazano powyżej.</span><span class="sxs-lookup"><span data-stu-id="96dc1-138">You may want to specify the type of the `out` variable for clarity, as shown above.</span></span> <span data-ttu-id="96dc1-139">Jednak język obsługuje przy użyciu niejawnie wpisanej zmiennej lokalnej:</span><span class="sxs-lookup"><span data-stu-id="96dc1-139">However, the language does support using an implicitly typed local variable:</span></span>

[!code-csharp[OutVarVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVarVariableDeclarations "Implicitly typed Out variable")]

- <span data-ttu-id="96dc1-140">Kod jest łatwiejszy do odczytania.</span><span class="sxs-lookup"><span data-stu-id="96dc1-140">The code is easier to read.</span></span>
  - <span data-ttu-id="96dc1-141">Deklarujesz zmienną out, w której jej używasz, a nie w innym wierszu powyżej.</span><span class="sxs-lookup"><span data-stu-id="96dc1-141">You declare the out variable where you use it, not on another line above.</span></span>
- <span data-ttu-id="96dc1-142">Nie ma potrzeby przypisywania wartości początkowej.</span><span class="sxs-lookup"><span data-stu-id="96dc1-142">No need to assign an initial value.</span></span>
  - <span data-ttu-id="96dc1-143">Deklarując zmienną, `out` gdzie jest używana w wywołaniu metody, nie można przypadkowo użyć go przed przypisaniem.</span><span class="sxs-lookup"><span data-stu-id="96dc1-143">By declaring the `out` variable where it's used in a method call, you can't accidentally use it before it is assigned.</span></span>

## <a name="tuples"></a><span data-ttu-id="96dc1-144">Krotki</span><span class="sxs-lookup"><span data-stu-id="96dc1-144">Tuples</span></span>

<span data-ttu-id="96dc1-145">C# zawiera bogatą składnię dla klas i struktur, który jest używany do wyjaśnienia intencji projektu.</span><span class="sxs-lookup"><span data-stu-id="96dc1-145">C# provides a rich syntax for classes and structs that is used to explain your design intent.</span></span> <span data-ttu-id="96dc1-146">Ale czasami ta bogata składnia wymaga dodatkowej pracy przy minimalnych korzyściach.</span><span class="sxs-lookup"><span data-stu-id="96dc1-146">But sometimes that rich syntax requires extra work with minimal benefit.</span></span> <span data-ttu-id="96dc1-147">Często można napisać metody, które wymagają prostej struktury zawierającej więcej niż jeden element danych.</span><span class="sxs-lookup"><span data-stu-id="96dc1-147">You may often write methods that need a simple structure containing more than one data element.</span></span> <span data-ttu-id="96dc1-148">Aby obsługiwać te scenariusze *krotki* zostały dodane do języka C#.</span><span class="sxs-lookup"><span data-stu-id="96dc1-148">To support these scenarios *tuples* were added to C#.</span></span> <span data-ttu-id="96dc1-149">Krotek są odciążone struktury danych, które zawierają wiele pól do reprezentowania elementów członkowskich danych.</span><span class="sxs-lookup"><span data-stu-id="96dc1-149">Tuples are lightweight data structures that contain multiple fields to represent the data members.</span></span>
<span data-ttu-id="96dc1-150">Pola nie są sprawdzane i nie można definiować własnych metod</span><span class="sxs-lookup"><span data-stu-id="96dc1-150">The fields aren't validated, and you can't define your own methods</span></span>

> [!NOTE]
> <span data-ttu-id="96dc1-151">Krotek były dostępne przed C# 7.0, ale były nieefektywne i nie miał obsługi języka.</span><span class="sxs-lookup"><span data-stu-id="96dc1-151">Tuples were available before C# 7.0, but they were inefficient and had no language support.</span></span>
> <span data-ttu-id="96dc1-152">Oznaczało to, że elementy krotki `Item1`można `Item2` było odwoływać się tylko jako , i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="96dc1-152">This meant that tuple elements could only be referenced as `Item1`, `Item2` and so on.</span></span> <span data-ttu-id="96dc1-153">C# 7.0 wprowadza obsługę języka krotek, która umożliwia semantyczne nazwy dla pól krotki przy użyciu nowych, bardziej wydajnych typów krotki.</span><span class="sxs-lookup"><span data-stu-id="96dc1-153">C# 7.0 introduces language support for tuples, which enables semantic names for the fields of a tuple using new, more efficient tuple types.</span></span>

<span data-ttu-id="96dc1-154">Kroli można utworzyć, przypisując wartość do każdego elementu członkowskiego i opcjonalnie podając nazwy semantyczne do każdego z elementów członkowskich krotki:</span><span class="sxs-lookup"><span data-stu-id="96dc1-154">You can create a tuple by assigning a value to each member, and optionally providing semantic names to each of the members of the tuple:</span></span>

[!code-csharp[NamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#NamedTuple "Named tuple")]

<span data-ttu-id="96dc1-155">Krotka `namedLetters` zawiera pola określane `Alpha` `Beta`jako .</span><span class="sxs-lookup"><span data-stu-id="96dc1-155">The `namedLetters` tuple contains fields referred to as `Alpha` and `Beta`.</span></span> <span data-ttu-id="96dc1-156">Te nazwy istnieją tylko w czasie kompilacji i nie są zachowywane, na przykład podczas sprawdzania krotki przy użyciu odbicia w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="96dc1-156">Those names exist only at compile time and aren't preserved, for example when inspecting the tuple using reflection at runtime.</span></span>

<span data-ttu-id="96dc1-157">W przypisaniu krotki można również określić nazwy pól po prawej stronie przydziału:</span><span class="sxs-lookup"><span data-stu-id="96dc1-157">In a tuple assignment, you can also specify the names of the fields on the right-hand side of the assignment:</span></span>

[!code-csharp[ImplicitNamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#ImplicitNamedTuple "Implicitly named tuple")]

<span data-ttu-id="96dc1-158">Może się okazać, że chcesz rozpakować elementy członkowskie krotki, które zostały zwrócone z metody.</span><span class="sxs-lookup"><span data-stu-id="96dc1-158">There may be times when you want to unpackage the members of a tuple that were returned from a method.</span></span>  <span data-ttu-id="96dc1-159">Można to zrobić, deklarując oddzielne zmienne dla każdej z wartości w krocie.</span><span class="sxs-lookup"><span data-stu-id="96dc1-159">You can do that by declaring separate variables for each of the values in the tuple.</span></span> <span data-ttu-id="96dc1-160">To rozpakowanie jest nazywane *dekonstrukcji* krotki:</span><span class="sxs-lookup"><span data-stu-id="96dc1-160">This unpackaging is called *deconstructing* the tuple:</span></span>

[!code-csharp[CallingWithDeconstructor](~/samples/snippets/csharp/new-in-7/program.cs#CallingWithDeconstructor "Deconstructing a tuple")]

<span data-ttu-id="96dc1-161">Można również podać podobną dekonstrukcję dla dowolnego typu w .NET.</span><span class="sxs-lookup"><span data-stu-id="96dc1-161">You can also provide a similar deconstruction for any type in .NET.</span></span> <span data-ttu-id="96dc1-162">Piszesz `Deconstruct` metodę jako członek klasy.</span><span class="sxs-lookup"><span data-stu-id="96dc1-162">You write a `Deconstruct` method as a member of the class.</span></span> <span data-ttu-id="96dc1-163">Ta `Deconstruct` metoda zawiera `out` zestaw argumentów dla każdej właściwości, które chcesz wyodrębnić.</span><span class="sxs-lookup"><span data-stu-id="96dc1-163">That `Deconstruct` method provides a set of `out` arguments for each of the properties you want to extract.</span></span> <span data-ttu-id="96dc1-164">Należy `Point` wziąć pod uwagę tę klasę, która `X` zapewnia `Y` metodę dekonstruktora, która wyodrębnia i współrzędne:</span><span class="sxs-lookup"><span data-stu-id="96dc1-164">Consider this `Point` class that provides a deconstructor method that extracts the `X` and `Y` coordinates:</span></span>

[!code-csharp[PointWithDeconstruction](~/samples/snippets/csharp/new-in-7/point.cs#PointWithDeconstruction "Point with deconstruction method")]

<span data-ttu-id="96dc1-165">Poszczególne pola można wyodrębnić, `Point` przypisując je do krotki:</span><span class="sxs-lookup"><span data-stu-id="96dc1-165">You can extract the individual fields by assigning a `Point` to a tuple:</span></span>

[!code-csharp[DeconstructPoint](~/samples/snippets/csharp/new-in-7/program.cs#DeconstructPoint "Deconstruct a point")]

<span data-ttu-id="96dc1-166">Więcej informacji na temat krotek można dowiedzieć się więcej na temat krotek w [artykule krotek](../tuples.md).</span><span class="sxs-lookup"><span data-stu-id="96dc1-166">You can learn more in depth about tuples in the [tuples article](../tuples.md).</span></span>

## <a name="discards"></a><span data-ttu-id="96dc1-167">Odrzucenia</span><span class="sxs-lookup"><span data-stu-id="96dc1-167">Discards</span></span>

<span data-ttu-id="96dc1-168">Często podczas dekonstrukcji krotki lub `out` wywoływania metody z parametrami, jesteś zmuszony do zdefiniowania zmiennej, której wartość nie dbasz o i nie zamierza używać.</span><span class="sxs-lookup"><span data-stu-id="96dc1-168">Often when deconstructing a tuple or calling a method with `out` parameters, you're forced to define a variable whose value you don't care about and don't intend to use.</span></span> <span data-ttu-id="96dc1-169">C# dodaje obsługę *odrzuceń* do obsługi tego scenariusza.</span><span class="sxs-lookup"><span data-stu-id="96dc1-169">C# adds support for *discards* to handle this scenario.</span></span> <span data-ttu-id="96dc1-170">Odrzucenie jest zmienną tylko do `_` zapisu, której nazwa jest (znak podkreślenia); można przypisać wszystkie wartości, które mają zostać odrzucone do pojedynczej zmiennej.</span><span class="sxs-lookup"><span data-stu-id="96dc1-170">A discard is a write-only variable whose name is `_` (the underscore character); you can assign all of the values that you intend to discard to the single variable.</span></span> <span data-ttu-id="96dc1-171">Odrzucenie jest jak nieprzypisane zmiennej; oprócz instrukcji przypisania, odrzucenia nie można użyć w kodzie.</span><span class="sxs-lookup"><span data-stu-id="96dc1-171">A discard is like an unassigned variable; apart from the assignment statement, the discard can't be used in code.</span></span>

<span data-ttu-id="96dc1-172">Odrzuty są obsługiwane w następujących scenariuszach:</span><span class="sxs-lookup"><span data-stu-id="96dc1-172">Discards are supported in the following scenarios:</span></span>

- <span data-ttu-id="96dc1-173">Podczas dekonstrukcji krotek lub typów zdefiniowanych przez użytkownika.</span><span class="sxs-lookup"><span data-stu-id="96dc1-173">When deconstructing tuples or user-defined types.</span></span>
- <span data-ttu-id="96dc1-174">Podczas wywoływania metod z parametrami [out.](../language-reference/keywords/out-parameter-modifier.md)</span><span class="sxs-lookup"><span data-stu-id="96dc1-174">When calling methods with [out](../language-reference/keywords/out-parameter-modifier.md) parameters.</span></span>
- <span data-ttu-id="96dc1-175">W operacji dopasowywania wzorca z [instrukcjami is](../language-reference/keywords/is.md) i [switch.](../language-reference/keywords/switch.md)</span><span class="sxs-lookup"><span data-stu-id="96dc1-175">In a pattern matching operation with the [is](../language-reference/keywords/is.md) and [switch](../language-reference/keywords/switch.md) statements.</span></span>
- <span data-ttu-id="96dc1-176">Jako samodzielny identyfikator, gdy chcesz jawnie zidentyfikować wartość przypisania jako odrzucenie.</span><span class="sxs-lookup"><span data-stu-id="96dc1-176">As a standalone identifier when you want to explicitly identify the value of an assignment as a discard.</span></span>

<span data-ttu-id="96dc1-177">Poniższy przykład definiuje `QueryCityDataForYears` metodę, która zwraca 6-krotki, która zawiera dane dla miasta dla dwóch różnych lat.</span><span class="sxs-lookup"><span data-stu-id="96dc1-177">The following example defines a `QueryCityDataForYears` method that returns a 6-tuple that contains data for a city for two different years.</span></span> <span data-ttu-id="96dc1-178">Wywołanie metody w przykładzie dotyczy tylko dwóch wartości populacji zwracanych przez metodę i dlatego traktuje pozostałe wartości w krocie jako odrzucane, gdy dekonstruuje krotki.</span><span class="sxs-lookup"><span data-stu-id="96dc1-178">The method call in the example is concerned only with the two population values returned by the method and so treats the remaining values in the tuple as discards when it deconstructs the tuple.</span></span>

[!code-csharp[Tuple-discard](~/samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="96dc1-179">Aby uzyskać więcej informacji, zobacz [Odrzuca](../discards.md).</span><span class="sxs-lookup"><span data-stu-id="96dc1-179">For more information, see [Discards](../discards.md).</span></span>

## <a name="pattern-matching"></a><span data-ttu-id="96dc1-180">Dopasowanie do wzorca</span><span class="sxs-lookup"><span data-stu-id="96dc1-180">Pattern matching</span></span>

<span data-ttu-id="96dc1-181">*Dopasowywanie wzorca* jest funkcją, która umożliwia implementowanie wysyłki metody na właściwości innych niż typ obiektu.</span><span class="sxs-lookup"><span data-stu-id="96dc1-181">*Pattern matching* is a feature that allows you to implement method dispatch on properties other than the type of an object.</span></span> <span data-ttu-id="96dc1-182">Prawdopodobnie jesteś już zaznajomiony z wywoływania metody na podstawie typu obiektu.</span><span class="sxs-lookup"><span data-stu-id="96dc1-182">You're probably already familiar with method dispatch based on the type of an object.</span></span> <span data-ttu-id="96dc1-183">W programowaniu obiektowym metody wirtualne i zastępowanie zapewniają składnię języka w celu zaimplementowania dyspozycyjności metody na podstawie typu obiektu.</span><span class="sxs-lookup"><span data-stu-id="96dc1-183">In object-oriented programming, virtual and override methods provide language syntax to implement method dispatching based on an object's type.</span></span> <span data-ttu-id="96dc1-184">Klasy podstawowe i pochodne zapewniają różne implementacje.</span><span class="sxs-lookup"><span data-stu-id="96dc1-184">Base and Derived classes provide different implementations.</span></span>
<span data-ttu-id="96dc1-185">Wyrażenia dopasowywania wzorców rozszerzają tę koncepcję, dzięki czemu można łatwo zaimplementować podobne wzorce wysyłki dla typów i elementów danych, które nie są powiązane za pośrednictwem hierarchii dziedziczenia.</span><span class="sxs-lookup"><span data-stu-id="96dc1-185">Pattern matching expressions extend this concept so that you can easily implement similar dispatch patterns for types and data elements that aren't related through an inheritance hierarchy.</span></span>

<span data-ttu-id="96dc1-186">Dopasowywanie wzorców obsługuje `is` wyrażenia i `switch` wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="96dc1-186">Pattern matching supports `is` expressions and `switch` expressions.</span></span> <span data-ttu-id="96dc1-187">Każdy umożliwia sprawdzanie obiektu i jego właściwości, aby ustalić, czy ten obiekt spełnia żądany wzorzec.</span><span class="sxs-lookup"><span data-stu-id="96dc1-187">Each enables inspecting an object and its properties to determine if that object satisfies the sought pattern.</span></span> <span data-ttu-id="96dc1-188">Użyj słowa `when` kluczowego, aby określić dodatkowe reguły wzorca.</span><span class="sxs-lookup"><span data-stu-id="96dc1-188">You use the `when` keyword to specify additional rules to the pattern.</span></span>

<span data-ttu-id="96dc1-189">Wyrażenie `is` wzorca rozszerza [ `is` ](../language-reference/keywords/is.md#pattern-matching-with-is) znanego operatora do kwerendy obiektu o jego typ i przypisać wynik w jednej instrukcji.</span><span class="sxs-lookup"><span data-stu-id="96dc1-189">The `is` pattern expression extends the familiar [`is` operator](../language-reference/keywords/is.md#pattern-matching-with-is) to query an object about its type and assign the result in one instruction.</span></span> <span data-ttu-id="96dc1-190">Poniższy kod sprawdza, czy `int`zmienna jest , a jeśli tak, dodaje ją do bieżącej sumy:</span><span class="sxs-lookup"><span data-stu-id="96dc1-190">The following code checks if a variable is an `int`, and if so, adds it to the current sum:</span></span>

```csharp
if (input is int count)
    sum += count;
```

<span data-ttu-id="96dc1-191">W poprzednim małym przykładzie przedstawiono `is` ulepszenia wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="96dc1-191">The preceding small example demonstrates the enhancements to the `is` expression.</span></span> <span data-ttu-id="96dc1-192">Można przetestować na typy wartości, jak również typy odwołań i można przypisać wynik pomyślny do nowej zmiennej o poprawnym typie.</span><span class="sxs-lookup"><span data-stu-id="96dc1-192">You can test against value types as well as reference types, and you can assign the successful result to a new variable of the correct type.</span></span>

<span data-ttu-id="96dc1-193">Wyrażenie dopasowania przełącznika ma znaną składnię, opartą `switch` na instrukcji już części języka C#.</span><span class="sxs-lookup"><span data-stu-id="96dc1-193">The switch match expression has a familiar syntax, based on the `switch` statement already part of the C# language.</span></span> <span data-ttu-id="96dc1-194">Zaktualizowana instrukcja switch ma kilka nowych konstrukcji:</span><span class="sxs-lookup"><span data-stu-id="96dc1-194">The updated switch statement has several new constructs:</span></span>

- <span data-ttu-id="96dc1-195">Typ określania `switch` wartości wyrażeń nie jest `Enum` już `string`ograniczony do typów, typów lub typu nullable odpowiadającego jednemu z tych typów.</span><span class="sxs-lookup"><span data-stu-id="96dc1-195">The governing type of a `switch` expression is no longer restricted to integral types, `Enum` types, `string`, or a nullable type corresponding to one of those types.</span></span> <span data-ttu-id="96dc1-196">Można użyć dowolnego typu.</span><span class="sxs-lookup"><span data-stu-id="96dc1-196">Any type may be used.</span></span>
- <span data-ttu-id="96dc1-197">Typ `switch` wyrażenia można przetestować w `case` każdej etykiecie.</span><span class="sxs-lookup"><span data-stu-id="96dc1-197">You can test the type of the `switch` expression in each `case` label.</span></span> <span data-ttu-id="96dc1-198">Podobnie jak `is` w odniesieniu do wyrażenia, można przypisać nową zmienną do tego typu.</span><span class="sxs-lookup"><span data-stu-id="96dc1-198">As with the `is` expression, you may assign a new variable to that type.</span></span>
- <span data-ttu-id="96dc1-199">Można dodać `when` klauzulę do dalszych warunków testowych dla tej zmiennej.</span><span class="sxs-lookup"><span data-stu-id="96dc1-199">You may add a `when` clause to further test conditions on that variable.</span></span>
- <span data-ttu-id="96dc1-200">Kolejność `case` etykiet jest teraz ważna.</span><span class="sxs-lookup"><span data-stu-id="96dc1-200">The order of `case` labels is now important.</span></span> <span data-ttu-id="96dc1-201">Pierwsza gałąź do dopasowania jest wykonywana; inne są pomijane.</span><span class="sxs-lookup"><span data-stu-id="96dc1-201">The first branch to match is executed; others are skipped.</span></span>

<span data-ttu-id="96dc1-202">Poniższy kod demonstruje te nowe funkcje:</span><span class="sxs-lookup"><span data-stu-id="96dc1-202">The following code demonstrates these new features:</span></span>

```csharp
public static int SumPositiveNumbers(IEnumerable<object> sequence)
{
    int sum = 0;
    foreach (var i in sequence)
    {
        switch (i)
        {
            case 0:
                break;
            case IEnumerable<int> childSequence:
            {
                foreach(var item in childSequence)
                    sum += (item > 0) ? item : 0;
                break;
            }
            case int n when n > 0:
                sum += n;
                break;
            case null:
                throw new NullReferenceException("Null found in sequence");
            default:
                throw new InvalidOperationException("Unrecognized type");
        }
    }
    return sum;
}
```

- <span data-ttu-id="96dc1-203">`case 0:`jest znanym stałym wzorem.</span><span class="sxs-lookup"><span data-stu-id="96dc1-203">`case 0:` is the familiar constant pattern.</span></span>
- <span data-ttu-id="96dc1-204">`case IEnumerable<int> childSequence:`jest wzorcem typu.</span><span class="sxs-lookup"><span data-stu-id="96dc1-204">`case IEnumerable<int> childSequence:` is a type pattern.</span></span>
- <span data-ttu-id="96dc1-205">`case int n when n > 0:`jest wzorem typu `when` z dodatkowym warunkiem.</span><span class="sxs-lookup"><span data-stu-id="96dc1-205">`case int n when n > 0:` is a type pattern with an additional `when` condition.</span></span>
- <span data-ttu-id="96dc1-206">`case null:`jest wzorcem null.</span><span class="sxs-lookup"><span data-stu-id="96dc1-206">`case null:` is the null pattern.</span></span>
- <span data-ttu-id="96dc1-207">`default:`to znana domyślna obudowa.</span><span class="sxs-lookup"><span data-stu-id="96dc1-207">`default:` is the familiar default case.</span></span>

<span data-ttu-id="96dc1-208">Więcej informacji na temat dopasowywania wzorców można dowiedzieć się w języku [C# .You](../pattern-matching.md)can learn more about pattern matching in Pattern Matching in C# .</span><span class="sxs-lookup"><span data-stu-id="96dc1-208">You can learn more about pattern matching in [Pattern Matching in C#](../pattern-matching.md).</span></span>

## <a name="ref-locals-and-returns"></a><span data-ttu-id="96dc1-209">Ref mieszkańców i zwraca</span><span class="sxs-lookup"><span data-stu-id="96dc1-209">Ref locals and returns</span></span>

<span data-ttu-id="96dc1-210">Ta funkcja umożliwia algorytmy, które używają i zwracają odwołania do zmiennych zdefiniowanych w innym miejscu.</span><span class="sxs-lookup"><span data-stu-id="96dc1-210">This feature enables algorithms that use and return references to variables defined elsewhere.</span></span> <span data-ttu-id="96dc1-211">Jednym z przykładów jest praca z dużymi macierzy i znalezienie jednej lokalizacji z pewnymi cechami.</span><span class="sxs-lookup"><span data-stu-id="96dc1-211">One example is working with large matrices, and finding a single location with certain characteristics.</span></span> <span data-ttu-id="96dc1-212">Następująca metoda zwraca **odwołanie** do tego magazynu w macierzy:</span><span class="sxs-lookup"><span data-stu-id="96dc1-212">The following method returns a **reference** to that storage in the matrix:</span></span>

[!code-csharp[FindReturningRef](~/samples/snippets/csharp/new-in-7/MatrixSearch.cs#FindReturningRef "Find returning by reference")]

<span data-ttu-id="96dc1-213">Można zadeklarować wartość zwracaną `ref` jako i zmodyfikować tę wartość w macierzy, jak pokazano w poniższym kodzie:</span><span class="sxs-lookup"><span data-stu-id="96dc1-213">You can declare the return value as a `ref` and modify that value in the matrix, as shown in the following code:</span></span>

[!code-csharp[AssignRefReturn](~/samples/snippets/csharp/new-in-7/Program.cs#AssignRefReturn "Assign ref return")]

<span data-ttu-id="96dc1-214">Język C# ma kilka reguł, które chronią przed nadużywaniem `ref` mieszkańców i zwraca:</span><span class="sxs-lookup"><span data-stu-id="96dc1-214">The C# language has several rules that protect you from misusing the `ref` locals and returns:</span></span>

- <span data-ttu-id="96dc1-215">Należy dodać `ref` słowo kluczowe do podpisu `return` metody i do wszystkich instrukcji w metodzie.</span><span class="sxs-lookup"><span data-stu-id="96dc1-215">You must add the `ref` keyword to the method signature and to all `return` statements in a method.</span></span>
  - <span data-ttu-id="96dc1-216">To sprawia, że jasne, że metoda zwraca przez odwołanie w całej metody.</span><span class="sxs-lookup"><span data-stu-id="96dc1-216">That makes it clear the method returns by reference throughout the method.</span></span>
- <span data-ttu-id="96dc1-217">A `ref return` mogą być przypisane do zmiennej wartości lub zmiennej. `ref`</span><span class="sxs-lookup"><span data-stu-id="96dc1-217">A `ref return` may be assigned to a value variable, or a `ref` variable.</span></span>
  - <span data-ttu-id="96dc1-218">Wywołujący określa, czy zwracana wartość jest kopiowana, czy nie.</span><span class="sxs-lookup"><span data-stu-id="96dc1-218">The caller controls whether the return value is copied or not.</span></span> <span data-ttu-id="96dc1-219">Pominięcie modyfikatora `ref` podczas przypisywania zwracanej wartości wskazuje, że wywołujący chce kopię wartości, a nie odwołanie do magazynu.</span><span class="sxs-lookup"><span data-stu-id="96dc1-219">Omitting the `ref` modifier when assigning the return value indicates that the caller wants a copy of the value, not a reference to the storage.</span></span>
- <span data-ttu-id="96dc1-220">Nie można przypisać standardowej wartości zwracanej metody do zmiennej `ref` lokalnej.</span><span class="sxs-lookup"><span data-stu-id="96dc1-220">You can't assign a standard method return value to a `ref` local variable.</span></span>
  - <span data-ttu-id="96dc1-221">To nie zezwala na stwierdzenia takie jak`ref int i = sequence.Count();`</span><span class="sxs-lookup"><span data-stu-id="96dc1-221">That disallows statements like `ref int i = sequence.Count();`</span></span>
- <span data-ttu-id="96dc1-222">Nie można zwrócić `ref` do zmiennej, której okres istnienia nie wykracza poza wykonanie metody.</span><span class="sxs-lookup"><span data-stu-id="96dc1-222">You can't return a `ref` to a variable whose lifetime doesn't extend beyond the execution of the method.</span></span>
  - <span data-ttu-id="96dc1-223">Oznacza to, że nie można zwrócić odwołania do zmiennej lokalnej lub zmiennej o podobnym zakresie.</span><span class="sxs-lookup"><span data-stu-id="96dc1-223">That means you can't return a reference to a local variable or a variable with a similar scope.</span></span>
- <span data-ttu-id="96dc1-224">`ref`miejscowych i zwraca nie mogą być używane z metodami asynchronizowymi.</span><span class="sxs-lookup"><span data-stu-id="96dc1-224">`ref` locals and returns can't be used with async methods.</span></span>
  - <span data-ttu-id="96dc1-225">Kompilator nie może wiedzieć, czy zmienna, do którego istnieje odwołanie, została ustawiona na wartość końcową, gdy zwracana jest metoda asynchronizowana.</span><span class="sxs-lookup"><span data-stu-id="96dc1-225">The compiler can't know if the referenced variable has been set to its final value when the async method returns.</span></span>

<span data-ttu-id="96dc1-226">Dodanie ref locals i ref zwraca umożliwia algorytmy, które są bardziej wydajne, unikając kopiowania wartości lub wykonywania operacji dereferencing wiele razy.</span><span class="sxs-lookup"><span data-stu-id="96dc1-226">The addition of ref locals and ref returns enables algorithms that are more efficient by avoiding copying values, or performing dereferencing operations multiple times.</span></span>

<span data-ttu-id="96dc1-227">Dodanie `ref` do wartości zwracanej jest [zmianą zgodną ze źródłem](version-update-considerations.md#source-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="96dc1-227">Adding `ref` to the return value is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span> <span data-ttu-id="96dc1-228">Istniejący kod kompiluje, ale wartość zwracana ref jest kopiowana po przypisaniu.</span><span class="sxs-lookup"><span data-stu-id="96dc1-228">Existing code compiles, but the ref return value is copied when assigned.</span></span> <span data-ttu-id="96dc1-229">Wywołujący muszą zaktualizować magazyn dla wartości `ref` zwracanej do zmiennej lokalnej do przechowywania zwracanego jako odwołania.</span><span class="sxs-lookup"><span data-stu-id="96dc1-229">Callers must update the storage for the return value to a `ref` local variable to store the return as a reference.</span></span>

<span data-ttu-id="96dc1-230">Aby uzyskać więcej informacji, zobacz artykuł [słowiowa ref.](../language-reference/keywords/ref.md)</span><span class="sxs-lookup"><span data-stu-id="96dc1-230">For more information, see the [ref keyword](../language-reference/keywords/ref.md) article.</span></span>

## <a name="local-functions"></a><span data-ttu-id="96dc1-231">Funkcje lokalne</span><span class="sxs-lookup"><span data-stu-id="96dc1-231">Local functions</span></span>

<span data-ttu-id="96dc1-232">Wiele projektów dla klas zawiera metody, które są wywoływane tylko z jednej lokalizacji.</span><span class="sxs-lookup"><span data-stu-id="96dc1-232">Many designs for classes include methods that are called from only one location.</span></span> <span data-ttu-id="96dc1-233">Te dodatkowe metody prywatne zachować każdą metodę małe i skoncentrowane.</span><span class="sxs-lookup"><span data-stu-id="96dc1-233">These additional private methods keep each method small and focused.</span></span> <span data-ttu-id="96dc1-234">*Funkcje lokalne* umożliwiają deklarowanie metod w kontekście innej metody.</span><span class="sxs-lookup"><span data-stu-id="96dc1-234">*Local functions* enable you to declare methods inside the context of another method.</span></span> <span data-ttu-id="96dc1-235">Funkcje lokalne ułatwiają czytelnikom klasy, aby zobaczyć, że metoda lokalna jest wywoływana tylko z kontekstu, w którym jest zadeklarowany.</span><span class="sxs-lookup"><span data-stu-id="96dc1-235">Local functions make it easier for readers of the class to see that the local method is only called from the context in which it is declared.</span></span>

<span data-ttu-id="96dc1-236">Istnieją dwa typowe przypadki użycia dla funkcji lokalnych: metody iteratora publicznego i publiczne metody asynchronizowania.</span><span class="sxs-lookup"><span data-stu-id="96dc1-236">There are two common use cases for local functions: public iterator methods and public async methods.</span></span> <span data-ttu-id="96dc1-237">Oba typy metod generują kod, który zgłasza błędy później niż programiści mogą oczekiwać.</span><span class="sxs-lookup"><span data-stu-id="96dc1-237">Both types of methods generate code that reports errors later than programmers might expect.</span></span> <span data-ttu-id="96dc1-238">W metodach iteratora wszelkie wyjątki są obserwowane tylko podczas wywoływania kodu, który wylicza zwróconą sekwencję.</span><span class="sxs-lookup"><span data-stu-id="96dc1-238">In iterator methods, any exceptions are observed only when calling code that enumerates the returned sequence.</span></span> <span data-ttu-id="96dc1-239">W metodach asynchronizowych wszelkie wyjątki są obserwowane tylko wtedy, gdy zwracany `Task` jest oczekiwany.</span><span class="sxs-lookup"><span data-stu-id="96dc1-239">In async methods, any exceptions are only observed when the returned `Task` is awaited.</span></span> <span data-ttu-id="96dc1-240">Poniższy przykład pokazuje oddzielenie sprawdzania poprawności parametru z implementacji iteratora przy użyciu funkcji lokalnej:</span><span class="sxs-lookup"><span data-stu-id="96dc1-240">The following example demonstrates separating parameter validation from the iterator implementation using a local function:</span></span>

[!code-csharp[22_IteratorMethodLocal](~/samples/snippets/csharp/new-in-7/Iterator.cs#IteratorMethodLocal "Iterator method with local function")]

<span data-ttu-id="96dc1-241">Tę samą technikę `async` można zastosować za pomocą metod, aby upewnić się, że wyjątki wynikające z sprawdzania poprawności argumentów są generowane przed rozpoczęciem pracy asynchronicznej:</span><span class="sxs-lookup"><span data-stu-id="96dc1-241">The same technique can be employed with `async` methods to ensure that exceptions arising from argument validation are thrown before the asynchronous work begins:</span></span>

[!code-csharp[TaskExample](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#TaskExample "Task returning method with local function")]

> [!NOTE]
> <span data-ttu-id="96dc1-242">Niektóre projekty, które są obsługiwane przez funkcje lokalne można również wykonać za pomocą *wyrażeń lambda*.</span><span class="sxs-lookup"><span data-stu-id="96dc1-242">Some of the designs that are supported by local functions can also be accomplished using *lambda expressions*.</span></span> <span data-ttu-id="96dc1-243">Aby uzyskać więcej informacji, zobacz [Funkcje lokalne a wyrażenia lambda](../programming-guide/classes-and-structs/local-functions.md#local-functions-vs-lambda-expressions).</span><span class="sxs-lookup"><span data-stu-id="96dc1-243">For more information, see [Local functions vs. lambda expressions](../programming-guide/classes-and-structs/local-functions.md#local-functions-vs-lambda-expressions).</span></span>

## <a name="more-expression-bodied-members"></a><span data-ttu-id="96dc1-244">Więcej członków wyrazu</span><span class="sxs-lookup"><span data-stu-id="96dc1-244">More expression-bodied members</span></span>

<span data-ttu-id="96dc1-245">C# 6 wprowadzono [elementy członkowskie zabudowane wyrażenia](csharp-6.md#expression-bodied-function-members) dla funkcji członkowskich i właściwości tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="96dc1-245">C# 6 introduced [expression-bodied members](csharp-6.md#expression-bodied-function-members) for member functions, and read-only properties.</span></span> <span data-ttu-id="96dc1-246">C# 7.0 rozszerza dozwolone elementy członkowskie, które mogą być implementowane jako wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="96dc1-246">C# 7.0 expands the allowed members that can be implemented as expressions.</span></span> <span data-ttu-id="96dc1-247">W języku C# 7.0 można zaimplementować `set` *konstruktory,* *finalizatory* `get` i akcesory we *właściwościach* i *indeksatorach.*</span><span class="sxs-lookup"><span data-stu-id="96dc1-247">In C# 7.0, you can implement *constructors*, *finalizers*, and `get` and `set` accessors on *properties* and *indexers*.</span></span> <span data-ttu-id="96dc1-248">Poniższy kod przedstawia przykłady każdego z nich:</span><span class="sxs-lookup"><span data-stu-id="96dc1-248">The following code shows examples of each:</span></span>

[!code-csharp[ExpressionBodiedMembers](~/samples/snippets/csharp/new-in-7/expressionmembers.cs#ExpressionBodiedEverything "new expression-bodied members")]

> [!NOTE]
> <span data-ttu-id="96dc1-249">W tym przykładzie nie wymaga finalizatora, ale jest wyświetlany, aby zademonstrować składnię.</span><span class="sxs-lookup"><span data-stu-id="96dc1-249">This example does not need a finalizer, but it is shown to demonstrate the syntax.</span></span> <span data-ttu-id="96dc1-250">Nie należy implementować finalizatora w klasie, chyba że jest to konieczne do uwolnienia zasobów niezarządzanych.</span><span class="sxs-lookup"><span data-stu-id="96dc1-250">You should not implement a finalizer in your class unless it is necessary to  release unmanaged resources.</span></span> <span data-ttu-id="96dc1-251">Należy również rozważyć <xref:System.Runtime.InteropServices.SafeHandle> użycie klasy zamiast bezpośredniego zarządzania zasobami niezarządzanymi.</span><span class="sxs-lookup"><span data-stu-id="96dc1-251">You should also consider using the <xref:System.Runtime.InteropServices.SafeHandle> class instead of managing unmanaged resources directly.</span></span>

<span data-ttu-id="96dc1-252">Te nowe lokalizacje dla członków zabudowanych wyrażenia stanowią ważny punkt kontrolny dla języka C#: Te funkcje zostały zaimplementowane przez członków społeczności pracujących nad projektem [Roslyn](https://github.com/dotnet/Roslyn) open source.</span><span class="sxs-lookup"><span data-stu-id="96dc1-252">These new locations for expression-bodied members represent an important milestone for the C# language: These features were implemented by community members working on the open-source [Roslyn](https://github.com/dotnet/Roslyn) project.</span></span>

<span data-ttu-id="96dc1-253">Zmiana metody na element członkowski zabudowany wyrażenie jest [zmianą zgodną z binarną](version-update-considerations.md#binary-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="96dc1-253">Changing a method to an expression bodied member is a [binary compatible change](version-update-considerations.md#binary-compatible-changes).</span></span>

## <a name="throw-expressions"></a><span data-ttu-id="96dc1-254">Wyrażenia throw</span><span class="sxs-lookup"><span data-stu-id="96dc1-254">Throw expressions</span></span>

<span data-ttu-id="96dc1-255">W języku `throw` C#zawsze było stwierdzeniem.</span><span class="sxs-lookup"><span data-stu-id="96dc1-255">In C#, `throw` has always been a statement.</span></span> <span data-ttu-id="96dc1-256">Ponieważ `throw` jest instrukcja, a nie wyrażenie, nie było konstrukcji Języka C#, gdzie nie można go użyć.</span><span class="sxs-lookup"><span data-stu-id="96dc1-256">Because `throw` is a statement, not an expression, there were C# constructs where you couldn't use it.</span></span> <span data-ttu-id="96dc1-257">Obejmowały one wyrażenia warunkowe, wyrażenia scalania null i niektóre wyrażenia lambda.</span><span class="sxs-lookup"><span data-stu-id="96dc1-257">These included conditional expressions, null coalescing expressions, and some lambda expressions.</span></span> <span data-ttu-id="96dc1-258">Dodanie elementów członkowskich zabudowanych wyrażeniami `throw` dodaje więcej lokalizacji, w których wyrażenia byłyby przydatne.</span><span class="sxs-lookup"><span data-stu-id="96dc1-258">The addition of expression-bodied members adds more locations where `throw` expressions would be useful.</span></span> <span data-ttu-id="96dc1-259">Tak, że można napisać dowolną z tych konstrukcji, C# 7.0 wprowadza [*throw wyrażeń*](../language-reference/keywords/throw.md#the-throw-expression).</span><span class="sxs-lookup"><span data-stu-id="96dc1-259">So that you can write any of these constructs, C# 7.0 introduces [*throw expressions*](../language-reference/keywords/throw.md#the-throw-expression).</span></span>

<span data-ttu-id="96dc1-260">To dodanie ułatwia pisanie kodu opartego na wyrażeniach.</span><span class="sxs-lookup"><span data-stu-id="96dc1-260">This addition makes it easier to write more expression-based code.</span></span> <span data-ttu-id="96dc1-261">Nie potrzebujesz dodatkowych instrukcji do sprawdzania błędów.</span><span class="sxs-lookup"><span data-stu-id="96dc1-261">You don't need additional statements for error checking.</span></span>

## <a name="generalized-async-return-types"></a><span data-ttu-id="96dc1-262">Uogólnione typy zwrotów asynchronizowanych</span><span class="sxs-lookup"><span data-stu-id="96dc1-262">Generalized async return types</span></span>

<span data-ttu-id="96dc1-263">Zwracanie `Task` obiektu z metod asynchroniiowych może wprowadzić wąskie gardła wydajności w niektórych ścieżkach.</span><span class="sxs-lookup"><span data-stu-id="96dc1-263">Returning a `Task` object from async methods can introduce performance bottlenecks in certain paths.</span></span> <span data-ttu-id="96dc1-264">`Task`jest typem odwołania, więc użycie go oznacza przydzielanie obiektu.</span><span class="sxs-lookup"><span data-stu-id="96dc1-264">`Task` is a reference type, so using it means allocating an object.</span></span> <span data-ttu-id="96dc1-265">W przypadkach, gdy metoda `async` zadeklarowana za pomocą modyfikatora zwraca wynik w pamięci podręcznej lub kończy się synchronicznie, dodatkowe alokacje mogą stać się znaczący koszt czasu w krytycznych sekcjach kodu wydajności.</span><span class="sxs-lookup"><span data-stu-id="96dc1-265">In cases where a method declared with the `async` modifier returns a cached result, or completes synchronously, the extra allocations can become a significant time cost in performance critical sections of code.</span></span> <span data-ttu-id="96dc1-266">Może stać się kosztowne, jeśli te alokacje występują w ciasnych pętli.</span><span class="sxs-lookup"><span data-stu-id="96dc1-266">It can become costly if those allocations occur in tight loops.</span></span>

<span data-ttu-id="96dc1-267">Nowa funkcja języka oznacza, że typy zwracania `Task`metody `Task<T>`asynchronizowej nie są ograniczone do , i `void`.</span><span class="sxs-lookup"><span data-stu-id="96dc1-267">The new language feature means that async method return types aren't limited to `Task`, `Task<T>`, and `void`.</span></span> <span data-ttu-id="96dc1-268">Zwracany typ musi nadal spełniać wzorzec asynchronii, co oznacza, że `GetAwaiter` metoda musi być dostępna.</span><span class="sxs-lookup"><span data-stu-id="96dc1-268">The returned type must still satisfy the async pattern, meaning a `GetAwaiter` method must be accessible.</span></span> <span data-ttu-id="96dc1-269">Jako jeden konkretny `ValueTask` przykład, typ został dodany do platformy .NET, aby korzystać z tej nowej funkcji języka:</span><span class="sxs-lookup"><span data-stu-id="96dc1-269">As one concrete example, the `ValueTask` type has been added to .NET to make use of this new language feature:</span></span>

[!code-csharp[UsingValueTask](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#UsingValueTask "Using ValueTask")]

> [!NOTE]
> <span data-ttu-id="96dc1-270">Należy dodać pakiet [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) NuGet w celu <xref:System.Threading.Tasks.ValueTask%601> użycia typu.</span><span class="sxs-lookup"><span data-stu-id="96dc1-270">You need to add the NuGet package [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) in order to use the <xref:System.Threading.Tasks.ValueTask%601> type.</span></span>

<span data-ttu-id="96dc1-271">To ulepszenie jest najbardziej przydatne dla autorów biblioteki, aby uniknąć przydzielania kodu krytycznego `Task` w wydajności.</span><span class="sxs-lookup"><span data-stu-id="96dc1-271">This enhancement is most useful for library authors to avoid allocating a `Task` in performance critical code.</span></span>

## <a name="numeric-literal-syntax-improvements"></a><span data-ttu-id="96dc1-272">Ulepszenia składni literału liczbowego</span><span class="sxs-lookup"><span data-stu-id="96dc1-272">Numeric literal syntax improvements</span></span>

<span data-ttu-id="96dc1-273">Błędna interpretacja stałych liczbowych może utrudnić zrozumienie kodu podczas czytania go po raz pierwszy.</span><span class="sxs-lookup"><span data-stu-id="96dc1-273">Misreading numeric constants can make it harder to understand code when reading it for the first time.</span></span> <span data-ttu-id="96dc1-274">Maski bitowe lub inne wartości symboliczne są podatne na nieporozumienia.</span><span class="sxs-lookup"><span data-stu-id="96dc1-274">Bit masks or other symbolic values are prone to misunderstanding.</span></span> <span data-ttu-id="96dc1-275">C# 7.0 zawiera dwie nowe funkcje do pisania liczb w najbardziej czytelny sposób do zamierzonego zastosowania: *literały binarne*i *separatory cyfr*.</span><span class="sxs-lookup"><span data-stu-id="96dc1-275">C# 7.0 includes two new features to write numbers in the most readable fashion for the intended use: *binary literals*, and *digit separators*.</span></span>

<span data-ttu-id="96dc1-276">W tych czasach, gdy tworzysz maski bitowe lub gdy binarna reprezentacja liczby tworzy najbardziej czytelny kod, zapisz tę liczbę w formacie binarnym:</span><span class="sxs-lookup"><span data-stu-id="96dc1-276">For those times when you're creating bit masks, or whenever a binary representation of a number makes the most readable code, write that number in binary:</span></span>

[!code-csharp[ThousandSeparators](~/samples/snippets/csharp/new-in-7/Program.cs#ThousandSeparators "Thousands separators")]

<span data-ttu-id="96dc1-277">Na `0b` początku stałej wskazuje, że liczba jest zapisywana jako liczba binarna.</span><span class="sxs-lookup"><span data-stu-id="96dc1-277">The `0b` at the beginning of the constant indicates that the number is written as a binary number.</span></span> <span data-ttu-id="96dc1-278">Liczby binarne mogą się długo, więc często łatwiej jest zobaczyć wzorce bitowe, wprowadzając separator `_` jako cyfrę, jak pokazano powyżej w stałej binarnej.</span><span class="sxs-lookup"><span data-stu-id="96dc1-278">Binary numbers can get long, so it's often easier to see the bit patterns by introducing the `_` as a digit separator, as shown above in the binary constant.</span></span> <span data-ttu-id="96dc1-279">Separator cyfr może pojawić się w dowolnym miejscu stałej.</span><span class="sxs-lookup"><span data-stu-id="96dc1-279">The digit separator can appear anywhere in the constant.</span></span> <span data-ttu-id="96dc1-280">W przypadku liczb bazowych 10 często używa się go jako separatora tysięcy:</span><span class="sxs-lookup"><span data-stu-id="96dc1-280">For base 10 numbers, it is common to use it as a thousands separator:</span></span>

[!code-csharp[LargeIntegers](~/samples/snippets/csharp/new-in-7/Program.cs#LargeIntegers "Large integer")]

<span data-ttu-id="96dc1-281">Separator cyfr może być `decimal` `float`używany `double` z , i typy, jak również:</span><span class="sxs-lookup"><span data-stu-id="96dc1-281">The digit separator can be used with `decimal`, `float`, and `double` types as well:</span></span>

[!code-csharp[OtherConstants](~/samples/snippets/csharp/new-in-7/Program.cs#OtherConstants "non-integral constants")]

<span data-ttu-id="96dc1-282">Razem wzięte, można zadeklarować stałe numeryczne o wiele bardziej czytelności.</span><span class="sxs-lookup"><span data-stu-id="96dc1-282">Taken together, you can declare numeric constants with much more readability.</span></span>
