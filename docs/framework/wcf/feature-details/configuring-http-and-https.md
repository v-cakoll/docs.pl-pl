---
title: Konfigurowanie protokołów HTTP i HTTPS - WCF
ms.date: 04/08/2019
helpviewer_keywords:
- configuring HTTP [WCF]
ms.assetid: b0c29a86-bc0c-41b3-bc1e-4eb5bb5714d4
ms.openlocfilehash: 86705a4f8daa327c442ac6c53c9b44c5b5c5c2ad
ms.sourcegitcommit: d21bee9dbd32b9540ad30f9d0e2e874227040be3
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/09/2019
ms.locfileid: "59427178"
---
# <a name="configuring-http-and-https"></a><span data-ttu-id="e8a68-102">Konfigurowanie protokołów HTTP i HTTPS</span><span class="sxs-lookup"><span data-stu-id="e8a68-102">Configuring HTTP and HTTPS</span></span>

<span data-ttu-id="e8a68-103">Usługi WCF i klienci mogą komunikować się za pośrednictwem protokołów HTTP i HTTPS.</span><span class="sxs-lookup"><span data-stu-id="e8a68-103">WCF services and clients can communicate over HTTP and HTTPS.</span></span> <span data-ttu-id="e8a68-104">Ustawienia HTTP/HTTPS są skonfigurowane przy użyciu usług Internet Information Services (IIS) lub za pomocą narzędzia wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="e8a68-104">The HTTP/HTTPS settings are configured by using Internet Information Services (IIS) or through the use of a command-line tool.</span></span> <span data-ttu-id="e8a68-105">Gdy usługa WCF jest hostowana w ustawieniach usług IIS HTTP lub HTTPS można skonfigurować w ramach usług IIS (za pomocą narzędzia inetmgr.exe).</span><span class="sxs-lookup"><span data-stu-id="e8a68-105">When a WCF service is hosted under IIS HTTP or HTTPS settings can be configured within IIS (using the inetmgr.exe tool).</span></span> <span data-ttu-id="e8a68-106">Jeśli usługa WCF jest samodzielnie hostowana, ustawienia protokołu HTTP lub HTTPS są konfigurowane za pomocą narzędzia wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="e8a68-106">If a WCF service is self-hosted, HTTP or HTTPS settings are configured by using a command-line tool.</span></span>

<span data-ttu-id="e8a68-107">Co najmniej, dla których chcesz skonfigurować rejestracji adresu URL i dodać wyjątek zapory dla adresu URL, będzie korzystać z usługi.</span><span class="sxs-lookup"><span data-stu-id="e8a68-107">At a minimum, you want to configure a URL registration and add a Firewall exception for the URL your service will be using.</span></span> <span data-ttu-id="e8a68-108">Te ustawienia można skonfigurować za pomocą narzędzia Netsh.exe.</span><span class="sxs-lookup"><span data-stu-id="e8a68-108">You can configure these settings with the Netsh.exe tool.</span></span>

## <a name="configuring-namespace-reservations"></a><span data-ttu-id="e8a68-109">Konfigurowanie rezerwacji przestrzeni nazw</span><span class="sxs-lookup"><span data-stu-id="e8a68-109">Configuring namespace reservations</span></span>

<span data-ttu-id="e8a68-110">Namespace rezerwacji przypisuje uprawnienia dla części nazw adresem URL protokołu HTTP do określonej grupy użytkowników.</span><span class="sxs-lookup"><span data-stu-id="e8a68-110">Namespace reservation assigns the rights for a portion of the HTTP URL namespace to a particular group of users.</span></span> <span data-ttu-id="e8a68-111">Rezerwacja zapewnia tych użytkowników, prawa do tworzenia usług, które nasłuchują na część przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="e8a68-111">A reservation gives those users the right to create services that listen on that portion of the namespace.</span></span> <span data-ttu-id="e8a68-112">Rezerwacje są przedrostki adresów URL, co oznacza, że rezerwacji obejmuje wszystkie podrzędne ścieżki rezerwacji.</span><span class="sxs-lookup"><span data-stu-id="e8a68-112">Reservations are URL prefixes, meaning that the reservation covers all subpaths of the reservation path.</span></span> <span data-ttu-id="e8a68-113">Rezerwacje Namespace zezwalać na dwa sposoby użycia symboli wieloznacznych.</span><span class="sxs-lookup"><span data-stu-id="e8a68-113">Namespace reservations permit two ways to use wildcards.</span></span> <span data-ttu-id="e8a68-114">W tym artykule opisano w dokumentacji interfejsu API serwera HTTP [kolejność rozpoznawanie między oświadczeń przestrzeni nazw, które obejmują symboli wieloznacznych](/windows/desktop/Http/routing-incoming-requests).</span><span class="sxs-lookup"><span data-stu-id="e8a68-114">The HTTP Server API documentation describes the [order of resolution between namespace claims that involve wildcards](/windows/desktop/Http/routing-incoming-requests).</span></span>

<span data-ttu-id="e8a68-115">Uruchomionej aplikacji można utworzyć żądania podobne do dodawania rejestracji przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="e8a68-115">A running application can create a similar request to add namespace registrations.</span></span> <span data-ttu-id="e8a68-116">Rejestracje i zastrzeżenia konkurują o części przestrzeni nazw.</span><span class="sxs-lookup"><span data-stu-id="e8a68-116">Registrations and reservations compete for portions of the namespace.</span></span> <span data-ttu-id="e8a68-117">Rezerwacja mogą mieć pierwszeństwo nad rejestracji zgodnie z porządkiem rozwiązania podany w [kolejność rozpoznawanie między oświadczeń przestrzeni nazw, które obejmują symboli wieloznacznych](/windows/desktop/Http/routing-incoming-requests).</span><span class="sxs-lookup"><span data-stu-id="e8a68-117">A reservation may have precedence over a registration according to the order of resolution given in the [order of resolution between namespace claims that involve wildcards](/windows/desktop/Http/routing-incoming-requests).</span></span> <span data-ttu-id="e8a68-118">W tym przypadku rezerwacji blokuje uruchomionej aplikacji odbieranie żądań.</span><span class="sxs-lookup"><span data-stu-id="e8a68-118">In this case, the reservation blocks the running application from receiving requests.</span></span>

<span data-ttu-id="e8a68-119">W poniższym przykładzie użyto narzędzia Netsh.exe:</span><span class="sxs-lookup"><span data-stu-id="e8a68-119">The following example uses the Netsh.exe tool:</span></span>

```console
netsh http add urlacl url=http://+:80/MyUri user=DOMAIN\user
```

<span data-ttu-id="e8a68-120">To polecenie dodaje rezerwację adresu URL, dla określonego obszaru nazw adresu URL konta domena\użytkownik.</span><span class="sxs-lookup"><span data-stu-id="e8a68-120">This command adds a URL reservation for the specified URL namespace for the DOMAIN\user account.</span></span> <span data-ttu-id="e8a68-121">Aby uzyskać więcej informacji na temat korzystania z polecenia netsh, wpisz `netsh http add urlacl /?` w wierszu polecenia i naciśnij klawisz Enter.</span><span class="sxs-lookup"><span data-stu-id="e8a68-121">For more information on using the netsh command, type `netsh http add urlacl /?` in a command-prompt and press Enter.</span></span>

## <a name="configuring-a-firewall-exception"></a><span data-ttu-id="e8a68-122">Konfigurowanie wyjątków zapory</span><span class="sxs-lookup"><span data-stu-id="e8a68-122">Configuring a firewall exception</span></span>

<span data-ttu-id="e8a68-123">Hostując samodzielnie usługi WCF, która komunikuje się za pośrednictwem protokołu HTTP, należy dodać wyjątek do konfiguracji zapory, aby zezwolić na połączenia przychodzące przy użyciu określonego adresu URL.</span><span class="sxs-lookup"><span data-stu-id="e8a68-123">When self-hosting a WCF service that communicates over HTTP, an exception must be added to the firewall configuration to allow inbound connections using a particular URL.</span></span>

## <a name="configuring-ssl-certificates"></a><span data-ttu-id="e8a68-124">Konfigurowanie certyfikatów protokołu SSL</span><span class="sxs-lookup"><span data-stu-id="e8a68-124">Configuring SSL certificates</span></span>

<span data-ttu-id="e8a68-125">Protokół Secure Sockets Layer (SSL) używa certyfikatów na kliencie i serwerze w celu przechowywania kluczy szyfrowania.</span><span class="sxs-lookup"><span data-stu-id="e8a68-125">The Secure Sockets Layer (SSL) protocol uses certificates on the client and server to store encryption keys.</span></span> <span data-ttu-id="e8a68-126">Serwer udostępnia swój certyfikat protokołu SSL, po nawiązaniu połączenia, dzięki czemu klient może sprawdzić tożsamość serwera.</span><span class="sxs-lookup"><span data-stu-id="e8a68-126">The server provides its SSL certificate when a connection is made so that the client can verify the server identity.</span></span> <span data-ttu-id="e8a68-127">Serwer można także zażądać certyfikatu od klienta w celu zapewnienia wzajemnego uwierzytelniania obu stronach połączenia.</span><span class="sxs-lookup"><span data-stu-id="e8a68-127">The server can also request a certificate from the client to provide mutual authentication of both sides of the connection.</span></span>

<span data-ttu-id="e8a68-128">Certyfikaty są przechowywane w magazynie scentralizowane zgodnie z liczbą adres i port IP połączenia.</span><span class="sxs-lookup"><span data-stu-id="e8a68-128">Certificates are stored in a centralized store according to the IP address and port number of the connection.</span></span> <span data-ttu-id="e8a68-129">Specjalny adres IP 0.0.0.0 pasuje do dowolnego adresu IP dla komputera lokalnego.</span><span class="sxs-lookup"><span data-stu-id="e8a68-129">The special IP address 0.0.0.0 matches any IP address for the local machine.</span></span> <span data-ttu-id="e8a68-130">Należy pamiętać, że magazyn certyfikatów nie odróżnić adresy URL na podstawie ścieżki.</span><span class="sxs-lookup"><span data-stu-id="e8a68-130">Note that the certificate store doesn't distinguish URLs based on the path.</span></span> <span data-ttu-id="e8a68-131">Usługi za pomocą tej samej kombinacji adres i port IP muszą mieć certyfikaty, nawet, jeśli ścieżka w adresie URL dla usług różni się.</span><span class="sxs-lookup"><span data-stu-id="e8a68-131">Services with the same IP address and port combination must share certificates even if the path in the URL for the services is different.</span></span>

<span data-ttu-id="e8a68-132">Aby uzyskać instrukcje krok po kroku, zobacz [jak: Konfigurowanie portu z certyfikatem SSL](how-to-configure-a-port-with-an-ssl-certificate.md).</span><span class="sxs-lookup"><span data-stu-id="e8a68-132">For step-by-step instructions, see [How to: Configure a Port with an SSL Certificate](how-to-configure-a-port-with-an-ssl-certificate.md).</span></span>

## <a name="configuring-the-ip-listen-list"></a><span data-ttu-id="e8a68-133">Konfigurowanie listy nasłuchiwania adresów IP</span><span class="sxs-lookup"><span data-stu-id="e8a68-133">Configuring the IP Listen List</span></span>

<span data-ttu-id="e8a68-134">Interfejs API serwera HTTP tylko wiąże adres IP i port po użytkownik rejestruje adresu URL.</span><span class="sxs-lookup"><span data-stu-id="e8a68-134">The HTTP Server API only binds to an IP address and port once a user registers a URL.</span></span> <span data-ttu-id="e8a68-135">Domyślnie interfejsu API serwera HTTP wiąże się z portem w adresie URL, dla wszystkich adresów IP maszyny.</span><span class="sxs-lookup"><span data-stu-id="e8a68-135">By default, the HTTP Server API binds to the port in the URL for all of the IP addresses of the machine.</span></span> <span data-ttu-id="e8a68-136">Wystąpi konflikt, jeśli aplikacja, która nie korzysta z interfejsu API serwera HTTP został wcześniej powiązany z tej kombinacji adres IP i port.</span><span class="sxs-lookup"><span data-stu-id="e8a68-136">A conflict arises if an application that doesn't use the HTTP Server API has previously bound to that combination of IP address and port.</span></span> <span data-ttu-id="e8a68-137">Lista nasłuchiwania IP pozwala usług WCF pod kątem współistnienia z aplikacji, które używają portu dla niektórych adresów IP maszyny.</span><span class="sxs-lookup"><span data-stu-id="e8a68-137">The IP Listen List allows WCF services to coexist with applications that use a port for some of the IP addresses of the machine.</span></span> <span data-ttu-id="e8a68-138">Jeśli lista nasłuchiwania IP zawiera wszystkie wpisy, interfejsu API serwera HTTP wiąże tylko te adresy IP, które określa listy.</span><span class="sxs-lookup"><span data-stu-id="e8a68-138">If the IP Listen List contains any entries, the HTTP Server API only binds to those IP addresses that the list specifies.</span></span> <span data-ttu-id="e8a68-139">Modyfikowanie listy IP nasłuchiwania wymaga uprawnień administracyjnych.</span><span class="sxs-lookup"><span data-stu-id="e8a68-139">Modifying the IP Listen List requires administrative privileges.</span></span>

<span data-ttu-id="e8a68-140">Aby zmodyfikować listę nasłuchiwania adresów IP, należy użyć narzędzia netsh, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="e8a68-140">Use the netsh tool to modify the IP Listen List, as shown in the following example:</span></span>

```console
netsh http add iplisten ipaddress=0.0.0.0:8000
```

## <a name="other-configuration-settings"></a><span data-ttu-id="e8a68-141">Pozostałe ustawienia konfiguracyjne</span><span class="sxs-lookup"><span data-stu-id="e8a68-141">Other configuration settings</span></span>

<span data-ttu-id="e8a68-142">Korzystając z <xref:System.ServiceModel.WSDualHttpBinding>, wartości domyślne, które są zgodne z rezerwacji przestrzeni nazw i zapory Windows korzysta z połączenia klienta.</span><span class="sxs-lookup"><span data-stu-id="e8a68-142">When using <xref:System.ServiceModel.WSDualHttpBinding>, the client connection uses defaults that are compatible with namespace reservations and the Windows firewall.</span></span> <span data-ttu-id="e8a68-143">Jeśli użytkownik chce dostosować adres bazowy podwójnego połączenia klienta, również musisz skonfigurować te ustawienia protokołu HTTP na kliencie, aby dopasować nowy adres.</span><span class="sxs-lookup"><span data-stu-id="e8a68-143">If you choose to customize the client base address of a dual connection, then you also must configure these HTTP settings on the client to match the new address.</span></span>

<span data-ttu-id="e8a68-144">Interfejs API serwera HTTP zawiera niektóre zaawansowane ustawienia konfiguracji, które nie są dostępne za pośrednictwem tak.</span><span class="sxs-lookup"><span data-stu-id="e8a68-144">The HTTP Server API has some advanced configuration settings that aren't available through HttpCfg.</span></span> <span data-ttu-id="e8a68-145">Te ustawienia są przechowywane w rejestrze i dotyczą wszystkich aplikacji działających w systemach, które używają interfejsów API serwera HTTP.</span><span class="sxs-lookup"><span data-stu-id="e8a68-145">These settings are maintained in the registry and apply to all applications running on the systems that use the HTTP Server APIs.</span></span> <span data-ttu-id="e8a68-146">Aby uzyskać informacje o tych ustawieniach, zobacz [ustawienia rejestru Http.sys w usługach IIS](https://support.microsoft.com/en-us/help/820129/http-sys-registry-settings-for-windows).</span><span class="sxs-lookup"><span data-stu-id="e8a68-146">For information about these settings, see [Http.sys registry settings for IIS](https://support.microsoft.com/en-us/help/820129/http-sys-registry-settings-for-windows).</span></span> <span data-ttu-id="e8a68-147">Większość użytkowników nie muszą zmieniać te ustawienia.</span><span class="sxs-lookup"><span data-stu-id="e8a68-147">Most users don't need to change these settings.</span></span>

## <a name="see-also"></a><span data-ttu-id="e8a68-148">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="e8a68-148">See also</span></span>

- <xref:System.ServiceModel.WSDualHttpBinding>
- [<span data-ttu-id="e8a68-149">Instrukcje: konfigurowanie portu z certyfikatem SSL</span><span class="sxs-lookup"><span data-stu-id="e8a68-149">How to: Configure a Port with an SSL Certificate</span></span>](how-to-configure-a-port-with-an-ssl-certificate.md)
