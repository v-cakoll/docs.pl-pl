---
title: Pakowanie i wdrażanie zasobów w aplikacjach platformy .NET
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- deploying applications [.NET Framework], resources
- resource files, deploying
- hub-and-spoke resource deployment model
- resource files, packaging
- application resources, packaging
- single resource assembly
- satellite assemblies
- default culture for applications
- names [.NET Framework], resources
- fallback process for resources
- grouping cultures
- application resources, deploying
- application resources, naming conventions
- culture, packaging and deploying resources
- resource files, naming conventions
- packaging application resources
- application resources, fallback processes
- resource files, fallback processes
- localizing resources
- neutral cultures
ms.assetid: b224d7c0-35f8-4e82-a705-dd76795e8d16
ms.openlocfilehash: d64e3b5201e34541fdafa5724b0c7e8c3f6c0c0d
ms.sourcegitcommit: 7980a91f90ae5eca859db7e6bfa03e23e76a1a50
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/13/2020
ms.locfileid: "81243053"
---
# <a name="packaging-and-deploying-resources-in-net-apps"></a><span data-ttu-id="ab8cd-102">Pakowanie i wdrażanie zasobów w aplikacjach platformy .NET</span><span class="sxs-lookup"><span data-stu-id="ab8cd-102">Packaging and Deploying Resources in .NET Apps</span></span>

<span data-ttu-id="ab8cd-103">Aplikacje polegają na programie .NET Framework <xref:System.Resources.ResourceManager> Resource Manager, reprezentowanym przez klasę, w celu pobrania zlokalizowanych zasobów.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-103">Applications rely on the .NET Framework Resource Manager, represented by the <xref:System.Resources.ResourceManager> class, to retrieve localized resources.</span></span> <span data-ttu-id="ab8cd-104">Menedżer zasobów zakłada, że model koncentratora i szprychy jest używany do pakowania i wdrażania zasobów.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-104">The Resource Manager assumes that a hub and spoke model is used to package and deploy resources.</span></span> <span data-ttu-id="ab8cd-105">Centrum jest głównym zestawem, który zawiera niezlokalizowany kod wykonywalny i zasoby dla pojedynczej kultury, o nazwie kultury neutralnej lub domyślnej.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-105">The hub is the main assembly that contains the nonlocalizable executable code and the resources for a single culture, called the neutral or default culture.</span></span> <span data-ttu-id="ab8cd-106">Kultury domyślnej jest kultury rezerwowej dla aplikacji; jest to kultura, której zasoby są używane, jeśli nie można odnaleźć zlokalizowanych zasobów.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-106">The default culture is the fallback culture for the application; it is the culture whose resources are used if localized resources cannot be found.</span></span> <span data-ttu-id="ab8cd-107">Każda szprycha łączy się z zestawem satelickiego, który zawiera zasoby dla pojedynczej kultury, ale nie zawiera żadnego kodu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-107">Each spoke connects to a satellite assembly that contains the resources for a single culture, but does not contain any code.</span></span>

<span data-ttu-id="ab8cd-108">Istnieje kilka zalet tego modelu:</span><span class="sxs-lookup"><span data-stu-id="ab8cd-108">There are several advantages to this model:</span></span>

- <span data-ttu-id="ab8cd-109">Po wdrożeniu aplikacji można stopniowo dodawać zasoby dla nowych kultur.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-109">You can incrementally add resources for new cultures after you have deployed an application.</span></span> <span data-ttu-id="ab8cd-110">Ponieważ późniejsze rozwijanie zasobów specyficznych dla kultury może wymagać znacznej ilości czasu, umożliwia to najpierw zwolnić aplikację główną i dostarczyć zasoby specyficzne dla kultury w późniejszym terminie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-110">Because subsequent development of culture-specific resources can require a significant amount of time, this allows you to release your main application first, and deliver culture-specific resources at a later date.</span></span>
- <span data-ttu-id="ab8cd-111">Można aktualizować i zmieniać zestawy satelickie aplikacji bez ponownej kompilacji aplikacji.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-111">You can update and change an application's satellite assemblies without recompiling the application.</span></span>
- <span data-ttu-id="ab8cd-112">Aplikacja musi załadować tylko te zestawy satelityczne, które zawierają zasoby potrzebne dla określonej kultury.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-112">An application needs to load only those satellite assemblies that contain the resources needed for a particular culture.</span></span> <span data-ttu-id="ab8cd-113">Może to znacznie zmniejszyć wykorzystanie zasobów systemowych.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-113">This can significantly reduce the use of system resources.</span></span>

 <span data-ttu-id="ab8cd-114">Istnieją jednak również wady tego modelu:</span><span class="sxs-lookup"><span data-stu-id="ab8cd-114">However, there are also disadvantages to this model:</span></span>

- <span data-ttu-id="ab8cd-115">Należy zarządzać wieloma zestawami zasobów.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-115">You must manage multiple sets of resources.</span></span>
- <span data-ttu-id="ab8cd-116">Początkowy koszt testowania aplikacji wzrasta, ponieważ należy przetestować kilka konfiguracji.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-116">The initial cost of testing an application increases, because you must test several configurations.</span></span> <span data-ttu-id="ab8cd-117">Należy pamiętać, że w dłuższej perspektywie będzie łatwiej i mniej kosztowne, aby przetestować jedną podstawową aplikację z kilku satelitów, niż do testowania i utrzymania kilku równoległych wersji międzynarodowych.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-117">Note that in the long term it will be easier and less expensive to test one core application with several satellites, than to test and maintain several parallel international versions.</span></span>

## <a name="resource-naming-conventions"></a><span data-ttu-id="ab8cd-118">Konwencje nazewnictwa zasobów</span><span class="sxs-lookup"><span data-stu-id="ab8cd-118">Resource naming conventions</span></span>

<span data-ttu-id="ab8cd-119">Podczas pakowania zasobów aplikacji, należy nazwać je przy użyciu konwencji nazewnictwa zasobów, które oczekuje środowiska wykonawczego języka wspólnego.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-119">When you package your application's resources, you must name them using the resource naming conventions that the common language runtime expects.</span></span> <span data-ttu-id="ab8cd-120">Środowisko wykonawcze identyfikuje zasób według jego nazwy kultury.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-120">The runtime identifies a resource by its culture name.</span></span> <span data-ttu-id="ab8cd-121">Każdej kulturze nadano unikatową nazwę, która jest zwykle kombinacją dwuliterowej, małych nazw kultury skojarzonej z językiem i, w razie potrzeby, dwuliterowej, pisanej, najwyższej nazwy subkultury skojarzonej z krajem lub regionem.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-121">Each culture is given a unique name, which is usually a combination of a two-letter, lowercase culture name associated with a language and, if required, a two-letter, uppercase subculture name associated with a country or region.</span></span> <span data-ttu-id="ab8cd-122">Nazwa subkultury jest zgodna z nazwą kultury, oddzieloną kreską (-).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-122">The subculture name follows the culture name, separated by a dash (-).</span></span> <span data-ttu-id="ab8cd-123">Przykłady obejmują ja-JP dla języka japońskiego, jak mówi się w Japonii, en-US dla języka angielskiego, jak mówi się w Stanach Zjednoczonych, de-DE dla języka niemieckiego, jak mówi się w Niemczech, lub de-AT dla języka niemieckiego, jak mówi się w Austrii.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-123">Examples include ja-JP for Japanese as spoken in Japan, en-US for English as spoken in the United States, de-DE for German as spoken in Germany, or de-AT for German as spoken in Austria.</span></span> <span data-ttu-id="ab8cd-124">Zobacz kolumnę **Tag języka** na liście [nazw języków/regionów obsługiwanych przez system Windows](https://docs.microsoft.com/openspecs/windows_protocols/ms-lcid/a9eac961-e77d-41a6-90a5-ce1a8b0cdb9c).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-124">See the **Language tag** column in the [list of language/region names supported by Windows](https://docs.microsoft.com/openspecs/windows_protocols/ms-lcid/a9eac961-e77d-41a6-90a5-ce1a8b0cdb9c).</span></span> <span data-ttu-id="ab8cd-125">Nazwy kultur są zgodne ze standardem zdefiniowanym przez [BCP 47](https://tools.ietf.org/html/bcp47).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-125">Culture names follow the standard defined by [BCP 47](https://tools.ietf.org/html/bcp47).</span></span>

> [!NOTE]
> <span data-ttu-id="ab8cd-126">Istnieją pewne wyjątki dla nazw kultury dwuliterowej, takich jak `zh-Hans` chiński (uproszczony).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-126">There are some exceptions for the two-letter culture names, such as `zh-Hans` for Chinese (Simplified).</span></span>

> [!NOTE]
> <span data-ttu-id="ab8cd-127">Aby uzyskać informacje dotyczące tworzenia plików zasobów, zobacz [Tworzenie plików zasobów](creating-resource-files-for-desktop-apps.md) i tworzenie [zestawów satelickich](creating-satellite-assemblies-for-desktop-apps.md).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-127">For information about creating resource files, see [Creating Resource Files](creating-resource-files-for-desktop-apps.md) and [Creating Satellite Assemblies](creating-satellite-assemblies-for-desktop-apps.md).</span></span>

<a name="cpconpackagingdeployingresourcesanchor1"></a>

## <a name="the-resource-fallback-process"></a><span data-ttu-id="ab8cd-128">Proces bazowy zasobu</span><span class="sxs-lookup"><span data-stu-id="ab8cd-128">The Resource Fallback Process</span></span>

<span data-ttu-id="ab8cd-129">Model koncentratora i szprychy do pakowania i wdrażania zasobów używa procesu awaryjnego w celu zlokalizowania odpowiednich zasobów.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-129">The hub and spoke model for packaging and deploying resources uses a fallback process to locate appropriate resources.</span></span> <span data-ttu-id="ab8cd-130">Jeśli aplikacja żąda zlokalizowanego zasobu, który jest niedostępny, środowisko uruchomieniowe języka wspólnego przeszukuje hierarchię kultur dla odpowiedniego zasobu rezerwowego, który najbardziej odpowiada żądaniu aplikacji użytkownika i zgłasza wyjątek tylko w ostateczności.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-130">If an application requests a localized resource  that is unavailable, the common language runtime searches the hierarchy of cultures for an appropriate fallback resource that most closely matches the user's application's request, and throws an exception only as a last resort.</span></span> <span data-ttu-id="ab8cd-131">Na każdym poziomie hierarchii, jeśli zostanie znaleziony odpowiedni zasób, środowisko wykonawcze używa go.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-131">At each level of the hierarchy, if an appropriate resource is found, the runtime uses it.</span></span> <span data-ttu-id="ab8cd-132">Jeśli zasób nie zostanie znaleziony, wyszukiwanie będzie kontynuowane na następnym poziomie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-132">If the resource is not found, the search continues at the next level.</span></span>

<span data-ttu-id="ab8cd-133">Aby zwiększyć wydajność wyszukiwania, <xref:System.Resources.NeutralResourcesLanguageAttribute> zastosuj atrybut do głównego zestawu i przekaż go nazwę języka neutralnego, który będzie działać z głównym zestawem.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-133">To improve lookup performance, apply the <xref:System.Resources.NeutralResourcesLanguageAttribute> attribute to your main assembly, and pass it the name of the neutral language that will work with your main assembly.</span></span>

### <a name="net-framework-resource-fallback-process"></a><span data-ttu-id="ab8cd-134">Proces awaryjny zasobów programu .NET Framework</span><span class="sxs-lookup"><span data-stu-id="ab8cd-134">.NET Framework resource fallback process</span></span>

<span data-ttu-id="ab8cd-135">Proces rezerwy zasobu platformy .NET Framework obejmuje następujące kroki:</span><span class="sxs-lookup"><span data-stu-id="ab8cd-135">The .NET Framework resource fallback process involves the following steps:</span></span>

> [!TIP]
> <span data-ttu-id="ab8cd-136">Można użyć [ \<relativeBindForResources>](../configure-apps/file-schema/runtime/relativebindforresources-element.md) element konfiguracji w celu optymalizacji procesu rezerwowego zasobów i procesu, w którym sondy środowiska wykonawczego dla zestawów zasobów.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-136">You may be able to use the [\<relativeBindForResources>](../configure-apps/file-schema/runtime/relativebindforresources-element.md) configuration element to optimize the resource fallback process and the process by which the runtime probes for resource assemblies.</span></span> <span data-ttu-id="ab8cd-137">Aby uzyskać więcej informacji, zobacz [optymalizacji procesu awaryjnego zasobów](packaging-and-deploying-resources-in-desktop-apps.md#Optimizing) sekcji.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-137">For more information, see the [Optimizing the Resource Fallback Process](packaging-and-deploying-resources-in-desktop-apps.md#Optimizing) section.</span></span>

1. <span data-ttu-id="ab8cd-138">Środowisko wykonawcze najpierw sprawdza [globalnej pamięci podręcznej zestawu](../app-domains/gac.md) dla zestawu, który pasuje do żądanej kultury dla aplikacji.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-138">The runtime first checks the [global assembly cache](../app-domains/gac.md) for an assembly that matches the requested culture for your application.</span></span>

     <span data-ttu-id="ab8cd-139">Globalna pamięć podręczna zestawów może przechowywać zestawy zasobów, które są współużytkowane przez wiele aplikacji.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-139">The global assembly cache can store resource assemblies that are shared by many applications.</span></span> <span data-ttu-id="ab8cd-140">Zwalnia to z konieczności dołączania określonych zestawów zasobów do struktury katalogów każdej utworzonej aplikacji.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-140">This frees you from having to include specific sets of resources in the directory structure of every application you create.</span></span> <span data-ttu-id="ab8cd-141">Jeśli środowisko wykonawcze znajdzie odwołanie do zestawu, przeszukuje zestaw dla żądanego zasobu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-141">If the runtime finds a reference to the assembly, it searches the assembly for the requested resource.</span></span> <span data-ttu-id="ab8cd-142">Jeśli znajdzie wpis w zestawie, używa żądanego zasobu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-142">If it finds the entry in the assembly, it uses the requested resource.</span></span> <span data-ttu-id="ab8cd-143">Jeśli nie znajdzie wpisu, kontynuuje wyszukiwanie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-143">If it doesn't find the entry, it continues the search.</span></span>

2. <span data-ttu-id="ab8cd-144">Środowisko uruchomieniowe następnie sprawdza katalog aktualnie wykonywanego zestawu dla podkatalogu, który pasuje do żądanej kultury.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-144">The runtime next checks the directory of the currently executing assembly for a subdirectory that matches the requested culture.</span></span> <span data-ttu-id="ab8cd-145">Jeśli znajdzie podkatalog, przeszukuje ten podkatalog dla prawidłowego zestawu satelickiego dla żądanej kultury.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-145">If it finds the subdirectory, it searches that subdirectory for a valid satellite assembly for the requested culture.</span></span> <span data-ttu-id="ab8cd-146">Środowisko wykonawcze następnie przeszukuje zestaw satelickiego żądanego zasobu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-146">The runtime then searches the satellite assembly for the requested resource.</span></span> <span data-ttu-id="ab8cd-147">Jeśli znajdzie zasób w zestawie, używa go.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-147">If it finds the resource in the assembly, it uses it.</span></span> <span data-ttu-id="ab8cd-148">Jeśli nie znajdzie zasobu, kontynuuje wyszukiwanie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-148">If it doesn't find the resource, it continues the search.</span></span>

3. <span data-ttu-id="ab8cd-149">Środowisko wykonawcze następnie wysyła zapytanie do Instalatora Windows, aby ustalić, czy zestaw satelickiego ma być zainstalowany na żądanie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-149">The runtime next queries the Windows Installer to determine whether the satellite assembly is to be installed on demand.</span></span> <span data-ttu-id="ab8cd-150">Jeśli tak, obsługuje instalację, ładuje zestaw i przeszukuje go lub żądany zasób.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-150">If so, it handles the installation, loads the assembly, and searches it or the requested resource.</span></span> <span data-ttu-id="ab8cd-151">Jeśli znajdzie zasób w zestawie, używa go.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-151">If it finds the resource in the assembly, it uses it.</span></span> <span data-ttu-id="ab8cd-152">Jeśli nie znajdzie zasobu, kontynuuje wyszukiwanie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-152">If it doesn't find the resource, it continues the search.</span></span>

4. <span data-ttu-id="ab8cd-153">Środowisko wykonawcze wywołuje <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> zdarzenie, aby wskazać, że nie można znaleźć zestawu satelickiego.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-153">The runtime raises the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event to indicate that it is unable to find the satellite assembly.</span></span> <span data-ttu-id="ab8cd-154">Jeśli zdecydujesz się obsłużyć zdarzenie, program obsługi zdarzeń może zwrócić odwołanie do zestawu satelickiego, którego zasoby będą używane do wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-154">If you choose to handle the event, your event handler can return a reference to the satellite assembly whose resources will be used for the lookup.</span></span> <span data-ttu-id="ab8cd-155">W przeciwnym razie `null` program obsługi zdarzeń zwraca i wyszukiwanie jest kontynuowane.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-155">Otherwise, the event handler returns `null` and the search continues.</span></span>

5. <span data-ttu-id="ab8cd-156">Środowisko uruchomieniowe ponownie przeszukuje globalną pamięć podręczną zestawu, tym razem dla zestawu nadrzędnego żądanej kultury.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-156">The runtime next searches the global assembly cache again, this time for the parent assembly of the requested culture.</span></span> <span data-ttu-id="ab8cd-157">Jeśli zestaw nadrzędny istnieje w globalnej pamięci podręcznej zestawów, środowisko wykonawcze przeszukuje zestaw żądanego zasobu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-157">If the parent assembly exists in the global assembly cache, the runtime searches the assembly for the requested resource.</span></span>

     <span data-ttu-id="ab8cd-158">Kultury nadrzędnej jest zdefiniowany jako kultury rezerwowej odpowiednie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-158">The parent culture is defined as the appropriate fallback culture.</span></span> <span data-ttu-id="ab8cd-159">Należy wziąć pod uwagę rodziców jako rezerwowych kandydatów, ponieważ zapewnienie dowolnego zasobu jest lepsze niż zgłaszanie wyjątku.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-159">Consider parents as fallback candidates, because providing any resource is preferable to throwing an exception.</span></span> <span data-ttu-id="ab8cd-160">Ten proces umożliwia również ponowne użycie zasobów.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-160">This process also allows you to reuse resources.</span></span> <span data-ttu-id="ab8cd-161">Należy dołączyć określonego zasobu na poziomie nadrzędnym tylko wtedy, gdy kultury podrzędnej nie trzeba lokalizować żądanego zasobu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-161">You should include a particular resource at the parent level only if the child culture doesn't need to localize the requested resource.</span></span> <span data-ttu-id="ab8cd-162">Na przykład jeśli dostarczasz zestawy `en` satelitarne `en-GB` dla (neutralny angielski), (angielski używany w Wielkiej `en-US` Brytanii) `en` i (angielski używany w `en-GB` Stanach Zjednoczonych), satelita będzie zawierać wspólną terminologię, a satelity i `en-US` satelity mogą zapewnić zastąpienia tylko tych terminów, które różnią się.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-162">For example, if you supply satellite assemblies for `en` (neutral English), `en-GB` (English as spoken in the United Kingdom), and `en-US` (English as spoken in the United States), the `en` satellite would contain the common terminology, and the `en-GB` and `en-US` satellites could provide overrides for only those terms that differ.</span></span>

6. <span data-ttu-id="ab8cd-163">Następnie środowisko uruchomieniowe sprawdza katalog aktualnie wykonywanego zestawu, aby sprawdzić, czy zawiera katalog nadrzędny.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-163">The runtime next checks the directory of the currently executing assembly to see if it contains a parent directory.</span></span> <span data-ttu-id="ab8cd-164">Jeśli katalog nadrzędny istnieje, środowisko wykonawcze przeszukuje katalog dla prawidłowego zestawu satelickiego dla kultury nadrzędnej.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-164">If a parent directory exists, the runtime searches the directory for a valid satellite assembly for the parent culture.</span></span> <span data-ttu-id="ab8cd-165">Jeśli znajdzie zestaw, środowisko wykonawcze przeszukuje zestaw żądanego zasobu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-165">If it finds the assembly, the runtime searches the assembly for the requested resource.</span></span> <span data-ttu-id="ab8cd-166">Jeśli znajdzie zasób, używa go.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-166">If it finds the resource, it uses it.</span></span> <span data-ttu-id="ab8cd-167">Jeśli nie znajdzie zasobu, kontynuuje wyszukiwanie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-167">If it doesn't find the resource, it continues the search.</span></span>

7. <span data-ttu-id="ab8cd-168">Środowisko wykonawcze następnie wysyła zapytanie do Instalatora Windows, aby ustalić, czy nadrzędny zestaw satelickiego ma być zainstalowany na żądanie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-168">The runtime next queries the Windows Installer to determine whether the parent satellite assembly is to be installed on demand.</span></span> <span data-ttu-id="ab8cd-169">Jeśli tak, obsługuje instalację, ładuje zestaw i przeszukuje go lub żądany zasób.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-169">If so, it handles the installation, loads the assembly, and searches it or the requested resource.</span></span> <span data-ttu-id="ab8cd-170">Jeśli znajdzie zasób w zestawie, używa go.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-170">If it finds the resource in the assembly, it uses it.</span></span> <span data-ttu-id="ab8cd-171">Jeśli nie znajdzie zasobu, kontynuuje wyszukiwanie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-171">If it doesn't find the resource, it continues the search.</span></span>

8. <span data-ttu-id="ab8cd-172">Środowisko wykonawcze wywołuje <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> zdarzenie, aby wskazać, że nie można znaleźć odpowiedniego zasobu rezerwowego.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-172">The runtime raises the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event to indicate that it is unable to find an appropriate fallback resource.</span></span> <span data-ttu-id="ab8cd-173">Jeśli zdecydujesz się obsłużyć zdarzenie, program obsługi zdarzeń może zwrócić odwołanie do zestawu satelickiego, którego zasoby będą używane do wyszukiwania.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-173">If you choose to handle the event, your event handler can return a reference to the satellite assembly whose resources will be used for the lookup.</span></span> <span data-ttu-id="ab8cd-174">W przeciwnym razie `null` program obsługi zdarzeń zwraca i wyszukiwanie jest kontynuowane.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-174">Otherwise, the event handler returns `null` and the search continues.</span></span>

9. <span data-ttu-id="ab8cd-175">Środowisko uruchomieniowe następnego przeszukuje zestawy nadrzędne, podobnie jak w poprzednich trzech krokach, za pośrednictwem wielu potencjalnych poziomów.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-175">The runtime next searches parent assemblies, as in the previous three steps, through many potential levels.</span></span> <span data-ttu-id="ab8cd-176">Każda kultura ma tylko jeden element <xref:System.Globalization.CultureInfo.Parent%2A?displayProperty=nameWithType> nadrzędny, który jest zdefiniowany przez właściwość, ale element nadrzędny może mieć własny element nadrzędny.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-176">Each culture has only one parent, which is defined by the <xref:System.Globalization.CultureInfo.Parent%2A?displayProperty=nameWithType> property, but a parent might have its own parent.</span></span> <span data-ttu-id="ab8cd-177">Wyszukiwanie kultur nadrzędnych zatrzymuje się, <xref:System.Globalization.CultureInfo.Parent%2A> gdy <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType>właściwość kultury zwraca; dla rezerwy zasobów kultury niezmienne nie jest uważany za kultury nadrzędnej lub kultury, które mogą mieć zasoby.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-177">The search for parent cultures stops when a culture's <xref:System.Globalization.CultureInfo.Parent%2A> property returns <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType>; for resource fallback, the invariant culture is not considered a parent culture or a culture that can have resources.</span></span>

10. <span data-ttu-id="ab8cd-178">Jeśli kultura, która została pierwotnie określona i wszystkie rodzice zostały przeszukane, a zasób nadal nie zostanie znaleziony, używany jest zasób dla kultury domyślnej (rezerwowej).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-178">If the culture that was originally specified and all parents have been searched and the resource is still not found, the resource for the default (fallback) culture is used.</span></span> <span data-ttu-id="ab8cd-179">Zazwyczaj zasoby dla kultury domyślnej są uwzględniane w głównym zestawie aplikacji.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-179">Typically, the resources for the default culture are included in the main application assembly.</span></span> <span data-ttu-id="ab8cd-180">Można jednak określić wartość <xref:System.Resources.UltimateResourceFallbackLocation.Satellite> właściwości <xref:System.Resources.NeutralResourcesLanguageAttribute.Location%2A> atrybutu, <xref:System.Resources.NeutralResourcesLanguageAttribute> aby wskazać, że ostateczną lokalizacją rezerwową dla zasobów jest zestaw satelicki, a nie główny zestaw.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-180">However, you can specify a value of <xref:System.Resources.UltimateResourceFallbackLocation.Satellite> for the <xref:System.Resources.NeutralResourcesLanguageAttribute.Location%2A> property of the <xref:System.Resources.NeutralResourcesLanguageAttribute> attribute to indicate that the ultimate fallback location for resources is a satellite assembly, rather than the main assembly.</span></span>

    > [!NOTE]
    > <span data-ttu-id="ab8cd-181">Zasób domyślny jest jedynym zasobem, który można skompilować z zespołem głównym.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-181">The default resource is the only resource that can be compiled with the main assembly.</span></span> <span data-ttu-id="ab8cd-182">O ile nie określisz <xref:System.Resources.NeutralResourcesLanguageAttribute> zestawu satelickiego przy użyciu atrybutu, jest to ostateczny rezerwowy (końcowy element nadrzędny).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-182">Unless you specify a satellite assembly by using the <xref:System.Resources.NeutralResourcesLanguageAttribute> attribute, it is the ultimate fallback (final parent).</span></span> <span data-ttu-id="ab8cd-183">W związku z tym zaleca się, aby zawsze zawierać domyślny zestaw zasobów w zestawie głównym.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-183">Therefore, we recommend that you always include a default set of resources in your main assembly.</span></span> <span data-ttu-id="ab8cd-184">Pomaga to zapobiec zgłaszaniu wyjątków.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-184">This helps prevent exceptions from being thrown.</span></span> <span data-ttu-id="ab8cd-185">Dołączając domyślny zasób, plik zapewnia rezerwowy dla wszystkich zasobów i upewnij się, że co najmniej jeden zasób jest zawsze obecny dla użytkownika, nawet jeśli nie jest specyficzny kulturowo.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-185">By including a default resource, file you provide a fallback for all resources and ensure that at least one resource is always present for the user, even if it is not culturally specific.</span></span>

11. <span data-ttu-id="ab8cd-186">Na koniec, jeśli środowisko wykonawcze nie znajdzie zasobu dla domyślnej <xref:System.Resources.MissingManifestResourceException> <xref:System.Resources.MissingSatelliteAssemblyException> kultury (rezerwowej), zgłaszany jest wyjątek lub wyjątek wskazujący, że nie można odnaleźć zasobu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-186">Finally, if the runtime doesn't find a resource for a default (fallback) culture, a <xref:System.Resources.MissingManifestResourceException> or <xref:System.Resources.MissingSatelliteAssemblyException> exception is thrown to indicate that the resource could not be found.</span></span>

<span data-ttu-id="ab8cd-187">Załóżmy na przykład, że aplikacja żąda zasobu `es-MX` zlokalizowanego dla języka hiszpańskiego (Meksyk) (kultury).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-187">For example, suppose the application requests a resource localized for Spanish (Mexico) (the `es-MX` culture).</span></span> <span data-ttu-id="ab8cd-188">Środowisko wykonawcze najpierw przeszukuje globalną `es-MX`pamięć podręczną zestawu dla zestawu, który pasuje, ale nie znajduje go.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-188">The runtime first searches the global assembly cache for the assembly that matches `es-MX`, but doesn't find it.</span></span> <span data-ttu-id="ab8cd-189">Środowisko wykonawcze następnie przeszukuje katalog aktualnie wykonywanego zestawu dla `es-MX` katalogu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-189">The runtime then searches the directory of the currently executing assembly for an `es-MX` directory.</span></span> <span data-ttu-id="ab8cd-190">W przeciwnym razie środowisko wykonawcze ponownie przeszukuje globalną pamięć podręczną zestawów w `es` poszukiwaniu zestawu nadrzędnego, który odzwierciedla odpowiednią kulturę rezerwową — w tym przypadku (hiszpański).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-190">Failing that, the runtime searches the global assembly cache again for a parent assembly that reflects the appropriate fallback culture — in this case, `es` (Spanish).</span></span> <span data-ttu-id="ab8cd-191">Jeśli zestaw nadrzędny nie zostanie znaleziony, środowisko wykonawcze przeszukuje wszystkie potencjalne poziomy zestawów nadrzędnych dla `es-MX` kultury, dopóki nie znajdzie odpowiedniego zasobu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-191">If the parent assembly is not found, the runtime searches all potential levels of parent assemblies for the `es-MX` culture until it finds a corresponding resource.</span></span> <span data-ttu-id="ab8cd-192">Jeśli zasób nie zostanie znaleziony, środowisko wykonawcze używa zasobu dla kultury domyślnej.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-192">If a resource isn't found, the runtime uses the resource for the default culture.</span></span>

<a name="Optimizing"></a>

#### <a name="optimizing-the-net-framework-resource-fallback-process"></a><span data-ttu-id="ab8cd-193">Optymalizacja procesu awaryjnego zasobu programu .NET Framework</span><span class="sxs-lookup"><span data-stu-id="ab8cd-193">Optimizing the .NET Framework Resource Fallback Process</span></span>

<span data-ttu-id="ab8cd-194">W następujących warunkach można zoptymalizować proces wyszukiwania zasobów w zestawach satelickich</span><span class="sxs-lookup"><span data-stu-id="ab8cd-194">Under the following conditions, you can optimize the process by which the runtime searches for resources in satellite assemblies</span></span>

- <span data-ttu-id="ab8cd-195">Zestawy satelitarne są wdrażane w tej samej lokalizacji co zestaw kodu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-195">Satellite assemblies are deployed in the same location as the code assembly.</span></span> <span data-ttu-id="ab8cd-196">Jeśli zestaw kodu jest zainstalowany w [globalnej pamięci podręcznej zestawów,](../app-domains/gac.md)zestawy satelitowane są również instalowane w globalnej pamięci podręcznej zestawów.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-196">If the code assembly is installed in the [Global Assembly Cache](../app-domains/gac.md), satellite assemblies are also installed in the global assembly cache.</span></span> <span data-ttu-id="ab8cd-197">Jeśli zestaw kodu jest zainstalowany w katalogu, zestawy satelickiego są instalowane w folderach specyficznych dla kultury tego katalogu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-197">If the code assembly is installed in a directory, satellite assemblies are installed in culture-specific folders of that directory.</span></span>

- <span data-ttu-id="ab8cd-198">Zestawy satelitarne nie są instalowane na żądanie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-198">Satellite assemblies are not installed on demand.</span></span>

- <span data-ttu-id="ab8cd-199">Kod aplikacji nie <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> obsługuje zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-199">Application code does not handle the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event.</span></span>

<span data-ttu-id="ab8cd-200">Optymalizacja sondy dla zestawów satelitowych przez [ \<dołączenie relativeBindForResources](../configure-apps/file-schema/runtime/relativebindforresources-element.md)>`true` element i ustawienie jego `enabled` atrybutu w pliku konfiguracji aplikacji, jak pokazano w poniższym przykładzie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-200">You optimize the probe for satellite assemblies by including the [\<relativeBindForResources>](../configure-apps/file-schema/runtime/relativebindforresources-element.md) element and setting its `enabled` attribute to `true` in the application configuration file, as shown in the following example.</span></span>

```xml
<configuration>
   <runtime>
      <relativeBindForResources enabled="true" />
   </runtime>
</configuration>
```

<span data-ttu-id="ab8cd-201">Zoptymalizowana sonda dla zestawów satelitarnych jest funkcją opt-in.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-201">The optimized probe for satellite assemblies is an opt-in feature.</span></span> <span data-ttu-id="ab8cd-202">Oznacza to, że środowisko wykonawcze następuje kroki opisane w [Proces rezerwowy zasobów,](packaging-and-deploying-resources-in-desktop-apps.md#cpconpackagingdeployingresourcesanchor1) chyba że [ \<relativeBindForResources>](../configure-apps/file-schema/runtime/relativebindforresources-element.md) element jest obecny w pliku konfiguracyjnym aplikacji i jego `enabled` atrybut jest ustawiony na `true`.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-202">That is, the runtime follows the steps documented in [The Resource Fallback Process](packaging-and-deploying-resources-in-desktop-apps.md#cpconpackagingdeployingresourcesanchor1) unless the [\<relativeBindForResources>](../configure-apps/file-schema/runtime/relativebindforresources-element.md) element is present in the application's configuration file and its `enabled` attribute is set to `true`.</span></span> <span data-ttu-id="ab8cd-203">W takim przypadku proces sondowania dla zespołu satelickiego jest modyfikowany w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="ab8cd-203">If this is the case, the process of probing for a satellite assembly is modified as follows:</span></span>

- <span data-ttu-id="ab8cd-204">Środowisko wykonawcze używa lokalizacji zestawu kodu nadrzędnego do sondowania dla zestawu satelickiego.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-204">The runtime uses the location of the parent code assembly to probe for the satellite assembly.</span></span> <span data-ttu-id="ab8cd-205">Jeśli zestaw nadrzędny jest zainstalowany w globalnej pamięci podręcznej zestawów, sondy środowiska wykonawczego w pamięci podręcznej, ale nie w katalogu aplikacji.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-205">If the parent assembly is installed in the global assembly cache, the runtime probes in the cache but not in the application's directory.</span></span> <span data-ttu-id="ab8cd-206">Jeśli zestaw nadrzędny jest zainstalowany w katalogu aplikacji, sondy środowiska wykonawczego w katalogu aplikacji, ale nie w globalnej pamięci podręcznej zestawów.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-206">If the parent assembly is installed in an application directory, the runtime probes in the application directory but not in the global assembly cache.</span></span>

- <span data-ttu-id="ab8cd-207">Środowisko wykonawcze nie wysyła zapytania do Instalatora Windows w celu instalacji na żądanie zestawów satelickich.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-207">The runtime doesn't query the Windows Installer for on-demand installation of satellite assemblies.</span></span>

- <span data-ttu-id="ab8cd-208">Jeśli sonda dla określonego zestawu zasobów nie powiedzie <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> się, środowisko wykonawcze nie wywołuje zdarzenia.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-208">If the probe for a particular resource assembly fails, the runtime does not raise the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event.</span></span>

### <a name="net-core-resource-fallback-process"></a><span data-ttu-id="ab8cd-209">Proces awaryjny zasobu .NET Core</span><span class="sxs-lookup"><span data-stu-id="ab8cd-209">.NET Core resource fallback process</span></span>

<span data-ttu-id="ab8cd-210">Proces rezerwy zasobu .NET Core obejmuje następujące kroki:</span><span class="sxs-lookup"><span data-stu-id="ab8cd-210">The .NET Core resource fallback process involves the following steps:</span></span>

1. <span data-ttu-id="ab8cd-211">Środowisko wykonawcze próbuje załadować zestaw satelickiego dla żądanej kultury.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-211">The runtime attempts to load a satellite assembly for the requested culture.</span></span>
     - <span data-ttu-id="ab8cd-212">Sprawdza katalog aktualnie wykonywanego zestawu dla podkatalogu, który pasuje do żądanej kultury.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-212">Checks the directory of the currently executing assembly for a subdirectory that matches the requested culture.</span></span> <span data-ttu-id="ab8cd-213">Jeśli znajdzie podkatalog, przeszukuje ten podkatalog dla prawidłowego zestawu satelickiego dla żądanej kultury i ładuje go.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-213">If it finds the subdirectory, it searches that subdirectory for a valid satellite assembly for the requested culture and loads it.</span></span>

       > [!NOTE]
       > <span data-ttu-id="ab8cd-214">W systemach operacyjnych z systemami plików z uwzględnieniem wielkości liter (czyli Linux i macOS) wyszukiwanie podkatalogu nazwy kultury jest rozróżniane.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-214">On operating systems with case-sensitive file systems (that is, Linux and macOS), the culture name subdirectory search is case-sensitive.</span></span> <span data-ttu-id="ab8cd-215">Nazwa podkatalogu musi być dokładnie <xref:System.Globalization.CultureInfo.Name?displayProperty=nameWithType> zgodna z `es` `es-MX`przypadkiem (na przykład lub ).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-215">The subdirectory name must exactly match the case of the <xref:System.Globalization.CultureInfo.Name?displayProperty=nameWithType> (for example, `es` or `es-MX`).</span></span>

       > [!NOTE]
       > <span data-ttu-id="ab8cd-216">Jeśli programista wyprowadził niestandardowy kontekst <xref:System.Runtime.Loader.AssemblyLoadContext>obciążenia zestawu z , sytuacja jest skomplikowana.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-216">If the programmer has derived a custom assembly load context from <xref:System.Runtime.Loader.AssemblyLoadContext>, the situation is complicated.</span></span> <span data-ttu-id="ab8cd-217">Jeśli zestaw wykonujący został załadowany do kontekstu niestandardowego, środowisko wykonawcze ładuje zestaw satelickiego w kontekście niestandardowym.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-217">If the executing assembly was loaded into the custom context, the runtime loads the satellite assembly into the custom context.</span></span> <span data-ttu-id="ab8cd-218">Szczegóły są poza zakresem dla tego dokumentu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-218">The details are out of scope for this document.</span></span> <span data-ttu-id="ab8cd-219">Zobacz <xref:System.Runtime.Loader.AssemblyLoadContext>.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-219">See  <xref:System.Runtime.Loader.AssemblyLoadContext>.</span></span>

     - <span data-ttu-id="ab8cd-220">Jeśli nie znaleziono zestawu satelickiego, <xref:System.Runtime.Loader.AssemblyLoadContext> wywołuje <xref:System.Runtime.Loader.AssemblyLoadContext.Resolving?displayProperty=nameWithType> zdarzenie, aby wskazać, że nie jest w stanie znaleźć zestawu satelickiego.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-220">If a satellite assemble has not been found, the <xref:System.Runtime.Loader.AssemblyLoadContext> raises the <xref:System.Runtime.Loader.AssemblyLoadContext.Resolving?displayProperty=nameWithType> event to indicate that it is unable to find the satellite assembly.</span></span> <span data-ttu-id="ab8cd-221">Jeśli zdecydujesz się obsłużyć zdarzenie, program obsługi zdarzeń można załadować i zwrócić odwołanie do zestawu satelickiego.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-221">If you choose to handle the event, your event handler can load and return a reference to the satellite assembly.</span></span>
     - <span data-ttu-id="ab8cd-222">Jeśli zestaw satelickiego nadal nie został znaleziony, AssemblyLoadContext powoduje, że AppDomain wyzwolić <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> zdarzenie, aby wskazać, że nie można znaleźć zestawu satelickiego.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-222">If a satellite assembly still has not been found, the AssemblyLoadContext causes the AppDomain to trigger an <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event to indicate that it is unable to find the satellite assembly.</span></span> <span data-ttu-id="ab8cd-223">Jeśli zdecydujesz się obsłużyć zdarzenie, program obsługi zdarzeń można załadować i zwrócić odwołanie do zestawu satelickiego.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-223">If you choose to handle the event, your event handler can load and return a reference to the satellite assembly.</span></span>

2. <span data-ttu-id="ab8cd-224">Jeśli zostanie znaleziony zestaw satelickiego, środowisko wykonawcze przeszukuje go dla żądanego zasobu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-224">If a satellite assembly is found, the runtime searches it for the requested resource.</span></span> <span data-ttu-id="ab8cd-225">Jeśli znajdzie zasób w zestawie, używa go.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-225">If it finds the resource in the assembly, it uses it.</span></span> <span data-ttu-id="ab8cd-226">Jeśli nie znajdzie zasobu, kontynuuje wyszukiwanie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-226">If it doesn't find the resource, it continues the search.</span></span>

     > [!NOTE]
     > <span data-ttu-id="ab8cd-227">Aby znaleźć zasób w zestawie satelicie, środowisko wykonawcze <xref:System.Resources.ResourceManager> wyszukuje plik zasobu żądany przez bieżący <xref:System.Globalization.CultureInfo.Name?displayProperty=nameWithType>plik .</span><span class="sxs-lookup"><span data-stu-id="ab8cd-227">To find a resource within the satellite assembly, the runtime searches for the resource file requested by the <xref:System.Resources.ResourceManager> for the current <xref:System.Globalization.CultureInfo.Name?displayProperty=nameWithType>.</span></span> <span data-ttu-id="ab8cd-228">W pliku zasobu wyszukuje żądaną nazwę zasobu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-228">Within the resource file it searches for the requested resource name.</span></span> <span data-ttu-id="ab8cd-229">Jeśli którykolwiek nie zostanie znaleziony, zasób jest traktowany jako nie znaleziono.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-229">If either is not found, the resource is treated as not found.</span></span>

3. <span data-ttu-id="ab8cd-230">Środowisko uruchomieniowe następnie przeszukuje nadrzędnych zestawów kultury za pomocą wielu potencjalnych poziomów, za każdym razem powtarzając kroki 1 & 2.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-230">The runtime next searches the parent culture assemblies through many potential levels, each time repeating steps 1 & 2.</span></span>

     <span data-ttu-id="ab8cd-231">Kultury nadrzędnej jest zdefiniowany jako kultury rezerwowej odpowiednie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-231">The parent culture is defined as an appropriate fallback culture.</span></span> <span data-ttu-id="ab8cd-232">Należy wziąć pod uwagę rodziców jako rezerwowych kandydatów, ponieważ zapewnienie dowolnego zasobu jest lepsze niż zgłaszanie wyjątku.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-232">Consider parents as fallback candidates, because providing any resource is preferable to throwing an exception.</span></span> <span data-ttu-id="ab8cd-233">Ten proces umożliwia również ponowne użycie zasobów.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-233">This process also allows you to reuse resources.</span></span> <span data-ttu-id="ab8cd-234">Należy dołączyć określonego zasobu na poziomie nadrzędnym tylko wtedy, gdy kultury podrzędnej nie trzeba lokalizować żądanego zasobu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-234">You should include a particular resource at the parent level only if the child culture doesn't need to localize the requested resource.</span></span> <span data-ttu-id="ab8cd-235">Na przykład jeśli dostarczasz zestawy `en` satelitarne `en-GB` dla (neutralny angielski), (angielski używany w Wielkiej `en-US` Brytanii) `en` i (angielski używany w `en-GB` `en-US` Stanach Zjednoczonych), satelita zawiera wspólną terminologię, a satelity i satelity zapewniają zastąpienia tylko tych terminów, które różnią się.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-235">For example, if you supply satellite assemblies for `en` (neutral English), `en-GB` (English as spoken in the United Kingdom), and `en-US` (English as spoken in the United States), the `en` satellite contains the common terminology, and the `en-GB` and `en-US` satellites provides overrides for only those terms that differ.</span></span>

     <span data-ttu-id="ab8cd-236">Każda kultura ma tylko jeden element <xref:System.Globalization.CultureInfo.Parent%2A?displayProperty=nameWithType> nadrzędny, który jest zdefiniowany przez właściwość, ale element nadrzędny może mieć własny element nadrzędny.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-236">Each culture has only one parent, which is defined by the <xref:System.Globalization.CultureInfo.Parent%2A?displayProperty=nameWithType> property, but a parent might have its own parent.</span></span> <span data-ttu-id="ab8cd-237">Wyszukiwanie kultur nadrzędnych zatrzymuje się, <xref:System.Globalization.CultureInfo.Parent%2A> gdy <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType>właściwość kultury zwraca .</span><span class="sxs-lookup"><span data-stu-id="ab8cd-237">The search for parent cultures stops when a culture's <xref:System.Globalization.CultureInfo.Parent%2A> property returns <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="ab8cd-238">W przypadku rezerwowego zasobu niezmienna kultura nie jest uważana za kulturę nadrzędną ani kulturę, która może mieć zasoby.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-238">For resource fallback, the invariant culture is not considered a parent culture or a culture that can have resources.</span></span>

4. <span data-ttu-id="ab8cd-239">Jeśli kultura, która została pierwotnie określona i wszystkie rodzice zostały przeszukane, a zasób nadal nie zostanie znaleziony, używany jest zasób dla kultury domyślnej (rezerwowej).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-239">If the culture that was originally specified and all parents have been searched and the resource is still not found, the resource for the default (fallback) culture is used.</span></span> <span data-ttu-id="ab8cd-240">Zazwyczaj zasoby dla kultury domyślnej są uwzględniane w głównym zestawie aplikacji.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-240">Typically, the resources for the default culture are included in the main application assembly.</span></span> <span data-ttu-id="ab8cd-241">Można jednak określić wartość <xref:System.Resources.UltimateResourceFallbackLocation.Satellite?displayProperty.nameWithType> dla <xref:System.Resources.NeutralResourcesLanguageAttribute.Location%2A> właściwości, aby wskazać, że ostateczna lokalizacja rezerwowa dla zasobów jest zestawem satelickiego, a nie głównym złożeniem.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-241">However, you can specify a value of <xref:System.Resources.UltimateResourceFallbackLocation.Satellite?displayProperty.nameWithType> for the <xref:System.Resources.NeutralResourcesLanguageAttribute.Location%2A> property to indicate that the ultimate fallback location for resources is a satellite assembly rather than the main assembly.</span></span>

    > [!NOTE]
    > <span data-ttu-id="ab8cd-242">Zasób domyślny jest jedynym zasobem, który można skompilować z zespołem głównym.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-242">The default resource is the only resource that can be compiled with the main assembly.</span></span> <span data-ttu-id="ab8cd-243">O ile nie określisz <xref:System.Resources.NeutralResourcesLanguageAttribute> zestawu satelickiego przy użyciu atrybutu, jest to ostateczny rezerwowy (końcowy element nadrzędny).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-243">Unless you specify a satellite assembly by using the <xref:System.Resources.NeutralResourcesLanguageAttribute> attribute, it is the ultimate fallback (final parent).</span></span> <span data-ttu-id="ab8cd-244">W związku z tym zaleca się, aby zawsze zawierać domyślny zestaw zasobów w zestawie głównym.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-244">Therefore, we recommend that you always include a default set of resources in your main assembly.</span></span> <span data-ttu-id="ab8cd-245">Pomaga to zapobiec zgłaszaniu wyjątków.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-245">This helps prevent exceptions from being thrown.</span></span> <span data-ttu-id="ab8cd-246">Dołączając domyślny plik zasobu, należy podać rezerwowe dla wszystkich zasobów i upewnij się, że co najmniej jeden zasób jest zawsze obecny dla użytkownika, nawet jeśli nie jest specyficzny kulturowo.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-246">By including a default resource file, you provide a fallback for all resources and ensure that at least one resource is always present for the user, even if it is not culturally specific.</span></span>

5. <span data-ttu-id="ab8cd-247">Na koniec, jeśli środowisko wykonawcze nie znajdzie pliku zasobów dla domyślnej <xref:System.Resources.MissingManifestResourceException> <xref:System.Resources.MissingSatelliteAssemblyException> kultury (rezerwowej), zostanie zgłoszony wyjątek lub wyjątek wskazujący, że nie można odnaleźć zasobu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-247">Finally, if the runtime doesn't find a resource file for a default (fallback) culture, a <xref:System.Resources.MissingManifestResourceException> or <xref:System.Resources.MissingSatelliteAssemblyException> exception is thrown to indicate that the resource could not be found.</span></span> <span data-ttu-id="ab8cd-248">Jeśli plik zasobu zostanie znaleziony, ale żądany `null`zasób nie jest obecny, żądanie zwraca .</span><span class="sxs-lookup"><span data-stu-id="ab8cd-248">If the resource file is found but the requested resource is not present the request returns `null`.</span></span>

### <a name="ultimate-fallback-to-satellite-assembly"></a><span data-ttu-id="ab8cd-249">Ostateczny powrót do zestawu satelickiego</span><span class="sxs-lookup"><span data-stu-id="ab8cd-249">Ultimate Fallback to Satellite Assembly</span></span>

<span data-ttu-id="ab8cd-250">Opcjonalnie można usunąć zasoby z głównego zestawu i określić, że środowisko wykonawcze należy załadować ostateczny rezerwowy zasobów z zestawu satelickiego, który odpowiada określonej kultury.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-250">You can optionally remove resources from the main assembly and specify that the runtime should load the ultimate fallback resources from a satellite assembly that corresponds to a specific culture.</span></span> <span data-ttu-id="ab8cd-251">Aby kontrolować proces rezerwowy, należy <xref:System.Resources.NeutralResourcesLanguageAttribute.%23ctor%28System.String%2CSystem.Resources.UltimateResourceFallbackLocation%29> użyć konstruktora <xref:System.Resources.UltimateResourceFallbackLocation> i podać wartość parametru, który określa, czy Menedżer zasobów należy wyodrębnić rezerwowych zasobów z głównego zestawu lub z zestawu satelickiego.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-251">To control the fallback process, you use the <xref:System.Resources.NeutralResourcesLanguageAttribute.%23ctor%28System.String%2CSystem.Resources.UltimateResourceFallbackLocation%29> constructor and supply a value for the <xref:System.Resources.UltimateResourceFallbackLocation> parameter that specifies whether Resource Manager should extract the fallback resources from the main assembly or from a satellite assembly.</span></span>

<span data-ttu-id="ab8cd-252">W poniższym przykładzie .NET Framework używa atrybutu <xref:System.Resources.NeutralResourcesLanguageAttribute> do przechowywania rezerwowych zasobów aplikacji`fr`w zestawie satelicie dla języka francuskiego ( ).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-252">The following .NET Framework example uses the <xref:System.Resources.NeutralResourcesLanguageAttribute> attribute to store an application's fallback resources in a satellite assembly for the French (`fr`) language.</span></span> <span data-ttu-id="ab8cd-253">W przykładzie znajdują się dwa tekstowe pliki `Greeting`zasobów definiujące pojedynczy zasób o nazwie .</span><span class="sxs-lookup"><span data-stu-id="ab8cd-253">The example has two text-based resource files that define a single string resource named `Greeting`.</span></span> <span data-ttu-id="ab8cd-254">Pierwszy, resources.fr.txt, zawiera zasób języka francuskiego.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-254">The first, resources.fr.txt, contains a French language resource.</span></span>

```text
Greeting=Bon jour!
```

<span data-ttu-id="ab8cd-255">Drugi, resources,ru.txt, zawiera zasób języka rosyjskiego.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-255">The second, resources,ru.txt, contains a Russian language resource.</span></span>

```text
Greeting=Добрый день
```

<span data-ttu-id="ab8cd-256">Te dwa pliki są kompilowane do plików .resources, uruchamiając [generator plików zasobów (Resgen.exe)](../tools/resgen-exe-resource-file-generator.md) z wiersza polecenia.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-256">These two files are compiled to .resources files by running the [resource file generator (Resgen.exe)](../tools/resgen-exe-resource-file-generator.md) from the command line.</span></span> <span data-ttu-id="ab8cd-257">W przypadku zasobu języka francuskiego polecenie jest następujące:</span><span class="sxs-lookup"><span data-stu-id="ab8cd-257">For the French language resource, the command is:</span></span>

<span data-ttu-id="ab8cd-258">**resgen.exe resources.fr.txt**</span><span class="sxs-lookup"><span data-stu-id="ab8cd-258">**resgen.exe resources.fr.txt**</span></span>

<span data-ttu-id="ab8cd-259">Dla zasobu języka rosyjskiego polecenie jest następujące:</span><span class="sxs-lookup"><span data-stu-id="ab8cd-259">For the Russian language resource, the command is:</span></span>

<span data-ttu-id="ab8cd-260">**resgen.exe resources.ru.txt**</span><span class="sxs-lookup"><span data-stu-id="ab8cd-260">**resgen.exe resources.ru.txt**</span></span>

<span data-ttu-id="ab8cd-261">Pliki .resources są osadzone w bibliotekach łączy dynamicznych przez uruchomienie [konsolidatora zestawu (Al.exe)](../tools/al-exe-assembly-linker.md) z wiersza polecenia zasobu języka francuskiego w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="ab8cd-261">The .resources files are embedded into dynamic link libraries by running [assembly linker (Al.exe)](../tools/al-exe-assembly-linker.md) from the command line for the French language resource as follows:</span></span>

<span data-ttu-id="ab8cd-262">**al /t:lib /embed:resources.fr.resources /culture:fr /out:fr\Example1.resources.dll**</span><span class="sxs-lookup"><span data-stu-id="ab8cd-262">**al /t:lib /embed:resources.fr.resources /culture:fr /out:fr\Example1.resources.dll**</span></span>

<span data-ttu-id="ab8cd-263">oraz dla zasobu języka rosyjskiego w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="ab8cd-263">and for the Russian language resource as follows:</span></span>

<span data-ttu-id="ab8cd-264">**al /t:lib /embed:resources.ru.resources /culture:ru /out:ru\Example1.resources.dll**</span><span class="sxs-lookup"><span data-stu-id="ab8cd-264">**al /t:lib /embed:resources.ru.resources /culture:ru /out:ru\Example1.resources.dll**</span></span>

<span data-ttu-id="ab8cd-265">Kod źródłowy aplikacji znajduje się w pliku o nazwie Example1.cs lub Example1.vb.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-265">The application source code resides in a file named Example1.cs or Example1.vb.</span></span> <span data-ttu-id="ab8cd-266">Zawiera atrybut <xref:System.Resources.NeutralResourcesLanguageAttribute> wskazujący, że domyślny zasób aplikacji znajduje się w podkatalogu fr.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-266">It includes the <xref:System.Resources.NeutralResourcesLanguageAttribute> attribute to indicate that the default application resource is in the fr subdirectory.</span></span> <span data-ttu-id="ab8cd-267">Tworzenie wystąpienia Menedżera zasobów, pobiera wartość zasobu `Greeting` i wyświetla go w konsoli.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-267">It instantiates the Resource Manager, retrieves the value of the `Greeting` resource, and displays it to the console.</span></span>

[!code-csharp[Conceptual.Resources.Packaging#1](~/samples/snippets/csharp/VS_Snippets_CLR/conceptual.resources.packaging/cs/example1.cs#1)]
[!code-vb[Conceptual.Resources.Packaging#1](~/samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.resources.packaging/vb/example1.vb#1)]

<span data-ttu-id="ab8cd-268">Następnie można skompilować kod źródłowy języka C# z wiersza polecenia w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="ab8cd-268">You can then compile C# source code from the command line as follows:</span></span>

```console
csc Example1.cs
```

<span data-ttu-id="ab8cd-269">Polecenie dla kompilatora języka Visual Basic jest bardzo podobne:</span><span class="sxs-lookup"><span data-stu-id="ab8cd-269">The command for the Visual Basic compiler is very similar:</span></span>

```console
vbc Example1.vb
```

<span data-ttu-id="ab8cd-270">Ponieważ nie ma żadnych zasobów osadzonych w zestawie głównym, nie `/resource` trzeba kompilować przy użyciu przełącznika.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-270">Because there are no resources embedded in the main assembly, you do not have to compile by using the `/resource` switch.</span></span>

<span data-ttu-id="ab8cd-271">Po uruchomieniu przykład z systemu, którego język jest inny niż rosyjski, wyświetla następujące dane wyjściowe:</span><span class="sxs-lookup"><span data-stu-id="ab8cd-271">When you run the example from a system whose language is anything other than Russian, it displays the following output:</span></span>

```output
Bon jour!
```

## <a name="suggested-packaging-alternative"></a><span data-ttu-id="ab8cd-272">Sugerowane alternatywne opakowanie</span><span class="sxs-lookup"><span data-stu-id="ab8cd-272">Suggested Packaging Alternative</span></span>

<span data-ttu-id="ab8cd-273">Ograniczenia czasu lub budżetu mogą uniemożliwić utworzenie zestawu zasobów dla każdej subkultury, która obsługuje aplikację.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-273">Time or budget constraints might prevent you from creating a set of resources for every subculture that your application supports.</span></span> <span data-ttu-id="ab8cd-274">Zamiast tego można utworzyć pojedynczy zestaw satelickiego dla kultury nadrzędnej, której mogą używać wszystkie powiązane subkultury.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-274">Instead, you can create a single satellite assembly for a parent culture that all related subcultures can use.</span></span> <span data-ttu-id="ab8cd-275">Na przykład można podać jeden angielski zestaw satelijski (en), który jest pobierany przez użytkowników, którzy żądają zasobów w języku angielskim specyficznych dla regionu, i jeden niemiecki zestaw satelijski (de) dla użytkowników, którzy żądają zasobów niemieckich specyficznych dla regionu.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-275">For example, you can provide a single English satellite assembly (en) that is retrieved by users who request region-specific English resources, and a single German satellite assembly (de) for users who request region-specific German resources.</span></span> <span data-ttu-id="ab8cd-276">Na przykład żądania dotyczące języka niemieckiego w Niemczech (de-DE), Austrii (de-AT) i Szwajcarii (de-CH) spadłyby z powrotem do niemieckiego zgromadzenia satelitarnego (de).</span><span class="sxs-lookup"><span data-stu-id="ab8cd-276">For example, requests for German as spoken in Germany (de-DE), Austria (de-AT), and Switzerland (de-CH) would fall back to the German satellite assembly (de).</span></span> <span data-ttu-id="ab8cd-277">Zasoby domyślne są ostatecznym rezerwowym i dlatego powinny być zasoby, które będą wymagane przez większość użytkowników aplikacji, więc wybierz te zasoby ostrożnie.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-277">The default resources are the final fallback and therefore should be the resources that will be requested by the majority of your application's users, so choose these resources carefully.</span></span> <span data-ttu-id="ab8cd-278">Takie podejście wdraża zasoby, które są mniej specyficzne dla kultury, ale może znacznie zmniejszyć koszty lokalizacji aplikacji.</span><span class="sxs-lookup"><span data-stu-id="ab8cd-278">This approach deploys resources that are less culturally specific, but can significantly reduce your application's localization costs.</span></span>

## <a name="see-also"></a><span data-ttu-id="ab8cd-279">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="ab8cd-279">See also</span></span>

- [<span data-ttu-id="ab8cd-280">Zasoby w aplikacjach klasycznych</span><span class="sxs-lookup"><span data-stu-id="ab8cd-280">Resources in Desktop Apps</span></span>](index.md)
- [<span data-ttu-id="ab8cd-281">Global Assembly Cache</span><span class="sxs-lookup"><span data-stu-id="ab8cd-281">Global Assembly Cache</span></span>](../app-domains/gac.md)
- [<span data-ttu-id="ab8cd-282">Tworzenie plików zasobów</span><span class="sxs-lookup"><span data-stu-id="ab8cd-282">Creating Resource Files</span></span>](creating-resource-files-for-desktop-apps.md)
- [<span data-ttu-id="ab8cd-283">Tworzenie zestawów satelickich</span><span class="sxs-lookup"><span data-stu-id="ab8cd-283">Creating Satellite Assemblies</span></span>](creating-satellite-assemblies-for-desktop-apps.md)
