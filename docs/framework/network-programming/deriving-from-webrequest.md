---
title: Wyprowadzanie z elementu WebRequest
ms.date: 03/30/2017
helpviewer_keywords:
- WebRequest class, pluggable protocols
- protocol-specific request handler
- sending data, pluggable protocols
- pluggable protocols, class criteria
- Internet, pluggable protocols
- receiving data, pluggable protocols
- protocols, pluggable
ms.assetid: 9810c177-973e-43d7-823c-14960bd625ea
ms.openlocfilehash: 6bee864f8d24076d16f226c29d61801e856739d9
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/15/2020
ms.locfileid: "71048599"
---
# <a name="deriving-from-webrequest"></a><span data-ttu-id="3f23e-102">Wyprowadzanie z elementu WebRequest</span><span class="sxs-lookup"><span data-stu-id="3f23e-102">Deriving from WebRequest</span></span>
<span data-ttu-id="3f23e-103">Klasa <xref:System.Net.WebRequest> jest abstrakcyjną klasą podstawową, która udostępnia podstawowe metody i właściwości tworzenia programu obsługi żądań specyficznych dla protokołu, który pasuje do modelu protokołu .NET Framework pluggable protocol.</span><span class="sxs-lookup"><span data-stu-id="3f23e-103">The <xref:System.Net.WebRequest> class is an abstract base class that provides the basic methods and properties for creating a protocol-specific request handler that fits the .NET Framework pluggable protocol model.</span></span> <span data-ttu-id="3f23e-104">Aplikacje korzystające z klasy **WebRequest** mogą żądać danych przy użyciu dowolnego obsługiwanego protokołu bez konieczności określania używanego protokołu.</span><span class="sxs-lookup"><span data-stu-id="3f23e-104">Applications that use the **WebRequest** class can request data using any supported protocol without needing to specify the protocol used.</span></span>  
  
 <span data-ttu-id="3f23e-105">Muszą być spełnione dwa kryteria, aby klasa specyficzne dla protokołu była używana jako protokół <xref:System.Net.IWebRequestCreate> pluggable: Klasa <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> musi implementować interfejs i musi zarejestrować się przy użyciu metody.</span><span class="sxs-lookup"><span data-stu-id="3f23e-105">Two criteria must be met in order for a protocol-specific class to be used as a pluggable protocol: The class must implement the <xref:System.Net.IWebRequestCreate> interface, and it must register with the <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="3f23e-106">Klasa musi zastąpić wszystkie metody abstrakcyjne i właściwości **WebRequest,** aby zapewnić interfejs pluggable.</span><span class="sxs-lookup"><span data-stu-id="3f23e-106">The class must override all the abstract methods and properties of **WebRequest** to provide the pluggable interface.</span></span>  
  
 <span data-ttu-id="3f23e-107">**WebRequest** wystąpienia są przeznaczone do jednorazowego użytku; jeśli chcesz złożyć kolejne żądanie, utwórz nowy **WebRequest**.</span><span class="sxs-lookup"><span data-stu-id="3f23e-107">**WebRequest** instances are intended for one-time use; if you want to make another request, create a new **WebRequest**.</span></span> <span data-ttu-id="3f23e-108">**WebRequest** obsługuje <xref:System.Runtime.Serialization.ISerializable> interfejs, aby umożliwić deweloperom serializacji **szablonu WebRequest,** a następnie zrekonstruować szablon dla dodatkowych żądań.</span><span class="sxs-lookup"><span data-stu-id="3f23e-108">**WebRequest** supports the <xref:System.Runtime.Serialization.ISerializable> interface to enable developers to serialize a template **WebRequest** and then reconstruct the template for additional requests.</span></span>  
  
## <a name="iwebrequest-create-method"></a><span data-ttu-id="3f23e-109">Metoda tworzenia iWebRequest</span><span class="sxs-lookup"><span data-stu-id="3f23e-109">IWebRequest Create Method</span></span>  
 <span data-ttu-id="3f23e-110">Metoda <xref:System.Net.IWebRequestCreate.Create%2A> jest odpowiedzialna za inicjowanie nowego wystąpienia klasy specyficznej dla protokołu.</span><span class="sxs-lookup"><span data-stu-id="3f23e-110">The <xref:System.Net.IWebRequestCreate.Create%2A> method is responsible for initializing a new instance of the protocol-specific class.</span></span> <span data-ttu-id="3f23e-111">Po utworzeniu nowego <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> **WebRequest,** metoda pasuje do żądanego identyfikatora URI z prefiksami URI zarejestrowanymi za pomocą **metody RegisterPrefix.**</span><span class="sxs-lookup"><span data-stu-id="3f23e-111">When a new **WebRequest** is created, the <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> method matches the requested URI with the URI prefixes registered with the **RegisterPrefix** method.</span></span> <span data-ttu-id="3f23e-112">Create **Create** Metoda właściwego protokołu specyficznego elementa podrzędnego musi zwracać zainicjowane wystąpienie elementa podrzędnego zdolnego do wykonywania standardowej transakcji żądania/odpowiedzi dla protokołu bez konieczności modyfikowanego pola specyficznego dla protokołu.</span><span class="sxs-lookup"><span data-stu-id="3f23e-112">The **Create** method of the proper protocol-specific descendant must return an initialized instance of the descendant capable of performing a standard request/response transaction for the protocol without needing any protocol-specific fields modified.</span></span>  
  
## <a name="connectiongroupname-property"></a><span data-ttu-id="3f23e-113">Właściwość ConnectionGroupName</span><span class="sxs-lookup"><span data-stu-id="3f23e-113">ConnectionGroupName Property</span></span>  
 <span data-ttu-id="3f23e-114">Właściwość <xref:System.Net.WebRequest.ConnectionGroupName%2A> jest używana do nazwy grupy połączeń z zasobem, dzięki czemu można nawiązywać wiele żądań za pośrednictwem jednego połączenia.</span><span class="sxs-lookup"><span data-stu-id="3f23e-114">The <xref:System.Net.WebRequest.ConnectionGroupName%2A> property is used to name a group of connections to a resource so that multiple requests can be made over a single connection.</span></span> <span data-ttu-id="3f23e-115">Aby zaimplementować udostępnianie połączeń, należy użyć specyficznej dla protokołu metody buforowania i przypisywania połączeń.</span><span class="sxs-lookup"><span data-stu-id="3f23e-115">To implement connection-sharing, you must use a protocol-specific method of pooling and assigning connections.</span></span> <span data-ttu-id="3f23e-116">Na przykład podana <xref:System.Net.ServicePointManager> klasa implementuje <xref:System.Net.HttpWebRequest> udostępnianie połączenia dla klasy.</span><span class="sxs-lookup"><span data-stu-id="3f23e-116">For example, the provided <xref:System.Net.ServicePointManager> class implements connection sharing for the <xref:System.Net.HttpWebRequest> class.</span></span> <span data-ttu-id="3f23e-117">**Klasa ServicePointManager** <xref:System.Net.ServicePoint> tworzy, który zapewnia połączenie z określonym serwerem dla każdej grupy połączeń.</span><span class="sxs-lookup"><span data-stu-id="3f23e-117">The **ServicePointManager** class creates a <xref:System.Net.ServicePoint> that provides a connection to a specific server for each connection group.</span></span>  
  
## <a name="contentlength-property"></a><span data-ttu-id="3f23e-118">Właściwość ContentLength</span><span class="sxs-lookup"><span data-stu-id="3f23e-118">ContentLength Property</span></span>  
 <span data-ttu-id="3f23e-119">Właściwość <xref:System.Net.WebRequest.ContentLength%2A> określa liczbę bajtów danych, które będą wysyłane do serwera podczas przekazywania danych.</span><span class="sxs-lookup"><span data-stu-id="3f23e-119">The <xref:System.Net.WebRequest.ContentLength%2A> property specifies the number of bytes of data that will be sent to the server when uploading data.</span></span>  
  
 <span data-ttu-id="3f23e-120">Zazwyczaj <xref:System.Net.WebRequest.Method%2A> właściwość musi być ustawiona, aby wskazać, że przekazywanie odbywa się, gdy **ContentLength** właściwość jest ustawiona na wartość większą niż zero.</span><span class="sxs-lookup"><span data-stu-id="3f23e-120">Typically the <xref:System.Net.WebRequest.Method%2A> property must be set to indicate that an upload is taking place when the **ContentLength** property is set to a value greater than zero.</span></span>  
  
## <a name="contenttype-property"></a><span data-ttu-id="3f23e-121">Właściwość ContentType</span><span class="sxs-lookup"><span data-stu-id="3f23e-121">ContentType Property</span></span>  
 <span data-ttu-id="3f23e-122">Właściwość <xref:System.Net.WebRequest.ContentType%2A> zawiera wszelkie specjalne informacje, które protokół wymaga wysłania do serwera w celu zidentyfikowania typu zawartości, które wysyłasz.</span><span class="sxs-lookup"><span data-stu-id="3f23e-122">The <xref:System.Net.WebRequest.ContentType%2A> property provides any special information that your protocol requires you to send to the server to identify the type of content that you are sending.</span></span> <span data-ttu-id="3f23e-123">Zazwyczaj jest to typ zawartości MIME wszystkich przesłanych danych.</span><span class="sxs-lookup"><span data-stu-id="3f23e-123">Typically this is the MIME content type of any data uploaded.</span></span>  
  
## <a name="credentials-property"></a><span data-ttu-id="3f23e-124">Właściwość poświadczenia</span><span class="sxs-lookup"><span data-stu-id="3f23e-124">Credentials Property</span></span>  
 <span data-ttu-id="3f23e-125">Właściwość <xref:System.Net.WebRequest.Credentials%2A> zawiera informacje potrzebne do uwierzytelnienia żądania z serwerem.</span><span class="sxs-lookup"><span data-stu-id="3f23e-125">The <xref:System.Net.WebRequest.Credentials%2A> property contains information needed to authenticate the request with the server.</span></span> <span data-ttu-id="3f23e-126">Należy zaimplementować szczegóły procesu uwierzytelniania protokołu.</span><span class="sxs-lookup"><span data-stu-id="3f23e-126">You must implement the details of the authentication process for your protocol.</span></span> <span data-ttu-id="3f23e-127">Klasa <xref:System.Net.AuthenticationManager> jest odpowiedzialna za uwierzytelnianie żądań i dostarczanie tokenu uwierzytelniania.</span><span class="sxs-lookup"><span data-stu-id="3f23e-127">The <xref:System.Net.AuthenticationManager> class is responsible for authenticating requests and providing an authentication token.</span></span> <span data-ttu-id="3f23e-128">Klasa, która zawiera poświadczenia używane przez protokół <xref:System.Net.ICredentials> musi implementować interfejs.</span><span class="sxs-lookup"><span data-stu-id="3f23e-128">The class that provides the credentials used by your protocol must implement the <xref:System.Net.ICredentials> interface.</span></span>  
  
## <a name="headers-property"></a><span data-ttu-id="3f23e-129">Właściwość nagłówków</span><span class="sxs-lookup"><span data-stu-id="3f23e-129">Headers Property</span></span>  
 <span data-ttu-id="3f23e-130">Właściwość <xref:System.Net.WebRequest.Headers%2A> zawiera dowolną kolekcję pary nazw/wartości metadanych skojarzonych z żądaniem.</span><span class="sxs-lookup"><span data-stu-id="3f23e-130">The <xref:System.Net.WebRequest.Headers%2A> property contains an arbitrary collection of name/value pairs of metadata associated with the request.</span></span> <span data-ttu-id="3f23e-131">Wszystkie metadane wymagane przez protokół, które mogą być wyrażone jako para nazwa/wartość mogą być zawarte w **Headers** właściwości.</span><span class="sxs-lookup"><span data-stu-id="3f23e-131">Any metadata needed by the protocol that can be expressed as a name/value pair can be included in the **Headers** property.</span></span> <span data-ttu-id="3f23e-132">Zazwyczaj te informacje muszą być <xref:System.Net.WebRequest.GetRequestStream%2A> ustawione <xref:System.Net.WebRequest.GetResponse%2A> przed wywołaniem lub metody; po zdaniu żądania metadane są uznawane za tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="3f23e-132">Typically this information must be set before calling the <xref:System.Net.WebRequest.GetRequestStream%2A> or <xref:System.Net.WebRequest.GetResponse%2A> methods; once the request has been made, the metadata is considered read-only.</span></span>  
  
 <span data-ttu-id="3f23e-133">Nie musisz używać właściwości **Nagłówki** do używania metadanych nagłówka.</span><span class="sxs-lookup"><span data-stu-id="3f23e-133">You are not required to use the **Headers** property to use header metadata.</span></span> <span data-ttu-id="3f23e-134">Metadane specyficzne dla protokołu mogą być udostępniane jako właściwości; na przykład <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> właściwość udostępnia nagłówek HTTP **agenta użytkownika.**</span><span class="sxs-lookup"><span data-stu-id="3f23e-134">Protocol-specific metadata can be exposed as properties; for example, the <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> property exposes the **User-Agent** HTTP header.</span></span> <span data-ttu-id="3f23e-135">Podczas uwidaczniania metadanych nagłówka jako właściwości, nie należy zezwalać na tę samą właściwość, aby ustawić przy użyciu **headers** właściwości.</span><span class="sxs-lookup"><span data-stu-id="3f23e-135">When you expose header metadata as a property, you should not allow the same property to be set using the **Headers** property.</span></span>  
  
## <a name="method-property"></a><span data-ttu-id="3f23e-136">Właściwość metody</span><span class="sxs-lookup"><span data-stu-id="3f23e-136">Method Property</span></span>  
 <span data-ttu-id="3f23e-137">Właściwość <xref:System.Net.WebRequest.Method%2A> zawiera zlecenie lub akcję, która żądać prosi serwer do wykonania.</span><span class="sxs-lookup"><span data-stu-id="3f23e-137">The <xref:System.Net.WebRequest.Method%2A> property contains the verb or action that the request is asking the server to perform.</span></span> <span data-ttu-id="3f23e-138">Wartość domyślna dla **Method** właściwość musi włączyć akcję żądania/odpowiedzi standardowej bez konieczności ustawiania żadnych właściwości specyficznych dla protokołu.</span><span class="sxs-lookup"><span data-stu-id="3f23e-138">The default for the **Method** property must enable a standard request/response action without requiring any protocol-specific properties to be set.</span></span> <span data-ttu-id="3f23e-139">Na przykład <xref:System.Net.HttpWebResponse.Method%2A> metoda domyślnie GET, który żąda zasobu z serwera sieci Web i zwraca odpowiedź.</span><span class="sxs-lookup"><span data-stu-id="3f23e-139">For example, the <xref:System.Net.HttpWebResponse.Method%2A> method defaults to GET, which requests a resource from a Web server and returns the response.</span></span>  
  
 <span data-ttu-id="3f23e-140">Zazwyczaj **ContentLength** właściwość musi być ustawiona na wartość większą niż zero, gdy **Method** właściwość jest ustawiona na zlecenie lub akcji, która wskazuje, że przekazywanie ma miejsce.</span><span class="sxs-lookup"><span data-stu-id="3f23e-140">Typically the **ContentLength** property must be set to a value greater than zero when the **Method** property is set to a verb or action that indicates that an upload is taking place.</span></span>  
  
## <a name="preauthenticate-property"></a><span data-ttu-id="3f23e-141">Właściwość preauthenticate</span><span class="sxs-lookup"><span data-stu-id="3f23e-141">PreAuthenticate Property</span></span>  
 <span data-ttu-id="3f23e-142">Aplikacje <xref:System.Net.WebRequest.PreAuthenticate%2A> ustawić właściwość, aby wskazać, że informacje o uwierzytelnianiu powinny być wysyłane z początkowego żądania, a nie czeka na wyzwanie uwierzytelniania.</span><span class="sxs-lookup"><span data-stu-id="3f23e-142">Applications set the <xref:System.Net.WebRequest.PreAuthenticate%2A> property to indicate that authentication information should be sent with the initial request rather than waiting for an authentication challenge.</span></span> <span data-ttu-id="3f23e-143">**Właściwość PreAuthenticate** ma znaczenie tylko wtedy, gdy protokół obsługuje poświadczenia uwierzytelniania wysyłane z żądaniem początkowym.</span><span class="sxs-lookup"><span data-stu-id="3f23e-143">The **PreAuthenticate** property is only meaningful if the protocol supports authentication credentials sent with the initial request.</span></span>  
  
## <a name="proxy-property"></a><span data-ttu-id="3f23e-144">Właściwość serwera proxy</span><span class="sxs-lookup"><span data-stu-id="3f23e-144">Proxy Property</span></span>  
 <span data-ttu-id="3f23e-145">Właściwość <xref:System.Net.WebRequest.Proxy%2A> zawiera <xref:System.Net.IWebProxy> interfejs, który jest używany do uzyskiwania dostępu do żądanego zasobu.</span><span class="sxs-lookup"><span data-stu-id="3f23e-145">The <xref:System.Net.WebRequest.Proxy%2A> property contains an <xref:System.Net.IWebProxy> interface that is used to access the requested resource.</span></span> <span data-ttu-id="3f23e-146">Właściwość **serwera proxy** ma znaczenie tylko wtedy, gdy protokół obsługuje proxied żądań.</span><span class="sxs-lookup"><span data-stu-id="3f23e-146">The **Proxy** property is meaningful only if your protocol supports proxied requests.</span></span> <span data-ttu-id="3f23e-147">Należy ustawić domyślny serwer proxy, jeśli jest wymagany przez protokół.</span><span class="sxs-lookup"><span data-stu-id="3f23e-147">You must set the default proxy if one is required by your protocol.</span></span>  
  
 <span data-ttu-id="3f23e-148">W niektórych środowiskach, takich jak za zaporą firmową, protokół może być wymagany do użycia serwera proxy.</span><span class="sxs-lookup"><span data-stu-id="3f23e-148">In some environments, such as behind a corporate firewall, your protocol might be required to use a proxy.</span></span> <span data-ttu-id="3f23e-149">W takim przypadku należy zaimplementować interfejs **IWebProxy,** aby utworzyć klasę proxy, która będzie działać dla protokołu.</span><span class="sxs-lookup"><span data-stu-id="3f23e-149">In that case, you must implement the **IWebProxy** interface to create a proxy class that will work for your protocol.</span></span>  
  
## <a name="requesturi-property"></a><span data-ttu-id="3f23e-150">Właściwość RequestUri</span><span class="sxs-lookup"><span data-stu-id="3f23e-150">RequestUri Property</span></span>  
 <span data-ttu-id="3f23e-151">Właściwość <xref:System.Net.WebRequest.RequestUri%2A> zawiera identyfikator URI, który został przekazany do **WebRequest.Create** metody.</span><span class="sxs-lookup"><span data-stu-id="3f23e-151">The <xref:System.Net.WebRequest.RequestUri%2A> property contains the URI that was passed to the **WebRequest.Create** method.</span></span> <span data-ttu-id="3f23e-152">Jest tylko do odczytu i nie można go zmienić po utworzeniu **WebRequest.**</span><span class="sxs-lookup"><span data-stu-id="3f23e-152">It is read-only and cannot be changed once the **WebRequest** has been created.</span></span> <span data-ttu-id="3f23e-153">Jeśli protokół obsługuje przekierowanie, odpowiedź może pochodzić z zasobu zidentyfikowanego przez inny identyfikator URI.</span><span class="sxs-lookup"><span data-stu-id="3f23e-153">If your protocol supports redirection, the response can come from a resource identified by a different URI.</span></span> <span data-ttu-id="3f23e-154">Jeśli musisz zapewnić dostęp do identyfikatora URI, który odpowiedział, należy podać dodatkową właściwość zawierającą ten identyfikator URI.</span><span class="sxs-lookup"><span data-stu-id="3f23e-154">If you need to provide access to the URI that responded, you must provide an additional property containing that URI.</span></span>  
  
## <a name="timeout-property"></a><span data-ttu-id="3f23e-155">Właściwość limit czasu</span><span class="sxs-lookup"><span data-stu-id="3f23e-155">Timeout Property</span></span>  
 <span data-ttu-id="3f23e-156">Właściwość <xref:System.Net.WebRequest.Timeout%2A> zawiera czas, w milisekundach, aby czekać przed upłyniem czasu żądania i zgłasza wyjątek.</span><span class="sxs-lookup"><span data-stu-id="3f23e-156">The <xref:System.Net.WebRequest.Timeout%2A> property contains the length of time, in milliseconds, to wait before the request times out and throws an exception.</span></span> <span data-ttu-id="3f23e-157">**Limit czasu** ma zastosowanie tylko do synchronicznych żądań wykonanych za <xref:System.Net.WebRequest.GetResponse%2A> pomocą metody; asynchroniczne żądania należy <xref:System.Net.WebRequest.Abort%2A> użyć metody, aby anulować oczekujące żądanie.</span><span class="sxs-lookup"><span data-stu-id="3f23e-157">**Timeout** applies only to synchronous requests made with the <xref:System.Net.WebRequest.GetResponse%2A> method; asynchronous requests must use the <xref:System.Net.WebRequest.Abort%2A> method to cancel a pending request.</span></span>  
  
 <span data-ttu-id="3f23e-158">Ustawienie **Timeout** właściwość ma znaczenie tylko wtedy, gdy klasa specyficzne dla protokołu implementuje proces limitu czasu.</span><span class="sxs-lookup"><span data-stu-id="3f23e-158">Setting the **Timeout** property is meaningful only if the protocol-specific class implements a time-out process.</span></span>  
  
## <a name="abort-method"></a><span data-ttu-id="3f23e-159">Abort — Metoda</span><span class="sxs-lookup"><span data-stu-id="3f23e-159">Abort Method</span></span>  
 <span data-ttu-id="3f23e-160">Metoda <xref:System.Net.WebRequest.Abort%2A> anuluje oczekujące żądanie asynchroniczne do serwera.</span><span class="sxs-lookup"><span data-stu-id="3f23e-160">The <xref:System.Net.WebRequest.Abort%2A> method cancels a pending asynchronous request to a server.</span></span> <span data-ttu-id="3f23e-161">Po anulowaniu żądania **wywołanie GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**, **BeginGetRequestStream** <xref:System.Net.WebException> lub <xref:System.Net.WebException.Status%2A> **EndGetRequestStream** spowoduje wrzucenie a z właściwością ustawioną na <xref:System.Net.WebExceptionStatus>.</span><span class="sxs-lookup"><span data-stu-id="3f23e-161">After the request has been canceled, calling **GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**, **BeginGetRequestStream**, or **EndGetRequestStream** will throw a <xref:System.Net.WebException> with the <xref:System.Net.WebException.Status%2A> property set to <xref:System.Net.WebExceptionStatus>.</span></span>  
  
## <a name="begingetrequeststream-and-endgetrequeststream-methods"></a><span data-ttu-id="3f23e-162">Metody BeginGetRequestStream i EndGetRequestStream</span><span class="sxs-lookup"><span data-stu-id="3f23e-162">BeginGetRequestStream and EndGetRequestStream Methods</span></span>  
 <span data-ttu-id="3f23e-163">Metoda <xref:System.Net.WebRequest.BeginGetRequestStream%2A> uruchamia asynchroniczne żądanie dla strumienia, który jest używany do przekazywania danych na serwer.</span><span class="sxs-lookup"><span data-stu-id="3f23e-163">The <xref:System.Net.WebRequest.BeginGetRequestStream%2A> method starts an asynchronous request for the stream that is used to upload data to the server.</span></span> <span data-ttu-id="3f23e-164">Metoda <xref:System.Net.WebRequest.EndGetRequestStream%2A> kończy żądanie asynchroniczne i zwraca żądany strumień.</span><span class="sxs-lookup"><span data-stu-id="3f23e-164">The <xref:System.Net.WebRequest.EndGetRequestStream%2A> method completes the asynchronous request and returns the requested stream.</span></span> <span data-ttu-id="3f23e-165">Te metody implementują **GetRequestStream** metody przy użyciu standardowego wzorca asynchronicznym .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="3f23e-165">These methods implement the **GetRequestStream** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="begingetresponse-and-endgetresponse-methods"></a><span data-ttu-id="3f23e-166">Metody BeginGetResponse i EndGetResponse</span><span class="sxs-lookup"><span data-stu-id="3f23e-166">BeginGetResponse and EndGetResponse Methods</span></span>  
 <span data-ttu-id="3f23e-167">Metoda <xref:System.Net.WebRequest.BeginGetResponse%2A> uruchamia żądanie asynchroniczne do serwera.</span><span class="sxs-lookup"><span data-stu-id="3f23e-167">The <xref:System.Net.WebRequest.BeginGetResponse%2A> method starts an asynchronous request to a server.</span></span> <span data-ttu-id="3f23e-168">Metoda <xref:System.Net.WebRequest.EndGetResponse%2A> kończy żądanie asynchroniczne i zwraca żądaną odpowiedź.</span><span class="sxs-lookup"><span data-stu-id="3f23e-168">The <xref:System.Net.WebRequest.EndGetResponse%2A> method completes the asynchronous request and returns the requested response.</span></span> <span data-ttu-id="3f23e-169">Te metody implementują **GetResponse** metody przy użyciu standardowego wzorca asynchroniczne .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="3f23e-169">These methods implement the **GetResponse** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="getrequeststream-method"></a><span data-ttu-id="3f23e-170">Metoda GetRequestStream</span><span class="sxs-lookup"><span data-stu-id="3f23e-170">GetRequestStream Method</span></span>  
 <span data-ttu-id="3f23e-171">Metoda <xref:System.Net.WebRequest.GetRequestStream%2A> zwraca strumień, który jest używany do zapisywania danych na żądanym serwerze.</span><span class="sxs-lookup"><span data-stu-id="3f23e-171">The <xref:System.Net.WebRequest.GetRequestStream%2A> method returns a stream that is used to write data to the requested server.</span></span> <span data-ttu-id="3f23e-172">Strumień zwrócony powinien być strumień tylko do zapisu, który nie szuka; jest przeznaczony jako strumień jednokierunkowy danych, który jest zapisywany na serwerze.</span><span class="sxs-lookup"><span data-stu-id="3f23e-172">The stream returned should be a write-only stream that does not seek; it is intended as a one-way stream of data that is written to the server.</span></span> <span data-ttu-id="3f23e-173">Strumień zwraca false <xref:System.IO.Stream.CanRead%2A> dla <xref:System.IO.Stream.CanSeek%2A> i właściwości i <xref:System.IO.Stream.CanWrite%2A> true dla właściwości.</span><span class="sxs-lookup"><span data-stu-id="3f23e-173">The stream returns false for the <xref:System.IO.Stream.CanRead%2A> and <xref:System.IO.Stream.CanSeek%2A> properties and true for the <xref:System.IO.Stream.CanWrite%2A> property.</span></span>  
  
 <span data-ttu-id="3f23e-174">**GetRequestStream** Metoda zazwyczaj otwiera połączenie z serwerem i przed zwróceniem strumienia, wysyła informacje nagłówka, który wskazuje, że dane są wysyłane do serwera.</span><span class="sxs-lookup"><span data-stu-id="3f23e-174">The **GetRequestStream** method typically opens a connection to the server and, before returning the stream, sends header information that indicates that data is being sent to the server.</span></span> <span data-ttu-id="3f23e-175">Ponieważ **GetRequestStream** rozpoczyna żądanie, ustawienie żadnych właściwości **nagłówka** lub **ContentLength** właściwość jest zazwyczaj niedozwolone po wywołaniu **GetRequestStream**.</span><span class="sxs-lookup"><span data-stu-id="3f23e-175">Because **GetRequestStream** begins the request, setting any **Header** properties or the **ContentLength** property is typically not allowed after calling **GetRequestStream**.</span></span>  
  
## <a name="getresponse-method"></a><span data-ttu-id="3f23e-176">Metoda GetResponse</span><span class="sxs-lookup"><span data-stu-id="3f23e-176">GetResponse Method</span></span>  
 <span data-ttu-id="3f23e-177">Metoda <xref:System.Net.WebRequest.GetResponse%2A> zwraca element podrzędny specyficzne <xref:System.Net.WebResponse> dla protokołu klasy, która reprezentuje odpowiedź z serwera.</span><span class="sxs-lookup"><span data-stu-id="3f23e-177">The <xref:System.Net.WebRequest.GetResponse%2A> method returns a protocol-specific descendant of the <xref:System.Net.WebResponse> class that represents the response from the server.</span></span> <span data-ttu-id="3f23e-178">Chyba że żądanie zostało już zainicjowane przez **GetRequestStream** metody, **GetResponse** metoda tworzy połączenie z zasobem identyfikowane przez **RequestUri**, wysyła informacje nagłówka wskazując typ żądania, a następnie odbiera odpowiedź z zasobu.</span><span class="sxs-lookup"><span data-stu-id="3f23e-178">Unless the request has already been initiated by the **GetRequestStream** method, the **GetResponse** method creates a connection to the resource identified by **RequestUri**, sends header information indicating the type of request being made, and then receives the response from the resource.</span></span>  
  
 <span data-ttu-id="3f23e-179">Po **wywołaniu GetResponse** metoda, wszystkie właściwości powinny być traktowane tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="3f23e-179">Once the **GetResponse** method is called, all properties should be considered read-only.</span></span> <span data-ttu-id="3f23e-180">**WebRequest** wystąpienia są przeznaczone do jednorazowego użytku; jeśli chcesz złożyć kolejne żądanie, należy utworzyć nowy **WebRequest**.</span><span class="sxs-lookup"><span data-stu-id="3f23e-180">**WebRequest** instances are intended for one-time use; if you want to make another request, you should create a new **WebRequest**.</span></span>  
  
 <span data-ttu-id="3f23e-181">**Metoda GetResponse** jest odpowiedzialna za tworzenie odpowiedniego elementa podrzędnego **WebResponse,** który zawiera odpowiedź przychodzącą.</span><span class="sxs-lookup"><span data-stu-id="3f23e-181">The **GetResponse** method is responsible for creating an appropriate **WebResponse** descendant to contain the incoming response.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="3f23e-182">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="3f23e-182">See also</span></span>

- <xref:System.Net.WebRequest>
- <xref:System.Net.HttpWebRequest>
- <xref:System.Net.FileWebRequest>
- [<span data-ttu-id="3f23e-183">Programowanie protokołów podłączanych</span><span class="sxs-lookup"><span data-stu-id="3f23e-183">Programming Pluggable Protocols</span></span>](programming-pluggable-protocols.md)
- [<span data-ttu-id="3f23e-184">Wyprowadzanie z elementu WebResponse</span><span class="sxs-lookup"><span data-stu-id="3f23e-184">Deriving from WebResponse</span></span>](deriving-from-webresponse.md)
