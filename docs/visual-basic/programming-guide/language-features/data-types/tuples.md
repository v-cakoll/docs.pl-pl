---
title: Kolekcje w języku Visual Basic
ms.date: 04/23/2017
helpviewer_keywords:
- tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
ms.openlocfilehash: 146e9c2360cea153d2f487769d5b983516861e8d
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/23/2019
ms.locfileid: "61663300"
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="b686a-102">Krotki (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="b686a-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="b686a-103">Począwszy od 2017 Visual Basic języka Visual Basic oferuje wbudowaną obsługę krotek, który ułatwia tworzenie krotek i uzyskiwania dostępu do elementów krotek łatwiejsze.</span><span class="sxs-lookup"><span data-stu-id="b686a-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="b686a-104">Spójna Kolekcja to struktura danych lekki, która ma określoną liczbę i sekwencję wartości.</span><span class="sxs-lookup"><span data-stu-id="b686a-104">A tuple is a light-weight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="b686a-105">Podczas tworzenia wystąpienia spójnej kolekcji należy zdefiniować numer i typ danych w każdej wartości (lub elementu).</span><span class="sxs-lookup"><span data-stu-id="b686a-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="b686a-106">Na przykład 2-krotka (lub para) ma dwa elementy.</span><span class="sxs-lookup"><span data-stu-id="b686a-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="b686a-107">Może być pierwszym `Boolean` wartości, a drugą jest wartość `String`.</span><span class="sxs-lookup"><span data-stu-id="b686a-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="b686a-108">Ponieważ kolekcje ułatwiają do przechowywania wielu wartości w pojedynczy obiekt, często są one używane jako lekka sposób zwracanie wielu wartości z metody.</span><span class="sxs-lookup"><span data-stu-id="b686a-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="b686a-109">Obsługa krotki wymaga <xref:System.ValueTuple> typu.</span><span class="sxs-lookup"><span data-stu-id="b686a-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="b686a-110">Jeśli nie zainstalowano programu .NET Framework 4.7, należy dodać pakiet NuGet `System.ValueTuple`, który jest dostępny w galerii NuGet.</span><span class="sxs-lookup"><span data-stu-id="b686a-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="b686a-111">Bez tego pakietu może wystąpić błąd kompilacji podobny do "Wstępnie zdefiniowany typ"ValueTuple(Of,,,)"nie został zdefiniowany ani zaimportowany."</span><span class="sxs-lookup"><span data-stu-id="b686a-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="b686a-112">Utworzenie wystąpienia i korzystania z krotki</span><span class="sxs-lookup"><span data-stu-id="b686a-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="b686a-113">Umieszczając nawiasami wiadomości błyskawicznych wartości rozdzielonych przecinkami Utwórz wystąpienie spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="b686a-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="b686a-114">Każda z tych wartości stanie się polem spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="b686a-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="b686a-115">Na przykład, poniższy kod definiuje triple (lub 3-krotka) za pomocą `Date` jako wartość pierwszej `String` jako jego sekunda i `Boolean` jako jego innych.</span><span class="sxs-lookup"><span data-stu-id="b686a-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="b686a-116">Domyślnie nazwa każdego pola w krotce składa się z ciągu `Item` wraz z pozycji liczonego od jednego pola w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="b686a-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="b686a-117">Dla tego 3-krotka `Date` pole jest `Item1`, `String` pole jest `Item2`i `Boolean` pole jest `Item3`.</span><span class="sxs-lookup"><span data-stu-id="b686a-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="b686a-118">Poniższy przykład wyświetla wartości pól spójna kolekcja znajdująca się w poprzednim wierszu kodu</span><span class="sxs-lookup"><span data-stu-id="b686a-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="b686a-119">Pola krotki języka Visual Basic są odczytu / zapisu Po został uruchomiony spójnej kolekcji, można zmodyfikować jego wartości.</span><span class="sxs-lookup"><span data-stu-id="b686a-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="b686a-120">Poniższy przykład modyfikuje dwa z trzech pól krotki utworzony w poprzednim przykładzie i wyświetla wynik.</span><span class="sxs-lookup"><span data-stu-id="b686a-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="b686a-121">Utworzenie wystąpienia i przy użyciu nazwanego krotki</span><span class="sxs-lookup"><span data-stu-id="b686a-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="b686a-122">Zamiast używania nazw domyślnych pól spójnej kolekcji, można utworzyć wystąpienie *o nazwie krotki* , przypisując własnymi nazwami elementów krotki.</span><span class="sxs-lookup"><span data-stu-id="b686a-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="b686a-123">Pola spójnej kolekcji następnie są dostępne przy użyciu ich nazw przypisane *lub* przez ich domyślne nazwy.</span><span class="sxs-lookup"><span data-stu-id="b686a-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="b686a-124">Poniższy przykład tworzy wystąpienie tego samego 3-krotka jak poprzednio, chyba że jawnie nazw pierwsze pole `EventDate`, drugi `Name`, a trzeci `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="b686a-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="b686a-125">Go następnie wyświetla wartości pól modyfikuje je i ponownie wyświetla wartości pól.</span><span class="sxs-lookup"><span data-stu-id="b686a-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="inferred-tuple-element-names"></a><span data-ttu-id="b686a-126">Wywnioskowane nazwy elementów krotki</span><span class="sxs-lookup"><span data-stu-id="b686a-126">Inferred tuple element names</span></span>

<span data-ttu-id="b686a-127">Począwszy od 15.3 programu Visual Basic, Visual Basic można wywnioskować nazwami elementów krotki; nie trzeba przypisać je jawnie.</span><span class="sxs-lookup"><span data-stu-id="b686a-127">Starting with Visual Basic 15.3, Visual Basic can infer the names of tuple elements; you do not have to assign them explicitly.</span></span> <span data-ttu-id="b686a-128">Nazwy wywnioskowanego krotki są przydatne podczas inicjowania spójną kolekcję na podstawie zestawu zmiennych i chcesz, aby nazwa elementu krotki być taka sama jak nazwa zmiennej.</span><span class="sxs-lookup"><span data-stu-id="b686a-128">Inferred tuple names are useful when you initialize a tuple from a set of variables, and you want the tuple element name to be the same as the variable name.</span></span> 

<span data-ttu-id="b686a-129">Poniższy przykład tworzy `stateInfo` spójną kolekcją, która zawiera trzy jawnie nazwane elementy `state`, `stateName`, i `capital`.</span><span class="sxs-lookup"><span data-stu-id="b686a-129">The following example creates a `stateInfo` tuple that contains three explicitly named elements, `state`, `stateName`, and `capital`.</span></span> <span data-ttu-id="b686a-130">Należy zauważyć, że w nazwach elementów, instrukcja inicjowania krotki po prostu przypisuje elementy o wartości zmiennych o identycznej nazwie.</span><span class="sxs-lookup"><span data-stu-id="b686a-130">Note that, in naming the elements, the tuple initialization statement simply assigns the named elements the values of the identically named variables.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#1)]
 
<span data-ttu-id="b686a-131">Ponieważ elementy i zmienne mają taką samą nazwę, kompilator Visual Basic można wywnioskować nazw pól, co ilustruje poniższy przykład.</span><span class="sxs-lookup"><span data-stu-id="b686a-131">Because elements and variables have the same name, the Visual Basic compiler can infer the names of the fields, as the following example shows.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#2)]

<span data-ttu-id="b686a-132">Aby włączyć wywnioskowane nazwy elementów krotki, należy zdefiniować wersji kompilatora języka Visual Basic do użycia w projekcie języka Visual Basic (\*.vbproj) pliku:</span><span class="sxs-lookup"><span data-stu-id="b686a-132">To enable inferred tuple element names, you must define the version of the Visual Basic compiler to use in your Visual Basic project (\*.vbproj) file:</span></span> 

```xml 
<PropertyGroup> 
  <LangVersion>15.3</LangVersion> 
</PropertyGroup> 
```

<span data-ttu-id="b686a-133">Numer wersji może być dowolną wersję kompilatora języka Visual Basic, począwszy od 15.3.</span><span class="sxs-lookup"><span data-stu-id="b686a-133">The version number can be any version of the Visual Basic compiler starting with 15.3.</span></span> <span data-ttu-id="b686a-134">Zamiast kodować na określonej wersji kompilatora, można również określić "najnowszej wersji" jako wartość `LangVersion` można skompilować przy użyciu najnowszej wersji kompilatora języka Visual Basic, zainstalowanych w systemie.</span><span class="sxs-lookup"><span data-stu-id="b686a-134">Rather than hard-coding a specific compiler version, you can also specify "Latest" as the value of `LangVersion` to compile with the most recent version of the Visual Basic compiler installed on your system.</span></span>

<span data-ttu-id="b686a-135">Aby uzyskać więcej informacji, zobacz [ustawienie wersji języka Visual Basic](../../../language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="b686a-135">For more information, see [setting the Visual Basic language version](../../../language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="b686a-136">W niektórych przypadkach kompilator Visual Basic nie można wywnioskować nazwa elementu krotki z nazwy Release candidate, a pole spójnej kolekcji mogą być przywoływane tylko, przy użyciu domyślnej nazwy, takie jak `Item1`, `Item2`itp. Należą do nich następujące elementy:</span><span class="sxs-lookup"><span data-stu-id="b686a-136">In some cases, the Visual Basic compiler cannot infer the tuple element name from the candidate name, and the tuple field can only be referenced using its default name, such as `Item1`, `Item2`, etc. These include:</span></span>

- <span data-ttu-id="b686a-137">Nazwa Release candidate jest taka sama jak nazwa elementu członkowskiego spójnej kolekcji, takie jak `Item3`, `Rest`, lub `ToString`.</span><span class="sxs-lookup"><span data-stu-id="b686a-137">The candidate name is the same as the name of a tuple member, such as `Item3`, `Rest`, or `ToString`.</span></span>

- <span data-ttu-id="b686a-138">Nazwa Release candidate jest zduplikowana w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="b686a-138">The candidate name is duplicated in the tuple.</span></span>
 
<span data-ttu-id="b686a-139">Podczas wnioskowania Nazwa pola nie powiedzie się, Visual Basic nie generuje błąd kompilatora nie jest wyjątek w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="b686a-139">When field name inference fails, Visual Basic does not generate a compiler error, nor is an exception thrown at runtime.</span></span> <span data-ttu-id="b686a-140">Zamiast tego pola krotki musi odwoływać się ich wstępnie zdefiniowanych nazw, takich jak `Item1` i `Item2`.</span><span class="sxs-lookup"><span data-stu-id="b686a-140">Instead, tuple fields must be referenced by their predefined names, such as `Item1` and `Item2`.</span></span> 
  
## <a name="tuples-versus-structures"></a><span data-ttu-id="b686a-141">Kolekcje i struktury</span><span class="sxs-lookup"><span data-stu-id="b686a-141">Tuples versus structures</span></span>

<span data-ttu-id="b686a-142">Krotki języka Visual Basic jest typem wartości, który jest wystąpieniem jednej z **System.ValueTuple** typów ogólnych.</span><span class="sxs-lookup"><span data-stu-id="b686a-142">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="b686a-143">Na przykład `holiday` krotki zdefiniowane w poprzednim przykładzie to wystąpienie <xref:System.ValueTuple%603> struktury.</span><span class="sxs-lookup"><span data-stu-id="b686a-143">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="b686a-144">Zaprojektowano go jako lekka kontener dla danych.</span><span class="sxs-lookup"><span data-stu-id="b686a-144">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="b686a-145">Ponieważ spójnej kolekcji ma na celu ułatwić utworzenia obiektu z wielu elementów danych, brakuje niektórych funkcji, które może mieć strukturze niestandardowej.</span><span class="sxs-lookup"><span data-stu-id="b686a-145">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="b686a-146">Należą do nich następujące elementy:</span><span class="sxs-lookup"><span data-stu-id="b686a-146">These include:</span></span>

- <span data-ttu-id="b686a-147">Niestandardowe elementy członkowskie.</span><span class="sxs-lookup"><span data-stu-id="b686a-147">Custom members.</span></span> <span data-ttu-id="b686a-148">Nie można zdefiniować własne właściwości, metody i zdarzenia dla krotki.</span><span class="sxs-lookup"><span data-stu-id="b686a-148">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="b686a-149">Sprawdzanie poprawności.</span><span class="sxs-lookup"><span data-stu-id="b686a-149">Validation.</span></span> <span data-ttu-id="b686a-150">Nie można zweryfikować danych przypisane do pól.</span><span class="sxs-lookup"><span data-stu-id="b686a-150">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="b686a-151">Niezmienności.</span><span class="sxs-lookup"><span data-stu-id="b686a-151">Immutability.</span></span> <span data-ttu-id="b686a-152">Kolekcje języka Visual Basic są modyfikowalna.</span><span class="sxs-lookup"><span data-stu-id="b686a-152">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="b686a-153">Natomiast w strukturze niestandardowej umożliwia kontrolowanie czy wystąpienie jest modyfikowalną niezmienne.</span><span class="sxs-lookup"><span data-stu-id="b686a-153">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="b686a-154">W przypadku niestandardowych elementów członkowskich, właściwości i sprawdzanie poprawności pól lub niezmienności ważna, należy używać języka Visual Basic [struktury](../../../language-reference/statements/structure-statement.md) instrukcji, aby zdefiniować typ wartości niestandardowych.</span><span class="sxs-lookup"><span data-stu-id="b686a-154">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="b686a-155">Krotki języka Visual Basic dziedziczą członkowie jej **ValueTuple** typu.</span><span class="sxs-lookup"><span data-stu-id="b686a-155">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="b686a-156">Oprócz jego pól należą do nich następujące metody:</span><span class="sxs-lookup"><span data-stu-id="b686a-156">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="b686a-157">Element członkowski</span><span class="sxs-lookup"><span data-stu-id="b686a-157">Member</span></span> | <span data-ttu-id="b686a-158">Opis</span><span class="sxs-lookup"><span data-stu-id="b686a-158">Description</span></span> |
| ---|---|
| <span data-ttu-id="b686a-159">Element compareTo</span><span class="sxs-lookup"><span data-stu-id="b686a-159">CompareTo</span></span> | <span data-ttu-id="b686a-160">Porównuje bieżącą spójną kolekcję do innego krotki z taką samą liczbę elementów.</span><span class="sxs-lookup"><span data-stu-id="b686a-160">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="b686a-161">Równa się</span><span class="sxs-lookup"><span data-stu-id="b686a-161">Equals</span></span> | <span data-ttu-id="b686a-162">Określa, czy bieżącą spójną kolekcję jest równa innego spójnej kolekcji lub obiektu.</span><span class="sxs-lookup"><span data-stu-id="b686a-162">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="b686a-163">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="b686a-163">GetHashCode</span></span> | <span data-ttu-id="b686a-164">Oblicza wartość skrótu dla bieżącego wystąpienia.</span><span class="sxs-lookup"><span data-stu-id="b686a-164">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="b686a-165">ToString</span><span class="sxs-lookup"><span data-stu-id="b686a-165">ToString</span></span> | <span data-ttu-id="b686a-166">Zwraca reprezentację ciągu tego spójna kolekcja, która ma postać `(Item1, Item2...)`, gdzie `Item1` i `Item2` reprezentują wartości pól spójnej.</span><span class="sxs-lookup"><span data-stu-id="b686a-166">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="b686a-167">Ponadto **ValueTuple** typy implementują <xref:System.Collections.IStructuralComparable> i <xref:System.Collections.IStructuralEquatable> interfejsów, które umożliwiają definiowanie porównywanie klienta.</span><span class="sxs-lookup"><span data-stu-id="b686a-167">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="b686a-168">Przypisanie i krotki</span><span class="sxs-lookup"><span data-stu-id="b686a-168">Assignment and tuples</span></span>

<span data-ttu-id="b686a-169">Visual Basic obsługuje przypisanie między typy krotek, które mają taką samą liczbę pól.</span><span class="sxs-lookup"><span data-stu-id="b686a-169">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="b686a-170">Typy pól mogą być konwertowane, jeśli jest spełniony jeden z następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="b686a-170">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="b686a-171">Pole źródłowe i docelowe mają tego samego typu.</span><span class="sxs-lookup"><span data-stu-id="b686a-171">The source and target field are of the same type.</span></span>

- <span data-ttu-id="b686a-172">Zdefiniowano rozszerzającą (lub niejawnie) konwersji typu źródłowego na typ docelowy.</span><span class="sxs-lookup"><span data-stu-id="b686a-172">A widening (or implicit) conversion of the source type to the target type is defined.</span></span> 

- <span data-ttu-id="b686a-173">`Option Strict` jest `On`, i zdefiniowano konwersji zawężającej (lub jawny) typu źródłowego na typ docelowy.</span><span class="sxs-lookup"><span data-stu-id="b686a-173">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="b686a-174">Ta konwersja może zgłosić wyjątek, jeśli wartość źródła znajduje się poza zakresem typu docelowego.</span><span class="sxs-lookup"><span data-stu-id="b686a-174">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="b686a-175">Inne konwersje nie są uwzględniane przydziałów.</span><span class="sxs-lookup"><span data-stu-id="b686a-175">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="b686a-176">Przyjrzyjmy się rodzaje przypisania, które są dozwolone między typami spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="b686a-176">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="b686a-177">Należy wziąć pod uwagę te zmienne używane w następujących przykładach:</span><span class="sxs-lookup"><span data-stu-id="b686a-177">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="b686a-178">Pierwsze dwie zmienne `unnamed` i `anonymous`, nie zawiera semantyczną nazw podane dla pól.</span><span class="sxs-lookup"><span data-stu-id="b686a-178">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="b686a-179">Ich nazwy pól są domyślnie `Item1` i `Item2`.</span><span class="sxs-lookup"><span data-stu-id="b686a-179">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="b686a-180">Ostatnie dwie zmienne `named` i `differentName` mają nazwy pól semantycznego.</span><span class="sxs-lookup"><span data-stu-id="b686a-180">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="b686a-181">Należy pamiętać, że te dwie spójne kolekcje mają różne nazwy pól.</span><span class="sxs-lookup"><span data-stu-id="b686a-181">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="b686a-182">Wszystkie cztery krotek, te mają taką samą liczbę pola (nazywane "liczby argumentów") i typy tych pól są identyczne.</span><span class="sxs-lookup"><span data-stu-id="b686a-182">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="b686a-183">W związku z tym wszystkie te przydziały pracy:</span><span class="sxs-lookup"><span data-stu-id="b686a-183">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="b686a-184">Należy zauważyć, że nazwy kolekcje nie są przypisane.</span><span class="sxs-lookup"><span data-stu-id="b686a-184">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="b686a-185">Wartości pól są przypisywane w kolejności pól w spójnej kolekcji.</span><span class="sxs-lookup"><span data-stu-id="b686a-185">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="b686a-186">Na koniec Zauważ, że można przypisywać `named` krotka `conversion` spójnej kolekcji, nawet jeśli pierwsze pole `named` jest `Integer`, a pierwsze pole `conversion` jest `Long`.</span><span class="sxs-lookup"><span data-stu-id="b686a-186">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="b686a-187">To przypisanie zakończy się pomyślnie, ponieważ konwersja `Integer` do `Long` konwersji rozszerzającej.</span><span class="sxs-lookup"><span data-stu-id="b686a-187">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="b686a-188">Krotki z różną liczbą pól nie są możliwe do przypisania:</span><span class="sxs-lookup"><span data-stu-id="b686a-188">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="b686a-189">Krotki jako wartości zwracane metody</span><span class="sxs-lookup"><span data-stu-id="b686a-189">Tuples as method return values</span></span>

<span data-ttu-id="b686a-190">Metoda może zwracać pojedynczą wartość.</span><span class="sxs-lookup"><span data-stu-id="b686a-190">A method can return only a single value.</span></span> <span data-ttu-id="b686a-191">Często jednak chcesz wywołanie metody zwracanie wielu wartości.</span><span class="sxs-lookup"><span data-stu-id="b686a-191">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="b686a-192">Istnieje kilka sposobów obejścia tego ograniczenia:</span><span class="sxs-lookup"><span data-stu-id="b686a-192">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="b686a-193">Można utworzyć niestandardową klasę lub strukturę którego właściwości lub pól reprezentują wartości zwracanych przez metodę.</span><span class="sxs-lookup"><span data-stu-id="b686a-193">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="b686a-194">Ten sposób jest rozwiązaniem niewielkich; wymaga zdefiniowania typ niestandardowy, którego jedynym celem jest można pobrać wartości z wywołania metody.</span><span class="sxs-lookup"><span data-stu-id="b686a-194">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="b686a-195">Może zwracać pojedynczą wartość z metody, a pozostałe wartości są zwracane, przekazując je przez odwołanie do metody.</span><span class="sxs-lookup"><span data-stu-id="b686a-195">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="b686a-196">Obejmuje to obciążenie związane z wystąpienia zmienną i ryzyko nieodwracalne nadpisanie wartość zmiennej, który jest przekazywany przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="b686a-196">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="b686a-197">Można użyć spójnej kolekcji, która zapewnia rozwiązanie o małych wymaganiach do pobierania z wieloma wartościami zwracanymi.</span><span class="sxs-lookup"><span data-stu-id="b686a-197">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="b686a-198">Na przykład **TryParse** metod .NET powrotu `Boolean` wartość, która wskazuje, czy podczas analizowania operacja zakończyła się pomyślnie.</span><span class="sxs-lookup"><span data-stu-id="b686a-198">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="b686a-199">Wynik operacji analizowania jest zwracany w zmiennej, przekazywany przez odwołanie do metody.</span><span class="sxs-lookup"><span data-stu-id="b686a-199">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="b686a-200">Zazwyczaj wywołanie w celu analizowania metody takie jak <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> wygląda podobnie do następującego:</span><span class="sxs-lookup"><span data-stu-id="b686a-200">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="b686a-201">Firma Microsoft zwraca spójną kolekcję z operacji analizowania, jeśli możemy opakować wywołanie <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> metody w własną metodę.</span><span class="sxs-lookup"><span data-stu-id="b686a-201">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="b686a-202">W poniższym przykładzie `NumericLibrary.ParseInteger` wywołania <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> metodę i zwraca spójną kolekcję o nazwie z dwóch elementów.</span><span class="sxs-lookup"><span data-stu-id="b686a-202">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span> 

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="b686a-203">Następnie możesz wywołać metody z kodu, jak pokazano poniżej:</span><span class="sxs-lookup"><span data-stu-id="b686a-203">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="b686a-204">Spójne kolekcje w programie .NET Framework i Visual Basic kolekcje</span><span class="sxs-lookup"><span data-stu-id="b686a-204">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="b686a-205">Spójna kolekcja języka Visual Basic to wystąpienie jednego z **System.ValueTuple** typów ogólnych, które zostały wprowadzone w .NET Framework 4.7.</span><span class="sxs-lookup"><span data-stu-id="b686a-205">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="b686a-206">Program .NET Framework zawiera również zestaw ogólnych **System.Tuple** klasy.</span><span class="sxs-lookup"><span data-stu-id="b686a-206">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="b686a-207">Te klasy jednak różnić się od krotek języka Visual Basic i **System.ValueTuple** typów ogólnych w na kilka sposobów:</span><span class="sxs-lookup"><span data-stu-id="b686a-207">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="b686a-208">Elementy **krotki** klasy są właściwości o nazwie `Item1`, `Item2`i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="b686a-208">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="b686a-209">W języku Visual Basic w spójnych kolekcjach i **ValueTuple** typy, elementy krotki są polami.</span><span class="sxs-lookup"><span data-stu-id="b686a-209">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="b686a-210">Nie można przypisać opisowych nazw elementów **krotki** wystąpienia lub **ValueTuple** wystąpienia.</span><span class="sxs-lookup"><span data-stu-id="b686a-210">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="b686a-211">Visual Basic umożliwia można przypisać nazwy, które komunikują się znaczenie pola.</span><span class="sxs-lookup"><span data-stu-id="b686a-211">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="b686a-212">Właściwości **krotki** wystąpienia są przeznaczone tylko do odczytu; kolekcje są niezmienne.</span><span class="sxs-lookup"><span data-stu-id="b686a-212">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="b686a-213">W języku Visual Basic w spójnych kolekcjach i **ValueTuple** typy, pola krotki są odczytu i zapisu; kolekcje są modyfikowalna.</span><span class="sxs-lookup"><span data-stu-id="b686a-213">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="b686a-214">Ogólny **krotki** typy są typami odwołań.</span><span class="sxs-lookup"><span data-stu-id="b686a-214">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="b686a-215">Korzystanie z tych **krotki** typy oznacza, że alokacja obiektów.</span><span class="sxs-lookup"><span data-stu-id="b686a-215">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="b686a-216">W ścieżkach, gorąca może to mieć zauważalnego wpływu na wydajność aplikacji.</span><span class="sxs-lookup"><span data-stu-id="b686a-216">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="b686a-217">Krotek języka Visual Basic i **ValueTuple** typy są typami wartości.</span><span class="sxs-lookup"><span data-stu-id="b686a-217">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="b686a-218">Metody rozszerzające w <xref:System.TupleExtensions> klasy ułatwiają konwertowanie krotek języka Visual Basic .NET **krotki** obiektów.</span><span class="sxs-lookup"><span data-stu-id="b686a-218">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="b686a-219">**ToTuple** metoda konwertuje krotki języka Visual Basic .NET **krotki** obiektu, a **ToValueTuple** metoda konwertuje .NET **krotki** obiekt do krotki języka Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="b686a-219">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="b686a-220">Poniższy przykład tworzy spójną kolekcję, konwertuje go na .NET **krotki** obiektu i konwertuje je z powrotem do krotki języka Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="b686a-220">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="b686a-221">Przykład następnie porównuje ten krotki z oryginałem, aby upewnić się, że są równe.</span><span class="sxs-lookup"><span data-stu-id="b686a-221">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="b686a-222">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="b686a-222">See also</span></span>

- [<span data-ttu-id="b686a-223">Dokumentacja języka Visual Basic</span><span class="sxs-lookup"><span data-stu-id="b686a-223">Visual Basic Language Reference</span></span>](index.md)
