---
title: Typy zwrotu asynchronicznego (C#)
ms.date: 05/29/2017
ms.assetid: ddb2539c-c898-48c1-ad92-245e4a996df8
ms.openlocfilehash: 9926fea5308f9088ad924bcc98d8deed319c6300
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/14/2020
ms.locfileid: "79170042"
---
# <a name="async-return-types-c"></a><span data-ttu-id="a9f9e-102">Typy zwrotu asynchronicznego (C#)</span><span class="sxs-lookup"><span data-stu-id="a9f9e-102">Async Return Types (C#)</span></span>
<span data-ttu-id="a9f9e-103">Metody asynchroniczne mogą mieć następujące typy zwracane:</span><span class="sxs-lookup"><span data-stu-id="a9f9e-103">Async methods can have the following return types:</span></span>

- <span data-ttu-id="a9f9e-104"><xref:System.Threading.Tasks.Task%601>, dla metody asynchronicznej, która zwraca wartość.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-104"><xref:System.Threading.Tasks.Task%601>, for an async method that returns a value.</span></span>

- <span data-ttu-id="a9f9e-105"><xref:System.Threading.Tasks.Task>, dla metody asynchronicznej, która wykonuje operację, ale nie zwraca żadnej wartości.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-105"><xref:System.Threading.Tasks.Task>, for an async method that performs an operation but returns no value.</span></span>

- <span data-ttu-id="a9f9e-106">`void`, dla programu obsługi zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-106">`void`, for an event handler.</span></span>

- <span data-ttu-id="a9f9e-107">Począwszy od języka C# 7.0, `GetAwaiter` dowolnego typu, który ma metodę dostępną.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-107">Starting with C# 7.0, any type that has an accessible `GetAwaiter` method.</span></span> <span data-ttu-id="a9f9e-108">Obiekt zwrócony przez `GetAwaiter` metodę musi <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> implementować interfejs.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-108">The object returned by the `GetAwaiter` method must implement the <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> interface.</span></span>
  
<span data-ttu-id="a9f9e-109">Aby uzyskać więcej informacji na temat metod asynchronicznych, zobacz [Programowanie asynchroniczne z async i await (C#)](./index.md).</span><span class="sxs-lookup"><span data-stu-id="a9f9e-109">For more information about async methods, see [Asynchronous Programming with async and await (C#)](./index.md).</span></span>  
  
<span data-ttu-id="a9f9e-110">Każdy typ zwracany jest badany w jednej z poniższych sekcji i można znaleźć pełny przykład, który używa wszystkich trzech typów na końcu tematu.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-110">Each return type is examined in one of the following sections, and you can find a full example that uses all three types at the end of the topic.</span></span>  
  
## <a name="BKMK_TaskTReturnType"></a><span data-ttu-id="a9f9e-111">Typ\<zwracania tresult\> zadania</span><span class="sxs-lookup"><span data-stu-id="a9f9e-111">Task\<TResult\> Return Type</span></span>  
<span data-ttu-id="a9f9e-112">Zwracany <xref:System.Threading.Tasks.Task%601> typ jest używany dla metody asynchronicznej, która zawiera [return](../../../language-reference/keywords/return.md) (C#) instrukcji, w którym operand ma typ `TResult`.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-112">The <xref:System.Threading.Tasks.Task%601> return type is used for an async method that contains a [return](../../../language-reference/keywords/return.md) (C#) statement in which the operand has type `TResult`.</span></span>  
  
<span data-ttu-id="a9f9e-113">W poniższym przykładzie `GetLeisureHours` metoda asynchroniczna zawiera instrukcję, `return` która zwraca liczby całkowitej.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-113">In the following example, the `GetLeisureHours` async method contains a `return` statement that returns an integer.</span></span> <span data-ttu-id="a9f9e-114">W związku z tym deklaracja `Task<int>`metody musi określać typ zwracany .</span><span class="sxs-lookup"><span data-stu-id="a9f9e-114">Therefore, the method declaration must specify a return type of `Task<int>`.</span></span>  <span data-ttu-id="a9f9e-115">Metoda <xref:System.Threading.Tasks.Task.FromResult%2A> asynchroniczna jest symbolem zastępczym dla operacji, która zwraca ciąg.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-115">The <xref:System.Threading.Tasks.Task.FromResult%2A> async method is a placeholder for an operation that returns a string.</span></span>
  
[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns1.cs)]

<span data-ttu-id="a9f9e-116">Gdy `GetLeisureHours` jest wywoływana z wewnątrz `ShowTodaysInfo` await wyrażenie w metodzie, await wyrażenie pobiera `leisureHours`wartość całkowitą (wartość ), który `GetLeisureHours` jest przechowywany w zadaniu zwracanym przez metodę.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-116">When `GetLeisureHours` is called from within an await expression in the `ShowTodaysInfo` method, the await expression retrieves the integer value (the value of `leisureHours`) that's stored in the task returned by the `GetLeisureHours` method.</span></span> <span data-ttu-id="a9f9e-117">Aby uzyskać więcej informacji na temat wyrażeń await, zobacz [await](../../../language-reference/operators/await.md).</span><span class="sxs-lookup"><span data-stu-id="a9f9e-117">For more information about await expressions, see [await](../../../language-reference/operators/await.md).</span></span>  
  
<span data-ttu-id="a9f9e-118">Można lepiej zrozumieć, jak to się `GetLeisureHours` dzieje, oddzielając wywołanie od stosowania `await`, jak pokazano w poniższym kodzie.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-118">You can better understand how this happens by separating the call to `GetLeisureHours` from the application of `await`, as the following code shows.</span></span> <span data-ttu-id="a9f9e-119">Wywołanie metody, `GetLeisureHours` która nie jest natychmiast `Task<int>`oczekiwana zwraca , jak można oczekiwać od deklaracji metody.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-119">A call to method `GetLeisureHours` that isn't immediately awaited returns a `Task<int>`, as you would expect from the declaration of the method.</span></span> <span data-ttu-id="a9f9e-120">Zadanie jest przypisane do `integerTask` zmiennej w przykładzie.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-120">The task is assigned to the `integerTask` variable in the example.</span></span> <span data-ttu-id="a9f9e-121">Ponieważ `integerTask` jest <xref:System.Threading.Tasks.Task%601>, zawiera <xref:System.Threading.Tasks.Task%601.Result> właściwość `TResult`typu .</span><span class="sxs-lookup"><span data-stu-id="a9f9e-121">Because `integerTask` is a <xref:System.Threading.Tasks.Task%601>, it contains a <xref:System.Threading.Tasks.Task%601.Result> property of type `TResult`.</span></span> <span data-ttu-id="a9f9e-122">W takim `TResult` przypadku reprezentuje typ liczby całkowitej.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-122">In this case, `TResult` represents an integer type.</span></span> <span data-ttu-id="a9f9e-123">Po `await` zastosowaniu do `integerTask`wyrażenia await oblicza zawartość <xref:System.Threading.Tasks.Task%601.Result%2A> właściwości . `integerTask`</span><span class="sxs-lookup"><span data-stu-id="a9f9e-123">When `await` is applied to `integerTask`, the await expression evaluates to the contents of the <xref:System.Threading.Tasks.Task%601.Result%2A> property of `integerTask`.</span></span> <span data-ttu-id="a9f9e-124">Wartość jest przypisywana `ret` do zmiennej.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-124">The value is assigned to the `ret` variable.</span></span>  
  
> [!IMPORTANT]
> <span data-ttu-id="a9f9e-125">Obiekt <xref:System.Threading.Tasks.Task%601.Result%2A> jest obiektem blokującym.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-125">The <xref:System.Threading.Tasks.Task%601.Result%2A> property is a blocking property.</span></span> <span data-ttu-id="a9f9e-126">Jeśli spróbujesz uzyskać do niego dostęp przed zakończeniem jego zadania, wątek, który jest aktualnie aktywny jest zablokowany, dopóki zadanie nie zostanie zakończone, a wartość jest dostępna.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-126">If you try to access it before its task is finished, the thread that's currently active is blocked until the task completes and the value is available.</span></span> <span data-ttu-id="a9f9e-127">W większości przypadków należy uzyskać dostęp `await` do wartości przy użyciu zamiast uzyskiwania dostępu do właściwości bezpośrednio.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-127">In most cases, you should access the value by using `await` instead of accessing the property directly.</span></span> <br/> <span data-ttu-id="a9f9e-128">W poprzednim przykładzie pobrano <xref:System.Threading.Tasks.Task%601.Result%2A> wartość właściwości, aby zablokować `ShowTodaysInfo` wątek główny, dzięki czemu metoda może zakończyć wykonywanie przed zakończeniem aplikacji.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-128">The previous example retrieved the value of the <xref:System.Threading.Tasks.Task%601.Result%2A> property to block the main thread so that the `ShowTodaysInfo` method could finish execution before the application ended.</span></span>  

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns1a.cs#1)]
  
## <a name="BKMK_TaskReturnType"></a><span data-ttu-id="a9f9e-129">Typ zwrotu zadania</span><span class="sxs-lookup"><span data-stu-id="a9f9e-129">Task Return Type</span></span>  
<span data-ttu-id="a9f9e-130">Metody asynchroniczne, które `return` nie zawierają instrukcji `return` lub które zawierają instrukcję, która nie <xref:System.Threading.Tasks.Task>zwraca operand zwykle mają typ zwracany .</span><span class="sxs-lookup"><span data-stu-id="a9f9e-130">Async methods that don't contain a `return` statement or that contain a `return` statement that doesn't return an operand usually have a return type of <xref:System.Threading.Tasks.Task>.</span></span> <span data-ttu-id="a9f9e-131">Takie metody `void` zwracają, jeśli są uruchamiane synchronicznie.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-131">Such methods return `void` if they run synchronously.</span></span> <span data-ttu-id="a9f9e-132">Jeśli używasz <xref:System.Threading.Tasks.Task> typu zwracanego dla metody asynchronicznej, `await` metoda wywołująca może użyć operatora do zawieszenia zakończenia wywołującego, dopóki nie zostanie zakończona wywoływana metoda asynchronia.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-132">If you use a <xref:System.Threading.Tasks.Task> return type for an async method, a calling method can use an `await` operator to suspend the caller's completion until the called async method has finished.</span></span>  
  
<span data-ttu-id="a9f9e-133">W poniższym przykładzie `WaitAndApologize` metoda asynchroniczne `return` nie zawiera instrukcji, <xref:System.Threading.Tasks.Task> więc metoda zwraca obiekt.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-133">In the following example, the `WaitAndApologize` async method doesn't contain a `return` statement, so the method returns a <xref:System.Threading.Tasks.Task> object.</span></span> <span data-ttu-id="a9f9e-134">Dzięki `WaitAndApologize` temu można się wyczekiwać.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-134">This enables `WaitAndApologize` to be awaited.</span></span> <span data-ttu-id="a9f9e-135">Należy zauważyć, że <xref:System.Threading.Tasks.Task> typ `Result` nie zawiera właściwości, ponieważ nie ma wartości zwracanej.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-135">Note that the <xref:System.Threading.Tasks.Task> type doesn't include a `Result` property because it has no return value.</span></span>  

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns2.cs)]  
  
<span data-ttu-id="a9f9e-136">`WaitAndApologize`jest oczekiwany przy użyciu await instrukcji zamiast await wyrażenie, podobne do instrukcji wywołującej dla synchronicznej metody zwracania void.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-136">`WaitAndApologize` is awaited by using an await statement instead of an await expression, similar to the calling statement for a synchronous void-returning method.</span></span> <span data-ttu-id="a9f9e-137">Zastosowanie await operatora w tym przypadku nie generuje wartość.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-137">The application of an await operator in this case doesn't produce a value.</span></span>  
  
<span data-ttu-id="a9f9e-138">Podobnie jak <xref:System.Threading.Tasks.Task%601> w poprzednim przykładzie, można `WaitAndApologize` oddzielić wywołanie od stosowania operatora await, jak pokazano w poniższym kodzie.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-138">As in the previous <xref:System.Threading.Tasks.Task%601> example, you can separate the call to `WaitAndApologize` from the application of an await operator, as the following code shows.</span></span> <span data-ttu-id="a9f9e-139">Należy jednak pamiętać, że `Task` a `Result` nie ma właściwości i że żadna wartość jest `Task`tworzona, gdy operator await jest stosowany do .</span><span class="sxs-lookup"><span data-stu-id="a9f9e-139">However, remember that a `Task` doesn't have a `Result` property, and that no value is produced when an await operator is applied to a `Task`.</span></span>  
  
<span data-ttu-id="a9f9e-140">Poniższy kod oddziela `WaitAndApologize` wywoływanie metody z oczekiwania na zadanie, które zwraca metoda.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-140">The following code separates calling the `WaitAndApologize` method from awaiting the task that the method returns.</span></span>  

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns2a.cs#1)]  

## <a name="BKMK_VoidReturnType"></a><span data-ttu-id="a9f9e-141">Nieważne zwracanie typu</span><span class="sxs-lookup"><span data-stu-id="a9f9e-141">Void return type</span></span>

<span data-ttu-id="a9f9e-142">Typ zwracany `void` jest używany w programach obsługi zdarzeń asynchronicznych, które wymagają typu zwracanego. `void`</span><span class="sxs-lookup"><span data-stu-id="a9f9e-142">You use the `void` return type in asynchronous event handlers, which require a `void` return type.</span></span> <span data-ttu-id="a9f9e-143">W przypadku metod innych niż programy obsługi zdarzeń, które <xref:System.Threading.Tasks.Task> nie zwracają wartości, należy `void` zwrócić zamiast tego, ponieważ nie można oczekiwać metody asynchronicznej, która zwraca.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-143">For methods other than event handlers that don't return a value, you should return a <xref:System.Threading.Tasks.Task> instead, because an async method that returns `void` can't be awaited.</span></span> <span data-ttu-id="a9f9e-144">Każdy obiekt wywołujący takiej metody musi być w stanie kontynuować wykonywanie bez oczekiwania na wywołaną metodę asynchronicznej, aby zakończyć, a obiekt wywołujący musi być niezależny od wszelkich wartości lub wyjątków, które generuje metoda asynchronii.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-144">Any caller of such a method must be able to continue to completion without waiting for the called async method to finish, and the caller must be independent of any values or exceptions that the async method generates.</span></span>  
  
<span data-ttu-id="a9f9e-145">Obiekt wywołujący metodę asynchronia zwracającą nieważność nie może przechwycić wyjątków, które są generowane z metody, a takie nieobsługiwane wyjątki mogą spowodować niepowodzenie aplikacji.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-145">The caller of a void-returning async method can't catch exceptions that are thrown from the method, and such unhandled exceptions are likely to cause your application to fail.</span></span> <span data-ttu-id="a9f9e-146">Jeśli wystąpi wyjątek w metodzie asynchronicznej, która zwraca <xref:System.Threading.Tasks.Task> lub <xref:System.Threading.Tasks.Task%601>, wyjątek jest przechowywany w zwróconym zadaniu i jest rethrown, gdy oczekuje na zadanie.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-146">If an exception occurs in an async method that returns a <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601>, the exception is stored in the returned task and is rethrown when the task is awaited.</span></span> <span data-ttu-id="a9f9e-147">W związku z tym upewnij się, że każda metoda <xref:System.Threading.Tasks.Task> asynchroniczne, które mogą powodować wyjątek ma typ zwracany lub <xref:System.Threading.Tasks.Task%601> że wywołania metody są oczekiwane.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-147">Therefore, make sure that any async method that can produce an exception has a return type of <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> and that calls to the method are awaited.</span></span>  
  
<span data-ttu-id="a9f9e-148">Aby uzyskać więcej informacji na temat sposobu wychwytywania wyjątków w metodach asynchronicznych, zobacz [wyjątki w metodach asynchronicznych](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) sekcji tematu [try-catch.](../../../language-reference/keywords/try-catch.md)</span><span class="sxs-lookup"><span data-stu-id="a9f9e-148">For more information about how to catch exceptions in async methods, see the [Exceptions in Async Methods](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) section of the [try-catch](../../../language-reference/keywords/try-catch.md) topic.</span></span>  
  
<span data-ttu-id="a9f9e-149">W poniższym przykładzie przedstawiono zachowanie programu obsługi zdarzeń asynchronicznego.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-149">The following example shows the behavior of an async event handler.</span></span> <span data-ttu-id="a9f9e-150">Należy zauważyć, że w przykładowym kodzie program obsługi zdarzeń asynchronicznej musi poinformować główny wątek po jego zakończeniu.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-150">Note that in the example code, an async event handler must let the main thread know when it finishes.</span></span> <span data-ttu-id="a9f9e-151">Następnie główny wątek może czekać na zakończenie programu obsługi zdarzeń asynchronicznej przed zamknięciem programu.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-151">Then the main thread can wait for an async event handler to complete before exiting the program.</span></span>

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns3.cs)]  

## <a name="generalized-async-return-types-and-valuetasktresult"></a><span data-ttu-id="a9f9e-152">Uogólnione typy zwrotów\<asynchronicznej i Wynik Zadania ValueTask TResult\></span><span class="sxs-lookup"><span data-stu-id="a9f9e-152">Generalized async return types and ValueTask\<TResult\></span></span>

<span data-ttu-id="a9f9e-153">Począwszy od języka C# 7.0, metoda asynchroniczne można zwrócić dowolny typ, który ma metodę dostępną. `GetAwaiter`</span><span class="sxs-lookup"><span data-stu-id="a9f9e-153">Starting with C# 7.0, an async method can return any type that has an accessible `GetAwaiter` method.</span></span>

<span data-ttu-id="a9f9e-154">Ponieważ <xref:System.Threading.Tasks.Task> <xref:System.Threading.Tasks.Task%601> i są typy odwołań, alokacji pamięci w ścieżkach o krytycznym znaczeniu dla wydajności, szczególnie gdy alokacje występują w wąskich pętli, może niekorzystnie wpłynąć na wydajność.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-154">Because <xref:System.Threading.Tasks.Task> and <xref:System.Threading.Tasks.Task%601> are reference types, memory allocation in performance-critical paths, particularly when allocations occur in tight loops, can adversely affect performance.</span></span> <span data-ttu-id="a9f9e-155">Obsługa ogólnych typów zwracanych oznacza, że można zwrócić typ wartości uproszczonej zamiast typu odwołania, aby uniknąć dodatkowych alokacji pamięci.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-155">Support for generalized return types means that you can return a lightweight value type instead of a reference type to avoid additional memory allocations.</span></span>

<span data-ttu-id="a9f9e-156">.NET udostępnia <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> strukturę jako uproszczoną implementację uogólnionej wartości zwracanej zadania.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-156">.NET provides the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> structure as a lightweight implementation of a generalized task-returning value.</span></span> <span data-ttu-id="a9f9e-157">Aby użyć <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> tego typu, `System.Threading.Tasks.Extensions` należy dodać pakiet NuGet do projektu.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-157">To use the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> type, you must add the `System.Threading.Tasks.Extensions` NuGet package to your project.</span></span> <span data-ttu-id="a9f9e-158">W poniższym <xref:System.Threading.Tasks.ValueTask%601> przykładzie użyto struktury, aby pobrać wartość dwóch rzutów kości.</span><span class="sxs-lookup"><span data-stu-id="a9f9e-158">The following example uses the <xref:System.Threading.Tasks.ValueTask%601> structure to retrieve the value of two dice rolls.</span></span>
  
[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-valuetask.cs)]

## <a name="see-also"></a><span data-ttu-id="a9f9e-159">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="a9f9e-159">See also</span></span>

- <xref:System.Threading.Tasks.Task.FromResult%2A>
- [<span data-ttu-id="a9f9e-160">Instruktaż: Uzyskiwanie dostępu do sieci Web przy użyciu asynchronii i oczekiwanie (C#)</span><span class="sxs-lookup"><span data-stu-id="a9f9e-160">Walkthrough: Accessing the Web by Using async and await (C#)</span></span>](./walkthrough-accessing-the-web-by-using-async-and-await.md)
- [<span data-ttu-id="a9f9e-161">Przepływ sterowania w programach asynchroniczny (C#)</span><span class="sxs-lookup"><span data-stu-id="a9f9e-161">Control Flow in Async Programs (C#)</span></span>](./control-flow-in-async-programs.md)
- [<span data-ttu-id="a9f9e-162">async</span><span class="sxs-lookup"><span data-stu-id="a9f9e-162">async</span></span>](../../../language-reference/keywords/async.md)
- [<span data-ttu-id="a9f9e-163">await</span><span class="sxs-lookup"><span data-stu-id="a9f9e-163">await</span></span>](../../../language-reference/operators/await.md)
