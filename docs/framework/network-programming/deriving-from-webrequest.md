---
title: Wyprowadzanie z WebRequest
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.tgt_pltfrm: 
ms.topic: article
helpviewer_keywords:
- WebRequest class, pluggable protocols
- protocol-specific request handler
- sending data, pluggable protocols
- pluggable protocols, class criteria
- Internet, pluggable protocols
- receiving data, pluggable protocols
- protocols, pluggable
ms.assetid: 9810c177-973e-43d7-823c-14960bd625ea
caps.latest.revision: "9"
author: mcleblanc
ms.author: markl
manager: markl
ms.workload: dotnet
ms.openlocfilehash: 83579c25c154462cb21488acf9fcf84999b9a2d4
ms.sourcegitcommit: 16186c34a957fdd52e5db7294f291f7530ac9d24
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 12/22/2017
---
# <a name="deriving-from-webrequest"></a><span data-ttu-id="2a9fe-102">Wyprowadzanie z WebRequest</span><span class="sxs-lookup"><span data-stu-id="2a9fe-102">Deriving from WebRequest</span></span>
<span data-ttu-id="2a9fe-103"><xref:System.Net.WebRequest> Klasa jest abstrakcyjna klasa podstawowa, która udostępnia podstawowe metody i właściwości dla tworzenia protokołem żądania obsługi, który pasuje do modelu protokołu podłączanej .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-103">The <xref:System.Net.WebRequest> class is an abstract base class that provides the basic methods and properties for creating a protocol-specific request handler that fits the .NET Framework pluggable protocol model.</span></span> <span data-ttu-id="2a9fe-104">Aplikacje używające **WebRequest** klasy można zażądać danych, aby określić protokół używany przy użyciu obsługiwanych protokołów bez konieczności.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-104">Applications that use the **WebRequest** class can request data using any supported protocol without needing to specify the protocol used.</span></span>  
  
 <span data-ttu-id="2a9fe-105">Dwa kryteria muszą zostać spełnione w kolejności dla klasy oparte na protokole ma być używany jako protokołu podłączanej: musi implementować klasę <xref:System.Net.IWebRequestCreate> interfejsu który należy zarejestrować się <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-105">Two criteria must be met in order for a protocol-specific class to be used as a pluggable protocol: The class must implement the <xref:System.Net.IWebRequestCreate> interface, and it must register with the <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="2a9fe-106">Klasa musi zastępować wszystkie metody abstrakcyjne i właściwości **WebRequest** podłączany interfejs.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-106">The class must override all the abstract methods and properties of **WebRequest** to provide the pluggable interface.</span></span>  
  
 <span data-ttu-id="2a9fe-107">**WebRequest** wystąpienia są przeznaczone do jednorazowego użytku; Jeśli chcesz utworzyć innego żądania, Utwórz nową **WebRequest**.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-107">**WebRequest** instances are intended for one-time use; if you want to make another request, create a new **WebRequest**.</span></span> <span data-ttu-id="2a9fe-108">**WebRequest** obsługuje <xref:System.Runtime.Serialization.ISerializable> interfejs umożliwia deweloperom serializować szablonu **WebRequest** i ponownie skonstruuj szablonu dla dodatkowych żądań.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-108">**WebRequest** supports the <xref:System.Runtime.Serialization.ISerializable> interface to enable developers to serialize a template **WebRequest** and then reconstruct the template for additional requests.</span></span>  
  
## <a name="iwebrequest-create-method"></a><span data-ttu-id="2a9fe-109">IWebRequest Create — metoda</span><span class="sxs-lookup"><span data-stu-id="2a9fe-109">IWebRequest Create Method</span></span>  
 <span data-ttu-id="2a9fe-110"><xref:System.Net.IWebRequestCreate.Create%2A> Metody jest odpowiedzialny za inicjowanie nowe wystąpienie klasy specyficzne dla protokołu.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-110">The <xref:System.Net.IWebRequestCreate.Create%2A> method is responsible for initializing a new instance of the protocol-specific class.</span></span> <span data-ttu-id="2a9fe-111">Podczas tworzenia nowego **WebRequest** jest tworzony, <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> metody żądanego identyfikatora URI jest zgodny z prefiksy URI zarejestrowany **RegisterPrefix** metody.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-111">When a new **WebRequest** is created, the <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> method matches the requested URI with the URI prefixes registered with the **RegisterPrefix** method.</span></span> <span data-ttu-id="2a9fe-112">**Utwórz** metody prawidłowego obiektu podrzędnego protokołem wróć zainicjowane wystąpienia obiektu podrzędnego może wykonać transakcji standardowe żądanie/odpowiedź protokołu bez konieczności żadnego pola oparte na protokole modyfikowane.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-112">The **Create** method of the proper protocol-specific descendant must return an initialized instance of the descendant capable of performing a standard request/response transaction for the protocol without needing any protocol-specific fields modified.</span></span>  
  
## <a name="connectiongroupname-property"></a><span data-ttu-id="2a9fe-113">Właściwość ConnectionGroupName</span><span class="sxs-lookup"><span data-stu-id="2a9fe-113">ConnectionGroupName Property</span></span>  
 <span data-ttu-id="2a9fe-114"><xref:System.Net.WebRequest.ConnectionGroupName%2A> Właściwość jest używana do nazwy grupy połączeń do zasobu, aby można było wprowadzić wiele żądań za pośrednictwem jednego połączenia.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-114">The <xref:System.Net.WebRequest.ConnectionGroupName%2A> property is used to name a group of connections to a resource so that multiple requests can be made over a single connection.</span></span> <span data-ttu-id="2a9fe-115">Aby zaimplementować Udostępnianie połączenia internetowego, należy użyć metody oparte na protokole buforowanie i przypisywanie połączenia.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-115">To implement connection-sharing, you must use a protocol-specific method of pooling and assigning connections.</span></span> <span data-ttu-id="2a9fe-116">Przykładowo, podana <xref:System.Net.ServicePointManager> klasa implementuje Udostępnianie połączenia dla <xref:System.Net.HttpWebRequest> klasy.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-116">For example, the provided <xref:System.Net.ServicePointManager> class implements connection sharing for the <xref:System.Net.HttpWebRequest> class.</span></span> <span data-ttu-id="2a9fe-117">**ServicePointManager —** tworzy klasy <xref:System.Net.ServicePoint> zapewnia połączenie z określonym serwerem dla każdej grupy połączeń.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-117">The **ServicePointManager** class creates a <xref:System.Net.ServicePoint> that provides a connection to a specific server for each connection group.</span></span>  
  
## <a name="contentlength-property"></a><span data-ttu-id="2a9fe-118">Właściwość ContentLength</span><span class="sxs-lookup"><span data-stu-id="2a9fe-118">ContentLength Property</span></span>  
 <span data-ttu-id="2a9fe-119"><xref:System.Net.WebRequest.ContentLength%2A> Właściwość określa liczbę bajtów danych, które zostaną wysłane do serwera podczas przekazywania danych.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-119">The <xref:System.Net.WebRequest.ContentLength%2A> property specifies the number of bytes of data that will be sent to the server when uploading data.</span></span>  
  
 <span data-ttu-id="2a9fe-120">Zazwyczaj <xref:System.Net.WebRequest.Method%2A> musi być ustawiona właściwość, aby wskazać, że trwa przekazywanie umieść kiedy **ContentLength** właściwości ustawiono wartość większą niż zero.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-120">Typically the <xref:System.Net.WebRequest.Method%2A> property must be set to indicate that an upload is taking place when the **ContentLength** property is set to a value greater than zero.</span></span>  
  
## <a name="contenttype-property"></a><span data-ttu-id="2a9fe-121">Właściwość ContentType</span><span class="sxs-lookup"><span data-stu-id="2a9fe-121">ContentType Property</span></span>  
 <span data-ttu-id="2a9fe-122"><xref:System.Net.WebRequest.ContentType%2A> Właściwość zapewnia wszystkie informacje specjalne protokołu użytkownika wymaga do wysyłania do serwera, aby zidentyfikować typu zawartości, który jest wysyłany.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-122">The <xref:System.Net.WebRequest.ContentType%2A> property provides any special information that your protocol requires you to send to the server to identify the type of content that you are sending.</span></span> <span data-ttu-id="2a9fe-123">Zazwyczaj jest to typ zawartości MIME wszelkie dane przekazane.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-123">Typically this is the MIME content type of any data uploaded.</span></span>  
  
## <a name="credentials-property"></a><span data-ttu-id="2a9fe-124">Właściwość poświadczeń</span><span class="sxs-lookup"><span data-stu-id="2a9fe-124">Credentials Property</span></span>  
 <span data-ttu-id="2a9fe-125"><xref:System.Net.WebRequest.Credentials%2A> Właściwość zawiera informacje potrzebne do uwierzytelnienia żądania na serwerze.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-125">The <xref:System.Net.WebRequest.Credentials%2A> property contains information needed to authenticate the request with the server.</span></span> <span data-ttu-id="2a9fe-126">Szczegóły procesu uwierzytelniania musi implementować sieci protokołu.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-126">You must implement the details of the authentication process for your protocol.</span></span> <span data-ttu-id="2a9fe-127"><xref:System.Net.AuthenticationManager> Klasy jest odpowiedzialny za uwierzytelniania żądań i zapewnianie tokenu uwierzytelniania.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-127">The <xref:System.Net.AuthenticationManager> class is responsible for authenticating requests and providing an authentication token.</span></span> <span data-ttu-id="2a9fe-128">Klasa, która zapewnia poświadczenia używane przez użytkownika protokołu musi implementować <xref:System.Net.ICredentials> interfejsu.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-128">The class that provides the credentials used by your protocol must implement the <xref:System.Net.ICredentials> interface.</span></span>  
  
## <a name="headers-property"></a><span data-ttu-id="2a9fe-129">Właściwość nagłówki</span><span class="sxs-lookup"><span data-stu-id="2a9fe-129">Headers Property</span></span>  
 <span data-ttu-id="2a9fe-130"><xref:System.Net.WebRequest.Headers%2A> Właściwość zawiera dowolną kolekcję par nazwa/wartość metadane skojarzone z żądaniem.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-130">The <xref:System.Net.WebRequest.Headers%2A> property contains an arbitrary collection of name/value pairs of metadata associated with the request.</span></span> <span data-ttu-id="2a9fe-131">Wszystkie metadane wymagane przez protokół, który może zostać wyrażona jako pary nazwa/wartość może być uwzględniony w **nagłówki** właściwości.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-131">Any metadata needed by the protocol that can be expressed as a name/value pair can be included in the **Headers** property.</span></span> <span data-ttu-id="2a9fe-132">Zwykle te informacje należy ustawić przed wywołaniem <xref:System.Net.WebRequest.GetRequestStream%2A> lub <xref:System.Net.WebRequest.GetResponse%2A> metod; po wprowadzono żądanie metadanych jest traktowane jako tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-132">Typically this information must be set before calling the <xref:System.Net.WebRequest.GetRequestStream%2A> or <xref:System.Net.WebRequest.GetResponse%2A> methods; once the request has been made, the metadata is considered read-only.</span></span>  
  
 <span data-ttu-id="2a9fe-133">Nie należy używać **nagłówki** właściwość, aby użyć nagłówka metadanych.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-133">You are not required to use the **Headers** property to use header metadata.</span></span> <span data-ttu-id="2a9fe-134">Metadane specyficzne dla protokołu może być udostępniany jako właściwości; na przykład <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> ujawnia właściwości **agenta użytkownika** nagłówka HTTP.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-134">Protocol-specific metadata can be exposed as properties; for example, the <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> property exposes the **User-Agent** HTTP header.</span></span> <span data-ttu-id="2a9fe-135">Gdy uwidaczniać metadane nagłówka jako właściwość, nie należy zezwalać tę samą właściwość można ustawić za pomocą **nagłówki** właściwości.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-135">When you expose header metadata as a property, you should not allow the same property to be set using the **Headers** property.</span></span>  
  
## <a name="method-property"></a><span data-ttu-id="2a9fe-136">Właściwość — metoda</span><span class="sxs-lookup"><span data-stu-id="2a9fe-136">Method Property</span></span>  
 <span data-ttu-id="2a9fe-137"><xref:System.Net.WebRequest.Method%2A> Właściwość zawiera zlecenie lub akcję, która jest "proszenia" serwera do wykonania żądania.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-137">The <xref:System.Net.WebRequest.Method%2A> property contains the verb or action that the request is asking the server to perform.</span></span> <span data-ttu-id="2a9fe-138">Wartość domyślna dla **metody** właściwość, należy włączyć działanie standardowe żądanie/odpowiedź bez konieczności ustawiania żadnych właściwości specyficzne dla protokołu.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-138">The default for the **Method** property must enable a standard request/response action without requiring any protocol-specific properties to be set.</span></span> <span data-ttu-id="2a9fe-139">Na przykład <xref:System.Net.HttpWebResponse.Method%2A> ustawienia domyślne metody GET, który żąda zasobu z serwera sieci Web i zwraca odpowiedź.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-139">For example, the <xref:System.Net.HttpWebResponse.Method%2A> method defaults to GET, which requests a resource from a Web server and returns the response.</span></span>  
  
 <span data-ttu-id="2a9fe-140">Zazwyczaj **ContentLength** właściwości musi być ustawiona na wartość większą niż zero w przypadku **metody** właściwości ustawiono zlecenie lub akcji, która wskazuje, że przekazanie odbywa się.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-140">Typically the **ContentLength** property must be set to a value greater than zero when the **Method** property is set to a verb or action that indicates that an upload is taking place.</span></span>  
  
## <a name="preauthenticate-property"></a><span data-ttu-id="2a9fe-141">PreAuthenticate właściwości</span><span class="sxs-lookup"><span data-stu-id="2a9fe-141">PreAuthenticate Property</span></span>  
 <span data-ttu-id="2a9fe-142">Zestaw aplikacji <xref:System.Net.WebRequest.PreAuthenticate%2A> właściwości, aby wskazać, że informacje o uwierzytelnianiu mają być wysyłane z żądania początkowego zamiast oczekiwania na żądanie uwierzytelniania.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-142">Applications set the <xref:System.Net.WebRequest.PreAuthenticate%2A> property to indicate that authentication information should be sent with the initial request rather than waiting for an authentication challenge.</span></span> <span data-ttu-id="2a9fe-143">**PreAuthenticate** właściwość jest tylko istotne, jeśli protokół obsługuje poświadczenia uwierzytelniania wysyłane z żądania początkowego.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-143">The **PreAuthenticate** property is only meaningful if the protocol supports authentication credentials sent with the initial request.</span></span>  
  
## <a name="proxy-property"></a><span data-ttu-id="2a9fe-144">Właściwości serwera proxy</span><span class="sxs-lookup"><span data-stu-id="2a9fe-144">Proxy Property</span></span>  
 <span data-ttu-id="2a9fe-145"><xref:System.Net.WebRequest.Proxy%2A> Zawiera właściwość <xref:System.Net.IWebProxy> interfejsu, który umożliwia dostęp do żądanego zasobu.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-145">The <xref:System.Net.WebRequest.Proxy%2A> property contains an <xref:System.Net.IWebProxy> interface that is used to access the requested resource.</span></span> <span data-ttu-id="2a9fe-146">**Proxy** właściwość ma znaczenie tylko wtedy, gdy Twoje protokół obsługuje żądania serwerem proxy.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-146">The **Proxy** property is meaningful only if your protocol supports proxied requests.</span></span> <span data-ttu-id="2a9fe-147">Domyślny serwer proxy musi być ustawiona, jeśli jest on wymagany przez protokół użytkownika.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-147">You must set the default proxy if one is required by your protocol.</span></span>  
  
 <span data-ttu-id="2a9fe-148">W niektórych środowiskach takie jak za firmową zaporą, Twoje protokół może być konieczne do korzystania z serwera proxy.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-148">In some environments, such as behind a corporate firewall, your protocol might be required to use a proxy.</span></span> <span data-ttu-id="2a9fe-149">W takim przypadku należy zaimplementować **IWebProxy** interfejsu można utworzyć klasy proxy, który będzie działać z protokołu.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-149">In that case, you must implement the **IWebProxy** interface to create a proxy class that will work for your protocol.</span></span>  
  
## <a name="requesturi-property"></a><span data-ttu-id="2a9fe-150">Właściwość RequestUri</span><span class="sxs-lookup"><span data-stu-id="2a9fe-150">RequestUri Property</span></span>  
 <span data-ttu-id="2a9fe-151"><xref:System.Net.WebRequest.RequestUri%2A> Właściwość zawiera identyfikator URI, który został przekazany do **WebRequest.Create** metody.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-151">The <xref:System.Net.WebRequest.RequestUri%2A> property contains the URI that was passed to the **WebRequest.Create** method.</span></span> <span data-ttu-id="2a9fe-152">Jest tylko do odczytu i nie można zmienić raz **WebRequest** został utworzony.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-152">It is read-only and cannot be changed once the **WebRequest** has been created.</span></span> <span data-ttu-id="2a9fe-153">Jeśli Twoje protokołu obsługuje przekierowania, odpowiedzi mogą pochodzić z zasobu identyfikowanego przez inny identyfikator URI.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-153">If your protocol supports redirection, the response can come from a resource identified by a different URI.</span></span> <span data-ttu-id="2a9fe-154">Jeśli należy zapewnić dostęp do identyfikatora URI, który odpowiedzi, należy podać dodatkowe właściwości zawierające tego identyfikatora URI.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-154">If you need to provide access to the URI that responded, you must provide an additional property containing that URI.</span></span>  
  
## <a name="timeout-property"></a><span data-ttu-id="2a9fe-155">Właściwość limit czasu</span><span class="sxs-lookup"><span data-stu-id="2a9fe-155">Timeout Property</span></span>  
 <span data-ttu-id="2a9fe-156"><xref:System.Net.WebRequest.Timeout%2A> Właściwość zawiera czas (w milisekundach) oczekiwania na żądanie upłynie limit czasu i zgłasza wyjątek.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-156">The <xref:System.Net.WebRequest.Timeout%2A> property contains the length of time, in milliseconds, to wait before the request times out and throws an exception.</span></span> <span data-ttu-id="2a9fe-157">**Limit czasu** ma zastosowanie tylko do synchroniczne żądań wysyłanych z <xref:System.Net.WebRequest.GetResponse%2A> metody; żądania asynchroniczne muszą używać <xref:System.Net.WebRequest.Abort%2A> metodę, aby anulować oczekujące żądania.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-157">**Timeout** applies only to synchronous requests made with the <xref:System.Net.WebRequest.GetResponse%2A> method; asynchronous requests must use the <xref:System.Net.WebRequest.Abort%2A> method to cancel a pending request.</span></span>  
  
 <span data-ttu-id="2a9fe-158">Ustawienie **limitu czasu** właściwość ma znaczenie tylko wtedy, gdy klasa protokołem implementuje procesu limitu czasu.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-158">Setting the **Timeout** property is meaningful only if the protocol-specific class implements a time-out process.</span></span>  
  
## <a name="abort-method"></a><span data-ttu-id="2a9fe-159">Abort — Metoda</span><span class="sxs-lookup"><span data-stu-id="2a9fe-159">Abort Method</span></span>  
 <span data-ttu-id="2a9fe-160"><xref:System.Net.WebRequest.Abort%2A> Metody anuluje oczekujące żądania asynchronicznego na serwerze.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-160">The <xref:System.Net.WebRequest.Abort%2A> method cancels a pending asynchronous request to a server.</span></span> <span data-ttu-id="2a9fe-161">Po anulowaniu żądania wywoływania **GetResponse**, **BeginGetResponse w czasie**, **EndGetResponse**, **GetRequestStream**,  **Elementu BeginGetRequestStream**, lub **EndGetRequestStream** zgłosi <xref:System.Net.WebException> z <xref:System.Net.WebException.Status%2A> ustawioną właściwość <xref:System.Net.WebExceptionStatus>.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-161">After the request has been canceled, calling **GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**, **BeginGetRequestStream**, or **EndGetRequestStream** will throw a <xref:System.Net.WebException> with the <xref:System.Net.WebException.Status%2A> property set to <xref:System.Net.WebExceptionStatus>.</span></span>  
  
## <a name="begingetrequeststream-and-endgetrequeststream-methods"></a><span data-ttu-id="2a9fe-162">Elementu BeginGetRequestStream i EndGetRequestStream metody</span><span class="sxs-lookup"><span data-stu-id="2a9fe-162">BeginGetRequestStream and EndGetRequestStream Methods</span></span>  
 <span data-ttu-id="2a9fe-163"><xref:System.Net.WebRequest.BeginGetRequestStream%2A> Metoda uruchamia asynchroniczne żądanie dla tego strumienia, który służy do przekazywania danych do serwera.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-163">The <xref:System.Net.WebRequest.BeginGetRequestStream%2A> method starts an asynchronous request for the stream that is used to upload data to the server.</span></span> <span data-ttu-id="2a9fe-164"><xref:System.Net.WebRequest.EndGetRequestStream%2A> Metoda kończy żądanie asynchroniczne i zwraca strumienia żądane.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-164">The <xref:System.Net.WebRequest.EndGetRequestStream%2A> method completes the asynchronous request and returns the requested stream.</span></span> <span data-ttu-id="2a9fe-165">Wdrożenie tych metod **GetRequestStream** metodę przy użyciu standardowego wzorca asynchronicznego dla platformy .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-165">These methods implement the **GetRequestStream** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="begingetresponse-and-endgetresponse-methods"></a><span data-ttu-id="2a9fe-166">BeginGetResponse w czasie i metody EndGetResponse</span><span class="sxs-lookup"><span data-stu-id="2a9fe-166">BeginGetResponse and EndGetResponse Methods</span></span>  
 <span data-ttu-id="2a9fe-167"><xref:System.Net.WebRequest.BeginGetResponse%2A> Metoda uruchamia asynchroniczne żądanie do serwera.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-167">The <xref:System.Net.WebRequest.BeginGetResponse%2A> method starts an asynchronous request to a server.</span></span> <span data-ttu-id="2a9fe-168"><xref:System.Net.WebRequest.EndGetResponse%2A> Metoda kończy żądanie asynchroniczne i zwraca żądanych odpowiedzi.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-168">The <xref:System.Net.WebRequest.EndGetResponse%2A> method completes the asynchronous request and returns the requested response.</span></span> <span data-ttu-id="2a9fe-169">Wdrożenie tych metod **GetResponse** metodę przy użyciu standardowego wzorca asynchronicznego dla platformy .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-169">These methods implement the **GetResponse** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="getrequeststream-method"></a><span data-ttu-id="2a9fe-170">GetRequestStream — metoda</span><span class="sxs-lookup"><span data-stu-id="2a9fe-170">GetRequestStream Method</span></span>  
 <span data-ttu-id="2a9fe-171"><xref:System.Net.WebRequest.GetRequestStream%2A> Metoda zwraca strumień, który służy do zapisywania danych do żądanego serwera.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-171">The <xref:System.Net.WebRequest.GetRequestStream%2A> method returns a stream that is used to write data to the requested server.</span></span> <span data-ttu-id="2a9fe-172">Strumień zwrócony powinny być tylko do zapisu strumienia, który nie ma; jest on przeznaczony jako jednokierunkowe strumień danych, które są zapisywane na serwerze.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-172">The stream returned should be a write-only stream that does not seek; it is intended as a one-way stream of data that is written to the server.</span></span> <span data-ttu-id="2a9fe-173">Zwraca wartość false dla strumienia <xref:System.IO.Stream.CanRead%2A> i <xref:System.IO.Stream.CanSeek%2A> właściwości i wartość true dla <xref:System.IO.Stream.CanWrite%2A> właściwości.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-173">The stream returns false for the <xref:System.IO.Stream.CanRead%2A> and <xref:System.IO.Stream.CanSeek%2A> properties and true for the <xref:System.IO.Stream.CanWrite%2A> property.</span></span>  
  
 <span data-ttu-id="2a9fe-174">**GetRequestStream** metody zazwyczaj otwiera połączenie z serwerem, a przed zwróceniem strumienia, wysyła informacje o nagłówku, która wskazuje, że dane jest wysyłane do serwera.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-174">The **GetRequestStream** method typically opens a connection to the server and, before returning the stream, sends header information that indicates that data is being sent to the server.</span></span> <span data-ttu-id="2a9fe-175">Ponieważ **GetRequestStream** rozpoczyna żądanie dowolne ustawienie **nagłówka** właściwości lub **ContentLength** właściwości zwykle jest niedozwolone po wywołaniu metody **GetRequestStream**.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-175">Because **GetRequestStream** begins the request, setting any **Header** properties or the **ContentLength** property is typically not allowed after calling **GetRequestStream**.</span></span>  
  
## <a name="getresponse-method"></a><span data-ttu-id="2a9fe-176">GetResponse — metoda</span><span class="sxs-lookup"><span data-stu-id="2a9fe-176">GetResponse Method</span></span>  
 <span data-ttu-id="2a9fe-177"><xref:System.Net.WebRequest.GetResponse%2A> Metoda zwraca obiekt podrzędny protokołem <xref:System.Net.WebResponse> klasa, która reprezentuje odpowiedzi z serwera.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-177">The <xref:System.Net.WebRequest.GetResponse%2A> method returns a protocol-specific descendant of the <xref:System.Net.WebResponse> class that represents the response from the server.</span></span> <span data-ttu-id="2a9fe-178">Jeśli żądanie już została zainicjowana przez **GetRequestStream** metody **GetResponse** metoda tworzy połączenie zasobu identyfikowanego przez **RequestUri**, wysyła informacje o nagłówku wskazujący typ żądanie wykonywane i następnie odbiera odpowiedź z zasobu.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-178">Unless the request has already been initiated by the **GetRequestStream** method, the **GetResponse** method creates a connection to the resource identified by **RequestUri**, sends header information indicating the type of request being made, and then receives the response from the resource.</span></span>  
  
 <span data-ttu-id="2a9fe-179">Raz **GetResponse** metoda jest wywoływana, wszystkie właściwości należy traktować jako tylko do odczytu.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-179">Once the **GetResponse** method is called, all properties should be considered read-only.</span></span> <span data-ttu-id="2a9fe-180">**WebRequest** wystąpienia są przeznaczone do jednorazowego użytku; mają być innego żądania, należy utworzyć nowy **WebRequest**.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-180">**WebRequest** instances are intended for one-time use; if you want to make another request, you should create a new **WebRequest**.</span></span>  
  
 <span data-ttu-id="2a9fe-181">**GetResponse** metoda jest odpowiedzialna za tworzenie odpowiednie **WebResponse** podrzędne zawierają przychodzące odpowiedzi.</span><span class="sxs-lookup"><span data-stu-id="2a9fe-181">The **GetResponse** method is responsible for creating an appropriate **WebResponse** descendant to contain the incoming response.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="2a9fe-182">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="2a9fe-182">See Also</span></span>  
 <xref:System.Net.WebRequest>  
 <xref:System.Net.HttpWebRequest>  
 <xref:System.Net.FileWebRequest>  
 [<span data-ttu-id="2a9fe-183">Programowanie protokołów podłączanych</span><span class="sxs-lookup"><span data-stu-id="2a9fe-183">Programming Pluggable Protocols</span></span>](../../../docs/framework/network-programming/programming-pluggable-protocols.md)  
 [<span data-ttu-id="2a9fe-184">Wyprowadzanie z elementu WebResponse</span><span class="sxs-lookup"><span data-stu-id="2a9fe-184">Deriving from WebResponse</span></span>](../../../docs/framework/network-programming/deriving-from-webresponse.md)
