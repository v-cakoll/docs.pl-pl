---
title: Zabezpieczanie kodu otoki
ms.date: 03/30/2017
helpviewer_keywords:
- security [.NET Framework], wrapper code
- wrapper code, securing
- secure coding, wrapper code
- code security, wrapper code
ms.assetid: 1df6c516-5bba-48bd-b450-1070e04b7389
author: mairaw
ms.author: mairaw
ms.openlocfilehash: e824fd686176d83c26ca2c042348c9423fbcc884
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 08/22/2019
ms.locfileid: "69910749"
---
# <a name="securing-wrapper-code"></a><span data-ttu-id="e9bca-102">Zabezpieczanie kodu otoki</span><span class="sxs-lookup"><span data-stu-id="e9bca-102">Securing Wrapper Code</span></span>
[!INCLUDE[net_security_note](../../../includes/net-security-note-md.md)]  
  
 <span data-ttu-id="e9bca-103">Kod otoki, szczególnie w przypadku, gdy otoka ma wyższy poziom zaufania niż kod, który go używa, może otwierać unikatowy zestaw słabych zabezpieczeń.</span><span class="sxs-lookup"><span data-stu-id="e9bca-103">Wrapper code, especially where the wrapper has higher trust than code that uses it, can open a unique set of security weaknesses.</span></span> <span data-ttu-id="e9bca-104">Wszystkie elementy wykonywane w imieniu obiektu wywołującego, w którym ograniczone uprawnienia obiektu wywołującego nie są uwzględnione w odpowiednim sprawdzaniu zabezpieczeń, to potencjalna słaba próba wykorzystania.</span><span class="sxs-lookup"><span data-stu-id="e9bca-104">Anything done on behalf of a caller, where the caller's limited permissions are not included in the appropriate security check, is a potential weakness to be exploited.</span></span>  
  
 <span data-ttu-id="e9bca-105">Nigdy nie należy włączać elementu przez otokę, której obiekt wywołujący nie może sam wykonać.</span><span class="sxs-lookup"><span data-stu-id="e9bca-105">Never enable something through the wrapper that the caller could not do itself.</span></span> <span data-ttu-id="e9bca-106">Jest to specjalne zagrożenie podczas wykonywania czynności obejmujących ograniczone sprawdzanie zabezpieczeń, w przeciwieństwie do żądania pełnego przechodzenia stosu.</span><span class="sxs-lookup"><span data-stu-id="e9bca-106">This is a special danger when doing something that involves a limited security check, as opposed to a full stack walk demand.</span></span> <span data-ttu-id="e9bca-107">Gdy są wykorzystywane kontrole na jednym poziomie, zaproponowanie kodu otoki między rzeczywistym obiektem wywołującym i elementem interfejsu API w danej sytuacji może być w stanie łatwo spowodować pomyślne sprawdzenie zabezpieczeń, gdy nie będzie to miało wpływu na zabezpieczenia.</span><span class="sxs-lookup"><span data-stu-id="e9bca-107">When single-level checks are involved, interposing the wrapper code between the real caller and the API element in question can easily cause the security check to succeed when it should not, thereby weakening security.</span></span>  
  
## <a name="delegates"></a><span data-ttu-id="e9bca-108">Delegaty</span><span class="sxs-lookup"><span data-stu-id="e9bca-108">Delegates</span></span>  
 <span data-ttu-id="e9bca-109">Delegowanie zabezpieczeń różni się między wersjami .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="e9bca-109">Delegate security differs between versions of the .NET Framework.</span></span>  <span data-ttu-id="e9bca-110">W tej sekcji opisano różne zachowania delegata i powiązane zagadnienia dotyczące zabezpieczeń.</span><span class="sxs-lookup"><span data-stu-id="e9bca-110">This section describes the different delegate behaviors and associated security considerations.</span></span>  
  
### <a name="in-version-10-and-11-of-the-net-framework"></a><span data-ttu-id="e9bca-111">W wersji 1,0 i 1,1 .NET Framework</span><span class="sxs-lookup"><span data-stu-id="e9bca-111">In version 1.0 and 1.1 of the .NET Framework</span></span>  
 <span data-ttu-id="e9bca-112">W wersji 1,0 i 1,1 .NET Framework wykonać następujące akcje zabezpieczeń wobec twórcy delegatów i obiektu wywołującego delegata.</span><span class="sxs-lookup"><span data-stu-id="e9bca-112">Version 1.0 and 1.1 of the .NET Framework perform the following security actions against a delegate creator and a delegate caller.</span></span>  
  
- <span data-ttu-id="e9bca-113">Gdy obiekt delegowany jest tworzony, żądania zabezpieczeń dotyczące metody obiektu docelowego delegata są wykonywane względem zestawu uprawnień twórcy delegowania.</span><span class="sxs-lookup"><span data-stu-id="e9bca-113">When a delegate is created, security link demands on the delegate target method are performed against the grant set of the delegate creator.</span></span>  <span data-ttu-id="e9bca-114">Niespełnienie wyników <xref:System.Security.SecurityException>akcji zabezpieczeń nie powiodło się.</span><span class="sxs-lookup"><span data-stu-id="e9bca-114">Failure to satisfy the security action results in a <xref:System.Security.SecurityException>.</span></span>  
  
- <span data-ttu-id="e9bca-115">Gdy obiekt delegowany jest wywoływany, wszystkie istniejące wymagania dotyczące zabezpieczeń w obiekcie wywołującym delegata są wykonywane.</span><span class="sxs-lookup"><span data-stu-id="e9bca-115">When the delegate is invoked, any existing security demands on the delegate caller are performed.</span></span>  
  
 <span data-ttu-id="e9bca-116">Za każdym razem, gdy <xref:System.Delegate> kod pobiera z mniej zaufanego kodu, który może go wywoływać, należy się upewnić, że nie jest włączony niezaufany kod w celu eskalacji jego uprawnień.</span><span class="sxs-lookup"><span data-stu-id="e9bca-116">Whenever your code takes a <xref:System.Delegate> from less-trusted code that might call it, make sure that you are not enabling the less-trusted code to escalate its permissions.</span></span> <span data-ttu-id="e9bca-117">Jeśli obiekt delegowany zostanie użyty później, kod, który utworzył delegata, nie znajduje się na stosie wywołań i jego uprawnienia nie będą badane, jeśli kod w lub w obszarze delegat próbuje wykonać operację chronioną.</span><span class="sxs-lookup"><span data-stu-id="e9bca-117">If you take a delegate and use it later, the code that created the delegate is not on the call stack and its permissions will not be tested if code in or under the delegate attempts a protected operation.</span></span> <span data-ttu-id="e9bca-118">Jeśli kod i kod wywołujący mają wyższe uprawnienia niż twórca, twórca może organizować ścieżkę wywołania bez części stosu wywołań.</span><span class="sxs-lookup"><span data-stu-id="e9bca-118">If your code and the caller code have higher privileges than the creator, the creator can orchestrate the call path without being part of the call stack.</span></span>  
  
### <a name="in-version-20-and-later-versions-of-the-net-framework"></a><span data-ttu-id="e9bca-119">W wersji 2,0 i nowszych .NET Framework</span><span class="sxs-lookup"><span data-stu-id="e9bca-119">In version 2.0 and later versions of the .NET Framework</span></span>  
 <span data-ttu-id="e9bca-120">W przeciwieństwie do poprzednich wersji, wersja 2,0 i nowsze wersje .NET Framework wykonuje akcję zabezpieczeń względem twórcy delegata, gdy delegat zostanie utworzony i wywołany.</span><span class="sxs-lookup"><span data-stu-id="e9bca-120">Unlike previous versions, version 2.0 and later versions of the .NET Framework performs security action against the delegate creator when the delegate is created and called.</span></span>  
  
- <span data-ttu-id="e9bca-121">Gdy obiekt delegowany jest tworzony, żądania zabezpieczeń dotyczące metody obiektu docelowego delegata są wykonywane względem zestawu uprawnień twórcy delegowania.</span><span class="sxs-lookup"><span data-stu-id="e9bca-121">When a delegate is created, security link demands on the delegate target method are performed against the grant set of the delegate creator.</span></span>  <span data-ttu-id="e9bca-122">Niespełnienie wyników <xref:System.Security.SecurityException>akcji zabezpieczeń nie powiodło się.</span><span class="sxs-lookup"><span data-stu-id="e9bca-122">Failure to satisfy the security action results in a <xref:System.Security.SecurityException>.</span></span>  
  
- <span data-ttu-id="e9bca-123">Zestaw dotacji twórcy delegata jest również przechwytywany podczas tworzenia delegata i przechowywany z delegatem.</span><span class="sxs-lookup"><span data-stu-id="e9bca-123">The delegate creator's grant set is also captured during delegate creation and stored with the delegate.</span></span>  
  
- <span data-ttu-id="e9bca-124">Gdy obiekt delegowany jest wywoływany, wychwycony zestaw dotacji twórcy delegowanego jest najpierw oceniany względem wszelkich żądań w bieżącym kontekście, jeśli twórca delegata i obiekt wywołujący należą do różnych zestawów.</span><span class="sxs-lookup"><span data-stu-id="e9bca-124">When the delegate is invoked, the delegate creator's captured grant set is first evaluated against any demands in the current context if the delegate creator and caller belong to different assemblies.</span></span>  <span data-ttu-id="e9bca-125">Następnie zostaną wykonane wszelkie istniejące wymagania dotyczące zabezpieczeń dotyczące obiektu wywołującego delegowania.</span><span class="sxs-lookup"><span data-stu-id="e9bca-125">Next, any existing security demands on the delegate caller are performed.</span></span>  
  
## <a name="link-demands-and-wrappers"></a><span data-ttu-id="e9bca-126">Wymagania dotyczące linków i otoki</span><span class="sxs-lookup"><span data-stu-id="e9bca-126">Link demands and wrappers</span></span>  
 <span data-ttu-id="e9bca-127">Specjalna sprawa ochrony z wymaganiami dotyczącymi linków została wzmocniona w infrastrukturze zabezpieczeń, ale nadal jest źródłem możliwej słabej przyczyny w kodzie.</span><span class="sxs-lookup"><span data-stu-id="e9bca-127">A special protection case with link demands has been strengthened in the security infrastructure, but it is still a source of possible weakness in your code.</span></span>  
  
 <span data-ttu-id="e9bca-128">Jeśli w pełni zaufany kod wywołuje właściwość, zdarzenie lub metodę chronioną przez [LinkDemand](../../../docs/framework/misc/link-demands.md), wywołanie powiedzie się, jeśli zostanie spełnione sprawdzenie uprawnień **LinkDemand** dla elementu wywołującego.</span><span class="sxs-lookup"><span data-stu-id="e9bca-128">If fully trusted code calls a property, event, or method protected by a [LinkDemand](../../../docs/framework/misc/link-demands.md), the call succeeds if the **LinkDemand** permission check for the caller is satisfied.</span></span> <span data-ttu-id="e9bca-129">Ponadto jeśli w pełni zaufany kod ujawnia klasę, która przyjmuje nazwę właściwości i wywołuje metodę dostępu **Get** przy użyciu odbicia, to wywołanie metody dostępu **Get** powiedzie się, mimo że kod użytkownika nie ma prawa dostępu do tej właściwości.</span><span class="sxs-lookup"><span data-stu-id="e9bca-129">Additionally, if the fully trusted code exposes a class that takes the name of a property and calls its **get** accessor using reflection, that call to the **get** accessor succeeds even though the user code does not have the right to access this property.</span></span> <span data-ttu-id="e9bca-130">Wynika to z faktu, że **LinkDemand** sprawdza tylko bezpośredni obiekt wywołujący, który jest w pełni zaufanym kodem.</span><span class="sxs-lookup"><span data-stu-id="e9bca-130">This is because the **LinkDemand** checks only the immediate caller, which is the fully trusted code.</span></span> <span data-ttu-id="e9bca-131">W zasadzie w pełni zaufany kod dokonuje uprzywilejowanego wywołania w imieniu kodu użytkownika bez upewnienia się, że kod użytkownika ma prawo do tego wywołania.</span><span class="sxs-lookup"><span data-stu-id="e9bca-131">In essence, the fully trusted code is making a privileged call on behalf of user code without making sure that the user code has the right to make that call.</span></span>  
  
 <span data-ttu-id="e9bca-132">Aby zapobiec występowaniu takich luk w zabezpieczeniach, środowisko uruchomieniowe języka wspólnego rozszerza kontrolę na pełne zapotrzebowanie na stosy dla każdego pośredniego wywołania metody, konstruktora, właściwości lub zdarzenia chronionego przez **LinkDemand**.</span><span class="sxs-lookup"><span data-stu-id="e9bca-132">To help prevent such security holes, the common language runtime extends the check into a full stack-walking demand on any indirect call to a method, constructor, property, or event protected by a **LinkDemand**.</span></span> <span data-ttu-id="e9bca-133">Ta ochrona wiąże się z pewnymi kosztami wydajności i zmienia semantykę kontroli zabezpieczeń; pełne żądanie przechodzenia stosu może zakończyć się niepowodzeniem w przypadku szybszego sprawdzenia na jednym poziomie.</span><span class="sxs-lookup"><span data-stu-id="e9bca-133">This protection incurs some performance costs and changes the semantics of the security check; the full stack-walk demand might fail where the faster, one-level check would have passed.</span></span>  
  
## <a name="assembly-loading-wrappers"></a><span data-ttu-id="e9bca-134">Otoki ładowania zestawu</span><span class="sxs-lookup"><span data-stu-id="e9bca-134">Assembly loading wrappers</span></span>  
 <span data-ttu-id="e9bca-135">Kilka metod służących do ładowania kodu zarządzanego, <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>w tym, ładowania zestawów z dowodem wywołującym.</span><span class="sxs-lookup"><span data-stu-id="e9bca-135">Several methods used to load managed code, including <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>, load assemblies with the evidence of the caller.</span></span> <span data-ttu-id="e9bca-136">W przypadku oblewania którejkolwiek z tych metod system zabezpieczeń może użyć przydzielenia uprawnień kodu zamiast uprawnień obiektu wywołującego do otoki, aby załadować zestawy.</span><span class="sxs-lookup"><span data-stu-id="e9bca-136">If you wrap any of these methods, the security system could use your code's permission grant, instead of the permissions of the caller to your wrapper, to load the assemblies.</span></span> <span data-ttu-id="e9bca-137">Nie należy zezwalać na ładowanie kodu, który ma wyższy poziom uprawnień niż w przypadku obiektu wywołującego do otoki.</span><span class="sxs-lookup"><span data-stu-id="e9bca-137">You should not allow less-trusted code to load code that is granted higher permissions than those of the caller to your wrapper.</span></span>  
  
 <span data-ttu-id="e9bca-138">Każdy kod z pełnym zaufaniem lub znacznie wyższym zaufaniem niż potencjalny obiekt wywołujący (w tym obiekt wywołujący na poziomie uprawnień internetowych) może osłabić zabezpieczenia w ten sposób.</span><span class="sxs-lookup"><span data-stu-id="e9bca-138">Any code that has full trust or significantly higher trust than a potential caller (including an Internet-permissions-level caller) could weaken security in this way.</span></span> <span data-ttu-id="e9bca-139">Jeśli kod ma metodę publiczną, która pobiera tablicę bajtową i przekazuje ją do **zestawu. Load**, tworząc zestaw w imieniu obiektu wywołującego, może przerwać zabezpieczenia.</span><span class="sxs-lookup"><span data-stu-id="e9bca-139">If your code has a public method that takes a byte array and passes it to **Assembly.Load**, thereby creating an assembly on the caller's behalf, it might break security.</span></span>  
  
 <span data-ttu-id="e9bca-140">Ten problem dotyczy następujących elementów interfejsu API:</span><span class="sxs-lookup"><span data-stu-id="e9bca-140">This issue applies to the following API elements:</span></span>  
  
- <xref:System.AppDomain.DefineDynamicAssembly%2A?displayProperty=nameWithType>  
  
- <xref:System.AppDomain.Load%2A?displayProperty=nameWithType>  
  
- <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>  
  
- <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>  
  
## <a name="demand-vs-linkdemand"></a><span data-ttu-id="e9bca-141">Zapotrzebowanie a LinkDemand</span><span class="sxs-lookup"><span data-stu-id="e9bca-141">Demand vs. LinkDemand</span></span>  
 <span data-ttu-id="e9bca-142">Zabezpieczenia deklaracyjne oferują dwa rodzaje kontroli zabezpieczeń, które są podobne, ale wykonują bardzo różne sprawdzenia.</span><span class="sxs-lookup"><span data-stu-id="e9bca-142">Declarative security offers two kinds of security checks that are similar but perform very different checks.</span></span> <span data-ttu-id="e9bca-143">Należy zrozumieć obie formy, ponieważ niewłaściwy wybór może spowodować słabe zabezpieczenia lub utratę wydajności.</span><span class="sxs-lookup"><span data-stu-id="e9bca-143">You should understand both forms because the wrong choice can result in weak security or performance loss.</span></span>  
  
 <span data-ttu-id="e9bca-144">Zabezpieczenia deklaracyjne oferują następujące sprawdzenia zabezpieczeń:</span><span class="sxs-lookup"><span data-stu-id="e9bca-144">Declarative security offers the following security checks:</span></span>  
  
- <span data-ttu-id="e9bca-145"><xref:System.Security.Permissions.SecurityAction.Demand>Określa przeszukiwanie stosu zabezpieczeń dostępu kodu.</span><span class="sxs-lookup"><span data-stu-id="e9bca-145"><xref:System.Security.Permissions.SecurityAction.Demand> specifies the code access security stack walk.</span></span> <span data-ttu-id="e9bca-146">Wszystkie obiekty wywołujące na stosie muszą mieć określone uprawnienie lub tożsamość do przekazania.</span><span class="sxs-lookup"><span data-stu-id="e9bca-146">All callers on the stack must have the specified permission or identity to pass.</span></span> <span data-ttu-id="e9bca-147">**Żądanie** występuje dla każdego wywołania, ponieważ stos może zawierać różne obiekty wywołujące.</span><span class="sxs-lookup"><span data-stu-id="e9bca-147">**Demand** occurs on every call because the stack might contain different callers.</span></span> <span data-ttu-id="e9bca-148">Jeśli wywołasz metodę wielokrotnie, to sprawdzanie zabezpieczeń odbywa się za każdym razem.</span><span class="sxs-lookup"><span data-stu-id="e9bca-148">If you call a method repeatedly, this security check occurs each time.</span></span> <span data-ttu-id="e9bca-149">**Zapotrzebowanie** jest dobrą ochronę przed atakami luringymi; zostanie wykryty nieautoryzowany kod, który próbuje się pobrać.</span><span class="sxs-lookup"><span data-stu-id="e9bca-149">**Demand** is good protection against luring attacks; unauthorized code trying to get through it will be detected.</span></span>  
  
- <span data-ttu-id="e9bca-150">[LinkDemand](../../../docs/framework/misc/link-demands.md) odbywa się w czasie kompilacji just-in-Time (JIT) i sprawdza tylko bezpośredni obiekt wywołujący.</span><span class="sxs-lookup"><span data-stu-id="e9bca-150">[LinkDemand](../../../docs/framework/misc/link-demands.md) happens at just-in-time (JIT) compilation time and checks only the immediate caller.</span></span> <span data-ttu-id="e9bca-151">To sprawdzenie zabezpieczeń nie sprawdza wywołującego obiektu wywołującego.</span><span class="sxs-lookup"><span data-stu-id="e9bca-151">This security check does not check the caller's caller.</span></span> <span data-ttu-id="e9bca-152">Po pomyślnym sprawdzeniu nie ma żadnych dodatkowych obciążeń związanych z zabezpieczeniami niezależnie od tego, ile razy proces wywołujący może wywoływać.</span><span class="sxs-lookup"><span data-stu-id="e9bca-152">Once this check passes, there is no additional security overhead no matter how many times the caller might call.</span></span> <span data-ttu-id="e9bca-153">Nie ma jednak żadnej ochrony przed atakami luring.</span><span class="sxs-lookup"><span data-stu-id="e9bca-153">However, there is also no protection from luring attacks.</span></span> <span data-ttu-id="e9bca-154">Dzięki **LinkDemand**każdy kod, który przekazuje test i może odwoływać się do kodu, może potencjalnie przerwać zabezpieczenia, umożliwiając złośliwemu kodowi wywoływanie przy użyciu autoryzowanego kodu.</span><span class="sxs-lookup"><span data-stu-id="e9bca-154">With **LinkDemand**, any code that passes the test and can reference your code can potentially break security by allowing malicious code to call using the authorized code.</span></span> <span data-ttu-id="e9bca-155">W związku z tym nie należy używać **LinkDemand** , chyba że wszystkie możliwe słabe luki nie będą widoczne.</span><span class="sxs-lookup"><span data-stu-id="e9bca-155">Therefore, do not use **LinkDemand** unless all the possible weaknesses can be thoroughly avoided.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="e9bca-156">W .NET Framework 4 wymagania dotyczące linków zostały zastąpione przez <xref:System.Security.SecurityCriticalAttribute> atrybut w <xref:System.Security.SecurityRuleSet.Level2> zestawach.</span><span class="sxs-lookup"><span data-stu-id="e9bca-156">In the .NET Framework 4, link demands have been replaced by the <xref:System.Security.SecurityCriticalAttribute> attribute in <xref:System.Security.SecurityRuleSet.Level2> assemblies.</span></span> <span data-ttu-id="e9bca-157"><xref:System.Security.SecurityCriticalAttribute> Jest odpowiednikiem żądania połączenia dla pełnego zaufania, ale również ma wpływ na reguły dziedziczenia.</span><span class="sxs-lookup"><span data-stu-id="e9bca-157">The <xref:System.Security.SecurityCriticalAttribute> is equivalent to a link demand for full trust; however, it also affects inheritance rules.</span></span> <span data-ttu-id="e9bca-158">Aby uzyskać więcej informacji na temat tej zmiany, zobacz [kod przezroczysty zabezpieczeń, poziom 2](../../../docs/framework/misc/security-transparent-code-level-2.md).</span><span class="sxs-lookup"><span data-stu-id="e9bca-158">For more information about this change, see [Security-Transparent Code, Level 2](../../../docs/framework/misc/security-transparent-code-level-2.md).</span></span>  
  
 <span data-ttu-id="e9bca-159">Dodatkowe środki ostrożności wymagane, gdy użycie **LinkDemand** muszą być zaprogramowane osobno; System zabezpieczeń może pomóc wymuszać.</span><span class="sxs-lookup"><span data-stu-id="e9bca-159">The extra precautions required when using **LinkDemand** must be programmed individually; the security system can help with enforcement.</span></span> <span data-ttu-id="e9bca-160">Dowolny błąd powoduje otwarcie luki w zabezpieczeniach.</span><span class="sxs-lookup"><span data-stu-id="e9bca-160">Any mistake opens a security weakness.</span></span> <span data-ttu-id="e9bca-161">Cały autoryzowany kod, który korzysta z kodu, musi być odpowiedzialny za wdrożenie dodatkowych zabezpieczeń, wykonując następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="e9bca-161">All authorized code that uses your code must be responsible for implementing additional security by doing the following:</span></span>  
  
- <span data-ttu-id="e9bca-162">Ograniczanie dostępu do kodu wywołującego do klasy lub zestawu.</span><span class="sxs-lookup"><span data-stu-id="e9bca-162">Restricting the calling code's access to the class or assembly.</span></span>  
  
- <span data-ttu-id="e9bca-163">Umieszczenie tego samego sprawdzenia zabezpieczeń na wywoływanym kodzie, który pojawia się na wywoływanym kodzie i obligating jego wywołujący.</span><span class="sxs-lookup"><span data-stu-id="e9bca-163">Placing the same security checks on the calling code that appear on the code being called and obligating its callers to do so.</span></span> <span data-ttu-id="e9bca-164">Na przykład jeśli piszesz kod, który wywołuje metodę, która jest chroniona za pomocą elementu **LinkDemand** <xref:System.Security.Permissions.SecurityPermission> z <xref:System.Security.Permissions.SecurityPermissionFlag.UnmanagedCode> określoną flagą, metoda powinna również mieć **LinkDemand** (lub **żądanie**, które jest silniejsze) dla tego zezwolenie.</span><span class="sxs-lookup"><span data-stu-id="e9bca-164">For example, if you write code that calls a method that is protected with a **LinkDemand** for the <xref:System.Security.Permissions.SecurityPermission> with the <xref:System.Security.Permissions.SecurityPermissionFlag.UnmanagedCode> flag specified, your method should also make a **LinkDemand** (or **Demand**, which is stronger) for this permission.</span></span> <span data-ttu-id="e9bca-165">Wyjątek polega na tym, że kod korzysta z metody chronionej przez **LinkDemand**w ograniczony sposób, który jest bezpieczny, z uwzględnieniem innych mechanizmów ochrony zabezpieczeń (takich jak wymagania) w kodzie.</span><span class="sxs-lookup"><span data-stu-id="e9bca-165">The exception is if your code uses the **LinkDemand**-protected method in a limited way that you decide is safe, given other security protection mechanisms (such as demands) in your code.</span></span> <span data-ttu-id="e9bca-166">W tym wyjątkowym przypadku wywołujący odpowiada za osłabienie ochrony zabezpieczeń w kodzie źródłowym.</span><span class="sxs-lookup"><span data-stu-id="e9bca-166">In this exceptional case, the caller takes responsibility in weakening the security protection on the underlying code.</span></span>  
  
- <span data-ttu-id="e9bca-167">Upewnienie się, że wywołujący kod nie może dochodzić do kodu w celu wywołania chronionego kodu w ich imieniu.</span><span class="sxs-lookup"><span data-stu-id="e9bca-167">Ensuring that your code's callers cannot trick your code into calling the protected code on their behalf.</span></span> <span data-ttu-id="e9bca-168">Innymi słowy, obiekty wywołujące nie mogą wymusić, aby uprawniony kod mógł przekazać określone parametry do chronionego kodu lub uzyskać z powrotem wyniki.</span><span class="sxs-lookup"><span data-stu-id="e9bca-168">In other words, callers cannot force the authorized code to pass specific parameters to the protected code, or to get results back from it.</span></span>  
  
### <a name="interfaces-and-link-demands"></a><span data-ttu-id="e9bca-169">Interfejsy i wymagania dotyczące linków</span><span class="sxs-lookup"><span data-stu-id="e9bca-169">Interfaces and Link Demands</span></span>  
 <span data-ttu-id="e9bca-170">Jeśli wirtualna metoda, właściwość lub zdarzenie z **LinkDemand** przesłania metodę klasy bazowej, metoda klasy bazowej musi również mieć ten sam **LinkDemand** dla zastąpionej metody, aby obowiązywać.</span><span class="sxs-lookup"><span data-stu-id="e9bca-170">If a virtual method, property, or event with **LinkDemand** overrides a base class method, the base class method must also have the same **LinkDemand** for the overridden method in order to be effective.</span></span> <span data-ttu-id="e9bca-171">Istnieje możliwość, że złośliwy kod będzie rzutować z powrotem na typ podstawowy i wywołać metodę klasy bazowej.</span><span class="sxs-lookup"><span data-stu-id="e9bca-171">It is possible for malicious code to cast back to the base type and call the base class method.</span></span> <span data-ttu-id="e9bca-172">Należy również zauważyć, że wymagania dotyczące linków mogą być dodawane niejawnie do zestawów, <xref:System.Security.AllowPartiallyTrustedCallersAttribute> które nie mają atrybutu na poziomie zestawu.</span><span class="sxs-lookup"><span data-stu-id="e9bca-172">Also note that link demands can be added implicitly to assemblies that do not have the <xref:System.Security.AllowPartiallyTrustedCallersAttribute> assembly-level attribute.</span></span>  
  
 <span data-ttu-id="e9bca-173">Dobrym sposobem jest ochrona implementacji metod za pomocą żądań linków, gdy metody interfejsu również mają wymagania dotyczące linków.</span><span class="sxs-lookup"><span data-stu-id="e9bca-173">It is a good practice to protect method implementations with link demands when interface methods also have link demands.</span></span> <span data-ttu-id="e9bca-174">Należy pamiętać o następujących kwestiach dotyczących używania żądań linków z interfejsami:</span><span class="sxs-lookup"><span data-stu-id="e9bca-174">Note the following about using link demands with interfaces:</span></span>  
  
- <span data-ttu-id="e9bca-175">Jeśli umieścisz **LinkDemand** na metodzie publicznej klasy implementującej metodę interfejsu, **LinkDemand** nie zostanie wymuszony, jeśli następnie rzutowane do interfejsu i Wywołaj metodę.</span><span class="sxs-lookup"><span data-stu-id="e9bca-175">If you place a **LinkDemand** on a public method of a class that implements an interface method, the **LinkDemand** will not be enforced if you then cast to the interface and call the method.</span></span> <span data-ttu-id="e9bca-176">W tym przypadku, ponieważ połączono z interfejsem, tylko **LinkDemand** w interfejsie jest uznawany za.</span><span class="sxs-lookup"><span data-stu-id="e9bca-176">In this case, because you linked against the interface, only the **LinkDemand** on the interface is honored.</span></span>  
  
 <span data-ttu-id="e9bca-177">Przejrzyj następujące elementy dotyczące problemów z zabezpieczeniami:</span><span class="sxs-lookup"><span data-stu-id="e9bca-177">Review the following items for security issues:</span></span>  
  
- <span data-ttu-id="e9bca-178">Jawne wymagania dotyczące linków w metodach interfejsu.</span><span class="sxs-lookup"><span data-stu-id="e9bca-178">Explicit link demands on interface methods.</span></span> <span data-ttu-id="e9bca-179">Upewnij się, że te wymagania dotyczące linków oferują oczekiwaną ochronę.</span><span class="sxs-lookup"><span data-stu-id="e9bca-179">Make sure these link demands offer the expected protection.</span></span> <span data-ttu-id="e9bca-180">Należy określić, czy złośliwy kod może użyć rzutowania, aby uzyskać więcej, jak opisano powyżej.</span><span class="sxs-lookup"><span data-stu-id="e9bca-180">Determine whether malicious code can use a cast to get around the link demands as described previously.</span></span>  
  
- <span data-ttu-id="e9bca-181">Zastosowano metody wirtualne z wymaganiami dotyczącymi linków.</span><span class="sxs-lookup"><span data-stu-id="e9bca-181">Virtual methods with link demands applied.</span></span>  
  
- <span data-ttu-id="e9bca-182">Typy i interfejsy, które implementują.</span><span class="sxs-lookup"><span data-stu-id="e9bca-182">Types and the interfaces they implement.</span></span> <span data-ttu-id="e9bca-183">Należy spójnie stosować wymagania dotyczące linków.</span><span class="sxs-lookup"><span data-stu-id="e9bca-183">These should use link demands consistently.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="e9bca-184">Zobacz także</span><span class="sxs-lookup"><span data-stu-id="e9bca-184">See also</span></span>

- [<span data-ttu-id="e9bca-185">Wytyczne dotyczące bezpiecznego programowania</span><span class="sxs-lookup"><span data-stu-id="e9bca-185">Secure Coding Guidelines</span></span>](../../standard/security/secure-coding-guidelines.md)
