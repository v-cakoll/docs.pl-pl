---
title: Dostawcy CLR ETW
ms.date: 03/30/2017
helpviewer_keywords:
- ETW, CLR providers
- CLR ETW providers
ms.assetid: 0beafad4-b2c8-47f4-b342-83411d57a51f
ms.openlocfilehash: 33ef7491c2bffeda4ef737ed8f826cdfbfbb119d
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/14/2020
ms.locfileid: "79400079"
---
# <a name="clr-etw-providers"></a><span data-ttu-id="e903d-102">Dostawcy CLR ETW</span><span class="sxs-lookup"><span data-stu-id="e903d-102">CLR ETW Providers</span></span>
<span data-ttu-id="e903d-103">Środowisko uruchomieniowe języka wspólnego (CLR) ma dwóch dostawców: dostawcę środowiska wykonawczego i dostawcę wybiegu.</span><span class="sxs-lookup"><span data-stu-id="e903d-103">The common language runtime (CLR) has two providers: the runtime provider and the rundown provider.</span></span>  
  
 <span data-ttu-id="e903d-104">Dostawca środowiska wykonawczego wywołuje zdarzenia, w zależności od tego, które słowa kluczowe (kategorie zdarzeń) są włączone.</span><span class="sxs-lookup"><span data-stu-id="e903d-104">The runtime provider raises events, depending on which keywords (categories of events) are enabled.</span></span> <span data-ttu-id="e903d-105">Na przykład można zbierać zdarzenia modułu `LoaderKeyword` ładującego, włączając słowo kluczowe.</span><span class="sxs-lookup"><span data-stu-id="e903d-105">For example, you can collect loader events by enabling the `LoaderKeyword` keyword.</span></span>  
  
 <span data-ttu-id="e903d-106">Zdarzenia śledzenia zdarzeń dla systemu Windows (ETW) są rejestrowane w pliku z rozszerzeniem .etl, które można później przetworzyć w plikach wartości oddzielonych przecinkami (csv).</span><span class="sxs-lookup"><span data-stu-id="e903d-106">Event Tracing for Windows (ETW) events are logged into a file that has an .etl extension, which can later be post-processed in comma-separated value (.csv) files as needed.</span></span> <span data-ttu-id="e903d-107">Aby uzyskać informacje dotyczące sposobu konwertowania pliku .etl na plik csv, zobacz [Kontrolowanie rejestrowania programu .NET Framework .](controlling-logging.md)</span><span class="sxs-lookup"><span data-stu-id="e903d-107">For information about how to convert the .etl file to a .csv file, see [Controlling .NET Framework Logging](controlling-logging.md).</span></span>  
  
## <a name="the-runtime-provider"></a><span data-ttu-id="e903d-108">Dostawca środowiska wykonawczego</span><span class="sxs-lookup"><span data-stu-id="e903d-108">The Runtime Provider</span></span>  
 <span data-ttu-id="e903d-109">Dostawca środowiska wykonawczego jest głównym dostawcą CLR ETW.</span><span class="sxs-lookup"><span data-stu-id="e903d-109">The runtime provider is the main CLR ETW provider.</span></span>  
  
 <span data-ttu-id="e903d-110">Identyfikator GUID dostawcy środowiska wykonawczego CLR to e13c0d23-ccbc-4e12-931b-d9cc2eee27e4.</span><span class="sxs-lookup"><span data-stu-id="e903d-110">The CLR runtime provider GUID is e13c0d23-ccbc-4e12-931b-d9cc2eee27e4.</span></span>  
  
 <span data-ttu-id="e903d-111">Aby zapoznać się z przykładami sposobu rejestrowania i wyświetlania zdarzeń CLR ETW przy użyciu powszechnie dostępnych narzędzi, zobacz [Kontrolowanie rejestrowania programu .NET Framework .](controlling-logging.md)</span><span class="sxs-lookup"><span data-stu-id="e903d-111">For examples of how to log and view CLR ETW events by using commonly available tools, see [Controlling .NET Framework Logging](controlling-logging.md).</span></span>  
  
 <span data-ttu-id="e903d-112">Oprócz używania słów kluczowych, takich jak `LoaderKeyword`, może być wymagane włączenie słów kluczowych dla zdarzeń rejestrowania, które mogą być wywoływane zbyt często.</span><span class="sxs-lookup"><span data-stu-id="e903d-112">In addition to using keywords such as `LoaderKeyword`, you may have to enable keywords for logging events that may be raised too frequently.</span></span> <span data-ttu-id="e903d-113">I `StartEnumerationKeyword` `EndEnumerationKeyword` słowa kluczowe włączyć te zdarzenia i są podsumowane w [CLR ETW Słowa kluczowe i poziomy](clr-etw-keywords-and-levels.md).</span><span class="sxs-lookup"><span data-stu-id="e903d-113">The `StartEnumerationKeyword` and the `EndEnumerationKeyword` keywords enable these events and are summarized in [CLR ETW Keywords and Levels](clr-etw-keywords-and-levels.md).</span></span>  
  
## <a name="the-rundown-provider"></a><span data-ttu-id="e903d-114">Dostawca wybiegiem</span><span class="sxs-lookup"><span data-stu-id="e903d-114">The Rundown Provider</span></span>  
 <span data-ttu-id="e903d-115">Dostawca wybiegiem musi być włączony dla niektórych zastosowań specjalnego przeznaczenia.</span><span class="sxs-lookup"><span data-stu-id="e903d-115">The rundown provider must be turned on for certain special-purpose uses.</span></span> <span data-ttu-id="e903d-116">Jednak dla większości użytkowników dostawca środowiska wykonawczego powinien wystarczyć.</span><span class="sxs-lookup"><span data-stu-id="e903d-116">However, for a majority of users, the runtime provider should suffice.</span></span>  
  
 <span data-ttu-id="e903d-117">Identyfikator GUID dostawcy wybiegiem CLR to A669021C-C450-4609-A035-5AF59AF4DF18.</span><span class="sxs-lookup"><span data-stu-id="e903d-117">The CLR rundown provider GUID is A669021C-C450-4609-A035-5AF59AF4DF18.</span></span>  
  
 <span data-ttu-id="e903d-118">Zwykle rejestrowanie ETW jest włączone przed uruchomieniem procesu, a rejestrowanie jest wyłączone po zakończeniu procesu.</span><span class="sxs-lookup"><span data-stu-id="e903d-118">Normally, ETW logging is enabled before a process launches, and the logging is turned off after the process exits.</span></span> <span data-ttu-id="e903d-119">Jeśli jednak rejestrowanie ETW jest włączone podczas wykonywania procesu, potrzebne są dodatkowe informacje o procesie.</span><span class="sxs-lookup"><span data-stu-id="e903d-119">However, if ETW logging is turned on while the process is executing, additional information is needed about the process.</span></span> <span data-ttu-id="e903d-120">Na przykład dla rozpoznawania symbolu należy rejestrować zdarzenia metody dla metod, które zostały już załadowane przed włączeniem rejestrowania.</span><span class="sxs-lookup"><span data-stu-id="e903d-120">For example, for symbol resolution you have to log method events for methods that were already loaded before logging was turned on.</span></span>  
  
 <span data-ttu-id="e903d-121">Zdarzenia `DCStart` `DCEnd` i zdarzenia przechwytują stan procesu podczas pobierania danych został uruchomiony i zatrzymany.</span><span class="sxs-lookup"><span data-stu-id="e903d-121">The `DCStart` and `DCEnd` events capture the state of the process when data collection was started and stopped.</span></span> <span data-ttu-id="e903d-122">(Stan odnosi się do informacji na wysokim poziomie, w tym metod, które zostały już just-in-time (JIT) skompilowane i zestawów, które zostały załadowane.) Te dwa zdarzenia mogą dostarczyć informacji o tym, co już się wydarzyło w procesie; na przykład, które metody były JIT- skompilowany, i tak dalej.</span><span class="sxs-lookup"><span data-stu-id="e903d-122">(State refers to information at a high level, including the methods that were already just-in-time (JIT) compiled and assemblies that were loaded.) These two events can provide information about what has already happened in the process; for example, which methods were JIT- compiled, and so on.</span></span>  
  
 <span data-ttu-id="e903d-123">Tylko zdarzenia `DC`z `DCStart` `DCEnd`, `DCInit` , , lub w ich nazwach są wywoływane pod dostawcą rundown.</span><span class="sxs-lookup"><span data-stu-id="e903d-123">Only the events with `DC`, `DCStart`, `DCEnd`, or `DCInit` in their names are raised under the rundown provider.</span></span> <span data-ttu-id="e903d-124">Ponadto te zdarzenia są wywoływane tylko w ramach dostawcy rundown.</span><span class="sxs-lookup"><span data-stu-id="e903d-124">Additionally, these events are raised only under the rundown provider.</span></span>  
  
 <span data-ttu-id="e903d-125">Oprócz filtrów słów kluczowych zdarzeń dostawca wybiegiem `StartRundownKeyword` obsługuje `EndRundownKeyword` również słowa kluczowe i słowa kluczowe, aby zapewnić ukierunkowane filtrowanie.</span><span class="sxs-lookup"><span data-stu-id="e903d-125">In addition to the event keyword filters, the rundown provider also supports the `StartRundownKeyword` and `EndRundownKeyword` keywords to provide targeted filtering.</span></span>  
  
### <a name="start-rundown"></a><span data-ttu-id="e903d-126">Rozpocznij przegląd</span><span class="sxs-lookup"><span data-stu-id="e903d-126">Start Rundown</span></span>  
 <span data-ttu-id="e903d-127">Rundown start jest wyzwalany, gdy rejestrowanie w obszarze `StartRundownKeyword` dostawcy wybiegiem jest włączona za pomocą słowa kluczowego.</span><span class="sxs-lookup"><span data-stu-id="e903d-127">A start rundown is triggered when logging under the rundown provider is enabled with the `StartRundownKeyword` keyword.</span></span> <span data-ttu-id="e903d-128">Powoduje to, że `DCStart` zdarzenie ma zostać podniesione i przechwytuje stan systemu.</span><span class="sxs-lookup"><span data-stu-id="e903d-128">This causes the `DCStart` event to be raised, and captures the state of the system.</span></span> <span data-ttu-id="e903d-129">Przed rozpoczęciem wyliczenia `DCStartInit` zdarzenie jest wywoływane.</span><span class="sxs-lookup"><span data-stu-id="e903d-129">Before the start of the enumeration, the `DCStartInit` event is raised.</span></span> <span data-ttu-id="e903d-130">Na końcu wyliczenia `DCStartComplete` zdarzenie jest wywoływane, aby powiadomić kontrolera, że zbieranie danych zakończone normalnie.</span><span class="sxs-lookup"><span data-stu-id="e903d-130">At the end of the enumeration, the `DCStartComplete` event is raised to notify the controller that data collection terminated normally.</span></span>  
  
### <a name="end-rundown"></a><span data-ttu-id="e903d-131">Koniec schyłk</span><span class="sxs-lookup"><span data-stu-id="e903d-131">End Rundown</span></span>  
 <span data-ttu-id="e903d-132">Zakończenie rundown jest wyzwalany, gdy rejestrowanie w ramach `EndRundownKeyword` dostawcy rundown jest włączona za pomocą słowa kluczowego.</span><span class="sxs-lookup"><span data-stu-id="e903d-132">An end rundown is triggered when logging under the rundown provider is enabled with the `EndRundownKeyword` keyword.</span></span> <span data-ttu-id="e903d-133">Koniec spływu zatrzymuje profilowanie w procesie, który jest kontynuowany.</span><span class="sxs-lookup"><span data-stu-id="e903d-133">End rundown stops profiling on a process that continues to execute.</span></span> <span data-ttu-id="e903d-134">Zdarzenia `DCEnd` przechwytują stan systemu po zatrzymaniu profilowania.</span><span class="sxs-lookup"><span data-stu-id="e903d-134">The `DCEnd` events capture the state of the system when profiling is stopped.</span></span>  
  
 <span data-ttu-id="e903d-135">Przed rozpoczęciem wyliczenia `DCEndInit` zdarzenie jest wywoływane.</span><span class="sxs-lookup"><span data-stu-id="e903d-135">Before the start of the enumeration, the `DCEndInit` event is raised.</span></span> <span data-ttu-id="e903d-136">Na końcu wyliczenia `DCEndComplete` zdarzenie jest wywoływane, aby powiadomić konsumenta, że zbieranie danych zakończone normalnie.</span><span class="sxs-lookup"><span data-stu-id="e903d-136">At the end of the enumeration, the `DCEndComplete` event is raised to notify the consumer that data collection terminated normally.</span></span> <span data-ttu-id="e903d-137">Rundown start i end rundown są używane głównie do rozpoznawania symboli zarządzanych.</span><span class="sxs-lookup"><span data-stu-id="e903d-137">Start rundown and end rundown are primarily used for managed symbol resolution.</span></span> <span data-ttu-id="e903d-138">Rozpocznij rundown może dostarczyć informacji o zakresie adresów dla metod, które zostały już Skompilowane JIT przed rozpoczęciem sesji profilowania.</span><span class="sxs-lookup"><span data-stu-id="e903d-138">Start rundown can provide address range information for methods that were already JIT-compiled before the profiling session was started.</span></span> <span data-ttu-id="e903d-139">End rundown może dostarczyć informacji o zakresie adresów dla wszystkich metod, które zostały skompilowane przez JIT, gdy profilowanie ma zostać wyłączone.</span><span class="sxs-lookup"><span data-stu-id="e903d-139">End rundown can provide address range information for all methods that have been JIT-compiled when profiling is about to be turned off.</span></span>  
  
 <span data-ttu-id="e903d-140">Zakończenie zakończenia nie odbywa się automatycznie po zatrzymaniu sesji profilowania.</span><span class="sxs-lookup"><span data-stu-id="e903d-140">End rundown does not happen automatically when a profiling session is stopped.</span></span> <span data-ttu-id="e903d-141">Zamiast tego narzędzie, które stara się wykonać rozpoznawanie symboli zarządzanych musi jawnie `EndRundownKeyword` wywołać sesję dostawcy rundown CLR z włączoną słowem kluczowym, tuż przed zatrzymaniem profilowania.</span><span class="sxs-lookup"><span data-stu-id="e903d-141">Instead, a tool that is seeking to perform managed symbol resolution has to explicitly invoke a CLR rundown provider session with the `EndRundownKeyword` keyword enabled, just before profiling is stopped.</span></span>  
  
 <span data-ttu-id="e903d-142">Mimo że uruchomienie lub zakończenie rundown może dostarczyć informacji o zakresie adresów metody `EndRundownKeyword` dla rozpoznawania `DCEnd` symboli zarządzanych, `StartRundownKeyword` zaleca się `DCStart` użycie słowa kluczowego (które dostarcza zdarzenia) zamiast słowa kluczowego (które dostarcza zdarzenia).</span><span class="sxs-lookup"><span data-stu-id="e903d-142">Although either start rundown or end rundown can provide method address range information for managed symbol resolution, we recommend that you use the `EndRundownKeyword` keyword (which supplies `DCEnd` events) instead of the `StartRundownKeyword` keyword (which supplies `DCStart` events).</span></span> <span data-ttu-id="e903d-143">Za `StartRundownKeyword` pomocą powoduje, że rundown się podczas sesji profilowania, które mogą zakłócać profilowane scenariusz.</span><span class="sxs-lookup"><span data-stu-id="e903d-143">Using `StartRundownKeyword` causes the rundown to happen during the profiling session, which may disturb the profiled scenario.</span></span>  
  
## <a name="etw-data-collection-using-runtime-and-rundown-providers"></a><span data-ttu-id="e903d-144">Zbieranie danych ETW przy użyciu dostawców środowiska uruchomieniowego i wybiegiem</span><span class="sxs-lookup"><span data-stu-id="e903d-144">ETW Data Collection Using Runtime and Rundown Providers</span></span>  
 <span data-ttu-id="e903d-145">W poniższym przykładzie pokazano, jak używać dostawcy wybiegiem CLR w sposób, który umożliwia rozpoznawanie symboli zarządzanych procesów o minimalnym wpływie, niezależnie od tego, czy procesy rozpoczynają się, czy kończą wewnątrz, czy na zewnątrz okna profilowanego.</span><span class="sxs-lookup"><span data-stu-id="e903d-145">The following example demonstrates how to use the CLR rundown provider in a way that allows symbol resolution of managed processes with minimal impact, regardless of whether the processes start or end inside or outside the profiled window.</span></span>  
  
1. <span data-ttu-id="e903d-146">Włącz rejestrowanie ETW przy użyciu dostawcy środowiska wykonawczego CLR:</span><span class="sxs-lookup"><span data-stu-id="e903d-146">Turn on ETW logging by using the CLR runtime provider:</span></span>  
  
    ```console
    xperf -start clr -on e13c0d23-ccbc-4e12-931b-d9cc2eee27e4:0x1CCBD:0x5 -f clr1.etl
    ```  
  
     <span data-ttu-id="e903d-147">Dziennik zostanie zapisany w pliku clr1.etl.</span><span class="sxs-lookup"><span data-stu-id="e903d-147">The log will be saved to the clr1.etl file.</span></span>  
  
2. <span data-ttu-id="e903d-148">Aby zatrzymać profilowanie, podczas gdy proces jest kontynuowany, `DCEnd` uruchom dostawcę wybiegu, aby przechwycić zdarzenia:</span><span class="sxs-lookup"><span data-stu-id="e903d-148">To stop profiling while the process continues to execute, start the rundown provider to capture the `DCEnd` events:</span></span>  
  
    ```console
    xperf -start clrRundown -on A669021C-C450-4609-A035-5AF59AF4DF18:0xB8:0x5 -f clr2.etl
    ```  
  
     <span data-ttu-id="e903d-149">Dzięki temu kolekcja `DCEnd` zdarzeń, aby rozpocząć sesję schyłka.</span><span class="sxs-lookup"><span data-stu-id="e903d-149">This enables the collection of `DCEnd` events to start a rundown session.</span></span> <span data-ttu-id="e903d-150">Może być konieczne odczekanie od 30 do 60 sekund na zebranie wszystkich zdarzeń.</span><span class="sxs-lookup"><span data-stu-id="e903d-150">You may need to wait 30 to 60 seconds for all events to be collected.</span></span> <span data-ttu-id="e903d-151">Dziennik zostanie zapisany w pliku clr1.et2.</span><span class="sxs-lookup"><span data-stu-id="e903d-151">The log will be saved to the clr1.et2 file.</span></span>  
  
3. <span data-ttu-id="e903d-152">Wyłącz wszystkie profilowania ETW:</span><span class="sxs-lookup"><span data-stu-id="e903d-152">Turn off all ETW profiling:</span></span>  
  
    ```console
    xperf -stop clrRundown
    xperf -stop clr  
    ```  
  
4. <span data-ttu-id="e903d-153">Scal profile, aby utworzyć jeden plik dziennika:</span><span class="sxs-lookup"><span data-stu-id="e903d-153">Merge the profiles to create one log file:</span></span>  
  
    ```console
    xperf -merge clr1.etl clr2.etl merged.etl  
    ```  
  
     <span data-ttu-id="e903d-154">Plik merged.etl będzie zawierał zdarzenia ze środowiska wykonawczego i sesji dostawcy wybiegu.</span><span class="sxs-lookup"><span data-stu-id="e903d-154">The merged.etl file will contain the events from the runtime and the rundown provider sessions.</span></span>  
  
 <span data-ttu-id="e903d-155">Narzędzie może wykonać kroki 2 i 3 (rozpoczęcie sesji wybiegiem, a następnie zakończenie profilowania) zamiast natychmiast wyłączyć profilowanie, gdy użytkownik zażąda zatrzymania profilowania.</span><span class="sxs-lookup"><span data-stu-id="e903d-155">A tool can execute steps 2 and 3 (starting a rundown session and then terminating profiling) instead of immediately turning off profiling when a user requests profiling to be stopped.</span></span> <span data-ttu-id="e903d-156">Narzędzie może również wykonać krok 4.</span><span class="sxs-lookup"><span data-stu-id="e903d-156">A tool can also execute step 4.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="e903d-157">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="e903d-157">See also</span></span>

- [<span data-ttu-id="e903d-158">Zdarzenia ETW w środowisku CLR</span><span class="sxs-lookup"><span data-stu-id="e903d-158">ETW Events in the Common Language Runtime</span></span>](etw-events-in-the-common-language-runtime.md)
