---
title: Jak bezpiecznie rzucać za pomocą dopasowywania wzorców i jest i jako operatorzy
description: Dowiedz się, jak używać technik dopasowywania wzorców, aby bezpiecznie rzutować zmienne na inny typ. Można użyć dopasowywania wzorców, jak również is i operatorów do bezpiecznego konwertowania typów.
ms.date: 09/05/2018
helpviewer_keywords:
- cast operators [C#], as and is operators
- as operator [C#]
- is operator [C#]
ms.openlocfilehash: 34c34865a2feece5fe037daeb035292aafd015b9
ms.sourcegitcommit: 99b153b93bf94d0fecf7c7bcecb58ac424dfa47c
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/25/2020
ms.locfileid: "80249360"
---
# <a name="how-to-safely-cast-by-using-pattern-matching-and-the-is-and-as-operators"></a><span data-ttu-id="e5ab5-104">Jak bezpiecznie rzucać za pomocą dopasowywania wzorców i jest i jako operatorzy</span><span class="sxs-lookup"><span data-stu-id="e5ab5-104">How to safely cast by using pattern matching and the is and as operators</span></span>

<span data-ttu-id="e5ab5-105">Ponieważ obiekty są polimorficzne, zmienna typu klasy podstawowej może zawierać [typ](../programming-guide/types/index.md)pochodny .</span><span class="sxs-lookup"><span data-stu-id="e5ab5-105">Because objects are polymorphic, it's possible for a variable of a base class type to hold a derived [type](../programming-guide/types/index.md).</span></span> <span data-ttu-id="e5ab5-106">Aby uzyskać dostęp do elementów członkowskich wystąpienia typu pochodnego, konieczne jest [rzutanie](../programming-guide/types/casting-and-type-conversions.md) wartości z powrotem do typu pochodnego.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-106">To access the derived type's instance members, it's necessary to [cast](../programming-guide/types/casting-and-type-conversions.md) the value back to the derived type.</span></span> <span data-ttu-id="e5ab5-107">Jednak obsada stwarza ryzyko rzucania <xref:System.InvalidCastException>.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-107">However, a cast creates the risk of throwing an <xref:System.InvalidCastException>.</span></span> <span data-ttu-id="e5ab5-108">C# zawiera [instrukcje dopasowania wzorca,](../pattern-matching.md) które wykonują rzutowanie warunkowo tylko wtedy, gdy zakończy się pomyślnie.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-108">C# provides [pattern matching](../pattern-matching.md) statements that perform a cast conditionally only when it will succeed.</span></span> <span data-ttu-id="e5ab5-109">C# zapewnia również [is](../language-reference/operators/type-testing-and-cast.md#is-operator) i [jako](../language-reference/operators/type-testing-and-cast.md#as-operator) operatorów, aby przetestować, jeśli wartość jest pewnego typu.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-109">C# also provides the [is](../language-reference/operators/type-testing-and-cast.md#is-operator) and [as](../language-reference/operators/type-testing-and-cast.md#as-operator) operators to test if a value is of a certain type.</span></span>

<span data-ttu-id="e5ab5-110">W poniższym przykładzie pokazano, `is` jak używać instrukcji dopasowywania wzorców:</span><span class="sxs-lookup"><span data-stu-id="e5ab5-110">The following example shows how to use the pattern matching `is` statement:</span></span>

[!code-csharp[Pattern matching is statement](../../../samples/snippets/csharp/how-to/safelycast/patternmatching/Program.cs#PatternMatchingIs)]

<span data-ttu-id="e5ab5-111">W poprzednim przykładzie przedstawiono kilka funkcji składni dopasowania wzorca.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-111">The preceding sample demonstrates a few features of pattern matching syntax.</span></span> <span data-ttu-id="e5ab5-112">Instrukcja `if (a is Mammal m)` łączy test z przypisaniem inicjowania.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-112">The `if (a is Mammal m)` statement combines the test with an initialization assignment.</span></span> <span data-ttu-id="e5ab5-113">Przypisanie występuje tylko wtedy, gdy test zakończy się pomyślnie.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-113">The assignment occurs only when the test succeeds.</span></span> <span data-ttu-id="e5ab5-114">Zmienna `m` jest tylko w zakresie `if` w osadzonej instrukcji, gdzie została przypisana.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-114">The variable `m` is only in scope in the embedded `if` statement where it has been assigned.</span></span> <span data-ttu-id="e5ab5-115">Nie można `m` uzyskać dostępu później w tej samej metodzie.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-115">You cannot access `m` later in the same method.</span></span> <span data-ttu-id="e5ab5-116">W poprzednim przykładzie pokazano również, jak użyć [ `as` operatora](../language-reference/operators/type-testing-and-cast.md#as-operator) do konwersji obiektu na określony typ.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-116">The preceding example also shows how to use the [`as` operator](../language-reference/operators/type-testing-and-cast.md#as-operator) to convert an object to a specified type.</span></span>

<span data-ttu-id="e5ab5-117">Można również użyć tej samej składni do testowania, jeśli [typ wartości nullable](../language-reference/builtin-types/nullable-value-types.md) ma wartość, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="e5ab5-117">You can also use the same syntax for testing if a [nullable value type](../language-reference/builtin-types/nullable-value-types.md) has a value, as shown in the following example:</span></span>

[!code-csharp[Pattern matching with nullable types](../../../samples/snippets/csharp/how-to/safelycast/nullablepatternmatching/Program.cs#PatternMatchingNullable)]

<span data-ttu-id="e5ab5-118">W poprzednim przykładzie przedstawiono inne funkcje dopasowania wzorca do użycia z konwersjami.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-118">The preceding sample demonstrates other features of pattern matching to use with conversions.</span></span> <span data-ttu-id="e5ab5-119">Można przetestować zmienną dla wzorca null, `null` sprawdzając specjalnie dla wartości.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-119">You can test a variable for the null pattern by checking specifically for the `null` value.</span></span> <span data-ttu-id="e5ab5-120">Gdy wartość środowiska uruchomieniowego `null`zmiennej jest , `is` instrukcja `false`sprawdzania typu zawsze zwraca .</span><span class="sxs-lookup"><span data-stu-id="e5ab5-120">When the runtime value of the variable is `null`, an `is` statement checking for a type always returns `false`.</span></span> <span data-ttu-id="e5ab5-121">Instrukcja `is` dopasowywania wzorców nie zezwala na `int?` typ `Nullable<int>`wartości nullable, takich jak lub , ale można przetestować dla dowolnego innego typu wartości.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-121">The pattern matching `is` statement doesn't allow a nullable value type, such as `int?` or `Nullable<int>`, but you can test for any other value type.</span></span> <span data-ttu-id="e5ab5-122">Wzorce `is` z poprzedniego przykładu nie są ograniczone do typów wartości nullable.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-122">The `is` patterns from the preceding example are not limited to the nullable value types.</span></span> <span data-ttu-id="e5ab5-123">Można również użyć tych wzorców, aby sprawdzić, czy zmienna typu `null`odwołania ma wartość lub jest .</span><span class="sxs-lookup"><span data-stu-id="e5ab5-123">You also can use those patterns to test if a variable of a reference type has a value or it's `null`.</span></span>

<span data-ttu-id="e5ab5-124">W poprzednim przykładzie pokazano również, jak `switch` używać wzorca typu w instrukcji, gdzie zmienna może być jednym z wielu różnych typów.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-124">The preceding sample also shows how you use the type pattern in a `switch` statement where the variable may be one of many different types.</span></span>

<span data-ttu-id="e5ab5-125">Jeśli chcesz przetestować, czy zmienna jest danym typem, ale nie przypisać `is` `as` go do nowej zmiennej, można użyć i operatorów dla typów odwołań i nullable typów wartości.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-125">If you want to test if a variable is a given type, but not assign it to a new variable, you can use the `is` and `as` operators for reference types and nullable value types.</span></span> <span data-ttu-id="e5ab5-126">Poniższy kod pokazuje, `is` jak `as` używać i instrukcji, które były częścią języka C# przed dopasowaniem wzorca został wprowadzony do testowania, jeśli zmienna jest danego typu:</span><span class="sxs-lookup"><span data-stu-id="e5ab5-126">The following code shows how to use the `is` and `as` statements that were part of the C# language before pattern matching was introduced to test if a variable is of a given type:</span></span>

[!code-csharp[testing variable types with the is and as statements](../../../samples/snippets/csharp/how-to/safelycast/asandis/Program.cs#IsAndAs)]

<span data-ttu-id="e5ab5-127">Jak widać, porównując ten kod z kodem dopasowania wzorca, składnia pasująca do wzorca zapewnia bardziej niezawodne funkcje, łącząc test i przypisanie w jednej instrukcji.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-127">As you can see by comparing this code with the pattern matching code, the pattern matching syntax provides more robust features by combining the test and the assignment in a single statement.</span></span> <span data-ttu-id="e5ab5-128">W miarę możliwości należy używać składni dopasowywania wzorca.</span><span class="sxs-lookup"><span data-stu-id="e5ab5-128">Use the pattern matching syntax whenever possible.</span></span>

<span data-ttu-id="e5ab5-129">Możesz wypróbować te przykłady, patrząc na kod w naszym [repozytorium GitHub](https://github.com/dotnet/samples/tree/master/snippets/csharp/how-to/safelycast).</span><span class="sxs-lookup"><span data-stu-id="e5ab5-129">You can try these samples by looking at the code in our [GitHub repository](https://github.com/dotnet/samples/tree/master/snippets/csharp/how-to/safelycast).</span></span> <span data-ttu-id="e5ab5-130">Możesz też pobrać próbki [jako plik zip](https://github.com/dotnet/samples/raw/master/snippets/csharp/how-to/safelycast.zip).</span><span class="sxs-lookup"><span data-stu-id="e5ab5-130">Or you can download the samples [as a zip file](https://github.com/dotnet/samples/raw/master/snippets/csharp/how-to/safelycast.zip).</span></span>
