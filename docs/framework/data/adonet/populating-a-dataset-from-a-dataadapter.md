---
title: Wypełnianie zestawu danych z elementu DataAdapter
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: 3fa0ac7d-e266-4954-bfac-3fbe2f913153
ms.openlocfilehash: d2d7719c7f6c2cacd6d68ecae226673248bbd680
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/12/2020
ms.locfileid: "79149235"
---
# <a name="populating-a-dataset-from-a-dataadapter"></a><span data-ttu-id="0a839-102">Wypełnianie zestawu danych z elementu DataAdapter</span><span class="sxs-lookup"><span data-stu-id="0a839-102">Populating a DataSet from a DataAdapter</span></span>
<span data-ttu-id="0a839-103">ADO.NET <xref:System.Data.DataSet> jest reprezentacją danych rezydentną w pamięci, która zapewnia spójny model programowania relacyjnego niezależny od źródła danych.</span><span class="sxs-lookup"><span data-stu-id="0a839-103">The ADO.NET <xref:System.Data.DataSet> is a memory-resident representation of data that provides a consistent relational programming model independent of the data source.</span></span> <span data-ttu-id="0a839-104">Reprezentuje `DataSet` kompletny zestaw danych, który zawiera tabele, ograniczenia i relacje między tabelami.</span><span class="sxs-lookup"><span data-stu-id="0a839-104">The `DataSet` represents a complete set of data that includes tables, constraints, and relationships among the tables.</span></span> <span data-ttu-id="0a839-105">Ponieważ `DataSet` jest niezależna od źródła `DataSet` danych, a może zawierać dane lokalne do aplikacji i dane z wielu źródeł danych.</span><span class="sxs-lookup"><span data-stu-id="0a839-105">Because the `DataSet` is independent of the data source, a `DataSet` can include data local to the application, and data from multiple data sources.</span></span> <span data-ttu-id="0a839-106">Interakcja z istniejącymi źródłami danych jest kontrolowana za pośrednictwem pliku `DataAdapter`.</span><span class="sxs-lookup"><span data-stu-id="0a839-106">Interaction with existing data sources is controlled through the `DataAdapter`.</span></span>  
  
 <span data-ttu-id="0a839-107">Właściwość `SelectCommand` `DataAdapter` obiektu jest `Command` obiektem pobieranym dane ze źródła danych.</span><span class="sxs-lookup"><span data-stu-id="0a839-107">The `SelectCommand` property of the `DataAdapter` is a `Command` object that retrieves data from the data source.</span></span> <span data-ttu-id="0a839-108">Obiekty `InsertCommand` `UpdateCommand`, `DeleteCommand` i właściwości `DataAdapter` obiektów `Command` są, które zarządzają aktualizacjami danych w źródle danych `DataSet`zgodnie z modyfikacjami wprowadzonymi do danych w pliku .</span><span class="sxs-lookup"><span data-stu-id="0a839-108">The `InsertCommand`, `UpdateCommand`, and `DeleteCommand` properties of the `DataAdapter` are `Command` objects that manage updates to the data in the data source according to modifications made to the data in the `DataSet`.</span></span> <span data-ttu-id="0a839-109">Te właściwości są bardziej szczegółowo opisane w [aktualizacji źródeł danych za pomocą programów DataAdapters](updating-data-sources-with-dataadapters.md).</span><span class="sxs-lookup"><span data-stu-id="0a839-109">These properties are covered in more detail in [Updating Data Sources with DataAdapters](updating-data-sources-with-dataadapters.md).</span></span>  
  
 <span data-ttu-id="0a839-110">Metoda `Fill` `DataAdapter` jest używana do zapełnienia `DataSet` a z `SelectCommand` wynikami `DataAdapter`.</span><span class="sxs-lookup"><span data-stu-id="0a839-110">The `Fill` method of the `DataAdapter` is used to populate a `DataSet` with the results of the `SelectCommand` of the `DataAdapter`.</span></span> <span data-ttu-id="0a839-111">`Fill`przyjmuje za swoje `DataSet` argumenty a do `DataTable` wypełnienia, a obiekt `DataTable` lub nazwę do wypełnienia wierszami zwróconymi z `SelectCommand`.</span><span class="sxs-lookup"><span data-stu-id="0a839-111">`Fill` takes as its arguments a `DataSet` to be populated, and a `DataTable` object, or the name of the `DataTable` to be filled with the rows returned from the `SelectCommand`.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="0a839-112">Za `DataAdapter` pomocą do pobrania wszystkich tabeli wymaga czasu, zwłaszcza jeśli istnieje wiele wierszy w tabeli.</span><span class="sxs-lookup"><span data-stu-id="0a839-112">Using the `DataAdapter` to retrieve all of a table takes time, especially if there are many rows in the table.</span></span> <span data-ttu-id="0a839-113">Dzieje się tak, ponieważ uzyskiwanie dostępu do bazy danych, lokalizowanie i przetwarzanie danych, a następnie przesyłanie danych do klienta jest czasochłonne.</span><span class="sxs-lookup"><span data-stu-id="0a839-113">This is because accessing the database, locating and processing the data, and then transferring the data to the client is time-consuming.</span></span> <span data-ttu-id="0a839-114">Ciągnięcie całej tabeli do klienta blokuje również wszystkie wiersze na serwerze.</span><span class="sxs-lookup"><span data-stu-id="0a839-114">Pulling all of the table to the client also locks all of the rows on the server.</span></span> <span data-ttu-id="0a839-115">Aby zwiększyć wydajność, można `WHERE` użyć klauzuli, aby znacznie zmniejszyć liczbę wierszy zwróconych do klienta.</span><span class="sxs-lookup"><span data-stu-id="0a839-115">To improve performance, you can use the `WHERE` clause to greatly reduce the number of rows returned to the client.</span></span> <span data-ttu-id="0a839-116">Można również zmniejszyć ilość danych zwróconych do klienta tylko jawnie aukcji `SELECT` wymaganych kolumn w instrukcji.</span><span class="sxs-lookup"><span data-stu-id="0a839-116">You can also reduce the amount of data returned to the client by only explicitly listing required columns in the `SELECT` statement.</span></span> <span data-ttu-id="0a839-117">Innym dobrym rozwiązaniem jest pobranie wierszy w partiach (na przykład kilkaset wierszy naraz) i pobieranie następnej partii tylko po zakończeniu klienta z bieżącą partią.</span><span class="sxs-lookup"><span data-stu-id="0a839-117">Another good workaround is to retrieve the rows in batches (such as several hundred rows at a time) and only retrieve the next batch when the client is finished with the current batch.</span></span>  
  
 <span data-ttu-id="0a839-118">Metoda `Fill` używa `DataReader` obiektu niejawnie do zwrócenia nazw kolumn i typów, które `DataSet`są używane do tworzenia tabel w , i `DataSet`danych do wypełniania wierszy tabel w .</span><span class="sxs-lookup"><span data-stu-id="0a839-118">The `Fill` method uses the `DataReader` object implicitly to return the column names and types that are used to create the tables in the `DataSet`, and the data to populate the rows of the tables in the `DataSet`.</span></span> <span data-ttu-id="0a839-119">Tabele i kolumny są tworzone tylko wtedy, gdy jeszcze nie istnieją; w `Fill` przeciwnym razie `DataSet` używa istniejącego schematu.</span><span class="sxs-lookup"><span data-stu-id="0a839-119">Tables and columns are only created if they do not already exist; otherwise `Fill` uses the existing `DataSet` schema.</span></span> <span data-ttu-id="0a839-120">Typy kolumn są tworzone jako typy programu .NET Framework zgodnie z tabelami w [mapowaniach typów danych w ADO.NET](data-type-mappings-in-ado-net.md).</span><span class="sxs-lookup"><span data-stu-id="0a839-120">Column types are created as .NET Framework types according to the tables in [Data Type Mappings in ADO.NET](data-type-mappings-in-ado-net.md).</span></span> <span data-ttu-id="0a839-121">Klucze podstawowe nie są tworzone, chyba `DataAdapter`że istnieją w źródle danych i **.**`MissingSchemaAction`</span><span class="sxs-lookup"><span data-stu-id="0a839-121">Primary keys are not created unless they exist in the data source and `DataAdapter`**.**`MissingSchemaAction`</span></span> <span data-ttu-id="0a839-122">jest ustawiona na `MissingSchemaAction` **.** `AddWithKey`.</span><span class="sxs-lookup"><span data-stu-id="0a839-122">is set to `MissingSchemaAction`**.**`AddWithKey`.</span></span> <span data-ttu-id="0a839-123">Jeśli `Fill` stwierdzi, że klucz podstawowy istnieje dla tabeli, `DataSet` zastąpi dane z danymi ze źródła danych dla wierszy, w których wartości kolumny klucza podstawowego są zgodne z wartościami wiersza zwróconego ze źródła danych.</span><span class="sxs-lookup"><span data-stu-id="0a839-123">If `Fill` finds that a primary key exists for a table, it will overwrite data in the `DataSet` with data from the data source for rows where the primary key column values match those of the row returned from the data source.</span></span> <span data-ttu-id="0a839-124">Jeśli nie zostanie znaleziony żaden klucz podstawowy, dane są `DataSet`dołączane do tabel w pliku .</span><span class="sxs-lookup"><span data-stu-id="0a839-124">If no primary key is found, the data is appended to the tables in the `DataSet`.</span></span> <span data-ttu-id="0a839-125">`Fill`używa wszelkich mapowań, które mogą istnieć `DataSet` podczas wypełniania (zobacz [DataAdapter DataTable i DataColumn Mappings](dataadapter-datatable-and-datacolumn-mappings.md)).</span><span class="sxs-lookup"><span data-stu-id="0a839-125">`Fill` uses any mappings that may exist when you populate the `DataSet` (see [DataAdapter DataTable and DataColumn Mappings](dataadapter-datatable-and-datacolumn-mappings.md)).</span></span>  
  
> [!NOTE]
> <span data-ttu-id="0a839-126">Jeśli `SelectCommand` zwraca wyniki SPRZĘŻENIA `DataAdapter` ZEWNĘTRZNEGO, nie `PrimaryKey` ustawia wartości `DataTable`wynikowego .</span><span class="sxs-lookup"><span data-stu-id="0a839-126">If the `SelectCommand` returns the results of an OUTER JOIN, the `DataAdapter` does not set a `PrimaryKey` value for the resulting `DataTable`.</span></span> <span data-ttu-id="0a839-127">Należy zdefiniować `PrimaryKey` samodzielnie, aby upewnić się, że zduplikowane wiersze są poprawnie rozpoznawane.</span><span class="sxs-lookup"><span data-stu-id="0a839-127">You must define the `PrimaryKey` yourself to make sure that duplicate rows are resolved correctly.</span></span> <span data-ttu-id="0a839-128">Aby uzyskać więcej informacji, zobacz [Definiowanie kluczy podstawowych](./dataset-datatable-dataview/defining-primary-keys.md).</span><span class="sxs-lookup"><span data-stu-id="0a839-128">For more information, see [Defining Primary Keys](./dataset-datatable-dataview/defining-primary-keys.md).</span></span>  
  
 <span data-ttu-id="0a839-129">Poniższy przykład kodu tworzy <xref:System.Data.SqlClient.SqlDataAdapter> wystąpienie, <xref:System.Data.SqlClient.SqlConnection> które używa do `Northwind` bazy danych programu <xref:System.Data.DataTable> Microsoft `DataSet` SQL Server i wypełnia w a z listy klientów.</span><span class="sxs-lookup"><span data-stu-id="0a839-129">The following code example creates an instance of a <xref:System.Data.SqlClient.SqlDataAdapter> that uses a <xref:System.Data.SqlClient.SqlConnection> to the Microsoft SQL Server `Northwind` database and populates a <xref:System.Data.DataTable> in a `DataSet` with the list of customers.</span></span> <span data-ttu-id="0a839-130">Instrukcja SQL <xref:System.Data.SqlClient.SqlConnection> i argumenty <xref:System.Data.SqlClient.SqlDataAdapter> przekazywane do konstruktora są używane do tworzenia <xref:System.Data.SqlClient.SqlDataAdapter.SelectCommand%2A> właściwości <xref:System.Data.SqlClient.SqlDataAdapter>.</span><span class="sxs-lookup"><span data-stu-id="0a839-130">The SQL statement and <xref:System.Data.SqlClient.SqlConnection> arguments passed to the <xref:System.Data.SqlClient.SqlDataAdapter> constructor are used to create the <xref:System.Data.SqlClient.SqlDataAdapter.SelectCommand%2A> property of the <xref:System.Data.SqlClient.SqlDataAdapter>.</span></span>  
  
## <a name="example"></a><span data-ttu-id="0a839-131">Przykład</span><span class="sxs-lookup"><span data-stu-id="0a839-131">Example</span></span>  
  
```vb  
' Assumes that connection is a valid SqlConnection object.  
Dim queryString As String = _  
  "SELECT CustomerID, CompanyName FROM dbo.Customers"  
Dim adapter As SqlDataAdapter = New SqlDataAdapter( _  
  queryString, connection)  
  
Dim customers As DataSet = New DataSet  
adapter.Fill(customers, "Customers")  
```  
  
```csharp  
// Assumes that connection is a valid SqlConnection object.  
string queryString =
  "SELECT CustomerID, CompanyName FROM dbo.Customers";  
SqlDataAdapter adapter = new SqlDataAdapter(queryString, connection);  
  
DataSet customers = new DataSet();  
adapter.Fill(customers, "Customers");  
```  
  
> [!NOTE]
> <span data-ttu-id="0a839-132">Kod pokazany w tym przykładzie nie otwiera `Connection`jawnie i zamyka plik .</span><span class="sxs-lookup"><span data-stu-id="0a839-132">The code shown in this example does not explicitly open and close the `Connection`.</span></span> <span data-ttu-id="0a839-133">Metoda `Fill` niejawnie `Connection` otwiera, `DataAdapter` że używa, jeśli stwierdzi, że połączenie nie jest jeszcze otwarte.</span><span class="sxs-lookup"><span data-stu-id="0a839-133">The `Fill` method implicitly opens the `Connection` that the `DataAdapter` is using if it finds that the connection is not already open.</span></span> <span data-ttu-id="0a839-134">Po `Fill` otwarciu połączenia, to również `Fill` zamyka połączenie po zakończeniu.</span><span class="sxs-lookup"><span data-stu-id="0a839-134">If `Fill` opened the connection, it also closes the connection when `Fill` is finished.</span></span> <span data-ttu-id="0a839-135">Może to uprościć kod, gdy masz `Fill` do `Update`czynienia z pojedynczą operacją, taką jak a lub .</span><span class="sxs-lookup"><span data-stu-id="0a839-135">This can simplify your code when you deal with a single operation such as a `Fill` or an `Update`.</span></span> <span data-ttu-id="0a839-136">Jeśli jednak wykonujesz wiele operacji, które wymagają otwartego połączenia, możesz poprawić wydajność `Open` aplikacji, `Connection`jawnie wywołując metodę , wykonując operacje `Close` ze `Connection`źródłem danych, a następnie wywołując metodę .</span><span class="sxs-lookup"><span data-stu-id="0a839-136">However, if you are performing multiple operations that require an open connection, you can improve the performance of your application by explicitly calling the `Open` method of the `Connection`, performing the operations against the data source, and then calling the `Close` method of the `Connection`.</span></span> <span data-ttu-id="0a839-137">Należy starać się zachować połączenia ze źródłem danych otwarte tak krótko, jak to możliwe, aby zwolnić zasoby do użytku przez inne aplikacje klienckie.</span><span class="sxs-lookup"><span data-stu-id="0a839-137">You should try to keep connections to the data source open as briefly as possible to free resources for use by other client applications.</span></span>  
  
## <a name="multiple-result-sets"></a><span data-ttu-id="0a839-138">Wiele zestawów wyników</span><span class="sxs-lookup"><span data-stu-id="0a839-138">Multiple Result Sets</span></span>  
 <span data-ttu-id="0a839-139">Jeśli `DataAdapter` napotka wiele zestawów wyników, `DataSet`tworzy wiele tabel w .</span><span class="sxs-lookup"><span data-stu-id="0a839-139">If the `DataAdapter` encounters multiple result sets, it creates multiple tables in the `DataSet`.</span></span> <span data-ttu-id="0a839-140">Tabele otrzymują przyrostową domyślną nazwę tabeli*N,* zaczynając od "Tabela" dla tabeli0.</span><span class="sxs-lookup"><span data-stu-id="0a839-140">The tables are given an incremental default name of Table*N*, starting with "Table" for Table0.</span></span> <span data-ttu-id="0a839-141">Jeśli nazwa tabeli jest przekazywana jako argument do `Fill` metody, tabele otrzymują przyrostową domyślną nazwę TableName*N,* począwszy od "TableName" dla TableName0.</span><span class="sxs-lookup"><span data-stu-id="0a839-141">If a table name is passed as an argument to the `Fill` method, the tables are given an incremental default name of TableName*N*, starting with "TableName" for TableName0.</span></span>  
  
## <a name="populating-a-dataset-from-multiple-dataadapters"></a><span data-ttu-id="0a839-142">Wypełnianie zestawu danych z wielu danychAdapters</span><span class="sxs-lookup"><span data-stu-id="0a839-142">Populating a DataSet from Multiple DataAdapters</span></span>  
 <span data-ttu-id="0a839-143">Z dowolnym obiektem `DataAdapter` `DataSet`można używać dowolnej liczby obiektów z obiektem .</span><span class="sxs-lookup"><span data-stu-id="0a839-143">Any number of `DataAdapter` objects can be used with a `DataSet`.</span></span> <span data-ttu-id="0a839-144">Każdy `DataAdapter` może służyć do wypełnienia `DataTable` jednego lub więcej obiektów i rozpoznać aktualizacje z powrotem do odpowiedniego źródła danych.</span><span class="sxs-lookup"><span data-stu-id="0a839-144">Each `DataAdapter` can be used to fill one or more `DataTable` objects and resolve updates back to the relevant data source.</span></span> <span data-ttu-id="0a839-145">`DataRelation`i `Constraint` obiekty mogą być `DataSet` dodawane do lokalnie, co umożliwia powiązanie danych z różnych źródeł danych.</span><span class="sxs-lookup"><span data-stu-id="0a839-145">`DataRelation` and `Constraint` objects can be added to the `DataSet` locally, which enables you to relate data from dissimilar data sources.</span></span> <span data-ttu-id="0a839-146">Na przykład `DataSet` może zawierać dane z bazy danych programu Microsoft SQL Server, bazy danych IBM DB2 udostępniane za pośrednictwem ole DB i źródła danych, które strumieniuje XML.</span><span class="sxs-lookup"><span data-stu-id="0a839-146">For example, a `DataSet` can contain data from a Microsoft SQL Server database, an IBM DB2 database exposed through OLE DB, and a data source that streams XML.</span></span> <span data-ttu-id="0a839-147">Jeden lub `DataAdapter` więcej obiektów może obsługiwać komunikację z każdym źródłem danych.</span><span class="sxs-lookup"><span data-stu-id="0a839-147">One or more `DataAdapter` objects can handle communication to each data source.</span></span>  
  
### <a name="example"></a><span data-ttu-id="0a839-148">Przykład</span><span class="sxs-lookup"><span data-stu-id="0a839-148">Example</span></span>  
 <span data-ttu-id="0a839-149">Poniższy przykład kodu wypełnia listę klientów `Northwind` z bazy danych na programie Microsoft SQL `Northwind` Server oraz listę zamówień z bazy danych przechowywanej w programie Microsoft Access 2000.</span><span class="sxs-lookup"><span data-stu-id="0a839-149">The following code example populates a list of customers from the `Northwind` database on Microsoft SQL Server, and a list of orders from the `Northwind` database stored in Microsoft Access 2000.</span></span> <span data-ttu-id="0a839-150">Wypełnione tabele są `DataRelation`powiązane z programem , a lista odbiorców jest następnie wyświetlana z zamówieniami dla tego odbiorcy.</span><span class="sxs-lookup"><span data-stu-id="0a839-150">The filled tables are related with a `DataRelation`, and the list of customers is then displayed with the orders for that customer.</span></span> <span data-ttu-id="0a839-151">Aby uzyskać `DataRelation` więcej informacji o obiektach, zobacz [Dodawanie danychrelacji](./dataset-datatable-dataview/adding-datarelations.md) i [nawigowanie do danych](./dataset-datatable-dataview/navigating-datarelations.md).</span><span class="sxs-lookup"><span data-stu-id="0a839-151">For more information about `DataRelation` objects, see [Adding DataRelations](./dataset-datatable-dataview/adding-datarelations.md) and [Navigating DataRelations](./dataset-datatable-dataview/navigating-datarelations.md).</span></span>  
  
```vb  
' Assumes that customerConnection is a valid SqlConnection object.  
' Assumes that orderConnection is a valid OleDbConnection object.  
Dim custAdapter As SqlDataAdapter = New SqlDataAdapter( _  
  "SELECT * FROM dbo.Customers", customerConnection)  
  
Dim ordAdapter As OleDbDataAdapter = New OleDbDataAdapter( _  
  "SELECT * FROM Orders", orderConnection)  
  
Dim customerOrders As DataSet = New DataSet()  
custAdapter.Fill(customerOrders, "Customers")  
ordAdapter.Fill(customerOrders, "Orders")  
  
Dim relation As DataRelation = _  
  customerOrders.Relations.Add("CustOrders", _  
  customerOrders.Tables("Customers").Columns("CustomerID"), _
  customerOrders.Tables("Orders").Columns("CustomerID"))  
  
Dim pRow, cRow As DataRow  
For Each pRow In customerOrders.Tables("Customers").Rows  
  Console.WriteLine(pRow("CustomerID").ToString())  
  
  For Each cRow In pRow.GetChildRows(relation)  
    Console.WriteLine(vbTab & cRow("OrderID").ToString())  
  Next  
Next  
```  
  
```csharp  
// Assumes that customerConnection is a valid SqlConnection object.  
// Assumes that orderConnection is a valid OleDbConnection object.  
SqlDataAdapter custAdapter = new SqlDataAdapter(  
  "SELECT * FROM dbo.Customers", customerConnection);  
OleDbDataAdapter ordAdapter = new OleDbDataAdapter(  
  "SELECT * FROM Orders", orderConnection);  
  
DataSet customerOrders = new DataSet();  
  
custAdapter.Fill(customerOrders, "Customers");  
ordAdapter.Fill(customerOrders, "Orders");  
  
DataRelation relation = customerOrders.Relations.Add("CustOrders",  
  customerOrders.Tables["Customers"].Columns["CustomerID"],  
  customerOrders.Tables["Orders"].Columns["CustomerID"]);  
  
foreach (DataRow pRow in customerOrders.Tables["Customers"].Rows)  
{  
  Console.WriteLine(pRow["CustomerID"]);  
   foreach (DataRow cRow in pRow.GetChildRows(relation))  
    Console.WriteLine("\t" + cRow["OrderID"]);  
}  
```  
  
## <a name="sql-server-decimal-type"></a><span data-ttu-id="0a839-152">Typ dziesiętny programu SQL Server</span><span class="sxs-lookup"><span data-stu-id="0a839-152">SQL Server Decimal Type</span></span>  
 <span data-ttu-id="0a839-153">Domyślnie przechowuje `DataSet` dane przy użyciu typów danych .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="0a839-153">By default, the `DataSet` stores data by using .NET Framework data types.</span></span> <span data-ttu-id="0a839-154">W przypadku większości aplikacji zapewniają one wygodną reprezentację informacji o źródle danych.</span><span class="sxs-lookup"><span data-stu-id="0a839-154">For most applications, these provide a convenient representation of data source information.</span></span> <span data-ttu-id="0a839-155">Jednak ta reprezentacja może powodować problem, gdy typ danych w źródle danych jest typem danych dziesiętnych lub liczbowych programu SQL Server.</span><span class="sxs-lookup"><span data-stu-id="0a839-155">However, this representation may cause a problem when the data type in the data source is a SQL Server decimal or numeric data type.</span></span> <span data-ttu-id="0a839-156">Typ danych `decimal` programu .NET Framework umożliwia maksymalnie 28 cyfr znaczących, podczas gdy typ danych programu SQL Server `decimal` umożliwia 38 cyfr znaczących.</span><span class="sxs-lookup"><span data-stu-id="0a839-156">The .NET Framework `decimal` data type allows a maximum of 28 significant digits, whereas the SQL Server `decimal` data type allows 38 significant digits.</span></span> <span data-ttu-id="0a839-157">Jeśli `SqlDataAdapter` podczas `Fill` operacji zostanie stwierdzi, że `decimal` dokładność pola programu SQL Server jest większa niż `DataTable`28 znaków, bieżący wiersz nie zostanie dodany do pliku .</span><span class="sxs-lookup"><span data-stu-id="0a839-157">If the `SqlDataAdapter` determines during a `Fill` operation that the precision of a SQL Server `decimal` field is larger than 28 characters, the current row is not added to the `DataTable`.</span></span> <span data-ttu-id="0a839-158">Zamiast tego `FillError` występuje zdarzenie, które umożliwia określenie, czy wystąpi utrata precyzji i odpowiednio reagować.</span><span class="sxs-lookup"><span data-stu-id="0a839-158">Instead the `FillError` event occurs, which enables you to determine whether a loss of precision will occur, and respond appropriately.</span></span> <span data-ttu-id="0a839-159">Aby uzyskać więcej `FillError` informacji na temat zdarzenia, zobacz [Obsługa zdarzeń dataadapter](handling-dataadapter-events.md).</span><span class="sxs-lookup"><span data-stu-id="0a839-159">For more information about the `FillError` event, see [Handling DataAdapter Events](handling-dataadapter-events.md).</span></span> <span data-ttu-id="0a839-160">Aby uzyskać wartość `decimal` programu SQL Server, <xref:System.Data.SqlClient.SqlDataReader> można również <xref:System.Data.SqlClient.SqlDataReader.GetSqlDecimal%2A> użyć obiektu i wywołać metodę.</span><span class="sxs-lookup"><span data-stu-id="0a839-160">To get the SQL Server `decimal` value, you can also use a <xref:System.Data.SqlClient.SqlDataReader> object and call the <xref:System.Data.SqlClient.SqlDataReader.GetSqlDecimal%2A> method.</span></span>  
  
 <span data-ttu-id="0a839-161">ADO.NET 2.0 wprowadzono rozszerzone wsparcie <xref:System.Data.SqlTypes> w `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="0a839-161">ADO.NET 2.0 introduced enhanced support for <xref:System.Data.SqlTypes> in the `DataSet`.</span></span> <span data-ttu-id="0a839-162">Aby uzyskać więcej informacji, zobacz [SqlTypes i DataSet](./sql/sqltypes-and-the-dataset.md).</span><span class="sxs-lookup"><span data-stu-id="0a839-162">For more information, see [SqlTypes and the DataSet](./sql/sqltypes-and-the-dataset.md).</span></span>  
  
## <a name="ole-db-chapters"></a><span data-ttu-id="0a839-163">Rozdziały OLE DB</span><span class="sxs-lookup"><span data-stu-id="0a839-163">OLE DB Chapters</span></span>  
 <span data-ttu-id="0a839-164">Hierarchiczne zestawy wierszy lub rozdziały (typ `DBTYPE_HCHAPTER`OLE `adChapter`DB , typ ADO `DataSet`) mogą być używane do wypełniania zawartości pliku .</span><span class="sxs-lookup"><span data-stu-id="0a839-164">Hierarchical rowsets, or chapters (OLE DB type `DBTYPE_HCHAPTER`, ADO type `adChapter`) can be used to fill the contents of a `DataSet`.</span></span> <span data-ttu-id="0a839-165">Gdy <xref:System.Data.OleDb.OleDbDataAdapter> napotka roz chaptered `Fill` kolumny `DataTable` podczas operacji, a jest tworzony dla kolumny rozdziałów, a tabela jest wypełniona kolumn i wierszy z rozdziału.</span><span class="sxs-lookup"><span data-stu-id="0a839-165">When the <xref:System.Data.OleDb.OleDbDataAdapter> encounters a chaptered column during a `Fill` operation, a `DataTable` is created for the chaptered column, and that table is filled with the columns and rows from the chapter.</span></span> <span data-ttu-id="0a839-166">Tabela utworzona dla kolumny rozdzielanej nosi nazwę zarówno nazwy tabeli nadrzędnej, jak i nazwy kolumny rozdzielanej w formularzu "*ParentTableNameChapteredColumnName*".</span><span class="sxs-lookup"><span data-stu-id="0a839-166">The table created for the chaptered column is named by using both the parent table name and the chaptered column name in the form "*ParentTableNameChapteredColumnName*".</span></span> <span data-ttu-id="0a839-167">Jeśli tabela już istnieje `DataSet` w nazwie kolumny roz chaptered, bieżąca tabela jest wypełniona danymi rozdziału.</span><span class="sxs-lookup"><span data-stu-id="0a839-167">If a table already exists in the `DataSet` that matches the name of the chaptered column, the current table is filled with the chapter data.</span></span> <span data-ttu-id="0a839-168">Jeśli w istniejącej tabeli nie ma kolumny zgodnej z kolumną znalezioną w rozdziale, dodawana jest nowa kolumna.</span><span class="sxs-lookup"><span data-stu-id="0a839-168">If there is no column in an existing table that matches a column found in the chapter, a new column is added.</span></span>  
  
 <span data-ttu-id="0a839-169">Przed wypełnionymi `DataSet` tabelami w kolumnach rozdziałów tworzona jest relacja między tabelami nadrzędnymi i podrzędnymi hierarchicznego zestawu wierszy przez dodanie kolumny całkowitej do tabeli nadrzędnej i `DataRelation` podrzędnej, ustawienie kolumny nadrzędnej na automatyczny przyrost i utworzenie przy użyciu dodanych kolumn z obu tabel.</span><span class="sxs-lookup"><span data-stu-id="0a839-169">Before the tables in the `DataSet` are filled with the data in the chaptered columns, a relation is created between the parent and child tables of the hierarchical rowset by adding an integer column to both the parent and child table, setting the parent column to auto-increment, and creating a `DataRelation` using the added columns from both tables.</span></span> <span data-ttu-id="0a839-170">Dodana relacja jest nazywana przy użyciu nazw tabeli nadrzędnej i kolumn rozdziału w formularzu "*ParentTableNameChapterColumnName*".</span><span class="sxs-lookup"><span data-stu-id="0a839-170">The added relation is named by using the parent table and chapter column names in the form "*ParentTableNameChapterColumnName*".</span></span>  
  
 <span data-ttu-id="0a839-171">Należy zauważyć, że powiązana `DataSet`kolumna istnieje tylko w pliku .</span><span class="sxs-lookup"><span data-stu-id="0a839-171">Note that the related column only exists in the `DataSet`.</span></span> <span data-ttu-id="0a839-172">Kolejne wypełnienia ze źródła danych mogą powodować dodawanie nowych wierszy do tabel zamiast scalania zmian z istniejącymi wierszami.</span><span class="sxs-lookup"><span data-stu-id="0a839-172">Subsequent fills from the data source can cause new rows to be added to the tables instead of changes being merged into existing rows.</span></span>  
  
 <span data-ttu-id="0a839-173">Należy również zauważyć, że `DataAdapter.Fill` w przypadku `DataTable`użycia przeciążenia, które ma , tylko ta tabela zostanie wypełniona.</span><span class="sxs-lookup"><span data-stu-id="0a839-173">Note also that, if you use the `DataAdapter.Fill` overload that takes a `DataTable`, only that table will be filled.</span></span> <span data-ttu-id="0a839-174">Kolumna liczby całkowitej automatycznego zwiększania liczby całkowitej nadal będzie dodawana do tabeli, ale nie zostanie utworzona ani wypełniona żadna tabela podrzędna i nie zostanie utworzona żadna relacja.</span><span class="sxs-lookup"><span data-stu-id="0a839-174">An auto-incrementing integer column will still be added to the table, but no child table will be created or filled, and no relation will be created.</span></span>  
  
 <span data-ttu-id="0a839-175">W poniższym przykładzie użyto dostawcy MSDataShape do generowania kolumny rozdział zamówień dla każdego klienta na liście odbiorców.</span><span class="sxs-lookup"><span data-stu-id="0a839-175">The following example uses the MSDataShape Provider to generate a chapter column of orders for each customer in a list of customers.</span></span> <span data-ttu-id="0a839-176">A `DataSet` jest następnie wypełniona danymi.</span><span class="sxs-lookup"><span data-stu-id="0a839-176">A `DataSet` is then filled with the data.</span></span>  
  
```vb  
Using connection As OleDbConnection = New OleDbConnection( _  
  "Provider=MSDataShape;Data Provider=SQLOLEDB;" & _  
  "Data Source=(local);Integrated " & _  
  "Security=SSPI;Initial Catalog=northwind")  
  
Dim adapter As OleDbDataAdapter = New OleDbDataAdapter( _  
  "SHAPE {SELECT CustomerID, CompanyName FROM Customers} " & _  
  "APPEND ({SELECT CustomerID, OrderID FROM Orders} AS Orders " & _  
  "RELATE CustomerID TO CustomerID)", connection)  
  
Dim customers As DataSet = New DataSet()  
  
adapter.Fill(customers, "Customers")  
End Using  
```  
  
```csharp  
using (OleDbConnection connection = new OleDbConnection("Provider=MSDataShape;Data Provider=SQLOLEDB;" +  
  "Data Source=(local);Integrated Security=SSPI;Initial Catalog=northwind"))  
{  
OleDbDataAdapter adapter = new OleDbDataAdapter("SHAPE {SELECT CustomerID, CompanyName FROM Customers} " +  
  "APPEND ({SELECT CustomerID, OrderID FROM Orders} AS Orders " +  
  "RELATE CustomerID TO CustomerID)", connection);  
  
DataSet customers = new DataSet();  
adapter.Fill(customers, "Customers");  
}  
```  
  
 <span data-ttu-id="0a839-177">Po `Fill` zakończeniu `DataSet` operacji zawiera dwie `Customers` tabele: i `CustomersOrders`, gdzie `CustomersOrders` reprezentuje kolumnę rozdziałów.</span><span class="sxs-lookup"><span data-stu-id="0a839-177">When the `Fill` operation is complete, the `DataSet` contains two tables: `Customers` and `CustomersOrders`, where `CustomersOrders` represents the chaptered column.</span></span> <span data-ttu-id="0a839-178">Dodatkowa kolumna `Orders` o nazwie `Customers` zostanie dodana do `CustomersOrders` tabeli, `CustomersOrders` a do tabeli zostanie dodana dodatkowa kolumna o nazwie.</span><span class="sxs-lookup"><span data-stu-id="0a839-178">An additional column named `Orders` is added to the `Customers` table, and an additional column named `CustomersOrders` is added to the `CustomersOrders` table.</span></span> <span data-ttu-id="0a839-179">Kolumna `Orders` w `Customers` tabeli jest ustawiona na automatyczne zwiększanie.</span><span class="sxs-lookup"><span data-stu-id="0a839-179">The `Orders` column in the `Customers` table is set to auto-increment.</span></span> <span data-ttu-id="0a839-180">A `DataRelation` `CustomersOrders`, jest tworzony przy użyciu kolumn, które `Customers` zostały dodane do tabel z jako tabela nadrzędna.</span><span class="sxs-lookup"><span data-stu-id="0a839-180">A `DataRelation`, `CustomersOrders`, is created by using the columns that were added to the tables with `Customers` as the parent table.</span></span> <span data-ttu-id="0a839-181">W poniższych tabelach przedstawiono niektóre przykładowe wyniki.</span><span class="sxs-lookup"><span data-stu-id="0a839-181">The following tables show some sample results.</span></span>  
  
### <a name="tablename-customers"></a><span data-ttu-id="0a839-182">Nazwa tabeli: Klienci</span><span class="sxs-lookup"><span data-stu-id="0a839-182">TableName: Customers</span></span>  
  
|<span data-ttu-id="0a839-183">CustomerID</span><span class="sxs-lookup"><span data-stu-id="0a839-183">CustomerID</span></span>|<span data-ttu-id="0a839-184">CompanyName</span><span class="sxs-lookup"><span data-stu-id="0a839-184">CompanyName</span></span>|<span data-ttu-id="0a839-185">Orders</span><span class="sxs-lookup"><span data-stu-id="0a839-185">Orders</span></span>|  
|----------------|-----------------|------------|  
|<span data-ttu-id="0a839-186">Alfki</span><span class="sxs-lookup"><span data-stu-id="0a839-186">ALFKI</span></span>|<span data-ttu-id="0a839-187">Alfreds Futterkiste</span><span class="sxs-lookup"><span data-stu-id="0a839-187">Alfreds Futterkiste</span></span>|<span data-ttu-id="0a839-188">0</span><span class="sxs-lookup"><span data-stu-id="0a839-188">0</span></span>|  
|<span data-ttu-id="0a839-189">ANATR (ANATR)</span><span class="sxs-lookup"><span data-stu-id="0a839-189">ANATR</span></span>|<span data-ttu-id="0a839-190">Ana Trujillo Emparedados y helados</span><span class="sxs-lookup"><span data-stu-id="0a839-190">Ana Trujillo Emparedados y helados</span></span>|<span data-ttu-id="0a839-191">1</span><span class="sxs-lookup"><span data-stu-id="0a839-191">1</span></span>|  
  
### <a name="tablename-customersorders"></a><span data-ttu-id="0a839-192">Nazwa tabeli: CustomersOrders</span><span class="sxs-lookup"><span data-stu-id="0a839-192">TableName: CustomersOrders</span></span>  
  
|<span data-ttu-id="0a839-193">CustomerID</span><span class="sxs-lookup"><span data-stu-id="0a839-193">CustomerID</span></span>|<span data-ttu-id="0a839-194">OrderID</span><span class="sxs-lookup"><span data-stu-id="0a839-194">OrderID</span></span>|<span data-ttu-id="0a839-195">Liczba zamówień na klientów</span><span class="sxs-lookup"><span data-stu-id="0a839-195">CustomersOrders</span></span>|  
|----------------|-------------|---------------------|  
|<span data-ttu-id="0a839-196">Alfki</span><span class="sxs-lookup"><span data-stu-id="0a839-196">ALFKI</span></span>|<span data-ttu-id="0a839-197">10643</span><span class="sxs-lookup"><span data-stu-id="0a839-197">10643</span></span>|<span data-ttu-id="0a839-198">0</span><span class="sxs-lookup"><span data-stu-id="0a839-198">0</span></span>|  
|<span data-ttu-id="0a839-199">Alfki</span><span class="sxs-lookup"><span data-stu-id="0a839-199">ALFKI</span></span>|<span data-ttu-id="0a839-200">10692</span><span class="sxs-lookup"><span data-stu-id="0a839-200">10692</span></span>|<span data-ttu-id="0a839-201">0</span><span class="sxs-lookup"><span data-stu-id="0a839-201">0</span></span>|  
|<span data-ttu-id="0a839-202">ANATR (ANATR)</span><span class="sxs-lookup"><span data-stu-id="0a839-202">ANATR</span></span>|<span data-ttu-id="0a839-203">10308</span><span class="sxs-lookup"><span data-stu-id="0a839-203">10308</span></span>|<span data-ttu-id="0a839-204">1</span><span class="sxs-lookup"><span data-stu-id="0a839-204">1</span></span>|  
|<span data-ttu-id="0a839-205">ANATR (ANATR)</span><span class="sxs-lookup"><span data-stu-id="0a839-205">ANATR</span></span>|<span data-ttu-id="0a839-206">10625</span><span class="sxs-lookup"><span data-stu-id="0a839-206">10625</span></span>|<span data-ttu-id="0a839-207">1</span><span class="sxs-lookup"><span data-stu-id="0a839-207">1</span></span>|  
  
## <a name="see-also"></a><span data-ttu-id="0a839-208">Zobacz też</span><span class="sxs-lookup"><span data-stu-id="0a839-208">See also</span></span>

- [<span data-ttu-id="0a839-209">Elementy DataAdapter i DataReader</span><span class="sxs-lookup"><span data-stu-id="0a839-209">DataAdapters and DataReaders</span></span>](dataadapters-and-datareaders.md)
- [<span data-ttu-id="0a839-210">Mapowanie typu danych w ADO.NET</span><span class="sxs-lookup"><span data-stu-id="0a839-210">Data Type Mappings in ADO.NET</span></span>](data-type-mappings-in-ado-net.md)
- [<span data-ttu-id="0a839-211">Modyfikowanie danych za pomocą obiektu DbDataAdapter</span><span class="sxs-lookup"><span data-stu-id="0a839-211">Modifying Data with a DbDataAdapter</span></span>](modifying-data-with-a-dbdataadapter.md)
- [<span data-ttu-id="0a839-212">Wiele aktywnych zestawów wyników (MARS)</span><span class="sxs-lookup"><span data-stu-id="0a839-212">Multiple Active Result Sets (MARS)</span></span>](./sql/multiple-active-result-sets-mars.md)
- [<span data-ttu-id="0a839-213">Omówienie ADO.NET</span><span class="sxs-lookup"><span data-stu-id="0a839-213">ADO.NET Overview</span></span>](ado-net-overview.md)
