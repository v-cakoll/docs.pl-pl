---
title: Omówienie odnajdywania WCF
ms.date: 03/30/2017
ms.assetid: 84fad0e4-23b1-45b5-a2d4-c9cdf90bbb22
ms.openlocfilehash: 449d54e0dd1948885a7298fb4da46067de3eb9d9
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 03/12/2020
ms.locfileid: "79184211"
---
# <a name="wcf-discovery-overview"></a><span data-ttu-id="ee4de-102">Omówienie odnajdywania WCF</span><span class="sxs-lookup"><span data-stu-id="ee4de-102">WCF Discovery Overview</span></span>
<span data-ttu-id="ee4de-103">Interfejsy API odnajdywania zapewniają ujednolicony model programowania dla dynamicznej publikacji i odnajdowania usług sieci Web przy użyciu protokołu WS-Discovery.</span><span class="sxs-lookup"><span data-stu-id="ee4de-103">The Discovery APIs provide a unified programming model for the dynamic publication and discovery of Web services using the WS-Discovery protocol.</span></span> <span data-ttu-id="ee4de-104">Te interfejsy API umożliwiają usługom publikowanie siebie i klientów w celu znalezienia opublikowanych usług.</span><span class="sxs-lookup"><span data-stu-id="ee4de-104">These APIs allow services to publish themselves and clients to find published services.</span></span> <span data-ttu-id="ee4de-105">Gdy usługa jest wykrywalna, usługa ma możliwość wysyłania komunikatów anonsu, a także nasłuchiwania i odpowiadania na żądania odnajdywania.</span><span class="sxs-lookup"><span data-stu-id="ee4de-105">Once a service is made discoverable, the service has the ability to send announcement messages as well as listen for and respond to discovery requests.</span></span> <span data-ttu-id="ee4de-106">Wykrywalne usługi mogą wysyłać wiadomości Hello, aby ogłosić ich przybycie do sieci i wiadomości Bye, aby ogłosić ich odejście z sieci.</span><span class="sxs-lookup"><span data-stu-id="ee4de-106">Discoverable services can send Hello messages to announce their arrival on a network and Bye messages to announce their departure from a network.</span></span> <span data-ttu-id="ee4de-107">Aby znaleźć usługę, klienci `Probe` wysyłają żądanie, które zawiera określone kryteria, takie jak typ umowy serwisowej, słowa kluczowe i zakres w sieci.</span><span class="sxs-lookup"><span data-stu-id="ee4de-107">To find a service, clients send a `Probe` request that contains specific criteria such as service contract type, keywords, and scope on the network.</span></span> <span data-ttu-id="ee4de-108">Usługi otrzymują `Probe` żądanie i określają, czy są zgodne z kryteriami.</span><span class="sxs-lookup"><span data-stu-id="ee4de-108">Services receive the `Probe` request and determine whether they match the criteria.</span></span> <span data-ttu-id="ee4de-109">Jeśli usługa jest zgodna, odpowiada, `ProbeMatch` wysyłając wiadomość z powrotem do klienta z informacjami niezbędnymi do skontaktowania się z usługą.</span><span class="sxs-lookup"><span data-stu-id="ee4de-109">If a service matches, it responds by sending a `ProbeMatch` message back to the client with the information necessary to contact the service.</span></span> <span data-ttu-id="ee4de-110">Klienci mogą `Resolve` również wysyłać żądania, które umożliwiają im znajdowanie usług, które mogły zmienić ich adres punktu końcowego.</span><span class="sxs-lookup"><span data-stu-id="ee4de-110">Clients can also send `Resolve` requests that allow them to find services that may have changed their endpoint address.</span></span> <span data-ttu-id="ee4de-111">Pasujące usługi `Resolve` odpowiadają na `ResolveMatch` żądania, wysyłając wiadomość z powrotem do klienta.</span><span class="sxs-lookup"><span data-stu-id="ee4de-111">Matching services respond to `Resolve` requests by sending a `ResolveMatch` message back to the client.</span></span>  
  
## <a name="ad-hoc-and-managed-modes"></a><span data-ttu-id="ee4de-112">Tryby ad-hoc i zarządzane</span><span class="sxs-lookup"><span data-stu-id="ee4de-112">Ad-Hoc and Managed Modes</span></span>  
 <span data-ttu-id="ee4de-113">Interfejs API odnajdywania obsługuje dwa różne tryby: zarządzany i ad-hoc.</span><span class="sxs-lookup"><span data-stu-id="ee4de-113">The Discovery API supports two different modes: Managed and Ad-Hoc.</span></span> <span data-ttu-id="ee4de-114">W trybie zarządzanym istnieje scentralizowany serwer o nazwie serwer proxy odnajdowania, który przechowuje informacje o dostępnych usługach.</span><span class="sxs-lookup"><span data-stu-id="ee4de-114">In Managed mode there is a centralized server called a discovery proxy that maintains information about available services.</span></span> <span data-ttu-id="ee4de-115">Serwer proxy odnajdywania może być wypełniany informacjami o usługach na różne sposoby.</span><span class="sxs-lookup"><span data-stu-id="ee4de-115">The discovery proxy can be populated with information about services in a variety of ways.</span></span> <span data-ttu-id="ee4de-116">Na przykład usługi mogą wysyłać komunikaty anonsów podczas uruchamiania do serwera proxy odnajdowania lub serwer proxy może odczytywać dane z bazy danych lub pliku konfiguracji, aby określić, jakie usługi są dostępne.</span><span class="sxs-lookup"><span data-stu-id="ee4de-116">For example, services can send announcement messages during start up to the discovery proxy or the proxy may read data from a database or a configuration file to determine what services are available.</span></span> <span data-ttu-id="ee4de-117">Sposób wypełniania serwera proxy odnajdywania zależy całkowicie od dewelopera.</span><span class="sxs-lookup"><span data-stu-id="ee4de-117">How the discovery proxy is populated is completely up to the developer.</span></span> <span data-ttu-id="ee4de-118">Klienci używają serwera proxy odnajdowania do pobierania informacji o dostępnych usługach.</span><span class="sxs-lookup"><span data-stu-id="ee4de-118">Clients use the discovery proxy to retrieve information about available services.</span></span> <span data-ttu-id="ee4de-119">Gdy klient wyszukuje usługę `Probe` wysyła wiadomość do serwera proxy odnajdywania, a serwer proxy określa, czy którakolwiek z usług, które zna o dopasowaniu usługi, którą klient szuka.</span><span class="sxs-lookup"><span data-stu-id="ee4de-119">When a client searches for a service it sends a `Probe` message to the discovery proxy and the proxy determines whether any of the services it knows about match the service the client is searching for.</span></span> <span data-ttu-id="ee4de-120">Jeśli są zgodne serwera proxy `ProbeMatch` odnajdywania wysyła odpowiedź z powrotem do klienta.</span><span class="sxs-lookup"><span data-stu-id="ee4de-120">If there are matches the discovery proxy sends a `ProbeMatch` response back to the client.</span></span> <span data-ttu-id="ee4de-121">Klient może następnie skontaktować się z usługą bezpośrednio przy użyciu informacji o usłudze zwróconych z serwera proxy.</span><span class="sxs-lookup"><span data-stu-id="ee4de-121">The client can then contact the service directly using the service information returned from the proxy.</span></span> <span data-ttu-id="ee4de-122">Kluczową zasadą trybu zarządzanego jest to, że żądania odnajdywania są wysyłane w sposób emisji pojedynczej do jednego urzędu, serwera proxy odnajdywania.</span><span class="sxs-lookup"><span data-stu-id="ee4de-122">The key principle behind Managed mode is that the discovery requests are sent in a unicast manner to one authority, the discovery proxy.</span></span> <span data-ttu-id="ee4de-123">.NET Framework zawiera kluczowe składniki, które umożliwiają tworzenie własnego serwera proxy.</span><span class="sxs-lookup"><span data-stu-id="ee4de-123">The .NET Framework contains key components that allow you to build your own proxy.</span></span> <span data-ttu-id="ee4de-124">Klienci i usługi mogą zlokalizować serwer proxy za pomocą wielu metod:</span><span class="sxs-lookup"><span data-stu-id="ee4de-124">Clients and services can locate the proxy by multiple methods:</span></span>  
  
- <span data-ttu-id="ee4de-125">Serwer proxy może odpowiadać na wiadomości ad hoc.</span><span class="sxs-lookup"><span data-stu-id="ee4de-125">The proxy can respond to ad-hoc messages.</span></span>  
  
- <span data-ttu-id="ee4de-126">Serwer proxy może wysłać komunikat anonsu podczas uruchamiania.</span><span class="sxs-lookup"><span data-stu-id="ee4de-126">The proxy can send an announcement message during start up.</span></span>  
  
- <span data-ttu-id="ee4de-127">Klienci i usługi mogą być zapisywane w celu wyszukywana określonego dobrze znanego punktu końcowego.</span><span class="sxs-lookup"><span data-stu-id="ee4de-127">Clients and services can be written to look for a specific well-known endpoint.</span></span>  
  
 <span data-ttu-id="ee4de-128">W trybie Ad-Hoc nie ma scentralizowanego serwera.</span><span class="sxs-lookup"><span data-stu-id="ee4de-128">In Ad-Hoc mode, there is no centralized server.</span></span> <span data-ttu-id="ee4de-129">Wszystkie komunikaty odnajdywania, takie jak anonse usług i żądania klientów są wysyłane w sposób multiemisji.</span><span class="sxs-lookup"><span data-stu-id="ee4de-129">All discovery messages such as service announcements and client requests are sent in a multicast fashion.</span></span> <span data-ttu-id="ee4de-130">Domyślnie program .NET Framework zawiera obsługę odnajdowania ad-hoc za pomocą protokołu UDP.</span><span class="sxs-lookup"><span data-stu-id="ee4de-130">By default the .NET Framework contains support for Ad-Hoc discovery over the UDP protocol.</span></span> <span data-ttu-id="ee4de-131">Na przykład jeśli usługa jest skonfigurowana do wysyłania hello anonsu podczas uruchamiania, wysyła go za pośrednictwem dobrze znanego adresu multiemisji przy użyciu protokołu UDP.</span><span class="sxs-lookup"><span data-stu-id="ee4de-131">For example, if a service is configured to send out a Hello announcement on start up, it sends it out over a well-known, multicast address using the UDP protocol.</span></span> <span data-ttu-id="ee4de-132">Klienci muszą aktywnie słuchać tych ogłoszeń i odpowiednio je przetwarzać.</span><span class="sxs-lookup"><span data-stu-id="ee4de-132">Clients have to actively listen for these announcements and process them accordingly.</span></span> <span data-ttu-id="ee4de-133">Gdy klient wysyła `Probe` wiadomość dla usługi, jest on wysyłany przez sieć przy użyciu protokołu multiemisji.</span><span class="sxs-lookup"><span data-stu-id="ee4de-133">When a client sends a `Probe` message for a service it is sent over the network using a multicast protocol.</span></span> <span data-ttu-id="ee4de-134">Każda usługa, która odbiera żądanie określa, czy `Probe` spełnia kryteria w wiadomości i `ProbeMatch` odpowiada bezpośrednio do klienta z `Probe` komunikatem, jeśli usługa spełnia kryteria określone w komunikacie.</span><span class="sxs-lookup"><span data-stu-id="ee4de-134">Each service that receives the request determines whether it matches the criteria in the `Probe` message and responds directly to the client with a `ProbeMatch` message if the service matches the criteria specified in the `Probe` message.</span></span>  
  
## <a name="benefits-of-using-wcf-discovery"></a><span data-ttu-id="ee4de-135">Korzyści z korzystania z WCF Discovery</span><span class="sxs-lookup"><span data-stu-id="ee4de-135">Benefits of Using WCF Discovery</span></span>  
 <span data-ttu-id="ee4de-136">Ponieważ WCF Discovery jest zaimplementowana przy użyciu protokołu WS-Discovery jest interoperacyjne z innymi klientami, usługami i serwerami proxy, które implementują WS-Discovery, jak również.</span><span class="sxs-lookup"><span data-stu-id="ee4de-136">Because WCF Discovery is implemented using the WS-Discovery protocol it is interoperable with other clients, services, and proxies that implement WS-Discovery as well.</span></span> <span data-ttu-id="ee4de-137">Odnajdowanie WCF jest zbudowany na istniejących interfejsów API WCF, co ułatwia dodawanie funkcji odnajdywania do istniejących usług i klientów.</span><span class="sxs-lookup"><span data-stu-id="ee4de-137">WCF Discovery is built upon the existing WCF APIs, which makes it easy to add Discovery functionality to your existing services and clients.</span></span> <span data-ttu-id="ee4de-138">Wykrywalność usługi można łatwo dodać za pomocą ustawień konfiguracji aplikacji.</span><span class="sxs-lookup"><span data-stu-id="ee4de-138">Service discoverability can be easily added through the application configuration settings.</span></span> <span data-ttu-id="ee4de-139">Ponadto WCF Discovery obsługuje również przy użyciu protokołu odnajdywania za pośrednictwem innych transportów, takich jak sieć równorzędna, nakładka nazewnictwa i HTTP.</span><span class="sxs-lookup"><span data-stu-id="ee4de-139">In addition, WCF Discovery also supports using the discovery protocol over other transports such as peer net, naming overlay, and HTTP.</span></span> <span data-ttu-id="ee4de-140">Odnajdowanie WCF zapewnia obsługę zarządzanego trybu działania, w którym używany jest serwer proxy odnajdywania.</span><span class="sxs-lookup"><span data-stu-id="ee4de-140">WCF Discovery provides support for a Managed mode of operation where a discovery proxy is used.</span></span> <span data-ttu-id="ee4de-141">Może to zmniejszyć ruch sieciowy, ponieważ wiadomości są wysyłane bezpośrednio do serwera proxy odnajdywania zamiast wysyłać wiadomości multiemisji do całej sieci.</span><span class="sxs-lookup"><span data-stu-id="ee4de-141">This can reduce network traffic as messages are sent directly to the discovery proxy instead of sending multicast messages to the entire network.</span></span> <span data-ttu-id="ee4de-142">WCF Discovery pozwala również na większą elastyczność podczas pracy z usługami sieci Web.</span><span class="sxs-lookup"><span data-stu-id="ee4de-142">WCF Discovery also allows for more flexibility when working with Web services.</span></span> <span data-ttu-id="ee4de-143">Na przykład można zmienić adres usługi bez konieczności ponownego konfigurowania klienta lub usługi.</span><span class="sxs-lookup"><span data-stu-id="ee4de-143">For example, you can change the address of a service without having to reconfigure the client or the service.</span></span> <span data-ttu-id="ee4de-144">Gdy klient musi uzyskać dostęp do `Probe` usługi, `Find` może wydać komunikat za pośrednictwem żądania i oczekiwać, że usługa odpowie za pomocą bieżącego adresu.</span><span class="sxs-lookup"><span data-stu-id="ee4de-144">When a client must access the service it can issue a `Probe` message through a `Find` request and expect the service to respond with its current address.</span></span> <span data-ttu-id="ee4de-145">Odnajdywanie WCF umożliwia klientowi wyszukiwanie usługi na podstawie różnych kryteriów, w tym typów umów, elementów wiązania, przestrzeni nazw, zakresu i słów kluczowych lub numerów wersji.</span><span class="sxs-lookup"><span data-stu-id="ee4de-145">WCF Discovery allows a client to search for a service based on different criteria including contract types, binding elements, namespace, scope, and keywords or version numbers.</span></span> <span data-ttu-id="ee4de-146">WCF Discovery umożliwia odnajdowanie czasu wykonywania i projektowania.</span><span class="sxs-lookup"><span data-stu-id="ee4de-146">WCF Discovery enables runtime and design time discovery.</span></span> <span data-ttu-id="ee4de-147">Dodawanie odnajdowania do aplikacji może służyć do włączania innych scenariuszy, takich jak odporność na uszkodzenia i konfiguracji automatycznej.</span><span class="sxs-lookup"><span data-stu-id="ee4de-147">Adding discovery to your application can be used to enable other scenarios such as fault tolerance and auto configuration.</span></span>  
  
## <a name="service-publication"></a><span data-ttu-id="ee4de-148">Publikacja usługi</span><span class="sxs-lookup"><span data-stu-id="ee4de-148">Service Publication</span></span>  
 <span data-ttu-id="ee4de-149">Aby usługa można było <xref:System.ServiceModel.Discovery.ServiceDiscoveryBehavior> odnajdować, należy dodać do hosta usługi, a punkt końcowy odnajdywania musi zostać dodany, aby określić, gdzie nasłuchiwać komunikaty odnajdywania.</span><span class="sxs-lookup"><span data-stu-id="ee4de-149">To make a service discoverable, a <xref:System.ServiceModel.Discovery.ServiceDiscoveryBehavior> must be added to the service host and a discovery endpoint must be added to specify where to listen for discovery messages.</span></span> <span data-ttu-id="ee4de-150">Poniższy przykład kodu pokazuje, jak usługa hostowana samodzielnie może być modyfikowana, aby była wykrywalna.</span><span class="sxs-lookup"><span data-stu-id="ee4de-150">The following code example shows how a self-hosted service can be modified to make it discoverable.</span></span>  
  
```csharp  
Uri baseAddress = new Uri($"http://{System.Net.Dns.GetHostName()}:8000/discovery/scenarios/calculatorservice/{Guid.NewGuid().ToString()}/");

// Create a ServiceHost for the CalculatorService type.
using (ServiceHost serviceHost = new ServiceHost(typeof(CalculatorService), baseAddress))
{
    // Add calculator endpoint
    serviceHost.AddServiceEndpoint(typeof(ICalculator), new WSHttpBinding(), string.Empty);

    // ** DISCOVERY ** //
    // Make the service discoverable by adding the discovery behavior
    serviceHost.Description.Behaviors.Add(new ServiceDiscoveryBehavior());

    // ** DISCOVERY ** //
    // Add the discovery endpoint that specifies where to publish the services
    serviceHost.AddServiceEndpoint(new UdpDiscoveryEndpoint());

    // Open the ServiceHost to create listeners and start listening for messages.
    serviceHost.Open();

    // The service can now be accessed.
    Console.WriteLine("Press <ENTER> to terminate service.");
    Console.ReadLine();
}
```  
  
 <span data-ttu-id="ee4de-151">Wystąpienie <xref:System.ServiceModel.Discovery.ServiceDiscoveryBehavior> musi zostać dodane do opisu usługi, aby usługa była wykrywalna.</span><span class="sxs-lookup"><span data-stu-id="ee4de-151">A <xref:System.ServiceModel.Discovery.ServiceDiscoveryBehavior> instance must be added to a service description for the service to be discoverable.</span></span> <span data-ttu-id="ee4de-152">Wystąpienie <xref:System.ServiceModel.Discovery.DiscoveryEndpoint> musi zostać dodane do hosta usługi, aby poinformować usługę, gdzie można nasłuchiwać żądań odnajdywania.</span><span class="sxs-lookup"><span data-stu-id="ee4de-152">A <xref:System.ServiceModel.Discovery.DiscoveryEndpoint> instance must be added to the service host to tell the service where to listen for discovery requests.</span></span> <span data-ttu-id="ee4de-153">W tym przykładzie <xref:System.ServiceModel.Discovery.UdpDiscoveryEndpoint> (który pochodzi <xref:System.ServiceModel.Discovery.DiscoveryEndpoint>od ) jest dodawany, aby określić, że usługa powinna nasłuchiwać żądań odnajdywania za sprawą transportu multiemisji UDP.</span><span class="sxs-lookup"><span data-stu-id="ee4de-153">In this example, a <xref:System.ServiceModel.Discovery.UdpDiscoveryEndpoint> (which is derived from <xref:System.ServiceModel.Discovery.DiscoveryEndpoint>) is added to specify that the service should listen for discovery requests over the UDP multicast transport.</span></span> <span data-ttu-id="ee4de-154">Jest <xref:System.ServiceModel.Discovery.UdpDiscoveryEndpoint> używany do odnajdowania ad-hoc, ponieważ wszystkie wiadomości są wysyłane w sposób multiemisji.</span><span class="sxs-lookup"><span data-stu-id="ee4de-154">The <xref:System.ServiceModel.Discovery.UdpDiscoveryEndpoint> is used for Ad-Hoc discovery because all messages are sent in a multicast fashion.</span></span>  
  
## <a name="announcement"></a><span data-ttu-id="ee4de-155">Ogłoszenie</span><span class="sxs-lookup"><span data-stu-id="ee4de-155">Announcement</span></span>  
 <span data-ttu-id="ee4de-156">Domyślnie publikacja usługi nie wysyła komunikatów anonsu.</span><span class="sxs-lookup"><span data-stu-id="ee4de-156">By default, service publication does not send out announcement messages.</span></span> <span data-ttu-id="ee4de-157">Usługa musi być skonfigurowana do wysyłania komunikatów anonsu.</span><span class="sxs-lookup"><span data-stu-id="ee4de-157">The service must be configured to send out announcement messages.</span></span> <span data-ttu-id="ee4de-158">Zapewnia to dodatkową elastyczność dla modułów zapisu usług, ponieważ mogą one ogłosić usługi oddzielnie od nasłuchiwania komunikatów odnajdywania.</span><span class="sxs-lookup"><span data-stu-id="ee4de-158">This provides additional flexibility for service writers because they can announce the service separately from listening for discovery messages.</span></span> <span data-ttu-id="ee4de-159">Anons usługi może być również używany jako mechanizm rejestrowania usług za pomocą serwera proxy odnajdywania lub innych rejestrów usług.</span><span class="sxs-lookup"><span data-stu-id="ee4de-159">Service announcement can also be used as a mechanism for registering services with a discovery proxy or other service registries.</span></span> <span data-ttu-id="ee4de-160">Poniższy kod pokazuje, jak skonfigurować usługę do wysyłania komunikatów anonsu za sprawą powiązania UDP.</span><span class="sxs-lookup"><span data-stu-id="ee4de-160">The following code shows how to configure a service to send announcement messages over a UDP binding.</span></span>  
  
```csharp  
Uri baseAddress = new Uri($"http://{System.Net.Dns.GetHostName()}:8000/discovery/scenarios/calculatorservice/{Guid.NewGuid().ToString()}/");

// Create a ServiceHost for the CalculatorService type.
using (ServiceHost serviceHost = new ServiceHost(typeof(CalculatorService), baseAddress))
{
    // Add calculator endpoint
    serviceHost.AddServiceEndpoint(typeof(ICalculator), new WSHttpBinding(), string.Empty);

    // ** DISCOVERY ** //
    // Make the service discoverable by adding the discovery behavior
    ServiceDiscoveryBehavior discoveryBehavior = new ServiceDiscoveryBehavior();
    serviceHost.Description.Behaviors.Add(discoveryBehavior);

    // Send announcements on UDP multicast transport
    discoveryBehavior.AnnouncementEndpoints.Add(
      new UdpAnnouncementEndpoint());

    // ** DISCOVERY ** //
    // Add the discovery endpoint that specifies where to publish the services
    serviceHost.Description.Endpoints.Add(new UdpDiscoveryEndpoint());

    // Open the ServiceHost to create listeners and start listening for messages.
    serviceHost.Open();

    // The service can now be accessed.
    Console.WriteLine("Press <ENTER> to terminate service.");
    Console.ReadLine();
}
```  
  
## <a name="service-discovery"></a><span data-ttu-id="ee4de-161">Odnajdywanie usług</span><span class="sxs-lookup"><span data-stu-id="ee4de-161">Service Discovery</span></span>  
 <span data-ttu-id="ee4de-162">Aplikacja kliencka <xref:System.ServiceModel.Discovery.DiscoveryClient> może używać klasy do znajdowania usług.</span><span class="sxs-lookup"><span data-stu-id="ee4de-162">A client application can use the <xref:System.ServiceModel.Discovery.DiscoveryClient> class to find services.</span></span> <span data-ttu-id="ee4de-163">Deweloper tworzy wystąpienie <xref:System.ServiceModel.Discovery.DiscoveryClient> klasy, która przekazuje w punkcie końcowym odnajdywania, który określa, gdzie wysłać `Probe` lub `Resolve` wiadomości.</span><span class="sxs-lookup"><span data-stu-id="ee4de-163">The developer creates an instance of the <xref:System.ServiceModel.Discovery.DiscoveryClient> class that passes in a discovery endpoint that specifies where to send `Probe` or `Resolve` messages.</span></span> <span data-ttu-id="ee4de-164">Następnie klient <xref:System.ServiceModel.Discovery.DiscoveryClient.Find%2A> wywołuje, który określa <xref:System.ServiceModel.Discovery.FindCriteria> kryteria wyszukiwania w wystąpieniu.</span><span class="sxs-lookup"><span data-stu-id="ee4de-164">The client then calls <xref:System.ServiceModel.Discovery.DiscoveryClient.Find%2A> that specifies search criteria within a <xref:System.ServiceModel.Discovery.FindCriteria> instance.</span></span> <span data-ttu-id="ee4de-165">Jeśli zostaną znalezione <xref:System.ServiceModel.Discovery.DiscoveryClient.Find%2A> pasujące usługi, zwraca kolekcję <xref:System.ServiceModel.Discovery.EndpointDiscoveryMetadata>.</span><span class="sxs-lookup"><span data-stu-id="ee4de-165">If matching services are found, <xref:System.ServiceModel.Discovery.DiscoveryClient.Find%2A> returns a collection of <xref:System.ServiceModel.Discovery.EndpointDiscoveryMetadata>.</span></span> <span data-ttu-id="ee4de-166">Poniższy kod pokazuje, `Find` jak wywołać metodę, a następnie połączyć się z odnalezioną usługą.</span><span class="sxs-lookup"><span data-stu-id="ee4de-166">The following code shows how to call the `Find` method and then connect to a discovered service.</span></span>  
  
```csharp  
class Client
{
    static EndpointAddress serviceAddress;
  
    static void Main()
    {  
        if (FindService())
        {
            InvokeService();
        }
    }  
  
    // ** DISCOVERY ** //  
    static bool FindService()  
    {  
        Console.WriteLine("\nFinding Calculator Service ..");  
        DiscoveryClient discoveryClient =
            new DiscoveryClient(new UdpDiscoveryEndpoint());  
  
        Collection<EndpointDiscoveryMetadata> calculatorServices =
            (Collection<EndpointDiscoveryMetadata>)discoveryClient.Find(new FindCriteria(typeof(ICalculator))).Endpoints;  
  
        discoveryClient.Close();  
  
        if (calculatorServices.Count == 0)  
        {  
            Console.WriteLine("\nNo services are found.");  
            return false;  
        }  
        else  
        {  
            serviceAddress = calculatorServices[0].Address;  
            return true;  
        }  
    }  
  
    static void InvokeService()  
    {  
        Console.WriteLine("\nInvoking Calculator Service at {0}\n", serviceAddress);  
  
        // Create a client  
        CalculatorClient client = new CalculatorClient();  
        client.Endpoint.Address = serviceAddress;  
        client.Add(10,3);  
    }
}  
```  
  
## <a name="discovery-and-message-level-security"></a><span data-ttu-id="ee4de-167">Zabezpieczenia odnajdowania i poziomu wiadomości</span><span class="sxs-lookup"><span data-stu-id="ee4de-167">Discovery and Message Level Security</span></span>  
 <span data-ttu-id="ee4de-168">Podczas korzystania z zabezpieczeń na poziomie <xref:System.ServiceModel.EndpointIdentity> wiadomości jest konieczne, aby <xref:System.ServiceModel.EndpointIdentity> określić punkt końcowy odnajdywania usługi i dopasowania w punkcie końcowym odnajdywania klienta.</span><span class="sxs-lookup"><span data-stu-id="ee4de-168">When using message level security it is necessary to specify an <xref:System.ServiceModel.EndpointIdentity> on the service discovery endpoint and a matching <xref:System.ServiceModel.EndpointIdentity> on the client discovery endpoint.</span></span> <span data-ttu-id="ee4de-169">Aby uzyskać więcej informacji na temat zabezpieczeń na poziomie wiadomości, zobacz [Zabezpieczenia wiadomości](../../../../docs/framework/wcf/feature-details/message-security-in-wcf.md).</span><span class="sxs-lookup"><span data-stu-id="ee4de-169">For more information about message level security, see [Message Security](../../../../docs/framework/wcf/feature-details/message-security-in-wcf.md).</span></span>  
  
## <a name="discovery-and-web-hosted-services"></a><span data-ttu-id="ee4de-170">Odnajdowanie i usługi hostowane w sieci Web</span><span class="sxs-lookup"><span data-stu-id="ee4de-170">Discovery and Web Hosted Services</span></span>  
 <span data-ttu-id="ee4de-171">Aby usługi WCF być wykrywalne muszą być uruchomione.</span><span class="sxs-lookup"><span data-stu-id="ee4de-171">In order for WCF services to be discoverable they must be running.</span></span> <span data-ttu-id="ee4de-172">Usługi WCF hostowane w usługach IIS lub WAS nie są uruchamiane, dopóki usługi IIS/WAS nie otrzymają wiadomości powiązanej z usługą, więc domyślnie nie można ich odnaleźć.</span><span class="sxs-lookup"><span data-stu-id="ee4de-172">WCF services hosted under IIS or WAS do not run until IIS/WAS receives a message bound for the service, so they cannot be discoverable by default.</span></span>  <span data-ttu-id="ee4de-173">Istnieją dwie opcje, dzięki czemu usługi hostowane w sieci Web są wykrywalne:</span><span class="sxs-lookup"><span data-stu-id="ee4de-173">There are two options for making Web-Hosted services discoverable:</span></span>  
  
1. <span data-ttu-id="ee4de-174">Korzystanie z funkcji Automatycznego uruchamiania aplikacji systemu Windows Server</span><span class="sxs-lookup"><span data-stu-id="ee4de-174">Use the Windows Server AppFabric Auto-Start feature</span></span>  
  
2. <span data-ttu-id="ee4de-175">Używanie serwera proxy odnajdywania do komunikowania się w imieniu usługi</span><span class="sxs-lookup"><span data-stu-id="ee4de-175">Use a discovery proxy to communicate on behalf of the service</span></span>  
  
 <span data-ttu-id="ee4de-176">System Windows Server AppFabric ma funkcję automatycznego uruchamiania, która umożliwia uruchomienie usługi przed odebraniem jakichkolwiek wiadomości.</span><span class="sxs-lookup"><span data-stu-id="ee4de-176">Windows Server AppFabric has an Auto-Start feature that will allow a service to be started before receiving any messages.</span></span> <span data-ttu-id="ee4de-177">Za pomocą tego zestawu autostart można skonfigurować usługę hosta IIS/WAS do wykrywania.</span><span class="sxs-lookup"><span data-stu-id="ee4de-177">With this Auto-Start set, an IIS/WAS hosted service can be configured to be discoverable.</span></span> <span data-ttu-id="ee4de-178">Aby uzyskać więcej informacji na temat funkcji Automatycznego uruchamiania, zobacz [Funkcja automatycznego uruchamiania aplikacji systemu Windows Server AppFabric](https://docs.microsoft.com/previous-versions/appfabric/ee677260(v=azure.10)).</span><span class="sxs-lookup"><span data-stu-id="ee4de-178">For more information about the Auto-Start feature see, [Windows Server AppFabric Auto-Start Feature](https://docs.microsoft.com/previous-versions/appfabric/ee677260(v=azure.10)).</span></span> <span data-ttu-id="ee4de-179">Wraz z włączeniem funkcji Auto-Start należy skonfigurować usługę do odnajdowania.</span><span class="sxs-lookup"><span data-stu-id="ee4de-179">Along with turning on the Auto-Start feature, you must configure the service for discovery.</span></span> <span data-ttu-id="ee4de-180">Aby uzyskać więcej informacji, zobacz [Jak: Programowo dodawanie wykrywalności do usługi WCF i](../../../../docs/framework/wcf/feature-details/how-to-programmatically-add-discoverability-to-a-wcf-service-and-client.md)[odnajdowanie konfiguracji klienta w pliku konfiguracyjnym](../../../../docs/framework/wcf/feature-details/configuring-discovery-in-a-configuration-file.md).</span><span class="sxs-lookup"><span data-stu-id="ee4de-180">For more information, see [How to: Programmatically Add Discoverability to a WCF Service and Client](../../../../docs/framework/wcf/feature-details/how-to-programmatically-add-discoverability-to-a-wcf-service-and-client.md)[Configuring Discovery in a Configuration File](../../../../docs/framework/wcf/feature-details/configuring-discovery-in-a-configuration-file.md).</span></span>  
  
 <span data-ttu-id="ee4de-181">Serwer proxy odnajdywania może służyć do komunikowania się w imieniu usługi WCF, gdy usługa nie jest uruchomiona.</span><span class="sxs-lookup"><span data-stu-id="ee4de-181">A discovery proxy can be used to communicate on behalf of the WCF service when the service is not running.</span></span> <span data-ttu-id="ee4de-182">Serwer proxy może nasłuchiwać sondy lub rozpoznawania wiadomości i odpowiadać na klienta.</span><span class="sxs-lookup"><span data-stu-id="ee4de-182">The proxy can listen for probe or resolve messages and respond to the client.</span></span> <span data-ttu-id="ee4de-183">Klient może następnie wysyłać wiadomości bezpośrednio do usługi.</span><span class="sxs-lookup"><span data-stu-id="ee4de-183">The client can then send messages directly to the service.</span></span> <span data-ttu-id="ee4de-184">Gdy klient wyśle wiadomość do usługi, zostanie on smówiąc się, aby odpowiedzieć na wiadomość.</span><span class="sxs-lookup"><span data-stu-id="ee4de-184">When the client sends a message to the service it will be instantiated to respond to the message.</span></span> <span data-ttu-id="ee4de-185">Aby uzyskać więcej informacji na temat implementowania serwera proxy [odnajdywania, zobacz Implementowanie serwera proxy odnajdywania](../../../../docs/framework/wcf/feature-details/implementing-a-discovery-proxy.md).</span><span class="sxs-lookup"><span data-stu-id="ee4de-185">For more information about implementing a discovery proxy see, [Implementing a Discovery Proxy](../../../../docs/framework/wcf/feature-details/implementing-a-discovery-proxy.md).</span></span>  
  
> [!NOTE]
> <span data-ttu-id="ee4de-186">Aby odnajdywanie WCF działało poprawnie, wszystkie karty sieciowe (kontroler interfejsu sieciowego) powinny mieć tylko 1 adres IP.</span><span class="sxs-lookup"><span data-stu-id="ee4de-186">For WCF Discovery to work correctly, all NICs (Network Interface Controller) should only have 1 IP address.</span></span>
